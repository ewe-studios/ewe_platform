//! Auto-generated API clients for gcp/compute.
//!
//! Generated by `cargo run --bin ewe_platform gen_provider_clients`.
//! DO NOT EDIT MANUALLY.

use crate::providers::gcp::clients::types::*;
use crate::providers::gcp::resources::*;
use foundation_core::valtron::{execute, StreamIterator, StreamIteratorExt};
use foundation_core::wire::simple_http::client::{
    body_reader, ClientRequestBuilder, RequestIntro, SimpleHttpClient, SystemDnsResolver,
};

/// GET projects/{project}/aggregated/acceleratorTypes
/// Retrieves an aggregated list of accelerator types.  To prevent failure, it is recommended that you set the `returnPartialSuccess` parameter to `true`.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_accelerator_types_aggregated_list_execute()` to send, or `compute_accelerator_types_aggregated_list` for simplest API.

pub fn compute_accelerator_types_aggregated_list_builder(
    client: &SimpleHttpClient,
    project: &str,
    filter: Option<&str>,
    includeAllScopes: Option<bool>,
    maxResults: Option<i32>,
    orderBy: Option<&str>,
    pageToken: Option<&str>,
    returnPartialSuccess: Option<bool>,
    serviceProjectNumber: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/aggregated/acceleratorTypes",
        project,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "filter",
            filter
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "includeAllScopes",
            includeAllScopes
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "maxResults",
            maxResults
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "orderBy",
            orderBy
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageToken",
            pageToken
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "returnPartialSuccess",
            returnPartialSuccess
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "serviceProjectNumber",
            serviceProjectNumber
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET projects/{project}/aggregated/acceleratorTypes
/// Retrieves an aggregated list of accelerator types.  To prevent failure, it is recommended that you set the `returnPartialSuccess` parameter to `true`.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_accelerator_types_aggregated_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_accelerator_types_aggregated_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_accelerator_types_aggregated_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<AcceleratorTypeAggregatedList>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: AcceleratorTypeAggregatedList = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/aggregated/acceleratorTypes
/// Retrieves an aggregated list of accelerator types.  To prevent failure, it is recommended that you set the `returnPartialSuccess` parameter to `true`.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_accelerator_types_aggregated_list_builder()` + `compute_accelerator_types_aggregated_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_accelerator_types_aggregated_list(
    client: &SimpleHttpClient,
    project: &str,
    filter: Option<&str>,
    includeAllScopes: Option<bool>,
    maxResults: Option<i32>,
    orderBy: Option<&str>,
    pageToken: Option<&str>,
    returnPartialSuccess: Option<bool>,
    serviceProjectNumber: Option<&str>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<AcceleratorTypeAggregatedList>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = compute_accelerator_types_aggregated_list_builder(
        client,
        project,
        filter,
        includeAllScopes,
        maxResults,
        orderBy,
        pageToken,
        returnPartialSuccess,
        serviceProjectNumber,
    )?;
    compute_accelerator_types_aggregated_list_execute(builder)
}

/// GET projects/{project}/zones/{zone}/acceleratorTypes/{acceleratorType}
/// Returns the specified accelerator type.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_accelerator_types_get_execute()` to send, or `compute_accelerator_types_get` for simplest API.

pub fn compute_accelerator_types_get_builder(
    client: &SimpleHttpClient,
    acceleratorType: &str,
    project: &str,
    zone: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/zones/{}/acceleratorTypes/{}",
        acceleratorType, project, zone,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/zones/{zone}/acceleratorTypes/{acceleratorType}
/// Returns the specified accelerator type.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_accelerator_types_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_accelerator_types_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_accelerator_types_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<AcceleratorType>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: AcceleratorType = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/zones/{zone}/acceleratorTypes/{acceleratorType}
/// Returns the specified accelerator type.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_accelerator_types_get_builder()` + `compute_accelerator_types_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_accelerator_types_get(
    client: &SimpleHttpClient,
    acceleratorType: &str,
    project: &str,
    zone: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<AcceleratorType>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = compute_accelerator_types_get_builder(client, acceleratorType, project, zone)?;
    compute_accelerator_types_get_execute(builder)
}

/// GET projects/{project}/zones/{zone}/acceleratorTypes
/// Retrieves a list of accelerator types that are available to the specified project.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_accelerator_types_list_execute()` to send, or `compute_accelerator_types_list` for simplest API.

pub fn compute_accelerator_types_list_builder(
    client: &SimpleHttpClient,
    project: &str,
    zone: &str,
    filter: Option<&str>,
    maxResults: Option<i32>,
    orderBy: Option<&str>,
    pageToken: Option<&str>,
    returnPartialSuccess: Option<bool>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/zones/{}/acceleratorTypes",
        project, zone,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "filter",
            filter
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "maxResults",
            maxResults
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "orderBy",
            orderBy
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageToken",
            pageToken
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "returnPartialSuccess",
            returnPartialSuccess
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET projects/{project}/zones/{zone}/acceleratorTypes
/// Retrieves a list of accelerator types that are available to the specified project.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_accelerator_types_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_accelerator_types_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_accelerator_types_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<AcceleratorTypeList>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: AcceleratorTypeList = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/zones/{zone}/acceleratorTypes
/// Retrieves a list of accelerator types that are available to the specified project.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_accelerator_types_list_builder()` + `compute_accelerator_types_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_accelerator_types_list(
    client: &SimpleHttpClient,
    project: &str,
    zone: &str,
    filter: Option<&str>,
    maxResults: Option<i32>,
    orderBy: Option<&str>,
    pageToken: Option<&str>,
    returnPartialSuccess: Option<bool>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<AcceleratorTypeList>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = compute_accelerator_types_list_builder(
        client,
        project,
        zone,
        filter,
        maxResults,
        orderBy,
        pageToken,
        returnPartialSuccess,
    )?;
    compute_accelerator_types_list_execute(builder)
}

/// GET projects/{project}/aggregated/addresses
/// Retrieves an aggregated list of addresses.  To prevent failure, it is recommended that you set the `returnPartialSuccess` parameter to `true`.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_addresses_aggregated_list_execute()` to send, or `compute_addresses_aggregated_list` for simplest API.

pub fn compute_addresses_aggregated_list_builder(
    client: &SimpleHttpClient,
    project: &str,
    filter: Option<&str>,
    includeAllScopes: Option<bool>,
    maxResults: Option<i32>,
    orderBy: Option<&str>,
    pageToken: Option<&str>,
    returnPartialSuccess: Option<bool>,
    serviceProjectNumber: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/aggregated/addresses",
        project,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "filter",
            filter
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "includeAllScopes",
            includeAllScopes
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "maxResults",
            maxResults
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "orderBy",
            orderBy
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageToken",
            pageToken
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "returnPartialSuccess",
            returnPartialSuccess
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "serviceProjectNumber",
            serviceProjectNumber
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET projects/{project}/aggregated/addresses
/// Retrieves an aggregated list of addresses.  To prevent failure, it is recommended that you set the `returnPartialSuccess` parameter to `true`.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_addresses_aggregated_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_addresses_aggregated_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_addresses_aggregated_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<AddressAggregatedList>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: AddressAggregatedList = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/aggregated/addresses
/// Retrieves an aggregated list of addresses.  To prevent failure, it is recommended that you set the `returnPartialSuccess` parameter to `true`.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_addresses_aggregated_list_builder()` + `compute_addresses_aggregated_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_addresses_aggregated_list(
    client: &SimpleHttpClient,
    project: &str,
    filter: Option<&str>,
    includeAllScopes: Option<bool>,
    maxResults: Option<i32>,
    orderBy: Option<&str>,
    pageToken: Option<&str>,
    returnPartialSuccess: Option<bool>,
    serviceProjectNumber: Option<&str>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<AddressAggregatedList>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = compute_addresses_aggregated_list_builder(
        client,
        project,
        filter,
        includeAllScopes,
        maxResults,
        orderBy,
        pageToken,
        returnPartialSuccess,
        serviceProjectNumber,
    )?;
    compute_addresses_aggregated_list_execute(builder)
}

/// GET projects/{project}/regions/{region}/addresses/{address}
/// Deletes the specified address resource.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_addresses_delete_execute()` to send, or `compute_addresses_delete` for simplest API.

pub fn compute_addresses_delete_builder(
    client: &SimpleHttpClient,
    address: &str,
    project: &str,
    region: &str,
    requestId: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/regions/{}/addresses/{}",
        address, project, region,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "requestId",
            requestId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET projects/{project}/regions/{region}/addresses/{address}
/// Deletes the specified address resource.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_addresses_delete_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_addresses_delete_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_addresses_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/regions/{region}/addresses/{address}
/// Deletes the specified address resource.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_addresses_delete_builder()` + `compute_addresses_delete_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_addresses_delete(
    client: &SimpleHttpClient,
    address: &str,
    project: &str,
    region: &str,
    requestId: Option<&str>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_addresses_delete_builder(client, address, project, region, requestId)?;
    compute_addresses_delete_execute(builder)
}

/// GET projects/{project}/regions/{region}/addresses/{address}
/// Returns the specified address resource.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_addresses_get_execute()` to send, or `compute_addresses_get` for simplest API.

pub fn compute_addresses_get_builder(
    client: &SimpleHttpClient,
    address: &str,
    project: &str,
    region: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/regions/{}/addresses/{}",
        address, project, region,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/regions/{region}/addresses/{address}
/// Returns the specified address resource.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_addresses_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_addresses_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_addresses_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Address>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Address = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/regions/{region}/addresses/{address}
/// Returns the specified address resource.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_addresses_get_builder()` + `compute_addresses_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_addresses_get(
    client: &SimpleHttpClient,
    address: &str,
    project: &str,
    region: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Address>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_addresses_get_builder(client, address, project, region)?;
    compute_addresses_get_execute(builder)
}

/// GET projects/{project}/regions/{region}/addresses
/// Creates an address resource in the specified project by using the data included in the request.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_addresses_insert_execute()` to send, or `compute_addresses_insert` for simplest API.

pub fn compute_addresses_insert_builder(
    client: &SimpleHttpClient,
    project: &str,
    region: &str,
    requestId: Option<&str>,
    body: &Address,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/regions/{}/addresses",
        project, region,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "requestId",
            requestId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET projects/{project}/regions/{region}/addresses
/// Creates an address resource in the specified project by using the data included in the request.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_addresses_insert_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_addresses_insert_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_addresses_insert_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/regions/{region}/addresses
/// Creates an address resource in the specified project by using the data included in the request.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_addresses_insert_builder()` + `compute_addresses_insert_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_addresses_insert(
    client: &SimpleHttpClient,
    project: &str,
    region: &str,
    requestId: Option<&str>,
    body: &Address,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_addresses_insert_builder(client, project, region, requestId, body)?;
    compute_addresses_insert_execute(builder)
}

/// GET projects/{project}/regions/{region}/addresses
/// Retrieves a list of addresses contained within the specified region.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_addresses_list_execute()` to send, or `compute_addresses_list` for simplest API.

pub fn compute_addresses_list_builder(
    client: &SimpleHttpClient,
    project: &str,
    region: &str,
    filter: Option<&str>,
    maxResults: Option<i32>,
    orderBy: Option<&str>,
    pageToken: Option<&str>,
    returnPartialSuccess: Option<bool>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/regions/{}/addresses",
        project, region,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "filter",
            filter
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "maxResults",
            maxResults
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "orderBy",
            orderBy
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageToken",
            pageToken
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "returnPartialSuccess",
            returnPartialSuccess
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET projects/{project}/regions/{region}/addresses
/// Retrieves a list of addresses contained within the specified region.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_addresses_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_addresses_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_addresses_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<AddressList>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: AddressList = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/regions/{region}/addresses
/// Retrieves a list of addresses contained within the specified region.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_addresses_list_builder()` + `compute_addresses_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_addresses_list(
    client: &SimpleHttpClient,
    project: &str,
    region: &str,
    filter: Option<&str>,
    maxResults: Option<i32>,
    orderBy: Option<&str>,
    pageToken: Option<&str>,
    returnPartialSuccess: Option<bool>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<AddressList>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_addresses_list_builder(
        client,
        project,
        region,
        filter,
        maxResults,
        orderBy,
        pageToken,
        returnPartialSuccess,
    )?;
    compute_addresses_list_execute(builder)
}

/// GET projects/{project}/regions/{region}/addresses/{address}/move
/// Moves the specified address resource.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_addresses_move_execute()` to send, or `compute_addresses_move` for simplest API.

pub fn compute_addresses_move_builder(
    client: &SimpleHttpClient,
    address: &str,
    project: &str,
    region: &str,
    requestId: Option<&str>,
    body: &RegionAddressesMoveRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/regions/{}/addresses/{}/move",
        address, project, region,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "requestId",
            requestId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET projects/{project}/regions/{region}/addresses/{address}/move
/// Moves the specified address resource.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_addresses_move_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_addresses_move_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_addresses_move_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/regions/{region}/addresses/{address}/move
/// Moves the specified address resource.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_addresses_move_builder()` + `compute_addresses_move_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_addresses_move(
    client: &SimpleHttpClient,
    address: &str,
    project: &str,
    region: &str,
    requestId: Option<&str>,
    body: &RegionAddressesMoveRequest,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        compute_addresses_move_builder(client, address, project, region, requestId, body)?;
    compute_addresses_move_execute(builder)
}

/// GET projects/{project}/regions/{region}/addresses/{resource}/setLabels
/// Sets the labels on an Address. To learn more about labels, read `theLabeling` Resources documentation.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_addresses_set_labels_execute()` to send, or `compute_addresses_set_labels` for simplest API.

pub fn compute_addresses_set_labels_builder(
    client: &SimpleHttpClient,
    project: &str,
    region: &str,
    resource: &str,
    requestId: Option<&str>,
    body: &RegionSetLabelsRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/regions/{}/addresses/{}/setLabels",
        project, region, resource,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "requestId",
            requestId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET projects/{project}/regions/{region}/addresses/{resource}/setLabels
/// Sets the labels on an Address. To learn more about labels, read `theLabeling` Resources documentation.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_addresses_set_labels_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_addresses_set_labels_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_addresses_set_labels_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/regions/{region}/addresses/{resource}/setLabels
/// Sets the labels on an Address. To learn more about labels, read `theLabeling` Resources documentation.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_addresses_set_labels_builder()` + `compute_addresses_set_labels_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_addresses_set_labels(
    client: &SimpleHttpClient,
    project: &str,
    region: &str,
    resource: &str,
    requestId: Option<&str>,
    body: &RegionSetLabelsRequest,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        compute_addresses_set_labels_builder(client, project, region, resource, requestId, body)?;
    compute_addresses_set_labels_execute(builder)
}

/// GET projects/{project}/regions/{region}/addresses/{resource}/testIamPermissions
/// Returns permissions that a caller has on the specified resource.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_addresses_test_iam_permissions_execute()` to send, or `compute_addresses_test_iam_permissions` for simplest API.

pub fn compute_addresses_test_iam_permissions_builder(
    client: &SimpleHttpClient,
    project: &str,
    region: &str,
    resource: &str,
    body: &TestPermissionsRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/regions/{}/addresses/{}/testIamPermissions",
        project, region, resource,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET projects/{project}/regions/{region}/addresses/{resource}/testIamPermissions
/// Returns permissions that a caller has on the specified resource.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_addresses_test_iam_permissions_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_addresses_test_iam_permissions_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_addresses_test_iam_permissions_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<TestPermissionsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: TestPermissionsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/regions/{region}/addresses/{resource}/testIamPermissions
/// Returns permissions that a caller has on the specified resource.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_addresses_test_iam_permissions_builder()` + `compute_addresses_test_iam_permissions_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_addresses_test_iam_permissions(
    client: &SimpleHttpClient,
    project: &str,
    region: &str,
    resource: &str,
    body: &TestPermissionsRequest,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<TestPermissionsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        compute_addresses_test_iam_permissions_builder(client, project, region, resource, body)?;
    compute_addresses_test_iam_permissions_execute(builder)
}

/// GET projects/{project}/regions/{region}/advice/calendarMode
/// Advise how, where and when to create the requested amount of instances with specified accelerators, within the specified time and location limits. The method recommends creating future reservations for the requested resources.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_advice_calendar_mode_execute()` to send, or `compute_advice_calendar_mode` for simplest API.

pub fn compute_advice_calendar_mode_builder(
    client: &SimpleHttpClient,
    project: &str,
    region: &str,
    body: &CalendarModeAdviceRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/regions/{}/advice/calendarMode",
        project, region,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET projects/{project}/regions/{region}/advice/calendarMode
/// Advise how, where and when to create the requested amount of instances with specified accelerators, within the specified time and location limits. The method recommends creating future reservations for the requested resources.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_advice_calendar_mode_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_advice_calendar_mode_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_advice_calendar_mode_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<CalendarModeAdviceResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: CalendarModeAdviceResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/regions/{region}/advice/calendarMode
/// Advise how, where and when to create the requested amount of instances with specified accelerators, within the specified time and location limits. The method recommends creating future reservations for the requested resources.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_advice_calendar_mode_builder()` + `compute_advice_calendar_mode_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_advice_calendar_mode(
    client: &SimpleHttpClient,
    project: &str,
    region: &str,
    body: &CalendarModeAdviceRequest,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<CalendarModeAdviceResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = compute_advice_calendar_mode_builder(client, project, region, body)?;
    compute_advice_calendar_mode_execute(builder)
}

/// GET projects/{project}/aggregated/autoscalers
/// Retrieves an aggregated list of autoscalers.  To prevent failure, it is recommended that you set the `returnPartialSuccess` parameter to `true`.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_autoscalers_aggregated_list_execute()` to send, or `compute_autoscalers_aggregated_list` for simplest API.

pub fn compute_autoscalers_aggregated_list_builder(
    client: &SimpleHttpClient,
    project: &str,
    filter: Option<&str>,
    includeAllScopes: Option<bool>,
    maxResults: Option<i32>,
    orderBy: Option<&str>,
    pageToken: Option<&str>,
    returnPartialSuccess: Option<bool>,
    serviceProjectNumber: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/aggregated/autoscalers",
        project,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "filter",
            filter
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "includeAllScopes",
            includeAllScopes
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "maxResults",
            maxResults
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "orderBy",
            orderBy
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageToken",
            pageToken
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "returnPartialSuccess",
            returnPartialSuccess
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "serviceProjectNumber",
            serviceProjectNumber
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET projects/{project}/aggregated/autoscalers
/// Retrieves an aggregated list of autoscalers.  To prevent failure, it is recommended that you set the `returnPartialSuccess` parameter to `true`.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_autoscalers_aggregated_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_autoscalers_aggregated_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_autoscalers_aggregated_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<AutoscalerAggregatedList>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: AutoscalerAggregatedList = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/aggregated/autoscalers
/// Retrieves an aggregated list of autoscalers.  To prevent failure, it is recommended that you set the `returnPartialSuccess` parameter to `true`.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_autoscalers_aggregated_list_builder()` + `compute_autoscalers_aggregated_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_autoscalers_aggregated_list(
    client: &SimpleHttpClient,
    project: &str,
    filter: Option<&str>,
    includeAllScopes: Option<bool>,
    maxResults: Option<i32>,
    orderBy: Option<&str>,
    pageToken: Option<&str>,
    returnPartialSuccess: Option<bool>,
    serviceProjectNumber: Option<&str>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<AutoscalerAggregatedList>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = compute_autoscalers_aggregated_list_builder(
        client,
        project,
        filter,
        includeAllScopes,
        maxResults,
        orderBy,
        pageToken,
        returnPartialSuccess,
        serviceProjectNumber,
    )?;
    compute_autoscalers_aggregated_list_execute(builder)
}

/// GET projects/{project}/zones/{zone}/autoscalers/{autoscaler}
/// Deletes the specified autoscaler.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_autoscalers_delete_execute()` to send, or `compute_autoscalers_delete` for simplest API.

pub fn compute_autoscalers_delete_builder(
    client: &SimpleHttpClient,
    autoscaler: &str,
    project: &str,
    zone: &str,
    requestId: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/zones/{}/autoscalers/{}",
        autoscaler, project, zone,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "requestId",
            requestId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET projects/{project}/zones/{zone}/autoscalers/{autoscaler}
/// Deletes the specified autoscaler.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_autoscalers_delete_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_autoscalers_delete_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_autoscalers_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/zones/{zone}/autoscalers/{autoscaler}
/// Deletes the specified autoscaler.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_autoscalers_delete_builder()` + `compute_autoscalers_delete_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_autoscalers_delete(
    client: &SimpleHttpClient,
    autoscaler: &str,
    project: &str,
    zone: &str,
    requestId: Option<&str>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_autoscalers_delete_builder(client, autoscaler, project, zone, requestId)?;
    compute_autoscalers_delete_execute(builder)
}

/// GET projects/{project}/zones/{zone}/autoscalers/{autoscaler}
/// Returns the specified autoscaler resource.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_autoscalers_get_execute()` to send, or `compute_autoscalers_get` for simplest API.

pub fn compute_autoscalers_get_builder(
    client: &SimpleHttpClient,
    autoscaler: &str,
    project: &str,
    zone: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/zones/{}/autoscalers/{}",
        autoscaler, project, zone,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/zones/{zone}/autoscalers/{autoscaler}
/// Returns the specified autoscaler resource.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_autoscalers_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_autoscalers_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_autoscalers_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Autoscaler>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Autoscaler = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/zones/{zone}/autoscalers/{autoscaler}
/// Returns the specified autoscaler resource.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_autoscalers_get_builder()` + `compute_autoscalers_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_autoscalers_get(
    client: &SimpleHttpClient,
    autoscaler: &str,
    project: &str,
    zone: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Autoscaler>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_autoscalers_get_builder(client, autoscaler, project, zone)?;
    compute_autoscalers_get_execute(builder)
}

/// GET projects/{project}/zones/{zone}/autoscalers
/// Creates an autoscaler in the specified project using the data included in the request.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_autoscalers_insert_execute()` to send, or `compute_autoscalers_insert` for simplest API.

pub fn compute_autoscalers_insert_builder(
    client: &SimpleHttpClient,
    project: &str,
    zone: &str,
    requestId: Option<&str>,
    body: &Autoscaler,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/zones/{}/autoscalers",
        project, zone,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "requestId",
            requestId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET projects/{project}/zones/{zone}/autoscalers
/// Creates an autoscaler in the specified project using the data included in the request.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_autoscalers_insert_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_autoscalers_insert_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_autoscalers_insert_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/zones/{zone}/autoscalers
/// Creates an autoscaler in the specified project using the data included in the request.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_autoscalers_insert_builder()` + `compute_autoscalers_insert_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_autoscalers_insert(
    client: &SimpleHttpClient,
    project: &str,
    zone: &str,
    requestId: Option<&str>,
    body: &Autoscaler,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_autoscalers_insert_builder(client, project, zone, requestId, body)?;
    compute_autoscalers_insert_execute(builder)
}

/// GET projects/{project}/zones/{zone}/autoscalers
/// Retrieves a list of autoscalers contained within the specified zone.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_autoscalers_list_execute()` to send, or `compute_autoscalers_list` for simplest API.

pub fn compute_autoscalers_list_builder(
    client: &SimpleHttpClient,
    project: &str,
    zone: &str,
    filter: Option<&str>,
    maxResults: Option<i32>,
    orderBy: Option<&str>,
    pageToken: Option<&str>,
    returnPartialSuccess: Option<bool>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/zones/{}/autoscalers",
        project, zone,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "filter",
            filter
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "maxResults",
            maxResults
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "orderBy",
            orderBy
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageToken",
            pageToken
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "returnPartialSuccess",
            returnPartialSuccess
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET projects/{project}/zones/{zone}/autoscalers
/// Retrieves a list of autoscalers contained within the specified zone.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_autoscalers_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_autoscalers_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_autoscalers_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<AutoscalerList>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: AutoscalerList = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/zones/{zone}/autoscalers
/// Retrieves a list of autoscalers contained within the specified zone.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_autoscalers_list_builder()` + `compute_autoscalers_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_autoscalers_list(
    client: &SimpleHttpClient,
    project: &str,
    zone: &str,
    filter: Option<&str>,
    maxResults: Option<i32>,
    orderBy: Option<&str>,
    pageToken: Option<&str>,
    returnPartialSuccess: Option<bool>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<AutoscalerList>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = compute_autoscalers_list_builder(
        client,
        project,
        zone,
        filter,
        maxResults,
        orderBy,
        pageToken,
        returnPartialSuccess,
    )?;
    compute_autoscalers_list_execute(builder)
}

/// GET projects/{project}/zones/{zone}/autoscalers
/// Updates an autoscaler in the specified project using the data included in the request. This method `supportsPATCH` semantics and uses `theJSON` merge patch format and processing rules.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_autoscalers_patch_execute()` to send, or `compute_autoscalers_patch` for simplest API.

pub fn compute_autoscalers_patch_builder(
    client: &SimpleHttpClient,
    project: &str,
    zone: &str,
    autoscaler: Option<&str>,
    requestId: Option<&str>,
    body: &Autoscaler,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/zones/{}/autoscalers",
        project, zone,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "autoscaler",
            autoscaler
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "requestId",
            requestId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET projects/{project}/zones/{zone}/autoscalers
/// Updates an autoscaler in the specified project using the data included in the request. This method `supportsPATCH` semantics and uses `theJSON` merge patch format and processing rules.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_autoscalers_patch_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_autoscalers_patch_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_autoscalers_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/zones/{zone}/autoscalers
/// Updates an autoscaler in the specified project using the data included in the request. This method `supportsPATCH` semantics and uses `theJSON` merge patch format and processing rules.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_autoscalers_patch_builder()` + `compute_autoscalers_patch_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_autoscalers_patch(
    client: &SimpleHttpClient,
    project: &str,
    zone: &str,
    autoscaler: Option<&str>,
    requestId: Option<&str>,
    body: &Autoscaler,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        compute_autoscalers_patch_builder(client, project, zone, autoscaler, requestId, body)?;
    compute_autoscalers_patch_execute(builder)
}

/// GET projects/{project}/zones/{zone}/autoscalers/{resource}/testIamPermissions
/// Returns permissions that a caller has on the specified resource.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_autoscalers_test_iam_permissions_execute()` to send, or `compute_autoscalers_test_iam_permissions` for simplest API.

pub fn compute_autoscalers_test_iam_permissions_builder(
    client: &SimpleHttpClient,
    project: &str,
    resource: &str,
    zone: &str,
    body: &TestPermissionsRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/zones/{}/autoscalers/{}/testIamPermissions",
        project, resource, zone,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET projects/{project}/zones/{zone}/autoscalers/{resource}/testIamPermissions
/// Returns permissions that a caller has on the specified resource.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_autoscalers_test_iam_permissions_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_autoscalers_test_iam_permissions_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_autoscalers_test_iam_permissions_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<TestPermissionsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: TestPermissionsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/zones/{zone}/autoscalers/{resource}/testIamPermissions
/// Returns permissions that a caller has on the specified resource.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_autoscalers_test_iam_permissions_builder()` + `compute_autoscalers_test_iam_permissions_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_autoscalers_test_iam_permissions(
    client: &SimpleHttpClient,
    project: &str,
    resource: &str,
    zone: &str,
    body: &TestPermissionsRequest,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<TestPermissionsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        compute_autoscalers_test_iam_permissions_builder(client, project, resource, zone, body)?;
    compute_autoscalers_test_iam_permissions_execute(builder)
}

/// GET projects/{project}/zones/{zone}/autoscalers
/// Updates an autoscaler in the specified project using the data included in the request.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_autoscalers_update_execute()` to send, or `compute_autoscalers_update` for simplest API.

pub fn compute_autoscalers_update_builder(
    client: &SimpleHttpClient,
    project: &str,
    zone: &str,
    autoscaler: Option<&str>,
    requestId: Option<&str>,
    body: &Autoscaler,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/zones/{}/autoscalers",
        project, zone,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "autoscaler",
            autoscaler
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "requestId",
            requestId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET projects/{project}/zones/{zone}/autoscalers
/// Updates an autoscaler in the specified project using the data included in the request.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_autoscalers_update_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_autoscalers_update_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_autoscalers_update_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/zones/{zone}/autoscalers
/// Updates an autoscaler in the specified project using the data included in the request.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_autoscalers_update_builder()` + `compute_autoscalers_update_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_autoscalers_update(
    client: &SimpleHttpClient,
    project: &str,
    zone: &str,
    autoscaler: Option<&str>,
    requestId: Option<&str>,
    body: &Autoscaler,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        compute_autoscalers_update_builder(client, project, zone, autoscaler, requestId, body)?;
    compute_autoscalers_update_execute(builder)
}

/// GET projects/{project}/global/backendBuckets/{backendBucket}/addSignedUrlKey
/// Adds a key for validating requests with signed URLs for this backend bucket.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_backend_buckets_add_signed_url_key_execute()` to send, or `compute_backend_buckets_add_signed_url_key` for simplest API.

pub fn compute_backend_buckets_add_signed_url_key_builder(
    client: &SimpleHttpClient,
    backendBucket: &str,
    project: &str,
    requestId: Option<&str>,
    body: &SignedUrlKey,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/global/backendBuckets/{}/addSignedUrlKey",
        backendBucket, project,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "requestId",
            requestId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET projects/{project}/global/backendBuckets/{backendBucket}/addSignedUrlKey
/// Adds a key for validating requests with signed URLs for this backend bucket.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_backend_buckets_add_signed_url_key_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_backend_buckets_add_signed_url_key_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_backend_buckets_add_signed_url_key_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/global/backendBuckets/{backendBucket}/addSignedUrlKey
/// Adds a key for validating requests with signed URLs for this backend bucket.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_backend_buckets_add_signed_url_key_builder()` + `compute_backend_buckets_add_signed_url_key_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_backend_buckets_add_signed_url_key(
    client: &SimpleHttpClient,
    backendBucket: &str,
    project: &str,
    requestId: Option<&str>,
    body: &SignedUrlKey,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_backend_buckets_add_signed_url_key_builder(
        client,
        backendBucket,
        project,
        requestId,
        body,
    )?;
    compute_backend_buckets_add_signed_url_key_execute(builder)
}

/// GET projects/{project}/global/backendBuckets/{backendBucket}
/// Deletes the specified BackendBucket resource.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_backend_buckets_delete_execute()` to send, or `compute_backend_buckets_delete` for simplest API.

pub fn compute_backend_buckets_delete_builder(
    client: &SimpleHttpClient,
    backendBucket: &str,
    project: &str,
    requestId: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/global/backendBuckets/{}",
        backendBucket, project,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "requestId",
            requestId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET projects/{project}/global/backendBuckets/{backendBucket}
/// Deletes the specified BackendBucket resource.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_backend_buckets_delete_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_backend_buckets_delete_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_backend_buckets_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/global/backendBuckets/{backendBucket}
/// Deletes the specified BackendBucket resource.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_backend_buckets_delete_builder()` + `compute_backend_buckets_delete_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_backend_buckets_delete(
    client: &SimpleHttpClient,
    backendBucket: &str,
    project: &str,
    requestId: Option<&str>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        compute_backend_buckets_delete_builder(client, backendBucket, project, requestId)?;
    compute_backend_buckets_delete_execute(builder)
}

/// GET projects/{project}/global/backendBuckets/{backendBucket}/deleteSignedUrlKey
/// Deletes a key for validating requests with signed URLs for this backend bucket.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_backend_buckets_delete_signed_url_key_execute()` to send, or `compute_backend_buckets_delete_signed_url_key` for simplest API.

pub fn compute_backend_buckets_delete_signed_url_key_builder(
    client: &SimpleHttpClient,
    backendBucket: &str,
    project: &str,
    keyName: Option<&str>,
    requestId: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/global/backendBuckets/{}/deleteSignedUrlKey",
        backendBucket, project,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "keyName",
            keyName
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "requestId",
            requestId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET projects/{project}/global/backendBuckets/{backendBucket}/deleteSignedUrlKey
/// Deletes a key for validating requests with signed URLs for this backend bucket.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_backend_buckets_delete_signed_url_key_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_backend_buckets_delete_signed_url_key_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_backend_buckets_delete_signed_url_key_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/global/backendBuckets/{backendBucket}/deleteSignedUrlKey
/// Deletes a key for validating requests with signed URLs for this backend bucket.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_backend_buckets_delete_signed_url_key_builder()` + `compute_backend_buckets_delete_signed_url_key_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_backend_buckets_delete_signed_url_key(
    client: &SimpleHttpClient,
    backendBucket: &str,
    project: &str,
    keyName: Option<&str>,
    requestId: Option<&str>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_backend_buckets_delete_signed_url_key_builder(
        client,
        backendBucket,
        project,
        keyName,
        requestId,
    )?;
    compute_backend_buckets_delete_signed_url_key_execute(builder)
}

/// GET projects/{project}/global/backendBuckets/{backendBucket}
/// Returns the specified BackendBucket resource.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_backend_buckets_get_execute()` to send, or `compute_backend_buckets_get` for simplest API.

pub fn compute_backend_buckets_get_builder(
    client: &SimpleHttpClient,
    backendBucket: &str,
    project: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/global/backendBuckets/{}",
        backendBucket, project,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/global/backendBuckets/{backendBucket}
/// Returns the specified BackendBucket resource.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_backend_buckets_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_backend_buckets_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_backend_buckets_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<BackendBucket>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: BackendBucket = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/global/backendBuckets/{backendBucket}
/// Returns the specified BackendBucket resource.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_backend_buckets_get_builder()` + `compute_backend_buckets_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_backend_buckets_get(
    client: &SimpleHttpClient,
    backendBucket: &str,
    project: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<BackendBucket>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = compute_backend_buckets_get_builder(client, backendBucket, project)?;
    compute_backend_buckets_get_execute(builder)
}

/// GET projects/{project}/global/backendBuckets/{resource}/getIamPolicy
/// Gets the access control policy for a resource. May be empty if no such policy or resource exists.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_backend_buckets_get_iam_policy_execute()` to send, or `compute_backend_buckets_get_iam_policy` for simplest API.

pub fn compute_backend_buckets_get_iam_policy_builder(
    client: &SimpleHttpClient,
    project: &str,
    resource: &str,
    optionsRequestedPolicyVersion: Option<i32>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/global/backendBuckets/{}/getIamPolicy",
        project, resource,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "optionsRequestedPolicyVersion",
            optionsRequestedPolicyVersion
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET projects/{project}/global/backendBuckets/{resource}/getIamPolicy
/// Gets the access control policy for a resource. May be empty if no such policy or resource exists.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_backend_buckets_get_iam_policy_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_backend_buckets_get_iam_policy_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_backend_buckets_get_iam_policy_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Policy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Policy = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/global/backendBuckets/{resource}/getIamPolicy
/// Gets the access control policy for a resource. May be empty if no such policy or resource exists.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_backend_buckets_get_iam_policy_builder()` + `compute_backend_buckets_get_iam_policy_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_backend_buckets_get_iam_policy(
    client: &SimpleHttpClient,
    project: &str,
    resource: &str,
    optionsRequestedPolicyVersion: Option<i32>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Policy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_backend_buckets_get_iam_policy_builder(
        client,
        project,
        resource,
        optionsRequestedPolicyVersion,
    )?;
    compute_backend_buckets_get_iam_policy_execute(builder)
}

/// GET projects/{project}/global/backendBuckets
/// Creates a BackendBucket resource in the specified project using the data included in the request.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_backend_buckets_insert_execute()` to send, or `compute_backend_buckets_insert` for simplest API.

pub fn compute_backend_buckets_insert_builder(
    client: &SimpleHttpClient,
    project: &str,
    requestId: Option<&str>,
    body: &BackendBucket,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/global/backendBuckets",
        project,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "requestId",
            requestId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET projects/{project}/global/backendBuckets
/// Creates a BackendBucket resource in the specified project using the data included in the request.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_backend_buckets_insert_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_backend_buckets_insert_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_backend_buckets_insert_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/global/backendBuckets
/// Creates a BackendBucket resource in the specified project using the data included in the request.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_backend_buckets_insert_builder()` + `compute_backend_buckets_insert_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_backend_buckets_insert(
    client: &SimpleHttpClient,
    project: &str,
    requestId: Option<&str>,
    body: &BackendBucket,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_backend_buckets_insert_builder(client, project, requestId, body)?;
    compute_backend_buckets_insert_execute(builder)
}

/// GET projects/{project}/global/backendBuckets
/// Retrieves the list of BackendBucket resources available to the specified project.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_backend_buckets_list_execute()` to send, or `compute_backend_buckets_list` for simplest API.

pub fn compute_backend_buckets_list_builder(
    client: &SimpleHttpClient,
    project: &str,
    filter: Option<&str>,
    maxResults: Option<i32>,
    orderBy: Option<&str>,
    pageToken: Option<&str>,
    returnPartialSuccess: Option<bool>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/global/backendBuckets",
        project,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "filter",
            filter
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "maxResults",
            maxResults
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "orderBy",
            orderBy
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageToken",
            pageToken
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "returnPartialSuccess",
            returnPartialSuccess
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET projects/{project}/global/backendBuckets
/// Retrieves the list of BackendBucket resources available to the specified project.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_backend_buckets_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_backend_buckets_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_backend_buckets_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<BackendBucketList>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: BackendBucketList = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/global/backendBuckets
/// Retrieves the list of BackendBucket resources available to the specified project.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_backend_buckets_list_builder()` + `compute_backend_buckets_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_backend_buckets_list(
    client: &SimpleHttpClient,
    project: &str,
    filter: Option<&str>,
    maxResults: Option<i32>,
    orderBy: Option<&str>,
    pageToken: Option<&str>,
    returnPartialSuccess: Option<bool>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<BackendBucketList>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = compute_backend_buckets_list_builder(
        client,
        project,
        filter,
        maxResults,
        orderBy,
        pageToken,
        returnPartialSuccess,
    )?;
    compute_backend_buckets_list_execute(builder)
}

/// GET projects/{project}/global/backendBuckets/{backendBucket}
/// Updates the specified BackendBucket resource with the data included in the request. This method `supportsPATCH` semantics and uses `theJSON` merge patch format and processing rules.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_backend_buckets_patch_execute()` to send, or `compute_backend_buckets_patch` for simplest API.

pub fn compute_backend_buckets_patch_builder(
    client: &SimpleHttpClient,
    backendBucket: &str,
    project: &str,
    requestId: Option<&str>,
    body: &BackendBucket,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/global/backendBuckets/{}",
        backendBucket, project,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "requestId",
            requestId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET projects/{project}/global/backendBuckets/{backendBucket}
/// Updates the specified BackendBucket resource with the data included in the request. This method `supportsPATCH` semantics and uses `theJSON` merge patch format and processing rules.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_backend_buckets_patch_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_backend_buckets_patch_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_backend_buckets_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/global/backendBuckets/{backendBucket}
/// Updates the specified BackendBucket resource with the data included in the request. This method `supportsPATCH` semantics and uses `theJSON` merge patch format and processing rules.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_backend_buckets_patch_builder()` + `compute_backend_buckets_patch_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_backend_buckets_patch(
    client: &SimpleHttpClient,
    backendBucket: &str,
    project: &str,
    requestId: Option<&str>,
    body: &BackendBucket,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        compute_backend_buckets_patch_builder(client, backendBucket, project, requestId, body)?;
    compute_backend_buckets_patch_execute(builder)
}

/// GET projects/{project}/global/backendBuckets/{backendBucket}/setEdgeSecurityPolicy
/// Sets the edge security policy for the specified backend bucket.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_backend_buckets_set_edge_security_policy_execute()` to send, or `compute_backend_buckets_set_edge_security_policy` for simplest API.

pub fn compute_backend_buckets_set_edge_security_policy_builder(
    client: &SimpleHttpClient,
    backendBucket: &str,
    project: &str,
    requestId: Option<&str>,
    body: &SecurityPolicyReference,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/global/backendBuckets/{}/setEdgeSecurityPolicy",
        backendBucket, project,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "requestId",
            requestId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET projects/{project}/global/backendBuckets/{backendBucket}/setEdgeSecurityPolicy
/// Sets the edge security policy for the specified backend bucket.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_backend_buckets_set_edge_security_policy_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_backend_buckets_set_edge_security_policy_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_backend_buckets_set_edge_security_policy_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/global/backendBuckets/{backendBucket}/setEdgeSecurityPolicy
/// Sets the edge security policy for the specified backend bucket.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_backend_buckets_set_edge_security_policy_builder()` + `compute_backend_buckets_set_edge_security_policy_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_backend_buckets_set_edge_security_policy(
    client: &SimpleHttpClient,
    backendBucket: &str,
    project: &str,
    requestId: Option<&str>,
    body: &SecurityPolicyReference,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_backend_buckets_set_edge_security_policy_builder(
        client,
        backendBucket,
        project,
        requestId,
        body,
    )?;
    compute_backend_buckets_set_edge_security_policy_execute(builder)
}

/// GET projects/{project}/global/backendBuckets/{resource}/setIamPolicy
/// Sets the access control policy on the specified resource. Replaces any existing policy.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_backend_buckets_set_iam_policy_execute()` to send, or `compute_backend_buckets_set_iam_policy` for simplest API.

pub fn compute_backend_buckets_set_iam_policy_builder(
    client: &SimpleHttpClient,
    project: &str,
    resource: &str,
    body: &GlobalSetPolicyRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/global/backendBuckets/{}/setIamPolicy",
        project, resource,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET projects/{project}/global/backendBuckets/{resource}/setIamPolicy
/// Sets the access control policy on the specified resource. Replaces any existing policy.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_backend_buckets_set_iam_policy_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_backend_buckets_set_iam_policy_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_backend_buckets_set_iam_policy_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Policy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Policy = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/global/backendBuckets/{resource}/setIamPolicy
/// Sets the access control policy on the specified resource. Replaces any existing policy.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_backend_buckets_set_iam_policy_builder()` + `compute_backend_buckets_set_iam_policy_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_backend_buckets_set_iam_policy(
    client: &SimpleHttpClient,
    project: &str,
    resource: &str,
    body: &GlobalSetPolicyRequest,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Policy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_backend_buckets_set_iam_policy_builder(client, project, resource, body)?;
    compute_backend_buckets_set_iam_policy_execute(builder)
}

/// GET projects/{project}/global/backendBuckets/{resource}/testIamPermissions
/// Returns permissions that a caller has on the specified resource.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_backend_buckets_test_iam_permissions_execute()` to send, or `compute_backend_buckets_test_iam_permissions` for simplest API.

pub fn compute_backend_buckets_test_iam_permissions_builder(
    client: &SimpleHttpClient,
    project: &str,
    resource: &str,
    body: &TestPermissionsRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/global/backendBuckets/{}/testIamPermissions",
        project, resource,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET projects/{project}/global/backendBuckets/{resource}/testIamPermissions
/// Returns permissions that a caller has on the specified resource.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_backend_buckets_test_iam_permissions_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_backend_buckets_test_iam_permissions_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_backend_buckets_test_iam_permissions_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<TestPermissionsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: TestPermissionsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/global/backendBuckets/{resource}/testIamPermissions
/// Returns permissions that a caller has on the specified resource.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_backend_buckets_test_iam_permissions_builder()` + `compute_backend_buckets_test_iam_permissions_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_backend_buckets_test_iam_permissions(
    client: &SimpleHttpClient,
    project: &str,
    resource: &str,
    body: &TestPermissionsRequest,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<TestPermissionsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        compute_backend_buckets_test_iam_permissions_builder(client, project, resource, body)?;
    compute_backend_buckets_test_iam_permissions_execute(builder)
}

/// GET projects/{project}/global/backendBuckets/{backendBucket}
/// Updates the specified BackendBucket resource with the data included in the request.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_backend_buckets_update_execute()` to send, or `compute_backend_buckets_update` for simplest API.

pub fn compute_backend_buckets_update_builder(
    client: &SimpleHttpClient,
    backendBucket: &str,
    project: &str,
    requestId: Option<&str>,
    body: &BackendBucket,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/global/backendBuckets/{}",
        backendBucket, project,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "requestId",
            requestId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET projects/{project}/global/backendBuckets/{backendBucket}
/// Updates the specified BackendBucket resource with the data included in the request.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_backend_buckets_update_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_backend_buckets_update_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_backend_buckets_update_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/global/backendBuckets/{backendBucket}
/// Updates the specified BackendBucket resource with the data included in the request.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_backend_buckets_update_builder()` + `compute_backend_buckets_update_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_backend_buckets_update(
    client: &SimpleHttpClient,
    backendBucket: &str,
    project: &str,
    requestId: Option<&str>,
    body: &BackendBucket,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        compute_backend_buckets_update_builder(client, backendBucket, project, requestId, body)?;
    compute_backend_buckets_update_execute(builder)
}

/// GET projects/{project}/global/backendServices/{backendService}/addSignedUrlKey
/// Adds a key for validating requests with signed URLs for this backend service.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_backend_services_add_signed_url_key_execute()` to send, or `compute_backend_services_add_signed_url_key` for simplest API.

pub fn compute_backend_services_add_signed_url_key_builder(
    client: &SimpleHttpClient,
    backendService: &str,
    project: &str,
    requestId: Option<&str>,
    body: &SignedUrlKey,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/global/backendServices/{}/addSignedUrlKey",
        backendService, project,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "requestId",
            requestId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET projects/{project}/global/backendServices/{backendService}/addSignedUrlKey
/// Adds a key for validating requests with signed URLs for this backend service.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_backend_services_add_signed_url_key_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_backend_services_add_signed_url_key_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_backend_services_add_signed_url_key_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/global/backendServices/{backendService}/addSignedUrlKey
/// Adds a key for validating requests with signed URLs for this backend service.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_backend_services_add_signed_url_key_builder()` + `compute_backend_services_add_signed_url_key_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_backend_services_add_signed_url_key(
    client: &SimpleHttpClient,
    backendService: &str,
    project: &str,
    requestId: Option<&str>,
    body: &SignedUrlKey,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_backend_services_add_signed_url_key_builder(
        client,
        backendService,
        project,
        requestId,
        body,
    )?;
    compute_backend_services_add_signed_url_key_execute(builder)
}

/// GET projects/{project}/aggregated/backendServices
/// Retrieves the list of all BackendService resources, regional and global, available to the specified project.  To prevent failure, it is recommended that you set the `returnPartialSuccess` parameter to `true`.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_backend_services_aggregated_list_execute()` to send, or `compute_backend_services_aggregated_list` for simplest API.

pub fn compute_backend_services_aggregated_list_builder(
    client: &SimpleHttpClient,
    project: &str,
    filter: Option<&str>,
    includeAllScopes: Option<bool>,
    maxResults: Option<i32>,
    orderBy: Option<&str>,
    pageToken: Option<&str>,
    returnPartialSuccess: Option<bool>,
    serviceProjectNumber: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/aggregated/backendServices",
        project,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "filter",
            filter
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "includeAllScopes",
            includeAllScopes
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "maxResults",
            maxResults
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "orderBy",
            orderBy
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageToken",
            pageToken
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "returnPartialSuccess",
            returnPartialSuccess
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "serviceProjectNumber",
            serviceProjectNumber
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET projects/{project}/aggregated/backendServices
/// Retrieves the list of all BackendService resources, regional and global, available to the specified project.  To prevent failure, it is recommended that you set the `returnPartialSuccess` parameter to `true`.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_backend_services_aggregated_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_backend_services_aggregated_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_backend_services_aggregated_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<BackendServiceAggregatedList>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: BackendServiceAggregatedList = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/aggregated/backendServices
/// Retrieves the list of all BackendService resources, regional and global, available to the specified project.  To prevent failure, it is recommended that you set the `returnPartialSuccess` parameter to `true`.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_backend_services_aggregated_list_builder()` + `compute_backend_services_aggregated_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_backend_services_aggregated_list(
    client: &SimpleHttpClient,
    project: &str,
    filter: Option<&str>,
    includeAllScopes: Option<bool>,
    maxResults: Option<i32>,
    orderBy: Option<&str>,
    pageToken: Option<&str>,
    returnPartialSuccess: Option<bool>,
    serviceProjectNumber: Option<&str>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<BackendServiceAggregatedList>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = compute_backend_services_aggregated_list_builder(
        client,
        project,
        filter,
        includeAllScopes,
        maxResults,
        orderBy,
        pageToken,
        returnPartialSuccess,
        serviceProjectNumber,
    )?;
    compute_backend_services_aggregated_list_execute(builder)
}

/// GET projects/{project}/global/backendServices/{backendService}
/// Deletes the specified BackendService resource.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_backend_services_delete_execute()` to send, or `compute_backend_services_delete` for simplest API.

pub fn compute_backend_services_delete_builder(
    client: &SimpleHttpClient,
    backendService: &str,
    project: &str,
    requestId: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/global/backendServices/{}",
        backendService, project,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "requestId",
            requestId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET projects/{project}/global/backendServices/{backendService}
/// Deletes the specified BackendService resource.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_backend_services_delete_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_backend_services_delete_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_backend_services_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/global/backendServices/{backendService}
/// Deletes the specified BackendService resource.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_backend_services_delete_builder()` + `compute_backend_services_delete_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_backend_services_delete(
    client: &SimpleHttpClient,
    backendService: &str,
    project: &str,
    requestId: Option<&str>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        compute_backend_services_delete_builder(client, backendService, project, requestId)?;
    compute_backend_services_delete_execute(builder)
}

/// GET projects/{project}/global/backendServices/{backendService}/deleteSignedUrlKey
/// Deletes a key for validating requests with signed URLs for this backend service.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_backend_services_delete_signed_url_key_execute()` to send, or `compute_backend_services_delete_signed_url_key` for simplest API.

pub fn compute_backend_services_delete_signed_url_key_builder(
    client: &SimpleHttpClient,
    backendService: &str,
    project: &str,
    keyName: Option<&str>,
    requestId: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/global/backendServices/{}/deleteSignedUrlKey",
        backendService, project,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "keyName",
            keyName
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "requestId",
            requestId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET projects/{project}/global/backendServices/{backendService}/deleteSignedUrlKey
/// Deletes a key for validating requests with signed URLs for this backend service.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_backend_services_delete_signed_url_key_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_backend_services_delete_signed_url_key_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_backend_services_delete_signed_url_key_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/global/backendServices/{backendService}/deleteSignedUrlKey
/// Deletes a key for validating requests with signed URLs for this backend service.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_backend_services_delete_signed_url_key_builder()` + `compute_backend_services_delete_signed_url_key_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_backend_services_delete_signed_url_key(
    client: &SimpleHttpClient,
    backendService: &str,
    project: &str,
    keyName: Option<&str>,
    requestId: Option<&str>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_backend_services_delete_signed_url_key_builder(
        client,
        backendService,
        project,
        keyName,
        requestId,
    )?;
    compute_backend_services_delete_signed_url_key_execute(builder)
}

/// GET projects/{project}/global/backendServices/{backendService}
/// Returns the specified BackendService resource.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_backend_services_get_execute()` to send, or `compute_backend_services_get` for simplest API.

pub fn compute_backend_services_get_builder(
    client: &SimpleHttpClient,
    backendService: &str,
    project: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/global/backendServices/{}",
        backendService, project,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/global/backendServices/{backendService}
/// Returns the specified BackendService resource.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_backend_services_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_backend_services_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_backend_services_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<BackendService>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: BackendService = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/global/backendServices/{backendService}
/// Returns the specified BackendService resource.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_backend_services_get_builder()` + `compute_backend_services_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_backend_services_get(
    client: &SimpleHttpClient,
    backendService: &str,
    project: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<BackendService>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = compute_backend_services_get_builder(client, backendService, project)?;
    compute_backend_services_get_execute(builder)
}

/// GET projects/{project}/global/backendServices/{backendService}/getEffectiveSecurityPolicies
/// Returns effective security policies applied to this backend service.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_backend_services_get_effective_security_policies_execute()` to send, or `compute_backend_services_get_effective_security_policies` for simplest API.

pub fn compute_backend_services_get_effective_security_policies_builder(
    client: &SimpleHttpClient,
    backendService: &str,
    project: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/global/backendServices/{}/getEffectiveSecurityPolicies",
        backendService, project,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/global/backendServices/{backendService}/getEffectiveSecurityPolicies
/// Returns effective security policies applied to this backend service.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_backend_services_get_effective_security_policies_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_backend_services_get_effective_security_policies_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_backend_services_get_effective_security_policies_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/global/backendServices/{backendService}/getEffectiveSecurityPolicies
/// Returns effective security policies applied to this backend service.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_backend_services_get_effective_security_policies_builder()` + `compute_backend_services_get_effective_security_policies_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_backend_services_get_effective_security_policies(
    client: &SimpleHttpClient,
    backendService: &str,
    project: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_backend_services_get_effective_security_policies_builder(
        client,
        backendService,
        project,
    )?;
    compute_backend_services_get_effective_security_policies_execute(builder)
}

/// GET projects/{project}/global/backendServices/{backendService}/getHealth
/// Gets the most recent health check results for this BackendService.  Example request body:  { "group": "/`zones/us-east1-b/`instanceGroups`/lb-backend-example`" }
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_backend_services_get_health_execute()` to send, or `compute_backend_services_get_health` for simplest API.

pub fn compute_backend_services_get_health_builder(
    client: &SimpleHttpClient,
    backendService: &str,
    project: &str,
    body: &ResourceGroupReference,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/global/backendServices/{}/getHealth",
        backendService, project,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET projects/{project}/global/backendServices/{backendService}/getHealth
/// Gets the most recent health check results for this BackendService.  Example request body:  { "group": "/`zones/us-east1-b/`instanceGroups`/lb-backend-example`" }
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_backend_services_get_health_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_backend_services_get_health_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_backend_services_get_health_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<BackendServiceGroupHealth>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: BackendServiceGroupHealth = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/global/backendServices/{backendService}/getHealth
/// Gets the most recent health check results for this BackendService.  Example request body:  { "group": "/`zones/us-east1-b/`instanceGroups`/lb-backend-example`" }
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_backend_services_get_health_builder()` + `compute_backend_services_get_health_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_backend_services_get_health(
    client: &SimpleHttpClient,
    backendService: &str,
    project: &str,
    body: &ResourceGroupReference,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<BackendServiceGroupHealth>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        compute_backend_services_get_health_builder(client, backendService, project, body)?;
    compute_backend_services_get_health_execute(builder)
}

/// GET projects/{project}/global/backendServices/{resource}/getIamPolicy
/// Gets the access control policy for a resource. May be empty if no such policy or resource exists.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_backend_services_get_iam_policy_execute()` to send, or `compute_backend_services_get_iam_policy` for simplest API.

pub fn compute_backend_services_get_iam_policy_builder(
    client: &SimpleHttpClient,
    project: &str,
    resource: &str,
    optionsRequestedPolicyVersion: Option<i32>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/global/backendServices/{}/getIamPolicy",
        project, resource,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "optionsRequestedPolicyVersion",
            optionsRequestedPolicyVersion
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET projects/{project}/global/backendServices/{resource}/getIamPolicy
/// Gets the access control policy for a resource. May be empty if no such policy or resource exists.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_backend_services_get_iam_policy_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_backend_services_get_iam_policy_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_backend_services_get_iam_policy_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Policy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Policy = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/global/backendServices/{resource}/getIamPolicy
/// Gets the access control policy for a resource. May be empty if no such policy or resource exists.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_backend_services_get_iam_policy_builder()` + `compute_backend_services_get_iam_policy_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_backend_services_get_iam_policy(
    client: &SimpleHttpClient,
    project: &str,
    resource: &str,
    optionsRequestedPolicyVersion: Option<i32>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Policy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_backend_services_get_iam_policy_builder(
        client,
        project,
        resource,
        optionsRequestedPolicyVersion,
    )?;
    compute_backend_services_get_iam_policy_execute(builder)
}

/// GET projects/{project}/global/backendServices
/// Creates a BackendService resource in the specified project using the data included in the request. For more information, see Backend services overview.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_backend_services_insert_execute()` to send, or `compute_backend_services_insert` for simplest API.

pub fn compute_backend_services_insert_builder(
    client: &SimpleHttpClient,
    project: &str,
    requestId: Option<&str>,
    body: &BackendService,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/global/backendServices",
        project,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "requestId",
            requestId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET projects/{project}/global/backendServices
/// Creates a BackendService resource in the specified project using the data included in the request. For more information, see Backend services overview.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_backend_services_insert_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_backend_services_insert_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_backend_services_insert_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/global/backendServices
/// Creates a BackendService resource in the specified project using the data included in the request. For more information, see Backend services overview.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_backend_services_insert_builder()` + `compute_backend_services_insert_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_backend_services_insert(
    client: &SimpleHttpClient,
    project: &str,
    requestId: Option<&str>,
    body: &BackendService,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_backend_services_insert_builder(client, project, requestId, body)?;
    compute_backend_services_insert_execute(builder)
}

/// GET projects/{project}/global/backendServices
/// Retrieves the list of BackendService resources available to the specified project.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_backend_services_list_execute()` to send, or `compute_backend_services_list` for simplest API.

pub fn compute_backend_services_list_builder(
    client: &SimpleHttpClient,
    project: &str,
    filter: Option<&str>,
    maxResults: Option<i32>,
    orderBy: Option<&str>,
    pageToken: Option<&str>,
    returnPartialSuccess: Option<bool>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/global/backendServices",
        project,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "filter",
            filter
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "maxResults",
            maxResults
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "orderBy",
            orderBy
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageToken",
            pageToken
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "returnPartialSuccess",
            returnPartialSuccess
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET projects/{project}/global/backendServices
/// Retrieves the list of BackendService resources available to the specified project.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_backend_services_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_backend_services_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_backend_services_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<BackendServiceList>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: BackendServiceList = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/global/backendServices
/// Retrieves the list of BackendService resources available to the specified project.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_backend_services_list_builder()` + `compute_backend_services_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_backend_services_list(
    client: &SimpleHttpClient,
    project: &str,
    filter: Option<&str>,
    maxResults: Option<i32>,
    orderBy: Option<&str>,
    pageToken: Option<&str>,
    returnPartialSuccess: Option<bool>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<BackendServiceList>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = compute_backend_services_list_builder(
        client,
        project,
        filter,
        maxResults,
        orderBy,
        pageToken,
        returnPartialSuccess,
    )?;
    compute_backend_services_list_execute(builder)
}

/// GET projects/{project}/global/backendServices/listUsable
/// Retrieves a list of all usable backend services in the specified project.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_backend_services_list_usable_execute()` to send, or `compute_backend_services_list_usable` for simplest API.

pub fn compute_backend_services_list_usable_builder(
    client: &SimpleHttpClient,
    project: &str,
    filter: Option<&str>,
    maxResults: Option<i32>,
    orderBy: Option<&str>,
    pageToken: Option<&str>,
    returnPartialSuccess: Option<bool>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/global/backendServices/listUsable",
        project,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "filter",
            filter
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "maxResults",
            maxResults
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "orderBy",
            orderBy
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageToken",
            pageToken
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "returnPartialSuccess",
            returnPartialSuccess
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET projects/{project}/global/backendServices/listUsable
/// Retrieves a list of all usable backend services in the specified project.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_backend_services_list_usable_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_backend_services_list_usable_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_backend_services_list_usable_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<BackendServiceListUsable>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: BackendServiceListUsable = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/global/backendServices/listUsable
/// Retrieves a list of all usable backend services in the specified project.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_backend_services_list_usable_builder()` + `compute_backend_services_list_usable_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_backend_services_list_usable(
    client: &SimpleHttpClient,
    project: &str,
    filter: Option<&str>,
    maxResults: Option<i32>,
    orderBy: Option<&str>,
    pageToken: Option<&str>,
    returnPartialSuccess: Option<bool>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<BackendServiceListUsable>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = compute_backend_services_list_usable_builder(
        client,
        project,
        filter,
        maxResults,
        orderBy,
        pageToken,
        returnPartialSuccess,
    )?;
    compute_backend_services_list_usable_execute(builder)
}

/// GET projects/{project}/global/backendServices/{backendService}
/// Patches the specified BackendService resource with the data included in the request. For more information, see Backend services overview. This method supports PATCH semantics and uses the JSON merge patch format and processing rules.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_backend_services_patch_execute()` to send, or `compute_backend_services_patch` for simplest API.

pub fn compute_backend_services_patch_builder(
    client: &SimpleHttpClient,
    backendService: &str,
    project: &str,
    requestId: Option<&str>,
    body: &BackendService,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/global/backendServices/{}",
        backendService, project,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "requestId",
            requestId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET projects/{project}/global/backendServices/{backendService}
/// Patches the specified BackendService resource with the data included in the request. For more information, see Backend services overview. This method supports PATCH semantics and uses the JSON merge patch format and processing rules.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_backend_services_patch_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_backend_services_patch_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_backend_services_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/global/backendServices/{backendService}
/// Patches the specified BackendService resource with the data included in the request. For more information, see Backend services overview. This method supports PATCH semantics and uses the JSON merge patch format and processing rules.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_backend_services_patch_builder()` + `compute_backend_services_patch_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_backend_services_patch(
    client: &SimpleHttpClient,
    backendService: &str,
    project: &str,
    requestId: Option<&str>,
    body: &BackendService,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        compute_backend_services_patch_builder(client, backendService, project, requestId, body)?;
    compute_backend_services_patch_execute(builder)
}

/// GET projects/{project}/global/backendServices/{backendService}/setEdgeSecurityPolicy
/// Sets the edge security policy for the specified backend service.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_backend_services_set_edge_security_policy_execute()` to send, or `compute_backend_services_set_edge_security_policy` for simplest API.

pub fn compute_backend_services_set_edge_security_policy_builder(
    client: &SimpleHttpClient,
    backendService: &str,
    project: &str,
    requestId: Option<&str>,
    body: &SecurityPolicyReference,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/global/backendServices/{}/setEdgeSecurityPolicy",
        backendService, project,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "requestId",
            requestId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET projects/{project}/global/backendServices/{backendService}/setEdgeSecurityPolicy
/// Sets the edge security policy for the specified backend service.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_backend_services_set_edge_security_policy_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_backend_services_set_edge_security_policy_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_backend_services_set_edge_security_policy_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/global/backendServices/{backendService}/setEdgeSecurityPolicy
/// Sets the edge security policy for the specified backend service.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_backend_services_set_edge_security_policy_builder()` + `compute_backend_services_set_edge_security_policy_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_backend_services_set_edge_security_policy(
    client: &SimpleHttpClient,
    backendService: &str,
    project: &str,
    requestId: Option<&str>,
    body: &SecurityPolicyReference,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_backend_services_set_edge_security_policy_builder(
        client,
        backendService,
        project,
        requestId,
        body,
    )?;
    compute_backend_services_set_edge_security_policy_execute(builder)
}

/// GET projects/{project}/global/backendServices/{resource}/setIamPolicy
/// Sets the access control policy on the specified resource. Replaces any existing policy.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_backend_services_set_iam_policy_execute()` to send, or `compute_backend_services_set_iam_policy` for simplest API.

pub fn compute_backend_services_set_iam_policy_builder(
    client: &SimpleHttpClient,
    project: &str,
    resource: &str,
    body: &GlobalSetPolicyRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/global/backendServices/{}/setIamPolicy",
        project, resource,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET projects/{project}/global/backendServices/{resource}/setIamPolicy
/// Sets the access control policy on the specified resource. Replaces any existing policy.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_backend_services_set_iam_policy_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_backend_services_set_iam_policy_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_backend_services_set_iam_policy_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Policy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Policy = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/global/backendServices/{resource}/setIamPolicy
/// Sets the access control policy on the specified resource. Replaces any existing policy.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_backend_services_set_iam_policy_builder()` + `compute_backend_services_set_iam_policy_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_backend_services_set_iam_policy(
    client: &SimpleHttpClient,
    project: &str,
    resource: &str,
    body: &GlobalSetPolicyRequest,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Policy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_backend_services_set_iam_policy_builder(client, project, resource, body)?;
    compute_backend_services_set_iam_policy_execute(builder)
}

/// GET projects/{project}/global/backendServices/{backendService}/setSecurityPolicy
/// Sets the Google Cloud Armor security policy for the specified backend service. For more information, `seeGoogle` Cloud Armor Overview
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_backend_services_set_security_policy_execute()` to send, or `compute_backend_services_set_security_policy` for simplest API.

pub fn compute_backend_services_set_security_policy_builder(
    client: &SimpleHttpClient,
    backendService: &str,
    project: &str,
    requestId: Option<&str>,
    body: &SecurityPolicyReference,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/global/backendServices/{}/setSecurityPolicy",
        backendService, project,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "requestId",
            requestId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET projects/{project}/global/backendServices/{backendService}/setSecurityPolicy
/// Sets the Google Cloud Armor security policy for the specified backend service. For more information, `seeGoogle` Cloud Armor Overview
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_backend_services_set_security_policy_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_backend_services_set_security_policy_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_backend_services_set_security_policy_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/global/backendServices/{backendService}/setSecurityPolicy
/// Sets the Google Cloud Armor security policy for the specified backend service. For more information, `seeGoogle` Cloud Armor Overview
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_backend_services_set_security_policy_builder()` + `compute_backend_services_set_security_policy_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_backend_services_set_security_policy(
    client: &SimpleHttpClient,
    backendService: &str,
    project: &str,
    requestId: Option<&str>,
    body: &SecurityPolicyReference,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_backend_services_set_security_policy_builder(
        client,
        backendService,
        project,
        requestId,
        body,
    )?;
    compute_backend_services_set_security_policy_execute(builder)
}

/// GET projects/{project}/global/backendServices/{resource}/testIamPermissions
/// Returns permissions that a caller has on the specified resource.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_backend_services_test_iam_permissions_execute()` to send, or `compute_backend_services_test_iam_permissions` for simplest API.

pub fn compute_backend_services_test_iam_permissions_builder(
    client: &SimpleHttpClient,
    project: &str,
    resource: &str,
    body: &TestPermissionsRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/global/backendServices/{}/testIamPermissions",
        project, resource,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET projects/{project}/global/backendServices/{resource}/testIamPermissions
/// Returns permissions that a caller has on the specified resource.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_backend_services_test_iam_permissions_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_backend_services_test_iam_permissions_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_backend_services_test_iam_permissions_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<TestPermissionsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: TestPermissionsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/global/backendServices/{resource}/testIamPermissions
/// Returns permissions that a caller has on the specified resource.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_backend_services_test_iam_permissions_builder()` + `compute_backend_services_test_iam_permissions_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_backend_services_test_iam_permissions(
    client: &SimpleHttpClient,
    project: &str,
    resource: &str,
    body: &TestPermissionsRequest,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<TestPermissionsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        compute_backend_services_test_iam_permissions_builder(client, project, resource, body)?;
    compute_backend_services_test_iam_permissions_execute(builder)
}

/// GET projects/{project}/global/backendServices/{backendService}
/// Updates the specified BackendService resource with the data included in the request. For more information, `seeBackend` services overview.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_backend_services_update_execute()` to send, or `compute_backend_services_update` for simplest API.

pub fn compute_backend_services_update_builder(
    client: &SimpleHttpClient,
    backendService: &str,
    project: &str,
    requestId: Option<&str>,
    body: &BackendService,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/global/backendServices/{}",
        backendService, project,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "requestId",
            requestId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET projects/{project}/global/backendServices/{backendService}
/// Updates the specified BackendService resource with the data included in the request. For more information, `seeBackend` services overview.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_backend_services_update_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_backend_services_update_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_backend_services_update_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/global/backendServices/{backendService}
/// Updates the specified BackendService resource with the data included in the request. For more information, `seeBackend` services overview.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_backend_services_update_builder()` + `compute_backend_services_update_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_backend_services_update(
    client: &SimpleHttpClient,
    backendService: &str,
    project: &str,
    requestId: Option<&str>,
    body: &BackendService,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        compute_backend_services_update_builder(client, backendService, project, requestId, body)?;
    compute_backend_services_update_execute(builder)
}

/// GET projects/{project}/global/crossSiteNetworks/{crossSiteNetwork}
/// Deletes the specified cross-site network in the given scope.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_cross_site_networks_delete_execute()` to send, or `compute_cross_site_networks_delete` for simplest API.

pub fn compute_cross_site_networks_delete_builder(
    client: &SimpleHttpClient,
    crossSiteNetwork: &str,
    project: &str,
    requestId: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/global/crossSiteNetworks/{}",
        crossSiteNetwork, project,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "requestId",
            requestId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET projects/{project}/global/crossSiteNetworks/{crossSiteNetwork}
/// Deletes the specified cross-site network in the given scope.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_cross_site_networks_delete_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_cross_site_networks_delete_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_cross_site_networks_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/global/crossSiteNetworks/{crossSiteNetwork}
/// Deletes the specified cross-site network in the given scope.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_cross_site_networks_delete_builder()` + `compute_cross_site_networks_delete_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_cross_site_networks_delete(
    client: &SimpleHttpClient,
    crossSiteNetwork: &str,
    project: &str,
    requestId: Option<&str>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        compute_cross_site_networks_delete_builder(client, crossSiteNetwork, project, requestId)?;
    compute_cross_site_networks_delete_execute(builder)
}

/// GET projects/{project}/global/crossSiteNetworks/{crossSiteNetwork}
/// Returns the specified cross-site network in the given scope.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_cross_site_networks_get_execute()` to send, or `compute_cross_site_networks_get` for simplest API.

pub fn compute_cross_site_networks_get_builder(
    client: &SimpleHttpClient,
    crossSiteNetwork: &str,
    project: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/global/crossSiteNetworks/{}",
        crossSiteNetwork, project,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/global/crossSiteNetworks/{crossSiteNetwork}
/// Returns the specified cross-site network in the given scope.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_cross_site_networks_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_cross_site_networks_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_cross_site_networks_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<CrossSiteNetwork>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: CrossSiteNetwork = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/global/crossSiteNetworks/{crossSiteNetwork}
/// Returns the specified cross-site network in the given scope.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_cross_site_networks_get_builder()` + `compute_cross_site_networks_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_cross_site_networks_get(
    client: &SimpleHttpClient,
    crossSiteNetwork: &str,
    project: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<CrossSiteNetwork>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = compute_cross_site_networks_get_builder(client, crossSiteNetwork, project)?;
    compute_cross_site_networks_get_execute(builder)
}

/// GET projects/{project}/global/crossSiteNetworks
/// Creates a cross-site network in the specified project in the given scope using the parameters that are included in the request.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_cross_site_networks_insert_execute()` to send, or `compute_cross_site_networks_insert` for simplest API.

pub fn compute_cross_site_networks_insert_builder(
    client: &SimpleHttpClient,
    project: &str,
    requestId: Option<&str>,
    validateOnly: Option<bool>,
    body: &CrossSiteNetwork,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/global/crossSiteNetworks",
        project,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "requestId",
            requestId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "validateOnly",
            validateOnly
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET projects/{project}/global/crossSiteNetworks
/// Creates a cross-site network in the specified project in the given scope using the parameters that are included in the request.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_cross_site_networks_insert_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_cross_site_networks_insert_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_cross_site_networks_insert_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/global/crossSiteNetworks
/// Creates a cross-site network in the specified project in the given scope using the parameters that are included in the request.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_cross_site_networks_insert_builder()` + `compute_cross_site_networks_insert_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_cross_site_networks_insert(
    client: &SimpleHttpClient,
    project: &str,
    requestId: Option<&str>,
    validateOnly: Option<bool>,
    body: &CrossSiteNetwork,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        compute_cross_site_networks_insert_builder(client, project, requestId, validateOnly, body)?;
    compute_cross_site_networks_insert_execute(builder)
}

/// GET projects/{project}/global/crossSiteNetworks
/// Lists the cross-site networks for a project in the given scope.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_cross_site_networks_list_execute()` to send, or `compute_cross_site_networks_list` for simplest API.

pub fn compute_cross_site_networks_list_builder(
    client: &SimpleHttpClient,
    project: &str,
    filter: Option<&str>,
    maxResults: Option<i32>,
    orderBy: Option<&str>,
    pageToken: Option<&str>,
    returnPartialSuccess: Option<bool>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/global/crossSiteNetworks",
        project,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "filter",
            filter
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "maxResults",
            maxResults
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "orderBy",
            orderBy
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageToken",
            pageToken
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "returnPartialSuccess",
            returnPartialSuccess
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET projects/{project}/global/crossSiteNetworks
/// Lists the cross-site networks for a project in the given scope.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_cross_site_networks_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_cross_site_networks_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_cross_site_networks_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<CrossSiteNetworkList>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: CrossSiteNetworkList = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/global/crossSiteNetworks
/// Lists the cross-site networks for a project in the given scope.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_cross_site_networks_list_builder()` + `compute_cross_site_networks_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_cross_site_networks_list(
    client: &SimpleHttpClient,
    project: &str,
    filter: Option<&str>,
    maxResults: Option<i32>,
    orderBy: Option<&str>,
    pageToken: Option<&str>,
    returnPartialSuccess: Option<bool>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<CrossSiteNetworkList>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = compute_cross_site_networks_list_builder(
        client,
        project,
        filter,
        maxResults,
        orderBy,
        pageToken,
        returnPartialSuccess,
    )?;
    compute_cross_site_networks_list_execute(builder)
}

/// GET projects/{project}/global/crossSiteNetworks/{crossSiteNetwork}
/// Updates the specified cross-site network with the data included in the request. This method `supportsPATCH` semantics and uses `theJSON` merge patch format and processing rules.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_cross_site_networks_patch_execute()` to send, or `compute_cross_site_networks_patch` for simplest API.

pub fn compute_cross_site_networks_patch_builder(
    client: &SimpleHttpClient,
    crossSiteNetwork: &str,
    project: &str,
    requestId: Option<&str>,
    updateMask: Option<&str>,
    validateOnly: Option<bool>,
    body: &CrossSiteNetwork,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/global/crossSiteNetworks/{}",
        crossSiteNetwork, project,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "requestId",
            requestId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "updateMask",
            updateMask
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "validateOnly",
            validateOnly
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET projects/{project}/global/crossSiteNetworks/{crossSiteNetwork}
/// Updates the specified cross-site network with the data included in the request. This method `supportsPATCH` semantics and uses `theJSON` merge patch format and processing rules.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_cross_site_networks_patch_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_cross_site_networks_patch_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_cross_site_networks_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/global/crossSiteNetworks/{crossSiteNetwork}
/// Updates the specified cross-site network with the data included in the request. This method `supportsPATCH` semantics and uses `theJSON` merge patch format and processing rules.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_cross_site_networks_patch_builder()` + `compute_cross_site_networks_patch_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_cross_site_networks_patch(
    client: &SimpleHttpClient,
    crossSiteNetwork: &str,
    project: &str,
    requestId: Option<&str>,
    updateMask: Option<&str>,
    validateOnly: Option<bool>,
    body: &CrossSiteNetwork,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_cross_site_networks_patch_builder(
        client,
        crossSiteNetwork,
        project,
        requestId,
        updateMask,
        validateOnly,
        body,
    )?;
    compute_cross_site_networks_patch_execute(builder)
}

/// GET projects/{project}/aggregated/diskTypes
/// Retrieves an aggregated list of disk types.  To prevent failure, it is recommended that you set the `returnPartialSuccess` parameter to `true`.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_disk_types_aggregated_list_execute()` to send, or `compute_disk_types_aggregated_list` for simplest API.

pub fn compute_disk_types_aggregated_list_builder(
    client: &SimpleHttpClient,
    project: &str,
    filter: Option<&str>,
    includeAllScopes: Option<bool>,
    maxResults: Option<i32>,
    orderBy: Option<&str>,
    pageToken: Option<&str>,
    returnPartialSuccess: Option<bool>,
    serviceProjectNumber: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/aggregated/diskTypes",
        project,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "filter",
            filter
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "includeAllScopes",
            includeAllScopes
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "maxResults",
            maxResults
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "orderBy",
            orderBy
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageToken",
            pageToken
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "returnPartialSuccess",
            returnPartialSuccess
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "serviceProjectNumber",
            serviceProjectNumber
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET projects/{project}/aggregated/diskTypes
/// Retrieves an aggregated list of disk types.  To prevent failure, it is recommended that you set the `returnPartialSuccess` parameter to `true`.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_disk_types_aggregated_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_disk_types_aggregated_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_disk_types_aggregated_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<DiskTypeAggregatedList>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: DiskTypeAggregatedList = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/aggregated/diskTypes
/// Retrieves an aggregated list of disk types.  To prevent failure, it is recommended that you set the `returnPartialSuccess` parameter to `true`.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_disk_types_aggregated_list_builder()` + `compute_disk_types_aggregated_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_disk_types_aggregated_list(
    client: &SimpleHttpClient,
    project: &str,
    filter: Option<&str>,
    includeAllScopes: Option<bool>,
    maxResults: Option<i32>,
    orderBy: Option<&str>,
    pageToken: Option<&str>,
    returnPartialSuccess: Option<bool>,
    serviceProjectNumber: Option<&str>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<DiskTypeAggregatedList>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = compute_disk_types_aggregated_list_builder(
        client,
        project,
        filter,
        includeAllScopes,
        maxResults,
        orderBy,
        pageToken,
        returnPartialSuccess,
        serviceProjectNumber,
    )?;
    compute_disk_types_aggregated_list_execute(builder)
}

/// GET projects/{project}/zones/{zone}/diskTypes/{diskType}
/// Returns the specified disk type.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_disk_types_get_execute()` to send, or `compute_disk_types_get` for simplest API.

pub fn compute_disk_types_get_builder(
    client: &SimpleHttpClient,
    diskType: &str,
    project: &str,
    zone: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/zones/{}/diskTypes/{}",
        diskType, project, zone,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/zones/{zone}/diskTypes/{diskType}
/// Returns the specified disk type.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_disk_types_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_disk_types_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_disk_types_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<DiskType>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: DiskType = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/zones/{zone}/diskTypes/{diskType}
/// Returns the specified disk type.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_disk_types_get_builder()` + `compute_disk_types_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_disk_types_get(
    client: &SimpleHttpClient,
    diskType: &str,
    project: &str,
    zone: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<DiskType>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_disk_types_get_builder(client, diskType, project, zone)?;
    compute_disk_types_get_execute(builder)
}

/// GET projects/{project}/zones/{zone}/diskTypes
/// Retrieves a list of disk types available to the specified project.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_disk_types_list_execute()` to send, or `compute_disk_types_list` for simplest API.

pub fn compute_disk_types_list_builder(
    client: &SimpleHttpClient,
    project: &str,
    zone: &str,
    filter: Option<&str>,
    maxResults: Option<i32>,
    orderBy: Option<&str>,
    pageToken: Option<&str>,
    returnPartialSuccess: Option<bool>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/zones/{}/diskTypes",
        project, zone,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "filter",
            filter
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "maxResults",
            maxResults
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "orderBy",
            orderBy
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageToken",
            pageToken
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "returnPartialSuccess",
            returnPartialSuccess
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET projects/{project}/zones/{zone}/diskTypes
/// Retrieves a list of disk types available to the specified project.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_disk_types_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_disk_types_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_disk_types_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<DiskTypeList>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: DiskTypeList = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/zones/{zone}/diskTypes
/// Retrieves a list of disk types available to the specified project.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_disk_types_list_builder()` + `compute_disk_types_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_disk_types_list(
    client: &SimpleHttpClient,
    project: &str,
    zone: &str,
    filter: Option<&str>,
    maxResults: Option<i32>,
    orderBy: Option<&str>,
    pageToken: Option<&str>,
    returnPartialSuccess: Option<bool>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<DiskTypeList>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = compute_disk_types_list_builder(
        client,
        project,
        zone,
        filter,
        maxResults,
        orderBy,
        pageToken,
        returnPartialSuccess,
    )?;
    compute_disk_types_list_execute(builder)
}

/// GET projects/{project}/zones/{zone}/disks/{disk}/addResourcePolicies
/// Adds existing resource policies to a disk. You can only add one policy which will be applied to this disk for scheduling snapshot creation.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_disks_add_resource_policies_execute()` to send, or `compute_disks_add_resource_policies` for simplest API.

pub fn compute_disks_add_resource_policies_builder(
    client: &SimpleHttpClient,
    disk: &str,
    project: &str,
    zone: &str,
    requestId: Option<&str>,
    body: &DisksAddResourcePoliciesRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/zones/{}/disks/{}/addResourcePolicies",
        disk, project, zone,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "requestId",
            requestId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET projects/{project}/zones/{zone}/disks/{disk}/addResourcePolicies
/// Adds existing resource policies to a disk. You can only add one policy which will be applied to this disk for scheduling snapshot creation.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_disks_add_resource_policies_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_disks_add_resource_policies_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_disks_add_resource_policies_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/zones/{zone}/disks/{disk}/addResourcePolicies
/// Adds existing resource policies to a disk. You can only add one policy which will be applied to this disk for scheduling snapshot creation.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_disks_add_resource_policies_builder()` + `compute_disks_add_resource_policies_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_disks_add_resource_policies(
    client: &SimpleHttpClient,
    disk: &str,
    project: &str,
    zone: &str,
    requestId: Option<&str>,
    body: &DisksAddResourcePoliciesRequest,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        compute_disks_add_resource_policies_builder(client, disk, project, zone, requestId, body)?;
    compute_disks_add_resource_policies_execute(builder)
}

/// GET projects/{project}/aggregated/disks
/// Retrieves an aggregated list of persistent disks.  To prevent failure, it is recommended that you set the `returnPartialSuccess` parameter to `true`.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_disks_aggregated_list_execute()` to send, or `compute_disks_aggregated_list` for simplest API.

pub fn compute_disks_aggregated_list_builder(
    client: &SimpleHttpClient,
    project: &str,
    filter: Option<&str>,
    includeAllScopes: Option<bool>,
    maxResults: Option<i32>,
    orderBy: Option<&str>,
    pageToken: Option<&str>,
    returnPartialSuccess: Option<bool>,
    serviceProjectNumber: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/aggregated/disks",
        project,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "filter",
            filter
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "includeAllScopes",
            includeAllScopes
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "maxResults",
            maxResults
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "orderBy",
            orderBy
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageToken",
            pageToken
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "returnPartialSuccess",
            returnPartialSuccess
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "serviceProjectNumber",
            serviceProjectNumber
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET projects/{project}/aggregated/disks
/// Retrieves an aggregated list of persistent disks.  To prevent failure, it is recommended that you set the `returnPartialSuccess` parameter to `true`.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_disks_aggregated_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_disks_aggregated_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_disks_aggregated_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<DiskAggregatedList>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: DiskAggregatedList = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/aggregated/disks
/// Retrieves an aggregated list of persistent disks.  To prevent failure, it is recommended that you set the `returnPartialSuccess` parameter to `true`.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_disks_aggregated_list_builder()` + `compute_disks_aggregated_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_disks_aggregated_list(
    client: &SimpleHttpClient,
    project: &str,
    filter: Option<&str>,
    includeAllScopes: Option<bool>,
    maxResults: Option<i32>,
    orderBy: Option<&str>,
    pageToken: Option<&str>,
    returnPartialSuccess: Option<bool>,
    serviceProjectNumber: Option<&str>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<DiskAggregatedList>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = compute_disks_aggregated_list_builder(
        client,
        project,
        filter,
        includeAllScopes,
        maxResults,
        orderBy,
        pageToken,
        returnPartialSuccess,
        serviceProjectNumber,
    )?;
    compute_disks_aggregated_list_execute(builder)
}

/// GET projects/{project}/zones/{zone}/disks/bulkInsert
/// Bulk create a set of disks.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_disks_bulk_insert_execute()` to send, or `compute_disks_bulk_insert` for simplest API.

pub fn compute_disks_bulk_insert_builder(
    client: &SimpleHttpClient,
    project: &str,
    zone: &str,
    requestId: Option<&str>,
    body: &BulkInsertDiskResource,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/zones/{}/disks/bulkInsert",
        project, zone,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "requestId",
            requestId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET projects/{project}/zones/{zone}/disks/bulkInsert
/// Bulk create a set of disks.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_disks_bulk_insert_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_disks_bulk_insert_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_disks_bulk_insert_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/zones/{zone}/disks/bulkInsert
/// Bulk create a set of disks.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_disks_bulk_insert_builder()` + `compute_disks_bulk_insert_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_disks_bulk_insert(
    client: &SimpleHttpClient,
    project: &str,
    zone: &str,
    requestId: Option<&str>,
    body: &BulkInsertDiskResource,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_disks_bulk_insert_builder(client, project, zone, requestId, body)?;
    compute_disks_bulk_insert_execute(builder)
}

/// GET projects/{project}/zones/{zone}/disks/bulkSetLabels
/// Sets the labels on many disks at once. To learn more about labels, read `theLabeling` Resources documentation.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_disks_bulk_set_labels_execute()` to send, or `compute_disks_bulk_set_labels` for simplest API.

pub fn compute_disks_bulk_set_labels_builder(
    client: &SimpleHttpClient,
    project: &str,
    zone: &str,
    requestId: Option<&str>,
    resource: Option<&str>,
    body: &BulkZoneSetLabelsRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/zones/{}/disks/bulkSetLabels",
        project, zone,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "requestId",
            requestId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "resource",
            resource
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET projects/{project}/zones/{zone}/disks/bulkSetLabels
/// Sets the labels on many disks at once. To learn more about labels, read `theLabeling` Resources documentation.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_disks_bulk_set_labels_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_disks_bulk_set_labels_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_disks_bulk_set_labels_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/zones/{zone}/disks/bulkSetLabels
/// Sets the labels on many disks at once. To learn more about labels, read `theLabeling` Resources documentation.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_disks_bulk_set_labels_builder()` + `compute_disks_bulk_set_labels_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_disks_bulk_set_labels(
    client: &SimpleHttpClient,
    project: &str,
    zone: &str,
    requestId: Option<&str>,
    resource: Option<&str>,
    body: &BulkZoneSetLabelsRequest,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        compute_disks_bulk_set_labels_builder(client, project, zone, requestId, resource, body)?;
    compute_disks_bulk_set_labels_execute(builder)
}

/// GET projects/{project}/zones/{zone}/disks/{disk}/createSnapshot
/// Creates a snapshot of a specified persistent disk. For regular snapshot creation, consider using snapshots.insert instead, as that method supports more features, such as creating snapshots in a project different from the source disk project.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_disks_create_snapshot_execute()` to send, or `compute_disks_create_snapshot` for simplest API.

pub fn compute_disks_create_snapshot_builder(
    client: &SimpleHttpClient,
    disk: &str,
    project: &str,
    zone: &str,
    guestFlush: Option<bool>,
    requestId: Option<&str>,
    body: &Snapshot,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/zones/{}/disks/{}/createSnapshot",
        disk, project, zone,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "guestFlush",
            guestFlush
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "requestId",
            requestId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET projects/{project}/zones/{zone}/disks/{disk}/createSnapshot
/// Creates a snapshot of a specified persistent disk. For regular snapshot creation, consider using snapshots.insert instead, as that method supports more features, such as creating snapshots in a project different from the source disk project.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_disks_create_snapshot_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_disks_create_snapshot_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_disks_create_snapshot_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/zones/{zone}/disks/{disk}/createSnapshot
/// Creates a snapshot of a specified persistent disk. For regular snapshot creation, consider using snapshots.insert instead, as that method supports more features, such as creating snapshots in a project different from the source disk project.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_disks_create_snapshot_builder()` + `compute_disks_create_snapshot_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_disks_create_snapshot(
    client: &SimpleHttpClient,
    disk: &str,
    project: &str,
    zone: &str,
    guestFlush: Option<bool>,
    requestId: Option<&str>,
    body: &Snapshot,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_disks_create_snapshot_builder(
        client, disk, project, zone, guestFlush, requestId, body,
    )?;
    compute_disks_create_snapshot_execute(builder)
}

/// GET projects/{project}/zones/{zone}/disks/{disk}
/// Deletes the specified persistent disk. Deleting a disk removes its data permanently and is irreversible. However, deleting a disk does not delete any snapshots previously made from the disk. You must separatelydelete snapshots.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_disks_delete_execute()` to send, or `compute_disks_delete` for simplest API.

pub fn compute_disks_delete_builder(
    client: &SimpleHttpClient,
    disk: &str,
    project: &str,
    zone: &str,
    requestId: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/zones/{}/disks/{}",
        disk, project, zone,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "requestId",
            requestId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET projects/{project}/zones/{zone}/disks/{disk}
/// Deletes the specified persistent disk. Deleting a disk removes its data permanently and is irreversible. However, deleting a disk does not delete any snapshots previously made from the disk. You must separatelydelete snapshots.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_disks_delete_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_disks_delete_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_disks_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/zones/{zone}/disks/{disk}
/// Deletes the specified persistent disk. Deleting a disk removes its data permanently and is irreversible. However, deleting a disk does not delete any snapshots previously made from the disk. You must separatelydelete snapshots.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_disks_delete_builder()` + `compute_disks_delete_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_disks_delete(
    client: &SimpleHttpClient,
    disk: &str,
    project: &str,
    zone: &str,
    requestId: Option<&str>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_disks_delete_builder(client, disk, project, zone, requestId)?;
    compute_disks_delete_execute(builder)
}

/// GET projects/{project}/zones/{zone}/disks/{disk}
/// Returns the specified persistent disk.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_disks_get_execute()` to send, or `compute_disks_get` for simplest API.

pub fn compute_disks_get_builder(
    client: &SimpleHttpClient,
    disk: &str,
    project: &str,
    zone: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/zones/{}/disks/{}",
        disk, project, zone,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/zones/{zone}/disks/{disk}
/// Returns the specified persistent disk.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_disks_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_disks_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_disks_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Disk>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Disk = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/zones/{zone}/disks/{disk}
/// Returns the specified persistent disk.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_disks_get_builder()` + `compute_disks_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_disks_get(
    client: &SimpleHttpClient,
    disk: &str,
    project: &str,
    zone: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Disk>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_disks_get_builder(client, disk, project, zone)?;
    compute_disks_get_execute(builder)
}

/// GET projects/{project}/zones/{zone}/disks/{resource}/getIamPolicy
/// Gets the access control policy for a resource. May be empty if no such policy or resource exists.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_disks_get_iam_policy_execute()` to send, or `compute_disks_get_iam_policy` for simplest API.

pub fn compute_disks_get_iam_policy_builder(
    client: &SimpleHttpClient,
    project: &str,
    resource: &str,
    zone: &str,
    optionsRequestedPolicyVersion: Option<i32>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/zones/{}/disks/{}/getIamPolicy",
        project, resource, zone,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "optionsRequestedPolicyVersion",
            optionsRequestedPolicyVersion
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET projects/{project}/zones/{zone}/disks/{resource}/getIamPolicy
/// Gets the access control policy for a resource. May be empty if no such policy or resource exists.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_disks_get_iam_policy_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_disks_get_iam_policy_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_disks_get_iam_policy_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Policy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Policy = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/zones/{zone}/disks/{resource}/getIamPolicy
/// Gets the access control policy for a resource. May be empty if no such policy or resource exists.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_disks_get_iam_policy_builder()` + `compute_disks_get_iam_policy_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_disks_get_iam_policy(
    client: &SimpleHttpClient,
    project: &str,
    resource: &str,
    zone: &str,
    optionsRequestedPolicyVersion: Option<i32>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Policy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_disks_get_iam_policy_builder(
        client,
        project,
        resource,
        zone,
        optionsRequestedPolicyVersion,
    )?;
    compute_disks_get_iam_policy_execute(builder)
}

/// GET projects/{project}/zones/{zone}/disks
/// Creates a persistent disk in the specified project using the data in the request. You can create a disk from a source (`sourceImage`, `sourceSnapshot`, `orsourceDisk`) or create an empty 500 GB data disk by omitting all properties. You can also create a disk that is larger than the default size by specifying the `sizeGb` property.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_disks_insert_execute()` to send, or `compute_disks_insert` for simplest API.

pub fn compute_disks_insert_builder(
    client: &SimpleHttpClient,
    project: &str,
    zone: &str,
    requestId: Option<&str>,
    sourceImage: Option<&str>,
    body: &Disk,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/zones/{}/disks",
        project, zone,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "requestId",
            requestId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "sourceImage",
            sourceImage
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET projects/{project}/zones/{zone}/disks
/// Creates a persistent disk in the specified project using the data in the request. You can create a disk from a source (`sourceImage`, `sourceSnapshot`, `orsourceDisk`) or create an empty 500 GB data disk by omitting all properties. You can also create a disk that is larger than the default size by specifying the `sizeGb` property.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_disks_insert_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_disks_insert_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_disks_insert_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/zones/{zone}/disks
/// Creates a persistent disk in the specified project using the data in the request. You can create a disk from a source (`sourceImage`, `sourceSnapshot`, `orsourceDisk`) or create an empty 500 GB data disk by omitting all properties. You can also create a disk that is larger than the default size by specifying the `sizeGb` property.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_disks_insert_builder()` + `compute_disks_insert_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_disks_insert(
    client: &SimpleHttpClient,
    project: &str,
    zone: &str,
    requestId: Option<&str>,
    sourceImage: Option<&str>,
    body: &Disk,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        compute_disks_insert_builder(client, project, zone, requestId, sourceImage, body)?;
    compute_disks_insert_execute(builder)
}

/// GET projects/{project}/zones/{zone}/disks
/// Retrieves a list of persistent disks contained within the specified zone.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_disks_list_execute()` to send, or `compute_disks_list` for simplest API.

pub fn compute_disks_list_builder(
    client: &SimpleHttpClient,
    project: &str,
    zone: &str,
    filter: Option<&str>,
    maxResults: Option<i32>,
    orderBy: Option<&str>,
    pageToken: Option<&str>,
    returnPartialSuccess: Option<bool>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/zones/{}/disks",
        project, zone,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "filter",
            filter
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "maxResults",
            maxResults
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "orderBy",
            orderBy
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageToken",
            pageToken
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "returnPartialSuccess",
            returnPartialSuccess
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET projects/{project}/zones/{zone}/disks
/// Retrieves a list of persistent disks contained within the specified zone.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_disks_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_disks_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_disks_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<DiskList>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: DiskList = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/zones/{zone}/disks
/// Retrieves a list of persistent disks contained within the specified zone.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_disks_list_builder()` + `compute_disks_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_disks_list(
    client: &SimpleHttpClient,
    project: &str,
    zone: &str,
    filter: Option<&str>,
    maxResults: Option<i32>,
    orderBy: Option<&str>,
    pageToken: Option<&str>,
    returnPartialSuccess: Option<bool>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<DiskList>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_disks_list_builder(
        client,
        project,
        zone,
        filter,
        maxResults,
        orderBy,
        pageToken,
        returnPartialSuccess,
    )?;
    compute_disks_list_execute(builder)
}

/// GET projects/{project}/zones/{zone}/disks/{disk}/removeResourcePolicies
/// Removes resource policies from a disk.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_disks_remove_resource_policies_execute()` to send, or `compute_disks_remove_resource_policies` for simplest API.

pub fn compute_disks_remove_resource_policies_builder(
    client: &SimpleHttpClient,
    disk: &str,
    project: &str,
    zone: &str,
    requestId: Option<&str>,
    body: &DisksRemoveResourcePoliciesRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/zones/{}/disks/{}/removeResourcePolicies",
        disk, project, zone,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "requestId",
            requestId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET projects/{project}/zones/{zone}/disks/{disk}/removeResourcePolicies
/// Removes resource policies from a disk.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_disks_remove_resource_policies_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_disks_remove_resource_policies_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_disks_remove_resource_policies_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/zones/{zone}/disks/{disk}/removeResourcePolicies
/// Removes resource policies from a disk.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_disks_remove_resource_policies_builder()` + `compute_disks_remove_resource_policies_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_disks_remove_resource_policies(
    client: &SimpleHttpClient,
    disk: &str,
    project: &str,
    zone: &str,
    requestId: Option<&str>,
    body: &DisksRemoveResourcePoliciesRequest,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_disks_remove_resource_policies_builder(
        client, disk, project, zone, requestId, body,
    )?;
    compute_disks_remove_resource_policies_execute(builder)
}

/// GET projects/{project}/zones/{zone}/disks/{disk}/resize
/// Resizes the specified persistent disk. You can only increase the size of the disk.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_disks_resize_execute()` to send, or `compute_disks_resize` for simplest API.

pub fn compute_disks_resize_builder(
    client: &SimpleHttpClient,
    disk: &str,
    project: &str,
    zone: &str,
    requestId: Option<&str>,
    body: &DisksResizeRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/zones/{}/disks/{}/resize",
        disk, project, zone,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "requestId",
            requestId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET projects/{project}/zones/{zone}/disks/{disk}/resize
/// Resizes the specified persistent disk. You can only increase the size of the disk.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_disks_resize_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_disks_resize_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_disks_resize_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/zones/{zone}/disks/{disk}/resize
/// Resizes the specified persistent disk. You can only increase the size of the disk.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_disks_resize_builder()` + `compute_disks_resize_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_disks_resize(
    client: &SimpleHttpClient,
    disk: &str,
    project: &str,
    zone: &str,
    requestId: Option<&str>,
    body: &DisksResizeRequest,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_disks_resize_builder(client, disk, project, zone, requestId, body)?;
    compute_disks_resize_execute(builder)
}

/// GET projects/{project}/zones/{zone}/disks/{resource}/setIamPolicy
/// Sets the access control policy on the specified resource. Replaces any existing policy.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_disks_set_iam_policy_execute()` to send, or `compute_disks_set_iam_policy` for simplest API.

pub fn compute_disks_set_iam_policy_builder(
    client: &SimpleHttpClient,
    project: &str,
    resource: &str,
    zone: &str,
    body: &ZoneSetPolicyRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/zones/{}/disks/{}/setIamPolicy",
        project, resource, zone,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET projects/{project}/zones/{zone}/disks/{resource}/setIamPolicy
/// Sets the access control policy on the specified resource. Replaces any existing policy.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_disks_set_iam_policy_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_disks_set_iam_policy_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_disks_set_iam_policy_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Policy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Policy = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/zones/{zone}/disks/{resource}/setIamPolicy
/// Sets the access control policy on the specified resource. Replaces any existing policy.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_disks_set_iam_policy_builder()` + `compute_disks_set_iam_policy_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_disks_set_iam_policy(
    client: &SimpleHttpClient,
    project: &str,
    resource: &str,
    zone: &str,
    body: &ZoneSetPolicyRequest,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Policy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_disks_set_iam_policy_builder(client, project, resource, zone, body)?;
    compute_disks_set_iam_policy_execute(builder)
}

/// GET projects/{project}/zones/{zone}/disks/{resource}/setLabels
/// Sets the labels on a disk. To learn more about labels, read `theLabeling` Resources documentation.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_disks_set_labels_execute()` to send, or `compute_disks_set_labels` for simplest API.

pub fn compute_disks_set_labels_builder(
    client: &SimpleHttpClient,
    project: &str,
    resource: &str,
    zone: &str,
    requestId: Option<&str>,
    body: &ZoneSetLabelsRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/zones/{}/disks/{}/setLabels",
        project, resource, zone,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "requestId",
            requestId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET projects/{project}/zones/{zone}/disks/{resource}/setLabels
/// Sets the labels on a disk. To learn more about labels, read `theLabeling` Resources documentation.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_disks_set_labels_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_disks_set_labels_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_disks_set_labels_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/zones/{zone}/disks/{resource}/setLabels
/// Sets the labels on a disk. To learn more about labels, read `theLabeling` Resources documentation.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_disks_set_labels_builder()` + `compute_disks_set_labels_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_disks_set_labels(
    client: &SimpleHttpClient,
    project: &str,
    resource: &str,
    zone: &str,
    requestId: Option<&str>,
    body: &ZoneSetLabelsRequest,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        compute_disks_set_labels_builder(client, project, resource, zone, requestId, body)?;
    compute_disks_set_labels_execute(builder)
}

/// GET projects/{project}/zones/{zone}/disks/{disk}/startAsyncReplication
/// Starts asynchronous replication. Must be invoked on the primary disk.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_disks_start_async_replication_execute()` to send, or `compute_disks_start_async_replication` for simplest API.

pub fn compute_disks_start_async_replication_builder(
    client: &SimpleHttpClient,
    disk: &str,
    project: &str,
    zone: &str,
    requestId: Option<&str>,
    body: &DisksStartAsyncReplicationRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/zones/{}/disks/{}/startAsyncReplication",
        disk, project, zone,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "requestId",
            requestId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET projects/{project}/zones/{zone}/disks/{disk}/startAsyncReplication
/// Starts asynchronous replication. Must be invoked on the primary disk.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_disks_start_async_replication_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_disks_start_async_replication_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_disks_start_async_replication_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/zones/{zone}/disks/{disk}/startAsyncReplication
/// Starts asynchronous replication. Must be invoked on the primary disk.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_disks_start_async_replication_builder()` + `compute_disks_start_async_replication_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_disks_start_async_replication(
    client: &SimpleHttpClient,
    disk: &str,
    project: &str,
    zone: &str,
    requestId: Option<&str>,
    body: &DisksStartAsyncReplicationRequest,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_disks_start_async_replication_builder(
        client, disk, project, zone, requestId, body,
    )?;
    compute_disks_start_async_replication_execute(builder)
}

/// GET projects/{project}/zones/{zone}/disks/{disk}/stopAsyncReplication
/// Stops asynchronous replication. Can be invoked either on the primary or on the secondary disk.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_disks_stop_async_replication_execute()` to send, or `compute_disks_stop_async_replication` for simplest API.

pub fn compute_disks_stop_async_replication_builder(
    client: &SimpleHttpClient,
    disk: &str,
    project: &str,
    zone: &str,
    requestId: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/zones/{}/disks/{}/stopAsyncReplication",
        disk, project, zone,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "requestId",
            requestId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET projects/{project}/zones/{zone}/disks/{disk}/stopAsyncReplication
/// Stops asynchronous replication. Can be invoked either on the primary or on the secondary disk.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_disks_stop_async_replication_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_disks_stop_async_replication_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_disks_stop_async_replication_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/zones/{zone}/disks/{disk}/stopAsyncReplication
/// Stops asynchronous replication. Can be invoked either on the primary or on the secondary disk.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_disks_stop_async_replication_builder()` + `compute_disks_stop_async_replication_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_disks_stop_async_replication(
    client: &SimpleHttpClient,
    disk: &str,
    project: &str,
    zone: &str,
    requestId: Option<&str>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        compute_disks_stop_async_replication_builder(client, disk, project, zone, requestId)?;
    compute_disks_stop_async_replication_execute(builder)
}

/// GET projects/{project}/zones/{zone}/disks/stopGroupAsyncReplication
/// Stops asynchronous replication for a consistency group of disks. Can be invoked either in the primary or secondary scope.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_disks_stop_group_async_replication_execute()` to send, or `compute_disks_stop_group_async_replication` for simplest API.

pub fn compute_disks_stop_group_async_replication_builder(
    client: &SimpleHttpClient,
    project: &str,
    zone: &str,
    requestId: Option<&str>,
    body: &DisksStopGroupAsyncReplicationResource,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/zones/{}/disks/stopGroupAsyncReplication",
        project, zone,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "requestId",
            requestId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET projects/{project}/zones/{zone}/disks/stopGroupAsyncReplication
/// Stops asynchronous replication for a consistency group of disks. Can be invoked either in the primary or secondary scope.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_disks_stop_group_async_replication_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_disks_stop_group_async_replication_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_disks_stop_group_async_replication_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/zones/{zone}/disks/stopGroupAsyncReplication
/// Stops asynchronous replication for a consistency group of disks. Can be invoked either in the primary or secondary scope.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_disks_stop_group_async_replication_builder()` + `compute_disks_stop_group_async_replication_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_disks_stop_group_async_replication(
    client: &SimpleHttpClient,
    project: &str,
    zone: &str,
    requestId: Option<&str>,
    body: &DisksStopGroupAsyncReplicationResource,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        compute_disks_stop_group_async_replication_builder(client, project, zone, requestId, body)?;
    compute_disks_stop_group_async_replication_execute(builder)
}

/// GET projects/{project}/zones/{zone}/disks/{resource}/testIamPermissions
/// Returns permissions that a caller has on the specified resource.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_disks_test_iam_permissions_execute()` to send, or `compute_disks_test_iam_permissions` for simplest API.

pub fn compute_disks_test_iam_permissions_builder(
    client: &SimpleHttpClient,
    project: &str,
    resource: &str,
    zone: &str,
    body: &TestPermissionsRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/zones/{}/disks/{}/testIamPermissions",
        project, resource, zone,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET projects/{project}/zones/{zone}/disks/{resource}/testIamPermissions
/// Returns permissions that a caller has on the specified resource.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_disks_test_iam_permissions_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_disks_test_iam_permissions_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_disks_test_iam_permissions_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<TestPermissionsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: TestPermissionsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/zones/{zone}/disks/{resource}/testIamPermissions
/// Returns permissions that a caller has on the specified resource.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_disks_test_iam_permissions_builder()` + `compute_disks_test_iam_permissions_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_disks_test_iam_permissions(
    client: &SimpleHttpClient,
    project: &str,
    resource: &str,
    zone: &str,
    body: &TestPermissionsRequest,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<TestPermissionsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        compute_disks_test_iam_permissions_builder(client, project, resource, zone, body)?;
    compute_disks_test_iam_permissions_execute(builder)
}

/// GET projects/{project}/zones/{zone}/disks/{disk}
/// Updates the specified disk with the data included in the request. The update is performed only on selected fields included as part of update-mask.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_disks_update_execute()` to send, or `compute_disks_update` for simplest API.

pub fn compute_disks_update_builder(
    client: &SimpleHttpClient,
    disk: &str,
    project: &str,
    zone: &str,
    paths: Option<&str>,
    requestId: Option<&str>,
    updateMask: Option<&str>,
    body: &Disk,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/zones/{}/disks/{}",
        disk, project, zone,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "paths",
            paths
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "requestId",
            requestId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "updateMask",
            updateMask
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET projects/{project}/zones/{zone}/disks/{disk}
/// Updates the specified disk with the data included in the request. The update is performed only on selected fields included as part of update-mask.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_disks_update_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_disks_update_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_disks_update_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/zones/{zone}/disks/{disk}
/// Updates the specified disk with the data included in the request. The update is performed only on selected fields included as part of update-mask.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_disks_update_builder()` + `compute_disks_update_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_disks_update(
    client: &SimpleHttpClient,
    disk: &str,
    project: &str,
    zone: &str,
    paths: Option<&str>,
    requestId: Option<&str>,
    updateMask: Option<&str>,
    body: &Disk,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_disks_update_builder(
        client, disk, project, zone, paths, requestId, updateMask, body,
    )?;
    compute_disks_update_execute(builder)
}

/// GET projects/{project}/global/externalVpnGateways/{externalVpnGateway}
/// Deletes the specified `externalVpnGateway`.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_external_vpn_gateways_delete_execute()` to send, or `compute_external_vpn_gateways_delete` for simplest API.

pub fn compute_external_vpn_gateways_delete_builder(
    client: &SimpleHttpClient,
    externalVpnGateway: &str,
    project: &str,
    requestId: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/global/externalVpnGateways/{}",
        externalVpnGateway, project,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "requestId",
            requestId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET projects/{project}/global/externalVpnGateways/{externalVpnGateway}
/// Deletes the specified `externalVpnGateway`.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_external_vpn_gateways_delete_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_external_vpn_gateways_delete_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_external_vpn_gateways_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/global/externalVpnGateways/{externalVpnGateway}
/// Deletes the specified `externalVpnGateway`.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_external_vpn_gateways_delete_builder()` + `compute_external_vpn_gateways_delete_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_external_vpn_gateways_delete(
    client: &SimpleHttpClient,
    externalVpnGateway: &str,
    project: &str,
    requestId: Option<&str>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_external_vpn_gateways_delete_builder(
        client,
        externalVpnGateway,
        project,
        requestId,
    )?;
    compute_external_vpn_gateways_delete_execute(builder)
}

/// GET projects/{project}/global/externalVpnGateways/{externalVpnGateway}
/// Returns the specified `externalVpnGateway`. Get a list of available `externalVpnGateways` by making a list() request.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_external_vpn_gateways_get_execute()` to send, or `compute_external_vpn_gateways_get` for simplest API.

pub fn compute_external_vpn_gateways_get_builder(
    client: &SimpleHttpClient,
    externalVpnGateway: &str,
    project: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/global/externalVpnGateways/{}",
        externalVpnGateway, project,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/global/externalVpnGateways/{externalVpnGateway}
/// Returns the specified `externalVpnGateway`. Get a list of available `externalVpnGateways` by making a list() request.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_external_vpn_gateways_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_external_vpn_gateways_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_external_vpn_gateways_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ExternalVpnGateway>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ExternalVpnGateway = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/global/externalVpnGateways/{externalVpnGateway}
/// Returns the specified `externalVpnGateway`. Get a list of available `externalVpnGateways` by making a list() request.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_external_vpn_gateways_get_builder()` + `compute_external_vpn_gateways_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_external_vpn_gateways_get(
    client: &SimpleHttpClient,
    externalVpnGateway: &str,
    project: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ExternalVpnGateway>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = compute_external_vpn_gateways_get_builder(client, externalVpnGateway, project)?;
    compute_external_vpn_gateways_get_execute(builder)
}

/// GET projects/{project}/global/externalVpnGateways
/// Creates a ExternalVpnGateway in the specified project using the data included in the request.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_external_vpn_gateways_insert_execute()` to send, or `compute_external_vpn_gateways_insert` for simplest API.

pub fn compute_external_vpn_gateways_insert_builder(
    client: &SimpleHttpClient,
    project: &str,
    requestId: Option<&str>,
    body: &ExternalVpnGateway,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/global/externalVpnGateways",
        project,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "requestId",
            requestId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET projects/{project}/global/externalVpnGateways
/// Creates a ExternalVpnGateway in the specified project using the data included in the request.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_external_vpn_gateways_insert_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_external_vpn_gateways_insert_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_external_vpn_gateways_insert_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/global/externalVpnGateways
/// Creates a ExternalVpnGateway in the specified project using the data included in the request.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_external_vpn_gateways_insert_builder()` + `compute_external_vpn_gateways_insert_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_external_vpn_gateways_insert(
    client: &SimpleHttpClient,
    project: &str,
    requestId: Option<&str>,
    body: &ExternalVpnGateway,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_external_vpn_gateways_insert_builder(client, project, requestId, body)?;
    compute_external_vpn_gateways_insert_execute(builder)
}

/// GET projects/{project}/global/externalVpnGateways
/// Retrieves the list of ExternalVpnGateway available to the specified project.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_external_vpn_gateways_list_execute()` to send, or `compute_external_vpn_gateways_list` for simplest API.

pub fn compute_external_vpn_gateways_list_builder(
    client: &SimpleHttpClient,
    project: &str,
    filter: Option<&str>,
    maxResults: Option<i32>,
    orderBy: Option<&str>,
    pageToken: Option<&str>,
    returnPartialSuccess: Option<bool>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/global/externalVpnGateways",
        project,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "filter",
            filter
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "maxResults",
            maxResults
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "orderBy",
            orderBy
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageToken",
            pageToken
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "returnPartialSuccess",
            returnPartialSuccess
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET projects/{project}/global/externalVpnGateways
/// Retrieves the list of ExternalVpnGateway available to the specified project.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_external_vpn_gateways_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_external_vpn_gateways_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_external_vpn_gateways_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ExternalVpnGatewayList>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ExternalVpnGatewayList = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/global/externalVpnGateways
/// Retrieves the list of ExternalVpnGateway available to the specified project.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_external_vpn_gateways_list_builder()` + `compute_external_vpn_gateways_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_external_vpn_gateways_list(
    client: &SimpleHttpClient,
    project: &str,
    filter: Option<&str>,
    maxResults: Option<i32>,
    orderBy: Option<&str>,
    pageToken: Option<&str>,
    returnPartialSuccess: Option<bool>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ExternalVpnGatewayList>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = compute_external_vpn_gateways_list_builder(
        client,
        project,
        filter,
        maxResults,
        orderBy,
        pageToken,
        returnPartialSuccess,
    )?;
    compute_external_vpn_gateways_list_execute(builder)
}

/// GET projects/{project}/global/externalVpnGateways/{resource}/setLabels
/// Sets the labels on an ExternalVpnGateway. To learn more about labels, read the Labeling Resources documentation.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_external_vpn_gateways_set_labels_execute()` to send, or `compute_external_vpn_gateways_set_labels` for simplest API.

pub fn compute_external_vpn_gateways_set_labels_builder(
    client: &SimpleHttpClient,
    project: &str,
    resource: &str,
    body: &GlobalSetLabelsRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/global/externalVpnGateways/{}/setLabels",
        project, resource,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET projects/{project}/global/externalVpnGateways/{resource}/setLabels
/// Sets the labels on an ExternalVpnGateway. To learn more about labels, read the Labeling Resources documentation.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_external_vpn_gateways_set_labels_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_external_vpn_gateways_set_labels_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_external_vpn_gateways_set_labels_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/global/externalVpnGateways/{resource}/setLabels
/// Sets the labels on an ExternalVpnGateway. To learn more about labels, read the Labeling Resources documentation.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_external_vpn_gateways_set_labels_builder()` + `compute_external_vpn_gateways_set_labels_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_external_vpn_gateways_set_labels(
    client: &SimpleHttpClient,
    project: &str,
    resource: &str,
    body: &GlobalSetLabelsRequest,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        compute_external_vpn_gateways_set_labels_builder(client, project, resource, body)?;
    compute_external_vpn_gateways_set_labels_execute(builder)
}

/// GET projects/{project}/global/externalVpnGateways/{resource}/testIamPermissions
/// Returns permissions that a caller has on the specified resource.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_external_vpn_gateways_test_iam_permissions_execute()` to send, or `compute_external_vpn_gateways_test_iam_permissions` for simplest API.

pub fn compute_external_vpn_gateways_test_iam_permissions_builder(
    client: &SimpleHttpClient,
    project: &str,
    resource: &str,
    body: &TestPermissionsRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/global/externalVpnGateways/{}/testIamPermissions",
        project, resource,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET projects/{project}/global/externalVpnGateways/{resource}/testIamPermissions
/// Returns permissions that a caller has on the specified resource.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_external_vpn_gateways_test_iam_permissions_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_external_vpn_gateways_test_iam_permissions_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_external_vpn_gateways_test_iam_permissions_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<TestPermissionsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: TestPermissionsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/global/externalVpnGateways/{resource}/testIamPermissions
/// Returns permissions that a caller has on the specified resource.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_external_vpn_gateways_test_iam_permissions_builder()` + `compute_external_vpn_gateways_test_iam_permissions_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_external_vpn_gateways_test_iam_permissions(
    client: &SimpleHttpClient,
    project: &str,
    resource: &str,
    body: &TestPermissionsRequest,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<TestPermissionsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = compute_external_vpn_gateways_test_iam_permissions_builder(
        client, project, resource, body,
    )?;
    compute_external_vpn_gateways_test_iam_permissions_execute(builder)
}

/// GET locations/global/firewallPolicies/{firewallPolicy}/addAssociation
/// Inserts an association for the specified firewall policy.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_firewall_policies_add_association_execute()` to send, or `compute_firewall_policies_add_association` for simplest API.

pub fn compute_firewall_policies_add_association_builder(
    client: &SimpleHttpClient,
    firewallPolicy: &str,
    replaceExistingAssociation: Option<bool>,
    requestId: Option<&str>,
    body: &FirewallPolicyAssociation,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comlocations/global/firewallPolicies/{}/addAssociation",
        firewallPolicy,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "replaceExistingAssociation",
            replaceExistingAssociation
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "requestId",
            requestId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET locations/global/firewallPolicies/{firewallPolicy}/addAssociation
/// Inserts an association for the specified firewall policy.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_firewall_policies_add_association_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_firewall_policies_add_association_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_firewall_policies_add_association_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET locations/global/firewallPolicies/{firewallPolicy}/addAssociation
/// Inserts an association for the specified firewall policy.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_firewall_policies_add_association_builder()` + `compute_firewall_policies_add_association_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_firewall_policies_add_association(
    client: &SimpleHttpClient,
    firewallPolicy: &str,
    replaceExistingAssociation: Option<bool>,
    requestId: Option<&str>,
    body: &FirewallPolicyAssociation,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_firewall_policies_add_association_builder(
        client,
        firewallPolicy,
        replaceExistingAssociation,
        requestId,
        body,
    )?;
    compute_firewall_policies_add_association_execute(builder)
}

/// GET locations/global/firewallPolicies/{firewallPolicy}/addRule
/// Inserts a rule into a firewall policy.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_firewall_policies_add_rule_execute()` to send, or `compute_firewall_policies_add_rule` for simplest API.

pub fn compute_firewall_policies_add_rule_builder(
    client: &SimpleHttpClient,
    firewallPolicy: &str,
    requestId: Option<&str>,
    body: &FirewallPolicyRule,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comlocations/global/firewallPolicies/{}/addRule",
        firewallPolicy,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "requestId",
            requestId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET locations/global/firewallPolicies/{firewallPolicy}/addRule
/// Inserts a rule into a firewall policy.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_firewall_policies_add_rule_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_firewall_policies_add_rule_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_firewall_policies_add_rule_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET locations/global/firewallPolicies/{firewallPolicy}/addRule
/// Inserts a rule into a firewall policy.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_firewall_policies_add_rule_builder()` + `compute_firewall_policies_add_rule_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_firewall_policies_add_rule(
    client: &SimpleHttpClient,
    firewallPolicy: &str,
    requestId: Option<&str>,
    body: &FirewallPolicyRule,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        compute_firewall_policies_add_rule_builder(client, firewallPolicy, requestId, body)?;
    compute_firewall_policies_add_rule_execute(builder)
}

/// GET locations/global/firewallPolicies/{firewallPolicy}/cloneRules
/// Copies rules to the specified firewall policy.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_firewall_policies_clone_rules_execute()` to send, or `compute_firewall_policies_clone_rules` for simplest API.

pub fn compute_firewall_policies_clone_rules_builder(
    client: &SimpleHttpClient,
    firewallPolicy: &str,
    requestId: Option<&str>,
    sourceFirewallPolicy: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comlocations/global/firewallPolicies/{}/cloneRules",
        firewallPolicy,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "requestId",
            requestId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "sourceFirewallPolicy",
            sourceFirewallPolicy
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET locations/global/firewallPolicies/{firewallPolicy}/cloneRules
/// Copies rules to the specified firewall policy.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_firewall_policies_clone_rules_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_firewall_policies_clone_rules_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_firewall_policies_clone_rules_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET locations/global/firewallPolicies/{firewallPolicy}/cloneRules
/// Copies rules to the specified firewall policy.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_firewall_policies_clone_rules_builder()` + `compute_firewall_policies_clone_rules_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_firewall_policies_clone_rules(
    client: &SimpleHttpClient,
    firewallPolicy: &str,
    requestId: Option<&str>,
    sourceFirewallPolicy: Option<&str>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_firewall_policies_clone_rules_builder(
        client,
        firewallPolicy,
        requestId,
        sourceFirewallPolicy,
    )?;
    compute_firewall_policies_clone_rules_execute(builder)
}

/// GET locations/global/firewallPolicies/{firewallPolicy}
/// Deletes the specified policy.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_firewall_policies_delete_execute()` to send, or `compute_firewall_policies_delete` for simplest API.

pub fn compute_firewall_policies_delete_builder(
    client: &SimpleHttpClient,
    firewallPolicy: &str,
    requestId: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comlocations/global/firewallPolicies/{}",
        firewallPolicy,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "requestId",
            requestId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET locations/global/firewallPolicies/{firewallPolicy}
/// Deletes the specified policy.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_firewall_policies_delete_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_firewall_policies_delete_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_firewall_policies_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET locations/global/firewallPolicies/{firewallPolicy}
/// Deletes the specified policy.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_firewall_policies_delete_builder()` + `compute_firewall_policies_delete_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_firewall_policies_delete(
    client: &SimpleHttpClient,
    firewallPolicy: &str,
    requestId: Option<&str>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_firewall_policies_delete_builder(client, firewallPolicy, requestId)?;
    compute_firewall_policies_delete_execute(builder)
}

/// GET locations/global/firewallPolicies/{firewallPolicy}
/// Returns the specified firewall policy.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_firewall_policies_get_execute()` to send, or `compute_firewall_policies_get` for simplest API.

pub fn compute_firewall_policies_get_builder(
    client: &SimpleHttpClient,
    firewallPolicy: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comlocations/global/firewallPolicies/{}",
        firewallPolicy,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET locations/global/firewallPolicies/{firewallPolicy}
/// Returns the specified firewall policy.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_firewall_policies_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_firewall_policies_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_firewall_policies_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<FirewallPolicy>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: FirewallPolicy = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET locations/global/firewallPolicies/{firewallPolicy}
/// Returns the specified firewall policy.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_firewall_policies_get_builder()` + `compute_firewall_policies_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_firewall_policies_get(
    client: &SimpleHttpClient,
    firewallPolicy: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<FirewallPolicy>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = compute_firewall_policies_get_builder(client, firewallPolicy)?;
    compute_firewall_policies_get_execute(builder)
}

/// GET locations/global/firewallPolicies/{firewallPolicy}/getAssociation
/// Gets an association with the specified name.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_firewall_policies_get_association_execute()` to send, or `compute_firewall_policies_get_association` for simplest API.

pub fn compute_firewall_policies_get_association_builder(
    client: &SimpleHttpClient,
    firewallPolicy: &str,
    name: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comlocations/global/firewallPolicies/{}/getAssociation",
        firewallPolicy,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "name",
            name.as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET locations/global/firewallPolicies/{firewallPolicy}/getAssociation
/// Gets an association with the specified name.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_firewall_policies_get_association_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_firewall_policies_get_association_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_firewall_policies_get_association_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<FirewallPolicyAssociation>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: FirewallPolicyAssociation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET locations/global/firewallPolicies/{firewallPolicy}/getAssociation
/// Gets an association with the specified name.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_firewall_policies_get_association_builder()` + `compute_firewall_policies_get_association_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_firewall_policies_get_association(
    client: &SimpleHttpClient,
    firewallPolicy: &str,
    name: Option<&str>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<FirewallPolicyAssociation>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = compute_firewall_policies_get_association_builder(client, firewallPolicy, name)?;
    compute_firewall_policies_get_association_execute(builder)
}

/// GET locations/global/firewallPolicies/{resource}/getIamPolicy
/// Gets the access control policy for a resource. May be empty if no such policy or resource exists.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_firewall_policies_get_iam_policy_execute()` to send, or `compute_firewall_policies_get_iam_policy` for simplest API.

pub fn compute_firewall_policies_get_iam_policy_builder(
    client: &SimpleHttpClient,
    resource: &str,
    optionsRequestedPolicyVersion: Option<i32>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comlocations/global/firewallPolicies/{}/getIamPolicy",
        resource,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "optionsRequestedPolicyVersion",
            optionsRequestedPolicyVersion
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET locations/global/firewallPolicies/{resource}/getIamPolicy
/// Gets the access control policy for a resource. May be empty if no such policy or resource exists.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_firewall_policies_get_iam_policy_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_firewall_policies_get_iam_policy_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_firewall_policies_get_iam_policy_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Policy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Policy = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET locations/global/firewallPolicies/{resource}/getIamPolicy
/// Gets the access control policy for a resource. May be empty if no such policy or resource exists.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_firewall_policies_get_iam_policy_builder()` + `compute_firewall_policies_get_iam_policy_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_firewall_policies_get_iam_policy(
    client: &SimpleHttpClient,
    resource: &str,
    optionsRequestedPolicyVersion: Option<i32>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Policy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_firewall_policies_get_iam_policy_builder(
        client,
        resource,
        optionsRequestedPolicyVersion,
    )?;
    compute_firewall_policies_get_iam_policy_execute(builder)
}

/// GET locations/global/firewallPolicies/{firewallPolicy}/getRule
/// Gets a rule of the specified priority.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_firewall_policies_get_rule_execute()` to send, or `compute_firewall_policies_get_rule` for simplest API.

pub fn compute_firewall_policies_get_rule_builder(
    client: &SimpleHttpClient,
    firewallPolicy: &str,
    priority: Option<i32>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comlocations/global/firewallPolicies/{}/getRule",
        firewallPolicy,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "priority",
            priority
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET locations/global/firewallPolicies/{firewallPolicy}/getRule
/// Gets a rule of the specified priority.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_firewall_policies_get_rule_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_firewall_policies_get_rule_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_firewall_policies_get_rule_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<FirewallPolicyRule>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: FirewallPolicyRule = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET locations/global/firewallPolicies/{firewallPolicy}/getRule
/// Gets a rule of the specified priority.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_firewall_policies_get_rule_builder()` + `compute_firewall_policies_get_rule_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_firewall_policies_get_rule(
    client: &SimpleHttpClient,
    firewallPolicy: &str,
    priority: Option<i32>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<FirewallPolicyRule>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = compute_firewall_policies_get_rule_builder(client, firewallPolicy, priority)?;
    compute_firewall_policies_get_rule_execute(builder)
}

/// GET locations/global/firewallPolicies
/// Creates a new policy in the specified project using the data included in the request.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_firewall_policies_insert_execute()` to send, or `compute_firewall_policies_insert` for simplest API.

pub fn compute_firewall_policies_insert_builder(
    client: &SimpleHttpClient,
    parentId: Option<&str>,
    requestId: Option<&str>,
    body: &FirewallPolicy,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comlocations/global/firewallPolicies",);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "parentId",
            parentId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "requestId",
            requestId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET locations/global/firewallPolicies
/// Creates a new policy in the specified project using the data included in the request.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_firewall_policies_insert_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_firewall_policies_insert_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_firewall_policies_insert_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET locations/global/firewallPolicies
/// Creates a new policy in the specified project using the data included in the request.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_firewall_policies_insert_builder()` + `compute_firewall_policies_insert_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_firewall_policies_insert(
    client: &SimpleHttpClient,
    parentId: Option<&str>,
    requestId: Option<&str>,
    body: &FirewallPolicy,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_firewall_policies_insert_builder(client, parentId, requestId, body)?;
    compute_firewall_policies_insert_execute(builder)
}

/// GET locations/global/firewallPolicies
/// Lists all the policies that have been configured for the specified folder or organization.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_firewall_policies_list_execute()` to send, or `compute_firewall_policies_list` for simplest API.

pub fn compute_firewall_policies_list_builder(
    client: &SimpleHttpClient,
    filter: Option<&str>,
    maxResults: Option<i32>,
    orderBy: Option<&str>,
    pageToken: Option<&str>,
    parentId: Option<&str>,
    returnPartialSuccess: Option<bool>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comlocations/global/firewallPolicies",);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "filter",
            filter
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "maxResults",
            maxResults
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "orderBy",
            orderBy
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageToken",
            pageToken
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "parentId",
            parentId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "returnPartialSuccess",
            returnPartialSuccess
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET locations/global/firewallPolicies
/// Lists all the policies that have been configured for the specified folder or organization.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_firewall_policies_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_firewall_policies_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_firewall_policies_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<FirewallPolicyList>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: FirewallPolicyList = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET locations/global/firewallPolicies
/// Lists all the policies that have been configured for the specified folder or organization.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_firewall_policies_list_builder()` + `compute_firewall_policies_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_firewall_policies_list(
    client: &SimpleHttpClient,
    filter: Option<&str>,
    maxResults: Option<i32>,
    orderBy: Option<&str>,
    pageToken: Option<&str>,
    parentId: Option<&str>,
    returnPartialSuccess: Option<bool>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<FirewallPolicyList>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = compute_firewall_policies_list_builder(
        client,
        filter,
        maxResults,
        orderBy,
        pageToken,
        parentId,
        returnPartialSuccess,
    )?;
    compute_firewall_policies_list_execute(builder)
}

/// GET locations/global/firewallPolicies/listAssociations
/// Lists associations of a specified target, i.e., organization or folder.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_firewall_policies_list_associations_execute()` to send, or `compute_firewall_policies_list_associations` for simplest API.

pub fn compute_firewall_policies_list_associations_builder(
    client: &SimpleHttpClient,
    includeInheritedPolicies: Option<bool>,
    targetResource: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comlocations/global/firewallPolicies/listAssociations",);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "includeInheritedPolicies",
            includeInheritedPolicies
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "targetResource",
            targetResource
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET locations/global/firewallPolicies/listAssociations
/// Lists associations of a specified target, i.e., organization or folder.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_firewall_policies_list_associations_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_firewall_policies_list_associations_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_firewall_policies_list_associations_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<FirewallPoliciesListAssociationsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: FirewallPoliciesListAssociationsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET locations/global/firewallPolicies/listAssociations
/// Lists associations of a specified target, i.e., organization or folder.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_firewall_policies_list_associations_builder()` + `compute_firewall_policies_list_associations_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_firewall_policies_list_associations(
    client: &SimpleHttpClient,
    includeInheritedPolicies: Option<bool>,
    targetResource: Option<&str>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<FirewallPoliciesListAssociationsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = compute_firewall_policies_list_associations_builder(
        client,
        includeInheritedPolicies,
        targetResource,
    )?;
    compute_firewall_policies_list_associations_execute(builder)
}

/// GET locations/global/firewallPolicies/{firewallPolicy}/move
/// Moves the specified firewall policy.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_firewall_policies_move_execute()` to send, or `compute_firewall_policies_move` for simplest API.

pub fn compute_firewall_policies_move_builder(
    client: &SimpleHttpClient,
    firewallPolicy: &str,
    parentId: Option<&str>,
    requestId: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comlocations/global/firewallPolicies/{}/move",
        firewallPolicy,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "parentId",
            parentId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "requestId",
            requestId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET locations/global/firewallPolicies/{firewallPolicy}/move
/// Moves the specified firewall policy.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_firewall_policies_move_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_firewall_policies_move_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_firewall_policies_move_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET locations/global/firewallPolicies/{firewallPolicy}/move
/// Moves the specified firewall policy.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_firewall_policies_move_builder()` + `compute_firewall_policies_move_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_firewall_policies_move(
    client: &SimpleHttpClient,
    firewallPolicy: &str,
    parentId: Option<&str>,
    requestId: Option<&str>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        compute_firewall_policies_move_builder(client, firewallPolicy, parentId, requestId)?;
    compute_firewall_policies_move_execute(builder)
}

/// GET locations/global/firewallPolicies/{firewallPolicy}
/// Patches the specified policy with the data included in the request.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_firewall_policies_patch_execute()` to send, or `compute_firewall_policies_patch` for simplest API.

pub fn compute_firewall_policies_patch_builder(
    client: &SimpleHttpClient,
    firewallPolicy: &str,
    requestId: Option<&str>,
    body: &FirewallPolicy,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comlocations/global/firewallPolicies/{}",
        firewallPolicy,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "requestId",
            requestId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET locations/global/firewallPolicies/{firewallPolicy}
/// Patches the specified policy with the data included in the request.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_firewall_policies_patch_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_firewall_policies_patch_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_firewall_policies_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET locations/global/firewallPolicies/{firewallPolicy}
/// Patches the specified policy with the data included in the request.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_firewall_policies_patch_builder()` + `compute_firewall_policies_patch_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_firewall_policies_patch(
    client: &SimpleHttpClient,
    firewallPolicy: &str,
    requestId: Option<&str>,
    body: &FirewallPolicy,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_firewall_policies_patch_builder(client, firewallPolicy, requestId, body)?;
    compute_firewall_policies_patch_execute(builder)
}

/// GET locations/global/firewallPolicies/{firewallPolicy}/patchRule
/// Patches a rule of the specified priority.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_firewall_policies_patch_rule_execute()` to send, or `compute_firewall_policies_patch_rule` for simplest API.

pub fn compute_firewall_policies_patch_rule_builder(
    client: &SimpleHttpClient,
    firewallPolicy: &str,
    priority: Option<i32>,
    requestId: Option<&str>,
    body: &FirewallPolicyRule,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comlocations/global/firewallPolicies/{}/patchRule",
        firewallPolicy,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "priority",
            priority
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "requestId",
            requestId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET locations/global/firewallPolicies/{firewallPolicy}/patchRule
/// Patches a rule of the specified priority.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_firewall_policies_patch_rule_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_firewall_policies_patch_rule_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_firewall_policies_patch_rule_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET locations/global/firewallPolicies/{firewallPolicy}/patchRule
/// Patches a rule of the specified priority.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_firewall_policies_patch_rule_builder()` + `compute_firewall_policies_patch_rule_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_firewall_policies_patch_rule(
    client: &SimpleHttpClient,
    firewallPolicy: &str,
    priority: Option<i32>,
    requestId: Option<&str>,
    body: &FirewallPolicyRule,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_firewall_policies_patch_rule_builder(
        client,
        firewallPolicy,
        priority,
        requestId,
        body,
    )?;
    compute_firewall_policies_patch_rule_execute(builder)
}

/// GET locations/global/firewallPolicies/{firewallPolicy}/removeAssociation
/// Removes an association for the specified firewall policy.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_firewall_policies_remove_association_execute()` to send, or `compute_firewall_policies_remove_association` for simplest API.

pub fn compute_firewall_policies_remove_association_builder(
    client: &SimpleHttpClient,
    firewallPolicy: &str,
    name: Option<&str>,
    requestId: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comlocations/global/firewallPolicies/{}/removeAssociation",
        firewallPolicy,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "name",
            name.as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "requestId",
            requestId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET locations/global/firewallPolicies/{firewallPolicy}/removeAssociation
/// Removes an association for the specified firewall policy.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_firewall_policies_remove_association_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_firewall_policies_remove_association_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_firewall_policies_remove_association_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET locations/global/firewallPolicies/{firewallPolicy}/removeAssociation
/// Removes an association for the specified firewall policy.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_firewall_policies_remove_association_builder()` + `compute_firewall_policies_remove_association_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_firewall_policies_remove_association(
    client: &SimpleHttpClient,
    firewallPolicy: &str,
    name: Option<&str>,
    requestId: Option<&str>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_firewall_policies_remove_association_builder(
        client,
        firewallPolicy,
        name,
        requestId,
    )?;
    compute_firewall_policies_remove_association_execute(builder)
}

/// GET locations/global/firewallPolicies/{firewallPolicy}/removeRule
/// Deletes a rule of the specified priority.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_firewall_policies_remove_rule_execute()` to send, or `compute_firewall_policies_remove_rule` for simplest API.

pub fn compute_firewall_policies_remove_rule_builder(
    client: &SimpleHttpClient,
    firewallPolicy: &str,
    priority: Option<i32>,
    requestId: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comlocations/global/firewallPolicies/{}/removeRule",
        firewallPolicy,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "priority",
            priority
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "requestId",
            requestId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET locations/global/firewallPolicies/{firewallPolicy}/removeRule
/// Deletes a rule of the specified priority.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_firewall_policies_remove_rule_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_firewall_policies_remove_rule_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_firewall_policies_remove_rule_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET locations/global/firewallPolicies/{firewallPolicy}/removeRule
/// Deletes a rule of the specified priority.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_firewall_policies_remove_rule_builder()` + `compute_firewall_policies_remove_rule_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_firewall_policies_remove_rule(
    client: &SimpleHttpClient,
    firewallPolicy: &str,
    priority: Option<i32>,
    requestId: Option<&str>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        compute_firewall_policies_remove_rule_builder(client, firewallPolicy, priority, requestId)?;
    compute_firewall_policies_remove_rule_execute(builder)
}

/// GET locations/global/firewallPolicies/{resource}/setIamPolicy
/// Sets the access control policy on the specified resource. Replaces any existing policy.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_firewall_policies_set_iam_policy_execute()` to send, or `compute_firewall_policies_set_iam_policy` for simplest API.

pub fn compute_firewall_policies_set_iam_policy_builder(
    client: &SimpleHttpClient,
    resource: &str,
    body: &GlobalOrganizationSetPolicyRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comlocations/global/firewallPolicies/{}/setIamPolicy",
        resource,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET locations/global/firewallPolicies/{resource}/setIamPolicy
/// Sets the access control policy on the specified resource. Replaces any existing policy.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_firewall_policies_set_iam_policy_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_firewall_policies_set_iam_policy_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_firewall_policies_set_iam_policy_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Policy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Policy = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET locations/global/firewallPolicies/{resource}/setIamPolicy
/// Sets the access control policy on the specified resource. Replaces any existing policy.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_firewall_policies_set_iam_policy_builder()` + `compute_firewall_policies_set_iam_policy_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_firewall_policies_set_iam_policy(
    client: &SimpleHttpClient,
    resource: &str,
    body: &GlobalOrganizationSetPolicyRequest,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Policy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_firewall_policies_set_iam_policy_builder(client, resource, body)?;
    compute_firewall_policies_set_iam_policy_execute(builder)
}

/// GET locations/global/firewallPolicies/{resource}/testIamPermissions
/// Returns permissions that a caller has on the specified resource.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_firewall_policies_test_iam_permissions_execute()` to send, or `compute_firewall_policies_test_iam_permissions` for simplest API.

pub fn compute_firewall_policies_test_iam_permissions_builder(
    client: &SimpleHttpClient,
    resource: &str,
    body: &TestPermissionsRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comlocations/global/firewallPolicies/{}/testIamPermissions",
        resource,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET locations/global/firewallPolicies/{resource}/testIamPermissions
/// Returns permissions that a caller has on the specified resource.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_firewall_policies_test_iam_permissions_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_firewall_policies_test_iam_permissions_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_firewall_policies_test_iam_permissions_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<TestPermissionsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: TestPermissionsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET locations/global/firewallPolicies/{resource}/testIamPermissions
/// Returns permissions that a caller has on the specified resource.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_firewall_policies_test_iam_permissions_builder()` + `compute_firewall_policies_test_iam_permissions_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_firewall_policies_test_iam_permissions(
    client: &SimpleHttpClient,
    resource: &str,
    body: &TestPermissionsRequest,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<TestPermissionsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = compute_firewall_policies_test_iam_permissions_builder(client, resource, body)?;
    compute_firewall_policies_test_iam_permissions_execute(builder)
}

/// GET projects/{project}/global/firewalls/{firewall}
/// Deletes the specified firewall.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_firewalls_delete_execute()` to send, or `compute_firewalls_delete` for simplest API.

pub fn compute_firewalls_delete_builder(
    client: &SimpleHttpClient,
    firewall: &str,
    project: &str,
    requestId: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/global/firewalls/{}",
        firewall, project,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "requestId",
            requestId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET projects/{project}/global/firewalls/{firewall}
/// Deletes the specified firewall.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_firewalls_delete_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_firewalls_delete_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_firewalls_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/global/firewalls/{firewall}
/// Deletes the specified firewall.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_firewalls_delete_builder()` + `compute_firewalls_delete_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_firewalls_delete(
    client: &SimpleHttpClient,
    firewall: &str,
    project: &str,
    requestId: Option<&str>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_firewalls_delete_builder(client, firewall, project, requestId)?;
    compute_firewalls_delete_execute(builder)
}

/// GET projects/{project}/global/firewalls/{firewall}
/// Returns the specified firewall.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_firewalls_get_execute()` to send, or `compute_firewalls_get` for simplest API.

pub fn compute_firewalls_get_builder(
    client: &SimpleHttpClient,
    firewall: &str,
    project: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/global/firewalls/{}",
        firewall, project,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/global/firewalls/{firewall}
/// Returns the specified firewall.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_firewalls_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_firewalls_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_firewalls_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Firewall>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Firewall = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/global/firewalls/{firewall}
/// Returns the specified firewall.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_firewalls_get_builder()` + `compute_firewalls_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_firewalls_get(
    client: &SimpleHttpClient,
    firewall: &str,
    project: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Firewall>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_firewalls_get_builder(client, firewall, project)?;
    compute_firewalls_get_execute(builder)
}

/// GET projects/{project}/global/firewalls
/// Creates a firewall rule in the specified project using the data included in the request.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_firewalls_insert_execute()` to send, or `compute_firewalls_insert` for simplest API.

pub fn compute_firewalls_insert_builder(
    client: &SimpleHttpClient,
    project: &str,
    requestId: Option<&str>,
    body: &Firewall,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/global/firewalls",
        project,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "requestId",
            requestId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET projects/{project}/global/firewalls
/// Creates a firewall rule in the specified project using the data included in the request.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_firewalls_insert_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_firewalls_insert_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_firewalls_insert_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/global/firewalls
/// Creates a firewall rule in the specified project using the data included in the request.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_firewalls_insert_builder()` + `compute_firewalls_insert_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_firewalls_insert(
    client: &SimpleHttpClient,
    project: &str,
    requestId: Option<&str>,
    body: &Firewall,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_firewalls_insert_builder(client, project, requestId, body)?;
    compute_firewalls_insert_execute(builder)
}

/// GET projects/{project}/global/firewalls
/// Retrieves the list of firewall rules available to the specified project.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_firewalls_list_execute()` to send, or `compute_firewalls_list` for simplest API.

pub fn compute_firewalls_list_builder(
    client: &SimpleHttpClient,
    project: &str,
    filter: Option<&str>,
    maxResults: Option<i32>,
    orderBy: Option<&str>,
    pageToken: Option<&str>,
    returnPartialSuccess: Option<bool>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/global/firewalls",
        project,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "filter",
            filter
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "maxResults",
            maxResults
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "orderBy",
            orderBy
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageToken",
            pageToken
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "returnPartialSuccess",
            returnPartialSuccess
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET projects/{project}/global/firewalls
/// Retrieves the list of firewall rules available to the specified project.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_firewalls_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_firewalls_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_firewalls_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<FirewallList>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: FirewallList = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/global/firewalls
/// Retrieves the list of firewall rules available to the specified project.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_firewalls_list_builder()` + `compute_firewalls_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_firewalls_list(
    client: &SimpleHttpClient,
    project: &str,
    filter: Option<&str>,
    maxResults: Option<i32>,
    orderBy: Option<&str>,
    pageToken: Option<&str>,
    returnPartialSuccess: Option<bool>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<FirewallList>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = compute_firewalls_list_builder(
        client,
        project,
        filter,
        maxResults,
        orderBy,
        pageToken,
        returnPartialSuccess,
    )?;
    compute_firewalls_list_execute(builder)
}

/// GET projects/{project}/global/firewalls/{firewall}
/// Updates the specified firewall rule with the data included in the request. This method `supportsPATCH` semantics and uses `theJSON` merge patch format and processing rules.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_firewalls_patch_execute()` to send, or `compute_firewalls_patch` for simplest API.

pub fn compute_firewalls_patch_builder(
    client: &SimpleHttpClient,
    firewall: &str,
    project: &str,
    requestId: Option<&str>,
    body: &Firewall,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/global/firewalls/{}",
        firewall, project,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "requestId",
            requestId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET projects/{project}/global/firewalls/{firewall}
/// Updates the specified firewall rule with the data included in the request. This method `supportsPATCH` semantics and uses `theJSON` merge patch format and processing rules.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_firewalls_patch_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_firewalls_patch_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_firewalls_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/global/firewalls/{firewall}
/// Updates the specified firewall rule with the data included in the request. This method `supportsPATCH` semantics and uses `theJSON` merge patch format and processing rules.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_firewalls_patch_builder()` + `compute_firewalls_patch_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_firewalls_patch(
    client: &SimpleHttpClient,
    firewall: &str,
    project: &str,
    requestId: Option<&str>,
    body: &Firewall,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_firewalls_patch_builder(client, firewall, project, requestId, body)?;
    compute_firewalls_patch_execute(builder)
}

/// GET projects/{project}/global/firewalls/{resource}/testIamPermissions
/// Returns permissions that a caller has on the specified resource.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_firewalls_test_iam_permissions_execute()` to send, or `compute_firewalls_test_iam_permissions` for simplest API.

pub fn compute_firewalls_test_iam_permissions_builder(
    client: &SimpleHttpClient,
    project: &str,
    resource: &str,
    body: &TestPermissionsRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/global/firewalls/{}/testIamPermissions",
        project, resource,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET projects/{project}/global/firewalls/{resource}/testIamPermissions
/// Returns permissions that a caller has on the specified resource.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_firewalls_test_iam_permissions_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_firewalls_test_iam_permissions_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_firewalls_test_iam_permissions_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<TestPermissionsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: TestPermissionsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/global/firewalls/{resource}/testIamPermissions
/// Returns permissions that a caller has on the specified resource.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_firewalls_test_iam_permissions_builder()` + `compute_firewalls_test_iam_permissions_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_firewalls_test_iam_permissions(
    client: &SimpleHttpClient,
    project: &str,
    resource: &str,
    body: &TestPermissionsRequest,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<TestPermissionsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = compute_firewalls_test_iam_permissions_builder(client, project, resource, body)?;
    compute_firewalls_test_iam_permissions_execute(builder)
}

/// GET projects/{project}/global/firewalls/{firewall}
/// Updates the specified firewall rule with the data included in the request. Note that all fields will be updated if using PUT, even fields that are not specified. To update individual fields, please use PATCH instead.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_firewalls_update_execute()` to send, or `compute_firewalls_update` for simplest API.

pub fn compute_firewalls_update_builder(
    client: &SimpleHttpClient,
    firewall: &str,
    project: &str,
    requestId: Option<&str>,
    body: &Firewall,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/global/firewalls/{}",
        firewall, project,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "requestId",
            requestId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET projects/{project}/global/firewalls/{firewall}
/// Updates the specified firewall rule with the data included in the request. Note that all fields will be updated if using PUT, even fields that are not specified. To update individual fields, please use PATCH instead.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_firewalls_update_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_firewalls_update_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_firewalls_update_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/global/firewalls/{firewall}
/// Updates the specified firewall rule with the data included in the request. Note that all fields will be updated if using PUT, even fields that are not specified. To update individual fields, please use PATCH instead.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_firewalls_update_builder()` + `compute_firewalls_update_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_firewalls_update(
    client: &SimpleHttpClient,
    firewall: &str,
    project: &str,
    requestId: Option<&str>,
    body: &Firewall,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_firewalls_update_builder(client, firewall, project, requestId, body)?;
    compute_firewalls_update_execute(builder)
}

/// GET projects/{project}/aggregated/forwardingRules
/// Retrieves an aggregated list of forwarding rules.  To prevent failure, it is recommended that you set the `returnPartialSuccess` parameter to `true`.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_forwarding_rules_aggregated_list_execute()` to send, or `compute_forwarding_rules_aggregated_list` for simplest API.

pub fn compute_forwarding_rules_aggregated_list_builder(
    client: &SimpleHttpClient,
    project: &str,
    filter: Option<&str>,
    includeAllScopes: Option<bool>,
    maxResults: Option<i32>,
    orderBy: Option<&str>,
    pageToken: Option<&str>,
    returnPartialSuccess: Option<bool>,
    serviceProjectNumber: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/aggregated/forwardingRules",
        project,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "filter",
            filter
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "includeAllScopes",
            includeAllScopes
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "maxResults",
            maxResults
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "orderBy",
            orderBy
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageToken",
            pageToken
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "returnPartialSuccess",
            returnPartialSuccess
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "serviceProjectNumber",
            serviceProjectNumber
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET projects/{project}/aggregated/forwardingRules
/// Retrieves an aggregated list of forwarding rules.  To prevent failure, it is recommended that you set the `returnPartialSuccess` parameter to `true`.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_forwarding_rules_aggregated_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_forwarding_rules_aggregated_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_forwarding_rules_aggregated_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ForwardingRuleAggregatedList>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ForwardingRuleAggregatedList = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/aggregated/forwardingRules
/// Retrieves an aggregated list of forwarding rules.  To prevent failure, it is recommended that you set the `returnPartialSuccess` parameter to `true`.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_forwarding_rules_aggregated_list_builder()` + `compute_forwarding_rules_aggregated_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_forwarding_rules_aggregated_list(
    client: &SimpleHttpClient,
    project: &str,
    filter: Option<&str>,
    includeAllScopes: Option<bool>,
    maxResults: Option<i32>,
    orderBy: Option<&str>,
    pageToken: Option<&str>,
    returnPartialSuccess: Option<bool>,
    serviceProjectNumber: Option<&str>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ForwardingRuleAggregatedList>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = compute_forwarding_rules_aggregated_list_builder(
        client,
        project,
        filter,
        includeAllScopes,
        maxResults,
        orderBy,
        pageToken,
        returnPartialSuccess,
        serviceProjectNumber,
    )?;
    compute_forwarding_rules_aggregated_list_execute(builder)
}

/// GET projects/{project}/regions/{region}/forwardingRules/{forwardingRule}
/// Deletes the specified ForwardingRule resource.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_forwarding_rules_delete_execute()` to send, or `compute_forwarding_rules_delete` for simplest API.

pub fn compute_forwarding_rules_delete_builder(
    client: &SimpleHttpClient,
    forwardingRule: &str,
    project: &str,
    region: &str,
    requestId: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/regions/{}/forwardingRules/{}",
        forwardingRule, project, region,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "requestId",
            requestId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET projects/{project}/regions/{region}/forwardingRules/{forwardingRule}
/// Deletes the specified ForwardingRule resource.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_forwarding_rules_delete_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_forwarding_rules_delete_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_forwarding_rules_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/regions/{region}/forwardingRules/{forwardingRule}
/// Deletes the specified ForwardingRule resource.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_forwarding_rules_delete_builder()` + `compute_forwarding_rules_delete_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_forwarding_rules_delete(
    client: &SimpleHttpClient,
    forwardingRule: &str,
    project: &str,
    region: &str,
    requestId: Option<&str>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_forwarding_rules_delete_builder(
        client,
        forwardingRule,
        project,
        region,
        requestId,
    )?;
    compute_forwarding_rules_delete_execute(builder)
}

/// GET projects/{project}/regions/{region}/forwardingRules/{forwardingRule}
/// Returns the specified ForwardingRule resource.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_forwarding_rules_get_execute()` to send, or `compute_forwarding_rules_get` for simplest API.

pub fn compute_forwarding_rules_get_builder(
    client: &SimpleHttpClient,
    forwardingRule: &str,
    project: &str,
    region: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/regions/{}/forwardingRules/{}",
        forwardingRule, project, region,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/regions/{region}/forwardingRules/{forwardingRule}
/// Returns the specified ForwardingRule resource.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_forwarding_rules_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_forwarding_rules_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_forwarding_rules_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ForwardingRule>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ForwardingRule = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/regions/{region}/forwardingRules/{forwardingRule}
/// Returns the specified ForwardingRule resource.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_forwarding_rules_get_builder()` + `compute_forwarding_rules_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_forwarding_rules_get(
    client: &SimpleHttpClient,
    forwardingRule: &str,
    project: &str,
    region: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ForwardingRule>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = compute_forwarding_rules_get_builder(client, forwardingRule, project, region)?;
    compute_forwarding_rules_get_execute(builder)
}

/// GET projects/{project}/regions/{region}/forwardingRules
/// Creates a ForwardingRule resource in the specified project and region using the data included in the request.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_forwarding_rules_insert_execute()` to send, or `compute_forwarding_rules_insert` for simplest API.

pub fn compute_forwarding_rules_insert_builder(
    client: &SimpleHttpClient,
    project: &str,
    region: &str,
    requestId: Option<&str>,
    body: &ForwardingRule,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/regions/{}/forwardingRules",
        project, region,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "requestId",
            requestId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET projects/{project}/regions/{region}/forwardingRules
/// Creates a ForwardingRule resource in the specified project and region using the data included in the request.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_forwarding_rules_insert_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_forwarding_rules_insert_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_forwarding_rules_insert_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/regions/{region}/forwardingRules
/// Creates a ForwardingRule resource in the specified project and region using the data included in the request.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_forwarding_rules_insert_builder()` + `compute_forwarding_rules_insert_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_forwarding_rules_insert(
    client: &SimpleHttpClient,
    project: &str,
    region: &str,
    requestId: Option<&str>,
    body: &ForwardingRule,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        compute_forwarding_rules_insert_builder(client, project, region, requestId, body)?;
    compute_forwarding_rules_insert_execute(builder)
}

/// GET projects/{project}/regions/{region}/forwardingRules
/// Retrieves a list of ForwardingRule resources available to the specified project and region.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_forwarding_rules_list_execute()` to send, or `compute_forwarding_rules_list` for simplest API.

pub fn compute_forwarding_rules_list_builder(
    client: &SimpleHttpClient,
    project: &str,
    region: &str,
    filter: Option<&str>,
    maxResults: Option<i32>,
    orderBy: Option<&str>,
    pageToken: Option<&str>,
    returnPartialSuccess: Option<bool>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/regions/{}/forwardingRules",
        project, region,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "filter",
            filter
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "maxResults",
            maxResults
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "orderBy",
            orderBy
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageToken",
            pageToken
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "returnPartialSuccess",
            returnPartialSuccess
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET projects/{project}/regions/{region}/forwardingRules
/// Retrieves a list of ForwardingRule resources available to the specified project and region.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_forwarding_rules_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_forwarding_rules_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_forwarding_rules_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ForwardingRuleList>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ForwardingRuleList = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/regions/{region}/forwardingRules
/// Retrieves a list of ForwardingRule resources available to the specified project and region.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_forwarding_rules_list_builder()` + `compute_forwarding_rules_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_forwarding_rules_list(
    client: &SimpleHttpClient,
    project: &str,
    region: &str,
    filter: Option<&str>,
    maxResults: Option<i32>,
    orderBy: Option<&str>,
    pageToken: Option<&str>,
    returnPartialSuccess: Option<bool>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ForwardingRuleList>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = compute_forwarding_rules_list_builder(
        client,
        project,
        region,
        filter,
        maxResults,
        orderBy,
        pageToken,
        returnPartialSuccess,
    )?;
    compute_forwarding_rules_list_execute(builder)
}

/// GET projects/{project}/regions/{region}/forwardingRules/{forwardingRule}
/// Updates the specified forwarding rule with the data included in the request. This method `supportsPATCH` semantics and uses `theJSON` merge patch format and processing rules. Currently, you can only patch the network_tier field.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_forwarding_rules_patch_execute()` to send, or `compute_forwarding_rules_patch` for simplest API.

pub fn compute_forwarding_rules_patch_builder(
    client: &SimpleHttpClient,
    forwardingRule: &str,
    project: &str,
    region: &str,
    requestId: Option<&str>,
    body: &ForwardingRule,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/regions/{}/forwardingRules/{}",
        forwardingRule, project, region,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "requestId",
            requestId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET projects/{project}/regions/{region}/forwardingRules/{forwardingRule}
/// Updates the specified forwarding rule with the data included in the request. This method `supportsPATCH` semantics and uses `theJSON` merge patch format and processing rules. Currently, you can only patch the network_tier field.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_forwarding_rules_patch_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_forwarding_rules_patch_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_forwarding_rules_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/regions/{region}/forwardingRules/{forwardingRule}
/// Updates the specified forwarding rule with the data included in the request. This method `supportsPATCH` semantics and uses `theJSON` merge patch format and processing rules. Currently, you can only patch the network_tier field.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_forwarding_rules_patch_builder()` + `compute_forwarding_rules_patch_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_forwarding_rules_patch(
    client: &SimpleHttpClient,
    forwardingRule: &str,
    project: &str,
    region: &str,
    requestId: Option<&str>,
    body: &ForwardingRule,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_forwarding_rules_patch_builder(
        client,
        forwardingRule,
        project,
        region,
        requestId,
        body,
    )?;
    compute_forwarding_rules_patch_execute(builder)
}

/// GET projects/{project}/regions/{region}/forwardingRules/{resource}/setLabels
/// Sets the labels on the specified resource. To learn more about labels, read the Labeling Resources documentation.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_forwarding_rules_set_labels_execute()` to send, or `compute_forwarding_rules_set_labels` for simplest API.

pub fn compute_forwarding_rules_set_labels_builder(
    client: &SimpleHttpClient,
    project: &str,
    region: &str,
    resource: &str,
    requestId: Option<&str>,
    body: &RegionSetLabelsRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/regions/{}/forwardingRules/{}/setLabels",
        project, region, resource,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "requestId",
            requestId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET projects/{project}/regions/{region}/forwardingRules/{resource}/setLabels
/// Sets the labels on the specified resource. To learn more about labels, read the Labeling Resources documentation.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_forwarding_rules_set_labels_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_forwarding_rules_set_labels_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_forwarding_rules_set_labels_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/regions/{region}/forwardingRules/{resource}/setLabels
/// Sets the labels on the specified resource. To learn more about labels, read the Labeling Resources documentation.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_forwarding_rules_set_labels_builder()` + `compute_forwarding_rules_set_labels_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_forwarding_rules_set_labels(
    client: &SimpleHttpClient,
    project: &str,
    region: &str,
    resource: &str,
    requestId: Option<&str>,
    body: &RegionSetLabelsRequest,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_forwarding_rules_set_labels_builder(
        client, project, region, resource, requestId, body,
    )?;
    compute_forwarding_rules_set_labels_execute(builder)
}

/// GET projects/{project}/regions/{region}/forwardingRules/{forwardingRule}/setTarget
/// Changes target URL for forwarding rule. The new target should be of the same type as the old target.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_forwarding_rules_set_target_execute()` to send, or `compute_forwarding_rules_set_target` for simplest API.

pub fn compute_forwarding_rules_set_target_builder(
    client: &SimpleHttpClient,
    forwardingRule: &str,
    project: &str,
    region: &str,
    requestId: Option<&str>,
    body: &TargetReference,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/regions/{}/forwardingRules/{}/setTarget",
        forwardingRule, project, region,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "requestId",
            requestId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET projects/{project}/regions/{region}/forwardingRules/{forwardingRule}/setTarget
/// Changes target URL for forwarding rule. The new target should be of the same type as the old target.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_forwarding_rules_set_target_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_forwarding_rules_set_target_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_forwarding_rules_set_target_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/regions/{region}/forwardingRules/{forwardingRule}/setTarget
/// Changes target URL for forwarding rule. The new target should be of the same type as the old target.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_forwarding_rules_set_target_builder()` + `compute_forwarding_rules_set_target_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_forwarding_rules_set_target(
    client: &SimpleHttpClient,
    forwardingRule: &str,
    project: &str,
    region: &str,
    requestId: Option<&str>,
    body: &TargetReference,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_forwarding_rules_set_target_builder(
        client,
        forwardingRule,
        project,
        region,
        requestId,
        body,
    )?;
    compute_forwarding_rules_set_target_execute(builder)
}

/// GET projects/{project}/aggregated/futureReservations
/// Retrieves an aggregated list of future reservations.  To prevent failure, it is recommended that you set the `returnPartialSuccess` parameter to `true`.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_future_reservations_aggregated_list_execute()` to send, or `compute_future_reservations_aggregated_list` for simplest API.

pub fn compute_future_reservations_aggregated_list_builder(
    client: &SimpleHttpClient,
    project: &str,
    filter: Option<&str>,
    includeAllScopes: Option<bool>,
    maxResults: Option<i32>,
    orderBy: Option<&str>,
    pageToken: Option<&str>,
    returnPartialSuccess: Option<bool>,
    serviceProjectNumber: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/aggregated/futureReservations",
        project,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "filter",
            filter
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "includeAllScopes",
            includeAllScopes
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "maxResults",
            maxResults
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "orderBy",
            orderBy
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageToken",
            pageToken
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "returnPartialSuccess",
            returnPartialSuccess
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "serviceProjectNumber",
            serviceProjectNumber
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET projects/{project}/aggregated/futureReservations
/// Retrieves an aggregated list of future reservations.  To prevent failure, it is recommended that you set the `returnPartialSuccess` parameter to `true`.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_future_reservations_aggregated_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_future_reservations_aggregated_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_future_reservations_aggregated_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<FutureReservationsAggregatedListResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: FutureReservationsAggregatedListResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/aggregated/futureReservations
/// Retrieves an aggregated list of future reservations.  To prevent failure, it is recommended that you set the `returnPartialSuccess` parameter to `true`.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_future_reservations_aggregated_list_builder()` + `compute_future_reservations_aggregated_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_future_reservations_aggregated_list(
    client: &SimpleHttpClient,
    project: &str,
    filter: Option<&str>,
    includeAllScopes: Option<bool>,
    maxResults: Option<i32>,
    orderBy: Option<&str>,
    pageToken: Option<&str>,
    returnPartialSuccess: Option<bool>,
    serviceProjectNumber: Option<&str>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<FutureReservationsAggregatedListResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = compute_future_reservations_aggregated_list_builder(
        client,
        project,
        filter,
        includeAllScopes,
        maxResults,
        orderBy,
        pageToken,
        returnPartialSuccess,
        serviceProjectNumber,
    )?;
    compute_future_reservations_aggregated_list_execute(builder)
}

/// GET projects/{project}/zones/{zone}/futureReservations/{futureReservation}/cancel
/// Cancel the specified future reservation.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_future_reservations_cancel_execute()` to send, or `compute_future_reservations_cancel` for simplest API.

pub fn compute_future_reservations_cancel_builder(
    client: &SimpleHttpClient,
    futureReservation: &str,
    project: &str,
    zone: &str,
    requestId: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/zones/{}/futureReservations/{}/cancel",
        futureReservation, project, zone,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "requestId",
            requestId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET projects/{project}/zones/{zone}/futureReservations/{futureReservation}/cancel
/// Cancel the specified future reservation.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_future_reservations_cancel_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_future_reservations_cancel_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_future_reservations_cancel_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/zones/{zone}/futureReservations/{futureReservation}/cancel
/// Cancel the specified future reservation.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_future_reservations_cancel_builder()` + `compute_future_reservations_cancel_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_future_reservations_cancel(
    client: &SimpleHttpClient,
    futureReservation: &str,
    project: &str,
    zone: &str,
    requestId: Option<&str>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_future_reservations_cancel_builder(
        client,
        futureReservation,
        project,
        zone,
        requestId,
    )?;
    compute_future_reservations_cancel_execute(builder)
}

/// GET projects/{project}/zones/{zone}/futureReservations/{futureReservation}
/// Deletes the specified future reservation.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_future_reservations_delete_execute()` to send, or `compute_future_reservations_delete` for simplest API.

pub fn compute_future_reservations_delete_builder(
    client: &SimpleHttpClient,
    futureReservation: &str,
    project: &str,
    zone: &str,
    requestId: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/zones/{}/futureReservations/{}",
        futureReservation, project, zone,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "requestId",
            requestId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET projects/{project}/zones/{zone}/futureReservations/{futureReservation}
/// Deletes the specified future reservation.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_future_reservations_delete_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_future_reservations_delete_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_future_reservations_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/zones/{zone}/futureReservations/{futureReservation}
/// Deletes the specified future reservation.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_future_reservations_delete_builder()` + `compute_future_reservations_delete_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_future_reservations_delete(
    client: &SimpleHttpClient,
    futureReservation: &str,
    project: &str,
    zone: &str,
    requestId: Option<&str>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_future_reservations_delete_builder(
        client,
        futureReservation,
        project,
        zone,
        requestId,
    )?;
    compute_future_reservations_delete_execute(builder)
}

/// GET projects/{project}/zones/{zone}/futureReservations/{futureReservation}
/// Retrieves information about the specified future reservation.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_future_reservations_get_execute()` to send, or `compute_future_reservations_get` for simplest API.

pub fn compute_future_reservations_get_builder(
    client: &SimpleHttpClient,
    futureReservation: &str,
    project: &str,
    zone: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/zones/{}/futureReservations/{}",
        futureReservation, project, zone,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/zones/{zone}/futureReservations/{futureReservation}
/// Retrieves information about the specified future reservation.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_future_reservations_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_future_reservations_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_future_reservations_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<FutureReservation>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: FutureReservation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/zones/{zone}/futureReservations/{futureReservation}
/// Retrieves information about the specified future reservation.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_future_reservations_get_builder()` + `compute_future_reservations_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_future_reservations_get(
    client: &SimpleHttpClient,
    futureReservation: &str,
    project: &str,
    zone: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<FutureReservation>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        compute_future_reservations_get_builder(client, futureReservation, project, zone)?;
    compute_future_reservations_get_execute(builder)
}

/// GET projects/{project}/zones/{zone}/futureReservations
/// Creates a new Future Reservation.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_future_reservations_insert_execute()` to send, or `compute_future_reservations_insert` for simplest API.

pub fn compute_future_reservations_insert_builder(
    client: &SimpleHttpClient,
    project: &str,
    zone: &str,
    requestId: Option<&str>,
    body: &FutureReservation,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/zones/{}/futureReservations",
        project, zone,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "requestId",
            requestId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET projects/{project}/zones/{zone}/futureReservations
/// Creates a new Future Reservation.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_future_reservations_insert_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_future_reservations_insert_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_future_reservations_insert_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/zones/{zone}/futureReservations
/// Creates a new Future Reservation.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_future_reservations_insert_builder()` + `compute_future_reservations_insert_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_future_reservations_insert(
    client: &SimpleHttpClient,
    project: &str,
    zone: &str,
    requestId: Option<&str>,
    body: &FutureReservation,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        compute_future_reservations_insert_builder(client, project, zone, requestId, body)?;
    compute_future_reservations_insert_execute(builder)
}

/// GET projects/{project}/zones/{zone}/futureReservations
/// A list of all the future reservations that have been configured for the specified project in specified zone.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_future_reservations_list_execute()` to send, or `compute_future_reservations_list` for simplest API.

pub fn compute_future_reservations_list_builder(
    client: &SimpleHttpClient,
    project: &str,
    zone: &str,
    filter: Option<&str>,
    maxResults: Option<i32>,
    orderBy: Option<&str>,
    pageToken: Option<&str>,
    returnPartialSuccess: Option<bool>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/zones/{}/futureReservations",
        project, zone,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "filter",
            filter
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "maxResults",
            maxResults
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "orderBy",
            orderBy
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageToken",
            pageToken
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "returnPartialSuccess",
            returnPartialSuccess
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET projects/{project}/zones/{zone}/futureReservations
/// A list of all the future reservations that have been configured for the specified project in specified zone.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_future_reservations_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_future_reservations_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_future_reservations_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<FutureReservationsListResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: FutureReservationsListResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/zones/{zone}/futureReservations
/// A list of all the future reservations that have been configured for the specified project in specified zone.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_future_reservations_list_builder()` + `compute_future_reservations_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_future_reservations_list(
    client: &SimpleHttpClient,
    project: &str,
    zone: &str,
    filter: Option<&str>,
    maxResults: Option<i32>,
    orderBy: Option<&str>,
    pageToken: Option<&str>,
    returnPartialSuccess: Option<bool>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<FutureReservationsListResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = compute_future_reservations_list_builder(
        client,
        project,
        zone,
        filter,
        maxResults,
        orderBy,
        pageToken,
        returnPartialSuccess,
    )?;
    compute_future_reservations_list_execute(builder)
}

/// GET projects/{project}/zones/{zone}/futureReservations/{futureReservation}
/// Updates the specified future reservation.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_future_reservations_update_execute()` to send, or `compute_future_reservations_update` for simplest API.

pub fn compute_future_reservations_update_builder(
    client: &SimpleHttpClient,
    futureReservation: &str,
    project: &str,
    zone: &str,
    requestId: Option<&str>,
    updateMask: Option<&str>,
    body: &FutureReservation,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/zones/{}/futureReservations/{}",
        futureReservation, project, zone,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "requestId",
            requestId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "updateMask",
            updateMask
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET projects/{project}/zones/{zone}/futureReservations/{futureReservation}
/// Updates the specified future reservation.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_future_reservations_update_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_future_reservations_update_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_future_reservations_update_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/zones/{zone}/futureReservations/{futureReservation}
/// Updates the specified future reservation.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_future_reservations_update_builder()` + `compute_future_reservations_update_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_future_reservations_update(
    client: &SimpleHttpClient,
    futureReservation: &str,
    project: &str,
    zone: &str,
    requestId: Option<&str>,
    updateMask: Option<&str>,
    body: &FutureReservation,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_future_reservations_update_builder(
        client,
        futureReservation,
        project,
        zone,
        requestId,
        updateMask,
        body,
    )?;
    compute_future_reservations_update_execute(builder)
}

/// GET projects/{project}/global/addresses/{address}
/// Deletes the specified address resource.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_global_addresses_delete_execute()` to send, or `compute_global_addresses_delete` for simplest API.

pub fn compute_global_addresses_delete_builder(
    client: &SimpleHttpClient,
    address: &str,
    project: &str,
    requestId: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/global/addresses/{}",
        address, project,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "requestId",
            requestId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET projects/{project}/global/addresses/{address}
/// Deletes the specified address resource.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_global_addresses_delete_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_global_addresses_delete_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_global_addresses_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/global/addresses/{address}
/// Deletes the specified address resource.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_global_addresses_delete_builder()` + `compute_global_addresses_delete_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_global_addresses_delete(
    client: &SimpleHttpClient,
    address: &str,
    project: &str,
    requestId: Option<&str>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_global_addresses_delete_builder(client, address, project, requestId)?;
    compute_global_addresses_delete_execute(builder)
}

/// GET projects/{project}/global/addresses/{address}
/// Returns the specified address resource.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_global_addresses_get_execute()` to send, or `compute_global_addresses_get` for simplest API.

pub fn compute_global_addresses_get_builder(
    client: &SimpleHttpClient,
    address: &str,
    project: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/global/addresses/{}",
        address, project,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/global/addresses/{address}
/// Returns the specified address resource.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_global_addresses_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_global_addresses_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_global_addresses_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Address>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Address = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/global/addresses/{address}
/// Returns the specified address resource.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_global_addresses_get_builder()` + `compute_global_addresses_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_global_addresses_get(
    client: &SimpleHttpClient,
    address: &str,
    project: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Address>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_global_addresses_get_builder(client, address, project)?;
    compute_global_addresses_get_execute(builder)
}

/// GET projects/{project}/global/addresses
/// Creates an address resource in the specified project by using the data included in the request.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_global_addresses_insert_execute()` to send, or `compute_global_addresses_insert` for simplest API.

pub fn compute_global_addresses_insert_builder(
    client: &SimpleHttpClient,
    project: &str,
    requestId: Option<&str>,
    body: &Address,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/global/addresses",
        project,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "requestId",
            requestId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET projects/{project}/global/addresses
/// Creates an address resource in the specified project by using the data included in the request.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_global_addresses_insert_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_global_addresses_insert_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_global_addresses_insert_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/global/addresses
/// Creates an address resource in the specified project by using the data included in the request.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_global_addresses_insert_builder()` + `compute_global_addresses_insert_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_global_addresses_insert(
    client: &SimpleHttpClient,
    project: &str,
    requestId: Option<&str>,
    body: &Address,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_global_addresses_insert_builder(client, project, requestId, body)?;
    compute_global_addresses_insert_execute(builder)
}

/// GET projects/{project}/global/addresses
/// Retrieves a list of global addresses.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_global_addresses_list_execute()` to send, or `compute_global_addresses_list` for simplest API.

pub fn compute_global_addresses_list_builder(
    client: &SimpleHttpClient,
    project: &str,
    filter: Option<&str>,
    maxResults: Option<i32>,
    orderBy: Option<&str>,
    pageToken: Option<&str>,
    returnPartialSuccess: Option<bool>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/global/addresses",
        project,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "filter",
            filter
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "maxResults",
            maxResults
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "orderBy",
            orderBy
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageToken",
            pageToken
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "returnPartialSuccess",
            returnPartialSuccess
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET projects/{project}/global/addresses
/// Retrieves a list of global addresses.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_global_addresses_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_global_addresses_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_global_addresses_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<AddressList>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: AddressList = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/global/addresses
/// Retrieves a list of global addresses.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_global_addresses_list_builder()` + `compute_global_addresses_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_global_addresses_list(
    client: &SimpleHttpClient,
    project: &str,
    filter: Option<&str>,
    maxResults: Option<i32>,
    orderBy: Option<&str>,
    pageToken: Option<&str>,
    returnPartialSuccess: Option<bool>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<AddressList>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_global_addresses_list_builder(
        client,
        project,
        filter,
        maxResults,
        orderBy,
        pageToken,
        returnPartialSuccess,
    )?;
    compute_global_addresses_list_execute(builder)
}

/// GET projects/{project}/global/addresses/{address}/move
/// Moves the specified address resource from one project to another project.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_global_addresses_move_execute()` to send, or `compute_global_addresses_move` for simplest API.

pub fn compute_global_addresses_move_builder(
    client: &SimpleHttpClient,
    address: &str,
    project: &str,
    requestId: Option<&str>,
    body: &GlobalAddressesMoveRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/global/addresses/{}/move",
        address, project,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "requestId",
            requestId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET projects/{project}/global/addresses/{address}/move
/// Moves the specified address resource from one project to another project.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_global_addresses_move_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_global_addresses_move_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_global_addresses_move_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/global/addresses/{address}/move
/// Moves the specified address resource from one project to another project.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_global_addresses_move_builder()` + `compute_global_addresses_move_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_global_addresses_move(
    client: &SimpleHttpClient,
    address: &str,
    project: &str,
    requestId: Option<&str>,
    body: &GlobalAddressesMoveRequest,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_global_addresses_move_builder(client, address, project, requestId, body)?;
    compute_global_addresses_move_execute(builder)
}

/// GET projects/{project}/global/addresses/{resource}/setLabels
/// Sets the labels on a GlobalAddress. To learn more about labels, read `theLabeling` Resources documentation.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_global_addresses_set_labels_execute()` to send, or `compute_global_addresses_set_labels` for simplest API.

pub fn compute_global_addresses_set_labels_builder(
    client: &SimpleHttpClient,
    project: &str,
    resource: &str,
    body: &GlobalSetLabelsRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/global/addresses/{}/setLabels",
        project, resource,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET projects/{project}/global/addresses/{resource}/setLabels
/// Sets the labels on a GlobalAddress. To learn more about labels, read `theLabeling` Resources documentation.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_global_addresses_set_labels_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_global_addresses_set_labels_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_global_addresses_set_labels_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/global/addresses/{resource}/setLabels
/// Sets the labels on a GlobalAddress. To learn more about labels, read `theLabeling` Resources documentation.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_global_addresses_set_labels_builder()` + `compute_global_addresses_set_labels_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_global_addresses_set_labels(
    client: &SimpleHttpClient,
    project: &str,
    resource: &str,
    body: &GlobalSetLabelsRequest,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_global_addresses_set_labels_builder(client, project, resource, body)?;
    compute_global_addresses_set_labels_execute(builder)
}

/// GET projects/{project}/global/addresses/{resource}/testIamPermissions
/// Returns permissions that a caller has on the specified resource.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_global_addresses_test_iam_permissions_execute()` to send, or `compute_global_addresses_test_iam_permissions` for simplest API.

pub fn compute_global_addresses_test_iam_permissions_builder(
    client: &SimpleHttpClient,
    project: &str,
    resource: &str,
    body: &TestPermissionsRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/global/addresses/{}/testIamPermissions",
        project, resource,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET projects/{project}/global/addresses/{resource}/testIamPermissions
/// Returns permissions that a caller has on the specified resource.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_global_addresses_test_iam_permissions_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_global_addresses_test_iam_permissions_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_global_addresses_test_iam_permissions_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<TestPermissionsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: TestPermissionsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/global/addresses/{resource}/testIamPermissions
/// Returns permissions that a caller has on the specified resource.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_global_addresses_test_iam_permissions_builder()` + `compute_global_addresses_test_iam_permissions_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_global_addresses_test_iam_permissions(
    client: &SimpleHttpClient,
    project: &str,
    resource: &str,
    body: &TestPermissionsRequest,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<TestPermissionsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        compute_global_addresses_test_iam_permissions_builder(client, project, resource, body)?;
    compute_global_addresses_test_iam_permissions_execute(builder)
}

/// GET projects/{project}/global/forwardingRules/{forwardingRule}
/// Deletes the specified GlobalForwardingRule resource.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_global_forwarding_rules_delete_execute()` to send, or `compute_global_forwarding_rules_delete` for simplest API.

pub fn compute_global_forwarding_rules_delete_builder(
    client: &SimpleHttpClient,
    forwardingRule: &str,
    project: &str,
    requestId: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/global/forwardingRules/{}",
        forwardingRule, project,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "requestId",
            requestId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET projects/{project}/global/forwardingRules/{forwardingRule}
/// Deletes the specified GlobalForwardingRule resource.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_global_forwarding_rules_delete_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_global_forwarding_rules_delete_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_global_forwarding_rules_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/global/forwardingRules/{forwardingRule}
/// Deletes the specified GlobalForwardingRule resource.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_global_forwarding_rules_delete_builder()` + `compute_global_forwarding_rules_delete_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_global_forwarding_rules_delete(
    client: &SimpleHttpClient,
    forwardingRule: &str,
    project: &str,
    requestId: Option<&str>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        compute_global_forwarding_rules_delete_builder(client, forwardingRule, project, requestId)?;
    compute_global_forwarding_rules_delete_execute(builder)
}

/// GET projects/{project}/global/forwardingRules/{forwardingRule}
/// Returns the specified GlobalForwardingRule resource. Gets a list of available forwarding rules by making a list() request.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_global_forwarding_rules_get_execute()` to send, or `compute_global_forwarding_rules_get` for simplest API.

pub fn compute_global_forwarding_rules_get_builder(
    client: &SimpleHttpClient,
    forwardingRule: &str,
    project: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/global/forwardingRules/{}",
        forwardingRule, project,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/global/forwardingRules/{forwardingRule}
/// Returns the specified GlobalForwardingRule resource. Gets a list of available forwarding rules by making a list() request.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_global_forwarding_rules_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_global_forwarding_rules_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_global_forwarding_rules_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ForwardingRule>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ForwardingRule = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/global/forwardingRules/{forwardingRule}
/// Returns the specified GlobalForwardingRule resource. Gets a list of available forwarding rules by making a list() request.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_global_forwarding_rules_get_builder()` + `compute_global_forwarding_rules_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_global_forwarding_rules_get(
    client: &SimpleHttpClient,
    forwardingRule: &str,
    project: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ForwardingRule>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = compute_global_forwarding_rules_get_builder(client, forwardingRule, project)?;
    compute_global_forwarding_rules_get_execute(builder)
}

/// GET projects/{project}/global/forwardingRules
/// Creates a GlobalForwardingRule resource in the specified project using the data included in the request.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_global_forwarding_rules_insert_execute()` to send, or `compute_global_forwarding_rules_insert` for simplest API.

pub fn compute_global_forwarding_rules_insert_builder(
    client: &SimpleHttpClient,
    project: &str,
    requestId: Option<&str>,
    body: &ForwardingRule,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/global/forwardingRules",
        project,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "requestId",
            requestId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET projects/{project}/global/forwardingRules
/// Creates a GlobalForwardingRule resource in the specified project using the data included in the request.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_global_forwarding_rules_insert_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_global_forwarding_rules_insert_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_global_forwarding_rules_insert_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/global/forwardingRules
/// Creates a GlobalForwardingRule resource in the specified project using the data included in the request.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_global_forwarding_rules_insert_builder()` + `compute_global_forwarding_rules_insert_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_global_forwarding_rules_insert(
    client: &SimpleHttpClient,
    project: &str,
    requestId: Option<&str>,
    body: &ForwardingRule,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_global_forwarding_rules_insert_builder(client, project, requestId, body)?;
    compute_global_forwarding_rules_insert_execute(builder)
}

/// GET projects/{project}/global/forwardingRules
/// Retrieves a list of GlobalForwardingRule resources available to the specified project.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_global_forwarding_rules_list_execute()` to send, or `compute_global_forwarding_rules_list` for simplest API.

pub fn compute_global_forwarding_rules_list_builder(
    client: &SimpleHttpClient,
    project: &str,
    filter: Option<&str>,
    maxResults: Option<i32>,
    orderBy: Option<&str>,
    pageToken: Option<&str>,
    returnPartialSuccess: Option<bool>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/global/forwardingRules",
        project,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "filter",
            filter
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "maxResults",
            maxResults
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "orderBy",
            orderBy
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageToken",
            pageToken
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "returnPartialSuccess",
            returnPartialSuccess
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET projects/{project}/global/forwardingRules
/// Retrieves a list of GlobalForwardingRule resources available to the specified project.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_global_forwarding_rules_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_global_forwarding_rules_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_global_forwarding_rules_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ForwardingRuleList>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ForwardingRuleList = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/global/forwardingRules
/// Retrieves a list of GlobalForwardingRule resources available to the specified project.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_global_forwarding_rules_list_builder()` + `compute_global_forwarding_rules_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_global_forwarding_rules_list(
    client: &SimpleHttpClient,
    project: &str,
    filter: Option<&str>,
    maxResults: Option<i32>,
    orderBy: Option<&str>,
    pageToken: Option<&str>,
    returnPartialSuccess: Option<bool>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ForwardingRuleList>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = compute_global_forwarding_rules_list_builder(
        client,
        project,
        filter,
        maxResults,
        orderBy,
        pageToken,
        returnPartialSuccess,
    )?;
    compute_global_forwarding_rules_list_execute(builder)
}

/// GET projects/{project}/global/forwardingRules/{forwardingRule}
/// Updates the specified forwarding rule with the data included in the request. This method `supportsPATCH` semantics and uses `theJSON` merge patch format and processing rules. Currently, you can only patch the network_tier field.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_global_forwarding_rules_patch_execute()` to send, or `compute_global_forwarding_rules_patch` for simplest API.

pub fn compute_global_forwarding_rules_patch_builder(
    client: &SimpleHttpClient,
    forwardingRule: &str,
    project: &str,
    requestId: Option<&str>,
    body: &ForwardingRule,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/global/forwardingRules/{}",
        forwardingRule, project,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "requestId",
            requestId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET projects/{project}/global/forwardingRules/{forwardingRule}
/// Updates the specified forwarding rule with the data included in the request. This method `supportsPATCH` semantics and uses `theJSON` merge patch format and processing rules. Currently, you can only patch the network_tier field.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_global_forwarding_rules_patch_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_global_forwarding_rules_patch_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_global_forwarding_rules_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/global/forwardingRules/{forwardingRule}
/// Updates the specified forwarding rule with the data included in the request. This method `supportsPATCH` semantics and uses `theJSON` merge patch format and processing rules. Currently, you can only patch the network_tier field.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_global_forwarding_rules_patch_builder()` + `compute_global_forwarding_rules_patch_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_global_forwarding_rules_patch(
    client: &SimpleHttpClient,
    forwardingRule: &str,
    project: &str,
    requestId: Option<&str>,
    body: &ForwardingRule,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_global_forwarding_rules_patch_builder(
        client,
        forwardingRule,
        project,
        requestId,
        body,
    )?;
    compute_global_forwarding_rules_patch_execute(builder)
}

/// GET projects/{project}/global/forwardingRules/{resource}/setLabels
/// Sets the labels on the specified resource. To learn more about labels, read the Labeling resources documentation.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_global_forwarding_rules_set_labels_execute()` to send, or `compute_global_forwarding_rules_set_labels` for simplest API.

pub fn compute_global_forwarding_rules_set_labels_builder(
    client: &SimpleHttpClient,
    project: &str,
    resource: &str,
    body: &GlobalSetLabelsRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/global/forwardingRules/{}/setLabels",
        project, resource,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET projects/{project}/global/forwardingRules/{resource}/setLabels
/// Sets the labels on the specified resource. To learn more about labels, read the Labeling resources documentation.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_global_forwarding_rules_set_labels_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_global_forwarding_rules_set_labels_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_global_forwarding_rules_set_labels_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/global/forwardingRules/{resource}/setLabels
/// Sets the labels on the specified resource. To learn more about labels, read the Labeling resources documentation.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_global_forwarding_rules_set_labels_builder()` + `compute_global_forwarding_rules_set_labels_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_global_forwarding_rules_set_labels(
    client: &SimpleHttpClient,
    project: &str,
    resource: &str,
    body: &GlobalSetLabelsRequest,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        compute_global_forwarding_rules_set_labels_builder(client, project, resource, body)?;
    compute_global_forwarding_rules_set_labels_execute(builder)
}

/// GET projects/{project}/global/forwardingRules/{forwardingRule}/setTarget
/// Changes target URL for the GlobalForwardingRule resource. The new target should be of the same type as the old target.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_global_forwarding_rules_set_target_execute()` to send, or `compute_global_forwarding_rules_set_target` for simplest API.

pub fn compute_global_forwarding_rules_set_target_builder(
    client: &SimpleHttpClient,
    forwardingRule: &str,
    project: &str,
    requestId: Option<&str>,
    body: &TargetReference,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/global/forwardingRules/{}/setTarget",
        forwardingRule, project,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "requestId",
            requestId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET projects/{project}/global/forwardingRules/{forwardingRule}/setTarget
/// Changes target URL for the GlobalForwardingRule resource. The new target should be of the same type as the old target.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_global_forwarding_rules_set_target_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_global_forwarding_rules_set_target_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_global_forwarding_rules_set_target_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/global/forwardingRules/{forwardingRule}/setTarget
/// Changes target URL for the GlobalForwardingRule resource. The new target should be of the same type as the old target.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_global_forwarding_rules_set_target_builder()` + `compute_global_forwarding_rules_set_target_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_global_forwarding_rules_set_target(
    client: &SimpleHttpClient,
    forwardingRule: &str,
    project: &str,
    requestId: Option<&str>,
    body: &TargetReference,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_global_forwarding_rules_set_target_builder(
        client,
        forwardingRule,
        project,
        requestId,
        body,
    )?;
    compute_global_forwarding_rules_set_target_execute(builder)
}

/// GET projects/{project}/global/networkEndpointGroups/{networkEndpointGroup}/attachNetworkEndpoints
/// Attach a network endpoint to the specified network endpoint group.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_global_network_endpoint_groups_attach_network_endpoints_execute()` to send, or `compute_global_network_endpoint_groups_attach_network_endpoints` for simplest API.

pub fn compute_global_network_endpoint_groups_attach_network_endpoints_builder(
    client: &SimpleHttpClient,
    networkEndpointGroup: &str,
    project: &str,
    requestId: Option<&str>,
    body: &GlobalNetworkEndpointGroupsAttachEndpointsRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/global/networkEndpointGroups/{}/attachNetworkEndpoints",
        networkEndpointGroup, project,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "requestId",
            requestId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET projects/{project}/global/networkEndpointGroups/{networkEndpointGroup}/attachNetworkEndpoints
/// Attach a network endpoint to the specified network endpoint group.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_global_network_endpoint_groups_attach_network_endpoints_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_global_network_endpoint_groups_attach_network_endpoints_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_global_network_endpoint_groups_attach_network_endpoints_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/global/networkEndpointGroups/{networkEndpointGroup}/attachNetworkEndpoints
/// Attach a network endpoint to the specified network endpoint group.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_global_network_endpoint_groups_attach_network_endpoints_builder()` + `compute_global_network_endpoint_groups_attach_network_endpoints_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_global_network_endpoint_groups_attach_network_endpoints(
    client: &SimpleHttpClient,
    networkEndpointGroup: &str,
    project: &str,
    requestId: Option<&str>,
    body: &GlobalNetworkEndpointGroupsAttachEndpointsRequest,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_global_network_endpoint_groups_attach_network_endpoints_builder(
        client,
        networkEndpointGroup,
        project,
        requestId,
        body,
    )?;
    compute_global_network_endpoint_groups_attach_network_endpoints_execute(builder)
}

/// GET projects/{project}/global/networkEndpointGroups/{networkEndpointGroup}
/// Deletes the specified network endpoint group.Note that the NEG cannot be deleted if there are backend services referencing it.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_global_network_endpoint_groups_delete_execute()` to send, or `compute_global_network_endpoint_groups_delete` for simplest API.

pub fn compute_global_network_endpoint_groups_delete_builder(
    client: &SimpleHttpClient,
    networkEndpointGroup: &str,
    project: &str,
    requestId: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/global/networkEndpointGroups/{}",
        networkEndpointGroup, project,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "requestId",
            requestId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET projects/{project}/global/networkEndpointGroups/{networkEndpointGroup}
/// Deletes the specified network endpoint group.Note that the NEG cannot be deleted if there are backend services referencing it.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_global_network_endpoint_groups_delete_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_global_network_endpoint_groups_delete_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_global_network_endpoint_groups_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/global/networkEndpointGroups/{networkEndpointGroup}
/// Deletes the specified network endpoint group.Note that the NEG cannot be deleted if there are backend services referencing it.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_global_network_endpoint_groups_delete_builder()` + `compute_global_network_endpoint_groups_delete_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_global_network_endpoint_groups_delete(
    client: &SimpleHttpClient,
    networkEndpointGroup: &str,
    project: &str,
    requestId: Option<&str>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_global_network_endpoint_groups_delete_builder(
        client,
        networkEndpointGroup,
        project,
        requestId,
    )?;
    compute_global_network_endpoint_groups_delete_execute(builder)
}

/// GET projects/{project}/global/networkEndpointGroups/{networkEndpointGroup}/detachNetworkEndpoints
/// Detach the network endpoint from the specified network endpoint group.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_global_network_endpoint_groups_detach_network_endpoints_execute()` to send, or `compute_global_network_endpoint_groups_detach_network_endpoints` for simplest API.

pub fn compute_global_network_endpoint_groups_detach_network_endpoints_builder(
    client: &SimpleHttpClient,
    networkEndpointGroup: &str,
    project: &str,
    requestId: Option<&str>,
    body: &GlobalNetworkEndpointGroupsDetachEndpointsRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/global/networkEndpointGroups/{}/detachNetworkEndpoints",
        networkEndpointGroup, project,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "requestId",
            requestId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET projects/{project}/global/networkEndpointGroups/{networkEndpointGroup}/detachNetworkEndpoints
/// Detach the network endpoint from the specified network endpoint group.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_global_network_endpoint_groups_detach_network_endpoints_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_global_network_endpoint_groups_detach_network_endpoints_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_global_network_endpoint_groups_detach_network_endpoints_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/global/networkEndpointGroups/{networkEndpointGroup}/detachNetworkEndpoints
/// Detach the network endpoint from the specified network endpoint group.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_global_network_endpoint_groups_detach_network_endpoints_builder()` + `compute_global_network_endpoint_groups_detach_network_endpoints_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_global_network_endpoint_groups_detach_network_endpoints(
    client: &SimpleHttpClient,
    networkEndpointGroup: &str,
    project: &str,
    requestId: Option<&str>,
    body: &GlobalNetworkEndpointGroupsDetachEndpointsRequest,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_global_network_endpoint_groups_detach_network_endpoints_builder(
        client,
        networkEndpointGroup,
        project,
        requestId,
        body,
    )?;
    compute_global_network_endpoint_groups_detach_network_endpoints_execute(builder)
}

/// GET projects/{project}/global/networkEndpointGroups/{networkEndpointGroup}
/// Returns the specified network endpoint group.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_global_network_endpoint_groups_get_execute()` to send, or `compute_global_network_endpoint_groups_get` for simplest API.

pub fn compute_global_network_endpoint_groups_get_builder(
    client: &SimpleHttpClient,
    networkEndpointGroup: &str,
    project: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/global/networkEndpointGroups/{}",
        networkEndpointGroup, project,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/global/networkEndpointGroups/{networkEndpointGroup}
/// Returns the specified network endpoint group.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_global_network_endpoint_groups_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_global_network_endpoint_groups_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_global_network_endpoint_groups_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<NetworkEndpointGroup>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: NetworkEndpointGroup = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/global/networkEndpointGroups/{networkEndpointGroup}
/// Returns the specified network endpoint group.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_global_network_endpoint_groups_get_builder()` + `compute_global_network_endpoint_groups_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_global_network_endpoint_groups_get(
    client: &SimpleHttpClient,
    networkEndpointGroup: &str,
    project: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<NetworkEndpointGroup>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        compute_global_network_endpoint_groups_get_builder(client, networkEndpointGroup, project)?;
    compute_global_network_endpoint_groups_get_execute(builder)
}

/// GET projects/{project}/global/networkEndpointGroups
/// Creates a network endpoint group in the specified project using the parameters that are included in the request.  Note: Use the following APIs to manage network endpoint groups:  - To manage NEGs with zonal scope (such as zonal NEGs, hybrid connectivity NEGs): zonal API - To manage NEGs with regional scope (such as regional internet NEGs, serverless NEGs, Private Service Connect NEGs): regional API - To manage NEGs with global scope (such as global internet NEGs):global API
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_global_network_endpoint_groups_insert_execute()` to send, or `compute_global_network_endpoint_groups_insert` for simplest API.

pub fn compute_global_network_endpoint_groups_insert_builder(
    client: &SimpleHttpClient,
    project: &str,
    requestId: Option<&str>,
    body: &NetworkEndpointGroup,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/global/networkEndpointGroups",
        project,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "requestId",
            requestId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET projects/{project}/global/networkEndpointGroups
/// Creates a network endpoint group in the specified project using the parameters that are included in the request.  Note: Use the following APIs to manage network endpoint groups:  - To manage NEGs with zonal scope (such as zonal NEGs, hybrid connectivity NEGs): zonal API - To manage NEGs with regional scope (such as regional internet NEGs, serverless NEGs, Private Service Connect NEGs): regional API - To manage NEGs with global scope (such as global internet NEGs):global API
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_global_network_endpoint_groups_insert_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_global_network_endpoint_groups_insert_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_global_network_endpoint_groups_insert_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/global/networkEndpointGroups
/// Creates a network endpoint group in the specified project using the parameters that are included in the request.  Note: Use the following APIs to manage network endpoint groups:  - To manage NEGs with zonal scope (such as zonal NEGs, hybrid connectivity NEGs): zonal API - To manage NEGs with regional scope (such as regional internet NEGs, serverless NEGs, Private Service Connect NEGs): regional API - To manage NEGs with global scope (such as global internet NEGs):global API
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_global_network_endpoint_groups_insert_builder()` + `compute_global_network_endpoint_groups_insert_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_global_network_endpoint_groups_insert(
    client: &SimpleHttpClient,
    project: &str,
    requestId: Option<&str>,
    body: &NetworkEndpointGroup,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        compute_global_network_endpoint_groups_insert_builder(client, project, requestId, body)?;
    compute_global_network_endpoint_groups_insert_execute(builder)
}

/// GET projects/{project}/global/networkEndpointGroups
/// Retrieves the list of network endpoint groups that are located in the specified project.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_global_network_endpoint_groups_list_execute()` to send, or `compute_global_network_endpoint_groups_list` for simplest API.

pub fn compute_global_network_endpoint_groups_list_builder(
    client: &SimpleHttpClient,
    project: &str,
    filter: Option<&str>,
    maxResults: Option<i32>,
    orderBy: Option<&str>,
    pageToken: Option<&str>,
    returnPartialSuccess: Option<bool>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/global/networkEndpointGroups",
        project,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "filter",
            filter
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "maxResults",
            maxResults
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "orderBy",
            orderBy
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageToken",
            pageToken
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "returnPartialSuccess",
            returnPartialSuccess
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET projects/{project}/global/networkEndpointGroups
/// Retrieves the list of network endpoint groups that are located in the specified project.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_global_network_endpoint_groups_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_global_network_endpoint_groups_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_global_network_endpoint_groups_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<NetworkEndpointGroupList>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: NetworkEndpointGroupList = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/global/networkEndpointGroups
/// Retrieves the list of network endpoint groups that are located in the specified project.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_global_network_endpoint_groups_list_builder()` + `compute_global_network_endpoint_groups_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_global_network_endpoint_groups_list(
    client: &SimpleHttpClient,
    project: &str,
    filter: Option<&str>,
    maxResults: Option<i32>,
    orderBy: Option<&str>,
    pageToken: Option<&str>,
    returnPartialSuccess: Option<bool>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<NetworkEndpointGroupList>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = compute_global_network_endpoint_groups_list_builder(
        client,
        project,
        filter,
        maxResults,
        orderBy,
        pageToken,
        returnPartialSuccess,
    )?;
    compute_global_network_endpoint_groups_list_execute(builder)
}

/// GET projects/{project}/global/networkEndpointGroups/{networkEndpointGroup}/listNetworkEndpoints
/// Lists the network endpoints in the specified network endpoint group.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_global_network_endpoint_groups_list_network_endpoints_execute()` to send, or `compute_global_network_endpoint_groups_list_network_endpoints` for simplest API.

pub fn compute_global_network_endpoint_groups_list_network_endpoints_builder(
    client: &SimpleHttpClient,
    networkEndpointGroup: &str,
    project: &str,
    filter: Option<&str>,
    maxResults: Option<i32>,
    orderBy: Option<&str>,
    pageToken: Option<&str>,
    returnPartialSuccess: Option<bool>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/global/networkEndpointGroups/{}/listNetworkEndpoints",
        networkEndpointGroup, project,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "filter",
            filter
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "maxResults",
            maxResults
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "orderBy",
            orderBy
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageToken",
            pageToken
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "returnPartialSuccess",
            returnPartialSuccess
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET projects/{project}/global/networkEndpointGroups/{networkEndpointGroup}/listNetworkEndpoints
/// Lists the network endpoints in the specified network endpoint group.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_global_network_endpoint_groups_list_network_endpoints_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_global_network_endpoint_groups_list_network_endpoints_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_global_network_endpoint_groups_list_network_endpoints_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<NetworkEndpointGroupsListNetworkEndpoints>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: NetworkEndpointGroupsListNetworkEndpoints = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/global/networkEndpointGroups/{networkEndpointGroup}/listNetworkEndpoints
/// Lists the network endpoints in the specified network endpoint group.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_global_network_endpoint_groups_list_network_endpoints_builder()` + `compute_global_network_endpoint_groups_list_network_endpoints_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_global_network_endpoint_groups_list_network_endpoints(
    client: &SimpleHttpClient,
    networkEndpointGroup: &str,
    project: &str,
    filter: Option<&str>,
    maxResults: Option<i32>,
    orderBy: Option<&str>,
    pageToken: Option<&str>,
    returnPartialSuccess: Option<bool>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<NetworkEndpointGroupsListNetworkEndpoints>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = compute_global_network_endpoint_groups_list_network_endpoints_builder(
        client,
        networkEndpointGroup,
        project,
        filter,
        maxResults,
        orderBy,
        pageToken,
        returnPartialSuccess,
    )?;
    compute_global_network_endpoint_groups_list_network_endpoints_execute(builder)
}

/// GET projects/{project}/aggregated/operations
/// Retrieves an aggregated list of all operations.  To prevent failure, Google recommends that you set the `returnPartialSuccess` parameter to `true`.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_global_operations_aggregated_list_execute()` to send, or `compute_global_operations_aggregated_list` for simplest API.

pub fn compute_global_operations_aggregated_list_builder(
    client: &SimpleHttpClient,
    project: &str,
    filter: Option<&str>,
    includeAllScopes: Option<bool>,
    maxResults: Option<i32>,
    orderBy: Option<&str>,
    pageToken: Option<&str>,
    returnPartialSuccess: Option<bool>,
    serviceProjectNumber: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/aggregated/operations",
        project,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "filter",
            filter
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "includeAllScopes",
            includeAllScopes
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "maxResults",
            maxResults
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "orderBy",
            orderBy
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageToken",
            pageToken
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "returnPartialSuccess",
            returnPartialSuccess
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "serviceProjectNumber",
            serviceProjectNumber
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET projects/{project}/aggregated/operations
/// Retrieves an aggregated list of all operations.  To prevent failure, Google recommends that you set the `returnPartialSuccess` parameter to `true`.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_global_operations_aggregated_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_global_operations_aggregated_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_global_operations_aggregated_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<OperationAggregatedList>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: OperationAggregatedList = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/aggregated/operations
/// Retrieves an aggregated list of all operations.  To prevent failure, Google recommends that you set the `returnPartialSuccess` parameter to `true`.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_global_operations_aggregated_list_builder()` + `compute_global_operations_aggregated_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_global_operations_aggregated_list(
    client: &SimpleHttpClient,
    project: &str,
    filter: Option<&str>,
    includeAllScopes: Option<bool>,
    maxResults: Option<i32>,
    orderBy: Option<&str>,
    pageToken: Option<&str>,
    returnPartialSuccess: Option<bool>,
    serviceProjectNumber: Option<&str>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<OperationAggregatedList>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = compute_global_operations_aggregated_list_builder(
        client,
        project,
        filter,
        includeAllScopes,
        maxResults,
        orderBy,
        pageToken,
        returnPartialSuccess,
        serviceProjectNumber,
    )?;
    compute_global_operations_aggregated_list_execute(builder)
}

/// GET projects/{project}/global/operations/{operation}
/// Deletes the specified Operations resource.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_global_operations_delete_execute()` to send, or `compute_global_operations_delete` for simplest API.

pub fn compute_global_operations_delete_builder(
    client: &SimpleHttpClient,
    operation: &str,
    project: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/global/operations/{}",
        operation, project,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/global/operations/{operation}
/// Deletes the specified Operations resource.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_global_operations_delete_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_global_operations_delete_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_global_operations_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/global/operations/{operation}
/// Deletes the specified Operations resource.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_global_operations_delete_builder()` + `compute_global_operations_delete_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_global_operations_delete(
    client: &SimpleHttpClient,
    operation: &str,
    project: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_global_operations_delete_builder(client, operation, project)?;
    compute_global_operations_delete_execute(builder)
}

/// GET projects/{project}/global/operations/{operation}
/// Retrieves the specified Operations resource.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_global_operations_get_execute()` to send, or `compute_global_operations_get` for simplest API.

pub fn compute_global_operations_get_builder(
    client: &SimpleHttpClient,
    operation: &str,
    project: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/global/operations/{}",
        operation, project,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/global/operations/{operation}
/// Retrieves the specified Operations resource.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_global_operations_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_global_operations_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_global_operations_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/global/operations/{operation}
/// Retrieves the specified Operations resource.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_global_operations_get_builder()` + `compute_global_operations_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_global_operations_get(
    client: &SimpleHttpClient,
    operation: &str,
    project: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_global_operations_get_builder(client, operation, project)?;
    compute_global_operations_get_execute(builder)
}

/// GET projects/{project}/global/operations
/// Retrieves a list of Operation resources contained within the specified project.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_global_operations_list_execute()` to send, or `compute_global_operations_list` for simplest API.

pub fn compute_global_operations_list_builder(
    client: &SimpleHttpClient,
    project: &str,
    filter: Option<&str>,
    maxResults: Option<i32>,
    orderBy: Option<&str>,
    pageToken: Option<&str>,
    returnPartialSuccess: Option<bool>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/global/operations",
        project,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "filter",
            filter
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "maxResults",
            maxResults
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "orderBy",
            orderBy
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageToken",
            pageToken
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "returnPartialSuccess",
            returnPartialSuccess
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET projects/{project}/global/operations
/// Retrieves a list of Operation resources contained within the specified project.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_global_operations_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_global_operations_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_global_operations_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<OperationList>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: OperationList = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/global/operations
/// Retrieves a list of Operation resources contained within the specified project.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_global_operations_list_builder()` + `compute_global_operations_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_global_operations_list(
    client: &SimpleHttpClient,
    project: &str,
    filter: Option<&str>,
    maxResults: Option<i32>,
    orderBy: Option<&str>,
    pageToken: Option<&str>,
    returnPartialSuccess: Option<bool>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<OperationList>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = compute_global_operations_list_builder(
        client,
        project,
        filter,
        maxResults,
        orderBy,
        pageToken,
        returnPartialSuccess,
    )?;
    compute_global_operations_list_execute(builder)
}

/// GET projects/{project}/global/operations/{operation}/wait
/// Waits for the specified Operation resource to return as DONE or for the request to approach the 2 minute deadline, and retrieves the specified Operation resource. This method differs from the GET method in that it waits for no more than the default deadline (2 minutes) and then returns the current state of the operation, which might be DONE or still in progress.  This method is called on a best-effort basis. Specifically:   - In uncommon cases, when the server is overloaded, the request might return before the default deadline is reached, or might return after zero seconds. - If the default deadline is reached, there is no guarantee that the operation is actually done when the method returns. Be prepared to retry if the operation is not DONE.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_global_operations_wait_execute()` to send, or `compute_global_operations_wait` for simplest API.

pub fn compute_global_operations_wait_builder(
    client: &SimpleHttpClient,
    operation: &str,
    project: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/global/operations/{}/wait",
        operation, project,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/global/operations/{operation}/wait
/// Waits for the specified Operation resource to return as DONE or for the request to approach the 2 minute deadline, and retrieves the specified Operation resource. This method differs from the GET method in that it waits for no more than the default deadline (2 minutes) and then returns the current state of the operation, which might be DONE or still in progress.  This method is called on a best-effort basis. Specifically:   - In uncommon cases, when the server is overloaded, the request might return before the default deadline is reached, or might return after zero seconds. - If the default deadline is reached, there is no guarantee that the operation is actually done when the method returns. Be prepared to retry if the operation is not DONE.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_global_operations_wait_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_global_operations_wait_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_global_operations_wait_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/global/operations/{operation}/wait
/// Waits for the specified Operation resource to return as DONE or for the request to approach the 2 minute deadline, and retrieves the specified Operation resource. This method differs from the GET method in that it waits for no more than the default deadline (2 minutes) and then returns the current state of the operation, which might be DONE or still in progress.  This method is called on a best-effort basis. Specifically:   - In uncommon cases, when the server is overloaded, the request might return before the default deadline is reached, or might return after zero seconds. - If the default deadline is reached, there is no guarantee that the operation is actually done when the method returns. Be prepared to retry if the operation is not DONE.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_global_operations_wait_builder()` + `compute_global_operations_wait_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_global_operations_wait(
    client: &SimpleHttpClient,
    operation: &str,
    project: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_global_operations_wait_builder(client, operation, project)?;
    compute_global_operations_wait_execute(builder)
}

/// GET locations/global/operations/{operation}
/// Deletes the specified Operations resource.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_global_organization_operations_delete_execute()` to send, or `compute_global_organization_operations_delete` for simplest API.

pub fn compute_global_organization_operations_delete_builder(
    client: &SimpleHttpClient,
    operation: &str,
    parentId: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comlocations/global/operations/{}",
        operation,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "parentId",
            parentId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET locations/global/operations/{operation}
/// Deletes the specified Operations resource.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_global_organization_operations_delete_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_global_organization_operations_delete_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_global_organization_operations_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET locations/global/operations/{operation}
/// Deletes the specified Operations resource.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_global_organization_operations_delete_builder()` + `compute_global_organization_operations_delete_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_global_organization_operations_delete(
    client: &SimpleHttpClient,
    operation: &str,
    parentId: Option<&str>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        compute_global_organization_operations_delete_builder(client, operation, parentId)?;
    compute_global_organization_operations_delete_execute(builder)
}

/// GET locations/global/operations/{operation}
/// Retrieves the specified Operations resource. Gets a list of operations by making a list() request.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_global_organization_operations_get_execute()` to send, or `compute_global_organization_operations_get` for simplest API.

pub fn compute_global_organization_operations_get_builder(
    client: &SimpleHttpClient,
    operation: &str,
    parentId: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comlocations/global/operations/{}",
        operation,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "parentId",
            parentId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET locations/global/operations/{operation}
/// Retrieves the specified Operations resource. Gets a list of operations by making a list() request.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_global_organization_operations_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_global_organization_operations_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_global_organization_operations_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET locations/global/operations/{operation}
/// Retrieves the specified Operations resource. Gets a list of operations by making a list() request.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_global_organization_operations_get_builder()` + `compute_global_organization_operations_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_global_organization_operations_get(
    client: &SimpleHttpClient,
    operation: &str,
    parentId: Option<&str>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_global_organization_operations_get_builder(client, operation, parentId)?;
    compute_global_organization_operations_get_execute(builder)
}

/// GET locations/global/operations
/// Retrieves a list of Operation resources contained within the specified organization.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_global_organization_operations_list_execute()` to send, or `compute_global_organization_operations_list` for simplest API.

pub fn compute_global_organization_operations_list_builder(
    client: &SimpleHttpClient,
    filter: Option<&str>,
    maxResults: Option<i32>,
    orderBy: Option<&str>,
    pageToken: Option<&str>,
    parentId: Option<&str>,
    returnPartialSuccess: Option<bool>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comlocations/global/operations",);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "filter",
            filter
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "maxResults",
            maxResults
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "orderBy",
            orderBy
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageToken",
            pageToken
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "parentId",
            parentId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "returnPartialSuccess",
            returnPartialSuccess
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET locations/global/operations
/// Retrieves a list of Operation resources contained within the specified organization.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_global_organization_operations_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_global_organization_operations_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_global_organization_operations_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<OperationList>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: OperationList = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET locations/global/operations
/// Retrieves a list of Operation resources contained within the specified organization.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_global_organization_operations_list_builder()` + `compute_global_organization_operations_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_global_organization_operations_list(
    client: &SimpleHttpClient,
    filter: Option<&str>,
    maxResults: Option<i32>,
    orderBy: Option<&str>,
    pageToken: Option<&str>,
    parentId: Option<&str>,
    returnPartialSuccess: Option<bool>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<OperationList>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = compute_global_organization_operations_list_builder(
        client,
        filter,
        maxResults,
        orderBy,
        pageToken,
        parentId,
        returnPartialSuccess,
    )?;
    compute_global_organization_operations_list_execute(builder)
}

/// GET projects/{project}/global/publicDelegatedPrefixes/{publicDelegatedPrefix}
/// Deletes the specified global PublicDelegatedPrefix.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_global_public_delegated_prefixes_delete_execute()` to send, or `compute_global_public_delegated_prefixes_delete` for simplest API.

pub fn compute_global_public_delegated_prefixes_delete_builder(
    client: &SimpleHttpClient,
    project: &str,
    publicDelegatedPrefix: &str,
    requestId: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/global/publicDelegatedPrefixes/{}",
        project, publicDelegatedPrefix,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "requestId",
            requestId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET projects/{project}/global/publicDelegatedPrefixes/{publicDelegatedPrefix}
/// Deletes the specified global PublicDelegatedPrefix.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_global_public_delegated_prefixes_delete_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_global_public_delegated_prefixes_delete_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_global_public_delegated_prefixes_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/global/publicDelegatedPrefixes/{publicDelegatedPrefix}
/// Deletes the specified global PublicDelegatedPrefix.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_global_public_delegated_prefixes_delete_builder()` + `compute_global_public_delegated_prefixes_delete_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_global_public_delegated_prefixes_delete(
    client: &SimpleHttpClient,
    project: &str,
    publicDelegatedPrefix: &str,
    requestId: Option<&str>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_global_public_delegated_prefixes_delete_builder(
        client,
        project,
        publicDelegatedPrefix,
        requestId,
    )?;
    compute_global_public_delegated_prefixes_delete_execute(builder)
}

/// GET projects/{project}/global/publicDelegatedPrefixes/{publicDelegatedPrefix}
/// Returns the specified global PublicDelegatedPrefix resource.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_global_public_delegated_prefixes_get_execute()` to send, or `compute_global_public_delegated_prefixes_get` for simplest API.

pub fn compute_global_public_delegated_prefixes_get_builder(
    client: &SimpleHttpClient,
    project: &str,
    publicDelegatedPrefix: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/global/publicDelegatedPrefixes/{}",
        project, publicDelegatedPrefix,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/global/publicDelegatedPrefixes/{publicDelegatedPrefix}
/// Returns the specified global PublicDelegatedPrefix resource.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_global_public_delegated_prefixes_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_global_public_delegated_prefixes_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_global_public_delegated_prefixes_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<PublicDelegatedPrefix>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: PublicDelegatedPrefix = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/global/publicDelegatedPrefixes/{publicDelegatedPrefix}
/// Returns the specified global PublicDelegatedPrefix resource.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_global_public_delegated_prefixes_get_builder()` + `compute_global_public_delegated_prefixes_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_global_public_delegated_prefixes_get(
    client: &SimpleHttpClient,
    project: &str,
    publicDelegatedPrefix: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<PublicDelegatedPrefix>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = compute_global_public_delegated_prefixes_get_builder(
        client,
        project,
        publicDelegatedPrefix,
    )?;
    compute_global_public_delegated_prefixes_get_execute(builder)
}

/// GET projects/{project}/global/publicDelegatedPrefixes
/// Creates a global PublicDelegatedPrefix in the specified project using the parameters that are included in the request.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_global_public_delegated_prefixes_insert_execute()` to send, or `compute_global_public_delegated_prefixes_insert` for simplest API.

pub fn compute_global_public_delegated_prefixes_insert_builder(
    client: &SimpleHttpClient,
    project: &str,
    requestId: Option<&str>,
    body: &PublicDelegatedPrefix,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/global/publicDelegatedPrefixes",
        project,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "requestId",
            requestId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET projects/{project}/global/publicDelegatedPrefixes
/// Creates a global PublicDelegatedPrefix in the specified project using the parameters that are included in the request.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_global_public_delegated_prefixes_insert_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_global_public_delegated_prefixes_insert_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_global_public_delegated_prefixes_insert_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/global/publicDelegatedPrefixes
/// Creates a global PublicDelegatedPrefix in the specified project using the parameters that are included in the request.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_global_public_delegated_prefixes_insert_builder()` + `compute_global_public_delegated_prefixes_insert_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_global_public_delegated_prefixes_insert(
    client: &SimpleHttpClient,
    project: &str,
    requestId: Option<&str>,
    body: &PublicDelegatedPrefix,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        compute_global_public_delegated_prefixes_insert_builder(client, project, requestId, body)?;
    compute_global_public_delegated_prefixes_insert_execute(builder)
}

/// GET projects/{project}/global/publicDelegatedPrefixes
/// Lists the global PublicDelegatedPrefixes for a project.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_global_public_delegated_prefixes_list_execute()` to send, or `compute_global_public_delegated_prefixes_list` for simplest API.

pub fn compute_global_public_delegated_prefixes_list_builder(
    client: &SimpleHttpClient,
    project: &str,
    filter: Option<&str>,
    maxResults: Option<i32>,
    orderBy: Option<&str>,
    pageToken: Option<&str>,
    returnPartialSuccess: Option<bool>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/global/publicDelegatedPrefixes",
        project,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "filter",
            filter
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "maxResults",
            maxResults
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "orderBy",
            orderBy
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageToken",
            pageToken
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "returnPartialSuccess",
            returnPartialSuccess
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET projects/{project}/global/publicDelegatedPrefixes
/// Lists the global PublicDelegatedPrefixes for a project.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_global_public_delegated_prefixes_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_global_public_delegated_prefixes_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_global_public_delegated_prefixes_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<PublicDelegatedPrefixList>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: PublicDelegatedPrefixList = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/global/publicDelegatedPrefixes
/// Lists the global PublicDelegatedPrefixes for a project.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_global_public_delegated_prefixes_list_builder()` + `compute_global_public_delegated_prefixes_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_global_public_delegated_prefixes_list(
    client: &SimpleHttpClient,
    project: &str,
    filter: Option<&str>,
    maxResults: Option<i32>,
    orderBy: Option<&str>,
    pageToken: Option<&str>,
    returnPartialSuccess: Option<bool>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<PublicDelegatedPrefixList>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = compute_global_public_delegated_prefixes_list_builder(
        client,
        project,
        filter,
        maxResults,
        orderBy,
        pageToken,
        returnPartialSuccess,
    )?;
    compute_global_public_delegated_prefixes_list_execute(builder)
}

/// GET projects/{project}/global/publicDelegatedPrefixes/{publicDelegatedPrefix}
/// Patches the specified global PublicDelegatedPrefix resource with the data included in the request. This method `supportsPATCH` semantics and `usesJSON` merge patch format and processing rules.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_global_public_delegated_prefixes_patch_execute()` to send, or `compute_global_public_delegated_prefixes_patch` for simplest API.

pub fn compute_global_public_delegated_prefixes_patch_builder(
    client: &SimpleHttpClient,
    project: &str,
    publicDelegatedPrefix: &str,
    requestId: Option<&str>,
    body: &PublicDelegatedPrefix,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/global/publicDelegatedPrefixes/{}",
        project, publicDelegatedPrefix,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "requestId",
            requestId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET projects/{project}/global/publicDelegatedPrefixes/{publicDelegatedPrefix}
/// Patches the specified global PublicDelegatedPrefix resource with the data included in the request. This method `supportsPATCH` semantics and `usesJSON` merge patch format and processing rules.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_global_public_delegated_prefixes_patch_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_global_public_delegated_prefixes_patch_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_global_public_delegated_prefixes_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/global/publicDelegatedPrefixes/{publicDelegatedPrefix}
/// Patches the specified global PublicDelegatedPrefix resource with the data included in the request. This method `supportsPATCH` semantics and `usesJSON` merge patch format and processing rules.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_global_public_delegated_prefixes_patch_builder()` + `compute_global_public_delegated_prefixes_patch_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_global_public_delegated_prefixes_patch(
    client: &SimpleHttpClient,
    project: &str,
    publicDelegatedPrefix: &str,
    requestId: Option<&str>,
    body: &PublicDelegatedPrefix,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_global_public_delegated_prefixes_patch_builder(
        client,
        project,
        publicDelegatedPrefix,
        requestId,
        body,
    )?;
    compute_global_public_delegated_prefixes_patch_execute(builder)
}

/// GET projects/{project}/aggregated/healthChecks
/// Retrieves the list of all HealthCheck resources, regional and global, available to the specified project.  To prevent failure, Google recommends that you set the `returnPartialSuccess` parameter to `true`.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_health_checks_aggregated_list_execute()` to send, or `compute_health_checks_aggregated_list` for simplest API.

pub fn compute_health_checks_aggregated_list_builder(
    client: &SimpleHttpClient,
    project: &str,
    filter: Option<&str>,
    includeAllScopes: Option<bool>,
    maxResults: Option<i32>,
    orderBy: Option<&str>,
    pageToken: Option<&str>,
    returnPartialSuccess: Option<bool>,
    serviceProjectNumber: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/aggregated/healthChecks",
        project,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "filter",
            filter
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "includeAllScopes",
            includeAllScopes
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "maxResults",
            maxResults
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "orderBy",
            orderBy
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageToken",
            pageToken
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "returnPartialSuccess",
            returnPartialSuccess
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "serviceProjectNumber",
            serviceProjectNumber
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET projects/{project}/aggregated/healthChecks
/// Retrieves the list of all HealthCheck resources, regional and global, available to the specified project.  To prevent failure, Google recommends that you set the `returnPartialSuccess` parameter to `true`.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_health_checks_aggregated_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_health_checks_aggregated_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_health_checks_aggregated_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<HealthChecksAggregatedList>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: HealthChecksAggregatedList = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/aggregated/healthChecks
/// Retrieves the list of all HealthCheck resources, regional and global, available to the specified project.  To prevent failure, Google recommends that you set the `returnPartialSuccess` parameter to `true`.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_health_checks_aggregated_list_builder()` + `compute_health_checks_aggregated_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_health_checks_aggregated_list(
    client: &SimpleHttpClient,
    project: &str,
    filter: Option<&str>,
    includeAllScopes: Option<bool>,
    maxResults: Option<i32>,
    orderBy: Option<&str>,
    pageToken: Option<&str>,
    returnPartialSuccess: Option<bool>,
    serviceProjectNumber: Option<&str>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<HealthChecksAggregatedList>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = compute_health_checks_aggregated_list_builder(
        client,
        project,
        filter,
        includeAllScopes,
        maxResults,
        orderBy,
        pageToken,
        returnPartialSuccess,
        serviceProjectNumber,
    )?;
    compute_health_checks_aggregated_list_execute(builder)
}

/// GET projects/{project}/global/healthChecks/{healthCheck}
/// Deletes the specified HealthCheck resource.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_health_checks_delete_execute()` to send, or `compute_health_checks_delete` for simplest API.

pub fn compute_health_checks_delete_builder(
    client: &SimpleHttpClient,
    healthCheck: &str,
    project: &str,
    requestId: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/global/healthChecks/{}",
        healthCheck, project,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "requestId",
            requestId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET projects/{project}/global/healthChecks/{healthCheck}
/// Deletes the specified HealthCheck resource.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_health_checks_delete_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_health_checks_delete_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_health_checks_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/global/healthChecks/{healthCheck}
/// Deletes the specified HealthCheck resource.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_health_checks_delete_builder()` + `compute_health_checks_delete_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_health_checks_delete(
    client: &SimpleHttpClient,
    healthCheck: &str,
    project: &str,
    requestId: Option<&str>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_health_checks_delete_builder(client, healthCheck, project, requestId)?;
    compute_health_checks_delete_execute(builder)
}

/// GET projects/{project}/global/healthChecks/{healthCheck}
/// Returns the specified HealthCheck resource.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_health_checks_get_execute()` to send, or `compute_health_checks_get` for simplest API.

pub fn compute_health_checks_get_builder(
    client: &SimpleHttpClient,
    healthCheck: &str,
    project: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/global/healthChecks/{}",
        healthCheck, project,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/global/healthChecks/{healthCheck}
/// Returns the specified HealthCheck resource.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_health_checks_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_health_checks_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_health_checks_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<HealthCheck>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: HealthCheck = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/global/healthChecks/{healthCheck}
/// Returns the specified HealthCheck resource.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_health_checks_get_builder()` + `compute_health_checks_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_health_checks_get(
    client: &SimpleHttpClient,
    healthCheck: &str,
    project: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<HealthCheck>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_health_checks_get_builder(client, healthCheck, project)?;
    compute_health_checks_get_execute(builder)
}

/// GET projects/{project}/global/healthChecks
/// Creates a HealthCheck resource in the specified project using the data included in the request.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_health_checks_insert_execute()` to send, or `compute_health_checks_insert` for simplest API.

pub fn compute_health_checks_insert_builder(
    client: &SimpleHttpClient,
    project: &str,
    requestId: Option<&str>,
    body: &HealthCheck,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/global/healthChecks",
        project,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "requestId",
            requestId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET projects/{project}/global/healthChecks
/// Creates a HealthCheck resource in the specified project using the data included in the request.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_health_checks_insert_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_health_checks_insert_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_health_checks_insert_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/global/healthChecks
/// Creates a HealthCheck resource in the specified project using the data included in the request.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_health_checks_insert_builder()` + `compute_health_checks_insert_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_health_checks_insert(
    client: &SimpleHttpClient,
    project: &str,
    requestId: Option<&str>,
    body: &HealthCheck,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_health_checks_insert_builder(client, project, requestId, body)?;
    compute_health_checks_insert_execute(builder)
}

/// GET projects/{project}/global/healthChecks
/// Retrieves the list of HealthCheck resources available to the specified project.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_health_checks_list_execute()` to send, or `compute_health_checks_list` for simplest API.

pub fn compute_health_checks_list_builder(
    client: &SimpleHttpClient,
    project: &str,
    filter: Option<&str>,
    maxResults: Option<i32>,
    orderBy: Option<&str>,
    pageToken: Option<&str>,
    returnPartialSuccess: Option<bool>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/global/healthChecks",
        project,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "filter",
            filter
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "maxResults",
            maxResults
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "orderBy",
            orderBy
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageToken",
            pageToken
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "returnPartialSuccess",
            returnPartialSuccess
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET projects/{project}/global/healthChecks
/// Retrieves the list of HealthCheck resources available to the specified project.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_health_checks_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_health_checks_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_health_checks_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<HealthCheckList>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: HealthCheckList = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/global/healthChecks
/// Retrieves the list of HealthCheck resources available to the specified project.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_health_checks_list_builder()` + `compute_health_checks_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_health_checks_list(
    client: &SimpleHttpClient,
    project: &str,
    filter: Option<&str>,
    maxResults: Option<i32>,
    orderBy: Option<&str>,
    pageToken: Option<&str>,
    returnPartialSuccess: Option<bool>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<HealthCheckList>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = compute_health_checks_list_builder(
        client,
        project,
        filter,
        maxResults,
        orderBy,
        pageToken,
        returnPartialSuccess,
    )?;
    compute_health_checks_list_execute(builder)
}

/// GET projects/{project}/global/healthChecks/{healthCheck}
/// Updates a HealthCheck resource in the specified project using the data included in the request. This method `supportsPATCH` semantics and uses `theJSON` merge patch format and processing rules.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_health_checks_patch_execute()` to send, or `compute_health_checks_patch` for simplest API.

pub fn compute_health_checks_patch_builder(
    client: &SimpleHttpClient,
    healthCheck: &str,
    project: &str,
    requestId: Option<&str>,
    body: &HealthCheck,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/global/healthChecks/{}",
        healthCheck, project,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "requestId",
            requestId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET projects/{project}/global/healthChecks/{healthCheck}
/// Updates a HealthCheck resource in the specified project using the data included in the request. This method `supportsPATCH` semantics and uses `theJSON` merge patch format and processing rules.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_health_checks_patch_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_health_checks_patch_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_health_checks_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/global/healthChecks/{healthCheck}
/// Updates a HealthCheck resource in the specified project using the data included in the request. This method `supportsPATCH` semantics and uses `theJSON` merge patch format and processing rules.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_health_checks_patch_builder()` + `compute_health_checks_patch_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_health_checks_patch(
    client: &SimpleHttpClient,
    healthCheck: &str,
    project: &str,
    requestId: Option<&str>,
    body: &HealthCheck,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        compute_health_checks_patch_builder(client, healthCheck, project, requestId, body)?;
    compute_health_checks_patch_execute(builder)
}

/// GET projects/{project}/global/healthChecks/{resource}/testIamPermissions
/// Returns permissions that a caller has on the specified resource.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_health_checks_test_iam_permissions_execute()` to send, or `compute_health_checks_test_iam_permissions` for simplest API.

pub fn compute_health_checks_test_iam_permissions_builder(
    client: &SimpleHttpClient,
    project: &str,
    resource: &str,
    body: &TestPermissionsRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/global/healthChecks/{}/testIamPermissions",
        project, resource,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET projects/{project}/global/healthChecks/{resource}/testIamPermissions
/// Returns permissions that a caller has on the specified resource.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_health_checks_test_iam_permissions_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_health_checks_test_iam_permissions_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_health_checks_test_iam_permissions_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<TestPermissionsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: TestPermissionsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/global/healthChecks/{resource}/testIamPermissions
/// Returns permissions that a caller has on the specified resource.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_health_checks_test_iam_permissions_builder()` + `compute_health_checks_test_iam_permissions_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_health_checks_test_iam_permissions(
    client: &SimpleHttpClient,
    project: &str,
    resource: &str,
    body: &TestPermissionsRequest,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<TestPermissionsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        compute_health_checks_test_iam_permissions_builder(client, project, resource, body)?;
    compute_health_checks_test_iam_permissions_execute(builder)
}

/// GET projects/{project}/global/healthChecks/{healthCheck}
/// Updates a HealthCheck resource in the specified project using the data included in the request.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_health_checks_update_execute()` to send, or `compute_health_checks_update` for simplest API.

pub fn compute_health_checks_update_builder(
    client: &SimpleHttpClient,
    healthCheck: &str,
    project: &str,
    requestId: Option<&str>,
    body: &HealthCheck,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/global/healthChecks/{}",
        healthCheck, project,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "requestId",
            requestId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET projects/{project}/global/healthChecks/{healthCheck}
/// Updates a HealthCheck resource in the specified project using the data included in the request.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_health_checks_update_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_health_checks_update_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_health_checks_update_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/global/healthChecks/{healthCheck}
/// Updates a HealthCheck resource in the specified project using the data included in the request.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_health_checks_update_builder()` + `compute_health_checks_update_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_health_checks_update(
    client: &SimpleHttpClient,
    healthCheck: &str,
    project: &str,
    requestId: Option<&str>,
    body: &HealthCheck,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        compute_health_checks_update_builder(client, healthCheck, project, requestId, body)?;
    compute_health_checks_update_execute(builder)
}

/// GET projects/{project}/global/httpHealthChecks/{httpHealthCheck}
/// Deletes the specified HttpHealthCheck resource.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_http_health_checks_delete_execute()` to send, or `compute_http_health_checks_delete` for simplest API.

pub fn compute_http_health_checks_delete_builder(
    client: &SimpleHttpClient,
    httpHealthCheck: &str,
    project: &str,
    requestId: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/global/httpHealthChecks/{}",
        httpHealthCheck, project,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "requestId",
            requestId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET projects/{project}/global/httpHealthChecks/{httpHealthCheck}
/// Deletes the specified HttpHealthCheck resource.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_http_health_checks_delete_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_http_health_checks_delete_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_http_health_checks_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/global/httpHealthChecks/{httpHealthCheck}
/// Deletes the specified HttpHealthCheck resource.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_http_health_checks_delete_builder()` + `compute_http_health_checks_delete_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_http_health_checks_delete(
    client: &SimpleHttpClient,
    httpHealthCheck: &str,
    project: &str,
    requestId: Option<&str>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        compute_http_health_checks_delete_builder(client, httpHealthCheck, project, requestId)?;
    compute_http_health_checks_delete_execute(builder)
}

/// GET projects/{project}/global/httpHealthChecks/{httpHealthCheck}
/// Returns the specified HttpHealthCheck resource.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_http_health_checks_get_execute()` to send, or `compute_http_health_checks_get` for simplest API.

pub fn compute_http_health_checks_get_builder(
    client: &SimpleHttpClient,
    httpHealthCheck: &str,
    project: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/global/httpHealthChecks/{}",
        httpHealthCheck, project,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/global/httpHealthChecks/{httpHealthCheck}
/// Returns the specified HttpHealthCheck resource.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_http_health_checks_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_http_health_checks_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_http_health_checks_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<HttpHealthCheck>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: HttpHealthCheck = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/global/httpHealthChecks/{httpHealthCheck}
/// Returns the specified HttpHealthCheck resource.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_http_health_checks_get_builder()` + `compute_http_health_checks_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_http_health_checks_get(
    client: &SimpleHttpClient,
    httpHealthCheck: &str,
    project: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<HttpHealthCheck>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = compute_http_health_checks_get_builder(client, httpHealthCheck, project)?;
    compute_http_health_checks_get_execute(builder)
}

/// GET projects/{project}/global/httpHealthChecks
/// Creates a HttpHealthCheck resource in the specified project using the data included in the request.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_http_health_checks_insert_execute()` to send, or `compute_http_health_checks_insert` for simplest API.

pub fn compute_http_health_checks_insert_builder(
    client: &SimpleHttpClient,
    project: &str,
    requestId: Option<&str>,
    body: &HttpHealthCheck,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/global/httpHealthChecks",
        project,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "requestId",
            requestId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET projects/{project}/global/httpHealthChecks
/// Creates a HttpHealthCheck resource in the specified project using the data included in the request.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_http_health_checks_insert_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_http_health_checks_insert_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_http_health_checks_insert_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/global/httpHealthChecks
/// Creates a HttpHealthCheck resource in the specified project using the data included in the request.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_http_health_checks_insert_builder()` + `compute_http_health_checks_insert_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_http_health_checks_insert(
    client: &SimpleHttpClient,
    project: &str,
    requestId: Option<&str>,
    body: &HttpHealthCheck,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_http_health_checks_insert_builder(client, project, requestId, body)?;
    compute_http_health_checks_insert_execute(builder)
}

/// GET projects/{project}/global/httpHealthChecks
/// Retrieves the list of HttpHealthCheck resources available to the specified project.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_http_health_checks_list_execute()` to send, or `compute_http_health_checks_list` for simplest API.

pub fn compute_http_health_checks_list_builder(
    client: &SimpleHttpClient,
    project: &str,
    filter: Option<&str>,
    maxResults: Option<i32>,
    orderBy: Option<&str>,
    pageToken: Option<&str>,
    returnPartialSuccess: Option<bool>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/global/httpHealthChecks",
        project,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "filter",
            filter
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "maxResults",
            maxResults
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "orderBy",
            orderBy
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageToken",
            pageToken
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "returnPartialSuccess",
            returnPartialSuccess
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET projects/{project}/global/httpHealthChecks
/// Retrieves the list of HttpHealthCheck resources available to the specified project.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_http_health_checks_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_http_health_checks_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_http_health_checks_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<HttpHealthCheckList>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: HttpHealthCheckList = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/global/httpHealthChecks
/// Retrieves the list of HttpHealthCheck resources available to the specified project.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_http_health_checks_list_builder()` + `compute_http_health_checks_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_http_health_checks_list(
    client: &SimpleHttpClient,
    project: &str,
    filter: Option<&str>,
    maxResults: Option<i32>,
    orderBy: Option<&str>,
    pageToken: Option<&str>,
    returnPartialSuccess: Option<bool>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<HttpHealthCheckList>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = compute_http_health_checks_list_builder(
        client,
        project,
        filter,
        maxResults,
        orderBy,
        pageToken,
        returnPartialSuccess,
    )?;
    compute_http_health_checks_list_execute(builder)
}

/// GET projects/{project}/global/httpHealthChecks/{httpHealthCheck}
/// Updates a HttpHealthCheck resource in the specified project using the data included in the request. This method `supportsPATCH` semantics and uses `theJSON` merge patch format and processing rules.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_http_health_checks_patch_execute()` to send, or `compute_http_health_checks_patch` for simplest API.

pub fn compute_http_health_checks_patch_builder(
    client: &SimpleHttpClient,
    httpHealthCheck: &str,
    project: &str,
    requestId: Option<&str>,
    body: &HttpHealthCheck,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/global/httpHealthChecks/{}",
        httpHealthCheck, project,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "requestId",
            requestId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET projects/{project}/global/httpHealthChecks/{httpHealthCheck}
/// Updates a HttpHealthCheck resource in the specified project using the data included in the request. This method `supportsPATCH` semantics and uses `theJSON` merge patch format and processing rules.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_http_health_checks_patch_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_http_health_checks_patch_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_http_health_checks_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/global/httpHealthChecks/{httpHealthCheck}
/// Updates a HttpHealthCheck resource in the specified project using the data included in the request. This method `supportsPATCH` semantics and uses `theJSON` merge patch format and processing rules.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_http_health_checks_patch_builder()` + `compute_http_health_checks_patch_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_http_health_checks_patch(
    client: &SimpleHttpClient,
    httpHealthCheck: &str,
    project: &str,
    requestId: Option<&str>,
    body: &HttpHealthCheck,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_http_health_checks_patch_builder(
        client,
        httpHealthCheck,
        project,
        requestId,
        body,
    )?;
    compute_http_health_checks_patch_execute(builder)
}

/// GET projects/{project}/global/httpHealthChecks/{resource}/testIamPermissions
/// Returns permissions that a caller has on the specified resource.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_http_health_checks_test_iam_permissions_execute()` to send, or `compute_http_health_checks_test_iam_permissions` for simplest API.

pub fn compute_http_health_checks_test_iam_permissions_builder(
    client: &SimpleHttpClient,
    project: &str,
    resource: &str,
    body: &TestPermissionsRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/global/httpHealthChecks/{}/testIamPermissions",
        project, resource,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET projects/{project}/global/httpHealthChecks/{resource}/testIamPermissions
/// Returns permissions that a caller has on the specified resource.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_http_health_checks_test_iam_permissions_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_http_health_checks_test_iam_permissions_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_http_health_checks_test_iam_permissions_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<TestPermissionsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: TestPermissionsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/global/httpHealthChecks/{resource}/testIamPermissions
/// Returns permissions that a caller has on the specified resource.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_http_health_checks_test_iam_permissions_builder()` + `compute_http_health_checks_test_iam_permissions_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_http_health_checks_test_iam_permissions(
    client: &SimpleHttpClient,
    project: &str,
    resource: &str,
    body: &TestPermissionsRequest,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<TestPermissionsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        compute_http_health_checks_test_iam_permissions_builder(client, project, resource, body)?;
    compute_http_health_checks_test_iam_permissions_execute(builder)
}

/// GET projects/{project}/global/httpHealthChecks/{httpHealthCheck}
/// Updates a HttpHealthCheck resource in the specified project using the data included in the request.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_http_health_checks_update_execute()` to send, or `compute_http_health_checks_update` for simplest API.

pub fn compute_http_health_checks_update_builder(
    client: &SimpleHttpClient,
    httpHealthCheck: &str,
    project: &str,
    requestId: Option<&str>,
    body: &HttpHealthCheck,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/global/httpHealthChecks/{}",
        httpHealthCheck, project,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "requestId",
            requestId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET projects/{project}/global/httpHealthChecks/{httpHealthCheck}
/// Updates a HttpHealthCheck resource in the specified project using the data included in the request.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_http_health_checks_update_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_http_health_checks_update_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_http_health_checks_update_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/global/httpHealthChecks/{httpHealthCheck}
/// Updates a HttpHealthCheck resource in the specified project using the data included in the request.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_http_health_checks_update_builder()` + `compute_http_health_checks_update_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_http_health_checks_update(
    client: &SimpleHttpClient,
    httpHealthCheck: &str,
    project: &str,
    requestId: Option<&str>,
    body: &HttpHealthCheck,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_http_health_checks_update_builder(
        client,
        httpHealthCheck,
        project,
        requestId,
        body,
    )?;
    compute_http_health_checks_update_execute(builder)
}

/// GET projects/{project}/global/httpsHealthChecks/{httpsHealthCheck}
/// Deletes the specified HttpsHealthCheck resource.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_https_health_checks_delete_execute()` to send, or `compute_https_health_checks_delete` for simplest API.

pub fn compute_https_health_checks_delete_builder(
    client: &SimpleHttpClient,
    httpsHealthCheck: &str,
    project: &str,
    requestId: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/global/httpsHealthChecks/{}",
        httpsHealthCheck, project,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "requestId",
            requestId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET projects/{project}/global/httpsHealthChecks/{httpsHealthCheck}
/// Deletes the specified HttpsHealthCheck resource.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_https_health_checks_delete_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_https_health_checks_delete_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_https_health_checks_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/global/httpsHealthChecks/{httpsHealthCheck}
/// Deletes the specified HttpsHealthCheck resource.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_https_health_checks_delete_builder()` + `compute_https_health_checks_delete_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_https_health_checks_delete(
    client: &SimpleHttpClient,
    httpsHealthCheck: &str,
    project: &str,
    requestId: Option<&str>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        compute_https_health_checks_delete_builder(client, httpsHealthCheck, project, requestId)?;
    compute_https_health_checks_delete_execute(builder)
}

/// GET projects/{project}/global/httpsHealthChecks/{httpsHealthCheck}
/// Returns the specified HttpsHealthCheck resource.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_https_health_checks_get_execute()` to send, or `compute_https_health_checks_get` for simplest API.

pub fn compute_https_health_checks_get_builder(
    client: &SimpleHttpClient,
    httpsHealthCheck: &str,
    project: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/global/httpsHealthChecks/{}",
        httpsHealthCheck, project,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/global/httpsHealthChecks/{httpsHealthCheck}
/// Returns the specified HttpsHealthCheck resource.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_https_health_checks_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_https_health_checks_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_https_health_checks_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<HttpsHealthCheck>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: HttpsHealthCheck = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/global/httpsHealthChecks/{httpsHealthCheck}
/// Returns the specified HttpsHealthCheck resource.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_https_health_checks_get_builder()` + `compute_https_health_checks_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_https_health_checks_get(
    client: &SimpleHttpClient,
    httpsHealthCheck: &str,
    project: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<HttpsHealthCheck>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = compute_https_health_checks_get_builder(client, httpsHealthCheck, project)?;
    compute_https_health_checks_get_execute(builder)
}

/// GET projects/{project}/global/httpsHealthChecks
/// Creates a HttpsHealthCheck resource in the specified project using the data included in the request.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_https_health_checks_insert_execute()` to send, or `compute_https_health_checks_insert` for simplest API.

pub fn compute_https_health_checks_insert_builder(
    client: &SimpleHttpClient,
    project: &str,
    requestId: Option<&str>,
    body: &HttpsHealthCheck,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/global/httpsHealthChecks",
        project,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "requestId",
            requestId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET projects/{project}/global/httpsHealthChecks
/// Creates a HttpsHealthCheck resource in the specified project using the data included in the request.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_https_health_checks_insert_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_https_health_checks_insert_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_https_health_checks_insert_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/global/httpsHealthChecks
/// Creates a HttpsHealthCheck resource in the specified project using the data included in the request.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_https_health_checks_insert_builder()` + `compute_https_health_checks_insert_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_https_health_checks_insert(
    client: &SimpleHttpClient,
    project: &str,
    requestId: Option<&str>,
    body: &HttpsHealthCheck,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_https_health_checks_insert_builder(client, project, requestId, body)?;
    compute_https_health_checks_insert_execute(builder)
}

/// GET projects/{project}/global/httpsHealthChecks
/// Retrieves the list of HttpsHealthCheck resources available to the specified project.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_https_health_checks_list_execute()` to send, or `compute_https_health_checks_list` for simplest API.

pub fn compute_https_health_checks_list_builder(
    client: &SimpleHttpClient,
    project: &str,
    filter: Option<&str>,
    maxResults: Option<i32>,
    orderBy: Option<&str>,
    pageToken: Option<&str>,
    returnPartialSuccess: Option<bool>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/global/httpsHealthChecks",
        project,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "filter",
            filter
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "maxResults",
            maxResults
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "orderBy",
            orderBy
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageToken",
            pageToken
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "returnPartialSuccess",
            returnPartialSuccess
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET projects/{project}/global/httpsHealthChecks
/// Retrieves the list of HttpsHealthCheck resources available to the specified project.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_https_health_checks_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_https_health_checks_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_https_health_checks_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<HttpsHealthCheckList>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: HttpsHealthCheckList = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/global/httpsHealthChecks
/// Retrieves the list of HttpsHealthCheck resources available to the specified project.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_https_health_checks_list_builder()` + `compute_https_health_checks_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_https_health_checks_list(
    client: &SimpleHttpClient,
    project: &str,
    filter: Option<&str>,
    maxResults: Option<i32>,
    orderBy: Option<&str>,
    pageToken: Option<&str>,
    returnPartialSuccess: Option<bool>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<HttpsHealthCheckList>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = compute_https_health_checks_list_builder(
        client,
        project,
        filter,
        maxResults,
        orderBy,
        pageToken,
        returnPartialSuccess,
    )?;
    compute_https_health_checks_list_execute(builder)
}

/// GET projects/{project}/global/httpsHealthChecks/{httpsHealthCheck}
/// Updates a HttpsHealthCheck resource in the specified project using the data included in the request. This method `supportsPATCH` semantics and uses `theJSON` merge patch format and processing rules.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_https_health_checks_patch_execute()` to send, or `compute_https_health_checks_patch` for simplest API.

pub fn compute_https_health_checks_patch_builder(
    client: &SimpleHttpClient,
    httpsHealthCheck: &str,
    project: &str,
    requestId: Option<&str>,
    body: &HttpsHealthCheck,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/global/httpsHealthChecks/{}",
        httpsHealthCheck, project,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "requestId",
            requestId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET projects/{project}/global/httpsHealthChecks/{httpsHealthCheck}
/// Updates a HttpsHealthCheck resource in the specified project using the data included in the request. This method `supportsPATCH` semantics and uses `theJSON` merge patch format and processing rules.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_https_health_checks_patch_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_https_health_checks_patch_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_https_health_checks_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/global/httpsHealthChecks/{httpsHealthCheck}
/// Updates a HttpsHealthCheck resource in the specified project using the data included in the request. This method `supportsPATCH` semantics and uses `theJSON` merge patch format and processing rules.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_https_health_checks_patch_builder()` + `compute_https_health_checks_patch_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_https_health_checks_patch(
    client: &SimpleHttpClient,
    httpsHealthCheck: &str,
    project: &str,
    requestId: Option<&str>,
    body: &HttpsHealthCheck,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_https_health_checks_patch_builder(
        client,
        httpsHealthCheck,
        project,
        requestId,
        body,
    )?;
    compute_https_health_checks_patch_execute(builder)
}

/// GET projects/{project}/global/httpsHealthChecks/{resource}/testIamPermissions
/// Returns permissions that a caller has on the specified resource.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_https_health_checks_test_iam_permissions_execute()` to send, or `compute_https_health_checks_test_iam_permissions` for simplest API.

pub fn compute_https_health_checks_test_iam_permissions_builder(
    client: &SimpleHttpClient,
    project: &str,
    resource: &str,
    body: &TestPermissionsRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/global/httpsHealthChecks/{}/testIamPermissions",
        project, resource,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET projects/{project}/global/httpsHealthChecks/{resource}/testIamPermissions
/// Returns permissions that a caller has on the specified resource.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_https_health_checks_test_iam_permissions_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_https_health_checks_test_iam_permissions_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_https_health_checks_test_iam_permissions_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<TestPermissionsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: TestPermissionsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/global/httpsHealthChecks/{resource}/testIamPermissions
/// Returns permissions that a caller has on the specified resource.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_https_health_checks_test_iam_permissions_builder()` + `compute_https_health_checks_test_iam_permissions_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_https_health_checks_test_iam_permissions(
    client: &SimpleHttpClient,
    project: &str,
    resource: &str,
    body: &TestPermissionsRequest,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<TestPermissionsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        compute_https_health_checks_test_iam_permissions_builder(client, project, resource, body)?;
    compute_https_health_checks_test_iam_permissions_execute(builder)
}

/// GET projects/{project}/global/httpsHealthChecks/{httpsHealthCheck}
/// Updates a HttpsHealthCheck resource in the specified project using the data included in the request.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_https_health_checks_update_execute()` to send, or `compute_https_health_checks_update` for simplest API.

pub fn compute_https_health_checks_update_builder(
    client: &SimpleHttpClient,
    httpsHealthCheck: &str,
    project: &str,
    requestId: Option<&str>,
    body: &HttpsHealthCheck,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/global/httpsHealthChecks/{}",
        httpsHealthCheck, project,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "requestId",
            requestId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET projects/{project}/global/httpsHealthChecks/{httpsHealthCheck}
/// Updates a HttpsHealthCheck resource in the specified project using the data included in the request.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_https_health_checks_update_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_https_health_checks_update_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_https_health_checks_update_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/global/httpsHealthChecks/{httpsHealthCheck}
/// Updates a HttpsHealthCheck resource in the specified project using the data included in the request.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_https_health_checks_update_builder()` + `compute_https_health_checks_update_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_https_health_checks_update(
    client: &SimpleHttpClient,
    httpsHealthCheck: &str,
    project: &str,
    requestId: Option<&str>,
    body: &HttpsHealthCheck,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_https_health_checks_update_builder(
        client,
        httpsHealthCheck,
        project,
        requestId,
        body,
    )?;
    compute_https_health_checks_update_execute(builder)
}

/// GET projects/{project}/zones/{zone}/imageFamilyViews/{family}
/// Returns the latest image that is part of an image family, is not deprecated and is rolled out in the specified zone.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_image_family_views_get_execute()` to send, or `compute_image_family_views_get` for simplest API.

pub fn compute_image_family_views_get_builder(
    client: &SimpleHttpClient,
    family: &str,
    project: &str,
    zone: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/zones/{}/imageFamilyViews/{}",
        family, project, zone,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/zones/{zone}/imageFamilyViews/{family}
/// Returns the latest image that is part of an image family, is not deprecated and is rolled out in the specified zone.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_image_family_views_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_image_family_views_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_image_family_views_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ImageFamilyView>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ImageFamilyView = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/zones/{zone}/imageFamilyViews/{family}
/// Returns the latest image that is part of an image family, is not deprecated and is rolled out in the specified zone.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_image_family_views_get_builder()` + `compute_image_family_views_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_image_family_views_get(
    client: &SimpleHttpClient,
    family: &str,
    project: &str,
    zone: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ImageFamilyView>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = compute_image_family_views_get_builder(client, family, project, zone)?;
    compute_image_family_views_get_execute(builder)
}

/// GET projects/{project}/global/images/{image}
/// Deletes the specified image.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_images_delete_execute()` to send, or `compute_images_delete` for simplest API.

pub fn compute_images_delete_builder(
    client: &SimpleHttpClient,
    image: &str,
    project: &str,
    requestId: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/global/images/{}",
        image, project,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "requestId",
            requestId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET projects/{project}/global/images/{image}
/// Deletes the specified image.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_images_delete_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_images_delete_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_images_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/global/images/{image}
/// Deletes the specified image.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_images_delete_builder()` + `compute_images_delete_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_images_delete(
    client: &SimpleHttpClient,
    image: &str,
    project: &str,
    requestId: Option<&str>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_images_delete_builder(client, image, project, requestId)?;
    compute_images_delete_execute(builder)
}

/// GET projects/{project}/global/images/{image}/deprecate
/// Sets the deprecation status of an image.  If an empty request body is given, clears the deprecation status instead.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_images_deprecate_execute()` to send, or `compute_images_deprecate` for simplest API.

pub fn compute_images_deprecate_builder(
    client: &SimpleHttpClient,
    image: &str,
    project: &str,
    requestId: Option<&str>,
    body: &DeprecationStatus,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/global/images/{}/deprecate",
        image, project,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "requestId",
            requestId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET projects/{project}/global/images/{image}/deprecate
/// Sets the deprecation status of an image.  If an empty request body is given, clears the deprecation status instead.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_images_deprecate_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_images_deprecate_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_images_deprecate_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/global/images/{image}/deprecate
/// Sets the deprecation status of an image.  If an empty request body is given, clears the deprecation status instead.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_images_deprecate_builder()` + `compute_images_deprecate_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_images_deprecate(
    client: &SimpleHttpClient,
    image: &str,
    project: &str,
    requestId: Option<&str>,
    body: &DeprecationStatus,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_images_deprecate_builder(client, image, project, requestId, body)?;
    compute_images_deprecate_execute(builder)
}

/// GET projects/{project}/global/images/{image}
/// Returns the specified image.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_images_get_execute()` to send, or `compute_images_get` for simplest API.

pub fn compute_images_get_builder(
    client: &SimpleHttpClient,
    image: &str,
    project: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/global/images/{}",
        image, project,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/global/images/{image}
/// Returns the specified image.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_images_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_images_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_images_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Image>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Image = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/global/images/{image}
/// Returns the specified image.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_images_get_builder()` + `compute_images_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_images_get(
    client: &SimpleHttpClient,
    image: &str,
    project: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Image>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_images_get_builder(client, image, project)?;
    compute_images_get_execute(builder)
}

/// GET projects/{project}/global/images/family/{family}
/// Returns the latest image that is part of an image family and is not deprecated. For more information on image families, `seePublic` image families documentation.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_images_get_from_family_execute()` to send, or `compute_images_get_from_family` for simplest API.

pub fn compute_images_get_from_family_builder(
    client: &SimpleHttpClient,
    family: &str,
    project: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/global/images/family/{}",
        family, project,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/global/images/family/{family}
/// Returns the latest image that is part of an image family and is not deprecated. For more information on image families, `seePublic` image families documentation.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_images_get_from_family_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_images_get_from_family_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_images_get_from_family_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Image>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Image = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/global/images/family/{family}
/// Returns the latest image that is part of an image family and is not deprecated. For more information on image families, `seePublic` image families documentation.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_images_get_from_family_builder()` + `compute_images_get_from_family_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_images_get_from_family(
    client: &SimpleHttpClient,
    family: &str,
    project: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Image>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_images_get_from_family_builder(client, family, project)?;
    compute_images_get_from_family_execute(builder)
}

/// GET projects/{project}/global/images/{resource}/getIamPolicy
/// Gets the access control policy for a resource. May be empty if no such policy or resource exists.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_images_get_iam_policy_execute()` to send, or `compute_images_get_iam_policy` for simplest API.

pub fn compute_images_get_iam_policy_builder(
    client: &SimpleHttpClient,
    project: &str,
    resource: &str,
    optionsRequestedPolicyVersion: Option<i32>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/global/images/{}/getIamPolicy",
        project, resource,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "optionsRequestedPolicyVersion",
            optionsRequestedPolicyVersion
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET projects/{project}/global/images/{resource}/getIamPolicy
/// Gets the access control policy for a resource. May be empty if no such policy or resource exists.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_images_get_iam_policy_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_images_get_iam_policy_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_images_get_iam_policy_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Policy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Policy = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/global/images/{resource}/getIamPolicy
/// Gets the access control policy for a resource. May be empty if no such policy or resource exists.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_images_get_iam_policy_builder()` + `compute_images_get_iam_policy_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_images_get_iam_policy(
    client: &SimpleHttpClient,
    project: &str,
    resource: &str,
    optionsRequestedPolicyVersion: Option<i32>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Policy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_images_get_iam_policy_builder(
        client,
        project,
        resource,
        optionsRequestedPolicyVersion,
    )?;
    compute_images_get_iam_policy_execute(builder)
}

/// GET projects/{project}/global/images
/// Creates an image in the specified project using the data included in the request.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_images_insert_execute()` to send, or `compute_images_insert` for simplest API.

pub fn compute_images_insert_builder(
    client: &SimpleHttpClient,
    project: &str,
    forceCreate: Option<bool>,
    requestId: Option<&str>,
    body: &Image,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comprojects/{}/global/images", project,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "forceCreate",
            forceCreate
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "requestId",
            requestId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET projects/{project}/global/images
/// Creates an image in the specified project using the data included in the request.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_images_insert_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_images_insert_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_images_insert_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/global/images
/// Creates an image in the specified project using the data included in the request.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_images_insert_builder()` + `compute_images_insert_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_images_insert(
    client: &SimpleHttpClient,
    project: &str,
    forceCreate: Option<bool>,
    requestId: Option<&str>,
    body: &Image,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_images_insert_builder(client, project, forceCreate, requestId, body)?;
    compute_images_insert_execute(builder)
}

/// GET projects/{project}/global/images
/// Retrieves the list of custom images available to the specified project. Custom images are images you create that belong to your project. This method does not get any images that belong to other projects, including publicly-available images, like Debian 8. If you want to get a list of publicly-available images, use this method to make a request to the respective image project, such as debian-cloud or windows-cloud.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_images_list_execute()` to send, or `compute_images_list` for simplest API.

pub fn compute_images_list_builder(
    client: &SimpleHttpClient,
    project: &str,
    filter: Option<&str>,
    maxResults: Option<i32>,
    orderBy: Option<&str>,
    pageToken: Option<&str>,
    returnPartialSuccess: Option<bool>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comprojects/{}/global/images", project,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "filter",
            filter
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "maxResults",
            maxResults
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "orderBy",
            orderBy
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageToken",
            pageToken
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "returnPartialSuccess",
            returnPartialSuccess
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET projects/{project}/global/images
/// Retrieves the list of custom images available to the specified project. Custom images are images you create that belong to your project. This method does not get any images that belong to other projects, including publicly-available images, like Debian 8. If you want to get a list of publicly-available images, use this method to make a request to the respective image project, such as debian-cloud or windows-cloud.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_images_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_images_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_images_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ImageList>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ImageList = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/global/images
/// Retrieves the list of custom images available to the specified project. Custom images are images you create that belong to your project. This method does not get any images that belong to other projects, including publicly-available images, like Debian 8. If you want to get a list of publicly-available images, use this method to make a request to the respective image project, such as debian-cloud or windows-cloud.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_images_list_builder()` + `compute_images_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_images_list(
    client: &SimpleHttpClient,
    project: &str,
    filter: Option<&str>,
    maxResults: Option<i32>,
    orderBy: Option<&str>,
    pageToken: Option<&str>,
    returnPartialSuccess: Option<bool>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ImageList>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_images_list_builder(
        client,
        project,
        filter,
        maxResults,
        orderBy,
        pageToken,
        returnPartialSuccess,
    )?;
    compute_images_list_execute(builder)
}

/// GET projects/{project}/global/images/{image}
/// Patches the specified image with the data included in the request. Only the following fields can be modified: family, description, deprecation status.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_images_patch_execute()` to send, or `compute_images_patch` for simplest API.

pub fn compute_images_patch_builder(
    client: &SimpleHttpClient,
    image: &str,
    project: &str,
    requestId: Option<&str>,
    body: &Image,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/global/images/{}",
        image, project,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "requestId",
            requestId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET projects/{project}/global/images/{image}
/// Patches the specified image with the data included in the request. Only the following fields can be modified: family, description, deprecation status.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_images_patch_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_images_patch_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_images_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/global/images/{image}
/// Patches the specified image with the data included in the request. Only the following fields can be modified: family, description, deprecation status.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_images_patch_builder()` + `compute_images_patch_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_images_patch(
    client: &SimpleHttpClient,
    image: &str,
    project: &str,
    requestId: Option<&str>,
    body: &Image,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_images_patch_builder(client, image, project, requestId, body)?;
    compute_images_patch_execute(builder)
}

/// GET projects/{project}/global/images/{resource}/setIamPolicy
/// Sets the access control policy on the specified resource. Replaces any existing policy.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_images_set_iam_policy_execute()` to send, or `compute_images_set_iam_policy` for simplest API.

pub fn compute_images_set_iam_policy_builder(
    client: &SimpleHttpClient,
    project: &str,
    resource: &str,
    body: &GlobalSetPolicyRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/global/images/{}/setIamPolicy",
        project, resource,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET projects/{project}/global/images/{resource}/setIamPolicy
/// Sets the access control policy on the specified resource. Replaces any existing policy.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_images_set_iam_policy_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_images_set_iam_policy_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_images_set_iam_policy_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Policy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Policy = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/global/images/{resource}/setIamPolicy
/// Sets the access control policy on the specified resource. Replaces any existing policy.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_images_set_iam_policy_builder()` + `compute_images_set_iam_policy_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_images_set_iam_policy(
    client: &SimpleHttpClient,
    project: &str,
    resource: &str,
    body: &GlobalSetPolicyRequest,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Policy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_images_set_iam_policy_builder(client, project, resource, body)?;
    compute_images_set_iam_policy_execute(builder)
}

/// GET projects/{project}/global/images/{resource}/setLabels
/// Sets the labels on an image. To learn more about labels, read `theLabeling` Resources documentation.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_images_set_labels_execute()` to send, or `compute_images_set_labels` for simplest API.

pub fn compute_images_set_labels_builder(
    client: &SimpleHttpClient,
    project: &str,
    resource: &str,
    body: &GlobalSetLabelsRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/global/images/{}/setLabels",
        project, resource,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET projects/{project}/global/images/{resource}/setLabels
/// Sets the labels on an image. To learn more about labels, read `theLabeling` Resources documentation.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_images_set_labels_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_images_set_labels_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_images_set_labels_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/global/images/{resource}/setLabels
/// Sets the labels on an image. To learn more about labels, read `theLabeling` Resources documentation.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_images_set_labels_builder()` + `compute_images_set_labels_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_images_set_labels(
    client: &SimpleHttpClient,
    project: &str,
    resource: &str,
    body: &GlobalSetLabelsRequest,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_images_set_labels_builder(client, project, resource, body)?;
    compute_images_set_labels_execute(builder)
}

/// GET projects/{project}/global/images/{resource}/testIamPermissions
/// Returns permissions that a caller has on the specified resource.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_images_test_iam_permissions_execute()` to send, or `compute_images_test_iam_permissions` for simplest API.

pub fn compute_images_test_iam_permissions_builder(
    client: &SimpleHttpClient,
    project: &str,
    resource: &str,
    body: &TestPermissionsRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/global/images/{}/testIamPermissions",
        project, resource,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET projects/{project}/global/images/{resource}/testIamPermissions
/// Returns permissions that a caller has on the specified resource.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_images_test_iam_permissions_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_images_test_iam_permissions_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_images_test_iam_permissions_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<TestPermissionsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: TestPermissionsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/global/images/{resource}/testIamPermissions
/// Returns permissions that a caller has on the specified resource.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_images_test_iam_permissions_builder()` + `compute_images_test_iam_permissions_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_images_test_iam_permissions(
    client: &SimpleHttpClient,
    project: &str,
    resource: &str,
    body: &TestPermissionsRequest,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<TestPermissionsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = compute_images_test_iam_permissions_builder(client, project, resource, body)?;
    compute_images_test_iam_permissions_execute(builder)
}

/// GET projects/{project}/zones/{zone}/instanceGroupManagers/{instanceGroupManager}/resizeRequests/{resizeRequest}/cancel
/// Cancels the specified resize request and removes it from the queue. Cancelled resize request does no longer wait for the resources to be provisioned. Cancel is only possible for requests that are accepted in the queue.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_instance_group_manager_resize_requests_cancel_execute()` to send, or `compute_instance_group_manager_resize_requests_cancel` for simplest API.

pub fn compute_instance_group_manager_resize_requests_cancel_builder(
    client: &SimpleHttpClient,
    instanceGroupManager: &str,
    project: &str,
    resizeRequest: &str,
    zone: &str,
    requestId: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/zones/{}/instanceGroupManagers/{}/resizeRequests/{}/cancel",
        instanceGroupManager,
        project,
        resizeRequest,
        zone,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "requestId",
            requestId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET projects/{project}/zones/{zone}/instanceGroupManagers/{instanceGroupManager}/resizeRequests/{resizeRequest}/cancel
/// Cancels the specified resize request and removes it from the queue. Cancelled resize request does no longer wait for the resources to be provisioned. Cancel is only possible for requests that are accepted in the queue.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_instance_group_manager_resize_requests_cancel_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_instance_group_manager_resize_requests_cancel_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_instance_group_manager_resize_requests_cancel_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/zones/{zone}/instanceGroupManagers/{instanceGroupManager}/resizeRequests/{resizeRequest}/cancel
/// Cancels the specified resize request and removes it from the queue. Cancelled resize request does no longer wait for the resources to be provisioned. Cancel is only possible for requests that are accepted in the queue.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_instance_group_manager_resize_requests_cancel_builder()` + `compute_instance_group_manager_resize_requests_cancel_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_instance_group_manager_resize_requests_cancel(
    client: &SimpleHttpClient,
    instanceGroupManager: &str,
    project: &str,
    resizeRequest: &str,
    zone: &str,
    requestId: Option<&str>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_instance_group_manager_resize_requests_cancel_builder(
        client,
        instanceGroupManager,
        project,
        resizeRequest,
        zone,
        requestId,
    )?;
    compute_instance_group_manager_resize_requests_cancel_execute(builder)
}

/// GET projects/{project}/zones/{zone}/instanceGroupManagers/{instanceGroupManager}/resizeRequests/{resizeRequest}
/// Deletes the specified, inactive resize request. Requests that are still active cannot be deleted. Deleting request does not delete instances that were provisioned previously.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_instance_group_manager_resize_requests_delete_execute()` to send, or `compute_instance_group_manager_resize_requests_delete` for simplest API.

pub fn compute_instance_group_manager_resize_requests_delete_builder(
    client: &SimpleHttpClient,
    instanceGroupManager: &str,
    project: &str,
    resizeRequest: &str,
    zone: &str,
    requestId: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/zones/{}/instanceGroupManagers/{}/resizeRequests/{}",
        instanceGroupManager, project, resizeRequest, zone,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "requestId",
            requestId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET projects/{project}/zones/{zone}/instanceGroupManagers/{instanceGroupManager}/resizeRequests/{resizeRequest}
/// Deletes the specified, inactive resize request. Requests that are still active cannot be deleted. Deleting request does not delete instances that were provisioned previously.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_instance_group_manager_resize_requests_delete_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_instance_group_manager_resize_requests_delete_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_instance_group_manager_resize_requests_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/zones/{zone}/instanceGroupManagers/{instanceGroupManager}/resizeRequests/{resizeRequest}
/// Deletes the specified, inactive resize request. Requests that are still active cannot be deleted. Deleting request does not delete instances that were provisioned previously.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_instance_group_manager_resize_requests_delete_builder()` + `compute_instance_group_manager_resize_requests_delete_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_instance_group_manager_resize_requests_delete(
    client: &SimpleHttpClient,
    instanceGroupManager: &str,
    project: &str,
    resizeRequest: &str,
    zone: &str,
    requestId: Option<&str>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_instance_group_manager_resize_requests_delete_builder(
        client,
        instanceGroupManager,
        project,
        resizeRequest,
        zone,
        requestId,
    )?;
    compute_instance_group_manager_resize_requests_delete_execute(builder)
}

/// GET projects/{project}/zones/{zone}/instanceGroupManagers/{instanceGroupManager}/resizeRequests/{resizeRequest}
/// Returns all of the details about the specified resize request.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_instance_group_manager_resize_requests_get_execute()` to send, or `compute_instance_group_manager_resize_requests_get` for simplest API.

pub fn compute_instance_group_manager_resize_requests_get_builder(
    client: &SimpleHttpClient,
    instanceGroupManager: &str,
    project: &str,
    resizeRequest: &str,
    zone: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/zones/{}/instanceGroupManagers/{}/resizeRequests/{}",
        instanceGroupManager, project, resizeRequest, zone,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/zones/{zone}/instanceGroupManagers/{instanceGroupManager}/resizeRequests/{resizeRequest}
/// Returns all of the details about the specified resize request.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_instance_group_manager_resize_requests_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_instance_group_manager_resize_requests_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_instance_group_manager_resize_requests_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<InstanceGroupManagerResizeRequest>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: InstanceGroupManagerResizeRequest = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/zones/{zone}/instanceGroupManagers/{instanceGroupManager}/resizeRequests/{resizeRequest}
/// Returns all of the details about the specified resize request.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_instance_group_manager_resize_requests_get_builder()` + `compute_instance_group_manager_resize_requests_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_instance_group_manager_resize_requests_get(
    client: &SimpleHttpClient,
    instanceGroupManager: &str,
    project: &str,
    resizeRequest: &str,
    zone: &str,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<InstanceGroupManagerResizeRequest>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = compute_instance_group_manager_resize_requests_get_builder(
        client,
        instanceGroupManager,
        project,
        resizeRequest,
        zone,
    )?;
    compute_instance_group_manager_resize_requests_get_execute(builder)
}

/// GET projects/{project}/zones/{zone}/instanceGroupManagers/{instanceGroupManager}/resizeRequests
/// Creates a new resize request that starts provisioning VMs immediately or queues VM creation.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_instance_group_manager_resize_requests_insert_execute()` to send, or `compute_instance_group_manager_resize_requests_insert` for simplest API.

pub fn compute_instance_group_manager_resize_requests_insert_builder(
    client: &SimpleHttpClient,
    instanceGroupManager: &str,
    project: &str,
    zone: &str,
    requestId: Option<&str>,
    body: &InstanceGroupManagerResizeRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/zones/{}/instanceGroupManagers/{}/resizeRequests",
        instanceGroupManager, project, zone,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "requestId",
            requestId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET projects/{project}/zones/{zone}/instanceGroupManagers/{instanceGroupManager}/resizeRequests
/// Creates a new resize request that starts provisioning VMs immediately or queues VM creation.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_instance_group_manager_resize_requests_insert_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_instance_group_manager_resize_requests_insert_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_instance_group_manager_resize_requests_insert_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/zones/{zone}/instanceGroupManagers/{instanceGroupManager}/resizeRequests
/// Creates a new resize request that starts provisioning VMs immediately or queues VM creation.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_instance_group_manager_resize_requests_insert_builder()` + `compute_instance_group_manager_resize_requests_insert_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_instance_group_manager_resize_requests_insert(
    client: &SimpleHttpClient,
    instanceGroupManager: &str,
    project: &str,
    zone: &str,
    requestId: Option<&str>,
    body: &InstanceGroupManagerResizeRequest,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_instance_group_manager_resize_requests_insert_builder(
        client,
        instanceGroupManager,
        project,
        zone,
        requestId,
        body,
    )?;
    compute_instance_group_manager_resize_requests_insert_execute(builder)
}

/// GET projects/{project}/zones/{zone}/instanceGroupManagers/{instanceGroupManager}/resizeRequests
/// Retrieves a list of resize requests that are contained in the managed instance group.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_instance_group_manager_resize_requests_list_execute()` to send, or `compute_instance_group_manager_resize_requests_list` for simplest API.

pub fn compute_instance_group_manager_resize_requests_list_builder(
    client: &SimpleHttpClient,
    instanceGroupManager: &str,
    project: &str,
    zone: &str,
    filter: Option<&str>,
    maxResults: Option<i32>,
    orderBy: Option<&str>,
    pageToken: Option<&str>,
    returnPartialSuccess: Option<bool>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/zones/{}/instanceGroupManagers/{}/resizeRequests",
        instanceGroupManager, project, zone,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "filter",
            filter
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "maxResults",
            maxResults
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "orderBy",
            orderBy
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageToken",
            pageToken
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "returnPartialSuccess",
            returnPartialSuccess
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET projects/{project}/zones/{zone}/instanceGroupManagers/{instanceGroupManager}/resizeRequests
/// Retrieves a list of resize requests that are contained in the managed instance group.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_instance_group_manager_resize_requests_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_instance_group_manager_resize_requests_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_instance_group_manager_resize_requests_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<InstanceGroupManagerResizeRequestsListResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: InstanceGroupManagerResizeRequestsListResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/zones/{zone}/instanceGroupManagers/{instanceGroupManager}/resizeRequests
/// Retrieves a list of resize requests that are contained in the managed instance group.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_instance_group_manager_resize_requests_list_builder()` + `compute_instance_group_manager_resize_requests_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_instance_group_manager_resize_requests_list(
    client: &SimpleHttpClient,
    instanceGroupManager: &str,
    project: &str,
    zone: &str,
    filter: Option<&str>,
    maxResults: Option<i32>,
    orderBy: Option<&str>,
    pageToken: Option<&str>,
    returnPartialSuccess: Option<bool>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<InstanceGroupManagerResizeRequestsListResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = compute_instance_group_manager_resize_requests_list_builder(
        client,
        instanceGroupManager,
        project,
        zone,
        filter,
        maxResults,
        orderBy,
        pageToken,
        returnPartialSuccess,
    )?;
    compute_instance_group_manager_resize_requests_list_execute(builder)
}

/// GET projects/{project}/zones/{zone}/instanceGroupManagers/{instanceGroupManager}/abandonInstances
/// Flags the specified instances to be removed from the managed instance group. Abandoning an instance does not delete the instance, but it does remove the instance from any target pools that are applied by the managed instance group. This method reduces `thetargetSize` of the managed instance group by the number of instances that you abandon. This operation is marked `asDONE` when the action is scheduled even if the instances have not yet been removed from the group. You must separately verify the status of the abandoning action with thelistmanagedinstances method.  If the group is part of a backend service that has enabled connection draining, it can take up to 60 seconds after the connection draining duration has elapsed before the VM instance is removed or deleted.  You can specify a maximum of 1000 instances with this method per request.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_instance_group_managers_abandon_instances_execute()` to send, or `compute_instance_group_managers_abandon_instances` for simplest API.

pub fn compute_instance_group_managers_abandon_instances_builder(
    client: &SimpleHttpClient,
    instanceGroupManager: &str,
    project: &str,
    zone: &str,
    requestId: Option<&str>,
    body: &InstanceGroupManagersAbandonInstancesRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/zones/{}/instanceGroupManagers/{}/abandonInstances",
        instanceGroupManager, project, zone,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "requestId",
            requestId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET projects/{project}/zones/{zone}/instanceGroupManagers/{instanceGroupManager}/abandonInstances
/// Flags the specified instances to be removed from the managed instance group. Abandoning an instance does not delete the instance, but it does remove the instance from any target pools that are applied by the managed instance group. This method reduces `thetargetSize` of the managed instance group by the number of instances that you abandon. This operation is marked `asDONE` when the action is scheduled even if the instances have not yet been removed from the group. You must separately verify the status of the abandoning action with thelistmanagedinstances method.  If the group is part of a backend service that has enabled connection draining, it can take up to 60 seconds after the connection draining duration has elapsed before the VM instance is removed or deleted.  You can specify a maximum of 1000 instances with this method per request.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_instance_group_managers_abandon_instances_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_instance_group_managers_abandon_instances_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_instance_group_managers_abandon_instances_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/zones/{zone}/instanceGroupManagers/{instanceGroupManager}/abandonInstances
/// Flags the specified instances to be removed from the managed instance group. Abandoning an instance does not delete the instance, but it does remove the instance from any target pools that are applied by the managed instance group. This method reduces `thetargetSize` of the managed instance group by the number of instances that you abandon. This operation is marked `asDONE` when the action is scheduled even if the instances have not yet been removed from the group. You must separately verify the status of the abandoning action with thelistmanagedinstances method.  If the group is part of a backend service that has enabled connection draining, it can take up to 60 seconds after the connection draining duration has elapsed before the VM instance is removed or deleted.  You can specify a maximum of 1000 instances with this method per request.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_instance_group_managers_abandon_instances_builder()` + `compute_instance_group_managers_abandon_instances_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_instance_group_managers_abandon_instances(
    client: &SimpleHttpClient,
    instanceGroupManager: &str,
    project: &str,
    zone: &str,
    requestId: Option<&str>,
    body: &InstanceGroupManagersAbandonInstancesRequest,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_instance_group_managers_abandon_instances_builder(
        client,
        instanceGroupManager,
        project,
        zone,
        requestId,
        body,
    )?;
    compute_instance_group_managers_abandon_instances_execute(builder)
}

/// GET projects/{project}/aggregated/instanceGroupManagers
/// Retrieves the list of managed instance groups and groups them by zone.  To prevent failure, Google recommends that you set the `returnPartialSuccess` parameter to `true`.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_instance_group_managers_aggregated_list_execute()` to send, or `compute_instance_group_managers_aggregated_list` for simplest API.

pub fn compute_instance_group_managers_aggregated_list_builder(
    client: &SimpleHttpClient,
    project: &str,
    filter: Option<&str>,
    includeAllScopes: Option<bool>,
    maxResults: Option<i32>,
    orderBy: Option<&str>,
    pageToken: Option<&str>,
    returnPartialSuccess: Option<bool>,
    serviceProjectNumber: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/aggregated/instanceGroupManagers",
        project,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "filter",
            filter
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "includeAllScopes",
            includeAllScopes
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "maxResults",
            maxResults
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "orderBy",
            orderBy
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageToken",
            pageToken
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "returnPartialSuccess",
            returnPartialSuccess
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "serviceProjectNumber",
            serviceProjectNumber
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET projects/{project}/aggregated/instanceGroupManagers
/// Retrieves the list of managed instance groups and groups them by zone.  To prevent failure, Google recommends that you set the `returnPartialSuccess` parameter to `true`.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_instance_group_managers_aggregated_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_instance_group_managers_aggregated_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_instance_group_managers_aggregated_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<InstanceGroupManagerAggregatedList>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: InstanceGroupManagerAggregatedList = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/aggregated/instanceGroupManagers
/// Retrieves the list of managed instance groups and groups them by zone.  To prevent failure, Google recommends that you set the `returnPartialSuccess` parameter to `true`.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_instance_group_managers_aggregated_list_builder()` + `compute_instance_group_managers_aggregated_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_instance_group_managers_aggregated_list(
    client: &SimpleHttpClient,
    project: &str,
    filter: Option<&str>,
    includeAllScopes: Option<bool>,
    maxResults: Option<i32>,
    orderBy: Option<&str>,
    pageToken: Option<&str>,
    returnPartialSuccess: Option<bool>,
    serviceProjectNumber: Option<&str>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<InstanceGroupManagerAggregatedList>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = compute_instance_group_managers_aggregated_list_builder(
        client,
        project,
        filter,
        includeAllScopes,
        maxResults,
        orderBy,
        pageToken,
        returnPartialSuccess,
        serviceProjectNumber,
    )?;
    compute_instance_group_managers_aggregated_list_execute(builder)
}

/// GET projects/{project}/zones/{zone}/instanceGroupManagers/{instanceGroupManager}/applyUpdatesToInstances
/// Applies changes to selected instances on the managed instance group. This method can be used to apply new overrides `and/or` new versions.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_instance_group_managers_apply_updates_to_instances_execute()` to send, or `compute_instance_group_managers_apply_updates_to_instances` for simplest API.

pub fn compute_instance_group_managers_apply_updates_to_instances_builder(
    client: &SimpleHttpClient,
    instanceGroupManager: &str,
    project: &str,
    zone: &str,
    body: &InstanceGroupManagersApplyUpdatesRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/zones/{}/instanceGroupManagers/{}/applyUpdatesToInstances",
        instanceGroupManager,
        project,
        zone,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET projects/{project}/zones/{zone}/instanceGroupManagers/{instanceGroupManager}/applyUpdatesToInstances
/// Applies changes to selected instances on the managed instance group. This method can be used to apply new overrides `and/or` new versions.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_instance_group_managers_apply_updates_to_instances_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_instance_group_managers_apply_updates_to_instances_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_instance_group_managers_apply_updates_to_instances_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/zones/{zone}/instanceGroupManagers/{instanceGroupManager}/applyUpdatesToInstances
/// Applies changes to selected instances on the managed instance group. This method can be used to apply new overrides `and/or` new versions.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_instance_group_managers_apply_updates_to_instances_builder()` + `compute_instance_group_managers_apply_updates_to_instances_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_instance_group_managers_apply_updates_to_instances(
    client: &SimpleHttpClient,
    instanceGroupManager: &str,
    project: &str,
    zone: &str,
    body: &InstanceGroupManagersApplyUpdatesRequest,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_instance_group_managers_apply_updates_to_instances_builder(
        client,
        instanceGroupManager,
        project,
        zone,
        body,
    )?;
    compute_instance_group_managers_apply_updates_to_instances_execute(builder)
}

/// GET projects/{project}/zones/{zone}/instanceGroupManagers/{instanceGroupManager}/createInstances
/// Creates instances with per-instance configurations in this managed instance group. Instances are created using the current instance template. Thecreate instances operation is marked DONE if `thecreateInstances` request is successful. The underlying actions take additional time. You must separately verify the status of thecreating or actions with the listmanagedinstances method.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_instance_group_managers_create_instances_execute()` to send, or `compute_instance_group_managers_create_instances` for simplest API.

pub fn compute_instance_group_managers_create_instances_builder(
    client: &SimpleHttpClient,
    instanceGroupManager: &str,
    project: &str,
    zone: &str,
    requestId: Option<&str>,
    body: &InstanceGroupManagersCreateInstancesRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/zones/{}/instanceGroupManagers/{}/createInstances",
        instanceGroupManager, project, zone,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "requestId",
            requestId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET projects/{project}/zones/{zone}/instanceGroupManagers/{instanceGroupManager}/createInstances
/// Creates instances with per-instance configurations in this managed instance group. Instances are created using the current instance template. Thecreate instances operation is marked DONE if `thecreateInstances` request is successful. The underlying actions take additional time. You must separately verify the status of thecreating or actions with the listmanagedinstances method.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_instance_group_managers_create_instances_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_instance_group_managers_create_instances_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_instance_group_managers_create_instances_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/zones/{zone}/instanceGroupManagers/{instanceGroupManager}/createInstances
/// Creates instances with per-instance configurations in this managed instance group. Instances are created using the current instance template. Thecreate instances operation is marked DONE if `thecreateInstances` request is successful. The underlying actions take additional time. You must separately verify the status of thecreating or actions with the listmanagedinstances method.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_instance_group_managers_create_instances_builder()` + `compute_instance_group_managers_create_instances_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_instance_group_managers_create_instances(
    client: &SimpleHttpClient,
    instanceGroupManager: &str,
    project: &str,
    zone: &str,
    requestId: Option<&str>,
    body: &InstanceGroupManagersCreateInstancesRequest,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_instance_group_managers_create_instances_builder(
        client,
        instanceGroupManager,
        project,
        zone,
        requestId,
        body,
    )?;
    compute_instance_group_managers_create_instances_execute(builder)
}

/// GET projects/{project}/zones/{zone}/instanceGroupManagers/{instanceGroupManager}
/// Deletes the specified managed instance group and all of the instances in that group. Note that the instance group must not belong to a backend service. Read Deleting an instance group for more information.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_instance_group_managers_delete_execute()` to send, or `compute_instance_group_managers_delete` for simplest API.

pub fn compute_instance_group_managers_delete_builder(
    client: &SimpleHttpClient,
    instanceGroupManager: &str,
    project: &str,
    zone: &str,
    requestId: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/zones/{}/instanceGroupManagers/{}",
        instanceGroupManager, project, zone,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "requestId",
            requestId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET projects/{project}/zones/{zone}/instanceGroupManagers/{instanceGroupManager}
/// Deletes the specified managed instance group and all of the instances in that group. Note that the instance group must not belong to a backend service. Read Deleting an instance group for more information.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_instance_group_managers_delete_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_instance_group_managers_delete_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_instance_group_managers_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/zones/{zone}/instanceGroupManagers/{instanceGroupManager}
/// Deletes the specified managed instance group and all of the instances in that group. Note that the instance group must not belong to a backend service. Read Deleting an instance group for more information.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_instance_group_managers_delete_builder()` + `compute_instance_group_managers_delete_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_instance_group_managers_delete(
    client: &SimpleHttpClient,
    instanceGroupManager: &str,
    project: &str,
    zone: &str,
    requestId: Option<&str>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_instance_group_managers_delete_builder(
        client,
        instanceGroupManager,
        project,
        zone,
        requestId,
    )?;
    compute_instance_group_managers_delete_execute(builder)
}

/// GET projects/{project}/zones/{zone}/instanceGroupManagers/{instanceGroupManager}/deleteInstances
/// Flags the specified instances in the managed instance group for immediate deletion. The instances are also removed from any target pools of which they were a member. This method reduces `thetargetSize` of the managed instance group by the number of instances that you delete. This operation is marked as DONE when the action is scheduled even if the instances are still being deleted. You must separately verify the status of the deleting action with thelistmanagedinstances method.  If the group is part of a backend service that has enabled connection draining, it can take up to 60 seconds after the connection draining duration has elapsed before the VM instance is removed or deleted.  You can specify a maximum of 1000 instances with this method per request.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_instance_group_managers_delete_instances_execute()` to send, or `compute_instance_group_managers_delete_instances` for simplest API.

pub fn compute_instance_group_managers_delete_instances_builder(
    client: &SimpleHttpClient,
    instanceGroupManager: &str,
    project: &str,
    zone: &str,
    requestId: Option<&str>,
    body: &InstanceGroupManagersDeleteInstancesRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/zones/{}/instanceGroupManagers/{}/deleteInstances",
        instanceGroupManager, project, zone,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "requestId",
            requestId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET projects/{project}/zones/{zone}/instanceGroupManagers/{instanceGroupManager}/deleteInstances
/// Flags the specified instances in the managed instance group for immediate deletion. The instances are also removed from any target pools of which they were a member. This method reduces `thetargetSize` of the managed instance group by the number of instances that you delete. This operation is marked as DONE when the action is scheduled even if the instances are still being deleted. You must separately verify the status of the deleting action with thelistmanagedinstances method.  If the group is part of a backend service that has enabled connection draining, it can take up to 60 seconds after the connection draining duration has elapsed before the VM instance is removed or deleted.  You can specify a maximum of 1000 instances with this method per request.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_instance_group_managers_delete_instances_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_instance_group_managers_delete_instances_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_instance_group_managers_delete_instances_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/zones/{zone}/instanceGroupManagers/{instanceGroupManager}/deleteInstances
/// Flags the specified instances in the managed instance group for immediate deletion. The instances are also removed from any target pools of which they were a member. This method reduces `thetargetSize` of the managed instance group by the number of instances that you delete. This operation is marked as DONE when the action is scheduled even if the instances are still being deleted. You must separately verify the status of the deleting action with thelistmanagedinstances method.  If the group is part of a backend service that has enabled connection draining, it can take up to 60 seconds after the connection draining duration has elapsed before the VM instance is removed or deleted.  You can specify a maximum of 1000 instances with this method per request.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_instance_group_managers_delete_instances_builder()` + `compute_instance_group_managers_delete_instances_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_instance_group_managers_delete_instances(
    client: &SimpleHttpClient,
    instanceGroupManager: &str,
    project: &str,
    zone: &str,
    requestId: Option<&str>,
    body: &InstanceGroupManagersDeleteInstancesRequest,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_instance_group_managers_delete_instances_builder(
        client,
        instanceGroupManager,
        project,
        zone,
        requestId,
        body,
    )?;
    compute_instance_group_managers_delete_instances_execute(builder)
}

/// GET projects/{project}/zones/{zone}/instanceGroupManagers/{instanceGroupManager}/deletePerInstanceConfigs
/// Deletes selected per-instance configurations for the managed instance group.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_instance_group_managers_delete_per_instance_configs_execute()` to send, or `compute_instance_group_managers_delete_per_instance_configs` for simplest API.

pub fn compute_instance_group_managers_delete_per_instance_configs_builder(
    client: &SimpleHttpClient,
    instanceGroupManager: &str,
    project: &str,
    zone: &str,
    body: &InstanceGroupManagersDeletePerInstanceConfigsReq,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/zones/{}/instanceGroupManagers/{}/deletePerInstanceConfigs",
        instanceGroupManager,
        project,
        zone,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET projects/{project}/zones/{zone}/instanceGroupManagers/{instanceGroupManager}/deletePerInstanceConfigs
/// Deletes selected per-instance configurations for the managed instance group.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_instance_group_managers_delete_per_instance_configs_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_instance_group_managers_delete_per_instance_configs_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_instance_group_managers_delete_per_instance_configs_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/zones/{zone}/instanceGroupManagers/{instanceGroupManager}/deletePerInstanceConfigs
/// Deletes selected per-instance configurations for the managed instance group.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_instance_group_managers_delete_per_instance_configs_builder()` + `compute_instance_group_managers_delete_per_instance_configs_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_instance_group_managers_delete_per_instance_configs(
    client: &SimpleHttpClient,
    instanceGroupManager: &str,
    project: &str,
    zone: &str,
    body: &InstanceGroupManagersDeletePerInstanceConfigsReq,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_instance_group_managers_delete_per_instance_configs_builder(
        client,
        instanceGroupManager,
        project,
        zone,
        body,
    )?;
    compute_instance_group_managers_delete_per_instance_configs_execute(builder)
}

/// GET projects/{project}/zones/{zone}/instanceGroupManagers/{instanceGroupManager}
/// Returns all of the details about the specified managed instance group.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_instance_group_managers_get_execute()` to send, or `compute_instance_group_managers_get` for simplest API.

pub fn compute_instance_group_managers_get_builder(
    client: &SimpleHttpClient,
    instanceGroupManager: &str,
    project: &str,
    zone: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/zones/{}/instanceGroupManagers/{}",
        instanceGroupManager, project, zone,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/zones/{zone}/instanceGroupManagers/{instanceGroupManager}
/// Returns all of the details about the specified managed instance group.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_instance_group_managers_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_instance_group_managers_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_instance_group_managers_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<InstanceGroupManager>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: InstanceGroupManager = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/zones/{zone}/instanceGroupManagers/{instanceGroupManager}
/// Returns all of the details about the specified managed instance group.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_instance_group_managers_get_builder()` + `compute_instance_group_managers_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_instance_group_managers_get(
    client: &SimpleHttpClient,
    instanceGroupManager: &str,
    project: &str,
    zone: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<InstanceGroupManager>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        compute_instance_group_managers_get_builder(client, instanceGroupManager, project, zone)?;
    compute_instance_group_managers_get_execute(builder)
}

/// GET projects/{project}/zones/{zone}/instanceGroupManagers
/// Creates a managed instance group using the information that you specify in the request. After the group is created, instances in the group are created using the specified instance template. This operation is marked as DONE when the group is created even if the instances in the group have not yet been created. You must separately verify the status of the individual instances with thelistmanagedinstances method.  A managed instance group can have up to 1000 VM instances per group. Please contact Cloud Support if you need an increase in this limit.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_instance_group_managers_insert_execute()` to send, or `compute_instance_group_managers_insert` for simplest API.

pub fn compute_instance_group_managers_insert_builder(
    client: &SimpleHttpClient,
    project: &str,
    zone: &str,
    requestId: Option<&str>,
    body: &InstanceGroupManager,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/zones/{}/instanceGroupManagers",
        project, zone,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "requestId",
            requestId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET projects/{project}/zones/{zone}/instanceGroupManagers
/// Creates a managed instance group using the information that you specify in the request. After the group is created, instances in the group are created using the specified instance template. This operation is marked as DONE when the group is created even if the instances in the group have not yet been created. You must separately verify the status of the individual instances with thelistmanagedinstances method.  A managed instance group can have up to 1000 VM instances per group. Please contact Cloud Support if you need an increase in this limit.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_instance_group_managers_insert_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_instance_group_managers_insert_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_instance_group_managers_insert_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/zones/{zone}/instanceGroupManagers
/// Creates a managed instance group using the information that you specify in the request. After the group is created, instances in the group are created using the specified instance template. This operation is marked as DONE when the group is created even if the instances in the group have not yet been created. You must separately verify the status of the individual instances with thelistmanagedinstances method.  A managed instance group can have up to 1000 VM instances per group. Please contact Cloud Support if you need an increase in this limit.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_instance_group_managers_insert_builder()` + `compute_instance_group_managers_insert_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_instance_group_managers_insert(
    client: &SimpleHttpClient,
    project: &str,
    zone: &str,
    requestId: Option<&str>,
    body: &InstanceGroupManager,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        compute_instance_group_managers_insert_builder(client, project, zone, requestId, body)?;
    compute_instance_group_managers_insert_execute(builder)
}

/// GET projects/{project}/zones/{zone}/instanceGroupManagers
/// Retrieves a list of managed instance groups that are contained within the specified project and zone.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_instance_group_managers_list_execute()` to send, or `compute_instance_group_managers_list` for simplest API.

pub fn compute_instance_group_managers_list_builder(
    client: &SimpleHttpClient,
    project: &str,
    zone: &str,
    filter: Option<&str>,
    maxResults: Option<i32>,
    orderBy: Option<&str>,
    pageToken: Option<&str>,
    returnPartialSuccess: Option<bool>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/zones/{}/instanceGroupManagers",
        project, zone,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "filter",
            filter
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "maxResults",
            maxResults
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "orderBy",
            orderBy
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageToken",
            pageToken
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "returnPartialSuccess",
            returnPartialSuccess
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET projects/{project}/zones/{zone}/instanceGroupManagers
/// Retrieves a list of managed instance groups that are contained within the specified project and zone.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_instance_group_managers_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_instance_group_managers_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_instance_group_managers_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<InstanceGroupManagerList>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: InstanceGroupManagerList = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/zones/{zone}/instanceGroupManagers
/// Retrieves a list of managed instance groups that are contained within the specified project and zone.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_instance_group_managers_list_builder()` + `compute_instance_group_managers_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_instance_group_managers_list(
    client: &SimpleHttpClient,
    project: &str,
    zone: &str,
    filter: Option<&str>,
    maxResults: Option<i32>,
    orderBy: Option<&str>,
    pageToken: Option<&str>,
    returnPartialSuccess: Option<bool>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<InstanceGroupManagerList>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = compute_instance_group_managers_list_builder(
        client,
        project,
        zone,
        filter,
        maxResults,
        orderBy,
        pageToken,
        returnPartialSuccess,
    )?;
    compute_instance_group_managers_list_execute(builder)
}

/// GET projects/{project}/zones/{zone}/instanceGroupManagers/{instanceGroupManager}/listErrors
/// Lists all errors thrown by actions on instances for a given managed instance group. The filter and `orderBy` query parameters are not supported.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_instance_group_managers_list_errors_execute()` to send, or `compute_instance_group_managers_list_errors` for simplest API.

pub fn compute_instance_group_managers_list_errors_builder(
    client: &SimpleHttpClient,
    instanceGroupManager: &str,
    project: &str,
    zone: &str,
    filter: Option<&str>,
    maxResults: Option<i32>,
    orderBy: Option<&str>,
    pageToken: Option<&str>,
    returnPartialSuccess: Option<bool>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/zones/{}/instanceGroupManagers/{}/listErrors",
        instanceGroupManager, project, zone,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "filter",
            filter
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "maxResults",
            maxResults
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "orderBy",
            orderBy
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageToken",
            pageToken
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "returnPartialSuccess",
            returnPartialSuccess
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET projects/{project}/zones/{zone}/instanceGroupManagers/{instanceGroupManager}/listErrors
/// Lists all errors thrown by actions on instances for a given managed instance group. The filter and `orderBy` query parameters are not supported.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_instance_group_managers_list_errors_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_instance_group_managers_list_errors_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_instance_group_managers_list_errors_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<InstanceGroupManagersListErrorsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: InstanceGroupManagersListErrorsResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/zones/{zone}/instanceGroupManagers/{instanceGroupManager}/listErrors
/// Lists all errors thrown by actions on instances for a given managed instance group. The filter and `orderBy` query parameters are not supported.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_instance_group_managers_list_errors_builder()` + `compute_instance_group_managers_list_errors_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_instance_group_managers_list_errors(
    client: &SimpleHttpClient,
    instanceGroupManager: &str,
    project: &str,
    zone: &str,
    filter: Option<&str>,
    maxResults: Option<i32>,
    orderBy: Option<&str>,
    pageToken: Option<&str>,
    returnPartialSuccess: Option<bool>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<InstanceGroupManagersListErrorsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = compute_instance_group_managers_list_errors_builder(
        client,
        instanceGroupManager,
        project,
        zone,
        filter,
        maxResults,
        orderBy,
        pageToken,
        returnPartialSuccess,
    )?;
    compute_instance_group_managers_list_errors_execute(builder)
}

/// GET projects/{project}/zones/{zone}/instanceGroupManagers/{instanceGroupManager}/listManagedInstances
/// Lists all of the instances in the managed instance group. Each instance in the list has a `currentAction`, which indicates the action that the managed instance group is performing on the instance. For example, if the group is still creating an instance, the `currentAction` is CREATING. If a previous action failed, the list displays the errors for that failed action. The `orderBy` query parameter is not supported. The `pageToken` query parameter is supported only if the group's `listManagedInstancesResults` field is set to PAGINATED.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_instance_group_managers_list_managed_instances_execute()` to send, or `compute_instance_group_managers_list_managed_instances` for simplest API.

pub fn compute_instance_group_managers_list_managed_instances_builder(
    client: &SimpleHttpClient,
    instanceGroupManager: &str,
    project: &str,
    zone: &str,
    filter: Option<&str>,
    maxResults: Option<i32>,
    orderBy: Option<&str>,
    pageToken: Option<&str>,
    returnPartialSuccess: Option<bool>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/zones/{}/instanceGroupManagers/{}/listManagedInstances",
        instanceGroupManager, project, zone,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "filter",
            filter
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "maxResults",
            maxResults
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "orderBy",
            orderBy
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageToken",
            pageToken
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "returnPartialSuccess",
            returnPartialSuccess
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET projects/{project}/zones/{zone}/instanceGroupManagers/{instanceGroupManager}/listManagedInstances
/// Lists all of the instances in the managed instance group. Each instance in the list has a `currentAction`, which indicates the action that the managed instance group is performing on the instance. For example, if the group is still creating an instance, the `currentAction` is CREATING. If a previous action failed, the list displays the errors for that failed action. The `orderBy` query parameter is not supported. The `pageToken` query parameter is supported only if the group's `listManagedInstancesResults` field is set to PAGINATED.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_instance_group_managers_list_managed_instances_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_instance_group_managers_list_managed_instances_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_instance_group_managers_list_managed_instances_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<InstanceGroupManagersListManagedInstancesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: InstanceGroupManagersListManagedInstancesResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/zones/{zone}/instanceGroupManagers/{instanceGroupManager}/listManagedInstances
/// Lists all of the instances in the managed instance group. Each instance in the list has a `currentAction`, which indicates the action that the managed instance group is performing on the instance. For example, if the group is still creating an instance, the `currentAction` is CREATING. If a previous action failed, the list displays the errors for that failed action. The `orderBy` query parameter is not supported. The `pageToken` query parameter is supported only if the group's `listManagedInstancesResults` field is set to PAGINATED.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_instance_group_managers_list_managed_instances_builder()` + `compute_instance_group_managers_list_managed_instances_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_instance_group_managers_list_managed_instances(
    client: &SimpleHttpClient,
    instanceGroupManager: &str,
    project: &str,
    zone: &str,
    filter: Option<&str>,
    maxResults: Option<i32>,
    orderBy: Option<&str>,
    pageToken: Option<&str>,
    returnPartialSuccess: Option<bool>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<InstanceGroupManagersListManagedInstancesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = compute_instance_group_managers_list_managed_instances_builder(
        client,
        instanceGroupManager,
        project,
        zone,
        filter,
        maxResults,
        orderBy,
        pageToken,
        returnPartialSuccess,
    )?;
    compute_instance_group_managers_list_managed_instances_execute(builder)
}

/// GET projects/{project}/zones/{zone}/instanceGroupManagers/{instanceGroupManager}/listPerInstanceConfigs
/// Lists all of the per-instance configurations defined for the managed instance group. The `orderBy` query parameter is not supported.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_instance_group_managers_list_per_instance_configs_execute()` to send, or `compute_instance_group_managers_list_per_instance_configs` for simplest API.

pub fn compute_instance_group_managers_list_per_instance_configs_builder(
    client: &SimpleHttpClient,
    instanceGroupManager: &str,
    project: &str,
    zone: &str,
    filter: Option<&str>,
    maxResults: Option<i32>,
    orderBy: Option<&str>,
    pageToken: Option<&str>,
    returnPartialSuccess: Option<bool>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/zones/{}/instanceGroupManagers/{}/listPerInstanceConfigs",
        instanceGroupManager,
        project,
        zone,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "filter",
            filter
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "maxResults",
            maxResults
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "orderBy",
            orderBy
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageToken",
            pageToken
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "returnPartialSuccess",
            returnPartialSuccess
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET projects/{project}/zones/{zone}/instanceGroupManagers/{instanceGroupManager}/listPerInstanceConfigs
/// Lists all of the per-instance configurations defined for the managed instance group. The `orderBy` query parameter is not supported.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_instance_group_managers_list_per_instance_configs_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_instance_group_managers_list_per_instance_configs_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_instance_group_managers_list_per_instance_configs_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<InstanceGroupManagersListPerInstanceConfigsResp>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: InstanceGroupManagersListPerInstanceConfigsResp =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/zones/{zone}/instanceGroupManagers/{instanceGroupManager}/listPerInstanceConfigs
/// Lists all of the per-instance configurations defined for the managed instance group. The `orderBy` query parameter is not supported.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_instance_group_managers_list_per_instance_configs_builder()` + `compute_instance_group_managers_list_per_instance_configs_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_instance_group_managers_list_per_instance_configs(
    client: &SimpleHttpClient,
    instanceGroupManager: &str,
    project: &str,
    zone: &str,
    filter: Option<&str>,
    maxResults: Option<i32>,
    orderBy: Option<&str>,
    pageToken: Option<&str>,
    returnPartialSuccess: Option<bool>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<InstanceGroupManagersListPerInstanceConfigsResp>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = compute_instance_group_managers_list_per_instance_configs_builder(
        client,
        instanceGroupManager,
        project,
        zone,
        filter,
        maxResults,
        orderBy,
        pageToken,
        returnPartialSuccess,
    )?;
    compute_instance_group_managers_list_per_instance_configs_execute(builder)
}

/// GET projects/{project}/zones/{zone}/instanceGroupManagers/{instanceGroupManager}
/// Updates a managed instance group using the information that you specify in the request. This operation is marked as DONE when the group is patched even if the instances in the group are still in the process of being patched. You must separately verify the status of the individual instances with `thelistManagedInstances` method. This method `supportsPATCH` semantics and uses `theJSON` merge patch format and processing rules.  If you update your group to specify a new template or instance configuration, it's possible that your intended specification for each VM in the group is different from the current state of that VM. To learn how to apply an updated configuration to the VMs in a MIG, `seeUpdating` instances in a MIG.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_instance_group_managers_patch_execute()` to send, or `compute_instance_group_managers_patch` for simplest API.

pub fn compute_instance_group_managers_patch_builder(
    client: &SimpleHttpClient,
    instanceGroupManager: &str,
    project: &str,
    zone: &str,
    requestId: Option<&str>,
    body: &InstanceGroupManager,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/zones/{}/instanceGroupManagers/{}",
        instanceGroupManager, project, zone,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "requestId",
            requestId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET projects/{project}/zones/{zone}/instanceGroupManagers/{instanceGroupManager}
/// Updates a managed instance group using the information that you specify in the request. This operation is marked as DONE when the group is patched even if the instances in the group are still in the process of being patched. You must separately verify the status of the individual instances with `thelistManagedInstances` method. This method `supportsPATCH` semantics and uses `theJSON` merge patch format and processing rules.  If you update your group to specify a new template or instance configuration, it's possible that your intended specification for each VM in the group is different from the current state of that VM. To learn how to apply an updated configuration to the VMs in a MIG, `seeUpdating` instances in a MIG.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_instance_group_managers_patch_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_instance_group_managers_patch_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_instance_group_managers_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/zones/{zone}/instanceGroupManagers/{instanceGroupManager}
/// Updates a managed instance group using the information that you specify in the request. This operation is marked as DONE when the group is patched even if the instances in the group are still in the process of being patched. You must separately verify the status of the individual instances with `thelistManagedInstances` method. This method `supportsPATCH` semantics and uses `theJSON` merge patch format and processing rules.  If you update your group to specify a new template or instance configuration, it's possible that your intended specification for each VM in the group is different from the current state of that VM. To learn how to apply an updated configuration to the VMs in a MIG, `seeUpdating` instances in a MIG.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_instance_group_managers_patch_builder()` + `compute_instance_group_managers_patch_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_instance_group_managers_patch(
    client: &SimpleHttpClient,
    instanceGroupManager: &str,
    project: &str,
    zone: &str,
    requestId: Option<&str>,
    body: &InstanceGroupManager,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_instance_group_managers_patch_builder(
        client,
        instanceGroupManager,
        project,
        zone,
        requestId,
        body,
    )?;
    compute_instance_group_managers_patch_execute(builder)
}

/// GET projects/{project}/zones/{zone}/instanceGroupManagers/{instanceGroupManager}/patchPerInstanceConfigs
/// Inserts or patches per-instance configurations for the managed instance group. `perInstanceConfig`.name serves as a key used to distinguish whether to perform insert or patch.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_instance_group_managers_patch_per_instance_configs_execute()` to send, or `compute_instance_group_managers_patch_per_instance_configs` for simplest API.

pub fn compute_instance_group_managers_patch_per_instance_configs_builder(
    client: &SimpleHttpClient,
    instanceGroupManager: &str,
    project: &str,
    zone: &str,
    requestId: Option<&str>,
    body: &InstanceGroupManagersPatchPerInstanceConfigsReq,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/zones/{}/instanceGroupManagers/{}/patchPerInstanceConfigs",
        instanceGroupManager,
        project,
        zone,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "requestId",
            requestId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET projects/{project}/zones/{zone}/instanceGroupManagers/{instanceGroupManager}/patchPerInstanceConfigs
/// Inserts or patches per-instance configurations for the managed instance group. `perInstanceConfig`.name serves as a key used to distinguish whether to perform insert or patch.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_instance_group_managers_patch_per_instance_configs_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_instance_group_managers_patch_per_instance_configs_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_instance_group_managers_patch_per_instance_configs_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/zones/{zone}/instanceGroupManagers/{instanceGroupManager}/patchPerInstanceConfigs
/// Inserts or patches per-instance configurations for the managed instance group. `perInstanceConfig`.name serves as a key used to distinguish whether to perform insert or patch.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_instance_group_managers_patch_per_instance_configs_builder()` + `compute_instance_group_managers_patch_per_instance_configs_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_instance_group_managers_patch_per_instance_configs(
    client: &SimpleHttpClient,
    instanceGroupManager: &str,
    project: &str,
    zone: &str,
    requestId: Option<&str>,
    body: &InstanceGroupManagersPatchPerInstanceConfigsReq,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_instance_group_managers_patch_per_instance_configs_builder(
        client,
        instanceGroupManager,
        project,
        zone,
        requestId,
        body,
    )?;
    compute_instance_group_managers_patch_per_instance_configs_execute(builder)
}

/// GET projects/{project}/zones/{zone}/instanceGroupManagers/{instanceGroupManager}/recreateInstances
/// Flags the specified VM instances in the managed instance group to be immediately recreated. Each instance is recreated using the group's current configuration. This operation is marked as DONE when the flag is set even if the instances have not yet been recreated. You must separately verify the status of each instance by checking `itscurrentAction` field; for more information, see Checking the status of managed instances.  If the group is part of a backend service that has enabled connection draining, it can take up to 60 seconds after the connection draining duration has elapsed before the VM instance is removed or deleted.  You can specify a maximum of 1000 instances with this method per request.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_instance_group_managers_recreate_instances_execute()` to send, or `compute_instance_group_managers_recreate_instances` for simplest API.

pub fn compute_instance_group_managers_recreate_instances_builder(
    client: &SimpleHttpClient,
    instanceGroupManager: &str,
    project: &str,
    zone: &str,
    requestId: Option<&str>,
    body: &InstanceGroupManagersRecreateInstancesRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/zones/{}/instanceGroupManagers/{}/recreateInstances",
        instanceGroupManager, project, zone,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "requestId",
            requestId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET projects/{project}/zones/{zone}/instanceGroupManagers/{instanceGroupManager}/recreateInstances
/// Flags the specified VM instances in the managed instance group to be immediately recreated. Each instance is recreated using the group's current configuration. This operation is marked as DONE when the flag is set even if the instances have not yet been recreated. You must separately verify the status of each instance by checking `itscurrentAction` field; for more information, see Checking the status of managed instances.  If the group is part of a backend service that has enabled connection draining, it can take up to 60 seconds after the connection draining duration has elapsed before the VM instance is removed or deleted.  You can specify a maximum of 1000 instances with this method per request.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_instance_group_managers_recreate_instances_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_instance_group_managers_recreate_instances_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_instance_group_managers_recreate_instances_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/zones/{zone}/instanceGroupManagers/{instanceGroupManager}/recreateInstances
/// Flags the specified VM instances in the managed instance group to be immediately recreated. Each instance is recreated using the group's current configuration. This operation is marked as DONE when the flag is set even if the instances have not yet been recreated. You must separately verify the status of each instance by checking `itscurrentAction` field; for more information, see Checking the status of managed instances.  If the group is part of a backend service that has enabled connection draining, it can take up to 60 seconds after the connection draining duration has elapsed before the VM instance is removed or deleted.  You can specify a maximum of 1000 instances with this method per request.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_instance_group_managers_recreate_instances_builder()` + `compute_instance_group_managers_recreate_instances_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_instance_group_managers_recreate_instances(
    client: &SimpleHttpClient,
    instanceGroupManager: &str,
    project: &str,
    zone: &str,
    requestId: Option<&str>,
    body: &InstanceGroupManagersRecreateInstancesRequest,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_instance_group_managers_recreate_instances_builder(
        client,
        instanceGroupManager,
        project,
        zone,
        requestId,
        body,
    )?;
    compute_instance_group_managers_recreate_instances_execute(builder)
}

/// GET projects/{project}/zones/{zone}/instanceGroupManagers/{instanceGroupManager}/resize
/// Resizes the managed instance group. If you increase the size, the group creates new instances using the current instance template. If you decrease the size, the group deletes instances. The resize operation is `markedDONE` when the resize actions are scheduled even if the group has not yet added or deleted any instances. You must separately verify the status of the creating or deleting actions with thelistmanagedinstances method.  When resizing down, the instance group arbitrarily chooses the order in which VMs are deleted. The group takes into account some VM attributes when making the selection including:  + The status of the VM instance. + The health of the VM instance. + The instance template version the VM is based on. + For regional managed instance groups, the location of the VM instance.  This list is subject to change.  If the group is part of a backend service that has enabled connection draining, it can take up to 60 seconds after the connection draining duration has elapsed before the VM instance is removed or deleted.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_instance_group_managers_resize_execute()` to send, or `compute_instance_group_managers_resize` for simplest API.

pub fn compute_instance_group_managers_resize_builder(
    client: &SimpleHttpClient,
    instanceGroupManager: &str,
    project: &str,
    zone: &str,
    requestId: Option<&str>,
    size: Option<i32>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/zones/{}/instanceGroupManagers/{}/resize",
        instanceGroupManager, project, zone,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "requestId",
            requestId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "size",
            size.as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET projects/{project}/zones/{zone}/instanceGroupManagers/{instanceGroupManager}/resize
/// Resizes the managed instance group. If you increase the size, the group creates new instances using the current instance template. If you decrease the size, the group deletes instances. The resize operation is `markedDONE` when the resize actions are scheduled even if the group has not yet added or deleted any instances. You must separately verify the status of the creating or deleting actions with thelistmanagedinstances method.  When resizing down, the instance group arbitrarily chooses the order in which VMs are deleted. The group takes into account some VM attributes when making the selection including:  + The status of the VM instance. + The health of the VM instance. + The instance template version the VM is based on. + For regional managed instance groups, the location of the VM instance.  This list is subject to change.  If the group is part of a backend service that has enabled connection draining, it can take up to 60 seconds after the connection draining duration has elapsed before the VM instance is removed or deleted.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_instance_group_managers_resize_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_instance_group_managers_resize_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_instance_group_managers_resize_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/zones/{zone}/instanceGroupManagers/{instanceGroupManager}/resize
/// Resizes the managed instance group. If you increase the size, the group creates new instances using the current instance template. If you decrease the size, the group deletes instances. The resize operation is `markedDONE` when the resize actions are scheduled even if the group has not yet added or deleted any instances. You must separately verify the status of the creating or deleting actions with thelistmanagedinstances method.  When resizing down, the instance group arbitrarily chooses the order in which VMs are deleted. The group takes into account some VM attributes when making the selection including:  + The status of the VM instance. + The health of the VM instance. + The instance template version the VM is based on. + For regional managed instance groups, the location of the VM instance.  This list is subject to change.  If the group is part of a backend service that has enabled connection draining, it can take up to 60 seconds after the connection draining duration has elapsed before the VM instance is removed or deleted.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_instance_group_managers_resize_builder()` + `compute_instance_group_managers_resize_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_instance_group_managers_resize(
    client: &SimpleHttpClient,
    instanceGroupManager: &str,
    project: &str,
    zone: &str,
    requestId: Option<&str>,
    size: Option<i32>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_instance_group_managers_resize_builder(
        client,
        instanceGroupManager,
        project,
        zone,
        requestId,
        size,
    )?;
    compute_instance_group_managers_resize_execute(builder)
}

/// GET projects/{project}/zones/{zone}/instanceGroupManagers/{instanceGroupManager}/resumeInstances
/// Flags the specified instances in the managed instance group to be resumed. This method increases `thetargetSize` and decreases the `targetSuspendedSize` of the managed instance group by the number of instances that you resume. The `resumeInstances` operation is marked DONE if the `resumeInstances` request is successful. The underlying actions take additional time. You must separately verify the status of `theRESUMING` action with thelistmanagedinstances method.  In this request, you can only specify instances that are suspended. For example, if an instance was previously suspended using the `suspendInstances` method, it can be resumed using the `resumeInstances` method.  If a health check is attached to the managed instance group, the specified instances will be verified as healthy after they are resumed.  You can specify a maximum of 1000 instances with this method per request.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_instance_group_managers_resume_instances_execute()` to send, or `compute_instance_group_managers_resume_instances` for simplest API.

pub fn compute_instance_group_managers_resume_instances_builder(
    client: &SimpleHttpClient,
    instanceGroupManager: &str,
    project: &str,
    zone: &str,
    requestId: Option<&str>,
    body: &InstanceGroupManagersResumeInstancesRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/zones/{}/instanceGroupManagers/{}/resumeInstances",
        instanceGroupManager, project, zone,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "requestId",
            requestId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET projects/{project}/zones/{zone}/instanceGroupManagers/{instanceGroupManager}/resumeInstances
/// Flags the specified instances in the managed instance group to be resumed. This method increases `thetargetSize` and decreases the `targetSuspendedSize` of the managed instance group by the number of instances that you resume. The `resumeInstances` operation is marked DONE if the `resumeInstances` request is successful. The underlying actions take additional time. You must separately verify the status of `theRESUMING` action with thelistmanagedinstances method.  In this request, you can only specify instances that are suspended. For example, if an instance was previously suspended using the `suspendInstances` method, it can be resumed using the `resumeInstances` method.  If a health check is attached to the managed instance group, the specified instances will be verified as healthy after they are resumed.  You can specify a maximum of 1000 instances with this method per request.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_instance_group_managers_resume_instances_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_instance_group_managers_resume_instances_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_instance_group_managers_resume_instances_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/zones/{zone}/instanceGroupManagers/{instanceGroupManager}/resumeInstances
/// Flags the specified instances in the managed instance group to be resumed. This method increases `thetargetSize` and decreases the `targetSuspendedSize` of the managed instance group by the number of instances that you resume. The `resumeInstances` operation is marked DONE if the `resumeInstances` request is successful. The underlying actions take additional time. You must separately verify the status of `theRESUMING` action with thelistmanagedinstances method.  In this request, you can only specify instances that are suspended. For example, if an instance was previously suspended using the `suspendInstances` method, it can be resumed using the `resumeInstances` method.  If a health check is attached to the managed instance group, the specified instances will be verified as healthy after they are resumed.  You can specify a maximum of 1000 instances with this method per request.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_instance_group_managers_resume_instances_builder()` + `compute_instance_group_managers_resume_instances_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_instance_group_managers_resume_instances(
    client: &SimpleHttpClient,
    instanceGroupManager: &str,
    project: &str,
    zone: &str,
    requestId: Option<&str>,
    body: &InstanceGroupManagersResumeInstancesRequest,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_instance_group_managers_resume_instances_builder(
        client,
        instanceGroupManager,
        project,
        zone,
        requestId,
        body,
    )?;
    compute_instance_group_managers_resume_instances_execute(builder)
}

/// GET projects/{project}/zones/{zone}/instanceGroupManagers/{instanceGroupManager}/setInstanceTemplate
/// Specifies the instance template to use when creating new instances in this group. The templates for existing instances in the group do not change unless you run `recreateInstances`, `runapplyUpdatesToInstances`, or set the group'`supdatePolicy`.type to PROACTIVE.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_instance_group_managers_set_instance_template_execute()` to send, or `compute_instance_group_managers_set_instance_template` for simplest API.

pub fn compute_instance_group_managers_set_instance_template_builder(
    client: &SimpleHttpClient,
    instanceGroupManager: &str,
    project: &str,
    zone: &str,
    requestId: Option<&str>,
    body: &InstanceGroupManagersSetInstanceTemplateRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/zones/{}/instanceGroupManagers/{}/setInstanceTemplate",
        instanceGroupManager, project, zone,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "requestId",
            requestId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET projects/{project}/zones/{zone}/instanceGroupManagers/{instanceGroupManager}/setInstanceTemplate
/// Specifies the instance template to use when creating new instances in this group. The templates for existing instances in the group do not change unless you run `recreateInstances`, `runapplyUpdatesToInstances`, or set the group'`supdatePolicy`.type to PROACTIVE.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_instance_group_managers_set_instance_template_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_instance_group_managers_set_instance_template_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_instance_group_managers_set_instance_template_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/zones/{zone}/instanceGroupManagers/{instanceGroupManager}/setInstanceTemplate
/// Specifies the instance template to use when creating new instances in this group. The templates for existing instances in the group do not change unless you run `recreateInstances`, `runapplyUpdatesToInstances`, or set the group'`supdatePolicy`.type to PROACTIVE.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_instance_group_managers_set_instance_template_builder()` + `compute_instance_group_managers_set_instance_template_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_instance_group_managers_set_instance_template(
    client: &SimpleHttpClient,
    instanceGroupManager: &str,
    project: &str,
    zone: &str,
    requestId: Option<&str>,
    body: &InstanceGroupManagersSetInstanceTemplateRequest,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_instance_group_managers_set_instance_template_builder(
        client,
        instanceGroupManager,
        project,
        zone,
        requestId,
        body,
    )?;
    compute_instance_group_managers_set_instance_template_execute(builder)
}

/// GET projects/{project}/zones/{zone}/instanceGroupManagers/{instanceGroupManager}/setTargetPools
/// Modifies the target pools to which all instances in this managed instance group are assigned. The target pools automatically apply to all of the instances in the managed instance group. This operation is `markedDONE` when you make the request even if the instances have not yet been added to their target pools. The change might take some time to apply to all of the instances in the group depending on the size of the group.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_instance_group_managers_set_target_pools_execute()` to send, or `compute_instance_group_managers_set_target_pools` for simplest API.

pub fn compute_instance_group_managers_set_target_pools_builder(
    client: &SimpleHttpClient,
    instanceGroupManager: &str,
    project: &str,
    zone: &str,
    requestId: Option<&str>,
    body: &InstanceGroupManagersSetTargetPoolsRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/zones/{}/instanceGroupManagers/{}/setTargetPools",
        instanceGroupManager, project, zone,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "requestId",
            requestId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET projects/{project}/zones/{zone}/instanceGroupManagers/{instanceGroupManager}/setTargetPools
/// Modifies the target pools to which all instances in this managed instance group are assigned. The target pools automatically apply to all of the instances in the managed instance group. This operation is `markedDONE` when you make the request even if the instances have not yet been added to their target pools. The change might take some time to apply to all of the instances in the group depending on the size of the group.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_instance_group_managers_set_target_pools_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_instance_group_managers_set_target_pools_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_instance_group_managers_set_target_pools_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/zones/{zone}/instanceGroupManagers/{instanceGroupManager}/setTargetPools
/// Modifies the target pools to which all instances in this managed instance group are assigned. The target pools automatically apply to all of the instances in the managed instance group. This operation is `markedDONE` when you make the request even if the instances have not yet been added to their target pools. The change might take some time to apply to all of the instances in the group depending on the size of the group.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_instance_group_managers_set_target_pools_builder()` + `compute_instance_group_managers_set_target_pools_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_instance_group_managers_set_target_pools(
    client: &SimpleHttpClient,
    instanceGroupManager: &str,
    project: &str,
    zone: &str,
    requestId: Option<&str>,
    body: &InstanceGroupManagersSetTargetPoolsRequest,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_instance_group_managers_set_target_pools_builder(
        client,
        instanceGroupManager,
        project,
        zone,
        requestId,
        body,
    )?;
    compute_instance_group_managers_set_target_pools_execute(builder)
}

/// GET projects/{project}/zones/{zone}/instanceGroupManagers/{instanceGroupManager}/startInstances
/// Flags the specified instances in the managed instance group to be started. This method increases `thetargetSize` and decreases the `targetStoppedSize` of the managed instance group by the number of instances that you start. The `startInstances` operation is marked DONE if the `startInstances` request is successful. The underlying actions take additional time. You must separately verify the status of `theSTARTING` action with thelistmanagedinstances method.  In this request, you can only specify instances that are stopped. For example, if an instance was previously stopped using the `stopInstances` method, it can be started using the `startInstances` method.  If a health check is attached to the managed instance group, the specified instances will be verified as healthy after they are started.  You can specify a maximum of 1000 instances with this method per request.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_instance_group_managers_start_instances_execute()` to send, or `compute_instance_group_managers_start_instances` for simplest API.

pub fn compute_instance_group_managers_start_instances_builder(
    client: &SimpleHttpClient,
    instanceGroupManager: &str,
    project: &str,
    zone: &str,
    requestId: Option<&str>,
    body: &InstanceGroupManagersStartInstancesRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/zones/{}/instanceGroupManagers/{}/startInstances",
        instanceGroupManager, project, zone,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "requestId",
            requestId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET projects/{project}/zones/{zone}/instanceGroupManagers/{instanceGroupManager}/startInstances
/// Flags the specified instances in the managed instance group to be started. This method increases `thetargetSize` and decreases the `targetStoppedSize` of the managed instance group by the number of instances that you start. The `startInstances` operation is marked DONE if the `startInstances` request is successful. The underlying actions take additional time. You must separately verify the status of `theSTARTING` action with thelistmanagedinstances method.  In this request, you can only specify instances that are stopped. For example, if an instance was previously stopped using the `stopInstances` method, it can be started using the `startInstances` method.  If a health check is attached to the managed instance group, the specified instances will be verified as healthy after they are started.  You can specify a maximum of 1000 instances with this method per request.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_instance_group_managers_start_instances_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_instance_group_managers_start_instances_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_instance_group_managers_start_instances_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/zones/{zone}/instanceGroupManagers/{instanceGroupManager}/startInstances
/// Flags the specified instances in the managed instance group to be started. This method increases `thetargetSize` and decreases the `targetStoppedSize` of the managed instance group by the number of instances that you start. The `startInstances` operation is marked DONE if the `startInstances` request is successful. The underlying actions take additional time. You must separately verify the status of `theSTARTING` action with thelistmanagedinstances method.  In this request, you can only specify instances that are stopped. For example, if an instance was previously stopped using the `stopInstances` method, it can be started using the `startInstances` method.  If a health check is attached to the managed instance group, the specified instances will be verified as healthy after they are started.  You can specify a maximum of 1000 instances with this method per request.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_instance_group_managers_start_instances_builder()` + `compute_instance_group_managers_start_instances_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_instance_group_managers_start_instances(
    client: &SimpleHttpClient,
    instanceGroupManager: &str,
    project: &str,
    zone: &str,
    requestId: Option<&str>,
    body: &InstanceGroupManagersStartInstancesRequest,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_instance_group_managers_start_instances_builder(
        client,
        instanceGroupManager,
        project,
        zone,
        requestId,
        body,
    )?;
    compute_instance_group_managers_start_instances_execute(builder)
}

/// GET projects/{project}/zones/{zone}/instanceGroupManagers/{instanceGroupManager}/stopInstances
/// Flags the specified instances in the managed instance group to be immediately stopped. You can only specify instances that are running in this request. This method reduces `thetargetSize` and increases the `targetStoppedSize` of the managed instance group by the number of instances that you stop. The `stopInstances` operation is marked DONE if the `stopInstances` request is successful. The underlying actions take additional time. You must separately verify the status of `theSTOPPING` action with thelistmanagedinstances method.  If the `standbyPolicy`.`initialDelaySec` field is set, the group delays stopping the instances until `initialDelaySec` have passed from instance.`creationTimestamp` (that is, when the instance was created). This delay gives your application time to set itself up and initialize on the instance. If more `thaninitialDelaySec` seconds have passed sinceinstance.`creationTimestamp` when this method is called, there will be zero delay.  If the group is part of a backend service that has enabled connection draining, it can take up to 60 seconds after the connection draining duration has elapsed before the VM instance is stopped.  Stopped instances can be started using the `startInstances` method.  You can specify a maximum of 1000 instances with this method per request.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_instance_group_managers_stop_instances_execute()` to send, or `compute_instance_group_managers_stop_instances` for simplest API.

pub fn compute_instance_group_managers_stop_instances_builder(
    client: &SimpleHttpClient,
    instanceGroupManager: &str,
    project: &str,
    zone: &str,
    requestId: Option<&str>,
    body: &InstanceGroupManagersStopInstancesRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/zones/{}/instanceGroupManagers/{}/stopInstances",
        instanceGroupManager, project, zone,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "requestId",
            requestId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET projects/{project}/zones/{zone}/instanceGroupManagers/{instanceGroupManager}/stopInstances
/// Flags the specified instances in the managed instance group to be immediately stopped. You can only specify instances that are running in this request. This method reduces `thetargetSize` and increases the `targetStoppedSize` of the managed instance group by the number of instances that you stop. The `stopInstances` operation is marked DONE if the `stopInstances` request is successful. The underlying actions take additional time. You must separately verify the status of `theSTOPPING` action with thelistmanagedinstances method.  If the `standbyPolicy`.`initialDelaySec` field is set, the group delays stopping the instances until `initialDelaySec` have passed from instance.`creationTimestamp` (that is, when the instance was created). This delay gives your application time to set itself up and initialize on the instance. If more `thaninitialDelaySec` seconds have passed sinceinstance.`creationTimestamp` when this method is called, there will be zero delay.  If the group is part of a backend service that has enabled connection draining, it can take up to 60 seconds after the connection draining duration has elapsed before the VM instance is stopped.  Stopped instances can be started using the `startInstances` method.  You can specify a maximum of 1000 instances with this method per request.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_instance_group_managers_stop_instances_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_instance_group_managers_stop_instances_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_instance_group_managers_stop_instances_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/zones/{zone}/instanceGroupManagers/{instanceGroupManager}/stopInstances
/// Flags the specified instances in the managed instance group to be immediately stopped. You can only specify instances that are running in this request. This method reduces `thetargetSize` and increases the `targetStoppedSize` of the managed instance group by the number of instances that you stop. The `stopInstances` operation is marked DONE if the `stopInstances` request is successful. The underlying actions take additional time. You must separately verify the status of `theSTOPPING` action with thelistmanagedinstances method.  If the `standbyPolicy`.`initialDelaySec` field is set, the group delays stopping the instances until `initialDelaySec` have passed from instance.`creationTimestamp` (that is, when the instance was created). This delay gives your application time to set itself up and initialize on the instance. If more `thaninitialDelaySec` seconds have passed sinceinstance.`creationTimestamp` when this method is called, there will be zero delay.  If the group is part of a backend service that has enabled connection draining, it can take up to 60 seconds after the connection draining duration has elapsed before the VM instance is stopped.  Stopped instances can be started using the `startInstances` method.  You can specify a maximum of 1000 instances with this method per request.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_instance_group_managers_stop_instances_builder()` + `compute_instance_group_managers_stop_instances_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_instance_group_managers_stop_instances(
    client: &SimpleHttpClient,
    instanceGroupManager: &str,
    project: &str,
    zone: &str,
    requestId: Option<&str>,
    body: &InstanceGroupManagersStopInstancesRequest,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_instance_group_managers_stop_instances_builder(
        client,
        instanceGroupManager,
        project,
        zone,
        requestId,
        body,
    )?;
    compute_instance_group_managers_stop_instances_execute(builder)
}

/// GET projects/{project}/zones/{zone}/instanceGroupManagers/{instanceGroupManager}/suspendInstances
/// Flags the specified instances in the managed instance group to be immediately suspended. You can only specify instances that are running in this request. This method reduces `thetargetSize` and increases the `targetSuspendedSize` of the managed instance group by the number of instances that you suspend. The `suspendInstances` operation is marked DONE if the `suspendInstances` request is successful. The underlying actions take additional time. You must separately verify the status of `theSUSPENDING` action with thelistmanagedinstances method.  If the `standbyPolicy`.`initialDelaySec` field is set, the group delays suspension of the instances until `initialDelaySec` have passed from instance.`creationTimestamp` (that is, when the instance was created). This delay gives your application time to set itself up and initialize on the instance. If more `thaninitialDelaySec` seconds have passed sinceinstance.`creationTimestamp` when this method is called, there will be zero delay.  If the group is part of a backend service that has enabled connection draining, it can take up to 60 seconds after the connection draining duration has elapsed before the VM instance is suspended.  Suspended instances can be resumed using the `resumeInstances` method.  You can specify a maximum of 1000 instances with this method per request.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_instance_group_managers_suspend_instances_execute()` to send, or `compute_instance_group_managers_suspend_instances` for simplest API.

pub fn compute_instance_group_managers_suspend_instances_builder(
    client: &SimpleHttpClient,
    instanceGroupManager: &str,
    project: &str,
    zone: &str,
    requestId: Option<&str>,
    body: &InstanceGroupManagersSuspendInstancesRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/zones/{}/instanceGroupManagers/{}/suspendInstances",
        instanceGroupManager, project, zone,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "requestId",
            requestId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET projects/{project}/zones/{zone}/instanceGroupManagers/{instanceGroupManager}/suspendInstances
/// Flags the specified instances in the managed instance group to be immediately suspended. You can only specify instances that are running in this request. This method reduces `thetargetSize` and increases the `targetSuspendedSize` of the managed instance group by the number of instances that you suspend. The `suspendInstances` operation is marked DONE if the `suspendInstances` request is successful. The underlying actions take additional time. You must separately verify the status of `theSUSPENDING` action with thelistmanagedinstances method.  If the `standbyPolicy`.`initialDelaySec` field is set, the group delays suspension of the instances until `initialDelaySec` have passed from instance.`creationTimestamp` (that is, when the instance was created). This delay gives your application time to set itself up and initialize on the instance. If more `thaninitialDelaySec` seconds have passed sinceinstance.`creationTimestamp` when this method is called, there will be zero delay.  If the group is part of a backend service that has enabled connection draining, it can take up to 60 seconds after the connection draining duration has elapsed before the VM instance is suspended.  Suspended instances can be resumed using the `resumeInstances` method.  You can specify a maximum of 1000 instances with this method per request.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_instance_group_managers_suspend_instances_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_instance_group_managers_suspend_instances_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_instance_group_managers_suspend_instances_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/zones/{zone}/instanceGroupManagers/{instanceGroupManager}/suspendInstances
/// Flags the specified instances in the managed instance group to be immediately suspended. You can only specify instances that are running in this request. This method reduces `thetargetSize` and increases the `targetSuspendedSize` of the managed instance group by the number of instances that you suspend. The `suspendInstances` operation is marked DONE if the `suspendInstances` request is successful. The underlying actions take additional time. You must separately verify the status of `theSUSPENDING` action with thelistmanagedinstances method.  If the `standbyPolicy`.`initialDelaySec` field is set, the group delays suspension of the instances until `initialDelaySec` have passed from instance.`creationTimestamp` (that is, when the instance was created). This delay gives your application time to set itself up and initialize on the instance. If more `thaninitialDelaySec` seconds have passed sinceinstance.`creationTimestamp` when this method is called, there will be zero delay.  If the group is part of a backend service that has enabled connection draining, it can take up to 60 seconds after the connection draining duration has elapsed before the VM instance is suspended.  Suspended instances can be resumed using the `resumeInstances` method.  You can specify a maximum of 1000 instances with this method per request.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_instance_group_managers_suspend_instances_builder()` + `compute_instance_group_managers_suspend_instances_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_instance_group_managers_suspend_instances(
    client: &SimpleHttpClient,
    instanceGroupManager: &str,
    project: &str,
    zone: &str,
    requestId: Option<&str>,
    body: &InstanceGroupManagersSuspendInstancesRequest,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_instance_group_managers_suspend_instances_builder(
        client,
        instanceGroupManager,
        project,
        zone,
        requestId,
        body,
    )?;
    compute_instance_group_managers_suspend_instances_execute(builder)
}

/// GET projects/{project}/zones/{zone}/instanceGroupManagers/{instanceGroupManager}/updatePerInstanceConfigs
/// Inserts or updates per-instance configurations for the managed instance group. `perInstanceConfig`.name serves as a key used to distinguish whether to perform insert or patch.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_instance_group_managers_update_per_instance_configs_execute()` to send, or `compute_instance_group_managers_update_per_instance_configs` for simplest API.

pub fn compute_instance_group_managers_update_per_instance_configs_builder(
    client: &SimpleHttpClient,
    instanceGroupManager: &str,
    project: &str,
    zone: &str,
    requestId: Option<&str>,
    body: &InstanceGroupManagersUpdatePerInstanceConfigsReq,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/zones/{}/instanceGroupManagers/{}/updatePerInstanceConfigs",
        instanceGroupManager,
        project,
        zone,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "requestId",
            requestId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET projects/{project}/zones/{zone}/instanceGroupManagers/{instanceGroupManager}/updatePerInstanceConfigs
/// Inserts or updates per-instance configurations for the managed instance group. `perInstanceConfig`.name serves as a key used to distinguish whether to perform insert or patch.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_instance_group_managers_update_per_instance_configs_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_instance_group_managers_update_per_instance_configs_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_instance_group_managers_update_per_instance_configs_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/zones/{zone}/instanceGroupManagers/{instanceGroupManager}/updatePerInstanceConfigs
/// Inserts or updates per-instance configurations for the managed instance group. `perInstanceConfig`.name serves as a key used to distinguish whether to perform insert or patch.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_instance_group_managers_update_per_instance_configs_builder()` + `compute_instance_group_managers_update_per_instance_configs_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_instance_group_managers_update_per_instance_configs(
    client: &SimpleHttpClient,
    instanceGroupManager: &str,
    project: &str,
    zone: &str,
    requestId: Option<&str>,
    body: &InstanceGroupManagersUpdatePerInstanceConfigsReq,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_instance_group_managers_update_per_instance_configs_builder(
        client,
        instanceGroupManager,
        project,
        zone,
        requestId,
        body,
    )?;
    compute_instance_group_managers_update_per_instance_configs_execute(builder)
}

/// GET projects/{project}/zones/{zone}/instanceGroups/{instanceGroup}/addInstances
/// Adds a list of instances to the specified instance group.  All of the instances in the instance group must be in the same `network/subnetwork`. Read Adding instances for more information.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_instance_groups_add_instances_execute()` to send, or `compute_instance_groups_add_instances` for simplest API.

pub fn compute_instance_groups_add_instances_builder(
    client: &SimpleHttpClient,
    instanceGroup: &str,
    project: &str,
    zone: &str,
    requestId: Option<&str>,
    body: &InstanceGroupsAddInstancesRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/zones/{}/instanceGroups/{}/addInstances",
        instanceGroup, project, zone,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "requestId",
            requestId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET projects/{project}/zones/{zone}/instanceGroups/{instanceGroup}/addInstances
/// Adds a list of instances to the specified instance group.  All of the instances in the instance group must be in the same `network/subnetwork`. Read Adding instances for more information.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_instance_groups_add_instances_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_instance_groups_add_instances_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_instance_groups_add_instances_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/zones/{zone}/instanceGroups/{instanceGroup}/addInstances
/// Adds a list of instances to the specified instance group.  All of the instances in the instance group must be in the same `network/subnetwork`. Read Adding instances for more information.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_instance_groups_add_instances_builder()` + `compute_instance_groups_add_instances_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_instance_groups_add_instances(
    client: &SimpleHttpClient,
    instanceGroup: &str,
    project: &str,
    zone: &str,
    requestId: Option<&str>,
    body: &InstanceGroupsAddInstancesRequest,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_instance_groups_add_instances_builder(
        client,
        instanceGroup,
        project,
        zone,
        requestId,
        body,
    )?;
    compute_instance_groups_add_instances_execute(builder)
}

/// GET projects/{project}/aggregated/instanceGroups
/// Retrieves the list of instance groups and sorts them by zone.  To prevent failure, Google recommends that you set the `returnPartialSuccess` parameter to `true`.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_instance_groups_aggregated_list_execute()` to send, or `compute_instance_groups_aggregated_list` for simplest API.

pub fn compute_instance_groups_aggregated_list_builder(
    client: &SimpleHttpClient,
    project: &str,
    filter: Option<&str>,
    includeAllScopes: Option<bool>,
    maxResults: Option<i32>,
    orderBy: Option<&str>,
    pageToken: Option<&str>,
    returnPartialSuccess: Option<bool>,
    serviceProjectNumber: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/aggregated/instanceGroups",
        project,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "filter",
            filter
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "includeAllScopes",
            includeAllScopes
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "maxResults",
            maxResults
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "orderBy",
            orderBy
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageToken",
            pageToken
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "returnPartialSuccess",
            returnPartialSuccess
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "serviceProjectNumber",
            serviceProjectNumber
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET projects/{project}/aggregated/instanceGroups
/// Retrieves the list of instance groups and sorts them by zone.  To prevent failure, Google recommends that you set the `returnPartialSuccess` parameter to `true`.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_instance_groups_aggregated_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_instance_groups_aggregated_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_instance_groups_aggregated_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<InstanceGroupAggregatedList>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: InstanceGroupAggregatedList = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/aggregated/instanceGroups
/// Retrieves the list of instance groups and sorts them by zone.  To prevent failure, Google recommends that you set the `returnPartialSuccess` parameter to `true`.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_instance_groups_aggregated_list_builder()` + `compute_instance_groups_aggregated_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_instance_groups_aggregated_list(
    client: &SimpleHttpClient,
    project: &str,
    filter: Option<&str>,
    includeAllScopes: Option<bool>,
    maxResults: Option<i32>,
    orderBy: Option<&str>,
    pageToken: Option<&str>,
    returnPartialSuccess: Option<bool>,
    serviceProjectNumber: Option<&str>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<InstanceGroupAggregatedList>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = compute_instance_groups_aggregated_list_builder(
        client,
        project,
        filter,
        includeAllScopes,
        maxResults,
        orderBy,
        pageToken,
        returnPartialSuccess,
        serviceProjectNumber,
    )?;
    compute_instance_groups_aggregated_list_execute(builder)
}

/// GET projects/{project}/zones/{zone}/instanceGroups/{instanceGroup}
/// Deletes the specified instance group. The instances in the group are not deleted. Note that instance group must not belong to a backend service. Read Deleting an instance group for more information.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_instance_groups_delete_execute()` to send, or `compute_instance_groups_delete` for simplest API.

pub fn compute_instance_groups_delete_builder(
    client: &SimpleHttpClient,
    instanceGroup: &str,
    project: &str,
    zone: &str,
    requestId: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/zones/{}/instanceGroups/{}",
        instanceGroup, project, zone,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "requestId",
            requestId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET projects/{project}/zones/{zone}/instanceGroups/{instanceGroup}
/// Deletes the specified instance group. The instances in the group are not deleted. Note that instance group must not belong to a backend service. Read Deleting an instance group for more information.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_instance_groups_delete_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_instance_groups_delete_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_instance_groups_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/zones/{zone}/instanceGroups/{instanceGroup}
/// Deletes the specified instance group. The instances in the group are not deleted. Note that instance group must not belong to a backend service. Read Deleting an instance group for more information.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_instance_groups_delete_builder()` + `compute_instance_groups_delete_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_instance_groups_delete(
    client: &SimpleHttpClient,
    instanceGroup: &str,
    project: &str,
    zone: &str,
    requestId: Option<&str>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        compute_instance_groups_delete_builder(client, instanceGroup, project, zone, requestId)?;
    compute_instance_groups_delete_execute(builder)
}

/// GET projects/{project}/zones/{zone}/instanceGroups/{instanceGroup}
/// Returns the specified zonal instance group. Get a list of available zonal instance groups by making a list() request.  For managed instance groups, use `theinstanceGroupManagers` or `regionInstanceGroupManagers` methods instead.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_instance_groups_get_execute()` to send, or `compute_instance_groups_get` for simplest API.

pub fn compute_instance_groups_get_builder(
    client: &SimpleHttpClient,
    instanceGroup: &str,
    project: &str,
    zone: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/zones/{}/instanceGroups/{}",
        instanceGroup, project, zone,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/zones/{zone}/instanceGroups/{instanceGroup}
/// Returns the specified zonal instance group. Get a list of available zonal instance groups by making a list() request.  For managed instance groups, use `theinstanceGroupManagers` or `regionInstanceGroupManagers` methods instead.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_instance_groups_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_instance_groups_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_instance_groups_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<InstanceGroup>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: InstanceGroup = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/zones/{zone}/instanceGroups/{instanceGroup}
/// Returns the specified zonal instance group. Get a list of available zonal instance groups by making a list() request.  For managed instance groups, use `theinstanceGroupManagers` or `regionInstanceGroupManagers` methods instead.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_instance_groups_get_builder()` + `compute_instance_groups_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_instance_groups_get(
    client: &SimpleHttpClient,
    instanceGroup: &str,
    project: &str,
    zone: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<InstanceGroup>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = compute_instance_groups_get_builder(client, instanceGroup, project, zone)?;
    compute_instance_groups_get_execute(builder)
}

/// GET projects/{project}/zones/{zone}/instanceGroups
/// Creates an instance group in the specified project using the parameters that are included in the request.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_instance_groups_insert_execute()` to send, or `compute_instance_groups_insert` for simplest API.

pub fn compute_instance_groups_insert_builder(
    client: &SimpleHttpClient,
    project: &str,
    zone: &str,
    requestId: Option<&str>,
    body: &InstanceGroup,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/zones/{}/instanceGroups",
        project, zone,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "requestId",
            requestId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET projects/{project}/zones/{zone}/instanceGroups
/// Creates an instance group in the specified project using the parameters that are included in the request.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_instance_groups_insert_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_instance_groups_insert_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_instance_groups_insert_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/zones/{zone}/instanceGroups
/// Creates an instance group in the specified project using the parameters that are included in the request.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_instance_groups_insert_builder()` + `compute_instance_groups_insert_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_instance_groups_insert(
    client: &SimpleHttpClient,
    project: &str,
    zone: &str,
    requestId: Option<&str>,
    body: &InstanceGroup,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_instance_groups_insert_builder(client, project, zone, requestId, body)?;
    compute_instance_groups_insert_execute(builder)
}

/// GET projects/{project}/zones/{zone}/instanceGroups
/// Retrieves the list of zonal instance group resources contained within the specified zone.  For managed instance groups, use `theinstanceGroupManagers` or `regionInstanceGroupManagers` methods instead.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_instance_groups_list_execute()` to send, or `compute_instance_groups_list` for simplest API.

pub fn compute_instance_groups_list_builder(
    client: &SimpleHttpClient,
    project: &str,
    zone: &str,
    filter: Option<&str>,
    maxResults: Option<i32>,
    orderBy: Option<&str>,
    pageToken: Option<&str>,
    returnPartialSuccess: Option<bool>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/zones/{}/instanceGroups",
        project, zone,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "filter",
            filter
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "maxResults",
            maxResults
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "orderBy",
            orderBy
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageToken",
            pageToken
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "returnPartialSuccess",
            returnPartialSuccess
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET projects/{project}/zones/{zone}/instanceGroups
/// Retrieves the list of zonal instance group resources contained within the specified zone.  For managed instance groups, use `theinstanceGroupManagers` or `regionInstanceGroupManagers` methods instead.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_instance_groups_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_instance_groups_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_instance_groups_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<InstanceGroupList>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: InstanceGroupList = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/zones/{zone}/instanceGroups
/// Retrieves the list of zonal instance group resources contained within the specified zone.  For managed instance groups, use `theinstanceGroupManagers` or `regionInstanceGroupManagers` methods instead.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_instance_groups_list_builder()` + `compute_instance_groups_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_instance_groups_list(
    client: &SimpleHttpClient,
    project: &str,
    zone: &str,
    filter: Option<&str>,
    maxResults: Option<i32>,
    orderBy: Option<&str>,
    pageToken: Option<&str>,
    returnPartialSuccess: Option<bool>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<InstanceGroupList>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = compute_instance_groups_list_builder(
        client,
        project,
        zone,
        filter,
        maxResults,
        orderBy,
        pageToken,
        returnPartialSuccess,
    )?;
    compute_instance_groups_list_execute(builder)
}

/// GET projects/{project}/zones/{zone}/instanceGroups/{instanceGroup}/listInstances
/// Lists the instances in the specified instance group. The `orderBy` query parameter is not supported. The filter query parameter is supported, but only for expressions that use eq (equal) or ne (not equal) operators.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_instance_groups_list_instances_execute()` to send, or `compute_instance_groups_list_instances` for simplest API.

pub fn compute_instance_groups_list_instances_builder(
    client: &SimpleHttpClient,
    instanceGroup: &str,
    project: &str,
    zone: &str,
    filter: Option<&str>,
    maxResults: Option<i32>,
    orderBy: Option<&str>,
    pageToken: Option<&str>,
    returnPartialSuccess: Option<bool>,
    body: &InstanceGroupsListInstancesRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/zones/{}/instanceGroups/{}/listInstances",
        instanceGroup, project, zone,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "filter",
            filter
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "maxResults",
            maxResults
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "orderBy",
            orderBy
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageToken",
            pageToken
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "returnPartialSuccess",
            returnPartialSuccess
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET projects/{project}/zones/{zone}/instanceGroups/{instanceGroup}/listInstances
/// Lists the instances in the specified instance group. The `orderBy` query parameter is not supported. The filter query parameter is supported, but only for expressions that use eq (equal) or ne (not equal) operators.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_instance_groups_list_instances_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_instance_groups_list_instances_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_instance_groups_list_instances_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<InstanceGroupsListInstances>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: InstanceGroupsListInstances = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/zones/{zone}/instanceGroups/{instanceGroup}/listInstances
/// Lists the instances in the specified instance group. The `orderBy` query parameter is not supported. The filter query parameter is supported, but only for expressions that use eq (equal) or ne (not equal) operators.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_instance_groups_list_instances_builder()` + `compute_instance_groups_list_instances_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_instance_groups_list_instances(
    client: &SimpleHttpClient,
    instanceGroup: &str,
    project: &str,
    zone: &str,
    filter: Option<&str>,
    maxResults: Option<i32>,
    orderBy: Option<&str>,
    pageToken: Option<&str>,
    returnPartialSuccess: Option<bool>,
    body: &InstanceGroupsListInstancesRequest,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<InstanceGroupsListInstances>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = compute_instance_groups_list_instances_builder(
        client,
        instanceGroup,
        project,
        zone,
        filter,
        maxResults,
        orderBy,
        pageToken,
        returnPartialSuccess,
        body,
    )?;
    compute_instance_groups_list_instances_execute(builder)
}

/// GET projects/{project}/zones/{zone}/instanceGroups/{instanceGroup}/removeInstances
/// Removes one or more instances from the specified instance group, but does not delete those instances.  If the group is part of a backend service that has enabled connection draining, it can take up to 60 seconds after the connection draining duration before the VM instance is removed or deleted.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_instance_groups_remove_instances_execute()` to send, or `compute_instance_groups_remove_instances` for simplest API.

pub fn compute_instance_groups_remove_instances_builder(
    client: &SimpleHttpClient,
    instanceGroup: &str,
    project: &str,
    zone: &str,
    requestId: Option<&str>,
    body: &InstanceGroupsRemoveInstancesRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/zones/{}/instanceGroups/{}/removeInstances",
        instanceGroup, project, zone,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "requestId",
            requestId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET projects/{project}/zones/{zone}/instanceGroups/{instanceGroup}/removeInstances
/// Removes one or more instances from the specified instance group, but does not delete those instances.  If the group is part of a backend service that has enabled connection draining, it can take up to 60 seconds after the connection draining duration before the VM instance is removed or deleted.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_instance_groups_remove_instances_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_instance_groups_remove_instances_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_instance_groups_remove_instances_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/zones/{zone}/instanceGroups/{instanceGroup}/removeInstances
/// Removes one or more instances from the specified instance group, but does not delete those instances.  If the group is part of a backend service that has enabled connection draining, it can take up to 60 seconds after the connection draining duration before the VM instance is removed or deleted.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_instance_groups_remove_instances_builder()` + `compute_instance_groups_remove_instances_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_instance_groups_remove_instances(
    client: &SimpleHttpClient,
    instanceGroup: &str,
    project: &str,
    zone: &str,
    requestId: Option<&str>,
    body: &InstanceGroupsRemoveInstancesRequest,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_instance_groups_remove_instances_builder(
        client,
        instanceGroup,
        project,
        zone,
        requestId,
        body,
    )?;
    compute_instance_groups_remove_instances_execute(builder)
}

/// GET projects/{project}/zones/{zone}/instanceGroups/{instanceGroup}/setNamedPorts
/// Sets the named ports for the specified instance group.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_instance_groups_set_named_ports_execute()` to send, or `compute_instance_groups_set_named_ports` for simplest API.

pub fn compute_instance_groups_set_named_ports_builder(
    client: &SimpleHttpClient,
    instanceGroup: &str,
    project: &str,
    zone: &str,
    requestId: Option<&str>,
    body: &InstanceGroupsSetNamedPortsRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/zones/{}/instanceGroups/{}/setNamedPorts",
        instanceGroup, project, zone,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "requestId",
            requestId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET projects/{project}/zones/{zone}/instanceGroups/{instanceGroup}/setNamedPorts
/// Sets the named ports for the specified instance group.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_instance_groups_set_named_ports_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_instance_groups_set_named_ports_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_instance_groups_set_named_ports_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/zones/{zone}/instanceGroups/{instanceGroup}/setNamedPorts
/// Sets the named ports for the specified instance group.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_instance_groups_set_named_ports_builder()` + `compute_instance_groups_set_named_ports_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_instance_groups_set_named_ports(
    client: &SimpleHttpClient,
    instanceGroup: &str,
    project: &str,
    zone: &str,
    requestId: Option<&str>,
    body: &InstanceGroupsSetNamedPortsRequest,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_instance_groups_set_named_ports_builder(
        client,
        instanceGroup,
        project,
        zone,
        requestId,
        body,
    )?;
    compute_instance_groups_set_named_ports_execute(builder)
}

/// GET projects/{project}/zones/{zone}/instanceGroups/{resource}/testIamPermissions
/// Returns permissions that a caller has on the specified resource.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_instance_groups_test_iam_permissions_execute()` to send, or `compute_instance_groups_test_iam_permissions` for simplest API.

pub fn compute_instance_groups_test_iam_permissions_builder(
    client: &SimpleHttpClient,
    project: &str,
    resource: &str,
    zone: &str,
    body: &TestPermissionsRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/zones/{}/instanceGroups/{}/testIamPermissions",
        project, resource, zone,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET projects/{project}/zones/{zone}/instanceGroups/{resource}/testIamPermissions
/// Returns permissions that a caller has on the specified resource.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_instance_groups_test_iam_permissions_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_instance_groups_test_iam_permissions_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_instance_groups_test_iam_permissions_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<TestPermissionsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: TestPermissionsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/zones/{zone}/instanceGroups/{resource}/testIamPermissions
/// Returns permissions that a caller has on the specified resource.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_instance_groups_test_iam_permissions_builder()` + `compute_instance_groups_test_iam_permissions_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_instance_groups_test_iam_permissions(
    client: &SimpleHttpClient,
    project: &str,
    resource: &str,
    zone: &str,
    body: &TestPermissionsRequest,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<TestPermissionsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = compute_instance_groups_test_iam_permissions_builder(
        client, project, resource, zone, body,
    )?;
    compute_instance_groups_test_iam_permissions_execute(builder)
}

/// GET projects/{project}/zones/{zone}/instanceSettings
/// Get Instance settings.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_instance_settings_get_execute()` to send, or `compute_instance_settings_get` for simplest API.

pub fn compute_instance_settings_get_builder(
    client: &SimpleHttpClient,
    project: &str,
    zone: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/zones/{}/instanceSettings",
        project, zone,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/zones/{zone}/instanceSettings
/// Get Instance settings.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_instance_settings_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_instance_settings_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_instance_settings_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<InstanceSettings>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: InstanceSettings = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/zones/{zone}/instanceSettings
/// Get Instance settings.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_instance_settings_get_builder()` + `compute_instance_settings_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_instance_settings_get(
    client: &SimpleHttpClient,
    project: &str,
    zone: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<InstanceSettings>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = compute_instance_settings_get_builder(client, project, zone)?;
    compute_instance_settings_get_execute(builder)
}

/// GET projects/{project}/zones/{zone}/instanceSettings
/// Patch Instance settings
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_instance_settings_patch_execute()` to send, or `compute_instance_settings_patch` for simplest API.

pub fn compute_instance_settings_patch_builder(
    client: &SimpleHttpClient,
    project: &str,
    zone: &str,
    requestId: Option<&str>,
    updateMask: Option<&str>,
    body: &InstanceSettings,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/zones/{}/instanceSettings",
        project, zone,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "requestId",
            requestId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "updateMask",
            updateMask
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET projects/{project}/zones/{zone}/instanceSettings
/// Patch Instance settings
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_instance_settings_patch_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_instance_settings_patch_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_instance_settings_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/zones/{zone}/instanceSettings
/// Patch Instance settings
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_instance_settings_patch_builder()` + `compute_instance_settings_patch_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_instance_settings_patch(
    client: &SimpleHttpClient,
    project: &str,
    zone: &str,
    requestId: Option<&str>,
    updateMask: Option<&str>,
    body: &InstanceSettings,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_instance_settings_patch_builder(
        client, project, zone, requestId, updateMask, body,
    )?;
    compute_instance_settings_patch_execute(builder)
}

/// GET projects/{project}/aggregated/instanceTemplates
/// Retrieves the list of all InstanceTemplates resources, regional and global, available to the specified project.  To prevent failure, Google recommends that you set the `returnPartialSuccess` parameter to `true`.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_instance_templates_aggregated_list_execute()` to send, or `compute_instance_templates_aggregated_list` for simplest API.

pub fn compute_instance_templates_aggregated_list_builder(
    client: &SimpleHttpClient,
    project: &str,
    filter: Option<&str>,
    includeAllScopes: Option<bool>,
    maxResults: Option<i32>,
    orderBy: Option<&str>,
    pageToken: Option<&str>,
    returnPartialSuccess: Option<bool>,
    serviceProjectNumber: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/aggregated/instanceTemplates",
        project,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "filter",
            filter
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "includeAllScopes",
            includeAllScopes
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "maxResults",
            maxResults
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "orderBy",
            orderBy
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageToken",
            pageToken
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "returnPartialSuccess",
            returnPartialSuccess
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "serviceProjectNumber",
            serviceProjectNumber
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET projects/{project}/aggregated/instanceTemplates
/// Retrieves the list of all InstanceTemplates resources, regional and global, available to the specified project.  To prevent failure, Google recommends that you set the `returnPartialSuccess` parameter to `true`.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_instance_templates_aggregated_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_instance_templates_aggregated_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_instance_templates_aggregated_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<InstanceTemplateAggregatedList>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: InstanceTemplateAggregatedList = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/aggregated/instanceTemplates
/// Retrieves the list of all InstanceTemplates resources, regional and global, available to the specified project.  To prevent failure, Google recommends that you set the `returnPartialSuccess` parameter to `true`.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_instance_templates_aggregated_list_builder()` + `compute_instance_templates_aggregated_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_instance_templates_aggregated_list(
    client: &SimpleHttpClient,
    project: &str,
    filter: Option<&str>,
    includeAllScopes: Option<bool>,
    maxResults: Option<i32>,
    orderBy: Option<&str>,
    pageToken: Option<&str>,
    returnPartialSuccess: Option<bool>,
    serviceProjectNumber: Option<&str>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<InstanceTemplateAggregatedList>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = compute_instance_templates_aggregated_list_builder(
        client,
        project,
        filter,
        includeAllScopes,
        maxResults,
        orderBy,
        pageToken,
        returnPartialSuccess,
        serviceProjectNumber,
    )?;
    compute_instance_templates_aggregated_list_execute(builder)
}

/// GET projects/{project}/global/instanceTemplates/{instanceTemplate}
/// Deletes the specified instance template. Deleting an instance template is permanent and cannot be undone. It is not possible to delete templates that are already in use by a managed instance group.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_instance_templates_delete_execute()` to send, or `compute_instance_templates_delete` for simplest API.

pub fn compute_instance_templates_delete_builder(
    client: &SimpleHttpClient,
    instanceTemplate: &str,
    project: &str,
    requestId: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/global/instanceTemplates/{}",
        instanceTemplate, project,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "requestId",
            requestId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET projects/{project}/global/instanceTemplates/{instanceTemplate}
/// Deletes the specified instance template. Deleting an instance template is permanent and cannot be undone. It is not possible to delete templates that are already in use by a managed instance group.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_instance_templates_delete_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_instance_templates_delete_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_instance_templates_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/global/instanceTemplates/{instanceTemplate}
/// Deletes the specified instance template. Deleting an instance template is permanent and cannot be undone. It is not possible to delete templates that are already in use by a managed instance group.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_instance_templates_delete_builder()` + `compute_instance_templates_delete_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_instance_templates_delete(
    client: &SimpleHttpClient,
    instanceTemplate: &str,
    project: &str,
    requestId: Option<&str>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        compute_instance_templates_delete_builder(client, instanceTemplate, project, requestId)?;
    compute_instance_templates_delete_execute(builder)
}

/// GET projects/{project}/global/instanceTemplates/{instanceTemplate}
/// Returns the specified instance template.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_instance_templates_get_execute()` to send, or `compute_instance_templates_get` for simplest API.

pub fn compute_instance_templates_get_builder(
    client: &SimpleHttpClient,
    instanceTemplate: &str,
    project: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/global/instanceTemplates/{}",
        instanceTemplate, project,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/global/instanceTemplates/{instanceTemplate}
/// Returns the specified instance template.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_instance_templates_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_instance_templates_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_instance_templates_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<InstanceTemplate>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: InstanceTemplate = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/global/instanceTemplates/{instanceTemplate}
/// Returns the specified instance template.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_instance_templates_get_builder()` + `compute_instance_templates_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_instance_templates_get(
    client: &SimpleHttpClient,
    instanceTemplate: &str,
    project: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<InstanceTemplate>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = compute_instance_templates_get_builder(client, instanceTemplate, project)?;
    compute_instance_templates_get_execute(builder)
}

/// GET projects/{project}/global/instanceTemplates/{resource}/getIamPolicy
/// Gets the access control policy for a resource. May be empty if no such policy or resource exists.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_instance_templates_get_iam_policy_execute()` to send, or `compute_instance_templates_get_iam_policy` for simplest API.

pub fn compute_instance_templates_get_iam_policy_builder(
    client: &SimpleHttpClient,
    project: &str,
    resource: &str,
    optionsRequestedPolicyVersion: Option<i32>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/global/instanceTemplates/{}/getIamPolicy",
        project, resource,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "optionsRequestedPolicyVersion",
            optionsRequestedPolicyVersion
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET projects/{project}/global/instanceTemplates/{resource}/getIamPolicy
/// Gets the access control policy for a resource. May be empty if no such policy or resource exists.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_instance_templates_get_iam_policy_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_instance_templates_get_iam_policy_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_instance_templates_get_iam_policy_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Policy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Policy = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/global/instanceTemplates/{resource}/getIamPolicy
/// Gets the access control policy for a resource. May be empty if no such policy or resource exists.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_instance_templates_get_iam_policy_builder()` + `compute_instance_templates_get_iam_policy_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_instance_templates_get_iam_policy(
    client: &SimpleHttpClient,
    project: &str,
    resource: &str,
    optionsRequestedPolicyVersion: Option<i32>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Policy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_instance_templates_get_iam_policy_builder(
        client,
        project,
        resource,
        optionsRequestedPolicyVersion,
    )?;
    compute_instance_templates_get_iam_policy_execute(builder)
}

/// GET projects/{project}/global/instanceTemplates
/// Creates an instance template in the specified project using the data that is included in the request. If you are creating a new template to update an existing instance group, your new instance template must use the same network or, if applicable, the same subnetwork as the original template.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_instance_templates_insert_execute()` to send, or `compute_instance_templates_insert` for simplest API.

pub fn compute_instance_templates_insert_builder(
    client: &SimpleHttpClient,
    project: &str,
    requestId: Option<&str>,
    body: &InstanceTemplate,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/global/instanceTemplates",
        project,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "requestId",
            requestId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET projects/{project}/global/instanceTemplates
/// Creates an instance template in the specified project using the data that is included in the request. If you are creating a new template to update an existing instance group, your new instance template must use the same network or, if applicable, the same subnetwork as the original template.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_instance_templates_insert_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_instance_templates_insert_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_instance_templates_insert_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/global/instanceTemplates
/// Creates an instance template in the specified project using the data that is included in the request. If you are creating a new template to update an existing instance group, your new instance template must use the same network or, if applicable, the same subnetwork as the original template.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_instance_templates_insert_builder()` + `compute_instance_templates_insert_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_instance_templates_insert(
    client: &SimpleHttpClient,
    project: &str,
    requestId: Option<&str>,
    body: &InstanceTemplate,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_instance_templates_insert_builder(client, project, requestId, body)?;
    compute_instance_templates_insert_execute(builder)
}

/// GET projects/{project}/global/instanceTemplates
/// Retrieves a list of instance templates that are contained within the specified project.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_instance_templates_list_execute()` to send, or `compute_instance_templates_list` for simplest API.

pub fn compute_instance_templates_list_builder(
    client: &SimpleHttpClient,
    project: &str,
    filter: Option<&str>,
    maxResults: Option<i32>,
    orderBy: Option<&str>,
    pageToken: Option<&str>,
    returnPartialSuccess: Option<bool>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/global/instanceTemplates",
        project,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "filter",
            filter
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "maxResults",
            maxResults
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "orderBy",
            orderBy
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageToken",
            pageToken
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "returnPartialSuccess",
            returnPartialSuccess
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET projects/{project}/global/instanceTemplates
/// Retrieves a list of instance templates that are contained within the specified project.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_instance_templates_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_instance_templates_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_instance_templates_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<InstanceTemplateList>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: InstanceTemplateList = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/global/instanceTemplates
/// Retrieves a list of instance templates that are contained within the specified project.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_instance_templates_list_builder()` + `compute_instance_templates_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_instance_templates_list(
    client: &SimpleHttpClient,
    project: &str,
    filter: Option<&str>,
    maxResults: Option<i32>,
    orderBy: Option<&str>,
    pageToken: Option<&str>,
    returnPartialSuccess: Option<bool>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<InstanceTemplateList>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = compute_instance_templates_list_builder(
        client,
        project,
        filter,
        maxResults,
        orderBy,
        pageToken,
        returnPartialSuccess,
    )?;
    compute_instance_templates_list_execute(builder)
}

/// GET projects/{project}/global/instanceTemplates/{resource}/setIamPolicy
/// Sets the access control policy on the specified resource. Replaces any existing policy.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_instance_templates_set_iam_policy_execute()` to send, or `compute_instance_templates_set_iam_policy` for simplest API.

pub fn compute_instance_templates_set_iam_policy_builder(
    client: &SimpleHttpClient,
    project: &str,
    resource: &str,
    body: &GlobalSetPolicyRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/global/instanceTemplates/{}/setIamPolicy",
        project, resource,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET projects/{project}/global/instanceTemplates/{resource}/setIamPolicy
/// Sets the access control policy on the specified resource. Replaces any existing policy.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_instance_templates_set_iam_policy_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_instance_templates_set_iam_policy_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_instance_templates_set_iam_policy_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Policy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Policy = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/global/instanceTemplates/{resource}/setIamPolicy
/// Sets the access control policy on the specified resource. Replaces any existing policy.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_instance_templates_set_iam_policy_builder()` + `compute_instance_templates_set_iam_policy_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_instance_templates_set_iam_policy(
    client: &SimpleHttpClient,
    project: &str,
    resource: &str,
    body: &GlobalSetPolicyRequest,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Policy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        compute_instance_templates_set_iam_policy_builder(client, project, resource, body)?;
    compute_instance_templates_set_iam_policy_execute(builder)
}

/// GET projects/{project}/global/instanceTemplates/{resource}/testIamPermissions
/// Returns permissions that a caller has on the specified resource.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_instance_templates_test_iam_permissions_execute()` to send, or `compute_instance_templates_test_iam_permissions` for simplest API.

pub fn compute_instance_templates_test_iam_permissions_builder(
    client: &SimpleHttpClient,
    project: &str,
    resource: &str,
    body: &TestPermissionsRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/global/instanceTemplates/{}/testIamPermissions",
        project, resource,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET projects/{project}/global/instanceTemplates/{resource}/testIamPermissions
/// Returns permissions that a caller has on the specified resource.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_instance_templates_test_iam_permissions_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_instance_templates_test_iam_permissions_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_instance_templates_test_iam_permissions_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<TestPermissionsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: TestPermissionsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/global/instanceTemplates/{resource}/testIamPermissions
/// Returns permissions that a caller has on the specified resource.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_instance_templates_test_iam_permissions_builder()` + `compute_instance_templates_test_iam_permissions_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_instance_templates_test_iam_permissions(
    client: &SimpleHttpClient,
    project: &str,
    resource: &str,
    body: &TestPermissionsRequest,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<TestPermissionsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        compute_instance_templates_test_iam_permissions_builder(client, project, resource, body)?;
    compute_instance_templates_test_iam_permissions_execute(builder)
}

/// GET projects/{project}/zones/{zone}/instances/{instance}/addAccessConfig
/// Adds an access config to an instance's network interface.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_instances_add_access_config_execute()` to send, or `compute_instances_add_access_config` for simplest API.

pub fn compute_instances_add_access_config_builder(
    client: &SimpleHttpClient,
    instance: &str,
    project: &str,
    zone: &str,
    networkInterface: Option<&str>,
    requestId: Option<&str>,
    body: &AccessConfig,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/zones/{}/instances/{}/addAccessConfig",
        instance, project, zone,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "networkInterface",
            networkInterface
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "requestId",
            requestId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET projects/{project}/zones/{zone}/instances/{instance}/addAccessConfig
/// Adds an access config to an instance's network interface.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_instances_add_access_config_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_instances_add_access_config_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_instances_add_access_config_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/zones/{zone}/instances/{instance}/addAccessConfig
/// Adds an access config to an instance's network interface.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_instances_add_access_config_builder()` + `compute_instances_add_access_config_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_instances_add_access_config(
    client: &SimpleHttpClient,
    instance: &str,
    project: &str,
    zone: &str,
    networkInterface: Option<&str>,
    requestId: Option<&str>,
    body: &AccessConfig,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_instances_add_access_config_builder(
        client,
        instance,
        project,
        zone,
        networkInterface,
        requestId,
        body,
    )?;
    compute_instances_add_access_config_execute(builder)
}

/// GET projects/{project}/zones/{zone}/instances/{instance}/addNetworkInterface
/// Adds one dynamic network interface to an active instance.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_instances_add_network_interface_execute()` to send, or `compute_instances_add_network_interface` for simplest API.

pub fn compute_instances_add_network_interface_builder(
    client: &SimpleHttpClient,
    instance: &str,
    project: &str,
    zone: &str,
    requestId: Option<&str>,
    body: &NetworkInterface,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/zones/{}/instances/{}/addNetworkInterface",
        instance, project, zone,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "requestId",
            requestId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET projects/{project}/zones/{zone}/instances/{instance}/addNetworkInterface
/// Adds one dynamic network interface to an active instance.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_instances_add_network_interface_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_instances_add_network_interface_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_instances_add_network_interface_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/zones/{zone}/instances/{instance}/addNetworkInterface
/// Adds one dynamic network interface to an active instance.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_instances_add_network_interface_builder()` + `compute_instances_add_network_interface_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_instances_add_network_interface(
    client: &SimpleHttpClient,
    instance: &str,
    project: &str,
    zone: &str,
    requestId: Option<&str>,
    body: &NetworkInterface,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_instances_add_network_interface_builder(
        client, instance, project, zone, requestId, body,
    )?;
    compute_instances_add_network_interface_execute(builder)
}

/// GET projects/{project}/zones/{zone}/instances/{instance}/addResourcePolicies
/// Adds existing resource policies to an instance. You can only add one policy right now which will be applied to this instance for scheduling live migrations.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_instances_add_resource_policies_execute()` to send, or `compute_instances_add_resource_policies` for simplest API.

pub fn compute_instances_add_resource_policies_builder(
    client: &SimpleHttpClient,
    instance: &str,
    project: &str,
    zone: &str,
    requestId: Option<&str>,
    body: &InstancesAddResourcePoliciesRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/zones/{}/instances/{}/addResourcePolicies",
        instance, project, zone,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "requestId",
            requestId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET projects/{project}/zones/{zone}/instances/{instance}/addResourcePolicies
/// Adds existing resource policies to an instance. You can only add one policy right now which will be applied to this instance for scheduling live migrations.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_instances_add_resource_policies_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_instances_add_resource_policies_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_instances_add_resource_policies_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/zones/{zone}/instances/{instance}/addResourcePolicies
/// Adds existing resource policies to an instance. You can only add one policy right now which will be applied to this instance for scheduling live migrations.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_instances_add_resource_policies_builder()` + `compute_instances_add_resource_policies_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_instances_add_resource_policies(
    client: &SimpleHttpClient,
    instance: &str,
    project: &str,
    zone: &str,
    requestId: Option<&str>,
    body: &InstancesAddResourcePoliciesRequest,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_instances_add_resource_policies_builder(
        client, instance, project, zone, requestId, body,
    )?;
    compute_instances_add_resource_policies_execute(builder)
}

/// GET projects/{project}/aggregated/instances
/// Retrieves an aggregated list of all of the instances in your project across all regions and zones.  The performance of this method degrades when a filter is specified on a project that has a very large number of instances.  To prevent failure, Google recommends that you set the `returnPartialSuccess` parameter to `true`.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_instances_aggregated_list_execute()` to send, or `compute_instances_aggregated_list` for simplest API.

pub fn compute_instances_aggregated_list_builder(
    client: &SimpleHttpClient,
    project: &str,
    filter: Option<&str>,
    includeAllScopes: Option<bool>,
    maxResults: Option<i32>,
    orderBy: Option<&str>,
    pageToken: Option<&str>,
    returnPartialSuccess: Option<bool>,
    serviceProjectNumber: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/aggregated/instances",
        project,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "filter",
            filter
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "includeAllScopes",
            includeAllScopes
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "maxResults",
            maxResults
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "orderBy",
            orderBy
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageToken",
            pageToken
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "returnPartialSuccess",
            returnPartialSuccess
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "serviceProjectNumber",
            serviceProjectNumber
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET projects/{project}/aggregated/instances
/// Retrieves an aggregated list of all of the instances in your project across all regions and zones.  The performance of this method degrades when a filter is specified on a project that has a very large number of instances.  To prevent failure, Google recommends that you set the `returnPartialSuccess` parameter to `true`.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_instances_aggregated_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_instances_aggregated_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_instances_aggregated_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<InstanceAggregatedList>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: InstanceAggregatedList = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/aggregated/instances
/// Retrieves an aggregated list of all of the instances in your project across all regions and zones.  The performance of this method degrades when a filter is specified on a project that has a very large number of instances.  To prevent failure, Google recommends that you set the `returnPartialSuccess` parameter to `true`.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_instances_aggregated_list_builder()` + `compute_instances_aggregated_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_instances_aggregated_list(
    client: &SimpleHttpClient,
    project: &str,
    filter: Option<&str>,
    includeAllScopes: Option<bool>,
    maxResults: Option<i32>,
    orderBy: Option<&str>,
    pageToken: Option<&str>,
    returnPartialSuccess: Option<bool>,
    serviceProjectNumber: Option<&str>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<InstanceAggregatedList>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = compute_instances_aggregated_list_builder(
        client,
        project,
        filter,
        includeAllScopes,
        maxResults,
        orderBy,
        pageToken,
        returnPartialSuccess,
        serviceProjectNumber,
    )?;
    compute_instances_aggregated_list_execute(builder)
}

/// GET projects/{project}/zones/{zone}/instances/{instance}/attachDisk
/// Attaches an existing Disk resource to an instance. You must first create the disk before you can attach it. It is not possible to create and attach a disk at the same time. For more information, `readAdding` a persistent disk to your instance.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_instances_attach_disk_execute()` to send, or `compute_instances_attach_disk` for simplest API.

pub fn compute_instances_attach_disk_builder(
    client: &SimpleHttpClient,
    instance: &str,
    project: &str,
    zone: &str,
    forceAttach: Option<bool>,
    requestId: Option<&str>,
    body: &AttachedDisk,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/zones/{}/instances/{}/attachDisk",
        instance, project, zone,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "forceAttach",
            forceAttach
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "requestId",
            requestId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET projects/{project}/zones/{zone}/instances/{instance}/attachDisk
/// Attaches an existing Disk resource to an instance. You must first create the disk before you can attach it. It is not possible to create and attach a disk at the same time. For more information, `readAdding` a persistent disk to your instance.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_instances_attach_disk_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_instances_attach_disk_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_instances_attach_disk_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/zones/{zone}/instances/{instance}/attachDisk
/// Attaches an existing Disk resource to an instance. You must first create the disk before you can attach it. It is not possible to create and attach a disk at the same time. For more information, `readAdding` a persistent disk to your instance.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_instances_attach_disk_builder()` + `compute_instances_attach_disk_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_instances_attach_disk(
    client: &SimpleHttpClient,
    instance: &str,
    project: &str,
    zone: &str,
    forceAttach: Option<bool>,
    requestId: Option<&str>,
    body: &AttachedDisk,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_instances_attach_disk_builder(
        client,
        instance,
        project,
        zone,
        forceAttach,
        requestId,
        body,
    )?;
    compute_instances_attach_disk_execute(builder)
}

/// GET projects/{project}/zones/{zone}/instances/bulkInsert
/// Creates multiple instances. Count specifies the number of instances to create. For more information, `seeAbout` bulk creation of VMs.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_instances_bulk_insert_execute()` to send, or `compute_instances_bulk_insert` for simplest API.

pub fn compute_instances_bulk_insert_builder(
    client: &SimpleHttpClient,
    project: &str,
    zone: &str,
    requestId: Option<&str>,
    body: &BulkInsertInstanceResource,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/zones/{}/instances/bulkInsert",
        project, zone,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "requestId",
            requestId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET projects/{project}/zones/{zone}/instances/bulkInsert
/// Creates multiple instances. Count specifies the number of instances to create. For more information, `seeAbout` bulk creation of VMs.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_instances_bulk_insert_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_instances_bulk_insert_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_instances_bulk_insert_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/zones/{zone}/instances/bulkInsert
/// Creates multiple instances. Count specifies the number of instances to create. For more information, `seeAbout` bulk creation of VMs.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_instances_bulk_insert_builder()` + `compute_instances_bulk_insert_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_instances_bulk_insert(
    client: &SimpleHttpClient,
    project: &str,
    zone: &str,
    requestId: Option<&str>,
    body: &BulkInsertInstanceResource,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_instances_bulk_insert_builder(client, project, zone, requestId, body)?;
    compute_instances_bulk_insert_execute(builder)
}

/// GET projects/{project}/zones/{zone}/instances/{instance}
/// Deletes the specified Instance resource. For more information, `seeDeleting` an instance.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_instances_delete_execute()` to send, or `compute_instances_delete` for simplest API.

pub fn compute_instances_delete_builder(
    client: &SimpleHttpClient,
    instance: &str,
    project: &str,
    zone: &str,
    requestId: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/zones/{}/instances/{}",
        instance, project, zone,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "requestId",
            requestId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET projects/{project}/zones/{zone}/instances/{instance}
/// Deletes the specified Instance resource. For more information, `seeDeleting` an instance.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_instances_delete_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_instances_delete_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_instances_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/zones/{zone}/instances/{instance}
/// Deletes the specified Instance resource. For more information, `seeDeleting` an instance.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_instances_delete_builder()` + `compute_instances_delete_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_instances_delete(
    client: &SimpleHttpClient,
    instance: &str,
    project: &str,
    zone: &str,
    requestId: Option<&str>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_instances_delete_builder(client, instance, project, zone, requestId)?;
    compute_instances_delete_execute(builder)
}

/// GET projects/{project}/zones/{zone}/instances/{instance}/deleteAccessConfig
/// Deletes an access config from an instance's network interface.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_instances_delete_access_config_execute()` to send, or `compute_instances_delete_access_config` for simplest API.

pub fn compute_instances_delete_access_config_builder(
    client: &SimpleHttpClient,
    instance: &str,
    project: &str,
    zone: &str,
    accessConfig: Option<&str>,
    networkInterface: Option<&str>,
    requestId: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/zones/{}/instances/{}/deleteAccessConfig",
        instance, project, zone,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "accessConfig",
            accessConfig
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "networkInterface",
            networkInterface
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "requestId",
            requestId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET projects/{project}/zones/{zone}/instances/{instance}/deleteAccessConfig
/// Deletes an access config from an instance's network interface.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_instances_delete_access_config_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_instances_delete_access_config_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_instances_delete_access_config_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/zones/{zone}/instances/{instance}/deleteAccessConfig
/// Deletes an access config from an instance's network interface.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_instances_delete_access_config_builder()` + `compute_instances_delete_access_config_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_instances_delete_access_config(
    client: &SimpleHttpClient,
    instance: &str,
    project: &str,
    zone: &str,
    accessConfig: Option<&str>,
    networkInterface: Option<&str>,
    requestId: Option<&str>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_instances_delete_access_config_builder(
        client,
        instance,
        project,
        zone,
        accessConfig,
        networkInterface,
        requestId,
    )?;
    compute_instances_delete_access_config_execute(builder)
}

/// GET projects/{project}/zones/{zone}/instances/{instance}/deleteNetworkInterface
/// Deletes one dynamic network interface from an active instance. InstancesDeleteNetworkInterfaceRequest indicates: - instance from which to delete, using project+zone+resource_id fields; - dynamic network interface to be deleted, using network_interface_name field;
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_instances_delete_network_interface_execute()` to send, or `compute_instances_delete_network_interface` for simplest API.

pub fn compute_instances_delete_network_interface_builder(
    client: &SimpleHttpClient,
    instance: &str,
    project: &str,
    zone: &str,
    networkInterfaceName: Option<&str>,
    requestId: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/zones/{}/instances/{}/deleteNetworkInterface",
        instance, project, zone,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "networkInterfaceName",
            networkInterfaceName
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "requestId",
            requestId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET projects/{project}/zones/{zone}/instances/{instance}/deleteNetworkInterface
/// Deletes one dynamic network interface from an active instance. InstancesDeleteNetworkInterfaceRequest indicates: - instance from which to delete, using project+zone+resource_id fields; - dynamic network interface to be deleted, using network_interface_name field;
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_instances_delete_network_interface_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_instances_delete_network_interface_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_instances_delete_network_interface_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/zones/{zone}/instances/{instance}/deleteNetworkInterface
/// Deletes one dynamic network interface from an active instance. InstancesDeleteNetworkInterfaceRequest indicates: - instance from which to delete, using project+zone+resource_id fields; - dynamic network interface to be deleted, using network_interface_name field;
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_instances_delete_network_interface_builder()` + `compute_instances_delete_network_interface_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_instances_delete_network_interface(
    client: &SimpleHttpClient,
    instance: &str,
    project: &str,
    zone: &str,
    networkInterfaceName: Option<&str>,
    requestId: Option<&str>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_instances_delete_network_interface_builder(
        client,
        instance,
        project,
        zone,
        networkInterfaceName,
        requestId,
    )?;
    compute_instances_delete_network_interface_execute(builder)
}

/// GET projects/{project}/zones/{zone}/instances/{instance}/detachDisk
/// Detaches a disk from an instance.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_instances_detach_disk_execute()` to send, or `compute_instances_detach_disk` for simplest API.

pub fn compute_instances_detach_disk_builder(
    client: &SimpleHttpClient,
    instance: &str,
    project: &str,
    zone: &str,
    deviceName: Option<&str>,
    requestId: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/zones/{}/instances/{}/detachDisk",
        instance, project, zone,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "deviceName",
            deviceName
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "requestId",
            requestId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET projects/{project}/zones/{zone}/instances/{instance}/detachDisk
/// Detaches a disk from an instance.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_instances_detach_disk_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_instances_detach_disk_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_instances_detach_disk_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/zones/{zone}/instances/{instance}/detachDisk
/// Detaches a disk from an instance.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_instances_detach_disk_builder()` + `compute_instances_detach_disk_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_instances_detach_disk(
    client: &SimpleHttpClient,
    instance: &str,
    project: &str,
    zone: &str,
    deviceName: Option<&str>,
    requestId: Option<&str>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_instances_detach_disk_builder(
        client, instance, project, zone, deviceName, requestId,
    )?;
    compute_instances_detach_disk_execute(builder)
}

/// GET projects/{project}/zones/{zone}/instances/{instance}
/// Returns the specified Instance resource.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_instances_get_execute()` to send, or `compute_instances_get` for simplest API.

pub fn compute_instances_get_builder(
    client: &SimpleHttpClient,
    instance: &str,
    project: &str,
    zone: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/zones/{}/instances/{}",
        instance, project, zone,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/zones/{zone}/instances/{instance}
/// Returns the specified Instance resource.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_instances_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_instances_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_instances_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Instance>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Instance = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/zones/{zone}/instances/{instance}
/// Returns the specified Instance resource.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_instances_get_builder()` + `compute_instances_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_instances_get(
    client: &SimpleHttpClient,
    instance: &str,
    project: &str,
    zone: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Instance>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_instances_get_builder(client, instance, project, zone)?;
    compute_instances_get_execute(builder)
}

/// GET projects/{project}/zones/{zone}/instances/{instance}/getEffectiveFirewalls
/// Returns effective firewalls applied to an interface of the instance.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_instances_get_effective_firewalls_execute()` to send, or `compute_instances_get_effective_firewalls` for simplest API.

pub fn compute_instances_get_effective_firewalls_builder(
    client: &SimpleHttpClient,
    instance: &str,
    project: &str,
    zone: &str,
    networkInterface: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/zones/{}/instances/{}/getEffectiveFirewalls",
        instance, project, zone,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "networkInterface",
            networkInterface
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET projects/{project}/zones/{zone}/instances/{instance}/getEffectiveFirewalls
/// Returns effective firewalls applied to an interface of the instance.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_instances_get_effective_firewalls_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_instances_get_effective_firewalls_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_instances_get_effective_firewalls_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<InstancesGetEffectiveFirewallsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: InstancesGetEffectiveFirewallsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/zones/{zone}/instances/{instance}/getEffectiveFirewalls
/// Returns effective firewalls applied to an interface of the instance.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_instances_get_effective_firewalls_builder()` + `compute_instances_get_effective_firewalls_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_instances_get_effective_firewalls(
    client: &SimpleHttpClient,
    instance: &str,
    project: &str,
    zone: &str,
    networkInterface: Option<&str>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<InstancesGetEffectiveFirewallsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = compute_instances_get_effective_firewalls_builder(
        client,
        instance,
        project,
        zone,
        networkInterface,
    )?;
    compute_instances_get_effective_firewalls_execute(builder)
}

/// GET projects/{project}/zones/{zone}/instances/{instance}/getGuestAttributes
/// Returns the specified guest attributes entry.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_instances_get_guest_attributes_execute()` to send, or `compute_instances_get_guest_attributes` for simplest API.

pub fn compute_instances_get_guest_attributes_builder(
    client: &SimpleHttpClient,
    instance: &str,
    project: &str,
    zone: &str,
    queryPath: Option<&str>,
    variableKey: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/zones/{}/instances/{}/getGuestAttributes",
        instance, project, zone,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "queryPath",
            queryPath
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "variableKey",
            variableKey
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET projects/{project}/zones/{zone}/instances/{instance}/getGuestAttributes
/// Returns the specified guest attributes entry.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_instances_get_guest_attributes_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_instances_get_guest_attributes_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_instances_get_guest_attributes_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GuestAttributes>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GuestAttributes = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/zones/{zone}/instances/{instance}/getGuestAttributes
/// Returns the specified guest attributes entry.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_instances_get_guest_attributes_builder()` + `compute_instances_get_guest_attributes_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_instances_get_guest_attributes(
    client: &SimpleHttpClient,
    instance: &str,
    project: &str,
    zone: &str,
    queryPath: Option<&str>,
    variableKey: Option<&str>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GuestAttributes>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = compute_instances_get_guest_attributes_builder(
        client,
        instance,
        project,
        zone,
        queryPath,
        variableKey,
    )?;
    compute_instances_get_guest_attributes_execute(builder)
}

/// GET projects/{project}/zones/{zone}/instances/{resource}/getIamPolicy
/// Gets the access control policy for a resource. May be empty if no such policy or resource exists.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_instances_get_iam_policy_execute()` to send, or `compute_instances_get_iam_policy` for simplest API.

pub fn compute_instances_get_iam_policy_builder(
    client: &SimpleHttpClient,
    project: &str,
    resource: &str,
    zone: &str,
    optionsRequestedPolicyVersion: Option<i32>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/zones/{}/instances/{}/getIamPolicy",
        project, resource, zone,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "optionsRequestedPolicyVersion",
            optionsRequestedPolicyVersion
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET projects/{project}/zones/{zone}/instances/{resource}/getIamPolicy
/// Gets the access control policy for a resource. May be empty if no such policy or resource exists.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_instances_get_iam_policy_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_instances_get_iam_policy_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_instances_get_iam_policy_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Policy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Policy = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/zones/{zone}/instances/{resource}/getIamPolicy
/// Gets the access control policy for a resource. May be empty if no such policy or resource exists.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_instances_get_iam_policy_builder()` + `compute_instances_get_iam_policy_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_instances_get_iam_policy(
    client: &SimpleHttpClient,
    project: &str,
    resource: &str,
    zone: &str,
    optionsRequestedPolicyVersion: Option<i32>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Policy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_instances_get_iam_policy_builder(
        client,
        project,
        resource,
        zone,
        optionsRequestedPolicyVersion,
    )?;
    compute_instances_get_iam_policy_execute(builder)
}

/// GET projects/{project}/zones/{zone}/instances/{instance}/screenshot
/// Returns the screenshot from the specified instance.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_instances_get_screenshot_execute()` to send, or `compute_instances_get_screenshot` for simplest API.

pub fn compute_instances_get_screenshot_builder(
    client: &SimpleHttpClient,
    instance: &str,
    project: &str,
    zone: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/zones/{}/instances/{}/screenshot",
        instance, project, zone,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/zones/{zone}/instances/{instance}/screenshot
/// Returns the screenshot from the specified instance.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_instances_get_screenshot_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_instances_get_screenshot_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_instances_get_screenshot_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Screenshot>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Screenshot = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/zones/{zone}/instances/{instance}/screenshot
/// Returns the screenshot from the specified instance.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_instances_get_screenshot_builder()` + `compute_instances_get_screenshot_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_instances_get_screenshot(
    client: &SimpleHttpClient,
    instance: &str,
    project: &str,
    zone: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Screenshot>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_instances_get_screenshot_builder(client, instance, project, zone)?;
    compute_instances_get_screenshot_execute(builder)
}

/// GET projects/{project}/zones/{zone}/instances/{instance}/serialPort
/// Returns the last 1 MB of serial port output from the specified instance.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_instances_get_serial_port_output_execute()` to send, or `compute_instances_get_serial_port_output` for simplest API.

pub fn compute_instances_get_serial_port_output_builder(
    client: &SimpleHttpClient,
    instance: &str,
    project: &str,
    zone: &str,
    port: Option<i32>,
    start: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/zones/{}/instances/{}/serialPort",
        instance, project, zone,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "port",
            port.as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "start",
            start
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET projects/{project}/zones/{zone}/instances/{instance}/serialPort
/// Returns the last 1 MB of serial port output from the specified instance.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_instances_get_serial_port_output_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_instances_get_serial_port_output_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_instances_get_serial_port_output_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SerialPortOutput>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: SerialPortOutput = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/zones/{zone}/instances/{instance}/serialPort
/// Returns the last 1 MB of serial port output from the specified instance.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_instances_get_serial_port_output_builder()` + `compute_instances_get_serial_port_output_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_instances_get_serial_port_output(
    client: &SimpleHttpClient,
    instance: &str,
    project: &str,
    zone: &str,
    port: Option<i32>,
    start: Option<&str>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SerialPortOutput>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = compute_instances_get_serial_port_output_builder(
        client, instance, project, zone, port, start,
    )?;
    compute_instances_get_serial_port_output_execute(builder)
}

/// GET projects/{project}/zones/{zone}/instances/{instance}/getShieldedInstanceIdentity
/// Returns the Shielded Instance Identity of an instance
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_instances_get_shielded_instance_identity_execute()` to send, or `compute_instances_get_shielded_instance_identity` for simplest API.

pub fn compute_instances_get_shielded_instance_identity_builder(
    client: &SimpleHttpClient,
    instance: &str,
    project: &str,
    zone: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/zones/{}/instances/{}/getShieldedInstanceIdentity",
        instance, project, zone,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/zones/{zone}/instances/{instance}/getShieldedInstanceIdentity
/// Returns the Shielded Instance Identity of an instance
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_instances_get_shielded_instance_identity_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_instances_get_shielded_instance_identity_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_instances_get_shielded_instance_identity_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ShieldedInstanceIdentity>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ShieldedInstanceIdentity = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/zones/{zone}/instances/{instance}/getShieldedInstanceIdentity
/// Returns the Shielded Instance Identity of an instance
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_instances_get_shielded_instance_identity_builder()` + `compute_instances_get_shielded_instance_identity_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_instances_get_shielded_instance_identity(
    client: &SimpleHttpClient,
    instance: &str,
    project: &str,
    zone: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ShieldedInstanceIdentity>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        compute_instances_get_shielded_instance_identity_builder(client, instance, project, zone)?;
    compute_instances_get_shielded_instance_identity_execute(builder)
}

/// GET projects/{project}/zones/{zone}/instances
/// Creates an instance resource in the specified project using the data included in the request.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_instances_insert_execute()` to send, or `compute_instances_insert` for simplest API.

pub fn compute_instances_insert_builder(
    client: &SimpleHttpClient,
    project: &str,
    zone: &str,
    requestId: Option<&str>,
    sourceInstanceTemplate: Option<&str>,
    sourceMachineImage: Option<&str>,
    body: &Instance,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/zones/{}/instances",
        project, zone,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "requestId",
            requestId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "sourceInstanceTemplate",
            sourceInstanceTemplate
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "sourceMachineImage",
            sourceMachineImage
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET projects/{project}/zones/{zone}/instances
/// Creates an instance resource in the specified project using the data included in the request.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_instances_insert_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_instances_insert_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_instances_insert_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/zones/{zone}/instances
/// Creates an instance resource in the specified project using the data included in the request.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_instances_insert_builder()` + `compute_instances_insert_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_instances_insert(
    client: &SimpleHttpClient,
    project: &str,
    zone: &str,
    requestId: Option<&str>,
    sourceInstanceTemplate: Option<&str>,
    sourceMachineImage: Option<&str>,
    body: &Instance,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_instances_insert_builder(
        client,
        project,
        zone,
        requestId,
        sourceInstanceTemplate,
        sourceMachineImage,
        body,
    )?;
    compute_instances_insert_execute(builder)
}

/// GET projects/{project}/zones/{zone}/instances
/// Retrieves the list of instances contained within the specified zone.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_instances_list_execute()` to send, or `compute_instances_list` for simplest API.

pub fn compute_instances_list_builder(
    client: &SimpleHttpClient,
    project: &str,
    zone: &str,
    filter: Option<&str>,
    maxResults: Option<i32>,
    orderBy: Option<&str>,
    pageToken: Option<&str>,
    returnPartialSuccess: Option<bool>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/zones/{}/instances",
        project, zone,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "filter",
            filter
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "maxResults",
            maxResults
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "orderBy",
            orderBy
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageToken",
            pageToken
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "returnPartialSuccess",
            returnPartialSuccess
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET projects/{project}/zones/{zone}/instances
/// Retrieves the list of instances contained within the specified zone.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_instances_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_instances_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_instances_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<InstanceList>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: InstanceList = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/zones/{zone}/instances
/// Retrieves the list of instances contained within the specified zone.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_instances_list_builder()` + `compute_instances_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_instances_list(
    client: &SimpleHttpClient,
    project: &str,
    zone: &str,
    filter: Option<&str>,
    maxResults: Option<i32>,
    orderBy: Option<&str>,
    pageToken: Option<&str>,
    returnPartialSuccess: Option<bool>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<InstanceList>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = compute_instances_list_builder(
        client,
        project,
        zone,
        filter,
        maxResults,
        orderBy,
        pageToken,
        returnPartialSuccess,
    )?;
    compute_instances_list_execute(builder)
}

/// GET projects/{project}/zones/{zone}/instances/{instance}/referrers
/// Retrieves a list of resources that refer to the VM instance specified in the request. For example, if the VM instance is part of a managed or unmanaged instance group, the referrers list includes the instance group. For more information, `readViewing` referrers to VM instances.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_instances_list_referrers_execute()` to send, or `compute_instances_list_referrers` for simplest API.

pub fn compute_instances_list_referrers_builder(
    client: &SimpleHttpClient,
    instance: &str,
    project: &str,
    zone: &str,
    filter: Option<&str>,
    maxResults: Option<i32>,
    orderBy: Option<&str>,
    pageToken: Option<&str>,
    returnPartialSuccess: Option<bool>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/zones/{}/instances/{}/referrers",
        instance, project, zone,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "filter",
            filter
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "maxResults",
            maxResults
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "orderBy",
            orderBy
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageToken",
            pageToken
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "returnPartialSuccess",
            returnPartialSuccess
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET projects/{project}/zones/{zone}/instances/{instance}/referrers
/// Retrieves a list of resources that refer to the VM instance specified in the request. For example, if the VM instance is part of a managed or unmanaged instance group, the referrers list includes the instance group. For more information, `readViewing` referrers to VM instances.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_instances_list_referrers_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_instances_list_referrers_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_instances_list_referrers_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<InstanceListReferrers>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: InstanceListReferrers = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/zones/{zone}/instances/{instance}/referrers
/// Retrieves a list of resources that refer to the VM instance specified in the request. For example, if the VM instance is part of a managed or unmanaged instance group, the referrers list includes the instance group. For more information, `readViewing` referrers to VM instances.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_instances_list_referrers_builder()` + `compute_instances_list_referrers_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_instances_list_referrers(
    client: &SimpleHttpClient,
    instance: &str,
    project: &str,
    zone: &str,
    filter: Option<&str>,
    maxResults: Option<i32>,
    orderBy: Option<&str>,
    pageToken: Option<&str>,
    returnPartialSuccess: Option<bool>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<InstanceListReferrers>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = compute_instances_list_referrers_builder(
        client,
        instance,
        project,
        zone,
        filter,
        maxResults,
        orderBy,
        pageToken,
        returnPartialSuccess,
    )?;
    compute_instances_list_referrers_execute(builder)
}

/// GET projects/{project}/zones/{zone}/instances/{instance}/performMaintenance
/// Perform a manual maintenance on the instance.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_instances_perform_maintenance_execute()` to send, or `compute_instances_perform_maintenance` for simplest API.

pub fn compute_instances_perform_maintenance_builder(
    client: &SimpleHttpClient,
    instance: &str,
    project: &str,
    zone: &str,
    requestId: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/zones/{}/instances/{}/performMaintenance",
        instance, project, zone,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "requestId",
            requestId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET projects/{project}/zones/{zone}/instances/{instance}/performMaintenance
/// Perform a manual maintenance on the instance.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_instances_perform_maintenance_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_instances_perform_maintenance_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_instances_perform_maintenance_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/zones/{zone}/instances/{instance}/performMaintenance
/// Perform a manual maintenance on the instance.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_instances_perform_maintenance_builder()` + `compute_instances_perform_maintenance_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_instances_perform_maintenance(
    client: &SimpleHttpClient,
    instance: &str,
    project: &str,
    zone: &str,
    requestId: Option<&str>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        compute_instances_perform_maintenance_builder(client, instance, project, zone, requestId)?;
    compute_instances_perform_maintenance_execute(builder)
}

/// GET projects/{project}/zones/{zone}/instances/{instance}/removeResourcePolicies
/// Removes resource policies from an instance.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_instances_remove_resource_policies_execute()` to send, or `compute_instances_remove_resource_policies` for simplest API.

pub fn compute_instances_remove_resource_policies_builder(
    client: &SimpleHttpClient,
    instance: &str,
    project: &str,
    zone: &str,
    requestId: Option<&str>,
    body: &InstancesRemoveResourcePoliciesRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/zones/{}/instances/{}/removeResourcePolicies",
        instance, project, zone,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "requestId",
            requestId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET projects/{project}/zones/{zone}/instances/{instance}/removeResourcePolicies
/// Removes resource policies from an instance.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_instances_remove_resource_policies_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_instances_remove_resource_policies_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_instances_remove_resource_policies_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/zones/{zone}/instances/{instance}/removeResourcePolicies
/// Removes resource policies from an instance.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_instances_remove_resource_policies_builder()` + `compute_instances_remove_resource_policies_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_instances_remove_resource_policies(
    client: &SimpleHttpClient,
    instance: &str,
    project: &str,
    zone: &str,
    requestId: Option<&str>,
    body: &InstancesRemoveResourcePoliciesRequest,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_instances_remove_resource_policies_builder(
        client, instance, project, zone, requestId, body,
    )?;
    compute_instances_remove_resource_policies_execute(builder)
}

/// GET projects/{project}/zones/{zone}/instances/{instance}/reportHostAsFaulty
/// Mark the host as faulty and try to restart the instance on a new host.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_instances_report_host_as_faulty_execute()` to send, or `compute_instances_report_host_as_faulty` for simplest API.

pub fn compute_instances_report_host_as_faulty_builder(
    client: &SimpleHttpClient,
    instance: &str,
    project: &str,
    zone: &str,
    requestId: Option<&str>,
    body: &InstancesReportHostAsFaultyRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/zones/{}/instances/{}/reportHostAsFaulty",
        instance, project, zone,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "requestId",
            requestId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET projects/{project}/zones/{zone}/instances/{instance}/reportHostAsFaulty
/// Mark the host as faulty and try to restart the instance on a new host.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_instances_report_host_as_faulty_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_instances_report_host_as_faulty_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_instances_report_host_as_faulty_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/zones/{zone}/instances/{instance}/reportHostAsFaulty
/// Mark the host as faulty and try to restart the instance on a new host.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_instances_report_host_as_faulty_builder()` + `compute_instances_report_host_as_faulty_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_instances_report_host_as_faulty(
    client: &SimpleHttpClient,
    instance: &str,
    project: &str,
    zone: &str,
    requestId: Option<&str>,
    body: &InstancesReportHostAsFaultyRequest,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_instances_report_host_as_faulty_builder(
        client, instance, project, zone, requestId, body,
    )?;
    compute_instances_report_host_as_faulty_execute(builder)
}

/// GET projects/{project}/zones/{zone}/instances/{instance}/reset
/// Performs a reset on the instance. This is a hard reset. The VM does not do a graceful shutdown. For more information, `seeResetting` an instance.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_instances_reset_execute()` to send, or `compute_instances_reset` for simplest API.

pub fn compute_instances_reset_builder(
    client: &SimpleHttpClient,
    instance: &str,
    project: &str,
    zone: &str,
    requestId: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/zones/{}/instances/{}/reset",
        instance, project, zone,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "requestId",
            requestId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET projects/{project}/zones/{zone}/instances/{instance}/reset
/// Performs a reset on the instance. This is a hard reset. The VM does not do a graceful shutdown. For more information, `seeResetting` an instance.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_instances_reset_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_instances_reset_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_instances_reset_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/zones/{zone}/instances/{instance}/reset
/// Performs a reset on the instance. This is a hard reset. The VM does not do a graceful shutdown. For more information, `seeResetting` an instance.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_instances_reset_builder()` + `compute_instances_reset_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_instances_reset(
    client: &SimpleHttpClient,
    instance: &str,
    project: &str,
    zone: &str,
    requestId: Option<&str>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_instances_reset_builder(client, instance, project, zone, requestId)?;
    compute_instances_reset_execute(builder)
}

/// GET projects/{project}/zones/{zone}/instances/{instance}/resume
/// Resumes an instance that was suspended using theinstances().suspend method.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_instances_resume_execute()` to send, or `compute_instances_resume` for simplest API.

pub fn compute_instances_resume_builder(
    client: &SimpleHttpClient,
    instance: &str,
    project: &str,
    zone: &str,
    requestId: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/zones/{}/instances/{}/resume",
        instance, project, zone,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "requestId",
            requestId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET projects/{project}/zones/{zone}/instances/{instance}/resume
/// Resumes an instance that was suspended using theinstances().suspend method.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_instances_resume_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_instances_resume_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_instances_resume_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/zones/{zone}/instances/{instance}/resume
/// Resumes an instance that was suspended using theinstances().suspend method.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_instances_resume_builder()` + `compute_instances_resume_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_instances_resume(
    client: &SimpleHttpClient,
    instance: &str,
    project: &str,
    zone: &str,
    requestId: Option<&str>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_instances_resume_builder(client, instance, project, zone, requestId)?;
    compute_instances_resume_execute(builder)
}

/// GET projects/{project}/zones/{zone}/instances/{instance}/sendDiagnosticInterrupt
/// Sends diagnostic interrupt to the instance.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_instances_send_diagnostic_interrupt_execute()` to send, or `compute_instances_send_diagnostic_interrupt` for simplest API.

pub fn compute_instances_send_diagnostic_interrupt_builder(
    client: &SimpleHttpClient,
    instance: &str,
    project: &str,
    zone: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/zones/{}/instances/{}/sendDiagnosticInterrupt",
        instance, project, zone,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/zones/{zone}/instances/{instance}/sendDiagnosticInterrupt
/// Sends diagnostic interrupt to the instance.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_instances_send_diagnostic_interrupt_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_instances_send_diagnostic_interrupt_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_instances_send_diagnostic_interrupt_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/zones/{zone}/instances/{instance}/sendDiagnosticInterrupt
/// Sends diagnostic interrupt to the instance.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_instances_send_diagnostic_interrupt_builder()` + `compute_instances_send_diagnostic_interrupt_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_instances_send_diagnostic_interrupt(
    client: &SimpleHttpClient,
    instance: &str,
    project: &str,
    zone: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        compute_instances_send_diagnostic_interrupt_builder(client, instance, project, zone)?;
    compute_instances_send_diagnostic_interrupt_execute(builder)
}

/// GET projects/{project}/zones/{zone}/instances/{resource}/setDeletionProtection
/// Sets deletion protection on the instance.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_instances_set_deletion_protection_execute()` to send, or `compute_instances_set_deletion_protection` for simplest API.

pub fn compute_instances_set_deletion_protection_builder(
    client: &SimpleHttpClient,
    project: &str,
    resource: &str,
    zone: &str,
    deletionProtection: Option<bool>,
    requestId: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/zones/{}/instances/{}/setDeletionProtection",
        project, resource, zone,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "deletionProtection",
            deletionProtection
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "requestId",
            requestId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET projects/{project}/zones/{zone}/instances/{resource}/setDeletionProtection
/// Sets deletion protection on the instance.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_instances_set_deletion_protection_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_instances_set_deletion_protection_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_instances_set_deletion_protection_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/zones/{zone}/instances/{resource}/setDeletionProtection
/// Sets deletion protection on the instance.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_instances_set_deletion_protection_builder()` + `compute_instances_set_deletion_protection_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_instances_set_deletion_protection(
    client: &SimpleHttpClient,
    project: &str,
    resource: &str,
    zone: &str,
    deletionProtection: Option<bool>,
    requestId: Option<&str>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_instances_set_deletion_protection_builder(
        client,
        project,
        resource,
        zone,
        deletionProtection,
        requestId,
    )?;
    compute_instances_set_deletion_protection_execute(builder)
}

/// GET projects/{project}/zones/{zone}/instances/{instance}/setDiskAutoDelete
/// Sets the auto-delete flag for a disk attached to an instance.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_instances_set_disk_auto_delete_execute()` to send, or `compute_instances_set_disk_auto_delete` for simplest API.

pub fn compute_instances_set_disk_auto_delete_builder(
    client: &SimpleHttpClient,
    instance: &str,
    project: &str,
    zone: &str,
    autoDelete: Option<bool>,
    deviceName: Option<&str>,
    requestId: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/zones/{}/instances/{}/setDiskAutoDelete",
        instance, project, zone,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "autoDelete",
            autoDelete
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "deviceName",
            deviceName
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "requestId",
            requestId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET projects/{project}/zones/{zone}/instances/{instance}/setDiskAutoDelete
/// Sets the auto-delete flag for a disk attached to an instance.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_instances_set_disk_auto_delete_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_instances_set_disk_auto_delete_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_instances_set_disk_auto_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/zones/{zone}/instances/{instance}/setDiskAutoDelete
/// Sets the auto-delete flag for a disk attached to an instance.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_instances_set_disk_auto_delete_builder()` + `compute_instances_set_disk_auto_delete_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_instances_set_disk_auto_delete(
    client: &SimpleHttpClient,
    instance: &str,
    project: &str,
    zone: &str,
    autoDelete: Option<bool>,
    deviceName: Option<&str>,
    requestId: Option<&str>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_instances_set_disk_auto_delete_builder(
        client, instance, project, zone, autoDelete, deviceName, requestId,
    )?;
    compute_instances_set_disk_auto_delete_execute(builder)
}

/// GET projects/{project}/zones/{zone}/instances/{resource}/setIamPolicy
/// Sets the access control policy on the specified resource. Replaces any existing policy.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_instances_set_iam_policy_execute()` to send, or `compute_instances_set_iam_policy` for simplest API.

pub fn compute_instances_set_iam_policy_builder(
    client: &SimpleHttpClient,
    project: &str,
    resource: &str,
    zone: &str,
    body: &ZoneSetPolicyRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/zones/{}/instances/{}/setIamPolicy",
        project, resource, zone,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET projects/{project}/zones/{zone}/instances/{resource}/setIamPolicy
/// Sets the access control policy on the specified resource. Replaces any existing policy.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_instances_set_iam_policy_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_instances_set_iam_policy_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_instances_set_iam_policy_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Policy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Policy = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/zones/{zone}/instances/{resource}/setIamPolicy
/// Sets the access control policy on the specified resource. Replaces any existing policy.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_instances_set_iam_policy_builder()` + `compute_instances_set_iam_policy_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_instances_set_iam_policy(
    client: &SimpleHttpClient,
    project: &str,
    resource: &str,
    zone: &str,
    body: &ZoneSetPolicyRequest,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Policy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_instances_set_iam_policy_builder(client, project, resource, zone, body)?;
    compute_instances_set_iam_policy_execute(builder)
}

/// GET projects/{project}/zones/{zone}/instances/{instance}/setLabels
/// Sets labels on an instance.  To learn more about labels, read `theLabeling` Resources documentation.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_instances_set_labels_execute()` to send, or `compute_instances_set_labels` for simplest API.

pub fn compute_instances_set_labels_builder(
    client: &SimpleHttpClient,
    instance: &str,
    project: &str,
    zone: &str,
    requestId: Option<&str>,
    body: &InstancesSetLabelsRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/zones/{}/instances/{}/setLabels",
        instance, project, zone,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "requestId",
            requestId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET projects/{project}/zones/{zone}/instances/{instance}/setLabels
/// Sets labels on an instance.  To learn more about labels, read `theLabeling` Resources documentation.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_instances_set_labels_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_instances_set_labels_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_instances_set_labels_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/zones/{zone}/instances/{instance}/setLabels
/// Sets labels on an instance.  To learn more about labels, read `theLabeling` Resources documentation.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_instances_set_labels_builder()` + `compute_instances_set_labels_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_instances_set_labels(
    client: &SimpleHttpClient,
    instance: &str,
    project: &str,
    zone: &str,
    requestId: Option<&str>,
    body: &InstancesSetLabelsRequest,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        compute_instances_set_labels_builder(client, instance, project, zone, requestId, body)?;
    compute_instances_set_labels_execute(builder)
}

/// GET projects/{project}/zones/{zone}/instances/{instance}/setMachineResources
/// Changes the number `and/or` type of accelerator for a stopped instance to the values specified in the request.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_instances_set_machine_resources_execute()` to send, or `compute_instances_set_machine_resources` for simplest API.

pub fn compute_instances_set_machine_resources_builder(
    client: &SimpleHttpClient,
    instance: &str,
    project: &str,
    zone: &str,
    requestId: Option<&str>,
    body: &InstancesSetMachineResourcesRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/zones/{}/instances/{}/setMachineResources",
        instance, project, zone,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "requestId",
            requestId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET projects/{project}/zones/{zone}/instances/{instance}/setMachineResources
/// Changes the number `and/or` type of accelerator for a stopped instance to the values specified in the request.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_instances_set_machine_resources_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_instances_set_machine_resources_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_instances_set_machine_resources_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/zones/{zone}/instances/{instance}/setMachineResources
/// Changes the number `and/or` type of accelerator for a stopped instance to the values specified in the request.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_instances_set_machine_resources_builder()` + `compute_instances_set_machine_resources_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_instances_set_machine_resources(
    client: &SimpleHttpClient,
    instance: &str,
    project: &str,
    zone: &str,
    requestId: Option<&str>,
    body: &InstancesSetMachineResourcesRequest,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_instances_set_machine_resources_builder(
        client, instance, project, zone, requestId, body,
    )?;
    compute_instances_set_machine_resources_execute(builder)
}

/// GET projects/{project}/zones/{zone}/instances/{instance}/setMachineType
/// Changes the machine type for a stopped instance to the machine type specified in the request.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_instances_set_machine_type_execute()` to send, or `compute_instances_set_machine_type` for simplest API.

pub fn compute_instances_set_machine_type_builder(
    client: &SimpleHttpClient,
    instance: &str,
    project: &str,
    zone: &str,
    requestId: Option<&str>,
    body: &InstancesSetMachineTypeRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/zones/{}/instances/{}/setMachineType",
        instance, project, zone,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "requestId",
            requestId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET projects/{project}/zones/{zone}/instances/{instance}/setMachineType
/// Changes the machine type for a stopped instance to the machine type specified in the request.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_instances_set_machine_type_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_instances_set_machine_type_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_instances_set_machine_type_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/zones/{zone}/instances/{instance}/setMachineType
/// Changes the machine type for a stopped instance to the machine type specified in the request.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_instances_set_machine_type_builder()` + `compute_instances_set_machine_type_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_instances_set_machine_type(
    client: &SimpleHttpClient,
    instance: &str,
    project: &str,
    zone: &str,
    requestId: Option<&str>,
    body: &InstancesSetMachineTypeRequest,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_instances_set_machine_type_builder(
        client, instance, project, zone, requestId, body,
    )?;
    compute_instances_set_machine_type_execute(builder)
}

/// GET projects/{project}/zones/{zone}/instances/{instance}/setMetadata
/// Sets metadata for the specified instance to the data included in the request.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_instances_set_metadata_execute()` to send, or `compute_instances_set_metadata` for simplest API.

pub fn compute_instances_set_metadata_builder(
    client: &SimpleHttpClient,
    instance: &str,
    project: &str,
    zone: &str,
    requestId: Option<&str>,
    body: &Metadata,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/zones/{}/instances/{}/setMetadata",
        instance, project, zone,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "requestId",
            requestId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET projects/{project}/zones/{zone}/instances/{instance}/setMetadata
/// Sets metadata for the specified instance to the data included in the request.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_instances_set_metadata_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_instances_set_metadata_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_instances_set_metadata_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/zones/{zone}/instances/{instance}/setMetadata
/// Sets metadata for the specified instance to the data included in the request.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_instances_set_metadata_builder()` + `compute_instances_set_metadata_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_instances_set_metadata(
    client: &SimpleHttpClient,
    instance: &str,
    project: &str,
    zone: &str,
    requestId: Option<&str>,
    body: &Metadata,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        compute_instances_set_metadata_builder(client, instance, project, zone, requestId, body)?;
    compute_instances_set_metadata_execute(builder)
}

/// GET projects/{project}/zones/{zone}/instances/{instance}/setMinCpuPlatform
/// Changes the minimum CPU platform that this instance should use. This method can only be called on a stopped instance. For more information, `readSpecifying` a Minimum CPU Platform.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_instances_set_min_cpu_platform_execute()` to send, or `compute_instances_set_min_cpu_platform` for simplest API.

pub fn compute_instances_set_min_cpu_platform_builder(
    client: &SimpleHttpClient,
    instance: &str,
    project: &str,
    zone: &str,
    requestId: Option<&str>,
    body: &InstancesSetMinCpuPlatformRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/zones/{}/instances/{}/setMinCpuPlatform",
        instance, project, zone,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "requestId",
            requestId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET projects/{project}/zones/{zone}/instances/{instance}/setMinCpuPlatform
/// Changes the minimum CPU platform that this instance should use. This method can only be called on a stopped instance. For more information, `readSpecifying` a Minimum CPU Platform.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_instances_set_min_cpu_platform_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_instances_set_min_cpu_platform_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_instances_set_min_cpu_platform_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/zones/{zone}/instances/{instance}/setMinCpuPlatform
/// Changes the minimum CPU platform that this instance should use. This method can only be called on a stopped instance. For more information, `readSpecifying` a Minimum CPU Platform.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_instances_set_min_cpu_platform_builder()` + `compute_instances_set_min_cpu_platform_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_instances_set_min_cpu_platform(
    client: &SimpleHttpClient,
    instance: &str,
    project: &str,
    zone: &str,
    requestId: Option<&str>,
    body: &InstancesSetMinCpuPlatformRequest,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_instances_set_min_cpu_platform_builder(
        client, instance, project, zone, requestId, body,
    )?;
    compute_instances_set_min_cpu_platform_execute(builder)
}

/// GET projects/{project}/zones/{zone}/instances/{instance}/setName
/// Sets name of an instance.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_instances_set_name_execute()` to send, or `compute_instances_set_name` for simplest API.

pub fn compute_instances_set_name_builder(
    client: &SimpleHttpClient,
    instance: &str,
    project: &str,
    zone: &str,
    requestId: Option<&str>,
    body: &InstancesSetNameRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/zones/{}/instances/{}/setName",
        instance, project, zone,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "requestId",
            requestId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET projects/{project}/zones/{zone}/instances/{instance}/setName
/// Sets name of an instance.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_instances_set_name_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_instances_set_name_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_instances_set_name_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/zones/{zone}/instances/{instance}/setName
/// Sets name of an instance.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_instances_set_name_builder()` + `compute_instances_set_name_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_instances_set_name(
    client: &SimpleHttpClient,
    instance: &str,
    project: &str,
    zone: &str,
    requestId: Option<&str>,
    body: &InstancesSetNameRequest,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        compute_instances_set_name_builder(client, instance, project, zone, requestId, body)?;
    compute_instances_set_name_execute(builder)
}

/// GET projects/{project}/zones/{zone}/instances/{instance}/setScheduling
/// Sets an instance's scheduling options. You can only call this method on astopped instance, that is, a VM instance that is in a TERMINATED state. SeeInstance Life Cycle for more information on the possible instance states. For more information about setting scheduling options for a VM, `seeSet` VM host maintenance policy.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_instances_set_scheduling_execute()` to send, or `compute_instances_set_scheduling` for simplest API.

pub fn compute_instances_set_scheduling_builder(
    client: &SimpleHttpClient,
    instance: &str,
    project: &str,
    zone: &str,
    requestId: Option<&str>,
    body: &Scheduling,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/zones/{}/instances/{}/setScheduling",
        instance, project, zone,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "requestId",
            requestId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET projects/{project}/zones/{zone}/instances/{instance}/setScheduling
/// Sets an instance's scheduling options. You can only call this method on astopped instance, that is, a VM instance that is in a TERMINATED state. SeeInstance Life Cycle for more information on the possible instance states. For more information about setting scheduling options for a VM, `seeSet` VM host maintenance policy.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_instances_set_scheduling_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_instances_set_scheduling_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_instances_set_scheduling_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/zones/{zone}/instances/{instance}/setScheduling
/// Sets an instance's scheduling options. You can only call this method on astopped instance, that is, a VM instance that is in a TERMINATED state. SeeInstance Life Cycle for more information on the possible instance states. For more information about setting scheduling options for a VM, `seeSet` VM host maintenance policy.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_instances_set_scheduling_builder()` + `compute_instances_set_scheduling_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_instances_set_scheduling(
    client: &SimpleHttpClient,
    instance: &str,
    project: &str,
    zone: &str,
    requestId: Option<&str>,
    body: &Scheduling,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        compute_instances_set_scheduling_builder(client, instance, project, zone, requestId, body)?;
    compute_instances_set_scheduling_execute(builder)
}

/// GET projects/{project}/zones/{zone}/instances/{instance}/setSecurityPolicy
/// Sets the Google Cloud Armor security policy for the specified instance. For more information, `seeGoogle` Cloud Armor Overview
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_instances_set_security_policy_execute()` to send, or `compute_instances_set_security_policy` for simplest API.

pub fn compute_instances_set_security_policy_builder(
    client: &SimpleHttpClient,
    instance: &str,
    project: &str,
    zone: &str,
    requestId: Option<&str>,
    body: &InstancesSetSecurityPolicyRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/zones/{}/instances/{}/setSecurityPolicy",
        instance, project, zone,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "requestId",
            requestId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET projects/{project}/zones/{zone}/instances/{instance}/setSecurityPolicy
/// Sets the Google Cloud Armor security policy for the specified instance. For more information, `seeGoogle` Cloud Armor Overview
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_instances_set_security_policy_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_instances_set_security_policy_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_instances_set_security_policy_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/zones/{zone}/instances/{instance}/setSecurityPolicy
/// Sets the Google Cloud Armor security policy for the specified instance. For more information, `seeGoogle` Cloud Armor Overview
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_instances_set_security_policy_builder()` + `compute_instances_set_security_policy_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_instances_set_security_policy(
    client: &SimpleHttpClient,
    instance: &str,
    project: &str,
    zone: &str,
    requestId: Option<&str>,
    body: &InstancesSetSecurityPolicyRequest,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_instances_set_security_policy_builder(
        client, instance, project, zone, requestId, body,
    )?;
    compute_instances_set_security_policy_execute(builder)
}

/// GET projects/{project}/zones/{zone}/instances/{instance}/setServiceAccount
/// Sets the service account on the instance. For more information, `readChanging` the service account and access scopes for an instance.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_instances_set_service_account_execute()` to send, or `compute_instances_set_service_account` for simplest API.

pub fn compute_instances_set_service_account_builder(
    client: &SimpleHttpClient,
    instance: &str,
    project: &str,
    zone: &str,
    requestId: Option<&str>,
    body: &InstancesSetServiceAccountRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/zones/{}/instances/{}/setServiceAccount",
        instance, project, zone,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "requestId",
            requestId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET projects/{project}/zones/{zone}/instances/{instance}/setServiceAccount
/// Sets the service account on the instance. For more information, `readChanging` the service account and access scopes for an instance.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_instances_set_service_account_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_instances_set_service_account_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_instances_set_service_account_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/zones/{zone}/instances/{instance}/setServiceAccount
/// Sets the service account on the instance. For more information, `readChanging` the service account and access scopes for an instance.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_instances_set_service_account_builder()` + `compute_instances_set_service_account_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_instances_set_service_account(
    client: &SimpleHttpClient,
    instance: &str,
    project: &str,
    zone: &str,
    requestId: Option<&str>,
    body: &InstancesSetServiceAccountRequest,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_instances_set_service_account_builder(
        client, instance, project, zone, requestId, body,
    )?;
    compute_instances_set_service_account_execute(builder)
}

/// GET projects/{project}/zones/{zone}/instances/{instance}/setShieldedInstanceIntegrityPolicy
/// Sets the Shielded Instance integrity policy for an instance. You can only use this method on a running instance. This method supports PATCH semantics and uses the JSON merge patch format and processing rules.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_instances_set_shielded_instance_integrity_policy_execute()` to send, or `compute_instances_set_shielded_instance_integrity_policy` for simplest API.

pub fn compute_instances_set_shielded_instance_integrity_policy_builder(
    client: &SimpleHttpClient,
    instance: &str,
    project: &str,
    zone: &str,
    requestId: Option<&str>,
    body: &ShieldedInstanceIntegrityPolicy,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/zones/{}/instances/{}/setShieldedInstanceIntegrityPolicy",
        instance,
        project,
        zone,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "requestId",
            requestId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET projects/{project}/zones/{zone}/instances/{instance}/setShieldedInstanceIntegrityPolicy
/// Sets the Shielded Instance integrity policy for an instance. You can only use this method on a running instance. This method supports PATCH semantics and uses the JSON merge patch format and processing rules.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_instances_set_shielded_instance_integrity_policy_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_instances_set_shielded_instance_integrity_policy_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_instances_set_shielded_instance_integrity_policy_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/zones/{zone}/instances/{instance}/setShieldedInstanceIntegrityPolicy
/// Sets the Shielded Instance integrity policy for an instance. You can only use this method on a running instance. This method supports PATCH semantics and uses the JSON merge patch format and processing rules.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_instances_set_shielded_instance_integrity_policy_builder()` + `compute_instances_set_shielded_instance_integrity_policy_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_instances_set_shielded_instance_integrity_policy(
    client: &SimpleHttpClient,
    instance: &str,
    project: &str,
    zone: &str,
    requestId: Option<&str>,
    body: &ShieldedInstanceIntegrityPolicy,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_instances_set_shielded_instance_integrity_policy_builder(
        client, instance, project, zone, requestId, body,
    )?;
    compute_instances_set_shielded_instance_integrity_policy_execute(builder)
}

/// GET projects/{project}/zones/{zone}/instances/{instance}/setTags
/// Sets network tags for the specified instance to the data included in the request.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_instances_set_tags_execute()` to send, or `compute_instances_set_tags` for simplest API.

pub fn compute_instances_set_tags_builder(
    client: &SimpleHttpClient,
    instance: &str,
    project: &str,
    zone: &str,
    requestId: Option<&str>,
    body: &Tags,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/zones/{}/instances/{}/setTags",
        instance, project, zone,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "requestId",
            requestId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET projects/{project}/zones/{zone}/instances/{instance}/setTags
/// Sets network tags for the specified instance to the data included in the request.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_instances_set_tags_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_instances_set_tags_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_instances_set_tags_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/zones/{zone}/instances/{instance}/setTags
/// Sets network tags for the specified instance to the data included in the request.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_instances_set_tags_builder()` + `compute_instances_set_tags_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_instances_set_tags(
    client: &SimpleHttpClient,
    instance: &str,
    project: &str,
    zone: &str,
    requestId: Option<&str>,
    body: &Tags,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        compute_instances_set_tags_builder(client, instance, project, zone, requestId, body)?;
    compute_instances_set_tags_execute(builder)
}

/// GET projects/{project}/zones/{zone}/instances/{instance}/simulateMaintenanceEvent
/// Simulates a host maintenance event on a VM. For more information, see Simulate a host maintenance event.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_instances_simulate_maintenance_event_execute()` to send, or `compute_instances_simulate_maintenance_event` for simplest API.

pub fn compute_instances_simulate_maintenance_event_builder(
    client: &SimpleHttpClient,
    instance: &str,
    project: &str,
    zone: &str,
    requestId: Option<&str>,
    withExtendedNotifications: Option<bool>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/zones/{}/instances/{}/simulateMaintenanceEvent",
        instance, project, zone,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "requestId",
            requestId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "withExtendedNotifications",
            withExtendedNotifications
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET projects/{project}/zones/{zone}/instances/{instance}/simulateMaintenanceEvent
/// Simulates a host maintenance event on a VM. For more information, see Simulate a host maintenance event.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_instances_simulate_maintenance_event_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_instances_simulate_maintenance_event_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_instances_simulate_maintenance_event_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/zones/{zone}/instances/{instance}/simulateMaintenanceEvent
/// Simulates a host maintenance event on a VM. For more information, see Simulate a host maintenance event.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_instances_simulate_maintenance_event_builder()` + `compute_instances_simulate_maintenance_event_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_instances_simulate_maintenance_event(
    client: &SimpleHttpClient,
    instance: &str,
    project: &str,
    zone: &str,
    requestId: Option<&str>,
    withExtendedNotifications: Option<bool>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_instances_simulate_maintenance_event_builder(
        client,
        instance,
        project,
        zone,
        requestId,
        withExtendedNotifications,
    )?;
    compute_instances_simulate_maintenance_event_execute(builder)
}

/// GET projects/{project}/zones/{zone}/instances/{instance}/start
/// Starts an instance that was stopped using theinstances().stop method. For more information, `seeRestart` an instance.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_instances_start_execute()` to send, or `compute_instances_start` for simplest API.

pub fn compute_instances_start_builder(
    client: &SimpleHttpClient,
    instance: &str,
    project: &str,
    zone: &str,
    requestId: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/zones/{}/instances/{}/start",
        instance, project, zone,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "requestId",
            requestId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET projects/{project}/zones/{zone}/instances/{instance}/start
/// Starts an instance that was stopped using theinstances().stop method. For more information, `seeRestart` an instance.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_instances_start_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_instances_start_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_instances_start_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/zones/{zone}/instances/{instance}/start
/// Starts an instance that was stopped using theinstances().stop method. For more information, `seeRestart` an instance.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_instances_start_builder()` + `compute_instances_start_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_instances_start(
    client: &SimpleHttpClient,
    instance: &str,
    project: &str,
    zone: &str,
    requestId: Option<&str>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_instances_start_builder(client, instance, project, zone, requestId)?;
    compute_instances_start_execute(builder)
}

/// GET projects/{project}/zones/{zone}/instances/{instance}/startWithEncryptionKey
/// Starts an instance that was stopped using theinstances().stop method. For more information, `seeRestart` an instance.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_instances_start_with_encryption_key_execute()` to send, or `compute_instances_start_with_encryption_key` for simplest API.

pub fn compute_instances_start_with_encryption_key_builder(
    client: &SimpleHttpClient,
    instance: &str,
    project: &str,
    zone: &str,
    requestId: Option<&str>,
    body: &InstancesStartWithEncryptionKeyRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/zones/{}/instances/{}/startWithEncryptionKey",
        instance, project, zone,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "requestId",
            requestId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET projects/{project}/zones/{zone}/instances/{instance}/startWithEncryptionKey
/// Starts an instance that was stopped using theinstances().stop method. For more information, `seeRestart` an instance.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_instances_start_with_encryption_key_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_instances_start_with_encryption_key_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_instances_start_with_encryption_key_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/zones/{zone}/instances/{instance}/startWithEncryptionKey
/// Starts an instance that was stopped using theinstances().stop method. For more information, `seeRestart` an instance.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_instances_start_with_encryption_key_builder()` + `compute_instances_start_with_encryption_key_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_instances_start_with_encryption_key(
    client: &SimpleHttpClient,
    instance: &str,
    project: &str,
    zone: &str,
    requestId: Option<&str>,
    body: &InstancesStartWithEncryptionKeyRequest,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_instances_start_with_encryption_key_builder(
        client, instance, project, zone, requestId, body,
    )?;
    compute_instances_start_with_encryption_key_execute(builder)
}

/// GET projects/{project}/zones/{zone}/instances/{instance}/stop
/// Stops a running instance, shutting it down cleanly, and allows you to restart the instance at a later time. Stopped instances do not incur VM usage charges while they are stopped. However, resources that the VM is using, such as persistent disks and static IP addresses, will continue to be charged until they are deleted. For more information, `seeStopping` an instance.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_instances_stop_execute()` to send, or `compute_instances_stop` for simplest API.

pub fn compute_instances_stop_builder(
    client: &SimpleHttpClient,
    instance: &str,
    project: &str,
    zone: &str,
    discardLocalSsd: Option<bool>,
    requestId: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/zones/{}/instances/{}/stop",
        instance, project, zone,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "discardLocalSsd",
            discardLocalSsd
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "requestId",
            requestId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET projects/{project}/zones/{zone}/instances/{instance}/stop
/// Stops a running instance, shutting it down cleanly, and allows you to restart the instance at a later time. Stopped instances do not incur VM usage charges while they are stopped. However, resources that the VM is using, such as persistent disks and static IP addresses, will continue to be charged until they are deleted. For more information, `seeStopping` an instance.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_instances_stop_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_instances_stop_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_instances_stop_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/zones/{zone}/instances/{instance}/stop
/// Stops a running instance, shutting it down cleanly, and allows you to restart the instance at a later time. Stopped instances do not incur VM usage charges while they are stopped. However, resources that the VM is using, such as persistent disks and static IP addresses, will continue to be charged until they are deleted. For more information, `seeStopping` an instance.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_instances_stop_builder()` + `compute_instances_stop_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_instances_stop(
    client: &SimpleHttpClient,
    instance: &str,
    project: &str,
    zone: &str,
    discardLocalSsd: Option<bool>,
    requestId: Option<&str>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_instances_stop_builder(
        client,
        instance,
        project,
        zone,
        discardLocalSsd,
        requestId,
    )?;
    compute_instances_stop_execute(builder)
}

/// GET projects/{project}/zones/{zone}/instances/{instance}/suspend
/// This method suspends a running instance, saving its state to persistent storage, and allows you to resume the instance at a later time. Suspended instances have no compute costs (cores or RAM), and incur only storage charges for the saved VM memory and `localSSD` data. Any charged resources the virtual machine was using, such as persistent disks and static IP addresses, will continue to be charged while the instance is suspended. For more information, see Suspending and resuming an instance.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_instances_suspend_execute()` to send, or `compute_instances_suspend` for simplest API.

pub fn compute_instances_suspend_builder(
    client: &SimpleHttpClient,
    instance: &str,
    project: &str,
    zone: &str,
    discardLocalSsd: Option<bool>,
    requestId: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/zones/{}/instances/{}/suspend",
        instance, project, zone,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "discardLocalSsd",
            discardLocalSsd
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "requestId",
            requestId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET projects/{project}/zones/{zone}/instances/{instance}/suspend
/// This method suspends a running instance, saving its state to persistent storage, and allows you to resume the instance at a later time. Suspended instances have no compute costs (cores or RAM), and incur only storage charges for the saved VM memory and `localSSD` data. Any charged resources the virtual machine was using, such as persistent disks and static IP addresses, will continue to be charged while the instance is suspended. For more information, see Suspending and resuming an instance.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_instances_suspend_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_instances_suspend_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_instances_suspend_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/zones/{zone}/instances/{instance}/suspend
/// This method suspends a running instance, saving its state to persistent storage, and allows you to resume the instance at a later time. Suspended instances have no compute costs (cores or RAM), and incur only storage charges for the saved VM memory and `localSSD` data. Any charged resources the virtual machine was using, such as persistent disks and static IP addresses, will continue to be charged while the instance is suspended. For more information, see Suspending and resuming an instance.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_instances_suspend_builder()` + `compute_instances_suspend_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_instances_suspend(
    client: &SimpleHttpClient,
    instance: &str,
    project: &str,
    zone: &str,
    discardLocalSsd: Option<bool>,
    requestId: Option<&str>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_instances_suspend_builder(
        client,
        instance,
        project,
        zone,
        discardLocalSsd,
        requestId,
    )?;
    compute_instances_suspend_execute(builder)
}

/// GET projects/{project}/zones/{zone}/instances/{resource}/testIamPermissions
/// Returns permissions that a caller has on the specified resource.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_instances_test_iam_permissions_execute()` to send, or `compute_instances_test_iam_permissions` for simplest API.

pub fn compute_instances_test_iam_permissions_builder(
    client: &SimpleHttpClient,
    project: &str,
    resource: &str,
    zone: &str,
    body: &TestPermissionsRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/zones/{}/instances/{}/testIamPermissions",
        project, resource, zone,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET projects/{project}/zones/{zone}/instances/{resource}/testIamPermissions
/// Returns permissions that a caller has on the specified resource.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_instances_test_iam_permissions_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_instances_test_iam_permissions_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_instances_test_iam_permissions_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<TestPermissionsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: TestPermissionsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/zones/{zone}/instances/{resource}/testIamPermissions
/// Returns permissions that a caller has on the specified resource.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_instances_test_iam_permissions_builder()` + `compute_instances_test_iam_permissions_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_instances_test_iam_permissions(
    client: &SimpleHttpClient,
    project: &str,
    resource: &str,
    zone: &str,
    body: &TestPermissionsRequest,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<TestPermissionsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        compute_instances_test_iam_permissions_builder(client, project, resource, zone, body)?;
    compute_instances_test_iam_permissions_execute(builder)
}

/// GET projects/{project}/zones/{zone}/instances/{instance}
/// Updates an instance only if the necessary resources are available. This method can update only a specific set of instance properties. See Updating a running instance for a list of updatable instance properties.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_instances_update_execute()` to send, or `compute_instances_update` for simplest API.

pub fn compute_instances_update_builder(
    client: &SimpleHttpClient,
    instance: &str,
    project: &str,
    zone: &str,
    discardLocalSsd: Option<bool>,
    minimalAction: Option<&str>,
    mostDisruptiveAllowedAction: Option<&str>,
    requestId: Option<&str>,
    body: &Instance,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/zones/{}/instances/{}",
        instance, project, zone,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "discardLocalSsd",
            discardLocalSsd
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "minimalAction",
            minimalAction
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "mostDisruptiveAllowedAction",
            mostDisruptiveAllowedAction
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "requestId",
            requestId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET projects/{project}/zones/{zone}/instances/{instance}
/// Updates an instance only if the necessary resources are available. This method can update only a specific set of instance properties. See Updating a running instance for a list of updatable instance properties.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_instances_update_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_instances_update_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_instances_update_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/zones/{zone}/instances/{instance}
/// Updates an instance only if the necessary resources are available. This method can update only a specific set of instance properties. See Updating a running instance for a list of updatable instance properties.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_instances_update_builder()` + `compute_instances_update_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_instances_update(
    client: &SimpleHttpClient,
    instance: &str,
    project: &str,
    zone: &str,
    discardLocalSsd: Option<bool>,
    minimalAction: Option<&str>,
    mostDisruptiveAllowedAction: Option<&str>,
    requestId: Option<&str>,
    body: &Instance,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_instances_update_builder(
        client,
        instance,
        project,
        zone,
        discardLocalSsd,
        minimalAction,
        mostDisruptiveAllowedAction,
        requestId,
        body,
    )?;
    compute_instances_update_execute(builder)
}

/// GET projects/{project}/zones/{zone}/instances/{instance}/updateAccessConfig
/// Updates the specified access config from an instance's network interface with the data included in the request. This method `supportsPATCH` semantics and uses `theJSON` merge patch format and processing rules.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_instances_update_access_config_execute()` to send, or `compute_instances_update_access_config` for simplest API.

pub fn compute_instances_update_access_config_builder(
    client: &SimpleHttpClient,
    instance: &str,
    project: &str,
    zone: &str,
    networkInterface: Option<&str>,
    requestId: Option<&str>,
    body: &AccessConfig,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/zones/{}/instances/{}/updateAccessConfig",
        instance, project, zone,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "networkInterface",
            networkInterface
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "requestId",
            requestId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET projects/{project}/zones/{zone}/instances/{instance}/updateAccessConfig
/// Updates the specified access config from an instance's network interface with the data included in the request. This method `supportsPATCH` semantics and uses `theJSON` merge patch format and processing rules.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_instances_update_access_config_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_instances_update_access_config_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_instances_update_access_config_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/zones/{zone}/instances/{instance}/updateAccessConfig
/// Updates the specified access config from an instance's network interface with the data included in the request. This method `supportsPATCH` semantics and uses `theJSON` merge patch format and processing rules.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_instances_update_access_config_builder()` + `compute_instances_update_access_config_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_instances_update_access_config(
    client: &SimpleHttpClient,
    instance: &str,
    project: &str,
    zone: &str,
    networkInterface: Option<&str>,
    requestId: Option<&str>,
    body: &AccessConfig,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_instances_update_access_config_builder(
        client,
        instance,
        project,
        zone,
        networkInterface,
        requestId,
        body,
    )?;
    compute_instances_update_access_config_execute(builder)
}

/// GET projects/{project}/zones/{zone}/instances/{instance}/updateDisplayDevice
/// Updates the Display config for a VM instance. You can only use this method on a stopped VM instance. This method `supportsPATCH` semantics and uses `theJSON` merge patch format and processing rules.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_instances_update_display_device_execute()` to send, or `compute_instances_update_display_device` for simplest API.

pub fn compute_instances_update_display_device_builder(
    client: &SimpleHttpClient,
    instance: &str,
    project: &str,
    zone: &str,
    requestId: Option<&str>,
    body: &DisplayDevice,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/zones/{}/instances/{}/updateDisplayDevice",
        instance, project, zone,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "requestId",
            requestId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET projects/{project}/zones/{zone}/instances/{instance}/updateDisplayDevice
/// Updates the Display config for a VM instance. You can only use this method on a stopped VM instance. This method `supportsPATCH` semantics and uses `theJSON` merge patch format and processing rules.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_instances_update_display_device_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_instances_update_display_device_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_instances_update_display_device_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/zones/{zone}/instances/{instance}/updateDisplayDevice
/// Updates the Display config for a VM instance. You can only use this method on a stopped VM instance. This method `supportsPATCH` semantics and uses `theJSON` merge patch format and processing rules.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_instances_update_display_device_builder()` + `compute_instances_update_display_device_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_instances_update_display_device(
    client: &SimpleHttpClient,
    instance: &str,
    project: &str,
    zone: &str,
    requestId: Option<&str>,
    body: &DisplayDevice,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_instances_update_display_device_builder(
        client, instance, project, zone, requestId, body,
    )?;
    compute_instances_update_display_device_execute(builder)
}

/// GET projects/{project}/zones/{zone}/instances/{instance}/updateNetworkInterface
/// Updates an instance's network interface. This method can only update an interface's alias IP range and attached network. See Modifying alias IP ranges for an existing instance for instructions on changing alias IP ranges. See Migrating a VM between networks for instructions on migrating an interface. This method follows PATCH semantics.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_instances_update_network_interface_execute()` to send, or `compute_instances_update_network_interface` for simplest API.

pub fn compute_instances_update_network_interface_builder(
    client: &SimpleHttpClient,
    instance: &str,
    project: &str,
    zone: &str,
    networkInterface: Option<&str>,
    requestId: Option<&str>,
    body: &NetworkInterface,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/zones/{}/instances/{}/updateNetworkInterface",
        instance, project, zone,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "networkInterface",
            networkInterface
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "requestId",
            requestId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET projects/{project}/zones/{zone}/instances/{instance}/updateNetworkInterface
/// Updates an instance's network interface. This method can only update an interface's alias IP range and attached network. See Modifying alias IP ranges for an existing instance for instructions on changing alias IP ranges. See Migrating a VM between networks for instructions on migrating an interface. This method follows PATCH semantics.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_instances_update_network_interface_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_instances_update_network_interface_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_instances_update_network_interface_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/zones/{zone}/instances/{instance}/updateNetworkInterface
/// Updates an instance's network interface. This method can only update an interface's alias IP range and attached network. See Modifying alias IP ranges for an existing instance for instructions on changing alias IP ranges. See Migrating a VM between networks for instructions on migrating an interface. This method follows PATCH semantics.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_instances_update_network_interface_builder()` + `compute_instances_update_network_interface_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_instances_update_network_interface(
    client: &SimpleHttpClient,
    instance: &str,
    project: &str,
    zone: &str,
    networkInterface: Option<&str>,
    requestId: Option<&str>,
    body: &NetworkInterface,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_instances_update_network_interface_builder(
        client,
        instance,
        project,
        zone,
        networkInterface,
        requestId,
        body,
    )?;
    compute_instances_update_network_interface_execute(builder)
}

/// GET projects/{project}/zones/{zone}/instances/{instance}/updateShieldedInstanceConfig
/// Updates the Shielded Instance config for an instance. You can only use this method on a stopped instance. This method `supportsPATCH` semantics and uses `theJSON` merge patch format and processing rules.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_instances_update_shielded_instance_config_execute()` to send, or `compute_instances_update_shielded_instance_config` for simplest API.

pub fn compute_instances_update_shielded_instance_config_builder(
    client: &SimpleHttpClient,
    instance: &str,
    project: &str,
    zone: &str,
    requestId: Option<&str>,
    body: &ShieldedInstanceConfig,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/zones/{}/instances/{}/updateShieldedInstanceConfig",
        instance, project, zone,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "requestId",
            requestId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET projects/{project}/zones/{zone}/instances/{instance}/updateShieldedInstanceConfig
/// Updates the Shielded Instance config for an instance. You can only use this method on a stopped instance. This method `supportsPATCH` semantics and uses `theJSON` merge patch format and processing rules.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_instances_update_shielded_instance_config_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_instances_update_shielded_instance_config_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_instances_update_shielded_instance_config_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/zones/{zone}/instances/{instance}/updateShieldedInstanceConfig
/// Updates the Shielded Instance config for an instance. You can only use this method on a stopped instance. This method `supportsPATCH` semantics and uses `theJSON` merge patch format and processing rules.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_instances_update_shielded_instance_config_builder()` + `compute_instances_update_shielded_instance_config_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_instances_update_shielded_instance_config(
    client: &SimpleHttpClient,
    instance: &str,
    project: &str,
    zone: &str,
    requestId: Option<&str>,
    body: &ShieldedInstanceConfig,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_instances_update_shielded_instance_config_builder(
        client, instance, project, zone, requestId, body,
    )?;
    compute_instances_update_shielded_instance_config_execute(builder)
}

/// GET projects/{project}/aggregated/instantSnapshots
/// Retrieves an aggregated list of `instantSnapshots`.  To prevent failure, Google recommends that you set the `returnPartialSuccess` parameter to `true`.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_instant_snapshots_aggregated_list_execute()` to send, or `compute_instant_snapshots_aggregated_list` for simplest API.

pub fn compute_instant_snapshots_aggregated_list_builder(
    client: &SimpleHttpClient,
    project: &str,
    filter: Option<&str>,
    includeAllScopes: Option<bool>,
    maxResults: Option<i32>,
    orderBy: Option<&str>,
    pageToken: Option<&str>,
    returnPartialSuccess: Option<bool>,
    serviceProjectNumber: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/aggregated/instantSnapshots",
        project,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "filter",
            filter
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "includeAllScopes",
            includeAllScopes
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "maxResults",
            maxResults
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "orderBy",
            orderBy
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageToken",
            pageToken
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "returnPartialSuccess",
            returnPartialSuccess
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "serviceProjectNumber",
            serviceProjectNumber
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET projects/{project}/aggregated/instantSnapshots
/// Retrieves an aggregated list of `instantSnapshots`.  To prevent failure, Google recommends that you set the `returnPartialSuccess` parameter to `true`.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_instant_snapshots_aggregated_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_instant_snapshots_aggregated_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_instant_snapshots_aggregated_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<InstantSnapshotAggregatedList>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: InstantSnapshotAggregatedList = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/aggregated/instantSnapshots
/// Retrieves an aggregated list of `instantSnapshots`.  To prevent failure, Google recommends that you set the `returnPartialSuccess` parameter to `true`.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_instant_snapshots_aggregated_list_builder()` + `compute_instant_snapshots_aggregated_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_instant_snapshots_aggregated_list(
    client: &SimpleHttpClient,
    project: &str,
    filter: Option<&str>,
    includeAllScopes: Option<bool>,
    maxResults: Option<i32>,
    orderBy: Option<&str>,
    pageToken: Option<&str>,
    returnPartialSuccess: Option<bool>,
    serviceProjectNumber: Option<&str>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<InstantSnapshotAggregatedList>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = compute_instant_snapshots_aggregated_list_builder(
        client,
        project,
        filter,
        includeAllScopes,
        maxResults,
        orderBy,
        pageToken,
        returnPartialSuccess,
        serviceProjectNumber,
    )?;
    compute_instant_snapshots_aggregated_list_execute(builder)
}

/// GET projects/{project}/zones/{zone}/instantSnapshots/{instantSnapshot}
/// Deletes the specified InstantSnapshot resource. Keep in mind that deleting a single `instantSnapshot` might not necessarily delete all the data on that `instantSnapshot`. If any data on the `instantSnapshot` that is marked for deletion is needed for subsequent `instantSnapshots`, the data will be moved to the next corresponding `instantSnapshot`.  For more information, `seeDeleting` `instantSnapshots`.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_instant_snapshots_delete_execute()` to send, or `compute_instant_snapshots_delete` for simplest API.

pub fn compute_instant_snapshots_delete_builder(
    client: &SimpleHttpClient,
    instantSnapshot: &str,
    project: &str,
    zone: &str,
    requestId: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/zones/{}/instantSnapshots/{}",
        instantSnapshot, project, zone,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "requestId",
            requestId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET projects/{project}/zones/{zone}/instantSnapshots/{instantSnapshot}
/// Deletes the specified InstantSnapshot resource. Keep in mind that deleting a single `instantSnapshot` might not necessarily delete all the data on that `instantSnapshot`. If any data on the `instantSnapshot` that is marked for deletion is needed for subsequent `instantSnapshots`, the data will be moved to the next corresponding `instantSnapshot`.  For more information, `seeDeleting` `instantSnapshots`.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_instant_snapshots_delete_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_instant_snapshots_delete_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_instant_snapshots_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/zones/{zone}/instantSnapshots/{instantSnapshot}
/// Deletes the specified InstantSnapshot resource. Keep in mind that deleting a single `instantSnapshot` might not necessarily delete all the data on that `instantSnapshot`. If any data on the `instantSnapshot` that is marked for deletion is needed for subsequent `instantSnapshots`, the data will be moved to the next corresponding `instantSnapshot`.  For more information, `seeDeleting` `instantSnapshots`.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_instant_snapshots_delete_builder()` + `compute_instant_snapshots_delete_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_instant_snapshots_delete(
    client: &SimpleHttpClient,
    instantSnapshot: &str,
    project: &str,
    zone: &str,
    requestId: Option<&str>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_instant_snapshots_delete_builder(
        client,
        instantSnapshot,
        project,
        zone,
        requestId,
    )?;
    compute_instant_snapshots_delete_execute(builder)
}

/// GET projects/{project}/zones/{zone}/instantSnapshots/{instantSnapshot}
/// Returns the specified InstantSnapshot resource in the specified zone.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_instant_snapshots_get_execute()` to send, or `compute_instant_snapshots_get` for simplest API.

pub fn compute_instant_snapshots_get_builder(
    client: &SimpleHttpClient,
    instantSnapshot: &str,
    project: &str,
    zone: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/zones/{}/instantSnapshots/{}",
        instantSnapshot, project, zone,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/zones/{zone}/instantSnapshots/{instantSnapshot}
/// Returns the specified InstantSnapshot resource in the specified zone.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_instant_snapshots_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_instant_snapshots_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_instant_snapshots_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<InstantSnapshot>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: InstantSnapshot = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/zones/{zone}/instantSnapshots/{instantSnapshot}
/// Returns the specified InstantSnapshot resource in the specified zone.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_instant_snapshots_get_builder()` + `compute_instant_snapshots_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_instant_snapshots_get(
    client: &SimpleHttpClient,
    instantSnapshot: &str,
    project: &str,
    zone: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<InstantSnapshot>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = compute_instant_snapshots_get_builder(client, instantSnapshot, project, zone)?;
    compute_instant_snapshots_get_execute(builder)
}

/// GET projects/{project}/zones/{zone}/instantSnapshots/{resource}/getIamPolicy
/// Gets the access control policy for a resource. May be empty if no such policy or resource exists.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_instant_snapshots_get_iam_policy_execute()` to send, or `compute_instant_snapshots_get_iam_policy` for simplest API.

pub fn compute_instant_snapshots_get_iam_policy_builder(
    client: &SimpleHttpClient,
    project: &str,
    resource: &str,
    zone: &str,
    optionsRequestedPolicyVersion: Option<i32>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/zones/{}/instantSnapshots/{}/getIamPolicy",
        project, resource, zone,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "optionsRequestedPolicyVersion",
            optionsRequestedPolicyVersion
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET projects/{project}/zones/{zone}/instantSnapshots/{resource}/getIamPolicy
/// Gets the access control policy for a resource. May be empty if no such policy or resource exists.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_instant_snapshots_get_iam_policy_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_instant_snapshots_get_iam_policy_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_instant_snapshots_get_iam_policy_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Policy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Policy = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/zones/{zone}/instantSnapshots/{resource}/getIamPolicy
/// Gets the access control policy for a resource. May be empty if no such policy or resource exists.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_instant_snapshots_get_iam_policy_builder()` + `compute_instant_snapshots_get_iam_policy_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_instant_snapshots_get_iam_policy(
    client: &SimpleHttpClient,
    project: &str,
    resource: &str,
    zone: &str,
    optionsRequestedPolicyVersion: Option<i32>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Policy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_instant_snapshots_get_iam_policy_builder(
        client,
        project,
        resource,
        zone,
        optionsRequestedPolicyVersion,
    )?;
    compute_instant_snapshots_get_iam_policy_execute(builder)
}

/// GET projects/{project}/zones/{zone}/instantSnapshots
/// Creates an instant snapshot in the specified zone.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_instant_snapshots_insert_execute()` to send, or `compute_instant_snapshots_insert` for simplest API.

pub fn compute_instant_snapshots_insert_builder(
    client: &SimpleHttpClient,
    project: &str,
    zone: &str,
    requestId: Option<&str>,
    body: &InstantSnapshot,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/zones/{}/instantSnapshots",
        project, zone,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "requestId",
            requestId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET projects/{project}/zones/{zone}/instantSnapshots
/// Creates an instant snapshot in the specified zone.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_instant_snapshots_insert_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_instant_snapshots_insert_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_instant_snapshots_insert_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/zones/{zone}/instantSnapshots
/// Creates an instant snapshot in the specified zone.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_instant_snapshots_insert_builder()` + `compute_instant_snapshots_insert_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_instant_snapshots_insert(
    client: &SimpleHttpClient,
    project: &str,
    zone: &str,
    requestId: Option<&str>,
    body: &InstantSnapshot,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_instant_snapshots_insert_builder(client, project, zone, requestId, body)?;
    compute_instant_snapshots_insert_execute(builder)
}

/// GET projects/{project}/zones/{zone}/instantSnapshots
/// Retrieves the list of InstantSnapshot resources contained within the specified zone.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_instant_snapshots_list_execute()` to send, or `compute_instant_snapshots_list` for simplest API.

pub fn compute_instant_snapshots_list_builder(
    client: &SimpleHttpClient,
    project: &str,
    zone: &str,
    filter: Option<&str>,
    maxResults: Option<i32>,
    orderBy: Option<&str>,
    pageToken: Option<&str>,
    returnPartialSuccess: Option<bool>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/zones/{}/instantSnapshots",
        project, zone,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "filter",
            filter
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "maxResults",
            maxResults
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "orderBy",
            orderBy
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageToken",
            pageToken
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "returnPartialSuccess",
            returnPartialSuccess
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET projects/{project}/zones/{zone}/instantSnapshots
/// Retrieves the list of InstantSnapshot resources contained within the specified zone.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_instant_snapshots_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_instant_snapshots_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_instant_snapshots_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<InstantSnapshotList>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: InstantSnapshotList = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/zones/{zone}/instantSnapshots
/// Retrieves the list of InstantSnapshot resources contained within the specified zone.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_instant_snapshots_list_builder()` + `compute_instant_snapshots_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_instant_snapshots_list(
    client: &SimpleHttpClient,
    project: &str,
    zone: &str,
    filter: Option<&str>,
    maxResults: Option<i32>,
    orderBy: Option<&str>,
    pageToken: Option<&str>,
    returnPartialSuccess: Option<bool>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<InstantSnapshotList>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = compute_instant_snapshots_list_builder(
        client,
        project,
        zone,
        filter,
        maxResults,
        orderBy,
        pageToken,
        returnPartialSuccess,
    )?;
    compute_instant_snapshots_list_execute(builder)
}

/// GET projects/{project}/zones/{zone}/instantSnapshots/{resource}/setIamPolicy
/// Sets the access control policy on the specified resource. Replaces any existing policy.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_instant_snapshots_set_iam_policy_execute()` to send, or `compute_instant_snapshots_set_iam_policy` for simplest API.

pub fn compute_instant_snapshots_set_iam_policy_builder(
    client: &SimpleHttpClient,
    project: &str,
    resource: &str,
    zone: &str,
    body: &ZoneSetPolicyRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/zones/{}/instantSnapshots/{}/setIamPolicy",
        project, resource, zone,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET projects/{project}/zones/{zone}/instantSnapshots/{resource}/setIamPolicy
/// Sets the access control policy on the specified resource. Replaces any existing policy.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_instant_snapshots_set_iam_policy_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_instant_snapshots_set_iam_policy_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_instant_snapshots_set_iam_policy_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Policy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Policy = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/zones/{zone}/instantSnapshots/{resource}/setIamPolicy
/// Sets the access control policy on the specified resource. Replaces any existing policy.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_instant_snapshots_set_iam_policy_builder()` + `compute_instant_snapshots_set_iam_policy_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_instant_snapshots_set_iam_policy(
    client: &SimpleHttpClient,
    project: &str,
    resource: &str,
    zone: &str,
    body: &ZoneSetPolicyRequest,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Policy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        compute_instant_snapshots_set_iam_policy_builder(client, project, resource, zone, body)?;
    compute_instant_snapshots_set_iam_policy_execute(builder)
}

/// GET projects/{project}/zones/{zone}/instantSnapshots/{resource}/setLabels
/// Sets the labels on a `instantSnapshot` in the given zone. To learn more about labels, read the Labeling Resources documentation.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_instant_snapshots_set_labels_execute()` to send, or `compute_instant_snapshots_set_labels` for simplest API.

pub fn compute_instant_snapshots_set_labels_builder(
    client: &SimpleHttpClient,
    project: &str,
    resource: &str,
    zone: &str,
    requestId: Option<&str>,
    body: &ZoneSetLabelsRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/zones/{}/instantSnapshots/{}/setLabels",
        project, resource, zone,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "requestId",
            requestId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET projects/{project}/zones/{zone}/instantSnapshots/{resource}/setLabels
/// Sets the labels on a `instantSnapshot` in the given zone. To learn more about labels, read the Labeling Resources documentation.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_instant_snapshots_set_labels_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_instant_snapshots_set_labels_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_instant_snapshots_set_labels_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/zones/{zone}/instantSnapshots/{resource}/setLabels
/// Sets the labels on a `instantSnapshot` in the given zone. To learn more about labels, read the Labeling Resources documentation.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_instant_snapshots_set_labels_builder()` + `compute_instant_snapshots_set_labels_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_instant_snapshots_set_labels(
    client: &SimpleHttpClient,
    project: &str,
    resource: &str,
    zone: &str,
    requestId: Option<&str>,
    body: &ZoneSetLabelsRequest,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_instant_snapshots_set_labels_builder(
        client, project, resource, zone, requestId, body,
    )?;
    compute_instant_snapshots_set_labels_execute(builder)
}

/// GET projects/{project}/zones/{zone}/instantSnapshots/{resource}/testIamPermissions
/// Returns permissions that a caller has on the specified resource.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_instant_snapshots_test_iam_permissions_execute()` to send, or `compute_instant_snapshots_test_iam_permissions` for simplest API.

pub fn compute_instant_snapshots_test_iam_permissions_builder(
    client: &SimpleHttpClient,
    project: &str,
    resource: &str,
    zone: &str,
    body: &TestPermissionsRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/zones/{}/instantSnapshots/{}/testIamPermissions",
        project, resource, zone,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET projects/{project}/zones/{zone}/instantSnapshots/{resource}/testIamPermissions
/// Returns permissions that a caller has on the specified resource.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_instant_snapshots_test_iam_permissions_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_instant_snapshots_test_iam_permissions_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_instant_snapshots_test_iam_permissions_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<TestPermissionsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: TestPermissionsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/zones/{zone}/instantSnapshots/{resource}/testIamPermissions
/// Returns permissions that a caller has on the specified resource.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_instant_snapshots_test_iam_permissions_builder()` + `compute_instant_snapshots_test_iam_permissions_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_instant_snapshots_test_iam_permissions(
    client: &SimpleHttpClient,
    project: &str,
    resource: &str,
    zone: &str,
    body: &TestPermissionsRequest,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<TestPermissionsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = compute_instant_snapshots_test_iam_permissions_builder(
        client, project, resource, zone, body,
    )?;
    compute_instant_snapshots_test_iam_permissions_execute(builder)
}

/// GET projects/{project}/global/interconnectAttachmentGroups/{interconnectAttachmentGroup}
/// Deletes the specified InterconnectAttachmentGroup in the given scope
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_interconnect_attachment_groups_delete_execute()` to send, or `compute_interconnect_attachment_groups_delete` for simplest API.

pub fn compute_interconnect_attachment_groups_delete_builder(
    client: &SimpleHttpClient,
    interconnectAttachmentGroup: &str,
    project: &str,
    requestId: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/global/interconnectAttachmentGroups/{}",
        interconnectAttachmentGroup, project,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "requestId",
            requestId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET projects/{project}/global/interconnectAttachmentGroups/{interconnectAttachmentGroup}
/// Deletes the specified InterconnectAttachmentGroup in the given scope
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_interconnect_attachment_groups_delete_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_interconnect_attachment_groups_delete_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_interconnect_attachment_groups_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/global/interconnectAttachmentGroups/{interconnectAttachmentGroup}
/// Deletes the specified InterconnectAttachmentGroup in the given scope
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_interconnect_attachment_groups_delete_builder()` + `compute_interconnect_attachment_groups_delete_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_interconnect_attachment_groups_delete(
    client: &SimpleHttpClient,
    interconnectAttachmentGroup: &str,
    project: &str,
    requestId: Option<&str>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_interconnect_attachment_groups_delete_builder(
        client,
        interconnectAttachmentGroup,
        project,
        requestId,
    )?;
    compute_interconnect_attachment_groups_delete_execute(builder)
}

/// GET projects/{project}/global/interconnectAttachmentGroups/{interconnectAttachmentGroup}
/// Returns the specified InterconnectAttachmentGroup resource in the given scope.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_interconnect_attachment_groups_get_execute()` to send, or `compute_interconnect_attachment_groups_get` for simplest API.

pub fn compute_interconnect_attachment_groups_get_builder(
    client: &SimpleHttpClient,
    interconnectAttachmentGroup: &str,
    project: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/global/interconnectAttachmentGroups/{}",
        interconnectAttachmentGroup, project,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/global/interconnectAttachmentGroups/{interconnectAttachmentGroup}
/// Returns the specified InterconnectAttachmentGroup resource in the given scope.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_interconnect_attachment_groups_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_interconnect_attachment_groups_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_interconnect_attachment_groups_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<InterconnectAttachmentGroup>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: InterconnectAttachmentGroup = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/global/interconnectAttachmentGroups/{interconnectAttachmentGroup}
/// Returns the specified InterconnectAttachmentGroup resource in the given scope.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_interconnect_attachment_groups_get_builder()` + `compute_interconnect_attachment_groups_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_interconnect_attachment_groups_get(
    client: &SimpleHttpClient,
    interconnectAttachmentGroup: &str,
    project: &str,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<InterconnectAttachmentGroup>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = compute_interconnect_attachment_groups_get_builder(
        client,
        interconnectAttachmentGroup,
        project,
    )?;
    compute_interconnect_attachment_groups_get_execute(builder)
}

/// GET projects/{project}/global/interconnectAttachmentGroups/{resource}/getIamPolicy
/// Gets the access control policy for a resource. May be empty if no such policy or resource exists.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_interconnect_attachment_groups_get_iam_policy_execute()` to send, or `compute_interconnect_attachment_groups_get_iam_policy` for simplest API.

pub fn compute_interconnect_attachment_groups_get_iam_policy_builder(
    client: &SimpleHttpClient,
    project: &str,
    resource: &str,
    optionsRequestedPolicyVersion: Option<i32>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/global/interconnectAttachmentGroups/{}/getIamPolicy",
        project, resource,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "optionsRequestedPolicyVersion",
            optionsRequestedPolicyVersion
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET projects/{project}/global/interconnectAttachmentGroups/{resource}/getIamPolicy
/// Gets the access control policy for a resource. May be empty if no such policy or resource exists.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_interconnect_attachment_groups_get_iam_policy_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_interconnect_attachment_groups_get_iam_policy_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_interconnect_attachment_groups_get_iam_policy_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Policy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Policy = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/global/interconnectAttachmentGroups/{resource}/getIamPolicy
/// Gets the access control policy for a resource. May be empty if no such policy or resource exists.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_interconnect_attachment_groups_get_iam_policy_builder()` + `compute_interconnect_attachment_groups_get_iam_policy_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_interconnect_attachment_groups_get_iam_policy(
    client: &SimpleHttpClient,
    project: &str,
    resource: &str,
    optionsRequestedPolicyVersion: Option<i32>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Policy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_interconnect_attachment_groups_get_iam_policy_builder(
        client,
        project,
        resource,
        optionsRequestedPolicyVersion,
    )?;
    compute_interconnect_attachment_groups_get_iam_policy_execute(builder)
}

/// GET projects/{project}/global/interconnectAttachmentGroups/{interconnectAttachmentGroup}/getOperationalStatus
/// Returns the InterconnectAttachmentStatuses for the specified InterconnectAttachmentGroup resource.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_interconnect_attachment_groups_get_operational_status_execute()` to send, or `compute_interconnect_attachment_groups_get_operational_status` for simplest API.

pub fn compute_interconnect_attachment_groups_get_operational_status_builder(
    client: &SimpleHttpClient,
    interconnectAttachmentGroup: &str,
    project: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/global/interconnectAttachmentGroups/{}/getOperationalStatus",
        interconnectAttachmentGroup,
        project,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/global/interconnectAttachmentGroups/{interconnectAttachmentGroup}/getOperationalStatus
/// Returns the InterconnectAttachmentStatuses for the specified InterconnectAttachmentGroup resource.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_interconnect_attachment_groups_get_operational_status_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_interconnect_attachment_groups_get_operational_status_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_interconnect_attachment_groups_get_operational_status_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<InterconnectAttachmentGroupsGetOperationalStatusResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: InterconnectAttachmentGroupsGetOperationalStatusResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/global/interconnectAttachmentGroups/{interconnectAttachmentGroup}/getOperationalStatus
/// Returns the InterconnectAttachmentStatuses for the specified InterconnectAttachmentGroup resource.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_interconnect_attachment_groups_get_operational_status_builder()` + `compute_interconnect_attachment_groups_get_operational_status_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_interconnect_attachment_groups_get_operational_status(
    client: &SimpleHttpClient,
    interconnectAttachmentGroup: &str,
    project: &str,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<InterconnectAttachmentGroupsGetOperationalStatusResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = compute_interconnect_attachment_groups_get_operational_status_builder(
        client,
        interconnectAttachmentGroup,
        project,
    )?;
    compute_interconnect_attachment_groups_get_operational_status_execute(builder)
}

/// GET projects/{project}/global/interconnectAttachmentGroups
/// Creates a InterconnectAttachmentGroup in the specified project in the given scope using the parameters that are included in the request.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_interconnect_attachment_groups_insert_execute()` to send, or `compute_interconnect_attachment_groups_insert` for simplest API.

pub fn compute_interconnect_attachment_groups_insert_builder(
    client: &SimpleHttpClient,
    project: &str,
    requestId: Option<&str>,
    body: &InterconnectAttachmentGroup,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/global/interconnectAttachmentGroups",
        project,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "requestId",
            requestId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET projects/{project}/global/interconnectAttachmentGroups
/// Creates a InterconnectAttachmentGroup in the specified project in the given scope using the parameters that are included in the request.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_interconnect_attachment_groups_insert_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_interconnect_attachment_groups_insert_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_interconnect_attachment_groups_insert_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/global/interconnectAttachmentGroups
/// Creates a InterconnectAttachmentGroup in the specified project in the given scope using the parameters that are included in the request.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_interconnect_attachment_groups_insert_builder()` + `compute_interconnect_attachment_groups_insert_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_interconnect_attachment_groups_insert(
    client: &SimpleHttpClient,
    project: &str,
    requestId: Option<&str>,
    body: &InterconnectAttachmentGroup,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        compute_interconnect_attachment_groups_insert_builder(client, project, requestId, body)?;
    compute_interconnect_attachment_groups_insert_execute(builder)
}

/// GET projects/{project}/global/interconnectAttachmentGroups
/// Lists the InterconnectAttachmentGroups for a project in the given scope.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_interconnect_attachment_groups_list_execute()` to send, or `compute_interconnect_attachment_groups_list` for simplest API.

pub fn compute_interconnect_attachment_groups_list_builder(
    client: &SimpleHttpClient,
    project: &str,
    filter: Option<&str>,
    maxResults: Option<i32>,
    orderBy: Option<&str>,
    pageToken: Option<&str>,
    returnPartialSuccess: Option<bool>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/global/interconnectAttachmentGroups",
        project,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "filter",
            filter
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "maxResults",
            maxResults
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "orderBy",
            orderBy
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageToken",
            pageToken
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "returnPartialSuccess",
            returnPartialSuccess
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET projects/{project}/global/interconnectAttachmentGroups
/// Lists the InterconnectAttachmentGroups for a project in the given scope.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_interconnect_attachment_groups_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_interconnect_attachment_groups_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_interconnect_attachment_groups_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<InterconnectAttachmentGroupsListResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: InterconnectAttachmentGroupsListResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/global/interconnectAttachmentGroups
/// Lists the InterconnectAttachmentGroups for a project in the given scope.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_interconnect_attachment_groups_list_builder()` + `compute_interconnect_attachment_groups_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_interconnect_attachment_groups_list(
    client: &SimpleHttpClient,
    project: &str,
    filter: Option<&str>,
    maxResults: Option<i32>,
    orderBy: Option<&str>,
    pageToken: Option<&str>,
    returnPartialSuccess: Option<bool>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<InterconnectAttachmentGroupsListResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = compute_interconnect_attachment_groups_list_builder(
        client,
        project,
        filter,
        maxResults,
        orderBy,
        pageToken,
        returnPartialSuccess,
    )?;
    compute_interconnect_attachment_groups_list_execute(builder)
}

/// GET projects/{project}/global/interconnectAttachmentGroups/{interconnectAttachmentGroup}
/// Patches the specified InterconnectAttachmentGroup resource with the data included in the request. This method supports PATCH semantics and `usesJSON` merge patch format and processing rules.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_interconnect_attachment_groups_patch_execute()` to send, or `compute_interconnect_attachment_groups_patch` for simplest API.

pub fn compute_interconnect_attachment_groups_patch_builder(
    client: &SimpleHttpClient,
    interconnectAttachmentGroup: &str,
    project: &str,
    requestId: Option<&str>,
    updateMask: Option<&str>,
    body: &InterconnectAttachmentGroup,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/global/interconnectAttachmentGroups/{}",
        interconnectAttachmentGroup, project,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "requestId",
            requestId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "updateMask",
            updateMask
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET projects/{project}/global/interconnectAttachmentGroups/{interconnectAttachmentGroup}
/// Patches the specified InterconnectAttachmentGroup resource with the data included in the request. This method supports PATCH semantics and `usesJSON` merge patch format and processing rules.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_interconnect_attachment_groups_patch_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_interconnect_attachment_groups_patch_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_interconnect_attachment_groups_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/global/interconnectAttachmentGroups/{interconnectAttachmentGroup}
/// Patches the specified InterconnectAttachmentGroup resource with the data included in the request. This method supports PATCH semantics and `usesJSON` merge patch format and processing rules.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_interconnect_attachment_groups_patch_builder()` + `compute_interconnect_attachment_groups_patch_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_interconnect_attachment_groups_patch(
    client: &SimpleHttpClient,
    interconnectAttachmentGroup: &str,
    project: &str,
    requestId: Option<&str>,
    updateMask: Option<&str>,
    body: &InterconnectAttachmentGroup,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_interconnect_attachment_groups_patch_builder(
        client,
        interconnectAttachmentGroup,
        project,
        requestId,
        updateMask,
        body,
    )?;
    compute_interconnect_attachment_groups_patch_execute(builder)
}

/// GET projects/{project}/global/interconnectAttachmentGroups/{resource}/setIamPolicy
/// Sets the access control policy on the specified resource. Replaces any existing policy.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_interconnect_attachment_groups_set_iam_policy_execute()` to send, or `compute_interconnect_attachment_groups_set_iam_policy` for simplest API.

pub fn compute_interconnect_attachment_groups_set_iam_policy_builder(
    client: &SimpleHttpClient,
    project: &str,
    resource: &str,
    body: &GlobalSetPolicyRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/global/interconnectAttachmentGroups/{}/setIamPolicy",
        project, resource,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET projects/{project}/global/interconnectAttachmentGroups/{resource}/setIamPolicy
/// Sets the access control policy on the specified resource. Replaces any existing policy.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_interconnect_attachment_groups_set_iam_policy_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_interconnect_attachment_groups_set_iam_policy_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_interconnect_attachment_groups_set_iam_policy_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Policy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Policy = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/global/interconnectAttachmentGroups/{resource}/setIamPolicy
/// Sets the access control policy on the specified resource. Replaces any existing policy.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_interconnect_attachment_groups_set_iam_policy_builder()` + `compute_interconnect_attachment_groups_set_iam_policy_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_interconnect_attachment_groups_set_iam_policy(
    client: &SimpleHttpClient,
    project: &str,
    resource: &str,
    body: &GlobalSetPolicyRequest,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Policy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_interconnect_attachment_groups_set_iam_policy_builder(
        client, project, resource, body,
    )?;
    compute_interconnect_attachment_groups_set_iam_policy_execute(builder)
}

/// GET projects/{project}/global/interconnectAttachmentGroups/{resource}/testIamPermissions
/// Returns permissions that a caller has on the specified resource.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_interconnect_attachment_groups_test_iam_permissions_execute()` to send, or `compute_interconnect_attachment_groups_test_iam_permissions` for simplest API.

pub fn compute_interconnect_attachment_groups_test_iam_permissions_builder(
    client: &SimpleHttpClient,
    project: &str,
    resource: &str,
    body: &TestPermissionsRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/global/interconnectAttachmentGroups/{}/testIamPermissions",
        project,
        resource,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET projects/{project}/global/interconnectAttachmentGroups/{resource}/testIamPermissions
/// Returns permissions that a caller has on the specified resource.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_interconnect_attachment_groups_test_iam_permissions_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_interconnect_attachment_groups_test_iam_permissions_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_interconnect_attachment_groups_test_iam_permissions_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<TestPermissionsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: TestPermissionsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/global/interconnectAttachmentGroups/{resource}/testIamPermissions
/// Returns permissions that a caller has on the specified resource.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_interconnect_attachment_groups_test_iam_permissions_builder()` + `compute_interconnect_attachment_groups_test_iam_permissions_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_interconnect_attachment_groups_test_iam_permissions(
    client: &SimpleHttpClient,
    project: &str,
    resource: &str,
    body: &TestPermissionsRequest,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<TestPermissionsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = compute_interconnect_attachment_groups_test_iam_permissions_builder(
        client, project, resource, body,
    )?;
    compute_interconnect_attachment_groups_test_iam_permissions_execute(builder)
}

/// GET projects/{project}/aggregated/interconnectAttachments
/// Retrieves an aggregated list of interconnect attachments.  To prevent failure, Google recommends that you set the `returnPartialSuccess` parameter to `true`.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_interconnect_attachments_aggregated_list_execute()` to send, or `compute_interconnect_attachments_aggregated_list` for simplest API.

pub fn compute_interconnect_attachments_aggregated_list_builder(
    client: &SimpleHttpClient,
    project: &str,
    filter: Option<&str>,
    includeAllScopes: Option<bool>,
    maxResults: Option<i32>,
    orderBy: Option<&str>,
    pageToken: Option<&str>,
    returnPartialSuccess: Option<bool>,
    serviceProjectNumber: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/aggregated/interconnectAttachments",
        project,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "filter",
            filter
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "includeAllScopes",
            includeAllScopes
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "maxResults",
            maxResults
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "orderBy",
            orderBy
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageToken",
            pageToken
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "returnPartialSuccess",
            returnPartialSuccess
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "serviceProjectNumber",
            serviceProjectNumber
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET projects/{project}/aggregated/interconnectAttachments
/// Retrieves an aggregated list of interconnect attachments.  To prevent failure, Google recommends that you set the `returnPartialSuccess` parameter to `true`.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_interconnect_attachments_aggregated_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_interconnect_attachments_aggregated_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_interconnect_attachments_aggregated_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<InterconnectAttachmentAggregatedList>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: InterconnectAttachmentAggregatedList = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/aggregated/interconnectAttachments
/// Retrieves an aggregated list of interconnect attachments.  To prevent failure, Google recommends that you set the `returnPartialSuccess` parameter to `true`.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_interconnect_attachments_aggregated_list_builder()` + `compute_interconnect_attachments_aggregated_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_interconnect_attachments_aggregated_list(
    client: &SimpleHttpClient,
    project: &str,
    filter: Option<&str>,
    includeAllScopes: Option<bool>,
    maxResults: Option<i32>,
    orderBy: Option<&str>,
    pageToken: Option<&str>,
    returnPartialSuccess: Option<bool>,
    serviceProjectNumber: Option<&str>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<InterconnectAttachmentAggregatedList>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = compute_interconnect_attachments_aggregated_list_builder(
        client,
        project,
        filter,
        includeAllScopes,
        maxResults,
        orderBy,
        pageToken,
        returnPartialSuccess,
        serviceProjectNumber,
    )?;
    compute_interconnect_attachments_aggregated_list_execute(builder)
}

/// GET projects/{project}/regions/{region}/interconnectAttachments/{interconnectAttachment}
/// Deletes the specified interconnect attachment.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_interconnect_attachments_delete_execute()` to send, or `compute_interconnect_attachments_delete` for simplest API.

pub fn compute_interconnect_attachments_delete_builder(
    client: &SimpleHttpClient,
    interconnectAttachment: &str,
    project: &str,
    region: &str,
    requestId: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/regions/{}/interconnectAttachments/{}",
        interconnectAttachment, project, region,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "requestId",
            requestId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET projects/{project}/regions/{region}/interconnectAttachments/{interconnectAttachment}
/// Deletes the specified interconnect attachment.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_interconnect_attachments_delete_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_interconnect_attachments_delete_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_interconnect_attachments_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/regions/{region}/interconnectAttachments/{interconnectAttachment}
/// Deletes the specified interconnect attachment.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_interconnect_attachments_delete_builder()` + `compute_interconnect_attachments_delete_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_interconnect_attachments_delete(
    client: &SimpleHttpClient,
    interconnectAttachment: &str,
    project: &str,
    region: &str,
    requestId: Option<&str>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_interconnect_attachments_delete_builder(
        client,
        interconnectAttachment,
        project,
        region,
        requestId,
    )?;
    compute_interconnect_attachments_delete_execute(builder)
}

/// GET projects/{project}/regions/{region}/interconnectAttachments/{interconnectAttachment}
/// Returns the specified interconnect attachment.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_interconnect_attachments_get_execute()` to send, or `compute_interconnect_attachments_get` for simplest API.

pub fn compute_interconnect_attachments_get_builder(
    client: &SimpleHttpClient,
    interconnectAttachment: &str,
    project: &str,
    region: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/regions/{}/interconnectAttachments/{}",
        interconnectAttachment, project, region,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/regions/{region}/interconnectAttachments/{interconnectAttachment}
/// Returns the specified interconnect attachment.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_interconnect_attachments_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_interconnect_attachments_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_interconnect_attachments_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<InterconnectAttachment>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: InterconnectAttachment = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/regions/{region}/interconnectAttachments/{interconnectAttachment}
/// Returns the specified interconnect attachment.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_interconnect_attachments_get_builder()` + `compute_interconnect_attachments_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_interconnect_attachments_get(
    client: &SimpleHttpClient,
    interconnectAttachment: &str,
    project: &str,
    region: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<InterconnectAttachment>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = compute_interconnect_attachments_get_builder(
        client,
        interconnectAttachment,
        project,
        region,
    )?;
    compute_interconnect_attachments_get_execute(builder)
}

/// GET projects/{project}/regions/{region}/interconnectAttachments
/// Creates an InterconnectAttachment in the specified project using the data included in the request.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_interconnect_attachments_insert_execute()` to send, or `compute_interconnect_attachments_insert` for simplest API.

pub fn compute_interconnect_attachments_insert_builder(
    client: &SimpleHttpClient,
    project: &str,
    region: &str,
    requestId: Option<&str>,
    validateOnly: Option<bool>,
    body: &InterconnectAttachment,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/regions/{}/interconnectAttachments",
        project, region,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "requestId",
            requestId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "validateOnly",
            validateOnly
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET projects/{project}/regions/{region}/interconnectAttachments
/// Creates an InterconnectAttachment in the specified project using the data included in the request.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_interconnect_attachments_insert_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_interconnect_attachments_insert_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_interconnect_attachments_insert_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/regions/{region}/interconnectAttachments
/// Creates an InterconnectAttachment in the specified project using the data included in the request.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_interconnect_attachments_insert_builder()` + `compute_interconnect_attachments_insert_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_interconnect_attachments_insert(
    client: &SimpleHttpClient,
    project: &str,
    region: &str,
    requestId: Option<&str>,
    validateOnly: Option<bool>,
    body: &InterconnectAttachment,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_interconnect_attachments_insert_builder(
        client,
        project,
        region,
        requestId,
        validateOnly,
        body,
    )?;
    compute_interconnect_attachments_insert_execute(builder)
}

/// GET projects/{project}/regions/{region}/interconnectAttachments
/// Retrieves the list of interconnect attachments contained within the specified region.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_interconnect_attachments_list_execute()` to send, or `compute_interconnect_attachments_list` for simplest API.

pub fn compute_interconnect_attachments_list_builder(
    client: &SimpleHttpClient,
    project: &str,
    region: &str,
    filter: Option<&str>,
    maxResults: Option<i32>,
    orderBy: Option<&str>,
    pageToken: Option<&str>,
    returnPartialSuccess: Option<bool>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/regions/{}/interconnectAttachments",
        project, region,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "filter",
            filter
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "maxResults",
            maxResults
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "orderBy",
            orderBy
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageToken",
            pageToken
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "returnPartialSuccess",
            returnPartialSuccess
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET projects/{project}/regions/{region}/interconnectAttachments
/// Retrieves the list of interconnect attachments contained within the specified region.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_interconnect_attachments_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_interconnect_attachments_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_interconnect_attachments_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<InterconnectAttachmentList>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: InterconnectAttachmentList = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/regions/{region}/interconnectAttachments
/// Retrieves the list of interconnect attachments contained within the specified region.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_interconnect_attachments_list_builder()` + `compute_interconnect_attachments_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_interconnect_attachments_list(
    client: &SimpleHttpClient,
    project: &str,
    region: &str,
    filter: Option<&str>,
    maxResults: Option<i32>,
    orderBy: Option<&str>,
    pageToken: Option<&str>,
    returnPartialSuccess: Option<bool>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<InterconnectAttachmentList>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = compute_interconnect_attachments_list_builder(
        client,
        project,
        region,
        filter,
        maxResults,
        orderBy,
        pageToken,
        returnPartialSuccess,
    )?;
    compute_interconnect_attachments_list_execute(builder)
}

/// GET projects/{project}/regions/{region}/interconnectAttachments/{interconnectAttachment}
/// Updates the specified interconnect attachment with the data included in the request. This method `supportsPATCH` semantics and uses `theJSON` merge patch format and processing rules.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_interconnect_attachments_patch_execute()` to send, or `compute_interconnect_attachments_patch` for simplest API.

pub fn compute_interconnect_attachments_patch_builder(
    client: &SimpleHttpClient,
    interconnectAttachment: &str,
    project: &str,
    region: &str,
    requestId: Option<&str>,
    body: &InterconnectAttachment,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/regions/{}/interconnectAttachments/{}",
        interconnectAttachment, project, region,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "requestId",
            requestId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET projects/{project}/regions/{region}/interconnectAttachments/{interconnectAttachment}
/// Updates the specified interconnect attachment with the data included in the request. This method `supportsPATCH` semantics and uses `theJSON` merge patch format and processing rules.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_interconnect_attachments_patch_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_interconnect_attachments_patch_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_interconnect_attachments_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/regions/{region}/interconnectAttachments/{interconnectAttachment}
/// Updates the specified interconnect attachment with the data included in the request. This method `supportsPATCH` semantics and uses `theJSON` merge patch format and processing rules.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_interconnect_attachments_patch_builder()` + `compute_interconnect_attachments_patch_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_interconnect_attachments_patch(
    client: &SimpleHttpClient,
    interconnectAttachment: &str,
    project: &str,
    region: &str,
    requestId: Option<&str>,
    body: &InterconnectAttachment,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_interconnect_attachments_patch_builder(
        client,
        interconnectAttachment,
        project,
        region,
        requestId,
        body,
    )?;
    compute_interconnect_attachments_patch_execute(builder)
}

/// GET projects/{project}/regions/{region}/interconnectAttachments/{resource}/setLabels
/// Sets the labels on an InterconnectAttachment. To learn more about labels, read the Labeling Resources documentation.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_interconnect_attachments_set_labels_execute()` to send, or `compute_interconnect_attachments_set_labels` for simplest API.

pub fn compute_interconnect_attachments_set_labels_builder(
    client: &SimpleHttpClient,
    project: &str,
    region: &str,
    resource: &str,
    requestId: Option<&str>,
    body: &RegionSetLabelsRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/regions/{}/interconnectAttachments/{}/setLabels",
        project, region, resource,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "requestId",
            requestId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET projects/{project}/regions/{region}/interconnectAttachments/{resource}/setLabels
/// Sets the labels on an InterconnectAttachment. To learn more about labels, read the Labeling Resources documentation.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_interconnect_attachments_set_labels_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_interconnect_attachments_set_labels_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_interconnect_attachments_set_labels_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/regions/{region}/interconnectAttachments/{resource}/setLabels
/// Sets the labels on an InterconnectAttachment. To learn more about labels, read the Labeling Resources documentation.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_interconnect_attachments_set_labels_builder()` + `compute_interconnect_attachments_set_labels_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_interconnect_attachments_set_labels(
    client: &SimpleHttpClient,
    project: &str,
    region: &str,
    resource: &str,
    requestId: Option<&str>,
    body: &RegionSetLabelsRequest,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_interconnect_attachments_set_labels_builder(
        client, project, region, resource, requestId, body,
    )?;
    compute_interconnect_attachments_set_labels_execute(builder)
}

/// GET projects/{project}/global/interconnectGroups/{interconnectGroup}/createMembers
/// Create Interconnects with redundancy by creating them in a specified interconnect group.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_interconnect_groups_create_members_execute()` to send, or `compute_interconnect_groups_create_members` for simplest API.

pub fn compute_interconnect_groups_create_members_builder(
    client: &SimpleHttpClient,
    interconnectGroup: &str,
    project: &str,
    body: &InterconnectGroupsCreateMembersRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/global/interconnectGroups/{}/createMembers",
        interconnectGroup, project,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET projects/{project}/global/interconnectGroups/{interconnectGroup}/createMembers
/// Create Interconnects with redundancy by creating them in a specified interconnect group.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_interconnect_groups_create_members_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_interconnect_groups_create_members_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_interconnect_groups_create_members_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/global/interconnectGroups/{interconnectGroup}/createMembers
/// Create Interconnects with redundancy by creating them in a specified interconnect group.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_interconnect_groups_create_members_builder()` + `compute_interconnect_groups_create_members_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_interconnect_groups_create_members(
    client: &SimpleHttpClient,
    interconnectGroup: &str,
    project: &str,
    body: &InterconnectGroupsCreateMembersRequest,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_interconnect_groups_create_members_builder(
        client,
        interconnectGroup,
        project,
        body,
    )?;
    compute_interconnect_groups_create_members_execute(builder)
}

/// GET projects/{project}/global/interconnectGroups/{interconnectGroup}
/// Deletes the specified InterconnectGroup in the given scope
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_interconnect_groups_delete_execute()` to send, or `compute_interconnect_groups_delete` for simplest API.

pub fn compute_interconnect_groups_delete_builder(
    client: &SimpleHttpClient,
    interconnectGroup: &str,
    project: &str,
    requestId: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/global/interconnectGroups/{}",
        interconnectGroup, project,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "requestId",
            requestId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET projects/{project}/global/interconnectGroups/{interconnectGroup}
/// Deletes the specified InterconnectGroup in the given scope
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_interconnect_groups_delete_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_interconnect_groups_delete_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_interconnect_groups_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/global/interconnectGroups/{interconnectGroup}
/// Deletes the specified InterconnectGroup in the given scope
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_interconnect_groups_delete_builder()` + `compute_interconnect_groups_delete_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_interconnect_groups_delete(
    client: &SimpleHttpClient,
    interconnectGroup: &str,
    project: &str,
    requestId: Option<&str>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        compute_interconnect_groups_delete_builder(client, interconnectGroup, project, requestId)?;
    compute_interconnect_groups_delete_execute(builder)
}

/// GET projects/{project}/global/interconnectGroups/{interconnectGroup}
/// Returns the specified InterconnectGroup resource in the given scope.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_interconnect_groups_get_execute()` to send, or `compute_interconnect_groups_get` for simplest API.

pub fn compute_interconnect_groups_get_builder(
    client: &SimpleHttpClient,
    interconnectGroup: &str,
    project: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/global/interconnectGroups/{}",
        interconnectGroup, project,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/global/interconnectGroups/{interconnectGroup}
/// Returns the specified InterconnectGroup resource in the given scope.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_interconnect_groups_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_interconnect_groups_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_interconnect_groups_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<InterconnectGroup>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: InterconnectGroup = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/global/interconnectGroups/{interconnectGroup}
/// Returns the specified InterconnectGroup resource in the given scope.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_interconnect_groups_get_builder()` + `compute_interconnect_groups_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_interconnect_groups_get(
    client: &SimpleHttpClient,
    interconnectGroup: &str,
    project: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<InterconnectGroup>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = compute_interconnect_groups_get_builder(client, interconnectGroup, project)?;
    compute_interconnect_groups_get_execute(builder)
}

/// GET projects/{project}/global/interconnectGroups/{resource}/getIamPolicy
/// Gets the access control policy for a resource. May be empty if no such policy or resource exists.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_interconnect_groups_get_iam_policy_execute()` to send, or `compute_interconnect_groups_get_iam_policy` for simplest API.

pub fn compute_interconnect_groups_get_iam_policy_builder(
    client: &SimpleHttpClient,
    project: &str,
    resource: &str,
    optionsRequestedPolicyVersion: Option<i32>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/global/interconnectGroups/{}/getIamPolicy",
        project, resource,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "optionsRequestedPolicyVersion",
            optionsRequestedPolicyVersion
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET projects/{project}/global/interconnectGroups/{resource}/getIamPolicy
/// Gets the access control policy for a resource. May be empty if no such policy or resource exists.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_interconnect_groups_get_iam_policy_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_interconnect_groups_get_iam_policy_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_interconnect_groups_get_iam_policy_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Policy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Policy = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/global/interconnectGroups/{resource}/getIamPolicy
/// Gets the access control policy for a resource. May be empty if no such policy or resource exists.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_interconnect_groups_get_iam_policy_builder()` + `compute_interconnect_groups_get_iam_policy_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_interconnect_groups_get_iam_policy(
    client: &SimpleHttpClient,
    project: &str,
    resource: &str,
    optionsRequestedPolicyVersion: Option<i32>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Policy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_interconnect_groups_get_iam_policy_builder(
        client,
        project,
        resource,
        optionsRequestedPolicyVersion,
    )?;
    compute_interconnect_groups_get_iam_policy_execute(builder)
}

/// GET projects/{project}/global/interconnectGroups/{interconnectGroup}/getOperationalStatus
/// Returns the `interconnectStatuses` for the specified InterconnectGroup.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_interconnect_groups_get_operational_status_execute()` to send, or `compute_interconnect_groups_get_operational_status` for simplest API.

pub fn compute_interconnect_groups_get_operational_status_builder(
    client: &SimpleHttpClient,
    interconnectGroup: &str,
    project: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/global/interconnectGroups/{}/getOperationalStatus",
        interconnectGroup, project,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/global/interconnectGroups/{interconnectGroup}/getOperationalStatus
/// Returns the `interconnectStatuses` for the specified InterconnectGroup.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_interconnect_groups_get_operational_status_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_interconnect_groups_get_operational_status_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_interconnect_groups_get_operational_status_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<InterconnectGroupsGetOperationalStatusResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: InterconnectGroupsGetOperationalStatusResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/global/interconnectGroups/{interconnectGroup}/getOperationalStatus
/// Returns the `interconnectStatuses` for the specified InterconnectGroup.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_interconnect_groups_get_operational_status_builder()` + `compute_interconnect_groups_get_operational_status_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_interconnect_groups_get_operational_status(
    client: &SimpleHttpClient,
    interconnectGroup: &str,
    project: &str,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<InterconnectGroupsGetOperationalStatusResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = compute_interconnect_groups_get_operational_status_builder(
        client,
        interconnectGroup,
        project,
    )?;
    compute_interconnect_groups_get_operational_status_execute(builder)
}

/// GET projects/{project}/global/interconnectGroups
/// Creates a InterconnectGroup in the specified project in the given scope using the parameters that are included in the request.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_interconnect_groups_insert_execute()` to send, or `compute_interconnect_groups_insert` for simplest API.

pub fn compute_interconnect_groups_insert_builder(
    client: &SimpleHttpClient,
    project: &str,
    requestId: Option<&str>,
    body: &InterconnectGroup,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/global/interconnectGroups",
        project,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "requestId",
            requestId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET projects/{project}/global/interconnectGroups
/// Creates a InterconnectGroup in the specified project in the given scope using the parameters that are included in the request.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_interconnect_groups_insert_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_interconnect_groups_insert_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_interconnect_groups_insert_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/global/interconnectGroups
/// Creates a InterconnectGroup in the specified project in the given scope using the parameters that are included in the request.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_interconnect_groups_insert_builder()` + `compute_interconnect_groups_insert_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_interconnect_groups_insert(
    client: &SimpleHttpClient,
    project: &str,
    requestId: Option<&str>,
    body: &InterconnectGroup,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_interconnect_groups_insert_builder(client, project, requestId, body)?;
    compute_interconnect_groups_insert_execute(builder)
}

/// GET projects/{project}/global/interconnectGroups
/// Lists the InterconnectGroups for a project in the given scope.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_interconnect_groups_list_execute()` to send, or `compute_interconnect_groups_list` for simplest API.

pub fn compute_interconnect_groups_list_builder(
    client: &SimpleHttpClient,
    project: &str,
    filter: Option<&str>,
    maxResults: Option<i32>,
    orderBy: Option<&str>,
    pageToken: Option<&str>,
    returnPartialSuccess: Option<bool>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/global/interconnectGroups",
        project,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "filter",
            filter
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "maxResults",
            maxResults
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "orderBy",
            orderBy
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageToken",
            pageToken
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "returnPartialSuccess",
            returnPartialSuccess
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET projects/{project}/global/interconnectGroups
/// Lists the InterconnectGroups for a project in the given scope.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_interconnect_groups_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_interconnect_groups_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_interconnect_groups_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<InterconnectGroupsListResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: InterconnectGroupsListResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/global/interconnectGroups
/// Lists the InterconnectGroups for a project in the given scope.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_interconnect_groups_list_builder()` + `compute_interconnect_groups_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_interconnect_groups_list(
    client: &SimpleHttpClient,
    project: &str,
    filter: Option<&str>,
    maxResults: Option<i32>,
    orderBy: Option<&str>,
    pageToken: Option<&str>,
    returnPartialSuccess: Option<bool>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<InterconnectGroupsListResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = compute_interconnect_groups_list_builder(
        client,
        project,
        filter,
        maxResults,
        orderBy,
        pageToken,
        returnPartialSuccess,
    )?;
    compute_interconnect_groups_list_execute(builder)
}

/// GET projects/{project}/global/interconnectGroups/{interconnectGroup}
/// Patches the specified InterconnectGroup resource with the data included in the request. This method supports PATCH semantics and `usesJSON` merge patch format and processing rules.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_interconnect_groups_patch_execute()` to send, or `compute_interconnect_groups_patch` for simplest API.

pub fn compute_interconnect_groups_patch_builder(
    client: &SimpleHttpClient,
    interconnectGroup: &str,
    project: &str,
    requestId: Option<&str>,
    updateMask: Option<&str>,
    body: &InterconnectGroup,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/global/interconnectGroups/{}",
        interconnectGroup, project,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "requestId",
            requestId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "updateMask",
            updateMask
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET projects/{project}/global/interconnectGroups/{interconnectGroup}
/// Patches the specified InterconnectGroup resource with the data included in the request. This method supports PATCH semantics and `usesJSON` merge patch format and processing rules.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_interconnect_groups_patch_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_interconnect_groups_patch_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_interconnect_groups_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/global/interconnectGroups/{interconnectGroup}
/// Patches the specified InterconnectGroup resource with the data included in the request. This method supports PATCH semantics and `usesJSON` merge patch format and processing rules.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_interconnect_groups_patch_builder()` + `compute_interconnect_groups_patch_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_interconnect_groups_patch(
    client: &SimpleHttpClient,
    interconnectGroup: &str,
    project: &str,
    requestId: Option<&str>,
    updateMask: Option<&str>,
    body: &InterconnectGroup,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_interconnect_groups_patch_builder(
        client,
        interconnectGroup,
        project,
        requestId,
        updateMask,
        body,
    )?;
    compute_interconnect_groups_patch_execute(builder)
}

/// GET projects/{project}/global/interconnectGroups/{resource}/setIamPolicy
/// Sets the access control policy on the specified resource. Replaces any existing policy.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_interconnect_groups_set_iam_policy_execute()` to send, or `compute_interconnect_groups_set_iam_policy` for simplest API.

pub fn compute_interconnect_groups_set_iam_policy_builder(
    client: &SimpleHttpClient,
    project: &str,
    resource: &str,
    body: &GlobalSetPolicyRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/global/interconnectGroups/{}/setIamPolicy",
        project, resource,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET projects/{project}/global/interconnectGroups/{resource}/setIamPolicy
/// Sets the access control policy on the specified resource. Replaces any existing policy.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_interconnect_groups_set_iam_policy_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_interconnect_groups_set_iam_policy_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_interconnect_groups_set_iam_policy_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Policy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Policy = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/global/interconnectGroups/{resource}/setIamPolicy
/// Sets the access control policy on the specified resource. Replaces any existing policy.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_interconnect_groups_set_iam_policy_builder()` + `compute_interconnect_groups_set_iam_policy_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_interconnect_groups_set_iam_policy(
    client: &SimpleHttpClient,
    project: &str,
    resource: &str,
    body: &GlobalSetPolicyRequest,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Policy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        compute_interconnect_groups_set_iam_policy_builder(client, project, resource, body)?;
    compute_interconnect_groups_set_iam_policy_execute(builder)
}

/// GET projects/{project}/global/interconnectGroups/{resource}/testIamPermissions
/// Returns permissions that a caller has on the specified resource.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_interconnect_groups_test_iam_permissions_execute()` to send, or `compute_interconnect_groups_test_iam_permissions` for simplest API.

pub fn compute_interconnect_groups_test_iam_permissions_builder(
    client: &SimpleHttpClient,
    project: &str,
    resource: &str,
    body: &TestPermissionsRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/global/interconnectGroups/{}/testIamPermissions",
        project, resource,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET projects/{project}/global/interconnectGroups/{resource}/testIamPermissions
/// Returns permissions that a caller has on the specified resource.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_interconnect_groups_test_iam_permissions_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_interconnect_groups_test_iam_permissions_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_interconnect_groups_test_iam_permissions_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<TestPermissionsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: TestPermissionsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/global/interconnectGroups/{resource}/testIamPermissions
/// Returns permissions that a caller has on the specified resource.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_interconnect_groups_test_iam_permissions_builder()` + `compute_interconnect_groups_test_iam_permissions_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_interconnect_groups_test_iam_permissions(
    client: &SimpleHttpClient,
    project: &str,
    resource: &str,
    body: &TestPermissionsRequest,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<TestPermissionsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        compute_interconnect_groups_test_iam_permissions_builder(client, project, resource, body)?;
    compute_interconnect_groups_test_iam_permissions_execute(builder)
}

/// GET projects/{project}/global/interconnectLocations/{interconnectLocation}
/// Returns the details for the specified interconnect location. Gets a list of available interconnect locations by making a list() request.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_interconnect_locations_get_execute()` to send, or `compute_interconnect_locations_get` for simplest API.

pub fn compute_interconnect_locations_get_builder(
    client: &SimpleHttpClient,
    interconnectLocation: &str,
    project: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/global/interconnectLocations/{}",
        interconnectLocation, project,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/global/interconnectLocations/{interconnectLocation}
/// Returns the details for the specified interconnect location. Gets a list of available interconnect locations by making a list() request.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_interconnect_locations_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_interconnect_locations_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_interconnect_locations_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<InterconnectLocation>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: InterconnectLocation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/global/interconnectLocations/{interconnectLocation}
/// Returns the details for the specified interconnect location. Gets a list of available interconnect locations by making a list() request.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_interconnect_locations_get_builder()` + `compute_interconnect_locations_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_interconnect_locations_get(
    client: &SimpleHttpClient,
    interconnectLocation: &str,
    project: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<InterconnectLocation>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        compute_interconnect_locations_get_builder(client, interconnectLocation, project)?;
    compute_interconnect_locations_get_execute(builder)
}

/// GET projects/{project}/global/interconnectLocations
/// Retrieves the list of interconnect locations available to the specified project.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_interconnect_locations_list_execute()` to send, or `compute_interconnect_locations_list` for simplest API.

pub fn compute_interconnect_locations_list_builder(
    client: &SimpleHttpClient,
    project: &str,
    filter: Option<&str>,
    maxResults: Option<i32>,
    orderBy: Option<&str>,
    pageToken: Option<&str>,
    returnPartialSuccess: Option<bool>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/global/interconnectLocations",
        project,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "filter",
            filter
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "maxResults",
            maxResults
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "orderBy",
            orderBy
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageToken",
            pageToken
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "returnPartialSuccess",
            returnPartialSuccess
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET projects/{project}/global/interconnectLocations
/// Retrieves the list of interconnect locations available to the specified project.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_interconnect_locations_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_interconnect_locations_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_interconnect_locations_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<InterconnectLocationList>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: InterconnectLocationList = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/global/interconnectLocations
/// Retrieves the list of interconnect locations available to the specified project.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_interconnect_locations_list_builder()` + `compute_interconnect_locations_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_interconnect_locations_list(
    client: &SimpleHttpClient,
    project: &str,
    filter: Option<&str>,
    maxResults: Option<i32>,
    orderBy: Option<&str>,
    pageToken: Option<&str>,
    returnPartialSuccess: Option<bool>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<InterconnectLocationList>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = compute_interconnect_locations_list_builder(
        client,
        project,
        filter,
        maxResults,
        orderBy,
        pageToken,
        returnPartialSuccess,
    )?;
    compute_interconnect_locations_list_execute(builder)
}

/// GET projects/{project}/global/interconnectRemoteLocations/{interconnectRemoteLocation}
/// Returns the details for the specified interconnect remote location. Gets a list of available interconnect remote locations by making alist() request.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_interconnect_remote_locations_get_execute()` to send, or `compute_interconnect_remote_locations_get` for simplest API.

pub fn compute_interconnect_remote_locations_get_builder(
    client: &SimpleHttpClient,
    interconnectRemoteLocation: &str,
    project: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/global/interconnectRemoteLocations/{}",
        interconnectRemoteLocation, project,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/global/interconnectRemoteLocations/{interconnectRemoteLocation}
/// Returns the details for the specified interconnect remote location. Gets a list of available interconnect remote locations by making alist() request.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_interconnect_remote_locations_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_interconnect_remote_locations_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_interconnect_remote_locations_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<InterconnectRemoteLocation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: InterconnectRemoteLocation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/global/interconnectRemoteLocations/{interconnectRemoteLocation}
/// Returns the details for the specified interconnect remote location. Gets a list of available interconnect remote locations by making alist() request.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_interconnect_remote_locations_get_builder()` + `compute_interconnect_remote_locations_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_interconnect_remote_locations_get(
    client: &SimpleHttpClient,
    interconnectRemoteLocation: &str,
    project: &str,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<InterconnectRemoteLocation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = compute_interconnect_remote_locations_get_builder(
        client,
        interconnectRemoteLocation,
        project,
    )?;
    compute_interconnect_remote_locations_get_execute(builder)
}

/// GET projects/{project}/global/interconnectRemoteLocations
/// Retrieves the list of interconnect remote locations available to the specified project.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_interconnect_remote_locations_list_execute()` to send, or `compute_interconnect_remote_locations_list` for simplest API.

pub fn compute_interconnect_remote_locations_list_builder(
    client: &SimpleHttpClient,
    project: &str,
    filter: Option<&str>,
    maxResults: Option<i32>,
    orderBy: Option<&str>,
    pageToken: Option<&str>,
    returnPartialSuccess: Option<bool>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/global/interconnectRemoteLocations",
        project,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "filter",
            filter
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "maxResults",
            maxResults
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "orderBy",
            orderBy
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageToken",
            pageToken
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "returnPartialSuccess",
            returnPartialSuccess
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET projects/{project}/global/interconnectRemoteLocations
/// Retrieves the list of interconnect remote locations available to the specified project.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_interconnect_remote_locations_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_interconnect_remote_locations_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_interconnect_remote_locations_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<InterconnectRemoteLocationList>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: InterconnectRemoteLocationList = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/global/interconnectRemoteLocations
/// Retrieves the list of interconnect remote locations available to the specified project.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_interconnect_remote_locations_list_builder()` + `compute_interconnect_remote_locations_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_interconnect_remote_locations_list(
    client: &SimpleHttpClient,
    project: &str,
    filter: Option<&str>,
    maxResults: Option<i32>,
    orderBy: Option<&str>,
    pageToken: Option<&str>,
    returnPartialSuccess: Option<bool>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<InterconnectRemoteLocationList>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = compute_interconnect_remote_locations_list_builder(
        client,
        project,
        filter,
        maxResults,
        orderBy,
        pageToken,
        returnPartialSuccess,
    )?;
    compute_interconnect_remote_locations_list_execute(builder)
}

/// GET projects/{project}/global/interconnects/{interconnect}
/// Deletes the specified Interconnect.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_interconnects_delete_execute()` to send, or `compute_interconnects_delete` for simplest API.

pub fn compute_interconnects_delete_builder(
    client: &SimpleHttpClient,
    interconnect: &str,
    project: &str,
    requestId: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/global/interconnects/{}",
        interconnect, project,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "requestId",
            requestId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET projects/{project}/global/interconnects/{interconnect}
/// Deletes the specified Interconnect.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_interconnects_delete_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_interconnects_delete_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_interconnects_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/global/interconnects/{interconnect}
/// Deletes the specified Interconnect.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_interconnects_delete_builder()` + `compute_interconnects_delete_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_interconnects_delete(
    client: &SimpleHttpClient,
    interconnect: &str,
    project: &str,
    requestId: Option<&str>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_interconnects_delete_builder(client, interconnect, project, requestId)?;
    compute_interconnects_delete_execute(builder)
}

/// GET projects/{project}/global/interconnects/{interconnect}
/// Returns the specified Interconnect. Get a list of available Interconnects by making a list() request.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_interconnects_get_execute()` to send, or `compute_interconnects_get` for simplest API.

pub fn compute_interconnects_get_builder(
    client: &SimpleHttpClient,
    interconnect: &str,
    project: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/global/interconnects/{}",
        interconnect, project,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/global/interconnects/{interconnect}
/// Returns the specified Interconnect. Get a list of available Interconnects by making a list() request.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_interconnects_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_interconnects_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_interconnects_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Interconnect>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Interconnect = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/global/interconnects/{interconnect}
/// Returns the specified Interconnect. Get a list of available Interconnects by making a list() request.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_interconnects_get_builder()` + `compute_interconnects_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_interconnects_get(
    client: &SimpleHttpClient,
    interconnect: &str,
    project: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Interconnect>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = compute_interconnects_get_builder(client, interconnect, project)?;
    compute_interconnects_get_execute(builder)
}

/// GET projects/{project}/global/interconnects/{interconnect}/getDiagnostics
/// Returns the `interconnectDiagnostics` for the specified Interconnect.  In the event of a global outage, do not use this API to make decisions about where to redirect your network traffic.  Unlike a VLAN attachment, which is regional, a Cloud Interconnect connection is a global resource. A global outage can prevent this API from functioning properly.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_interconnects_get_diagnostics_execute()` to send, or `compute_interconnects_get_diagnostics` for simplest API.

pub fn compute_interconnects_get_diagnostics_builder(
    client: &SimpleHttpClient,
    interconnect: &str,
    project: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/global/interconnects/{}/getDiagnostics",
        interconnect, project,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/global/interconnects/{interconnect}/getDiagnostics
/// Returns the `interconnectDiagnostics` for the specified Interconnect.  In the event of a global outage, do not use this API to make decisions about where to redirect your network traffic.  Unlike a VLAN attachment, which is regional, a Cloud Interconnect connection is a global resource. A global outage can prevent this API from functioning properly.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_interconnects_get_diagnostics_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_interconnects_get_diagnostics_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_interconnects_get_diagnostics_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<InterconnectsGetDiagnosticsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: InterconnectsGetDiagnosticsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/global/interconnects/{interconnect}/getDiagnostics
/// Returns the `interconnectDiagnostics` for the specified Interconnect.  In the event of a global outage, do not use this API to make decisions about where to redirect your network traffic.  Unlike a VLAN attachment, which is regional, a Cloud Interconnect connection is a global resource. A global outage can prevent this API from functioning properly.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_interconnects_get_diagnostics_builder()` + `compute_interconnects_get_diagnostics_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_interconnects_get_diagnostics(
    client: &SimpleHttpClient,
    interconnect: &str,
    project: &str,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<InterconnectsGetDiagnosticsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = compute_interconnects_get_diagnostics_builder(client, interconnect, project)?;
    compute_interconnects_get_diagnostics_execute(builder)
}

/// GET projects/{project}/global/interconnects/{interconnect}/getMacsecConfig
/// Returns the `interconnectMacsecConfig` for the specified Interconnect.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_interconnects_get_macsec_config_execute()` to send, or `compute_interconnects_get_macsec_config` for simplest API.

pub fn compute_interconnects_get_macsec_config_builder(
    client: &SimpleHttpClient,
    interconnect: &str,
    project: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/global/interconnects/{}/getMacsecConfig",
        interconnect, project,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/global/interconnects/{interconnect}/getMacsecConfig
/// Returns the `interconnectMacsecConfig` for the specified Interconnect.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_interconnects_get_macsec_config_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_interconnects_get_macsec_config_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_interconnects_get_macsec_config_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<InterconnectsGetMacsecConfigResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: InterconnectsGetMacsecConfigResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/global/interconnects/{interconnect}/getMacsecConfig
/// Returns the `interconnectMacsecConfig` for the specified Interconnect.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_interconnects_get_macsec_config_builder()` + `compute_interconnects_get_macsec_config_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_interconnects_get_macsec_config(
    client: &SimpleHttpClient,
    interconnect: &str,
    project: &str,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<InterconnectsGetMacsecConfigResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = compute_interconnects_get_macsec_config_builder(client, interconnect, project)?;
    compute_interconnects_get_macsec_config_execute(builder)
}

/// GET projects/{project}/global/interconnects
/// Creates an Interconnect in the specified project using the data included in the request.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_interconnects_insert_execute()` to send, or `compute_interconnects_insert` for simplest API.

pub fn compute_interconnects_insert_builder(
    client: &SimpleHttpClient,
    project: &str,
    requestId: Option<&str>,
    body: &Interconnect,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/global/interconnects",
        project,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "requestId",
            requestId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET projects/{project}/global/interconnects
/// Creates an Interconnect in the specified project using the data included in the request.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_interconnects_insert_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_interconnects_insert_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_interconnects_insert_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/global/interconnects
/// Creates an Interconnect in the specified project using the data included in the request.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_interconnects_insert_builder()` + `compute_interconnects_insert_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_interconnects_insert(
    client: &SimpleHttpClient,
    project: &str,
    requestId: Option<&str>,
    body: &Interconnect,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_interconnects_insert_builder(client, project, requestId, body)?;
    compute_interconnects_insert_execute(builder)
}

/// GET projects/{project}/global/interconnects
/// Retrieves the list of Interconnects available to the specified project.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_interconnects_list_execute()` to send, or `compute_interconnects_list` for simplest API.

pub fn compute_interconnects_list_builder(
    client: &SimpleHttpClient,
    project: &str,
    filter: Option<&str>,
    maxResults: Option<i32>,
    orderBy: Option<&str>,
    pageToken: Option<&str>,
    returnPartialSuccess: Option<bool>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/global/interconnects",
        project,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "filter",
            filter
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "maxResults",
            maxResults
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "orderBy",
            orderBy
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageToken",
            pageToken
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "returnPartialSuccess",
            returnPartialSuccess
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET projects/{project}/global/interconnects
/// Retrieves the list of Interconnects available to the specified project.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_interconnects_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_interconnects_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_interconnects_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<InterconnectList>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: InterconnectList = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/global/interconnects
/// Retrieves the list of Interconnects available to the specified project.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_interconnects_list_builder()` + `compute_interconnects_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_interconnects_list(
    client: &SimpleHttpClient,
    project: &str,
    filter: Option<&str>,
    maxResults: Option<i32>,
    orderBy: Option<&str>,
    pageToken: Option<&str>,
    returnPartialSuccess: Option<bool>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<InterconnectList>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = compute_interconnects_list_builder(
        client,
        project,
        filter,
        maxResults,
        orderBy,
        pageToken,
        returnPartialSuccess,
    )?;
    compute_interconnects_list_execute(builder)
}

/// GET projects/{project}/global/interconnects/{interconnect}
/// Updates the specified Interconnect with the data included in the request. This method `supportsPATCH` semantics and uses `theJSON` merge patch format and processing rules.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_interconnects_patch_execute()` to send, or `compute_interconnects_patch` for simplest API.

pub fn compute_interconnects_patch_builder(
    client: &SimpleHttpClient,
    interconnect: &str,
    project: &str,
    requestId: Option<&str>,
    body: &Interconnect,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/global/interconnects/{}",
        interconnect, project,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "requestId",
            requestId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET projects/{project}/global/interconnects/{interconnect}
/// Updates the specified Interconnect with the data included in the request. This method `supportsPATCH` semantics and uses `theJSON` merge patch format and processing rules.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_interconnects_patch_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_interconnects_patch_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_interconnects_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/global/interconnects/{interconnect}
/// Updates the specified Interconnect with the data included in the request. This method `supportsPATCH` semantics and uses `theJSON` merge patch format and processing rules.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_interconnects_patch_builder()` + `compute_interconnects_patch_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_interconnects_patch(
    client: &SimpleHttpClient,
    interconnect: &str,
    project: &str,
    requestId: Option<&str>,
    body: &Interconnect,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        compute_interconnects_patch_builder(client, interconnect, project, requestId, body)?;
    compute_interconnects_patch_execute(builder)
}

/// GET projects/{project}/global/interconnects/{resource}/setLabels
/// Sets the labels on an Interconnect. To learn more about labels, read the Labeling Resources documentation.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_interconnects_set_labels_execute()` to send, or `compute_interconnects_set_labels` for simplest API.

pub fn compute_interconnects_set_labels_builder(
    client: &SimpleHttpClient,
    project: &str,
    resource: &str,
    body: &GlobalSetLabelsRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/global/interconnects/{}/setLabels",
        project, resource,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET projects/{project}/global/interconnects/{resource}/setLabels
/// Sets the labels on an Interconnect. To learn more about labels, read the Labeling Resources documentation.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_interconnects_set_labels_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_interconnects_set_labels_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_interconnects_set_labels_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/global/interconnects/{resource}/setLabels
/// Sets the labels on an Interconnect. To learn more about labels, read the Labeling Resources documentation.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_interconnects_set_labels_builder()` + `compute_interconnects_set_labels_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_interconnects_set_labels(
    client: &SimpleHttpClient,
    project: &str,
    resource: &str,
    body: &GlobalSetLabelsRequest,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_interconnects_set_labels_builder(client, project, resource, body)?;
    compute_interconnects_set_labels_execute(builder)
}

/// GET projects/{project}/global/licenseCodes/{licenseCode}
/// Return a specified license code. License codes are mirrored across all projects that have permissions to read the License Code. *Caution* This resource is intended for use only by third-party partners who are `creatingCloud` Marketplace images.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_license_codes_get_execute()` to send, or `compute_license_codes_get` for simplest API.

pub fn compute_license_codes_get_builder(
    client: &SimpleHttpClient,
    licenseCode: &str,
    project: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/global/licenseCodes/{}",
        licenseCode, project,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/global/licenseCodes/{licenseCode}
/// Return a specified license code. License codes are mirrored across all projects that have permissions to read the License Code. *Caution* This resource is intended for use only by third-party partners who are `creatingCloud` Marketplace images.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_license_codes_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_license_codes_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_license_codes_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<LicenseCode>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: LicenseCode = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/global/licenseCodes/{licenseCode}
/// Return a specified license code. License codes are mirrored across all projects that have permissions to read the License Code. *Caution* This resource is intended for use only by third-party partners who are `creatingCloud` Marketplace images.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_license_codes_get_builder()` + `compute_license_codes_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_license_codes_get(
    client: &SimpleHttpClient,
    licenseCode: &str,
    project: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<LicenseCode>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_license_codes_get_builder(client, licenseCode, project)?;
    compute_license_codes_get_execute(builder)
}

/// GET projects/{project}/global/licenseCodes/{resource}/testIamPermissions
/// Returns permissions that a caller has on the specified resource. *Caution* This resource is intended for use only by third-party partners who are `creatingCloud` Marketplace images.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_license_codes_test_iam_permissions_execute()` to send, or `compute_license_codes_test_iam_permissions` for simplest API.

pub fn compute_license_codes_test_iam_permissions_builder(
    client: &SimpleHttpClient,
    project: &str,
    resource: &str,
    body: &TestPermissionsRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/global/licenseCodes/{}/testIamPermissions",
        project, resource,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET projects/{project}/global/licenseCodes/{resource}/testIamPermissions
/// Returns permissions that a caller has on the specified resource. *Caution* This resource is intended for use only by third-party partners who are `creatingCloud` Marketplace images.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_license_codes_test_iam_permissions_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_license_codes_test_iam_permissions_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_license_codes_test_iam_permissions_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<TestPermissionsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: TestPermissionsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/global/licenseCodes/{resource}/testIamPermissions
/// Returns permissions that a caller has on the specified resource. *Caution* This resource is intended for use only by third-party partners who are `creatingCloud` Marketplace images.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_license_codes_test_iam_permissions_builder()` + `compute_license_codes_test_iam_permissions_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_license_codes_test_iam_permissions(
    client: &SimpleHttpClient,
    project: &str,
    resource: &str,
    body: &TestPermissionsRequest,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<TestPermissionsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        compute_license_codes_test_iam_permissions_builder(client, project, resource, body)?;
    compute_license_codes_test_iam_permissions_execute(builder)
}

/// GET projects/{project}/global/licenses/{license}
/// Deletes the specified license. *Caution* This resource is intended for use only by third-party partners who are `creatingCloud` Marketplace images.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_licenses_delete_execute()` to send, or `compute_licenses_delete` for simplest API.

pub fn compute_licenses_delete_builder(
    client: &SimpleHttpClient,
    license: &str,
    project: &str,
    requestId: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/global/licenses/{}",
        license, project,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "requestId",
            requestId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET projects/{project}/global/licenses/{license}
/// Deletes the specified license. *Caution* This resource is intended for use only by third-party partners who are `creatingCloud` Marketplace images.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_licenses_delete_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_licenses_delete_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_licenses_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/global/licenses/{license}
/// Deletes the specified license. *Caution* This resource is intended for use only by third-party partners who are `creatingCloud` Marketplace images.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_licenses_delete_builder()` + `compute_licenses_delete_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_licenses_delete(
    client: &SimpleHttpClient,
    license: &str,
    project: &str,
    requestId: Option<&str>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_licenses_delete_builder(client, license, project, requestId)?;
    compute_licenses_delete_execute(builder)
}

/// GET projects/{project}/global/licenses/{license}
/// Returns the specified License resource. *Caution* This resource is intended for use only by third-party partners who are `creatingCloud` Marketplace images.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_licenses_get_execute()` to send, or `compute_licenses_get` for simplest API.

pub fn compute_licenses_get_builder(
    client: &SimpleHttpClient,
    license: &str,
    project: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/global/licenses/{}",
        license, project,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/global/licenses/{license}
/// Returns the specified License resource. *Caution* This resource is intended for use only by third-party partners who are `creatingCloud` Marketplace images.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_licenses_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_licenses_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_licenses_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<License>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: License = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/global/licenses/{license}
/// Returns the specified License resource. *Caution* This resource is intended for use only by third-party partners who are `creatingCloud` Marketplace images.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_licenses_get_builder()` + `compute_licenses_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_licenses_get(
    client: &SimpleHttpClient,
    license: &str,
    project: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<License>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_licenses_get_builder(client, license, project)?;
    compute_licenses_get_execute(builder)
}

/// GET projects/{project}/global/licenses/{resource}/getIamPolicy
/// Gets the access control policy for a resource. May be empty if no such policy or resource exists. *Caution* This resource is intended for use only by third-party partners who are `creatingCloud` Marketplace images.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_licenses_get_iam_policy_execute()` to send, or `compute_licenses_get_iam_policy` for simplest API.

pub fn compute_licenses_get_iam_policy_builder(
    client: &SimpleHttpClient,
    project: &str,
    resource: &str,
    optionsRequestedPolicyVersion: Option<i32>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/global/licenses/{}/getIamPolicy",
        project, resource,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "optionsRequestedPolicyVersion",
            optionsRequestedPolicyVersion
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET projects/{project}/global/licenses/{resource}/getIamPolicy
/// Gets the access control policy for a resource. May be empty if no such policy or resource exists. *Caution* This resource is intended for use only by third-party partners who are `creatingCloud` Marketplace images.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_licenses_get_iam_policy_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_licenses_get_iam_policy_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_licenses_get_iam_policy_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Policy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Policy = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/global/licenses/{resource}/getIamPolicy
/// Gets the access control policy for a resource. May be empty if no such policy or resource exists. *Caution* This resource is intended for use only by third-party partners who are `creatingCloud` Marketplace images.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_licenses_get_iam_policy_builder()` + `compute_licenses_get_iam_policy_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_licenses_get_iam_policy(
    client: &SimpleHttpClient,
    project: &str,
    resource: &str,
    optionsRequestedPolicyVersion: Option<i32>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Policy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_licenses_get_iam_policy_builder(
        client,
        project,
        resource,
        optionsRequestedPolicyVersion,
    )?;
    compute_licenses_get_iam_policy_execute(builder)
}

/// GET projects/{project}/global/licenses
/// Create a License resource in the specified project. *Caution* This resource is intended for use only by third-party partners who are `creatingCloud` Marketplace images.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_licenses_insert_execute()` to send, or `compute_licenses_insert` for simplest API.

pub fn compute_licenses_insert_builder(
    client: &SimpleHttpClient,
    project: &str,
    requestId: Option<&str>,
    body: &License,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/global/licenses",
        project,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "requestId",
            requestId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET projects/{project}/global/licenses
/// Create a License resource in the specified project. *Caution* This resource is intended for use only by third-party partners who are `creatingCloud` Marketplace images.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_licenses_insert_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_licenses_insert_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_licenses_insert_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/global/licenses
/// Create a License resource in the specified project. *Caution* This resource is intended for use only by third-party partners who are `creatingCloud` Marketplace images.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_licenses_insert_builder()` + `compute_licenses_insert_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_licenses_insert(
    client: &SimpleHttpClient,
    project: &str,
    requestId: Option<&str>,
    body: &License,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_licenses_insert_builder(client, project, requestId, body)?;
    compute_licenses_insert_execute(builder)
}

/// GET projects/{project}/global/licenses
/// Retrieves the list of licenses available in the specified project. This method does not get any licenses that belong to other projects, including licenses attached to publicly-available images, like Debian 9. If you want to get a list of publicly-available licenses, use this method to make a request to the respective image project, such as debian-cloud orwindows-cloud. *Caution* This resource is intended for use only by third-party partners who are `creatingCloud` Marketplace images.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_licenses_list_execute()` to send, or `compute_licenses_list` for simplest API.

pub fn compute_licenses_list_builder(
    client: &SimpleHttpClient,
    project: &str,
    filter: Option<&str>,
    maxResults: Option<i32>,
    orderBy: Option<&str>,
    pageToken: Option<&str>,
    returnPartialSuccess: Option<bool>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/global/licenses",
        project,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "filter",
            filter
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "maxResults",
            maxResults
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "orderBy",
            orderBy
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageToken",
            pageToken
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "returnPartialSuccess",
            returnPartialSuccess
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET projects/{project}/global/licenses
/// Retrieves the list of licenses available in the specified project. This method does not get any licenses that belong to other projects, including licenses attached to publicly-available images, like Debian 9. If you want to get a list of publicly-available licenses, use this method to make a request to the respective image project, such as debian-cloud orwindows-cloud. *Caution* This resource is intended for use only by third-party partners who are `creatingCloud` Marketplace images.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_licenses_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_licenses_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_licenses_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<LicensesListResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: LicensesListResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/global/licenses
/// Retrieves the list of licenses available in the specified project. This method does not get any licenses that belong to other projects, including licenses attached to publicly-available images, like Debian 9. If you want to get a list of publicly-available licenses, use this method to make a request to the respective image project, such as debian-cloud orwindows-cloud. *Caution* This resource is intended for use only by third-party partners who are `creatingCloud` Marketplace images.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_licenses_list_builder()` + `compute_licenses_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_licenses_list(
    client: &SimpleHttpClient,
    project: &str,
    filter: Option<&str>,
    maxResults: Option<i32>,
    orderBy: Option<&str>,
    pageToken: Option<&str>,
    returnPartialSuccess: Option<bool>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<LicensesListResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = compute_licenses_list_builder(
        client,
        project,
        filter,
        maxResults,
        orderBy,
        pageToken,
        returnPartialSuccess,
    )?;
    compute_licenses_list_execute(builder)
}

/// GET projects/{project}/global/licenses/{resource}/setIamPolicy
/// Sets the access control policy on the specified resource. Replaces any existing policy. *Caution* This resource is intended for use only by third-party partners who are `creatingCloud` Marketplace images.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_licenses_set_iam_policy_execute()` to send, or `compute_licenses_set_iam_policy` for simplest API.

pub fn compute_licenses_set_iam_policy_builder(
    client: &SimpleHttpClient,
    project: &str,
    resource: &str,
    body: &GlobalSetPolicyRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/global/licenses/{}/setIamPolicy",
        project, resource,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET projects/{project}/global/licenses/{resource}/setIamPolicy
/// Sets the access control policy on the specified resource. Replaces any existing policy. *Caution* This resource is intended for use only by third-party partners who are `creatingCloud` Marketplace images.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_licenses_set_iam_policy_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_licenses_set_iam_policy_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_licenses_set_iam_policy_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Policy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Policy = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/global/licenses/{resource}/setIamPolicy
/// Sets the access control policy on the specified resource. Replaces any existing policy. *Caution* This resource is intended for use only by third-party partners who are `creatingCloud` Marketplace images.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_licenses_set_iam_policy_builder()` + `compute_licenses_set_iam_policy_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_licenses_set_iam_policy(
    client: &SimpleHttpClient,
    project: &str,
    resource: &str,
    body: &GlobalSetPolicyRequest,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Policy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_licenses_set_iam_policy_builder(client, project, resource, body)?;
    compute_licenses_set_iam_policy_execute(builder)
}

/// GET projects/{project}/global/licenses/{resource}/testIamPermissions
/// Returns permissions that a caller has on the specified resource. *Caution* This resource is intended for use only by third-party partners who are `creatingCloud` Marketplace images.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_licenses_test_iam_permissions_execute()` to send, or `compute_licenses_test_iam_permissions` for simplest API.

pub fn compute_licenses_test_iam_permissions_builder(
    client: &SimpleHttpClient,
    project: &str,
    resource: &str,
    body: &TestPermissionsRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/global/licenses/{}/testIamPermissions",
        project, resource,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET projects/{project}/global/licenses/{resource}/testIamPermissions
/// Returns permissions that a caller has on the specified resource. *Caution* This resource is intended for use only by third-party partners who are `creatingCloud` Marketplace images.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_licenses_test_iam_permissions_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_licenses_test_iam_permissions_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_licenses_test_iam_permissions_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<TestPermissionsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: TestPermissionsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/global/licenses/{resource}/testIamPermissions
/// Returns permissions that a caller has on the specified resource. *Caution* This resource is intended for use only by third-party partners who are `creatingCloud` Marketplace images.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_licenses_test_iam_permissions_builder()` + `compute_licenses_test_iam_permissions_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_licenses_test_iam_permissions(
    client: &SimpleHttpClient,
    project: &str,
    resource: &str,
    body: &TestPermissionsRequest,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<TestPermissionsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = compute_licenses_test_iam_permissions_builder(client, project, resource, body)?;
    compute_licenses_test_iam_permissions_execute(builder)
}

/// GET projects/{project}/global/licenses/{license}
/// Updates a License resource in the specified project. *Caution* This resource is intended for use only by third-party partners who are `creatingCloud` Marketplace images.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_licenses_update_execute()` to send, or `compute_licenses_update` for simplest API.

pub fn compute_licenses_update_builder(
    client: &SimpleHttpClient,
    license: &str,
    project: &str,
    requestId: Option<&str>,
    updateMask: Option<&str>,
    body: &License,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/global/licenses/{}",
        license, project,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "requestId",
            requestId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "updateMask",
            updateMask
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET projects/{project}/global/licenses/{license}
/// Updates a License resource in the specified project. *Caution* This resource is intended for use only by third-party partners who are `creatingCloud` Marketplace images.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_licenses_update_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_licenses_update_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_licenses_update_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/global/licenses/{license}
/// Updates a License resource in the specified project. *Caution* This resource is intended for use only by third-party partners who are `creatingCloud` Marketplace images.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_licenses_update_builder()` + `compute_licenses_update_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_licenses_update(
    client: &SimpleHttpClient,
    license: &str,
    project: &str,
    requestId: Option<&str>,
    updateMask: Option<&str>,
    body: &License,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        compute_licenses_update_builder(client, license, project, requestId, updateMask, body)?;
    compute_licenses_update_execute(builder)
}

/// GET projects/{project}/global/machineImages/{machineImage}
/// Deletes the specified machine image. Deleting a machine image is permanent and cannot be undone.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_machine_images_delete_execute()` to send, or `compute_machine_images_delete` for simplest API.

pub fn compute_machine_images_delete_builder(
    client: &SimpleHttpClient,
    machineImage: &str,
    project: &str,
    requestId: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/global/machineImages/{}",
        machineImage, project,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "requestId",
            requestId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET projects/{project}/global/machineImages/{machineImage}
/// Deletes the specified machine image. Deleting a machine image is permanent and cannot be undone.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_machine_images_delete_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_machine_images_delete_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_machine_images_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/global/machineImages/{machineImage}
/// Deletes the specified machine image. Deleting a machine image is permanent and cannot be undone.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_machine_images_delete_builder()` + `compute_machine_images_delete_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_machine_images_delete(
    client: &SimpleHttpClient,
    machineImage: &str,
    project: &str,
    requestId: Option<&str>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_machine_images_delete_builder(client, machineImage, project, requestId)?;
    compute_machine_images_delete_execute(builder)
}

/// GET projects/{project}/global/machineImages/{machineImage}
/// Returns the specified machine image.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_machine_images_get_execute()` to send, or `compute_machine_images_get` for simplest API.

pub fn compute_machine_images_get_builder(
    client: &SimpleHttpClient,
    machineImage: &str,
    project: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/global/machineImages/{}",
        machineImage, project,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/global/machineImages/{machineImage}
/// Returns the specified machine image.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_machine_images_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_machine_images_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_machine_images_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<MachineImage>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: MachineImage = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/global/machineImages/{machineImage}
/// Returns the specified machine image.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_machine_images_get_builder()` + `compute_machine_images_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_machine_images_get(
    client: &SimpleHttpClient,
    machineImage: &str,
    project: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<MachineImage>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = compute_machine_images_get_builder(client, machineImage, project)?;
    compute_machine_images_get_execute(builder)
}

/// GET projects/{project}/global/machineImages/{resource}/getIamPolicy
/// Gets the access control policy for a resource. May be empty if no such policy or resource exists.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_machine_images_get_iam_policy_execute()` to send, or `compute_machine_images_get_iam_policy` for simplest API.

pub fn compute_machine_images_get_iam_policy_builder(
    client: &SimpleHttpClient,
    project: &str,
    resource: &str,
    optionsRequestedPolicyVersion: Option<i32>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/global/machineImages/{}/getIamPolicy",
        project, resource,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "optionsRequestedPolicyVersion",
            optionsRequestedPolicyVersion
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET projects/{project}/global/machineImages/{resource}/getIamPolicy
/// Gets the access control policy for a resource. May be empty if no such policy or resource exists.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_machine_images_get_iam_policy_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_machine_images_get_iam_policy_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_machine_images_get_iam_policy_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Policy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Policy = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/global/machineImages/{resource}/getIamPolicy
/// Gets the access control policy for a resource. May be empty if no such policy or resource exists.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_machine_images_get_iam_policy_builder()` + `compute_machine_images_get_iam_policy_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_machine_images_get_iam_policy(
    client: &SimpleHttpClient,
    project: &str,
    resource: &str,
    optionsRequestedPolicyVersion: Option<i32>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Policy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_machine_images_get_iam_policy_builder(
        client,
        project,
        resource,
        optionsRequestedPolicyVersion,
    )?;
    compute_machine_images_get_iam_policy_execute(builder)
}

/// GET projects/{project}/global/machineImages
/// Creates a machine image in the specified project using the data that is included in the request. If you are creating a new machine image to update an existing instance, your new machine image should use the same network or, if applicable, the same subnetwork as the original instance.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_machine_images_insert_execute()` to send, or `compute_machine_images_insert` for simplest API.

pub fn compute_machine_images_insert_builder(
    client: &SimpleHttpClient,
    project: &str,
    requestId: Option<&str>,
    sourceInstance: Option<&str>,
    body: &MachineImage,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/global/machineImages",
        project,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "requestId",
            requestId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "sourceInstance",
            sourceInstance
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET projects/{project}/global/machineImages
/// Creates a machine image in the specified project using the data that is included in the request. If you are creating a new machine image to update an existing instance, your new machine image should use the same network or, if applicable, the same subnetwork as the original instance.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_machine_images_insert_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_machine_images_insert_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_machine_images_insert_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/global/machineImages
/// Creates a machine image in the specified project using the data that is included in the request. If you are creating a new machine image to update an existing instance, your new machine image should use the same network or, if applicable, the same subnetwork as the original instance.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_machine_images_insert_builder()` + `compute_machine_images_insert_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_machine_images_insert(
    client: &SimpleHttpClient,
    project: &str,
    requestId: Option<&str>,
    sourceInstance: Option<&str>,
    body: &MachineImage,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        compute_machine_images_insert_builder(client, project, requestId, sourceInstance, body)?;
    compute_machine_images_insert_execute(builder)
}

/// GET projects/{project}/global/machineImages
/// Retrieves a list of machine images that are contained within the specified project.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_machine_images_list_execute()` to send, or `compute_machine_images_list` for simplest API.

pub fn compute_machine_images_list_builder(
    client: &SimpleHttpClient,
    project: &str,
    filter: Option<&str>,
    maxResults: Option<i32>,
    orderBy: Option<&str>,
    pageToken: Option<&str>,
    returnPartialSuccess: Option<bool>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/global/machineImages",
        project,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "filter",
            filter
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "maxResults",
            maxResults
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "orderBy",
            orderBy
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageToken",
            pageToken
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "returnPartialSuccess",
            returnPartialSuccess
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET projects/{project}/global/machineImages
/// Retrieves a list of machine images that are contained within the specified project.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_machine_images_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_machine_images_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_machine_images_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<MachineImageList>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: MachineImageList = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/global/machineImages
/// Retrieves a list of machine images that are contained within the specified project.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_machine_images_list_builder()` + `compute_machine_images_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_machine_images_list(
    client: &SimpleHttpClient,
    project: &str,
    filter: Option<&str>,
    maxResults: Option<i32>,
    orderBy: Option<&str>,
    pageToken: Option<&str>,
    returnPartialSuccess: Option<bool>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<MachineImageList>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = compute_machine_images_list_builder(
        client,
        project,
        filter,
        maxResults,
        orderBy,
        pageToken,
        returnPartialSuccess,
    )?;
    compute_machine_images_list_execute(builder)
}

/// GET projects/{project}/global/machineImages/{resource}/setIamPolicy
/// Sets the access control policy on the specified resource. Replaces any existing policy.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_machine_images_set_iam_policy_execute()` to send, or `compute_machine_images_set_iam_policy` for simplest API.

pub fn compute_machine_images_set_iam_policy_builder(
    client: &SimpleHttpClient,
    project: &str,
    resource: &str,
    body: &GlobalSetPolicyRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/global/machineImages/{}/setIamPolicy",
        project, resource,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET projects/{project}/global/machineImages/{resource}/setIamPolicy
/// Sets the access control policy on the specified resource. Replaces any existing policy.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_machine_images_set_iam_policy_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_machine_images_set_iam_policy_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_machine_images_set_iam_policy_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Policy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Policy = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/global/machineImages/{resource}/setIamPolicy
/// Sets the access control policy on the specified resource. Replaces any existing policy.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_machine_images_set_iam_policy_builder()` + `compute_machine_images_set_iam_policy_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_machine_images_set_iam_policy(
    client: &SimpleHttpClient,
    project: &str,
    resource: &str,
    body: &GlobalSetPolicyRequest,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Policy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_machine_images_set_iam_policy_builder(client, project, resource, body)?;
    compute_machine_images_set_iam_policy_execute(builder)
}

/// GET projects/{project}/global/machineImages/{resource}/setLabels
/// Sets the labels on a machine image. To learn more about labels, read `theLabeling` Resources documentation.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_machine_images_set_labels_execute()` to send, or `compute_machine_images_set_labels` for simplest API.

pub fn compute_machine_images_set_labels_builder(
    client: &SimpleHttpClient,
    project: &str,
    resource: &str,
    body: &GlobalSetLabelsRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/global/machineImages/{}/setLabels",
        project, resource,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET projects/{project}/global/machineImages/{resource}/setLabels
/// Sets the labels on a machine image. To learn more about labels, read `theLabeling` Resources documentation.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_machine_images_set_labels_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_machine_images_set_labels_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_machine_images_set_labels_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/global/machineImages/{resource}/setLabels
/// Sets the labels on a machine image. To learn more about labels, read `theLabeling` Resources documentation.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_machine_images_set_labels_builder()` + `compute_machine_images_set_labels_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_machine_images_set_labels(
    client: &SimpleHttpClient,
    project: &str,
    resource: &str,
    body: &GlobalSetLabelsRequest,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_machine_images_set_labels_builder(client, project, resource, body)?;
    compute_machine_images_set_labels_execute(builder)
}

/// GET projects/{project}/global/machineImages/{resource}/testIamPermissions
/// Returns permissions that a caller has on the specified resource.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_machine_images_test_iam_permissions_execute()` to send, or `compute_machine_images_test_iam_permissions` for simplest API.

pub fn compute_machine_images_test_iam_permissions_builder(
    client: &SimpleHttpClient,
    project: &str,
    resource: &str,
    body: &TestPermissionsRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/global/machineImages/{}/testIamPermissions",
        project, resource,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET projects/{project}/global/machineImages/{resource}/testIamPermissions
/// Returns permissions that a caller has on the specified resource.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_machine_images_test_iam_permissions_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_machine_images_test_iam_permissions_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_machine_images_test_iam_permissions_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<TestPermissionsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: TestPermissionsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/global/machineImages/{resource}/testIamPermissions
/// Returns permissions that a caller has on the specified resource.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_machine_images_test_iam_permissions_builder()` + `compute_machine_images_test_iam_permissions_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_machine_images_test_iam_permissions(
    client: &SimpleHttpClient,
    project: &str,
    resource: &str,
    body: &TestPermissionsRequest,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<TestPermissionsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        compute_machine_images_test_iam_permissions_builder(client, project, resource, body)?;
    compute_machine_images_test_iam_permissions_execute(builder)
}

/// GET projects/{project}/aggregated/machineTypes
/// Retrieves an aggregated list of machine types.  To prevent failure, Google recommends that you set the `returnPartialSuccess` parameter to `true`.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_machine_types_aggregated_list_execute()` to send, or `compute_machine_types_aggregated_list` for simplest API.

pub fn compute_machine_types_aggregated_list_builder(
    client: &SimpleHttpClient,
    project: &str,
    filter: Option<&str>,
    includeAllScopes: Option<bool>,
    maxResults: Option<i32>,
    orderBy: Option<&str>,
    pageToken: Option<&str>,
    returnPartialSuccess: Option<bool>,
    serviceProjectNumber: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/aggregated/machineTypes",
        project,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "filter",
            filter
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "includeAllScopes",
            includeAllScopes
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "maxResults",
            maxResults
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "orderBy",
            orderBy
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageToken",
            pageToken
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "returnPartialSuccess",
            returnPartialSuccess
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "serviceProjectNumber",
            serviceProjectNumber
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET projects/{project}/aggregated/machineTypes
/// Retrieves an aggregated list of machine types.  To prevent failure, Google recommends that you set the `returnPartialSuccess` parameter to `true`.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_machine_types_aggregated_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_machine_types_aggregated_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_machine_types_aggregated_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<MachineTypeAggregatedList>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: MachineTypeAggregatedList = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/aggregated/machineTypes
/// Retrieves an aggregated list of machine types.  To prevent failure, Google recommends that you set the `returnPartialSuccess` parameter to `true`.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_machine_types_aggregated_list_builder()` + `compute_machine_types_aggregated_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_machine_types_aggregated_list(
    client: &SimpleHttpClient,
    project: &str,
    filter: Option<&str>,
    includeAllScopes: Option<bool>,
    maxResults: Option<i32>,
    orderBy: Option<&str>,
    pageToken: Option<&str>,
    returnPartialSuccess: Option<bool>,
    serviceProjectNumber: Option<&str>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<MachineTypeAggregatedList>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = compute_machine_types_aggregated_list_builder(
        client,
        project,
        filter,
        includeAllScopes,
        maxResults,
        orderBy,
        pageToken,
        returnPartialSuccess,
        serviceProjectNumber,
    )?;
    compute_machine_types_aggregated_list_execute(builder)
}

/// GET projects/{project}/zones/{zone}/machineTypes/{machineType}
/// Returns the specified machine type.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_machine_types_get_execute()` to send, or `compute_machine_types_get` for simplest API.

pub fn compute_machine_types_get_builder(
    client: &SimpleHttpClient,
    machineType: &str,
    project: &str,
    zone: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/zones/{}/machineTypes/{}",
        machineType, project, zone,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/zones/{zone}/machineTypes/{machineType}
/// Returns the specified machine type.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_machine_types_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_machine_types_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_machine_types_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<MachineType>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: MachineType = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/zones/{zone}/machineTypes/{machineType}
/// Returns the specified machine type.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_machine_types_get_builder()` + `compute_machine_types_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_machine_types_get(
    client: &SimpleHttpClient,
    machineType: &str,
    project: &str,
    zone: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<MachineType>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_machine_types_get_builder(client, machineType, project, zone)?;
    compute_machine_types_get_execute(builder)
}

/// GET projects/{project}/zones/{zone}/machineTypes
/// Retrieves a list of machine types available to the specified project.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_machine_types_list_execute()` to send, or `compute_machine_types_list` for simplest API.

pub fn compute_machine_types_list_builder(
    client: &SimpleHttpClient,
    project: &str,
    zone: &str,
    filter: Option<&str>,
    maxResults: Option<i32>,
    orderBy: Option<&str>,
    pageToken: Option<&str>,
    returnPartialSuccess: Option<bool>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/zones/{}/machineTypes",
        project, zone,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "filter",
            filter
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "maxResults",
            maxResults
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "orderBy",
            orderBy
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageToken",
            pageToken
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "returnPartialSuccess",
            returnPartialSuccess
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET projects/{project}/zones/{zone}/machineTypes
/// Retrieves a list of machine types available to the specified project.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_machine_types_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_machine_types_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_machine_types_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<MachineTypeList>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: MachineTypeList = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/zones/{zone}/machineTypes
/// Retrieves a list of machine types available to the specified project.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_machine_types_list_builder()` + `compute_machine_types_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_machine_types_list(
    client: &SimpleHttpClient,
    project: &str,
    zone: &str,
    filter: Option<&str>,
    maxResults: Option<i32>,
    orderBy: Option<&str>,
    pageToken: Option<&str>,
    returnPartialSuccess: Option<bool>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<MachineTypeList>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = compute_machine_types_list_builder(
        client,
        project,
        zone,
        filter,
        maxResults,
        orderBy,
        pageToken,
        returnPartialSuccess,
    )?;
    compute_machine_types_list_execute(builder)
}

/// GET projects/{project}/aggregated/networkAttachments
/// Retrieves the list of all NetworkAttachment resources, regional and global, available to the specified project.  To prevent failure, Google recommends that you set the `returnPartialSuccess` parameter to `true`.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_network_attachments_aggregated_list_execute()` to send, or `compute_network_attachments_aggregated_list` for simplest API.

pub fn compute_network_attachments_aggregated_list_builder(
    client: &SimpleHttpClient,
    project: &str,
    filter: Option<&str>,
    includeAllScopes: Option<bool>,
    maxResults: Option<i32>,
    orderBy: Option<&str>,
    pageToken: Option<&str>,
    returnPartialSuccess: Option<bool>,
    serviceProjectNumber: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/aggregated/networkAttachments",
        project,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "filter",
            filter
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "includeAllScopes",
            includeAllScopes
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "maxResults",
            maxResults
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "orderBy",
            orderBy
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageToken",
            pageToken
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "returnPartialSuccess",
            returnPartialSuccess
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "serviceProjectNumber",
            serviceProjectNumber
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET projects/{project}/aggregated/networkAttachments
/// Retrieves the list of all NetworkAttachment resources, regional and global, available to the specified project.  To prevent failure, Google recommends that you set the `returnPartialSuccess` parameter to `true`.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_network_attachments_aggregated_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_network_attachments_aggregated_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_network_attachments_aggregated_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<NetworkAttachmentAggregatedList>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: NetworkAttachmentAggregatedList = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/aggregated/networkAttachments
/// Retrieves the list of all NetworkAttachment resources, regional and global, available to the specified project.  To prevent failure, Google recommends that you set the `returnPartialSuccess` parameter to `true`.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_network_attachments_aggregated_list_builder()` + `compute_network_attachments_aggregated_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_network_attachments_aggregated_list(
    client: &SimpleHttpClient,
    project: &str,
    filter: Option<&str>,
    includeAllScopes: Option<bool>,
    maxResults: Option<i32>,
    orderBy: Option<&str>,
    pageToken: Option<&str>,
    returnPartialSuccess: Option<bool>,
    serviceProjectNumber: Option<&str>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<NetworkAttachmentAggregatedList>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = compute_network_attachments_aggregated_list_builder(
        client,
        project,
        filter,
        includeAllScopes,
        maxResults,
        orderBy,
        pageToken,
        returnPartialSuccess,
        serviceProjectNumber,
    )?;
    compute_network_attachments_aggregated_list_execute(builder)
}

/// GET projects/{project}/regions/{region}/networkAttachments/{networkAttachment}
/// Deletes the specified NetworkAttachment in the given scope
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_network_attachments_delete_execute()` to send, or `compute_network_attachments_delete` for simplest API.

pub fn compute_network_attachments_delete_builder(
    client: &SimpleHttpClient,
    networkAttachment: &str,
    project: &str,
    region: &str,
    requestId: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/regions/{}/networkAttachments/{}",
        networkAttachment, project, region,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "requestId",
            requestId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET projects/{project}/regions/{region}/networkAttachments/{networkAttachment}
/// Deletes the specified NetworkAttachment in the given scope
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_network_attachments_delete_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_network_attachments_delete_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_network_attachments_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/regions/{region}/networkAttachments/{networkAttachment}
/// Deletes the specified NetworkAttachment in the given scope
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_network_attachments_delete_builder()` + `compute_network_attachments_delete_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_network_attachments_delete(
    client: &SimpleHttpClient,
    networkAttachment: &str,
    project: &str,
    region: &str,
    requestId: Option<&str>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_network_attachments_delete_builder(
        client,
        networkAttachment,
        project,
        region,
        requestId,
    )?;
    compute_network_attachments_delete_execute(builder)
}

/// GET projects/{project}/regions/{region}/networkAttachments/{networkAttachment}
/// Returns the specified NetworkAttachment resource in the given scope.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_network_attachments_get_execute()` to send, or `compute_network_attachments_get` for simplest API.

pub fn compute_network_attachments_get_builder(
    client: &SimpleHttpClient,
    networkAttachment: &str,
    project: &str,
    region: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/regions/{}/networkAttachments/{}",
        networkAttachment, project, region,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/regions/{region}/networkAttachments/{networkAttachment}
/// Returns the specified NetworkAttachment resource in the given scope.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_network_attachments_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_network_attachments_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_network_attachments_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<NetworkAttachment>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: NetworkAttachment = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/regions/{region}/networkAttachments/{networkAttachment}
/// Returns the specified NetworkAttachment resource in the given scope.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_network_attachments_get_builder()` + `compute_network_attachments_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_network_attachments_get(
    client: &SimpleHttpClient,
    networkAttachment: &str,
    project: &str,
    region: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<NetworkAttachment>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        compute_network_attachments_get_builder(client, networkAttachment, project, region)?;
    compute_network_attachments_get_execute(builder)
}

/// GET projects/{project}/regions/{region}/networkAttachments/{resource}/getIamPolicy
/// Gets the access control policy for a resource. May be empty if no such policy or resource exists.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_network_attachments_get_iam_policy_execute()` to send, or `compute_network_attachments_get_iam_policy` for simplest API.

pub fn compute_network_attachments_get_iam_policy_builder(
    client: &SimpleHttpClient,
    project: &str,
    region: &str,
    resource: &str,
    optionsRequestedPolicyVersion: Option<i32>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/regions/{}/networkAttachments/{}/getIamPolicy",
        project, region, resource,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "optionsRequestedPolicyVersion",
            optionsRequestedPolicyVersion
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET projects/{project}/regions/{region}/networkAttachments/{resource}/getIamPolicy
/// Gets the access control policy for a resource. May be empty if no such policy or resource exists.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_network_attachments_get_iam_policy_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_network_attachments_get_iam_policy_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_network_attachments_get_iam_policy_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Policy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Policy = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/regions/{region}/networkAttachments/{resource}/getIamPolicy
/// Gets the access control policy for a resource. May be empty if no such policy or resource exists.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_network_attachments_get_iam_policy_builder()` + `compute_network_attachments_get_iam_policy_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_network_attachments_get_iam_policy(
    client: &SimpleHttpClient,
    project: &str,
    region: &str,
    resource: &str,
    optionsRequestedPolicyVersion: Option<i32>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Policy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_network_attachments_get_iam_policy_builder(
        client,
        project,
        region,
        resource,
        optionsRequestedPolicyVersion,
    )?;
    compute_network_attachments_get_iam_policy_execute(builder)
}

/// GET projects/{project}/regions/{region}/networkAttachments
/// Creates a NetworkAttachment in the specified project in the given scope using the parameters that are included in the request.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_network_attachments_insert_execute()` to send, or `compute_network_attachments_insert` for simplest API.

pub fn compute_network_attachments_insert_builder(
    client: &SimpleHttpClient,
    project: &str,
    region: &str,
    requestId: Option<&str>,
    body: &NetworkAttachment,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/regions/{}/networkAttachments",
        project, region,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "requestId",
            requestId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET projects/{project}/regions/{region}/networkAttachments
/// Creates a NetworkAttachment in the specified project in the given scope using the parameters that are included in the request.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_network_attachments_insert_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_network_attachments_insert_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_network_attachments_insert_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/regions/{region}/networkAttachments
/// Creates a NetworkAttachment in the specified project in the given scope using the parameters that are included in the request.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_network_attachments_insert_builder()` + `compute_network_attachments_insert_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_network_attachments_insert(
    client: &SimpleHttpClient,
    project: &str,
    region: &str,
    requestId: Option<&str>,
    body: &NetworkAttachment,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        compute_network_attachments_insert_builder(client, project, region, requestId, body)?;
    compute_network_attachments_insert_execute(builder)
}

/// GET projects/{project}/regions/{region}/networkAttachments
/// Lists the NetworkAttachments for a project in the given scope.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_network_attachments_list_execute()` to send, or `compute_network_attachments_list` for simplest API.

pub fn compute_network_attachments_list_builder(
    client: &SimpleHttpClient,
    project: &str,
    region: &str,
    filter: Option<&str>,
    maxResults: Option<i32>,
    orderBy: Option<&str>,
    pageToken: Option<&str>,
    returnPartialSuccess: Option<bool>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/regions/{}/networkAttachments",
        project, region,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "filter",
            filter
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "maxResults",
            maxResults
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "orderBy",
            orderBy
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageToken",
            pageToken
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "returnPartialSuccess",
            returnPartialSuccess
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET projects/{project}/regions/{region}/networkAttachments
/// Lists the NetworkAttachments for a project in the given scope.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_network_attachments_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_network_attachments_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_network_attachments_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<NetworkAttachmentList>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: NetworkAttachmentList = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/regions/{region}/networkAttachments
/// Lists the NetworkAttachments for a project in the given scope.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_network_attachments_list_builder()` + `compute_network_attachments_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_network_attachments_list(
    client: &SimpleHttpClient,
    project: &str,
    region: &str,
    filter: Option<&str>,
    maxResults: Option<i32>,
    orderBy: Option<&str>,
    pageToken: Option<&str>,
    returnPartialSuccess: Option<bool>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<NetworkAttachmentList>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = compute_network_attachments_list_builder(
        client,
        project,
        region,
        filter,
        maxResults,
        orderBy,
        pageToken,
        returnPartialSuccess,
    )?;
    compute_network_attachments_list_execute(builder)
}

/// GET projects/{project}/regions/{region}/networkAttachments/{networkAttachment}
/// Patches the specified NetworkAttachment resource with the data included in the request. This method supports PATCH semantics and `usesJSON` merge patch format and processing rules.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_network_attachments_patch_execute()` to send, or `compute_network_attachments_patch` for simplest API.

pub fn compute_network_attachments_patch_builder(
    client: &SimpleHttpClient,
    networkAttachment: &str,
    project: &str,
    region: &str,
    requestId: Option<&str>,
    body: &NetworkAttachment,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/regions/{}/networkAttachments/{}",
        networkAttachment, project, region,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "requestId",
            requestId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET projects/{project}/regions/{region}/networkAttachments/{networkAttachment}
/// Patches the specified NetworkAttachment resource with the data included in the request. This method supports PATCH semantics and `usesJSON` merge patch format and processing rules.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_network_attachments_patch_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_network_attachments_patch_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_network_attachments_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/regions/{region}/networkAttachments/{networkAttachment}
/// Patches the specified NetworkAttachment resource with the data included in the request. This method supports PATCH semantics and `usesJSON` merge patch format and processing rules.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_network_attachments_patch_builder()` + `compute_network_attachments_patch_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_network_attachments_patch(
    client: &SimpleHttpClient,
    networkAttachment: &str,
    project: &str,
    region: &str,
    requestId: Option<&str>,
    body: &NetworkAttachment,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_network_attachments_patch_builder(
        client,
        networkAttachment,
        project,
        region,
        requestId,
        body,
    )?;
    compute_network_attachments_patch_execute(builder)
}

/// GET projects/{project}/regions/{region}/networkAttachments/{resource}/setIamPolicy
/// Sets the access control policy on the specified resource. Replaces any existing policy.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_network_attachments_set_iam_policy_execute()` to send, or `compute_network_attachments_set_iam_policy` for simplest API.

pub fn compute_network_attachments_set_iam_policy_builder(
    client: &SimpleHttpClient,
    project: &str,
    region: &str,
    resource: &str,
    body: &RegionSetPolicyRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/regions/{}/networkAttachments/{}/setIamPolicy",
        project, region, resource,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET projects/{project}/regions/{region}/networkAttachments/{resource}/setIamPolicy
/// Sets the access control policy on the specified resource. Replaces any existing policy.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_network_attachments_set_iam_policy_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_network_attachments_set_iam_policy_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_network_attachments_set_iam_policy_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Policy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Policy = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/regions/{region}/networkAttachments/{resource}/setIamPolicy
/// Sets the access control policy on the specified resource. Replaces any existing policy.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_network_attachments_set_iam_policy_builder()` + `compute_network_attachments_set_iam_policy_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_network_attachments_set_iam_policy(
    client: &SimpleHttpClient,
    project: &str,
    region: &str,
    resource: &str,
    body: &RegionSetPolicyRequest,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Policy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_network_attachments_set_iam_policy_builder(
        client, project, region, resource, body,
    )?;
    compute_network_attachments_set_iam_policy_execute(builder)
}

/// GET projects/{project}/regions/{region}/networkAttachments/{resource}/testIamPermissions
/// Returns permissions that a caller has on the specified resource.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_network_attachments_test_iam_permissions_execute()` to send, or `compute_network_attachments_test_iam_permissions` for simplest API.

pub fn compute_network_attachments_test_iam_permissions_builder(
    client: &SimpleHttpClient,
    project: &str,
    region: &str,
    resource: &str,
    body: &TestPermissionsRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/regions/{}/networkAttachments/{}/testIamPermissions",
        project, region, resource,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET projects/{project}/regions/{region}/networkAttachments/{resource}/testIamPermissions
/// Returns permissions that a caller has on the specified resource.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_network_attachments_test_iam_permissions_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_network_attachments_test_iam_permissions_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_network_attachments_test_iam_permissions_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<TestPermissionsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: TestPermissionsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/regions/{region}/networkAttachments/{resource}/testIamPermissions
/// Returns permissions that a caller has on the specified resource.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_network_attachments_test_iam_permissions_builder()` + `compute_network_attachments_test_iam_permissions_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_network_attachments_test_iam_permissions(
    client: &SimpleHttpClient,
    project: &str,
    region: &str,
    resource: &str,
    body: &TestPermissionsRequest,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<TestPermissionsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = compute_network_attachments_test_iam_permissions_builder(
        client, project, region, resource, body,
    )?;
    compute_network_attachments_test_iam_permissions_execute(builder)
}

/// GET projects/{project}/aggregated/networkEdgeSecurityServices
/// Retrieves the list of all NetworkEdgeSecurityService resources available to the specified project.  To prevent failure, Google recommends that you set the `returnPartialSuccess` parameter to `true`.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_network_edge_security_services_aggregated_list_execute()` to send, or `compute_network_edge_security_services_aggregated_list` for simplest API.

pub fn compute_network_edge_security_services_aggregated_list_builder(
    client: &SimpleHttpClient,
    project: &str,
    filter: Option<&str>,
    includeAllScopes: Option<bool>,
    maxResults: Option<i32>,
    orderBy: Option<&str>,
    pageToken: Option<&str>,
    returnPartialSuccess: Option<bool>,
    serviceProjectNumber: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/aggregated/networkEdgeSecurityServices",
        project,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "filter",
            filter
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "includeAllScopes",
            includeAllScopes
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "maxResults",
            maxResults
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "orderBy",
            orderBy
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageToken",
            pageToken
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "returnPartialSuccess",
            returnPartialSuccess
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "serviceProjectNumber",
            serviceProjectNumber
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET projects/{project}/aggregated/networkEdgeSecurityServices
/// Retrieves the list of all NetworkEdgeSecurityService resources available to the specified project.  To prevent failure, Google recommends that you set the `returnPartialSuccess` parameter to `true`.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_network_edge_security_services_aggregated_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_network_edge_security_services_aggregated_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_network_edge_security_services_aggregated_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<NetworkEdgeSecurityServiceAggregatedList>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: NetworkEdgeSecurityServiceAggregatedList = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/aggregated/networkEdgeSecurityServices
/// Retrieves the list of all NetworkEdgeSecurityService resources available to the specified project.  To prevent failure, Google recommends that you set the `returnPartialSuccess` parameter to `true`.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_network_edge_security_services_aggregated_list_builder()` + `compute_network_edge_security_services_aggregated_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_network_edge_security_services_aggregated_list(
    client: &SimpleHttpClient,
    project: &str,
    filter: Option<&str>,
    includeAllScopes: Option<bool>,
    maxResults: Option<i32>,
    orderBy: Option<&str>,
    pageToken: Option<&str>,
    returnPartialSuccess: Option<bool>,
    serviceProjectNumber: Option<&str>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<NetworkEdgeSecurityServiceAggregatedList>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = compute_network_edge_security_services_aggregated_list_builder(
        client,
        project,
        filter,
        includeAllScopes,
        maxResults,
        orderBy,
        pageToken,
        returnPartialSuccess,
        serviceProjectNumber,
    )?;
    compute_network_edge_security_services_aggregated_list_execute(builder)
}

/// GET projects/{project}/regions/{region}/networkEdgeSecurityServices/{networkEdgeSecurityService}
/// Deletes the specified service.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_network_edge_security_services_delete_execute()` to send, or `compute_network_edge_security_services_delete` for simplest API.

pub fn compute_network_edge_security_services_delete_builder(
    client: &SimpleHttpClient,
    networkEdgeSecurityService: &str,
    project: &str,
    region: &str,
    requestId: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/regions/{}/networkEdgeSecurityServices/{}",
        networkEdgeSecurityService, project, region,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "requestId",
            requestId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET projects/{project}/regions/{region}/networkEdgeSecurityServices/{networkEdgeSecurityService}
/// Deletes the specified service.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_network_edge_security_services_delete_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_network_edge_security_services_delete_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_network_edge_security_services_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/regions/{region}/networkEdgeSecurityServices/{networkEdgeSecurityService}
/// Deletes the specified service.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_network_edge_security_services_delete_builder()` + `compute_network_edge_security_services_delete_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_network_edge_security_services_delete(
    client: &SimpleHttpClient,
    networkEdgeSecurityService: &str,
    project: &str,
    region: &str,
    requestId: Option<&str>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_network_edge_security_services_delete_builder(
        client,
        networkEdgeSecurityService,
        project,
        region,
        requestId,
    )?;
    compute_network_edge_security_services_delete_execute(builder)
}

/// GET projects/{project}/regions/{region}/networkEdgeSecurityServices/{networkEdgeSecurityService}
/// Gets a specified NetworkEdgeSecurityService.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_network_edge_security_services_get_execute()` to send, or `compute_network_edge_security_services_get` for simplest API.

pub fn compute_network_edge_security_services_get_builder(
    client: &SimpleHttpClient,
    networkEdgeSecurityService: &str,
    project: &str,
    region: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/regions/{}/networkEdgeSecurityServices/{}",
        networkEdgeSecurityService, project, region,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/regions/{region}/networkEdgeSecurityServices/{networkEdgeSecurityService}
/// Gets a specified NetworkEdgeSecurityService.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_network_edge_security_services_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_network_edge_security_services_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_network_edge_security_services_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<NetworkEdgeSecurityService>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: NetworkEdgeSecurityService = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/regions/{region}/networkEdgeSecurityServices/{networkEdgeSecurityService}
/// Gets a specified NetworkEdgeSecurityService.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_network_edge_security_services_get_builder()` + `compute_network_edge_security_services_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_network_edge_security_services_get(
    client: &SimpleHttpClient,
    networkEdgeSecurityService: &str,
    project: &str,
    region: &str,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<NetworkEdgeSecurityService>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = compute_network_edge_security_services_get_builder(
        client,
        networkEdgeSecurityService,
        project,
        region,
    )?;
    compute_network_edge_security_services_get_execute(builder)
}

/// GET projects/{project}/regions/{region}/networkEdgeSecurityServices
/// Creates a new service in the specified project using the data included in the request.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_network_edge_security_services_insert_execute()` to send, or `compute_network_edge_security_services_insert` for simplest API.

pub fn compute_network_edge_security_services_insert_builder(
    client: &SimpleHttpClient,
    project: &str,
    region: &str,
    requestId: Option<&str>,
    validateOnly: Option<bool>,
    body: &NetworkEdgeSecurityService,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/regions/{}/networkEdgeSecurityServices",
        project, region,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "requestId",
            requestId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "validateOnly",
            validateOnly
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET projects/{project}/regions/{region}/networkEdgeSecurityServices
/// Creates a new service in the specified project using the data included in the request.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_network_edge_security_services_insert_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_network_edge_security_services_insert_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_network_edge_security_services_insert_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/regions/{region}/networkEdgeSecurityServices
/// Creates a new service in the specified project using the data included in the request.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_network_edge_security_services_insert_builder()` + `compute_network_edge_security_services_insert_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_network_edge_security_services_insert(
    client: &SimpleHttpClient,
    project: &str,
    region: &str,
    requestId: Option<&str>,
    validateOnly: Option<bool>,
    body: &NetworkEdgeSecurityService,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_network_edge_security_services_insert_builder(
        client,
        project,
        region,
        requestId,
        validateOnly,
        body,
    )?;
    compute_network_edge_security_services_insert_execute(builder)
}

/// GET projects/{project}/regions/{region}/networkEdgeSecurityServices/{networkEdgeSecurityService}
/// Patches the specified policy with the data included in the request.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_network_edge_security_services_patch_execute()` to send, or `compute_network_edge_security_services_patch` for simplest API.

pub fn compute_network_edge_security_services_patch_builder(
    client: &SimpleHttpClient,
    networkEdgeSecurityService: &str,
    project: &str,
    region: &str,
    paths: Option<&str>,
    requestId: Option<&str>,
    updateMask: Option<&str>,
    body: &NetworkEdgeSecurityService,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/regions/{}/networkEdgeSecurityServices/{}",
        networkEdgeSecurityService, project, region,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "paths",
            paths
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "requestId",
            requestId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "updateMask",
            updateMask
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET projects/{project}/regions/{region}/networkEdgeSecurityServices/{networkEdgeSecurityService}
/// Patches the specified policy with the data included in the request.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_network_edge_security_services_patch_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_network_edge_security_services_patch_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_network_edge_security_services_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/regions/{region}/networkEdgeSecurityServices/{networkEdgeSecurityService}
/// Patches the specified policy with the data included in the request.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_network_edge_security_services_patch_builder()` + `compute_network_edge_security_services_patch_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_network_edge_security_services_patch(
    client: &SimpleHttpClient,
    networkEdgeSecurityService: &str,
    project: &str,
    region: &str,
    paths: Option<&str>,
    requestId: Option<&str>,
    updateMask: Option<&str>,
    body: &NetworkEdgeSecurityService,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_network_edge_security_services_patch_builder(
        client,
        networkEdgeSecurityService,
        project,
        region,
        paths,
        requestId,
        updateMask,
        body,
    )?;
    compute_network_edge_security_services_patch_execute(builder)
}

/// GET projects/{project}/aggregated/networkEndpointGroups
/// Retrieves the list of network endpoint groups and sorts them by zone.  To prevent failure, Google recommends that you set the `returnPartialSuccess` parameter to `true`.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_network_endpoint_groups_aggregated_list_execute()` to send, or `compute_network_endpoint_groups_aggregated_list` for simplest API.

pub fn compute_network_endpoint_groups_aggregated_list_builder(
    client: &SimpleHttpClient,
    project: &str,
    filter: Option<&str>,
    includeAllScopes: Option<bool>,
    maxResults: Option<i32>,
    orderBy: Option<&str>,
    pageToken: Option<&str>,
    returnPartialSuccess: Option<bool>,
    serviceProjectNumber: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/aggregated/networkEndpointGroups",
        project,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "filter",
            filter
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "includeAllScopes",
            includeAllScopes
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "maxResults",
            maxResults
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "orderBy",
            orderBy
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageToken",
            pageToken
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "returnPartialSuccess",
            returnPartialSuccess
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "serviceProjectNumber",
            serviceProjectNumber
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET projects/{project}/aggregated/networkEndpointGroups
/// Retrieves the list of network endpoint groups and sorts them by zone.  To prevent failure, Google recommends that you set the `returnPartialSuccess` parameter to `true`.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_network_endpoint_groups_aggregated_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_network_endpoint_groups_aggregated_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_network_endpoint_groups_aggregated_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<NetworkEndpointGroupAggregatedList>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: NetworkEndpointGroupAggregatedList = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/aggregated/networkEndpointGroups
/// Retrieves the list of network endpoint groups and sorts them by zone.  To prevent failure, Google recommends that you set the `returnPartialSuccess` parameter to `true`.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_network_endpoint_groups_aggregated_list_builder()` + `compute_network_endpoint_groups_aggregated_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_network_endpoint_groups_aggregated_list(
    client: &SimpleHttpClient,
    project: &str,
    filter: Option<&str>,
    includeAllScopes: Option<bool>,
    maxResults: Option<i32>,
    orderBy: Option<&str>,
    pageToken: Option<&str>,
    returnPartialSuccess: Option<bool>,
    serviceProjectNumber: Option<&str>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<NetworkEndpointGroupAggregatedList>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = compute_network_endpoint_groups_aggregated_list_builder(
        client,
        project,
        filter,
        includeAllScopes,
        maxResults,
        orderBy,
        pageToken,
        returnPartialSuccess,
        serviceProjectNumber,
    )?;
    compute_network_endpoint_groups_aggregated_list_execute(builder)
}

/// GET projects/{project}/zones/{zone}/networkEndpointGroups/{networkEndpointGroup}/attachNetworkEndpoints
/// Attach a list of network endpoints to the specified network endpoint group.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_network_endpoint_groups_attach_network_endpoints_execute()` to send, or `compute_network_endpoint_groups_attach_network_endpoints` for simplest API.

pub fn compute_network_endpoint_groups_attach_network_endpoints_builder(
    client: &SimpleHttpClient,
    networkEndpointGroup: &str,
    project: &str,
    zone: &str,
    requestId: Option<&str>,
    body: &NetworkEndpointGroupsAttachEndpointsRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/zones/{}/networkEndpointGroups/{}/attachNetworkEndpoints",
        networkEndpointGroup,
        project,
        zone,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "requestId",
            requestId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET projects/{project}/zones/{zone}/networkEndpointGroups/{networkEndpointGroup}/attachNetworkEndpoints
/// Attach a list of network endpoints to the specified network endpoint group.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_network_endpoint_groups_attach_network_endpoints_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_network_endpoint_groups_attach_network_endpoints_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_network_endpoint_groups_attach_network_endpoints_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/zones/{zone}/networkEndpointGroups/{networkEndpointGroup}/attachNetworkEndpoints
/// Attach a list of network endpoints to the specified network endpoint group.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_network_endpoint_groups_attach_network_endpoints_builder()` + `compute_network_endpoint_groups_attach_network_endpoints_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_network_endpoint_groups_attach_network_endpoints(
    client: &SimpleHttpClient,
    networkEndpointGroup: &str,
    project: &str,
    zone: &str,
    requestId: Option<&str>,
    body: &NetworkEndpointGroupsAttachEndpointsRequest,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_network_endpoint_groups_attach_network_endpoints_builder(
        client,
        networkEndpointGroup,
        project,
        zone,
        requestId,
        body,
    )?;
    compute_network_endpoint_groups_attach_network_endpoints_execute(builder)
}

/// GET projects/{project}/zones/{zone}/networkEndpointGroups/{networkEndpointGroup}
/// Deletes the specified network endpoint group. The network endpoints in the NEG and the VM instances they belong to are not terminated when the NEG is deleted. Note that the NEG cannot be deleted if there are backend services referencing it.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_network_endpoint_groups_delete_execute()` to send, or `compute_network_endpoint_groups_delete` for simplest API.

pub fn compute_network_endpoint_groups_delete_builder(
    client: &SimpleHttpClient,
    networkEndpointGroup: &str,
    project: &str,
    zone: &str,
    requestId: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/zones/{}/networkEndpointGroups/{}",
        networkEndpointGroup, project, zone,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "requestId",
            requestId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET projects/{project}/zones/{zone}/networkEndpointGroups/{networkEndpointGroup}
/// Deletes the specified network endpoint group. The network endpoints in the NEG and the VM instances they belong to are not terminated when the NEG is deleted. Note that the NEG cannot be deleted if there are backend services referencing it.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_network_endpoint_groups_delete_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_network_endpoint_groups_delete_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_network_endpoint_groups_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/zones/{zone}/networkEndpointGroups/{networkEndpointGroup}
/// Deletes the specified network endpoint group. The network endpoints in the NEG and the VM instances they belong to are not terminated when the NEG is deleted. Note that the NEG cannot be deleted if there are backend services referencing it.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_network_endpoint_groups_delete_builder()` + `compute_network_endpoint_groups_delete_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_network_endpoint_groups_delete(
    client: &SimpleHttpClient,
    networkEndpointGroup: &str,
    project: &str,
    zone: &str,
    requestId: Option<&str>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_network_endpoint_groups_delete_builder(
        client,
        networkEndpointGroup,
        project,
        zone,
        requestId,
    )?;
    compute_network_endpoint_groups_delete_execute(builder)
}

/// GET projects/{project}/zones/{zone}/networkEndpointGroups/{networkEndpointGroup}/detachNetworkEndpoints
/// Detach a list of network endpoints from the specified network endpoint group.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_network_endpoint_groups_detach_network_endpoints_execute()` to send, or `compute_network_endpoint_groups_detach_network_endpoints` for simplest API.

pub fn compute_network_endpoint_groups_detach_network_endpoints_builder(
    client: &SimpleHttpClient,
    networkEndpointGroup: &str,
    project: &str,
    zone: &str,
    requestId: Option<&str>,
    body: &NetworkEndpointGroupsDetachEndpointsRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/zones/{}/networkEndpointGroups/{}/detachNetworkEndpoints",
        networkEndpointGroup,
        project,
        zone,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "requestId",
            requestId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET projects/{project}/zones/{zone}/networkEndpointGroups/{networkEndpointGroup}/detachNetworkEndpoints
/// Detach a list of network endpoints from the specified network endpoint group.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_network_endpoint_groups_detach_network_endpoints_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_network_endpoint_groups_detach_network_endpoints_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_network_endpoint_groups_detach_network_endpoints_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/zones/{zone}/networkEndpointGroups/{networkEndpointGroup}/detachNetworkEndpoints
/// Detach a list of network endpoints from the specified network endpoint group.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_network_endpoint_groups_detach_network_endpoints_builder()` + `compute_network_endpoint_groups_detach_network_endpoints_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_network_endpoint_groups_detach_network_endpoints(
    client: &SimpleHttpClient,
    networkEndpointGroup: &str,
    project: &str,
    zone: &str,
    requestId: Option<&str>,
    body: &NetworkEndpointGroupsDetachEndpointsRequest,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_network_endpoint_groups_detach_network_endpoints_builder(
        client,
        networkEndpointGroup,
        project,
        zone,
        requestId,
        body,
    )?;
    compute_network_endpoint_groups_detach_network_endpoints_execute(builder)
}

/// GET projects/{project}/zones/{zone}/networkEndpointGroups/{networkEndpointGroup}
/// Returns the specified network endpoint group.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_network_endpoint_groups_get_execute()` to send, or `compute_network_endpoint_groups_get` for simplest API.

pub fn compute_network_endpoint_groups_get_builder(
    client: &SimpleHttpClient,
    networkEndpointGroup: &str,
    project: &str,
    zone: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/zones/{}/networkEndpointGroups/{}",
        networkEndpointGroup, project, zone,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/zones/{zone}/networkEndpointGroups/{networkEndpointGroup}
/// Returns the specified network endpoint group.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_network_endpoint_groups_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_network_endpoint_groups_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_network_endpoint_groups_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<NetworkEndpointGroup>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: NetworkEndpointGroup = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/zones/{zone}/networkEndpointGroups/{networkEndpointGroup}
/// Returns the specified network endpoint group.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_network_endpoint_groups_get_builder()` + `compute_network_endpoint_groups_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_network_endpoint_groups_get(
    client: &SimpleHttpClient,
    networkEndpointGroup: &str,
    project: &str,
    zone: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<NetworkEndpointGroup>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        compute_network_endpoint_groups_get_builder(client, networkEndpointGroup, project, zone)?;
    compute_network_endpoint_groups_get_execute(builder)
}

/// GET projects/{project}/zones/{zone}/networkEndpointGroups
/// Creates a network endpoint group in the specified project using the parameters that are included in the request.  Note: Use the following APIs to manage network endpoint groups:  - To manage NEGs with zonal scope (such as zonal NEGs, hybrid connectivity NEGs): zonal API - To manage NEGs with regional scope (such as regional internet NEGs, serverless NEGs, Private Service Connect NEGs): regional API - To manage NEGs with global scope (such as global internet NEGs):global API
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_network_endpoint_groups_insert_execute()` to send, or `compute_network_endpoint_groups_insert` for simplest API.

pub fn compute_network_endpoint_groups_insert_builder(
    client: &SimpleHttpClient,
    project: &str,
    zone: &str,
    requestId: Option<&str>,
    body: &NetworkEndpointGroup,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/zones/{}/networkEndpointGroups",
        project, zone,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "requestId",
            requestId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET projects/{project}/zones/{zone}/networkEndpointGroups
/// Creates a network endpoint group in the specified project using the parameters that are included in the request.  Note: Use the following APIs to manage network endpoint groups:  - To manage NEGs with zonal scope (such as zonal NEGs, hybrid connectivity NEGs): zonal API - To manage NEGs with regional scope (such as regional internet NEGs, serverless NEGs, Private Service Connect NEGs): regional API - To manage NEGs with global scope (such as global internet NEGs):global API
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_network_endpoint_groups_insert_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_network_endpoint_groups_insert_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_network_endpoint_groups_insert_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/zones/{zone}/networkEndpointGroups
/// Creates a network endpoint group in the specified project using the parameters that are included in the request.  Note: Use the following APIs to manage network endpoint groups:  - To manage NEGs with zonal scope (such as zonal NEGs, hybrid connectivity NEGs): zonal API - To manage NEGs with regional scope (such as regional internet NEGs, serverless NEGs, Private Service Connect NEGs): regional API - To manage NEGs with global scope (such as global internet NEGs):global API
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_network_endpoint_groups_insert_builder()` + `compute_network_endpoint_groups_insert_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_network_endpoint_groups_insert(
    client: &SimpleHttpClient,
    project: &str,
    zone: &str,
    requestId: Option<&str>,
    body: &NetworkEndpointGroup,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        compute_network_endpoint_groups_insert_builder(client, project, zone, requestId, body)?;
    compute_network_endpoint_groups_insert_execute(builder)
}

/// GET projects/{project}/zones/{zone}/networkEndpointGroups
/// Retrieves the list of network endpoint groups that are located in the specified project and zone.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_network_endpoint_groups_list_execute()` to send, or `compute_network_endpoint_groups_list` for simplest API.

pub fn compute_network_endpoint_groups_list_builder(
    client: &SimpleHttpClient,
    project: &str,
    zone: &str,
    filter: Option<&str>,
    maxResults: Option<i32>,
    orderBy: Option<&str>,
    pageToken: Option<&str>,
    returnPartialSuccess: Option<bool>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/zones/{}/networkEndpointGroups",
        project, zone,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "filter",
            filter
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "maxResults",
            maxResults
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "orderBy",
            orderBy
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageToken",
            pageToken
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "returnPartialSuccess",
            returnPartialSuccess
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET projects/{project}/zones/{zone}/networkEndpointGroups
/// Retrieves the list of network endpoint groups that are located in the specified project and zone.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_network_endpoint_groups_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_network_endpoint_groups_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_network_endpoint_groups_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<NetworkEndpointGroupList>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: NetworkEndpointGroupList = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/zones/{zone}/networkEndpointGroups
/// Retrieves the list of network endpoint groups that are located in the specified project and zone.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_network_endpoint_groups_list_builder()` + `compute_network_endpoint_groups_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_network_endpoint_groups_list(
    client: &SimpleHttpClient,
    project: &str,
    zone: &str,
    filter: Option<&str>,
    maxResults: Option<i32>,
    orderBy: Option<&str>,
    pageToken: Option<&str>,
    returnPartialSuccess: Option<bool>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<NetworkEndpointGroupList>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = compute_network_endpoint_groups_list_builder(
        client,
        project,
        zone,
        filter,
        maxResults,
        orderBy,
        pageToken,
        returnPartialSuccess,
    )?;
    compute_network_endpoint_groups_list_execute(builder)
}

/// GET projects/{project}/zones/{zone}/networkEndpointGroups/{networkEndpointGroup}/listNetworkEndpoints
/// Lists the network endpoints in the specified network endpoint group.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_network_endpoint_groups_list_network_endpoints_execute()` to send, or `compute_network_endpoint_groups_list_network_endpoints` for simplest API.

pub fn compute_network_endpoint_groups_list_network_endpoints_builder(
    client: &SimpleHttpClient,
    networkEndpointGroup: &str,
    project: &str,
    zone: &str,
    filter: Option<&str>,
    maxResults: Option<i32>,
    orderBy: Option<&str>,
    pageToken: Option<&str>,
    returnPartialSuccess: Option<bool>,
    body: &NetworkEndpointGroupsListEndpointsRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/zones/{}/networkEndpointGroups/{}/listNetworkEndpoints",
        networkEndpointGroup, project, zone,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "filter",
            filter
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "maxResults",
            maxResults
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "orderBy",
            orderBy
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageToken",
            pageToken
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "returnPartialSuccess",
            returnPartialSuccess
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET projects/{project}/zones/{zone}/networkEndpointGroups/{networkEndpointGroup}/listNetworkEndpoints
/// Lists the network endpoints in the specified network endpoint group.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_network_endpoint_groups_list_network_endpoints_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_network_endpoint_groups_list_network_endpoints_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_network_endpoint_groups_list_network_endpoints_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<NetworkEndpointGroupsListNetworkEndpoints>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: NetworkEndpointGroupsListNetworkEndpoints = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/zones/{zone}/networkEndpointGroups/{networkEndpointGroup}/listNetworkEndpoints
/// Lists the network endpoints in the specified network endpoint group.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_network_endpoint_groups_list_network_endpoints_builder()` + `compute_network_endpoint_groups_list_network_endpoints_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_network_endpoint_groups_list_network_endpoints(
    client: &SimpleHttpClient,
    networkEndpointGroup: &str,
    project: &str,
    zone: &str,
    filter: Option<&str>,
    maxResults: Option<i32>,
    orderBy: Option<&str>,
    pageToken: Option<&str>,
    returnPartialSuccess: Option<bool>,
    body: &NetworkEndpointGroupsListEndpointsRequest,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<NetworkEndpointGroupsListNetworkEndpoints>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = compute_network_endpoint_groups_list_network_endpoints_builder(
        client,
        networkEndpointGroup,
        project,
        zone,
        filter,
        maxResults,
        orderBy,
        pageToken,
        returnPartialSuccess,
        body,
    )?;
    compute_network_endpoint_groups_list_network_endpoints_execute(builder)
}

/// GET projects/{project}/zones/{zone}/networkEndpointGroups/{resource}/testIamPermissions
/// Returns permissions that a caller has on the specified resource.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_network_endpoint_groups_test_iam_permissions_execute()` to send, or `compute_network_endpoint_groups_test_iam_permissions` for simplest API.

pub fn compute_network_endpoint_groups_test_iam_permissions_builder(
    client: &SimpleHttpClient,
    project: &str,
    resource: &str,
    zone: &str,
    body: &TestPermissionsRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/zones/{}/networkEndpointGroups/{}/testIamPermissions",
        project, resource, zone,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET projects/{project}/zones/{zone}/networkEndpointGroups/{resource}/testIamPermissions
/// Returns permissions that a caller has on the specified resource.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_network_endpoint_groups_test_iam_permissions_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_network_endpoint_groups_test_iam_permissions_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_network_endpoint_groups_test_iam_permissions_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<TestPermissionsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: TestPermissionsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/zones/{zone}/networkEndpointGroups/{resource}/testIamPermissions
/// Returns permissions that a caller has on the specified resource.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_network_endpoint_groups_test_iam_permissions_builder()` + `compute_network_endpoint_groups_test_iam_permissions_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_network_endpoint_groups_test_iam_permissions(
    client: &SimpleHttpClient,
    project: &str,
    resource: &str,
    zone: &str,
    body: &TestPermissionsRequest,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<TestPermissionsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = compute_network_endpoint_groups_test_iam_permissions_builder(
        client, project, resource, zone, body,
    )?;
    compute_network_endpoint_groups_test_iam_permissions_execute(builder)
}

/// GET projects/{project}/global/firewallPolicies/{firewallPolicy}/addAssociation
/// Inserts an association for the specified firewall policy.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_network_firewall_policies_add_association_execute()` to send, or `compute_network_firewall_policies_add_association` for simplest API.

pub fn compute_network_firewall_policies_add_association_builder(
    client: &SimpleHttpClient,
    firewallPolicy: &str,
    project: &str,
    replaceExistingAssociation: Option<bool>,
    requestId: Option<&str>,
    body: &FirewallPolicyAssociation,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/global/firewallPolicies/{}/addAssociation",
        firewallPolicy, project,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "replaceExistingAssociation",
            replaceExistingAssociation
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "requestId",
            requestId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET projects/{project}/global/firewallPolicies/{firewallPolicy}/addAssociation
/// Inserts an association for the specified firewall policy.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_network_firewall_policies_add_association_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_network_firewall_policies_add_association_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_network_firewall_policies_add_association_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/global/firewallPolicies/{firewallPolicy}/addAssociation
/// Inserts an association for the specified firewall policy.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_network_firewall_policies_add_association_builder()` + `compute_network_firewall_policies_add_association_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_network_firewall_policies_add_association(
    client: &SimpleHttpClient,
    firewallPolicy: &str,
    project: &str,
    replaceExistingAssociation: Option<bool>,
    requestId: Option<&str>,
    body: &FirewallPolicyAssociation,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_network_firewall_policies_add_association_builder(
        client,
        firewallPolicy,
        project,
        replaceExistingAssociation,
        requestId,
        body,
    )?;
    compute_network_firewall_policies_add_association_execute(builder)
}

/// GET projects/{project}/global/firewallPolicies/{firewallPolicy}/addPacketMirroringRule
/// Inserts a packet mirroring rule into a firewall policy.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_network_firewall_policies_add_packet_mirroring_rule_execute()` to send, or `compute_network_firewall_policies_add_packet_mirroring_rule` for simplest API.

pub fn compute_network_firewall_policies_add_packet_mirroring_rule_builder(
    client: &SimpleHttpClient,
    firewallPolicy: &str,
    project: &str,
    maxPriority: Option<i32>,
    minPriority: Option<i32>,
    requestId: Option<&str>,
    body: &FirewallPolicyRule,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/global/firewallPolicies/{}/addPacketMirroringRule",
        firewallPolicy, project,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "maxPriority",
            maxPriority
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "minPriority",
            minPriority
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "requestId",
            requestId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET projects/{project}/global/firewallPolicies/{firewallPolicy}/addPacketMirroringRule
/// Inserts a packet mirroring rule into a firewall policy.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_network_firewall_policies_add_packet_mirroring_rule_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_network_firewall_policies_add_packet_mirroring_rule_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_network_firewall_policies_add_packet_mirroring_rule_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/global/firewallPolicies/{firewallPolicy}/addPacketMirroringRule
/// Inserts a packet mirroring rule into a firewall policy.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_network_firewall_policies_add_packet_mirroring_rule_builder()` + `compute_network_firewall_policies_add_packet_mirroring_rule_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_network_firewall_policies_add_packet_mirroring_rule(
    client: &SimpleHttpClient,
    firewallPolicy: &str,
    project: &str,
    maxPriority: Option<i32>,
    minPriority: Option<i32>,
    requestId: Option<&str>,
    body: &FirewallPolicyRule,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_network_firewall_policies_add_packet_mirroring_rule_builder(
        client,
        firewallPolicy,
        project,
        maxPriority,
        minPriority,
        requestId,
        body,
    )?;
    compute_network_firewall_policies_add_packet_mirroring_rule_execute(builder)
}

/// GET projects/{project}/global/firewallPolicies/{firewallPolicy}/addRule
/// Inserts a rule into a firewall policy.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_network_firewall_policies_add_rule_execute()` to send, or `compute_network_firewall_policies_add_rule` for simplest API.

pub fn compute_network_firewall_policies_add_rule_builder(
    client: &SimpleHttpClient,
    firewallPolicy: &str,
    project: &str,
    maxPriority: Option<i32>,
    minPriority: Option<i32>,
    requestId: Option<&str>,
    body: &FirewallPolicyRule,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/global/firewallPolicies/{}/addRule",
        firewallPolicy, project,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "maxPriority",
            maxPriority
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "minPriority",
            minPriority
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "requestId",
            requestId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET projects/{project}/global/firewallPolicies/{firewallPolicy}/addRule
/// Inserts a rule into a firewall policy.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_network_firewall_policies_add_rule_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_network_firewall_policies_add_rule_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_network_firewall_policies_add_rule_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/global/firewallPolicies/{firewallPolicy}/addRule
/// Inserts a rule into a firewall policy.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_network_firewall_policies_add_rule_builder()` + `compute_network_firewall_policies_add_rule_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_network_firewall_policies_add_rule(
    client: &SimpleHttpClient,
    firewallPolicy: &str,
    project: &str,
    maxPriority: Option<i32>,
    minPriority: Option<i32>,
    requestId: Option<&str>,
    body: &FirewallPolicyRule,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_network_firewall_policies_add_rule_builder(
        client,
        firewallPolicy,
        project,
        maxPriority,
        minPriority,
        requestId,
        body,
    )?;
    compute_network_firewall_policies_add_rule_execute(builder)
}

/// GET projects/{project}/aggregated/firewallPolicies
/// Retrieves an aggregated list of network firewall policies, listing network firewall policies from all applicable scopes (global and regional) and grouping the results per scope.  To prevent failure, it is recommended that you set the `returnPartialSuccess` parameter to `true`.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_network_firewall_policies_aggregated_list_execute()` to send, or `compute_network_firewall_policies_aggregated_list` for simplest API.

pub fn compute_network_firewall_policies_aggregated_list_builder(
    client: &SimpleHttpClient,
    project: &str,
    filter: Option<&str>,
    includeAllScopes: Option<bool>,
    maxResults: Option<i32>,
    orderBy: Option<&str>,
    pageToken: Option<&str>,
    returnPartialSuccess: Option<bool>,
    serviceProjectNumber: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/aggregated/firewallPolicies",
        project,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "filter",
            filter
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "includeAllScopes",
            includeAllScopes
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "maxResults",
            maxResults
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "orderBy",
            orderBy
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageToken",
            pageToken
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "returnPartialSuccess",
            returnPartialSuccess
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "serviceProjectNumber",
            serviceProjectNumber
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET projects/{project}/aggregated/firewallPolicies
/// Retrieves an aggregated list of network firewall policies, listing network firewall policies from all applicable scopes (global and regional) and grouping the results per scope.  To prevent failure, it is recommended that you set the `returnPartialSuccess` parameter to `true`.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_network_firewall_policies_aggregated_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_network_firewall_policies_aggregated_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_network_firewall_policies_aggregated_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<NetworkFirewallPolicyAggregatedList>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: NetworkFirewallPolicyAggregatedList = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/aggregated/firewallPolicies
/// Retrieves an aggregated list of network firewall policies, listing network firewall policies from all applicable scopes (global and regional) and grouping the results per scope.  To prevent failure, it is recommended that you set the `returnPartialSuccess` parameter to `true`.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_network_firewall_policies_aggregated_list_builder()` + `compute_network_firewall_policies_aggregated_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_network_firewall_policies_aggregated_list(
    client: &SimpleHttpClient,
    project: &str,
    filter: Option<&str>,
    includeAllScopes: Option<bool>,
    maxResults: Option<i32>,
    orderBy: Option<&str>,
    pageToken: Option<&str>,
    returnPartialSuccess: Option<bool>,
    serviceProjectNumber: Option<&str>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<NetworkFirewallPolicyAggregatedList>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = compute_network_firewall_policies_aggregated_list_builder(
        client,
        project,
        filter,
        includeAllScopes,
        maxResults,
        orderBy,
        pageToken,
        returnPartialSuccess,
        serviceProjectNumber,
    )?;
    compute_network_firewall_policies_aggregated_list_execute(builder)
}

/// GET projects/{project}/global/firewallPolicies/{firewallPolicy}/cloneRules
/// Copies rules to the specified firewall policy.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_network_firewall_policies_clone_rules_execute()` to send, or `compute_network_firewall_policies_clone_rules` for simplest API.

pub fn compute_network_firewall_policies_clone_rules_builder(
    client: &SimpleHttpClient,
    firewallPolicy: &str,
    project: &str,
    requestId: Option<&str>,
    sourceFirewallPolicy: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/global/firewallPolicies/{}/cloneRules",
        firewallPolicy, project,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "requestId",
            requestId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "sourceFirewallPolicy",
            sourceFirewallPolicy
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET projects/{project}/global/firewallPolicies/{firewallPolicy}/cloneRules
/// Copies rules to the specified firewall policy.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_network_firewall_policies_clone_rules_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_network_firewall_policies_clone_rules_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_network_firewall_policies_clone_rules_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/global/firewallPolicies/{firewallPolicy}/cloneRules
/// Copies rules to the specified firewall policy.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_network_firewall_policies_clone_rules_builder()` + `compute_network_firewall_policies_clone_rules_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_network_firewall_policies_clone_rules(
    client: &SimpleHttpClient,
    firewallPolicy: &str,
    project: &str,
    requestId: Option<&str>,
    sourceFirewallPolicy: Option<&str>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_network_firewall_policies_clone_rules_builder(
        client,
        firewallPolicy,
        project,
        requestId,
        sourceFirewallPolicy,
    )?;
    compute_network_firewall_policies_clone_rules_execute(builder)
}

/// GET projects/{project}/global/firewallPolicies/{firewallPolicy}
/// Deletes the specified policy.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_network_firewall_policies_delete_execute()` to send, or `compute_network_firewall_policies_delete` for simplest API.

pub fn compute_network_firewall_policies_delete_builder(
    client: &SimpleHttpClient,
    firewallPolicy: &str,
    project: &str,
    requestId: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/global/firewallPolicies/{}",
        firewallPolicy, project,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "requestId",
            requestId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET projects/{project}/global/firewallPolicies/{firewallPolicy}
/// Deletes the specified policy.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_network_firewall_policies_delete_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_network_firewall_policies_delete_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_network_firewall_policies_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/global/firewallPolicies/{firewallPolicy}
/// Deletes the specified policy.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_network_firewall_policies_delete_builder()` + `compute_network_firewall_policies_delete_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_network_firewall_policies_delete(
    client: &SimpleHttpClient,
    firewallPolicy: &str,
    project: &str,
    requestId: Option<&str>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_network_firewall_policies_delete_builder(
        client,
        firewallPolicy,
        project,
        requestId,
    )?;
    compute_network_firewall_policies_delete_execute(builder)
}

/// GET projects/{project}/global/firewallPolicies/{firewallPolicy}
/// Returns the specified network firewall policy.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_network_firewall_policies_get_execute()` to send, or `compute_network_firewall_policies_get` for simplest API.

pub fn compute_network_firewall_policies_get_builder(
    client: &SimpleHttpClient,
    firewallPolicy: &str,
    project: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/global/firewallPolicies/{}",
        firewallPolicy, project,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/global/firewallPolicies/{firewallPolicy}
/// Returns the specified network firewall policy.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_network_firewall_policies_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_network_firewall_policies_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_network_firewall_policies_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<FirewallPolicy>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: FirewallPolicy = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/global/firewallPolicies/{firewallPolicy}
/// Returns the specified network firewall policy.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_network_firewall_policies_get_builder()` + `compute_network_firewall_policies_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_network_firewall_policies_get(
    client: &SimpleHttpClient,
    firewallPolicy: &str,
    project: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<FirewallPolicy>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = compute_network_firewall_policies_get_builder(client, firewallPolicy, project)?;
    compute_network_firewall_policies_get_execute(builder)
}

/// GET projects/{project}/global/firewallPolicies/{firewallPolicy}/getAssociation
/// Gets an association with the specified name.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_network_firewall_policies_get_association_execute()` to send, or `compute_network_firewall_policies_get_association` for simplest API.

pub fn compute_network_firewall_policies_get_association_builder(
    client: &SimpleHttpClient,
    firewallPolicy: &str,
    project: &str,
    name: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/global/firewallPolicies/{}/getAssociation",
        firewallPolicy, project,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "name",
            name.as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET projects/{project}/global/firewallPolicies/{firewallPolicy}/getAssociation
/// Gets an association with the specified name.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_network_firewall_policies_get_association_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_network_firewall_policies_get_association_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_network_firewall_policies_get_association_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<FirewallPolicyAssociation>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: FirewallPolicyAssociation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/global/firewallPolicies/{firewallPolicy}/getAssociation
/// Gets an association with the specified name.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_network_firewall_policies_get_association_builder()` + `compute_network_firewall_policies_get_association_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_network_firewall_policies_get_association(
    client: &SimpleHttpClient,
    firewallPolicy: &str,
    project: &str,
    name: Option<&str>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<FirewallPolicyAssociation>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = compute_network_firewall_policies_get_association_builder(
        client,
        firewallPolicy,
        project,
        name,
    )?;
    compute_network_firewall_policies_get_association_execute(builder)
}

/// GET projects/{project}/global/firewallPolicies/{resource}/getIamPolicy
/// Gets the access control policy for a resource. May be empty if no such policy or resource exists.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_network_firewall_policies_get_iam_policy_execute()` to send, or `compute_network_firewall_policies_get_iam_policy` for simplest API.

pub fn compute_network_firewall_policies_get_iam_policy_builder(
    client: &SimpleHttpClient,
    project: &str,
    resource: &str,
    optionsRequestedPolicyVersion: Option<i32>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/global/firewallPolicies/{}/getIamPolicy",
        project, resource,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "optionsRequestedPolicyVersion",
            optionsRequestedPolicyVersion
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET projects/{project}/global/firewallPolicies/{resource}/getIamPolicy
/// Gets the access control policy for a resource. May be empty if no such policy or resource exists.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_network_firewall_policies_get_iam_policy_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_network_firewall_policies_get_iam_policy_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_network_firewall_policies_get_iam_policy_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Policy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Policy = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/global/firewallPolicies/{resource}/getIamPolicy
/// Gets the access control policy for a resource. May be empty if no such policy or resource exists.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_network_firewall_policies_get_iam_policy_builder()` + `compute_network_firewall_policies_get_iam_policy_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_network_firewall_policies_get_iam_policy(
    client: &SimpleHttpClient,
    project: &str,
    resource: &str,
    optionsRequestedPolicyVersion: Option<i32>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Policy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_network_firewall_policies_get_iam_policy_builder(
        client,
        project,
        resource,
        optionsRequestedPolicyVersion,
    )?;
    compute_network_firewall_policies_get_iam_policy_execute(builder)
}

/// GET projects/{project}/global/firewallPolicies/{firewallPolicy}/getPacketMirroringRule
/// Gets a packet mirroring rule of the specified priority.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_network_firewall_policies_get_packet_mirroring_rule_execute()` to send, or `compute_network_firewall_policies_get_packet_mirroring_rule` for simplest API.

pub fn compute_network_firewall_policies_get_packet_mirroring_rule_builder(
    client: &SimpleHttpClient,
    firewallPolicy: &str,
    project: &str,
    priority: Option<i32>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/global/firewallPolicies/{}/getPacketMirroringRule",
        firewallPolicy, project,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "priority",
            priority
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET projects/{project}/global/firewallPolicies/{firewallPolicy}/getPacketMirroringRule
/// Gets a packet mirroring rule of the specified priority.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_network_firewall_policies_get_packet_mirroring_rule_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_network_firewall_policies_get_packet_mirroring_rule_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_network_firewall_policies_get_packet_mirroring_rule_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<FirewallPolicyRule>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: FirewallPolicyRule = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/global/firewallPolicies/{firewallPolicy}/getPacketMirroringRule
/// Gets a packet mirroring rule of the specified priority.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_network_firewall_policies_get_packet_mirroring_rule_builder()` + `compute_network_firewall_policies_get_packet_mirroring_rule_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_network_firewall_policies_get_packet_mirroring_rule(
    client: &SimpleHttpClient,
    firewallPolicy: &str,
    project: &str,
    priority: Option<i32>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<FirewallPolicyRule>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = compute_network_firewall_policies_get_packet_mirroring_rule_builder(
        client,
        firewallPolicy,
        project,
        priority,
    )?;
    compute_network_firewall_policies_get_packet_mirroring_rule_execute(builder)
}

/// GET projects/{project}/global/firewallPolicies/{firewallPolicy}/getRule
/// Gets a rule of the specified priority.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_network_firewall_policies_get_rule_execute()` to send, or `compute_network_firewall_policies_get_rule` for simplest API.

pub fn compute_network_firewall_policies_get_rule_builder(
    client: &SimpleHttpClient,
    firewallPolicy: &str,
    project: &str,
    priority: Option<i32>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/global/firewallPolicies/{}/getRule",
        firewallPolicy, project,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "priority",
            priority
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET projects/{project}/global/firewallPolicies/{firewallPolicy}/getRule
/// Gets a rule of the specified priority.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_network_firewall_policies_get_rule_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_network_firewall_policies_get_rule_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_network_firewall_policies_get_rule_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<FirewallPolicyRule>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: FirewallPolicyRule = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/global/firewallPolicies/{firewallPolicy}/getRule
/// Gets a rule of the specified priority.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_network_firewall_policies_get_rule_builder()` + `compute_network_firewall_policies_get_rule_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_network_firewall_policies_get_rule(
    client: &SimpleHttpClient,
    firewallPolicy: &str,
    project: &str,
    priority: Option<i32>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<FirewallPolicyRule>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = compute_network_firewall_policies_get_rule_builder(
        client,
        firewallPolicy,
        project,
        priority,
    )?;
    compute_network_firewall_policies_get_rule_execute(builder)
}

/// GET projects/{project}/global/firewallPolicies
/// Creates a new policy in the specified project using the data included in the request.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_network_firewall_policies_insert_execute()` to send, or `compute_network_firewall_policies_insert` for simplest API.

pub fn compute_network_firewall_policies_insert_builder(
    client: &SimpleHttpClient,
    project: &str,
    requestId: Option<&str>,
    body: &FirewallPolicy,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/global/firewallPolicies",
        project,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "requestId",
            requestId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET projects/{project}/global/firewallPolicies
/// Creates a new policy in the specified project using the data included in the request.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_network_firewall_policies_insert_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_network_firewall_policies_insert_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_network_firewall_policies_insert_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/global/firewallPolicies
/// Creates a new policy in the specified project using the data included in the request.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_network_firewall_policies_insert_builder()` + `compute_network_firewall_policies_insert_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_network_firewall_policies_insert(
    client: &SimpleHttpClient,
    project: &str,
    requestId: Option<&str>,
    body: &FirewallPolicy,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        compute_network_firewall_policies_insert_builder(client, project, requestId, body)?;
    compute_network_firewall_policies_insert_execute(builder)
}

/// GET projects/{project}/global/firewallPolicies
/// Lists all the policies that have been configured for the specified project.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_network_firewall_policies_list_execute()` to send, or `compute_network_firewall_policies_list` for simplest API.

pub fn compute_network_firewall_policies_list_builder(
    client: &SimpleHttpClient,
    project: &str,
    filter: Option<&str>,
    maxResults: Option<i32>,
    orderBy: Option<&str>,
    pageToken: Option<&str>,
    returnPartialSuccess: Option<bool>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/global/firewallPolicies",
        project,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "filter",
            filter
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "maxResults",
            maxResults
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "orderBy",
            orderBy
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageToken",
            pageToken
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "returnPartialSuccess",
            returnPartialSuccess
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET projects/{project}/global/firewallPolicies
/// Lists all the policies that have been configured for the specified project.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_network_firewall_policies_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_network_firewall_policies_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_network_firewall_policies_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<FirewallPolicyList>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: FirewallPolicyList = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/global/firewallPolicies
/// Lists all the policies that have been configured for the specified project.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_network_firewall_policies_list_builder()` + `compute_network_firewall_policies_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_network_firewall_policies_list(
    client: &SimpleHttpClient,
    project: &str,
    filter: Option<&str>,
    maxResults: Option<i32>,
    orderBy: Option<&str>,
    pageToken: Option<&str>,
    returnPartialSuccess: Option<bool>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<FirewallPolicyList>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = compute_network_firewall_policies_list_builder(
        client,
        project,
        filter,
        maxResults,
        orderBy,
        pageToken,
        returnPartialSuccess,
    )?;
    compute_network_firewall_policies_list_execute(builder)
}

/// GET projects/{project}/global/firewallPolicies/{firewallPolicy}
/// Patches the specified policy with the data included in the request.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_network_firewall_policies_patch_execute()` to send, or `compute_network_firewall_policies_patch` for simplest API.

pub fn compute_network_firewall_policies_patch_builder(
    client: &SimpleHttpClient,
    firewallPolicy: &str,
    project: &str,
    requestId: Option<&str>,
    body: &FirewallPolicy,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/global/firewallPolicies/{}",
        firewallPolicy, project,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "requestId",
            requestId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET projects/{project}/global/firewallPolicies/{firewallPolicy}
/// Patches the specified policy with the data included in the request.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_network_firewall_policies_patch_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_network_firewall_policies_patch_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_network_firewall_policies_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/global/firewallPolicies/{firewallPolicy}
/// Patches the specified policy with the data included in the request.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_network_firewall_policies_patch_builder()` + `compute_network_firewall_policies_patch_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_network_firewall_policies_patch(
    client: &SimpleHttpClient,
    firewallPolicy: &str,
    project: &str,
    requestId: Option<&str>,
    body: &FirewallPolicy,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_network_firewall_policies_patch_builder(
        client,
        firewallPolicy,
        project,
        requestId,
        body,
    )?;
    compute_network_firewall_policies_patch_execute(builder)
}

/// GET projects/{project}/global/firewallPolicies/{firewallPolicy}/patchPacketMirroringRule
/// Patches a packet mirroring rule of the specified priority.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_network_firewall_policies_patch_packet_mirroring_rule_execute()` to send, or `compute_network_firewall_policies_patch_packet_mirroring_rule` for simplest API.

pub fn compute_network_firewall_policies_patch_packet_mirroring_rule_builder(
    client: &SimpleHttpClient,
    firewallPolicy: &str,
    project: &str,
    priority: Option<i32>,
    requestId: Option<&str>,
    body: &FirewallPolicyRule,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/global/firewallPolicies/{}/patchPacketMirroringRule",
        firewallPolicy, project,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "priority",
            priority
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "requestId",
            requestId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET projects/{project}/global/firewallPolicies/{firewallPolicy}/patchPacketMirroringRule
/// Patches a packet mirroring rule of the specified priority.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_network_firewall_policies_patch_packet_mirroring_rule_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_network_firewall_policies_patch_packet_mirroring_rule_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_network_firewall_policies_patch_packet_mirroring_rule_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/global/firewallPolicies/{firewallPolicy}/patchPacketMirroringRule
/// Patches a packet mirroring rule of the specified priority.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_network_firewall_policies_patch_packet_mirroring_rule_builder()` + `compute_network_firewall_policies_patch_packet_mirroring_rule_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_network_firewall_policies_patch_packet_mirroring_rule(
    client: &SimpleHttpClient,
    firewallPolicy: &str,
    project: &str,
    priority: Option<i32>,
    requestId: Option<&str>,
    body: &FirewallPolicyRule,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_network_firewall_policies_patch_packet_mirroring_rule_builder(
        client,
        firewallPolicy,
        project,
        priority,
        requestId,
        body,
    )?;
    compute_network_firewall_policies_patch_packet_mirroring_rule_execute(builder)
}

/// GET projects/{project}/global/firewallPolicies/{firewallPolicy}/patchRule
/// Patches a rule of the specified priority.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_network_firewall_policies_patch_rule_execute()` to send, or `compute_network_firewall_policies_patch_rule` for simplest API.

pub fn compute_network_firewall_policies_patch_rule_builder(
    client: &SimpleHttpClient,
    firewallPolicy: &str,
    project: &str,
    priority: Option<i32>,
    requestId: Option<&str>,
    body: &FirewallPolicyRule,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/global/firewallPolicies/{}/patchRule",
        firewallPolicy, project,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "priority",
            priority
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "requestId",
            requestId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET projects/{project}/global/firewallPolicies/{firewallPolicy}/patchRule
/// Patches a rule of the specified priority.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_network_firewall_policies_patch_rule_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_network_firewall_policies_patch_rule_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_network_firewall_policies_patch_rule_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/global/firewallPolicies/{firewallPolicy}/patchRule
/// Patches a rule of the specified priority.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_network_firewall_policies_patch_rule_builder()` + `compute_network_firewall_policies_patch_rule_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_network_firewall_policies_patch_rule(
    client: &SimpleHttpClient,
    firewallPolicy: &str,
    project: &str,
    priority: Option<i32>,
    requestId: Option<&str>,
    body: &FirewallPolicyRule,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_network_firewall_policies_patch_rule_builder(
        client,
        firewallPolicy,
        project,
        priority,
        requestId,
        body,
    )?;
    compute_network_firewall_policies_patch_rule_execute(builder)
}

/// GET projects/{project}/global/firewallPolicies/{firewallPolicy}/removeAssociation
/// Removes an association for the specified firewall policy.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_network_firewall_policies_remove_association_execute()` to send, or `compute_network_firewall_policies_remove_association` for simplest API.

pub fn compute_network_firewall_policies_remove_association_builder(
    client: &SimpleHttpClient,
    firewallPolicy: &str,
    project: &str,
    name: Option<&str>,
    requestId: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/global/firewallPolicies/{}/removeAssociation",
        firewallPolicy, project,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "name",
            name.as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "requestId",
            requestId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET projects/{project}/global/firewallPolicies/{firewallPolicy}/removeAssociation
/// Removes an association for the specified firewall policy.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_network_firewall_policies_remove_association_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_network_firewall_policies_remove_association_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_network_firewall_policies_remove_association_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/global/firewallPolicies/{firewallPolicy}/removeAssociation
/// Removes an association for the specified firewall policy.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_network_firewall_policies_remove_association_builder()` + `compute_network_firewall_policies_remove_association_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_network_firewall_policies_remove_association(
    client: &SimpleHttpClient,
    firewallPolicy: &str,
    project: &str,
    name: Option<&str>,
    requestId: Option<&str>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_network_firewall_policies_remove_association_builder(
        client,
        firewallPolicy,
        project,
        name,
        requestId,
    )?;
    compute_network_firewall_policies_remove_association_execute(builder)
}

/// GET projects/{project}/global/firewallPolicies/{firewallPolicy}/removePacketMirroringRule
/// Deletes a packet mirroring rule of the specified priority.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_network_firewall_policies_remove_packet_mirroring_rule_execute()` to send, or `compute_network_firewall_policies_remove_packet_mirroring_rule` for simplest API.

pub fn compute_network_firewall_policies_remove_packet_mirroring_rule_builder(
    client: &SimpleHttpClient,
    firewallPolicy: &str,
    project: &str,
    priority: Option<i32>,
    requestId: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/global/firewallPolicies/{}/removePacketMirroringRule",
        firewallPolicy, project,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "priority",
            priority
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "requestId",
            requestId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET projects/{project}/global/firewallPolicies/{firewallPolicy}/removePacketMirroringRule
/// Deletes a packet mirroring rule of the specified priority.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_network_firewall_policies_remove_packet_mirroring_rule_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_network_firewall_policies_remove_packet_mirroring_rule_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_network_firewall_policies_remove_packet_mirroring_rule_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/global/firewallPolicies/{firewallPolicy}/removePacketMirroringRule
/// Deletes a packet mirroring rule of the specified priority.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_network_firewall_policies_remove_packet_mirroring_rule_builder()` + `compute_network_firewall_policies_remove_packet_mirroring_rule_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_network_firewall_policies_remove_packet_mirroring_rule(
    client: &SimpleHttpClient,
    firewallPolicy: &str,
    project: &str,
    priority: Option<i32>,
    requestId: Option<&str>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_network_firewall_policies_remove_packet_mirroring_rule_builder(
        client,
        firewallPolicy,
        project,
        priority,
        requestId,
    )?;
    compute_network_firewall_policies_remove_packet_mirroring_rule_execute(builder)
}

/// GET projects/{project}/global/firewallPolicies/{firewallPolicy}/removeRule
/// Deletes a rule of the specified priority.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_network_firewall_policies_remove_rule_execute()` to send, or `compute_network_firewall_policies_remove_rule` for simplest API.

pub fn compute_network_firewall_policies_remove_rule_builder(
    client: &SimpleHttpClient,
    firewallPolicy: &str,
    project: &str,
    priority: Option<i32>,
    requestId: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/global/firewallPolicies/{}/removeRule",
        firewallPolicy, project,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "priority",
            priority
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "requestId",
            requestId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET projects/{project}/global/firewallPolicies/{firewallPolicy}/removeRule
/// Deletes a rule of the specified priority.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_network_firewall_policies_remove_rule_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_network_firewall_policies_remove_rule_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_network_firewall_policies_remove_rule_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/global/firewallPolicies/{firewallPolicy}/removeRule
/// Deletes a rule of the specified priority.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_network_firewall_policies_remove_rule_builder()` + `compute_network_firewall_policies_remove_rule_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_network_firewall_policies_remove_rule(
    client: &SimpleHttpClient,
    firewallPolicy: &str,
    project: &str,
    priority: Option<i32>,
    requestId: Option<&str>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_network_firewall_policies_remove_rule_builder(
        client,
        firewallPolicy,
        project,
        priority,
        requestId,
    )?;
    compute_network_firewall_policies_remove_rule_execute(builder)
}

/// GET projects/{project}/global/firewallPolicies/{resource}/setIamPolicy
/// Sets the access control policy on the specified resource. Replaces any existing policy.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_network_firewall_policies_set_iam_policy_execute()` to send, or `compute_network_firewall_policies_set_iam_policy` for simplest API.

pub fn compute_network_firewall_policies_set_iam_policy_builder(
    client: &SimpleHttpClient,
    project: &str,
    resource: &str,
    body: &GlobalSetPolicyRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/global/firewallPolicies/{}/setIamPolicy",
        project, resource,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET projects/{project}/global/firewallPolicies/{resource}/setIamPolicy
/// Sets the access control policy on the specified resource. Replaces any existing policy.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_network_firewall_policies_set_iam_policy_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_network_firewall_policies_set_iam_policy_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_network_firewall_policies_set_iam_policy_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Policy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Policy = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/global/firewallPolicies/{resource}/setIamPolicy
/// Sets the access control policy on the specified resource. Replaces any existing policy.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_network_firewall_policies_set_iam_policy_builder()` + `compute_network_firewall_policies_set_iam_policy_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_network_firewall_policies_set_iam_policy(
    client: &SimpleHttpClient,
    project: &str,
    resource: &str,
    body: &GlobalSetPolicyRequest,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Policy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        compute_network_firewall_policies_set_iam_policy_builder(client, project, resource, body)?;
    compute_network_firewall_policies_set_iam_policy_execute(builder)
}

/// GET projects/{project}/global/firewallPolicies/{resource}/testIamPermissions
/// Returns permissions that a caller has on the specified resource.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_network_firewall_policies_test_iam_permissions_execute()` to send, or `compute_network_firewall_policies_test_iam_permissions` for simplest API.

pub fn compute_network_firewall_policies_test_iam_permissions_builder(
    client: &SimpleHttpClient,
    project: &str,
    resource: &str,
    body: &TestPermissionsRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/global/firewallPolicies/{}/testIamPermissions",
        project, resource,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET projects/{project}/global/firewallPolicies/{resource}/testIamPermissions
/// Returns permissions that a caller has on the specified resource.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_network_firewall_policies_test_iam_permissions_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_network_firewall_policies_test_iam_permissions_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_network_firewall_policies_test_iam_permissions_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<TestPermissionsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: TestPermissionsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/global/firewallPolicies/{resource}/testIamPermissions
/// Returns permissions that a caller has on the specified resource.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_network_firewall_policies_test_iam_permissions_builder()` + `compute_network_firewall_policies_test_iam_permissions_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_network_firewall_policies_test_iam_permissions(
    client: &SimpleHttpClient,
    project: &str,
    resource: &str,
    body: &TestPermissionsRequest,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<TestPermissionsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = compute_network_firewall_policies_test_iam_permissions_builder(
        client, project, resource, body,
    )?;
    compute_network_firewall_policies_test_iam_permissions_execute(builder)
}

/// GET projects/{project}/global/networkProfiles/{networkProfile}
/// Returns the specified network profile.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_network_profiles_get_execute()` to send, or `compute_network_profiles_get` for simplest API.

pub fn compute_network_profiles_get_builder(
    client: &SimpleHttpClient,
    networkProfile: &str,
    project: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/global/networkProfiles/{}",
        networkProfile, project,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/global/networkProfiles/{networkProfile}
/// Returns the specified network profile.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_network_profiles_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_network_profiles_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_network_profiles_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<NetworkProfile>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: NetworkProfile = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/global/networkProfiles/{networkProfile}
/// Returns the specified network profile.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_network_profiles_get_builder()` + `compute_network_profiles_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_network_profiles_get(
    client: &SimpleHttpClient,
    networkProfile: &str,
    project: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<NetworkProfile>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = compute_network_profiles_get_builder(client, networkProfile, project)?;
    compute_network_profiles_get_execute(builder)
}

/// GET projects/{project}/global/networkProfiles
/// Retrieves a list of network profiles available to the specified project.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_network_profiles_list_execute()` to send, or `compute_network_profiles_list` for simplest API.

pub fn compute_network_profiles_list_builder(
    client: &SimpleHttpClient,
    project: &str,
    filter: Option<&str>,
    maxResults: Option<i32>,
    orderBy: Option<&str>,
    pageToken: Option<&str>,
    returnPartialSuccess: Option<bool>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/global/networkProfiles",
        project,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "filter",
            filter
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "maxResults",
            maxResults
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "orderBy",
            orderBy
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageToken",
            pageToken
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "returnPartialSuccess",
            returnPartialSuccess
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET projects/{project}/global/networkProfiles
/// Retrieves a list of network profiles available to the specified project.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_network_profiles_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_network_profiles_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_network_profiles_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<NetworkProfilesListResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: NetworkProfilesListResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/global/networkProfiles
/// Retrieves a list of network profiles available to the specified project.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_network_profiles_list_builder()` + `compute_network_profiles_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_network_profiles_list(
    client: &SimpleHttpClient,
    project: &str,
    filter: Option<&str>,
    maxResults: Option<i32>,
    orderBy: Option<&str>,
    pageToken: Option<&str>,
    returnPartialSuccess: Option<bool>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<NetworkProfilesListResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = compute_network_profiles_list_builder(
        client,
        project,
        filter,
        maxResults,
        orderBy,
        pageToken,
        returnPartialSuccess,
    )?;
    compute_network_profiles_list_execute(builder)
}

/// GET projects/{project}/global/networks/{network}/addPeering
/// Adds a peering to the specified network.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_networks_add_peering_execute()` to send, or `compute_networks_add_peering` for simplest API.

pub fn compute_networks_add_peering_builder(
    client: &SimpleHttpClient,
    network: &str,
    project: &str,
    requestId: Option<&str>,
    body: &NetworksAddPeeringRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/global/networks/{}/addPeering",
        network, project,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "requestId",
            requestId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET projects/{project}/global/networks/{network}/addPeering
/// Adds a peering to the specified network.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_networks_add_peering_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_networks_add_peering_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_networks_add_peering_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/global/networks/{network}/addPeering
/// Adds a peering to the specified network.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_networks_add_peering_builder()` + `compute_networks_add_peering_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_networks_add_peering(
    client: &SimpleHttpClient,
    network: &str,
    project: &str,
    requestId: Option<&str>,
    body: &NetworksAddPeeringRequest,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_networks_add_peering_builder(client, network, project, requestId, body)?;
    compute_networks_add_peering_execute(builder)
}

/// GET projects/{project}/global/networks/{network}
/// Deletes the specified network.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_networks_delete_execute()` to send, or `compute_networks_delete` for simplest API.

pub fn compute_networks_delete_builder(
    client: &SimpleHttpClient,
    network: &str,
    project: &str,
    requestId: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/global/networks/{}",
        network, project,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "requestId",
            requestId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET projects/{project}/global/networks/{network}
/// Deletes the specified network.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_networks_delete_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_networks_delete_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_networks_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/global/networks/{network}
/// Deletes the specified network.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_networks_delete_builder()` + `compute_networks_delete_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_networks_delete(
    client: &SimpleHttpClient,
    network: &str,
    project: &str,
    requestId: Option<&str>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_networks_delete_builder(client, network, project, requestId)?;
    compute_networks_delete_execute(builder)
}

/// GET projects/{project}/global/networks/{network}
/// Returns the specified network.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_networks_get_execute()` to send, or `compute_networks_get` for simplest API.

pub fn compute_networks_get_builder(
    client: &SimpleHttpClient,
    network: &str,
    project: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/global/networks/{}",
        network, project,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/global/networks/{network}
/// Returns the specified network.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_networks_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_networks_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_networks_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Network>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Network = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/global/networks/{network}
/// Returns the specified network.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_networks_get_builder()` + `compute_networks_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_networks_get(
    client: &SimpleHttpClient,
    network: &str,
    project: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Network>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_networks_get_builder(client, network, project)?;
    compute_networks_get_execute(builder)
}

/// GET projects/{project}/global/networks/{network}/getEffectiveFirewalls
/// Returns the effective firewalls on a given network.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_networks_get_effective_firewalls_execute()` to send, or `compute_networks_get_effective_firewalls` for simplest API.

pub fn compute_networks_get_effective_firewalls_builder(
    client: &SimpleHttpClient,
    network: &str,
    project: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/global/networks/{}/getEffectiveFirewalls",
        network, project,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/global/networks/{network}/getEffectiveFirewalls
/// Returns the effective firewalls on a given network.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_networks_get_effective_firewalls_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_networks_get_effective_firewalls_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_networks_get_effective_firewalls_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<NetworksGetEffectiveFirewallsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: NetworksGetEffectiveFirewallsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/global/networks/{network}/getEffectiveFirewalls
/// Returns the effective firewalls on a given network.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_networks_get_effective_firewalls_builder()` + `compute_networks_get_effective_firewalls_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_networks_get_effective_firewalls(
    client: &SimpleHttpClient,
    network: &str,
    project: &str,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<NetworksGetEffectiveFirewallsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = compute_networks_get_effective_firewalls_builder(client, network, project)?;
    compute_networks_get_effective_firewalls_execute(builder)
}

/// GET projects/{project}/global/networks
/// Creates a network in the specified project using the data included in the request.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_networks_insert_execute()` to send, or `compute_networks_insert` for simplest API.

pub fn compute_networks_insert_builder(
    client: &SimpleHttpClient,
    project: &str,
    requestId: Option<&str>,
    body: &Network,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/global/networks",
        project,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "requestId",
            requestId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET projects/{project}/global/networks
/// Creates a network in the specified project using the data included in the request.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_networks_insert_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_networks_insert_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_networks_insert_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/global/networks
/// Creates a network in the specified project using the data included in the request.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_networks_insert_builder()` + `compute_networks_insert_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_networks_insert(
    client: &SimpleHttpClient,
    project: &str,
    requestId: Option<&str>,
    body: &Network,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_networks_insert_builder(client, project, requestId, body)?;
    compute_networks_insert_execute(builder)
}

/// GET projects/{project}/global/networks
/// Retrieves the list of networks available to the specified project.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_networks_list_execute()` to send, or `compute_networks_list` for simplest API.

pub fn compute_networks_list_builder(
    client: &SimpleHttpClient,
    project: &str,
    filter: Option<&str>,
    maxResults: Option<i32>,
    orderBy: Option<&str>,
    pageToken: Option<&str>,
    returnPartialSuccess: Option<bool>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/global/networks",
        project,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "filter",
            filter
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "maxResults",
            maxResults
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "orderBy",
            orderBy
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageToken",
            pageToken
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "returnPartialSuccess",
            returnPartialSuccess
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET projects/{project}/global/networks
/// Retrieves the list of networks available to the specified project.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_networks_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_networks_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_networks_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<NetworkList>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: NetworkList = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/global/networks
/// Retrieves the list of networks available to the specified project.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_networks_list_builder()` + `compute_networks_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_networks_list(
    client: &SimpleHttpClient,
    project: &str,
    filter: Option<&str>,
    maxResults: Option<i32>,
    orderBy: Option<&str>,
    pageToken: Option<&str>,
    returnPartialSuccess: Option<bool>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<NetworkList>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_networks_list_builder(
        client,
        project,
        filter,
        maxResults,
        orderBy,
        pageToken,
        returnPartialSuccess,
    )?;
    compute_networks_list_execute(builder)
}

/// GET projects/{project}/global/networks/{network}/listPeeringRoutes
/// Lists the peering routes exchanged over peering connection.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_networks_list_peering_routes_execute()` to send, or `compute_networks_list_peering_routes` for simplest API.

pub fn compute_networks_list_peering_routes_builder(
    client: &SimpleHttpClient,
    network: &str,
    project: &str,
    direction: Option<&str>,
    filter: Option<&str>,
    maxResults: Option<i32>,
    orderBy: Option<&str>,
    pageToken: Option<&str>,
    peeringName: Option<&str>,
    region: Option<&str>,
    returnPartialSuccess: Option<bool>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/global/networks/{}/listPeeringRoutes",
        network, project,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "direction",
            direction
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "filter",
            filter
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "maxResults",
            maxResults
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "orderBy",
            orderBy
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageToken",
            pageToken
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "peeringName",
            peeringName
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "region",
            region
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "returnPartialSuccess",
            returnPartialSuccess
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET projects/{project}/global/networks/{network}/listPeeringRoutes
/// Lists the peering routes exchanged over peering connection.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_networks_list_peering_routes_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_networks_list_peering_routes_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_networks_list_peering_routes_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ExchangedPeeringRoutesList>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ExchangedPeeringRoutesList = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/global/networks/{network}/listPeeringRoutes
/// Lists the peering routes exchanged over peering connection.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_networks_list_peering_routes_builder()` + `compute_networks_list_peering_routes_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_networks_list_peering_routes(
    client: &SimpleHttpClient,
    network: &str,
    project: &str,
    direction: Option<&str>,
    filter: Option<&str>,
    maxResults: Option<i32>,
    orderBy: Option<&str>,
    pageToken: Option<&str>,
    peeringName: Option<&str>,
    region: Option<&str>,
    returnPartialSuccess: Option<bool>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ExchangedPeeringRoutesList>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = compute_networks_list_peering_routes_builder(
        client,
        network,
        project,
        direction,
        filter,
        maxResults,
        orderBy,
        pageToken,
        peeringName,
        region,
        returnPartialSuccess,
    )?;
    compute_networks_list_peering_routes_execute(builder)
}

/// GET projects/{project}/global/networks/{network}
/// Patches the specified network with the data included in the request. Only `routingConfig` can be modified.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_networks_patch_execute()` to send, or `compute_networks_patch` for simplest API.

pub fn compute_networks_patch_builder(
    client: &SimpleHttpClient,
    network: &str,
    project: &str,
    requestId: Option<&str>,
    body: &Network,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/global/networks/{}",
        network, project,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "requestId",
            requestId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET projects/{project}/global/networks/{network}
/// Patches the specified network with the data included in the request. Only `routingConfig` can be modified.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_networks_patch_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_networks_patch_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_networks_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/global/networks/{network}
/// Patches the specified network with the data included in the request. Only `routingConfig` can be modified.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_networks_patch_builder()` + `compute_networks_patch_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_networks_patch(
    client: &SimpleHttpClient,
    network: &str,
    project: &str,
    requestId: Option<&str>,
    body: &Network,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_networks_patch_builder(client, network, project, requestId, body)?;
    compute_networks_patch_execute(builder)
}

/// GET projects/{project}/global/networks/{network}/removePeering
/// Removes a peering from the specified network.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_networks_remove_peering_execute()` to send, or `compute_networks_remove_peering` for simplest API.

pub fn compute_networks_remove_peering_builder(
    client: &SimpleHttpClient,
    network: &str,
    project: &str,
    requestId: Option<&str>,
    body: &NetworksRemovePeeringRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/global/networks/{}/removePeering",
        network, project,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "requestId",
            requestId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET projects/{project}/global/networks/{network}/removePeering
/// Removes a peering from the specified network.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_networks_remove_peering_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_networks_remove_peering_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_networks_remove_peering_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/global/networks/{network}/removePeering
/// Removes a peering from the specified network.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_networks_remove_peering_builder()` + `compute_networks_remove_peering_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_networks_remove_peering(
    client: &SimpleHttpClient,
    network: &str,
    project: &str,
    requestId: Option<&str>,
    body: &NetworksRemovePeeringRequest,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        compute_networks_remove_peering_builder(client, network, project, requestId, body)?;
    compute_networks_remove_peering_execute(builder)
}

/// GET projects/{project}/global/networks/{network}/requestRemovePeering
/// Requests to remove a peering from the specified network. Applicable only for PeeringConnection with update_strategy=CONSENSUS.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_networks_request_remove_peering_execute()` to send, or `compute_networks_request_remove_peering` for simplest API.

pub fn compute_networks_request_remove_peering_builder(
    client: &SimpleHttpClient,
    network: &str,
    project: &str,
    requestId: Option<&str>,
    body: &NetworksRequestRemovePeeringRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/global/networks/{}/requestRemovePeering",
        network, project,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "requestId",
            requestId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET projects/{project}/global/networks/{network}/requestRemovePeering
/// Requests to remove a peering from the specified network. Applicable only for PeeringConnection with update_strategy=CONSENSUS.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_networks_request_remove_peering_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_networks_request_remove_peering_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_networks_request_remove_peering_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/global/networks/{network}/requestRemovePeering
/// Requests to remove a peering from the specified network. Applicable only for PeeringConnection with update_strategy=CONSENSUS.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_networks_request_remove_peering_builder()` + `compute_networks_request_remove_peering_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_networks_request_remove_peering(
    client: &SimpleHttpClient,
    network: &str,
    project: &str,
    requestId: Option<&str>,
    body: &NetworksRequestRemovePeeringRequest,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        compute_networks_request_remove_peering_builder(client, network, project, requestId, body)?;
    compute_networks_request_remove_peering_execute(builder)
}

/// GET projects/{project}/global/networks/{network}/switchToCustomMode
/// Switches the network mode from auto subnet mode to custom subnet mode.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_networks_switch_to_custom_mode_execute()` to send, or `compute_networks_switch_to_custom_mode` for simplest API.

pub fn compute_networks_switch_to_custom_mode_builder(
    client: &SimpleHttpClient,
    network: &str,
    project: &str,
    requestId: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/global/networks/{}/switchToCustomMode",
        network, project,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "requestId",
            requestId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET projects/{project}/global/networks/{network}/switchToCustomMode
/// Switches the network mode from auto subnet mode to custom subnet mode.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_networks_switch_to_custom_mode_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_networks_switch_to_custom_mode_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_networks_switch_to_custom_mode_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/global/networks/{network}/switchToCustomMode
/// Switches the network mode from auto subnet mode to custom subnet mode.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_networks_switch_to_custom_mode_builder()` + `compute_networks_switch_to_custom_mode_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_networks_switch_to_custom_mode(
    client: &SimpleHttpClient,
    network: &str,
    project: &str,
    requestId: Option<&str>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        compute_networks_switch_to_custom_mode_builder(client, network, project, requestId)?;
    compute_networks_switch_to_custom_mode_execute(builder)
}

/// GET projects/{project}/global/networks/{network}/updatePeering
/// Updates the specified network peering with the data included in the request. You can only modify the NetworkPeering.export_custom_routes field and the NetworkPeering.import_custom_routes field.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_networks_update_peering_execute()` to send, or `compute_networks_update_peering` for simplest API.

pub fn compute_networks_update_peering_builder(
    client: &SimpleHttpClient,
    network: &str,
    project: &str,
    requestId: Option<&str>,
    body: &NetworksUpdatePeeringRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/global/networks/{}/updatePeering",
        network, project,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "requestId",
            requestId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET projects/{project}/global/networks/{network}/updatePeering
/// Updates the specified network peering with the data included in the request. You can only modify the NetworkPeering.export_custom_routes field and the NetworkPeering.import_custom_routes field.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_networks_update_peering_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_networks_update_peering_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_networks_update_peering_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/global/networks/{network}/updatePeering
/// Updates the specified network peering with the data included in the request. You can only modify the NetworkPeering.export_custom_routes field and the NetworkPeering.import_custom_routes field.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_networks_update_peering_builder()` + `compute_networks_update_peering_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_networks_update_peering(
    client: &SimpleHttpClient,
    network: &str,
    project: &str,
    requestId: Option<&str>,
    body: &NetworksUpdatePeeringRequest,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        compute_networks_update_peering_builder(client, network, project, requestId, body)?;
    compute_networks_update_peering_execute(builder)
}

/// GET projects/{project}/zones/{zone}/nodeGroups/{nodeGroup}/addNodes
/// Adds specified number of nodes to the node group.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_node_groups_add_nodes_execute()` to send, or `compute_node_groups_add_nodes` for simplest API.

pub fn compute_node_groups_add_nodes_builder(
    client: &SimpleHttpClient,
    nodeGroup: &str,
    project: &str,
    zone: &str,
    requestId: Option<&str>,
    body: &NodeGroupsAddNodesRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/zones/{}/nodeGroups/{}/addNodes",
        nodeGroup, project, zone,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "requestId",
            requestId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET projects/{project}/zones/{zone}/nodeGroups/{nodeGroup}/addNodes
/// Adds specified number of nodes to the node group.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_node_groups_add_nodes_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_node_groups_add_nodes_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_node_groups_add_nodes_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/zones/{zone}/nodeGroups/{nodeGroup}/addNodes
/// Adds specified number of nodes to the node group.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_node_groups_add_nodes_builder()` + `compute_node_groups_add_nodes_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_node_groups_add_nodes(
    client: &SimpleHttpClient,
    nodeGroup: &str,
    project: &str,
    zone: &str,
    requestId: Option<&str>,
    body: &NodeGroupsAddNodesRequest,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        compute_node_groups_add_nodes_builder(client, nodeGroup, project, zone, requestId, body)?;
    compute_node_groups_add_nodes_execute(builder)
}

/// GET projects/{project}/aggregated/nodeGroups
/// Retrieves an aggregated list of node groups. Note: use `nodeGroups`.`listNodes` for more details about each group.  To prevent failure, Google recommends that you set the `returnPartialSuccess` parameter to `true`.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_node_groups_aggregated_list_execute()` to send, or `compute_node_groups_aggregated_list` for simplest API.

pub fn compute_node_groups_aggregated_list_builder(
    client: &SimpleHttpClient,
    project: &str,
    filter: Option<&str>,
    includeAllScopes: Option<bool>,
    maxResults: Option<i32>,
    orderBy: Option<&str>,
    pageToken: Option<&str>,
    returnPartialSuccess: Option<bool>,
    serviceProjectNumber: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/aggregated/nodeGroups",
        project,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "filter",
            filter
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "includeAllScopes",
            includeAllScopes
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "maxResults",
            maxResults
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "orderBy",
            orderBy
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageToken",
            pageToken
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "returnPartialSuccess",
            returnPartialSuccess
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "serviceProjectNumber",
            serviceProjectNumber
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET projects/{project}/aggregated/nodeGroups
/// Retrieves an aggregated list of node groups. Note: use `nodeGroups`.`listNodes` for more details about each group.  To prevent failure, Google recommends that you set the `returnPartialSuccess` parameter to `true`.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_node_groups_aggregated_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_node_groups_aggregated_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_node_groups_aggregated_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<NodeGroupAggregatedList>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: NodeGroupAggregatedList = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/aggregated/nodeGroups
/// Retrieves an aggregated list of node groups. Note: use `nodeGroups`.`listNodes` for more details about each group.  To prevent failure, Google recommends that you set the `returnPartialSuccess` parameter to `true`.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_node_groups_aggregated_list_builder()` + `compute_node_groups_aggregated_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_node_groups_aggregated_list(
    client: &SimpleHttpClient,
    project: &str,
    filter: Option<&str>,
    includeAllScopes: Option<bool>,
    maxResults: Option<i32>,
    orderBy: Option<&str>,
    pageToken: Option<&str>,
    returnPartialSuccess: Option<bool>,
    serviceProjectNumber: Option<&str>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<NodeGroupAggregatedList>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = compute_node_groups_aggregated_list_builder(
        client,
        project,
        filter,
        includeAllScopes,
        maxResults,
        orderBy,
        pageToken,
        returnPartialSuccess,
        serviceProjectNumber,
    )?;
    compute_node_groups_aggregated_list_execute(builder)
}

/// GET projects/{project}/zones/{zone}/nodeGroups/{nodeGroup}
/// Deletes the specified NodeGroup resource.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_node_groups_delete_execute()` to send, or `compute_node_groups_delete` for simplest API.

pub fn compute_node_groups_delete_builder(
    client: &SimpleHttpClient,
    nodeGroup: &str,
    project: &str,
    zone: &str,
    requestId: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/zones/{}/nodeGroups/{}",
        nodeGroup, project, zone,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "requestId",
            requestId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET projects/{project}/zones/{zone}/nodeGroups/{nodeGroup}
/// Deletes the specified NodeGroup resource.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_node_groups_delete_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_node_groups_delete_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_node_groups_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/zones/{zone}/nodeGroups/{nodeGroup}
/// Deletes the specified NodeGroup resource.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_node_groups_delete_builder()` + `compute_node_groups_delete_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_node_groups_delete(
    client: &SimpleHttpClient,
    nodeGroup: &str,
    project: &str,
    zone: &str,
    requestId: Option<&str>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_node_groups_delete_builder(client, nodeGroup, project, zone, requestId)?;
    compute_node_groups_delete_execute(builder)
}

/// GET projects/{project}/zones/{zone}/nodeGroups/{nodeGroup}/deleteNodes
/// Deletes specified nodes from the node group.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_node_groups_delete_nodes_execute()` to send, or `compute_node_groups_delete_nodes` for simplest API.

pub fn compute_node_groups_delete_nodes_builder(
    client: &SimpleHttpClient,
    nodeGroup: &str,
    project: &str,
    zone: &str,
    requestId: Option<&str>,
    body: &NodeGroupsDeleteNodesRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/zones/{}/nodeGroups/{}/deleteNodes",
        nodeGroup, project, zone,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "requestId",
            requestId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET projects/{project}/zones/{zone}/nodeGroups/{nodeGroup}/deleteNodes
/// Deletes specified nodes from the node group.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_node_groups_delete_nodes_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_node_groups_delete_nodes_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_node_groups_delete_nodes_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/zones/{zone}/nodeGroups/{nodeGroup}/deleteNodes
/// Deletes specified nodes from the node group.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_node_groups_delete_nodes_builder()` + `compute_node_groups_delete_nodes_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_node_groups_delete_nodes(
    client: &SimpleHttpClient,
    nodeGroup: &str,
    project: &str,
    zone: &str,
    requestId: Option<&str>,
    body: &NodeGroupsDeleteNodesRequest,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_node_groups_delete_nodes_builder(
        client, nodeGroup, project, zone, requestId, body,
    )?;
    compute_node_groups_delete_nodes_execute(builder)
}

/// GET projects/{project}/zones/{zone}/nodeGroups/{nodeGroup}
/// Returns the specified NodeGroup. Get a list of available NodeGroups by making a list() request. Note: the "nodes" field should not be used. Use `nodeGroups`.`listNodes` instead.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_node_groups_get_execute()` to send, or `compute_node_groups_get` for simplest API.

pub fn compute_node_groups_get_builder(
    client: &SimpleHttpClient,
    nodeGroup: &str,
    project: &str,
    zone: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/zones/{}/nodeGroups/{}",
        nodeGroup, project, zone,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/zones/{zone}/nodeGroups/{nodeGroup}
/// Returns the specified NodeGroup. Get a list of available NodeGroups by making a list() request. Note: the "nodes" field should not be used. Use `nodeGroups`.`listNodes` instead.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_node_groups_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_node_groups_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_node_groups_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<NodeGroup>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: NodeGroup = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/zones/{zone}/nodeGroups/{nodeGroup}
/// Returns the specified NodeGroup. Get a list of available NodeGroups by making a list() request. Note: the "nodes" field should not be used. Use `nodeGroups`.`listNodes` instead.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_node_groups_get_builder()` + `compute_node_groups_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_node_groups_get(
    client: &SimpleHttpClient,
    nodeGroup: &str,
    project: &str,
    zone: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<NodeGroup>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_node_groups_get_builder(client, nodeGroup, project, zone)?;
    compute_node_groups_get_execute(builder)
}

/// GET projects/{project}/zones/{zone}/nodeGroups/{resource}/getIamPolicy
/// Gets the access control policy for a resource. May be empty if no such policy or resource exists.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_node_groups_get_iam_policy_execute()` to send, or `compute_node_groups_get_iam_policy` for simplest API.

pub fn compute_node_groups_get_iam_policy_builder(
    client: &SimpleHttpClient,
    project: &str,
    resource: &str,
    zone: &str,
    optionsRequestedPolicyVersion: Option<i32>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/zones/{}/nodeGroups/{}/getIamPolicy",
        project, resource, zone,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "optionsRequestedPolicyVersion",
            optionsRequestedPolicyVersion
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET projects/{project}/zones/{zone}/nodeGroups/{resource}/getIamPolicy
/// Gets the access control policy for a resource. May be empty if no such policy or resource exists.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_node_groups_get_iam_policy_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_node_groups_get_iam_policy_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_node_groups_get_iam_policy_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Policy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Policy = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/zones/{zone}/nodeGroups/{resource}/getIamPolicy
/// Gets the access control policy for a resource. May be empty if no such policy or resource exists.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_node_groups_get_iam_policy_builder()` + `compute_node_groups_get_iam_policy_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_node_groups_get_iam_policy(
    client: &SimpleHttpClient,
    project: &str,
    resource: &str,
    zone: &str,
    optionsRequestedPolicyVersion: Option<i32>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Policy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_node_groups_get_iam_policy_builder(
        client,
        project,
        resource,
        zone,
        optionsRequestedPolicyVersion,
    )?;
    compute_node_groups_get_iam_policy_execute(builder)
}

/// GET projects/{project}/zones/{zone}/nodeGroups
/// Creates a NodeGroup resource in the specified project using the data included in the request.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_node_groups_insert_execute()` to send, or `compute_node_groups_insert` for simplest API.

pub fn compute_node_groups_insert_builder(
    client: &SimpleHttpClient,
    project: &str,
    zone: &str,
    initialNodeCount: Option<i32>,
    requestId: Option<&str>,
    body: &NodeGroup,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/zones/{}/nodeGroups",
        project, zone,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "initialNodeCount",
            initialNodeCount
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "requestId",
            requestId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET projects/{project}/zones/{zone}/nodeGroups
/// Creates a NodeGroup resource in the specified project using the data included in the request.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_node_groups_insert_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_node_groups_insert_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_node_groups_insert_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/zones/{zone}/nodeGroups
/// Creates a NodeGroup resource in the specified project using the data included in the request.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_node_groups_insert_builder()` + `compute_node_groups_insert_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_node_groups_insert(
    client: &SimpleHttpClient,
    project: &str,
    zone: &str,
    initialNodeCount: Option<i32>,
    requestId: Option<&str>,
    body: &NodeGroup,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_node_groups_insert_builder(
        client,
        project,
        zone,
        initialNodeCount,
        requestId,
        body,
    )?;
    compute_node_groups_insert_execute(builder)
}

/// GET projects/{project}/zones/{zone}/nodeGroups
/// Retrieves a list of node groups available to the specified project. Note: use `nodeGroups`.`listNodes` for more details about each group.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_node_groups_list_execute()` to send, or `compute_node_groups_list` for simplest API.

pub fn compute_node_groups_list_builder(
    client: &SimpleHttpClient,
    project: &str,
    zone: &str,
    filter: Option<&str>,
    maxResults: Option<i32>,
    orderBy: Option<&str>,
    pageToken: Option<&str>,
    returnPartialSuccess: Option<bool>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/zones/{}/nodeGroups",
        project, zone,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "filter",
            filter
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "maxResults",
            maxResults
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "orderBy",
            orderBy
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageToken",
            pageToken
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "returnPartialSuccess",
            returnPartialSuccess
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET projects/{project}/zones/{zone}/nodeGroups
/// Retrieves a list of node groups available to the specified project. Note: use `nodeGroups`.`listNodes` for more details about each group.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_node_groups_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_node_groups_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_node_groups_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<NodeGroupList>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: NodeGroupList = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/zones/{zone}/nodeGroups
/// Retrieves a list of node groups available to the specified project. Note: use `nodeGroups`.`listNodes` for more details about each group.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_node_groups_list_builder()` + `compute_node_groups_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_node_groups_list(
    client: &SimpleHttpClient,
    project: &str,
    zone: &str,
    filter: Option<&str>,
    maxResults: Option<i32>,
    orderBy: Option<&str>,
    pageToken: Option<&str>,
    returnPartialSuccess: Option<bool>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<NodeGroupList>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = compute_node_groups_list_builder(
        client,
        project,
        zone,
        filter,
        maxResults,
        orderBy,
        pageToken,
        returnPartialSuccess,
    )?;
    compute_node_groups_list_execute(builder)
}

/// GET projects/{project}/zones/{zone}/nodeGroups/{nodeGroup}/listNodes
/// Lists nodes in the node group.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_node_groups_list_nodes_execute()` to send, or `compute_node_groups_list_nodes` for simplest API.

pub fn compute_node_groups_list_nodes_builder(
    client: &SimpleHttpClient,
    nodeGroup: &str,
    project: &str,
    zone: &str,
    filter: Option<&str>,
    maxResults: Option<i32>,
    orderBy: Option<&str>,
    pageToken: Option<&str>,
    returnPartialSuccess: Option<bool>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/zones/{}/nodeGroups/{}/listNodes",
        nodeGroup, project, zone,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "filter",
            filter
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "maxResults",
            maxResults
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "orderBy",
            orderBy
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageToken",
            pageToken
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "returnPartialSuccess",
            returnPartialSuccess
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET projects/{project}/zones/{zone}/nodeGroups/{nodeGroup}/listNodes
/// Lists nodes in the node group.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_node_groups_list_nodes_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_node_groups_list_nodes_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_node_groups_list_nodes_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<NodeGroupsListNodes>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: NodeGroupsListNodes = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/zones/{zone}/nodeGroups/{nodeGroup}/listNodes
/// Lists nodes in the node group.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_node_groups_list_nodes_builder()` + `compute_node_groups_list_nodes_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_node_groups_list_nodes(
    client: &SimpleHttpClient,
    nodeGroup: &str,
    project: &str,
    zone: &str,
    filter: Option<&str>,
    maxResults: Option<i32>,
    orderBy: Option<&str>,
    pageToken: Option<&str>,
    returnPartialSuccess: Option<bool>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<NodeGroupsListNodes>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = compute_node_groups_list_nodes_builder(
        client,
        nodeGroup,
        project,
        zone,
        filter,
        maxResults,
        orderBy,
        pageToken,
        returnPartialSuccess,
    )?;
    compute_node_groups_list_nodes_execute(builder)
}

/// GET projects/{project}/zones/{zone}/nodeGroups/{nodeGroup}
/// Updates the specified node group.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_node_groups_patch_execute()` to send, or `compute_node_groups_patch` for simplest API.

pub fn compute_node_groups_patch_builder(
    client: &SimpleHttpClient,
    nodeGroup: &str,
    project: &str,
    zone: &str,
    requestId: Option<&str>,
    body: &NodeGroup,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/zones/{}/nodeGroups/{}",
        nodeGroup, project, zone,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "requestId",
            requestId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET projects/{project}/zones/{zone}/nodeGroups/{nodeGroup}
/// Updates the specified node group.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_node_groups_patch_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_node_groups_patch_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_node_groups_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/zones/{zone}/nodeGroups/{nodeGroup}
/// Updates the specified node group.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_node_groups_patch_builder()` + `compute_node_groups_patch_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_node_groups_patch(
    client: &SimpleHttpClient,
    nodeGroup: &str,
    project: &str,
    zone: &str,
    requestId: Option<&str>,
    body: &NodeGroup,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        compute_node_groups_patch_builder(client, nodeGroup, project, zone, requestId, body)?;
    compute_node_groups_patch_execute(builder)
}

/// GET projects/{project}/zones/{zone}/nodeGroups/{nodeGroup}/performMaintenance
/// Perform maintenance on a subset of nodes in the node group.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_node_groups_perform_maintenance_execute()` to send, or `compute_node_groups_perform_maintenance` for simplest API.

pub fn compute_node_groups_perform_maintenance_builder(
    client: &SimpleHttpClient,
    nodeGroup: &str,
    project: &str,
    zone: &str,
    requestId: Option<&str>,
    body: &NodeGroupsPerformMaintenanceRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/zones/{}/nodeGroups/{}/performMaintenance",
        nodeGroup, project, zone,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "requestId",
            requestId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET projects/{project}/zones/{zone}/nodeGroups/{nodeGroup}/performMaintenance
/// Perform maintenance on a subset of nodes in the node group.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_node_groups_perform_maintenance_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_node_groups_perform_maintenance_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_node_groups_perform_maintenance_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/zones/{zone}/nodeGroups/{nodeGroup}/performMaintenance
/// Perform maintenance on a subset of nodes in the node group.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_node_groups_perform_maintenance_builder()` + `compute_node_groups_perform_maintenance_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_node_groups_perform_maintenance(
    client: &SimpleHttpClient,
    nodeGroup: &str,
    project: &str,
    zone: &str,
    requestId: Option<&str>,
    body: &NodeGroupsPerformMaintenanceRequest,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_node_groups_perform_maintenance_builder(
        client, nodeGroup, project, zone, requestId, body,
    )?;
    compute_node_groups_perform_maintenance_execute(builder)
}

/// GET projects/{project}/zones/{zone}/nodeGroups/{resource}/setIamPolicy
/// Sets the access control policy on the specified resource. Replaces any existing policy.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_node_groups_set_iam_policy_execute()` to send, or `compute_node_groups_set_iam_policy` for simplest API.

pub fn compute_node_groups_set_iam_policy_builder(
    client: &SimpleHttpClient,
    project: &str,
    resource: &str,
    zone: &str,
    body: &ZoneSetPolicyRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/zones/{}/nodeGroups/{}/setIamPolicy",
        project, resource, zone,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET projects/{project}/zones/{zone}/nodeGroups/{resource}/setIamPolicy
/// Sets the access control policy on the specified resource. Replaces any existing policy.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_node_groups_set_iam_policy_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_node_groups_set_iam_policy_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_node_groups_set_iam_policy_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Policy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Policy = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/zones/{zone}/nodeGroups/{resource}/setIamPolicy
/// Sets the access control policy on the specified resource. Replaces any existing policy.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_node_groups_set_iam_policy_builder()` + `compute_node_groups_set_iam_policy_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_node_groups_set_iam_policy(
    client: &SimpleHttpClient,
    project: &str,
    resource: &str,
    zone: &str,
    body: &ZoneSetPolicyRequest,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Policy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        compute_node_groups_set_iam_policy_builder(client, project, resource, zone, body)?;
    compute_node_groups_set_iam_policy_execute(builder)
}

/// GET projects/{project}/zones/{zone}/nodeGroups/{nodeGroup}/setNodeTemplate
/// Updates the node template of the node group.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_node_groups_set_node_template_execute()` to send, or `compute_node_groups_set_node_template` for simplest API.

pub fn compute_node_groups_set_node_template_builder(
    client: &SimpleHttpClient,
    nodeGroup: &str,
    project: &str,
    zone: &str,
    requestId: Option<&str>,
    body: &NodeGroupsSetNodeTemplateRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/zones/{}/nodeGroups/{}/setNodeTemplate",
        nodeGroup, project, zone,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "requestId",
            requestId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET projects/{project}/zones/{zone}/nodeGroups/{nodeGroup}/setNodeTemplate
/// Updates the node template of the node group.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_node_groups_set_node_template_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_node_groups_set_node_template_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_node_groups_set_node_template_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/zones/{zone}/nodeGroups/{nodeGroup}/setNodeTemplate
/// Updates the node template of the node group.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_node_groups_set_node_template_builder()` + `compute_node_groups_set_node_template_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_node_groups_set_node_template(
    client: &SimpleHttpClient,
    nodeGroup: &str,
    project: &str,
    zone: &str,
    requestId: Option<&str>,
    body: &NodeGroupsSetNodeTemplateRequest,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_node_groups_set_node_template_builder(
        client, nodeGroup, project, zone, requestId, body,
    )?;
    compute_node_groups_set_node_template_execute(builder)
}

/// GET projects/{project}/zones/{zone}/nodeGroups/{nodeGroup}/simulateMaintenanceEvent
/// Simulates maintenance event on specified nodes from the node group.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_node_groups_simulate_maintenance_event_execute()` to send, or `compute_node_groups_simulate_maintenance_event` for simplest API.

pub fn compute_node_groups_simulate_maintenance_event_builder(
    client: &SimpleHttpClient,
    nodeGroup: &str,
    project: &str,
    zone: &str,
    requestId: Option<&str>,
    body: &NodeGroupsSimulateMaintenanceEventRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/zones/{}/nodeGroups/{}/simulateMaintenanceEvent",
        nodeGroup, project, zone,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "requestId",
            requestId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET projects/{project}/zones/{zone}/nodeGroups/{nodeGroup}/simulateMaintenanceEvent
/// Simulates maintenance event on specified nodes from the node group.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_node_groups_simulate_maintenance_event_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_node_groups_simulate_maintenance_event_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_node_groups_simulate_maintenance_event_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/zones/{zone}/nodeGroups/{nodeGroup}/simulateMaintenanceEvent
/// Simulates maintenance event on specified nodes from the node group.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_node_groups_simulate_maintenance_event_builder()` + `compute_node_groups_simulate_maintenance_event_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_node_groups_simulate_maintenance_event(
    client: &SimpleHttpClient,
    nodeGroup: &str,
    project: &str,
    zone: &str,
    requestId: Option<&str>,
    body: &NodeGroupsSimulateMaintenanceEventRequest,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_node_groups_simulate_maintenance_event_builder(
        client, nodeGroup, project, zone, requestId, body,
    )?;
    compute_node_groups_simulate_maintenance_event_execute(builder)
}

/// GET projects/{project}/zones/{zone}/nodeGroups/{resource}/testIamPermissions
/// Returns permissions that a caller has on the specified resource.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_node_groups_test_iam_permissions_execute()` to send, or `compute_node_groups_test_iam_permissions` for simplest API.

pub fn compute_node_groups_test_iam_permissions_builder(
    client: &SimpleHttpClient,
    project: &str,
    resource: &str,
    zone: &str,
    body: &TestPermissionsRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/zones/{}/nodeGroups/{}/testIamPermissions",
        project, resource, zone,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET projects/{project}/zones/{zone}/nodeGroups/{resource}/testIamPermissions
/// Returns permissions that a caller has on the specified resource.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_node_groups_test_iam_permissions_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_node_groups_test_iam_permissions_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_node_groups_test_iam_permissions_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<TestPermissionsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: TestPermissionsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/zones/{zone}/nodeGroups/{resource}/testIamPermissions
/// Returns permissions that a caller has on the specified resource.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_node_groups_test_iam_permissions_builder()` + `compute_node_groups_test_iam_permissions_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_node_groups_test_iam_permissions(
    client: &SimpleHttpClient,
    project: &str,
    resource: &str,
    zone: &str,
    body: &TestPermissionsRequest,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<TestPermissionsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        compute_node_groups_test_iam_permissions_builder(client, project, resource, zone, body)?;
    compute_node_groups_test_iam_permissions_execute(builder)
}

/// GET projects/{project}/aggregated/nodeTemplates
/// Retrieves an aggregated list of node templates.  To prevent failure, Google recommends that you set the `returnPartialSuccess` parameter to `true`.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_node_templates_aggregated_list_execute()` to send, or `compute_node_templates_aggregated_list` for simplest API.

pub fn compute_node_templates_aggregated_list_builder(
    client: &SimpleHttpClient,
    project: &str,
    filter: Option<&str>,
    includeAllScopes: Option<bool>,
    maxResults: Option<i32>,
    orderBy: Option<&str>,
    pageToken: Option<&str>,
    returnPartialSuccess: Option<bool>,
    serviceProjectNumber: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/aggregated/nodeTemplates",
        project,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "filter",
            filter
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "includeAllScopes",
            includeAllScopes
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "maxResults",
            maxResults
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "orderBy",
            orderBy
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageToken",
            pageToken
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "returnPartialSuccess",
            returnPartialSuccess
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "serviceProjectNumber",
            serviceProjectNumber
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET projects/{project}/aggregated/nodeTemplates
/// Retrieves an aggregated list of node templates.  To prevent failure, Google recommends that you set the `returnPartialSuccess` parameter to `true`.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_node_templates_aggregated_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_node_templates_aggregated_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_node_templates_aggregated_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<NodeTemplateAggregatedList>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: NodeTemplateAggregatedList = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/aggregated/nodeTemplates
/// Retrieves an aggregated list of node templates.  To prevent failure, Google recommends that you set the `returnPartialSuccess` parameter to `true`.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_node_templates_aggregated_list_builder()` + `compute_node_templates_aggregated_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_node_templates_aggregated_list(
    client: &SimpleHttpClient,
    project: &str,
    filter: Option<&str>,
    includeAllScopes: Option<bool>,
    maxResults: Option<i32>,
    orderBy: Option<&str>,
    pageToken: Option<&str>,
    returnPartialSuccess: Option<bool>,
    serviceProjectNumber: Option<&str>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<NodeTemplateAggregatedList>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = compute_node_templates_aggregated_list_builder(
        client,
        project,
        filter,
        includeAllScopes,
        maxResults,
        orderBy,
        pageToken,
        returnPartialSuccess,
        serviceProjectNumber,
    )?;
    compute_node_templates_aggregated_list_execute(builder)
}

/// GET projects/{project}/regions/{region}/nodeTemplates/{nodeTemplate}
/// Deletes the specified NodeTemplate resource.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_node_templates_delete_execute()` to send, or `compute_node_templates_delete` for simplest API.

pub fn compute_node_templates_delete_builder(
    client: &SimpleHttpClient,
    nodeTemplate: &str,
    project: &str,
    region: &str,
    requestId: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/regions/{}/nodeTemplates/{}",
        nodeTemplate, project, region,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "requestId",
            requestId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET projects/{project}/regions/{region}/nodeTemplates/{nodeTemplate}
/// Deletes the specified NodeTemplate resource.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_node_templates_delete_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_node_templates_delete_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_node_templates_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/regions/{region}/nodeTemplates/{nodeTemplate}
/// Deletes the specified NodeTemplate resource.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_node_templates_delete_builder()` + `compute_node_templates_delete_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_node_templates_delete(
    client: &SimpleHttpClient,
    nodeTemplate: &str,
    project: &str,
    region: &str,
    requestId: Option<&str>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        compute_node_templates_delete_builder(client, nodeTemplate, project, region, requestId)?;
    compute_node_templates_delete_execute(builder)
}

/// GET projects/{project}/regions/{region}/nodeTemplates/{nodeTemplate}
/// Returns the specified node template.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_node_templates_get_execute()` to send, or `compute_node_templates_get` for simplest API.

pub fn compute_node_templates_get_builder(
    client: &SimpleHttpClient,
    nodeTemplate: &str,
    project: &str,
    region: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/regions/{}/nodeTemplates/{}",
        nodeTemplate, project, region,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/regions/{region}/nodeTemplates/{nodeTemplate}
/// Returns the specified node template.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_node_templates_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_node_templates_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_node_templates_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<NodeTemplate>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: NodeTemplate = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/regions/{region}/nodeTemplates/{nodeTemplate}
/// Returns the specified node template.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_node_templates_get_builder()` + `compute_node_templates_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_node_templates_get(
    client: &SimpleHttpClient,
    nodeTemplate: &str,
    project: &str,
    region: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<NodeTemplate>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = compute_node_templates_get_builder(client, nodeTemplate, project, region)?;
    compute_node_templates_get_execute(builder)
}

/// GET projects/{project}/regions/{region}/nodeTemplates/{resource}/getIamPolicy
/// Gets the access control policy for a resource. May be empty if no such policy or resource exists.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_node_templates_get_iam_policy_execute()` to send, or `compute_node_templates_get_iam_policy` for simplest API.

pub fn compute_node_templates_get_iam_policy_builder(
    client: &SimpleHttpClient,
    project: &str,
    region: &str,
    resource: &str,
    optionsRequestedPolicyVersion: Option<i32>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/regions/{}/nodeTemplates/{}/getIamPolicy",
        project, region, resource,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "optionsRequestedPolicyVersion",
            optionsRequestedPolicyVersion
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET projects/{project}/regions/{region}/nodeTemplates/{resource}/getIamPolicy
/// Gets the access control policy for a resource. May be empty if no such policy or resource exists.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_node_templates_get_iam_policy_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_node_templates_get_iam_policy_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_node_templates_get_iam_policy_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Policy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Policy = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/regions/{region}/nodeTemplates/{resource}/getIamPolicy
/// Gets the access control policy for a resource. May be empty if no such policy or resource exists.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_node_templates_get_iam_policy_builder()` + `compute_node_templates_get_iam_policy_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_node_templates_get_iam_policy(
    client: &SimpleHttpClient,
    project: &str,
    region: &str,
    resource: &str,
    optionsRequestedPolicyVersion: Option<i32>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Policy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_node_templates_get_iam_policy_builder(
        client,
        project,
        region,
        resource,
        optionsRequestedPolicyVersion,
    )?;
    compute_node_templates_get_iam_policy_execute(builder)
}

/// GET projects/{project}/regions/{region}/nodeTemplates
/// Creates a NodeTemplate resource in the specified project using the data included in the request.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_node_templates_insert_execute()` to send, or `compute_node_templates_insert` for simplest API.

pub fn compute_node_templates_insert_builder(
    client: &SimpleHttpClient,
    project: &str,
    region: &str,
    requestId: Option<&str>,
    body: &NodeTemplate,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/regions/{}/nodeTemplates",
        project, region,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "requestId",
            requestId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET projects/{project}/regions/{region}/nodeTemplates
/// Creates a NodeTemplate resource in the specified project using the data included in the request.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_node_templates_insert_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_node_templates_insert_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_node_templates_insert_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/regions/{region}/nodeTemplates
/// Creates a NodeTemplate resource in the specified project using the data included in the request.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_node_templates_insert_builder()` + `compute_node_templates_insert_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_node_templates_insert(
    client: &SimpleHttpClient,
    project: &str,
    region: &str,
    requestId: Option<&str>,
    body: &NodeTemplate,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_node_templates_insert_builder(client, project, region, requestId, body)?;
    compute_node_templates_insert_execute(builder)
}

/// GET projects/{project}/regions/{region}/nodeTemplates
/// Retrieves a list of node templates available to the specified project.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_node_templates_list_execute()` to send, or `compute_node_templates_list` for simplest API.

pub fn compute_node_templates_list_builder(
    client: &SimpleHttpClient,
    project: &str,
    region: &str,
    filter: Option<&str>,
    maxResults: Option<i32>,
    orderBy: Option<&str>,
    pageToken: Option<&str>,
    returnPartialSuccess: Option<bool>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/regions/{}/nodeTemplates",
        project, region,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "filter",
            filter
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "maxResults",
            maxResults
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "orderBy",
            orderBy
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageToken",
            pageToken
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "returnPartialSuccess",
            returnPartialSuccess
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET projects/{project}/regions/{region}/nodeTemplates
/// Retrieves a list of node templates available to the specified project.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_node_templates_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_node_templates_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_node_templates_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<NodeTemplateList>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: NodeTemplateList = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/regions/{region}/nodeTemplates
/// Retrieves a list of node templates available to the specified project.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_node_templates_list_builder()` + `compute_node_templates_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_node_templates_list(
    client: &SimpleHttpClient,
    project: &str,
    region: &str,
    filter: Option<&str>,
    maxResults: Option<i32>,
    orderBy: Option<&str>,
    pageToken: Option<&str>,
    returnPartialSuccess: Option<bool>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<NodeTemplateList>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = compute_node_templates_list_builder(
        client,
        project,
        region,
        filter,
        maxResults,
        orderBy,
        pageToken,
        returnPartialSuccess,
    )?;
    compute_node_templates_list_execute(builder)
}

/// GET projects/{project}/regions/{region}/nodeTemplates/{resource}/setIamPolicy
/// Sets the access control policy on the specified resource. Replaces any existing policy.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_node_templates_set_iam_policy_execute()` to send, or `compute_node_templates_set_iam_policy` for simplest API.

pub fn compute_node_templates_set_iam_policy_builder(
    client: &SimpleHttpClient,
    project: &str,
    region: &str,
    resource: &str,
    body: &RegionSetPolicyRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/regions/{}/nodeTemplates/{}/setIamPolicy",
        project, region, resource,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET projects/{project}/regions/{region}/nodeTemplates/{resource}/setIamPolicy
/// Sets the access control policy on the specified resource. Replaces any existing policy.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_node_templates_set_iam_policy_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_node_templates_set_iam_policy_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_node_templates_set_iam_policy_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Policy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Policy = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/regions/{region}/nodeTemplates/{resource}/setIamPolicy
/// Sets the access control policy on the specified resource. Replaces any existing policy.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_node_templates_set_iam_policy_builder()` + `compute_node_templates_set_iam_policy_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_node_templates_set_iam_policy(
    client: &SimpleHttpClient,
    project: &str,
    region: &str,
    resource: &str,
    body: &RegionSetPolicyRequest,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Policy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        compute_node_templates_set_iam_policy_builder(client, project, region, resource, body)?;
    compute_node_templates_set_iam_policy_execute(builder)
}

/// GET projects/{project}/regions/{region}/nodeTemplates/{resource}/testIamPermissions
/// Returns permissions that a caller has on the specified resource.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_node_templates_test_iam_permissions_execute()` to send, or `compute_node_templates_test_iam_permissions` for simplest API.

pub fn compute_node_templates_test_iam_permissions_builder(
    client: &SimpleHttpClient,
    project: &str,
    region: &str,
    resource: &str,
    body: &TestPermissionsRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/regions/{}/nodeTemplates/{}/testIamPermissions",
        project, region, resource,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET projects/{project}/regions/{region}/nodeTemplates/{resource}/testIamPermissions
/// Returns permissions that a caller has on the specified resource.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_node_templates_test_iam_permissions_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_node_templates_test_iam_permissions_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_node_templates_test_iam_permissions_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<TestPermissionsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: TestPermissionsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/regions/{region}/nodeTemplates/{resource}/testIamPermissions
/// Returns permissions that a caller has on the specified resource.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_node_templates_test_iam_permissions_builder()` + `compute_node_templates_test_iam_permissions_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_node_templates_test_iam_permissions(
    client: &SimpleHttpClient,
    project: &str,
    region: &str,
    resource: &str,
    body: &TestPermissionsRequest,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<TestPermissionsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = compute_node_templates_test_iam_permissions_builder(
        client, project, region, resource, body,
    )?;
    compute_node_templates_test_iam_permissions_execute(builder)
}

/// GET projects/{project}/aggregated/nodeTypes
/// Retrieves an aggregated list of node types.  To prevent failure, Google recommends that you set the `returnPartialSuccess` parameter to `true`.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_node_types_aggregated_list_execute()` to send, or `compute_node_types_aggregated_list` for simplest API.

pub fn compute_node_types_aggregated_list_builder(
    client: &SimpleHttpClient,
    project: &str,
    filter: Option<&str>,
    includeAllScopes: Option<bool>,
    maxResults: Option<i32>,
    orderBy: Option<&str>,
    pageToken: Option<&str>,
    returnPartialSuccess: Option<bool>,
    serviceProjectNumber: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/aggregated/nodeTypes",
        project,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "filter",
            filter
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "includeAllScopes",
            includeAllScopes
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "maxResults",
            maxResults
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "orderBy",
            orderBy
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageToken",
            pageToken
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "returnPartialSuccess",
            returnPartialSuccess
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "serviceProjectNumber",
            serviceProjectNumber
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET projects/{project}/aggregated/nodeTypes
/// Retrieves an aggregated list of node types.  To prevent failure, Google recommends that you set the `returnPartialSuccess` parameter to `true`.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_node_types_aggregated_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_node_types_aggregated_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_node_types_aggregated_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<NodeTypeAggregatedList>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: NodeTypeAggregatedList = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/aggregated/nodeTypes
/// Retrieves an aggregated list of node types.  To prevent failure, Google recommends that you set the `returnPartialSuccess` parameter to `true`.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_node_types_aggregated_list_builder()` + `compute_node_types_aggregated_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_node_types_aggregated_list(
    client: &SimpleHttpClient,
    project: &str,
    filter: Option<&str>,
    includeAllScopes: Option<bool>,
    maxResults: Option<i32>,
    orderBy: Option<&str>,
    pageToken: Option<&str>,
    returnPartialSuccess: Option<bool>,
    serviceProjectNumber: Option<&str>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<NodeTypeAggregatedList>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = compute_node_types_aggregated_list_builder(
        client,
        project,
        filter,
        includeAllScopes,
        maxResults,
        orderBy,
        pageToken,
        returnPartialSuccess,
        serviceProjectNumber,
    )?;
    compute_node_types_aggregated_list_execute(builder)
}

/// GET projects/{project}/zones/{zone}/nodeTypes/{nodeType}
/// Returns the specified node type.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_node_types_get_execute()` to send, or `compute_node_types_get` for simplest API.

pub fn compute_node_types_get_builder(
    client: &SimpleHttpClient,
    nodeType: &str,
    project: &str,
    zone: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/zones/{}/nodeTypes/{}",
        nodeType, project, zone,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/zones/{zone}/nodeTypes/{nodeType}
/// Returns the specified node type.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_node_types_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_node_types_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_node_types_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<NodeType>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: NodeType = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/zones/{zone}/nodeTypes/{nodeType}
/// Returns the specified node type.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_node_types_get_builder()` + `compute_node_types_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_node_types_get(
    client: &SimpleHttpClient,
    nodeType: &str,
    project: &str,
    zone: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<NodeType>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_node_types_get_builder(client, nodeType, project, zone)?;
    compute_node_types_get_execute(builder)
}

/// GET projects/{project}/zones/{zone}/nodeTypes
/// Retrieves a list of node types available to the specified project.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_node_types_list_execute()` to send, or `compute_node_types_list` for simplest API.

pub fn compute_node_types_list_builder(
    client: &SimpleHttpClient,
    project: &str,
    zone: &str,
    filter: Option<&str>,
    maxResults: Option<i32>,
    orderBy: Option<&str>,
    pageToken: Option<&str>,
    returnPartialSuccess: Option<bool>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/zones/{}/nodeTypes",
        project, zone,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "filter",
            filter
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "maxResults",
            maxResults
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "orderBy",
            orderBy
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageToken",
            pageToken
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "returnPartialSuccess",
            returnPartialSuccess
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET projects/{project}/zones/{zone}/nodeTypes
/// Retrieves a list of node types available to the specified project.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_node_types_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_node_types_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_node_types_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<NodeTypeList>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: NodeTypeList = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/zones/{zone}/nodeTypes
/// Retrieves a list of node types available to the specified project.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_node_types_list_builder()` + `compute_node_types_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_node_types_list(
    client: &SimpleHttpClient,
    project: &str,
    zone: &str,
    filter: Option<&str>,
    maxResults: Option<i32>,
    orderBy: Option<&str>,
    pageToken: Option<&str>,
    returnPartialSuccess: Option<bool>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<NodeTypeList>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = compute_node_types_list_builder(
        client,
        project,
        zone,
        filter,
        maxResults,
        orderBy,
        pageToken,
        returnPartialSuccess,
    )?;
    compute_node_types_list_execute(builder)
}

/// GET locations/global/securityPolicies/{securityPolicy}/addAssociation
/// Inserts an association for the specified security policy.  This has billing implications.  Projects in the hierarchy with effective hierarchical security policies will be automatically enrolled into Cloud Armor Enterprise if not already enrolled.  Use this API to modify Cloud Armor policies. Previously, alpha and beta versions of this API were used to modify firewall policies. This usage is now disabled for most organizations. Use `firewallPolicies`.`addAssociation` instead.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_organization_security_policies_add_association_execute()` to send, or `compute_organization_security_policies_add_association` for simplest API.

pub fn compute_organization_security_policies_add_association_builder(
    client: &SimpleHttpClient,
    securityPolicy: &str,
    replaceExistingAssociation: Option<bool>,
    requestId: Option<&str>,
    body: &SecurityPolicyAssociation,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comlocations/global/securityPolicies/{}/addAssociation",
        securityPolicy,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "replaceExistingAssociation",
            replaceExistingAssociation
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "requestId",
            requestId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET locations/global/securityPolicies/{securityPolicy}/addAssociation
/// Inserts an association for the specified security policy.  This has billing implications.  Projects in the hierarchy with effective hierarchical security policies will be automatically enrolled into Cloud Armor Enterprise if not already enrolled.  Use this API to modify Cloud Armor policies. Previously, alpha and beta versions of this API were used to modify firewall policies. This usage is now disabled for most organizations. Use `firewallPolicies`.`addAssociation` instead.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_organization_security_policies_add_association_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_organization_security_policies_add_association_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_organization_security_policies_add_association_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET locations/global/securityPolicies/{securityPolicy}/addAssociation
/// Inserts an association for the specified security policy.  This has billing implications.  Projects in the hierarchy with effective hierarchical security policies will be automatically enrolled into Cloud Armor Enterprise if not already enrolled.  Use this API to modify Cloud Armor policies. Previously, alpha and beta versions of this API were used to modify firewall policies. This usage is now disabled for most organizations. Use `firewallPolicies`.`addAssociation` instead.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_organization_security_policies_add_association_builder()` + `compute_organization_security_policies_add_association_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_organization_security_policies_add_association(
    client: &SimpleHttpClient,
    securityPolicy: &str,
    replaceExistingAssociation: Option<bool>,
    requestId: Option<&str>,
    body: &SecurityPolicyAssociation,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_organization_security_policies_add_association_builder(
        client,
        securityPolicy,
        replaceExistingAssociation,
        requestId,
        body,
    )?;
    compute_organization_security_policies_add_association_execute(builder)
}

/// GET locations/global/securityPolicies/{securityPolicy}/addRule
/// Inserts a rule into a security policy.  Use this API to modify Cloud Armor policies. Previously, alpha and beta versions of this API were used to modify firewall policies. This usage is now disabled for most organizations. Use `firewallPolicies`.`addRule` instead.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_organization_security_policies_add_rule_execute()` to send, or `compute_organization_security_policies_add_rule` for simplest API.

pub fn compute_organization_security_policies_add_rule_builder(
    client: &SimpleHttpClient,
    securityPolicy: &str,
    requestId: Option<&str>,
    body: &SecurityPolicyRule,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comlocations/global/securityPolicies/{}/addRule",
        securityPolicy,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "requestId",
            requestId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET locations/global/securityPolicies/{securityPolicy}/addRule
/// Inserts a rule into a security policy.  Use this API to modify Cloud Armor policies. Previously, alpha and beta versions of this API were used to modify firewall policies. This usage is now disabled for most organizations. Use `firewallPolicies`.`addRule` instead.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_organization_security_policies_add_rule_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_organization_security_policies_add_rule_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_organization_security_policies_add_rule_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET locations/global/securityPolicies/{securityPolicy}/addRule
/// Inserts a rule into a security policy.  Use this API to modify Cloud Armor policies. Previously, alpha and beta versions of this API were used to modify firewall policies. This usage is now disabled for most organizations. Use `firewallPolicies`.`addRule` instead.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_organization_security_policies_add_rule_builder()` + `compute_organization_security_policies_add_rule_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_organization_security_policies_add_rule(
    client: &SimpleHttpClient,
    securityPolicy: &str,
    requestId: Option<&str>,
    body: &SecurityPolicyRule,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_organization_security_policies_add_rule_builder(
        client,
        securityPolicy,
        requestId,
        body,
    )?;
    compute_organization_security_policies_add_rule_execute(builder)
}

/// GET locations/global/securityPolicies/{securityPolicy}/copyRules
/// Copies rules to the specified security policy.  Use this API to modify Cloud Armor policies. Previously, alpha and beta versions of this API were used to modify firewall policies. This usage is now disabled for most organizations. Use `firewallPolicies`.`cloneRules` instead.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_organization_security_policies_copy_rules_execute()` to send, or `compute_organization_security_policies_copy_rules` for simplest API.

pub fn compute_organization_security_policies_copy_rules_builder(
    client: &SimpleHttpClient,
    securityPolicy: &str,
    requestId: Option<&str>,
    sourceSecurityPolicy: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comlocations/global/securityPolicies/{}/copyRules",
        securityPolicy,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "requestId",
            requestId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "sourceSecurityPolicy",
            sourceSecurityPolicy
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET locations/global/securityPolicies/{securityPolicy}/copyRules
/// Copies rules to the specified security policy.  Use this API to modify Cloud Armor policies. Previously, alpha and beta versions of this API were used to modify firewall policies. This usage is now disabled for most organizations. Use `firewallPolicies`.`cloneRules` instead.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_organization_security_policies_copy_rules_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_organization_security_policies_copy_rules_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_organization_security_policies_copy_rules_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET locations/global/securityPolicies/{securityPolicy}/copyRules
/// Copies rules to the specified security policy.  Use this API to modify Cloud Armor policies. Previously, alpha and beta versions of this API were used to modify firewall policies. This usage is now disabled for most organizations. Use `firewallPolicies`.`cloneRules` instead.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_organization_security_policies_copy_rules_builder()` + `compute_organization_security_policies_copy_rules_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_organization_security_policies_copy_rules(
    client: &SimpleHttpClient,
    securityPolicy: &str,
    requestId: Option<&str>,
    sourceSecurityPolicy: Option<&str>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_organization_security_policies_copy_rules_builder(
        client,
        securityPolicy,
        requestId,
        sourceSecurityPolicy,
    )?;
    compute_organization_security_policies_copy_rules_execute(builder)
}

/// GET locations/global/securityPolicies/{securityPolicy}
/// Deletes the specified policy.  Use this API to remove Cloud Armor policies. Previously, alpha and beta versions of this API were used to remove firewall policies. This usage is now disabled for most organizations. Use `firewallPolicies`.delete instead.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_organization_security_policies_delete_execute()` to send, or `compute_organization_security_policies_delete` for simplest API.

pub fn compute_organization_security_policies_delete_builder(
    client: &SimpleHttpClient,
    securityPolicy: &str,
    requestId: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comlocations/global/securityPolicies/{}",
        securityPolicy,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "requestId",
            requestId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET locations/global/securityPolicies/{securityPolicy}
/// Deletes the specified policy.  Use this API to remove Cloud Armor policies. Previously, alpha and beta versions of this API were used to remove firewall policies. This usage is now disabled for most organizations. Use `firewallPolicies`.delete instead.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_organization_security_policies_delete_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_organization_security_policies_delete_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_organization_security_policies_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET locations/global/securityPolicies/{securityPolicy}
/// Deletes the specified policy.  Use this API to remove Cloud Armor policies. Previously, alpha and beta versions of this API were used to remove firewall policies. This usage is now disabled for most organizations. Use `firewallPolicies`.delete instead.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_organization_security_policies_delete_builder()` + `compute_organization_security_policies_delete_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_organization_security_policies_delete(
    client: &SimpleHttpClient,
    securityPolicy: &str,
    requestId: Option<&str>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        compute_organization_security_policies_delete_builder(client, securityPolicy, requestId)?;
    compute_organization_security_policies_delete_execute(builder)
}

/// GET locations/global/securityPolicies/{securityPolicy}
/// List all of the ordered rules present in a single specified policy.  Use this API to read Cloud Armor policies. Previously, alpha and beta versions of this API were used to read firewall policies. This usage is now disabled for most organizations. Use `firewallPolicies`.get instead.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_organization_security_policies_get_execute()` to send, or `compute_organization_security_policies_get` for simplest API.

pub fn compute_organization_security_policies_get_builder(
    client: &SimpleHttpClient,
    securityPolicy: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comlocations/global/securityPolicies/{}",
        securityPolicy,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET locations/global/securityPolicies/{securityPolicy}
/// List all of the ordered rules present in a single specified policy.  Use this API to read Cloud Armor policies. Previously, alpha and beta versions of this API were used to read firewall policies. This usage is now disabled for most organizations. Use `firewallPolicies`.get instead.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_organization_security_policies_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_organization_security_policies_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_organization_security_policies_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SecurityPolicy>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: SecurityPolicy = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET locations/global/securityPolicies/{securityPolicy}
/// List all of the ordered rules present in a single specified policy.  Use this API to read Cloud Armor policies. Previously, alpha and beta versions of this API were used to read firewall policies. This usage is now disabled for most organizations. Use `firewallPolicies`.get instead.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_organization_security_policies_get_builder()` + `compute_organization_security_policies_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_organization_security_policies_get(
    client: &SimpleHttpClient,
    securityPolicy: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SecurityPolicy>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = compute_organization_security_policies_get_builder(client, securityPolicy)?;
    compute_organization_security_policies_get_execute(builder)
}

/// GET locations/global/securityPolicies/{securityPolicy}/getAssociation
/// Gets an association with the specified name.  Use this API to read Cloud Armor policies. Previously, alpha and beta versions of this API were used to read firewall policies. This usage is now disabled for most organizations. Use `firewallPolicies`.`getAssociation` instead.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_organization_security_policies_get_association_execute()` to send, or `compute_organization_security_policies_get_association` for simplest API.

pub fn compute_organization_security_policies_get_association_builder(
    client: &SimpleHttpClient,
    securityPolicy: &str,
    name: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comlocations/global/securityPolicies/{}/getAssociation",
        securityPolicy,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "name",
            name.as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET locations/global/securityPolicies/{securityPolicy}/getAssociation
/// Gets an association with the specified name.  Use this API to read Cloud Armor policies. Previously, alpha and beta versions of this API were used to read firewall policies. This usage is now disabled for most organizations. Use `firewallPolicies`.`getAssociation` instead.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_organization_security_policies_get_association_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_organization_security_policies_get_association_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_organization_security_policies_get_association_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SecurityPolicyAssociation>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: SecurityPolicyAssociation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET locations/global/securityPolicies/{securityPolicy}/getAssociation
/// Gets an association with the specified name.  Use this API to read Cloud Armor policies. Previously, alpha and beta versions of this API were used to read firewall policies. This usage is now disabled for most organizations. Use `firewallPolicies`.`getAssociation` instead.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_organization_security_policies_get_association_builder()` + `compute_organization_security_policies_get_association_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_organization_security_policies_get_association(
    client: &SimpleHttpClient,
    securityPolicy: &str,
    name: Option<&str>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SecurityPolicyAssociation>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = compute_organization_security_policies_get_association_builder(
        client,
        securityPolicy,
        name,
    )?;
    compute_organization_security_policies_get_association_execute(builder)
}

/// GET locations/global/securityPolicies/{securityPolicy}/getRule
/// Gets a rule at the specified priority.  Use this API to read Cloud Armor policies. Previously, alpha and beta versions of this API were used to read firewall policies. This usage is now disabled for most organizations. Use `firewallPolicies`.`getRule` instead.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_organization_security_policies_get_rule_execute()` to send, or `compute_organization_security_policies_get_rule` for simplest API.

pub fn compute_organization_security_policies_get_rule_builder(
    client: &SimpleHttpClient,
    securityPolicy: &str,
    priority: Option<i32>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comlocations/global/securityPolicies/{}/getRule",
        securityPolicy,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "priority",
            priority
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET locations/global/securityPolicies/{securityPolicy}/getRule
/// Gets a rule at the specified priority.  Use this API to read Cloud Armor policies. Previously, alpha and beta versions of this API were used to read firewall policies. This usage is now disabled for most organizations. Use `firewallPolicies`.`getRule` instead.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_organization_security_policies_get_rule_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_organization_security_policies_get_rule_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_organization_security_policies_get_rule_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SecurityPolicyRule>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: SecurityPolicyRule = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET locations/global/securityPolicies/{securityPolicy}/getRule
/// Gets a rule at the specified priority.  Use this API to read Cloud Armor policies. Previously, alpha and beta versions of this API were used to read firewall policies. This usage is now disabled for most organizations. Use `firewallPolicies`.`getRule` instead.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_organization_security_policies_get_rule_builder()` + `compute_organization_security_policies_get_rule_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_organization_security_policies_get_rule(
    client: &SimpleHttpClient,
    securityPolicy: &str,
    priority: Option<i32>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SecurityPolicyRule>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        compute_organization_security_policies_get_rule_builder(client, securityPolicy, priority)?;
    compute_organization_security_policies_get_rule_execute(builder)
}

/// GET locations/global/securityPolicies
/// Creates a new policy in the specified organization using the data included in the request.  Use this API to add Cloud Armor policies. Previously, alpha and beta versions of this API were used to add firewall policies. This usage is now disabled for most organizations. Use `firewallPolicies`.insert instead.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_organization_security_policies_insert_execute()` to send, or `compute_organization_security_policies_insert` for simplest API.

pub fn compute_organization_security_policies_insert_builder(
    client: &SimpleHttpClient,
    parentId: Option<&str>,
    requestId: Option<&str>,
    body: &SecurityPolicy,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comlocations/global/securityPolicies",);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "parentId",
            parentId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "requestId",
            requestId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET locations/global/securityPolicies
/// Creates a new policy in the specified organization using the data included in the request.  Use this API to add Cloud Armor policies. Previously, alpha and beta versions of this API were used to add firewall policies. This usage is now disabled for most organizations. Use `firewallPolicies`.insert instead.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_organization_security_policies_insert_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_organization_security_policies_insert_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_organization_security_policies_insert_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET locations/global/securityPolicies
/// Creates a new policy in the specified organization using the data included in the request.  Use this API to add Cloud Armor policies. Previously, alpha and beta versions of this API were used to add firewall policies. This usage is now disabled for most organizations. Use `firewallPolicies`.insert instead.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_organization_security_policies_insert_builder()` + `compute_organization_security_policies_insert_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_organization_security_policies_insert(
    client: &SimpleHttpClient,
    parentId: Option<&str>,
    requestId: Option<&str>,
    body: &SecurityPolicy,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        compute_organization_security_policies_insert_builder(client, parentId, requestId, body)?;
    compute_organization_security_policies_insert_execute(builder)
}

/// GET locations/global/securityPolicies
/// List all the policies that have been configured for the specified organization.  Use this API to read Cloud Armor policies. Previously, alpha and beta versions of this API were used to read firewall policies. This usage is now disabled for most organizations. Use `firewallPolicies`.list instead.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_organization_security_policies_list_execute()` to send, or `compute_organization_security_policies_list` for simplest API.

pub fn compute_organization_security_policies_list_builder(
    client: &SimpleHttpClient,
    filter: Option<&str>,
    maxResults: Option<i32>,
    orderBy: Option<&str>,
    pageToken: Option<&str>,
    parentId: Option<&str>,
    returnPartialSuccess: Option<bool>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comlocations/global/securityPolicies",);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "filter",
            filter
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "maxResults",
            maxResults
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "orderBy",
            orderBy
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageToken",
            pageToken
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "parentId",
            parentId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "returnPartialSuccess",
            returnPartialSuccess
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET locations/global/securityPolicies
/// List all the policies that have been configured for the specified organization.  Use this API to read Cloud Armor policies. Previously, alpha and beta versions of this API were used to read firewall policies. This usage is now disabled for most organizations. Use `firewallPolicies`.list instead.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_organization_security_policies_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_organization_security_policies_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_organization_security_policies_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SecurityPolicyList>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: SecurityPolicyList = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET locations/global/securityPolicies
/// List all the policies that have been configured for the specified organization.  Use this API to read Cloud Armor policies. Previously, alpha and beta versions of this API were used to read firewall policies. This usage is now disabled for most organizations. Use `firewallPolicies`.list instead.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_organization_security_policies_list_builder()` + `compute_organization_security_policies_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_organization_security_policies_list(
    client: &SimpleHttpClient,
    filter: Option<&str>,
    maxResults: Option<i32>,
    orderBy: Option<&str>,
    pageToken: Option<&str>,
    parentId: Option<&str>,
    returnPartialSuccess: Option<bool>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SecurityPolicyList>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = compute_organization_security_policies_list_builder(
        client,
        filter,
        maxResults,
        orderBy,
        pageToken,
        parentId,
        returnPartialSuccess,
    )?;
    compute_organization_security_policies_list_execute(builder)
}

/// GET locations/global/securityPolicies/listAssociations
/// Lists associations of a specified target, i.e., organization or folder.  Use this API to read Cloud Armor policies. Previously, alpha and beta versions of this API were used to read firewall policies. This usage is now disabled for most organizations. Use `firewallPolicies`.`listAssociations` instead.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_organization_security_policies_list_associations_execute()` to send, or `compute_organization_security_policies_list_associations` for simplest API.

pub fn compute_organization_security_policies_list_associations_builder(
    client: &SimpleHttpClient,
    targetResource: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comlocations/global/securityPolicies/listAssociations",);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "targetResource",
            targetResource
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET locations/global/securityPolicies/listAssociations
/// Lists associations of a specified target, i.e., organization or folder.  Use this API to read Cloud Armor policies. Previously, alpha and beta versions of this API were used to read firewall policies. This usage is now disabled for most organizations. Use `firewallPolicies`.`listAssociations` instead.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_organization_security_policies_list_associations_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_organization_security_policies_list_associations_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_organization_security_policies_list_associations_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<OrganizationSecurityPoliciesListAssociationsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: OrganizationSecurityPoliciesListAssociationsResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET locations/global/securityPolicies/listAssociations
/// Lists associations of a specified target, i.e., organization or folder.  Use this API to read Cloud Armor policies. Previously, alpha and beta versions of this API were used to read firewall policies. This usage is now disabled for most organizations. Use `firewallPolicies`.`listAssociations` instead.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_organization_security_policies_list_associations_builder()` + `compute_organization_security_policies_list_associations_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_organization_security_policies_list_associations(
    client: &SimpleHttpClient,
    targetResource: Option<&str>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<OrganizationSecurityPoliciesListAssociationsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        compute_organization_security_policies_list_associations_builder(client, targetResource)?;
    compute_organization_security_policies_list_associations_execute(builder)
}

/// GET locations/global/securityPolicies/listPreconfiguredExpressionSets
/// Gets the current list of preconfigured Web Application Firewall (WAF) expressions.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_organization_security_policies_list_preconfigured_expression_sets_execute()` to send, or `compute_organization_security_policies_list_preconfigured_expression_sets` for simplest API.

pub fn compute_organization_security_policies_list_preconfigured_expression_sets_builder(
    client: &SimpleHttpClient,
    filter: Option<&str>,
    maxResults: Option<i32>,
    orderBy: Option<&str>,
    pageToken: Option<&str>,
    parentId: Option<&str>,
    returnPartialSuccess: Option<bool>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comlocations/global/securityPolicies/listPreconfiguredExpressionSets",
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "filter",
            filter
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "maxResults",
            maxResults
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "orderBy",
            orderBy
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageToken",
            pageToken
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "parentId",
            parentId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "returnPartialSuccess",
            returnPartialSuccess
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET locations/global/securityPolicies/listPreconfiguredExpressionSets
/// Gets the current list of preconfigured Web Application Firewall (WAF) expressions.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_organization_security_policies_list_preconfigured_expression_sets_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_organization_security_policies_list_preconfigured_expression_sets_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_organization_security_policies_list_preconfigured_expression_sets_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<SecurityPoliciesListPreconfiguredExpressionSetsResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: SecurityPoliciesListPreconfiguredExpressionSetsResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET locations/global/securityPolicies/listPreconfiguredExpressionSets
/// Gets the current list of preconfigured Web Application Firewall (WAF) expressions.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_organization_security_policies_list_preconfigured_expression_sets_builder()` + `compute_organization_security_policies_list_preconfigured_expression_sets_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_organization_security_policies_list_preconfigured_expression_sets(
    client: &SimpleHttpClient,
    filter: Option<&str>,
    maxResults: Option<i32>,
    orderBy: Option<&str>,
    pageToken: Option<&str>,
    parentId: Option<&str>,
    returnPartialSuccess: Option<bool>,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<SecurityPoliciesListPreconfiguredExpressionSetsResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        compute_organization_security_policies_list_preconfigured_expression_sets_builder(
            client,
            filter,
            maxResults,
            orderBy,
            pageToken,
            parentId,
            returnPartialSuccess,
        )?;
    compute_organization_security_policies_list_preconfigured_expression_sets_execute(builder)
}

/// GET locations/global/securityPolicies/{securityPolicy}/move
/// Moves the specified security policy.  Use this API to modify Cloud Armor policies. Previously, alpha and beta versions of this API were used to modify firewall policies. This usage is now disabled for most organizations. Use `firewallPolicies`.move instead.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_organization_security_policies_move_execute()` to send, or `compute_organization_security_policies_move` for simplest API.

pub fn compute_organization_security_policies_move_builder(
    client: &SimpleHttpClient,
    securityPolicy: &str,
    parentId: Option<&str>,
    requestId: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comlocations/global/securityPolicies/{}/move",
        securityPolicy,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "parentId",
            parentId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "requestId",
            requestId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET locations/global/securityPolicies/{securityPolicy}/move
/// Moves the specified security policy.  Use this API to modify Cloud Armor policies. Previously, alpha and beta versions of this API were used to modify firewall policies. This usage is now disabled for most organizations. Use `firewallPolicies`.move instead.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_organization_security_policies_move_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_organization_security_policies_move_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_organization_security_policies_move_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET locations/global/securityPolicies/{securityPolicy}/move
/// Moves the specified security policy.  Use this API to modify Cloud Armor policies. Previously, alpha and beta versions of this API were used to modify firewall policies. This usage is now disabled for most organizations. Use `firewallPolicies`.move instead.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_organization_security_policies_move_builder()` + `compute_organization_security_policies_move_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_organization_security_policies_move(
    client: &SimpleHttpClient,
    securityPolicy: &str,
    parentId: Option<&str>,
    requestId: Option<&str>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_organization_security_policies_move_builder(
        client,
        securityPolicy,
        parentId,
        requestId,
    )?;
    compute_organization_security_policies_move_execute(builder)
}

/// GET locations/global/securityPolicies/{securityPolicy}
/// Patches the specified policy with the data included in the request.  Use this API to modify Cloud Armor policies. Previously, alpha and beta versions of this API were used to modify firewall policies. This usage is now disabled for most organizations. Use `firewallPolicies`.patch instead.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_organization_security_policies_patch_execute()` to send, or `compute_organization_security_policies_patch` for simplest API.

pub fn compute_organization_security_policies_patch_builder(
    client: &SimpleHttpClient,
    securityPolicy: &str,
    requestId: Option<&str>,
    body: &SecurityPolicy,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comlocations/global/securityPolicies/{}",
        securityPolicy,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "requestId",
            requestId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET locations/global/securityPolicies/{securityPolicy}
/// Patches the specified policy with the data included in the request.  Use this API to modify Cloud Armor policies. Previously, alpha and beta versions of this API were used to modify firewall policies. This usage is now disabled for most organizations. Use `firewallPolicies`.patch instead.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_organization_security_policies_patch_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_organization_security_policies_patch_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_organization_security_policies_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET locations/global/securityPolicies/{securityPolicy}
/// Patches the specified policy with the data included in the request.  Use this API to modify Cloud Armor policies. Previously, alpha and beta versions of this API were used to modify firewall policies. This usage is now disabled for most organizations. Use `firewallPolicies`.patch instead.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_organization_security_policies_patch_builder()` + `compute_organization_security_policies_patch_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_organization_security_policies_patch(
    client: &SimpleHttpClient,
    securityPolicy: &str,
    requestId: Option<&str>,
    body: &SecurityPolicy,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_organization_security_policies_patch_builder(
        client,
        securityPolicy,
        requestId,
        body,
    )?;
    compute_organization_security_policies_patch_execute(builder)
}

/// GET locations/global/securityPolicies/{securityPolicy}/patchRule
/// Patches a rule at the specified priority.  Use this API to modify Cloud Armor policies. Previously, alpha and beta versions of this API were used to modify firewall policies. This usage is now disabled for most organizations. Use `firewallPolicies`.`patchRule` instead.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_organization_security_policies_patch_rule_execute()` to send, or `compute_organization_security_policies_patch_rule` for simplest API.

pub fn compute_organization_security_policies_patch_rule_builder(
    client: &SimpleHttpClient,
    securityPolicy: &str,
    priority: Option<i32>,
    requestId: Option<&str>,
    body: &SecurityPolicyRule,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comlocations/global/securityPolicies/{}/patchRule",
        securityPolicy,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "priority",
            priority
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "requestId",
            requestId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET locations/global/securityPolicies/{securityPolicy}/patchRule
/// Patches a rule at the specified priority.  Use this API to modify Cloud Armor policies. Previously, alpha and beta versions of this API were used to modify firewall policies. This usage is now disabled for most organizations. Use `firewallPolicies`.`patchRule` instead.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_organization_security_policies_patch_rule_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_organization_security_policies_patch_rule_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_organization_security_policies_patch_rule_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET locations/global/securityPolicies/{securityPolicy}/patchRule
/// Patches a rule at the specified priority.  Use this API to modify Cloud Armor policies. Previously, alpha and beta versions of this API were used to modify firewall policies. This usage is now disabled for most organizations. Use `firewallPolicies`.`patchRule` instead.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_organization_security_policies_patch_rule_builder()` + `compute_organization_security_policies_patch_rule_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_organization_security_policies_patch_rule(
    client: &SimpleHttpClient,
    securityPolicy: &str,
    priority: Option<i32>,
    requestId: Option<&str>,
    body: &SecurityPolicyRule,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_organization_security_policies_patch_rule_builder(
        client,
        securityPolicy,
        priority,
        requestId,
        body,
    )?;
    compute_organization_security_policies_patch_rule_execute(builder)
}

/// GET locations/global/securityPolicies/{securityPolicy}/removeAssociation
/// Removes an association for the specified security policy.  Use this API to modify Cloud Armor policies. Previously, alpha and beta versions of this API were used to modify firewall policies. This usage is now disabled for most organizations. Use `firewallPolicies`.`removeAssociation` instead.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_organization_security_policies_remove_association_execute()` to send, or `compute_organization_security_policies_remove_association` for simplest API.

pub fn compute_organization_security_policies_remove_association_builder(
    client: &SimpleHttpClient,
    securityPolicy: &str,
    name: Option<&str>,
    requestId: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comlocations/global/securityPolicies/{}/removeAssociation",
        securityPolicy,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "name",
            name.as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "requestId",
            requestId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET locations/global/securityPolicies/{securityPolicy}/removeAssociation
/// Removes an association for the specified security policy.  Use this API to modify Cloud Armor policies. Previously, alpha and beta versions of this API were used to modify firewall policies. This usage is now disabled for most organizations. Use `firewallPolicies`.`removeAssociation` instead.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_organization_security_policies_remove_association_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_organization_security_policies_remove_association_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_organization_security_policies_remove_association_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET locations/global/securityPolicies/{securityPolicy}/removeAssociation
/// Removes an association for the specified security policy.  Use this API to modify Cloud Armor policies. Previously, alpha and beta versions of this API were used to modify firewall policies. This usage is now disabled for most organizations. Use `firewallPolicies`.`removeAssociation` instead.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_organization_security_policies_remove_association_builder()` + `compute_organization_security_policies_remove_association_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_organization_security_policies_remove_association(
    client: &SimpleHttpClient,
    securityPolicy: &str,
    name: Option<&str>,
    requestId: Option<&str>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_organization_security_policies_remove_association_builder(
        client,
        securityPolicy,
        name,
        requestId,
    )?;
    compute_organization_security_policies_remove_association_execute(builder)
}

/// GET locations/global/securityPolicies/{securityPolicy}/removeRule
/// Deletes a rule at the specified priority.  Use this API to modify Cloud Armor policies. Previously, alpha and beta versions of this API were used to modify firewall policies. This usage is now disabled for most organizations. Use `firewallPolicies`.`removeRule` instead.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_organization_security_policies_remove_rule_execute()` to send, or `compute_organization_security_policies_remove_rule` for simplest API.

pub fn compute_organization_security_policies_remove_rule_builder(
    client: &SimpleHttpClient,
    securityPolicy: &str,
    priority: Option<i32>,
    requestId: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comlocations/global/securityPolicies/{}/removeRule",
        securityPolicy,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "priority",
            priority
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "requestId",
            requestId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET locations/global/securityPolicies/{securityPolicy}/removeRule
/// Deletes a rule at the specified priority.  Use this API to modify Cloud Armor policies. Previously, alpha and beta versions of this API were used to modify firewall policies. This usage is now disabled for most organizations. Use `firewallPolicies`.`removeRule` instead.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_organization_security_policies_remove_rule_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_organization_security_policies_remove_rule_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_organization_security_policies_remove_rule_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET locations/global/securityPolicies/{securityPolicy}/removeRule
/// Deletes a rule at the specified priority.  Use this API to modify Cloud Armor policies. Previously, alpha and beta versions of this API were used to modify firewall policies. This usage is now disabled for most organizations. Use `firewallPolicies`.`removeRule` instead.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_organization_security_policies_remove_rule_builder()` + `compute_organization_security_policies_remove_rule_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_organization_security_policies_remove_rule(
    client: &SimpleHttpClient,
    securityPolicy: &str,
    priority: Option<i32>,
    requestId: Option<&str>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_organization_security_policies_remove_rule_builder(
        client,
        securityPolicy,
        priority,
        requestId,
    )?;
    compute_organization_security_policies_remove_rule_execute(builder)
}

/// GET projects/{project}/aggregated/packetMirrorings
/// Retrieves an aggregated list of `packetMirrorings`.  To prevent failure, Google recommends that you set the `returnPartialSuccess` parameter to `true`.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_packet_mirrorings_aggregated_list_execute()` to send, or `compute_packet_mirrorings_aggregated_list` for simplest API.

pub fn compute_packet_mirrorings_aggregated_list_builder(
    client: &SimpleHttpClient,
    project: &str,
    filter: Option<&str>,
    includeAllScopes: Option<bool>,
    maxResults: Option<i32>,
    orderBy: Option<&str>,
    pageToken: Option<&str>,
    returnPartialSuccess: Option<bool>,
    serviceProjectNumber: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/aggregated/packetMirrorings",
        project,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "filter",
            filter
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "includeAllScopes",
            includeAllScopes
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "maxResults",
            maxResults
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "orderBy",
            orderBy
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageToken",
            pageToken
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "returnPartialSuccess",
            returnPartialSuccess
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "serviceProjectNumber",
            serviceProjectNumber
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET projects/{project}/aggregated/packetMirrorings
/// Retrieves an aggregated list of `packetMirrorings`.  To prevent failure, Google recommends that you set the `returnPartialSuccess` parameter to `true`.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_packet_mirrorings_aggregated_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_packet_mirrorings_aggregated_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_packet_mirrorings_aggregated_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<PacketMirroringAggregatedList>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: PacketMirroringAggregatedList = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/aggregated/packetMirrorings
/// Retrieves an aggregated list of `packetMirrorings`.  To prevent failure, Google recommends that you set the `returnPartialSuccess` parameter to `true`.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_packet_mirrorings_aggregated_list_builder()` + `compute_packet_mirrorings_aggregated_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_packet_mirrorings_aggregated_list(
    client: &SimpleHttpClient,
    project: &str,
    filter: Option<&str>,
    includeAllScopes: Option<bool>,
    maxResults: Option<i32>,
    orderBy: Option<&str>,
    pageToken: Option<&str>,
    returnPartialSuccess: Option<bool>,
    serviceProjectNumber: Option<&str>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<PacketMirroringAggregatedList>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = compute_packet_mirrorings_aggregated_list_builder(
        client,
        project,
        filter,
        includeAllScopes,
        maxResults,
        orderBy,
        pageToken,
        returnPartialSuccess,
        serviceProjectNumber,
    )?;
    compute_packet_mirrorings_aggregated_list_execute(builder)
}

/// GET projects/{project}/regions/{region}/packetMirrorings/{packetMirroring}
/// Deletes the specified PacketMirroring resource.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_packet_mirrorings_delete_execute()` to send, or `compute_packet_mirrorings_delete` for simplest API.

pub fn compute_packet_mirrorings_delete_builder(
    client: &SimpleHttpClient,
    packetMirroring: &str,
    project: &str,
    region: &str,
    requestId: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/regions/{}/packetMirrorings/{}",
        packetMirroring, project, region,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "requestId",
            requestId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET projects/{project}/regions/{region}/packetMirrorings/{packetMirroring}
/// Deletes the specified PacketMirroring resource.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_packet_mirrorings_delete_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_packet_mirrorings_delete_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_packet_mirrorings_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/regions/{region}/packetMirrorings/{packetMirroring}
/// Deletes the specified PacketMirroring resource.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_packet_mirrorings_delete_builder()` + `compute_packet_mirrorings_delete_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_packet_mirrorings_delete(
    client: &SimpleHttpClient,
    packetMirroring: &str,
    project: &str,
    region: &str,
    requestId: Option<&str>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_packet_mirrorings_delete_builder(
        client,
        packetMirroring,
        project,
        region,
        requestId,
    )?;
    compute_packet_mirrorings_delete_execute(builder)
}

/// GET projects/{project}/regions/{region}/packetMirrorings/{packetMirroring}
/// Returns the specified PacketMirroring resource.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_packet_mirrorings_get_execute()` to send, or `compute_packet_mirrorings_get` for simplest API.

pub fn compute_packet_mirrorings_get_builder(
    client: &SimpleHttpClient,
    packetMirroring: &str,
    project: &str,
    region: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/regions/{}/packetMirrorings/{}",
        packetMirroring, project, region,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/regions/{region}/packetMirrorings/{packetMirroring}
/// Returns the specified PacketMirroring resource.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_packet_mirrorings_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_packet_mirrorings_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_packet_mirrorings_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<PacketMirroring>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: PacketMirroring = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/regions/{region}/packetMirrorings/{packetMirroring}
/// Returns the specified PacketMirroring resource.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_packet_mirrorings_get_builder()` + `compute_packet_mirrorings_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_packet_mirrorings_get(
    client: &SimpleHttpClient,
    packetMirroring: &str,
    project: &str,
    region: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<PacketMirroring>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = compute_packet_mirrorings_get_builder(client, packetMirroring, project, region)?;
    compute_packet_mirrorings_get_execute(builder)
}

/// GET projects/{project}/regions/{region}/packetMirrorings
/// Creates a PacketMirroring resource in the specified project and region using the data included in the request.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_packet_mirrorings_insert_execute()` to send, or `compute_packet_mirrorings_insert` for simplest API.

pub fn compute_packet_mirrorings_insert_builder(
    client: &SimpleHttpClient,
    project: &str,
    region: &str,
    requestId: Option<&str>,
    body: &PacketMirroring,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/regions/{}/packetMirrorings",
        project, region,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "requestId",
            requestId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET projects/{project}/regions/{region}/packetMirrorings
/// Creates a PacketMirroring resource in the specified project and region using the data included in the request.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_packet_mirrorings_insert_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_packet_mirrorings_insert_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_packet_mirrorings_insert_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/regions/{region}/packetMirrorings
/// Creates a PacketMirroring resource in the specified project and region using the data included in the request.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_packet_mirrorings_insert_builder()` + `compute_packet_mirrorings_insert_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_packet_mirrorings_insert(
    client: &SimpleHttpClient,
    project: &str,
    region: &str,
    requestId: Option<&str>,
    body: &PacketMirroring,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        compute_packet_mirrorings_insert_builder(client, project, region, requestId, body)?;
    compute_packet_mirrorings_insert_execute(builder)
}

/// GET projects/{project}/regions/{region}/packetMirrorings
/// Retrieves a list of PacketMirroring resources available to the specified project and region.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_packet_mirrorings_list_execute()` to send, or `compute_packet_mirrorings_list` for simplest API.

pub fn compute_packet_mirrorings_list_builder(
    client: &SimpleHttpClient,
    project: &str,
    region: &str,
    filter: Option<&str>,
    maxResults: Option<i32>,
    orderBy: Option<&str>,
    pageToken: Option<&str>,
    returnPartialSuccess: Option<bool>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/regions/{}/packetMirrorings",
        project, region,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "filter",
            filter
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "maxResults",
            maxResults
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "orderBy",
            orderBy
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageToken",
            pageToken
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "returnPartialSuccess",
            returnPartialSuccess
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET projects/{project}/regions/{region}/packetMirrorings
/// Retrieves a list of PacketMirroring resources available to the specified project and region.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_packet_mirrorings_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_packet_mirrorings_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_packet_mirrorings_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<PacketMirroringList>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: PacketMirroringList = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/regions/{region}/packetMirrorings
/// Retrieves a list of PacketMirroring resources available to the specified project and region.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_packet_mirrorings_list_builder()` + `compute_packet_mirrorings_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_packet_mirrorings_list(
    client: &SimpleHttpClient,
    project: &str,
    region: &str,
    filter: Option<&str>,
    maxResults: Option<i32>,
    orderBy: Option<&str>,
    pageToken: Option<&str>,
    returnPartialSuccess: Option<bool>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<PacketMirroringList>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = compute_packet_mirrorings_list_builder(
        client,
        project,
        region,
        filter,
        maxResults,
        orderBy,
        pageToken,
        returnPartialSuccess,
    )?;
    compute_packet_mirrorings_list_execute(builder)
}

/// GET projects/{project}/regions/{region}/packetMirrorings/{packetMirroring}
/// Patches the specified PacketMirroring resource with the data included in the request. This method `supportsPATCH` semantics and `usesJSON` merge patch format and processing rules.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_packet_mirrorings_patch_execute()` to send, or `compute_packet_mirrorings_patch` for simplest API.

pub fn compute_packet_mirrorings_patch_builder(
    client: &SimpleHttpClient,
    packetMirroring: &str,
    project: &str,
    region: &str,
    requestId: Option<&str>,
    body: &PacketMirroring,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/regions/{}/packetMirrorings/{}",
        packetMirroring, project, region,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "requestId",
            requestId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET projects/{project}/regions/{region}/packetMirrorings/{packetMirroring}
/// Patches the specified PacketMirroring resource with the data included in the request. This method `supportsPATCH` semantics and `usesJSON` merge patch format and processing rules.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_packet_mirrorings_patch_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_packet_mirrorings_patch_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_packet_mirrorings_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/regions/{region}/packetMirrorings/{packetMirroring}
/// Patches the specified PacketMirroring resource with the data included in the request. This method `supportsPATCH` semantics and `usesJSON` merge patch format and processing rules.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_packet_mirrorings_patch_builder()` + `compute_packet_mirrorings_patch_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_packet_mirrorings_patch(
    client: &SimpleHttpClient,
    packetMirroring: &str,
    project: &str,
    region: &str,
    requestId: Option<&str>,
    body: &PacketMirroring,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_packet_mirrorings_patch_builder(
        client,
        packetMirroring,
        project,
        region,
        requestId,
        body,
    )?;
    compute_packet_mirrorings_patch_execute(builder)
}

/// GET projects/{project}/regions/{region}/packetMirrorings/{resource}/testIamPermissions
/// Returns permissions that a caller has on the specified resource.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_packet_mirrorings_test_iam_permissions_execute()` to send, or `compute_packet_mirrorings_test_iam_permissions` for simplest API.

pub fn compute_packet_mirrorings_test_iam_permissions_builder(
    client: &SimpleHttpClient,
    project: &str,
    region: &str,
    resource: &str,
    body: &TestPermissionsRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/regions/{}/packetMirrorings/{}/testIamPermissions",
        project, region, resource,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET projects/{project}/regions/{region}/packetMirrorings/{resource}/testIamPermissions
/// Returns permissions that a caller has on the specified resource.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_packet_mirrorings_test_iam_permissions_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_packet_mirrorings_test_iam_permissions_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_packet_mirrorings_test_iam_permissions_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<TestPermissionsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: TestPermissionsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/regions/{region}/packetMirrorings/{resource}/testIamPermissions
/// Returns permissions that a caller has on the specified resource.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_packet_mirrorings_test_iam_permissions_builder()` + `compute_packet_mirrorings_test_iam_permissions_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_packet_mirrorings_test_iam_permissions(
    client: &SimpleHttpClient,
    project: &str,
    region: &str,
    resource: &str,
    body: &TestPermissionsRequest,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<TestPermissionsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = compute_packet_mirrorings_test_iam_permissions_builder(
        client, project, region, resource, body,
    )?;
    compute_packet_mirrorings_test_iam_permissions_execute(builder)
}

/// GET projects/{project}/global/previewFeatures/{previewFeature}
/// Returns the details of the given PreviewFeature.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_preview_features_get_execute()` to send, or `compute_preview_features_get` for simplest API.

pub fn compute_preview_features_get_builder(
    client: &SimpleHttpClient,
    previewFeature: &str,
    project: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/global/previewFeatures/{}",
        previewFeature, project,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/global/previewFeatures/{previewFeature}
/// Returns the details of the given PreviewFeature.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_preview_features_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_preview_features_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_preview_features_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<PreviewFeature>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: PreviewFeature = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/global/previewFeatures/{previewFeature}
/// Returns the details of the given PreviewFeature.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_preview_features_get_builder()` + `compute_preview_features_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_preview_features_get(
    client: &SimpleHttpClient,
    previewFeature: &str,
    project: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<PreviewFeature>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = compute_preview_features_get_builder(client, previewFeature, project)?;
    compute_preview_features_get_execute(builder)
}

/// GET projects/{project}/global/previewFeatures
/// Returns the details of the given PreviewFeature.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_preview_features_list_execute()` to send, or `compute_preview_features_list` for simplest API.

pub fn compute_preview_features_list_builder(
    client: &SimpleHttpClient,
    project: &str,
    filter: Option<&str>,
    maxResults: Option<i32>,
    orderBy: Option<&str>,
    pageToken: Option<&str>,
    returnPartialSuccess: Option<bool>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/global/previewFeatures",
        project,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "filter",
            filter
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "maxResults",
            maxResults
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "orderBy",
            orderBy
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageToken",
            pageToken
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "returnPartialSuccess",
            returnPartialSuccess
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET projects/{project}/global/previewFeatures
/// Returns the details of the given PreviewFeature.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_preview_features_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_preview_features_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_preview_features_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<PreviewFeatureList>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: PreviewFeatureList = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/global/previewFeatures
/// Returns the details of the given PreviewFeature.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_preview_features_list_builder()` + `compute_preview_features_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_preview_features_list(
    client: &SimpleHttpClient,
    project: &str,
    filter: Option<&str>,
    maxResults: Option<i32>,
    orderBy: Option<&str>,
    pageToken: Option<&str>,
    returnPartialSuccess: Option<bool>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<PreviewFeatureList>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = compute_preview_features_list_builder(
        client,
        project,
        filter,
        maxResults,
        orderBy,
        pageToken,
        returnPartialSuccess,
    )?;
    compute_preview_features_list_execute(builder)
}

/// GET projects/{project}/global/previewFeatures/{previewFeature}
/// Patches the given PreviewFeature. This method is used to enable or disable a PreviewFeature.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_preview_features_update_execute()` to send, or `compute_preview_features_update` for simplest API.

pub fn compute_preview_features_update_builder(
    client: &SimpleHttpClient,
    previewFeature: &str,
    project: &str,
    requestId: Option<&str>,
    body: &PreviewFeature,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/global/previewFeatures/{}",
        previewFeature, project,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "requestId",
            requestId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET projects/{project}/global/previewFeatures/{previewFeature}
/// Patches the given PreviewFeature. This method is used to enable or disable a PreviewFeature.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_preview_features_update_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_preview_features_update_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_preview_features_update_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/global/previewFeatures/{previewFeature}
/// Patches the given PreviewFeature. This method is used to enable or disable a PreviewFeature.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_preview_features_update_builder()` + `compute_preview_features_update_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_preview_features_update(
    client: &SimpleHttpClient,
    previewFeature: &str,
    project: &str,
    requestId: Option<&str>,
    body: &PreviewFeature,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        compute_preview_features_update_builder(client, previewFeature, project, requestId, body)?;
    compute_preview_features_update_execute(builder)
}

/// GET projects/{project}/disableXpnHost
/// Disable this project as a shared VPC host project.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_projects_disable_xpn_host_execute()` to send, or `compute_projects_disable_xpn_host` for simplest API.

pub fn compute_projects_disable_xpn_host_builder(
    client: &SimpleHttpClient,
    project: &str,
    requestId: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comprojects/{}/disableXpnHost", project,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "requestId",
            requestId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET projects/{project}/disableXpnHost
/// Disable this project as a shared VPC host project.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_projects_disable_xpn_host_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_projects_disable_xpn_host_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_projects_disable_xpn_host_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/disableXpnHost
/// Disable this project as a shared VPC host project.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_projects_disable_xpn_host_builder()` + `compute_projects_disable_xpn_host_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_projects_disable_xpn_host(
    client: &SimpleHttpClient,
    project: &str,
    requestId: Option<&str>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_projects_disable_xpn_host_builder(client, project, requestId)?;
    compute_projects_disable_xpn_host_execute(builder)
}

/// GET projects/{project}/disableXpnResource
/// Disable a service resource (also known as service project) associated with this host project.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_projects_disable_xpn_resource_execute()` to send, or `compute_projects_disable_xpn_resource` for simplest API.

pub fn compute_projects_disable_xpn_resource_builder(
    client: &SimpleHttpClient,
    project: &str,
    requestId: Option<&str>,
    body: &ProjectsDisableXpnResourceRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/disableXpnResource",
        project,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "requestId",
            requestId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET projects/{project}/disableXpnResource
/// Disable a service resource (also known as service project) associated with this host project.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_projects_disable_xpn_resource_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_projects_disable_xpn_resource_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_projects_disable_xpn_resource_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/disableXpnResource
/// Disable a service resource (also known as service project) associated with this host project.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_projects_disable_xpn_resource_builder()` + `compute_projects_disable_xpn_resource_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_projects_disable_xpn_resource(
    client: &SimpleHttpClient,
    project: &str,
    requestId: Option<&str>,
    body: &ProjectsDisableXpnResourceRequest,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_projects_disable_xpn_resource_builder(client, project, requestId, body)?;
    compute_projects_disable_xpn_resource_execute(builder)
}

/// GET projects/{project}/enableXpnHost
/// Enable this project as a shared VPC host project.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_projects_enable_xpn_host_execute()` to send, or `compute_projects_enable_xpn_host` for simplest API.

pub fn compute_projects_enable_xpn_host_builder(
    client: &SimpleHttpClient,
    project: &str,
    requestId: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comprojects/{}/enableXpnHost", project,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "requestId",
            requestId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET projects/{project}/enableXpnHost
/// Enable this project as a shared VPC host project.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_projects_enable_xpn_host_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_projects_enable_xpn_host_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_projects_enable_xpn_host_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/enableXpnHost
/// Enable this project as a shared VPC host project.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_projects_enable_xpn_host_builder()` + `compute_projects_enable_xpn_host_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_projects_enable_xpn_host(
    client: &SimpleHttpClient,
    project: &str,
    requestId: Option<&str>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_projects_enable_xpn_host_builder(client, project, requestId)?;
    compute_projects_enable_xpn_host_execute(builder)
}

/// GET projects/{project}/enableXpnResource
/// Enable service resource (a.k.a service project) for a host project, so that subnets in the host project can be used by instances in the service project.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_projects_enable_xpn_resource_execute()` to send, or `compute_projects_enable_xpn_resource` for simplest API.

pub fn compute_projects_enable_xpn_resource_builder(
    client: &SimpleHttpClient,
    project: &str,
    requestId: Option<&str>,
    body: &ProjectsEnableXpnResourceRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/enableXpnResource",
        project,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "requestId",
            requestId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET projects/{project}/enableXpnResource
/// Enable service resource (a.k.a service project) for a host project, so that subnets in the host project can be used by instances in the service project.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_projects_enable_xpn_resource_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_projects_enable_xpn_resource_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_projects_enable_xpn_resource_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/enableXpnResource
/// Enable service resource (a.k.a service project) for a host project, so that subnets in the host project can be used by instances in the service project.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_projects_enable_xpn_resource_builder()` + `compute_projects_enable_xpn_resource_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_projects_enable_xpn_resource(
    client: &SimpleHttpClient,
    project: &str,
    requestId: Option<&str>,
    body: &ProjectsEnableXpnResourceRequest,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_projects_enable_xpn_resource_builder(client, project, requestId, body)?;
    compute_projects_enable_xpn_resource_execute(builder)
}

/// GET projects/{project}
/// Returns the specified Project resource.  To decrease latency for this method, you can optionally omit any unneeded information from the response by using a field mask. This practice is especially recommended for unused quota information (the quotas field). To exclude one or more fields, set your request's fields query parameter to only include the fields you need. For example, to only include the id and `selfLink` fields, add the query parameter ?fields=id,`selfLink` to your request.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_projects_get_execute()` to send, or `compute_projects_get` for simplest API.

pub fn compute_projects_get_builder(
    client: &SimpleHttpClient,
    project: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comprojects/{}", project,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}
/// Returns the specified Project resource.  To decrease latency for this method, you can optionally omit any unneeded information from the response by using a field mask. This practice is especially recommended for unused quota information (the quotas field). To exclude one or more fields, set your request's fields query parameter to only include the fields you need. For example, to only include the id and `selfLink` fields, add the query parameter ?fields=id,`selfLink` to your request.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_projects_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_projects_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_projects_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Project>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Project = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}
/// Returns the specified Project resource.  To decrease latency for this method, you can optionally omit any unneeded information from the response by using a field mask. This practice is especially recommended for unused quota information (the quotas field). To exclude one or more fields, set your request's fields query parameter to only include the fields you need. For example, to only include the id and `selfLink` fields, add the query parameter ?fields=id,`selfLink` to your request.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_projects_get_builder()` + `compute_projects_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_projects_get(
    client: &SimpleHttpClient,
    project: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Project>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_projects_get_builder(client, project)?;
    compute_projects_get_execute(builder)
}

/// GET projects/{project}/getXpnHost
/// Gets the shared VPC host project that this project links to. May be empty if no link exists.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_projects_get_xpn_host_execute()` to send, or `compute_projects_get_xpn_host` for simplest API.

pub fn compute_projects_get_xpn_host_builder(
    client: &SimpleHttpClient,
    project: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comprojects/{}/getXpnHost", project,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/getXpnHost
/// Gets the shared VPC host project that this project links to. May be empty if no link exists.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_projects_get_xpn_host_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_projects_get_xpn_host_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_projects_get_xpn_host_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Project>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Project = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/getXpnHost
/// Gets the shared VPC host project that this project links to. May be empty if no link exists.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_projects_get_xpn_host_builder()` + `compute_projects_get_xpn_host_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_projects_get_xpn_host(
    client: &SimpleHttpClient,
    project: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Project>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_projects_get_xpn_host_builder(client, project)?;
    compute_projects_get_xpn_host_execute(builder)
}

/// GET projects/{project}/getXpnResources
/// Gets service resources (a.k.a service project) associated with this host project.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_projects_get_xpn_resources_execute()` to send, or `compute_projects_get_xpn_resources` for simplest API.

pub fn compute_projects_get_xpn_resources_builder(
    client: &SimpleHttpClient,
    project: &str,
    filter: Option<&str>,
    maxResults: Option<i32>,
    orderBy: Option<&str>,
    pageToken: Option<&str>,
    returnPartialSuccess: Option<bool>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/getXpnResources",
        project,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "filter",
            filter
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "maxResults",
            maxResults
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "orderBy",
            orderBy
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageToken",
            pageToken
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "returnPartialSuccess",
            returnPartialSuccess
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET projects/{project}/getXpnResources
/// Gets service resources (a.k.a service project) associated with this host project.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_projects_get_xpn_resources_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_projects_get_xpn_resources_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_projects_get_xpn_resources_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ProjectsGetXpnResources>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ProjectsGetXpnResources = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/getXpnResources
/// Gets service resources (a.k.a service project) associated with this host project.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_projects_get_xpn_resources_builder()` + `compute_projects_get_xpn_resources_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_projects_get_xpn_resources(
    client: &SimpleHttpClient,
    project: &str,
    filter: Option<&str>,
    maxResults: Option<i32>,
    orderBy: Option<&str>,
    pageToken: Option<&str>,
    returnPartialSuccess: Option<bool>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ProjectsGetXpnResources>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = compute_projects_get_xpn_resources_builder(
        client,
        project,
        filter,
        maxResults,
        orderBy,
        pageToken,
        returnPartialSuccess,
    )?;
    compute_projects_get_xpn_resources_execute(builder)
}

/// GET projects/{project}/listXpnHosts
/// Lists all shared VPC host projects visible to the user in an organization.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_projects_list_xpn_hosts_execute()` to send, or `compute_projects_list_xpn_hosts` for simplest API.

pub fn compute_projects_list_xpn_hosts_builder(
    client: &SimpleHttpClient,
    project: &str,
    filter: Option<&str>,
    maxResults: Option<i32>,
    orderBy: Option<&str>,
    pageToken: Option<&str>,
    returnPartialSuccess: Option<bool>,
    body: &ProjectsListXpnHostsRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comprojects/{}/listXpnHosts", project,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "filter",
            filter
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "maxResults",
            maxResults
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "orderBy",
            orderBy
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageToken",
            pageToken
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "returnPartialSuccess",
            returnPartialSuccess
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET projects/{project}/listXpnHosts
/// Lists all shared VPC host projects visible to the user in an organization.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_projects_list_xpn_hosts_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_projects_list_xpn_hosts_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_projects_list_xpn_hosts_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<XpnHostList>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: XpnHostList = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/listXpnHosts
/// Lists all shared VPC host projects visible to the user in an organization.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_projects_list_xpn_hosts_builder()` + `compute_projects_list_xpn_hosts_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_projects_list_xpn_hosts(
    client: &SimpleHttpClient,
    project: &str,
    filter: Option<&str>,
    maxResults: Option<i32>,
    orderBy: Option<&str>,
    pageToken: Option<&str>,
    returnPartialSuccess: Option<bool>,
    body: &ProjectsListXpnHostsRequest,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<XpnHostList>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_projects_list_xpn_hosts_builder(
        client,
        project,
        filter,
        maxResults,
        orderBy,
        pageToken,
        returnPartialSuccess,
        body,
    )?;
    compute_projects_list_xpn_hosts_execute(builder)
}

/// GET projects/{project}/moveDisk
/// Moves a persistent disk from one zone to another. *Note*: The `moveDisk` API will be deprecated on September 29, 2026.  Starting September 29, 2025, you can't use the `moveDisk` API on new projects. To move a disk to a different region or zone, follow the steps in [Change the location of a disk](<https://cloud.google.`com/compute/docs/disks/migrate-to-hyperdisk`#migrate-to-hd>).  Projects that already use the `moveDisk` API can continue usage until September 29, 2026.  Starting November 1, 2025, API responses will include a warning message in the response body about the upcoming deprecation. You can skip the message to continue using the service without interruption.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_projects_move_disk_execute()` to send, or `compute_projects_move_disk` for simplest API.

pub fn compute_projects_move_disk_builder(
    client: &SimpleHttpClient,
    project: &str,
    requestId: Option<&str>,
    body: &DiskMoveRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comprojects/{}/moveDisk", project,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "requestId",
            requestId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET projects/{project}/moveDisk
/// Moves a persistent disk from one zone to another. *Note*: The `moveDisk` API will be deprecated on September 29, 2026.  Starting September 29, 2025, you can't use the `moveDisk` API on new projects. To move a disk to a different region or zone, follow the steps in [Change the location of a disk](<https://cloud.google.`com/compute/docs/disks/migrate-to-hyperdisk`#migrate-to-hd>).  Projects that already use the `moveDisk` API can continue usage until September 29, 2026.  Starting November 1, 2025, API responses will include a warning message in the response body about the upcoming deprecation. You can skip the message to continue using the service without interruption.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_projects_move_disk_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_projects_move_disk_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_projects_move_disk_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/moveDisk
/// Moves a persistent disk from one zone to another. *Note*: The `moveDisk` API will be deprecated on September 29, 2026.  Starting September 29, 2025, you can't use the `moveDisk` API on new projects. To move a disk to a different region or zone, follow the steps in [Change the location of a disk](<https://cloud.google.`com/compute/docs/disks/migrate-to-hyperdisk`#migrate-to-hd>).  Projects that already use the `moveDisk` API can continue usage until September 29, 2026.  Starting November 1, 2025, API responses will include a warning message in the response body about the upcoming deprecation. You can skip the message to continue using the service without interruption.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_projects_move_disk_builder()` + `compute_projects_move_disk_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_projects_move_disk(
    client: &SimpleHttpClient,
    project: &str,
    requestId: Option<&str>,
    body: &DiskMoveRequest,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_projects_move_disk_builder(client, project, requestId, body)?;
    compute_projects_move_disk_execute(builder)
}

/// GET projects/{project}/moveInstance
/// Moves an instance and its attached persistent disks from one zone to another. *Note*: Moving VMs or disks by using this method might cause unexpected behavior. For more information, see the [known issue](/`compute/docs/troubleshooting/known-issues`#moving_vms_or_disks_using_the_moveinstance_api_or_the_causes_unexpected_behavior). [Deprecated] This method is deprecated. See [moving instance across zones](/`compute/docs/instances/moving-instance-across-zones`) instead.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_projects_move_instance_execute()` to send, or `compute_projects_move_instance` for simplest API.

pub fn compute_projects_move_instance_builder(
    client: &SimpleHttpClient,
    project: &str,
    requestId: Option<&str>,
    body: &InstanceMoveRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comprojects/{}/moveInstance", project,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "requestId",
            requestId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET projects/{project}/moveInstance
/// Moves an instance and its attached persistent disks from one zone to another. *Note*: Moving VMs or disks by using this method might cause unexpected behavior. For more information, see the [known issue](/`compute/docs/troubleshooting/known-issues`#moving_vms_or_disks_using_the_moveinstance_api_or_the_causes_unexpected_behavior). [Deprecated] This method is deprecated. See [moving instance across zones](/`compute/docs/instances/moving-instance-across-zones`) instead.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_projects_move_instance_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_projects_move_instance_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_projects_move_instance_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/moveInstance
/// Moves an instance and its attached persistent disks from one zone to another. *Note*: Moving VMs or disks by using this method might cause unexpected behavior. For more information, see the [known issue](/`compute/docs/troubleshooting/known-issues`#moving_vms_or_disks_using_the_moveinstance_api_or_the_causes_unexpected_behavior). [Deprecated] This method is deprecated. See [moving instance across zones](/`compute/docs/instances/moving-instance-across-zones`) instead.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_projects_move_instance_builder()` + `compute_projects_move_instance_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_projects_move_instance(
    client: &SimpleHttpClient,
    project: &str,
    requestId: Option<&str>,
    body: &InstanceMoveRequest,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_projects_move_instance_builder(client, project, requestId, body)?;
    compute_projects_move_instance_execute(builder)
}

/// GET projects/{project}/setCloudArmorTier
/// Sets the Cloud Armor tier of the project. To set ENTERPRISE or above the billing account of the project must be subscribed to Cloud Armor Enterprise. See Subscribing to Cloud Armor Enterprise for more information.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_projects_set_cloud_armor_tier_execute()` to send, or `compute_projects_set_cloud_armor_tier` for simplest API.

pub fn compute_projects_set_cloud_armor_tier_builder(
    client: &SimpleHttpClient,
    project: &str,
    requestId: Option<&str>,
    body: &ProjectsSetCloudArmorTierRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/setCloudArmorTier",
        project,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "requestId",
            requestId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET projects/{project}/setCloudArmorTier
/// Sets the Cloud Armor tier of the project. To set ENTERPRISE or above the billing account of the project must be subscribed to Cloud Armor Enterprise. See Subscribing to Cloud Armor Enterprise for more information.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_projects_set_cloud_armor_tier_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_projects_set_cloud_armor_tier_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_projects_set_cloud_armor_tier_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/setCloudArmorTier
/// Sets the Cloud Armor tier of the project. To set ENTERPRISE or above the billing account of the project must be subscribed to Cloud Armor Enterprise. See Subscribing to Cloud Armor Enterprise for more information.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_projects_set_cloud_armor_tier_builder()` + `compute_projects_set_cloud_armor_tier_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_projects_set_cloud_armor_tier(
    client: &SimpleHttpClient,
    project: &str,
    requestId: Option<&str>,
    body: &ProjectsSetCloudArmorTierRequest,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_projects_set_cloud_armor_tier_builder(client, project, requestId, body)?;
    compute_projects_set_cloud_armor_tier_execute(builder)
}

/// GET projects/{project}/setCommonInstanceMetadata
/// Sets metadata common to all instances within the specified project using the data included in the request.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_projects_set_common_instance_metadata_execute()` to send, or `compute_projects_set_common_instance_metadata` for simplest API.

pub fn compute_projects_set_common_instance_metadata_builder(
    client: &SimpleHttpClient,
    project: &str,
    requestId: Option<&str>,
    body: &Metadata,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/setCommonInstanceMetadata",
        project,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "requestId",
            requestId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET projects/{project}/setCommonInstanceMetadata
/// Sets metadata common to all instances within the specified project using the data included in the request.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_projects_set_common_instance_metadata_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_projects_set_common_instance_metadata_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_projects_set_common_instance_metadata_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/setCommonInstanceMetadata
/// Sets metadata common to all instances within the specified project using the data included in the request.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_projects_set_common_instance_metadata_builder()` + `compute_projects_set_common_instance_metadata_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_projects_set_common_instance_metadata(
    client: &SimpleHttpClient,
    project: &str,
    requestId: Option<&str>,
    body: &Metadata,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        compute_projects_set_common_instance_metadata_builder(client, project, requestId, body)?;
    compute_projects_set_common_instance_metadata_execute(builder)
}

/// GET projects/{project}/setDefaultNetworkTier
/// Sets the default network tier of the project. The default network tier is used when an `address/`forwardingRule`/instance` is created without specifying the network tier field.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_projects_set_default_network_tier_execute()` to send, or `compute_projects_set_default_network_tier` for simplest API.

pub fn compute_projects_set_default_network_tier_builder(
    client: &SimpleHttpClient,
    project: &str,
    requestId: Option<&str>,
    body: &ProjectsSetDefaultNetworkTierRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/setDefaultNetworkTier",
        project,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "requestId",
            requestId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET projects/{project}/setDefaultNetworkTier
/// Sets the default network tier of the project. The default network tier is used when an `address/`forwardingRule`/instance` is created without specifying the network tier field.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_projects_set_default_network_tier_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_projects_set_default_network_tier_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_projects_set_default_network_tier_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/setDefaultNetworkTier
/// Sets the default network tier of the project. The default network tier is used when an `address/`forwardingRule`/instance` is created without specifying the network tier field.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_projects_set_default_network_tier_builder()` + `compute_projects_set_default_network_tier_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_projects_set_default_network_tier(
    client: &SimpleHttpClient,
    project: &str,
    requestId: Option<&str>,
    body: &ProjectsSetDefaultNetworkTierRequest,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        compute_projects_set_default_network_tier_builder(client, project, requestId, body)?;
    compute_projects_set_default_network_tier_execute(builder)
}

/// GET projects/{project}/setUsageExportBucket
/// Enables the usage export feature and sets theusage export bucket where reports are stored. If you provide an empty request body using this method, the usage export feature will be disabled.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_projects_set_usage_export_bucket_execute()` to send, or `compute_projects_set_usage_export_bucket` for simplest API.

pub fn compute_projects_set_usage_export_bucket_builder(
    client: &SimpleHttpClient,
    project: &str,
    requestId: Option<&str>,
    body: &UsageExportLocation,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/setUsageExportBucket",
        project,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "requestId",
            requestId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET projects/{project}/setUsageExportBucket
/// Enables the usage export feature and sets theusage export bucket where reports are stored. If you provide an empty request body using this method, the usage export feature will be disabled.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_projects_set_usage_export_bucket_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_projects_set_usage_export_bucket_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_projects_set_usage_export_bucket_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/setUsageExportBucket
/// Enables the usage export feature and sets theusage export bucket where reports are stored. If you provide an empty request body using this method, the usage export feature will be disabled.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_projects_set_usage_export_bucket_builder()` + `compute_projects_set_usage_export_bucket_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_projects_set_usage_export_bucket(
    client: &SimpleHttpClient,
    project: &str,
    requestId: Option<&str>,
    body: &UsageExportLocation,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        compute_projects_set_usage_export_bucket_builder(client, project, requestId, body)?;
    compute_projects_set_usage_export_bucket_execute(builder)
}

/// GET projects/{project}/global/publicAdvertisedPrefixes/{publicAdvertisedPrefix}/announce
/// Announces the specified PublicAdvertisedPrefix
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_public_advertised_prefixes_announce_execute()` to send, or `compute_public_advertised_prefixes_announce` for simplest API.

pub fn compute_public_advertised_prefixes_announce_builder(
    client: &SimpleHttpClient,
    project: &str,
    publicAdvertisedPrefix: &str,
    requestId: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/global/publicAdvertisedPrefixes/{}/announce",
        project, publicAdvertisedPrefix,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "requestId",
            requestId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET projects/{project}/global/publicAdvertisedPrefixes/{publicAdvertisedPrefix}/announce
/// Announces the specified PublicAdvertisedPrefix
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_public_advertised_prefixes_announce_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_public_advertised_prefixes_announce_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_public_advertised_prefixes_announce_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/global/publicAdvertisedPrefixes/{publicAdvertisedPrefix}/announce
/// Announces the specified PublicAdvertisedPrefix
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_public_advertised_prefixes_announce_builder()` + `compute_public_advertised_prefixes_announce_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_public_advertised_prefixes_announce(
    client: &SimpleHttpClient,
    project: &str,
    publicAdvertisedPrefix: &str,
    requestId: Option<&str>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_public_advertised_prefixes_announce_builder(
        client,
        project,
        publicAdvertisedPrefix,
        requestId,
    )?;
    compute_public_advertised_prefixes_announce_execute(builder)
}

/// GET projects/{project}/global/publicAdvertisedPrefixes/{publicAdvertisedPrefix}
/// Deletes the specified PublicAdvertisedPrefix
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_public_advertised_prefixes_delete_execute()` to send, or `compute_public_advertised_prefixes_delete` for simplest API.

pub fn compute_public_advertised_prefixes_delete_builder(
    client: &SimpleHttpClient,
    project: &str,
    publicAdvertisedPrefix: &str,
    requestId: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/global/publicAdvertisedPrefixes/{}",
        project, publicAdvertisedPrefix,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "requestId",
            requestId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET projects/{project}/global/publicAdvertisedPrefixes/{publicAdvertisedPrefix}
/// Deletes the specified PublicAdvertisedPrefix
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_public_advertised_prefixes_delete_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_public_advertised_prefixes_delete_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_public_advertised_prefixes_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/global/publicAdvertisedPrefixes/{publicAdvertisedPrefix}
/// Deletes the specified PublicAdvertisedPrefix
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_public_advertised_prefixes_delete_builder()` + `compute_public_advertised_prefixes_delete_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_public_advertised_prefixes_delete(
    client: &SimpleHttpClient,
    project: &str,
    publicAdvertisedPrefix: &str,
    requestId: Option<&str>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_public_advertised_prefixes_delete_builder(
        client,
        project,
        publicAdvertisedPrefix,
        requestId,
    )?;
    compute_public_advertised_prefixes_delete_execute(builder)
}

/// GET projects/{project}/global/publicAdvertisedPrefixes/{publicAdvertisedPrefix}
/// Returns the specified PublicAdvertisedPrefix resource.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_public_advertised_prefixes_get_execute()` to send, or `compute_public_advertised_prefixes_get` for simplest API.

pub fn compute_public_advertised_prefixes_get_builder(
    client: &SimpleHttpClient,
    project: &str,
    publicAdvertisedPrefix: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/global/publicAdvertisedPrefixes/{}",
        project, publicAdvertisedPrefix,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/global/publicAdvertisedPrefixes/{publicAdvertisedPrefix}
/// Returns the specified PublicAdvertisedPrefix resource.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_public_advertised_prefixes_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_public_advertised_prefixes_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_public_advertised_prefixes_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<PublicAdvertisedPrefix>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: PublicAdvertisedPrefix = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/global/publicAdvertisedPrefixes/{publicAdvertisedPrefix}
/// Returns the specified PublicAdvertisedPrefix resource.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_public_advertised_prefixes_get_builder()` + `compute_public_advertised_prefixes_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_public_advertised_prefixes_get(
    client: &SimpleHttpClient,
    project: &str,
    publicAdvertisedPrefix: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<PublicAdvertisedPrefix>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        compute_public_advertised_prefixes_get_builder(client, project, publicAdvertisedPrefix)?;
    compute_public_advertised_prefixes_get_execute(builder)
}

/// GET projects/{project}/global/publicAdvertisedPrefixes
/// Creates a PublicAdvertisedPrefix in the specified project using the parameters that are included in the request.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_public_advertised_prefixes_insert_execute()` to send, or `compute_public_advertised_prefixes_insert` for simplest API.

pub fn compute_public_advertised_prefixes_insert_builder(
    client: &SimpleHttpClient,
    project: &str,
    requestId: Option<&str>,
    body: &PublicAdvertisedPrefix,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/global/publicAdvertisedPrefixes",
        project,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "requestId",
            requestId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET projects/{project}/global/publicAdvertisedPrefixes
/// Creates a PublicAdvertisedPrefix in the specified project using the parameters that are included in the request.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_public_advertised_prefixes_insert_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_public_advertised_prefixes_insert_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_public_advertised_prefixes_insert_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/global/publicAdvertisedPrefixes
/// Creates a PublicAdvertisedPrefix in the specified project using the parameters that are included in the request.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_public_advertised_prefixes_insert_builder()` + `compute_public_advertised_prefixes_insert_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_public_advertised_prefixes_insert(
    client: &SimpleHttpClient,
    project: &str,
    requestId: Option<&str>,
    body: &PublicAdvertisedPrefix,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        compute_public_advertised_prefixes_insert_builder(client, project, requestId, body)?;
    compute_public_advertised_prefixes_insert_execute(builder)
}

/// GET projects/{project}/global/publicAdvertisedPrefixes
/// Lists the PublicAdvertisedPrefixes for a project.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_public_advertised_prefixes_list_execute()` to send, or `compute_public_advertised_prefixes_list` for simplest API.

pub fn compute_public_advertised_prefixes_list_builder(
    client: &SimpleHttpClient,
    project: &str,
    filter: Option<&str>,
    maxResults: Option<i32>,
    orderBy: Option<&str>,
    pageToken: Option<&str>,
    returnPartialSuccess: Option<bool>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/global/publicAdvertisedPrefixes",
        project,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "filter",
            filter
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "maxResults",
            maxResults
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "orderBy",
            orderBy
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageToken",
            pageToken
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "returnPartialSuccess",
            returnPartialSuccess
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET projects/{project}/global/publicAdvertisedPrefixes
/// Lists the PublicAdvertisedPrefixes for a project.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_public_advertised_prefixes_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_public_advertised_prefixes_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_public_advertised_prefixes_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<PublicAdvertisedPrefixList>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: PublicAdvertisedPrefixList = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/global/publicAdvertisedPrefixes
/// Lists the PublicAdvertisedPrefixes for a project.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_public_advertised_prefixes_list_builder()` + `compute_public_advertised_prefixes_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_public_advertised_prefixes_list(
    client: &SimpleHttpClient,
    project: &str,
    filter: Option<&str>,
    maxResults: Option<i32>,
    orderBy: Option<&str>,
    pageToken: Option<&str>,
    returnPartialSuccess: Option<bool>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<PublicAdvertisedPrefixList>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = compute_public_advertised_prefixes_list_builder(
        client,
        project,
        filter,
        maxResults,
        orderBy,
        pageToken,
        returnPartialSuccess,
    )?;
    compute_public_advertised_prefixes_list_execute(builder)
}

/// GET projects/{project}/global/publicAdvertisedPrefixes/{publicAdvertisedPrefix}
/// Patches the specified Router resource with the data included in the request. This method `supportsPATCH` semantics and `usesJSON` merge patch format and processing rules.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_public_advertised_prefixes_patch_execute()` to send, or `compute_public_advertised_prefixes_patch` for simplest API.

pub fn compute_public_advertised_prefixes_patch_builder(
    client: &SimpleHttpClient,
    project: &str,
    publicAdvertisedPrefix: &str,
    requestId: Option<&str>,
    body: &PublicAdvertisedPrefix,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/global/publicAdvertisedPrefixes/{}",
        project, publicAdvertisedPrefix,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "requestId",
            requestId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET projects/{project}/global/publicAdvertisedPrefixes/{publicAdvertisedPrefix}
/// Patches the specified Router resource with the data included in the request. This method `supportsPATCH` semantics and `usesJSON` merge patch format and processing rules.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_public_advertised_prefixes_patch_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_public_advertised_prefixes_patch_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_public_advertised_prefixes_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/global/publicAdvertisedPrefixes/{publicAdvertisedPrefix}
/// Patches the specified Router resource with the data included in the request. This method `supportsPATCH` semantics and `usesJSON` merge patch format and processing rules.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_public_advertised_prefixes_patch_builder()` + `compute_public_advertised_prefixes_patch_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_public_advertised_prefixes_patch(
    client: &SimpleHttpClient,
    project: &str,
    publicAdvertisedPrefix: &str,
    requestId: Option<&str>,
    body: &PublicAdvertisedPrefix,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_public_advertised_prefixes_patch_builder(
        client,
        project,
        publicAdvertisedPrefix,
        requestId,
        body,
    )?;
    compute_public_advertised_prefixes_patch_execute(builder)
}

/// GET projects/{project}/global/publicAdvertisedPrefixes/{publicAdvertisedPrefix}/withdraw
/// Withdraws the specified PublicAdvertisedPrefix
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_public_advertised_prefixes_withdraw_execute()` to send, or `compute_public_advertised_prefixes_withdraw` for simplest API.

pub fn compute_public_advertised_prefixes_withdraw_builder(
    client: &SimpleHttpClient,
    project: &str,
    publicAdvertisedPrefix: &str,
    requestId: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/global/publicAdvertisedPrefixes/{}/withdraw",
        project, publicAdvertisedPrefix,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "requestId",
            requestId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET projects/{project}/global/publicAdvertisedPrefixes/{publicAdvertisedPrefix}/withdraw
/// Withdraws the specified PublicAdvertisedPrefix
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_public_advertised_prefixes_withdraw_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_public_advertised_prefixes_withdraw_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_public_advertised_prefixes_withdraw_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/global/publicAdvertisedPrefixes/{publicAdvertisedPrefix}/withdraw
/// Withdraws the specified PublicAdvertisedPrefix
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_public_advertised_prefixes_withdraw_builder()` + `compute_public_advertised_prefixes_withdraw_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_public_advertised_prefixes_withdraw(
    client: &SimpleHttpClient,
    project: &str,
    publicAdvertisedPrefix: &str,
    requestId: Option<&str>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_public_advertised_prefixes_withdraw_builder(
        client,
        project,
        publicAdvertisedPrefix,
        requestId,
    )?;
    compute_public_advertised_prefixes_withdraw_execute(builder)
}

/// GET projects/{project}/aggregated/publicDelegatedPrefixes
/// Lists all PublicDelegatedPrefix resources owned by the specific project across all scopes.  To prevent failure, Google recommends that you set the `returnPartialSuccess` parameter to `true`.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_public_delegated_prefixes_aggregated_list_execute()` to send, or `compute_public_delegated_prefixes_aggregated_list` for simplest API.

pub fn compute_public_delegated_prefixes_aggregated_list_builder(
    client: &SimpleHttpClient,
    project: &str,
    filter: Option<&str>,
    includeAllScopes: Option<bool>,
    maxResults: Option<i32>,
    orderBy: Option<&str>,
    pageToken: Option<&str>,
    returnPartialSuccess: Option<bool>,
    serviceProjectNumber: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/aggregated/publicDelegatedPrefixes",
        project,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "filter",
            filter
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "includeAllScopes",
            includeAllScopes
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "maxResults",
            maxResults
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "orderBy",
            orderBy
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageToken",
            pageToken
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "returnPartialSuccess",
            returnPartialSuccess
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "serviceProjectNumber",
            serviceProjectNumber
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET projects/{project}/aggregated/publicDelegatedPrefixes
/// Lists all PublicDelegatedPrefix resources owned by the specific project across all scopes.  To prevent failure, Google recommends that you set the `returnPartialSuccess` parameter to `true`.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_public_delegated_prefixes_aggregated_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_public_delegated_prefixes_aggregated_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_public_delegated_prefixes_aggregated_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<PublicDelegatedPrefixAggregatedList>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: PublicDelegatedPrefixAggregatedList = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/aggregated/publicDelegatedPrefixes
/// Lists all PublicDelegatedPrefix resources owned by the specific project across all scopes.  To prevent failure, Google recommends that you set the `returnPartialSuccess` parameter to `true`.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_public_delegated_prefixes_aggregated_list_builder()` + `compute_public_delegated_prefixes_aggregated_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_public_delegated_prefixes_aggregated_list(
    client: &SimpleHttpClient,
    project: &str,
    filter: Option<&str>,
    includeAllScopes: Option<bool>,
    maxResults: Option<i32>,
    orderBy: Option<&str>,
    pageToken: Option<&str>,
    returnPartialSuccess: Option<bool>,
    serviceProjectNumber: Option<&str>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<PublicDelegatedPrefixAggregatedList>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = compute_public_delegated_prefixes_aggregated_list_builder(
        client,
        project,
        filter,
        includeAllScopes,
        maxResults,
        orderBy,
        pageToken,
        returnPartialSuccess,
        serviceProjectNumber,
    )?;
    compute_public_delegated_prefixes_aggregated_list_execute(builder)
}

/// GET projects/{project}/regions/{region}/publicDelegatedPrefixes/{publicDelegatedPrefix}/announce
/// Announces the specified PublicDelegatedPrefix in the given region.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_public_delegated_prefixes_announce_execute()` to send, or `compute_public_delegated_prefixes_announce` for simplest API.

pub fn compute_public_delegated_prefixes_announce_builder(
    client: &SimpleHttpClient,
    project: &str,
    publicDelegatedPrefix: &str,
    region: &str,
    requestId: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/regions/{}/publicDelegatedPrefixes/{}/announce",
        project, publicDelegatedPrefix, region,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "requestId",
            requestId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET projects/{project}/regions/{region}/publicDelegatedPrefixes/{publicDelegatedPrefix}/announce
/// Announces the specified PublicDelegatedPrefix in the given region.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_public_delegated_prefixes_announce_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_public_delegated_prefixes_announce_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_public_delegated_prefixes_announce_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/regions/{region}/publicDelegatedPrefixes/{publicDelegatedPrefix}/announce
/// Announces the specified PublicDelegatedPrefix in the given region.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_public_delegated_prefixes_announce_builder()` + `compute_public_delegated_prefixes_announce_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_public_delegated_prefixes_announce(
    client: &SimpleHttpClient,
    project: &str,
    publicDelegatedPrefix: &str,
    region: &str,
    requestId: Option<&str>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_public_delegated_prefixes_announce_builder(
        client,
        project,
        publicDelegatedPrefix,
        region,
        requestId,
    )?;
    compute_public_delegated_prefixes_announce_execute(builder)
}

/// GET projects/{project}/regions/{region}/publicDelegatedPrefixes/{publicDelegatedPrefix}
/// Deletes the specified PublicDelegatedPrefix in the given region.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_public_delegated_prefixes_delete_execute()` to send, or `compute_public_delegated_prefixes_delete` for simplest API.

pub fn compute_public_delegated_prefixes_delete_builder(
    client: &SimpleHttpClient,
    project: &str,
    publicDelegatedPrefix: &str,
    region: &str,
    requestId: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/regions/{}/publicDelegatedPrefixes/{}",
        project, publicDelegatedPrefix, region,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "requestId",
            requestId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET projects/{project}/regions/{region}/publicDelegatedPrefixes/{publicDelegatedPrefix}
/// Deletes the specified PublicDelegatedPrefix in the given region.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_public_delegated_prefixes_delete_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_public_delegated_prefixes_delete_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_public_delegated_prefixes_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/regions/{region}/publicDelegatedPrefixes/{publicDelegatedPrefix}
/// Deletes the specified PublicDelegatedPrefix in the given region.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_public_delegated_prefixes_delete_builder()` + `compute_public_delegated_prefixes_delete_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_public_delegated_prefixes_delete(
    client: &SimpleHttpClient,
    project: &str,
    publicDelegatedPrefix: &str,
    region: &str,
    requestId: Option<&str>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_public_delegated_prefixes_delete_builder(
        client,
        project,
        publicDelegatedPrefix,
        region,
        requestId,
    )?;
    compute_public_delegated_prefixes_delete_execute(builder)
}

/// GET projects/{project}/regions/{region}/publicDelegatedPrefixes/{publicDelegatedPrefix}
/// Returns the specified PublicDelegatedPrefix resource in the given region.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_public_delegated_prefixes_get_execute()` to send, or `compute_public_delegated_prefixes_get` for simplest API.

pub fn compute_public_delegated_prefixes_get_builder(
    client: &SimpleHttpClient,
    project: &str,
    publicDelegatedPrefix: &str,
    region: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/regions/{}/publicDelegatedPrefixes/{}",
        project, publicDelegatedPrefix, region,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/regions/{region}/publicDelegatedPrefixes/{publicDelegatedPrefix}
/// Returns the specified PublicDelegatedPrefix resource in the given region.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_public_delegated_prefixes_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_public_delegated_prefixes_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_public_delegated_prefixes_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<PublicDelegatedPrefix>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: PublicDelegatedPrefix = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/regions/{region}/publicDelegatedPrefixes/{publicDelegatedPrefix}
/// Returns the specified PublicDelegatedPrefix resource in the given region.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_public_delegated_prefixes_get_builder()` + `compute_public_delegated_prefixes_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_public_delegated_prefixes_get(
    client: &SimpleHttpClient,
    project: &str,
    publicDelegatedPrefix: &str,
    region: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<PublicDelegatedPrefix>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = compute_public_delegated_prefixes_get_builder(
        client,
        project,
        publicDelegatedPrefix,
        region,
    )?;
    compute_public_delegated_prefixes_get_execute(builder)
}

/// GET projects/{project}/regions/{region}/publicDelegatedPrefixes
/// Creates a PublicDelegatedPrefix in the specified project in the given region using the parameters that are included in the request.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_public_delegated_prefixes_insert_execute()` to send, or `compute_public_delegated_prefixes_insert` for simplest API.

pub fn compute_public_delegated_prefixes_insert_builder(
    client: &SimpleHttpClient,
    project: &str,
    region: &str,
    requestId: Option<&str>,
    body: &PublicDelegatedPrefix,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/regions/{}/publicDelegatedPrefixes",
        project, region,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "requestId",
            requestId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET projects/{project}/regions/{region}/publicDelegatedPrefixes
/// Creates a PublicDelegatedPrefix in the specified project in the given region using the parameters that are included in the request.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_public_delegated_prefixes_insert_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_public_delegated_prefixes_insert_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_public_delegated_prefixes_insert_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/regions/{region}/publicDelegatedPrefixes
/// Creates a PublicDelegatedPrefix in the specified project in the given region using the parameters that are included in the request.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_public_delegated_prefixes_insert_builder()` + `compute_public_delegated_prefixes_insert_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_public_delegated_prefixes_insert(
    client: &SimpleHttpClient,
    project: &str,
    region: &str,
    requestId: Option<&str>,
    body: &PublicDelegatedPrefix,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        compute_public_delegated_prefixes_insert_builder(client, project, region, requestId, body)?;
    compute_public_delegated_prefixes_insert_execute(builder)
}

/// GET projects/{project}/regions/{region}/publicDelegatedPrefixes
/// Lists the PublicDelegatedPrefixes for a project in the given region.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_public_delegated_prefixes_list_execute()` to send, or `compute_public_delegated_prefixes_list` for simplest API.

pub fn compute_public_delegated_prefixes_list_builder(
    client: &SimpleHttpClient,
    project: &str,
    region: &str,
    filter: Option<&str>,
    maxResults: Option<i32>,
    orderBy: Option<&str>,
    pageToken: Option<&str>,
    returnPartialSuccess: Option<bool>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/regions/{}/publicDelegatedPrefixes",
        project, region,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "filter",
            filter
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "maxResults",
            maxResults
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "orderBy",
            orderBy
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageToken",
            pageToken
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "returnPartialSuccess",
            returnPartialSuccess
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET projects/{project}/regions/{region}/publicDelegatedPrefixes
/// Lists the PublicDelegatedPrefixes for a project in the given region.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_public_delegated_prefixes_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_public_delegated_prefixes_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_public_delegated_prefixes_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<PublicDelegatedPrefixList>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: PublicDelegatedPrefixList = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/regions/{region}/publicDelegatedPrefixes
/// Lists the PublicDelegatedPrefixes for a project in the given region.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_public_delegated_prefixes_list_builder()` + `compute_public_delegated_prefixes_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_public_delegated_prefixes_list(
    client: &SimpleHttpClient,
    project: &str,
    region: &str,
    filter: Option<&str>,
    maxResults: Option<i32>,
    orderBy: Option<&str>,
    pageToken: Option<&str>,
    returnPartialSuccess: Option<bool>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<PublicDelegatedPrefixList>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = compute_public_delegated_prefixes_list_builder(
        client,
        project,
        region,
        filter,
        maxResults,
        orderBy,
        pageToken,
        returnPartialSuccess,
    )?;
    compute_public_delegated_prefixes_list_execute(builder)
}

/// GET projects/{project}/regions/{region}/publicDelegatedPrefixes/{publicDelegatedPrefix}
/// Patches the specified PublicDelegatedPrefix resource with the data included in the request. This method `supportsPATCH` semantics and `usesJSON` merge patch format and processing rules.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_public_delegated_prefixes_patch_execute()` to send, or `compute_public_delegated_prefixes_patch` for simplest API.

pub fn compute_public_delegated_prefixes_patch_builder(
    client: &SimpleHttpClient,
    project: &str,
    publicDelegatedPrefix: &str,
    region: &str,
    requestId: Option<&str>,
    body: &PublicDelegatedPrefix,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/regions/{}/publicDelegatedPrefixes/{}",
        project, publicDelegatedPrefix, region,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "requestId",
            requestId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET projects/{project}/regions/{region}/publicDelegatedPrefixes/{publicDelegatedPrefix}
/// Patches the specified PublicDelegatedPrefix resource with the data included in the request. This method `supportsPATCH` semantics and `usesJSON` merge patch format and processing rules.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_public_delegated_prefixes_patch_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_public_delegated_prefixes_patch_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_public_delegated_prefixes_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/regions/{region}/publicDelegatedPrefixes/{publicDelegatedPrefix}
/// Patches the specified PublicDelegatedPrefix resource with the data included in the request. This method `supportsPATCH` semantics and `usesJSON` merge patch format and processing rules.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_public_delegated_prefixes_patch_builder()` + `compute_public_delegated_prefixes_patch_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_public_delegated_prefixes_patch(
    client: &SimpleHttpClient,
    project: &str,
    publicDelegatedPrefix: &str,
    region: &str,
    requestId: Option<&str>,
    body: &PublicDelegatedPrefix,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_public_delegated_prefixes_patch_builder(
        client,
        project,
        publicDelegatedPrefix,
        region,
        requestId,
        body,
    )?;
    compute_public_delegated_prefixes_patch_execute(builder)
}

/// GET projects/{project}/regions/{region}/publicDelegatedPrefixes/{publicDelegatedPrefix}/withdraw
/// Withdraws the specified PublicDelegatedPrefix in the given region.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_public_delegated_prefixes_withdraw_execute()` to send, or `compute_public_delegated_prefixes_withdraw` for simplest API.

pub fn compute_public_delegated_prefixes_withdraw_builder(
    client: &SimpleHttpClient,
    project: &str,
    publicDelegatedPrefix: &str,
    region: &str,
    requestId: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/regions/{}/publicDelegatedPrefixes/{}/withdraw",
        project, publicDelegatedPrefix, region,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "requestId",
            requestId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET projects/{project}/regions/{region}/publicDelegatedPrefixes/{publicDelegatedPrefix}/withdraw
/// Withdraws the specified PublicDelegatedPrefix in the given region.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_public_delegated_prefixes_withdraw_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_public_delegated_prefixes_withdraw_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_public_delegated_prefixes_withdraw_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/regions/{region}/publicDelegatedPrefixes/{publicDelegatedPrefix}/withdraw
/// Withdraws the specified PublicDelegatedPrefix in the given region.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_public_delegated_prefixes_withdraw_builder()` + `compute_public_delegated_prefixes_withdraw_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_public_delegated_prefixes_withdraw(
    client: &SimpleHttpClient,
    project: &str,
    publicDelegatedPrefix: &str,
    region: &str,
    requestId: Option<&str>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_public_delegated_prefixes_withdraw_builder(
        client,
        project,
        publicDelegatedPrefix,
        region,
        requestId,
    )?;
    compute_public_delegated_prefixes_withdraw_execute(builder)
}

/// GET projects/{project}/regions/{region}/autoscalers/{autoscaler}
/// Deletes the specified autoscaler.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_region_autoscalers_delete_execute()` to send, or `compute_region_autoscalers_delete` for simplest API.

pub fn compute_region_autoscalers_delete_builder(
    client: &SimpleHttpClient,
    autoscaler: &str,
    project: &str,
    region: &str,
    requestId: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/regions/{}/autoscalers/{}",
        autoscaler, project, region,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "requestId",
            requestId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET projects/{project}/regions/{region}/autoscalers/{autoscaler}
/// Deletes the specified autoscaler.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_region_autoscalers_delete_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_region_autoscalers_delete_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_autoscalers_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/regions/{region}/autoscalers/{autoscaler}
/// Deletes the specified autoscaler.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_region_autoscalers_delete_builder()` + `compute_region_autoscalers_delete_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_autoscalers_delete(
    client: &SimpleHttpClient,
    autoscaler: &str,
    project: &str,
    region: &str,
    requestId: Option<&str>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        compute_region_autoscalers_delete_builder(client, autoscaler, project, region, requestId)?;
    compute_region_autoscalers_delete_execute(builder)
}

/// GET projects/{project}/regions/{region}/autoscalers/{autoscaler}
/// Returns the specified autoscaler.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_region_autoscalers_get_execute()` to send, or `compute_region_autoscalers_get` for simplest API.

pub fn compute_region_autoscalers_get_builder(
    client: &SimpleHttpClient,
    autoscaler: &str,
    project: &str,
    region: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/regions/{}/autoscalers/{}",
        autoscaler, project, region,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/regions/{region}/autoscalers/{autoscaler}
/// Returns the specified autoscaler.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_region_autoscalers_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_region_autoscalers_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_autoscalers_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Autoscaler>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Autoscaler = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/regions/{region}/autoscalers/{autoscaler}
/// Returns the specified autoscaler.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_region_autoscalers_get_builder()` + `compute_region_autoscalers_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_autoscalers_get(
    client: &SimpleHttpClient,
    autoscaler: &str,
    project: &str,
    region: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Autoscaler>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_region_autoscalers_get_builder(client, autoscaler, project, region)?;
    compute_region_autoscalers_get_execute(builder)
}

/// GET projects/{project}/regions/{region}/autoscalers
/// Creates an autoscaler in the specified project using the data included in the request.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_region_autoscalers_insert_execute()` to send, or `compute_region_autoscalers_insert` for simplest API.

pub fn compute_region_autoscalers_insert_builder(
    client: &SimpleHttpClient,
    project: &str,
    region: &str,
    requestId: Option<&str>,
    body: &Autoscaler,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/regions/{}/autoscalers",
        project, region,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "requestId",
            requestId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET projects/{project}/regions/{region}/autoscalers
/// Creates an autoscaler in the specified project using the data included in the request.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_region_autoscalers_insert_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_region_autoscalers_insert_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_autoscalers_insert_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/regions/{region}/autoscalers
/// Creates an autoscaler in the specified project using the data included in the request.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_region_autoscalers_insert_builder()` + `compute_region_autoscalers_insert_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_autoscalers_insert(
    client: &SimpleHttpClient,
    project: &str,
    region: &str,
    requestId: Option<&str>,
    body: &Autoscaler,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        compute_region_autoscalers_insert_builder(client, project, region, requestId, body)?;
    compute_region_autoscalers_insert_execute(builder)
}

/// GET projects/{project}/regions/{region}/autoscalers
/// Retrieves a list of autoscalers contained within the specified region.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_region_autoscalers_list_execute()` to send, or `compute_region_autoscalers_list` for simplest API.

pub fn compute_region_autoscalers_list_builder(
    client: &SimpleHttpClient,
    project: &str,
    region: &str,
    filter: Option<&str>,
    maxResults: Option<i32>,
    orderBy: Option<&str>,
    pageToken: Option<&str>,
    returnPartialSuccess: Option<bool>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/regions/{}/autoscalers",
        project, region,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "filter",
            filter
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "maxResults",
            maxResults
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "orderBy",
            orderBy
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageToken",
            pageToken
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "returnPartialSuccess",
            returnPartialSuccess
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET projects/{project}/regions/{region}/autoscalers
/// Retrieves a list of autoscalers contained within the specified region.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_region_autoscalers_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_region_autoscalers_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_autoscalers_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<RegionAutoscalerList>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: RegionAutoscalerList = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/regions/{region}/autoscalers
/// Retrieves a list of autoscalers contained within the specified region.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_region_autoscalers_list_builder()` + `compute_region_autoscalers_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_autoscalers_list(
    client: &SimpleHttpClient,
    project: &str,
    region: &str,
    filter: Option<&str>,
    maxResults: Option<i32>,
    orderBy: Option<&str>,
    pageToken: Option<&str>,
    returnPartialSuccess: Option<bool>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<RegionAutoscalerList>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = compute_region_autoscalers_list_builder(
        client,
        project,
        region,
        filter,
        maxResults,
        orderBy,
        pageToken,
        returnPartialSuccess,
    )?;
    compute_region_autoscalers_list_execute(builder)
}

/// GET projects/{project}/regions/{region}/autoscalers
/// Updates an autoscaler in the specified project using the data included in the request. This method `supportsPATCH` semantics and uses `theJSON` merge patch format and processing rules.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_region_autoscalers_patch_execute()` to send, or `compute_region_autoscalers_patch` for simplest API.

pub fn compute_region_autoscalers_patch_builder(
    client: &SimpleHttpClient,
    project: &str,
    region: &str,
    autoscaler: Option<&str>,
    requestId: Option<&str>,
    body: &Autoscaler,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/regions/{}/autoscalers",
        project, region,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "autoscaler",
            autoscaler
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "requestId",
            requestId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET projects/{project}/regions/{region}/autoscalers
/// Updates an autoscaler in the specified project using the data included in the request. This method `supportsPATCH` semantics and uses `theJSON` merge patch format and processing rules.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_region_autoscalers_patch_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_region_autoscalers_patch_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_autoscalers_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/regions/{region}/autoscalers
/// Updates an autoscaler in the specified project using the data included in the request. This method `supportsPATCH` semantics and uses `theJSON` merge patch format and processing rules.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_region_autoscalers_patch_builder()` + `compute_region_autoscalers_patch_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_autoscalers_patch(
    client: &SimpleHttpClient,
    project: &str,
    region: &str,
    autoscaler: Option<&str>,
    requestId: Option<&str>,
    body: &Autoscaler,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_region_autoscalers_patch_builder(
        client, project, region, autoscaler, requestId, body,
    )?;
    compute_region_autoscalers_patch_execute(builder)
}

/// GET projects/{project}/regions/{region}/autoscalers/{resource}/testIamPermissions
/// Returns permissions that a caller has on the specified resource.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_region_autoscalers_test_iam_permissions_execute()` to send, or `compute_region_autoscalers_test_iam_permissions` for simplest API.

pub fn compute_region_autoscalers_test_iam_permissions_builder(
    client: &SimpleHttpClient,
    project: &str,
    region: &str,
    resource: &str,
    body: &TestPermissionsRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/regions/{}/autoscalers/{}/testIamPermissions",
        project, region, resource,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET projects/{project}/regions/{region}/autoscalers/{resource}/testIamPermissions
/// Returns permissions that a caller has on the specified resource.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_region_autoscalers_test_iam_permissions_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_region_autoscalers_test_iam_permissions_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_autoscalers_test_iam_permissions_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<TestPermissionsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: TestPermissionsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/regions/{region}/autoscalers/{resource}/testIamPermissions
/// Returns permissions that a caller has on the specified resource.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_region_autoscalers_test_iam_permissions_builder()` + `compute_region_autoscalers_test_iam_permissions_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_autoscalers_test_iam_permissions(
    client: &SimpleHttpClient,
    project: &str,
    region: &str,
    resource: &str,
    body: &TestPermissionsRequest,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<TestPermissionsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = compute_region_autoscalers_test_iam_permissions_builder(
        client, project, region, resource, body,
    )?;
    compute_region_autoscalers_test_iam_permissions_execute(builder)
}

/// GET projects/{project}/regions/{region}/autoscalers
/// Updates an autoscaler in the specified project using the data included in the request.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_region_autoscalers_update_execute()` to send, or `compute_region_autoscalers_update` for simplest API.

pub fn compute_region_autoscalers_update_builder(
    client: &SimpleHttpClient,
    project: &str,
    region: &str,
    autoscaler: Option<&str>,
    requestId: Option<&str>,
    body: &Autoscaler,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/regions/{}/autoscalers",
        project, region,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "autoscaler",
            autoscaler
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "requestId",
            requestId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET projects/{project}/regions/{region}/autoscalers
/// Updates an autoscaler in the specified project using the data included in the request.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_region_autoscalers_update_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_region_autoscalers_update_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_autoscalers_update_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/regions/{region}/autoscalers
/// Updates an autoscaler in the specified project using the data included in the request.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_region_autoscalers_update_builder()` + `compute_region_autoscalers_update_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_autoscalers_update(
    client: &SimpleHttpClient,
    project: &str,
    region: &str,
    autoscaler: Option<&str>,
    requestId: Option<&str>,
    body: &Autoscaler,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_region_autoscalers_update_builder(
        client, project, region, autoscaler, requestId, body,
    )?;
    compute_region_autoscalers_update_execute(builder)
}

/// GET projects/{project}/regions/{region}/backendServices/{backendService}
/// Deletes the specified regional BackendService resource.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_region_backend_services_delete_execute()` to send, or `compute_region_backend_services_delete` for simplest API.

pub fn compute_region_backend_services_delete_builder(
    client: &SimpleHttpClient,
    backendService: &str,
    project: &str,
    region: &str,
    requestId: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/regions/{}/backendServices/{}",
        backendService, project, region,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "requestId",
            requestId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET projects/{project}/regions/{region}/backendServices/{backendService}
/// Deletes the specified regional BackendService resource.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_region_backend_services_delete_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_region_backend_services_delete_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_backend_services_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/regions/{region}/backendServices/{backendService}
/// Deletes the specified regional BackendService resource.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_region_backend_services_delete_builder()` + `compute_region_backend_services_delete_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_backend_services_delete(
    client: &SimpleHttpClient,
    backendService: &str,
    project: &str,
    region: &str,
    requestId: Option<&str>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_region_backend_services_delete_builder(
        client,
        backendService,
        project,
        region,
        requestId,
    )?;
    compute_region_backend_services_delete_execute(builder)
}

/// GET projects/{project}/regions/{region}/backendServices/{backendService}
/// Returns the specified regional BackendService resource.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_region_backend_services_get_execute()` to send, or `compute_region_backend_services_get` for simplest API.

pub fn compute_region_backend_services_get_builder(
    client: &SimpleHttpClient,
    backendService: &str,
    project: &str,
    region: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/regions/{}/backendServices/{}",
        backendService, project, region,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/regions/{region}/backendServices/{backendService}
/// Returns the specified regional BackendService resource.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_region_backend_services_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_region_backend_services_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_backend_services_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<BackendService>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: BackendService = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/regions/{region}/backendServices/{backendService}
/// Returns the specified regional BackendService resource.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_region_backend_services_get_builder()` + `compute_region_backend_services_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_backend_services_get(
    client: &SimpleHttpClient,
    backendService: &str,
    project: &str,
    region: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<BackendService>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        compute_region_backend_services_get_builder(client, backendService, project, region)?;
    compute_region_backend_services_get_execute(builder)
}

/// GET projects/{project}/regions/{region}/backendServices/{backendService}/getHealth
/// Gets the most recent health check results for this regional BackendService.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_region_backend_services_get_health_execute()` to send, or `compute_region_backend_services_get_health` for simplest API.

pub fn compute_region_backend_services_get_health_builder(
    client: &SimpleHttpClient,
    backendService: &str,
    project: &str,
    region: &str,
    body: &ResourceGroupReference,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/regions/{}/backendServices/{}/getHealth",
        backendService, project, region,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET projects/{project}/regions/{region}/backendServices/{backendService}/getHealth
/// Gets the most recent health check results for this regional BackendService.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_region_backend_services_get_health_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_region_backend_services_get_health_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_backend_services_get_health_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<BackendServiceGroupHealth>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: BackendServiceGroupHealth = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/regions/{region}/backendServices/{backendService}/getHealth
/// Gets the most recent health check results for this regional BackendService.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_region_backend_services_get_health_builder()` + `compute_region_backend_services_get_health_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_backend_services_get_health(
    client: &SimpleHttpClient,
    backendService: &str,
    project: &str,
    region: &str,
    body: &ResourceGroupReference,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<BackendServiceGroupHealth>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = compute_region_backend_services_get_health_builder(
        client,
        backendService,
        project,
        region,
        body,
    )?;
    compute_region_backend_services_get_health_execute(builder)
}

/// GET projects/{project}/regions/{region}/backendServices/{resource}/getIamPolicy
/// Gets the access control policy for a resource. May be empty if no such policy or resource exists.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_region_backend_services_get_iam_policy_execute()` to send, or `compute_region_backend_services_get_iam_policy` for simplest API.

pub fn compute_region_backend_services_get_iam_policy_builder(
    client: &SimpleHttpClient,
    project: &str,
    region: &str,
    resource: &str,
    optionsRequestedPolicyVersion: Option<i32>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/regions/{}/backendServices/{}/getIamPolicy",
        project, region, resource,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "optionsRequestedPolicyVersion",
            optionsRequestedPolicyVersion
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET projects/{project}/regions/{region}/backendServices/{resource}/getIamPolicy
/// Gets the access control policy for a resource. May be empty if no such policy or resource exists.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_region_backend_services_get_iam_policy_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_region_backend_services_get_iam_policy_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_backend_services_get_iam_policy_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Policy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Policy = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/regions/{region}/backendServices/{resource}/getIamPolicy
/// Gets the access control policy for a resource. May be empty if no such policy or resource exists.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_region_backend_services_get_iam_policy_builder()` + `compute_region_backend_services_get_iam_policy_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_backend_services_get_iam_policy(
    client: &SimpleHttpClient,
    project: &str,
    region: &str,
    resource: &str,
    optionsRequestedPolicyVersion: Option<i32>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Policy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_region_backend_services_get_iam_policy_builder(
        client,
        project,
        region,
        resource,
        optionsRequestedPolicyVersion,
    )?;
    compute_region_backend_services_get_iam_policy_execute(builder)
}

/// GET projects/{project}/regions/{region}/backendServices
/// Creates a regional BackendService resource in the specified project using the data included in the request. For more information, see Backend services overview.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_region_backend_services_insert_execute()` to send, or `compute_region_backend_services_insert` for simplest API.

pub fn compute_region_backend_services_insert_builder(
    client: &SimpleHttpClient,
    project: &str,
    region: &str,
    requestId: Option<&str>,
    body: &BackendService,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/regions/{}/backendServices",
        project, region,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "requestId",
            requestId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET projects/{project}/regions/{region}/backendServices
/// Creates a regional BackendService resource in the specified project using the data included in the request. For more information, see Backend services overview.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_region_backend_services_insert_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_region_backend_services_insert_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_backend_services_insert_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/regions/{region}/backendServices
/// Creates a regional BackendService resource in the specified project using the data included in the request. For more information, see Backend services overview.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_region_backend_services_insert_builder()` + `compute_region_backend_services_insert_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_backend_services_insert(
    client: &SimpleHttpClient,
    project: &str,
    region: &str,
    requestId: Option<&str>,
    body: &BackendService,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        compute_region_backend_services_insert_builder(client, project, region, requestId, body)?;
    compute_region_backend_services_insert_execute(builder)
}

/// GET projects/{project}/regions/{region}/backendServices
/// Retrieves the list of regional BackendService resources available to the specified project in the given region.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_region_backend_services_list_execute()` to send, or `compute_region_backend_services_list` for simplest API.

pub fn compute_region_backend_services_list_builder(
    client: &SimpleHttpClient,
    project: &str,
    region: &str,
    filter: Option<&str>,
    maxResults: Option<i32>,
    orderBy: Option<&str>,
    pageToken: Option<&str>,
    returnPartialSuccess: Option<bool>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/regions/{}/backendServices",
        project, region,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "filter",
            filter
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "maxResults",
            maxResults
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "orderBy",
            orderBy
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageToken",
            pageToken
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "returnPartialSuccess",
            returnPartialSuccess
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET projects/{project}/regions/{region}/backendServices
/// Retrieves the list of regional BackendService resources available to the specified project in the given region.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_region_backend_services_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_region_backend_services_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_backend_services_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<BackendServiceList>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: BackendServiceList = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/regions/{region}/backendServices
/// Retrieves the list of regional BackendService resources available to the specified project in the given region.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_region_backend_services_list_builder()` + `compute_region_backend_services_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_backend_services_list(
    client: &SimpleHttpClient,
    project: &str,
    region: &str,
    filter: Option<&str>,
    maxResults: Option<i32>,
    orderBy: Option<&str>,
    pageToken: Option<&str>,
    returnPartialSuccess: Option<bool>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<BackendServiceList>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = compute_region_backend_services_list_builder(
        client,
        project,
        region,
        filter,
        maxResults,
        orderBy,
        pageToken,
        returnPartialSuccess,
    )?;
    compute_region_backend_services_list_execute(builder)
}

/// GET projects/{project}/regions/{region}/backendServices/listUsable
/// Retrieves a list of all usable backend services in the specified project in the given region.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_region_backend_services_list_usable_execute()` to send, or `compute_region_backend_services_list_usable` for simplest API.

pub fn compute_region_backend_services_list_usable_builder(
    client: &SimpleHttpClient,
    project: &str,
    region: &str,
    filter: Option<&str>,
    maxResults: Option<i32>,
    orderBy: Option<&str>,
    pageToken: Option<&str>,
    returnPartialSuccess: Option<bool>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/regions/{}/backendServices/listUsable",
        project, region,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "filter",
            filter
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "maxResults",
            maxResults
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "orderBy",
            orderBy
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageToken",
            pageToken
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "returnPartialSuccess",
            returnPartialSuccess
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET projects/{project}/regions/{region}/backendServices/listUsable
/// Retrieves a list of all usable backend services in the specified project in the given region.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_region_backend_services_list_usable_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_region_backend_services_list_usable_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_backend_services_list_usable_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<BackendServiceListUsable>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: BackendServiceListUsable = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/regions/{region}/backendServices/listUsable
/// Retrieves a list of all usable backend services in the specified project in the given region.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_region_backend_services_list_usable_builder()` + `compute_region_backend_services_list_usable_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_backend_services_list_usable(
    client: &SimpleHttpClient,
    project: &str,
    region: &str,
    filter: Option<&str>,
    maxResults: Option<i32>,
    orderBy: Option<&str>,
    pageToken: Option<&str>,
    returnPartialSuccess: Option<bool>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<BackendServiceListUsable>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = compute_region_backend_services_list_usable_builder(
        client,
        project,
        region,
        filter,
        maxResults,
        orderBy,
        pageToken,
        returnPartialSuccess,
    )?;
    compute_region_backend_services_list_usable_execute(builder)
}

/// GET projects/{project}/regions/{region}/backendServices/{backendService}
/// Updates the specified regional BackendService resource with the data included in the request. For more information, see Understanding backend services This method supports PATCH semantics and uses the JSON merge patch format and processing rules.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_region_backend_services_patch_execute()` to send, or `compute_region_backend_services_patch` for simplest API.

pub fn compute_region_backend_services_patch_builder(
    client: &SimpleHttpClient,
    backendService: &str,
    project: &str,
    region: &str,
    requestId: Option<&str>,
    body: &BackendService,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/regions/{}/backendServices/{}",
        backendService, project, region,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "requestId",
            requestId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET projects/{project}/regions/{region}/backendServices/{backendService}
/// Updates the specified regional BackendService resource with the data included in the request. For more information, see Understanding backend services This method supports PATCH semantics and uses the JSON merge patch format and processing rules.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_region_backend_services_patch_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_region_backend_services_patch_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_backend_services_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/regions/{region}/backendServices/{backendService}
/// Updates the specified regional BackendService resource with the data included in the request. For more information, see Understanding backend services This method supports PATCH semantics and uses the JSON merge patch format and processing rules.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_region_backend_services_patch_builder()` + `compute_region_backend_services_patch_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_backend_services_patch(
    client: &SimpleHttpClient,
    backendService: &str,
    project: &str,
    region: &str,
    requestId: Option<&str>,
    body: &BackendService,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_region_backend_services_patch_builder(
        client,
        backendService,
        project,
        region,
        requestId,
        body,
    )?;
    compute_region_backend_services_patch_execute(builder)
}

/// GET projects/{project}/regions/{region}/backendServices/{resource}/setIamPolicy
/// Sets the access control policy on the specified resource. Replaces any existing policy.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_region_backend_services_set_iam_policy_execute()` to send, or `compute_region_backend_services_set_iam_policy` for simplest API.

pub fn compute_region_backend_services_set_iam_policy_builder(
    client: &SimpleHttpClient,
    project: &str,
    region: &str,
    resource: &str,
    body: &RegionSetPolicyRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/regions/{}/backendServices/{}/setIamPolicy",
        project, region, resource,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET projects/{project}/regions/{region}/backendServices/{resource}/setIamPolicy
/// Sets the access control policy on the specified resource. Replaces any existing policy.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_region_backend_services_set_iam_policy_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_region_backend_services_set_iam_policy_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_backend_services_set_iam_policy_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Policy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Policy = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/regions/{region}/backendServices/{resource}/setIamPolicy
/// Sets the access control policy on the specified resource. Replaces any existing policy.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_region_backend_services_set_iam_policy_builder()` + `compute_region_backend_services_set_iam_policy_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_backend_services_set_iam_policy(
    client: &SimpleHttpClient,
    project: &str,
    region: &str,
    resource: &str,
    body: &RegionSetPolicyRequest,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Policy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_region_backend_services_set_iam_policy_builder(
        client, project, region, resource, body,
    )?;
    compute_region_backend_services_set_iam_policy_execute(builder)
}

/// GET projects/{project}/regions/{region}/backendServices/{backendService}/setSecurityPolicy
/// Sets the Google Cloud Armor security policy for the specified backend service. For more information, `seeGoogle` Cloud Armor Overview
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_region_backend_services_set_security_policy_execute()` to send, or `compute_region_backend_services_set_security_policy` for simplest API.

pub fn compute_region_backend_services_set_security_policy_builder(
    client: &SimpleHttpClient,
    backendService: &str,
    project: &str,
    region: &str,
    requestId: Option<&str>,
    body: &SecurityPolicyReference,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/regions/{}/backendServices/{}/setSecurityPolicy",
        backendService, project, region,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "requestId",
            requestId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET projects/{project}/regions/{region}/backendServices/{backendService}/setSecurityPolicy
/// Sets the Google Cloud Armor security policy for the specified backend service. For more information, `seeGoogle` Cloud Armor Overview
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_region_backend_services_set_security_policy_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_region_backend_services_set_security_policy_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_backend_services_set_security_policy_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/regions/{region}/backendServices/{backendService}/setSecurityPolicy
/// Sets the Google Cloud Armor security policy for the specified backend service. For more information, `seeGoogle` Cloud Armor Overview
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_region_backend_services_set_security_policy_builder()` + `compute_region_backend_services_set_security_policy_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_backend_services_set_security_policy(
    client: &SimpleHttpClient,
    backendService: &str,
    project: &str,
    region: &str,
    requestId: Option<&str>,
    body: &SecurityPolicyReference,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_region_backend_services_set_security_policy_builder(
        client,
        backendService,
        project,
        region,
        requestId,
        body,
    )?;
    compute_region_backend_services_set_security_policy_execute(builder)
}

/// GET projects/{project}/regions/{region}/backendServices/{resource}/testIamPermissions
/// Returns permissions that a caller has on the specified resource.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_region_backend_services_test_iam_permissions_execute()` to send, or `compute_region_backend_services_test_iam_permissions` for simplest API.

pub fn compute_region_backend_services_test_iam_permissions_builder(
    client: &SimpleHttpClient,
    project: &str,
    region: &str,
    resource: &str,
    body: &TestPermissionsRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/regions/{}/backendServices/{}/testIamPermissions",
        project, region, resource,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET projects/{project}/regions/{region}/backendServices/{resource}/testIamPermissions
/// Returns permissions that a caller has on the specified resource.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_region_backend_services_test_iam_permissions_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_region_backend_services_test_iam_permissions_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_backend_services_test_iam_permissions_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<TestPermissionsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: TestPermissionsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/regions/{region}/backendServices/{resource}/testIamPermissions
/// Returns permissions that a caller has on the specified resource.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_region_backend_services_test_iam_permissions_builder()` + `compute_region_backend_services_test_iam_permissions_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_backend_services_test_iam_permissions(
    client: &SimpleHttpClient,
    project: &str,
    region: &str,
    resource: &str,
    body: &TestPermissionsRequest,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<TestPermissionsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = compute_region_backend_services_test_iam_permissions_builder(
        client, project, region, resource, body,
    )?;
    compute_region_backend_services_test_iam_permissions_execute(builder)
}

/// GET projects/{project}/regions/{region}/backendServices/{backendService}
/// Updates the specified regional BackendService resource with the data included in the request. For more information, see Backend services overview.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_region_backend_services_update_execute()` to send, or `compute_region_backend_services_update` for simplest API.

pub fn compute_region_backend_services_update_builder(
    client: &SimpleHttpClient,
    backendService: &str,
    project: &str,
    region: &str,
    requestId: Option<&str>,
    body: &BackendService,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/regions/{}/backendServices/{}",
        backendService, project, region,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "requestId",
            requestId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET projects/{project}/regions/{region}/backendServices/{backendService}
/// Updates the specified regional BackendService resource with the data included in the request. For more information, see Backend services overview.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_region_backend_services_update_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_region_backend_services_update_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_backend_services_update_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/regions/{region}/backendServices/{backendService}
/// Updates the specified regional BackendService resource with the data included in the request. For more information, see Backend services overview.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_region_backend_services_update_builder()` + `compute_region_backend_services_update_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_backend_services_update(
    client: &SimpleHttpClient,
    backendService: &str,
    project: &str,
    region: &str,
    requestId: Option<&str>,
    body: &BackendService,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_region_backend_services_update_builder(
        client,
        backendService,
        project,
        region,
        requestId,
        body,
    )?;
    compute_region_backend_services_update_execute(builder)
}

/// GET projects/{project}/aggregated/commitments
/// Retrieves an aggregated list of commitments by region.  To prevent failure, it is recommended that you set the `returnPartialSuccess` parameter to `true`.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_region_commitments_aggregated_list_execute()` to send, or `compute_region_commitments_aggregated_list` for simplest API.

pub fn compute_region_commitments_aggregated_list_builder(
    client: &SimpleHttpClient,
    project: &str,
    filter: Option<&str>,
    includeAllScopes: Option<bool>,
    maxResults: Option<i32>,
    orderBy: Option<&str>,
    pageToken: Option<&str>,
    returnPartialSuccess: Option<bool>,
    serviceProjectNumber: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/aggregated/commitments",
        project,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "filter",
            filter
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "includeAllScopes",
            includeAllScopes
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "maxResults",
            maxResults
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "orderBy",
            orderBy
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageToken",
            pageToken
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "returnPartialSuccess",
            returnPartialSuccess
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "serviceProjectNumber",
            serviceProjectNumber
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET projects/{project}/aggregated/commitments
/// Retrieves an aggregated list of commitments by region.  To prevent failure, it is recommended that you set the `returnPartialSuccess` parameter to `true`.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_region_commitments_aggregated_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_region_commitments_aggregated_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_commitments_aggregated_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<CommitmentAggregatedList>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: CommitmentAggregatedList = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/aggregated/commitments
/// Retrieves an aggregated list of commitments by region.  To prevent failure, it is recommended that you set the `returnPartialSuccess` parameter to `true`.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_region_commitments_aggregated_list_builder()` + `compute_region_commitments_aggregated_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_commitments_aggregated_list(
    client: &SimpleHttpClient,
    project: &str,
    filter: Option<&str>,
    includeAllScopes: Option<bool>,
    maxResults: Option<i32>,
    orderBy: Option<&str>,
    pageToken: Option<&str>,
    returnPartialSuccess: Option<bool>,
    serviceProjectNumber: Option<&str>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<CommitmentAggregatedList>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = compute_region_commitments_aggregated_list_builder(
        client,
        project,
        filter,
        includeAllScopes,
        maxResults,
        orderBy,
        pageToken,
        returnPartialSuccess,
        serviceProjectNumber,
    )?;
    compute_region_commitments_aggregated_list_execute(builder)
}

/// GET projects/{project}/regions/{region}/commitments/{commitment}
/// Returns the specified commitment resource.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_region_commitments_get_execute()` to send, or `compute_region_commitments_get` for simplest API.

pub fn compute_region_commitments_get_builder(
    client: &SimpleHttpClient,
    commitment: &str,
    project: &str,
    region: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/regions/{}/commitments/{}",
        commitment, project, region,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/regions/{region}/commitments/{commitment}
/// Returns the specified commitment resource.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_region_commitments_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_region_commitments_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_commitments_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Commitment>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Commitment = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/regions/{region}/commitments/{commitment}
/// Returns the specified commitment resource.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_region_commitments_get_builder()` + `compute_region_commitments_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_commitments_get(
    client: &SimpleHttpClient,
    commitment: &str,
    project: &str,
    region: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Commitment>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_region_commitments_get_builder(client, commitment, project, region)?;
    compute_region_commitments_get_execute(builder)
}

/// GET projects/{project}/regions/{region}/commitments
/// Creates a commitment in the specified project using the data included in the request.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_region_commitments_insert_execute()` to send, or `compute_region_commitments_insert` for simplest API.

pub fn compute_region_commitments_insert_builder(
    client: &SimpleHttpClient,
    project: &str,
    region: &str,
    requestId: Option<&str>,
    body: &Commitment,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/regions/{}/commitments",
        project, region,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "requestId",
            requestId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET projects/{project}/regions/{region}/commitments
/// Creates a commitment in the specified project using the data included in the request.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_region_commitments_insert_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_region_commitments_insert_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_commitments_insert_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/regions/{region}/commitments
/// Creates a commitment in the specified project using the data included in the request.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_region_commitments_insert_builder()` + `compute_region_commitments_insert_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_commitments_insert(
    client: &SimpleHttpClient,
    project: &str,
    region: &str,
    requestId: Option<&str>,
    body: &Commitment,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        compute_region_commitments_insert_builder(client, project, region, requestId, body)?;
    compute_region_commitments_insert_execute(builder)
}

/// GET projects/{project}/regions/{region}/commitments
/// Retrieves a list of commitments contained within the specified region.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_region_commitments_list_execute()` to send, or `compute_region_commitments_list` for simplest API.

pub fn compute_region_commitments_list_builder(
    client: &SimpleHttpClient,
    project: &str,
    region: &str,
    filter: Option<&str>,
    maxResults: Option<i32>,
    orderBy: Option<&str>,
    pageToken: Option<&str>,
    returnPartialSuccess: Option<bool>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/regions/{}/commitments",
        project, region,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "filter",
            filter
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "maxResults",
            maxResults
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "orderBy",
            orderBy
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageToken",
            pageToken
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "returnPartialSuccess",
            returnPartialSuccess
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET projects/{project}/regions/{region}/commitments
/// Retrieves a list of commitments contained within the specified region.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_region_commitments_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_region_commitments_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_commitments_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<CommitmentList>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: CommitmentList = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/regions/{region}/commitments
/// Retrieves a list of commitments contained within the specified region.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_region_commitments_list_builder()` + `compute_region_commitments_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_commitments_list(
    client: &SimpleHttpClient,
    project: &str,
    region: &str,
    filter: Option<&str>,
    maxResults: Option<i32>,
    orderBy: Option<&str>,
    pageToken: Option<&str>,
    returnPartialSuccess: Option<bool>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<CommitmentList>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = compute_region_commitments_list_builder(
        client,
        project,
        region,
        filter,
        maxResults,
        orderBy,
        pageToken,
        returnPartialSuccess,
    )?;
    compute_region_commitments_list_execute(builder)
}

/// GET projects/{project}/regions/{region}/commitments/{commitment}
/// Updates the specified commitment with the data included in the request. Update is performed only on selected fields included as part of update-mask. Only the following fields can be updated: auto_renew and plan.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_region_commitments_update_execute()` to send, or `compute_region_commitments_update` for simplest API.

pub fn compute_region_commitments_update_builder(
    client: &SimpleHttpClient,
    commitment: &str,
    project: &str,
    region: &str,
    paths: Option<&str>,
    requestId: Option<&str>,
    updateMask: Option<&str>,
    body: &Commitment,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/regions/{}/commitments/{}",
        commitment, project, region,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "paths",
            paths
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "requestId",
            requestId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "updateMask",
            updateMask
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET projects/{project}/regions/{region}/commitments/{commitment}
/// Updates the specified commitment with the data included in the request. Update is performed only on selected fields included as part of update-mask. Only the following fields can be updated: auto_renew and plan.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_region_commitments_update_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_region_commitments_update_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_commitments_update_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/regions/{region}/commitments/{commitment}
/// Updates the specified commitment with the data included in the request. Update is performed only on selected fields included as part of update-mask. Only the following fields can be updated: auto_renew and plan.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_region_commitments_update_builder()` + `compute_region_commitments_update_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_commitments_update(
    client: &SimpleHttpClient,
    commitment: &str,
    project: &str,
    region: &str,
    paths: Option<&str>,
    requestId: Option<&str>,
    updateMask: Option<&str>,
    body: &Commitment,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_region_commitments_update_builder(
        client, commitment, project, region, paths, requestId, updateMask, body,
    )?;
    compute_region_commitments_update_execute(builder)
}

/// GET projects/{project}/aggregated/compositeHealthChecks
/// Retrieves the list of all CompositeHealthCheck resources (all regional) available to the specified project.  To prevent failure, it is recommended that you set the `returnPartialSuccess` parameter to `true`.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_region_composite_health_checks_aggregated_list_execute()` to send, or `compute_region_composite_health_checks_aggregated_list` for simplest API.

pub fn compute_region_composite_health_checks_aggregated_list_builder(
    client: &SimpleHttpClient,
    project: &str,
    filter: Option<&str>,
    includeAllScopes: Option<bool>,
    maxResults: Option<i32>,
    orderBy: Option<&str>,
    pageToken: Option<&str>,
    returnPartialSuccess: Option<bool>,
    serviceProjectNumber: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/aggregated/compositeHealthChecks",
        project,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "filter",
            filter
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "includeAllScopes",
            includeAllScopes
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "maxResults",
            maxResults
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "orderBy",
            orderBy
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageToken",
            pageToken
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "returnPartialSuccess",
            returnPartialSuccess
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "serviceProjectNumber",
            serviceProjectNumber
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET projects/{project}/aggregated/compositeHealthChecks
/// Retrieves the list of all CompositeHealthCheck resources (all regional) available to the specified project.  To prevent failure, it is recommended that you set the `returnPartialSuccess` parameter to `true`.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_region_composite_health_checks_aggregated_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_region_composite_health_checks_aggregated_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_composite_health_checks_aggregated_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<CompositeHealthCheckAggregatedList>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: CompositeHealthCheckAggregatedList = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/aggregated/compositeHealthChecks
/// Retrieves the list of all CompositeHealthCheck resources (all regional) available to the specified project.  To prevent failure, it is recommended that you set the `returnPartialSuccess` parameter to `true`.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_region_composite_health_checks_aggregated_list_builder()` + `compute_region_composite_health_checks_aggregated_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_composite_health_checks_aggregated_list(
    client: &SimpleHttpClient,
    project: &str,
    filter: Option<&str>,
    includeAllScopes: Option<bool>,
    maxResults: Option<i32>,
    orderBy: Option<&str>,
    pageToken: Option<&str>,
    returnPartialSuccess: Option<bool>,
    serviceProjectNumber: Option<&str>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<CompositeHealthCheckAggregatedList>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = compute_region_composite_health_checks_aggregated_list_builder(
        client,
        project,
        filter,
        includeAllScopes,
        maxResults,
        orderBy,
        pageToken,
        returnPartialSuccess,
        serviceProjectNumber,
    )?;
    compute_region_composite_health_checks_aggregated_list_execute(builder)
}

/// GET projects/{project}/regions/{region}/compositeHealthChecks/{compositeHealthCheck}
/// Deletes the specified CompositeHealthCheck in the given region
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_region_composite_health_checks_delete_execute()` to send, or `compute_region_composite_health_checks_delete` for simplest API.

pub fn compute_region_composite_health_checks_delete_builder(
    client: &SimpleHttpClient,
    compositeHealthCheck: &str,
    project: &str,
    region: &str,
    requestId: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/regions/{}/compositeHealthChecks/{}",
        compositeHealthCheck, project, region,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "requestId",
            requestId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET projects/{project}/regions/{region}/compositeHealthChecks/{compositeHealthCheck}
/// Deletes the specified CompositeHealthCheck in the given region
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_region_composite_health_checks_delete_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_region_composite_health_checks_delete_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_composite_health_checks_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/regions/{region}/compositeHealthChecks/{compositeHealthCheck}
/// Deletes the specified CompositeHealthCheck in the given region
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_region_composite_health_checks_delete_builder()` + `compute_region_composite_health_checks_delete_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_composite_health_checks_delete(
    client: &SimpleHttpClient,
    compositeHealthCheck: &str,
    project: &str,
    region: &str,
    requestId: Option<&str>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_region_composite_health_checks_delete_builder(
        client,
        compositeHealthCheck,
        project,
        region,
        requestId,
    )?;
    compute_region_composite_health_checks_delete_execute(builder)
}

/// GET projects/{project}/regions/{region}/compositeHealthChecks/{compositeHealthCheck}
/// Returns the specified CompositeHealthCheck resource in the given region.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_region_composite_health_checks_get_execute()` to send, or `compute_region_composite_health_checks_get` for simplest API.

pub fn compute_region_composite_health_checks_get_builder(
    client: &SimpleHttpClient,
    compositeHealthCheck: &str,
    project: &str,
    region: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/regions/{}/compositeHealthChecks/{}",
        compositeHealthCheck, project, region,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/regions/{region}/compositeHealthChecks/{compositeHealthCheck}
/// Returns the specified CompositeHealthCheck resource in the given region.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_region_composite_health_checks_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_region_composite_health_checks_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_composite_health_checks_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<CompositeHealthCheck>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: CompositeHealthCheck = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/regions/{region}/compositeHealthChecks/{compositeHealthCheck}
/// Returns the specified CompositeHealthCheck resource in the given region.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_region_composite_health_checks_get_builder()` + `compute_region_composite_health_checks_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_composite_health_checks_get(
    client: &SimpleHttpClient,
    compositeHealthCheck: &str,
    project: &str,
    region: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<CompositeHealthCheck>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = compute_region_composite_health_checks_get_builder(
        client,
        compositeHealthCheck,
        project,
        region,
    )?;
    compute_region_composite_health_checks_get_execute(builder)
}

/// GET projects/{project}/regions/{region}/compositeHealthChecks
/// Create a CompositeHealthCheck in the specified project in the given region using the parameters that are included in the request.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_region_composite_health_checks_insert_execute()` to send, or `compute_region_composite_health_checks_insert` for simplest API.

pub fn compute_region_composite_health_checks_insert_builder(
    client: &SimpleHttpClient,
    project: &str,
    region: &str,
    requestId: Option<&str>,
    body: &CompositeHealthCheck,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/regions/{}/compositeHealthChecks",
        project, region,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "requestId",
            requestId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET projects/{project}/regions/{region}/compositeHealthChecks
/// Create a CompositeHealthCheck in the specified project in the given region using the parameters that are included in the request.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_region_composite_health_checks_insert_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_region_composite_health_checks_insert_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_composite_health_checks_insert_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/regions/{region}/compositeHealthChecks
/// Create a CompositeHealthCheck in the specified project in the given region using the parameters that are included in the request.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_region_composite_health_checks_insert_builder()` + `compute_region_composite_health_checks_insert_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_composite_health_checks_insert(
    client: &SimpleHttpClient,
    project: &str,
    region: &str,
    requestId: Option<&str>,
    body: &CompositeHealthCheck,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_region_composite_health_checks_insert_builder(
        client, project, region, requestId, body,
    )?;
    compute_region_composite_health_checks_insert_execute(builder)
}

/// GET projects/{project}/regions/{region}/compositeHealthChecks
/// Lists the CompositeHealthChecks for a project in the given region.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_region_composite_health_checks_list_execute()` to send, or `compute_region_composite_health_checks_list` for simplest API.

pub fn compute_region_composite_health_checks_list_builder(
    client: &SimpleHttpClient,
    project: &str,
    region: &str,
    filter: Option<&str>,
    maxResults: Option<i32>,
    orderBy: Option<&str>,
    pageToken: Option<&str>,
    returnPartialSuccess: Option<bool>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/regions/{}/compositeHealthChecks",
        project, region,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "filter",
            filter
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "maxResults",
            maxResults
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "orderBy",
            orderBy
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageToken",
            pageToken
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "returnPartialSuccess",
            returnPartialSuccess
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET projects/{project}/regions/{region}/compositeHealthChecks
/// Lists the CompositeHealthChecks for a project in the given region.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_region_composite_health_checks_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_region_composite_health_checks_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_composite_health_checks_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<CompositeHealthCheckList>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: CompositeHealthCheckList = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/regions/{region}/compositeHealthChecks
/// Lists the CompositeHealthChecks for a project in the given region.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_region_composite_health_checks_list_builder()` + `compute_region_composite_health_checks_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_composite_health_checks_list(
    client: &SimpleHttpClient,
    project: &str,
    region: &str,
    filter: Option<&str>,
    maxResults: Option<i32>,
    orderBy: Option<&str>,
    pageToken: Option<&str>,
    returnPartialSuccess: Option<bool>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<CompositeHealthCheckList>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = compute_region_composite_health_checks_list_builder(
        client,
        project,
        region,
        filter,
        maxResults,
        orderBy,
        pageToken,
        returnPartialSuccess,
    )?;
    compute_region_composite_health_checks_list_execute(builder)
}

/// GET projects/{project}/regions/{region}/compositeHealthChecks/{compositeHealthCheck}
/// Updates the specified regional CompositeHealthCheck resource with the data included in the request.  This method `supportsPATCH` semantics and uses `theJSON` merge patch format and processing rules.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_region_composite_health_checks_patch_execute()` to send, or `compute_region_composite_health_checks_patch` for simplest API.

pub fn compute_region_composite_health_checks_patch_builder(
    client: &SimpleHttpClient,
    compositeHealthCheck: &str,
    project: &str,
    region: &str,
    requestId: Option<&str>,
    body: &CompositeHealthCheck,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/regions/{}/compositeHealthChecks/{}",
        compositeHealthCheck, project, region,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "requestId",
            requestId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET projects/{project}/regions/{region}/compositeHealthChecks/{compositeHealthCheck}
/// Updates the specified regional CompositeHealthCheck resource with the data included in the request.  This method `supportsPATCH` semantics and uses `theJSON` merge patch format and processing rules.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_region_composite_health_checks_patch_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_region_composite_health_checks_patch_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_composite_health_checks_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/regions/{region}/compositeHealthChecks/{compositeHealthCheck}
/// Updates the specified regional CompositeHealthCheck resource with the data included in the request.  This method `supportsPATCH` semantics and uses `theJSON` merge patch format and processing rules.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_region_composite_health_checks_patch_builder()` + `compute_region_composite_health_checks_patch_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_composite_health_checks_patch(
    client: &SimpleHttpClient,
    compositeHealthCheck: &str,
    project: &str,
    region: &str,
    requestId: Option<&str>,
    body: &CompositeHealthCheck,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_region_composite_health_checks_patch_builder(
        client,
        compositeHealthCheck,
        project,
        region,
        requestId,
        body,
    )?;
    compute_region_composite_health_checks_patch_execute(builder)
}

/// GET projects/{project}/regions/{region}/compositeHealthChecks/{resource}/testIamPermissions
/// Returns permissions that a caller has on the specified resource.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_region_composite_health_checks_test_iam_permissions_execute()` to send, or `compute_region_composite_health_checks_test_iam_permissions` for simplest API.

pub fn compute_region_composite_health_checks_test_iam_permissions_builder(
    client: &SimpleHttpClient,
    project: &str,
    region: &str,
    resource: &str,
    body: &TestPermissionsRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/regions/{}/compositeHealthChecks/{}/testIamPermissions",
        project, region, resource,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET projects/{project}/regions/{region}/compositeHealthChecks/{resource}/testIamPermissions
/// Returns permissions that a caller has on the specified resource.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_region_composite_health_checks_test_iam_permissions_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_region_composite_health_checks_test_iam_permissions_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_composite_health_checks_test_iam_permissions_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<TestPermissionsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: TestPermissionsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/regions/{region}/compositeHealthChecks/{resource}/testIamPermissions
/// Returns permissions that a caller has on the specified resource.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_region_composite_health_checks_test_iam_permissions_builder()` + `compute_region_composite_health_checks_test_iam_permissions_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_composite_health_checks_test_iam_permissions(
    client: &SimpleHttpClient,
    project: &str,
    region: &str,
    resource: &str,
    body: &TestPermissionsRequest,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<TestPermissionsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = compute_region_composite_health_checks_test_iam_permissions_builder(
        client, project, region, resource, body,
    )?;
    compute_region_composite_health_checks_test_iam_permissions_execute(builder)
}

/// GET projects/{project}/regions/{region}/diskTypes/{diskType}
/// Returns the specified regional disk type.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_region_disk_types_get_execute()` to send, or `compute_region_disk_types_get` for simplest API.

pub fn compute_region_disk_types_get_builder(
    client: &SimpleHttpClient,
    diskType: &str,
    project: &str,
    region: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/regions/{}/diskTypes/{}",
        diskType, project, region,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/regions/{region}/diskTypes/{diskType}
/// Returns the specified regional disk type.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_region_disk_types_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_region_disk_types_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_disk_types_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<DiskType>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: DiskType = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/regions/{region}/diskTypes/{diskType}
/// Returns the specified regional disk type.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_region_disk_types_get_builder()` + `compute_region_disk_types_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_disk_types_get(
    client: &SimpleHttpClient,
    diskType: &str,
    project: &str,
    region: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<DiskType>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_region_disk_types_get_builder(client, diskType, project, region)?;
    compute_region_disk_types_get_execute(builder)
}

/// GET projects/{project}/regions/{region}/diskTypes
/// Retrieves a list of regional disk types available to the specified project.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_region_disk_types_list_execute()` to send, or `compute_region_disk_types_list` for simplest API.

pub fn compute_region_disk_types_list_builder(
    client: &SimpleHttpClient,
    project: &str,
    region: &str,
    filter: Option<&str>,
    maxResults: Option<i32>,
    orderBy: Option<&str>,
    pageToken: Option<&str>,
    returnPartialSuccess: Option<bool>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/regions/{}/diskTypes",
        project, region,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "filter",
            filter
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "maxResults",
            maxResults
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "orderBy",
            orderBy
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageToken",
            pageToken
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "returnPartialSuccess",
            returnPartialSuccess
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET projects/{project}/regions/{region}/diskTypes
/// Retrieves a list of regional disk types available to the specified project.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_region_disk_types_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_region_disk_types_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_disk_types_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<RegionDiskTypeList>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: RegionDiskTypeList = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/regions/{region}/diskTypes
/// Retrieves a list of regional disk types available to the specified project.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_region_disk_types_list_builder()` + `compute_region_disk_types_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_disk_types_list(
    client: &SimpleHttpClient,
    project: &str,
    region: &str,
    filter: Option<&str>,
    maxResults: Option<i32>,
    orderBy: Option<&str>,
    pageToken: Option<&str>,
    returnPartialSuccess: Option<bool>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<RegionDiskTypeList>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = compute_region_disk_types_list_builder(
        client,
        project,
        region,
        filter,
        maxResults,
        orderBy,
        pageToken,
        returnPartialSuccess,
    )?;
    compute_region_disk_types_list_execute(builder)
}

/// GET projects/{project}/regions/{region}/disks/{disk}/addResourcePolicies
/// Adds existing resource policies to a regional disk. You can only add one policy which will be applied to this disk for scheduling snapshot creation.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_region_disks_add_resource_policies_execute()` to send, or `compute_region_disks_add_resource_policies` for simplest API.

pub fn compute_region_disks_add_resource_policies_builder(
    client: &SimpleHttpClient,
    disk: &str,
    project: &str,
    region: &str,
    requestId: Option<&str>,
    body: &RegionDisksAddResourcePoliciesRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/regions/{}/disks/{}/addResourcePolicies",
        disk, project, region,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "requestId",
            requestId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET projects/{project}/regions/{region}/disks/{disk}/addResourcePolicies
/// Adds existing resource policies to a regional disk. You can only add one policy which will be applied to this disk for scheduling snapshot creation.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_region_disks_add_resource_policies_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_region_disks_add_resource_policies_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_disks_add_resource_policies_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/regions/{region}/disks/{disk}/addResourcePolicies
/// Adds existing resource policies to a regional disk. You can only add one policy which will be applied to this disk for scheduling snapshot creation.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_region_disks_add_resource_policies_builder()` + `compute_region_disks_add_resource_policies_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_disks_add_resource_policies(
    client: &SimpleHttpClient,
    disk: &str,
    project: &str,
    region: &str,
    requestId: Option<&str>,
    body: &RegionDisksAddResourcePoliciesRequest,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_region_disks_add_resource_policies_builder(
        client, disk, project, region, requestId, body,
    )?;
    compute_region_disks_add_resource_policies_execute(builder)
}

/// GET projects/{project}/regions/{region}/disks/bulkInsert
/// Bulk create a set of disks.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_region_disks_bulk_insert_execute()` to send, or `compute_region_disks_bulk_insert` for simplest API.

pub fn compute_region_disks_bulk_insert_builder(
    client: &SimpleHttpClient,
    project: &str,
    region: &str,
    requestId: Option<&str>,
    body: &BulkInsertDiskResource,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/regions/{}/disks/bulkInsert",
        project, region,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "requestId",
            requestId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET projects/{project}/regions/{region}/disks/bulkInsert
/// Bulk create a set of disks.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_region_disks_bulk_insert_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_region_disks_bulk_insert_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_disks_bulk_insert_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/regions/{region}/disks/bulkInsert
/// Bulk create a set of disks.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_region_disks_bulk_insert_builder()` + `compute_region_disks_bulk_insert_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_disks_bulk_insert(
    client: &SimpleHttpClient,
    project: &str,
    region: &str,
    requestId: Option<&str>,
    body: &BulkInsertDiskResource,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        compute_region_disks_bulk_insert_builder(client, project, region, requestId, body)?;
    compute_region_disks_bulk_insert_execute(builder)
}

/// GET projects/{project}/regions/{region}/disks/{disk}/createSnapshot
/// Creates a snapshot of a specified persistent disk. For regular snapshot creation, consider using snapshots.insert instead, as that method supports more features, such as creating snapshots in a project different from the source disk project.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_region_disks_create_snapshot_execute()` to send, or `compute_region_disks_create_snapshot` for simplest API.

pub fn compute_region_disks_create_snapshot_builder(
    client: &SimpleHttpClient,
    disk: &str,
    project: &str,
    region: &str,
    requestId: Option<&str>,
    body: &Snapshot,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/regions/{}/disks/{}/createSnapshot",
        disk, project, region,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "requestId",
            requestId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET projects/{project}/regions/{region}/disks/{disk}/createSnapshot
/// Creates a snapshot of a specified persistent disk. For regular snapshot creation, consider using snapshots.insert instead, as that method supports more features, such as creating snapshots in a project different from the source disk project.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_region_disks_create_snapshot_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_region_disks_create_snapshot_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_disks_create_snapshot_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/regions/{region}/disks/{disk}/createSnapshot
/// Creates a snapshot of a specified persistent disk. For regular snapshot creation, consider using snapshots.insert instead, as that method supports more features, such as creating snapshots in a project different from the source disk project.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_region_disks_create_snapshot_builder()` + `compute_region_disks_create_snapshot_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_disks_create_snapshot(
    client: &SimpleHttpClient,
    disk: &str,
    project: &str,
    region: &str,
    requestId: Option<&str>,
    body: &Snapshot,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_region_disks_create_snapshot_builder(
        client, disk, project, region, requestId, body,
    )?;
    compute_region_disks_create_snapshot_execute(builder)
}

/// GET projects/{project}/regions/{region}/disks/{disk}
/// Deletes the specified regional persistent disk. Deleting a regional disk removes all the replicas of its data permanently and is irreversible. However, deleting a disk does not delete anysnapshots previously made from the disk. You must separatelydelete snapshots.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_region_disks_delete_execute()` to send, or `compute_region_disks_delete` for simplest API.

pub fn compute_region_disks_delete_builder(
    client: &SimpleHttpClient,
    disk: &str,
    project: &str,
    region: &str,
    requestId: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/regions/{}/disks/{}",
        disk, project, region,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "requestId",
            requestId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET projects/{project}/regions/{region}/disks/{disk}
/// Deletes the specified regional persistent disk. Deleting a regional disk removes all the replicas of its data permanently and is irreversible. However, deleting a disk does not delete anysnapshots previously made from the disk. You must separatelydelete snapshots.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_region_disks_delete_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_region_disks_delete_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_disks_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/regions/{region}/disks/{disk}
/// Deletes the specified regional persistent disk. Deleting a regional disk removes all the replicas of its data permanently and is irreversible. However, deleting a disk does not delete anysnapshots previously made from the disk. You must separatelydelete snapshots.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_region_disks_delete_builder()` + `compute_region_disks_delete_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_disks_delete(
    client: &SimpleHttpClient,
    disk: &str,
    project: &str,
    region: &str,
    requestId: Option<&str>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_region_disks_delete_builder(client, disk, project, region, requestId)?;
    compute_region_disks_delete_execute(builder)
}

/// GET projects/{project}/regions/{region}/disks/{disk}
/// Returns a specified regional persistent disk.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_region_disks_get_execute()` to send, or `compute_region_disks_get` for simplest API.

pub fn compute_region_disks_get_builder(
    client: &SimpleHttpClient,
    disk: &str,
    project: &str,
    region: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/regions/{}/disks/{}",
        disk, project, region,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/regions/{region}/disks/{disk}
/// Returns a specified regional persistent disk.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_region_disks_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_region_disks_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_disks_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Disk>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Disk = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/regions/{region}/disks/{disk}
/// Returns a specified regional persistent disk.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_region_disks_get_builder()` + `compute_region_disks_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_disks_get(
    client: &SimpleHttpClient,
    disk: &str,
    project: &str,
    region: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Disk>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_region_disks_get_builder(client, disk, project, region)?;
    compute_region_disks_get_execute(builder)
}

/// GET projects/{project}/regions/{region}/disks/{resource}/getIamPolicy
/// Gets the access control policy for a resource. May be empty if no such policy or resource exists.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_region_disks_get_iam_policy_execute()` to send, or `compute_region_disks_get_iam_policy` for simplest API.

pub fn compute_region_disks_get_iam_policy_builder(
    client: &SimpleHttpClient,
    project: &str,
    region: &str,
    resource: &str,
    optionsRequestedPolicyVersion: Option<i32>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/regions/{}/disks/{}/getIamPolicy",
        project, region, resource,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "optionsRequestedPolicyVersion",
            optionsRequestedPolicyVersion
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET projects/{project}/regions/{region}/disks/{resource}/getIamPolicy
/// Gets the access control policy for a resource. May be empty if no such policy or resource exists.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_region_disks_get_iam_policy_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_region_disks_get_iam_policy_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_disks_get_iam_policy_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Policy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Policy = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/regions/{region}/disks/{resource}/getIamPolicy
/// Gets the access control policy for a resource. May be empty if no such policy or resource exists.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_region_disks_get_iam_policy_builder()` + `compute_region_disks_get_iam_policy_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_disks_get_iam_policy(
    client: &SimpleHttpClient,
    project: &str,
    region: &str,
    resource: &str,
    optionsRequestedPolicyVersion: Option<i32>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Policy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_region_disks_get_iam_policy_builder(
        client,
        project,
        region,
        resource,
        optionsRequestedPolicyVersion,
    )?;
    compute_region_disks_get_iam_policy_execute(builder)
}

/// GET projects/{project}/regions/{region}/disks
/// Creates a persistent regional disk in the specified project using the data included in the request.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_region_disks_insert_execute()` to send, or `compute_region_disks_insert` for simplest API.

pub fn compute_region_disks_insert_builder(
    client: &SimpleHttpClient,
    project: &str,
    region: &str,
    requestId: Option<&str>,
    sourceImage: Option<&str>,
    body: &Disk,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/regions/{}/disks",
        project, region,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "requestId",
            requestId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "sourceImage",
            sourceImage
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET projects/{project}/regions/{region}/disks
/// Creates a persistent regional disk in the specified project using the data included in the request.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_region_disks_insert_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_region_disks_insert_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_disks_insert_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/regions/{region}/disks
/// Creates a persistent regional disk in the specified project using the data included in the request.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_region_disks_insert_builder()` + `compute_region_disks_insert_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_disks_insert(
    client: &SimpleHttpClient,
    project: &str,
    region: &str,
    requestId: Option<&str>,
    sourceImage: Option<&str>,
    body: &Disk,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        compute_region_disks_insert_builder(client, project, region, requestId, sourceImage, body)?;
    compute_region_disks_insert_execute(builder)
}

/// GET projects/{project}/regions/{region}/disks
/// Retrieves the list of persistent disks contained within the specified region.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_region_disks_list_execute()` to send, or `compute_region_disks_list` for simplest API.

pub fn compute_region_disks_list_builder(
    client: &SimpleHttpClient,
    project: &str,
    region: &str,
    filter: Option<&str>,
    maxResults: Option<i32>,
    orderBy: Option<&str>,
    pageToken: Option<&str>,
    returnPartialSuccess: Option<bool>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/regions/{}/disks",
        project, region,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "filter",
            filter
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "maxResults",
            maxResults
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "orderBy",
            orderBy
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageToken",
            pageToken
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "returnPartialSuccess",
            returnPartialSuccess
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET projects/{project}/regions/{region}/disks
/// Retrieves the list of persistent disks contained within the specified region.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_region_disks_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_region_disks_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_disks_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<DiskList>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: DiskList = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/regions/{region}/disks
/// Retrieves the list of persistent disks contained within the specified region.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_region_disks_list_builder()` + `compute_region_disks_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_disks_list(
    client: &SimpleHttpClient,
    project: &str,
    region: &str,
    filter: Option<&str>,
    maxResults: Option<i32>,
    orderBy: Option<&str>,
    pageToken: Option<&str>,
    returnPartialSuccess: Option<bool>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<DiskList>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_region_disks_list_builder(
        client,
        project,
        region,
        filter,
        maxResults,
        orderBy,
        pageToken,
        returnPartialSuccess,
    )?;
    compute_region_disks_list_execute(builder)
}

/// GET projects/{project}/regions/{region}/disks/{disk}/removeResourcePolicies
/// Removes resource policies from a regional disk.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_region_disks_remove_resource_policies_execute()` to send, or `compute_region_disks_remove_resource_policies` for simplest API.

pub fn compute_region_disks_remove_resource_policies_builder(
    client: &SimpleHttpClient,
    disk: &str,
    project: &str,
    region: &str,
    requestId: Option<&str>,
    body: &RegionDisksRemoveResourcePoliciesRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/regions/{}/disks/{}/removeResourcePolicies",
        disk, project, region,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "requestId",
            requestId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET projects/{project}/regions/{region}/disks/{disk}/removeResourcePolicies
/// Removes resource policies from a regional disk.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_region_disks_remove_resource_policies_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_region_disks_remove_resource_policies_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_disks_remove_resource_policies_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/regions/{region}/disks/{disk}/removeResourcePolicies
/// Removes resource policies from a regional disk.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_region_disks_remove_resource_policies_builder()` + `compute_region_disks_remove_resource_policies_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_disks_remove_resource_policies(
    client: &SimpleHttpClient,
    disk: &str,
    project: &str,
    region: &str,
    requestId: Option<&str>,
    body: &RegionDisksRemoveResourcePoliciesRequest,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_region_disks_remove_resource_policies_builder(
        client, disk, project, region, requestId, body,
    )?;
    compute_region_disks_remove_resource_policies_execute(builder)
}

/// GET projects/{project}/regions/{region}/disks/{disk}/resize
/// Resizes the specified regional persistent disk.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_region_disks_resize_execute()` to send, or `compute_region_disks_resize` for simplest API.

pub fn compute_region_disks_resize_builder(
    client: &SimpleHttpClient,
    disk: &str,
    project: &str,
    region: &str,
    requestId: Option<&str>,
    body: &RegionDisksResizeRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/regions/{}/disks/{}/resize",
        disk, project, region,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "requestId",
            requestId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET projects/{project}/regions/{region}/disks/{disk}/resize
/// Resizes the specified regional persistent disk.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_region_disks_resize_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_region_disks_resize_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_disks_resize_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/regions/{region}/disks/{disk}/resize
/// Resizes the specified regional persistent disk.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_region_disks_resize_builder()` + `compute_region_disks_resize_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_disks_resize(
    client: &SimpleHttpClient,
    disk: &str,
    project: &str,
    region: &str,
    requestId: Option<&str>,
    body: &RegionDisksResizeRequest,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        compute_region_disks_resize_builder(client, disk, project, region, requestId, body)?;
    compute_region_disks_resize_execute(builder)
}

/// GET projects/{project}/regions/{region}/disks/{resource}/setIamPolicy
/// Sets the access control policy on the specified resource. Replaces any existing policy.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_region_disks_set_iam_policy_execute()` to send, or `compute_region_disks_set_iam_policy` for simplest API.

pub fn compute_region_disks_set_iam_policy_builder(
    client: &SimpleHttpClient,
    project: &str,
    region: &str,
    resource: &str,
    body: &RegionSetPolicyRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/regions/{}/disks/{}/setIamPolicy",
        project, region, resource,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET projects/{project}/regions/{region}/disks/{resource}/setIamPolicy
/// Sets the access control policy on the specified resource. Replaces any existing policy.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_region_disks_set_iam_policy_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_region_disks_set_iam_policy_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_disks_set_iam_policy_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Policy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Policy = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/regions/{region}/disks/{resource}/setIamPolicy
/// Sets the access control policy on the specified resource. Replaces any existing policy.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_region_disks_set_iam_policy_builder()` + `compute_region_disks_set_iam_policy_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_disks_set_iam_policy(
    client: &SimpleHttpClient,
    project: &str,
    region: &str,
    resource: &str,
    body: &RegionSetPolicyRequest,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Policy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        compute_region_disks_set_iam_policy_builder(client, project, region, resource, body)?;
    compute_region_disks_set_iam_policy_execute(builder)
}

/// GET projects/{project}/regions/{region}/disks/{resource}/setLabels
/// Sets the labels on the target regional disk.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_region_disks_set_labels_execute()` to send, or `compute_region_disks_set_labels` for simplest API.

pub fn compute_region_disks_set_labels_builder(
    client: &SimpleHttpClient,
    project: &str,
    region: &str,
    resource: &str,
    requestId: Option<&str>,
    body: &RegionSetLabelsRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/regions/{}/disks/{}/setLabels",
        project, region, resource,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "requestId",
            requestId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET projects/{project}/regions/{region}/disks/{resource}/setLabels
/// Sets the labels on the target regional disk.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_region_disks_set_labels_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_region_disks_set_labels_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_disks_set_labels_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/regions/{region}/disks/{resource}/setLabels
/// Sets the labels on the target regional disk.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_region_disks_set_labels_builder()` + `compute_region_disks_set_labels_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_disks_set_labels(
    client: &SimpleHttpClient,
    project: &str,
    region: &str,
    resource: &str,
    requestId: Option<&str>,
    body: &RegionSetLabelsRequest,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_region_disks_set_labels_builder(
        client, project, region, resource, requestId, body,
    )?;
    compute_region_disks_set_labels_execute(builder)
}

/// GET projects/{project}/regions/{region}/disks/{disk}/startAsyncReplication
/// Starts asynchronous replication. Must be invoked on the primary disk.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_region_disks_start_async_replication_execute()` to send, or `compute_region_disks_start_async_replication` for simplest API.

pub fn compute_region_disks_start_async_replication_builder(
    client: &SimpleHttpClient,
    disk: &str,
    project: &str,
    region: &str,
    requestId: Option<&str>,
    body: &RegionDisksStartAsyncReplicationRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/regions/{}/disks/{}/startAsyncReplication",
        disk, project, region,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "requestId",
            requestId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET projects/{project}/regions/{region}/disks/{disk}/startAsyncReplication
/// Starts asynchronous replication. Must be invoked on the primary disk.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_region_disks_start_async_replication_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_region_disks_start_async_replication_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_disks_start_async_replication_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/regions/{region}/disks/{disk}/startAsyncReplication
/// Starts asynchronous replication. Must be invoked on the primary disk.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_region_disks_start_async_replication_builder()` + `compute_region_disks_start_async_replication_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_disks_start_async_replication(
    client: &SimpleHttpClient,
    disk: &str,
    project: &str,
    region: &str,
    requestId: Option<&str>,
    body: &RegionDisksStartAsyncReplicationRequest,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_region_disks_start_async_replication_builder(
        client, disk, project, region, requestId, body,
    )?;
    compute_region_disks_start_async_replication_execute(builder)
}

/// GET projects/{project}/regions/{region}/disks/{disk}/stopAsyncReplication
/// Stops asynchronous replication. Can be invoked either on the primary or on the secondary disk.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_region_disks_stop_async_replication_execute()` to send, or `compute_region_disks_stop_async_replication` for simplest API.

pub fn compute_region_disks_stop_async_replication_builder(
    client: &SimpleHttpClient,
    disk: &str,
    project: &str,
    region: &str,
    requestId: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/regions/{}/disks/{}/stopAsyncReplication",
        disk, project, region,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "requestId",
            requestId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET projects/{project}/regions/{region}/disks/{disk}/stopAsyncReplication
/// Stops asynchronous replication. Can be invoked either on the primary or on the secondary disk.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_region_disks_stop_async_replication_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_region_disks_stop_async_replication_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_disks_stop_async_replication_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/regions/{region}/disks/{disk}/stopAsyncReplication
/// Stops asynchronous replication. Can be invoked either on the primary or on the secondary disk.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_region_disks_stop_async_replication_builder()` + `compute_region_disks_stop_async_replication_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_disks_stop_async_replication(
    client: &SimpleHttpClient,
    disk: &str,
    project: &str,
    region: &str,
    requestId: Option<&str>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_region_disks_stop_async_replication_builder(
        client, disk, project, region, requestId,
    )?;
    compute_region_disks_stop_async_replication_execute(builder)
}

/// GET projects/{project}/regions/{region}/disks/stopGroupAsyncReplication
/// Stops asynchronous replication for a consistency group of disks. Can be invoked either in the primary or secondary scope.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_region_disks_stop_group_async_replication_execute()` to send, or `compute_region_disks_stop_group_async_replication` for simplest API.

pub fn compute_region_disks_stop_group_async_replication_builder(
    client: &SimpleHttpClient,
    project: &str,
    region: &str,
    requestId: Option<&str>,
    body: &DisksStopGroupAsyncReplicationResource,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/regions/{}/disks/stopGroupAsyncReplication",
        project, region,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "requestId",
            requestId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET projects/{project}/regions/{region}/disks/stopGroupAsyncReplication
/// Stops asynchronous replication for a consistency group of disks. Can be invoked either in the primary or secondary scope.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_region_disks_stop_group_async_replication_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_region_disks_stop_group_async_replication_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_disks_stop_group_async_replication_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/regions/{region}/disks/stopGroupAsyncReplication
/// Stops asynchronous replication for a consistency group of disks. Can be invoked either in the primary or secondary scope.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_region_disks_stop_group_async_replication_builder()` + `compute_region_disks_stop_group_async_replication_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_disks_stop_group_async_replication(
    client: &SimpleHttpClient,
    project: &str,
    region: &str,
    requestId: Option<&str>,
    body: &DisksStopGroupAsyncReplicationResource,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_region_disks_stop_group_async_replication_builder(
        client, project, region, requestId, body,
    )?;
    compute_region_disks_stop_group_async_replication_execute(builder)
}

/// GET projects/{project}/regions/{region}/disks/{resource}/testIamPermissions
/// Returns permissions that a caller has on the specified resource.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_region_disks_test_iam_permissions_execute()` to send, or `compute_region_disks_test_iam_permissions` for simplest API.

pub fn compute_region_disks_test_iam_permissions_builder(
    client: &SimpleHttpClient,
    project: &str,
    region: &str,
    resource: &str,
    body: &TestPermissionsRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/regions/{}/disks/{}/testIamPermissions",
        project, region, resource,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET projects/{project}/regions/{region}/disks/{resource}/testIamPermissions
/// Returns permissions that a caller has on the specified resource.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_region_disks_test_iam_permissions_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_region_disks_test_iam_permissions_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_disks_test_iam_permissions_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<TestPermissionsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: TestPermissionsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/regions/{region}/disks/{resource}/testIamPermissions
/// Returns permissions that a caller has on the specified resource.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_region_disks_test_iam_permissions_builder()` + `compute_region_disks_test_iam_permissions_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_disks_test_iam_permissions(
    client: &SimpleHttpClient,
    project: &str,
    region: &str,
    resource: &str,
    body: &TestPermissionsRequest,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<TestPermissionsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        compute_region_disks_test_iam_permissions_builder(client, project, region, resource, body)?;
    compute_region_disks_test_iam_permissions_execute(builder)
}

/// GET projects/{project}/regions/{region}/disks/{disk}
/// Update the specified disk with the data included in the request. Update is performed only on selected fields included as part of update-mask.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_region_disks_update_execute()` to send, or `compute_region_disks_update` for simplest API.

pub fn compute_region_disks_update_builder(
    client: &SimpleHttpClient,
    disk: &str,
    project: &str,
    region: &str,
    paths: Option<&str>,
    requestId: Option<&str>,
    updateMask: Option<&str>,
    body: &Disk,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/regions/{}/disks/{}",
        disk, project, region,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "paths",
            paths
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "requestId",
            requestId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "updateMask",
            updateMask
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET projects/{project}/regions/{region}/disks/{disk}
/// Update the specified disk with the data included in the request. Update is performed only on selected fields included as part of update-mask.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_region_disks_update_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_region_disks_update_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_disks_update_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/regions/{region}/disks/{disk}
/// Update the specified disk with the data included in the request. Update is performed only on selected fields included as part of update-mask.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_region_disks_update_builder()` + `compute_region_disks_update_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_disks_update(
    client: &SimpleHttpClient,
    disk: &str,
    project: &str,
    region: &str,
    paths: Option<&str>,
    requestId: Option<&str>,
    updateMask: Option<&str>,
    body: &Disk,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_region_disks_update_builder(
        client, disk, project, region, paths, requestId, updateMask, body,
    )?;
    compute_region_disks_update_execute(builder)
}

/// GET projects/{project}/aggregated/healthAggregationPolicies
/// Retrieves the list of all HealthAggregationPolicy resources, regional and global, available to the specified project.  To prevent failure, it is recommended that you set the `returnPartialSuccess` parameter to `true`.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_region_health_aggregation_policies_aggregated_list_execute()` to send, or `compute_region_health_aggregation_policies_aggregated_list` for simplest API.

pub fn compute_region_health_aggregation_policies_aggregated_list_builder(
    client: &SimpleHttpClient,
    project: &str,
    filter: Option<&str>,
    includeAllScopes: Option<bool>,
    maxResults: Option<i32>,
    orderBy: Option<&str>,
    pageToken: Option<&str>,
    returnPartialSuccess: Option<bool>,
    serviceProjectNumber: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/aggregated/healthAggregationPolicies",
        project,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "filter",
            filter
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "includeAllScopes",
            includeAllScopes
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "maxResults",
            maxResults
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "orderBy",
            orderBy
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageToken",
            pageToken
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "returnPartialSuccess",
            returnPartialSuccess
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "serviceProjectNumber",
            serviceProjectNumber
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET projects/{project}/aggregated/healthAggregationPolicies
/// Retrieves the list of all HealthAggregationPolicy resources, regional and global, available to the specified project.  To prevent failure, it is recommended that you set the `returnPartialSuccess` parameter to `true`.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_region_health_aggregation_policies_aggregated_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_region_health_aggregation_policies_aggregated_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_health_aggregation_policies_aggregated_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<HealthAggregationPolicyAggregatedList>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: HealthAggregationPolicyAggregatedList = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/aggregated/healthAggregationPolicies
/// Retrieves the list of all HealthAggregationPolicy resources, regional and global, available to the specified project.  To prevent failure, it is recommended that you set the `returnPartialSuccess` parameter to `true`.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_region_health_aggregation_policies_aggregated_list_builder()` + `compute_region_health_aggregation_policies_aggregated_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_health_aggregation_policies_aggregated_list(
    client: &SimpleHttpClient,
    project: &str,
    filter: Option<&str>,
    includeAllScopes: Option<bool>,
    maxResults: Option<i32>,
    orderBy: Option<&str>,
    pageToken: Option<&str>,
    returnPartialSuccess: Option<bool>,
    serviceProjectNumber: Option<&str>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<HealthAggregationPolicyAggregatedList>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = compute_region_health_aggregation_policies_aggregated_list_builder(
        client,
        project,
        filter,
        includeAllScopes,
        maxResults,
        orderBy,
        pageToken,
        returnPartialSuccess,
        serviceProjectNumber,
    )?;
    compute_region_health_aggregation_policies_aggregated_list_execute(builder)
}

/// GET projects/{project}/regions/{region}/healthAggregationPolicies/{healthAggregationPolicy}
/// Deletes the specified HealthAggregationPolicy in the given region.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_region_health_aggregation_policies_delete_execute()` to send, or `compute_region_health_aggregation_policies_delete` for simplest API.

pub fn compute_region_health_aggregation_policies_delete_builder(
    client: &SimpleHttpClient,
    healthAggregationPolicy: &str,
    project: &str,
    region: &str,
    requestId: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/regions/{}/healthAggregationPolicies/{}",
        healthAggregationPolicy, project, region,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "requestId",
            requestId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET projects/{project}/regions/{region}/healthAggregationPolicies/{healthAggregationPolicy}
/// Deletes the specified HealthAggregationPolicy in the given region.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_region_health_aggregation_policies_delete_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_region_health_aggregation_policies_delete_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_health_aggregation_policies_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/regions/{region}/healthAggregationPolicies/{healthAggregationPolicy}
/// Deletes the specified HealthAggregationPolicy in the given region.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_region_health_aggregation_policies_delete_builder()` + `compute_region_health_aggregation_policies_delete_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_health_aggregation_policies_delete(
    client: &SimpleHttpClient,
    healthAggregationPolicy: &str,
    project: &str,
    region: &str,
    requestId: Option<&str>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_region_health_aggregation_policies_delete_builder(
        client,
        healthAggregationPolicy,
        project,
        region,
        requestId,
    )?;
    compute_region_health_aggregation_policies_delete_execute(builder)
}

/// GET projects/{project}/regions/{region}/healthAggregationPolicies/{healthAggregationPolicy}
/// Returns the specified HealthAggregationPolicy resource in the given region.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_region_health_aggregation_policies_get_execute()` to send, or `compute_region_health_aggregation_policies_get` for simplest API.

pub fn compute_region_health_aggregation_policies_get_builder(
    client: &SimpleHttpClient,
    healthAggregationPolicy: &str,
    project: &str,
    region: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/regions/{}/healthAggregationPolicies/{}",
        healthAggregationPolicy, project, region,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/regions/{region}/healthAggregationPolicies/{healthAggregationPolicy}
/// Returns the specified HealthAggregationPolicy resource in the given region.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_region_health_aggregation_policies_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_region_health_aggregation_policies_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_health_aggregation_policies_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<HealthAggregationPolicy>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: HealthAggregationPolicy = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/regions/{region}/healthAggregationPolicies/{healthAggregationPolicy}
/// Returns the specified HealthAggregationPolicy resource in the given region.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_region_health_aggregation_policies_get_builder()` + `compute_region_health_aggregation_policies_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_health_aggregation_policies_get(
    client: &SimpleHttpClient,
    healthAggregationPolicy: &str,
    project: &str,
    region: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<HealthAggregationPolicy>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = compute_region_health_aggregation_policies_get_builder(
        client,
        healthAggregationPolicy,
        project,
        region,
    )?;
    compute_region_health_aggregation_policies_get_execute(builder)
}

/// GET projects/{project}/regions/{region}/healthAggregationPolicies
/// Create a HealthAggregationPolicy in the specified project in the given region using the parameters that are included in the request.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_region_health_aggregation_policies_insert_execute()` to send, or `compute_region_health_aggregation_policies_insert` for simplest API.

pub fn compute_region_health_aggregation_policies_insert_builder(
    client: &SimpleHttpClient,
    project: &str,
    region: &str,
    requestId: Option<&str>,
    body: &HealthAggregationPolicy,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/regions/{}/healthAggregationPolicies",
        project, region,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "requestId",
            requestId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET projects/{project}/regions/{region}/healthAggregationPolicies
/// Create a HealthAggregationPolicy in the specified project in the given region using the parameters that are included in the request.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_region_health_aggregation_policies_insert_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_region_health_aggregation_policies_insert_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_health_aggregation_policies_insert_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/regions/{region}/healthAggregationPolicies
/// Create a HealthAggregationPolicy in the specified project in the given region using the parameters that are included in the request.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_region_health_aggregation_policies_insert_builder()` + `compute_region_health_aggregation_policies_insert_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_health_aggregation_policies_insert(
    client: &SimpleHttpClient,
    project: &str,
    region: &str,
    requestId: Option<&str>,
    body: &HealthAggregationPolicy,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_region_health_aggregation_policies_insert_builder(
        client, project, region, requestId, body,
    )?;
    compute_region_health_aggregation_policies_insert_execute(builder)
}

/// GET projects/{project}/regions/{region}/healthAggregationPolicies
/// Lists the HealthAggregationPolicies for a project in the given region.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_region_health_aggregation_policies_list_execute()` to send, or `compute_region_health_aggregation_policies_list` for simplest API.

pub fn compute_region_health_aggregation_policies_list_builder(
    client: &SimpleHttpClient,
    project: &str,
    region: &str,
    filter: Option<&str>,
    maxResults: Option<i32>,
    orderBy: Option<&str>,
    pageToken: Option<&str>,
    returnPartialSuccess: Option<bool>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/regions/{}/healthAggregationPolicies",
        project, region,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "filter",
            filter
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "maxResults",
            maxResults
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "orderBy",
            orderBy
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageToken",
            pageToken
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "returnPartialSuccess",
            returnPartialSuccess
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET projects/{project}/regions/{region}/healthAggregationPolicies
/// Lists the HealthAggregationPolicies for a project in the given region.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_region_health_aggregation_policies_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_region_health_aggregation_policies_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_health_aggregation_policies_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<HealthAggregationPolicyList>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: HealthAggregationPolicyList = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/regions/{region}/healthAggregationPolicies
/// Lists the HealthAggregationPolicies for a project in the given region.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_region_health_aggregation_policies_list_builder()` + `compute_region_health_aggregation_policies_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_health_aggregation_policies_list(
    client: &SimpleHttpClient,
    project: &str,
    region: &str,
    filter: Option<&str>,
    maxResults: Option<i32>,
    orderBy: Option<&str>,
    pageToken: Option<&str>,
    returnPartialSuccess: Option<bool>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<HealthAggregationPolicyList>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = compute_region_health_aggregation_policies_list_builder(
        client,
        project,
        region,
        filter,
        maxResults,
        orderBy,
        pageToken,
        returnPartialSuccess,
    )?;
    compute_region_health_aggregation_policies_list_execute(builder)
}

/// GET projects/{project}/regions/{region}/healthAggregationPolicies/{healthAggregationPolicy}
/// Updates the specified regional HealthAggregationPolicy resource with the data included in the request. This method `supportsPATCH` semantics and uses `theJSON` merge patch format and processing rules.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_region_health_aggregation_policies_patch_execute()` to send, or `compute_region_health_aggregation_policies_patch` for simplest API.

pub fn compute_region_health_aggregation_policies_patch_builder(
    client: &SimpleHttpClient,
    healthAggregationPolicy: &str,
    project: &str,
    region: &str,
    requestId: Option<&str>,
    body: &HealthAggregationPolicy,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/regions/{}/healthAggregationPolicies/{}",
        healthAggregationPolicy, project, region,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "requestId",
            requestId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET projects/{project}/regions/{region}/healthAggregationPolicies/{healthAggregationPolicy}
/// Updates the specified regional HealthAggregationPolicy resource with the data included in the request. This method `supportsPATCH` semantics and uses `theJSON` merge patch format and processing rules.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_region_health_aggregation_policies_patch_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_region_health_aggregation_policies_patch_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_health_aggregation_policies_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/regions/{region}/healthAggregationPolicies/{healthAggregationPolicy}
/// Updates the specified regional HealthAggregationPolicy resource with the data included in the request. This method `supportsPATCH` semantics and uses `theJSON` merge patch format and processing rules.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_region_health_aggregation_policies_patch_builder()` + `compute_region_health_aggregation_policies_patch_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_health_aggregation_policies_patch(
    client: &SimpleHttpClient,
    healthAggregationPolicy: &str,
    project: &str,
    region: &str,
    requestId: Option<&str>,
    body: &HealthAggregationPolicy,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_region_health_aggregation_policies_patch_builder(
        client,
        healthAggregationPolicy,
        project,
        region,
        requestId,
        body,
    )?;
    compute_region_health_aggregation_policies_patch_execute(builder)
}

/// GET projects/{project}/regions/{region}/healthAggregationPolicies/{resource}/testIamPermissions
/// Returns permissions that a caller has on the specified resource.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_region_health_aggregation_policies_test_iam_permissions_execute()` to send, or `compute_region_health_aggregation_policies_test_iam_permissions` for simplest API.

pub fn compute_region_health_aggregation_policies_test_iam_permissions_builder(
    client: &SimpleHttpClient,
    project: &str,
    region: &str,
    resource: &str,
    body: &TestPermissionsRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/regions/{}/healthAggregationPolicies/{}/testIamPermissions",
        project,
        region,
        resource,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET projects/{project}/regions/{region}/healthAggregationPolicies/{resource}/testIamPermissions
/// Returns permissions that a caller has on the specified resource.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_region_health_aggregation_policies_test_iam_permissions_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_region_health_aggregation_policies_test_iam_permissions_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_health_aggregation_policies_test_iam_permissions_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<TestPermissionsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: TestPermissionsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/regions/{region}/healthAggregationPolicies/{resource}/testIamPermissions
/// Returns permissions that a caller has on the specified resource.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_region_health_aggregation_policies_test_iam_permissions_builder()` + `compute_region_health_aggregation_policies_test_iam_permissions_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_health_aggregation_policies_test_iam_permissions(
    client: &SimpleHttpClient,
    project: &str,
    region: &str,
    resource: &str,
    body: &TestPermissionsRequest,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<TestPermissionsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = compute_region_health_aggregation_policies_test_iam_permissions_builder(
        client, project, region, resource, body,
    )?;
    compute_region_health_aggregation_policies_test_iam_permissions_execute(builder)
}

/// GET projects/{project}/aggregated/healthCheckServices
/// Retrieves the list of all HealthCheckService resources, regional and global, available to the specified project.  To prevent failure, it is recommended that you set the `returnPartialSuccess` parameter to `true`.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_region_health_check_services_aggregated_list_execute()` to send, or `compute_region_health_check_services_aggregated_list` for simplest API.

pub fn compute_region_health_check_services_aggregated_list_builder(
    client: &SimpleHttpClient,
    project: &str,
    filter: Option<&str>,
    includeAllScopes: Option<bool>,
    maxResults: Option<i32>,
    orderBy: Option<&str>,
    pageToken: Option<&str>,
    returnPartialSuccess: Option<bool>,
    serviceProjectNumber: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/aggregated/healthCheckServices",
        project,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "filter",
            filter
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "includeAllScopes",
            includeAllScopes
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "maxResults",
            maxResults
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "orderBy",
            orderBy
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageToken",
            pageToken
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "returnPartialSuccess",
            returnPartialSuccess
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "serviceProjectNumber",
            serviceProjectNumber
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET projects/{project}/aggregated/healthCheckServices
/// Retrieves the list of all HealthCheckService resources, regional and global, available to the specified project.  To prevent failure, it is recommended that you set the `returnPartialSuccess` parameter to `true`.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_region_health_check_services_aggregated_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_region_health_check_services_aggregated_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_health_check_services_aggregated_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<HealthCheckServiceAggregatedList>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: HealthCheckServiceAggregatedList = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/aggregated/healthCheckServices
/// Retrieves the list of all HealthCheckService resources, regional and global, available to the specified project.  To prevent failure, it is recommended that you set the `returnPartialSuccess` parameter to `true`.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_region_health_check_services_aggregated_list_builder()` + `compute_region_health_check_services_aggregated_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_health_check_services_aggregated_list(
    client: &SimpleHttpClient,
    project: &str,
    filter: Option<&str>,
    includeAllScopes: Option<bool>,
    maxResults: Option<i32>,
    orderBy: Option<&str>,
    pageToken: Option<&str>,
    returnPartialSuccess: Option<bool>,
    serviceProjectNumber: Option<&str>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<HealthCheckServiceAggregatedList>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = compute_region_health_check_services_aggregated_list_builder(
        client,
        project,
        filter,
        includeAllScopes,
        maxResults,
        orderBy,
        pageToken,
        returnPartialSuccess,
        serviceProjectNumber,
    )?;
    compute_region_health_check_services_aggregated_list_execute(builder)
}

/// GET projects/{project}/regions/{region}/healthCheckServices/{healthCheckService}
/// Deletes the specified regional HealthCheckService.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_region_health_check_services_delete_execute()` to send, or `compute_region_health_check_services_delete` for simplest API.

pub fn compute_region_health_check_services_delete_builder(
    client: &SimpleHttpClient,
    healthCheckService: &str,
    project: &str,
    region: &str,
    requestId: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/regions/{}/healthCheckServices/{}",
        healthCheckService, project, region,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "requestId",
            requestId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET projects/{project}/regions/{region}/healthCheckServices/{healthCheckService}
/// Deletes the specified regional HealthCheckService.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_region_health_check_services_delete_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_region_health_check_services_delete_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_health_check_services_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/regions/{region}/healthCheckServices/{healthCheckService}
/// Deletes the specified regional HealthCheckService.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_region_health_check_services_delete_builder()` + `compute_region_health_check_services_delete_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_health_check_services_delete(
    client: &SimpleHttpClient,
    healthCheckService: &str,
    project: &str,
    region: &str,
    requestId: Option<&str>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_region_health_check_services_delete_builder(
        client,
        healthCheckService,
        project,
        region,
        requestId,
    )?;
    compute_region_health_check_services_delete_execute(builder)
}

/// GET projects/{project}/regions/{region}/healthCheckServices/{healthCheckService}
/// Returns the specified regional HealthCheckService resource.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_region_health_check_services_get_execute()` to send, or `compute_region_health_check_services_get` for simplest API.

pub fn compute_region_health_check_services_get_builder(
    client: &SimpleHttpClient,
    healthCheckService: &str,
    project: &str,
    region: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/regions/{}/healthCheckServices/{}",
        healthCheckService, project, region,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/regions/{region}/healthCheckServices/{healthCheckService}
/// Returns the specified regional HealthCheckService resource.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_region_health_check_services_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_region_health_check_services_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_health_check_services_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<HealthCheckService>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: HealthCheckService = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/regions/{region}/healthCheckServices/{healthCheckService}
/// Returns the specified regional HealthCheckService resource.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_region_health_check_services_get_builder()` + `compute_region_health_check_services_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_health_check_services_get(
    client: &SimpleHttpClient,
    healthCheckService: &str,
    project: &str,
    region: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<HealthCheckService>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = compute_region_health_check_services_get_builder(
        client,
        healthCheckService,
        project,
        region,
    )?;
    compute_region_health_check_services_get_execute(builder)
}

/// GET projects/{project}/regions/{region}/healthCheckServices
/// Creates a regional HealthCheckService resource in the specified project and region using the data included in the request.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_region_health_check_services_insert_execute()` to send, or `compute_region_health_check_services_insert` for simplest API.

pub fn compute_region_health_check_services_insert_builder(
    client: &SimpleHttpClient,
    project: &str,
    region: &str,
    requestId: Option<&str>,
    body: &HealthCheckService,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/regions/{}/healthCheckServices",
        project, region,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "requestId",
            requestId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET projects/{project}/regions/{region}/healthCheckServices
/// Creates a regional HealthCheckService resource in the specified project and region using the data included in the request.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_region_health_check_services_insert_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_region_health_check_services_insert_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_health_check_services_insert_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/regions/{region}/healthCheckServices
/// Creates a regional HealthCheckService resource in the specified project and region using the data included in the request.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_region_health_check_services_insert_builder()` + `compute_region_health_check_services_insert_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_health_check_services_insert(
    client: &SimpleHttpClient,
    project: &str,
    region: &str,
    requestId: Option<&str>,
    body: &HealthCheckService,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_region_health_check_services_insert_builder(
        client, project, region, requestId, body,
    )?;
    compute_region_health_check_services_insert_execute(builder)
}

/// GET projects/{project}/regions/{region}/healthCheckServices
/// Lists all the HealthCheckService resources that have been configured for the specified project in the given region.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_region_health_check_services_list_execute()` to send, or `compute_region_health_check_services_list` for simplest API.

pub fn compute_region_health_check_services_list_builder(
    client: &SimpleHttpClient,
    project: &str,
    region: &str,
    filter: Option<&str>,
    maxResults: Option<i32>,
    orderBy: Option<&str>,
    pageToken: Option<&str>,
    returnPartialSuccess: Option<bool>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/regions/{}/healthCheckServices",
        project, region,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "filter",
            filter
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "maxResults",
            maxResults
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "orderBy",
            orderBy
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageToken",
            pageToken
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "returnPartialSuccess",
            returnPartialSuccess
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET projects/{project}/regions/{region}/healthCheckServices
/// Lists all the HealthCheckService resources that have been configured for the specified project in the given region.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_region_health_check_services_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_region_health_check_services_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_health_check_services_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<HealthCheckServicesList>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: HealthCheckServicesList = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/regions/{region}/healthCheckServices
/// Lists all the HealthCheckService resources that have been configured for the specified project in the given region.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_region_health_check_services_list_builder()` + `compute_region_health_check_services_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_health_check_services_list(
    client: &SimpleHttpClient,
    project: &str,
    region: &str,
    filter: Option<&str>,
    maxResults: Option<i32>,
    orderBy: Option<&str>,
    pageToken: Option<&str>,
    returnPartialSuccess: Option<bool>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<HealthCheckServicesList>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = compute_region_health_check_services_list_builder(
        client,
        project,
        region,
        filter,
        maxResults,
        orderBy,
        pageToken,
        returnPartialSuccess,
    )?;
    compute_region_health_check_services_list_execute(builder)
}

/// GET projects/{project}/regions/{region}/healthCheckServices/{healthCheckService}
/// Updates the specified regional HealthCheckService resource with the data included in the request.  This method `supportsPATCH` semantics and uses `theJSON` merge patch format and processing rules.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_region_health_check_services_patch_execute()` to send, or `compute_region_health_check_services_patch` for simplest API.

pub fn compute_region_health_check_services_patch_builder(
    client: &SimpleHttpClient,
    healthCheckService: &str,
    project: &str,
    region: &str,
    requestId: Option<&str>,
    body: &HealthCheckService,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/regions/{}/healthCheckServices/{}",
        healthCheckService, project, region,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "requestId",
            requestId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET projects/{project}/regions/{region}/healthCheckServices/{healthCheckService}
/// Updates the specified regional HealthCheckService resource with the data included in the request.  This method `supportsPATCH` semantics and uses `theJSON` merge patch format and processing rules.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_region_health_check_services_patch_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_region_health_check_services_patch_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_health_check_services_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/regions/{region}/healthCheckServices/{healthCheckService}
/// Updates the specified regional HealthCheckService resource with the data included in the request.  This method `supportsPATCH` semantics and uses `theJSON` merge patch format and processing rules.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_region_health_check_services_patch_builder()` + `compute_region_health_check_services_patch_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_health_check_services_patch(
    client: &SimpleHttpClient,
    healthCheckService: &str,
    project: &str,
    region: &str,
    requestId: Option<&str>,
    body: &HealthCheckService,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_region_health_check_services_patch_builder(
        client,
        healthCheckService,
        project,
        region,
        requestId,
        body,
    )?;
    compute_region_health_check_services_patch_execute(builder)
}

/// GET projects/{project}/regions/{region}/healthCheckServices/{resource}/testIamPermissions
/// Returns permissions that a caller has on the specified resource.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_region_health_check_services_test_iam_permissions_execute()` to send, or `compute_region_health_check_services_test_iam_permissions` for simplest API.

pub fn compute_region_health_check_services_test_iam_permissions_builder(
    client: &SimpleHttpClient,
    project: &str,
    region: &str,
    resource: &str,
    body: &TestPermissionsRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/regions/{}/healthCheckServices/{}/testIamPermissions",
        project, region, resource,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET projects/{project}/regions/{region}/healthCheckServices/{resource}/testIamPermissions
/// Returns permissions that a caller has on the specified resource.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_region_health_check_services_test_iam_permissions_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_region_health_check_services_test_iam_permissions_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_health_check_services_test_iam_permissions_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<TestPermissionsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: TestPermissionsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/regions/{region}/healthCheckServices/{resource}/testIamPermissions
/// Returns permissions that a caller has on the specified resource.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_region_health_check_services_test_iam_permissions_builder()` + `compute_region_health_check_services_test_iam_permissions_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_health_check_services_test_iam_permissions(
    client: &SimpleHttpClient,
    project: &str,
    region: &str,
    resource: &str,
    body: &TestPermissionsRequest,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<TestPermissionsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = compute_region_health_check_services_test_iam_permissions_builder(
        client, project, region, resource, body,
    )?;
    compute_region_health_check_services_test_iam_permissions_execute(builder)
}

/// GET projects/{project}/regions/{region}/healthChecks/{healthCheck}
/// Deletes the specified HealthCheck resource.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_region_health_checks_delete_execute()` to send, or `compute_region_health_checks_delete` for simplest API.

pub fn compute_region_health_checks_delete_builder(
    client: &SimpleHttpClient,
    healthCheck: &str,
    project: &str,
    region: &str,
    requestId: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/regions/{}/healthChecks/{}",
        healthCheck, project, region,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "requestId",
            requestId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET projects/{project}/regions/{region}/healthChecks/{healthCheck}
/// Deletes the specified HealthCheck resource.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_region_health_checks_delete_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_region_health_checks_delete_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_health_checks_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/regions/{region}/healthChecks/{healthCheck}
/// Deletes the specified HealthCheck resource.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_region_health_checks_delete_builder()` + `compute_region_health_checks_delete_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_health_checks_delete(
    client: &SimpleHttpClient,
    healthCheck: &str,
    project: &str,
    region: &str,
    requestId: Option<&str>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_region_health_checks_delete_builder(
        client,
        healthCheck,
        project,
        region,
        requestId,
    )?;
    compute_region_health_checks_delete_execute(builder)
}

/// GET projects/{project}/regions/{region}/healthChecks/{healthCheck}
/// Returns the specified HealthCheck resource.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_region_health_checks_get_execute()` to send, or `compute_region_health_checks_get` for simplest API.

pub fn compute_region_health_checks_get_builder(
    client: &SimpleHttpClient,
    healthCheck: &str,
    project: &str,
    region: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/regions/{}/healthChecks/{}",
        healthCheck, project, region,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/regions/{region}/healthChecks/{healthCheck}
/// Returns the specified HealthCheck resource.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_region_health_checks_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_region_health_checks_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_health_checks_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<HealthCheck>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: HealthCheck = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/regions/{region}/healthChecks/{healthCheck}
/// Returns the specified HealthCheck resource.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_region_health_checks_get_builder()` + `compute_region_health_checks_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_health_checks_get(
    client: &SimpleHttpClient,
    healthCheck: &str,
    project: &str,
    region: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<HealthCheck>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_region_health_checks_get_builder(client, healthCheck, project, region)?;
    compute_region_health_checks_get_execute(builder)
}

/// GET projects/{project}/regions/{region}/healthChecks
/// Creates a HealthCheck resource in the specified project using the data included in the request.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_region_health_checks_insert_execute()` to send, or `compute_region_health_checks_insert` for simplest API.

pub fn compute_region_health_checks_insert_builder(
    client: &SimpleHttpClient,
    project: &str,
    region: &str,
    requestId: Option<&str>,
    body: &HealthCheck,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/regions/{}/healthChecks",
        project, region,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "requestId",
            requestId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET projects/{project}/regions/{region}/healthChecks
/// Creates a HealthCheck resource in the specified project using the data included in the request.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_region_health_checks_insert_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_region_health_checks_insert_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_health_checks_insert_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/regions/{region}/healthChecks
/// Creates a HealthCheck resource in the specified project using the data included in the request.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_region_health_checks_insert_builder()` + `compute_region_health_checks_insert_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_health_checks_insert(
    client: &SimpleHttpClient,
    project: &str,
    region: &str,
    requestId: Option<&str>,
    body: &HealthCheck,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        compute_region_health_checks_insert_builder(client, project, region, requestId, body)?;
    compute_region_health_checks_insert_execute(builder)
}

/// GET projects/{project}/regions/{region}/healthChecks
/// Retrieves the list of HealthCheck resources available to the specified project.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_region_health_checks_list_execute()` to send, or `compute_region_health_checks_list` for simplest API.

pub fn compute_region_health_checks_list_builder(
    client: &SimpleHttpClient,
    project: &str,
    region: &str,
    filter: Option<&str>,
    maxResults: Option<i32>,
    orderBy: Option<&str>,
    pageToken: Option<&str>,
    returnPartialSuccess: Option<bool>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/regions/{}/healthChecks",
        project, region,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "filter",
            filter
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "maxResults",
            maxResults
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "orderBy",
            orderBy
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageToken",
            pageToken
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "returnPartialSuccess",
            returnPartialSuccess
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET projects/{project}/regions/{region}/healthChecks
/// Retrieves the list of HealthCheck resources available to the specified project.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_region_health_checks_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_region_health_checks_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_health_checks_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<HealthCheckList>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: HealthCheckList = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/regions/{region}/healthChecks
/// Retrieves the list of HealthCheck resources available to the specified project.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_region_health_checks_list_builder()` + `compute_region_health_checks_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_health_checks_list(
    client: &SimpleHttpClient,
    project: &str,
    region: &str,
    filter: Option<&str>,
    maxResults: Option<i32>,
    orderBy: Option<&str>,
    pageToken: Option<&str>,
    returnPartialSuccess: Option<bool>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<HealthCheckList>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = compute_region_health_checks_list_builder(
        client,
        project,
        region,
        filter,
        maxResults,
        orderBy,
        pageToken,
        returnPartialSuccess,
    )?;
    compute_region_health_checks_list_execute(builder)
}

/// GET projects/{project}/regions/{region}/healthChecks/{healthCheck}
/// Updates a HealthCheck resource in the specified project using the data included in the request. This method `supportsPATCH` semantics and uses `theJSON` merge patch format and processing rules.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_region_health_checks_patch_execute()` to send, or `compute_region_health_checks_patch` for simplest API.

pub fn compute_region_health_checks_patch_builder(
    client: &SimpleHttpClient,
    healthCheck: &str,
    project: &str,
    region: &str,
    requestId: Option<&str>,
    body: &HealthCheck,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/regions/{}/healthChecks/{}",
        healthCheck, project, region,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "requestId",
            requestId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET projects/{project}/regions/{region}/healthChecks/{healthCheck}
/// Updates a HealthCheck resource in the specified project using the data included in the request. This method `supportsPATCH` semantics and uses `theJSON` merge patch format and processing rules.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_region_health_checks_patch_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_region_health_checks_patch_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_health_checks_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/regions/{region}/healthChecks/{healthCheck}
/// Updates a HealthCheck resource in the specified project using the data included in the request. This method `supportsPATCH` semantics and uses `theJSON` merge patch format and processing rules.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_region_health_checks_patch_builder()` + `compute_region_health_checks_patch_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_health_checks_patch(
    client: &SimpleHttpClient,
    healthCheck: &str,
    project: &str,
    region: &str,
    requestId: Option<&str>,
    body: &HealthCheck,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_region_health_checks_patch_builder(
        client,
        healthCheck,
        project,
        region,
        requestId,
        body,
    )?;
    compute_region_health_checks_patch_execute(builder)
}

/// GET projects/{project}/regions/{region}/healthChecks/{resource}/testIamPermissions
/// Returns permissions that a caller has on the specified resource.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_region_health_checks_test_iam_permissions_execute()` to send, or `compute_region_health_checks_test_iam_permissions` for simplest API.

pub fn compute_region_health_checks_test_iam_permissions_builder(
    client: &SimpleHttpClient,
    project: &str,
    region: &str,
    resource: &str,
    body: &TestPermissionsRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/regions/{}/healthChecks/{}/testIamPermissions",
        project, region, resource,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET projects/{project}/regions/{region}/healthChecks/{resource}/testIamPermissions
/// Returns permissions that a caller has on the specified resource.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_region_health_checks_test_iam_permissions_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_region_health_checks_test_iam_permissions_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_health_checks_test_iam_permissions_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<TestPermissionsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: TestPermissionsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/regions/{region}/healthChecks/{resource}/testIamPermissions
/// Returns permissions that a caller has on the specified resource.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_region_health_checks_test_iam_permissions_builder()` + `compute_region_health_checks_test_iam_permissions_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_health_checks_test_iam_permissions(
    client: &SimpleHttpClient,
    project: &str,
    region: &str,
    resource: &str,
    body: &TestPermissionsRequest,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<TestPermissionsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = compute_region_health_checks_test_iam_permissions_builder(
        client, project, region, resource, body,
    )?;
    compute_region_health_checks_test_iam_permissions_execute(builder)
}

/// GET projects/{project}/regions/{region}/healthChecks/{healthCheck}
/// Updates a HealthCheck resource in the specified project using the data included in the request.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_region_health_checks_update_execute()` to send, or `compute_region_health_checks_update` for simplest API.

pub fn compute_region_health_checks_update_builder(
    client: &SimpleHttpClient,
    healthCheck: &str,
    project: &str,
    region: &str,
    requestId: Option<&str>,
    body: &HealthCheck,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/regions/{}/healthChecks/{}",
        healthCheck, project, region,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "requestId",
            requestId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET projects/{project}/regions/{region}/healthChecks/{healthCheck}
/// Updates a HealthCheck resource in the specified project using the data included in the request.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_region_health_checks_update_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_region_health_checks_update_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_health_checks_update_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/regions/{region}/healthChecks/{healthCheck}
/// Updates a HealthCheck resource in the specified project using the data included in the request.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_region_health_checks_update_builder()` + `compute_region_health_checks_update_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_health_checks_update(
    client: &SimpleHttpClient,
    healthCheck: &str,
    project: &str,
    region: &str,
    requestId: Option<&str>,
    body: &HealthCheck,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_region_health_checks_update_builder(
        client,
        healthCheck,
        project,
        region,
        requestId,
        body,
    )?;
    compute_region_health_checks_update_execute(builder)
}

/// GET projects/{project}/aggregated/healthSources
/// Retrieves the list of all HealthSource resources (all regional) available to the specified project.  To prevent failure, Google recommends that you set the `returnPartialSuccess` parameter to `true`.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_region_health_sources_aggregated_list_execute()` to send, or `compute_region_health_sources_aggregated_list` for simplest API.

pub fn compute_region_health_sources_aggregated_list_builder(
    client: &SimpleHttpClient,
    project: &str,
    filter: Option<&str>,
    includeAllScopes: Option<bool>,
    maxResults: Option<i32>,
    orderBy: Option<&str>,
    pageToken: Option<&str>,
    returnPartialSuccess: Option<bool>,
    serviceProjectNumber: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/aggregated/healthSources",
        project,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "filter",
            filter
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "includeAllScopes",
            includeAllScopes
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "maxResults",
            maxResults
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "orderBy",
            orderBy
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageToken",
            pageToken
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "returnPartialSuccess",
            returnPartialSuccess
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "serviceProjectNumber",
            serviceProjectNumber
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET projects/{project}/aggregated/healthSources
/// Retrieves the list of all HealthSource resources (all regional) available to the specified project.  To prevent failure, Google recommends that you set the `returnPartialSuccess` parameter to `true`.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_region_health_sources_aggregated_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_region_health_sources_aggregated_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_health_sources_aggregated_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<HealthSourceAggregatedList>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: HealthSourceAggregatedList = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/aggregated/healthSources
/// Retrieves the list of all HealthSource resources (all regional) available to the specified project.  To prevent failure, Google recommends that you set the `returnPartialSuccess` parameter to `true`.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_region_health_sources_aggregated_list_builder()` + `compute_region_health_sources_aggregated_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_health_sources_aggregated_list(
    client: &SimpleHttpClient,
    project: &str,
    filter: Option<&str>,
    includeAllScopes: Option<bool>,
    maxResults: Option<i32>,
    orderBy: Option<&str>,
    pageToken: Option<&str>,
    returnPartialSuccess: Option<bool>,
    serviceProjectNumber: Option<&str>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<HealthSourceAggregatedList>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = compute_region_health_sources_aggregated_list_builder(
        client,
        project,
        filter,
        includeAllScopes,
        maxResults,
        orderBy,
        pageToken,
        returnPartialSuccess,
        serviceProjectNumber,
    )?;
    compute_region_health_sources_aggregated_list_execute(builder)
}

/// GET projects/{project}/regions/{region}/healthSources/{healthSource}
/// Deletes the specified HealthSource in the given region
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_region_health_sources_delete_execute()` to send, or `compute_region_health_sources_delete` for simplest API.

pub fn compute_region_health_sources_delete_builder(
    client: &SimpleHttpClient,
    healthSource: &str,
    project: &str,
    region: &str,
    requestId: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/regions/{}/healthSources/{}",
        healthSource, project, region,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "requestId",
            requestId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET projects/{project}/regions/{region}/healthSources/{healthSource}
/// Deletes the specified HealthSource in the given region
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_region_health_sources_delete_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_region_health_sources_delete_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_health_sources_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/regions/{region}/healthSources/{healthSource}
/// Deletes the specified HealthSource in the given region
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_region_health_sources_delete_builder()` + `compute_region_health_sources_delete_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_health_sources_delete(
    client: &SimpleHttpClient,
    healthSource: &str,
    project: &str,
    region: &str,
    requestId: Option<&str>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_region_health_sources_delete_builder(
        client,
        healthSource,
        project,
        region,
        requestId,
    )?;
    compute_region_health_sources_delete_execute(builder)
}

/// GET projects/{project}/regions/{region}/healthSources/{healthSource}
/// Returns the specified HealthSource resource in the given region.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_region_health_sources_get_execute()` to send, or `compute_region_health_sources_get` for simplest API.

pub fn compute_region_health_sources_get_builder(
    client: &SimpleHttpClient,
    healthSource: &str,
    project: &str,
    region: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/regions/{}/healthSources/{}",
        healthSource, project, region,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/regions/{region}/healthSources/{healthSource}
/// Returns the specified HealthSource resource in the given region.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_region_health_sources_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_region_health_sources_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_health_sources_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<HealthSource>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: HealthSource = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/regions/{region}/healthSources/{healthSource}
/// Returns the specified HealthSource resource in the given region.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_region_health_sources_get_builder()` + `compute_region_health_sources_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_health_sources_get(
    client: &SimpleHttpClient,
    healthSource: &str,
    project: &str,
    region: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<HealthSource>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = compute_region_health_sources_get_builder(client, healthSource, project, region)?;
    compute_region_health_sources_get_execute(builder)
}

/// GET projects/{project}/regions/{region}/healthSources
/// Create a HealthSource in the specified project in the given region using the parameters that are included in the request.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_region_health_sources_insert_execute()` to send, or `compute_region_health_sources_insert` for simplest API.

pub fn compute_region_health_sources_insert_builder(
    client: &SimpleHttpClient,
    project: &str,
    region: &str,
    requestId: Option<&str>,
    body: &HealthSource,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/regions/{}/healthSources",
        project, region,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "requestId",
            requestId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET projects/{project}/regions/{region}/healthSources
/// Create a HealthSource in the specified project in the given region using the parameters that are included in the request.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_region_health_sources_insert_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_region_health_sources_insert_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_health_sources_insert_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/regions/{region}/healthSources
/// Create a HealthSource in the specified project in the given region using the parameters that are included in the request.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_region_health_sources_insert_builder()` + `compute_region_health_sources_insert_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_health_sources_insert(
    client: &SimpleHttpClient,
    project: &str,
    region: &str,
    requestId: Option<&str>,
    body: &HealthSource,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        compute_region_health_sources_insert_builder(client, project, region, requestId, body)?;
    compute_region_health_sources_insert_execute(builder)
}

/// GET projects/{project}/regions/{region}/healthSources
/// Lists the HealthSources for a project in the given region.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_region_health_sources_list_execute()` to send, or `compute_region_health_sources_list` for simplest API.

pub fn compute_region_health_sources_list_builder(
    client: &SimpleHttpClient,
    project: &str,
    region: &str,
    filter: Option<&str>,
    maxResults: Option<i32>,
    orderBy: Option<&str>,
    pageToken: Option<&str>,
    returnPartialSuccess: Option<bool>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/regions/{}/healthSources",
        project, region,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "filter",
            filter
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "maxResults",
            maxResults
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "orderBy",
            orderBy
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageToken",
            pageToken
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "returnPartialSuccess",
            returnPartialSuccess
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET projects/{project}/regions/{region}/healthSources
/// Lists the HealthSources for a project in the given region.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_region_health_sources_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_region_health_sources_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_health_sources_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<HealthSourceList>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: HealthSourceList = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/regions/{region}/healthSources
/// Lists the HealthSources for a project in the given region.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_region_health_sources_list_builder()` + `compute_region_health_sources_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_health_sources_list(
    client: &SimpleHttpClient,
    project: &str,
    region: &str,
    filter: Option<&str>,
    maxResults: Option<i32>,
    orderBy: Option<&str>,
    pageToken: Option<&str>,
    returnPartialSuccess: Option<bool>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<HealthSourceList>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = compute_region_health_sources_list_builder(
        client,
        project,
        region,
        filter,
        maxResults,
        orderBy,
        pageToken,
        returnPartialSuccess,
    )?;
    compute_region_health_sources_list_execute(builder)
}

/// GET projects/{project}/regions/{region}/healthSources/{healthSource}
/// Updates the specified regional HealthSource resource with the data included in the request.  This method `supportsPATCH` semantics and uses `theJSON` merge patch format and processing rules.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_region_health_sources_patch_execute()` to send, or `compute_region_health_sources_patch` for simplest API.

pub fn compute_region_health_sources_patch_builder(
    client: &SimpleHttpClient,
    healthSource: &str,
    project: &str,
    region: &str,
    requestId: Option<&str>,
    body: &HealthSource,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/regions/{}/healthSources/{}",
        healthSource, project, region,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "requestId",
            requestId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET projects/{project}/regions/{region}/healthSources/{healthSource}
/// Updates the specified regional HealthSource resource with the data included in the request.  This method `supportsPATCH` semantics and uses `theJSON` merge patch format and processing rules.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_region_health_sources_patch_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_region_health_sources_patch_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_health_sources_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/regions/{region}/healthSources/{healthSource}
/// Updates the specified regional HealthSource resource with the data included in the request.  This method `supportsPATCH` semantics and uses `theJSON` merge patch format and processing rules.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_region_health_sources_patch_builder()` + `compute_region_health_sources_patch_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_health_sources_patch(
    client: &SimpleHttpClient,
    healthSource: &str,
    project: &str,
    region: &str,
    requestId: Option<&str>,
    body: &HealthSource,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_region_health_sources_patch_builder(
        client,
        healthSource,
        project,
        region,
        requestId,
        body,
    )?;
    compute_region_health_sources_patch_execute(builder)
}

/// GET projects/{project}/regions/{region}/healthSources/{resource}/testIamPermissions
/// Returns permissions that a caller has on the specified resource.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_region_health_sources_test_iam_permissions_execute()` to send, or `compute_region_health_sources_test_iam_permissions` for simplest API.

pub fn compute_region_health_sources_test_iam_permissions_builder(
    client: &SimpleHttpClient,
    project: &str,
    region: &str,
    resource: &str,
    body: &TestPermissionsRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/regions/{}/healthSources/{}/testIamPermissions",
        project, region, resource,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET projects/{project}/regions/{region}/healthSources/{resource}/testIamPermissions
/// Returns permissions that a caller has on the specified resource.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_region_health_sources_test_iam_permissions_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_region_health_sources_test_iam_permissions_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_health_sources_test_iam_permissions_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<TestPermissionsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: TestPermissionsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/regions/{region}/healthSources/{resource}/testIamPermissions
/// Returns permissions that a caller has on the specified resource.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_region_health_sources_test_iam_permissions_builder()` + `compute_region_health_sources_test_iam_permissions_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_health_sources_test_iam_permissions(
    client: &SimpleHttpClient,
    project: &str,
    region: &str,
    resource: &str,
    body: &TestPermissionsRequest,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<TestPermissionsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = compute_region_health_sources_test_iam_permissions_builder(
        client, project, region, resource, body,
    )?;
    compute_region_health_sources_test_iam_permissions_execute(builder)
}

/// GET projects/{project}/regions/{region}/instanceGroupManagers/{instanceGroupManager}/abandonInstances
/// Flags the specified instances to be immediately removed from the managed instance group. Abandoning an instance does not delete the instance, but it does remove the instance from any target pools that are applied by the managed instance group. This method reduces `thetargetSize` of the managed instance group by the number of instances that you abandon. This operation is marked `asDONE` when the action is scheduled even if the instances have not yet been removed from the group. You must separately verify the status of the abandoning action with thelistmanagedinstances method.  If the group is part of a backend service that has enabled connection draining, it can take up to 60 seconds after the connection draining duration has elapsed before the VM instance is removed or deleted.  You can specify a maximum of 1000 instances with this method per request.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_region_instance_group_managers_abandon_instances_execute()` to send, or `compute_region_instance_group_managers_abandon_instances` for simplest API.

pub fn compute_region_instance_group_managers_abandon_instances_builder(
    client: &SimpleHttpClient,
    instanceGroupManager: &str,
    project: &str,
    region: &str,
    requestId: Option<&str>,
    body: &RegionInstanceGroupManagersAbandonInstancesRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/regions/{}/instanceGroupManagers/{}/abandonInstances",
        instanceGroupManager, project, region,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "requestId",
            requestId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET projects/{project}/regions/{region}/instanceGroupManagers/{instanceGroupManager}/abandonInstances
/// Flags the specified instances to be immediately removed from the managed instance group. Abandoning an instance does not delete the instance, but it does remove the instance from any target pools that are applied by the managed instance group. This method reduces `thetargetSize` of the managed instance group by the number of instances that you abandon. This operation is marked `asDONE` when the action is scheduled even if the instances have not yet been removed from the group. You must separately verify the status of the abandoning action with thelistmanagedinstances method.  If the group is part of a backend service that has enabled connection draining, it can take up to 60 seconds after the connection draining duration has elapsed before the VM instance is removed or deleted.  You can specify a maximum of 1000 instances with this method per request.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_region_instance_group_managers_abandon_instances_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_region_instance_group_managers_abandon_instances_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_instance_group_managers_abandon_instances_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/regions/{region}/instanceGroupManagers/{instanceGroupManager}/abandonInstances
/// Flags the specified instances to be immediately removed from the managed instance group. Abandoning an instance does not delete the instance, but it does remove the instance from any target pools that are applied by the managed instance group. This method reduces `thetargetSize` of the managed instance group by the number of instances that you abandon. This operation is marked `asDONE` when the action is scheduled even if the instances have not yet been removed from the group. You must separately verify the status of the abandoning action with thelistmanagedinstances method.  If the group is part of a backend service that has enabled connection draining, it can take up to 60 seconds after the connection draining duration has elapsed before the VM instance is removed or deleted.  You can specify a maximum of 1000 instances with this method per request.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_region_instance_group_managers_abandon_instances_builder()` + `compute_region_instance_group_managers_abandon_instances_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_instance_group_managers_abandon_instances(
    client: &SimpleHttpClient,
    instanceGroupManager: &str,
    project: &str,
    region: &str,
    requestId: Option<&str>,
    body: &RegionInstanceGroupManagersAbandonInstancesRequest,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_region_instance_group_managers_abandon_instances_builder(
        client,
        instanceGroupManager,
        project,
        region,
        requestId,
        body,
    )?;
    compute_region_instance_group_managers_abandon_instances_execute(builder)
}

/// GET projects/{project}/regions/{region}/instanceGroupManagers/{instanceGroupManager}/applyUpdatesToInstances
/// Apply updates to selected instances the managed instance group.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_region_instance_group_managers_apply_updates_to_instances_execute()` to send, or `compute_region_instance_group_managers_apply_updates_to_instances` for simplest API.

pub fn compute_region_instance_group_managers_apply_updates_to_instances_builder(
    client: &SimpleHttpClient,
    instanceGroupManager: &str,
    project: &str,
    region: &str,
    body: &RegionInstanceGroupManagersApplyUpdatesRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/regions/{}/instanceGroupManagers/{}/applyUpdatesToInstances",
        instanceGroupManager,
        project,
        region,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET projects/{project}/regions/{region}/instanceGroupManagers/{instanceGroupManager}/applyUpdatesToInstances
/// Apply updates to selected instances the managed instance group.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_region_instance_group_managers_apply_updates_to_instances_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_region_instance_group_managers_apply_updates_to_instances_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_instance_group_managers_apply_updates_to_instances_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/regions/{region}/instanceGroupManagers/{instanceGroupManager}/applyUpdatesToInstances
/// Apply updates to selected instances the managed instance group.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_region_instance_group_managers_apply_updates_to_instances_builder()` + `compute_region_instance_group_managers_apply_updates_to_instances_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_instance_group_managers_apply_updates_to_instances(
    client: &SimpleHttpClient,
    instanceGroupManager: &str,
    project: &str,
    region: &str,
    body: &RegionInstanceGroupManagersApplyUpdatesRequest,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_region_instance_group_managers_apply_updates_to_instances_builder(
        client,
        instanceGroupManager,
        project,
        region,
        body,
    )?;
    compute_region_instance_group_managers_apply_updates_to_instances_execute(builder)
}

/// GET projects/{project}/regions/{region}/instanceGroupManagers/{instanceGroupManager}/createInstances
/// Creates instances with per-instance configurations in this regional managed instance group. Instances are created using the current instance template. The create instances operation is marked DONE if the `createInstances` request is successful. The underlying actions take additional time. You must separately verify the status of thecreating or actions with the listmanagedinstances method.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_region_instance_group_managers_create_instances_execute()` to send, or `compute_region_instance_group_managers_create_instances` for simplest API.

pub fn compute_region_instance_group_managers_create_instances_builder(
    client: &SimpleHttpClient,
    instanceGroupManager: &str,
    project: &str,
    region: &str,
    requestId: Option<&str>,
    body: &RegionInstanceGroupManagersCreateInstancesRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/regions/{}/instanceGroupManagers/{}/createInstances",
        instanceGroupManager, project, region,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "requestId",
            requestId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET projects/{project}/regions/{region}/instanceGroupManagers/{instanceGroupManager}/createInstances
/// Creates instances with per-instance configurations in this regional managed instance group. Instances are created using the current instance template. The create instances operation is marked DONE if the `createInstances` request is successful. The underlying actions take additional time. You must separately verify the status of thecreating or actions with the listmanagedinstances method.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_region_instance_group_managers_create_instances_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_region_instance_group_managers_create_instances_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_instance_group_managers_create_instances_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/regions/{region}/instanceGroupManagers/{instanceGroupManager}/createInstances
/// Creates instances with per-instance configurations in this regional managed instance group. Instances are created using the current instance template. The create instances operation is marked DONE if the `createInstances` request is successful. The underlying actions take additional time. You must separately verify the status of thecreating or actions with the listmanagedinstances method.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_region_instance_group_managers_create_instances_builder()` + `compute_region_instance_group_managers_create_instances_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_instance_group_managers_create_instances(
    client: &SimpleHttpClient,
    instanceGroupManager: &str,
    project: &str,
    region: &str,
    requestId: Option<&str>,
    body: &RegionInstanceGroupManagersCreateInstancesRequest,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_region_instance_group_managers_create_instances_builder(
        client,
        instanceGroupManager,
        project,
        region,
        requestId,
        body,
    )?;
    compute_region_instance_group_managers_create_instances_execute(builder)
}

/// GET projects/{project}/regions/{region}/instanceGroupManagers/{instanceGroupManager}
/// Deletes the specified managed instance group and all of the instances in that group.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_region_instance_group_managers_delete_execute()` to send, or `compute_region_instance_group_managers_delete` for simplest API.

pub fn compute_region_instance_group_managers_delete_builder(
    client: &SimpleHttpClient,
    instanceGroupManager: &str,
    project: &str,
    region: &str,
    requestId: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/regions/{}/instanceGroupManagers/{}",
        instanceGroupManager, project, region,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "requestId",
            requestId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET projects/{project}/regions/{region}/instanceGroupManagers/{instanceGroupManager}
/// Deletes the specified managed instance group and all of the instances in that group.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_region_instance_group_managers_delete_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_region_instance_group_managers_delete_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_instance_group_managers_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/regions/{region}/instanceGroupManagers/{instanceGroupManager}
/// Deletes the specified managed instance group and all of the instances in that group.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_region_instance_group_managers_delete_builder()` + `compute_region_instance_group_managers_delete_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_instance_group_managers_delete(
    client: &SimpleHttpClient,
    instanceGroupManager: &str,
    project: &str,
    region: &str,
    requestId: Option<&str>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_region_instance_group_managers_delete_builder(
        client,
        instanceGroupManager,
        project,
        region,
        requestId,
    )?;
    compute_region_instance_group_managers_delete_execute(builder)
}

/// GET projects/{project}/regions/{region}/instanceGroupManagers/{instanceGroupManager}/deleteInstances
/// Flags the specified instances in the managed instance group to be immediately deleted. The instances are also removed from any target pools of which they were a member. This method reduces `thetargetSize` of the managed instance group by the number of instances that you delete. The `deleteInstances` operation is marked DONE if the `deleteInstances` request is successful. The underlying actions take additional time. You must separately verify the status of thedeleting action with thelistmanagedinstances method.  If the group is part of a backend service that has enabled connection draining, it can take up to 60 seconds after the connection draining duration has elapsed before the VM instance is removed or deleted.  You can specify a maximum of 1000 instances with this method per request.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_region_instance_group_managers_delete_instances_execute()` to send, or `compute_region_instance_group_managers_delete_instances` for simplest API.

pub fn compute_region_instance_group_managers_delete_instances_builder(
    client: &SimpleHttpClient,
    instanceGroupManager: &str,
    project: &str,
    region: &str,
    requestId: Option<&str>,
    body: &RegionInstanceGroupManagersDeleteInstancesRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/regions/{}/instanceGroupManagers/{}/deleteInstances",
        instanceGroupManager, project, region,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "requestId",
            requestId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET projects/{project}/regions/{region}/instanceGroupManagers/{instanceGroupManager}/deleteInstances
/// Flags the specified instances in the managed instance group to be immediately deleted. The instances are also removed from any target pools of which they were a member. This method reduces `thetargetSize` of the managed instance group by the number of instances that you delete. The `deleteInstances` operation is marked DONE if the `deleteInstances` request is successful. The underlying actions take additional time. You must separately verify the status of thedeleting action with thelistmanagedinstances method.  If the group is part of a backend service that has enabled connection draining, it can take up to 60 seconds after the connection draining duration has elapsed before the VM instance is removed or deleted.  You can specify a maximum of 1000 instances with this method per request.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_region_instance_group_managers_delete_instances_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_region_instance_group_managers_delete_instances_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_instance_group_managers_delete_instances_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/regions/{region}/instanceGroupManagers/{instanceGroupManager}/deleteInstances
/// Flags the specified instances in the managed instance group to be immediately deleted. The instances are also removed from any target pools of which they were a member. This method reduces `thetargetSize` of the managed instance group by the number of instances that you delete. The `deleteInstances` operation is marked DONE if the `deleteInstances` request is successful. The underlying actions take additional time. You must separately verify the status of thedeleting action with thelistmanagedinstances method.  If the group is part of a backend service that has enabled connection draining, it can take up to 60 seconds after the connection draining duration has elapsed before the VM instance is removed or deleted.  You can specify a maximum of 1000 instances with this method per request.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_region_instance_group_managers_delete_instances_builder()` + `compute_region_instance_group_managers_delete_instances_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_instance_group_managers_delete_instances(
    client: &SimpleHttpClient,
    instanceGroupManager: &str,
    project: &str,
    region: &str,
    requestId: Option<&str>,
    body: &RegionInstanceGroupManagersDeleteInstancesRequest,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_region_instance_group_managers_delete_instances_builder(
        client,
        instanceGroupManager,
        project,
        region,
        requestId,
        body,
    )?;
    compute_region_instance_group_managers_delete_instances_execute(builder)
}

/// GET projects/{project}/regions/{region}/instanceGroupManagers/{instanceGroupManager}/deletePerInstanceConfigs
/// Deletes selected per-instance configurations for the managed instance group.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_region_instance_group_managers_delete_per_instance_configs_execute()` to send, or `compute_region_instance_group_managers_delete_per_instance_configs` for simplest API.

pub fn compute_region_instance_group_managers_delete_per_instance_configs_builder(
    client: &SimpleHttpClient,
    instanceGroupManager: &str,
    project: &str,
    region: &str,
    body: &RegionInstanceGroupManagerDeleteInstanceConfigReq,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/regions/{}/instanceGroupManagers/{}/deletePerInstanceConfigs",
        instanceGroupManager,
        project,
        region,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET projects/{project}/regions/{region}/instanceGroupManagers/{instanceGroupManager}/deletePerInstanceConfigs
/// Deletes selected per-instance configurations for the managed instance group.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_region_instance_group_managers_delete_per_instance_configs_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_region_instance_group_managers_delete_per_instance_configs_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_instance_group_managers_delete_per_instance_configs_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/regions/{region}/instanceGroupManagers/{instanceGroupManager}/deletePerInstanceConfigs
/// Deletes selected per-instance configurations for the managed instance group.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_region_instance_group_managers_delete_per_instance_configs_builder()` + `compute_region_instance_group_managers_delete_per_instance_configs_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_instance_group_managers_delete_per_instance_configs(
    client: &SimpleHttpClient,
    instanceGroupManager: &str,
    project: &str,
    region: &str,
    body: &RegionInstanceGroupManagerDeleteInstanceConfigReq,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_region_instance_group_managers_delete_per_instance_configs_builder(
        client,
        instanceGroupManager,
        project,
        region,
        body,
    )?;
    compute_region_instance_group_managers_delete_per_instance_configs_execute(builder)
}

/// GET projects/{project}/regions/{region}/instanceGroupManagers/{instanceGroupManager}
/// Returns all of the details about the specified managed instance group.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_region_instance_group_managers_get_execute()` to send, or `compute_region_instance_group_managers_get` for simplest API.

pub fn compute_region_instance_group_managers_get_builder(
    client: &SimpleHttpClient,
    instanceGroupManager: &str,
    project: &str,
    region: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/regions/{}/instanceGroupManagers/{}",
        instanceGroupManager, project, region,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/regions/{region}/instanceGroupManagers/{instanceGroupManager}
/// Returns all of the details about the specified managed instance group.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_region_instance_group_managers_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_region_instance_group_managers_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_instance_group_managers_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<InstanceGroupManager>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: InstanceGroupManager = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/regions/{region}/instanceGroupManagers/{instanceGroupManager}
/// Returns all of the details about the specified managed instance group.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_region_instance_group_managers_get_builder()` + `compute_region_instance_group_managers_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_instance_group_managers_get(
    client: &SimpleHttpClient,
    instanceGroupManager: &str,
    project: &str,
    region: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<InstanceGroupManager>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = compute_region_instance_group_managers_get_builder(
        client,
        instanceGroupManager,
        project,
        region,
    )?;
    compute_region_instance_group_managers_get_execute(builder)
}

/// GET projects/{project}/regions/{region}/instanceGroupManagers
/// Creates a managed instance group using the information that you specify in the request. After the group is created, instances in the group are created using the specified instance template. This operation is marked as DONE when the group is created even if the instances in the group have not yet been created. You must separately verify the status of the individual instances with thelistmanagedinstances method.  A regional managed instance group can contain up to 2000 instances.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_region_instance_group_managers_insert_execute()` to send, or `compute_region_instance_group_managers_insert` for simplest API.

pub fn compute_region_instance_group_managers_insert_builder(
    client: &SimpleHttpClient,
    project: &str,
    region: &str,
    requestId: Option<&str>,
    body: &InstanceGroupManager,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/regions/{}/instanceGroupManagers",
        project, region,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "requestId",
            requestId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET projects/{project}/regions/{region}/instanceGroupManagers
/// Creates a managed instance group using the information that you specify in the request. After the group is created, instances in the group are created using the specified instance template. This operation is marked as DONE when the group is created even if the instances in the group have not yet been created. You must separately verify the status of the individual instances with thelistmanagedinstances method.  A regional managed instance group can contain up to 2000 instances.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_region_instance_group_managers_insert_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_region_instance_group_managers_insert_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_instance_group_managers_insert_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/regions/{region}/instanceGroupManagers
/// Creates a managed instance group using the information that you specify in the request. After the group is created, instances in the group are created using the specified instance template. This operation is marked as DONE when the group is created even if the instances in the group have not yet been created. You must separately verify the status of the individual instances with thelistmanagedinstances method.  A regional managed instance group can contain up to 2000 instances.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_region_instance_group_managers_insert_builder()` + `compute_region_instance_group_managers_insert_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_instance_group_managers_insert(
    client: &SimpleHttpClient,
    project: &str,
    region: &str,
    requestId: Option<&str>,
    body: &InstanceGroupManager,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_region_instance_group_managers_insert_builder(
        client, project, region, requestId, body,
    )?;
    compute_region_instance_group_managers_insert_execute(builder)
}

/// GET projects/{project}/regions/{region}/instanceGroupManagers
/// Retrieves the list of managed instance groups that are contained within the specified region.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_region_instance_group_managers_list_execute()` to send, or `compute_region_instance_group_managers_list` for simplest API.

pub fn compute_region_instance_group_managers_list_builder(
    client: &SimpleHttpClient,
    project: &str,
    region: &str,
    filter: Option<&str>,
    maxResults: Option<i32>,
    orderBy: Option<&str>,
    pageToken: Option<&str>,
    returnPartialSuccess: Option<bool>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/regions/{}/instanceGroupManagers",
        project, region,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "filter",
            filter
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "maxResults",
            maxResults
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "orderBy",
            orderBy
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageToken",
            pageToken
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "returnPartialSuccess",
            returnPartialSuccess
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET projects/{project}/regions/{region}/instanceGroupManagers
/// Retrieves the list of managed instance groups that are contained within the specified region.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_region_instance_group_managers_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_region_instance_group_managers_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_instance_group_managers_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<RegionInstanceGroupManagerList>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: RegionInstanceGroupManagerList = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/regions/{region}/instanceGroupManagers
/// Retrieves the list of managed instance groups that are contained within the specified region.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_region_instance_group_managers_list_builder()` + `compute_region_instance_group_managers_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_instance_group_managers_list(
    client: &SimpleHttpClient,
    project: &str,
    region: &str,
    filter: Option<&str>,
    maxResults: Option<i32>,
    orderBy: Option<&str>,
    pageToken: Option<&str>,
    returnPartialSuccess: Option<bool>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<RegionInstanceGroupManagerList>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = compute_region_instance_group_managers_list_builder(
        client,
        project,
        region,
        filter,
        maxResults,
        orderBy,
        pageToken,
        returnPartialSuccess,
    )?;
    compute_region_instance_group_managers_list_execute(builder)
}

/// GET projects/{project}/regions/{region}/instanceGroupManagers/{instanceGroupManager}/listErrors
/// Lists all errors thrown by actions on instances for a given regional managed instance group. The filter `andorderBy` query parameters are not supported.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_region_instance_group_managers_list_errors_execute()` to send, or `compute_region_instance_group_managers_list_errors` for simplest API.

pub fn compute_region_instance_group_managers_list_errors_builder(
    client: &SimpleHttpClient,
    instanceGroupManager: &str,
    project: &str,
    region: &str,
    filter: Option<&str>,
    maxResults: Option<i32>,
    orderBy: Option<&str>,
    pageToken: Option<&str>,
    returnPartialSuccess: Option<bool>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/regions/{}/instanceGroupManagers/{}/listErrors",
        instanceGroupManager, project, region,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "filter",
            filter
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "maxResults",
            maxResults
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "orderBy",
            orderBy
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageToken",
            pageToken
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "returnPartialSuccess",
            returnPartialSuccess
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET projects/{project}/regions/{region}/instanceGroupManagers/{instanceGroupManager}/listErrors
/// Lists all errors thrown by actions on instances for a given regional managed instance group. The filter `andorderBy` query parameters are not supported.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_region_instance_group_managers_list_errors_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_region_instance_group_managers_list_errors_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_instance_group_managers_list_errors_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<RegionInstanceGroupManagersListErrorsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: RegionInstanceGroupManagersListErrorsResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/regions/{region}/instanceGroupManagers/{instanceGroupManager}/listErrors
/// Lists all errors thrown by actions on instances for a given regional managed instance group. The filter `andorderBy` query parameters are not supported.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_region_instance_group_managers_list_errors_builder()` + `compute_region_instance_group_managers_list_errors_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_instance_group_managers_list_errors(
    client: &SimpleHttpClient,
    instanceGroupManager: &str,
    project: &str,
    region: &str,
    filter: Option<&str>,
    maxResults: Option<i32>,
    orderBy: Option<&str>,
    pageToken: Option<&str>,
    returnPartialSuccess: Option<bool>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<RegionInstanceGroupManagersListErrorsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = compute_region_instance_group_managers_list_errors_builder(
        client,
        instanceGroupManager,
        project,
        region,
        filter,
        maxResults,
        orderBy,
        pageToken,
        returnPartialSuccess,
    )?;
    compute_region_instance_group_managers_list_errors_execute(builder)
}

/// GET projects/{project}/regions/{region}/instanceGroupManagers/{instanceGroupManager}/listManagedInstances
/// Lists the instances in the managed instance group and instances that are scheduled to be created. The list includes any current actions that the group has scheduled for its instances. The `orderBy` query parameter is not supported.   The `pageToken` query parameter is supported only if the group's `listManagedInstancesResults` field is set to PAGINATED.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_region_instance_group_managers_list_managed_instances_execute()` to send, or `compute_region_instance_group_managers_list_managed_instances` for simplest API.

pub fn compute_region_instance_group_managers_list_managed_instances_builder(
    client: &SimpleHttpClient,
    instanceGroupManager: &str,
    project: &str,
    region: &str,
    filter: Option<&str>,
    maxResults: Option<i32>,
    orderBy: Option<&str>,
    pageToken: Option<&str>,
    returnPartialSuccess: Option<bool>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/regions/{}/instanceGroupManagers/{}/listManagedInstances",
        instanceGroupManager,
        project,
        region,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "filter",
            filter
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "maxResults",
            maxResults
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "orderBy",
            orderBy
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageToken",
            pageToken
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "returnPartialSuccess",
            returnPartialSuccess
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET projects/{project}/regions/{region}/instanceGroupManagers/{instanceGroupManager}/listManagedInstances
/// Lists the instances in the managed instance group and instances that are scheduled to be created. The list includes any current actions that the group has scheduled for its instances. The `orderBy` query parameter is not supported.   The `pageToken` query parameter is supported only if the group's `listManagedInstancesResults` field is set to PAGINATED.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_region_instance_group_managers_list_managed_instances_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_region_instance_group_managers_list_managed_instances_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_instance_group_managers_list_managed_instances_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<RegionInstanceGroupManagersListInstancesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: RegionInstanceGroupManagersListInstancesResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/regions/{region}/instanceGroupManagers/{instanceGroupManager}/listManagedInstances
/// Lists the instances in the managed instance group and instances that are scheduled to be created. The list includes any current actions that the group has scheduled for its instances. The `orderBy` query parameter is not supported.   The `pageToken` query parameter is supported only if the group's `listManagedInstancesResults` field is set to PAGINATED.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_region_instance_group_managers_list_managed_instances_builder()` + `compute_region_instance_group_managers_list_managed_instances_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_instance_group_managers_list_managed_instances(
    client: &SimpleHttpClient,
    instanceGroupManager: &str,
    project: &str,
    region: &str,
    filter: Option<&str>,
    maxResults: Option<i32>,
    orderBy: Option<&str>,
    pageToken: Option<&str>,
    returnPartialSuccess: Option<bool>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<RegionInstanceGroupManagersListInstancesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = compute_region_instance_group_managers_list_managed_instances_builder(
        client,
        instanceGroupManager,
        project,
        region,
        filter,
        maxResults,
        orderBy,
        pageToken,
        returnPartialSuccess,
    )?;
    compute_region_instance_group_managers_list_managed_instances_execute(builder)
}

/// GET projects/{project}/regions/{region}/instanceGroupManagers/{instanceGroupManager}/listPerInstanceConfigs
/// Lists all of the per-instance configurations defined for the managed instance group. The `orderBy` query parameter is not supported.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_region_instance_group_managers_list_per_instance_configs_execute()` to send, or `compute_region_instance_group_managers_list_per_instance_configs` for simplest API.

pub fn compute_region_instance_group_managers_list_per_instance_configs_builder(
    client: &SimpleHttpClient,
    instanceGroupManager: &str,
    project: &str,
    region: &str,
    filter: Option<&str>,
    maxResults: Option<i32>,
    orderBy: Option<&str>,
    pageToken: Option<&str>,
    returnPartialSuccess: Option<bool>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/regions/{}/instanceGroupManagers/{}/listPerInstanceConfigs",
        instanceGroupManager,
        project,
        region,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "filter",
            filter
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "maxResults",
            maxResults
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "orderBy",
            orderBy
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageToken",
            pageToken
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "returnPartialSuccess",
            returnPartialSuccess
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET projects/{project}/regions/{region}/instanceGroupManagers/{instanceGroupManager}/listPerInstanceConfigs
/// Lists all of the per-instance configurations defined for the managed instance group. The `orderBy` query parameter is not supported.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_region_instance_group_managers_list_per_instance_configs_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_region_instance_group_managers_list_per_instance_configs_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_instance_group_managers_list_per_instance_configs_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<RegionInstanceGroupManagersListInstanceConfigsResp>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: RegionInstanceGroupManagersListInstanceConfigsResp =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/regions/{region}/instanceGroupManagers/{instanceGroupManager}/listPerInstanceConfigs
/// Lists all of the per-instance configurations defined for the managed instance group. The `orderBy` query parameter is not supported.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_region_instance_group_managers_list_per_instance_configs_builder()` + `compute_region_instance_group_managers_list_per_instance_configs_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_instance_group_managers_list_per_instance_configs(
    client: &SimpleHttpClient,
    instanceGroupManager: &str,
    project: &str,
    region: &str,
    filter: Option<&str>,
    maxResults: Option<i32>,
    orderBy: Option<&str>,
    pageToken: Option<&str>,
    returnPartialSuccess: Option<bool>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<RegionInstanceGroupManagersListInstanceConfigsResp>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = compute_region_instance_group_managers_list_per_instance_configs_builder(
        client,
        instanceGroupManager,
        project,
        region,
        filter,
        maxResults,
        orderBy,
        pageToken,
        returnPartialSuccess,
    )?;
    compute_region_instance_group_managers_list_per_instance_configs_execute(builder)
}

/// GET projects/{project}/regions/{region}/instanceGroupManagers/{instanceGroupManager}
/// Updates a managed instance group using the information that you specify in the request. This operation is marked as DONE when the group is patched even if the instances in the group are still in the process of being patched. You must separately verify the status of the individual instances with the listmanagedinstances method. This method `supportsPATCH` semantics and uses `theJSON` merge patch format and processing rules.  If you update your group to specify a new template or instance configuration, it's possible that your intended specification for each VM in the group is different from the current state of that VM. To learn how to apply an updated configuration to the VMs in a MIG, `seeUpdating` instances in a MIG.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_region_instance_group_managers_patch_execute()` to send, or `compute_region_instance_group_managers_patch` for simplest API.

pub fn compute_region_instance_group_managers_patch_builder(
    client: &SimpleHttpClient,
    instanceGroupManager: &str,
    project: &str,
    region: &str,
    requestId: Option<&str>,
    body: &InstanceGroupManager,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/regions/{}/instanceGroupManagers/{}",
        instanceGroupManager, project, region,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "requestId",
            requestId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET projects/{project}/regions/{region}/instanceGroupManagers/{instanceGroupManager}
/// Updates a managed instance group using the information that you specify in the request. This operation is marked as DONE when the group is patched even if the instances in the group are still in the process of being patched. You must separately verify the status of the individual instances with the listmanagedinstances method. This method `supportsPATCH` semantics and uses `theJSON` merge patch format and processing rules.  If you update your group to specify a new template or instance configuration, it's possible that your intended specification for each VM in the group is different from the current state of that VM. To learn how to apply an updated configuration to the VMs in a MIG, `seeUpdating` instances in a MIG.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_region_instance_group_managers_patch_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_region_instance_group_managers_patch_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_instance_group_managers_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/regions/{region}/instanceGroupManagers/{instanceGroupManager}
/// Updates a managed instance group using the information that you specify in the request. This operation is marked as DONE when the group is patched even if the instances in the group are still in the process of being patched. You must separately verify the status of the individual instances with the listmanagedinstances method. This method `supportsPATCH` semantics and uses `theJSON` merge patch format and processing rules.  If you update your group to specify a new template or instance configuration, it's possible that your intended specification for each VM in the group is different from the current state of that VM. To learn how to apply an updated configuration to the VMs in a MIG, `seeUpdating` instances in a MIG.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_region_instance_group_managers_patch_builder()` + `compute_region_instance_group_managers_patch_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_instance_group_managers_patch(
    client: &SimpleHttpClient,
    instanceGroupManager: &str,
    project: &str,
    region: &str,
    requestId: Option<&str>,
    body: &InstanceGroupManager,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_region_instance_group_managers_patch_builder(
        client,
        instanceGroupManager,
        project,
        region,
        requestId,
        body,
    )?;
    compute_region_instance_group_managers_patch_execute(builder)
}

/// GET projects/{project}/regions/{region}/instanceGroupManagers/{instanceGroupManager}/patchPerInstanceConfigs
/// Inserts or patches per-instance configurations for the managed instance group. `perInstanceConfig`.name serves as a key used to distinguish whether to perform insert or patch.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_region_instance_group_managers_patch_per_instance_configs_execute()` to send, or `compute_region_instance_group_managers_patch_per_instance_configs` for simplest API.

pub fn compute_region_instance_group_managers_patch_per_instance_configs_builder(
    client: &SimpleHttpClient,
    instanceGroupManager: &str,
    project: &str,
    region: &str,
    requestId: Option<&str>,
    body: &RegionInstanceGroupManagerPatchInstanceConfigReq,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/regions/{}/instanceGroupManagers/{}/patchPerInstanceConfigs",
        instanceGroupManager,
        project,
        region,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "requestId",
            requestId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET projects/{project}/regions/{region}/instanceGroupManagers/{instanceGroupManager}/patchPerInstanceConfigs
/// Inserts or patches per-instance configurations for the managed instance group. `perInstanceConfig`.name serves as a key used to distinguish whether to perform insert or patch.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_region_instance_group_managers_patch_per_instance_configs_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_region_instance_group_managers_patch_per_instance_configs_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_instance_group_managers_patch_per_instance_configs_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/regions/{region}/instanceGroupManagers/{instanceGroupManager}/patchPerInstanceConfigs
/// Inserts or patches per-instance configurations for the managed instance group. `perInstanceConfig`.name serves as a key used to distinguish whether to perform insert or patch.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_region_instance_group_managers_patch_per_instance_configs_builder()` + `compute_region_instance_group_managers_patch_per_instance_configs_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_instance_group_managers_patch_per_instance_configs(
    client: &SimpleHttpClient,
    instanceGroupManager: &str,
    project: &str,
    region: &str,
    requestId: Option<&str>,
    body: &RegionInstanceGroupManagerPatchInstanceConfigReq,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_region_instance_group_managers_patch_per_instance_configs_builder(
        client,
        instanceGroupManager,
        project,
        region,
        requestId,
        body,
    )?;
    compute_region_instance_group_managers_patch_per_instance_configs_execute(builder)
}

/// GET projects/{project}/regions/{region}/instanceGroupManagers/{instanceGroupManager}/recreateInstances
/// Flags the specified VM instances in the managed instance group to be immediately recreated. Each instance is recreated using the group's current configuration. This operation is marked as DONE when the flag is set even if the instances have not yet been recreated. You must separately verify the status of each instance by checking `itscurrentAction` field; for more information, see Checking the status of managed instances.  If the group is part of a backend service that has enabled connection draining, it can take up to 60 seconds after the connection draining duration has elapsed before the VM instance is removed or deleted.  You can specify a maximum of 1000 instances with this method per request.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_region_instance_group_managers_recreate_instances_execute()` to send, or `compute_region_instance_group_managers_recreate_instances` for simplest API.

pub fn compute_region_instance_group_managers_recreate_instances_builder(
    client: &SimpleHttpClient,
    instanceGroupManager: &str,
    project: &str,
    region: &str,
    requestId: Option<&str>,
    body: &RegionInstanceGroupManagersRecreateRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/regions/{}/instanceGroupManagers/{}/recreateInstances",
        instanceGroupManager, project, region,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "requestId",
            requestId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET projects/{project}/regions/{region}/instanceGroupManagers/{instanceGroupManager}/recreateInstances
/// Flags the specified VM instances in the managed instance group to be immediately recreated. Each instance is recreated using the group's current configuration. This operation is marked as DONE when the flag is set even if the instances have not yet been recreated. You must separately verify the status of each instance by checking `itscurrentAction` field; for more information, see Checking the status of managed instances.  If the group is part of a backend service that has enabled connection draining, it can take up to 60 seconds after the connection draining duration has elapsed before the VM instance is removed or deleted.  You can specify a maximum of 1000 instances with this method per request.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_region_instance_group_managers_recreate_instances_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_region_instance_group_managers_recreate_instances_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_instance_group_managers_recreate_instances_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/regions/{region}/instanceGroupManagers/{instanceGroupManager}/recreateInstances
/// Flags the specified VM instances in the managed instance group to be immediately recreated. Each instance is recreated using the group's current configuration. This operation is marked as DONE when the flag is set even if the instances have not yet been recreated. You must separately verify the status of each instance by checking `itscurrentAction` field; for more information, see Checking the status of managed instances.  If the group is part of a backend service that has enabled connection draining, it can take up to 60 seconds after the connection draining duration has elapsed before the VM instance is removed or deleted.  You can specify a maximum of 1000 instances with this method per request.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_region_instance_group_managers_recreate_instances_builder()` + `compute_region_instance_group_managers_recreate_instances_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_instance_group_managers_recreate_instances(
    client: &SimpleHttpClient,
    instanceGroupManager: &str,
    project: &str,
    region: &str,
    requestId: Option<&str>,
    body: &RegionInstanceGroupManagersRecreateRequest,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_region_instance_group_managers_recreate_instances_builder(
        client,
        instanceGroupManager,
        project,
        region,
        requestId,
        body,
    )?;
    compute_region_instance_group_managers_recreate_instances_execute(builder)
}

/// GET projects/{project}/regions/{region}/instanceGroupManagers/{instanceGroupManager}/resize
/// Changes the intended size of the managed instance group. If you increase the size, the group creates new instances using the current instance template. If you decrease the size, the group deletes one or more instances.  The resize operation is marked DONE if theresize request is successful. The underlying actions take additional time. You must separately verify the status of thecreating or deleting actions with thelistmanagedinstances method.  If the group is part of a backend service that has enabled connection draining, it can take up to 60 seconds after the connection draining duration has elapsed before the VM instance is removed or deleted.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_region_instance_group_managers_resize_execute()` to send, or `compute_region_instance_group_managers_resize` for simplest API.

pub fn compute_region_instance_group_managers_resize_builder(
    client: &SimpleHttpClient,
    instanceGroupManager: &str,
    project: &str,
    region: &str,
    requestId: Option<&str>,
    size: Option<i32>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/regions/{}/instanceGroupManagers/{}/resize",
        instanceGroupManager, project, region,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "requestId",
            requestId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "size",
            size.as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET projects/{project}/regions/{region}/instanceGroupManagers/{instanceGroupManager}/resize
/// Changes the intended size of the managed instance group. If you increase the size, the group creates new instances using the current instance template. If you decrease the size, the group deletes one or more instances.  The resize operation is marked DONE if theresize request is successful. The underlying actions take additional time. You must separately verify the status of thecreating or deleting actions with thelistmanagedinstances method.  If the group is part of a backend service that has enabled connection draining, it can take up to 60 seconds after the connection draining duration has elapsed before the VM instance is removed or deleted.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_region_instance_group_managers_resize_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_region_instance_group_managers_resize_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_instance_group_managers_resize_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/regions/{region}/instanceGroupManagers/{instanceGroupManager}/resize
/// Changes the intended size of the managed instance group. If you increase the size, the group creates new instances using the current instance template. If you decrease the size, the group deletes one or more instances.  The resize operation is marked DONE if theresize request is successful. The underlying actions take additional time. You must separately verify the status of thecreating or deleting actions with thelistmanagedinstances method.  If the group is part of a backend service that has enabled connection draining, it can take up to 60 seconds after the connection draining duration has elapsed before the VM instance is removed or deleted.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_region_instance_group_managers_resize_builder()` + `compute_region_instance_group_managers_resize_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_instance_group_managers_resize(
    client: &SimpleHttpClient,
    instanceGroupManager: &str,
    project: &str,
    region: &str,
    requestId: Option<&str>,
    size: Option<i32>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_region_instance_group_managers_resize_builder(
        client,
        instanceGroupManager,
        project,
        region,
        requestId,
        size,
    )?;
    compute_region_instance_group_managers_resize_execute(builder)
}

/// GET projects/{project}/regions/{region}/instanceGroupManagers/{instanceGroupManager}/resumeInstances
/// Flags the specified instances in the managed instance group to be resumed. This method increases `thetargetSize` and decreases the `targetSuspendedSize` of the managed instance group by the number of instances that you resume. The `resumeInstances` operation is marked DONE if the `resumeInstances` request is successful. The underlying actions take additional time. You must separately verify the status of `theRESUMING` action with thelistmanagedinstances method.  In this request, you can only specify instances that are suspended. For example, if an instance was previously suspended using the `suspendInstances` method, it can be resumed using the `resumeInstances` method.  If a health check is attached to the managed instance group, the specified instances will be verified as healthy after they are resumed.  You can specify a maximum of 1000 instances with this method per request.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_region_instance_group_managers_resume_instances_execute()` to send, or `compute_region_instance_group_managers_resume_instances` for simplest API.

pub fn compute_region_instance_group_managers_resume_instances_builder(
    client: &SimpleHttpClient,
    instanceGroupManager: &str,
    project: &str,
    region: &str,
    requestId: Option<&str>,
    body: &RegionInstanceGroupManagersResumeInstancesRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/regions/{}/instanceGroupManagers/{}/resumeInstances",
        instanceGroupManager, project, region,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "requestId",
            requestId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET projects/{project}/regions/{region}/instanceGroupManagers/{instanceGroupManager}/resumeInstances
/// Flags the specified instances in the managed instance group to be resumed. This method increases `thetargetSize` and decreases the `targetSuspendedSize` of the managed instance group by the number of instances that you resume. The `resumeInstances` operation is marked DONE if the `resumeInstances` request is successful. The underlying actions take additional time. You must separately verify the status of `theRESUMING` action with thelistmanagedinstances method.  In this request, you can only specify instances that are suspended. For example, if an instance was previously suspended using the `suspendInstances` method, it can be resumed using the `resumeInstances` method.  If a health check is attached to the managed instance group, the specified instances will be verified as healthy after they are resumed.  You can specify a maximum of 1000 instances with this method per request.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_region_instance_group_managers_resume_instances_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_region_instance_group_managers_resume_instances_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_instance_group_managers_resume_instances_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/regions/{region}/instanceGroupManagers/{instanceGroupManager}/resumeInstances
/// Flags the specified instances in the managed instance group to be resumed. This method increases `thetargetSize` and decreases the `targetSuspendedSize` of the managed instance group by the number of instances that you resume. The `resumeInstances` operation is marked DONE if the `resumeInstances` request is successful. The underlying actions take additional time. You must separately verify the status of `theRESUMING` action with thelistmanagedinstances method.  In this request, you can only specify instances that are suspended. For example, if an instance was previously suspended using the `suspendInstances` method, it can be resumed using the `resumeInstances` method.  If a health check is attached to the managed instance group, the specified instances will be verified as healthy after they are resumed.  You can specify a maximum of 1000 instances with this method per request.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_region_instance_group_managers_resume_instances_builder()` + `compute_region_instance_group_managers_resume_instances_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_instance_group_managers_resume_instances(
    client: &SimpleHttpClient,
    instanceGroupManager: &str,
    project: &str,
    region: &str,
    requestId: Option<&str>,
    body: &RegionInstanceGroupManagersResumeInstancesRequest,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_region_instance_group_managers_resume_instances_builder(
        client,
        instanceGroupManager,
        project,
        region,
        requestId,
        body,
    )?;
    compute_region_instance_group_managers_resume_instances_execute(builder)
}

/// GET projects/{project}/regions/{region}/instanceGroupManagers/{instanceGroupManager}/setInstanceTemplate
/// Sets the instance template to use when creating new instances or recreating instances in this group. Existing instances are not affected.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_region_instance_group_managers_set_instance_template_execute()` to send, or `compute_region_instance_group_managers_set_instance_template` for simplest API.

pub fn compute_region_instance_group_managers_set_instance_template_builder(
    client: &SimpleHttpClient,
    instanceGroupManager: &str,
    project: &str,
    region: &str,
    requestId: Option<&str>,
    body: &RegionInstanceGroupManagersSetTemplateRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/regions/{}/instanceGroupManagers/{}/setInstanceTemplate",
        instanceGroupManager,
        project,
        region,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "requestId",
            requestId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET projects/{project}/regions/{region}/instanceGroupManagers/{instanceGroupManager}/setInstanceTemplate
/// Sets the instance template to use when creating new instances or recreating instances in this group. Existing instances are not affected.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_region_instance_group_managers_set_instance_template_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_region_instance_group_managers_set_instance_template_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_instance_group_managers_set_instance_template_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/regions/{region}/instanceGroupManagers/{instanceGroupManager}/setInstanceTemplate
/// Sets the instance template to use when creating new instances or recreating instances in this group. Existing instances are not affected.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_region_instance_group_managers_set_instance_template_builder()` + `compute_region_instance_group_managers_set_instance_template_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_instance_group_managers_set_instance_template(
    client: &SimpleHttpClient,
    instanceGroupManager: &str,
    project: &str,
    region: &str,
    requestId: Option<&str>,
    body: &RegionInstanceGroupManagersSetTemplateRequest,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_region_instance_group_managers_set_instance_template_builder(
        client,
        instanceGroupManager,
        project,
        region,
        requestId,
        body,
    )?;
    compute_region_instance_group_managers_set_instance_template_execute(builder)
}

/// GET projects/{project}/regions/{region}/instanceGroupManagers/{instanceGroupManager}/setTargetPools
/// Modifies the target pools to which all new instances in this group are assigned. Existing instances in the group are not affected.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_region_instance_group_managers_set_target_pools_execute()` to send, or `compute_region_instance_group_managers_set_target_pools` for simplest API.

pub fn compute_region_instance_group_managers_set_target_pools_builder(
    client: &SimpleHttpClient,
    instanceGroupManager: &str,
    project: &str,
    region: &str,
    requestId: Option<&str>,
    body: &RegionInstanceGroupManagersSetTargetPoolsRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/regions/{}/instanceGroupManagers/{}/setTargetPools",
        instanceGroupManager, project, region,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "requestId",
            requestId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET projects/{project}/regions/{region}/instanceGroupManagers/{instanceGroupManager}/setTargetPools
/// Modifies the target pools to which all new instances in this group are assigned. Existing instances in the group are not affected.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_region_instance_group_managers_set_target_pools_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_region_instance_group_managers_set_target_pools_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_instance_group_managers_set_target_pools_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/regions/{region}/instanceGroupManagers/{instanceGroupManager}/setTargetPools
/// Modifies the target pools to which all new instances in this group are assigned. Existing instances in the group are not affected.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_region_instance_group_managers_set_target_pools_builder()` + `compute_region_instance_group_managers_set_target_pools_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_instance_group_managers_set_target_pools(
    client: &SimpleHttpClient,
    instanceGroupManager: &str,
    project: &str,
    region: &str,
    requestId: Option<&str>,
    body: &RegionInstanceGroupManagersSetTargetPoolsRequest,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_region_instance_group_managers_set_target_pools_builder(
        client,
        instanceGroupManager,
        project,
        region,
        requestId,
        body,
    )?;
    compute_region_instance_group_managers_set_target_pools_execute(builder)
}

/// GET projects/{project}/regions/{region}/instanceGroupManagers/{instanceGroupManager}/startInstances
/// Flags the specified instances in the managed instance group to be started. This method increases `thetargetSize` and decreases the `targetStoppedSize` of the managed instance group by the number of instances that you start. The `startInstances` operation is marked DONE if the `startInstances` request is successful. The underlying actions take additional time. You must separately verify the status of `theSTARTING` action with thelistmanagedinstances method.  In this request, you can only specify instances that are stopped. For example, if an instance was previously stopped using the `stopInstances` method, it can be started using the `startInstances` method.  If a health check is attached to the managed instance group, the specified instances will be verified as healthy after they are started.  You can specify a maximum of 1000 instances with this method per request.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_region_instance_group_managers_start_instances_execute()` to send, or `compute_region_instance_group_managers_start_instances` for simplest API.

pub fn compute_region_instance_group_managers_start_instances_builder(
    client: &SimpleHttpClient,
    instanceGroupManager: &str,
    project: &str,
    region: &str,
    requestId: Option<&str>,
    body: &RegionInstanceGroupManagersStartInstancesRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/regions/{}/instanceGroupManagers/{}/startInstances",
        instanceGroupManager, project, region,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "requestId",
            requestId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET projects/{project}/regions/{region}/instanceGroupManagers/{instanceGroupManager}/startInstances
/// Flags the specified instances in the managed instance group to be started. This method increases `thetargetSize` and decreases the `targetStoppedSize` of the managed instance group by the number of instances that you start. The `startInstances` operation is marked DONE if the `startInstances` request is successful. The underlying actions take additional time. You must separately verify the status of `theSTARTING` action with thelistmanagedinstances method.  In this request, you can only specify instances that are stopped. For example, if an instance was previously stopped using the `stopInstances` method, it can be started using the `startInstances` method.  If a health check is attached to the managed instance group, the specified instances will be verified as healthy after they are started.  You can specify a maximum of 1000 instances with this method per request.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_region_instance_group_managers_start_instances_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_region_instance_group_managers_start_instances_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_instance_group_managers_start_instances_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/regions/{region}/instanceGroupManagers/{instanceGroupManager}/startInstances
/// Flags the specified instances in the managed instance group to be started. This method increases `thetargetSize` and decreases the `targetStoppedSize` of the managed instance group by the number of instances that you start. The `startInstances` operation is marked DONE if the `startInstances` request is successful. The underlying actions take additional time. You must separately verify the status of `theSTARTING` action with thelistmanagedinstances method.  In this request, you can only specify instances that are stopped. For example, if an instance was previously stopped using the `stopInstances` method, it can be started using the `startInstances` method.  If a health check is attached to the managed instance group, the specified instances will be verified as healthy after they are started.  You can specify a maximum of 1000 instances with this method per request.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_region_instance_group_managers_start_instances_builder()` + `compute_region_instance_group_managers_start_instances_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_instance_group_managers_start_instances(
    client: &SimpleHttpClient,
    instanceGroupManager: &str,
    project: &str,
    region: &str,
    requestId: Option<&str>,
    body: &RegionInstanceGroupManagersStartInstancesRequest,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_region_instance_group_managers_start_instances_builder(
        client,
        instanceGroupManager,
        project,
        region,
        requestId,
        body,
    )?;
    compute_region_instance_group_managers_start_instances_execute(builder)
}

/// GET projects/{project}/regions/{region}/instanceGroupManagers/{instanceGroupManager}/stopInstances
/// Flags the specified instances in the managed instance group to be immediately stopped. You can only specify instances that are running in this request. This method reduces `thetargetSize` and increases the `targetStoppedSize` of the managed instance group by the number of instances that you stop. The `stopInstances` operation is marked DONE if the `stopInstances` request is successful. The underlying actions take additional time. You must separately verify the status of `theSTOPPING` action with thelistmanagedinstances method.  If the `standbyPolicy`.`initialDelaySec` field is set, the group delays stopping the instances until `initialDelaySec` have passed from instance.`creationTimestamp` (that is, when the instance was created). This delay gives your application time to set itself up and initialize on the instance. If more `thaninitialDelaySec` seconds have passed sinceinstance.`creationTimestamp` when this method is called, there will be zero delay.  If the group is part of a backend service that has enabled connection draining, it can take up to 60 seconds after the connection draining duration has elapsed before the VM instance is stopped.  Stopped instances can be started using the `startInstances` method.  You can specify a maximum of 1000 instances with this method per request.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_region_instance_group_managers_stop_instances_execute()` to send, or `compute_region_instance_group_managers_stop_instances` for simplest API.

pub fn compute_region_instance_group_managers_stop_instances_builder(
    client: &SimpleHttpClient,
    instanceGroupManager: &str,
    project: &str,
    region: &str,
    requestId: Option<&str>,
    body: &RegionInstanceGroupManagersStopInstancesRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/regions/{}/instanceGroupManagers/{}/stopInstances",
        instanceGroupManager, project, region,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "requestId",
            requestId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET projects/{project}/regions/{region}/instanceGroupManagers/{instanceGroupManager}/stopInstances
/// Flags the specified instances in the managed instance group to be immediately stopped. You can only specify instances that are running in this request. This method reduces `thetargetSize` and increases the `targetStoppedSize` of the managed instance group by the number of instances that you stop. The `stopInstances` operation is marked DONE if the `stopInstances` request is successful. The underlying actions take additional time. You must separately verify the status of `theSTOPPING` action with thelistmanagedinstances method.  If the `standbyPolicy`.`initialDelaySec` field is set, the group delays stopping the instances until `initialDelaySec` have passed from instance.`creationTimestamp` (that is, when the instance was created). This delay gives your application time to set itself up and initialize on the instance. If more `thaninitialDelaySec` seconds have passed sinceinstance.`creationTimestamp` when this method is called, there will be zero delay.  If the group is part of a backend service that has enabled connection draining, it can take up to 60 seconds after the connection draining duration has elapsed before the VM instance is stopped.  Stopped instances can be started using the `startInstances` method.  You can specify a maximum of 1000 instances with this method per request.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_region_instance_group_managers_stop_instances_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_region_instance_group_managers_stop_instances_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_instance_group_managers_stop_instances_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/regions/{region}/instanceGroupManagers/{instanceGroupManager}/stopInstances
/// Flags the specified instances in the managed instance group to be immediately stopped. You can only specify instances that are running in this request. This method reduces `thetargetSize` and increases the `targetStoppedSize` of the managed instance group by the number of instances that you stop. The `stopInstances` operation is marked DONE if the `stopInstances` request is successful. The underlying actions take additional time. You must separately verify the status of `theSTOPPING` action with thelistmanagedinstances method.  If the `standbyPolicy`.`initialDelaySec` field is set, the group delays stopping the instances until `initialDelaySec` have passed from instance.`creationTimestamp` (that is, when the instance was created). This delay gives your application time to set itself up and initialize on the instance. If more `thaninitialDelaySec` seconds have passed sinceinstance.`creationTimestamp` when this method is called, there will be zero delay.  If the group is part of a backend service that has enabled connection draining, it can take up to 60 seconds after the connection draining duration has elapsed before the VM instance is stopped.  Stopped instances can be started using the `startInstances` method.  You can specify a maximum of 1000 instances with this method per request.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_region_instance_group_managers_stop_instances_builder()` + `compute_region_instance_group_managers_stop_instances_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_instance_group_managers_stop_instances(
    client: &SimpleHttpClient,
    instanceGroupManager: &str,
    project: &str,
    region: &str,
    requestId: Option<&str>,
    body: &RegionInstanceGroupManagersStopInstancesRequest,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_region_instance_group_managers_stop_instances_builder(
        client,
        instanceGroupManager,
        project,
        region,
        requestId,
        body,
    )?;
    compute_region_instance_group_managers_stop_instances_execute(builder)
}

/// GET projects/{project}/regions/{region}/instanceGroupManagers/{instanceGroupManager}/suspendInstances
/// Flags the specified instances in the managed instance group to be immediately suspended. You can only specify instances that are running in this request. This method reduces `thetargetSize` and increases the `targetSuspendedSize` of the managed instance group by the number of instances that you suspend. The `suspendInstances` operation is marked DONE if the `suspendInstances` request is successful. The underlying actions take additional time. You must separately verify the status of `theSUSPENDING` action with thelistmanagedinstances method.  If the `standbyPolicy`.`initialDelaySec` field is set, the group delays suspension of the instances until `initialDelaySec` have passed from instance.`creationTimestamp` (that is, when the instance was created). This delay gives your application time to set itself up and initialize on the instance. If more `thaninitialDelaySec` seconds have passed sinceinstance.`creationTimestamp` when this method is called, there will be zero delay.  If the group is part of a backend service that has enabled connection draining, it can take up to 60 seconds after the connection draining duration has elapsed before the VM instance is suspended.  Suspended instances can be resumed using the `resumeInstances` method.  You can specify a maximum of 1000 instances with this method per request.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_region_instance_group_managers_suspend_instances_execute()` to send, or `compute_region_instance_group_managers_suspend_instances` for simplest API.

pub fn compute_region_instance_group_managers_suspend_instances_builder(
    client: &SimpleHttpClient,
    instanceGroupManager: &str,
    project: &str,
    region: &str,
    requestId: Option<&str>,
    body: &RegionInstanceGroupManagersSuspendInstancesRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/regions/{}/instanceGroupManagers/{}/suspendInstances",
        instanceGroupManager, project, region,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "requestId",
            requestId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET projects/{project}/regions/{region}/instanceGroupManagers/{instanceGroupManager}/suspendInstances
/// Flags the specified instances in the managed instance group to be immediately suspended. You can only specify instances that are running in this request. This method reduces `thetargetSize` and increases the `targetSuspendedSize` of the managed instance group by the number of instances that you suspend. The `suspendInstances` operation is marked DONE if the `suspendInstances` request is successful. The underlying actions take additional time. You must separately verify the status of `theSUSPENDING` action with thelistmanagedinstances method.  If the `standbyPolicy`.`initialDelaySec` field is set, the group delays suspension of the instances until `initialDelaySec` have passed from instance.`creationTimestamp` (that is, when the instance was created). This delay gives your application time to set itself up and initialize on the instance. If more `thaninitialDelaySec` seconds have passed sinceinstance.`creationTimestamp` when this method is called, there will be zero delay.  If the group is part of a backend service that has enabled connection draining, it can take up to 60 seconds after the connection draining duration has elapsed before the VM instance is suspended.  Suspended instances can be resumed using the `resumeInstances` method.  You can specify a maximum of 1000 instances with this method per request.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_region_instance_group_managers_suspend_instances_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_region_instance_group_managers_suspend_instances_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_instance_group_managers_suspend_instances_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/regions/{region}/instanceGroupManagers/{instanceGroupManager}/suspendInstances
/// Flags the specified instances in the managed instance group to be immediately suspended. You can only specify instances that are running in this request. This method reduces `thetargetSize` and increases the `targetSuspendedSize` of the managed instance group by the number of instances that you suspend. The `suspendInstances` operation is marked DONE if the `suspendInstances` request is successful. The underlying actions take additional time. You must separately verify the status of `theSUSPENDING` action with thelistmanagedinstances method.  If the `standbyPolicy`.`initialDelaySec` field is set, the group delays suspension of the instances until `initialDelaySec` have passed from instance.`creationTimestamp` (that is, when the instance was created). This delay gives your application time to set itself up and initialize on the instance. If more `thaninitialDelaySec` seconds have passed sinceinstance.`creationTimestamp` when this method is called, there will be zero delay.  If the group is part of a backend service that has enabled connection draining, it can take up to 60 seconds after the connection draining duration has elapsed before the VM instance is suspended.  Suspended instances can be resumed using the `resumeInstances` method.  You can specify a maximum of 1000 instances with this method per request.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_region_instance_group_managers_suspend_instances_builder()` + `compute_region_instance_group_managers_suspend_instances_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_instance_group_managers_suspend_instances(
    client: &SimpleHttpClient,
    instanceGroupManager: &str,
    project: &str,
    region: &str,
    requestId: Option<&str>,
    body: &RegionInstanceGroupManagersSuspendInstancesRequest,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_region_instance_group_managers_suspend_instances_builder(
        client,
        instanceGroupManager,
        project,
        region,
        requestId,
        body,
    )?;
    compute_region_instance_group_managers_suspend_instances_execute(builder)
}

/// GET projects/{project}/regions/{region}/instanceGroupManagers/{instanceGroupManager}/updatePerInstanceConfigs
/// Inserts or updates per-instance configurations for the managed instance group. `perInstanceConfig`.name serves as a key used to distinguish whether to perform insert or patch.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_region_instance_group_managers_update_per_instance_configs_execute()` to send, or `compute_region_instance_group_managers_update_per_instance_configs` for simplest API.

pub fn compute_region_instance_group_managers_update_per_instance_configs_builder(
    client: &SimpleHttpClient,
    instanceGroupManager: &str,
    project: &str,
    region: &str,
    requestId: Option<&str>,
    body: &RegionInstanceGroupManagerUpdateInstanceConfigReq,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/regions/{}/instanceGroupManagers/{}/updatePerInstanceConfigs",
        instanceGroupManager,
        project,
        region,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "requestId",
            requestId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET projects/{project}/regions/{region}/instanceGroupManagers/{instanceGroupManager}/updatePerInstanceConfigs
/// Inserts or updates per-instance configurations for the managed instance group. `perInstanceConfig`.name serves as a key used to distinguish whether to perform insert or patch.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_region_instance_group_managers_update_per_instance_configs_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_region_instance_group_managers_update_per_instance_configs_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_instance_group_managers_update_per_instance_configs_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/regions/{region}/instanceGroupManagers/{instanceGroupManager}/updatePerInstanceConfigs
/// Inserts or updates per-instance configurations for the managed instance group. `perInstanceConfig`.name serves as a key used to distinguish whether to perform insert or patch.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_region_instance_group_managers_update_per_instance_configs_builder()` + `compute_region_instance_group_managers_update_per_instance_configs_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_instance_group_managers_update_per_instance_configs(
    client: &SimpleHttpClient,
    instanceGroupManager: &str,
    project: &str,
    region: &str,
    requestId: Option<&str>,
    body: &RegionInstanceGroupManagerUpdateInstanceConfigReq,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_region_instance_group_managers_update_per_instance_configs_builder(
        client,
        instanceGroupManager,
        project,
        region,
        requestId,
        body,
    )?;
    compute_region_instance_group_managers_update_per_instance_configs_execute(builder)
}

/// GET projects/{project}/regions/{region}/instanceGroups/{instanceGroup}
/// Returns the specified instance group resource.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_region_instance_groups_get_execute()` to send, or `compute_region_instance_groups_get` for simplest API.

pub fn compute_region_instance_groups_get_builder(
    client: &SimpleHttpClient,
    instanceGroup: &str,
    project: &str,
    region: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/regions/{}/instanceGroups/{}",
        instanceGroup, project, region,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/regions/{region}/instanceGroups/{instanceGroup}
/// Returns the specified instance group resource.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_region_instance_groups_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_region_instance_groups_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_instance_groups_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<InstanceGroup>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: InstanceGroup = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/regions/{region}/instanceGroups/{instanceGroup}
/// Returns the specified instance group resource.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_region_instance_groups_get_builder()` + `compute_region_instance_groups_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_instance_groups_get(
    client: &SimpleHttpClient,
    instanceGroup: &str,
    project: &str,
    region: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<InstanceGroup>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        compute_region_instance_groups_get_builder(client, instanceGroup, project, region)?;
    compute_region_instance_groups_get_execute(builder)
}

/// GET projects/{project}/regions/{region}/instanceGroups
/// Retrieves the list of instance group resources contained within the specified region.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_region_instance_groups_list_execute()` to send, or `compute_region_instance_groups_list` for simplest API.

pub fn compute_region_instance_groups_list_builder(
    client: &SimpleHttpClient,
    project: &str,
    region: &str,
    filter: Option<&str>,
    maxResults: Option<i32>,
    orderBy: Option<&str>,
    pageToken: Option<&str>,
    returnPartialSuccess: Option<bool>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/regions/{}/instanceGroups",
        project, region,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "filter",
            filter
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "maxResults",
            maxResults
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "orderBy",
            orderBy
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageToken",
            pageToken
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "returnPartialSuccess",
            returnPartialSuccess
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET projects/{project}/regions/{region}/instanceGroups
/// Retrieves the list of instance group resources contained within the specified region.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_region_instance_groups_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_region_instance_groups_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_instance_groups_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<RegionInstanceGroupList>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: RegionInstanceGroupList = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/regions/{region}/instanceGroups
/// Retrieves the list of instance group resources contained within the specified region.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_region_instance_groups_list_builder()` + `compute_region_instance_groups_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_instance_groups_list(
    client: &SimpleHttpClient,
    project: &str,
    region: &str,
    filter: Option<&str>,
    maxResults: Option<i32>,
    orderBy: Option<&str>,
    pageToken: Option<&str>,
    returnPartialSuccess: Option<bool>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<RegionInstanceGroupList>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = compute_region_instance_groups_list_builder(
        client,
        project,
        region,
        filter,
        maxResults,
        orderBy,
        pageToken,
        returnPartialSuccess,
    )?;
    compute_region_instance_groups_list_execute(builder)
}

/// GET projects/{project}/regions/{region}/instanceGroups/{instanceGroup}/listInstances
/// Lists the instances in the specified instance group and displays information about the named ports. Depending on the specified options, this method can list all instances or only the instances that are running. The `orderBy` query parameter is not supported.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_region_instance_groups_list_instances_execute()` to send, or `compute_region_instance_groups_list_instances` for simplest API.

pub fn compute_region_instance_groups_list_instances_builder(
    client: &SimpleHttpClient,
    instanceGroup: &str,
    project: &str,
    region: &str,
    filter: Option<&str>,
    maxResults: Option<i32>,
    orderBy: Option<&str>,
    pageToken: Option<&str>,
    returnPartialSuccess: Option<bool>,
    body: &RegionInstanceGroupsListInstancesRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/regions/{}/instanceGroups/{}/listInstances",
        instanceGroup, project, region,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "filter",
            filter
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "maxResults",
            maxResults
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "orderBy",
            orderBy
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageToken",
            pageToken
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "returnPartialSuccess",
            returnPartialSuccess
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET projects/{project}/regions/{region}/instanceGroups/{instanceGroup}/listInstances
/// Lists the instances in the specified instance group and displays information about the named ports. Depending on the specified options, this method can list all instances or only the instances that are running. The `orderBy` query parameter is not supported.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_region_instance_groups_list_instances_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_region_instance_groups_list_instances_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_instance_groups_list_instances_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<RegionInstanceGroupsListInstances>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: RegionInstanceGroupsListInstances = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/regions/{region}/instanceGroups/{instanceGroup}/listInstances
/// Lists the instances in the specified instance group and displays information about the named ports. Depending on the specified options, this method can list all instances or only the instances that are running. The `orderBy` query parameter is not supported.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_region_instance_groups_list_instances_builder()` + `compute_region_instance_groups_list_instances_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_instance_groups_list_instances(
    client: &SimpleHttpClient,
    instanceGroup: &str,
    project: &str,
    region: &str,
    filter: Option<&str>,
    maxResults: Option<i32>,
    orderBy: Option<&str>,
    pageToken: Option<&str>,
    returnPartialSuccess: Option<bool>,
    body: &RegionInstanceGroupsListInstancesRequest,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<RegionInstanceGroupsListInstances>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = compute_region_instance_groups_list_instances_builder(
        client,
        instanceGroup,
        project,
        region,
        filter,
        maxResults,
        orderBy,
        pageToken,
        returnPartialSuccess,
        body,
    )?;
    compute_region_instance_groups_list_instances_execute(builder)
}

/// GET projects/{project}/regions/{region}/instanceGroups/{instanceGroup}/setNamedPorts
/// Sets the named ports for the specified regional instance group.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_region_instance_groups_set_named_ports_execute()` to send, or `compute_region_instance_groups_set_named_ports` for simplest API.

pub fn compute_region_instance_groups_set_named_ports_builder(
    client: &SimpleHttpClient,
    instanceGroup: &str,
    project: &str,
    region: &str,
    requestId: Option<&str>,
    body: &RegionInstanceGroupsSetNamedPortsRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/regions/{}/instanceGroups/{}/setNamedPorts",
        instanceGroup, project, region,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "requestId",
            requestId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET projects/{project}/regions/{region}/instanceGroups/{instanceGroup}/setNamedPorts
/// Sets the named ports for the specified regional instance group.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_region_instance_groups_set_named_ports_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_region_instance_groups_set_named_ports_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_instance_groups_set_named_ports_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/regions/{region}/instanceGroups/{instanceGroup}/setNamedPorts
/// Sets the named ports for the specified regional instance group.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_region_instance_groups_set_named_ports_builder()` + `compute_region_instance_groups_set_named_ports_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_instance_groups_set_named_ports(
    client: &SimpleHttpClient,
    instanceGroup: &str,
    project: &str,
    region: &str,
    requestId: Option<&str>,
    body: &RegionInstanceGroupsSetNamedPortsRequest,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_region_instance_groups_set_named_ports_builder(
        client,
        instanceGroup,
        project,
        region,
        requestId,
        body,
    )?;
    compute_region_instance_groups_set_named_ports_execute(builder)
}

/// GET projects/{project}/regions/{region}/instanceGroups/{resource}/testIamPermissions
/// Returns permissions that a caller has on the specified resource.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_region_instance_groups_test_iam_permissions_execute()` to send, or `compute_region_instance_groups_test_iam_permissions` for simplest API.

pub fn compute_region_instance_groups_test_iam_permissions_builder(
    client: &SimpleHttpClient,
    project: &str,
    region: &str,
    resource: &str,
    body: &TestPermissionsRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/regions/{}/instanceGroups/{}/testIamPermissions",
        project, region, resource,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET projects/{project}/regions/{region}/instanceGroups/{resource}/testIamPermissions
/// Returns permissions that a caller has on the specified resource.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_region_instance_groups_test_iam_permissions_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_region_instance_groups_test_iam_permissions_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_instance_groups_test_iam_permissions_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<TestPermissionsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: TestPermissionsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/regions/{region}/instanceGroups/{resource}/testIamPermissions
/// Returns permissions that a caller has on the specified resource.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_region_instance_groups_test_iam_permissions_builder()` + `compute_region_instance_groups_test_iam_permissions_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_instance_groups_test_iam_permissions(
    client: &SimpleHttpClient,
    project: &str,
    region: &str,
    resource: &str,
    body: &TestPermissionsRequest,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<TestPermissionsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = compute_region_instance_groups_test_iam_permissions_builder(
        client, project, region, resource, body,
    )?;
    compute_region_instance_groups_test_iam_permissions_execute(builder)
}

/// GET projects/{project}/regions/{region}/instanceTemplates/{instanceTemplate}
/// Deletes the specified instance template. Deleting an instance template is permanent and cannot be undone.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_region_instance_templates_delete_execute()` to send, or `compute_region_instance_templates_delete` for simplest API.

pub fn compute_region_instance_templates_delete_builder(
    client: &SimpleHttpClient,
    instanceTemplate: &str,
    project: &str,
    region: &str,
    requestId: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/regions/{}/instanceTemplates/{}",
        instanceTemplate, project, region,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "requestId",
            requestId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET projects/{project}/regions/{region}/instanceTemplates/{instanceTemplate}
/// Deletes the specified instance template. Deleting an instance template is permanent and cannot be undone.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_region_instance_templates_delete_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_region_instance_templates_delete_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_instance_templates_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/regions/{region}/instanceTemplates/{instanceTemplate}
/// Deletes the specified instance template. Deleting an instance template is permanent and cannot be undone.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_region_instance_templates_delete_builder()` + `compute_region_instance_templates_delete_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_instance_templates_delete(
    client: &SimpleHttpClient,
    instanceTemplate: &str,
    project: &str,
    region: &str,
    requestId: Option<&str>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_region_instance_templates_delete_builder(
        client,
        instanceTemplate,
        project,
        region,
        requestId,
    )?;
    compute_region_instance_templates_delete_execute(builder)
}

/// GET projects/{project}/regions/{region}/instanceTemplates/{instanceTemplate}
/// Returns the specified instance template.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_region_instance_templates_get_execute()` to send, or `compute_region_instance_templates_get` for simplest API.

pub fn compute_region_instance_templates_get_builder(
    client: &SimpleHttpClient,
    instanceTemplate: &str,
    project: &str,
    region: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/regions/{}/instanceTemplates/{}",
        instanceTemplate, project, region,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/regions/{region}/instanceTemplates/{instanceTemplate}
/// Returns the specified instance template.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_region_instance_templates_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_region_instance_templates_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_instance_templates_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<InstanceTemplate>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: InstanceTemplate = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/regions/{region}/instanceTemplates/{instanceTemplate}
/// Returns the specified instance template.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_region_instance_templates_get_builder()` + `compute_region_instance_templates_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_instance_templates_get(
    client: &SimpleHttpClient,
    instanceTemplate: &str,
    project: &str,
    region: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<InstanceTemplate>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        compute_region_instance_templates_get_builder(client, instanceTemplate, project, region)?;
    compute_region_instance_templates_get_execute(builder)
}

/// GET projects/{project}/regions/{region}/instanceTemplates
/// Creates an instance template in the specified project and region using the global instance template whose URL is included in the request.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_region_instance_templates_insert_execute()` to send, or `compute_region_instance_templates_insert` for simplest API.

pub fn compute_region_instance_templates_insert_builder(
    client: &SimpleHttpClient,
    project: &str,
    region: &str,
    requestId: Option<&str>,
    body: &InstanceTemplate,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/regions/{}/instanceTemplates",
        project, region,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "requestId",
            requestId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET projects/{project}/regions/{region}/instanceTemplates
/// Creates an instance template in the specified project and region using the global instance template whose URL is included in the request.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_region_instance_templates_insert_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_region_instance_templates_insert_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_instance_templates_insert_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/regions/{region}/instanceTemplates
/// Creates an instance template in the specified project and region using the global instance template whose URL is included in the request.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_region_instance_templates_insert_builder()` + `compute_region_instance_templates_insert_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_instance_templates_insert(
    client: &SimpleHttpClient,
    project: &str,
    region: &str,
    requestId: Option<&str>,
    body: &InstanceTemplate,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        compute_region_instance_templates_insert_builder(client, project, region, requestId, body)?;
    compute_region_instance_templates_insert_execute(builder)
}

/// GET projects/{project}/regions/{region}/instanceTemplates
/// Retrieves a list of instance templates that are contained within the specified project and region.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_region_instance_templates_list_execute()` to send, or `compute_region_instance_templates_list` for simplest API.

pub fn compute_region_instance_templates_list_builder(
    client: &SimpleHttpClient,
    project: &str,
    region: &str,
    filter: Option<&str>,
    maxResults: Option<i32>,
    orderBy: Option<&str>,
    pageToken: Option<&str>,
    returnPartialSuccess: Option<bool>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/regions/{}/instanceTemplates",
        project, region,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "filter",
            filter
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "maxResults",
            maxResults
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "orderBy",
            orderBy
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageToken",
            pageToken
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "returnPartialSuccess",
            returnPartialSuccess
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET projects/{project}/regions/{region}/instanceTemplates
/// Retrieves a list of instance templates that are contained within the specified project and region.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_region_instance_templates_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_region_instance_templates_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_instance_templates_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<InstanceTemplateList>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: InstanceTemplateList = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/regions/{region}/instanceTemplates
/// Retrieves a list of instance templates that are contained within the specified project and region.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_region_instance_templates_list_builder()` + `compute_region_instance_templates_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_instance_templates_list(
    client: &SimpleHttpClient,
    project: &str,
    region: &str,
    filter: Option<&str>,
    maxResults: Option<i32>,
    orderBy: Option<&str>,
    pageToken: Option<&str>,
    returnPartialSuccess: Option<bool>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<InstanceTemplateList>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = compute_region_instance_templates_list_builder(
        client,
        project,
        region,
        filter,
        maxResults,
        orderBy,
        pageToken,
        returnPartialSuccess,
    )?;
    compute_region_instance_templates_list_execute(builder)
}

/// GET projects/{project}/regions/{region}/instances/bulkInsert
/// Creates multiple instances in a given region. Count specifies the number of instances to create.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_region_instances_bulk_insert_execute()` to send, or `compute_region_instances_bulk_insert` for simplest API.

pub fn compute_region_instances_bulk_insert_builder(
    client: &SimpleHttpClient,
    project: &str,
    region: &str,
    requestId: Option<&str>,
    body: &BulkInsertInstanceResource,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/regions/{}/instances/bulkInsert",
        project, region,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "requestId",
            requestId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET projects/{project}/regions/{region}/instances/bulkInsert
/// Creates multiple instances in a given region. Count specifies the number of instances to create.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_region_instances_bulk_insert_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_region_instances_bulk_insert_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_instances_bulk_insert_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/regions/{region}/instances/bulkInsert
/// Creates multiple instances in a given region. Count specifies the number of instances to create.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_region_instances_bulk_insert_builder()` + `compute_region_instances_bulk_insert_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_instances_bulk_insert(
    client: &SimpleHttpClient,
    project: &str,
    region: &str,
    requestId: Option<&str>,
    body: &BulkInsertInstanceResource,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        compute_region_instances_bulk_insert_builder(client, project, region, requestId, body)?;
    compute_region_instances_bulk_insert_execute(builder)
}

/// GET projects/{project}/regions/{region}/instantSnapshots/{instantSnapshot}
/// Deletes the specified InstantSnapshot resource. Keep in mind that deleting a single `instantSnapshot` might not necessarily delete all the data on that `instantSnapshot`. If any data on the `instantSnapshot` that is marked for deletion is needed for subsequent `instantSnapshots`, the data will be moved to the next corresponding `instantSnapshot`.  For more information, `seeDeleting` `instantSnapshots`.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_region_instant_snapshots_delete_execute()` to send, or `compute_region_instant_snapshots_delete` for simplest API.

pub fn compute_region_instant_snapshots_delete_builder(
    client: &SimpleHttpClient,
    instantSnapshot: &str,
    project: &str,
    region: &str,
    requestId: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/regions/{}/instantSnapshots/{}",
        instantSnapshot, project, region,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "requestId",
            requestId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET projects/{project}/regions/{region}/instantSnapshots/{instantSnapshot}
/// Deletes the specified InstantSnapshot resource. Keep in mind that deleting a single `instantSnapshot` might not necessarily delete all the data on that `instantSnapshot`. If any data on the `instantSnapshot` that is marked for deletion is needed for subsequent `instantSnapshots`, the data will be moved to the next corresponding `instantSnapshot`.  For more information, `seeDeleting` `instantSnapshots`.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_region_instant_snapshots_delete_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_region_instant_snapshots_delete_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_instant_snapshots_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/regions/{region}/instantSnapshots/{instantSnapshot}
/// Deletes the specified InstantSnapshot resource. Keep in mind that deleting a single `instantSnapshot` might not necessarily delete all the data on that `instantSnapshot`. If any data on the `instantSnapshot` that is marked for deletion is needed for subsequent `instantSnapshots`, the data will be moved to the next corresponding `instantSnapshot`.  For more information, `seeDeleting` `instantSnapshots`.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_region_instant_snapshots_delete_builder()` + `compute_region_instant_snapshots_delete_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_instant_snapshots_delete(
    client: &SimpleHttpClient,
    instantSnapshot: &str,
    project: &str,
    region: &str,
    requestId: Option<&str>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_region_instant_snapshots_delete_builder(
        client,
        instantSnapshot,
        project,
        region,
        requestId,
    )?;
    compute_region_instant_snapshots_delete_execute(builder)
}

/// GET projects/{project}/regions/{region}/instantSnapshots/{instantSnapshot}
/// Returns the specified InstantSnapshot resource in the specified region.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_region_instant_snapshots_get_execute()` to send, or `compute_region_instant_snapshots_get` for simplest API.

pub fn compute_region_instant_snapshots_get_builder(
    client: &SimpleHttpClient,
    instantSnapshot: &str,
    project: &str,
    region: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/regions/{}/instantSnapshots/{}",
        instantSnapshot, project, region,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/regions/{region}/instantSnapshots/{instantSnapshot}
/// Returns the specified InstantSnapshot resource in the specified region.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_region_instant_snapshots_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_region_instant_snapshots_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_instant_snapshots_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<InstantSnapshot>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: InstantSnapshot = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/regions/{region}/instantSnapshots/{instantSnapshot}
/// Returns the specified InstantSnapshot resource in the specified region.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_region_instant_snapshots_get_builder()` + `compute_region_instant_snapshots_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_instant_snapshots_get(
    client: &SimpleHttpClient,
    instantSnapshot: &str,
    project: &str,
    region: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<InstantSnapshot>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        compute_region_instant_snapshots_get_builder(client, instantSnapshot, project, region)?;
    compute_region_instant_snapshots_get_execute(builder)
}

/// GET projects/{project}/regions/{region}/instantSnapshots/{resource}/getIamPolicy
/// Gets the access control policy for a resource. May be empty if no such policy or resource exists.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_region_instant_snapshots_get_iam_policy_execute()` to send, or `compute_region_instant_snapshots_get_iam_policy` for simplest API.

pub fn compute_region_instant_snapshots_get_iam_policy_builder(
    client: &SimpleHttpClient,
    project: &str,
    region: &str,
    resource: &str,
    optionsRequestedPolicyVersion: Option<i32>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/regions/{}/instantSnapshots/{}/getIamPolicy",
        project, region, resource,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "optionsRequestedPolicyVersion",
            optionsRequestedPolicyVersion
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET projects/{project}/regions/{region}/instantSnapshots/{resource}/getIamPolicy
/// Gets the access control policy for a resource. May be empty if no such policy or resource exists.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_region_instant_snapshots_get_iam_policy_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_region_instant_snapshots_get_iam_policy_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_instant_snapshots_get_iam_policy_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Policy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Policy = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/regions/{region}/instantSnapshots/{resource}/getIamPolicy
/// Gets the access control policy for a resource. May be empty if no such policy or resource exists.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_region_instant_snapshots_get_iam_policy_builder()` + `compute_region_instant_snapshots_get_iam_policy_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_instant_snapshots_get_iam_policy(
    client: &SimpleHttpClient,
    project: &str,
    region: &str,
    resource: &str,
    optionsRequestedPolicyVersion: Option<i32>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Policy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_region_instant_snapshots_get_iam_policy_builder(
        client,
        project,
        region,
        resource,
        optionsRequestedPolicyVersion,
    )?;
    compute_region_instant_snapshots_get_iam_policy_execute(builder)
}

/// GET projects/{project}/regions/{region}/instantSnapshots
/// Creates an instant snapshot in the specified region.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_region_instant_snapshots_insert_execute()` to send, or `compute_region_instant_snapshots_insert` for simplest API.

pub fn compute_region_instant_snapshots_insert_builder(
    client: &SimpleHttpClient,
    project: &str,
    region: &str,
    requestId: Option<&str>,
    body: &InstantSnapshot,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/regions/{}/instantSnapshots",
        project, region,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "requestId",
            requestId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET projects/{project}/regions/{region}/instantSnapshots
/// Creates an instant snapshot in the specified region.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_region_instant_snapshots_insert_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_region_instant_snapshots_insert_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_instant_snapshots_insert_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/regions/{region}/instantSnapshots
/// Creates an instant snapshot in the specified region.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_region_instant_snapshots_insert_builder()` + `compute_region_instant_snapshots_insert_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_instant_snapshots_insert(
    client: &SimpleHttpClient,
    project: &str,
    region: &str,
    requestId: Option<&str>,
    body: &InstantSnapshot,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        compute_region_instant_snapshots_insert_builder(client, project, region, requestId, body)?;
    compute_region_instant_snapshots_insert_execute(builder)
}

/// GET projects/{project}/regions/{region}/instantSnapshots
/// Retrieves the list of InstantSnapshot resources contained within the specified region.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_region_instant_snapshots_list_execute()` to send, or `compute_region_instant_snapshots_list` for simplest API.

pub fn compute_region_instant_snapshots_list_builder(
    client: &SimpleHttpClient,
    project: &str,
    region: &str,
    filter: Option<&str>,
    maxResults: Option<i32>,
    orderBy: Option<&str>,
    pageToken: Option<&str>,
    returnPartialSuccess: Option<bool>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/regions/{}/instantSnapshots",
        project, region,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "filter",
            filter
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "maxResults",
            maxResults
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "orderBy",
            orderBy
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageToken",
            pageToken
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "returnPartialSuccess",
            returnPartialSuccess
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET projects/{project}/regions/{region}/instantSnapshots
/// Retrieves the list of InstantSnapshot resources contained within the specified region.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_region_instant_snapshots_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_region_instant_snapshots_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_instant_snapshots_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<InstantSnapshotList>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: InstantSnapshotList = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/regions/{region}/instantSnapshots
/// Retrieves the list of InstantSnapshot resources contained within the specified region.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_region_instant_snapshots_list_builder()` + `compute_region_instant_snapshots_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_instant_snapshots_list(
    client: &SimpleHttpClient,
    project: &str,
    region: &str,
    filter: Option<&str>,
    maxResults: Option<i32>,
    orderBy: Option<&str>,
    pageToken: Option<&str>,
    returnPartialSuccess: Option<bool>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<InstantSnapshotList>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = compute_region_instant_snapshots_list_builder(
        client,
        project,
        region,
        filter,
        maxResults,
        orderBy,
        pageToken,
        returnPartialSuccess,
    )?;
    compute_region_instant_snapshots_list_execute(builder)
}

/// GET projects/{project}/regions/{region}/instantSnapshots/{resource}/setIamPolicy
/// Sets the access control policy on the specified resource. Replaces any existing policy.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_region_instant_snapshots_set_iam_policy_execute()` to send, or `compute_region_instant_snapshots_set_iam_policy` for simplest API.

pub fn compute_region_instant_snapshots_set_iam_policy_builder(
    client: &SimpleHttpClient,
    project: &str,
    region: &str,
    resource: &str,
    body: &RegionSetPolicyRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/regions/{}/instantSnapshots/{}/setIamPolicy",
        project, region, resource,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET projects/{project}/regions/{region}/instantSnapshots/{resource}/setIamPolicy
/// Sets the access control policy on the specified resource. Replaces any existing policy.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_region_instant_snapshots_set_iam_policy_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_region_instant_snapshots_set_iam_policy_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_instant_snapshots_set_iam_policy_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Policy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Policy = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/regions/{region}/instantSnapshots/{resource}/setIamPolicy
/// Sets the access control policy on the specified resource. Replaces any existing policy.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_region_instant_snapshots_set_iam_policy_builder()` + `compute_region_instant_snapshots_set_iam_policy_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_instant_snapshots_set_iam_policy(
    client: &SimpleHttpClient,
    project: &str,
    region: &str,
    resource: &str,
    body: &RegionSetPolicyRequest,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Policy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_region_instant_snapshots_set_iam_policy_builder(
        client, project, region, resource, body,
    )?;
    compute_region_instant_snapshots_set_iam_policy_execute(builder)
}

/// GET projects/{project}/regions/{region}/instantSnapshots/{resource}/setLabels
/// Sets the labels on a `instantSnapshot` in the given region. To learn more about labels, read the Labeling Resources documentation.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_region_instant_snapshots_set_labels_execute()` to send, or `compute_region_instant_snapshots_set_labels` for simplest API.

pub fn compute_region_instant_snapshots_set_labels_builder(
    client: &SimpleHttpClient,
    project: &str,
    region: &str,
    resource: &str,
    requestId: Option<&str>,
    body: &RegionSetLabelsRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/regions/{}/instantSnapshots/{}/setLabels",
        project, region, resource,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "requestId",
            requestId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET projects/{project}/regions/{region}/instantSnapshots/{resource}/setLabels
/// Sets the labels on a `instantSnapshot` in the given region. To learn more about labels, read the Labeling Resources documentation.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_region_instant_snapshots_set_labels_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_region_instant_snapshots_set_labels_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_instant_snapshots_set_labels_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/regions/{region}/instantSnapshots/{resource}/setLabels
/// Sets the labels on a `instantSnapshot` in the given region. To learn more about labels, read the Labeling Resources documentation.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_region_instant_snapshots_set_labels_builder()` + `compute_region_instant_snapshots_set_labels_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_instant_snapshots_set_labels(
    client: &SimpleHttpClient,
    project: &str,
    region: &str,
    resource: &str,
    requestId: Option<&str>,
    body: &RegionSetLabelsRequest,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_region_instant_snapshots_set_labels_builder(
        client, project, region, resource, requestId, body,
    )?;
    compute_region_instant_snapshots_set_labels_execute(builder)
}

/// GET projects/{project}/regions/{region}/instantSnapshots/{resource}/testIamPermissions
/// Returns permissions that a caller has on the specified resource.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_region_instant_snapshots_test_iam_permissions_execute()` to send, or `compute_region_instant_snapshots_test_iam_permissions` for simplest API.

pub fn compute_region_instant_snapshots_test_iam_permissions_builder(
    client: &SimpleHttpClient,
    project: &str,
    region: &str,
    resource: &str,
    body: &TestPermissionsRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/regions/{}/instantSnapshots/{}/testIamPermissions",
        project, region, resource,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET projects/{project}/regions/{region}/instantSnapshots/{resource}/testIamPermissions
/// Returns permissions that a caller has on the specified resource.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_region_instant_snapshots_test_iam_permissions_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_region_instant_snapshots_test_iam_permissions_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_instant_snapshots_test_iam_permissions_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<TestPermissionsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: TestPermissionsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/regions/{region}/instantSnapshots/{resource}/testIamPermissions
/// Returns permissions that a caller has on the specified resource.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_region_instant_snapshots_test_iam_permissions_builder()` + `compute_region_instant_snapshots_test_iam_permissions_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_instant_snapshots_test_iam_permissions(
    client: &SimpleHttpClient,
    project: &str,
    region: &str,
    resource: &str,
    body: &TestPermissionsRequest,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<TestPermissionsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = compute_region_instant_snapshots_test_iam_permissions_builder(
        client, project, region, resource, body,
    )?;
    compute_region_instant_snapshots_test_iam_permissions_execute(builder)
}

/// GET projects/{project}/regions/{region}/networkEndpointGroups/{networkEndpointGroup}/attachNetworkEndpoints
/// Attach a list of network endpoints to the specified network endpoint group.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_region_network_endpoint_groups_attach_network_endpoints_execute()` to send, or `compute_region_network_endpoint_groups_attach_network_endpoints` for simplest API.

pub fn compute_region_network_endpoint_groups_attach_network_endpoints_builder(
    client: &SimpleHttpClient,
    networkEndpointGroup: &str,
    project: &str,
    region: &str,
    requestId: Option<&str>,
    body: &RegionNetworkEndpointGroupsAttachEndpointsRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/regions/{}/networkEndpointGroups/{}/attachNetworkEndpoints",
        networkEndpointGroup,
        project,
        region,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "requestId",
            requestId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET projects/{project}/regions/{region}/networkEndpointGroups/{networkEndpointGroup}/attachNetworkEndpoints
/// Attach a list of network endpoints to the specified network endpoint group.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_region_network_endpoint_groups_attach_network_endpoints_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_region_network_endpoint_groups_attach_network_endpoints_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_network_endpoint_groups_attach_network_endpoints_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/regions/{region}/networkEndpointGroups/{networkEndpointGroup}/attachNetworkEndpoints
/// Attach a list of network endpoints to the specified network endpoint group.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_region_network_endpoint_groups_attach_network_endpoints_builder()` + `compute_region_network_endpoint_groups_attach_network_endpoints_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_network_endpoint_groups_attach_network_endpoints(
    client: &SimpleHttpClient,
    networkEndpointGroup: &str,
    project: &str,
    region: &str,
    requestId: Option<&str>,
    body: &RegionNetworkEndpointGroupsAttachEndpointsRequest,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_region_network_endpoint_groups_attach_network_endpoints_builder(
        client,
        networkEndpointGroup,
        project,
        region,
        requestId,
        body,
    )?;
    compute_region_network_endpoint_groups_attach_network_endpoints_execute(builder)
}

/// GET projects/{project}/regions/{region}/networkEndpointGroups/{networkEndpointGroup}
/// Deletes the specified network endpoint group. Note that the NEG cannot be deleted if it is configured as a backend of a backend service.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_region_network_endpoint_groups_delete_execute()` to send, or `compute_region_network_endpoint_groups_delete` for simplest API.

pub fn compute_region_network_endpoint_groups_delete_builder(
    client: &SimpleHttpClient,
    networkEndpointGroup: &str,
    project: &str,
    region: &str,
    requestId: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/regions/{}/networkEndpointGroups/{}",
        networkEndpointGroup, project, region,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "requestId",
            requestId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET projects/{project}/regions/{region}/networkEndpointGroups/{networkEndpointGroup}
/// Deletes the specified network endpoint group. Note that the NEG cannot be deleted if it is configured as a backend of a backend service.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_region_network_endpoint_groups_delete_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_region_network_endpoint_groups_delete_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_network_endpoint_groups_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/regions/{region}/networkEndpointGroups/{networkEndpointGroup}
/// Deletes the specified network endpoint group. Note that the NEG cannot be deleted if it is configured as a backend of a backend service.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_region_network_endpoint_groups_delete_builder()` + `compute_region_network_endpoint_groups_delete_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_network_endpoint_groups_delete(
    client: &SimpleHttpClient,
    networkEndpointGroup: &str,
    project: &str,
    region: &str,
    requestId: Option<&str>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_region_network_endpoint_groups_delete_builder(
        client,
        networkEndpointGroup,
        project,
        region,
        requestId,
    )?;
    compute_region_network_endpoint_groups_delete_execute(builder)
}

/// GET projects/{project}/regions/{region}/networkEndpointGroups/{networkEndpointGroup}/detachNetworkEndpoints
/// Detach the network endpoint from the specified network endpoint group.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_region_network_endpoint_groups_detach_network_endpoints_execute()` to send, or `compute_region_network_endpoint_groups_detach_network_endpoints` for simplest API.

pub fn compute_region_network_endpoint_groups_detach_network_endpoints_builder(
    client: &SimpleHttpClient,
    networkEndpointGroup: &str,
    project: &str,
    region: &str,
    requestId: Option<&str>,
    body: &RegionNetworkEndpointGroupsDetachEndpointsRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/regions/{}/networkEndpointGroups/{}/detachNetworkEndpoints",
        networkEndpointGroup,
        project,
        region,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "requestId",
            requestId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET projects/{project}/regions/{region}/networkEndpointGroups/{networkEndpointGroup}/detachNetworkEndpoints
/// Detach the network endpoint from the specified network endpoint group.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_region_network_endpoint_groups_detach_network_endpoints_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_region_network_endpoint_groups_detach_network_endpoints_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_network_endpoint_groups_detach_network_endpoints_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/regions/{region}/networkEndpointGroups/{networkEndpointGroup}/detachNetworkEndpoints
/// Detach the network endpoint from the specified network endpoint group.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_region_network_endpoint_groups_detach_network_endpoints_builder()` + `compute_region_network_endpoint_groups_detach_network_endpoints_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_network_endpoint_groups_detach_network_endpoints(
    client: &SimpleHttpClient,
    networkEndpointGroup: &str,
    project: &str,
    region: &str,
    requestId: Option<&str>,
    body: &RegionNetworkEndpointGroupsDetachEndpointsRequest,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_region_network_endpoint_groups_detach_network_endpoints_builder(
        client,
        networkEndpointGroup,
        project,
        region,
        requestId,
        body,
    )?;
    compute_region_network_endpoint_groups_detach_network_endpoints_execute(builder)
}

/// GET projects/{project}/regions/{region}/networkEndpointGroups/{networkEndpointGroup}
/// Returns the specified network endpoint group.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_region_network_endpoint_groups_get_execute()` to send, or `compute_region_network_endpoint_groups_get` for simplest API.

pub fn compute_region_network_endpoint_groups_get_builder(
    client: &SimpleHttpClient,
    networkEndpointGroup: &str,
    project: &str,
    region: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/regions/{}/networkEndpointGroups/{}",
        networkEndpointGroup, project, region,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/regions/{region}/networkEndpointGroups/{networkEndpointGroup}
/// Returns the specified network endpoint group.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_region_network_endpoint_groups_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_region_network_endpoint_groups_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_network_endpoint_groups_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<NetworkEndpointGroup>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: NetworkEndpointGroup = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/regions/{region}/networkEndpointGroups/{networkEndpointGroup}
/// Returns the specified network endpoint group.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_region_network_endpoint_groups_get_builder()` + `compute_region_network_endpoint_groups_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_network_endpoint_groups_get(
    client: &SimpleHttpClient,
    networkEndpointGroup: &str,
    project: &str,
    region: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<NetworkEndpointGroup>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = compute_region_network_endpoint_groups_get_builder(
        client,
        networkEndpointGroup,
        project,
        region,
    )?;
    compute_region_network_endpoint_groups_get_execute(builder)
}

/// GET projects/{project}/regions/{region}/networkEndpointGroups
/// Creates a network endpoint group in the specified project using the parameters that are included in the request.  Note: Use the following APIs to manage network endpoint groups:  - To manage NEGs with zonal scope (such as zonal NEGs, hybrid connectivity NEGs): zonal API - To manage NEGs with regional scope (such as regional internet NEGs, serverless NEGs, Private Service Connect NEGs): regional API - To manage NEGs with global scope (such as global internet NEGs):global API
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_region_network_endpoint_groups_insert_execute()` to send, or `compute_region_network_endpoint_groups_insert` for simplest API.

pub fn compute_region_network_endpoint_groups_insert_builder(
    client: &SimpleHttpClient,
    project: &str,
    region: &str,
    requestId: Option<&str>,
    body: &NetworkEndpointGroup,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/regions/{}/networkEndpointGroups",
        project, region,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "requestId",
            requestId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET projects/{project}/regions/{region}/networkEndpointGroups
/// Creates a network endpoint group in the specified project using the parameters that are included in the request.  Note: Use the following APIs to manage network endpoint groups:  - To manage NEGs with zonal scope (such as zonal NEGs, hybrid connectivity NEGs): zonal API - To manage NEGs with regional scope (such as regional internet NEGs, serverless NEGs, Private Service Connect NEGs): regional API - To manage NEGs with global scope (such as global internet NEGs):global API
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_region_network_endpoint_groups_insert_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_region_network_endpoint_groups_insert_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_network_endpoint_groups_insert_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/regions/{region}/networkEndpointGroups
/// Creates a network endpoint group in the specified project using the parameters that are included in the request.  Note: Use the following APIs to manage network endpoint groups:  - To manage NEGs with zonal scope (such as zonal NEGs, hybrid connectivity NEGs): zonal API - To manage NEGs with regional scope (such as regional internet NEGs, serverless NEGs, Private Service Connect NEGs): regional API - To manage NEGs with global scope (such as global internet NEGs):global API
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_region_network_endpoint_groups_insert_builder()` + `compute_region_network_endpoint_groups_insert_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_network_endpoint_groups_insert(
    client: &SimpleHttpClient,
    project: &str,
    region: &str,
    requestId: Option<&str>,
    body: &NetworkEndpointGroup,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_region_network_endpoint_groups_insert_builder(
        client, project, region, requestId, body,
    )?;
    compute_region_network_endpoint_groups_insert_execute(builder)
}

/// GET projects/{project}/regions/{region}/networkEndpointGroups
/// Retrieves the list of regional network endpoint groups available to the specified project in the given region.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_region_network_endpoint_groups_list_execute()` to send, or `compute_region_network_endpoint_groups_list` for simplest API.

pub fn compute_region_network_endpoint_groups_list_builder(
    client: &SimpleHttpClient,
    project: &str,
    region: &str,
    filter: Option<&str>,
    maxResults: Option<i32>,
    orderBy: Option<&str>,
    pageToken: Option<&str>,
    returnPartialSuccess: Option<bool>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/regions/{}/networkEndpointGroups",
        project, region,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "filter",
            filter
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "maxResults",
            maxResults
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "orderBy",
            orderBy
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageToken",
            pageToken
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "returnPartialSuccess",
            returnPartialSuccess
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET projects/{project}/regions/{region}/networkEndpointGroups
/// Retrieves the list of regional network endpoint groups available to the specified project in the given region.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_region_network_endpoint_groups_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_region_network_endpoint_groups_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_network_endpoint_groups_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<NetworkEndpointGroupList>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: NetworkEndpointGroupList = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/regions/{region}/networkEndpointGroups
/// Retrieves the list of regional network endpoint groups available to the specified project in the given region.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_region_network_endpoint_groups_list_builder()` + `compute_region_network_endpoint_groups_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_network_endpoint_groups_list(
    client: &SimpleHttpClient,
    project: &str,
    region: &str,
    filter: Option<&str>,
    maxResults: Option<i32>,
    orderBy: Option<&str>,
    pageToken: Option<&str>,
    returnPartialSuccess: Option<bool>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<NetworkEndpointGroupList>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = compute_region_network_endpoint_groups_list_builder(
        client,
        project,
        region,
        filter,
        maxResults,
        orderBy,
        pageToken,
        returnPartialSuccess,
    )?;
    compute_region_network_endpoint_groups_list_execute(builder)
}

/// GET projects/{project}/regions/{region}/networkEndpointGroups/{networkEndpointGroup}/listNetworkEndpoints
/// Lists the network endpoints in the specified network endpoint group.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_region_network_endpoint_groups_list_network_endpoints_execute()` to send, or `compute_region_network_endpoint_groups_list_network_endpoints` for simplest API.

pub fn compute_region_network_endpoint_groups_list_network_endpoints_builder(
    client: &SimpleHttpClient,
    networkEndpointGroup: &str,
    project: &str,
    region: &str,
    filter: Option<&str>,
    maxResults: Option<i32>,
    orderBy: Option<&str>,
    pageToken: Option<&str>,
    returnPartialSuccess: Option<bool>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/regions/{}/networkEndpointGroups/{}/listNetworkEndpoints",
        networkEndpointGroup,
        project,
        region,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "filter",
            filter
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "maxResults",
            maxResults
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "orderBy",
            orderBy
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageToken",
            pageToken
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "returnPartialSuccess",
            returnPartialSuccess
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET projects/{project}/regions/{region}/networkEndpointGroups/{networkEndpointGroup}/listNetworkEndpoints
/// Lists the network endpoints in the specified network endpoint group.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_region_network_endpoint_groups_list_network_endpoints_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_region_network_endpoint_groups_list_network_endpoints_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_network_endpoint_groups_list_network_endpoints_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<NetworkEndpointGroupsListNetworkEndpoints>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: NetworkEndpointGroupsListNetworkEndpoints = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/regions/{region}/networkEndpointGroups/{networkEndpointGroup}/listNetworkEndpoints
/// Lists the network endpoints in the specified network endpoint group.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_region_network_endpoint_groups_list_network_endpoints_builder()` + `compute_region_network_endpoint_groups_list_network_endpoints_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_network_endpoint_groups_list_network_endpoints(
    client: &SimpleHttpClient,
    networkEndpointGroup: &str,
    project: &str,
    region: &str,
    filter: Option<&str>,
    maxResults: Option<i32>,
    orderBy: Option<&str>,
    pageToken: Option<&str>,
    returnPartialSuccess: Option<bool>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<NetworkEndpointGroupsListNetworkEndpoints>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = compute_region_network_endpoint_groups_list_network_endpoints_builder(
        client,
        networkEndpointGroup,
        project,
        region,
        filter,
        maxResults,
        orderBy,
        pageToken,
        returnPartialSuccess,
    )?;
    compute_region_network_endpoint_groups_list_network_endpoints_execute(builder)
}

/// GET projects/{project}/regions/{region}/firewallPolicies/{firewallPolicy}/addAssociation
/// Inserts an association for the specified network firewall policy.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_region_network_firewall_policies_add_association_execute()` to send, or `compute_region_network_firewall_policies_add_association` for simplest API.

pub fn compute_region_network_firewall_policies_add_association_builder(
    client: &SimpleHttpClient,
    firewallPolicy: &str,
    project: &str,
    region: &str,
    replaceExistingAssociation: Option<bool>,
    requestId: Option<&str>,
    body: &FirewallPolicyAssociation,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/regions/{}/firewallPolicies/{}/addAssociation",
        firewallPolicy, project, region,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "replaceExistingAssociation",
            replaceExistingAssociation
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "requestId",
            requestId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET projects/{project}/regions/{region}/firewallPolicies/{firewallPolicy}/addAssociation
/// Inserts an association for the specified network firewall policy.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_region_network_firewall_policies_add_association_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_region_network_firewall_policies_add_association_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_network_firewall_policies_add_association_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/regions/{region}/firewallPolicies/{firewallPolicy}/addAssociation
/// Inserts an association for the specified network firewall policy.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_region_network_firewall_policies_add_association_builder()` + `compute_region_network_firewall_policies_add_association_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_network_firewall_policies_add_association(
    client: &SimpleHttpClient,
    firewallPolicy: &str,
    project: &str,
    region: &str,
    replaceExistingAssociation: Option<bool>,
    requestId: Option<&str>,
    body: &FirewallPolicyAssociation,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_region_network_firewall_policies_add_association_builder(
        client,
        firewallPolicy,
        project,
        region,
        replaceExistingAssociation,
        requestId,
        body,
    )?;
    compute_region_network_firewall_policies_add_association_execute(builder)
}

/// GET projects/{project}/regions/{region}/firewallPolicies/{firewallPolicy}/addRule
/// Inserts a rule into a network firewall policy.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_region_network_firewall_policies_add_rule_execute()` to send, or `compute_region_network_firewall_policies_add_rule` for simplest API.

pub fn compute_region_network_firewall_policies_add_rule_builder(
    client: &SimpleHttpClient,
    firewallPolicy: &str,
    project: &str,
    region: &str,
    maxPriority: Option<i32>,
    minPriority: Option<i32>,
    requestId: Option<&str>,
    body: &FirewallPolicyRule,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/regions/{}/firewallPolicies/{}/addRule",
        firewallPolicy, project, region,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "maxPriority",
            maxPriority
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "minPriority",
            minPriority
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "requestId",
            requestId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET projects/{project}/regions/{region}/firewallPolicies/{firewallPolicy}/addRule
/// Inserts a rule into a network firewall policy.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_region_network_firewall_policies_add_rule_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_region_network_firewall_policies_add_rule_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_network_firewall_policies_add_rule_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/regions/{region}/firewallPolicies/{firewallPolicy}/addRule
/// Inserts a rule into a network firewall policy.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_region_network_firewall_policies_add_rule_builder()` + `compute_region_network_firewall_policies_add_rule_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_network_firewall_policies_add_rule(
    client: &SimpleHttpClient,
    firewallPolicy: &str,
    project: &str,
    region: &str,
    maxPriority: Option<i32>,
    minPriority: Option<i32>,
    requestId: Option<&str>,
    body: &FirewallPolicyRule,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_region_network_firewall_policies_add_rule_builder(
        client,
        firewallPolicy,
        project,
        region,
        maxPriority,
        minPriority,
        requestId,
        body,
    )?;
    compute_region_network_firewall_policies_add_rule_execute(builder)
}

/// GET projects/{project}/regions/{region}/firewallPolicies/{firewallPolicy}/cloneRules
/// Copies rules to the specified network firewall policy.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_region_network_firewall_policies_clone_rules_execute()` to send, or `compute_region_network_firewall_policies_clone_rules` for simplest API.

pub fn compute_region_network_firewall_policies_clone_rules_builder(
    client: &SimpleHttpClient,
    firewallPolicy: &str,
    project: &str,
    region: &str,
    requestId: Option<&str>,
    sourceFirewallPolicy: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/regions/{}/firewallPolicies/{}/cloneRules",
        firewallPolicy, project, region,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "requestId",
            requestId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "sourceFirewallPolicy",
            sourceFirewallPolicy
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET projects/{project}/regions/{region}/firewallPolicies/{firewallPolicy}/cloneRules
/// Copies rules to the specified network firewall policy.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_region_network_firewall_policies_clone_rules_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_region_network_firewall_policies_clone_rules_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_network_firewall_policies_clone_rules_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/regions/{region}/firewallPolicies/{firewallPolicy}/cloneRules
/// Copies rules to the specified network firewall policy.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_region_network_firewall_policies_clone_rules_builder()` + `compute_region_network_firewall_policies_clone_rules_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_network_firewall_policies_clone_rules(
    client: &SimpleHttpClient,
    firewallPolicy: &str,
    project: &str,
    region: &str,
    requestId: Option<&str>,
    sourceFirewallPolicy: Option<&str>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_region_network_firewall_policies_clone_rules_builder(
        client,
        firewallPolicy,
        project,
        region,
        requestId,
        sourceFirewallPolicy,
    )?;
    compute_region_network_firewall_policies_clone_rules_execute(builder)
}

/// GET projects/{project}/regions/{region}/firewallPolicies/{firewallPolicy}
/// Deletes the specified network firewall policy.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_region_network_firewall_policies_delete_execute()` to send, or `compute_region_network_firewall_policies_delete` for simplest API.

pub fn compute_region_network_firewall_policies_delete_builder(
    client: &SimpleHttpClient,
    firewallPolicy: &str,
    project: &str,
    region: &str,
    requestId: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/regions/{}/firewallPolicies/{}",
        firewallPolicy, project, region,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "requestId",
            requestId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET projects/{project}/regions/{region}/firewallPolicies/{firewallPolicy}
/// Deletes the specified network firewall policy.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_region_network_firewall_policies_delete_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_region_network_firewall_policies_delete_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_network_firewall_policies_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/regions/{region}/firewallPolicies/{firewallPolicy}
/// Deletes the specified network firewall policy.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_region_network_firewall_policies_delete_builder()` + `compute_region_network_firewall_policies_delete_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_network_firewall_policies_delete(
    client: &SimpleHttpClient,
    firewallPolicy: &str,
    project: &str,
    region: &str,
    requestId: Option<&str>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_region_network_firewall_policies_delete_builder(
        client,
        firewallPolicy,
        project,
        region,
        requestId,
    )?;
    compute_region_network_firewall_policies_delete_execute(builder)
}

/// GET projects/{project}/regions/{region}/firewallPolicies/{firewallPolicy}
/// Returns the specified network firewall policy.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_region_network_firewall_policies_get_execute()` to send, or `compute_region_network_firewall_policies_get` for simplest API.

pub fn compute_region_network_firewall_policies_get_builder(
    client: &SimpleHttpClient,
    firewallPolicy: &str,
    project: &str,
    region: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/regions/{}/firewallPolicies/{}",
        firewallPolicy, project, region,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/regions/{region}/firewallPolicies/{firewallPolicy}
/// Returns the specified network firewall policy.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_region_network_firewall_policies_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_region_network_firewall_policies_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_network_firewall_policies_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<FirewallPolicy>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: FirewallPolicy = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/regions/{region}/firewallPolicies/{firewallPolicy}
/// Returns the specified network firewall policy.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_region_network_firewall_policies_get_builder()` + `compute_region_network_firewall_policies_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_network_firewall_policies_get(
    client: &SimpleHttpClient,
    firewallPolicy: &str,
    project: &str,
    region: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<FirewallPolicy>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = compute_region_network_firewall_policies_get_builder(
        client,
        firewallPolicy,
        project,
        region,
    )?;
    compute_region_network_firewall_policies_get_execute(builder)
}

/// GET projects/{project}/regions/{region}/firewallPolicies/{firewallPolicy}/getAssociation
/// Gets an association with the specified name.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_region_network_firewall_policies_get_association_execute()` to send, or `compute_region_network_firewall_policies_get_association` for simplest API.

pub fn compute_region_network_firewall_policies_get_association_builder(
    client: &SimpleHttpClient,
    firewallPolicy: &str,
    project: &str,
    region: &str,
    name: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/regions/{}/firewallPolicies/{}/getAssociation",
        firewallPolicy, project, region,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "name",
            name.as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET projects/{project}/regions/{region}/firewallPolicies/{firewallPolicy}/getAssociation
/// Gets an association with the specified name.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_region_network_firewall_policies_get_association_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_region_network_firewall_policies_get_association_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_network_firewall_policies_get_association_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<FirewallPolicyAssociation>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: FirewallPolicyAssociation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/regions/{region}/firewallPolicies/{firewallPolicy}/getAssociation
/// Gets an association with the specified name.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_region_network_firewall_policies_get_association_builder()` + `compute_region_network_firewall_policies_get_association_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_network_firewall_policies_get_association(
    client: &SimpleHttpClient,
    firewallPolicy: &str,
    project: &str,
    region: &str,
    name: Option<&str>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<FirewallPolicyAssociation>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = compute_region_network_firewall_policies_get_association_builder(
        client,
        firewallPolicy,
        project,
        region,
        name,
    )?;
    compute_region_network_firewall_policies_get_association_execute(builder)
}

/// GET projects/{project}/regions/{region}/firewallPolicies/getEffectiveFirewalls
/// Returns the effective firewalls on a given network.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_region_network_firewall_policies_get_effective_firewalls_execute()` to send, or `compute_region_network_firewall_policies_get_effective_firewalls` for simplest API.

pub fn compute_region_network_firewall_policies_get_effective_firewalls_builder(
    client: &SimpleHttpClient,
    project: &str,
    region: &str,
    network: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/regions/{}/firewallPolicies/getEffectiveFirewalls",
        project, region,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "network",
            network
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET projects/{project}/regions/{region}/firewallPolicies/getEffectiveFirewalls
/// Returns the effective firewalls on a given network.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_region_network_firewall_policies_get_effective_firewalls_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_region_network_firewall_policies_get_effective_firewalls_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_network_firewall_policies_get_effective_firewalls_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<RegionNetworkFirewallPoliciesGetEffectiveFirewallsResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: RegionNetworkFirewallPoliciesGetEffectiveFirewallsResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/regions/{region}/firewallPolicies/getEffectiveFirewalls
/// Returns the effective firewalls on a given network.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_region_network_firewall_policies_get_effective_firewalls_builder()` + `compute_region_network_firewall_policies_get_effective_firewalls_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_network_firewall_policies_get_effective_firewalls(
    client: &SimpleHttpClient,
    project: &str,
    region: &str,
    network: Option<&str>,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<RegionNetworkFirewallPoliciesGetEffectiveFirewallsResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = compute_region_network_firewall_policies_get_effective_firewalls_builder(
        client, project, region, network,
    )?;
    compute_region_network_firewall_policies_get_effective_firewalls_execute(builder)
}

/// GET projects/{project}/regions/{region}/firewallPolicies/{resource}/getIamPolicy
/// Gets the access control policy for a resource. May be empty if no such policy or resource exists.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_region_network_firewall_policies_get_iam_policy_execute()` to send, or `compute_region_network_firewall_policies_get_iam_policy` for simplest API.

pub fn compute_region_network_firewall_policies_get_iam_policy_builder(
    client: &SimpleHttpClient,
    project: &str,
    region: &str,
    resource: &str,
    optionsRequestedPolicyVersion: Option<i32>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/regions/{}/firewallPolicies/{}/getIamPolicy",
        project, region, resource,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "optionsRequestedPolicyVersion",
            optionsRequestedPolicyVersion
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET projects/{project}/regions/{region}/firewallPolicies/{resource}/getIamPolicy
/// Gets the access control policy for a resource. May be empty if no such policy or resource exists.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_region_network_firewall_policies_get_iam_policy_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_region_network_firewall_policies_get_iam_policy_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_network_firewall_policies_get_iam_policy_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Policy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Policy = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/regions/{region}/firewallPolicies/{resource}/getIamPolicy
/// Gets the access control policy for a resource. May be empty if no such policy or resource exists.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_region_network_firewall_policies_get_iam_policy_builder()` + `compute_region_network_firewall_policies_get_iam_policy_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_network_firewall_policies_get_iam_policy(
    client: &SimpleHttpClient,
    project: &str,
    region: &str,
    resource: &str,
    optionsRequestedPolicyVersion: Option<i32>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Policy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_region_network_firewall_policies_get_iam_policy_builder(
        client,
        project,
        region,
        resource,
        optionsRequestedPolicyVersion,
    )?;
    compute_region_network_firewall_policies_get_iam_policy_execute(builder)
}

/// GET projects/{project}/regions/{region}/firewallPolicies/{firewallPolicy}/getRule
/// Gets a rule of the specified priority.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_region_network_firewall_policies_get_rule_execute()` to send, or `compute_region_network_firewall_policies_get_rule` for simplest API.

pub fn compute_region_network_firewall_policies_get_rule_builder(
    client: &SimpleHttpClient,
    firewallPolicy: &str,
    project: &str,
    region: &str,
    priority: Option<i32>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/regions/{}/firewallPolicies/{}/getRule",
        firewallPolicy, project, region,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "priority",
            priority
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET projects/{project}/regions/{region}/firewallPolicies/{firewallPolicy}/getRule
/// Gets a rule of the specified priority.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_region_network_firewall_policies_get_rule_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_region_network_firewall_policies_get_rule_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_network_firewall_policies_get_rule_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<FirewallPolicyRule>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: FirewallPolicyRule = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/regions/{region}/firewallPolicies/{firewallPolicy}/getRule
/// Gets a rule of the specified priority.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_region_network_firewall_policies_get_rule_builder()` + `compute_region_network_firewall_policies_get_rule_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_network_firewall_policies_get_rule(
    client: &SimpleHttpClient,
    firewallPolicy: &str,
    project: &str,
    region: &str,
    priority: Option<i32>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<FirewallPolicyRule>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = compute_region_network_firewall_policies_get_rule_builder(
        client,
        firewallPolicy,
        project,
        region,
        priority,
    )?;
    compute_region_network_firewall_policies_get_rule_execute(builder)
}

/// GET projects/{project}/regions/{region}/firewallPolicies
/// Creates a new network firewall policy in the specified project and region.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_region_network_firewall_policies_insert_execute()` to send, or `compute_region_network_firewall_policies_insert` for simplest API.

pub fn compute_region_network_firewall_policies_insert_builder(
    client: &SimpleHttpClient,
    project: &str,
    region: &str,
    requestId: Option<&str>,
    body: &FirewallPolicy,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/regions/{}/firewallPolicies",
        project, region,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "requestId",
            requestId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET projects/{project}/regions/{region}/firewallPolicies
/// Creates a new network firewall policy in the specified project and region.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_region_network_firewall_policies_insert_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_region_network_firewall_policies_insert_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_network_firewall_policies_insert_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/regions/{region}/firewallPolicies
/// Creates a new network firewall policy in the specified project and region.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_region_network_firewall_policies_insert_builder()` + `compute_region_network_firewall_policies_insert_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_network_firewall_policies_insert(
    client: &SimpleHttpClient,
    project: &str,
    region: &str,
    requestId: Option<&str>,
    body: &FirewallPolicy,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_region_network_firewall_policies_insert_builder(
        client, project, region, requestId, body,
    )?;
    compute_region_network_firewall_policies_insert_execute(builder)
}

/// GET projects/{project}/regions/{region}/firewallPolicies
/// Lists all the network firewall policies that have been configured for the specified project in the given region.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_region_network_firewall_policies_list_execute()` to send, or `compute_region_network_firewall_policies_list` for simplest API.

pub fn compute_region_network_firewall_policies_list_builder(
    client: &SimpleHttpClient,
    project: &str,
    region: &str,
    filter: Option<&str>,
    maxResults: Option<i32>,
    orderBy: Option<&str>,
    pageToken: Option<&str>,
    returnPartialSuccess: Option<bool>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/regions/{}/firewallPolicies",
        project, region,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "filter",
            filter
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "maxResults",
            maxResults
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "orderBy",
            orderBy
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageToken",
            pageToken
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "returnPartialSuccess",
            returnPartialSuccess
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET projects/{project}/regions/{region}/firewallPolicies
/// Lists all the network firewall policies that have been configured for the specified project in the given region.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_region_network_firewall_policies_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_region_network_firewall_policies_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_network_firewall_policies_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<FirewallPolicyList>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: FirewallPolicyList = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/regions/{region}/firewallPolicies
/// Lists all the network firewall policies that have been configured for the specified project in the given region.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_region_network_firewall_policies_list_builder()` + `compute_region_network_firewall_policies_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_network_firewall_policies_list(
    client: &SimpleHttpClient,
    project: &str,
    region: &str,
    filter: Option<&str>,
    maxResults: Option<i32>,
    orderBy: Option<&str>,
    pageToken: Option<&str>,
    returnPartialSuccess: Option<bool>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<FirewallPolicyList>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = compute_region_network_firewall_policies_list_builder(
        client,
        project,
        region,
        filter,
        maxResults,
        orderBy,
        pageToken,
        returnPartialSuccess,
    )?;
    compute_region_network_firewall_policies_list_execute(builder)
}

/// GET projects/{project}/regions/{region}/firewallPolicies/{firewallPolicy}
/// Patches the specified network firewall policy.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_region_network_firewall_policies_patch_execute()` to send, or `compute_region_network_firewall_policies_patch` for simplest API.

pub fn compute_region_network_firewall_policies_patch_builder(
    client: &SimpleHttpClient,
    firewallPolicy: &str,
    project: &str,
    region: &str,
    requestId: Option<&str>,
    body: &FirewallPolicy,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/regions/{}/firewallPolicies/{}",
        firewallPolicy, project, region,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "requestId",
            requestId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET projects/{project}/regions/{region}/firewallPolicies/{firewallPolicy}
/// Patches the specified network firewall policy.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_region_network_firewall_policies_patch_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_region_network_firewall_policies_patch_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_network_firewall_policies_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/regions/{region}/firewallPolicies/{firewallPolicy}
/// Patches the specified network firewall policy.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_region_network_firewall_policies_patch_builder()` + `compute_region_network_firewall_policies_patch_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_network_firewall_policies_patch(
    client: &SimpleHttpClient,
    firewallPolicy: &str,
    project: &str,
    region: &str,
    requestId: Option<&str>,
    body: &FirewallPolicy,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_region_network_firewall_policies_patch_builder(
        client,
        firewallPolicy,
        project,
        region,
        requestId,
        body,
    )?;
    compute_region_network_firewall_policies_patch_execute(builder)
}

/// GET projects/{project}/regions/{region}/firewallPolicies/{firewallPolicy}/patchRule
/// Patches a rule of the specified priority.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_region_network_firewall_policies_patch_rule_execute()` to send, or `compute_region_network_firewall_policies_patch_rule` for simplest API.

pub fn compute_region_network_firewall_policies_patch_rule_builder(
    client: &SimpleHttpClient,
    firewallPolicy: &str,
    project: &str,
    region: &str,
    priority: Option<i32>,
    requestId: Option<&str>,
    body: &FirewallPolicyRule,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/regions/{}/firewallPolicies/{}/patchRule",
        firewallPolicy, project, region,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "priority",
            priority
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "requestId",
            requestId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET projects/{project}/regions/{region}/firewallPolicies/{firewallPolicy}/patchRule
/// Patches a rule of the specified priority.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_region_network_firewall_policies_patch_rule_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_region_network_firewall_policies_patch_rule_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_network_firewall_policies_patch_rule_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/regions/{region}/firewallPolicies/{firewallPolicy}/patchRule
/// Patches a rule of the specified priority.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_region_network_firewall_policies_patch_rule_builder()` + `compute_region_network_firewall_policies_patch_rule_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_network_firewall_policies_patch_rule(
    client: &SimpleHttpClient,
    firewallPolicy: &str,
    project: &str,
    region: &str,
    priority: Option<i32>,
    requestId: Option<&str>,
    body: &FirewallPolicyRule,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_region_network_firewall_policies_patch_rule_builder(
        client,
        firewallPolicy,
        project,
        region,
        priority,
        requestId,
        body,
    )?;
    compute_region_network_firewall_policies_patch_rule_execute(builder)
}

/// GET projects/{project}/regions/{region}/firewallPolicies/{firewallPolicy}/removeAssociation
/// Removes an association for the specified network firewall policy.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_region_network_firewall_policies_remove_association_execute()` to send, or `compute_region_network_firewall_policies_remove_association` for simplest API.

pub fn compute_region_network_firewall_policies_remove_association_builder(
    client: &SimpleHttpClient,
    firewallPolicy: &str,
    project: &str,
    region: &str,
    name: Option<&str>,
    requestId: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/regions/{}/firewallPolicies/{}/removeAssociation",
        firewallPolicy, project, region,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "name",
            name.as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "requestId",
            requestId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET projects/{project}/regions/{region}/firewallPolicies/{firewallPolicy}/removeAssociation
/// Removes an association for the specified network firewall policy.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_region_network_firewall_policies_remove_association_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_region_network_firewall_policies_remove_association_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_network_firewall_policies_remove_association_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/regions/{region}/firewallPolicies/{firewallPolicy}/removeAssociation
/// Removes an association for the specified network firewall policy.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_region_network_firewall_policies_remove_association_builder()` + `compute_region_network_firewall_policies_remove_association_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_network_firewall_policies_remove_association(
    client: &SimpleHttpClient,
    firewallPolicy: &str,
    project: &str,
    region: &str,
    name: Option<&str>,
    requestId: Option<&str>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_region_network_firewall_policies_remove_association_builder(
        client,
        firewallPolicy,
        project,
        region,
        name,
        requestId,
    )?;
    compute_region_network_firewall_policies_remove_association_execute(builder)
}

/// GET projects/{project}/regions/{region}/firewallPolicies/{firewallPolicy}/removeRule
/// Deletes a rule of the specified priority.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_region_network_firewall_policies_remove_rule_execute()` to send, or `compute_region_network_firewall_policies_remove_rule` for simplest API.

pub fn compute_region_network_firewall_policies_remove_rule_builder(
    client: &SimpleHttpClient,
    firewallPolicy: &str,
    project: &str,
    region: &str,
    priority: Option<i32>,
    requestId: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/regions/{}/firewallPolicies/{}/removeRule",
        firewallPolicy, project, region,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "priority",
            priority
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "requestId",
            requestId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET projects/{project}/regions/{region}/firewallPolicies/{firewallPolicy}/removeRule
/// Deletes a rule of the specified priority.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_region_network_firewall_policies_remove_rule_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_region_network_firewall_policies_remove_rule_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_network_firewall_policies_remove_rule_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/regions/{region}/firewallPolicies/{firewallPolicy}/removeRule
/// Deletes a rule of the specified priority.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_region_network_firewall_policies_remove_rule_builder()` + `compute_region_network_firewall_policies_remove_rule_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_network_firewall_policies_remove_rule(
    client: &SimpleHttpClient,
    firewallPolicy: &str,
    project: &str,
    region: &str,
    priority: Option<i32>,
    requestId: Option<&str>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_region_network_firewall_policies_remove_rule_builder(
        client,
        firewallPolicy,
        project,
        region,
        priority,
        requestId,
    )?;
    compute_region_network_firewall_policies_remove_rule_execute(builder)
}

/// GET projects/{project}/regions/{region}/firewallPolicies/{resource}/setIamPolicy
/// Sets the access control policy on the specified resource. Replaces any existing policy.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_region_network_firewall_policies_set_iam_policy_execute()` to send, or `compute_region_network_firewall_policies_set_iam_policy` for simplest API.

pub fn compute_region_network_firewall_policies_set_iam_policy_builder(
    client: &SimpleHttpClient,
    project: &str,
    region: &str,
    resource: &str,
    body: &RegionSetPolicyRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/regions/{}/firewallPolicies/{}/setIamPolicy",
        project, region, resource,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET projects/{project}/regions/{region}/firewallPolicies/{resource}/setIamPolicy
/// Sets the access control policy on the specified resource. Replaces any existing policy.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_region_network_firewall_policies_set_iam_policy_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_region_network_firewall_policies_set_iam_policy_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_network_firewall_policies_set_iam_policy_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Policy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Policy = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/regions/{region}/firewallPolicies/{resource}/setIamPolicy
/// Sets the access control policy on the specified resource. Replaces any existing policy.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_region_network_firewall_policies_set_iam_policy_builder()` + `compute_region_network_firewall_policies_set_iam_policy_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_network_firewall_policies_set_iam_policy(
    client: &SimpleHttpClient,
    project: &str,
    region: &str,
    resource: &str,
    body: &RegionSetPolicyRequest,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Policy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_region_network_firewall_policies_set_iam_policy_builder(
        client, project, region, resource, body,
    )?;
    compute_region_network_firewall_policies_set_iam_policy_execute(builder)
}

/// GET projects/{project}/regions/{region}/firewallPolicies/{resource}/testIamPermissions
/// Returns permissions that a caller has on the specified resource.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_region_network_firewall_policies_test_iam_permissions_execute()` to send, or `compute_region_network_firewall_policies_test_iam_permissions` for simplest API.

pub fn compute_region_network_firewall_policies_test_iam_permissions_builder(
    client: &SimpleHttpClient,
    project: &str,
    region: &str,
    resource: &str,
    body: &TestPermissionsRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/regions/{}/firewallPolicies/{}/testIamPermissions",
        project, region, resource,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET projects/{project}/regions/{region}/firewallPolicies/{resource}/testIamPermissions
/// Returns permissions that a caller has on the specified resource.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_region_network_firewall_policies_test_iam_permissions_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_region_network_firewall_policies_test_iam_permissions_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_network_firewall_policies_test_iam_permissions_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<TestPermissionsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: TestPermissionsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/regions/{region}/firewallPolicies/{resource}/testIamPermissions
/// Returns permissions that a caller has on the specified resource.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_region_network_firewall_policies_test_iam_permissions_builder()` + `compute_region_network_firewall_policies_test_iam_permissions_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_network_firewall_policies_test_iam_permissions(
    client: &SimpleHttpClient,
    project: &str,
    region: &str,
    resource: &str,
    body: &TestPermissionsRequest,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<TestPermissionsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = compute_region_network_firewall_policies_test_iam_permissions_builder(
        client, project, region, resource, body,
    )?;
    compute_region_network_firewall_policies_test_iam_permissions_execute(builder)
}

/// GET projects/{project}/aggregated/notificationEndpoints
/// Retrieves the list of all NotificationEndpoint resources, regional and global, available to the specified project.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_region_notification_endpoints_aggregated_list_execute()` to send, or `compute_region_notification_endpoints_aggregated_list` for simplest API.

pub fn compute_region_notification_endpoints_aggregated_list_builder(
    client: &SimpleHttpClient,
    project: &str,
    filter: Option<&str>,
    includeAllScopes: Option<bool>,
    maxResults: Option<i32>,
    orderBy: Option<&str>,
    pageToken: Option<&str>,
    returnPartialSuccess: Option<bool>,
    serviceProjectNumber: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/aggregated/notificationEndpoints",
        project,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "filter",
            filter
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "includeAllScopes",
            includeAllScopes
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "maxResults",
            maxResults
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "orderBy",
            orderBy
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageToken",
            pageToken
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "returnPartialSuccess",
            returnPartialSuccess
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "serviceProjectNumber",
            serviceProjectNumber
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET projects/{project}/aggregated/notificationEndpoints
/// Retrieves the list of all NotificationEndpoint resources, regional and global, available to the specified project.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_region_notification_endpoints_aggregated_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_region_notification_endpoints_aggregated_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_notification_endpoints_aggregated_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<NotificationEndpointAggregatedList>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: NotificationEndpointAggregatedList = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/aggregated/notificationEndpoints
/// Retrieves the list of all NotificationEndpoint resources, regional and global, available to the specified project.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_region_notification_endpoints_aggregated_list_builder()` + `compute_region_notification_endpoints_aggregated_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_notification_endpoints_aggregated_list(
    client: &SimpleHttpClient,
    project: &str,
    filter: Option<&str>,
    includeAllScopes: Option<bool>,
    maxResults: Option<i32>,
    orderBy: Option<&str>,
    pageToken: Option<&str>,
    returnPartialSuccess: Option<bool>,
    serviceProjectNumber: Option<&str>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<NotificationEndpointAggregatedList>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = compute_region_notification_endpoints_aggregated_list_builder(
        client,
        project,
        filter,
        includeAllScopes,
        maxResults,
        orderBy,
        pageToken,
        returnPartialSuccess,
        serviceProjectNumber,
    )?;
    compute_region_notification_endpoints_aggregated_list_execute(builder)
}

/// GET projects/{project}/regions/{region}/notificationEndpoints/{notificationEndpoint}
/// Deletes the specified NotificationEndpoint in the given region
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_region_notification_endpoints_delete_execute()` to send, or `compute_region_notification_endpoints_delete` for simplest API.

pub fn compute_region_notification_endpoints_delete_builder(
    client: &SimpleHttpClient,
    notificationEndpoint: &str,
    project: &str,
    region: &str,
    requestId: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/regions/{}/notificationEndpoints/{}",
        notificationEndpoint, project, region,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "requestId",
            requestId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET projects/{project}/regions/{region}/notificationEndpoints/{notificationEndpoint}
/// Deletes the specified NotificationEndpoint in the given region
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_region_notification_endpoints_delete_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_region_notification_endpoints_delete_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_notification_endpoints_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/regions/{region}/notificationEndpoints/{notificationEndpoint}
/// Deletes the specified NotificationEndpoint in the given region
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_region_notification_endpoints_delete_builder()` + `compute_region_notification_endpoints_delete_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_notification_endpoints_delete(
    client: &SimpleHttpClient,
    notificationEndpoint: &str,
    project: &str,
    region: &str,
    requestId: Option<&str>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_region_notification_endpoints_delete_builder(
        client,
        notificationEndpoint,
        project,
        region,
        requestId,
    )?;
    compute_region_notification_endpoints_delete_execute(builder)
}

/// GET projects/{project}/regions/{region}/notificationEndpoints/{notificationEndpoint}
/// Returns the specified NotificationEndpoint resource in the given region.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_region_notification_endpoints_get_execute()` to send, or `compute_region_notification_endpoints_get` for simplest API.

pub fn compute_region_notification_endpoints_get_builder(
    client: &SimpleHttpClient,
    notificationEndpoint: &str,
    project: &str,
    region: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/regions/{}/notificationEndpoints/{}",
        notificationEndpoint, project, region,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/regions/{region}/notificationEndpoints/{notificationEndpoint}
/// Returns the specified NotificationEndpoint resource in the given region.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_region_notification_endpoints_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_region_notification_endpoints_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_notification_endpoints_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<NotificationEndpoint>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: NotificationEndpoint = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/regions/{region}/notificationEndpoints/{notificationEndpoint}
/// Returns the specified NotificationEndpoint resource in the given region.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_region_notification_endpoints_get_builder()` + `compute_region_notification_endpoints_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_notification_endpoints_get(
    client: &SimpleHttpClient,
    notificationEndpoint: &str,
    project: &str,
    region: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<NotificationEndpoint>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = compute_region_notification_endpoints_get_builder(
        client,
        notificationEndpoint,
        project,
        region,
    )?;
    compute_region_notification_endpoints_get_execute(builder)
}

/// GET projects/{project}/regions/{region}/notificationEndpoints
/// Create a NotificationEndpoint in the specified project in the given region using the parameters that are included in the request.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_region_notification_endpoints_insert_execute()` to send, or `compute_region_notification_endpoints_insert` for simplest API.

pub fn compute_region_notification_endpoints_insert_builder(
    client: &SimpleHttpClient,
    project: &str,
    region: &str,
    requestId: Option<&str>,
    body: &NotificationEndpoint,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/regions/{}/notificationEndpoints",
        project, region,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "requestId",
            requestId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET projects/{project}/regions/{region}/notificationEndpoints
/// Create a NotificationEndpoint in the specified project in the given region using the parameters that are included in the request.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_region_notification_endpoints_insert_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_region_notification_endpoints_insert_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_notification_endpoints_insert_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/regions/{region}/notificationEndpoints
/// Create a NotificationEndpoint in the specified project in the given region using the parameters that are included in the request.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_region_notification_endpoints_insert_builder()` + `compute_region_notification_endpoints_insert_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_notification_endpoints_insert(
    client: &SimpleHttpClient,
    project: &str,
    region: &str,
    requestId: Option<&str>,
    body: &NotificationEndpoint,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_region_notification_endpoints_insert_builder(
        client, project, region, requestId, body,
    )?;
    compute_region_notification_endpoints_insert_execute(builder)
}

/// GET projects/{project}/regions/{region}/notificationEndpoints
/// Lists the NotificationEndpoints for a project in the given region.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_region_notification_endpoints_list_execute()` to send, or `compute_region_notification_endpoints_list` for simplest API.

pub fn compute_region_notification_endpoints_list_builder(
    client: &SimpleHttpClient,
    project: &str,
    region: &str,
    filter: Option<&str>,
    maxResults: Option<i32>,
    orderBy: Option<&str>,
    pageToken: Option<&str>,
    returnPartialSuccess: Option<bool>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/regions/{}/notificationEndpoints",
        project, region,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "filter",
            filter
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "maxResults",
            maxResults
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "orderBy",
            orderBy
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageToken",
            pageToken
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "returnPartialSuccess",
            returnPartialSuccess
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET projects/{project}/regions/{region}/notificationEndpoints
/// Lists the NotificationEndpoints for a project in the given region.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_region_notification_endpoints_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_region_notification_endpoints_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_notification_endpoints_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<NotificationEndpointList>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: NotificationEndpointList = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/regions/{region}/notificationEndpoints
/// Lists the NotificationEndpoints for a project in the given region.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_region_notification_endpoints_list_builder()` + `compute_region_notification_endpoints_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_notification_endpoints_list(
    client: &SimpleHttpClient,
    project: &str,
    region: &str,
    filter: Option<&str>,
    maxResults: Option<i32>,
    orderBy: Option<&str>,
    pageToken: Option<&str>,
    returnPartialSuccess: Option<bool>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<NotificationEndpointList>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = compute_region_notification_endpoints_list_builder(
        client,
        project,
        region,
        filter,
        maxResults,
        orderBy,
        pageToken,
        returnPartialSuccess,
    )?;
    compute_region_notification_endpoints_list_execute(builder)
}

/// GET projects/{project}/regions/{region}/notificationEndpoints/{resource}/testIamPermissions
/// Returns permissions that a caller has on the specified resource.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_region_notification_endpoints_test_iam_permissions_execute()` to send, or `compute_region_notification_endpoints_test_iam_permissions` for simplest API.

pub fn compute_region_notification_endpoints_test_iam_permissions_builder(
    client: &SimpleHttpClient,
    project: &str,
    region: &str,
    resource: &str,
    body: &TestPermissionsRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/regions/{}/notificationEndpoints/{}/testIamPermissions",
        project, region, resource,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET projects/{project}/regions/{region}/notificationEndpoints/{resource}/testIamPermissions
/// Returns permissions that a caller has on the specified resource.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_region_notification_endpoints_test_iam_permissions_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_region_notification_endpoints_test_iam_permissions_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_notification_endpoints_test_iam_permissions_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<TestPermissionsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: TestPermissionsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/regions/{region}/notificationEndpoints/{resource}/testIamPermissions
/// Returns permissions that a caller has on the specified resource.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_region_notification_endpoints_test_iam_permissions_builder()` + `compute_region_notification_endpoints_test_iam_permissions_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_notification_endpoints_test_iam_permissions(
    client: &SimpleHttpClient,
    project: &str,
    region: &str,
    resource: &str,
    body: &TestPermissionsRequest,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<TestPermissionsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = compute_region_notification_endpoints_test_iam_permissions_builder(
        client, project, region, resource, body,
    )?;
    compute_region_notification_endpoints_test_iam_permissions_execute(builder)
}

/// GET projects/{project}/regions/{region}/operations/{operation}
/// Deletes the specified region-specific Operations resource.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_region_operations_delete_execute()` to send, or `compute_region_operations_delete` for simplest API.

pub fn compute_region_operations_delete_builder(
    client: &SimpleHttpClient,
    operation: &str,
    project: &str,
    region: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/regions/{}/operations/{}",
        operation, project, region,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/regions/{region}/operations/{operation}
/// Deletes the specified region-specific Operations resource.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_region_operations_delete_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_region_operations_delete_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_operations_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/regions/{region}/operations/{operation}
/// Deletes the specified region-specific Operations resource.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_region_operations_delete_builder()` + `compute_region_operations_delete_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_operations_delete(
    client: &SimpleHttpClient,
    operation: &str,
    project: &str,
    region: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_region_operations_delete_builder(client, operation, project, region)?;
    compute_region_operations_delete_execute(builder)
}

/// GET projects/{project}/regions/{region}/operations/{operation}
/// Retrieves the specified region-specific Operations resource.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_region_operations_get_execute()` to send, or `compute_region_operations_get` for simplest API.

pub fn compute_region_operations_get_builder(
    client: &SimpleHttpClient,
    operation: &str,
    project: &str,
    region: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/regions/{}/operations/{}",
        operation, project, region,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/regions/{region}/operations/{operation}
/// Retrieves the specified region-specific Operations resource.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_region_operations_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_region_operations_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_operations_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/regions/{region}/operations/{operation}
/// Retrieves the specified region-specific Operations resource.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_region_operations_get_builder()` + `compute_region_operations_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_operations_get(
    client: &SimpleHttpClient,
    operation: &str,
    project: &str,
    region: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_region_operations_get_builder(client, operation, project, region)?;
    compute_region_operations_get_execute(builder)
}

/// GET projects/{project}/regions/{region}/operations
/// Retrieves a list of Operation resources contained within the specified region.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_region_operations_list_execute()` to send, or `compute_region_operations_list` for simplest API.

pub fn compute_region_operations_list_builder(
    client: &SimpleHttpClient,
    project: &str,
    region: &str,
    filter: Option<&str>,
    maxResults: Option<i32>,
    orderBy: Option<&str>,
    pageToken: Option<&str>,
    returnPartialSuccess: Option<bool>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/regions/{}/operations",
        project, region,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "filter",
            filter
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "maxResults",
            maxResults
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "orderBy",
            orderBy
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageToken",
            pageToken
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "returnPartialSuccess",
            returnPartialSuccess
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET projects/{project}/regions/{region}/operations
/// Retrieves a list of Operation resources contained within the specified region.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_region_operations_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_region_operations_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_operations_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<OperationList>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: OperationList = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/regions/{region}/operations
/// Retrieves a list of Operation resources contained within the specified region.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_region_operations_list_builder()` + `compute_region_operations_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_operations_list(
    client: &SimpleHttpClient,
    project: &str,
    region: &str,
    filter: Option<&str>,
    maxResults: Option<i32>,
    orderBy: Option<&str>,
    pageToken: Option<&str>,
    returnPartialSuccess: Option<bool>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<OperationList>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = compute_region_operations_list_builder(
        client,
        project,
        region,
        filter,
        maxResults,
        orderBy,
        pageToken,
        returnPartialSuccess,
    )?;
    compute_region_operations_list_execute(builder)
}

/// GET projects/{project}/regions/{region}/operations/{operation}/wait
/// Waits for the specified Operation resource to return as DONE or for the request to approach the 2 minute deadline, and retrieves the specified Operation resource. This method differs from the GET method in that it waits for no more than the default deadline (2 minutes) and then returns the current state of the operation, which might be DONE or still in progress.  This method is called on a best-effort basis. Specifically:   - In uncommon cases, when the server is overloaded, the request might return before the default deadline is reached, or might return after zero seconds. - If the default deadline is reached, there is no guarantee that the operation is actually done when the method returns. Be prepared to retry if the operation is not DONE.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_region_operations_wait_execute()` to send, or `compute_region_operations_wait` for simplest API.

pub fn compute_region_operations_wait_builder(
    client: &SimpleHttpClient,
    operation: &str,
    project: &str,
    region: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/regions/{}/operations/{}/wait",
        operation, project, region,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/regions/{region}/operations/{operation}/wait
/// Waits for the specified Operation resource to return as DONE or for the request to approach the 2 minute deadline, and retrieves the specified Operation resource. This method differs from the GET method in that it waits for no more than the default deadline (2 minutes) and then returns the current state of the operation, which might be DONE or still in progress.  This method is called on a best-effort basis. Specifically:   - In uncommon cases, when the server is overloaded, the request might return before the default deadline is reached, or might return after zero seconds. - If the default deadline is reached, there is no guarantee that the operation is actually done when the method returns. Be prepared to retry if the operation is not DONE.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_region_operations_wait_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_region_operations_wait_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_operations_wait_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/regions/{region}/operations/{operation}/wait
/// Waits for the specified Operation resource to return as DONE or for the request to approach the 2 minute deadline, and retrieves the specified Operation resource. This method differs from the GET method in that it waits for no more than the default deadline (2 minutes) and then returns the current state of the operation, which might be DONE or still in progress.  This method is called on a best-effort basis. Specifically:   - In uncommon cases, when the server is overloaded, the request might return before the default deadline is reached, or might return after zero seconds. - If the default deadline is reached, there is no guarantee that the operation is actually done when the method returns. Be prepared to retry if the operation is not DONE.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_region_operations_wait_builder()` + `compute_region_operations_wait_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_operations_wait(
    client: &SimpleHttpClient,
    operation: &str,
    project: &str,
    region: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_region_operations_wait_builder(client, operation, project, region)?;
    compute_region_operations_wait_execute(builder)
}

/// GET projects/{project}/regions/{region}/securityPolicies/{securityPolicy}/addRule
/// Inserts a rule into a security policy.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_region_security_policies_add_rule_execute()` to send, or `compute_region_security_policies_add_rule` for simplest API.

pub fn compute_region_security_policies_add_rule_builder(
    client: &SimpleHttpClient,
    project: &str,
    region: &str,
    securityPolicy: &str,
    validateOnly: Option<bool>,
    body: &SecurityPolicyRule,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/regions/{}/securityPolicies/{}/addRule",
        project, region, securityPolicy,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "validateOnly",
            validateOnly
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET projects/{project}/regions/{region}/securityPolicies/{securityPolicy}/addRule
/// Inserts a rule into a security policy.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_region_security_policies_add_rule_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_region_security_policies_add_rule_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_security_policies_add_rule_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/regions/{region}/securityPolicies/{securityPolicy}/addRule
/// Inserts a rule into a security policy.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_region_security_policies_add_rule_builder()` + `compute_region_security_policies_add_rule_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_security_policies_add_rule(
    client: &SimpleHttpClient,
    project: &str,
    region: &str,
    securityPolicy: &str,
    validateOnly: Option<bool>,
    body: &SecurityPolicyRule,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_region_security_policies_add_rule_builder(
        client,
        project,
        region,
        securityPolicy,
        validateOnly,
        body,
    )?;
    compute_region_security_policies_add_rule_execute(builder)
}

/// GET projects/{project}/regions/{region}/securityPolicies/{securityPolicy}
/// Deletes the specified policy.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_region_security_policies_delete_execute()` to send, or `compute_region_security_policies_delete` for simplest API.

pub fn compute_region_security_policies_delete_builder(
    client: &SimpleHttpClient,
    project: &str,
    region: &str,
    securityPolicy: &str,
    requestId: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/regions/{}/securityPolicies/{}",
        project, region, securityPolicy,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "requestId",
            requestId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET projects/{project}/regions/{region}/securityPolicies/{securityPolicy}
/// Deletes the specified policy.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_region_security_policies_delete_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_region_security_policies_delete_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_security_policies_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/regions/{region}/securityPolicies/{securityPolicy}
/// Deletes the specified policy.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_region_security_policies_delete_builder()` + `compute_region_security_policies_delete_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_security_policies_delete(
    client: &SimpleHttpClient,
    project: &str,
    region: &str,
    securityPolicy: &str,
    requestId: Option<&str>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_region_security_policies_delete_builder(
        client,
        project,
        region,
        securityPolicy,
        requestId,
    )?;
    compute_region_security_policies_delete_execute(builder)
}

/// GET projects/{project}/regions/{region}/securityPolicies/{securityPolicy}
/// List all of the ordered rules present in a single specified policy.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_region_security_policies_get_execute()` to send, or `compute_region_security_policies_get` for simplest API.

pub fn compute_region_security_policies_get_builder(
    client: &SimpleHttpClient,
    project: &str,
    region: &str,
    securityPolicy: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/regions/{}/securityPolicies/{}",
        project, region, securityPolicy,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/regions/{region}/securityPolicies/{securityPolicy}
/// List all of the ordered rules present in a single specified policy.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_region_security_policies_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_region_security_policies_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_security_policies_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SecurityPolicy>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: SecurityPolicy = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/regions/{region}/securityPolicies/{securityPolicy}
/// List all of the ordered rules present in a single specified policy.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_region_security_policies_get_builder()` + `compute_region_security_policies_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_security_policies_get(
    client: &SimpleHttpClient,
    project: &str,
    region: &str,
    securityPolicy: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SecurityPolicy>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        compute_region_security_policies_get_builder(client, project, region, securityPolicy)?;
    compute_region_security_policies_get_execute(builder)
}

/// GET projects/{project}/regions/{region}/securityPolicies/{securityPolicy}/getRule
/// Gets a rule at the specified priority.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_region_security_policies_get_rule_execute()` to send, or `compute_region_security_policies_get_rule` for simplest API.

pub fn compute_region_security_policies_get_rule_builder(
    client: &SimpleHttpClient,
    project: &str,
    region: &str,
    securityPolicy: &str,
    priority: Option<i32>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/regions/{}/securityPolicies/{}/getRule",
        project, region, securityPolicy,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "priority",
            priority
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET projects/{project}/regions/{region}/securityPolicies/{securityPolicy}/getRule
/// Gets a rule at the specified priority.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_region_security_policies_get_rule_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_region_security_policies_get_rule_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_security_policies_get_rule_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SecurityPolicyRule>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: SecurityPolicyRule = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/regions/{region}/securityPolicies/{securityPolicy}/getRule
/// Gets a rule at the specified priority.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_region_security_policies_get_rule_builder()` + `compute_region_security_policies_get_rule_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_security_policies_get_rule(
    client: &SimpleHttpClient,
    project: &str,
    region: &str,
    securityPolicy: &str,
    priority: Option<i32>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SecurityPolicyRule>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = compute_region_security_policies_get_rule_builder(
        client,
        project,
        region,
        securityPolicy,
        priority,
    )?;
    compute_region_security_policies_get_rule_execute(builder)
}

/// GET projects/{project}/regions/{region}/securityPolicies
/// Creates a new policy in the specified project using the data included in the request.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_region_security_policies_insert_execute()` to send, or `compute_region_security_policies_insert` for simplest API.

pub fn compute_region_security_policies_insert_builder(
    client: &SimpleHttpClient,
    project: &str,
    region: &str,
    requestId: Option<&str>,
    validateOnly: Option<bool>,
    body: &SecurityPolicy,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/regions/{}/securityPolicies",
        project, region,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "requestId",
            requestId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "validateOnly",
            validateOnly
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET projects/{project}/regions/{region}/securityPolicies
/// Creates a new policy in the specified project using the data included in the request.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_region_security_policies_insert_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_region_security_policies_insert_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_security_policies_insert_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/regions/{region}/securityPolicies
/// Creates a new policy in the specified project using the data included in the request.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_region_security_policies_insert_builder()` + `compute_region_security_policies_insert_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_security_policies_insert(
    client: &SimpleHttpClient,
    project: &str,
    region: &str,
    requestId: Option<&str>,
    validateOnly: Option<bool>,
    body: &SecurityPolicy,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_region_security_policies_insert_builder(
        client,
        project,
        region,
        requestId,
        validateOnly,
        body,
    )?;
    compute_region_security_policies_insert_execute(builder)
}

/// GET projects/{project}/regions/{region}/securityPolicies
/// List all the policies that have been configured for the specified project and region.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_region_security_policies_list_execute()` to send, or `compute_region_security_policies_list` for simplest API.

pub fn compute_region_security_policies_list_builder(
    client: &SimpleHttpClient,
    project: &str,
    region: &str,
    filter: Option<&str>,
    maxResults: Option<i32>,
    orderBy: Option<&str>,
    pageToken: Option<&str>,
    returnPartialSuccess: Option<bool>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/regions/{}/securityPolicies",
        project, region,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "filter",
            filter
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "maxResults",
            maxResults
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "orderBy",
            orderBy
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageToken",
            pageToken
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "returnPartialSuccess",
            returnPartialSuccess
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET projects/{project}/regions/{region}/securityPolicies
/// List all the policies that have been configured for the specified project and region.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_region_security_policies_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_region_security_policies_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_security_policies_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SecurityPolicyList>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: SecurityPolicyList = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/regions/{region}/securityPolicies
/// List all the policies that have been configured for the specified project and region.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_region_security_policies_list_builder()` + `compute_region_security_policies_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_security_policies_list(
    client: &SimpleHttpClient,
    project: &str,
    region: &str,
    filter: Option<&str>,
    maxResults: Option<i32>,
    orderBy: Option<&str>,
    pageToken: Option<&str>,
    returnPartialSuccess: Option<bool>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SecurityPolicyList>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = compute_region_security_policies_list_builder(
        client,
        project,
        region,
        filter,
        maxResults,
        orderBy,
        pageToken,
        returnPartialSuccess,
    )?;
    compute_region_security_policies_list_execute(builder)
}

/// GET projects/{project}/regions/{region}/securityPolicies/{securityPolicy}
/// Patches the specified policy with the data included in the request. To clear fields in the policy, leave the fields empty and specify them in the `updateMask`. This cannot be used to be update the rules in the policy. Please use the per rule methods like `addRule`, `patchRule`, and `removeRule` instead.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_region_security_policies_patch_execute()` to send, or `compute_region_security_policies_patch` for simplest API.

pub fn compute_region_security_policies_patch_builder(
    client: &SimpleHttpClient,
    project: &str,
    region: &str,
    securityPolicy: &str,
    requestId: Option<&str>,
    updateMask: Option<&str>,
    body: &SecurityPolicy,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/regions/{}/securityPolicies/{}",
        project, region, securityPolicy,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "requestId",
            requestId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "updateMask",
            updateMask
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET projects/{project}/regions/{region}/securityPolicies/{securityPolicy}
/// Patches the specified policy with the data included in the request. To clear fields in the policy, leave the fields empty and specify them in the `updateMask`. This cannot be used to be update the rules in the policy. Please use the per rule methods like `addRule`, `patchRule`, and `removeRule` instead.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_region_security_policies_patch_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_region_security_policies_patch_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_security_policies_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/regions/{region}/securityPolicies/{securityPolicy}
/// Patches the specified policy with the data included in the request. To clear fields in the policy, leave the fields empty and specify them in the `updateMask`. This cannot be used to be update the rules in the policy. Please use the per rule methods like `addRule`, `patchRule`, and `removeRule` instead.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_region_security_policies_patch_builder()` + `compute_region_security_policies_patch_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_security_policies_patch(
    client: &SimpleHttpClient,
    project: &str,
    region: &str,
    securityPolicy: &str,
    requestId: Option<&str>,
    updateMask: Option<&str>,
    body: &SecurityPolicy,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_region_security_policies_patch_builder(
        client,
        project,
        region,
        securityPolicy,
        requestId,
        updateMask,
        body,
    )?;
    compute_region_security_policies_patch_execute(builder)
}

/// GET projects/{project}/regions/{region}/securityPolicies/{securityPolicy}/patchRule
/// Patches a rule at the specified priority. To clear fields in the rule, leave the fields empty and specify them in the `updateMask`.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_region_security_policies_patch_rule_execute()` to send, or `compute_region_security_policies_patch_rule` for simplest API.

pub fn compute_region_security_policies_patch_rule_builder(
    client: &SimpleHttpClient,
    project: &str,
    region: &str,
    securityPolicy: &str,
    priority: Option<i32>,
    updateMask: Option<&str>,
    validateOnly: Option<bool>,
    body: &SecurityPolicyRule,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/regions/{}/securityPolicies/{}/patchRule",
        project, region, securityPolicy,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "priority",
            priority
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "updateMask",
            updateMask
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "validateOnly",
            validateOnly
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET projects/{project}/regions/{region}/securityPolicies/{securityPolicy}/patchRule
/// Patches a rule at the specified priority. To clear fields in the rule, leave the fields empty and specify them in the `updateMask`.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_region_security_policies_patch_rule_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_region_security_policies_patch_rule_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_security_policies_patch_rule_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/regions/{region}/securityPolicies/{securityPolicy}/patchRule
/// Patches a rule at the specified priority. To clear fields in the rule, leave the fields empty and specify them in the `updateMask`.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_region_security_policies_patch_rule_builder()` + `compute_region_security_policies_patch_rule_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_security_policies_patch_rule(
    client: &SimpleHttpClient,
    project: &str,
    region: &str,
    securityPolicy: &str,
    priority: Option<i32>,
    updateMask: Option<&str>,
    validateOnly: Option<bool>,
    body: &SecurityPolicyRule,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_region_security_policies_patch_rule_builder(
        client,
        project,
        region,
        securityPolicy,
        priority,
        updateMask,
        validateOnly,
        body,
    )?;
    compute_region_security_policies_patch_rule_execute(builder)
}

/// GET projects/{project}/regions/{region}/securityPolicies/{securityPolicy}/removeRule
/// Deletes a rule at the specified priority.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_region_security_policies_remove_rule_execute()` to send, or `compute_region_security_policies_remove_rule` for simplest API.

pub fn compute_region_security_policies_remove_rule_builder(
    client: &SimpleHttpClient,
    project: &str,
    region: &str,
    securityPolicy: &str,
    priority: Option<i32>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/regions/{}/securityPolicies/{}/removeRule",
        project, region, securityPolicy,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "priority",
            priority
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET projects/{project}/regions/{region}/securityPolicies/{securityPolicy}/removeRule
/// Deletes a rule at the specified priority.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_region_security_policies_remove_rule_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_region_security_policies_remove_rule_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_security_policies_remove_rule_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/regions/{region}/securityPolicies/{securityPolicy}/removeRule
/// Deletes a rule at the specified priority.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_region_security_policies_remove_rule_builder()` + `compute_region_security_policies_remove_rule_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_security_policies_remove_rule(
    client: &SimpleHttpClient,
    project: &str,
    region: &str,
    securityPolicy: &str,
    priority: Option<i32>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_region_security_policies_remove_rule_builder(
        client,
        project,
        region,
        securityPolicy,
        priority,
    )?;
    compute_region_security_policies_remove_rule_execute(builder)
}

/// GET projects/{project}/regions/{region}/securityPolicies/{resource}/setLabels
/// Sets the labels on a security policy. To learn more about labels, read the Labeling Resources documentation.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_region_security_policies_set_labels_execute()` to send, or `compute_region_security_policies_set_labels` for simplest API.

pub fn compute_region_security_policies_set_labels_builder(
    client: &SimpleHttpClient,
    project: &str,
    region: &str,
    resource: &str,
    requestId: Option<&str>,
    body: &RegionSetLabelsRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/regions/{}/securityPolicies/{}/setLabels",
        project, region, resource,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "requestId",
            requestId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET projects/{project}/regions/{region}/securityPolicies/{resource}/setLabels
/// Sets the labels on a security policy. To learn more about labels, read the Labeling Resources documentation.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_region_security_policies_set_labels_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_region_security_policies_set_labels_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_security_policies_set_labels_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/regions/{region}/securityPolicies/{resource}/setLabels
/// Sets the labels on a security policy. To learn more about labels, read the Labeling Resources documentation.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_region_security_policies_set_labels_builder()` + `compute_region_security_policies_set_labels_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_security_policies_set_labels(
    client: &SimpleHttpClient,
    project: &str,
    region: &str,
    resource: &str,
    requestId: Option<&str>,
    body: &RegionSetLabelsRequest,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_region_security_policies_set_labels_builder(
        client, project, region, resource, requestId, body,
    )?;
    compute_region_security_policies_set_labels_execute(builder)
}

/// GET projects/{project}/regions/{region}/sslCertificates/{sslCertificate}
/// Deletes the specified SslCertificate resource in the region.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_region_ssl_certificates_delete_execute()` to send, or `compute_region_ssl_certificates_delete` for simplest API.

pub fn compute_region_ssl_certificates_delete_builder(
    client: &SimpleHttpClient,
    project: &str,
    region: &str,
    sslCertificate: &str,
    requestId: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/regions/{}/sslCertificates/{}",
        project, region, sslCertificate,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "requestId",
            requestId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET projects/{project}/regions/{region}/sslCertificates/{sslCertificate}
/// Deletes the specified SslCertificate resource in the region.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_region_ssl_certificates_delete_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_region_ssl_certificates_delete_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_ssl_certificates_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/regions/{region}/sslCertificates/{sslCertificate}
/// Deletes the specified SslCertificate resource in the region.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_region_ssl_certificates_delete_builder()` + `compute_region_ssl_certificates_delete_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_ssl_certificates_delete(
    client: &SimpleHttpClient,
    project: &str,
    region: &str,
    sslCertificate: &str,
    requestId: Option<&str>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_region_ssl_certificates_delete_builder(
        client,
        project,
        region,
        sslCertificate,
        requestId,
    )?;
    compute_region_ssl_certificates_delete_execute(builder)
}

/// GET projects/{project}/regions/{region}/sslCertificates/{sslCertificate}
/// Returns the specified SslCertificate resource in the specified region. Get a list of available SSL certificates by making a list() request.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_region_ssl_certificates_get_execute()` to send, or `compute_region_ssl_certificates_get` for simplest API.

pub fn compute_region_ssl_certificates_get_builder(
    client: &SimpleHttpClient,
    project: &str,
    region: &str,
    sslCertificate: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/regions/{}/sslCertificates/{}",
        project, region, sslCertificate,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/regions/{region}/sslCertificates/{sslCertificate}
/// Returns the specified SslCertificate resource in the specified region. Get a list of available SSL certificates by making a list() request.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_region_ssl_certificates_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_region_ssl_certificates_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_ssl_certificates_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SslCertificate>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: SslCertificate = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/regions/{region}/sslCertificates/{sslCertificate}
/// Returns the specified SslCertificate resource in the specified region. Get a list of available SSL certificates by making a list() request.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_region_ssl_certificates_get_builder()` + `compute_region_ssl_certificates_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_ssl_certificates_get(
    client: &SimpleHttpClient,
    project: &str,
    region: &str,
    sslCertificate: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SslCertificate>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        compute_region_ssl_certificates_get_builder(client, project, region, sslCertificate)?;
    compute_region_ssl_certificates_get_execute(builder)
}

/// GET projects/{project}/regions/{region}/sslCertificates
/// Creates a SslCertificate resource in the specified project and region using the data included in the request
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_region_ssl_certificates_insert_execute()` to send, or `compute_region_ssl_certificates_insert` for simplest API.

pub fn compute_region_ssl_certificates_insert_builder(
    client: &SimpleHttpClient,
    project: &str,
    region: &str,
    requestId: Option<&str>,
    body: &SslCertificate,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/regions/{}/sslCertificates",
        project, region,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "requestId",
            requestId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET projects/{project}/regions/{region}/sslCertificates
/// Creates a SslCertificate resource in the specified project and region using the data included in the request
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_region_ssl_certificates_insert_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_region_ssl_certificates_insert_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_ssl_certificates_insert_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/regions/{region}/sslCertificates
/// Creates a SslCertificate resource in the specified project and region using the data included in the request
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_region_ssl_certificates_insert_builder()` + `compute_region_ssl_certificates_insert_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_ssl_certificates_insert(
    client: &SimpleHttpClient,
    project: &str,
    region: &str,
    requestId: Option<&str>,
    body: &SslCertificate,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        compute_region_ssl_certificates_insert_builder(client, project, region, requestId, body)?;
    compute_region_ssl_certificates_insert_execute(builder)
}

/// GET projects/{project}/regions/{region}/sslCertificates
/// Retrieves the list of SslCertificate resources available to the specified project in the specified region.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_region_ssl_certificates_list_execute()` to send, or `compute_region_ssl_certificates_list` for simplest API.

pub fn compute_region_ssl_certificates_list_builder(
    client: &SimpleHttpClient,
    project: &str,
    region: &str,
    filter: Option<&str>,
    maxResults: Option<i32>,
    orderBy: Option<&str>,
    pageToken: Option<&str>,
    returnPartialSuccess: Option<bool>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/regions/{}/sslCertificates",
        project, region,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "filter",
            filter
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "maxResults",
            maxResults
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "orderBy",
            orderBy
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageToken",
            pageToken
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "returnPartialSuccess",
            returnPartialSuccess
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET projects/{project}/regions/{region}/sslCertificates
/// Retrieves the list of SslCertificate resources available to the specified project in the specified region.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_region_ssl_certificates_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_region_ssl_certificates_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_ssl_certificates_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SslCertificateList>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: SslCertificateList = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/regions/{region}/sslCertificates
/// Retrieves the list of SslCertificate resources available to the specified project in the specified region.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_region_ssl_certificates_list_builder()` + `compute_region_ssl_certificates_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_ssl_certificates_list(
    client: &SimpleHttpClient,
    project: &str,
    region: &str,
    filter: Option<&str>,
    maxResults: Option<i32>,
    orderBy: Option<&str>,
    pageToken: Option<&str>,
    returnPartialSuccess: Option<bool>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SslCertificateList>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = compute_region_ssl_certificates_list_builder(
        client,
        project,
        region,
        filter,
        maxResults,
        orderBy,
        pageToken,
        returnPartialSuccess,
    )?;
    compute_region_ssl_certificates_list_execute(builder)
}

/// GET projects/{project}/regions/{region}/sslPolicies/{sslPolicy}
/// Deletes the specified SSL policy. The SSL policy resource can be deleted only if it is not in use by any TargetHttpsProxy or TargetSslProxy resources.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_region_ssl_policies_delete_execute()` to send, or `compute_region_ssl_policies_delete` for simplest API.

pub fn compute_region_ssl_policies_delete_builder(
    client: &SimpleHttpClient,
    project: &str,
    region: &str,
    sslPolicy: &str,
    requestId: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/regions/{}/sslPolicies/{}",
        project, region, sslPolicy,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "requestId",
            requestId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET projects/{project}/regions/{region}/sslPolicies/{sslPolicy}
/// Deletes the specified SSL policy. The SSL policy resource can be deleted only if it is not in use by any TargetHttpsProxy or TargetSslProxy resources.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_region_ssl_policies_delete_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_region_ssl_policies_delete_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_ssl_policies_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/regions/{region}/sslPolicies/{sslPolicy}
/// Deletes the specified SSL policy. The SSL policy resource can be deleted only if it is not in use by any TargetHttpsProxy or TargetSslProxy resources.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_region_ssl_policies_delete_builder()` + `compute_region_ssl_policies_delete_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_ssl_policies_delete(
    client: &SimpleHttpClient,
    project: &str,
    region: &str,
    sslPolicy: &str,
    requestId: Option<&str>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        compute_region_ssl_policies_delete_builder(client, project, region, sslPolicy, requestId)?;
    compute_region_ssl_policies_delete_execute(builder)
}

/// GET projects/{project}/regions/{region}/sslPolicies/{sslPolicy}
/// Lists all of the ordered rules present in a single specified policy.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_region_ssl_policies_get_execute()` to send, or `compute_region_ssl_policies_get` for simplest API.

pub fn compute_region_ssl_policies_get_builder(
    client: &SimpleHttpClient,
    project: &str,
    region: &str,
    sslPolicy: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/regions/{}/sslPolicies/{}",
        project, region, sslPolicy,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/regions/{region}/sslPolicies/{sslPolicy}
/// Lists all of the ordered rules present in a single specified policy.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_region_ssl_policies_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_region_ssl_policies_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_ssl_policies_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SslPolicy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: SslPolicy = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/regions/{region}/sslPolicies/{sslPolicy}
/// Lists all of the ordered rules present in a single specified policy.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_region_ssl_policies_get_builder()` + `compute_region_ssl_policies_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_ssl_policies_get(
    client: &SimpleHttpClient,
    project: &str,
    region: &str,
    sslPolicy: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SslPolicy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_region_ssl_policies_get_builder(client, project, region, sslPolicy)?;
    compute_region_ssl_policies_get_execute(builder)
}

/// GET projects/{project}/regions/{region}/sslPolicies
/// Creates a new policy in the specified project and region using the data included in the request.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_region_ssl_policies_insert_execute()` to send, or `compute_region_ssl_policies_insert` for simplest API.

pub fn compute_region_ssl_policies_insert_builder(
    client: &SimpleHttpClient,
    project: &str,
    region: &str,
    requestId: Option<&str>,
    body: &SslPolicy,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/regions/{}/sslPolicies",
        project, region,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "requestId",
            requestId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET projects/{project}/regions/{region}/sslPolicies
/// Creates a new policy in the specified project and region using the data included in the request.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_region_ssl_policies_insert_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_region_ssl_policies_insert_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_ssl_policies_insert_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/regions/{region}/sslPolicies
/// Creates a new policy in the specified project and region using the data included in the request.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_region_ssl_policies_insert_builder()` + `compute_region_ssl_policies_insert_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_ssl_policies_insert(
    client: &SimpleHttpClient,
    project: &str,
    region: &str,
    requestId: Option<&str>,
    body: &SslPolicy,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        compute_region_ssl_policies_insert_builder(client, project, region, requestId, body)?;
    compute_region_ssl_policies_insert_execute(builder)
}

/// GET projects/{project}/regions/{region}/sslPolicies
/// Lists all the SSL policies that have been configured for the specified project and region.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_region_ssl_policies_list_execute()` to send, or `compute_region_ssl_policies_list` for simplest API.

pub fn compute_region_ssl_policies_list_builder(
    client: &SimpleHttpClient,
    project: &str,
    region: &str,
    filter: Option<&str>,
    maxResults: Option<i32>,
    orderBy: Option<&str>,
    pageToken: Option<&str>,
    returnPartialSuccess: Option<bool>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/regions/{}/sslPolicies",
        project, region,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "filter",
            filter
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "maxResults",
            maxResults
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "orderBy",
            orderBy
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageToken",
            pageToken
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "returnPartialSuccess",
            returnPartialSuccess
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET projects/{project}/regions/{region}/sslPolicies
/// Lists all the SSL policies that have been configured for the specified project and region.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_region_ssl_policies_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_region_ssl_policies_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_ssl_policies_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SslPoliciesList>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: SslPoliciesList = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/regions/{region}/sslPolicies
/// Lists all the SSL policies that have been configured for the specified project and region.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_region_ssl_policies_list_builder()` + `compute_region_ssl_policies_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_ssl_policies_list(
    client: &SimpleHttpClient,
    project: &str,
    region: &str,
    filter: Option<&str>,
    maxResults: Option<i32>,
    orderBy: Option<&str>,
    pageToken: Option<&str>,
    returnPartialSuccess: Option<bool>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SslPoliciesList>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = compute_region_ssl_policies_list_builder(
        client,
        project,
        region,
        filter,
        maxResults,
        orderBy,
        pageToken,
        returnPartialSuccess,
    )?;
    compute_region_ssl_policies_list_execute(builder)
}

/// GET projects/{project}/regions/{region}/sslPolicies/listAvailableFeatures
/// Lists all features that can be specified in the SSL policy when using custom profile.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_region_ssl_policies_list_available_features_execute()` to send, or `compute_region_ssl_policies_list_available_features` for simplest API.

pub fn compute_region_ssl_policies_list_available_features_builder(
    client: &SimpleHttpClient,
    project: &str,
    region: &str,
    filter: Option<&str>,
    maxResults: Option<i32>,
    orderBy: Option<&str>,
    pageToken: Option<&str>,
    returnPartialSuccess: Option<bool>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/regions/{}/sslPolicies/listAvailableFeatures",
        project, region,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "filter",
            filter
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "maxResults",
            maxResults
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "orderBy",
            orderBy
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageToken",
            pageToken
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "returnPartialSuccess",
            returnPartialSuccess
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET projects/{project}/regions/{region}/sslPolicies/listAvailableFeatures
/// Lists all features that can be specified in the SSL policy when using custom profile.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_region_ssl_policies_list_available_features_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_region_ssl_policies_list_available_features_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_ssl_policies_list_available_features_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<SslPoliciesListAvailableFeaturesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: SslPoliciesListAvailableFeaturesResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/regions/{region}/sslPolicies/listAvailableFeatures
/// Lists all features that can be specified in the SSL policy when using custom profile.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_region_ssl_policies_list_available_features_builder()` + `compute_region_ssl_policies_list_available_features_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_ssl_policies_list_available_features(
    client: &SimpleHttpClient,
    project: &str,
    region: &str,
    filter: Option<&str>,
    maxResults: Option<i32>,
    orderBy: Option<&str>,
    pageToken: Option<&str>,
    returnPartialSuccess: Option<bool>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<SslPoliciesListAvailableFeaturesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = compute_region_ssl_policies_list_available_features_builder(
        client,
        project,
        region,
        filter,
        maxResults,
        orderBy,
        pageToken,
        returnPartialSuccess,
    )?;
    compute_region_ssl_policies_list_available_features_execute(builder)
}

/// GET projects/{project}/regions/{region}/sslPolicies/{sslPolicy}
/// Patches the specified SSL policy with the data included in the request.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_region_ssl_policies_patch_execute()` to send, or `compute_region_ssl_policies_patch` for simplest API.

pub fn compute_region_ssl_policies_patch_builder(
    client: &SimpleHttpClient,
    project: &str,
    region: &str,
    sslPolicy: &str,
    requestId: Option<&str>,
    body: &SslPolicy,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/regions/{}/sslPolicies/{}",
        project, region, sslPolicy,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "requestId",
            requestId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET projects/{project}/regions/{region}/sslPolicies/{sslPolicy}
/// Patches the specified SSL policy with the data included in the request.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_region_ssl_policies_patch_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_region_ssl_policies_patch_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_ssl_policies_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/regions/{region}/sslPolicies/{sslPolicy}
/// Patches the specified SSL policy with the data included in the request.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_region_ssl_policies_patch_builder()` + `compute_region_ssl_policies_patch_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_ssl_policies_patch(
    client: &SimpleHttpClient,
    project: &str,
    region: &str,
    sslPolicy: &str,
    requestId: Option<&str>,
    body: &SslPolicy,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_region_ssl_policies_patch_builder(
        client, project, region, sslPolicy, requestId, body,
    )?;
    compute_region_ssl_policies_patch_execute(builder)
}

/// GET projects/{project}/regions/{region}/targetHttpProxies/{targetHttpProxy}
/// Deletes the specified TargetHttpProxy resource.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_region_target_http_proxies_delete_execute()` to send, or `compute_region_target_http_proxies_delete` for simplest API.

pub fn compute_region_target_http_proxies_delete_builder(
    client: &SimpleHttpClient,
    project: &str,
    region: &str,
    targetHttpProxy: &str,
    requestId: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/regions/{}/targetHttpProxies/{}",
        project, region, targetHttpProxy,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "requestId",
            requestId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET projects/{project}/regions/{region}/targetHttpProxies/{targetHttpProxy}
/// Deletes the specified TargetHttpProxy resource.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_region_target_http_proxies_delete_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_region_target_http_proxies_delete_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_target_http_proxies_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/regions/{region}/targetHttpProxies/{targetHttpProxy}
/// Deletes the specified TargetHttpProxy resource.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_region_target_http_proxies_delete_builder()` + `compute_region_target_http_proxies_delete_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_target_http_proxies_delete(
    client: &SimpleHttpClient,
    project: &str,
    region: &str,
    targetHttpProxy: &str,
    requestId: Option<&str>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_region_target_http_proxies_delete_builder(
        client,
        project,
        region,
        targetHttpProxy,
        requestId,
    )?;
    compute_region_target_http_proxies_delete_execute(builder)
}

/// GET projects/{project}/regions/{region}/targetHttpProxies/{targetHttpProxy}
/// Returns the specified TargetHttpProxy resource in the specified region.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_region_target_http_proxies_get_execute()` to send, or `compute_region_target_http_proxies_get` for simplest API.

pub fn compute_region_target_http_proxies_get_builder(
    client: &SimpleHttpClient,
    project: &str,
    region: &str,
    targetHttpProxy: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/regions/{}/targetHttpProxies/{}",
        project, region, targetHttpProxy,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/regions/{region}/targetHttpProxies/{targetHttpProxy}
/// Returns the specified TargetHttpProxy resource in the specified region.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_region_target_http_proxies_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_region_target_http_proxies_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_target_http_proxies_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<TargetHttpProxy>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: TargetHttpProxy = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/regions/{region}/targetHttpProxies/{targetHttpProxy}
/// Returns the specified TargetHttpProxy resource in the specified region.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_region_target_http_proxies_get_builder()` + `compute_region_target_http_proxies_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_target_http_proxies_get(
    client: &SimpleHttpClient,
    project: &str,
    region: &str,
    targetHttpProxy: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<TargetHttpProxy>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        compute_region_target_http_proxies_get_builder(client, project, region, targetHttpProxy)?;
    compute_region_target_http_proxies_get_execute(builder)
}

/// GET projects/{project}/regions/{region}/targetHttpProxies
/// Creates a TargetHttpProxy resource in the specified project and region using the data included in the request.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_region_target_http_proxies_insert_execute()` to send, or `compute_region_target_http_proxies_insert` for simplest API.

pub fn compute_region_target_http_proxies_insert_builder(
    client: &SimpleHttpClient,
    project: &str,
    region: &str,
    requestId: Option<&str>,
    body: &TargetHttpProxy,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/regions/{}/targetHttpProxies",
        project, region,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "requestId",
            requestId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET projects/{project}/regions/{region}/targetHttpProxies
/// Creates a TargetHttpProxy resource in the specified project and region using the data included in the request.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_region_target_http_proxies_insert_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_region_target_http_proxies_insert_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_target_http_proxies_insert_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/regions/{region}/targetHttpProxies
/// Creates a TargetHttpProxy resource in the specified project and region using the data included in the request.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_region_target_http_proxies_insert_builder()` + `compute_region_target_http_proxies_insert_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_target_http_proxies_insert(
    client: &SimpleHttpClient,
    project: &str,
    region: &str,
    requestId: Option<&str>,
    body: &TargetHttpProxy,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_region_target_http_proxies_insert_builder(
        client, project, region, requestId, body,
    )?;
    compute_region_target_http_proxies_insert_execute(builder)
}

/// GET projects/{project}/regions/{region}/targetHttpProxies
/// Retrieves the list of TargetHttpProxy resources available to the specified project in the specified region.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_region_target_http_proxies_list_execute()` to send, or `compute_region_target_http_proxies_list` for simplest API.

pub fn compute_region_target_http_proxies_list_builder(
    client: &SimpleHttpClient,
    project: &str,
    region: &str,
    filter: Option<&str>,
    maxResults: Option<i32>,
    orderBy: Option<&str>,
    pageToken: Option<&str>,
    returnPartialSuccess: Option<bool>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/regions/{}/targetHttpProxies",
        project, region,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "filter",
            filter
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "maxResults",
            maxResults
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "orderBy",
            orderBy
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageToken",
            pageToken
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "returnPartialSuccess",
            returnPartialSuccess
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET projects/{project}/regions/{region}/targetHttpProxies
/// Retrieves the list of TargetHttpProxy resources available to the specified project in the specified region.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_region_target_http_proxies_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_region_target_http_proxies_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_target_http_proxies_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<TargetHttpProxyList>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: TargetHttpProxyList = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/regions/{region}/targetHttpProxies
/// Retrieves the list of TargetHttpProxy resources available to the specified project in the specified region.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_region_target_http_proxies_list_builder()` + `compute_region_target_http_proxies_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_target_http_proxies_list(
    client: &SimpleHttpClient,
    project: &str,
    region: &str,
    filter: Option<&str>,
    maxResults: Option<i32>,
    orderBy: Option<&str>,
    pageToken: Option<&str>,
    returnPartialSuccess: Option<bool>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<TargetHttpProxyList>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = compute_region_target_http_proxies_list_builder(
        client,
        project,
        region,
        filter,
        maxResults,
        orderBy,
        pageToken,
        returnPartialSuccess,
    )?;
    compute_region_target_http_proxies_list_execute(builder)
}

/// GET projects/{project}/regions/{region}/targetHttpProxies/{targetHttpProxy}/setUrlMap
/// Changes the URL map for TargetHttpProxy.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_region_target_http_proxies_set_url_map_execute()` to send, or `compute_region_target_http_proxies_set_url_map` for simplest API.

pub fn compute_region_target_http_proxies_set_url_map_builder(
    client: &SimpleHttpClient,
    project: &str,
    region: &str,
    targetHttpProxy: &str,
    requestId: Option<&str>,
    body: &UrlMapReference,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/regions/{}/targetHttpProxies/{}/setUrlMap",
        project, region, targetHttpProxy,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "requestId",
            requestId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET projects/{project}/regions/{region}/targetHttpProxies/{targetHttpProxy}/setUrlMap
/// Changes the URL map for TargetHttpProxy.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_region_target_http_proxies_set_url_map_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_region_target_http_proxies_set_url_map_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_target_http_proxies_set_url_map_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/regions/{region}/targetHttpProxies/{targetHttpProxy}/setUrlMap
/// Changes the URL map for TargetHttpProxy.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_region_target_http_proxies_set_url_map_builder()` + `compute_region_target_http_proxies_set_url_map_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_target_http_proxies_set_url_map(
    client: &SimpleHttpClient,
    project: &str,
    region: &str,
    targetHttpProxy: &str,
    requestId: Option<&str>,
    body: &UrlMapReference,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_region_target_http_proxies_set_url_map_builder(
        client,
        project,
        region,
        targetHttpProxy,
        requestId,
        body,
    )?;
    compute_region_target_http_proxies_set_url_map_execute(builder)
}

/// GET projects/{project}/regions/{region}/targetHttpsProxies/{targetHttpsProxy}
/// Deletes the specified TargetHttpsProxy resource.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_region_target_https_proxies_delete_execute()` to send, or `compute_region_target_https_proxies_delete` for simplest API.

pub fn compute_region_target_https_proxies_delete_builder(
    client: &SimpleHttpClient,
    project: &str,
    region: &str,
    targetHttpsProxy: &str,
    requestId: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/regions/{}/targetHttpsProxies/{}",
        project, region, targetHttpsProxy,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "requestId",
            requestId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET projects/{project}/regions/{region}/targetHttpsProxies/{targetHttpsProxy}
/// Deletes the specified TargetHttpsProxy resource.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_region_target_https_proxies_delete_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_region_target_https_proxies_delete_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_target_https_proxies_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/regions/{region}/targetHttpsProxies/{targetHttpsProxy}
/// Deletes the specified TargetHttpsProxy resource.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_region_target_https_proxies_delete_builder()` + `compute_region_target_https_proxies_delete_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_target_https_proxies_delete(
    client: &SimpleHttpClient,
    project: &str,
    region: &str,
    targetHttpsProxy: &str,
    requestId: Option<&str>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_region_target_https_proxies_delete_builder(
        client,
        project,
        region,
        targetHttpsProxy,
        requestId,
    )?;
    compute_region_target_https_proxies_delete_execute(builder)
}

/// GET projects/{project}/regions/{region}/targetHttpsProxies/{targetHttpsProxy}
/// Returns the specified TargetHttpsProxy resource in the specified region.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_region_target_https_proxies_get_execute()` to send, or `compute_region_target_https_proxies_get` for simplest API.

pub fn compute_region_target_https_proxies_get_builder(
    client: &SimpleHttpClient,
    project: &str,
    region: &str,
    targetHttpsProxy: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/regions/{}/targetHttpsProxies/{}",
        project, region, targetHttpsProxy,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/regions/{region}/targetHttpsProxies/{targetHttpsProxy}
/// Returns the specified TargetHttpsProxy resource in the specified region.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_region_target_https_proxies_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_region_target_https_proxies_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_target_https_proxies_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<TargetHttpsProxy>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: TargetHttpsProxy = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/regions/{region}/targetHttpsProxies/{targetHttpsProxy}
/// Returns the specified TargetHttpsProxy resource in the specified region.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_region_target_https_proxies_get_builder()` + `compute_region_target_https_proxies_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_target_https_proxies_get(
    client: &SimpleHttpClient,
    project: &str,
    region: &str,
    targetHttpsProxy: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<TargetHttpsProxy>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        compute_region_target_https_proxies_get_builder(client, project, region, targetHttpsProxy)?;
    compute_region_target_https_proxies_get_execute(builder)
}

/// GET projects/{project}/regions/{region}/targetHttpsProxies
/// Creates a TargetHttpsProxy resource in the specified project and region using the data included in the request.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_region_target_https_proxies_insert_execute()` to send, or `compute_region_target_https_proxies_insert` for simplest API.

pub fn compute_region_target_https_proxies_insert_builder(
    client: &SimpleHttpClient,
    project: &str,
    region: &str,
    requestId: Option<&str>,
    body: &TargetHttpsProxy,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/regions/{}/targetHttpsProxies",
        project, region,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "requestId",
            requestId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET projects/{project}/regions/{region}/targetHttpsProxies
/// Creates a TargetHttpsProxy resource in the specified project and region using the data included in the request.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_region_target_https_proxies_insert_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_region_target_https_proxies_insert_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_target_https_proxies_insert_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/regions/{region}/targetHttpsProxies
/// Creates a TargetHttpsProxy resource in the specified project and region using the data included in the request.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_region_target_https_proxies_insert_builder()` + `compute_region_target_https_proxies_insert_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_target_https_proxies_insert(
    client: &SimpleHttpClient,
    project: &str,
    region: &str,
    requestId: Option<&str>,
    body: &TargetHttpsProxy,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_region_target_https_proxies_insert_builder(
        client, project, region, requestId, body,
    )?;
    compute_region_target_https_proxies_insert_execute(builder)
}

/// GET projects/{project}/regions/{region}/targetHttpsProxies
/// Retrieves the list of TargetHttpsProxy resources available to the specified project in the specified region.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_region_target_https_proxies_list_execute()` to send, or `compute_region_target_https_proxies_list` for simplest API.

pub fn compute_region_target_https_proxies_list_builder(
    client: &SimpleHttpClient,
    project: &str,
    region: &str,
    filter: Option<&str>,
    maxResults: Option<i32>,
    orderBy: Option<&str>,
    pageToken: Option<&str>,
    returnPartialSuccess: Option<bool>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/regions/{}/targetHttpsProxies",
        project, region,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "filter",
            filter
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "maxResults",
            maxResults
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "orderBy",
            orderBy
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageToken",
            pageToken
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "returnPartialSuccess",
            returnPartialSuccess
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET projects/{project}/regions/{region}/targetHttpsProxies
/// Retrieves the list of TargetHttpsProxy resources available to the specified project in the specified region.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_region_target_https_proxies_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_region_target_https_proxies_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_target_https_proxies_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<TargetHttpsProxyList>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: TargetHttpsProxyList = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/regions/{region}/targetHttpsProxies
/// Retrieves the list of TargetHttpsProxy resources available to the specified project in the specified region.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_region_target_https_proxies_list_builder()` + `compute_region_target_https_proxies_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_target_https_proxies_list(
    client: &SimpleHttpClient,
    project: &str,
    region: &str,
    filter: Option<&str>,
    maxResults: Option<i32>,
    orderBy: Option<&str>,
    pageToken: Option<&str>,
    returnPartialSuccess: Option<bool>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<TargetHttpsProxyList>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = compute_region_target_https_proxies_list_builder(
        client,
        project,
        region,
        filter,
        maxResults,
        orderBy,
        pageToken,
        returnPartialSuccess,
    )?;
    compute_region_target_https_proxies_list_execute(builder)
}

/// GET projects/{project}/regions/{region}/targetHttpsProxies/{targetHttpsProxy}
/// Patches the specified regional TargetHttpsProxy resource with the data included in the request. This method supports PATCH semantics and `usesJSON` merge patch format and processing rules.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_region_target_https_proxies_patch_execute()` to send, or `compute_region_target_https_proxies_patch` for simplest API.

pub fn compute_region_target_https_proxies_patch_builder(
    client: &SimpleHttpClient,
    project: &str,
    region: &str,
    targetHttpsProxy: &str,
    requestId: Option<&str>,
    body: &TargetHttpsProxy,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/regions/{}/targetHttpsProxies/{}",
        project, region, targetHttpsProxy,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "requestId",
            requestId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET projects/{project}/regions/{region}/targetHttpsProxies/{targetHttpsProxy}
/// Patches the specified regional TargetHttpsProxy resource with the data included in the request. This method supports PATCH semantics and `usesJSON` merge patch format and processing rules.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_region_target_https_proxies_patch_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_region_target_https_proxies_patch_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_target_https_proxies_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/regions/{region}/targetHttpsProxies/{targetHttpsProxy}
/// Patches the specified regional TargetHttpsProxy resource with the data included in the request. This method supports PATCH semantics and `usesJSON` merge patch format and processing rules.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_region_target_https_proxies_patch_builder()` + `compute_region_target_https_proxies_patch_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_target_https_proxies_patch(
    client: &SimpleHttpClient,
    project: &str,
    region: &str,
    targetHttpsProxy: &str,
    requestId: Option<&str>,
    body: &TargetHttpsProxy,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_region_target_https_proxies_patch_builder(
        client,
        project,
        region,
        targetHttpsProxy,
        requestId,
        body,
    )?;
    compute_region_target_https_proxies_patch_execute(builder)
}

/// GET projects/{project}/regions/{region}/targetHttpsProxies/{targetHttpsProxy}/setSslCertificates
/// Replaces SslCertificates for TargetHttpsProxy.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_region_target_https_proxies_set_ssl_certificates_execute()` to send, or `compute_region_target_https_proxies_set_ssl_certificates` for simplest API.

pub fn compute_region_target_https_proxies_set_ssl_certificates_builder(
    client: &SimpleHttpClient,
    project: &str,
    region: &str,
    targetHttpsProxy: &str,
    requestId: Option<&str>,
    body: &RegionTargetHttpsProxiesSetSslCertificatesRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/regions/{}/targetHttpsProxies/{}/setSslCertificates",
        project, region, targetHttpsProxy,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "requestId",
            requestId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET projects/{project}/regions/{region}/targetHttpsProxies/{targetHttpsProxy}/setSslCertificates
/// Replaces SslCertificates for TargetHttpsProxy.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_region_target_https_proxies_set_ssl_certificates_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_region_target_https_proxies_set_ssl_certificates_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_target_https_proxies_set_ssl_certificates_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/regions/{region}/targetHttpsProxies/{targetHttpsProxy}/setSslCertificates
/// Replaces SslCertificates for TargetHttpsProxy.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_region_target_https_proxies_set_ssl_certificates_builder()` + `compute_region_target_https_proxies_set_ssl_certificates_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_target_https_proxies_set_ssl_certificates(
    client: &SimpleHttpClient,
    project: &str,
    region: &str,
    targetHttpsProxy: &str,
    requestId: Option<&str>,
    body: &RegionTargetHttpsProxiesSetSslCertificatesRequest,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_region_target_https_proxies_set_ssl_certificates_builder(
        client,
        project,
        region,
        targetHttpsProxy,
        requestId,
        body,
    )?;
    compute_region_target_https_proxies_set_ssl_certificates_execute(builder)
}

/// GET projects/{project}/regions/{region}/targetHttpsProxies/{targetHttpsProxy}/setUrlMap
/// Changes the URL map for TargetHttpsProxy.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_region_target_https_proxies_set_url_map_execute()` to send, or `compute_region_target_https_proxies_set_url_map` for simplest API.

pub fn compute_region_target_https_proxies_set_url_map_builder(
    client: &SimpleHttpClient,
    project: &str,
    region: &str,
    targetHttpsProxy: &str,
    requestId: Option<&str>,
    body: &UrlMapReference,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/regions/{}/targetHttpsProxies/{}/setUrlMap",
        project, region, targetHttpsProxy,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "requestId",
            requestId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET projects/{project}/regions/{region}/targetHttpsProxies/{targetHttpsProxy}/setUrlMap
/// Changes the URL map for TargetHttpsProxy.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_region_target_https_proxies_set_url_map_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_region_target_https_proxies_set_url_map_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_target_https_proxies_set_url_map_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/regions/{region}/targetHttpsProxies/{targetHttpsProxy}/setUrlMap
/// Changes the URL map for TargetHttpsProxy.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_region_target_https_proxies_set_url_map_builder()` + `compute_region_target_https_proxies_set_url_map_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_target_https_proxies_set_url_map(
    client: &SimpleHttpClient,
    project: &str,
    region: &str,
    targetHttpsProxy: &str,
    requestId: Option<&str>,
    body: &UrlMapReference,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_region_target_https_proxies_set_url_map_builder(
        client,
        project,
        region,
        targetHttpsProxy,
        requestId,
        body,
    )?;
    compute_region_target_https_proxies_set_url_map_execute(builder)
}

/// GET projects/{project}/regions/{region}/targetTcpProxies/{targetTcpProxy}
/// Deletes the specified TargetTcpProxy resource.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_region_target_tcp_proxies_delete_execute()` to send, or `compute_region_target_tcp_proxies_delete` for simplest API.

pub fn compute_region_target_tcp_proxies_delete_builder(
    client: &SimpleHttpClient,
    project: &str,
    region: &str,
    targetTcpProxy: &str,
    requestId: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/regions/{}/targetTcpProxies/{}",
        project, region, targetTcpProxy,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "requestId",
            requestId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET projects/{project}/regions/{region}/targetTcpProxies/{targetTcpProxy}
/// Deletes the specified TargetTcpProxy resource.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_region_target_tcp_proxies_delete_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_region_target_tcp_proxies_delete_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_target_tcp_proxies_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/regions/{region}/targetTcpProxies/{targetTcpProxy}
/// Deletes the specified TargetTcpProxy resource.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_region_target_tcp_proxies_delete_builder()` + `compute_region_target_tcp_proxies_delete_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_target_tcp_proxies_delete(
    client: &SimpleHttpClient,
    project: &str,
    region: &str,
    targetTcpProxy: &str,
    requestId: Option<&str>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_region_target_tcp_proxies_delete_builder(
        client,
        project,
        region,
        targetTcpProxy,
        requestId,
    )?;
    compute_region_target_tcp_proxies_delete_execute(builder)
}

/// GET projects/{project}/regions/{region}/targetTcpProxies/{targetTcpProxy}
/// Returns the specified TargetTcpProxy resource.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_region_target_tcp_proxies_get_execute()` to send, or `compute_region_target_tcp_proxies_get` for simplest API.

pub fn compute_region_target_tcp_proxies_get_builder(
    client: &SimpleHttpClient,
    project: &str,
    region: &str,
    targetTcpProxy: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/regions/{}/targetTcpProxies/{}",
        project, region, targetTcpProxy,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/regions/{region}/targetTcpProxies/{targetTcpProxy}
/// Returns the specified TargetTcpProxy resource.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_region_target_tcp_proxies_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_region_target_tcp_proxies_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_target_tcp_proxies_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<TargetTcpProxy>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: TargetTcpProxy = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/regions/{region}/targetTcpProxies/{targetTcpProxy}
/// Returns the specified TargetTcpProxy resource.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_region_target_tcp_proxies_get_builder()` + `compute_region_target_tcp_proxies_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_target_tcp_proxies_get(
    client: &SimpleHttpClient,
    project: &str,
    region: &str,
    targetTcpProxy: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<TargetTcpProxy>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        compute_region_target_tcp_proxies_get_builder(client, project, region, targetTcpProxy)?;
    compute_region_target_tcp_proxies_get_execute(builder)
}

/// GET projects/{project}/regions/{region}/targetTcpProxies
/// Creates a TargetTcpProxy resource in the specified project and region using the data included in the request.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_region_target_tcp_proxies_insert_execute()` to send, or `compute_region_target_tcp_proxies_insert` for simplest API.

pub fn compute_region_target_tcp_proxies_insert_builder(
    client: &SimpleHttpClient,
    project: &str,
    region: &str,
    requestId: Option<&str>,
    body: &TargetTcpProxy,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/regions/{}/targetTcpProxies",
        project, region,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "requestId",
            requestId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET projects/{project}/regions/{region}/targetTcpProxies
/// Creates a TargetTcpProxy resource in the specified project and region using the data included in the request.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_region_target_tcp_proxies_insert_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_region_target_tcp_proxies_insert_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_target_tcp_proxies_insert_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/regions/{region}/targetTcpProxies
/// Creates a TargetTcpProxy resource in the specified project and region using the data included in the request.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_region_target_tcp_proxies_insert_builder()` + `compute_region_target_tcp_proxies_insert_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_target_tcp_proxies_insert(
    client: &SimpleHttpClient,
    project: &str,
    region: &str,
    requestId: Option<&str>,
    body: &TargetTcpProxy,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        compute_region_target_tcp_proxies_insert_builder(client, project, region, requestId, body)?;
    compute_region_target_tcp_proxies_insert_execute(builder)
}

/// GET projects/{project}/regions/{region}/targetTcpProxies
/// Retrieves a list of TargetTcpProxy resources available to the specified project in a given region.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_region_target_tcp_proxies_list_execute()` to send, or `compute_region_target_tcp_proxies_list` for simplest API.

pub fn compute_region_target_tcp_proxies_list_builder(
    client: &SimpleHttpClient,
    project: &str,
    region: &str,
    filter: Option<&str>,
    maxResults: Option<i32>,
    orderBy: Option<&str>,
    pageToken: Option<&str>,
    returnPartialSuccess: Option<bool>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/regions/{}/targetTcpProxies",
        project, region,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "filter",
            filter
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "maxResults",
            maxResults
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "orderBy",
            orderBy
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageToken",
            pageToken
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "returnPartialSuccess",
            returnPartialSuccess
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET projects/{project}/regions/{region}/targetTcpProxies
/// Retrieves a list of TargetTcpProxy resources available to the specified project in a given region.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_region_target_tcp_proxies_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_region_target_tcp_proxies_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_target_tcp_proxies_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<TargetTcpProxyList>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: TargetTcpProxyList = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/regions/{region}/targetTcpProxies
/// Retrieves a list of TargetTcpProxy resources available to the specified project in a given region.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_region_target_tcp_proxies_list_builder()` + `compute_region_target_tcp_proxies_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_target_tcp_proxies_list(
    client: &SimpleHttpClient,
    project: &str,
    region: &str,
    filter: Option<&str>,
    maxResults: Option<i32>,
    orderBy: Option<&str>,
    pageToken: Option<&str>,
    returnPartialSuccess: Option<bool>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<TargetTcpProxyList>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = compute_region_target_tcp_proxies_list_builder(
        client,
        project,
        region,
        filter,
        maxResults,
        orderBy,
        pageToken,
        returnPartialSuccess,
    )?;
    compute_region_target_tcp_proxies_list_execute(builder)
}

/// GET projects/{project}/regions/{region}/urlMaps/{urlMap}
/// Deletes the specified UrlMap resource.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_region_url_maps_delete_execute()` to send, or `compute_region_url_maps_delete` for simplest API.

pub fn compute_region_url_maps_delete_builder(
    client: &SimpleHttpClient,
    project: &str,
    region: &str,
    urlMap: &str,
    requestId: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/regions/{}/urlMaps/{}",
        project, region, urlMap,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "requestId",
            requestId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET projects/{project}/regions/{region}/urlMaps/{urlMap}
/// Deletes the specified UrlMap resource.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_region_url_maps_delete_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_region_url_maps_delete_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_url_maps_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/regions/{region}/urlMaps/{urlMap}
/// Deletes the specified UrlMap resource.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_region_url_maps_delete_builder()` + `compute_region_url_maps_delete_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_url_maps_delete(
    client: &SimpleHttpClient,
    project: &str,
    region: &str,
    urlMap: &str,
    requestId: Option<&str>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        compute_region_url_maps_delete_builder(client, project, region, urlMap, requestId)?;
    compute_region_url_maps_delete_execute(builder)
}

/// GET projects/{project}/regions/{region}/urlMaps/{urlMap}
/// Returns the specified UrlMap resource.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_region_url_maps_get_execute()` to send, or `compute_region_url_maps_get` for simplest API.

pub fn compute_region_url_maps_get_builder(
    client: &SimpleHttpClient,
    project: &str,
    region: &str,
    urlMap: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/regions/{}/urlMaps/{}",
        project, region, urlMap,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/regions/{region}/urlMaps/{urlMap}
/// Returns the specified UrlMap resource.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_region_url_maps_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_region_url_maps_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_url_maps_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<UrlMap>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: UrlMap = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/regions/{region}/urlMaps/{urlMap}
/// Returns the specified UrlMap resource.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_region_url_maps_get_builder()` + `compute_region_url_maps_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_url_maps_get(
    client: &SimpleHttpClient,
    project: &str,
    region: &str,
    urlMap: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<UrlMap>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_region_url_maps_get_builder(client, project, region, urlMap)?;
    compute_region_url_maps_get_execute(builder)
}

/// GET projects/{project}/regions/{region}/urlMaps
/// Creates a UrlMap resource in the specified project using the data included in the request.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_region_url_maps_insert_execute()` to send, or `compute_region_url_maps_insert` for simplest API.

pub fn compute_region_url_maps_insert_builder(
    client: &SimpleHttpClient,
    project: &str,
    region: &str,
    requestId: Option<&str>,
    body: &UrlMap,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/regions/{}/urlMaps",
        project, region,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "requestId",
            requestId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET projects/{project}/regions/{region}/urlMaps
/// Creates a UrlMap resource in the specified project using the data included in the request.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_region_url_maps_insert_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_region_url_maps_insert_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_url_maps_insert_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/regions/{region}/urlMaps
/// Creates a UrlMap resource in the specified project using the data included in the request.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_region_url_maps_insert_builder()` + `compute_region_url_maps_insert_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_url_maps_insert(
    client: &SimpleHttpClient,
    project: &str,
    region: &str,
    requestId: Option<&str>,
    body: &UrlMap,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_region_url_maps_insert_builder(client, project, region, requestId, body)?;
    compute_region_url_maps_insert_execute(builder)
}

/// GET projects/{project}/regions/{region}/urlMaps
/// Retrieves the list of UrlMap resources available to the specified project in the specified region.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_region_url_maps_list_execute()` to send, or `compute_region_url_maps_list` for simplest API.

pub fn compute_region_url_maps_list_builder(
    client: &SimpleHttpClient,
    project: &str,
    region: &str,
    filter: Option<&str>,
    maxResults: Option<i32>,
    orderBy: Option<&str>,
    pageToken: Option<&str>,
    returnPartialSuccess: Option<bool>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/regions/{}/urlMaps",
        project, region,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "filter",
            filter
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "maxResults",
            maxResults
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "orderBy",
            orderBy
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageToken",
            pageToken
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "returnPartialSuccess",
            returnPartialSuccess
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET projects/{project}/regions/{region}/urlMaps
/// Retrieves the list of UrlMap resources available to the specified project in the specified region.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_region_url_maps_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_region_url_maps_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_url_maps_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<UrlMapList>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: UrlMapList = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/regions/{region}/urlMaps
/// Retrieves the list of UrlMap resources available to the specified project in the specified region.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_region_url_maps_list_builder()` + `compute_region_url_maps_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_url_maps_list(
    client: &SimpleHttpClient,
    project: &str,
    region: &str,
    filter: Option<&str>,
    maxResults: Option<i32>,
    orderBy: Option<&str>,
    pageToken: Option<&str>,
    returnPartialSuccess: Option<bool>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<UrlMapList>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_region_url_maps_list_builder(
        client,
        project,
        region,
        filter,
        maxResults,
        orderBy,
        pageToken,
        returnPartialSuccess,
    )?;
    compute_region_url_maps_list_execute(builder)
}

/// GET projects/{project}/regions/{region}/urlMaps/{urlMap}
/// Patches the specified UrlMap resource with the data included in the request. This method `supportsPATCH` semantics and `usesJSON` merge patch format and processing rules.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_region_url_maps_patch_execute()` to send, or `compute_region_url_maps_patch` for simplest API.

pub fn compute_region_url_maps_patch_builder(
    client: &SimpleHttpClient,
    project: &str,
    region: &str,
    urlMap: &str,
    requestId: Option<&str>,
    body: &UrlMap,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/regions/{}/urlMaps/{}",
        project, region, urlMap,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "requestId",
            requestId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET projects/{project}/regions/{region}/urlMaps/{urlMap}
/// Patches the specified UrlMap resource with the data included in the request. This method `supportsPATCH` semantics and `usesJSON` merge patch format and processing rules.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_region_url_maps_patch_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_region_url_maps_patch_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_url_maps_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/regions/{region}/urlMaps/{urlMap}
/// Patches the specified UrlMap resource with the data included in the request. This method `supportsPATCH` semantics and `usesJSON` merge patch format and processing rules.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_region_url_maps_patch_builder()` + `compute_region_url_maps_patch_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_url_maps_patch(
    client: &SimpleHttpClient,
    project: &str,
    region: &str,
    urlMap: &str,
    requestId: Option<&str>,
    body: &UrlMap,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        compute_region_url_maps_patch_builder(client, project, region, urlMap, requestId, body)?;
    compute_region_url_maps_patch_execute(builder)
}

/// GET projects/{project}/regions/{region}/urlMaps/{urlMap}
/// Updates the specified UrlMap resource with the data included in the request.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_region_url_maps_update_execute()` to send, or `compute_region_url_maps_update` for simplest API.

pub fn compute_region_url_maps_update_builder(
    client: &SimpleHttpClient,
    project: &str,
    region: &str,
    urlMap: &str,
    requestId: Option<&str>,
    body: &UrlMap,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/regions/{}/urlMaps/{}",
        project, region, urlMap,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "requestId",
            requestId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET projects/{project}/regions/{region}/urlMaps/{urlMap}
/// Updates the specified UrlMap resource with the data included in the request.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_region_url_maps_update_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_region_url_maps_update_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_url_maps_update_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/regions/{region}/urlMaps/{urlMap}
/// Updates the specified UrlMap resource with the data included in the request.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_region_url_maps_update_builder()` + `compute_region_url_maps_update_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_url_maps_update(
    client: &SimpleHttpClient,
    project: &str,
    region: &str,
    urlMap: &str,
    requestId: Option<&str>,
    body: &UrlMap,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        compute_region_url_maps_update_builder(client, project, region, urlMap, requestId, body)?;
    compute_region_url_maps_update_execute(builder)
}

/// GET projects/{project}/regions/{region}/urlMaps/{urlMap}/validate
/// Runs static validation for the UrlMap. In particular, the tests of the provided UrlMap will be run. Calling this method does NOT create the UrlMap.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_region_url_maps_validate_execute()` to send, or `compute_region_url_maps_validate` for simplest API.

pub fn compute_region_url_maps_validate_builder(
    client: &SimpleHttpClient,
    project: &str,
    region: &str,
    urlMap: &str,
    body: &RegionUrlMapsValidateRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/regions/{}/urlMaps/{}/validate",
        project, region, urlMap,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET projects/{project}/regions/{region}/urlMaps/{urlMap}/validate
/// Runs static validation for the UrlMap. In particular, the tests of the provided UrlMap will be run. Calling this method does NOT create the UrlMap.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_region_url_maps_validate_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_region_url_maps_validate_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_url_maps_validate_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<UrlMapsValidateResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: UrlMapsValidateResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/regions/{region}/urlMaps/{urlMap}/validate
/// Runs static validation for the UrlMap. In particular, the tests of the provided UrlMap will be run. Calling this method does NOT create the UrlMap.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_region_url_maps_validate_builder()` + `compute_region_url_maps_validate_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_url_maps_validate(
    client: &SimpleHttpClient,
    project: &str,
    region: &str,
    urlMap: &str,
    body: &RegionUrlMapsValidateRequest,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<UrlMapsValidateResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = compute_region_url_maps_validate_builder(client, project, region, urlMap, body)?;
    compute_region_url_maps_validate_execute(builder)
}

/// GET projects/{project}/regions/{region}/zones
/// Retrieves the list of Zone resources under the specific region available to the specified project.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_region_zones_list_execute()` to send, or `compute_region_zones_list` for simplest API.

pub fn compute_region_zones_list_builder(
    client: &SimpleHttpClient,
    project: &str,
    region: &str,
    filter: Option<&str>,
    maxResults: Option<i32>,
    orderBy: Option<&str>,
    pageToken: Option<&str>,
    returnPartialSuccess: Option<bool>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/regions/{}/zones",
        project, region,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "filter",
            filter
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "maxResults",
            maxResults
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "orderBy",
            orderBy
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageToken",
            pageToken
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "returnPartialSuccess",
            returnPartialSuccess
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET projects/{project}/regions/{region}/zones
/// Retrieves the list of Zone resources under the specific region available to the specified project.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_region_zones_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_region_zones_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_zones_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ZoneList>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ZoneList = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/regions/{region}/zones
/// Retrieves the list of Zone resources under the specific region available to the specified project.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_region_zones_list_builder()` + `compute_region_zones_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_region_zones_list(
    client: &SimpleHttpClient,
    project: &str,
    region: &str,
    filter: Option<&str>,
    maxResults: Option<i32>,
    orderBy: Option<&str>,
    pageToken: Option<&str>,
    returnPartialSuccess: Option<bool>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ZoneList>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_region_zones_list_builder(
        client,
        project,
        region,
        filter,
        maxResults,
        orderBy,
        pageToken,
        returnPartialSuccess,
    )?;
    compute_region_zones_list_execute(builder)
}

/// GET projects/{project}/regions/{region}
/// Returns the specified Region resource.  To decrease latency for this method, you can optionally omit any unneeded information from the response by using a field mask. This practice is especially recommended for unused quota information (the quotas field). To exclude one or more fields, set your request's fields query parameter to only include the fields you need. For example, to only include the id and `selfLink` fields, add the query parameter ?fields=id,`selfLink` to your request.  This method fails if the quota information is unavailable for the region and if the organization policy constraint compute.`requireBasicQuotaInResponse` is enforced. This constraint, when enforced, disables the fail-open behaviour when quota information (the items.quotas field) is unavailable for the region. It is recommended to use the default setting for the constraint unless your application requires the fail-closed behaviour for this method.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_regions_get_execute()` to send, or `compute_regions_get` for simplest API.

pub fn compute_regions_get_builder(
    client: &SimpleHttpClient,
    project: &str,
    region: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/regions/{}",
        project, region,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/regions/{region}
/// Returns the specified Region resource.  To decrease latency for this method, you can optionally omit any unneeded information from the response by using a field mask. This practice is especially recommended for unused quota information (the quotas field). To exclude one or more fields, set your request's fields query parameter to only include the fields you need. For example, to only include the id and `selfLink` fields, add the query parameter ?fields=id,`selfLink` to your request.  This method fails if the quota information is unavailable for the region and if the organization policy constraint compute.`requireBasicQuotaInResponse` is enforced. This constraint, when enforced, disables the fail-open behaviour when quota information (the items.quotas field) is unavailable for the region. It is recommended to use the default setting for the constraint unless your application requires the fail-closed behaviour for this method.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_regions_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_regions_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_regions_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Region>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Region = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/regions/{region}
/// Returns the specified Region resource.  To decrease latency for this method, you can optionally omit any unneeded information from the response by using a field mask. This practice is especially recommended for unused quota information (the quotas field). To exclude one or more fields, set your request's fields query parameter to only include the fields you need. For example, to only include the id and `selfLink` fields, add the query parameter ?fields=id,`selfLink` to your request.  This method fails if the quota information is unavailable for the region and if the organization policy constraint compute.`requireBasicQuotaInResponse` is enforced. This constraint, when enforced, disables the fail-open behaviour when quota information (the items.quotas field) is unavailable for the region. It is recommended to use the default setting for the constraint unless your application requires the fail-closed behaviour for this method.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_regions_get_builder()` + `compute_regions_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_regions_get(
    client: &SimpleHttpClient,
    project: &str,
    region: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Region>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_regions_get_builder(client, project, region)?;
    compute_regions_get_execute(builder)
}

/// GET projects/{project}/regions
/// Retrieves the list of region resources available to the specified project.  To decrease latency for this method, you can optionally omit any unneeded information from the response by using a field mask. This practice is especially recommended for unused quota information (the items.quotas field). To exclude one or more fields, set your request's fields query parameter to only include the fields you need. For example, to only include the id and `selfLink` fields, add the query parameter ?fields=id,`selfLink` to your request.  This method fails if the quota information is unavailable for the region and if the organization policy constraint compute.`requireBasicQuotaInResponse` is enforced. This constraint, when enforced, disables the fail-open behaviour when quota information (the items.quotas field) is unavailable for the region. It is recommended to use the default setting for the constraint unless your application requires the fail-closed behaviour for this method.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_regions_list_execute()` to send, or `compute_regions_list` for simplest API.

pub fn compute_regions_list_builder(
    client: &SimpleHttpClient,
    project: &str,
    filter: Option<&str>,
    maxResults: Option<i32>,
    orderBy: Option<&str>,
    pageToken: Option<&str>,
    returnPartialSuccess: Option<bool>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comprojects/{}/regions", project,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "filter",
            filter
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "maxResults",
            maxResults
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "orderBy",
            orderBy
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageToken",
            pageToken
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "returnPartialSuccess",
            returnPartialSuccess
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET projects/{project}/regions
/// Retrieves the list of region resources available to the specified project.  To decrease latency for this method, you can optionally omit any unneeded information from the response by using a field mask. This practice is especially recommended for unused quota information (the items.quotas field). To exclude one or more fields, set your request's fields query parameter to only include the fields you need. For example, to only include the id and `selfLink` fields, add the query parameter ?fields=id,`selfLink` to your request.  This method fails if the quota information is unavailable for the region and if the organization policy constraint compute.`requireBasicQuotaInResponse` is enforced. This constraint, when enforced, disables the fail-open behaviour when quota information (the items.quotas field) is unavailable for the region. It is recommended to use the default setting for the constraint unless your application requires the fail-closed behaviour for this method.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_regions_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_regions_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_regions_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<RegionList>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: RegionList = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/regions
/// Retrieves the list of region resources available to the specified project.  To decrease latency for this method, you can optionally omit any unneeded information from the response by using a field mask. This practice is especially recommended for unused quota information (the items.quotas field). To exclude one or more fields, set your request's fields query parameter to only include the fields you need. For example, to only include the id and `selfLink` fields, add the query parameter ?fields=id,`selfLink` to your request.  This method fails if the quota information is unavailable for the region and if the organization policy constraint compute.`requireBasicQuotaInResponse` is enforced. This constraint, when enforced, disables the fail-open behaviour when quota information (the items.quotas field) is unavailable for the region. It is recommended to use the default setting for the constraint unless your application requires the fail-closed behaviour for this method.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_regions_list_builder()` + `compute_regions_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_regions_list(
    client: &SimpleHttpClient,
    project: &str,
    filter: Option<&str>,
    maxResults: Option<i32>,
    orderBy: Option<&str>,
    pageToken: Option<&str>,
    returnPartialSuccess: Option<bool>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<RegionList>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_regions_list_builder(
        client,
        project,
        filter,
        maxResults,
        orderBy,
        pageToken,
        returnPartialSuccess,
    )?;
    compute_regions_list_execute(builder)
}

/// GET projects/{project}/zones/{zone}/reservations/{reservation}/reservationBlocks/{reservationBlock}
/// Retrieves information about the specified reservation block.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_reservation_blocks_get_execute()` to send, or `compute_reservation_blocks_get` for simplest API.

pub fn compute_reservation_blocks_get_builder(
    client: &SimpleHttpClient,
    project: &str,
    reservation: &str,
    reservationBlock: &str,
    zone: &str,
    view: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/zones/{}/reservations/{}/reservationBlocks/{}",
        project, reservation, reservationBlock, zone,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "view",
            view.as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET projects/{project}/zones/{zone}/reservations/{reservation}/reservationBlocks/{reservationBlock}
/// Retrieves information about the specified reservation block.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_reservation_blocks_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_reservation_blocks_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_reservation_blocks_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ReservationBlocksGetResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ReservationBlocksGetResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/zones/{zone}/reservations/{reservation}/reservationBlocks/{reservationBlock}
/// Retrieves information about the specified reservation block.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_reservation_blocks_get_builder()` + `compute_reservation_blocks_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_reservation_blocks_get(
    client: &SimpleHttpClient,
    project: &str,
    reservation: &str,
    reservationBlock: &str,
    zone: &str,
    view: Option<&str>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ReservationBlocksGetResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = compute_reservation_blocks_get_builder(
        client,
        project,
        reservation,
        reservationBlock,
        zone,
        view,
    )?;
    compute_reservation_blocks_get_execute(builder)
}

/// GET projects/{project}/zones/{zone}/reservations/{parentResource}/reservationBlocks/{resource}/getIamPolicy
/// Gets the access control policy for a resource. May be empty if no such policy or resource exists.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_reservation_blocks_get_iam_policy_execute()` to send, or `compute_reservation_blocks_get_iam_policy` for simplest API.

pub fn compute_reservation_blocks_get_iam_policy_builder(
    client: &SimpleHttpClient,
    parentResource: &str,
    project: &str,
    resource: &str,
    zone: &str,
    optionsRequestedPolicyVersion: Option<i32>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/zones/{}/reservations/{}/reservationBlocks/{}/getIamPolicy",
        parentResource,
        project,
        resource,
        zone,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "optionsRequestedPolicyVersion",
            optionsRequestedPolicyVersion
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET projects/{project}/zones/{zone}/reservations/{parentResource}/reservationBlocks/{resource}/getIamPolicy
/// Gets the access control policy for a resource. May be empty if no such policy or resource exists.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_reservation_blocks_get_iam_policy_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_reservation_blocks_get_iam_policy_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_reservation_blocks_get_iam_policy_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Policy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Policy = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/zones/{zone}/reservations/{parentResource}/reservationBlocks/{resource}/getIamPolicy
/// Gets the access control policy for a resource. May be empty if no such policy or resource exists.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_reservation_blocks_get_iam_policy_builder()` + `compute_reservation_blocks_get_iam_policy_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_reservation_blocks_get_iam_policy(
    client: &SimpleHttpClient,
    parentResource: &str,
    project: &str,
    resource: &str,
    zone: &str,
    optionsRequestedPolicyVersion: Option<i32>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Policy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_reservation_blocks_get_iam_policy_builder(
        client,
        parentResource,
        project,
        resource,
        zone,
        optionsRequestedPolicyVersion,
    )?;
    compute_reservation_blocks_get_iam_policy_execute(builder)
}

/// GET projects/{project}/zones/{zone}/reservations/{reservation}/reservationBlocks
/// Retrieves a list of reservation blocks under a single reservation.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_reservation_blocks_list_execute()` to send, or `compute_reservation_blocks_list` for simplest API.

pub fn compute_reservation_blocks_list_builder(
    client: &SimpleHttpClient,
    project: &str,
    reservation: &str,
    zone: &str,
    filter: Option<&str>,
    maxResults: Option<i32>,
    orderBy: Option<&str>,
    pageToken: Option<&str>,
    returnPartialSuccess: Option<bool>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/zones/{}/reservations/{}/reservationBlocks",
        project, reservation, zone,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "filter",
            filter
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "maxResults",
            maxResults
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "orderBy",
            orderBy
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageToken",
            pageToken
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "returnPartialSuccess",
            returnPartialSuccess
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET projects/{project}/zones/{zone}/reservations/{reservation}/reservationBlocks
/// Retrieves a list of reservation blocks under a single reservation.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_reservation_blocks_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_reservation_blocks_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_reservation_blocks_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ReservationBlocksListResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ReservationBlocksListResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/zones/{zone}/reservations/{reservation}/reservationBlocks
/// Retrieves a list of reservation blocks under a single reservation.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_reservation_blocks_list_builder()` + `compute_reservation_blocks_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_reservation_blocks_list(
    client: &SimpleHttpClient,
    project: &str,
    reservation: &str,
    zone: &str,
    filter: Option<&str>,
    maxResults: Option<i32>,
    orderBy: Option<&str>,
    pageToken: Option<&str>,
    returnPartialSuccess: Option<bool>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ReservationBlocksListResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = compute_reservation_blocks_list_builder(
        client,
        project,
        reservation,
        zone,
        filter,
        maxResults,
        orderBy,
        pageToken,
        returnPartialSuccess,
    )?;
    compute_reservation_blocks_list_execute(builder)
}

/// GET projects/{project}/zones/{zone}/reservations/{reservation}/reservationBlocks/{reservationBlock}/performMaintenance
/// Allows customers to perform maintenance on a reservation block
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_reservation_blocks_perform_maintenance_execute()` to send, or `compute_reservation_blocks_perform_maintenance` for simplest API.

pub fn compute_reservation_blocks_perform_maintenance_builder(
    client: &SimpleHttpClient,
    project: &str,
    reservation: &str,
    reservationBlock: &str,
    zone: &str,
    requestId: Option<&str>,
    body: &ReservationsBlocksPerformMaintenanceRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/zones/{}/reservations/{}/reservationBlocks/{}/performMaintenance",
        project,
        reservation,
        reservationBlock,
        zone,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "requestId",
            requestId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET projects/{project}/zones/{zone}/reservations/{reservation}/reservationBlocks/{reservationBlock}/performMaintenance
/// Allows customers to perform maintenance on a reservation block
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_reservation_blocks_perform_maintenance_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_reservation_blocks_perform_maintenance_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_reservation_blocks_perform_maintenance_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/zones/{zone}/reservations/{reservation}/reservationBlocks/{reservationBlock}/performMaintenance
/// Allows customers to perform maintenance on a reservation block
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_reservation_blocks_perform_maintenance_builder()` + `compute_reservation_blocks_perform_maintenance_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_reservation_blocks_perform_maintenance(
    client: &SimpleHttpClient,
    project: &str,
    reservation: &str,
    reservationBlock: &str,
    zone: &str,
    requestId: Option<&str>,
    body: &ReservationsBlocksPerformMaintenanceRequest,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_reservation_blocks_perform_maintenance_builder(
        client,
        project,
        reservation,
        reservationBlock,
        zone,
        requestId,
        body,
    )?;
    compute_reservation_blocks_perform_maintenance_execute(builder)
}

/// GET projects/{project}/zones/{zone}/reservations/{parentResource}/reservationBlocks/{resource}/setIamPolicy
/// Sets the access control policy on the specified resource. Replaces any existing policy.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_reservation_blocks_set_iam_policy_execute()` to send, or `compute_reservation_blocks_set_iam_policy` for simplest API.

pub fn compute_reservation_blocks_set_iam_policy_builder(
    client: &SimpleHttpClient,
    parentResource: &str,
    project: &str,
    resource: &str,
    zone: &str,
    body: &ZoneSetNestedPolicyRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/zones/{}/reservations/{}/reservationBlocks/{}/setIamPolicy",
        parentResource,
        project,
        resource,
        zone,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET projects/{project}/zones/{zone}/reservations/{parentResource}/reservationBlocks/{resource}/setIamPolicy
/// Sets the access control policy on the specified resource. Replaces any existing policy.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_reservation_blocks_set_iam_policy_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_reservation_blocks_set_iam_policy_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_reservation_blocks_set_iam_policy_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Policy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Policy = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/zones/{zone}/reservations/{parentResource}/reservationBlocks/{resource}/setIamPolicy
/// Sets the access control policy on the specified resource. Replaces any existing policy.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_reservation_blocks_set_iam_policy_builder()` + `compute_reservation_blocks_set_iam_policy_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_reservation_blocks_set_iam_policy(
    client: &SimpleHttpClient,
    parentResource: &str,
    project: &str,
    resource: &str,
    zone: &str,
    body: &ZoneSetNestedPolicyRequest,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Policy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_reservation_blocks_set_iam_policy_builder(
        client,
        parentResource,
        project,
        resource,
        zone,
        body,
    )?;
    compute_reservation_blocks_set_iam_policy_execute(builder)
}

/// GET projects/{project}/zones/{zone}/reservations/{parentResource}/reservationBlocks/{resource}/testIamPermissions
/// Returns permissions that a caller has on the specified resource.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_reservation_blocks_test_iam_permissions_execute()` to send, or `compute_reservation_blocks_test_iam_permissions` for simplest API.

pub fn compute_reservation_blocks_test_iam_permissions_builder(
    client: &SimpleHttpClient,
    parentResource: &str,
    project: &str,
    resource: &str,
    zone: &str,
    body: &TestPermissionsRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/zones/{}/reservations/{}/reservationBlocks/{}/testIamPermissions",
        parentResource,
        project,
        resource,
        zone,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET projects/{project}/zones/{zone}/reservations/{parentResource}/reservationBlocks/{resource}/testIamPermissions
/// Returns permissions that a caller has on the specified resource.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_reservation_blocks_test_iam_permissions_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_reservation_blocks_test_iam_permissions_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_reservation_blocks_test_iam_permissions_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<TestPermissionsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: TestPermissionsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/zones/{zone}/reservations/{parentResource}/reservationBlocks/{resource}/testIamPermissions
/// Returns permissions that a caller has on the specified resource.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_reservation_blocks_test_iam_permissions_builder()` + `compute_reservation_blocks_test_iam_permissions_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_reservation_blocks_test_iam_permissions(
    client: &SimpleHttpClient,
    parentResource: &str,
    project: &str,
    resource: &str,
    zone: &str,
    body: &TestPermissionsRequest,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<TestPermissionsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = compute_reservation_blocks_test_iam_permissions_builder(
        client,
        parentResource,
        project,
        resource,
        zone,
        body,
    )?;
    compute_reservation_blocks_test_iam_permissions_execute(builder)
}

/// GET projects/{project}/zones/{zone}/{+parentName}/reservationSlots/{reservationSlot}
/// Retrieves information about the specified reservation slot.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_reservation_slots_get_execute()` to send, or `compute_reservation_slots_get` for simplest API.

pub fn compute_reservation_slots_get_builder(
    client: &SimpleHttpClient,
    parentName: &str,
    project: &str,
    reservationSlot: &str,
    zone: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/zones/{}/{+parentName}/reservationSlots/{}",
        parentName, project, reservationSlot, zone,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/zones/{zone}/{+parentName}/reservationSlots/{reservationSlot}
/// Retrieves information about the specified reservation slot.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_reservation_slots_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_reservation_slots_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_reservation_slots_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ReservationSlotsGetResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ReservationSlotsGetResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/zones/{zone}/{+parentName}/reservationSlots/{reservationSlot}
/// Retrieves information about the specified reservation slot.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_reservation_slots_get_builder()` + `compute_reservation_slots_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_reservation_slots_get(
    client: &SimpleHttpClient,
    parentName: &str,
    project: &str,
    reservationSlot: &str,
    zone: &str,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ReservationSlotsGetResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        compute_reservation_slots_get_builder(client, parentName, project, reservationSlot, zone)?;
    compute_reservation_slots_get_execute(builder)
}

/// GET projects/{project}/zones/{zone}/{+parentName}/reservationSlots/{reservationSlot}/getVersion
/// Allows customers to get SBOM versions of a reservation slot.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_reservation_slots_get_version_execute()` to send, or `compute_reservation_slots_get_version` for simplest API.

pub fn compute_reservation_slots_get_version_builder(
    client: &SimpleHttpClient,
    parentName: &str,
    project: &str,
    reservationSlot: &str,
    zone: &str,
    requestId: Option<&str>,
    body: &ReservationSlotsGetVersionRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/zones/{}/{+parentName}/reservationSlots/{}/getVersion",
        parentName, project, reservationSlot, zone,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "requestId",
            requestId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET projects/{project}/zones/{zone}/{+parentName}/reservationSlots/{reservationSlot}/getVersion
/// Allows customers to get SBOM versions of a reservation slot.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_reservation_slots_get_version_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_reservation_slots_get_version_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_reservation_slots_get_version_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/zones/{zone}/{+parentName}/reservationSlots/{reservationSlot}/getVersion
/// Allows customers to get SBOM versions of a reservation slot.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_reservation_slots_get_version_builder()` + `compute_reservation_slots_get_version_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_reservation_slots_get_version(
    client: &SimpleHttpClient,
    parentName: &str,
    project: &str,
    reservationSlot: &str,
    zone: &str,
    requestId: Option<&str>,
    body: &ReservationSlotsGetVersionRequest,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_reservation_slots_get_version_builder(
        client,
        parentName,
        project,
        reservationSlot,
        zone,
        requestId,
        body,
    )?;
    compute_reservation_slots_get_version_execute(builder)
}

/// GET projects/{project}/zones/{zone}/{+parentName}/reservationSlots
/// Retrieves a list of reservation slots under a single reservation.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_reservation_slots_list_execute()` to send, or `compute_reservation_slots_list` for simplest API.

pub fn compute_reservation_slots_list_builder(
    client: &SimpleHttpClient,
    parentName: &str,
    project: &str,
    zone: &str,
    filter: Option<&str>,
    maxResults: Option<i32>,
    orderBy: Option<&str>,
    pageToken: Option<&str>,
    returnPartialSuccess: Option<bool>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/zones/{}/{+parentName}/reservationSlots",
        parentName, project, zone,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "filter",
            filter
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "maxResults",
            maxResults
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "orderBy",
            orderBy
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageToken",
            pageToken
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "returnPartialSuccess",
            returnPartialSuccess
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET projects/{project}/zones/{zone}/{+parentName}/reservationSlots
/// Retrieves a list of reservation slots under a single reservation.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_reservation_slots_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_reservation_slots_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_reservation_slots_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ReservationSlotsListResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ReservationSlotsListResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/zones/{zone}/{+parentName}/reservationSlots
/// Retrieves a list of reservation slots under a single reservation.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_reservation_slots_list_builder()` + `compute_reservation_slots_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_reservation_slots_list(
    client: &SimpleHttpClient,
    parentName: &str,
    project: &str,
    zone: &str,
    filter: Option<&str>,
    maxResults: Option<i32>,
    orderBy: Option<&str>,
    pageToken: Option<&str>,
    returnPartialSuccess: Option<bool>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ReservationSlotsListResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = compute_reservation_slots_list_builder(
        client,
        parentName,
        project,
        zone,
        filter,
        maxResults,
        orderBy,
        pageToken,
        returnPartialSuccess,
    )?;
    compute_reservation_slots_list_execute(builder)
}

/// GET projects/{project}/zones/{zone}/{+parentName}/reservationSlots/{reservationSlot}
/// Update a reservation slot in the specified sub-block.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_reservation_slots_update_execute()` to send, or `compute_reservation_slots_update` for simplest API.

pub fn compute_reservation_slots_update_builder(
    client: &SimpleHttpClient,
    parentName: &str,
    project: &str,
    reservationSlot: &str,
    zone: &str,
    updateMask: Option<&str>,
    body: &ReservationSlot,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/zones/{}/{+parentName}/reservationSlots/{}",
        parentName, project, reservationSlot, zone,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "updateMask",
            updateMask
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET projects/{project}/zones/{zone}/{+parentName}/reservationSlots/{reservationSlot}
/// Update a reservation slot in the specified sub-block.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_reservation_slots_update_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_reservation_slots_update_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_reservation_slots_update_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/zones/{zone}/{+parentName}/reservationSlots/{reservationSlot}
/// Update a reservation slot in the specified sub-block.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_reservation_slots_update_builder()` + `compute_reservation_slots_update_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_reservation_slots_update(
    client: &SimpleHttpClient,
    parentName: &str,
    project: &str,
    reservationSlot: &str,
    zone: &str,
    updateMask: Option<&str>,
    body: &ReservationSlot,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_reservation_slots_update_builder(
        client,
        parentName,
        project,
        reservationSlot,
        zone,
        updateMask,
        body,
    )?;
    compute_reservation_slots_update_execute(builder)
}

/// GET projects/{project}/zones/{zone}/{+parentName}/reservationSubBlocks/{reservationSubBlock}
/// Retrieves information about the specified reservation `subBlock`.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_reservation_sub_blocks_get_execute()` to send, or `compute_reservation_sub_blocks_get` for simplest API.

pub fn compute_reservation_sub_blocks_get_builder(
    client: &SimpleHttpClient,
    parentName: &str,
    project: &str,
    reservationSubBlock: &str,
    zone: &str,
    view: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/zones/{}/{+parentName}/reservationSubBlocks/{}",
        parentName, project, reservationSubBlock, zone,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "view",
            view.as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET projects/{project}/zones/{zone}/{+parentName}/reservationSubBlocks/{reservationSubBlock}
/// Retrieves information about the specified reservation `subBlock`.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_reservation_sub_blocks_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_reservation_sub_blocks_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_reservation_sub_blocks_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ReservationSubBlocksGetResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ReservationSubBlocksGetResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/zones/{zone}/{+parentName}/reservationSubBlocks/{reservationSubBlock}
/// Retrieves information about the specified reservation `subBlock`.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_reservation_sub_blocks_get_builder()` + `compute_reservation_sub_blocks_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_reservation_sub_blocks_get(
    client: &SimpleHttpClient,
    parentName: &str,
    project: &str,
    reservationSubBlock: &str,
    zone: &str,
    view: Option<&str>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ReservationSubBlocksGetResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = compute_reservation_sub_blocks_get_builder(
        client,
        parentName,
        project,
        reservationSubBlock,
        zone,
        view,
    )?;
    compute_reservation_sub_blocks_get_execute(builder)
}

/// GET projects/{project}/zones/{zone}/{+parentResource}/reservationSubBlocks/{resource}/getIamPolicy
/// Gets the access control policy for a resource. May be empty if no such policy or resource exists.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_reservation_sub_blocks_get_iam_policy_execute()` to send, or `compute_reservation_sub_blocks_get_iam_policy` for simplest API.

pub fn compute_reservation_sub_blocks_get_iam_policy_builder(
    client: &SimpleHttpClient,
    parentResource: &str,
    project: &str,
    resource: &str,
    zone: &str,
    optionsRequestedPolicyVersion: Option<i32>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/zones/{}/{+parentResource}/reservationSubBlocks/{}/getIamPolicy",
        parentResource,
        project,
        resource,
        zone,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "optionsRequestedPolicyVersion",
            optionsRequestedPolicyVersion
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET projects/{project}/zones/{zone}/{+parentResource}/reservationSubBlocks/{resource}/getIamPolicy
/// Gets the access control policy for a resource. May be empty if no such policy or resource exists.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_reservation_sub_blocks_get_iam_policy_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_reservation_sub_blocks_get_iam_policy_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_reservation_sub_blocks_get_iam_policy_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Policy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Policy = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/zones/{zone}/{+parentResource}/reservationSubBlocks/{resource}/getIamPolicy
/// Gets the access control policy for a resource. May be empty if no such policy or resource exists.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_reservation_sub_blocks_get_iam_policy_builder()` + `compute_reservation_sub_blocks_get_iam_policy_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_reservation_sub_blocks_get_iam_policy(
    client: &SimpleHttpClient,
    parentResource: &str,
    project: &str,
    resource: &str,
    zone: &str,
    optionsRequestedPolicyVersion: Option<i32>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Policy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_reservation_sub_blocks_get_iam_policy_builder(
        client,
        parentResource,
        project,
        resource,
        zone,
        optionsRequestedPolicyVersion,
    )?;
    compute_reservation_sub_blocks_get_iam_policy_execute(builder)
}

/// GET projects/{project}/zones/{zone}/{+parentName}/reservationSubBlocks/{reservationSubBlock}/getVersion
/// Allows customers to get SBOM versions of a reservation `subBlock`.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_reservation_sub_blocks_get_version_execute()` to send, or `compute_reservation_sub_blocks_get_version` for simplest API.

pub fn compute_reservation_sub_blocks_get_version_builder(
    client: &SimpleHttpClient,
    parentName: &str,
    project: &str,
    reservationSubBlock: &str,
    zone: &str,
    requestId: Option<&str>,
    body: &ReservationSubBlocksGetVersionRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/zones/{}/{+parentName}/reservationSubBlocks/{}/getVersion",
        parentName,
        project,
        reservationSubBlock,
        zone,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "requestId",
            requestId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET projects/{project}/zones/{zone}/{+parentName}/reservationSubBlocks/{reservationSubBlock}/getVersion
/// Allows customers to get SBOM versions of a reservation `subBlock`.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_reservation_sub_blocks_get_version_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_reservation_sub_blocks_get_version_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_reservation_sub_blocks_get_version_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/zones/{zone}/{+parentName}/reservationSubBlocks/{reservationSubBlock}/getVersion
/// Allows customers to get SBOM versions of a reservation `subBlock`.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_reservation_sub_blocks_get_version_builder()` + `compute_reservation_sub_blocks_get_version_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_reservation_sub_blocks_get_version(
    client: &SimpleHttpClient,
    parentName: &str,
    project: &str,
    reservationSubBlock: &str,
    zone: &str,
    requestId: Option<&str>,
    body: &ReservationSubBlocksGetVersionRequest,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_reservation_sub_blocks_get_version_builder(
        client,
        parentName,
        project,
        reservationSubBlock,
        zone,
        requestId,
        body,
    )?;
    compute_reservation_sub_blocks_get_version_execute(builder)
}

/// GET projects/{project}/zones/{zone}/{+parentName}/reservationSubBlocks
/// Retrieves a list of reservation `subBlocks` under a single reservation.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_reservation_sub_blocks_list_execute()` to send, or `compute_reservation_sub_blocks_list` for simplest API.

pub fn compute_reservation_sub_blocks_list_builder(
    client: &SimpleHttpClient,
    parentName: &str,
    project: &str,
    zone: &str,
    filter: Option<&str>,
    maxResults: Option<i32>,
    orderBy: Option<&str>,
    pageToken: Option<&str>,
    returnPartialSuccess: Option<bool>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/zones/{}/{+parentName}/reservationSubBlocks",
        parentName, project, zone,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "filter",
            filter
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "maxResults",
            maxResults
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "orderBy",
            orderBy
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageToken",
            pageToken
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "returnPartialSuccess",
            returnPartialSuccess
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET projects/{project}/zones/{zone}/{+parentName}/reservationSubBlocks
/// Retrieves a list of reservation `subBlocks` under a single reservation.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_reservation_sub_blocks_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_reservation_sub_blocks_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_reservation_sub_blocks_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ReservationSubBlocksListResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ReservationSubBlocksListResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/zones/{zone}/{+parentName}/reservationSubBlocks
/// Retrieves a list of reservation `subBlocks` under a single reservation.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_reservation_sub_blocks_list_builder()` + `compute_reservation_sub_blocks_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_reservation_sub_blocks_list(
    client: &SimpleHttpClient,
    parentName: &str,
    project: &str,
    zone: &str,
    filter: Option<&str>,
    maxResults: Option<i32>,
    orderBy: Option<&str>,
    pageToken: Option<&str>,
    returnPartialSuccess: Option<bool>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ReservationSubBlocksListResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = compute_reservation_sub_blocks_list_builder(
        client,
        parentName,
        project,
        zone,
        filter,
        maxResults,
        orderBy,
        pageToken,
        returnPartialSuccess,
    )?;
    compute_reservation_sub_blocks_list_execute(builder)
}

/// GET projects/{project}/zones/{zone}/{+parentName}/reservationSubBlocks/{reservationSubBlock}/performMaintenance
/// Allows customers to perform maintenance on a reservation `subBlock`
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_reservation_sub_blocks_perform_maintenance_execute()` to send, or `compute_reservation_sub_blocks_perform_maintenance` for simplest API.

pub fn compute_reservation_sub_blocks_perform_maintenance_builder(
    client: &SimpleHttpClient,
    parentName: &str,
    project: &str,
    reservationSubBlock: &str,
    zone: &str,
    requestId: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/zones/{}/{+parentName}/reservationSubBlocks/{}/performMaintenance",
        parentName,
        project,
        reservationSubBlock,
        zone,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "requestId",
            requestId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET projects/{project}/zones/{zone}/{+parentName}/reservationSubBlocks/{reservationSubBlock}/performMaintenance
/// Allows customers to perform maintenance on a reservation `subBlock`
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_reservation_sub_blocks_perform_maintenance_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_reservation_sub_blocks_perform_maintenance_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_reservation_sub_blocks_perform_maintenance_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/zones/{zone}/{+parentName}/reservationSubBlocks/{reservationSubBlock}/performMaintenance
/// Allows customers to perform maintenance on a reservation `subBlock`
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_reservation_sub_blocks_perform_maintenance_builder()` + `compute_reservation_sub_blocks_perform_maintenance_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_reservation_sub_blocks_perform_maintenance(
    client: &SimpleHttpClient,
    parentName: &str,
    project: &str,
    reservationSubBlock: &str,
    zone: &str,
    requestId: Option<&str>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_reservation_sub_blocks_perform_maintenance_builder(
        client,
        parentName,
        project,
        reservationSubBlock,
        zone,
        requestId,
    )?;
    compute_reservation_sub_blocks_perform_maintenance_execute(builder)
}

/// GET projects/{project}/zones/{zone}/{+parentName}/reservationSubBlocks/{reservationSubBlock}/reportFaulty
/// Allows customers to report a faulty `subBlock`.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_reservation_sub_blocks_report_faulty_execute()` to send, or `compute_reservation_sub_blocks_report_faulty` for simplest API.

pub fn compute_reservation_sub_blocks_report_faulty_builder(
    client: &SimpleHttpClient,
    parentName: &str,
    project: &str,
    reservationSubBlock: &str,
    zone: &str,
    requestId: Option<&str>,
    body: &ReservationSubBlocksReportFaultyRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/zones/{}/{+parentName}/reservationSubBlocks/{}/reportFaulty",
        parentName,
        project,
        reservationSubBlock,
        zone,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "requestId",
            requestId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET projects/{project}/zones/{zone}/{+parentName}/reservationSubBlocks/{reservationSubBlock}/reportFaulty
/// Allows customers to report a faulty `subBlock`.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_reservation_sub_blocks_report_faulty_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_reservation_sub_blocks_report_faulty_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_reservation_sub_blocks_report_faulty_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/zones/{zone}/{+parentName}/reservationSubBlocks/{reservationSubBlock}/reportFaulty
/// Allows customers to report a faulty `subBlock`.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_reservation_sub_blocks_report_faulty_builder()` + `compute_reservation_sub_blocks_report_faulty_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_reservation_sub_blocks_report_faulty(
    client: &SimpleHttpClient,
    parentName: &str,
    project: &str,
    reservationSubBlock: &str,
    zone: &str,
    requestId: Option<&str>,
    body: &ReservationSubBlocksReportFaultyRequest,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_reservation_sub_blocks_report_faulty_builder(
        client,
        parentName,
        project,
        reservationSubBlock,
        zone,
        requestId,
        body,
    )?;
    compute_reservation_sub_blocks_report_faulty_execute(builder)
}

/// GET projects/{project}/zones/{zone}/{+parentResource}/reservationSubBlocks/{resource}/setIamPolicy
/// Sets the access control policy on the specified resource. Replaces any existing policy.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_reservation_sub_blocks_set_iam_policy_execute()` to send, or `compute_reservation_sub_blocks_set_iam_policy` for simplest API.

pub fn compute_reservation_sub_blocks_set_iam_policy_builder(
    client: &SimpleHttpClient,
    parentResource: &str,
    project: &str,
    resource: &str,
    zone: &str,
    body: &ZoneSetNestedPolicyRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/zones/{}/{+parentResource}/reservationSubBlocks/{}/setIamPolicy",
        parentResource,
        project,
        resource,
        zone,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET projects/{project}/zones/{zone}/{+parentResource}/reservationSubBlocks/{resource}/setIamPolicy
/// Sets the access control policy on the specified resource. Replaces any existing policy.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_reservation_sub_blocks_set_iam_policy_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_reservation_sub_blocks_set_iam_policy_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_reservation_sub_blocks_set_iam_policy_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Policy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Policy = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/zones/{zone}/{+parentResource}/reservationSubBlocks/{resource}/setIamPolicy
/// Sets the access control policy on the specified resource. Replaces any existing policy.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_reservation_sub_blocks_set_iam_policy_builder()` + `compute_reservation_sub_blocks_set_iam_policy_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_reservation_sub_blocks_set_iam_policy(
    client: &SimpleHttpClient,
    parentResource: &str,
    project: &str,
    resource: &str,
    zone: &str,
    body: &ZoneSetNestedPolicyRequest,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Policy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_reservation_sub_blocks_set_iam_policy_builder(
        client,
        parentResource,
        project,
        resource,
        zone,
        body,
    )?;
    compute_reservation_sub_blocks_set_iam_policy_execute(builder)
}

/// GET projects/{project}/zones/{zone}/{+parentResource}/reservationSubBlocks/{resource}/testIamPermissions
/// Returns permissions that a caller has on the specified resource.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_reservation_sub_blocks_test_iam_permissions_execute()` to send, or `compute_reservation_sub_blocks_test_iam_permissions` for simplest API.

pub fn compute_reservation_sub_blocks_test_iam_permissions_builder(
    client: &SimpleHttpClient,
    parentResource: &str,
    project: &str,
    resource: &str,
    zone: &str,
    body: &TestPermissionsRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/zones/{}/{+parentResource}/reservationSubBlocks/{}/testIamPermissions",
        parentResource,
        project,
        resource,
        zone,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET projects/{project}/zones/{zone}/{+parentResource}/reservationSubBlocks/{resource}/testIamPermissions
/// Returns permissions that a caller has on the specified resource.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_reservation_sub_blocks_test_iam_permissions_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_reservation_sub_blocks_test_iam_permissions_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_reservation_sub_blocks_test_iam_permissions_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<TestPermissionsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: TestPermissionsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/zones/{zone}/{+parentResource}/reservationSubBlocks/{resource}/testIamPermissions
/// Returns permissions that a caller has on the specified resource.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_reservation_sub_blocks_test_iam_permissions_builder()` + `compute_reservation_sub_blocks_test_iam_permissions_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_reservation_sub_blocks_test_iam_permissions(
    client: &SimpleHttpClient,
    parentResource: &str,
    project: &str,
    resource: &str,
    zone: &str,
    body: &TestPermissionsRequest,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<TestPermissionsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = compute_reservation_sub_blocks_test_iam_permissions_builder(
        client,
        parentResource,
        project,
        resource,
        zone,
        body,
    )?;
    compute_reservation_sub_blocks_test_iam_permissions_execute(builder)
}

/// GET projects/{project}/aggregated/reservations
/// Retrieves an aggregated list of reservations.  To prevent failure, it is recommended that you set the `returnPartialSuccess` parameter to `true`.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_reservations_aggregated_list_execute()` to send, or `compute_reservations_aggregated_list` for simplest API.

pub fn compute_reservations_aggregated_list_builder(
    client: &SimpleHttpClient,
    project: &str,
    filter: Option<&str>,
    includeAllScopes: Option<bool>,
    maxResults: Option<i32>,
    orderBy: Option<&str>,
    pageToken: Option<&str>,
    returnPartialSuccess: Option<bool>,
    serviceProjectNumber: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/aggregated/reservations",
        project,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "filter",
            filter
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "includeAllScopes",
            includeAllScopes
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "maxResults",
            maxResults
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "orderBy",
            orderBy
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageToken",
            pageToken
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "returnPartialSuccess",
            returnPartialSuccess
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "serviceProjectNumber",
            serviceProjectNumber
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET projects/{project}/aggregated/reservations
/// Retrieves an aggregated list of reservations.  To prevent failure, it is recommended that you set the `returnPartialSuccess` parameter to `true`.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_reservations_aggregated_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_reservations_aggregated_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_reservations_aggregated_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ReservationAggregatedList>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ReservationAggregatedList = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/aggregated/reservations
/// Retrieves an aggregated list of reservations.  To prevent failure, it is recommended that you set the `returnPartialSuccess` parameter to `true`.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_reservations_aggregated_list_builder()` + `compute_reservations_aggregated_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_reservations_aggregated_list(
    client: &SimpleHttpClient,
    project: &str,
    filter: Option<&str>,
    includeAllScopes: Option<bool>,
    maxResults: Option<i32>,
    orderBy: Option<&str>,
    pageToken: Option<&str>,
    returnPartialSuccess: Option<bool>,
    serviceProjectNumber: Option<&str>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ReservationAggregatedList>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = compute_reservations_aggregated_list_builder(
        client,
        project,
        filter,
        includeAllScopes,
        maxResults,
        orderBy,
        pageToken,
        returnPartialSuccess,
        serviceProjectNumber,
    )?;
    compute_reservations_aggregated_list_execute(builder)
}

/// GET projects/{project}/zones/{zone}/reservations/{reservation}
/// Deletes the specified reservation.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_reservations_delete_execute()` to send, or `compute_reservations_delete` for simplest API.

pub fn compute_reservations_delete_builder(
    client: &SimpleHttpClient,
    project: &str,
    reservation: &str,
    zone: &str,
    requestId: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/zones/{}/reservations/{}",
        project, reservation, zone,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "requestId",
            requestId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET projects/{project}/zones/{zone}/reservations/{reservation}
/// Deletes the specified reservation.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_reservations_delete_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_reservations_delete_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_reservations_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/zones/{zone}/reservations/{reservation}
/// Deletes the specified reservation.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_reservations_delete_builder()` + `compute_reservations_delete_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_reservations_delete(
    client: &SimpleHttpClient,
    project: &str,
    reservation: &str,
    zone: &str,
    requestId: Option<&str>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        compute_reservations_delete_builder(client, project, reservation, zone, requestId)?;
    compute_reservations_delete_execute(builder)
}

/// GET projects/{project}/zones/{zone}/reservations/{reservation}
/// Retrieves information about the specified reservation.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_reservations_get_execute()` to send, or `compute_reservations_get` for simplest API.

pub fn compute_reservations_get_builder(
    client: &SimpleHttpClient,
    project: &str,
    reservation: &str,
    zone: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/zones/{}/reservations/{}",
        project, reservation, zone,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/zones/{zone}/reservations/{reservation}
/// Retrieves information about the specified reservation.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_reservations_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_reservations_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_reservations_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Reservation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Reservation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/zones/{zone}/reservations/{reservation}
/// Retrieves information about the specified reservation.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_reservations_get_builder()` + `compute_reservations_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_reservations_get(
    client: &SimpleHttpClient,
    project: &str,
    reservation: &str,
    zone: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Reservation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_reservations_get_builder(client, project, reservation, zone)?;
    compute_reservations_get_execute(builder)
}

/// GET projects/{project}/zones/{zone}/reservations/{resource}/getIamPolicy
/// Gets the access control policy for a resource. May be empty if no such policy or resource exists.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_reservations_get_iam_policy_execute()` to send, or `compute_reservations_get_iam_policy` for simplest API.

pub fn compute_reservations_get_iam_policy_builder(
    client: &SimpleHttpClient,
    project: &str,
    resource: &str,
    zone: &str,
    optionsRequestedPolicyVersion: Option<i32>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/zones/{}/reservations/{}/getIamPolicy",
        project, resource, zone,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "optionsRequestedPolicyVersion",
            optionsRequestedPolicyVersion
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET projects/{project}/zones/{zone}/reservations/{resource}/getIamPolicy
/// Gets the access control policy for a resource. May be empty if no such policy or resource exists.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_reservations_get_iam_policy_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_reservations_get_iam_policy_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_reservations_get_iam_policy_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Policy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Policy = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/zones/{zone}/reservations/{resource}/getIamPolicy
/// Gets the access control policy for a resource. May be empty if no such policy or resource exists.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_reservations_get_iam_policy_builder()` + `compute_reservations_get_iam_policy_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_reservations_get_iam_policy(
    client: &SimpleHttpClient,
    project: &str,
    resource: &str,
    zone: &str,
    optionsRequestedPolicyVersion: Option<i32>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Policy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_reservations_get_iam_policy_builder(
        client,
        project,
        resource,
        zone,
        optionsRequestedPolicyVersion,
    )?;
    compute_reservations_get_iam_policy_execute(builder)
}

/// GET projects/{project}/zones/{zone}/reservations
/// Creates a new reservation. For more information, `readReserving` zonal resources.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_reservations_insert_execute()` to send, or `compute_reservations_insert` for simplest API.

pub fn compute_reservations_insert_builder(
    client: &SimpleHttpClient,
    project: &str,
    zone: &str,
    requestId: Option<&str>,
    body: &Reservation,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/zones/{}/reservations",
        project, zone,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "requestId",
            requestId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET projects/{project}/zones/{zone}/reservations
/// Creates a new reservation. For more information, `readReserving` zonal resources.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_reservations_insert_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_reservations_insert_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_reservations_insert_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/zones/{zone}/reservations
/// Creates a new reservation. For more information, `readReserving` zonal resources.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_reservations_insert_builder()` + `compute_reservations_insert_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_reservations_insert(
    client: &SimpleHttpClient,
    project: &str,
    zone: &str,
    requestId: Option<&str>,
    body: &Reservation,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_reservations_insert_builder(client, project, zone, requestId, body)?;
    compute_reservations_insert_execute(builder)
}

/// GET projects/{project}/zones/{zone}/reservations
/// A list of all the reservations that have been configured for the specified project in specified zone.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_reservations_list_execute()` to send, or `compute_reservations_list` for simplest API.

pub fn compute_reservations_list_builder(
    client: &SimpleHttpClient,
    project: &str,
    zone: &str,
    filter: Option<&str>,
    maxResults: Option<i32>,
    orderBy: Option<&str>,
    pageToken: Option<&str>,
    returnPartialSuccess: Option<bool>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/zones/{}/reservations",
        project, zone,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "filter",
            filter
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "maxResults",
            maxResults
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "orderBy",
            orderBy
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageToken",
            pageToken
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "returnPartialSuccess",
            returnPartialSuccess
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET projects/{project}/zones/{zone}/reservations
/// A list of all the reservations that have been configured for the specified project in specified zone.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_reservations_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_reservations_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_reservations_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ReservationList>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ReservationList = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/zones/{zone}/reservations
/// A list of all the reservations that have been configured for the specified project in specified zone.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_reservations_list_builder()` + `compute_reservations_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_reservations_list(
    client: &SimpleHttpClient,
    project: &str,
    zone: &str,
    filter: Option<&str>,
    maxResults: Option<i32>,
    orderBy: Option<&str>,
    pageToken: Option<&str>,
    returnPartialSuccess: Option<bool>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ReservationList>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = compute_reservations_list_builder(
        client,
        project,
        zone,
        filter,
        maxResults,
        orderBy,
        pageToken,
        returnPartialSuccess,
    )?;
    compute_reservations_list_execute(builder)
}

/// GET projects/{project}/zones/{zone}/reservations/{reservation}/performMaintenance
/// Perform maintenance on an extended reservation
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_reservations_perform_maintenance_execute()` to send, or `compute_reservations_perform_maintenance` for simplest API.

pub fn compute_reservations_perform_maintenance_builder(
    client: &SimpleHttpClient,
    project: &str,
    reservation: &str,
    zone: &str,
    requestId: Option<&str>,
    body: &ReservationsPerformMaintenanceRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/zones/{}/reservations/{}/performMaintenance",
        project, reservation, zone,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "requestId",
            requestId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET projects/{project}/zones/{zone}/reservations/{reservation}/performMaintenance
/// Perform maintenance on an extended reservation
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_reservations_perform_maintenance_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_reservations_perform_maintenance_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_reservations_perform_maintenance_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/zones/{zone}/reservations/{reservation}/performMaintenance
/// Perform maintenance on an extended reservation
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_reservations_perform_maintenance_builder()` + `compute_reservations_perform_maintenance_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_reservations_perform_maintenance(
    client: &SimpleHttpClient,
    project: &str,
    reservation: &str,
    zone: &str,
    requestId: Option<&str>,
    body: &ReservationsPerformMaintenanceRequest,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_reservations_perform_maintenance_builder(
        client,
        project,
        reservation,
        zone,
        requestId,
        body,
    )?;
    compute_reservations_perform_maintenance_execute(builder)
}

/// GET projects/{project}/zones/{zone}/reservations/{reservation}/resize
/// Resizes the reservation (applicable to standalone reservations only). For more information, `readModifying` reservations.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_reservations_resize_execute()` to send, or `compute_reservations_resize` for simplest API.

pub fn compute_reservations_resize_builder(
    client: &SimpleHttpClient,
    project: &str,
    reservation: &str,
    zone: &str,
    requestId: Option<&str>,
    body: &ReservationsResizeRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/zones/{}/reservations/{}/resize",
        project, reservation, zone,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "requestId",
            requestId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET projects/{project}/zones/{zone}/reservations/{reservation}/resize
/// Resizes the reservation (applicable to standalone reservations only). For more information, `readModifying` reservations.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_reservations_resize_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_reservations_resize_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_reservations_resize_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/zones/{zone}/reservations/{reservation}/resize
/// Resizes the reservation (applicable to standalone reservations only). For more information, `readModifying` reservations.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_reservations_resize_builder()` + `compute_reservations_resize_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_reservations_resize(
    client: &SimpleHttpClient,
    project: &str,
    reservation: &str,
    zone: &str,
    requestId: Option<&str>,
    body: &ReservationsResizeRequest,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        compute_reservations_resize_builder(client, project, reservation, zone, requestId, body)?;
    compute_reservations_resize_execute(builder)
}

/// GET projects/{project}/zones/{zone}/reservations/{resource}/setIamPolicy
/// Sets the access control policy on the specified resource. Replaces any existing policy.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_reservations_set_iam_policy_execute()` to send, or `compute_reservations_set_iam_policy` for simplest API.

pub fn compute_reservations_set_iam_policy_builder(
    client: &SimpleHttpClient,
    project: &str,
    resource: &str,
    zone: &str,
    body: &ZoneSetPolicyRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/zones/{}/reservations/{}/setIamPolicy",
        project, resource, zone,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET projects/{project}/zones/{zone}/reservations/{resource}/setIamPolicy
/// Sets the access control policy on the specified resource. Replaces any existing policy.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_reservations_set_iam_policy_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_reservations_set_iam_policy_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_reservations_set_iam_policy_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Policy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Policy = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/zones/{zone}/reservations/{resource}/setIamPolicy
/// Sets the access control policy on the specified resource. Replaces any existing policy.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_reservations_set_iam_policy_builder()` + `compute_reservations_set_iam_policy_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_reservations_set_iam_policy(
    client: &SimpleHttpClient,
    project: &str,
    resource: &str,
    zone: &str,
    body: &ZoneSetPolicyRequest,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Policy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        compute_reservations_set_iam_policy_builder(client, project, resource, zone, body)?;
    compute_reservations_set_iam_policy_execute(builder)
}

/// GET projects/{project}/zones/{zone}/reservations/{resource}/testIamPermissions
/// Returns permissions that a caller has on the specified resource.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_reservations_test_iam_permissions_execute()` to send, or `compute_reservations_test_iam_permissions` for simplest API.

pub fn compute_reservations_test_iam_permissions_builder(
    client: &SimpleHttpClient,
    project: &str,
    resource: &str,
    zone: &str,
    body: &TestPermissionsRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/zones/{}/reservations/{}/testIamPermissions",
        project, resource, zone,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET projects/{project}/zones/{zone}/reservations/{resource}/testIamPermissions
/// Returns permissions that a caller has on the specified resource.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_reservations_test_iam_permissions_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_reservations_test_iam_permissions_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_reservations_test_iam_permissions_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<TestPermissionsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: TestPermissionsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/zones/{zone}/reservations/{resource}/testIamPermissions
/// Returns permissions that a caller has on the specified resource.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_reservations_test_iam_permissions_builder()` + `compute_reservations_test_iam_permissions_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_reservations_test_iam_permissions(
    client: &SimpleHttpClient,
    project: &str,
    resource: &str,
    zone: &str,
    body: &TestPermissionsRequest,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<TestPermissionsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        compute_reservations_test_iam_permissions_builder(client, project, resource, zone, body)?;
    compute_reservations_test_iam_permissions_execute(builder)
}

/// GET projects/{project}/zones/{zone}/reservations/{reservation}
/// Update share settings of the reservation.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_reservations_update_execute()` to send, or `compute_reservations_update` for simplest API.

pub fn compute_reservations_update_builder(
    client: &SimpleHttpClient,
    project: &str,
    reservation: &str,
    zone: &str,
    paths: Option<&str>,
    requestId: Option<&str>,
    updateMask: Option<&str>,
    body: &Reservation,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/zones/{}/reservations/{}",
        project, reservation, zone,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "paths",
            paths
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "requestId",
            requestId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "updateMask",
            updateMask
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET projects/{project}/zones/{zone}/reservations/{reservation}
/// Update share settings of the reservation.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_reservations_update_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_reservations_update_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_reservations_update_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/zones/{zone}/reservations/{reservation}
/// Update share settings of the reservation.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_reservations_update_builder()` + `compute_reservations_update_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_reservations_update(
    client: &SimpleHttpClient,
    project: &str,
    reservation: &str,
    zone: &str,
    paths: Option<&str>,
    requestId: Option<&str>,
    updateMask: Option<&str>,
    body: &Reservation,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_reservations_update_builder(
        client,
        project,
        reservation,
        zone,
        paths,
        requestId,
        updateMask,
        body,
    )?;
    compute_reservations_update_execute(builder)
}

/// GET projects/{project}/aggregated/resourcePolicies
/// Retrieves an aggregated list of resource policies.  To prevent failure, Google recommends that you set the `returnPartialSuccess` parameter to `true`.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_resource_policies_aggregated_list_execute()` to send, or `compute_resource_policies_aggregated_list` for simplest API.

pub fn compute_resource_policies_aggregated_list_builder(
    client: &SimpleHttpClient,
    project: &str,
    filter: Option<&str>,
    includeAllScopes: Option<bool>,
    maxResults: Option<i32>,
    orderBy: Option<&str>,
    pageToken: Option<&str>,
    returnPartialSuccess: Option<bool>,
    serviceProjectNumber: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/aggregated/resourcePolicies",
        project,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "filter",
            filter
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "includeAllScopes",
            includeAllScopes
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "maxResults",
            maxResults
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "orderBy",
            orderBy
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageToken",
            pageToken
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "returnPartialSuccess",
            returnPartialSuccess
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "serviceProjectNumber",
            serviceProjectNumber
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET projects/{project}/aggregated/resourcePolicies
/// Retrieves an aggregated list of resource policies.  To prevent failure, Google recommends that you set the `returnPartialSuccess` parameter to `true`.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_resource_policies_aggregated_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_resource_policies_aggregated_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_resource_policies_aggregated_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ResourcePolicyAggregatedList>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ResourcePolicyAggregatedList = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/aggregated/resourcePolicies
/// Retrieves an aggregated list of resource policies.  To prevent failure, Google recommends that you set the `returnPartialSuccess` parameter to `true`.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_resource_policies_aggregated_list_builder()` + `compute_resource_policies_aggregated_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_resource_policies_aggregated_list(
    client: &SimpleHttpClient,
    project: &str,
    filter: Option<&str>,
    includeAllScopes: Option<bool>,
    maxResults: Option<i32>,
    orderBy: Option<&str>,
    pageToken: Option<&str>,
    returnPartialSuccess: Option<bool>,
    serviceProjectNumber: Option<&str>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ResourcePolicyAggregatedList>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = compute_resource_policies_aggregated_list_builder(
        client,
        project,
        filter,
        includeAllScopes,
        maxResults,
        orderBy,
        pageToken,
        returnPartialSuccess,
        serviceProjectNumber,
    )?;
    compute_resource_policies_aggregated_list_execute(builder)
}

/// GET projects/{project}/regions/{region}/resourcePolicies/{resourcePolicy}
/// Deletes the specified resource policy.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_resource_policies_delete_execute()` to send, or `compute_resource_policies_delete` for simplest API.

pub fn compute_resource_policies_delete_builder(
    client: &SimpleHttpClient,
    project: &str,
    region: &str,
    resourcePolicy: &str,
    requestId: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/regions/{}/resourcePolicies/{}",
        project, region, resourcePolicy,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "requestId",
            requestId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET projects/{project}/regions/{region}/resourcePolicies/{resourcePolicy}
/// Deletes the specified resource policy.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_resource_policies_delete_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_resource_policies_delete_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_resource_policies_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/regions/{region}/resourcePolicies/{resourcePolicy}
/// Deletes the specified resource policy.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_resource_policies_delete_builder()` + `compute_resource_policies_delete_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_resource_policies_delete(
    client: &SimpleHttpClient,
    project: &str,
    region: &str,
    resourcePolicy: &str,
    requestId: Option<&str>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_resource_policies_delete_builder(
        client,
        project,
        region,
        resourcePolicy,
        requestId,
    )?;
    compute_resource_policies_delete_execute(builder)
}

/// GET projects/{project}/regions/{region}/resourcePolicies/{resourcePolicy}
/// Retrieves all information of the specified resource policy.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_resource_policies_get_execute()` to send, or `compute_resource_policies_get` for simplest API.

pub fn compute_resource_policies_get_builder(
    client: &SimpleHttpClient,
    project: &str,
    region: &str,
    resourcePolicy: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/regions/{}/resourcePolicies/{}",
        project, region, resourcePolicy,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/regions/{region}/resourcePolicies/{resourcePolicy}
/// Retrieves all information of the specified resource policy.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_resource_policies_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_resource_policies_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_resource_policies_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ResourcePolicy>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ResourcePolicy = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/regions/{region}/resourcePolicies/{resourcePolicy}
/// Retrieves all information of the specified resource policy.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_resource_policies_get_builder()` + `compute_resource_policies_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_resource_policies_get(
    client: &SimpleHttpClient,
    project: &str,
    region: &str,
    resourcePolicy: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ResourcePolicy>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = compute_resource_policies_get_builder(client, project, region, resourcePolicy)?;
    compute_resource_policies_get_execute(builder)
}

/// GET projects/{project}/regions/{region}/resourcePolicies/{resource}/getIamPolicy
/// Gets the access control policy for a resource. May be empty if no such policy or resource exists.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_resource_policies_get_iam_policy_execute()` to send, or `compute_resource_policies_get_iam_policy` for simplest API.

pub fn compute_resource_policies_get_iam_policy_builder(
    client: &SimpleHttpClient,
    project: &str,
    region: &str,
    resource: &str,
    optionsRequestedPolicyVersion: Option<i32>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/regions/{}/resourcePolicies/{}/getIamPolicy",
        project, region, resource,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "optionsRequestedPolicyVersion",
            optionsRequestedPolicyVersion
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET projects/{project}/regions/{region}/resourcePolicies/{resource}/getIamPolicy
/// Gets the access control policy for a resource. May be empty if no such policy or resource exists.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_resource_policies_get_iam_policy_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_resource_policies_get_iam_policy_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_resource_policies_get_iam_policy_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Policy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Policy = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/regions/{region}/resourcePolicies/{resource}/getIamPolicy
/// Gets the access control policy for a resource. May be empty if no such policy or resource exists.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_resource_policies_get_iam_policy_builder()` + `compute_resource_policies_get_iam_policy_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_resource_policies_get_iam_policy(
    client: &SimpleHttpClient,
    project: &str,
    region: &str,
    resource: &str,
    optionsRequestedPolicyVersion: Option<i32>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Policy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_resource_policies_get_iam_policy_builder(
        client,
        project,
        region,
        resource,
        optionsRequestedPolicyVersion,
    )?;
    compute_resource_policies_get_iam_policy_execute(builder)
}

/// GET projects/{project}/regions/{region}/resourcePolicies
/// Creates a new resource policy.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_resource_policies_insert_execute()` to send, or `compute_resource_policies_insert` for simplest API.

pub fn compute_resource_policies_insert_builder(
    client: &SimpleHttpClient,
    project: &str,
    region: &str,
    requestId: Option<&str>,
    body: &ResourcePolicy,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/regions/{}/resourcePolicies",
        project, region,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "requestId",
            requestId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET projects/{project}/regions/{region}/resourcePolicies
/// Creates a new resource policy.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_resource_policies_insert_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_resource_policies_insert_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_resource_policies_insert_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/regions/{region}/resourcePolicies
/// Creates a new resource policy.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_resource_policies_insert_builder()` + `compute_resource_policies_insert_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_resource_policies_insert(
    client: &SimpleHttpClient,
    project: &str,
    region: &str,
    requestId: Option<&str>,
    body: &ResourcePolicy,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        compute_resource_policies_insert_builder(client, project, region, requestId, body)?;
    compute_resource_policies_insert_execute(builder)
}

/// GET projects/{project}/regions/{region}/resourcePolicies
/// A list all the resource policies that have been configured for the specified project in specified region.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_resource_policies_list_execute()` to send, or `compute_resource_policies_list` for simplest API.

pub fn compute_resource_policies_list_builder(
    client: &SimpleHttpClient,
    project: &str,
    region: &str,
    filter: Option<&str>,
    maxResults: Option<i32>,
    orderBy: Option<&str>,
    pageToken: Option<&str>,
    returnPartialSuccess: Option<bool>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/regions/{}/resourcePolicies",
        project, region,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "filter",
            filter
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "maxResults",
            maxResults
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "orderBy",
            orderBy
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageToken",
            pageToken
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "returnPartialSuccess",
            returnPartialSuccess
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET projects/{project}/regions/{region}/resourcePolicies
/// A list all the resource policies that have been configured for the specified project in specified region.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_resource_policies_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_resource_policies_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_resource_policies_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ResourcePolicyList>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ResourcePolicyList = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/regions/{region}/resourcePolicies
/// A list all the resource policies that have been configured for the specified project in specified region.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_resource_policies_list_builder()` + `compute_resource_policies_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_resource_policies_list(
    client: &SimpleHttpClient,
    project: &str,
    region: &str,
    filter: Option<&str>,
    maxResults: Option<i32>,
    orderBy: Option<&str>,
    pageToken: Option<&str>,
    returnPartialSuccess: Option<bool>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ResourcePolicyList>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = compute_resource_policies_list_builder(
        client,
        project,
        region,
        filter,
        maxResults,
        orderBy,
        pageToken,
        returnPartialSuccess,
    )?;
    compute_resource_policies_list_execute(builder)
}

/// GET projects/{project}/regions/{region}/resourcePolicies/{resourcePolicy}
/// Modify the specified resource policy.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_resource_policies_patch_execute()` to send, or `compute_resource_policies_patch` for simplest API.

pub fn compute_resource_policies_patch_builder(
    client: &SimpleHttpClient,
    project: &str,
    region: &str,
    resourcePolicy: &str,
    requestId: Option<&str>,
    updateMask: Option<&str>,
    body: &ResourcePolicy,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/regions/{}/resourcePolicies/{}",
        project, region, resourcePolicy,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "requestId",
            requestId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "updateMask",
            updateMask
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET projects/{project}/regions/{region}/resourcePolicies/{resourcePolicy}
/// Modify the specified resource policy.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_resource_policies_patch_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_resource_policies_patch_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_resource_policies_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/regions/{region}/resourcePolicies/{resourcePolicy}
/// Modify the specified resource policy.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_resource_policies_patch_builder()` + `compute_resource_policies_patch_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_resource_policies_patch(
    client: &SimpleHttpClient,
    project: &str,
    region: &str,
    resourcePolicy: &str,
    requestId: Option<&str>,
    updateMask: Option<&str>,
    body: &ResourcePolicy,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_resource_policies_patch_builder(
        client,
        project,
        region,
        resourcePolicy,
        requestId,
        updateMask,
        body,
    )?;
    compute_resource_policies_patch_execute(builder)
}

/// GET projects/{project}/regions/{region}/resourcePolicies/{resource}/setIamPolicy
/// Sets the access control policy on the specified resource. Replaces any existing policy.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_resource_policies_set_iam_policy_execute()` to send, or `compute_resource_policies_set_iam_policy` for simplest API.

pub fn compute_resource_policies_set_iam_policy_builder(
    client: &SimpleHttpClient,
    project: &str,
    region: &str,
    resource: &str,
    body: &RegionSetPolicyRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/regions/{}/resourcePolicies/{}/setIamPolicy",
        project, region, resource,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET projects/{project}/regions/{region}/resourcePolicies/{resource}/setIamPolicy
/// Sets the access control policy on the specified resource. Replaces any existing policy.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_resource_policies_set_iam_policy_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_resource_policies_set_iam_policy_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_resource_policies_set_iam_policy_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Policy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Policy = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/regions/{region}/resourcePolicies/{resource}/setIamPolicy
/// Sets the access control policy on the specified resource. Replaces any existing policy.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_resource_policies_set_iam_policy_builder()` + `compute_resource_policies_set_iam_policy_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_resource_policies_set_iam_policy(
    client: &SimpleHttpClient,
    project: &str,
    region: &str,
    resource: &str,
    body: &RegionSetPolicyRequest,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Policy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        compute_resource_policies_set_iam_policy_builder(client, project, region, resource, body)?;
    compute_resource_policies_set_iam_policy_execute(builder)
}

/// GET projects/{project}/regions/{region}/resourcePolicies/{resource}/testIamPermissions
/// Returns permissions that a caller has on the specified resource.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_resource_policies_test_iam_permissions_execute()` to send, or `compute_resource_policies_test_iam_permissions` for simplest API.

pub fn compute_resource_policies_test_iam_permissions_builder(
    client: &SimpleHttpClient,
    project: &str,
    region: &str,
    resource: &str,
    body: &TestPermissionsRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/regions/{}/resourcePolicies/{}/testIamPermissions",
        project, region, resource,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET projects/{project}/regions/{region}/resourcePolicies/{resource}/testIamPermissions
/// Returns permissions that a caller has on the specified resource.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_resource_policies_test_iam_permissions_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_resource_policies_test_iam_permissions_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_resource_policies_test_iam_permissions_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<TestPermissionsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: TestPermissionsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/regions/{region}/resourcePolicies/{resource}/testIamPermissions
/// Returns permissions that a caller has on the specified resource.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_resource_policies_test_iam_permissions_builder()` + `compute_resource_policies_test_iam_permissions_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_resource_policies_test_iam_permissions(
    client: &SimpleHttpClient,
    project: &str,
    region: &str,
    resource: &str,
    body: &TestPermissionsRequest,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<TestPermissionsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = compute_resource_policies_test_iam_permissions_builder(
        client, project, region, resource, body,
    )?;
    compute_resource_policies_test_iam_permissions_execute(builder)
}

/// GET projects/{project}/aggregated/routers
/// Retrieves an aggregated list of routers.  To prevent failure, Google recommends that you set the `returnPartialSuccess` parameter to `true`.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_routers_aggregated_list_execute()` to send, or `compute_routers_aggregated_list` for simplest API.

pub fn compute_routers_aggregated_list_builder(
    client: &SimpleHttpClient,
    project: &str,
    filter: Option<&str>,
    includeAllScopes: Option<bool>,
    maxResults: Option<i32>,
    orderBy: Option<&str>,
    pageToken: Option<&str>,
    returnPartialSuccess: Option<bool>,
    serviceProjectNumber: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/aggregated/routers",
        project,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "filter",
            filter
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "includeAllScopes",
            includeAllScopes
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "maxResults",
            maxResults
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "orderBy",
            orderBy
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageToken",
            pageToken
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "returnPartialSuccess",
            returnPartialSuccess
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "serviceProjectNumber",
            serviceProjectNumber
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET projects/{project}/aggregated/routers
/// Retrieves an aggregated list of routers.  To prevent failure, Google recommends that you set the `returnPartialSuccess` parameter to `true`.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_routers_aggregated_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_routers_aggregated_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_routers_aggregated_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<RouterAggregatedList>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: RouterAggregatedList = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/aggregated/routers
/// Retrieves an aggregated list of routers.  To prevent failure, Google recommends that you set the `returnPartialSuccess` parameter to `true`.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_routers_aggregated_list_builder()` + `compute_routers_aggregated_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_routers_aggregated_list(
    client: &SimpleHttpClient,
    project: &str,
    filter: Option<&str>,
    includeAllScopes: Option<bool>,
    maxResults: Option<i32>,
    orderBy: Option<&str>,
    pageToken: Option<&str>,
    returnPartialSuccess: Option<bool>,
    serviceProjectNumber: Option<&str>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<RouterAggregatedList>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = compute_routers_aggregated_list_builder(
        client,
        project,
        filter,
        includeAllScopes,
        maxResults,
        orderBy,
        pageToken,
        returnPartialSuccess,
        serviceProjectNumber,
    )?;
    compute_routers_aggregated_list_execute(builder)
}

/// GET projects/{project}/regions/{region}/routers/{router}
/// Deletes the specified Router resource.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_routers_delete_execute()` to send, or `compute_routers_delete` for simplest API.

pub fn compute_routers_delete_builder(
    client: &SimpleHttpClient,
    project: &str,
    region: &str,
    router: &str,
    requestId: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/regions/{}/routers/{}",
        project, region, router,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "requestId",
            requestId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET projects/{project}/regions/{region}/routers/{router}
/// Deletes the specified Router resource.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_routers_delete_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_routers_delete_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_routers_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/regions/{region}/routers/{router}
/// Deletes the specified Router resource.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_routers_delete_builder()` + `compute_routers_delete_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_routers_delete(
    client: &SimpleHttpClient,
    project: &str,
    region: &str,
    router: &str,
    requestId: Option<&str>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_routers_delete_builder(client, project, region, router, requestId)?;
    compute_routers_delete_execute(builder)
}

/// GET projects/{project}/regions/{region}/routers/{router}/deleteRoutePolicy
/// Deletes Route Policy
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_routers_delete_route_policy_execute()` to send, or `compute_routers_delete_route_policy` for simplest API.

pub fn compute_routers_delete_route_policy_builder(
    client: &SimpleHttpClient,
    project: &str,
    region: &str,
    router: &str,
    policy: Option<&str>,
    requestId: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/regions/{}/routers/{}/deleteRoutePolicy",
        project, region, router,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "policy",
            policy
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "requestId",
            requestId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET projects/{project}/regions/{region}/routers/{router}/deleteRoutePolicy
/// Deletes Route Policy
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_routers_delete_route_policy_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_routers_delete_route_policy_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_routers_delete_route_policy_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/regions/{region}/routers/{router}/deleteRoutePolicy
/// Deletes Route Policy
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_routers_delete_route_policy_builder()` + `compute_routers_delete_route_policy_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_routers_delete_route_policy(
    client: &SimpleHttpClient,
    project: &str,
    region: &str,
    router: &str,
    policy: Option<&str>,
    requestId: Option<&str>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_routers_delete_route_policy_builder(
        client, project, region, router, policy, requestId,
    )?;
    compute_routers_delete_route_policy_execute(builder)
}

/// GET projects/{project}/regions/{region}/routers/{router}
/// Returns the specified Router resource.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_routers_get_execute()` to send, or `compute_routers_get` for simplest API.

pub fn compute_routers_get_builder(
    client: &SimpleHttpClient,
    project: &str,
    region: &str,
    router: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/regions/{}/routers/{}",
        project, region, router,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/regions/{region}/routers/{router}
/// Returns the specified Router resource.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_routers_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_routers_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_routers_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Router>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Router = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/regions/{region}/routers/{router}
/// Returns the specified Router resource.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_routers_get_builder()` + `compute_routers_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_routers_get(
    client: &SimpleHttpClient,
    project: &str,
    region: &str,
    router: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Router>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_routers_get_builder(client, project, region, router)?;
    compute_routers_get_execute(builder)
}

/// GET projects/{project}/regions/{region}/routers/{router}/getNatIpInfo
/// Retrieves runtime NAT IP information.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_routers_get_nat_ip_info_execute()` to send, or `compute_routers_get_nat_ip_info` for simplest API.

pub fn compute_routers_get_nat_ip_info_builder(
    client: &SimpleHttpClient,
    project: &str,
    region: &str,
    router: &str,
    natName: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/regions/{}/routers/{}/getNatIpInfo",
        project, region, router,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "natName",
            natName
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET projects/{project}/regions/{region}/routers/{router}/getNatIpInfo
/// Retrieves runtime NAT IP information.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_routers_get_nat_ip_info_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_routers_get_nat_ip_info_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_routers_get_nat_ip_info_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<NatIpInfoResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: NatIpInfoResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/regions/{region}/routers/{router}/getNatIpInfo
/// Retrieves runtime NAT IP information.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_routers_get_nat_ip_info_builder()` + `compute_routers_get_nat_ip_info_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_routers_get_nat_ip_info(
    client: &SimpleHttpClient,
    project: &str,
    region: &str,
    router: &str,
    natName: Option<&str>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<NatIpInfoResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        compute_routers_get_nat_ip_info_builder(client, project, region, router, natName)?;
    compute_routers_get_nat_ip_info_execute(builder)
}

/// GET projects/{project}/regions/{region}/routers/{router}/getNatMappingInfo
/// Retrieves runtime Nat mapping information of VM endpoints.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_routers_get_nat_mapping_info_execute()` to send, or `compute_routers_get_nat_mapping_info` for simplest API.

pub fn compute_routers_get_nat_mapping_info_builder(
    client: &SimpleHttpClient,
    project: &str,
    region: &str,
    router: &str,
    filter: Option<&str>,
    maxResults: Option<i32>,
    natName: Option<&str>,
    orderBy: Option<&str>,
    pageToken: Option<&str>,
    returnPartialSuccess: Option<bool>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/regions/{}/routers/{}/getNatMappingInfo",
        project, region, router,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "filter",
            filter
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "maxResults",
            maxResults
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "natName",
            natName
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "orderBy",
            orderBy
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageToken",
            pageToken
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "returnPartialSuccess",
            returnPartialSuccess
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET projects/{project}/regions/{region}/routers/{router}/getNatMappingInfo
/// Retrieves runtime Nat mapping information of VM endpoints.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_routers_get_nat_mapping_info_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_routers_get_nat_mapping_info_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_routers_get_nat_mapping_info_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<VmEndpointNatMappingsList>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: VmEndpointNatMappingsList = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/regions/{region}/routers/{router}/getNatMappingInfo
/// Retrieves runtime Nat mapping information of VM endpoints.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_routers_get_nat_mapping_info_builder()` + `compute_routers_get_nat_mapping_info_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_routers_get_nat_mapping_info(
    client: &SimpleHttpClient,
    project: &str,
    region: &str,
    router: &str,
    filter: Option<&str>,
    maxResults: Option<i32>,
    natName: Option<&str>,
    orderBy: Option<&str>,
    pageToken: Option<&str>,
    returnPartialSuccess: Option<bool>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<VmEndpointNatMappingsList>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = compute_routers_get_nat_mapping_info_builder(
        client,
        project,
        region,
        router,
        filter,
        maxResults,
        natName,
        orderBy,
        pageToken,
        returnPartialSuccess,
    )?;
    compute_routers_get_nat_mapping_info_execute(builder)
}

/// GET projects/{project}/regions/{region}/routers/{router}/getRoutePolicy
/// Returns specified Route Policy
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_routers_get_route_policy_execute()` to send, or `compute_routers_get_route_policy` for simplest API.

pub fn compute_routers_get_route_policy_builder(
    client: &SimpleHttpClient,
    project: &str,
    region: &str,
    router: &str,
    policy: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/regions/{}/routers/{}/getRoutePolicy",
        project, region, router,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "policy",
            policy
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET projects/{project}/regions/{region}/routers/{router}/getRoutePolicy
/// Returns specified Route Policy
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_routers_get_route_policy_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_routers_get_route_policy_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_routers_get_route_policy_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<RoutersGetRoutePolicyResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: RoutersGetRoutePolicyResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/regions/{region}/routers/{router}/getRoutePolicy
/// Returns specified Route Policy
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_routers_get_route_policy_builder()` + `compute_routers_get_route_policy_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_routers_get_route_policy(
    client: &SimpleHttpClient,
    project: &str,
    region: &str,
    router: &str,
    policy: Option<&str>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<RoutersGetRoutePolicyResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        compute_routers_get_route_policy_builder(client, project, region, router, policy)?;
    compute_routers_get_route_policy_execute(builder)
}

/// GET projects/{project}/regions/{region}/routers/{router}/getRouterStatus
/// Retrieves runtime information of the specified router.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_routers_get_router_status_execute()` to send, or `compute_routers_get_router_status` for simplest API.

pub fn compute_routers_get_router_status_builder(
    client: &SimpleHttpClient,
    project: &str,
    region: &str,
    router: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/regions/{}/routers/{}/getRouterStatus",
        project, region, router,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/regions/{region}/routers/{router}/getRouterStatus
/// Retrieves runtime information of the specified router.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_routers_get_router_status_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_routers_get_router_status_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_routers_get_router_status_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<RouterStatusResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: RouterStatusResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/regions/{region}/routers/{router}/getRouterStatus
/// Retrieves runtime information of the specified router.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_routers_get_router_status_builder()` + `compute_routers_get_router_status_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_routers_get_router_status(
    client: &SimpleHttpClient,
    project: &str,
    region: &str,
    router: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<RouterStatusResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = compute_routers_get_router_status_builder(client, project, region, router)?;
    compute_routers_get_router_status_execute(builder)
}

/// GET projects/{project}/regions/{region}/routers
/// Creates a Router resource in the specified project and region using the data included in the request.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_routers_insert_execute()` to send, or `compute_routers_insert` for simplest API.

pub fn compute_routers_insert_builder(
    client: &SimpleHttpClient,
    project: &str,
    region: &str,
    requestId: Option<&str>,
    body: &Router,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/regions/{}/routers",
        project, region,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "requestId",
            requestId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET projects/{project}/regions/{region}/routers
/// Creates a Router resource in the specified project and region using the data included in the request.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_routers_insert_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_routers_insert_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_routers_insert_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/regions/{region}/routers
/// Creates a Router resource in the specified project and region using the data included in the request.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_routers_insert_builder()` + `compute_routers_insert_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_routers_insert(
    client: &SimpleHttpClient,
    project: &str,
    region: &str,
    requestId: Option<&str>,
    body: &Router,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_routers_insert_builder(client, project, region, requestId, body)?;
    compute_routers_insert_execute(builder)
}

/// GET projects/{project}/regions/{region}/routers
/// Retrieves a list of Router resources available to the specified project.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_routers_list_execute()` to send, or `compute_routers_list` for simplest API.

pub fn compute_routers_list_builder(
    client: &SimpleHttpClient,
    project: &str,
    region: &str,
    filter: Option<&str>,
    maxResults: Option<i32>,
    orderBy: Option<&str>,
    pageToken: Option<&str>,
    returnPartialSuccess: Option<bool>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/regions/{}/routers",
        project, region,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "filter",
            filter
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "maxResults",
            maxResults
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "orderBy",
            orderBy
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageToken",
            pageToken
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "returnPartialSuccess",
            returnPartialSuccess
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET projects/{project}/regions/{region}/routers
/// Retrieves a list of Router resources available to the specified project.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_routers_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_routers_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_routers_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<RouterList>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: RouterList = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/regions/{region}/routers
/// Retrieves a list of Router resources available to the specified project.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_routers_list_builder()` + `compute_routers_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_routers_list(
    client: &SimpleHttpClient,
    project: &str,
    region: &str,
    filter: Option<&str>,
    maxResults: Option<i32>,
    orderBy: Option<&str>,
    pageToken: Option<&str>,
    returnPartialSuccess: Option<bool>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<RouterList>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_routers_list_builder(
        client,
        project,
        region,
        filter,
        maxResults,
        orderBy,
        pageToken,
        returnPartialSuccess,
    )?;
    compute_routers_list_execute(builder)
}

/// GET projects/{project}/regions/{region}/routers/{router}/listBgpRoutes
/// Retrieves a list of router bgp routes available to the specified project.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_routers_list_bgp_routes_execute()` to send, or `compute_routers_list_bgp_routes` for simplest API.

pub fn compute_routers_list_bgp_routes_builder(
    client: &SimpleHttpClient,
    project: &str,
    region: &str,
    router: &str,
    addressFamily: Option<&str>,
    destinationPrefix: Option<&str>,
    filter: Option<&str>,
    maxResults: Option<i32>,
    orderBy: Option<&str>,
    pageToken: Option<&str>,
    peer: Option<&str>,
    policyApplied: Option<bool>,
    returnPartialSuccess: Option<bool>,
    routeType: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/regions/{}/routers/{}/listBgpRoutes",
        project, region, router,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "addressFamily",
            addressFamily
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "destinationPrefix",
            destinationPrefix
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "filter",
            filter
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "maxResults",
            maxResults
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "orderBy",
            orderBy
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageToken",
            pageToken
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "peer",
            peer.as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "policyApplied",
            policyApplied
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "returnPartialSuccess",
            returnPartialSuccess
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "routeType",
            routeType
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET projects/{project}/regions/{region}/routers/{router}/listBgpRoutes
/// Retrieves a list of router bgp routes available to the specified project.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_routers_list_bgp_routes_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_routers_list_bgp_routes_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_routers_list_bgp_routes_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<RoutersListBgpRoutes>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: RoutersListBgpRoutes = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/regions/{region}/routers/{router}/listBgpRoutes
/// Retrieves a list of router bgp routes available to the specified project.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_routers_list_bgp_routes_builder()` + `compute_routers_list_bgp_routes_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_routers_list_bgp_routes(
    client: &SimpleHttpClient,
    project: &str,
    region: &str,
    router: &str,
    addressFamily: Option<&str>,
    destinationPrefix: Option<&str>,
    filter: Option<&str>,
    maxResults: Option<i32>,
    orderBy: Option<&str>,
    pageToken: Option<&str>,
    peer: Option<&str>,
    policyApplied: Option<bool>,
    returnPartialSuccess: Option<bool>,
    routeType: Option<&str>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<RoutersListBgpRoutes>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = compute_routers_list_bgp_routes_builder(
        client,
        project,
        region,
        router,
        addressFamily,
        destinationPrefix,
        filter,
        maxResults,
        orderBy,
        pageToken,
        peer,
        policyApplied,
        returnPartialSuccess,
        routeType,
    )?;
    compute_routers_list_bgp_routes_execute(builder)
}

/// GET projects/{project}/regions/{region}/routers/{router}/listRoutePolicies
/// Retrieves a list of router route policy subresources available to the specified project.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_routers_list_route_policies_execute()` to send, or `compute_routers_list_route_policies` for simplest API.

pub fn compute_routers_list_route_policies_builder(
    client: &SimpleHttpClient,
    project: &str,
    region: &str,
    router: &str,
    filter: Option<&str>,
    maxResults: Option<i32>,
    orderBy: Option<&str>,
    pageToken: Option<&str>,
    returnPartialSuccess: Option<bool>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/regions/{}/routers/{}/listRoutePolicies",
        project, region, router,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "filter",
            filter
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "maxResults",
            maxResults
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "orderBy",
            orderBy
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageToken",
            pageToken
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "returnPartialSuccess",
            returnPartialSuccess
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET projects/{project}/regions/{region}/routers/{router}/listRoutePolicies
/// Retrieves a list of router route policy subresources available to the specified project.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_routers_list_route_policies_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_routers_list_route_policies_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_routers_list_route_policies_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<RoutersListRoutePolicies>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: RoutersListRoutePolicies = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/regions/{region}/routers/{router}/listRoutePolicies
/// Retrieves a list of router route policy subresources available to the specified project.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_routers_list_route_policies_builder()` + `compute_routers_list_route_policies_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_routers_list_route_policies(
    client: &SimpleHttpClient,
    project: &str,
    region: &str,
    router: &str,
    filter: Option<&str>,
    maxResults: Option<i32>,
    orderBy: Option<&str>,
    pageToken: Option<&str>,
    returnPartialSuccess: Option<bool>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<RoutersListRoutePolicies>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = compute_routers_list_route_policies_builder(
        client,
        project,
        region,
        router,
        filter,
        maxResults,
        orderBy,
        pageToken,
        returnPartialSuccess,
    )?;
    compute_routers_list_route_policies_execute(builder)
}

/// GET projects/{project}/regions/{region}/routers/{router}
/// Patches the specified Router resource with the data included in the request. This method `supportsPATCH` semantics and `usesJSON` merge patch format and processing rules.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_routers_patch_execute()` to send, or `compute_routers_patch` for simplest API.

pub fn compute_routers_patch_builder(
    client: &SimpleHttpClient,
    project: &str,
    region: &str,
    router: &str,
    requestId: Option<&str>,
    body: &Router,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/regions/{}/routers/{}",
        project, region, router,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "requestId",
            requestId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET projects/{project}/regions/{region}/routers/{router}
/// Patches the specified Router resource with the data included in the request. This method `supportsPATCH` semantics and `usesJSON` merge patch format and processing rules.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_routers_patch_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_routers_patch_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_routers_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/regions/{region}/routers/{router}
/// Patches the specified Router resource with the data included in the request. This method `supportsPATCH` semantics and `usesJSON` merge patch format and processing rules.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_routers_patch_builder()` + `compute_routers_patch_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_routers_patch(
    client: &SimpleHttpClient,
    project: &str,
    region: &str,
    router: &str,
    requestId: Option<&str>,
    body: &Router,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_routers_patch_builder(client, project, region, router, requestId, body)?;
    compute_routers_patch_execute(builder)
}

/// GET projects/{project}/regions/{region}/routers/{router}/patchRoutePolicy
/// Patches Route Policy
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_routers_patch_route_policy_execute()` to send, or `compute_routers_patch_route_policy` for simplest API.

pub fn compute_routers_patch_route_policy_builder(
    client: &SimpleHttpClient,
    project: &str,
    region: &str,
    router: &str,
    requestId: Option<&str>,
    body: &RoutePolicy,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/regions/{}/routers/{}/patchRoutePolicy",
        project, region, router,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "requestId",
            requestId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET projects/{project}/regions/{region}/routers/{router}/patchRoutePolicy
/// Patches Route Policy
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_routers_patch_route_policy_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_routers_patch_route_policy_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_routers_patch_route_policy_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/regions/{region}/routers/{router}/patchRoutePolicy
/// Patches Route Policy
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_routers_patch_route_policy_builder()` + `compute_routers_patch_route_policy_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_routers_patch_route_policy(
    client: &SimpleHttpClient,
    project: &str,
    region: &str,
    router: &str,
    requestId: Option<&str>,
    body: &RoutePolicy,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_routers_patch_route_policy_builder(
        client, project, region, router, requestId, body,
    )?;
    compute_routers_patch_route_policy_execute(builder)
}

/// GET projects/{project}/regions/{region}/routers/{router}/preview
/// Preview fields auto-generated during router create andupdate operations. Calling this method does NOT create or update the router.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_routers_preview_execute()` to send, or `compute_routers_preview` for simplest API.

pub fn compute_routers_preview_builder(
    client: &SimpleHttpClient,
    project: &str,
    region: &str,
    router: &str,
    body: &Router,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/regions/{}/routers/{}/preview",
        project, region, router,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET projects/{project}/regions/{region}/routers/{router}/preview
/// Preview fields auto-generated during router create andupdate operations. Calling this method does NOT create or update the router.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_routers_preview_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_routers_preview_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_routers_preview_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<RoutersPreviewResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: RoutersPreviewResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/regions/{region}/routers/{router}/preview
/// Preview fields auto-generated during router create andupdate operations. Calling this method does NOT create or update the router.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_routers_preview_builder()` + `compute_routers_preview_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_routers_preview(
    client: &SimpleHttpClient,
    project: &str,
    region: &str,
    router: &str,
    body: &Router,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<RoutersPreviewResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = compute_routers_preview_builder(client, project, region, router, body)?;
    compute_routers_preview_execute(builder)
}

/// GET projects/{project}/regions/{region}/routers/{router}
/// Updates the specified Router resource with the data included in the request.  This method conforms `toPUT` semantics, which requests that the state of the target resource be created or replaced with the state defined by the representation enclosed in the request message payload.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_routers_update_execute()` to send, or `compute_routers_update` for simplest API.

pub fn compute_routers_update_builder(
    client: &SimpleHttpClient,
    project: &str,
    region: &str,
    router: &str,
    requestId: Option<&str>,
    body: &Router,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/regions/{}/routers/{}",
        project, region, router,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "requestId",
            requestId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET projects/{project}/regions/{region}/routers/{router}
/// Updates the specified Router resource with the data included in the request.  This method conforms `toPUT` semantics, which requests that the state of the target resource be created or replaced with the state defined by the representation enclosed in the request message payload.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_routers_update_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_routers_update_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_routers_update_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/regions/{region}/routers/{router}
/// Updates the specified Router resource with the data included in the request.  This method conforms `toPUT` semantics, which requests that the state of the target resource be created or replaced with the state defined by the representation enclosed in the request message payload.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_routers_update_builder()` + `compute_routers_update_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_routers_update(
    client: &SimpleHttpClient,
    project: &str,
    region: &str,
    router: &str,
    requestId: Option<&str>,
    body: &Router,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_routers_update_builder(client, project, region, router, requestId, body)?;
    compute_routers_update_execute(builder)
}

/// GET projects/{project}/regions/{region}/routers/{router}/updateRoutePolicy
/// Updates or creates new Route Policy
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_routers_update_route_policy_execute()` to send, or `compute_routers_update_route_policy` for simplest API.

pub fn compute_routers_update_route_policy_builder(
    client: &SimpleHttpClient,
    project: &str,
    region: &str,
    router: &str,
    requestId: Option<&str>,
    body: &RoutePolicy,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/regions/{}/routers/{}/updateRoutePolicy",
        project, region, router,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "requestId",
            requestId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET projects/{project}/regions/{region}/routers/{router}/updateRoutePolicy
/// Updates or creates new Route Policy
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_routers_update_route_policy_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_routers_update_route_policy_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_routers_update_route_policy_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/regions/{region}/routers/{router}/updateRoutePolicy
/// Updates or creates new Route Policy
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_routers_update_route_policy_builder()` + `compute_routers_update_route_policy_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_routers_update_route_policy(
    client: &SimpleHttpClient,
    project: &str,
    region: &str,
    router: &str,
    requestId: Option<&str>,
    body: &RoutePolicy,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_routers_update_route_policy_builder(
        client, project, region, router, requestId, body,
    )?;
    compute_routers_update_route_policy_execute(builder)
}

/// GET projects/{project}/global/routes/{route}
/// Deletes the specified Route resource.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_routes_delete_execute()` to send, or `compute_routes_delete` for simplest API.

pub fn compute_routes_delete_builder(
    client: &SimpleHttpClient,
    project: &str,
    route: &str,
    requestId: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/global/routes/{}",
        project, route,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "requestId",
            requestId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET projects/{project}/global/routes/{route}
/// Deletes the specified Route resource.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_routes_delete_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_routes_delete_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_routes_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/global/routes/{route}
/// Deletes the specified Route resource.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_routes_delete_builder()` + `compute_routes_delete_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_routes_delete(
    client: &SimpleHttpClient,
    project: &str,
    route: &str,
    requestId: Option<&str>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_routes_delete_builder(client, project, route, requestId)?;
    compute_routes_delete_execute(builder)
}

/// GET projects/{project}/global/routes/{route}
/// Returns the specified Route resource.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_routes_get_execute()` to send, or `compute_routes_get` for simplest API.

pub fn compute_routes_get_builder(
    client: &SimpleHttpClient,
    project: &str,
    route: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/global/routes/{}",
        project, route,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/global/routes/{route}
/// Returns the specified Route resource.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_routes_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_routes_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_routes_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Route>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Route = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/global/routes/{route}
/// Returns the specified Route resource.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_routes_get_builder()` + `compute_routes_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_routes_get(
    client: &SimpleHttpClient,
    project: &str,
    route: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Route>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_routes_get_builder(client, project, route)?;
    compute_routes_get_execute(builder)
}

/// GET projects/{project}/global/routes
/// Creates a Route resource in the specified project using the data included in the request.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_routes_insert_execute()` to send, or `compute_routes_insert` for simplest API.

pub fn compute_routes_insert_builder(
    client: &SimpleHttpClient,
    project: &str,
    requestId: Option<&str>,
    body: &Route,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comprojects/{}/global/routes", project,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "requestId",
            requestId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET projects/{project}/global/routes
/// Creates a Route resource in the specified project using the data included in the request.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_routes_insert_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_routes_insert_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_routes_insert_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/global/routes
/// Creates a Route resource in the specified project using the data included in the request.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_routes_insert_builder()` + `compute_routes_insert_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_routes_insert(
    client: &SimpleHttpClient,
    project: &str,
    requestId: Option<&str>,
    body: &Route,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_routes_insert_builder(client, project, requestId, body)?;
    compute_routes_insert_execute(builder)
}

/// GET projects/{project}/global/routes
/// Retrieves the list of Route resources available to the specified project.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_routes_list_execute()` to send, or `compute_routes_list` for simplest API.

pub fn compute_routes_list_builder(
    client: &SimpleHttpClient,
    project: &str,
    filter: Option<&str>,
    maxResults: Option<i32>,
    orderBy: Option<&str>,
    pageToken: Option<&str>,
    returnPartialSuccess: Option<bool>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comprojects/{}/global/routes", project,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "filter",
            filter
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "maxResults",
            maxResults
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "orderBy",
            orderBy
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageToken",
            pageToken
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "returnPartialSuccess",
            returnPartialSuccess
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET projects/{project}/global/routes
/// Retrieves the list of Route resources available to the specified project.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_routes_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_routes_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_routes_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<RouteList>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: RouteList = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/global/routes
/// Retrieves the list of Route resources available to the specified project.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_routes_list_builder()` + `compute_routes_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_routes_list(
    client: &SimpleHttpClient,
    project: &str,
    filter: Option<&str>,
    maxResults: Option<i32>,
    orderBy: Option<&str>,
    pageToken: Option<&str>,
    returnPartialSuccess: Option<bool>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<RouteList>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_routes_list_builder(
        client,
        project,
        filter,
        maxResults,
        orderBy,
        pageToken,
        returnPartialSuccess,
    )?;
    compute_routes_list_execute(builder)
}

/// GET projects/{project}/global/routes/{resource}/testIamPermissions
/// Returns permissions that a caller has on the specified resource.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_routes_test_iam_permissions_execute()` to send, or `compute_routes_test_iam_permissions` for simplest API.

pub fn compute_routes_test_iam_permissions_builder(
    client: &SimpleHttpClient,
    project: &str,
    resource: &str,
    body: &TestPermissionsRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/global/routes/{}/testIamPermissions",
        project, resource,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET projects/{project}/global/routes/{resource}/testIamPermissions
/// Returns permissions that a caller has on the specified resource.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_routes_test_iam_permissions_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_routes_test_iam_permissions_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_routes_test_iam_permissions_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<TestPermissionsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: TestPermissionsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/global/routes/{resource}/testIamPermissions
/// Returns permissions that a caller has on the specified resource.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_routes_test_iam_permissions_builder()` + `compute_routes_test_iam_permissions_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_routes_test_iam_permissions(
    client: &SimpleHttpClient,
    project: &str,
    resource: &str,
    body: &TestPermissionsRequest,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<TestPermissionsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = compute_routes_test_iam_permissions_builder(client, project, resource, body)?;
    compute_routes_test_iam_permissions_execute(builder)
}

/// GET projects/{project}/global/securityPolicies/{securityPolicy}/addRule
/// Inserts a rule into a security policy.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_security_policies_add_rule_execute()` to send, or `compute_security_policies_add_rule` for simplest API.

pub fn compute_security_policies_add_rule_builder(
    client: &SimpleHttpClient,
    project: &str,
    securityPolicy: &str,
    validateOnly: Option<bool>,
    body: &SecurityPolicyRule,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/global/securityPolicies/{}/addRule",
        project, securityPolicy,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "validateOnly",
            validateOnly
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET projects/{project}/global/securityPolicies/{securityPolicy}/addRule
/// Inserts a rule into a security policy.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_security_policies_add_rule_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_security_policies_add_rule_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_security_policies_add_rule_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/global/securityPolicies/{securityPolicy}/addRule
/// Inserts a rule into a security policy.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_security_policies_add_rule_builder()` + `compute_security_policies_add_rule_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_security_policies_add_rule(
    client: &SimpleHttpClient,
    project: &str,
    securityPolicy: &str,
    validateOnly: Option<bool>,
    body: &SecurityPolicyRule,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_security_policies_add_rule_builder(
        client,
        project,
        securityPolicy,
        validateOnly,
        body,
    )?;
    compute_security_policies_add_rule_execute(builder)
}

/// GET projects/{project}/aggregated/securityPolicies
/// Retrieves the list of all SecurityPolicy resources, regional and global, available to the specified project.  To prevent failure, Google recommends that you set the `returnPartialSuccess` parameter to `true`.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_security_policies_aggregated_list_execute()` to send, or `compute_security_policies_aggregated_list` for simplest API.

pub fn compute_security_policies_aggregated_list_builder(
    client: &SimpleHttpClient,
    project: &str,
    filter: Option<&str>,
    includeAllScopes: Option<bool>,
    maxResults: Option<i32>,
    orderBy: Option<&str>,
    pageToken: Option<&str>,
    returnPartialSuccess: Option<bool>,
    serviceProjectNumber: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/aggregated/securityPolicies",
        project,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "filter",
            filter
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "includeAllScopes",
            includeAllScopes
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "maxResults",
            maxResults
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "orderBy",
            orderBy
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageToken",
            pageToken
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "returnPartialSuccess",
            returnPartialSuccess
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "serviceProjectNumber",
            serviceProjectNumber
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET projects/{project}/aggregated/securityPolicies
/// Retrieves the list of all SecurityPolicy resources, regional and global, available to the specified project.  To prevent failure, Google recommends that you set the `returnPartialSuccess` parameter to `true`.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_security_policies_aggregated_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_security_policies_aggregated_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_security_policies_aggregated_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<SecurityPoliciesAggregatedList>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: SecurityPoliciesAggregatedList = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/aggregated/securityPolicies
/// Retrieves the list of all SecurityPolicy resources, regional and global, available to the specified project.  To prevent failure, Google recommends that you set the `returnPartialSuccess` parameter to `true`.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_security_policies_aggregated_list_builder()` + `compute_security_policies_aggregated_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_security_policies_aggregated_list(
    client: &SimpleHttpClient,
    project: &str,
    filter: Option<&str>,
    includeAllScopes: Option<bool>,
    maxResults: Option<i32>,
    orderBy: Option<&str>,
    pageToken: Option<&str>,
    returnPartialSuccess: Option<bool>,
    serviceProjectNumber: Option<&str>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<SecurityPoliciesAggregatedList>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = compute_security_policies_aggregated_list_builder(
        client,
        project,
        filter,
        includeAllScopes,
        maxResults,
        orderBy,
        pageToken,
        returnPartialSuccess,
        serviceProjectNumber,
    )?;
    compute_security_policies_aggregated_list_execute(builder)
}

/// GET projects/{project}/global/securityPolicies/{securityPolicy}
/// Deletes the specified policy.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_security_policies_delete_execute()` to send, or `compute_security_policies_delete` for simplest API.

pub fn compute_security_policies_delete_builder(
    client: &SimpleHttpClient,
    project: &str,
    securityPolicy: &str,
    requestId: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/global/securityPolicies/{}",
        project, securityPolicy,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "requestId",
            requestId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET projects/{project}/global/securityPolicies/{securityPolicy}
/// Deletes the specified policy.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_security_policies_delete_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_security_policies_delete_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_security_policies_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/global/securityPolicies/{securityPolicy}
/// Deletes the specified policy.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_security_policies_delete_builder()` + `compute_security_policies_delete_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_security_policies_delete(
    client: &SimpleHttpClient,
    project: &str,
    securityPolicy: &str,
    requestId: Option<&str>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        compute_security_policies_delete_builder(client, project, securityPolicy, requestId)?;
    compute_security_policies_delete_execute(builder)
}

/// GET projects/{project}/global/securityPolicies/{securityPolicy}
/// List all of the ordered rules present in a single specified policy.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_security_policies_get_execute()` to send, or `compute_security_policies_get` for simplest API.

pub fn compute_security_policies_get_builder(
    client: &SimpleHttpClient,
    project: &str,
    securityPolicy: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/global/securityPolicies/{}",
        project, securityPolicy,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/global/securityPolicies/{securityPolicy}
/// List all of the ordered rules present in a single specified policy.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_security_policies_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_security_policies_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_security_policies_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SecurityPolicy>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: SecurityPolicy = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/global/securityPolicies/{securityPolicy}
/// List all of the ordered rules present in a single specified policy.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_security_policies_get_builder()` + `compute_security_policies_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_security_policies_get(
    client: &SimpleHttpClient,
    project: &str,
    securityPolicy: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SecurityPolicy>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = compute_security_policies_get_builder(client, project, securityPolicy)?;
    compute_security_policies_get_execute(builder)
}

/// GET projects/{project}/global/securityPolicies/{securityPolicy}/getRule
/// Gets a rule at the specified priority.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_security_policies_get_rule_execute()` to send, or `compute_security_policies_get_rule` for simplest API.

pub fn compute_security_policies_get_rule_builder(
    client: &SimpleHttpClient,
    project: &str,
    securityPolicy: &str,
    priority: Option<i32>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/global/securityPolicies/{}/getRule",
        project, securityPolicy,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "priority",
            priority
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET projects/{project}/global/securityPolicies/{securityPolicy}/getRule
/// Gets a rule at the specified priority.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_security_policies_get_rule_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_security_policies_get_rule_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_security_policies_get_rule_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SecurityPolicyRule>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: SecurityPolicyRule = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/global/securityPolicies/{securityPolicy}/getRule
/// Gets a rule at the specified priority.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_security_policies_get_rule_builder()` + `compute_security_policies_get_rule_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_security_policies_get_rule(
    client: &SimpleHttpClient,
    project: &str,
    securityPolicy: &str,
    priority: Option<i32>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SecurityPolicyRule>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        compute_security_policies_get_rule_builder(client, project, securityPolicy, priority)?;
    compute_security_policies_get_rule_execute(builder)
}

/// GET projects/{project}/global/securityPolicies
/// Creates a new policy in the specified project using the data included in the request.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_security_policies_insert_execute()` to send, or `compute_security_policies_insert` for simplest API.

pub fn compute_security_policies_insert_builder(
    client: &SimpleHttpClient,
    project: &str,
    requestId: Option<&str>,
    validateOnly: Option<bool>,
    body: &SecurityPolicy,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/global/securityPolicies",
        project,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "requestId",
            requestId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "validateOnly",
            validateOnly
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET projects/{project}/global/securityPolicies
/// Creates a new policy in the specified project using the data included in the request.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_security_policies_insert_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_security_policies_insert_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_security_policies_insert_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/global/securityPolicies
/// Creates a new policy in the specified project using the data included in the request.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_security_policies_insert_builder()` + `compute_security_policies_insert_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_security_policies_insert(
    client: &SimpleHttpClient,
    project: &str,
    requestId: Option<&str>,
    validateOnly: Option<bool>,
    body: &SecurityPolicy,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        compute_security_policies_insert_builder(client, project, requestId, validateOnly, body)?;
    compute_security_policies_insert_execute(builder)
}

/// GET projects/{project}/global/securityPolicies
/// List all the policies that have been configured for the specified project.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_security_policies_list_execute()` to send, or `compute_security_policies_list` for simplest API.

pub fn compute_security_policies_list_builder(
    client: &SimpleHttpClient,
    project: &str,
    filter: Option<&str>,
    maxResults: Option<i32>,
    orderBy: Option<&str>,
    pageToken: Option<&str>,
    returnPartialSuccess: Option<bool>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/global/securityPolicies",
        project,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "filter",
            filter
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "maxResults",
            maxResults
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "orderBy",
            orderBy
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageToken",
            pageToken
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "returnPartialSuccess",
            returnPartialSuccess
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET projects/{project}/global/securityPolicies
/// List all the policies that have been configured for the specified project.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_security_policies_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_security_policies_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_security_policies_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SecurityPolicyList>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: SecurityPolicyList = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/global/securityPolicies
/// List all the policies that have been configured for the specified project.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_security_policies_list_builder()` + `compute_security_policies_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_security_policies_list(
    client: &SimpleHttpClient,
    project: &str,
    filter: Option<&str>,
    maxResults: Option<i32>,
    orderBy: Option<&str>,
    pageToken: Option<&str>,
    returnPartialSuccess: Option<bool>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SecurityPolicyList>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = compute_security_policies_list_builder(
        client,
        project,
        filter,
        maxResults,
        orderBy,
        pageToken,
        returnPartialSuccess,
    )?;
    compute_security_policies_list_execute(builder)
}

/// GET projects/{project}/global/securityPolicies/listPreconfiguredExpressionSets
/// Gets the current list of preconfigured Web Application Firewall (WAF) expressions.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_security_policies_list_preconfigured_expression_sets_execute()` to send, or `compute_security_policies_list_preconfigured_expression_sets` for simplest API.

pub fn compute_security_policies_list_preconfigured_expression_sets_builder(
    client: &SimpleHttpClient,
    project: &str,
    filter: Option<&str>,
    maxResults: Option<i32>,
    orderBy: Option<&str>,
    pageToken: Option<&str>,
    returnPartialSuccess: Option<bool>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/global/securityPolicies/listPreconfiguredExpressionSets",
        project,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "filter",
            filter
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "maxResults",
            maxResults
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "orderBy",
            orderBy
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageToken",
            pageToken
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "returnPartialSuccess",
            returnPartialSuccess
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET projects/{project}/global/securityPolicies/listPreconfiguredExpressionSets
/// Gets the current list of preconfigured Web Application Firewall (WAF) expressions.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_security_policies_list_preconfigured_expression_sets_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_security_policies_list_preconfigured_expression_sets_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_security_policies_list_preconfigured_expression_sets_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<SecurityPoliciesListPreconfiguredExpressionSetsResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: SecurityPoliciesListPreconfiguredExpressionSetsResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/global/securityPolicies/listPreconfiguredExpressionSets
/// Gets the current list of preconfigured Web Application Firewall (WAF) expressions.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_security_policies_list_preconfigured_expression_sets_builder()` + `compute_security_policies_list_preconfigured_expression_sets_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_security_policies_list_preconfigured_expression_sets(
    client: &SimpleHttpClient,
    project: &str,
    filter: Option<&str>,
    maxResults: Option<i32>,
    orderBy: Option<&str>,
    pageToken: Option<&str>,
    returnPartialSuccess: Option<bool>,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<SecurityPoliciesListPreconfiguredExpressionSetsResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = compute_security_policies_list_preconfigured_expression_sets_builder(
        client,
        project,
        filter,
        maxResults,
        orderBy,
        pageToken,
        returnPartialSuccess,
    )?;
    compute_security_policies_list_preconfigured_expression_sets_execute(builder)
}

/// GET projects/{project}/global/securityPolicies/{securityPolicy}
/// Patches the specified policy with the data included in the request. To clear fields in the policy, leave the fields empty and specify them in the `updateMask`. This cannot be used to be update the rules in the policy. Please use the per rule methods like `addRule`, `patchRule`, and `removeRule` instead.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_security_policies_patch_execute()` to send, or `compute_security_policies_patch` for simplest API.

pub fn compute_security_policies_patch_builder(
    client: &SimpleHttpClient,
    project: &str,
    securityPolicy: &str,
    requestId: Option<&str>,
    updateMask: Option<&str>,
    body: &SecurityPolicy,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/global/securityPolicies/{}",
        project, securityPolicy,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "requestId",
            requestId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "updateMask",
            updateMask
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET projects/{project}/global/securityPolicies/{securityPolicy}
/// Patches the specified policy with the data included in the request. To clear fields in the policy, leave the fields empty and specify them in the `updateMask`. This cannot be used to be update the rules in the policy. Please use the per rule methods like `addRule`, `patchRule`, and `removeRule` instead.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_security_policies_patch_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_security_policies_patch_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_security_policies_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/global/securityPolicies/{securityPolicy}
/// Patches the specified policy with the data included in the request. To clear fields in the policy, leave the fields empty and specify them in the `updateMask`. This cannot be used to be update the rules in the policy. Please use the per rule methods like `addRule`, `patchRule`, and `removeRule` instead.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_security_policies_patch_builder()` + `compute_security_policies_patch_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_security_policies_patch(
    client: &SimpleHttpClient,
    project: &str,
    securityPolicy: &str,
    requestId: Option<&str>,
    updateMask: Option<&str>,
    body: &SecurityPolicy,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_security_policies_patch_builder(
        client,
        project,
        securityPolicy,
        requestId,
        updateMask,
        body,
    )?;
    compute_security_policies_patch_execute(builder)
}

/// GET projects/{project}/global/securityPolicies/{securityPolicy}/patchRule
/// Patches a rule at the specified priority. To clear fields in the rule, leave the fields empty and specify them in the `updateMask`.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_security_policies_patch_rule_execute()` to send, or `compute_security_policies_patch_rule` for simplest API.

pub fn compute_security_policies_patch_rule_builder(
    client: &SimpleHttpClient,
    project: &str,
    securityPolicy: &str,
    priority: Option<i32>,
    updateMask: Option<&str>,
    validateOnly: Option<bool>,
    body: &SecurityPolicyRule,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/global/securityPolicies/{}/patchRule",
        project, securityPolicy,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "priority",
            priority
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "updateMask",
            updateMask
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "validateOnly",
            validateOnly
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET projects/{project}/global/securityPolicies/{securityPolicy}/patchRule
/// Patches a rule at the specified priority. To clear fields in the rule, leave the fields empty and specify them in the `updateMask`.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_security_policies_patch_rule_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_security_policies_patch_rule_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_security_policies_patch_rule_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/global/securityPolicies/{securityPolicy}/patchRule
/// Patches a rule at the specified priority. To clear fields in the rule, leave the fields empty and specify them in the `updateMask`.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_security_policies_patch_rule_builder()` + `compute_security_policies_patch_rule_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_security_policies_patch_rule(
    client: &SimpleHttpClient,
    project: &str,
    securityPolicy: &str,
    priority: Option<i32>,
    updateMask: Option<&str>,
    validateOnly: Option<bool>,
    body: &SecurityPolicyRule,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_security_policies_patch_rule_builder(
        client,
        project,
        securityPolicy,
        priority,
        updateMask,
        validateOnly,
        body,
    )?;
    compute_security_policies_patch_rule_execute(builder)
}

/// GET projects/{project}/global/securityPolicies/{securityPolicy}/removeRule
/// Deletes a rule at the specified priority.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_security_policies_remove_rule_execute()` to send, or `compute_security_policies_remove_rule` for simplest API.

pub fn compute_security_policies_remove_rule_builder(
    client: &SimpleHttpClient,
    project: &str,
    securityPolicy: &str,
    priority: Option<i32>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/global/securityPolicies/{}/removeRule",
        project, securityPolicy,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "priority",
            priority
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET projects/{project}/global/securityPolicies/{securityPolicy}/removeRule
/// Deletes a rule at the specified priority.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_security_policies_remove_rule_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_security_policies_remove_rule_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_security_policies_remove_rule_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/global/securityPolicies/{securityPolicy}/removeRule
/// Deletes a rule at the specified priority.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_security_policies_remove_rule_builder()` + `compute_security_policies_remove_rule_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_security_policies_remove_rule(
    client: &SimpleHttpClient,
    project: &str,
    securityPolicy: &str,
    priority: Option<i32>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        compute_security_policies_remove_rule_builder(client, project, securityPolicy, priority)?;
    compute_security_policies_remove_rule_execute(builder)
}

/// GET projects/{project}/global/securityPolicies/{resource}/setLabels
/// Sets the labels on a security policy. To learn more about labels, read the Labeling Resources documentation.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_security_policies_set_labels_execute()` to send, or `compute_security_policies_set_labels` for simplest API.

pub fn compute_security_policies_set_labels_builder(
    client: &SimpleHttpClient,
    project: &str,
    resource: &str,
    body: &GlobalSetLabelsRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/global/securityPolicies/{}/setLabels",
        project, resource,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET projects/{project}/global/securityPolicies/{resource}/setLabels
/// Sets the labels on a security policy. To learn more about labels, read the Labeling Resources documentation.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_security_policies_set_labels_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_security_policies_set_labels_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_security_policies_set_labels_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/global/securityPolicies/{resource}/setLabels
/// Sets the labels on a security policy. To learn more about labels, read the Labeling Resources documentation.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_security_policies_set_labels_builder()` + `compute_security_policies_set_labels_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_security_policies_set_labels(
    client: &SimpleHttpClient,
    project: &str,
    resource: &str,
    body: &GlobalSetLabelsRequest,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_security_policies_set_labels_builder(client, project, resource, body)?;
    compute_security_policies_set_labels_execute(builder)
}

/// GET projects/{project}/aggregated/serviceAttachments
/// Retrieves the list of all ServiceAttachment resources, regional and global, available to the specified project.  To prevent failure, Google recommends that you set the `returnPartialSuccess` parameter to `true`.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_service_attachments_aggregated_list_execute()` to send, or `compute_service_attachments_aggregated_list` for simplest API.

pub fn compute_service_attachments_aggregated_list_builder(
    client: &SimpleHttpClient,
    project: &str,
    filter: Option<&str>,
    includeAllScopes: Option<bool>,
    maxResults: Option<i32>,
    orderBy: Option<&str>,
    pageToken: Option<&str>,
    returnPartialSuccess: Option<bool>,
    serviceProjectNumber: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/aggregated/serviceAttachments",
        project,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "filter",
            filter
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "includeAllScopes",
            includeAllScopes
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "maxResults",
            maxResults
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "orderBy",
            orderBy
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageToken",
            pageToken
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "returnPartialSuccess",
            returnPartialSuccess
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "serviceProjectNumber",
            serviceProjectNumber
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET projects/{project}/aggregated/serviceAttachments
/// Retrieves the list of all ServiceAttachment resources, regional and global, available to the specified project.  To prevent failure, Google recommends that you set the `returnPartialSuccess` parameter to `true`.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_service_attachments_aggregated_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_service_attachments_aggregated_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_service_attachments_aggregated_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ServiceAttachmentAggregatedList>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ServiceAttachmentAggregatedList = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/aggregated/serviceAttachments
/// Retrieves the list of all ServiceAttachment resources, regional and global, available to the specified project.  To prevent failure, Google recommends that you set the `returnPartialSuccess` parameter to `true`.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_service_attachments_aggregated_list_builder()` + `compute_service_attachments_aggregated_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_service_attachments_aggregated_list(
    client: &SimpleHttpClient,
    project: &str,
    filter: Option<&str>,
    includeAllScopes: Option<bool>,
    maxResults: Option<i32>,
    orderBy: Option<&str>,
    pageToken: Option<&str>,
    returnPartialSuccess: Option<bool>,
    serviceProjectNumber: Option<&str>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ServiceAttachmentAggregatedList>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = compute_service_attachments_aggregated_list_builder(
        client,
        project,
        filter,
        includeAllScopes,
        maxResults,
        orderBy,
        pageToken,
        returnPartialSuccess,
        serviceProjectNumber,
    )?;
    compute_service_attachments_aggregated_list_execute(builder)
}

/// GET projects/{project}/regions/{region}/serviceAttachments/{serviceAttachment}
/// Deletes the specified ServiceAttachment in the given scope
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_service_attachments_delete_execute()` to send, or `compute_service_attachments_delete` for simplest API.

pub fn compute_service_attachments_delete_builder(
    client: &SimpleHttpClient,
    project: &str,
    region: &str,
    serviceAttachment: &str,
    requestId: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/regions/{}/serviceAttachments/{}",
        project, region, serviceAttachment,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "requestId",
            requestId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET projects/{project}/regions/{region}/serviceAttachments/{serviceAttachment}
/// Deletes the specified ServiceAttachment in the given scope
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_service_attachments_delete_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_service_attachments_delete_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_service_attachments_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/regions/{region}/serviceAttachments/{serviceAttachment}
/// Deletes the specified ServiceAttachment in the given scope
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_service_attachments_delete_builder()` + `compute_service_attachments_delete_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_service_attachments_delete(
    client: &SimpleHttpClient,
    project: &str,
    region: &str,
    serviceAttachment: &str,
    requestId: Option<&str>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_service_attachments_delete_builder(
        client,
        project,
        region,
        serviceAttachment,
        requestId,
    )?;
    compute_service_attachments_delete_execute(builder)
}

/// GET projects/{project}/regions/{region}/serviceAttachments/{serviceAttachment}
/// Returns the specified ServiceAttachment resource in the given scope.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_service_attachments_get_execute()` to send, or `compute_service_attachments_get` for simplest API.

pub fn compute_service_attachments_get_builder(
    client: &SimpleHttpClient,
    project: &str,
    region: &str,
    serviceAttachment: &str,
    showNatIps: Option<bool>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/regions/{}/serviceAttachments/{}",
        project, region, serviceAttachment,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "showNatIps",
            showNatIps
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET projects/{project}/regions/{region}/serviceAttachments/{serviceAttachment}
/// Returns the specified ServiceAttachment resource in the given scope.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_service_attachments_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_service_attachments_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_service_attachments_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ServiceAttachment>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ServiceAttachment = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/regions/{region}/serviceAttachments/{serviceAttachment}
/// Returns the specified ServiceAttachment resource in the given scope.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_service_attachments_get_builder()` + `compute_service_attachments_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_service_attachments_get(
    client: &SimpleHttpClient,
    project: &str,
    region: &str,
    serviceAttachment: &str,
    showNatIps: Option<bool>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ServiceAttachment>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = compute_service_attachments_get_builder(
        client,
        project,
        region,
        serviceAttachment,
        showNatIps,
    )?;
    compute_service_attachments_get_execute(builder)
}

/// GET projects/{project}/regions/{region}/serviceAttachments/{resource}/getIamPolicy
/// Gets the access control policy for a resource. May be empty if no such policy or resource exists.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_service_attachments_get_iam_policy_execute()` to send, or `compute_service_attachments_get_iam_policy` for simplest API.

pub fn compute_service_attachments_get_iam_policy_builder(
    client: &SimpleHttpClient,
    project: &str,
    region: &str,
    resource: &str,
    optionsRequestedPolicyVersion: Option<i32>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/regions/{}/serviceAttachments/{}/getIamPolicy",
        project, region, resource,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "optionsRequestedPolicyVersion",
            optionsRequestedPolicyVersion
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET projects/{project}/regions/{region}/serviceAttachments/{resource}/getIamPolicy
/// Gets the access control policy for a resource. May be empty if no such policy or resource exists.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_service_attachments_get_iam_policy_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_service_attachments_get_iam_policy_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_service_attachments_get_iam_policy_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Policy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Policy = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/regions/{region}/serviceAttachments/{resource}/getIamPolicy
/// Gets the access control policy for a resource. May be empty if no such policy or resource exists.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_service_attachments_get_iam_policy_builder()` + `compute_service_attachments_get_iam_policy_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_service_attachments_get_iam_policy(
    client: &SimpleHttpClient,
    project: &str,
    region: &str,
    resource: &str,
    optionsRequestedPolicyVersion: Option<i32>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Policy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_service_attachments_get_iam_policy_builder(
        client,
        project,
        region,
        resource,
        optionsRequestedPolicyVersion,
    )?;
    compute_service_attachments_get_iam_policy_execute(builder)
}

/// GET projects/{project}/regions/{region}/serviceAttachments
/// Creates a ServiceAttachment in the specified project in the given scope using the parameters that are included in the request.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_service_attachments_insert_execute()` to send, or `compute_service_attachments_insert` for simplest API.

pub fn compute_service_attachments_insert_builder(
    client: &SimpleHttpClient,
    project: &str,
    region: &str,
    requestId: Option<&str>,
    body: &ServiceAttachment,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/regions/{}/serviceAttachments",
        project, region,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "requestId",
            requestId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET projects/{project}/regions/{region}/serviceAttachments
/// Creates a ServiceAttachment in the specified project in the given scope using the parameters that are included in the request.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_service_attachments_insert_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_service_attachments_insert_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_service_attachments_insert_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/regions/{region}/serviceAttachments
/// Creates a ServiceAttachment in the specified project in the given scope using the parameters that are included in the request.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_service_attachments_insert_builder()` + `compute_service_attachments_insert_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_service_attachments_insert(
    client: &SimpleHttpClient,
    project: &str,
    region: &str,
    requestId: Option<&str>,
    body: &ServiceAttachment,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        compute_service_attachments_insert_builder(client, project, region, requestId, body)?;
    compute_service_attachments_insert_execute(builder)
}

/// GET projects/{project}/regions/{region}/serviceAttachments
/// Lists the ServiceAttachments for a project in the given scope.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_service_attachments_list_execute()` to send, or `compute_service_attachments_list` for simplest API.

pub fn compute_service_attachments_list_builder(
    client: &SimpleHttpClient,
    project: &str,
    region: &str,
    filter: Option<&str>,
    maxResults: Option<i32>,
    orderBy: Option<&str>,
    pageToken: Option<&str>,
    returnPartialSuccess: Option<bool>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/regions/{}/serviceAttachments",
        project, region,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "filter",
            filter
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "maxResults",
            maxResults
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "orderBy",
            orderBy
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageToken",
            pageToken
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "returnPartialSuccess",
            returnPartialSuccess
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET projects/{project}/regions/{region}/serviceAttachments
/// Lists the ServiceAttachments for a project in the given scope.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_service_attachments_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_service_attachments_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_service_attachments_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ServiceAttachmentList>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ServiceAttachmentList = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/regions/{region}/serviceAttachments
/// Lists the ServiceAttachments for a project in the given scope.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_service_attachments_list_builder()` + `compute_service_attachments_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_service_attachments_list(
    client: &SimpleHttpClient,
    project: &str,
    region: &str,
    filter: Option<&str>,
    maxResults: Option<i32>,
    orderBy: Option<&str>,
    pageToken: Option<&str>,
    returnPartialSuccess: Option<bool>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ServiceAttachmentList>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = compute_service_attachments_list_builder(
        client,
        project,
        region,
        filter,
        maxResults,
        orderBy,
        pageToken,
        returnPartialSuccess,
    )?;
    compute_service_attachments_list_execute(builder)
}

/// GET projects/{project}/regions/{region}/serviceAttachments/{serviceAttachment}
/// Patches the specified ServiceAttachment resource with the data included in the request. This method supports PATCH semantics and `usesJSON` merge patch format and processing rules.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_service_attachments_patch_execute()` to send, or `compute_service_attachments_patch` for simplest API.

pub fn compute_service_attachments_patch_builder(
    client: &SimpleHttpClient,
    project: &str,
    region: &str,
    serviceAttachment: &str,
    requestId: Option<&str>,
    body: &ServiceAttachment,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/regions/{}/serviceAttachments/{}",
        project, region, serviceAttachment,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "requestId",
            requestId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET projects/{project}/regions/{region}/serviceAttachments/{serviceAttachment}
/// Patches the specified ServiceAttachment resource with the data included in the request. This method supports PATCH semantics and `usesJSON` merge patch format and processing rules.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_service_attachments_patch_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_service_attachments_patch_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_service_attachments_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/regions/{region}/serviceAttachments/{serviceAttachment}
/// Patches the specified ServiceAttachment resource with the data included in the request. This method supports PATCH semantics and `usesJSON` merge patch format and processing rules.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_service_attachments_patch_builder()` + `compute_service_attachments_patch_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_service_attachments_patch(
    client: &SimpleHttpClient,
    project: &str,
    region: &str,
    serviceAttachment: &str,
    requestId: Option<&str>,
    body: &ServiceAttachment,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_service_attachments_patch_builder(
        client,
        project,
        region,
        serviceAttachment,
        requestId,
        body,
    )?;
    compute_service_attachments_patch_execute(builder)
}

/// GET projects/{project}/regions/{region}/serviceAttachments/{resource}/setIamPolicy
/// Sets the access control policy on the specified resource. Replaces any existing policy.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_service_attachments_set_iam_policy_execute()` to send, or `compute_service_attachments_set_iam_policy` for simplest API.

pub fn compute_service_attachments_set_iam_policy_builder(
    client: &SimpleHttpClient,
    project: &str,
    region: &str,
    resource: &str,
    body: &RegionSetPolicyRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/regions/{}/serviceAttachments/{}/setIamPolicy",
        project, region, resource,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET projects/{project}/regions/{region}/serviceAttachments/{resource}/setIamPolicy
/// Sets the access control policy on the specified resource. Replaces any existing policy.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_service_attachments_set_iam_policy_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_service_attachments_set_iam_policy_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_service_attachments_set_iam_policy_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Policy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Policy = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/regions/{region}/serviceAttachments/{resource}/setIamPolicy
/// Sets the access control policy on the specified resource. Replaces any existing policy.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_service_attachments_set_iam_policy_builder()` + `compute_service_attachments_set_iam_policy_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_service_attachments_set_iam_policy(
    client: &SimpleHttpClient,
    project: &str,
    region: &str,
    resource: &str,
    body: &RegionSetPolicyRequest,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Policy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_service_attachments_set_iam_policy_builder(
        client, project, region, resource, body,
    )?;
    compute_service_attachments_set_iam_policy_execute(builder)
}

/// GET projects/{project}/regions/{region}/serviceAttachments/{resource}/testIamPermissions
/// Returns permissions that a caller has on the specified resource.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_service_attachments_test_iam_permissions_execute()` to send, or `compute_service_attachments_test_iam_permissions` for simplest API.

pub fn compute_service_attachments_test_iam_permissions_builder(
    client: &SimpleHttpClient,
    project: &str,
    region: &str,
    resource: &str,
    body: &TestPermissionsRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/regions/{}/serviceAttachments/{}/testIamPermissions",
        project, region, resource,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET projects/{project}/regions/{region}/serviceAttachments/{resource}/testIamPermissions
/// Returns permissions that a caller has on the specified resource.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_service_attachments_test_iam_permissions_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_service_attachments_test_iam_permissions_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_service_attachments_test_iam_permissions_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<TestPermissionsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: TestPermissionsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/regions/{region}/serviceAttachments/{resource}/testIamPermissions
/// Returns permissions that a caller has on the specified resource.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_service_attachments_test_iam_permissions_builder()` + `compute_service_attachments_test_iam_permissions_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_service_attachments_test_iam_permissions(
    client: &SimpleHttpClient,
    project: &str,
    region: &str,
    resource: &str,
    body: &TestPermissionsRequest,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<TestPermissionsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = compute_service_attachments_test_iam_permissions_builder(
        client, project, region, resource, body,
    )?;
    compute_service_attachments_test_iam_permissions_execute(builder)
}

/// GET projects/{project}/global/snapshotSettings
/// Get snapshot settings.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_snapshot_settings_get_execute()` to send, or `compute_snapshot_settings_get` for simplest API.

pub fn compute_snapshot_settings_get_builder(
    client: &SimpleHttpClient,
    project: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/global/snapshotSettings",
        project,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/global/snapshotSettings
/// Get snapshot settings.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_snapshot_settings_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_snapshot_settings_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_snapshot_settings_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SnapshotSettings>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: SnapshotSettings = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/global/snapshotSettings
/// Get snapshot settings.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_snapshot_settings_get_builder()` + `compute_snapshot_settings_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_snapshot_settings_get(
    client: &SimpleHttpClient,
    project: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SnapshotSettings>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = compute_snapshot_settings_get_builder(client, project)?;
    compute_snapshot_settings_get_execute(builder)
}

/// GET projects/{project}/global/snapshotSettings
/// Patch snapshot settings.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_snapshot_settings_patch_execute()` to send, or `compute_snapshot_settings_patch` for simplest API.

pub fn compute_snapshot_settings_patch_builder(
    client: &SimpleHttpClient,
    project: &str,
    requestId: Option<&str>,
    updateMask: Option<&str>,
    body: &SnapshotSettings,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/global/snapshotSettings",
        project,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "requestId",
            requestId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "updateMask",
            updateMask
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET projects/{project}/global/snapshotSettings
/// Patch snapshot settings.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_snapshot_settings_patch_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_snapshot_settings_patch_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_snapshot_settings_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/global/snapshotSettings
/// Patch snapshot settings.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_snapshot_settings_patch_builder()` + `compute_snapshot_settings_patch_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_snapshot_settings_patch(
    client: &SimpleHttpClient,
    project: &str,
    requestId: Option<&str>,
    updateMask: Option<&str>,
    body: &SnapshotSettings,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        compute_snapshot_settings_patch_builder(client, project, requestId, updateMask, body)?;
    compute_snapshot_settings_patch_execute(builder)
}

/// GET projects/{project}/global/snapshots/{snapshot}
/// Deletes the specified Snapshot resource. Keep in mind that deleting a single snapshot might not necessarily delete all the data on that snapshot. If any data on the snapshot that is marked for deletion is needed for subsequent snapshots, the data will be moved to the next corresponding snapshot.  For more information, `seeDeleting` snapshots.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_snapshots_delete_execute()` to send, or `compute_snapshots_delete` for simplest API.

pub fn compute_snapshots_delete_builder(
    client: &SimpleHttpClient,
    project: &str,
    snapshot: &str,
    requestId: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/global/snapshots/{}",
        project, snapshot,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "requestId",
            requestId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET projects/{project}/global/snapshots/{snapshot}
/// Deletes the specified Snapshot resource. Keep in mind that deleting a single snapshot might not necessarily delete all the data on that snapshot. If any data on the snapshot that is marked for deletion is needed for subsequent snapshots, the data will be moved to the next corresponding snapshot.  For more information, `seeDeleting` snapshots.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_snapshots_delete_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_snapshots_delete_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_snapshots_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/global/snapshots/{snapshot}
/// Deletes the specified Snapshot resource. Keep in mind that deleting a single snapshot might not necessarily delete all the data on that snapshot. If any data on the snapshot that is marked for deletion is needed for subsequent snapshots, the data will be moved to the next corresponding snapshot.  For more information, `seeDeleting` snapshots.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_snapshots_delete_builder()` + `compute_snapshots_delete_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_snapshots_delete(
    client: &SimpleHttpClient,
    project: &str,
    snapshot: &str,
    requestId: Option<&str>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_snapshots_delete_builder(client, project, snapshot, requestId)?;
    compute_snapshots_delete_execute(builder)
}

/// GET projects/{project}/global/snapshots/{snapshot}
/// Returns the specified Snapshot resource.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_snapshots_get_execute()` to send, or `compute_snapshots_get` for simplest API.

pub fn compute_snapshots_get_builder(
    client: &SimpleHttpClient,
    project: &str,
    snapshot: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/global/snapshots/{}",
        project, snapshot,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/global/snapshots/{snapshot}
/// Returns the specified Snapshot resource.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_snapshots_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_snapshots_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_snapshots_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Snapshot>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Snapshot = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/global/snapshots/{snapshot}
/// Returns the specified Snapshot resource.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_snapshots_get_builder()` + `compute_snapshots_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_snapshots_get(
    client: &SimpleHttpClient,
    project: &str,
    snapshot: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Snapshot>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_snapshots_get_builder(client, project, snapshot)?;
    compute_snapshots_get_execute(builder)
}

/// GET projects/{project}/global/snapshots/{resource}/getIamPolicy
/// Gets the access control policy for a resource. May be empty if no such policy or resource exists.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_snapshots_get_iam_policy_execute()` to send, or `compute_snapshots_get_iam_policy` for simplest API.

pub fn compute_snapshots_get_iam_policy_builder(
    client: &SimpleHttpClient,
    project: &str,
    resource: &str,
    optionsRequestedPolicyVersion: Option<i32>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/global/snapshots/{}/getIamPolicy",
        project, resource,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "optionsRequestedPolicyVersion",
            optionsRequestedPolicyVersion
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET projects/{project}/global/snapshots/{resource}/getIamPolicy
/// Gets the access control policy for a resource. May be empty if no such policy or resource exists.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_snapshots_get_iam_policy_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_snapshots_get_iam_policy_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_snapshots_get_iam_policy_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Policy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Policy = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/global/snapshots/{resource}/getIamPolicy
/// Gets the access control policy for a resource. May be empty if no such policy or resource exists.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_snapshots_get_iam_policy_builder()` + `compute_snapshots_get_iam_policy_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_snapshots_get_iam_policy(
    client: &SimpleHttpClient,
    project: &str,
    resource: &str,
    optionsRequestedPolicyVersion: Option<i32>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Policy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_snapshots_get_iam_policy_builder(
        client,
        project,
        resource,
        optionsRequestedPolicyVersion,
    )?;
    compute_snapshots_get_iam_policy_execute(builder)
}

/// GET projects/{project}/global/snapshots
/// Creates a snapshot in the specified project using the data included in the request. For regular snapshot creation, consider using this method instead of disks.`createSnapshot`, as this method supports more features, such as creating snapshots in a project different from the source disk project.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_snapshots_insert_execute()` to send, or `compute_snapshots_insert` for simplest API.

pub fn compute_snapshots_insert_builder(
    client: &SimpleHttpClient,
    project: &str,
    requestId: Option<&str>,
    body: &Snapshot,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/global/snapshots",
        project,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "requestId",
            requestId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET projects/{project}/global/snapshots
/// Creates a snapshot in the specified project using the data included in the request. For regular snapshot creation, consider using this method instead of disks.`createSnapshot`, as this method supports more features, such as creating snapshots in a project different from the source disk project.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_snapshots_insert_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_snapshots_insert_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_snapshots_insert_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/global/snapshots
/// Creates a snapshot in the specified project using the data included in the request. For regular snapshot creation, consider using this method instead of disks.`createSnapshot`, as this method supports more features, such as creating snapshots in a project different from the source disk project.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_snapshots_insert_builder()` + `compute_snapshots_insert_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_snapshots_insert(
    client: &SimpleHttpClient,
    project: &str,
    requestId: Option<&str>,
    body: &Snapshot,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_snapshots_insert_builder(client, project, requestId, body)?;
    compute_snapshots_insert_execute(builder)
}

/// GET projects/{project}/global/snapshots
/// Retrieves the list of Snapshot resources contained within the specified project.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_snapshots_list_execute()` to send, or `compute_snapshots_list` for simplest API.

pub fn compute_snapshots_list_builder(
    client: &SimpleHttpClient,
    project: &str,
    filter: Option<&str>,
    maxResults: Option<i32>,
    orderBy: Option<&str>,
    pageToken: Option<&str>,
    returnPartialSuccess: Option<bool>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/global/snapshots",
        project,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "filter",
            filter
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "maxResults",
            maxResults
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "orderBy",
            orderBy
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageToken",
            pageToken
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "returnPartialSuccess",
            returnPartialSuccess
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET projects/{project}/global/snapshots
/// Retrieves the list of Snapshot resources contained within the specified project.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_snapshots_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_snapshots_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_snapshots_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SnapshotList>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: SnapshotList = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/global/snapshots
/// Retrieves the list of Snapshot resources contained within the specified project.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_snapshots_list_builder()` + `compute_snapshots_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_snapshots_list(
    client: &SimpleHttpClient,
    project: &str,
    filter: Option<&str>,
    maxResults: Option<i32>,
    orderBy: Option<&str>,
    pageToken: Option<&str>,
    returnPartialSuccess: Option<bool>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SnapshotList>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = compute_snapshots_list_builder(
        client,
        project,
        filter,
        maxResults,
        orderBy,
        pageToken,
        returnPartialSuccess,
    )?;
    compute_snapshots_list_execute(builder)
}

/// GET projects/{project}/global/snapshots/{resource}/setIamPolicy
/// Sets the access control policy on the specified resource. Replaces any existing policy.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_snapshots_set_iam_policy_execute()` to send, or `compute_snapshots_set_iam_policy` for simplest API.

pub fn compute_snapshots_set_iam_policy_builder(
    client: &SimpleHttpClient,
    project: &str,
    resource: &str,
    body: &GlobalSetPolicyRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/global/snapshots/{}/setIamPolicy",
        project, resource,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET projects/{project}/global/snapshots/{resource}/setIamPolicy
/// Sets the access control policy on the specified resource. Replaces any existing policy.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_snapshots_set_iam_policy_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_snapshots_set_iam_policy_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_snapshots_set_iam_policy_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Policy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Policy = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/global/snapshots/{resource}/setIamPolicy
/// Sets the access control policy on the specified resource. Replaces any existing policy.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_snapshots_set_iam_policy_builder()` + `compute_snapshots_set_iam_policy_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_snapshots_set_iam_policy(
    client: &SimpleHttpClient,
    project: &str,
    resource: &str,
    body: &GlobalSetPolicyRequest,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Policy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_snapshots_set_iam_policy_builder(client, project, resource, body)?;
    compute_snapshots_set_iam_policy_execute(builder)
}

/// GET projects/{project}/global/snapshots/{resource}/setLabels
/// Sets the labels on a snapshot. To learn more about labels, read `theLabeling` Resources documentation.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_snapshots_set_labels_execute()` to send, or `compute_snapshots_set_labels` for simplest API.

pub fn compute_snapshots_set_labels_builder(
    client: &SimpleHttpClient,
    project: &str,
    resource: &str,
    body: &GlobalSetLabelsRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/global/snapshots/{}/setLabels",
        project, resource,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET projects/{project}/global/snapshots/{resource}/setLabels
/// Sets the labels on a snapshot. To learn more about labels, read `theLabeling` Resources documentation.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_snapshots_set_labels_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_snapshots_set_labels_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_snapshots_set_labels_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/global/snapshots/{resource}/setLabels
/// Sets the labels on a snapshot. To learn more about labels, read `theLabeling` Resources documentation.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_snapshots_set_labels_builder()` + `compute_snapshots_set_labels_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_snapshots_set_labels(
    client: &SimpleHttpClient,
    project: &str,
    resource: &str,
    body: &GlobalSetLabelsRequest,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_snapshots_set_labels_builder(client, project, resource, body)?;
    compute_snapshots_set_labels_execute(builder)
}

/// GET projects/{project}/global/snapshots/{resource}/testIamPermissions
/// Returns permissions that a caller has on the specified resource.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_snapshots_test_iam_permissions_execute()` to send, or `compute_snapshots_test_iam_permissions` for simplest API.

pub fn compute_snapshots_test_iam_permissions_builder(
    client: &SimpleHttpClient,
    project: &str,
    resource: &str,
    body: &TestPermissionsRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/global/snapshots/{}/testIamPermissions",
        project, resource,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET projects/{project}/global/snapshots/{resource}/testIamPermissions
/// Returns permissions that a caller has on the specified resource.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_snapshots_test_iam_permissions_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_snapshots_test_iam_permissions_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_snapshots_test_iam_permissions_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<TestPermissionsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: TestPermissionsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/global/snapshots/{resource}/testIamPermissions
/// Returns permissions that a caller has on the specified resource.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_snapshots_test_iam_permissions_builder()` + `compute_snapshots_test_iam_permissions_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_snapshots_test_iam_permissions(
    client: &SimpleHttpClient,
    project: &str,
    resource: &str,
    body: &TestPermissionsRequest,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<TestPermissionsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = compute_snapshots_test_iam_permissions_builder(client, project, resource, body)?;
    compute_snapshots_test_iam_permissions_execute(builder)
}

/// GET projects/{project}/aggregated/sslCertificates
/// Retrieves the list of all SslCertificate resources, regional and global, available to the specified project.  To prevent failure, Google recommends that you set the `returnPartialSuccess` parameter to `true`.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_ssl_certificates_aggregated_list_execute()` to send, or `compute_ssl_certificates_aggregated_list` for simplest API.

pub fn compute_ssl_certificates_aggregated_list_builder(
    client: &SimpleHttpClient,
    project: &str,
    filter: Option<&str>,
    includeAllScopes: Option<bool>,
    maxResults: Option<i32>,
    orderBy: Option<&str>,
    pageToken: Option<&str>,
    returnPartialSuccess: Option<bool>,
    serviceProjectNumber: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/aggregated/sslCertificates",
        project,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "filter",
            filter
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "includeAllScopes",
            includeAllScopes
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "maxResults",
            maxResults
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "orderBy",
            orderBy
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageToken",
            pageToken
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "returnPartialSuccess",
            returnPartialSuccess
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "serviceProjectNumber",
            serviceProjectNumber
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET projects/{project}/aggregated/sslCertificates
/// Retrieves the list of all SslCertificate resources, regional and global, available to the specified project.  To prevent failure, Google recommends that you set the `returnPartialSuccess` parameter to `true`.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_ssl_certificates_aggregated_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_ssl_certificates_aggregated_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_ssl_certificates_aggregated_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<SslCertificateAggregatedList>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: SslCertificateAggregatedList = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/aggregated/sslCertificates
/// Retrieves the list of all SslCertificate resources, regional and global, available to the specified project.  To prevent failure, Google recommends that you set the `returnPartialSuccess` parameter to `true`.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_ssl_certificates_aggregated_list_builder()` + `compute_ssl_certificates_aggregated_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_ssl_certificates_aggregated_list(
    client: &SimpleHttpClient,
    project: &str,
    filter: Option<&str>,
    includeAllScopes: Option<bool>,
    maxResults: Option<i32>,
    orderBy: Option<&str>,
    pageToken: Option<&str>,
    returnPartialSuccess: Option<bool>,
    serviceProjectNumber: Option<&str>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<SslCertificateAggregatedList>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = compute_ssl_certificates_aggregated_list_builder(
        client,
        project,
        filter,
        includeAllScopes,
        maxResults,
        orderBy,
        pageToken,
        returnPartialSuccess,
        serviceProjectNumber,
    )?;
    compute_ssl_certificates_aggregated_list_execute(builder)
}

/// GET projects/{project}/global/sslCertificates/{sslCertificate}
/// Deletes the specified SslCertificate resource.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_ssl_certificates_delete_execute()` to send, or `compute_ssl_certificates_delete` for simplest API.

pub fn compute_ssl_certificates_delete_builder(
    client: &SimpleHttpClient,
    project: &str,
    sslCertificate: &str,
    requestId: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/global/sslCertificates/{}",
        project, sslCertificate,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "requestId",
            requestId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET projects/{project}/global/sslCertificates/{sslCertificate}
/// Deletes the specified SslCertificate resource.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_ssl_certificates_delete_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_ssl_certificates_delete_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_ssl_certificates_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/global/sslCertificates/{sslCertificate}
/// Deletes the specified SslCertificate resource.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_ssl_certificates_delete_builder()` + `compute_ssl_certificates_delete_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_ssl_certificates_delete(
    client: &SimpleHttpClient,
    project: &str,
    sslCertificate: &str,
    requestId: Option<&str>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        compute_ssl_certificates_delete_builder(client, project, sslCertificate, requestId)?;
    compute_ssl_certificates_delete_execute(builder)
}

/// GET projects/{project}/global/sslCertificates/{sslCertificate}
/// Returns the specified SslCertificate resource.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_ssl_certificates_get_execute()` to send, or `compute_ssl_certificates_get` for simplest API.

pub fn compute_ssl_certificates_get_builder(
    client: &SimpleHttpClient,
    project: &str,
    sslCertificate: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/global/sslCertificates/{}",
        project, sslCertificate,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/global/sslCertificates/{sslCertificate}
/// Returns the specified SslCertificate resource.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_ssl_certificates_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_ssl_certificates_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_ssl_certificates_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SslCertificate>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: SslCertificate = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/global/sslCertificates/{sslCertificate}
/// Returns the specified SslCertificate resource.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_ssl_certificates_get_builder()` + `compute_ssl_certificates_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_ssl_certificates_get(
    client: &SimpleHttpClient,
    project: &str,
    sslCertificate: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SslCertificate>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = compute_ssl_certificates_get_builder(client, project, sslCertificate)?;
    compute_ssl_certificates_get_execute(builder)
}

/// GET projects/{project}/global/sslCertificates
/// Creates a SslCertificate resource in the specified project using the data included in the request.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_ssl_certificates_insert_execute()` to send, or `compute_ssl_certificates_insert` for simplest API.

pub fn compute_ssl_certificates_insert_builder(
    client: &SimpleHttpClient,
    project: &str,
    requestId: Option<&str>,
    body: &SslCertificate,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/global/sslCertificates",
        project,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "requestId",
            requestId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET projects/{project}/global/sslCertificates
/// Creates a SslCertificate resource in the specified project using the data included in the request.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_ssl_certificates_insert_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_ssl_certificates_insert_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_ssl_certificates_insert_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/global/sslCertificates
/// Creates a SslCertificate resource in the specified project using the data included in the request.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_ssl_certificates_insert_builder()` + `compute_ssl_certificates_insert_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_ssl_certificates_insert(
    client: &SimpleHttpClient,
    project: &str,
    requestId: Option<&str>,
    body: &SslCertificate,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_ssl_certificates_insert_builder(client, project, requestId, body)?;
    compute_ssl_certificates_insert_execute(builder)
}

/// GET projects/{project}/global/sslCertificates
/// Retrieves the list of SslCertificate resources available to the specified project.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_ssl_certificates_list_execute()` to send, or `compute_ssl_certificates_list` for simplest API.

pub fn compute_ssl_certificates_list_builder(
    client: &SimpleHttpClient,
    project: &str,
    filter: Option<&str>,
    maxResults: Option<i32>,
    orderBy: Option<&str>,
    pageToken: Option<&str>,
    returnPartialSuccess: Option<bool>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/global/sslCertificates",
        project,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "filter",
            filter
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "maxResults",
            maxResults
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "orderBy",
            orderBy
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageToken",
            pageToken
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "returnPartialSuccess",
            returnPartialSuccess
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET projects/{project}/global/sslCertificates
/// Retrieves the list of SslCertificate resources available to the specified project.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_ssl_certificates_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_ssl_certificates_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_ssl_certificates_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SslCertificateList>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: SslCertificateList = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/global/sslCertificates
/// Retrieves the list of SslCertificate resources available to the specified project.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_ssl_certificates_list_builder()` + `compute_ssl_certificates_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_ssl_certificates_list(
    client: &SimpleHttpClient,
    project: &str,
    filter: Option<&str>,
    maxResults: Option<i32>,
    orderBy: Option<&str>,
    pageToken: Option<&str>,
    returnPartialSuccess: Option<bool>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SslCertificateList>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = compute_ssl_certificates_list_builder(
        client,
        project,
        filter,
        maxResults,
        orderBy,
        pageToken,
        returnPartialSuccess,
    )?;
    compute_ssl_certificates_list_execute(builder)
}

/// GET projects/{project}/aggregated/sslPolicies
/// Retrieves the list of all SslPolicy resources, regional and global, available to the specified project.  To prevent failure, Google recommends that you set the `returnPartialSuccess` parameter to `true`.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_ssl_policies_aggregated_list_execute()` to send, or `compute_ssl_policies_aggregated_list` for simplest API.

pub fn compute_ssl_policies_aggregated_list_builder(
    client: &SimpleHttpClient,
    project: &str,
    filter: Option<&str>,
    includeAllScopes: Option<bool>,
    maxResults: Option<i32>,
    orderBy: Option<&str>,
    pageToken: Option<&str>,
    returnPartialSuccess: Option<bool>,
    serviceProjectNumber: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/aggregated/sslPolicies",
        project,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "filter",
            filter
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "includeAllScopes",
            includeAllScopes
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "maxResults",
            maxResults
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "orderBy",
            orderBy
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageToken",
            pageToken
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "returnPartialSuccess",
            returnPartialSuccess
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "serviceProjectNumber",
            serviceProjectNumber
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET projects/{project}/aggregated/sslPolicies
/// Retrieves the list of all SslPolicy resources, regional and global, available to the specified project.  To prevent failure, Google recommends that you set the `returnPartialSuccess` parameter to `true`.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_ssl_policies_aggregated_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_ssl_policies_aggregated_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_ssl_policies_aggregated_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SslPoliciesAggregatedList>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: SslPoliciesAggregatedList = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/aggregated/sslPolicies
/// Retrieves the list of all SslPolicy resources, regional and global, available to the specified project.  To prevent failure, Google recommends that you set the `returnPartialSuccess` parameter to `true`.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_ssl_policies_aggregated_list_builder()` + `compute_ssl_policies_aggregated_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_ssl_policies_aggregated_list(
    client: &SimpleHttpClient,
    project: &str,
    filter: Option<&str>,
    includeAllScopes: Option<bool>,
    maxResults: Option<i32>,
    orderBy: Option<&str>,
    pageToken: Option<&str>,
    returnPartialSuccess: Option<bool>,
    serviceProjectNumber: Option<&str>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SslPoliciesAggregatedList>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = compute_ssl_policies_aggregated_list_builder(
        client,
        project,
        filter,
        includeAllScopes,
        maxResults,
        orderBy,
        pageToken,
        returnPartialSuccess,
        serviceProjectNumber,
    )?;
    compute_ssl_policies_aggregated_list_execute(builder)
}

/// GET projects/{project}/global/sslPolicies/{sslPolicy}
/// Deletes the specified SSL policy. The SSL policy resource can be deleted only if it is not in use by any TargetHttpsProxy or TargetSslProxy resources.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_ssl_policies_delete_execute()` to send, or `compute_ssl_policies_delete` for simplest API.

pub fn compute_ssl_policies_delete_builder(
    client: &SimpleHttpClient,
    project: &str,
    sslPolicy: &str,
    requestId: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/global/sslPolicies/{}",
        project, sslPolicy,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "requestId",
            requestId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET projects/{project}/global/sslPolicies/{sslPolicy}
/// Deletes the specified SSL policy. The SSL policy resource can be deleted only if it is not in use by any TargetHttpsProxy or TargetSslProxy resources.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_ssl_policies_delete_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_ssl_policies_delete_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_ssl_policies_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/global/sslPolicies/{sslPolicy}
/// Deletes the specified SSL policy. The SSL policy resource can be deleted only if it is not in use by any TargetHttpsProxy or TargetSslProxy resources.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_ssl_policies_delete_builder()` + `compute_ssl_policies_delete_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_ssl_policies_delete(
    client: &SimpleHttpClient,
    project: &str,
    sslPolicy: &str,
    requestId: Option<&str>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_ssl_policies_delete_builder(client, project, sslPolicy, requestId)?;
    compute_ssl_policies_delete_execute(builder)
}

/// GET projects/{project}/global/sslPolicies/{sslPolicy}
/// Lists all of the ordered rules present in a single specified policy.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_ssl_policies_get_execute()` to send, or `compute_ssl_policies_get` for simplest API.

pub fn compute_ssl_policies_get_builder(
    client: &SimpleHttpClient,
    project: &str,
    sslPolicy: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/global/sslPolicies/{}",
        project, sslPolicy,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/global/sslPolicies/{sslPolicy}
/// Lists all of the ordered rules present in a single specified policy.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_ssl_policies_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_ssl_policies_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_ssl_policies_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SslPolicy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: SslPolicy = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/global/sslPolicies/{sslPolicy}
/// Lists all of the ordered rules present in a single specified policy.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_ssl_policies_get_builder()` + `compute_ssl_policies_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_ssl_policies_get(
    client: &SimpleHttpClient,
    project: &str,
    sslPolicy: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SslPolicy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_ssl_policies_get_builder(client, project, sslPolicy)?;
    compute_ssl_policies_get_execute(builder)
}

/// GET projects/{project}/global/sslPolicies
/// Returns the specified SSL policy resource.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_ssl_policies_insert_execute()` to send, or `compute_ssl_policies_insert` for simplest API.

pub fn compute_ssl_policies_insert_builder(
    client: &SimpleHttpClient,
    project: &str,
    requestId: Option<&str>,
    body: &SslPolicy,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/global/sslPolicies",
        project,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "requestId",
            requestId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET projects/{project}/global/sslPolicies
/// Returns the specified SSL policy resource.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_ssl_policies_insert_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_ssl_policies_insert_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_ssl_policies_insert_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/global/sslPolicies
/// Returns the specified SSL policy resource.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_ssl_policies_insert_builder()` + `compute_ssl_policies_insert_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_ssl_policies_insert(
    client: &SimpleHttpClient,
    project: &str,
    requestId: Option<&str>,
    body: &SslPolicy,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_ssl_policies_insert_builder(client, project, requestId, body)?;
    compute_ssl_policies_insert_execute(builder)
}

/// GET projects/{project}/global/sslPolicies
/// Lists all the SSL policies that have been configured for the specified project.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_ssl_policies_list_execute()` to send, or `compute_ssl_policies_list` for simplest API.

pub fn compute_ssl_policies_list_builder(
    client: &SimpleHttpClient,
    project: &str,
    filter: Option<&str>,
    maxResults: Option<i32>,
    orderBy: Option<&str>,
    pageToken: Option<&str>,
    returnPartialSuccess: Option<bool>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/global/sslPolicies",
        project,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "filter",
            filter
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "maxResults",
            maxResults
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "orderBy",
            orderBy
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageToken",
            pageToken
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "returnPartialSuccess",
            returnPartialSuccess
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET projects/{project}/global/sslPolicies
/// Lists all the SSL policies that have been configured for the specified project.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_ssl_policies_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_ssl_policies_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_ssl_policies_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SslPoliciesList>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: SslPoliciesList = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/global/sslPolicies
/// Lists all the SSL policies that have been configured for the specified project.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_ssl_policies_list_builder()` + `compute_ssl_policies_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_ssl_policies_list(
    client: &SimpleHttpClient,
    project: &str,
    filter: Option<&str>,
    maxResults: Option<i32>,
    orderBy: Option<&str>,
    pageToken: Option<&str>,
    returnPartialSuccess: Option<bool>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SslPoliciesList>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = compute_ssl_policies_list_builder(
        client,
        project,
        filter,
        maxResults,
        orderBy,
        pageToken,
        returnPartialSuccess,
    )?;
    compute_ssl_policies_list_execute(builder)
}

/// GET projects/{project}/global/sslPolicies/listAvailableFeatures
/// Lists all features that can be specified in the SSL policy when using custom profile.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_ssl_policies_list_available_features_execute()` to send, or `compute_ssl_policies_list_available_features` for simplest API.

pub fn compute_ssl_policies_list_available_features_builder(
    client: &SimpleHttpClient,
    project: &str,
    filter: Option<&str>,
    maxResults: Option<i32>,
    orderBy: Option<&str>,
    pageToken: Option<&str>,
    returnPartialSuccess: Option<bool>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/global/sslPolicies/listAvailableFeatures",
        project,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "filter",
            filter
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "maxResults",
            maxResults
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "orderBy",
            orderBy
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageToken",
            pageToken
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "returnPartialSuccess",
            returnPartialSuccess
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET projects/{project}/global/sslPolicies/listAvailableFeatures
/// Lists all features that can be specified in the SSL policy when using custom profile.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_ssl_policies_list_available_features_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_ssl_policies_list_available_features_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_ssl_policies_list_available_features_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<SslPoliciesListAvailableFeaturesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: SslPoliciesListAvailableFeaturesResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/global/sslPolicies/listAvailableFeatures
/// Lists all features that can be specified in the SSL policy when using custom profile.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_ssl_policies_list_available_features_builder()` + `compute_ssl_policies_list_available_features_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_ssl_policies_list_available_features(
    client: &SimpleHttpClient,
    project: &str,
    filter: Option<&str>,
    maxResults: Option<i32>,
    orderBy: Option<&str>,
    pageToken: Option<&str>,
    returnPartialSuccess: Option<bool>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<SslPoliciesListAvailableFeaturesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = compute_ssl_policies_list_available_features_builder(
        client,
        project,
        filter,
        maxResults,
        orderBy,
        pageToken,
        returnPartialSuccess,
    )?;
    compute_ssl_policies_list_available_features_execute(builder)
}

/// GET projects/{project}/global/sslPolicies/{sslPolicy}
/// Patches the specified SSL policy with the data included in the request.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_ssl_policies_patch_execute()` to send, or `compute_ssl_policies_patch` for simplest API.

pub fn compute_ssl_policies_patch_builder(
    client: &SimpleHttpClient,
    project: &str,
    sslPolicy: &str,
    requestId: Option<&str>,
    body: &SslPolicy,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/global/sslPolicies/{}",
        project, sslPolicy,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "requestId",
            requestId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET projects/{project}/global/sslPolicies/{sslPolicy}
/// Patches the specified SSL policy with the data included in the request.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_ssl_policies_patch_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_ssl_policies_patch_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_ssl_policies_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/global/sslPolicies/{sslPolicy}
/// Patches the specified SSL policy with the data included in the request.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_ssl_policies_patch_builder()` + `compute_ssl_policies_patch_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_ssl_policies_patch(
    client: &SimpleHttpClient,
    project: &str,
    sslPolicy: &str,
    requestId: Option<&str>,
    body: &SslPolicy,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_ssl_policies_patch_builder(client, project, sslPolicy, requestId, body)?;
    compute_ssl_policies_patch_execute(builder)
}

/// GET projects/{project}/aggregated/storagePoolTypes
/// Retrieves an aggregated list of storage pool types.  To prevent failure, Google recommends that you set the `returnPartialSuccess` parameter to `true`.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_storage_pool_types_aggregated_list_execute()` to send, or `compute_storage_pool_types_aggregated_list` for simplest API.

pub fn compute_storage_pool_types_aggregated_list_builder(
    client: &SimpleHttpClient,
    project: &str,
    filter: Option<&str>,
    includeAllScopes: Option<bool>,
    maxResults: Option<i32>,
    orderBy: Option<&str>,
    pageToken: Option<&str>,
    returnPartialSuccess: Option<bool>,
    serviceProjectNumber: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/aggregated/storagePoolTypes",
        project,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "filter",
            filter
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "includeAllScopes",
            includeAllScopes
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "maxResults",
            maxResults
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "orderBy",
            orderBy
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageToken",
            pageToken
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "returnPartialSuccess",
            returnPartialSuccess
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "serviceProjectNumber",
            serviceProjectNumber
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET projects/{project}/aggregated/storagePoolTypes
/// Retrieves an aggregated list of storage pool types.  To prevent failure, Google recommends that you set the `returnPartialSuccess` parameter to `true`.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_storage_pool_types_aggregated_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_storage_pool_types_aggregated_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_storage_pool_types_aggregated_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<StoragePoolTypeAggregatedList>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: StoragePoolTypeAggregatedList = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/aggregated/storagePoolTypes
/// Retrieves an aggregated list of storage pool types.  To prevent failure, Google recommends that you set the `returnPartialSuccess` parameter to `true`.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_storage_pool_types_aggregated_list_builder()` + `compute_storage_pool_types_aggregated_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_storage_pool_types_aggregated_list(
    client: &SimpleHttpClient,
    project: &str,
    filter: Option<&str>,
    includeAllScopes: Option<bool>,
    maxResults: Option<i32>,
    orderBy: Option<&str>,
    pageToken: Option<&str>,
    returnPartialSuccess: Option<bool>,
    serviceProjectNumber: Option<&str>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<StoragePoolTypeAggregatedList>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = compute_storage_pool_types_aggregated_list_builder(
        client,
        project,
        filter,
        includeAllScopes,
        maxResults,
        orderBy,
        pageToken,
        returnPartialSuccess,
        serviceProjectNumber,
    )?;
    compute_storage_pool_types_aggregated_list_execute(builder)
}

/// GET projects/{project}/zones/{zone}/storagePoolTypes/{storagePoolType}
/// Returns the specified storage pool type.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_storage_pool_types_get_execute()` to send, or `compute_storage_pool_types_get` for simplest API.

pub fn compute_storage_pool_types_get_builder(
    client: &SimpleHttpClient,
    project: &str,
    storagePoolType: &str,
    zone: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/zones/{}/storagePoolTypes/{}",
        project, storagePoolType, zone,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/zones/{zone}/storagePoolTypes/{storagePoolType}
/// Returns the specified storage pool type.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_storage_pool_types_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_storage_pool_types_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_storage_pool_types_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<StoragePoolType>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: StoragePoolType = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/zones/{zone}/storagePoolTypes/{storagePoolType}
/// Returns the specified storage pool type.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_storage_pool_types_get_builder()` + `compute_storage_pool_types_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_storage_pool_types_get(
    client: &SimpleHttpClient,
    project: &str,
    storagePoolType: &str,
    zone: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<StoragePoolType>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = compute_storage_pool_types_get_builder(client, project, storagePoolType, zone)?;
    compute_storage_pool_types_get_execute(builder)
}

/// GET projects/{project}/zones/{zone}/storagePoolTypes
/// Retrieves a list of storage pool types available to the specified project.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_storage_pool_types_list_execute()` to send, or `compute_storage_pool_types_list` for simplest API.

pub fn compute_storage_pool_types_list_builder(
    client: &SimpleHttpClient,
    project: &str,
    zone: &str,
    filter: Option<&str>,
    maxResults: Option<i32>,
    orderBy: Option<&str>,
    pageToken: Option<&str>,
    returnPartialSuccess: Option<bool>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/zones/{}/storagePoolTypes",
        project, zone,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "filter",
            filter
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "maxResults",
            maxResults
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "orderBy",
            orderBy
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageToken",
            pageToken
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "returnPartialSuccess",
            returnPartialSuccess
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET projects/{project}/zones/{zone}/storagePoolTypes
/// Retrieves a list of storage pool types available to the specified project.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_storage_pool_types_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_storage_pool_types_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_storage_pool_types_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<StoragePoolTypeList>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: StoragePoolTypeList = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/zones/{zone}/storagePoolTypes
/// Retrieves a list of storage pool types available to the specified project.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_storage_pool_types_list_builder()` + `compute_storage_pool_types_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_storage_pool_types_list(
    client: &SimpleHttpClient,
    project: &str,
    zone: &str,
    filter: Option<&str>,
    maxResults: Option<i32>,
    orderBy: Option<&str>,
    pageToken: Option<&str>,
    returnPartialSuccess: Option<bool>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<StoragePoolTypeList>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = compute_storage_pool_types_list_builder(
        client,
        project,
        zone,
        filter,
        maxResults,
        orderBy,
        pageToken,
        returnPartialSuccess,
    )?;
    compute_storage_pool_types_list_execute(builder)
}

/// GET projects/{project}/aggregated/storagePools
/// Retrieves an aggregated list of storage pools.  To prevent failure, Google recommends that you set the `returnPartialSuccess` parameter to `true`.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_storage_pools_aggregated_list_execute()` to send, or `compute_storage_pools_aggregated_list` for simplest API.

pub fn compute_storage_pools_aggregated_list_builder(
    client: &SimpleHttpClient,
    project: &str,
    filter: Option<&str>,
    includeAllScopes: Option<bool>,
    maxResults: Option<i32>,
    orderBy: Option<&str>,
    pageToken: Option<&str>,
    returnPartialSuccess: Option<bool>,
    serviceProjectNumber: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/aggregated/storagePools",
        project,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "filter",
            filter
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "includeAllScopes",
            includeAllScopes
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "maxResults",
            maxResults
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "orderBy",
            orderBy
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageToken",
            pageToken
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "returnPartialSuccess",
            returnPartialSuccess
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "serviceProjectNumber",
            serviceProjectNumber
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET projects/{project}/aggregated/storagePools
/// Retrieves an aggregated list of storage pools.  To prevent failure, Google recommends that you set the `returnPartialSuccess` parameter to `true`.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_storage_pools_aggregated_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_storage_pools_aggregated_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_storage_pools_aggregated_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<StoragePoolAggregatedList>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: StoragePoolAggregatedList = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/aggregated/storagePools
/// Retrieves an aggregated list of storage pools.  To prevent failure, Google recommends that you set the `returnPartialSuccess` parameter to `true`.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_storage_pools_aggregated_list_builder()` + `compute_storage_pools_aggregated_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_storage_pools_aggregated_list(
    client: &SimpleHttpClient,
    project: &str,
    filter: Option<&str>,
    includeAllScopes: Option<bool>,
    maxResults: Option<i32>,
    orderBy: Option<&str>,
    pageToken: Option<&str>,
    returnPartialSuccess: Option<bool>,
    serviceProjectNumber: Option<&str>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<StoragePoolAggregatedList>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = compute_storage_pools_aggregated_list_builder(
        client,
        project,
        filter,
        includeAllScopes,
        maxResults,
        orderBy,
        pageToken,
        returnPartialSuccess,
        serviceProjectNumber,
    )?;
    compute_storage_pools_aggregated_list_execute(builder)
}

/// GET projects/{project}/zones/{zone}/storagePools/{storagePool}
/// Deletes the specified storage pool. Deleting a `storagePool` removes its data permanently and is irreversible. However, deleting a `storagePool` does not delete any snapshots previously made from the `storagePool`. You must separately delete snapshots.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_storage_pools_delete_execute()` to send, or `compute_storage_pools_delete` for simplest API.

pub fn compute_storage_pools_delete_builder(
    client: &SimpleHttpClient,
    project: &str,
    storagePool: &str,
    zone: &str,
    requestId: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/zones/{}/storagePools/{}",
        project, storagePool, zone,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "requestId",
            requestId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET projects/{project}/zones/{zone}/storagePools/{storagePool}
/// Deletes the specified storage pool. Deleting a `storagePool` removes its data permanently and is irreversible. However, deleting a `storagePool` does not delete any snapshots previously made from the `storagePool`. You must separately delete snapshots.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_storage_pools_delete_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_storage_pools_delete_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_storage_pools_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/zones/{zone}/storagePools/{storagePool}
/// Deletes the specified storage pool. Deleting a `storagePool` removes its data permanently and is irreversible. However, deleting a `storagePool` does not delete any snapshots previously made from the `storagePool`. You must separately delete snapshots.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_storage_pools_delete_builder()` + `compute_storage_pools_delete_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_storage_pools_delete(
    client: &SimpleHttpClient,
    project: &str,
    storagePool: &str,
    zone: &str,
    requestId: Option<&str>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        compute_storage_pools_delete_builder(client, project, storagePool, zone, requestId)?;
    compute_storage_pools_delete_execute(builder)
}

/// GET projects/{project}/zones/{zone}/storagePools/{storagePool}
/// Returns a specified storage pool. Gets a list of available storage pools by making a list() request.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_storage_pools_get_execute()` to send, or `compute_storage_pools_get` for simplest API.

pub fn compute_storage_pools_get_builder(
    client: &SimpleHttpClient,
    project: &str,
    storagePool: &str,
    zone: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/zones/{}/storagePools/{}",
        project, storagePool, zone,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/zones/{zone}/storagePools/{storagePool}
/// Returns a specified storage pool. Gets a list of available storage pools by making a list() request.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_storage_pools_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_storage_pools_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_storage_pools_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<StoragePool>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: StoragePool = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/zones/{zone}/storagePools/{storagePool}
/// Returns a specified storage pool. Gets a list of available storage pools by making a list() request.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_storage_pools_get_builder()` + `compute_storage_pools_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_storage_pools_get(
    client: &SimpleHttpClient,
    project: &str,
    storagePool: &str,
    zone: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<StoragePool>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_storage_pools_get_builder(client, project, storagePool, zone)?;
    compute_storage_pools_get_execute(builder)
}

/// GET projects/{project}/zones/{zone}/storagePools/{resource}/getIamPolicy
/// Gets the access control policy for a resource. May be empty if no such policy or resource exists.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_storage_pools_get_iam_policy_execute()` to send, or `compute_storage_pools_get_iam_policy` for simplest API.

pub fn compute_storage_pools_get_iam_policy_builder(
    client: &SimpleHttpClient,
    project: &str,
    resource: &str,
    zone: &str,
    optionsRequestedPolicyVersion: Option<i32>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/zones/{}/storagePools/{}/getIamPolicy",
        project, resource, zone,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "optionsRequestedPolicyVersion",
            optionsRequestedPolicyVersion
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET projects/{project}/zones/{zone}/storagePools/{resource}/getIamPolicy
/// Gets the access control policy for a resource. May be empty if no such policy or resource exists.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_storage_pools_get_iam_policy_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_storage_pools_get_iam_policy_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_storage_pools_get_iam_policy_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Policy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Policy = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/zones/{zone}/storagePools/{resource}/getIamPolicy
/// Gets the access control policy for a resource. May be empty if no such policy or resource exists.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_storage_pools_get_iam_policy_builder()` + `compute_storage_pools_get_iam_policy_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_storage_pools_get_iam_policy(
    client: &SimpleHttpClient,
    project: &str,
    resource: &str,
    zone: &str,
    optionsRequestedPolicyVersion: Option<i32>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Policy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_storage_pools_get_iam_policy_builder(
        client,
        project,
        resource,
        zone,
        optionsRequestedPolicyVersion,
    )?;
    compute_storage_pools_get_iam_policy_execute(builder)
}

/// GET projects/{project}/zones/{zone}/storagePools
/// Creates a storage pool in the specified project using the data in the request.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_storage_pools_insert_execute()` to send, or `compute_storage_pools_insert` for simplest API.

pub fn compute_storage_pools_insert_builder(
    client: &SimpleHttpClient,
    project: &str,
    zone: &str,
    requestId: Option<&str>,
    body: &StoragePool,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/zones/{}/storagePools",
        project, zone,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "requestId",
            requestId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET projects/{project}/zones/{zone}/storagePools
/// Creates a storage pool in the specified project using the data in the request.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_storage_pools_insert_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_storage_pools_insert_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_storage_pools_insert_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/zones/{zone}/storagePools
/// Creates a storage pool in the specified project using the data in the request.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_storage_pools_insert_builder()` + `compute_storage_pools_insert_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_storage_pools_insert(
    client: &SimpleHttpClient,
    project: &str,
    zone: &str,
    requestId: Option<&str>,
    body: &StoragePool,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_storage_pools_insert_builder(client, project, zone, requestId, body)?;
    compute_storage_pools_insert_execute(builder)
}

/// GET projects/{project}/zones/{zone}/storagePools
/// Retrieves a list of storage pools contained within the specified zone.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_storage_pools_list_execute()` to send, or `compute_storage_pools_list` for simplest API.

pub fn compute_storage_pools_list_builder(
    client: &SimpleHttpClient,
    project: &str,
    zone: &str,
    filter: Option<&str>,
    maxResults: Option<i32>,
    orderBy: Option<&str>,
    pageToken: Option<&str>,
    returnPartialSuccess: Option<bool>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/zones/{}/storagePools",
        project, zone,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "filter",
            filter
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "maxResults",
            maxResults
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "orderBy",
            orderBy
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageToken",
            pageToken
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "returnPartialSuccess",
            returnPartialSuccess
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET projects/{project}/zones/{zone}/storagePools
/// Retrieves a list of storage pools contained within the specified zone.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_storage_pools_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_storage_pools_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_storage_pools_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<StoragePoolList>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: StoragePoolList = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/zones/{zone}/storagePools
/// Retrieves a list of storage pools contained within the specified zone.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_storage_pools_list_builder()` + `compute_storage_pools_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_storage_pools_list(
    client: &SimpleHttpClient,
    project: &str,
    zone: &str,
    filter: Option<&str>,
    maxResults: Option<i32>,
    orderBy: Option<&str>,
    pageToken: Option<&str>,
    returnPartialSuccess: Option<bool>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<StoragePoolList>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = compute_storage_pools_list_builder(
        client,
        project,
        zone,
        filter,
        maxResults,
        orderBy,
        pageToken,
        returnPartialSuccess,
    )?;
    compute_storage_pools_list_execute(builder)
}

/// GET projects/{project}/zones/{zone}/storagePools/{storagePool}/listDisks
/// Lists the disks in a specified storage pool.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_storage_pools_list_disks_execute()` to send, or `compute_storage_pools_list_disks` for simplest API.

pub fn compute_storage_pools_list_disks_builder(
    client: &SimpleHttpClient,
    project: &str,
    storagePool: &str,
    zone: &str,
    filter: Option<&str>,
    maxResults: Option<i32>,
    orderBy: Option<&str>,
    pageToken: Option<&str>,
    returnPartialSuccess: Option<bool>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/zones/{}/storagePools/{}/listDisks",
        project, storagePool, zone,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "filter",
            filter
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "maxResults",
            maxResults
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "orderBy",
            orderBy
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageToken",
            pageToken
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "returnPartialSuccess",
            returnPartialSuccess
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET projects/{project}/zones/{zone}/storagePools/{storagePool}/listDisks
/// Lists the disks in a specified storage pool.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_storage_pools_list_disks_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_storage_pools_list_disks_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_storage_pools_list_disks_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<StoragePoolListDisks>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: StoragePoolListDisks = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/zones/{zone}/storagePools/{storagePool}/listDisks
/// Lists the disks in a specified storage pool.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_storage_pools_list_disks_builder()` + `compute_storage_pools_list_disks_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_storage_pools_list_disks(
    client: &SimpleHttpClient,
    project: &str,
    storagePool: &str,
    zone: &str,
    filter: Option<&str>,
    maxResults: Option<i32>,
    orderBy: Option<&str>,
    pageToken: Option<&str>,
    returnPartialSuccess: Option<bool>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<StoragePoolListDisks>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = compute_storage_pools_list_disks_builder(
        client,
        project,
        storagePool,
        zone,
        filter,
        maxResults,
        orderBy,
        pageToken,
        returnPartialSuccess,
    )?;
    compute_storage_pools_list_disks_execute(builder)
}

/// GET projects/{project}/zones/{zone}/storagePools/{resource}/setIamPolicy
/// Sets the access control policy on the specified resource. Replaces any existing policy.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_storage_pools_set_iam_policy_execute()` to send, or `compute_storage_pools_set_iam_policy` for simplest API.

pub fn compute_storage_pools_set_iam_policy_builder(
    client: &SimpleHttpClient,
    project: &str,
    resource: &str,
    zone: &str,
    body: &ZoneSetPolicyRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/zones/{}/storagePools/{}/setIamPolicy",
        project, resource, zone,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET projects/{project}/zones/{zone}/storagePools/{resource}/setIamPolicy
/// Sets the access control policy on the specified resource. Replaces any existing policy.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_storage_pools_set_iam_policy_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_storage_pools_set_iam_policy_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_storage_pools_set_iam_policy_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Policy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Policy = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/zones/{zone}/storagePools/{resource}/setIamPolicy
/// Sets the access control policy on the specified resource. Replaces any existing policy.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_storage_pools_set_iam_policy_builder()` + `compute_storage_pools_set_iam_policy_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_storage_pools_set_iam_policy(
    client: &SimpleHttpClient,
    project: &str,
    resource: &str,
    zone: &str,
    body: &ZoneSetPolicyRequest,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Policy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        compute_storage_pools_set_iam_policy_builder(client, project, resource, zone, body)?;
    compute_storage_pools_set_iam_policy_execute(builder)
}

/// GET projects/{project}/zones/{zone}/storagePools/{resource}/testIamPermissions
/// Returns permissions that a caller has on the specified resource.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_storage_pools_test_iam_permissions_execute()` to send, or `compute_storage_pools_test_iam_permissions` for simplest API.

pub fn compute_storage_pools_test_iam_permissions_builder(
    client: &SimpleHttpClient,
    project: &str,
    resource: &str,
    zone: &str,
    body: &TestPermissionsRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/zones/{}/storagePools/{}/testIamPermissions",
        project, resource, zone,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET projects/{project}/zones/{zone}/storagePools/{resource}/testIamPermissions
/// Returns permissions that a caller has on the specified resource.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_storage_pools_test_iam_permissions_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_storage_pools_test_iam_permissions_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_storage_pools_test_iam_permissions_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<TestPermissionsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: TestPermissionsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/zones/{zone}/storagePools/{resource}/testIamPermissions
/// Returns permissions that a caller has on the specified resource.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_storage_pools_test_iam_permissions_builder()` + `compute_storage_pools_test_iam_permissions_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_storage_pools_test_iam_permissions(
    client: &SimpleHttpClient,
    project: &str,
    resource: &str,
    zone: &str,
    body: &TestPermissionsRequest,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<TestPermissionsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        compute_storage_pools_test_iam_permissions_builder(client, project, resource, zone, body)?;
    compute_storage_pools_test_iam_permissions_execute(builder)
}

/// GET projects/{project}/zones/{zone}/storagePools/{storagePool}
/// Updates the specified `storagePool` with the data included in the request. The update is performed only on selected fields included as part of update-mask. Only the following fields can be modified: pool_provisioned_capacity_gb, pool_provisioned_iops and pool_provisioned_throughput.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_storage_pools_update_execute()` to send, or `compute_storage_pools_update` for simplest API.

pub fn compute_storage_pools_update_builder(
    client: &SimpleHttpClient,
    project: &str,
    storagePool: &str,
    zone: &str,
    requestId: Option<&str>,
    updateMask: Option<&str>,
    body: &StoragePool,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/zones/{}/storagePools/{}",
        project, storagePool, zone,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "requestId",
            requestId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "updateMask",
            updateMask
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET projects/{project}/zones/{zone}/storagePools/{storagePool}
/// Updates the specified `storagePool` with the data included in the request. The update is performed only on selected fields included as part of update-mask. Only the following fields can be modified: pool_provisioned_capacity_gb, pool_provisioned_iops and pool_provisioned_throughput.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_storage_pools_update_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_storage_pools_update_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_storage_pools_update_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/zones/{zone}/storagePools/{storagePool}
/// Updates the specified `storagePool` with the data included in the request. The update is performed only on selected fields included as part of update-mask. Only the following fields can be modified: pool_provisioned_capacity_gb, pool_provisioned_iops and pool_provisioned_throughput.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_storage_pools_update_builder()` + `compute_storage_pools_update_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_storage_pools_update(
    client: &SimpleHttpClient,
    project: &str,
    storagePool: &str,
    zone: &str,
    requestId: Option<&str>,
    updateMask: Option<&str>,
    body: &StoragePool,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_storage_pools_update_builder(
        client,
        project,
        storagePool,
        zone,
        requestId,
        updateMask,
        body,
    )?;
    compute_storage_pools_update_execute(builder)
}

/// GET projects/{project}/aggregated/subnetworks
/// Retrieves an aggregated list of subnetworks.  To prevent failure, Google recommends that you set the `returnPartialSuccess` parameter to `true`.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_subnetworks_aggregated_list_execute()` to send, or `compute_subnetworks_aggregated_list` for simplest API.

pub fn compute_subnetworks_aggregated_list_builder(
    client: &SimpleHttpClient,
    project: &str,
    filter: Option<&str>,
    includeAllScopes: Option<bool>,
    maxResults: Option<i32>,
    orderBy: Option<&str>,
    pageToken: Option<&str>,
    returnPartialSuccess: Option<bool>,
    serviceProjectNumber: Option<&str>,
    views: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/aggregated/subnetworks",
        project,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "filter",
            filter
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "includeAllScopes",
            includeAllScopes
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "maxResults",
            maxResults
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "orderBy",
            orderBy
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageToken",
            pageToken
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "returnPartialSuccess",
            returnPartialSuccess
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "serviceProjectNumber",
            serviceProjectNumber
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "views",
            views
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET projects/{project}/aggregated/subnetworks
/// Retrieves an aggregated list of subnetworks.  To prevent failure, Google recommends that you set the `returnPartialSuccess` parameter to `true`.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_subnetworks_aggregated_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_subnetworks_aggregated_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_subnetworks_aggregated_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SubnetworkAggregatedList>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: SubnetworkAggregatedList = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/aggregated/subnetworks
/// Retrieves an aggregated list of subnetworks.  To prevent failure, Google recommends that you set the `returnPartialSuccess` parameter to `true`.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_subnetworks_aggregated_list_builder()` + `compute_subnetworks_aggregated_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_subnetworks_aggregated_list(
    client: &SimpleHttpClient,
    project: &str,
    filter: Option<&str>,
    includeAllScopes: Option<bool>,
    maxResults: Option<i32>,
    orderBy: Option<&str>,
    pageToken: Option<&str>,
    returnPartialSuccess: Option<bool>,
    serviceProjectNumber: Option<&str>,
    views: Option<&str>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SubnetworkAggregatedList>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = compute_subnetworks_aggregated_list_builder(
        client,
        project,
        filter,
        includeAllScopes,
        maxResults,
        orderBy,
        pageToken,
        returnPartialSuccess,
        serviceProjectNumber,
        views,
    )?;
    compute_subnetworks_aggregated_list_execute(builder)
}

/// GET projects/{project}/regions/{region}/subnetworks/{subnetwork}
/// Deletes the specified subnetwork.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_subnetworks_delete_execute()` to send, or `compute_subnetworks_delete` for simplest API.

pub fn compute_subnetworks_delete_builder(
    client: &SimpleHttpClient,
    project: &str,
    region: &str,
    subnetwork: &str,
    requestId: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/regions/{}/subnetworks/{}",
        project, region, subnetwork,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "requestId",
            requestId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET projects/{project}/regions/{region}/subnetworks/{subnetwork}
/// Deletes the specified subnetwork.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_subnetworks_delete_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_subnetworks_delete_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_subnetworks_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/regions/{region}/subnetworks/{subnetwork}
/// Deletes the specified subnetwork.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_subnetworks_delete_builder()` + `compute_subnetworks_delete_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_subnetworks_delete(
    client: &SimpleHttpClient,
    project: &str,
    region: &str,
    subnetwork: &str,
    requestId: Option<&str>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        compute_subnetworks_delete_builder(client, project, region, subnetwork, requestId)?;
    compute_subnetworks_delete_execute(builder)
}

/// GET projects/{project}/regions/{region}/subnetworks/{subnetwork}/expandIpCidrRange
/// Expands the IP CIDR range of the subnetwork to a specified value.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_subnetworks_expand_ip_cidr_range_execute()` to send, or `compute_subnetworks_expand_ip_cidr_range` for simplest API.

pub fn compute_subnetworks_expand_ip_cidr_range_builder(
    client: &SimpleHttpClient,
    project: &str,
    region: &str,
    subnetwork: &str,
    requestId: Option<&str>,
    body: &SubnetworksExpandIpCidrRangeRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/regions/{}/subnetworks/{}/expandIpCidrRange",
        project, region, subnetwork,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "requestId",
            requestId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET projects/{project}/regions/{region}/subnetworks/{subnetwork}/expandIpCidrRange
/// Expands the IP CIDR range of the subnetwork to a specified value.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_subnetworks_expand_ip_cidr_range_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_subnetworks_expand_ip_cidr_range_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_subnetworks_expand_ip_cidr_range_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/regions/{region}/subnetworks/{subnetwork}/expandIpCidrRange
/// Expands the IP CIDR range of the subnetwork to a specified value.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_subnetworks_expand_ip_cidr_range_builder()` + `compute_subnetworks_expand_ip_cidr_range_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_subnetworks_expand_ip_cidr_range(
    client: &SimpleHttpClient,
    project: &str,
    region: &str,
    subnetwork: &str,
    requestId: Option<&str>,
    body: &SubnetworksExpandIpCidrRangeRequest,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_subnetworks_expand_ip_cidr_range_builder(
        client, project, region, subnetwork, requestId, body,
    )?;
    compute_subnetworks_expand_ip_cidr_range_execute(builder)
}

/// GET projects/{project}/regions/{region}/subnetworks/{subnetwork}
/// Returns the specified subnetwork.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_subnetworks_get_execute()` to send, or `compute_subnetworks_get` for simplest API.

pub fn compute_subnetworks_get_builder(
    client: &SimpleHttpClient,
    project: &str,
    region: &str,
    subnetwork: &str,
    views: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/regions/{}/subnetworks/{}",
        project, region, subnetwork,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "views",
            views
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET projects/{project}/regions/{region}/subnetworks/{subnetwork}
/// Returns the specified subnetwork.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_subnetworks_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_subnetworks_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_subnetworks_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Subnetwork>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Subnetwork = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/regions/{region}/subnetworks/{subnetwork}
/// Returns the specified subnetwork.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_subnetworks_get_builder()` + `compute_subnetworks_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_subnetworks_get(
    client: &SimpleHttpClient,
    project: &str,
    region: &str,
    subnetwork: &str,
    views: Option<&str>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Subnetwork>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_subnetworks_get_builder(client, project, region, subnetwork, views)?;
    compute_subnetworks_get_execute(builder)
}

/// GET projects/{project}/regions/{region}/subnetworks/{resource}/getIamPolicy
/// Gets the access control policy for a resource. May be empty if no such policy or resource exists.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_subnetworks_get_iam_policy_execute()` to send, or `compute_subnetworks_get_iam_policy` for simplest API.

pub fn compute_subnetworks_get_iam_policy_builder(
    client: &SimpleHttpClient,
    project: &str,
    region: &str,
    resource: &str,
    optionsRequestedPolicyVersion: Option<i32>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/regions/{}/subnetworks/{}/getIamPolicy",
        project, region, resource,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "optionsRequestedPolicyVersion",
            optionsRequestedPolicyVersion
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET projects/{project}/regions/{region}/subnetworks/{resource}/getIamPolicy
/// Gets the access control policy for a resource. May be empty if no such policy or resource exists.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_subnetworks_get_iam_policy_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_subnetworks_get_iam_policy_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_subnetworks_get_iam_policy_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Policy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Policy = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/regions/{region}/subnetworks/{resource}/getIamPolicy
/// Gets the access control policy for a resource. May be empty if no such policy or resource exists.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_subnetworks_get_iam_policy_builder()` + `compute_subnetworks_get_iam_policy_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_subnetworks_get_iam_policy(
    client: &SimpleHttpClient,
    project: &str,
    region: &str,
    resource: &str,
    optionsRequestedPolicyVersion: Option<i32>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Policy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_subnetworks_get_iam_policy_builder(
        client,
        project,
        region,
        resource,
        optionsRequestedPolicyVersion,
    )?;
    compute_subnetworks_get_iam_policy_execute(builder)
}

/// GET projects/{project}/regions/{region}/subnetworks
/// Creates a subnetwork in the specified project using the data included in the request.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_subnetworks_insert_execute()` to send, or `compute_subnetworks_insert` for simplest API.

pub fn compute_subnetworks_insert_builder(
    client: &SimpleHttpClient,
    project: &str,
    region: &str,
    requestId: Option<&str>,
    body: &Subnetwork,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/regions/{}/subnetworks",
        project, region,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "requestId",
            requestId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET projects/{project}/regions/{region}/subnetworks
/// Creates a subnetwork in the specified project using the data included in the request.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_subnetworks_insert_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_subnetworks_insert_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_subnetworks_insert_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/regions/{region}/subnetworks
/// Creates a subnetwork in the specified project using the data included in the request.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_subnetworks_insert_builder()` + `compute_subnetworks_insert_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_subnetworks_insert(
    client: &SimpleHttpClient,
    project: &str,
    region: &str,
    requestId: Option<&str>,
    body: &Subnetwork,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_subnetworks_insert_builder(client, project, region, requestId, body)?;
    compute_subnetworks_insert_execute(builder)
}

/// GET projects/{project}/regions/{region}/subnetworks
/// Retrieves a list of subnetworks available to the specified project.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_subnetworks_list_execute()` to send, or `compute_subnetworks_list` for simplest API.

pub fn compute_subnetworks_list_builder(
    client: &SimpleHttpClient,
    project: &str,
    region: &str,
    filter: Option<&str>,
    maxResults: Option<i32>,
    orderBy: Option<&str>,
    pageToken: Option<&str>,
    returnPartialSuccess: Option<bool>,
    views: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/regions/{}/subnetworks",
        project, region,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "filter",
            filter
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "maxResults",
            maxResults
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "orderBy",
            orderBy
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageToken",
            pageToken
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "returnPartialSuccess",
            returnPartialSuccess
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "views",
            views
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET projects/{project}/regions/{region}/subnetworks
/// Retrieves a list of subnetworks available to the specified project.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_subnetworks_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_subnetworks_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_subnetworks_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SubnetworkList>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: SubnetworkList = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/regions/{region}/subnetworks
/// Retrieves a list of subnetworks available to the specified project.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_subnetworks_list_builder()` + `compute_subnetworks_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_subnetworks_list(
    client: &SimpleHttpClient,
    project: &str,
    region: &str,
    filter: Option<&str>,
    maxResults: Option<i32>,
    orderBy: Option<&str>,
    pageToken: Option<&str>,
    returnPartialSuccess: Option<bool>,
    views: Option<&str>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SubnetworkList>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = compute_subnetworks_list_builder(
        client,
        project,
        region,
        filter,
        maxResults,
        orderBy,
        pageToken,
        returnPartialSuccess,
        views,
    )?;
    compute_subnetworks_list_execute(builder)
}

/// GET projects/{project}/aggregated/subnetworks/listUsable
/// Retrieves an aggregated list of all usable subnetworks in the project.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_subnetworks_list_usable_execute()` to send, or `compute_subnetworks_list_usable` for simplest API.

pub fn compute_subnetworks_list_usable_builder(
    client: &SimpleHttpClient,
    project: &str,
    filter: Option<&str>,
    maxResults: Option<i32>,
    orderBy: Option<&str>,
    pageToken: Option<&str>,
    returnPartialSuccess: Option<bool>,
    serviceProject: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/aggregated/subnetworks/listUsable",
        project,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "filter",
            filter
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "maxResults",
            maxResults
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "orderBy",
            orderBy
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageToken",
            pageToken
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "returnPartialSuccess",
            returnPartialSuccess
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "serviceProject",
            serviceProject
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET projects/{project}/aggregated/subnetworks/listUsable
/// Retrieves an aggregated list of all usable subnetworks in the project.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_subnetworks_list_usable_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_subnetworks_list_usable_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_subnetworks_list_usable_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<UsableSubnetworksAggregatedList>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: UsableSubnetworksAggregatedList = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/aggregated/subnetworks/listUsable
/// Retrieves an aggregated list of all usable subnetworks in the project.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_subnetworks_list_usable_builder()` + `compute_subnetworks_list_usable_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_subnetworks_list_usable(
    client: &SimpleHttpClient,
    project: &str,
    filter: Option<&str>,
    maxResults: Option<i32>,
    orderBy: Option<&str>,
    pageToken: Option<&str>,
    returnPartialSuccess: Option<bool>,
    serviceProject: Option<&str>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<UsableSubnetworksAggregatedList>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = compute_subnetworks_list_usable_builder(
        client,
        project,
        filter,
        maxResults,
        orderBy,
        pageToken,
        returnPartialSuccess,
        serviceProject,
    )?;
    compute_subnetworks_list_usable_execute(builder)
}

/// GET projects/{project}/regions/{region}/subnetworks/{subnetwork}
/// Patches the specified subnetwork with the data included in the request. Only certain fields can be updated with a patch request as indicated in the field descriptions. You must specify the current fingerprint of the subnetwork resource being patched.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_subnetworks_patch_execute()` to send, or `compute_subnetworks_patch` for simplest API.

pub fn compute_subnetworks_patch_builder(
    client: &SimpleHttpClient,
    project: &str,
    region: &str,
    subnetwork: &str,
    drainTimeoutSeconds: Option<i32>,
    requestId: Option<&str>,
    body: &Subnetwork,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/regions/{}/subnetworks/{}",
        project, region, subnetwork,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "drainTimeoutSeconds",
            drainTimeoutSeconds
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "requestId",
            requestId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET projects/{project}/regions/{region}/subnetworks/{subnetwork}
/// Patches the specified subnetwork with the data included in the request. Only certain fields can be updated with a patch request as indicated in the field descriptions. You must specify the current fingerprint of the subnetwork resource being patched.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_subnetworks_patch_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_subnetworks_patch_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_subnetworks_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/regions/{region}/subnetworks/{subnetwork}
/// Patches the specified subnetwork with the data included in the request. Only certain fields can be updated with a patch request as indicated in the field descriptions. You must specify the current fingerprint of the subnetwork resource being patched.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_subnetworks_patch_builder()` + `compute_subnetworks_patch_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_subnetworks_patch(
    client: &SimpleHttpClient,
    project: &str,
    region: &str,
    subnetwork: &str,
    drainTimeoutSeconds: Option<i32>,
    requestId: Option<&str>,
    body: &Subnetwork,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_subnetworks_patch_builder(
        client,
        project,
        region,
        subnetwork,
        drainTimeoutSeconds,
        requestId,
        body,
    )?;
    compute_subnetworks_patch_execute(builder)
}

/// GET projects/{project}/regions/{region}/subnetworks/{resource}/setIamPolicy
/// Sets the access control policy on the specified resource. Replaces any existing policy.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_subnetworks_set_iam_policy_execute()` to send, or `compute_subnetworks_set_iam_policy` for simplest API.

pub fn compute_subnetworks_set_iam_policy_builder(
    client: &SimpleHttpClient,
    project: &str,
    region: &str,
    resource: &str,
    body: &RegionSetPolicyRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/regions/{}/subnetworks/{}/setIamPolicy",
        project, region, resource,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET projects/{project}/regions/{region}/subnetworks/{resource}/setIamPolicy
/// Sets the access control policy on the specified resource. Replaces any existing policy.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_subnetworks_set_iam_policy_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_subnetworks_set_iam_policy_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_subnetworks_set_iam_policy_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Policy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Policy = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/regions/{region}/subnetworks/{resource}/setIamPolicy
/// Sets the access control policy on the specified resource. Replaces any existing policy.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_subnetworks_set_iam_policy_builder()` + `compute_subnetworks_set_iam_policy_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_subnetworks_set_iam_policy(
    client: &SimpleHttpClient,
    project: &str,
    region: &str,
    resource: &str,
    body: &RegionSetPolicyRequest,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Policy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        compute_subnetworks_set_iam_policy_builder(client, project, region, resource, body)?;
    compute_subnetworks_set_iam_policy_execute(builder)
}

/// GET projects/{project}/regions/{region}/subnetworks/{subnetwork}/setPrivateIpGoogleAccess
/// Set whether VMs in this subnet can access Google services without assigning external IP addresses through Private Google Access.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_subnetworks_set_private_ip_google_access_execute()` to send, or `compute_subnetworks_set_private_ip_google_access` for simplest API.

pub fn compute_subnetworks_set_private_ip_google_access_builder(
    client: &SimpleHttpClient,
    project: &str,
    region: &str,
    subnetwork: &str,
    requestId: Option<&str>,
    body: &SubnetworksSetPrivateIpGoogleAccessRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/regions/{}/subnetworks/{}/setPrivateIpGoogleAccess",
        project, region, subnetwork,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "requestId",
            requestId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET projects/{project}/regions/{region}/subnetworks/{subnetwork}/setPrivateIpGoogleAccess
/// Set whether VMs in this subnet can access Google services without assigning external IP addresses through Private Google Access.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_subnetworks_set_private_ip_google_access_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_subnetworks_set_private_ip_google_access_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_subnetworks_set_private_ip_google_access_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/regions/{region}/subnetworks/{subnetwork}/setPrivateIpGoogleAccess
/// Set whether VMs in this subnet can access Google services without assigning external IP addresses through Private Google Access.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_subnetworks_set_private_ip_google_access_builder()` + `compute_subnetworks_set_private_ip_google_access_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_subnetworks_set_private_ip_google_access(
    client: &SimpleHttpClient,
    project: &str,
    region: &str,
    subnetwork: &str,
    requestId: Option<&str>,
    body: &SubnetworksSetPrivateIpGoogleAccessRequest,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_subnetworks_set_private_ip_google_access_builder(
        client, project, region, subnetwork, requestId, body,
    )?;
    compute_subnetworks_set_private_ip_google_access_execute(builder)
}

/// GET projects/{project}/regions/{region}/subnetworks/{resource}/testIamPermissions
/// Returns permissions that a caller has on the specified resource.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_subnetworks_test_iam_permissions_execute()` to send, or `compute_subnetworks_test_iam_permissions` for simplest API.

pub fn compute_subnetworks_test_iam_permissions_builder(
    client: &SimpleHttpClient,
    project: &str,
    region: &str,
    resource: &str,
    body: &TestPermissionsRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/regions/{}/subnetworks/{}/testIamPermissions",
        project, region, resource,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET projects/{project}/regions/{region}/subnetworks/{resource}/testIamPermissions
/// Returns permissions that a caller has on the specified resource.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_subnetworks_test_iam_permissions_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_subnetworks_test_iam_permissions_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_subnetworks_test_iam_permissions_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<TestPermissionsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: TestPermissionsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/regions/{region}/subnetworks/{resource}/testIamPermissions
/// Returns permissions that a caller has on the specified resource.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_subnetworks_test_iam_permissions_builder()` + `compute_subnetworks_test_iam_permissions_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_subnetworks_test_iam_permissions(
    client: &SimpleHttpClient,
    project: &str,
    region: &str,
    resource: &str,
    body: &TestPermissionsRequest,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<TestPermissionsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        compute_subnetworks_test_iam_permissions_builder(client, project, region, resource, body)?;
    compute_subnetworks_test_iam_permissions_execute(builder)
}

/// GET projects/{project}/global/targetGrpcProxies/{targetGrpcProxy}
/// Deletes the specified TargetGrpcProxy in the given scope
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_target_grpc_proxies_delete_execute()` to send, or `compute_target_grpc_proxies_delete` for simplest API.

pub fn compute_target_grpc_proxies_delete_builder(
    client: &SimpleHttpClient,
    project: &str,
    targetGrpcProxy: &str,
    requestId: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/global/targetGrpcProxies/{}",
        project, targetGrpcProxy,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "requestId",
            requestId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET projects/{project}/global/targetGrpcProxies/{targetGrpcProxy}
/// Deletes the specified TargetGrpcProxy in the given scope
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_target_grpc_proxies_delete_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_target_grpc_proxies_delete_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_target_grpc_proxies_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/global/targetGrpcProxies/{targetGrpcProxy}
/// Deletes the specified TargetGrpcProxy in the given scope
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_target_grpc_proxies_delete_builder()` + `compute_target_grpc_proxies_delete_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_target_grpc_proxies_delete(
    client: &SimpleHttpClient,
    project: &str,
    targetGrpcProxy: &str,
    requestId: Option<&str>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        compute_target_grpc_proxies_delete_builder(client, project, targetGrpcProxy, requestId)?;
    compute_target_grpc_proxies_delete_execute(builder)
}

/// GET projects/{project}/global/targetGrpcProxies/{targetGrpcProxy}
/// Returns the specified TargetGrpcProxy resource in the given scope.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_target_grpc_proxies_get_execute()` to send, or `compute_target_grpc_proxies_get` for simplest API.

pub fn compute_target_grpc_proxies_get_builder(
    client: &SimpleHttpClient,
    project: &str,
    targetGrpcProxy: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/global/targetGrpcProxies/{}",
        project, targetGrpcProxy,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/global/targetGrpcProxies/{targetGrpcProxy}
/// Returns the specified TargetGrpcProxy resource in the given scope.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_target_grpc_proxies_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_target_grpc_proxies_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_target_grpc_proxies_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<TargetGrpcProxy>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: TargetGrpcProxy = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/global/targetGrpcProxies/{targetGrpcProxy}
/// Returns the specified TargetGrpcProxy resource in the given scope.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_target_grpc_proxies_get_builder()` + `compute_target_grpc_proxies_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_target_grpc_proxies_get(
    client: &SimpleHttpClient,
    project: &str,
    targetGrpcProxy: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<TargetGrpcProxy>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = compute_target_grpc_proxies_get_builder(client, project, targetGrpcProxy)?;
    compute_target_grpc_proxies_get_execute(builder)
}

/// GET projects/{project}/global/targetGrpcProxies
/// Creates a TargetGrpcProxy in the specified project in the given scope using the parameters that are included in the request.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_target_grpc_proxies_insert_execute()` to send, or `compute_target_grpc_proxies_insert` for simplest API.

pub fn compute_target_grpc_proxies_insert_builder(
    client: &SimpleHttpClient,
    project: &str,
    requestId: Option<&str>,
    body: &TargetGrpcProxy,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/global/targetGrpcProxies",
        project,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "requestId",
            requestId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET projects/{project}/global/targetGrpcProxies
/// Creates a TargetGrpcProxy in the specified project in the given scope using the parameters that are included in the request.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_target_grpc_proxies_insert_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_target_grpc_proxies_insert_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_target_grpc_proxies_insert_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/global/targetGrpcProxies
/// Creates a TargetGrpcProxy in the specified project in the given scope using the parameters that are included in the request.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_target_grpc_proxies_insert_builder()` + `compute_target_grpc_proxies_insert_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_target_grpc_proxies_insert(
    client: &SimpleHttpClient,
    project: &str,
    requestId: Option<&str>,
    body: &TargetGrpcProxy,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_target_grpc_proxies_insert_builder(client, project, requestId, body)?;
    compute_target_grpc_proxies_insert_execute(builder)
}

/// GET projects/{project}/global/targetGrpcProxies
/// Lists the TargetGrpcProxies for a project in the given scope.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_target_grpc_proxies_list_execute()` to send, or `compute_target_grpc_proxies_list` for simplest API.

pub fn compute_target_grpc_proxies_list_builder(
    client: &SimpleHttpClient,
    project: &str,
    filter: Option<&str>,
    maxResults: Option<i32>,
    orderBy: Option<&str>,
    pageToken: Option<&str>,
    returnPartialSuccess: Option<bool>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/global/targetGrpcProxies",
        project,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "filter",
            filter
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "maxResults",
            maxResults
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "orderBy",
            orderBy
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageToken",
            pageToken
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "returnPartialSuccess",
            returnPartialSuccess
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET projects/{project}/global/targetGrpcProxies
/// Lists the TargetGrpcProxies for a project in the given scope.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_target_grpc_proxies_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_target_grpc_proxies_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_target_grpc_proxies_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<TargetGrpcProxyList>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: TargetGrpcProxyList = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/global/targetGrpcProxies
/// Lists the TargetGrpcProxies for a project in the given scope.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_target_grpc_proxies_list_builder()` + `compute_target_grpc_proxies_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_target_grpc_proxies_list(
    client: &SimpleHttpClient,
    project: &str,
    filter: Option<&str>,
    maxResults: Option<i32>,
    orderBy: Option<&str>,
    pageToken: Option<&str>,
    returnPartialSuccess: Option<bool>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<TargetGrpcProxyList>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = compute_target_grpc_proxies_list_builder(
        client,
        project,
        filter,
        maxResults,
        orderBy,
        pageToken,
        returnPartialSuccess,
    )?;
    compute_target_grpc_proxies_list_execute(builder)
}

/// GET projects/{project}/global/targetGrpcProxies/{targetGrpcProxy}
/// Patches the specified TargetGrpcProxy resource with the data included in the request. This method supports PATCH semantics and `usesJSON` merge patch format and processing rules.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_target_grpc_proxies_patch_execute()` to send, or `compute_target_grpc_proxies_patch` for simplest API.

pub fn compute_target_grpc_proxies_patch_builder(
    client: &SimpleHttpClient,
    project: &str,
    targetGrpcProxy: &str,
    requestId: Option<&str>,
    body: &TargetGrpcProxy,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/global/targetGrpcProxies/{}",
        project, targetGrpcProxy,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "requestId",
            requestId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET projects/{project}/global/targetGrpcProxies/{targetGrpcProxy}
/// Patches the specified TargetGrpcProxy resource with the data included in the request. This method supports PATCH semantics and `usesJSON` merge patch format and processing rules.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_target_grpc_proxies_patch_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_target_grpc_proxies_patch_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_target_grpc_proxies_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/global/targetGrpcProxies/{targetGrpcProxy}
/// Patches the specified TargetGrpcProxy resource with the data included in the request. This method supports PATCH semantics and `usesJSON` merge patch format and processing rules.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_target_grpc_proxies_patch_builder()` + `compute_target_grpc_proxies_patch_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_target_grpc_proxies_patch(
    client: &SimpleHttpClient,
    project: &str,
    targetGrpcProxy: &str,
    requestId: Option<&str>,
    body: &TargetGrpcProxy,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_target_grpc_proxies_patch_builder(
        client,
        project,
        targetGrpcProxy,
        requestId,
        body,
    )?;
    compute_target_grpc_proxies_patch_execute(builder)
}

/// GET projects/{project}/aggregated/targetHttpProxies
/// Retrieves the list of all TargetHttpProxy resources, regional and global, available to the specified project.  To prevent failure, Google recommends that you set the `returnPartialSuccess` parameter to `true`.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_target_http_proxies_aggregated_list_execute()` to send, or `compute_target_http_proxies_aggregated_list` for simplest API.

pub fn compute_target_http_proxies_aggregated_list_builder(
    client: &SimpleHttpClient,
    project: &str,
    filter: Option<&str>,
    includeAllScopes: Option<bool>,
    maxResults: Option<i32>,
    orderBy: Option<&str>,
    pageToken: Option<&str>,
    returnPartialSuccess: Option<bool>,
    serviceProjectNumber: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/aggregated/targetHttpProxies",
        project,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "filter",
            filter
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "includeAllScopes",
            includeAllScopes
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "maxResults",
            maxResults
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "orderBy",
            orderBy
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageToken",
            pageToken
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "returnPartialSuccess",
            returnPartialSuccess
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "serviceProjectNumber",
            serviceProjectNumber
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET projects/{project}/aggregated/targetHttpProxies
/// Retrieves the list of all TargetHttpProxy resources, regional and global, available to the specified project.  To prevent failure, Google recommends that you set the `returnPartialSuccess` parameter to `true`.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_target_http_proxies_aggregated_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_target_http_proxies_aggregated_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_target_http_proxies_aggregated_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<TargetHttpProxyAggregatedList>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: TargetHttpProxyAggregatedList = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/aggregated/targetHttpProxies
/// Retrieves the list of all TargetHttpProxy resources, regional and global, available to the specified project.  To prevent failure, Google recommends that you set the `returnPartialSuccess` parameter to `true`.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_target_http_proxies_aggregated_list_builder()` + `compute_target_http_proxies_aggregated_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_target_http_proxies_aggregated_list(
    client: &SimpleHttpClient,
    project: &str,
    filter: Option<&str>,
    includeAllScopes: Option<bool>,
    maxResults: Option<i32>,
    orderBy: Option<&str>,
    pageToken: Option<&str>,
    returnPartialSuccess: Option<bool>,
    serviceProjectNumber: Option<&str>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<TargetHttpProxyAggregatedList>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = compute_target_http_proxies_aggregated_list_builder(
        client,
        project,
        filter,
        includeAllScopes,
        maxResults,
        orderBy,
        pageToken,
        returnPartialSuccess,
        serviceProjectNumber,
    )?;
    compute_target_http_proxies_aggregated_list_execute(builder)
}

/// GET projects/{project}/global/targetHttpProxies/{targetHttpProxy}
/// Deletes the specified TargetHttpProxy resource.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_target_http_proxies_delete_execute()` to send, or `compute_target_http_proxies_delete` for simplest API.

pub fn compute_target_http_proxies_delete_builder(
    client: &SimpleHttpClient,
    project: &str,
    targetHttpProxy: &str,
    requestId: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/global/targetHttpProxies/{}",
        project, targetHttpProxy,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "requestId",
            requestId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET projects/{project}/global/targetHttpProxies/{targetHttpProxy}
/// Deletes the specified TargetHttpProxy resource.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_target_http_proxies_delete_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_target_http_proxies_delete_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_target_http_proxies_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/global/targetHttpProxies/{targetHttpProxy}
/// Deletes the specified TargetHttpProxy resource.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_target_http_proxies_delete_builder()` + `compute_target_http_proxies_delete_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_target_http_proxies_delete(
    client: &SimpleHttpClient,
    project: &str,
    targetHttpProxy: &str,
    requestId: Option<&str>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        compute_target_http_proxies_delete_builder(client, project, targetHttpProxy, requestId)?;
    compute_target_http_proxies_delete_execute(builder)
}

/// GET projects/{project}/global/targetHttpProxies/{targetHttpProxy}
/// Returns the specified TargetHttpProxy resource.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_target_http_proxies_get_execute()` to send, or `compute_target_http_proxies_get` for simplest API.

pub fn compute_target_http_proxies_get_builder(
    client: &SimpleHttpClient,
    project: &str,
    targetHttpProxy: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/global/targetHttpProxies/{}",
        project, targetHttpProxy,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/global/targetHttpProxies/{targetHttpProxy}
/// Returns the specified TargetHttpProxy resource.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_target_http_proxies_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_target_http_proxies_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_target_http_proxies_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<TargetHttpProxy>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: TargetHttpProxy = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/global/targetHttpProxies/{targetHttpProxy}
/// Returns the specified TargetHttpProxy resource.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_target_http_proxies_get_builder()` + `compute_target_http_proxies_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_target_http_proxies_get(
    client: &SimpleHttpClient,
    project: &str,
    targetHttpProxy: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<TargetHttpProxy>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = compute_target_http_proxies_get_builder(client, project, targetHttpProxy)?;
    compute_target_http_proxies_get_execute(builder)
}

/// GET projects/{project}/global/targetHttpProxies
/// Creates a TargetHttpProxy resource in the specified project using the data included in the request.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_target_http_proxies_insert_execute()` to send, or `compute_target_http_proxies_insert` for simplest API.

pub fn compute_target_http_proxies_insert_builder(
    client: &SimpleHttpClient,
    project: &str,
    requestId: Option<&str>,
    body: &TargetHttpProxy,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/global/targetHttpProxies",
        project,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "requestId",
            requestId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET projects/{project}/global/targetHttpProxies
/// Creates a TargetHttpProxy resource in the specified project using the data included in the request.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_target_http_proxies_insert_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_target_http_proxies_insert_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_target_http_proxies_insert_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/global/targetHttpProxies
/// Creates a TargetHttpProxy resource in the specified project using the data included in the request.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_target_http_proxies_insert_builder()` + `compute_target_http_proxies_insert_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_target_http_proxies_insert(
    client: &SimpleHttpClient,
    project: &str,
    requestId: Option<&str>,
    body: &TargetHttpProxy,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_target_http_proxies_insert_builder(client, project, requestId, body)?;
    compute_target_http_proxies_insert_execute(builder)
}

/// GET projects/{project}/global/targetHttpProxies
/// Retrieves the list of TargetHttpProxy resources available to the specified project.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_target_http_proxies_list_execute()` to send, or `compute_target_http_proxies_list` for simplest API.

pub fn compute_target_http_proxies_list_builder(
    client: &SimpleHttpClient,
    project: &str,
    filter: Option<&str>,
    maxResults: Option<i32>,
    orderBy: Option<&str>,
    pageToken: Option<&str>,
    returnPartialSuccess: Option<bool>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/global/targetHttpProxies",
        project,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "filter",
            filter
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "maxResults",
            maxResults
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "orderBy",
            orderBy
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageToken",
            pageToken
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "returnPartialSuccess",
            returnPartialSuccess
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET projects/{project}/global/targetHttpProxies
/// Retrieves the list of TargetHttpProxy resources available to the specified project.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_target_http_proxies_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_target_http_proxies_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_target_http_proxies_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<TargetHttpProxyList>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: TargetHttpProxyList = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/global/targetHttpProxies
/// Retrieves the list of TargetHttpProxy resources available to the specified project.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_target_http_proxies_list_builder()` + `compute_target_http_proxies_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_target_http_proxies_list(
    client: &SimpleHttpClient,
    project: &str,
    filter: Option<&str>,
    maxResults: Option<i32>,
    orderBy: Option<&str>,
    pageToken: Option<&str>,
    returnPartialSuccess: Option<bool>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<TargetHttpProxyList>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = compute_target_http_proxies_list_builder(
        client,
        project,
        filter,
        maxResults,
        orderBy,
        pageToken,
        returnPartialSuccess,
    )?;
    compute_target_http_proxies_list_execute(builder)
}

/// GET projects/{project}/global/targetHttpProxies/{targetHttpProxy}
/// Patches the specified TargetHttpProxy resource with the data included in the request. This method supports PATCH semantics and `usesJSON` merge patch format and processing rules.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_target_http_proxies_patch_execute()` to send, or `compute_target_http_proxies_patch` for simplest API.

pub fn compute_target_http_proxies_patch_builder(
    client: &SimpleHttpClient,
    project: &str,
    targetHttpProxy: &str,
    requestId: Option<&str>,
    body: &TargetHttpProxy,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/global/targetHttpProxies/{}",
        project, targetHttpProxy,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "requestId",
            requestId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET projects/{project}/global/targetHttpProxies/{targetHttpProxy}
/// Patches the specified TargetHttpProxy resource with the data included in the request. This method supports PATCH semantics and `usesJSON` merge patch format and processing rules.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_target_http_proxies_patch_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_target_http_proxies_patch_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_target_http_proxies_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/global/targetHttpProxies/{targetHttpProxy}
/// Patches the specified TargetHttpProxy resource with the data included in the request. This method supports PATCH semantics and `usesJSON` merge patch format and processing rules.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_target_http_proxies_patch_builder()` + `compute_target_http_proxies_patch_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_target_http_proxies_patch(
    client: &SimpleHttpClient,
    project: &str,
    targetHttpProxy: &str,
    requestId: Option<&str>,
    body: &TargetHttpProxy,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_target_http_proxies_patch_builder(
        client,
        project,
        targetHttpProxy,
        requestId,
        body,
    )?;
    compute_target_http_proxies_patch_execute(builder)
}

/// GET projects/{project}/targetHttpProxies/{targetHttpProxy}/setUrlMap
/// Changes the URL map for TargetHttpProxy.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_target_http_proxies_set_url_map_execute()` to send, or `compute_target_http_proxies_set_url_map` for simplest API.

pub fn compute_target_http_proxies_set_url_map_builder(
    client: &SimpleHttpClient,
    project: &str,
    targetHttpProxy: &str,
    requestId: Option<&str>,
    body: &UrlMapReference,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/targetHttpProxies/{}/setUrlMap",
        project, targetHttpProxy,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "requestId",
            requestId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET projects/{project}/targetHttpProxies/{targetHttpProxy}/setUrlMap
/// Changes the URL map for TargetHttpProxy.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_target_http_proxies_set_url_map_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_target_http_proxies_set_url_map_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_target_http_proxies_set_url_map_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/targetHttpProxies/{targetHttpProxy}/setUrlMap
/// Changes the URL map for TargetHttpProxy.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_target_http_proxies_set_url_map_builder()` + `compute_target_http_proxies_set_url_map_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_target_http_proxies_set_url_map(
    client: &SimpleHttpClient,
    project: &str,
    targetHttpProxy: &str,
    requestId: Option<&str>,
    body: &UrlMapReference,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_target_http_proxies_set_url_map_builder(
        client,
        project,
        targetHttpProxy,
        requestId,
        body,
    )?;
    compute_target_http_proxies_set_url_map_execute(builder)
}

/// GET projects/{project}/aggregated/targetHttpsProxies
/// Retrieves the list of all TargetHttpsProxy resources, regional and global, available to the specified project.  To prevent failure, Google recommends that you set the `returnPartialSuccess` parameter to `true`.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_target_https_proxies_aggregated_list_execute()` to send, or `compute_target_https_proxies_aggregated_list` for simplest API.

pub fn compute_target_https_proxies_aggregated_list_builder(
    client: &SimpleHttpClient,
    project: &str,
    filter: Option<&str>,
    includeAllScopes: Option<bool>,
    maxResults: Option<i32>,
    orderBy: Option<&str>,
    pageToken: Option<&str>,
    returnPartialSuccess: Option<bool>,
    serviceProjectNumber: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/aggregated/targetHttpsProxies",
        project,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "filter",
            filter
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "includeAllScopes",
            includeAllScopes
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "maxResults",
            maxResults
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "orderBy",
            orderBy
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageToken",
            pageToken
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "returnPartialSuccess",
            returnPartialSuccess
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "serviceProjectNumber",
            serviceProjectNumber
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET projects/{project}/aggregated/targetHttpsProxies
/// Retrieves the list of all TargetHttpsProxy resources, regional and global, available to the specified project.  To prevent failure, Google recommends that you set the `returnPartialSuccess` parameter to `true`.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_target_https_proxies_aggregated_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_target_https_proxies_aggregated_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_target_https_proxies_aggregated_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<TargetHttpsProxyAggregatedList>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: TargetHttpsProxyAggregatedList = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/aggregated/targetHttpsProxies
/// Retrieves the list of all TargetHttpsProxy resources, regional and global, available to the specified project.  To prevent failure, Google recommends that you set the `returnPartialSuccess` parameter to `true`.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_target_https_proxies_aggregated_list_builder()` + `compute_target_https_proxies_aggregated_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_target_https_proxies_aggregated_list(
    client: &SimpleHttpClient,
    project: &str,
    filter: Option<&str>,
    includeAllScopes: Option<bool>,
    maxResults: Option<i32>,
    orderBy: Option<&str>,
    pageToken: Option<&str>,
    returnPartialSuccess: Option<bool>,
    serviceProjectNumber: Option<&str>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<TargetHttpsProxyAggregatedList>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = compute_target_https_proxies_aggregated_list_builder(
        client,
        project,
        filter,
        includeAllScopes,
        maxResults,
        orderBy,
        pageToken,
        returnPartialSuccess,
        serviceProjectNumber,
    )?;
    compute_target_https_proxies_aggregated_list_execute(builder)
}

/// GET projects/{project}/global/targetHttpsProxies/{targetHttpsProxy}
/// Deletes the specified TargetHttpsProxy resource.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_target_https_proxies_delete_execute()` to send, or `compute_target_https_proxies_delete` for simplest API.

pub fn compute_target_https_proxies_delete_builder(
    client: &SimpleHttpClient,
    project: &str,
    targetHttpsProxy: &str,
    requestId: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/global/targetHttpsProxies/{}",
        project, targetHttpsProxy,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "requestId",
            requestId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET projects/{project}/global/targetHttpsProxies/{targetHttpsProxy}
/// Deletes the specified TargetHttpsProxy resource.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_target_https_proxies_delete_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_target_https_proxies_delete_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_target_https_proxies_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/global/targetHttpsProxies/{targetHttpsProxy}
/// Deletes the specified TargetHttpsProxy resource.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_target_https_proxies_delete_builder()` + `compute_target_https_proxies_delete_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_target_https_proxies_delete(
    client: &SimpleHttpClient,
    project: &str,
    targetHttpsProxy: &str,
    requestId: Option<&str>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        compute_target_https_proxies_delete_builder(client, project, targetHttpsProxy, requestId)?;
    compute_target_https_proxies_delete_execute(builder)
}

/// GET projects/{project}/global/targetHttpsProxies/{targetHttpsProxy}
/// Returns the specified TargetHttpsProxy resource.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_target_https_proxies_get_execute()` to send, or `compute_target_https_proxies_get` for simplest API.

pub fn compute_target_https_proxies_get_builder(
    client: &SimpleHttpClient,
    project: &str,
    targetHttpsProxy: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/global/targetHttpsProxies/{}",
        project, targetHttpsProxy,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/global/targetHttpsProxies/{targetHttpsProxy}
/// Returns the specified TargetHttpsProxy resource.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_target_https_proxies_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_target_https_proxies_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_target_https_proxies_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<TargetHttpsProxy>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: TargetHttpsProxy = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/global/targetHttpsProxies/{targetHttpsProxy}
/// Returns the specified TargetHttpsProxy resource.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_target_https_proxies_get_builder()` + `compute_target_https_proxies_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_target_https_proxies_get(
    client: &SimpleHttpClient,
    project: &str,
    targetHttpsProxy: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<TargetHttpsProxy>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = compute_target_https_proxies_get_builder(client, project, targetHttpsProxy)?;
    compute_target_https_proxies_get_execute(builder)
}

/// GET projects/{project}/global/targetHttpsProxies
/// Creates a TargetHttpsProxy resource in the specified project using the data included in the request.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_target_https_proxies_insert_execute()` to send, or `compute_target_https_proxies_insert` for simplest API.

pub fn compute_target_https_proxies_insert_builder(
    client: &SimpleHttpClient,
    project: &str,
    requestId: Option<&str>,
    body: &TargetHttpsProxy,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/global/targetHttpsProxies",
        project,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "requestId",
            requestId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET projects/{project}/global/targetHttpsProxies
/// Creates a TargetHttpsProxy resource in the specified project using the data included in the request.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_target_https_proxies_insert_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_target_https_proxies_insert_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_target_https_proxies_insert_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/global/targetHttpsProxies
/// Creates a TargetHttpsProxy resource in the specified project using the data included in the request.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_target_https_proxies_insert_builder()` + `compute_target_https_proxies_insert_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_target_https_proxies_insert(
    client: &SimpleHttpClient,
    project: &str,
    requestId: Option<&str>,
    body: &TargetHttpsProxy,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_target_https_proxies_insert_builder(client, project, requestId, body)?;
    compute_target_https_proxies_insert_execute(builder)
}

/// GET projects/{project}/global/targetHttpsProxies
/// Retrieves the list of TargetHttpsProxy resources available to the specified project.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_target_https_proxies_list_execute()` to send, or `compute_target_https_proxies_list` for simplest API.

pub fn compute_target_https_proxies_list_builder(
    client: &SimpleHttpClient,
    project: &str,
    filter: Option<&str>,
    maxResults: Option<i32>,
    orderBy: Option<&str>,
    pageToken: Option<&str>,
    returnPartialSuccess: Option<bool>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/global/targetHttpsProxies",
        project,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "filter",
            filter
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "maxResults",
            maxResults
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "orderBy",
            orderBy
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageToken",
            pageToken
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "returnPartialSuccess",
            returnPartialSuccess
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET projects/{project}/global/targetHttpsProxies
/// Retrieves the list of TargetHttpsProxy resources available to the specified project.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_target_https_proxies_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_target_https_proxies_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_target_https_proxies_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<TargetHttpsProxyList>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: TargetHttpsProxyList = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/global/targetHttpsProxies
/// Retrieves the list of TargetHttpsProxy resources available to the specified project.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_target_https_proxies_list_builder()` + `compute_target_https_proxies_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_target_https_proxies_list(
    client: &SimpleHttpClient,
    project: &str,
    filter: Option<&str>,
    maxResults: Option<i32>,
    orderBy: Option<&str>,
    pageToken: Option<&str>,
    returnPartialSuccess: Option<bool>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<TargetHttpsProxyList>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = compute_target_https_proxies_list_builder(
        client,
        project,
        filter,
        maxResults,
        orderBy,
        pageToken,
        returnPartialSuccess,
    )?;
    compute_target_https_proxies_list_execute(builder)
}

/// GET projects/{project}/global/targetHttpsProxies/{targetHttpsProxy}
/// Patches the specified TargetHttpsProxy resource with the data included in the request. This method supports PATCH semantics and `usesJSON` merge patch format and processing rules.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_target_https_proxies_patch_execute()` to send, or `compute_target_https_proxies_patch` for simplest API.

pub fn compute_target_https_proxies_patch_builder(
    client: &SimpleHttpClient,
    project: &str,
    targetHttpsProxy: &str,
    requestId: Option<&str>,
    body: &TargetHttpsProxy,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/global/targetHttpsProxies/{}",
        project, targetHttpsProxy,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "requestId",
            requestId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET projects/{project}/global/targetHttpsProxies/{targetHttpsProxy}
/// Patches the specified TargetHttpsProxy resource with the data included in the request. This method supports PATCH semantics and `usesJSON` merge patch format and processing rules.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_target_https_proxies_patch_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_target_https_proxies_patch_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_target_https_proxies_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/global/targetHttpsProxies/{targetHttpsProxy}
/// Patches the specified TargetHttpsProxy resource with the data included in the request. This method supports PATCH semantics and `usesJSON` merge patch format and processing rules.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_target_https_proxies_patch_builder()` + `compute_target_https_proxies_patch_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_target_https_proxies_patch(
    client: &SimpleHttpClient,
    project: &str,
    targetHttpsProxy: &str,
    requestId: Option<&str>,
    body: &TargetHttpsProxy,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_target_https_proxies_patch_builder(
        client,
        project,
        targetHttpsProxy,
        requestId,
        body,
    )?;
    compute_target_https_proxies_patch_execute(builder)
}

/// GET projects/{project}/global/targetHttpsProxies/{targetHttpsProxy}/setCertificateMap
/// Changes the Certificate Map for TargetHttpsProxy.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_target_https_proxies_set_certificate_map_execute()` to send, or `compute_target_https_proxies_set_certificate_map` for simplest API.

pub fn compute_target_https_proxies_set_certificate_map_builder(
    client: &SimpleHttpClient,
    project: &str,
    targetHttpsProxy: &str,
    requestId: Option<&str>,
    body: &TargetHttpsProxiesSetCertificateMapRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/global/targetHttpsProxies/{}/setCertificateMap",
        project, targetHttpsProxy,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "requestId",
            requestId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET projects/{project}/global/targetHttpsProxies/{targetHttpsProxy}/setCertificateMap
/// Changes the Certificate Map for TargetHttpsProxy.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_target_https_proxies_set_certificate_map_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_target_https_proxies_set_certificate_map_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_target_https_proxies_set_certificate_map_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/global/targetHttpsProxies/{targetHttpsProxy}/setCertificateMap
/// Changes the Certificate Map for TargetHttpsProxy.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_target_https_proxies_set_certificate_map_builder()` + `compute_target_https_proxies_set_certificate_map_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_target_https_proxies_set_certificate_map(
    client: &SimpleHttpClient,
    project: &str,
    targetHttpsProxy: &str,
    requestId: Option<&str>,
    body: &TargetHttpsProxiesSetCertificateMapRequest,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_target_https_proxies_set_certificate_map_builder(
        client,
        project,
        targetHttpsProxy,
        requestId,
        body,
    )?;
    compute_target_https_proxies_set_certificate_map_execute(builder)
}

/// GET projects/{project}/global/targetHttpsProxies/{targetHttpsProxy}/setQuicOverride
/// Sets the QUIC override policy for TargetHttpsProxy.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_target_https_proxies_set_quic_override_execute()` to send, or `compute_target_https_proxies_set_quic_override` for simplest API.

pub fn compute_target_https_proxies_set_quic_override_builder(
    client: &SimpleHttpClient,
    project: &str,
    targetHttpsProxy: &str,
    requestId: Option<&str>,
    body: &TargetHttpsProxiesSetQuicOverrideRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/global/targetHttpsProxies/{}/setQuicOverride",
        project, targetHttpsProxy,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "requestId",
            requestId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET projects/{project}/global/targetHttpsProxies/{targetHttpsProxy}/setQuicOverride
/// Sets the QUIC override policy for TargetHttpsProxy.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_target_https_proxies_set_quic_override_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_target_https_proxies_set_quic_override_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_target_https_proxies_set_quic_override_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/global/targetHttpsProxies/{targetHttpsProxy}/setQuicOverride
/// Sets the QUIC override policy for TargetHttpsProxy.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_target_https_proxies_set_quic_override_builder()` + `compute_target_https_proxies_set_quic_override_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_target_https_proxies_set_quic_override(
    client: &SimpleHttpClient,
    project: &str,
    targetHttpsProxy: &str,
    requestId: Option<&str>,
    body: &TargetHttpsProxiesSetQuicOverrideRequest,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_target_https_proxies_set_quic_override_builder(
        client,
        project,
        targetHttpsProxy,
        requestId,
        body,
    )?;
    compute_target_https_proxies_set_quic_override_execute(builder)
}

/// GET projects/{project}/targetHttpsProxies/{targetHttpsProxy}/setSslCertificates
/// Replaces SslCertificates for TargetHttpsProxy.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_target_https_proxies_set_ssl_certificates_execute()` to send, or `compute_target_https_proxies_set_ssl_certificates` for simplest API.

pub fn compute_target_https_proxies_set_ssl_certificates_builder(
    client: &SimpleHttpClient,
    project: &str,
    targetHttpsProxy: &str,
    requestId: Option<&str>,
    body: &TargetHttpsProxiesSetSslCertificatesRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/targetHttpsProxies/{}/setSslCertificates",
        project, targetHttpsProxy,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "requestId",
            requestId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET projects/{project}/targetHttpsProxies/{targetHttpsProxy}/setSslCertificates
/// Replaces SslCertificates for TargetHttpsProxy.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_target_https_proxies_set_ssl_certificates_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_target_https_proxies_set_ssl_certificates_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_target_https_proxies_set_ssl_certificates_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/targetHttpsProxies/{targetHttpsProxy}/setSslCertificates
/// Replaces SslCertificates for TargetHttpsProxy.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_target_https_proxies_set_ssl_certificates_builder()` + `compute_target_https_proxies_set_ssl_certificates_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_target_https_proxies_set_ssl_certificates(
    client: &SimpleHttpClient,
    project: &str,
    targetHttpsProxy: &str,
    requestId: Option<&str>,
    body: &TargetHttpsProxiesSetSslCertificatesRequest,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_target_https_proxies_set_ssl_certificates_builder(
        client,
        project,
        targetHttpsProxy,
        requestId,
        body,
    )?;
    compute_target_https_proxies_set_ssl_certificates_execute(builder)
}

/// GET projects/{project}/global/targetHttpsProxies/{targetHttpsProxy}/setSslPolicy
/// Sets the SSL policy for TargetHttpsProxy. The SSL policy specifies the server-side support for SSL features. This affects connections between clients and the HTTPS proxy load balancer. They do not affect the connection between the load balancer and the backends.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_target_https_proxies_set_ssl_policy_execute()` to send, or `compute_target_https_proxies_set_ssl_policy` for simplest API.

pub fn compute_target_https_proxies_set_ssl_policy_builder(
    client: &SimpleHttpClient,
    project: &str,
    targetHttpsProxy: &str,
    requestId: Option<&str>,
    body: &SslPolicyReference,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/global/targetHttpsProxies/{}/setSslPolicy",
        project, targetHttpsProxy,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "requestId",
            requestId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET projects/{project}/global/targetHttpsProxies/{targetHttpsProxy}/setSslPolicy
/// Sets the SSL policy for TargetHttpsProxy. The SSL policy specifies the server-side support for SSL features. This affects connections between clients and the HTTPS proxy load balancer. They do not affect the connection between the load balancer and the backends.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_target_https_proxies_set_ssl_policy_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_target_https_proxies_set_ssl_policy_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_target_https_proxies_set_ssl_policy_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/global/targetHttpsProxies/{targetHttpsProxy}/setSslPolicy
/// Sets the SSL policy for TargetHttpsProxy. The SSL policy specifies the server-side support for SSL features. This affects connections between clients and the HTTPS proxy load balancer. They do not affect the connection between the load balancer and the backends.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_target_https_proxies_set_ssl_policy_builder()` + `compute_target_https_proxies_set_ssl_policy_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_target_https_proxies_set_ssl_policy(
    client: &SimpleHttpClient,
    project: &str,
    targetHttpsProxy: &str,
    requestId: Option<&str>,
    body: &SslPolicyReference,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_target_https_proxies_set_ssl_policy_builder(
        client,
        project,
        targetHttpsProxy,
        requestId,
        body,
    )?;
    compute_target_https_proxies_set_ssl_policy_execute(builder)
}

/// GET projects/{project}/targetHttpsProxies/{targetHttpsProxy}/setUrlMap
/// Changes the URL map for TargetHttpsProxy.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_target_https_proxies_set_url_map_execute()` to send, or `compute_target_https_proxies_set_url_map` for simplest API.

pub fn compute_target_https_proxies_set_url_map_builder(
    client: &SimpleHttpClient,
    project: &str,
    targetHttpsProxy: &str,
    requestId: Option<&str>,
    body: &UrlMapReference,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/targetHttpsProxies/{}/setUrlMap",
        project, targetHttpsProxy,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "requestId",
            requestId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET projects/{project}/targetHttpsProxies/{targetHttpsProxy}/setUrlMap
/// Changes the URL map for TargetHttpsProxy.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_target_https_proxies_set_url_map_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_target_https_proxies_set_url_map_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_target_https_proxies_set_url_map_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/targetHttpsProxies/{targetHttpsProxy}/setUrlMap
/// Changes the URL map for TargetHttpsProxy.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_target_https_proxies_set_url_map_builder()` + `compute_target_https_proxies_set_url_map_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_target_https_proxies_set_url_map(
    client: &SimpleHttpClient,
    project: &str,
    targetHttpsProxy: &str,
    requestId: Option<&str>,
    body: &UrlMapReference,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_target_https_proxies_set_url_map_builder(
        client,
        project,
        targetHttpsProxy,
        requestId,
        body,
    )?;
    compute_target_https_proxies_set_url_map_execute(builder)
}

/// GET projects/{project}/aggregated/targetInstances
/// Retrieves an aggregated list of target instances.  To prevent failure, Google recommends that you set the `returnPartialSuccess` parameter to `true`.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_target_instances_aggregated_list_execute()` to send, or `compute_target_instances_aggregated_list` for simplest API.

pub fn compute_target_instances_aggregated_list_builder(
    client: &SimpleHttpClient,
    project: &str,
    filter: Option<&str>,
    includeAllScopes: Option<bool>,
    maxResults: Option<i32>,
    orderBy: Option<&str>,
    pageToken: Option<&str>,
    returnPartialSuccess: Option<bool>,
    serviceProjectNumber: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/aggregated/targetInstances",
        project,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "filter",
            filter
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "includeAllScopes",
            includeAllScopes
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "maxResults",
            maxResults
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "orderBy",
            orderBy
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageToken",
            pageToken
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "returnPartialSuccess",
            returnPartialSuccess
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "serviceProjectNumber",
            serviceProjectNumber
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET projects/{project}/aggregated/targetInstances
/// Retrieves an aggregated list of target instances.  To prevent failure, Google recommends that you set the `returnPartialSuccess` parameter to `true`.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_target_instances_aggregated_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_target_instances_aggregated_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_target_instances_aggregated_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<TargetInstanceAggregatedList>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: TargetInstanceAggregatedList = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/aggregated/targetInstances
/// Retrieves an aggregated list of target instances.  To prevent failure, Google recommends that you set the `returnPartialSuccess` parameter to `true`.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_target_instances_aggregated_list_builder()` + `compute_target_instances_aggregated_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_target_instances_aggregated_list(
    client: &SimpleHttpClient,
    project: &str,
    filter: Option<&str>,
    includeAllScopes: Option<bool>,
    maxResults: Option<i32>,
    orderBy: Option<&str>,
    pageToken: Option<&str>,
    returnPartialSuccess: Option<bool>,
    serviceProjectNumber: Option<&str>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<TargetInstanceAggregatedList>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = compute_target_instances_aggregated_list_builder(
        client,
        project,
        filter,
        includeAllScopes,
        maxResults,
        orderBy,
        pageToken,
        returnPartialSuccess,
        serviceProjectNumber,
    )?;
    compute_target_instances_aggregated_list_execute(builder)
}

/// GET projects/{project}/zones/{zone}/targetInstances/{targetInstance}
/// Deletes the specified TargetInstance resource.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_target_instances_delete_execute()` to send, or `compute_target_instances_delete` for simplest API.

pub fn compute_target_instances_delete_builder(
    client: &SimpleHttpClient,
    project: &str,
    targetInstance: &str,
    zone: &str,
    requestId: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/zones/{}/targetInstances/{}",
        project, targetInstance, zone,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "requestId",
            requestId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET projects/{project}/zones/{zone}/targetInstances/{targetInstance}
/// Deletes the specified TargetInstance resource.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_target_instances_delete_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_target_instances_delete_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_target_instances_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/zones/{zone}/targetInstances/{targetInstance}
/// Deletes the specified TargetInstance resource.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_target_instances_delete_builder()` + `compute_target_instances_delete_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_target_instances_delete(
    client: &SimpleHttpClient,
    project: &str,
    targetInstance: &str,
    zone: &str,
    requestId: Option<&str>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        compute_target_instances_delete_builder(client, project, targetInstance, zone, requestId)?;
    compute_target_instances_delete_execute(builder)
}

/// GET projects/{project}/zones/{zone}/targetInstances/{targetInstance}
/// Returns the specified TargetInstance resource.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_target_instances_get_execute()` to send, or `compute_target_instances_get` for simplest API.

pub fn compute_target_instances_get_builder(
    client: &SimpleHttpClient,
    project: &str,
    targetInstance: &str,
    zone: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/zones/{}/targetInstances/{}",
        project, targetInstance, zone,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/zones/{zone}/targetInstances/{targetInstance}
/// Returns the specified TargetInstance resource.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_target_instances_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_target_instances_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_target_instances_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<TargetInstance>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: TargetInstance = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/zones/{zone}/targetInstances/{targetInstance}
/// Returns the specified TargetInstance resource.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_target_instances_get_builder()` + `compute_target_instances_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_target_instances_get(
    client: &SimpleHttpClient,
    project: &str,
    targetInstance: &str,
    zone: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<TargetInstance>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = compute_target_instances_get_builder(client, project, targetInstance, zone)?;
    compute_target_instances_get_execute(builder)
}

/// GET projects/{project}/zones/{zone}/targetInstances
/// Creates a TargetInstance resource in the specified project and zone using the data included in the request.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_target_instances_insert_execute()` to send, or `compute_target_instances_insert` for simplest API.

pub fn compute_target_instances_insert_builder(
    client: &SimpleHttpClient,
    project: &str,
    zone: &str,
    requestId: Option<&str>,
    body: &TargetInstance,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/zones/{}/targetInstances",
        project, zone,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "requestId",
            requestId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET projects/{project}/zones/{zone}/targetInstances
/// Creates a TargetInstance resource in the specified project and zone using the data included in the request.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_target_instances_insert_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_target_instances_insert_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_target_instances_insert_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/zones/{zone}/targetInstances
/// Creates a TargetInstance resource in the specified project and zone using the data included in the request.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_target_instances_insert_builder()` + `compute_target_instances_insert_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_target_instances_insert(
    client: &SimpleHttpClient,
    project: &str,
    zone: &str,
    requestId: Option<&str>,
    body: &TargetInstance,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_target_instances_insert_builder(client, project, zone, requestId, body)?;
    compute_target_instances_insert_execute(builder)
}

/// GET projects/{project}/zones/{zone}/targetInstances
/// Retrieves a list of TargetInstance resources available to the specified project and zone.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_target_instances_list_execute()` to send, or `compute_target_instances_list` for simplest API.

pub fn compute_target_instances_list_builder(
    client: &SimpleHttpClient,
    project: &str,
    zone: &str,
    filter: Option<&str>,
    maxResults: Option<i32>,
    orderBy: Option<&str>,
    pageToken: Option<&str>,
    returnPartialSuccess: Option<bool>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/zones/{}/targetInstances",
        project, zone,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "filter",
            filter
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "maxResults",
            maxResults
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "orderBy",
            orderBy
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageToken",
            pageToken
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "returnPartialSuccess",
            returnPartialSuccess
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET projects/{project}/zones/{zone}/targetInstances
/// Retrieves a list of TargetInstance resources available to the specified project and zone.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_target_instances_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_target_instances_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_target_instances_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<TargetInstanceList>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: TargetInstanceList = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/zones/{zone}/targetInstances
/// Retrieves a list of TargetInstance resources available to the specified project and zone.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_target_instances_list_builder()` + `compute_target_instances_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_target_instances_list(
    client: &SimpleHttpClient,
    project: &str,
    zone: &str,
    filter: Option<&str>,
    maxResults: Option<i32>,
    orderBy: Option<&str>,
    pageToken: Option<&str>,
    returnPartialSuccess: Option<bool>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<TargetInstanceList>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = compute_target_instances_list_builder(
        client,
        project,
        zone,
        filter,
        maxResults,
        orderBy,
        pageToken,
        returnPartialSuccess,
    )?;
    compute_target_instances_list_execute(builder)
}

/// GET projects/{project}/zones/{zone}/targetInstances/{targetInstance}/setSecurityPolicy
/// Sets the Google Cloud Armor security policy for the specified target instance. For more information, `seeGoogle` Cloud Armor Overview
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_target_instances_set_security_policy_execute()` to send, or `compute_target_instances_set_security_policy` for simplest API.

pub fn compute_target_instances_set_security_policy_builder(
    client: &SimpleHttpClient,
    project: &str,
    targetInstance: &str,
    zone: &str,
    requestId: Option<&str>,
    body: &SecurityPolicyReference,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/zones/{}/targetInstances/{}/setSecurityPolicy",
        project, targetInstance, zone,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "requestId",
            requestId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET projects/{project}/zones/{zone}/targetInstances/{targetInstance}/setSecurityPolicy
/// Sets the Google Cloud Armor security policy for the specified target instance. For more information, `seeGoogle` Cloud Armor Overview
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_target_instances_set_security_policy_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_target_instances_set_security_policy_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_target_instances_set_security_policy_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/zones/{zone}/targetInstances/{targetInstance}/setSecurityPolicy
/// Sets the Google Cloud Armor security policy for the specified target instance. For more information, `seeGoogle` Cloud Armor Overview
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_target_instances_set_security_policy_builder()` + `compute_target_instances_set_security_policy_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_target_instances_set_security_policy(
    client: &SimpleHttpClient,
    project: &str,
    targetInstance: &str,
    zone: &str,
    requestId: Option<&str>,
    body: &SecurityPolicyReference,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_target_instances_set_security_policy_builder(
        client,
        project,
        targetInstance,
        zone,
        requestId,
        body,
    )?;
    compute_target_instances_set_security_policy_execute(builder)
}

/// GET projects/{project}/zones/{zone}/targetInstances/{resource}/testIamPermissions
/// Returns permissions that a caller has on the specified resource.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_target_instances_test_iam_permissions_execute()` to send, or `compute_target_instances_test_iam_permissions` for simplest API.

pub fn compute_target_instances_test_iam_permissions_builder(
    client: &SimpleHttpClient,
    project: &str,
    resource: &str,
    zone: &str,
    body: &TestPermissionsRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/zones/{}/targetInstances/{}/testIamPermissions",
        project, resource, zone,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET projects/{project}/zones/{zone}/targetInstances/{resource}/testIamPermissions
/// Returns permissions that a caller has on the specified resource.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_target_instances_test_iam_permissions_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_target_instances_test_iam_permissions_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_target_instances_test_iam_permissions_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<TestPermissionsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: TestPermissionsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/zones/{zone}/targetInstances/{resource}/testIamPermissions
/// Returns permissions that a caller has on the specified resource.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_target_instances_test_iam_permissions_builder()` + `compute_target_instances_test_iam_permissions_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_target_instances_test_iam_permissions(
    client: &SimpleHttpClient,
    project: &str,
    resource: &str,
    zone: &str,
    body: &TestPermissionsRequest,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<TestPermissionsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = compute_target_instances_test_iam_permissions_builder(
        client, project, resource, zone, body,
    )?;
    compute_target_instances_test_iam_permissions_execute(builder)
}

/// GET projects/{project}/regions/{region}/targetPools/{targetPool}/addHealthCheck
/// Adds health check URLs to a target pool.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_target_pools_add_health_check_execute()` to send, or `compute_target_pools_add_health_check` for simplest API.

pub fn compute_target_pools_add_health_check_builder(
    client: &SimpleHttpClient,
    project: &str,
    region: &str,
    targetPool: &str,
    requestId: Option<&str>,
    body: &TargetPoolsAddHealthCheckRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/regions/{}/targetPools/{}/addHealthCheck",
        project, region, targetPool,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "requestId",
            requestId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET projects/{project}/regions/{region}/targetPools/{targetPool}/addHealthCheck
/// Adds health check URLs to a target pool.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_target_pools_add_health_check_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_target_pools_add_health_check_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_target_pools_add_health_check_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/regions/{region}/targetPools/{targetPool}/addHealthCheck
/// Adds health check URLs to a target pool.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_target_pools_add_health_check_builder()` + `compute_target_pools_add_health_check_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_target_pools_add_health_check(
    client: &SimpleHttpClient,
    project: &str,
    region: &str,
    targetPool: &str,
    requestId: Option<&str>,
    body: &TargetPoolsAddHealthCheckRequest,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_target_pools_add_health_check_builder(
        client, project, region, targetPool, requestId, body,
    )?;
    compute_target_pools_add_health_check_execute(builder)
}

/// GET projects/{project}/regions/{region}/targetPools/{targetPool}/addInstance
/// Adds an instance to a target pool.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_target_pools_add_instance_execute()` to send, or `compute_target_pools_add_instance` for simplest API.

pub fn compute_target_pools_add_instance_builder(
    client: &SimpleHttpClient,
    project: &str,
    region: &str,
    targetPool: &str,
    requestId: Option<&str>,
    body: &TargetPoolsAddInstanceRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/regions/{}/targetPools/{}/addInstance",
        project, region, targetPool,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "requestId",
            requestId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET projects/{project}/regions/{region}/targetPools/{targetPool}/addInstance
/// Adds an instance to a target pool.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_target_pools_add_instance_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_target_pools_add_instance_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_target_pools_add_instance_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/regions/{region}/targetPools/{targetPool}/addInstance
/// Adds an instance to a target pool.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_target_pools_add_instance_builder()` + `compute_target_pools_add_instance_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_target_pools_add_instance(
    client: &SimpleHttpClient,
    project: &str,
    region: &str,
    targetPool: &str,
    requestId: Option<&str>,
    body: &TargetPoolsAddInstanceRequest,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_target_pools_add_instance_builder(
        client, project, region, targetPool, requestId, body,
    )?;
    compute_target_pools_add_instance_execute(builder)
}

/// GET projects/{project}/aggregated/targetPools
/// Retrieves an aggregated list of target pools.  To prevent failure, Google recommends that you set the `returnPartialSuccess` parameter to `true`.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_target_pools_aggregated_list_execute()` to send, or `compute_target_pools_aggregated_list` for simplest API.

pub fn compute_target_pools_aggregated_list_builder(
    client: &SimpleHttpClient,
    project: &str,
    filter: Option<&str>,
    includeAllScopes: Option<bool>,
    maxResults: Option<i32>,
    orderBy: Option<&str>,
    pageToken: Option<&str>,
    returnPartialSuccess: Option<bool>,
    serviceProjectNumber: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/aggregated/targetPools",
        project,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "filter",
            filter
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "includeAllScopes",
            includeAllScopes
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "maxResults",
            maxResults
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "orderBy",
            orderBy
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageToken",
            pageToken
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "returnPartialSuccess",
            returnPartialSuccess
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "serviceProjectNumber",
            serviceProjectNumber
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET projects/{project}/aggregated/targetPools
/// Retrieves an aggregated list of target pools.  To prevent failure, Google recommends that you set the `returnPartialSuccess` parameter to `true`.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_target_pools_aggregated_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_target_pools_aggregated_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_target_pools_aggregated_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<TargetPoolAggregatedList>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: TargetPoolAggregatedList = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/aggregated/targetPools
/// Retrieves an aggregated list of target pools.  To prevent failure, Google recommends that you set the `returnPartialSuccess` parameter to `true`.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_target_pools_aggregated_list_builder()` + `compute_target_pools_aggregated_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_target_pools_aggregated_list(
    client: &SimpleHttpClient,
    project: &str,
    filter: Option<&str>,
    includeAllScopes: Option<bool>,
    maxResults: Option<i32>,
    orderBy: Option<&str>,
    pageToken: Option<&str>,
    returnPartialSuccess: Option<bool>,
    serviceProjectNumber: Option<&str>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<TargetPoolAggregatedList>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = compute_target_pools_aggregated_list_builder(
        client,
        project,
        filter,
        includeAllScopes,
        maxResults,
        orderBy,
        pageToken,
        returnPartialSuccess,
        serviceProjectNumber,
    )?;
    compute_target_pools_aggregated_list_execute(builder)
}

/// GET projects/{project}/regions/{region}/targetPools/{targetPool}
/// Deletes the specified target pool.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_target_pools_delete_execute()` to send, or `compute_target_pools_delete` for simplest API.

pub fn compute_target_pools_delete_builder(
    client: &SimpleHttpClient,
    project: &str,
    region: &str,
    targetPool: &str,
    requestId: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/regions/{}/targetPools/{}",
        project, region, targetPool,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "requestId",
            requestId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET projects/{project}/regions/{region}/targetPools/{targetPool}
/// Deletes the specified target pool.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_target_pools_delete_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_target_pools_delete_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_target_pools_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/regions/{region}/targetPools/{targetPool}
/// Deletes the specified target pool.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_target_pools_delete_builder()` + `compute_target_pools_delete_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_target_pools_delete(
    client: &SimpleHttpClient,
    project: &str,
    region: &str,
    targetPool: &str,
    requestId: Option<&str>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        compute_target_pools_delete_builder(client, project, region, targetPool, requestId)?;
    compute_target_pools_delete_execute(builder)
}

/// GET projects/{project}/regions/{region}/targetPools/{targetPool}
/// Returns the specified target pool.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_target_pools_get_execute()` to send, or `compute_target_pools_get` for simplest API.

pub fn compute_target_pools_get_builder(
    client: &SimpleHttpClient,
    project: &str,
    region: &str,
    targetPool: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/regions/{}/targetPools/{}",
        project, region, targetPool,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/regions/{region}/targetPools/{targetPool}
/// Returns the specified target pool.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_target_pools_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_target_pools_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_target_pools_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<TargetPool>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: TargetPool = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/regions/{region}/targetPools/{targetPool}
/// Returns the specified target pool.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_target_pools_get_builder()` + `compute_target_pools_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_target_pools_get(
    client: &SimpleHttpClient,
    project: &str,
    region: &str,
    targetPool: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<TargetPool>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_target_pools_get_builder(client, project, region, targetPool)?;
    compute_target_pools_get_execute(builder)
}

/// GET projects/{project}/regions/{region}/targetPools/{targetPool}/getHealth
/// Gets the most recent health check results for each IP for the instance that is referenced by the given target pool.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_target_pools_get_health_execute()` to send, or `compute_target_pools_get_health` for simplest API.

pub fn compute_target_pools_get_health_builder(
    client: &SimpleHttpClient,
    project: &str,
    region: &str,
    targetPool: &str,
    body: &InstanceReference,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/regions/{}/targetPools/{}/getHealth",
        project, region, targetPool,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET projects/{project}/regions/{region}/targetPools/{targetPool}/getHealth
/// Gets the most recent health check results for each IP for the instance that is referenced by the given target pool.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_target_pools_get_health_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_target_pools_get_health_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_target_pools_get_health_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<TargetPoolInstanceHealth>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: TargetPoolInstanceHealth = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/regions/{region}/targetPools/{targetPool}/getHealth
/// Gets the most recent health check results for each IP for the instance that is referenced by the given target pool.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_target_pools_get_health_builder()` + `compute_target_pools_get_health_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_target_pools_get_health(
    client: &SimpleHttpClient,
    project: &str,
    region: &str,
    targetPool: &str,
    body: &InstanceReference,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<TargetPoolInstanceHealth>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        compute_target_pools_get_health_builder(client, project, region, targetPool, body)?;
    compute_target_pools_get_health_execute(builder)
}

/// GET projects/{project}/regions/{region}/targetPools
/// Creates a target pool in the specified project and region using the data included in the request.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_target_pools_insert_execute()` to send, or `compute_target_pools_insert` for simplest API.

pub fn compute_target_pools_insert_builder(
    client: &SimpleHttpClient,
    project: &str,
    region: &str,
    requestId: Option<&str>,
    body: &TargetPool,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/regions/{}/targetPools",
        project, region,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "requestId",
            requestId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET projects/{project}/regions/{region}/targetPools
/// Creates a target pool in the specified project and region using the data included in the request.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_target_pools_insert_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_target_pools_insert_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_target_pools_insert_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/regions/{region}/targetPools
/// Creates a target pool in the specified project and region using the data included in the request.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_target_pools_insert_builder()` + `compute_target_pools_insert_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_target_pools_insert(
    client: &SimpleHttpClient,
    project: &str,
    region: &str,
    requestId: Option<&str>,
    body: &TargetPool,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_target_pools_insert_builder(client, project, region, requestId, body)?;
    compute_target_pools_insert_execute(builder)
}

/// GET projects/{project}/regions/{region}/targetPools
/// Retrieves a list of target pools available to the specified project and region.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_target_pools_list_execute()` to send, or `compute_target_pools_list` for simplest API.

pub fn compute_target_pools_list_builder(
    client: &SimpleHttpClient,
    project: &str,
    region: &str,
    filter: Option<&str>,
    maxResults: Option<i32>,
    orderBy: Option<&str>,
    pageToken: Option<&str>,
    returnPartialSuccess: Option<bool>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/regions/{}/targetPools",
        project, region,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "filter",
            filter
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "maxResults",
            maxResults
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "orderBy",
            orderBy
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageToken",
            pageToken
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "returnPartialSuccess",
            returnPartialSuccess
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET projects/{project}/regions/{region}/targetPools
/// Retrieves a list of target pools available to the specified project and region.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_target_pools_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_target_pools_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_target_pools_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<TargetPoolList>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: TargetPoolList = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/regions/{region}/targetPools
/// Retrieves a list of target pools available to the specified project and region.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_target_pools_list_builder()` + `compute_target_pools_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_target_pools_list(
    client: &SimpleHttpClient,
    project: &str,
    region: &str,
    filter: Option<&str>,
    maxResults: Option<i32>,
    orderBy: Option<&str>,
    pageToken: Option<&str>,
    returnPartialSuccess: Option<bool>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<TargetPoolList>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = compute_target_pools_list_builder(
        client,
        project,
        region,
        filter,
        maxResults,
        orderBy,
        pageToken,
        returnPartialSuccess,
    )?;
    compute_target_pools_list_execute(builder)
}

/// GET projects/{project}/regions/{region}/targetPools/{targetPool}/removeHealthCheck
/// Removes health check URL from a target pool.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_target_pools_remove_health_check_execute()` to send, or `compute_target_pools_remove_health_check` for simplest API.

pub fn compute_target_pools_remove_health_check_builder(
    client: &SimpleHttpClient,
    project: &str,
    region: &str,
    targetPool: &str,
    requestId: Option<&str>,
    body: &TargetPoolsRemoveHealthCheckRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/regions/{}/targetPools/{}/removeHealthCheck",
        project, region, targetPool,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "requestId",
            requestId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET projects/{project}/regions/{region}/targetPools/{targetPool}/removeHealthCheck
/// Removes health check URL from a target pool.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_target_pools_remove_health_check_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_target_pools_remove_health_check_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_target_pools_remove_health_check_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/regions/{region}/targetPools/{targetPool}/removeHealthCheck
/// Removes health check URL from a target pool.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_target_pools_remove_health_check_builder()` + `compute_target_pools_remove_health_check_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_target_pools_remove_health_check(
    client: &SimpleHttpClient,
    project: &str,
    region: &str,
    targetPool: &str,
    requestId: Option<&str>,
    body: &TargetPoolsRemoveHealthCheckRequest,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_target_pools_remove_health_check_builder(
        client, project, region, targetPool, requestId, body,
    )?;
    compute_target_pools_remove_health_check_execute(builder)
}

/// GET projects/{project}/regions/{region}/targetPools/{targetPool}/removeInstance
/// Removes instance URL from a target pool.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_target_pools_remove_instance_execute()` to send, or `compute_target_pools_remove_instance` for simplest API.

pub fn compute_target_pools_remove_instance_builder(
    client: &SimpleHttpClient,
    project: &str,
    region: &str,
    targetPool: &str,
    requestId: Option<&str>,
    body: &TargetPoolsRemoveInstanceRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/regions/{}/targetPools/{}/removeInstance",
        project, region, targetPool,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "requestId",
            requestId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET projects/{project}/regions/{region}/targetPools/{targetPool}/removeInstance
/// Removes instance URL from a target pool.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_target_pools_remove_instance_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_target_pools_remove_instance_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_target_pools_remove_instance_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/regions/{region}/targetPools/{targetPool}/removeInstance
/// Removes instance URL from a target pool.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_target_pools_remove_instance_builder()` + `compute_target_pools_remove_instance_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_target_pools_remove_instance(
    client: &SimpleHttpClient,
    project: &str,
    region: &str,
    targetPool: &str,
    requestId: Option<&str>,
    body: &TargetPoolsRemoveInstanceRequest,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_target_pools_remove_instance_builder(
        client, project, region, targetPool, requestId, body,
    )?;
    compute_target_pools_remove_instance_execute(builder)
}

/// GET projects/{project}/regions/{region}/targetPools/{targetPool}/setBackup
/// Changes a backup target pool's configurations.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_target_pools_set_backup_execute()` to send, or `compute_target_pools_set_backup` for simplest API.

pub fn compute_target_pools_set_backup_builder(
    client: &SimpleHttpClient,
    project: &str,
    region: &str,
    targetPool: &str,
    failoverRatio: Option<f32>,
    requestId: Option<&str>,
    body: &TargetReference,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/regions/{}/targetPools/{}/setBackup",
        project, region, targetPool,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "failoverRatio",
            failoverRatio
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "requestId",
            requestId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET projects/{project}/regions/{region}/targetPools/{targetPool}/setBackup
/// Changes a backup target pool's configurations.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_target_pools_set_backup_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_target_pools_set_backup_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_target_pools_set_backup_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/regions/{region}/targetPools/{targetPool}/setBackup
/// Changes a backup target pool's configurations.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_target_pools_set_backup_builder()` + `compute_target_pools_set_backup_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_target_pools_set_backup(
    client: &SimpleHttpClient,
    project: &str,
    region: &str,
    targetPool: &str,
    failoverRatio: Option<f32>,
    requestId: Option<&str>,
    body: &TargetReference,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_target_pools_set_backup_builder(
        client,
        project,
        region,
        targetPool,
        failoverRatio,
        requestId,
        body,
    )?;
    compute_target_pools_set_backup_execute(builder)
}

/// GET projects/{project}/regions/{region}/targetPools/{targetPool}/setSecurityPolicy
/// Sets the Google Cloud Armor security policy for the specified target pool. For more information, `seeGoogle` Cloud Armor Overview
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_target_pools_set_security_policy_execute()` to send, or `compute_target_pools_set_security_policy` for simplest API.

pub fn compute_target_pools_set_security_policy_builder(
    client: &SimpleHttpClient,
    project: &str,
    region: &str,
    targetPool: &str,
    requestId: Option<&str>,
    body: &SecurityPolicyReference,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/regions/{}/targetPools/{}/setSecurityPolicy",
        project, region, targetPool,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "requestId",
            requestId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET projects/{project}/regions/{region}/targetPools/{targetPool}/setSecurityPolicy
/// Sets the Google Cloud Armor security policy for the specified target pool. For more information, `seeGoogle` Cloud Armor Overview
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_target_pools_set_security_policy_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_target_pools_set_security_policy_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_target_pools_set_security_policy_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/regions/{region}/targetPools/{targetPool}/setSecurityPolicy
/// Sets the Google Cloud Armor security policy for the specified target pool. For more information, `seeGoogle` Cloud Armor Overview
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_target_pools_set_security_policy_builder()` + `compute_target_pools_set_security_policy_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_target_pools_set_security_policy(
    client: &SimpleHttpClient,
    project: &str,
    region: &str,
    targetPool: &str,
    requestId: Option<&str>,
    body: &SecurityPolicyReference,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_target_pools_set_security_policy_builder(
        client, project, region, targetPool, requestId, body,
    )?;
    compute_target_pools_set_security_policy_execute(builder)
}

/// GET projects/{project}/regions/{region}/targetPools/{resource}/testIamPermissions
/// Returns permissions that a caller has on the specified resource.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_target_pools_test_iam_permissions_execute()` to send, or `compute_target_pools_test_iam_permissions` for simplest API.

pub fn compute_target_pools_test_iam_permissions_builder(
    client: &SimpleHttpClient,
    project: &str,
    region: &str,
    resource: &str,
    body: &TestPermissionsRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/regions/{}/targetPools/{}/testIamPermissions",
        project, region, resource,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET projects/{project}/regions/{region}/targetPools/{resource}/testIamPermissions
/// Returns permissions that a caller has on the specified resource.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_target_pools_test_iam_permissions_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_target_pools_test_iam_permissions_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_target_pools_test_iam_permissions_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<TestPermissionsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: TestPermissionsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/regions/{region}/targetPools/{resource}/testIamPermissions
/// Returns permissions that a caller has on the specified resource.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_target_pools_test_iam_permissions_builder()` + `compute_target_pools_test_iam_permissions_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_target_pools_test_iam_permissions(
    client: &SimpleHttpClient,
    project: &str,
    region: &str,
    resource: &str,
    body: &TestPermissionsRequest,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<TestPermissionsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        compute_target_pools_test_iam_permissions_builder(client, project, region, resource, body)?;
    compute_target_pools_test_iam_permissions_execute(builder)
}

/// GET projects/{project}/global/targetSslProxies/{targetSslProxy}
/// Deletes the specified TargetSslProxy resource.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_target_ssl_proxies_delete_execute()` to send, or `compute_target_ssl_proxies_delete` for simplest API.

pub fn compute_target_ssl_proxies_delete_builder(
    client: &SimpleHttpClient,
    project: &str,
    targetSslProxy: &str,
    requestId: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/global/targetSslProxies/{}",
        project, targetSslProxy,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "requestId",
            requestId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET projects/{project}/global/targetSslProxies/{targetSslProxy}
/// Deletes the specified TargetSslProxy resource.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_target_ssl_proxies_delete_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_target_ssl_proxies_delete_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_target_ssl_proxies_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/global/targetSslProxies/{targetSslProxy}
/// Deletes the specified TargetSslProxy resource.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_target_ssl_proxies_delete_builder()` + `compute_target_ssl_proxies_delete_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_target_ssl_proxies_delete(
    client: &SimpleHttpClient,
    project: &str,
    targetSslProxy: &str,
    requestId: Option<&str>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        compute_target_ssl_proxies_delete_builder(client, project, targetSslProxy, requestId)?;
    compute_target_ssl_proxies_delete_execute(builder)
}

/// GET projects/{project}/global/targetSslProxies/{targetSslProxy}
/// Returns the specified TargetSslProxy resource.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_target_ssl_proxies_get_execute()` to send, or `compute_target_ssl_proxies_get` for simplest API.

pub fn compute_target_ssl_proxies_get_builder(
    client: &SimpleHttpClient,
    project: &str,
    targetSslProxy: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/global/targetSslProxies/{}",
        project, targetSslProxy,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/global/targetSslProxies/{targetSslProxy}
/// Returns the specified TargetSslProxy resource.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_target_ssl_proxies_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_target_ssl_proxies_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_target_ssl_proxies_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<TargetSslProxy>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: TargetSslProxy = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/global/targetSslProxies/{targetSslProxy}
/// Returns the specified TargetSslProxy resource.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_target_ssl_proxies_get_builder()` + `compute_target_ssl_proxies_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_target_ssl_proxies_get(
    client: &SimpleHttpClient,
    project: &str,
    targetSslProxy: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<TargetSslProxy>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = compute_target_ssl_proxies_get_builder(client, project, targetSslProxy)?;
    compute_target_ssl_proxies_get_execute(builder)
}

/// GET projects/{project}/global/targetSslProxies
/// Creates a TargetSslProxy resource in the specified project using the data included in the request.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_target_ssl_proxies_insert_execute()` to send, or `compute_target_ssl_proxies_insert` for simplest API.

pub fn compute_target_ssl_proxies_insert_builder(
    client: &SimpleHttpClient,
    project: &str,
    requestId: Option<&str>,
    body: &TargetSslProxy,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/global/targetSslProxies",
        project,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "requestId",
            requestId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET projects/{project}/global/targetSslProxies
/// Creates a TargetSslProxy resource in the specified project using the data included in the request.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_target_ssl_proxies_insert_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_target_ssl_proxies_insert_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_target_ssl_proxies_insert_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/global/targetSslProxies
/// Creates a TargetSslProxy resource in the specified project using the data included in the request.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_target_ssl_proxies_insert_builder()` + `compute_target_ssl_proxies_insert_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_target_ssl_proxies_insert(
    client: &SimpleHttpClient,
    project: &str,
    requestId: Option<&str>,
    body: &TargetSslProxy,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_target_ssl_proxies_insert_builder(client, project, requestId, body)?;
    compute_target_ssl_proxies_insert_execute(builder)
}

/// GET projects/{project}/global/targetSslProxies
/// Retrieves the list of TargetSslProxy resources available to the specified project.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_target_ssl_proxies_list_execute()` to send, or `compute_target_ssl_proxies_list` for simplest API.

pub fn compute_target_ssl_proxies_list_builder(
    client: &SimpleHttpClient,
    project: &str,
    filter: Option<&str>,
    maxResults: Option<i32>,
    orderBy: Option<&str>,
    pageToken: Option<&str>,
    returnPartialSuccess: Option<bool>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/global/targetSslProxies",
        project,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "filter",
            filter
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "maxResults",
            maxResults
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "orderBy",
            orderBy
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageToken",
            pageToken
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "returnPartialSuccess",
            returnPartialSuccess
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET projects/{project}/global/targetSslProxies
/// Retrieves the list of TargetSslProxy resources available to the specified project.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_target_ssl_proxies_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_target_ssl_proxies_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_target_ssl_proxies_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<TargetSslProxyList>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: TargetSslProxyList = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/global/targetSslProxies
/// Retrieves the list of TargetSslProxy resources available to the specified project.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_target_ssl_proxies_list_builder()` + `compute_target_ssl_proxies_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_target_ssl_proxies_list(
    client: &SimpleHttpClient,
    project: &str,
    filter: Option<&str>,
    maxResults: Option<i32>,
    orderBy: Option<&str>,
    pageToken: Option<&str>,
    returnPartialSuccess: Option<bool>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<TargetSslProxyList>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = compute_target_ssl_proxies_list_builder(
        client,
        project,
        filter,
        maxResults,
        orderBy,
        pageToken,
        returnPartialSuccess,
    )?;
    compute_target_ssl_proxies_list_execute(builder)
}

/// GET projects/{project}/global/targetSslProxies/{targetSslProxy}/setBackendService
/// Changes the BackendService for TargetSslProxy.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_target_ssl_proxies_set_backend_service_execute()` to send, or `compute_target_ssl_proxies_set_backend_service` for simplest API.

pub fn compute_target_ssl_proxies_set_backend_service_builder(
    client: &SimpleHttpClient,
    project: &str,
    targetSslProxy: &str,
    requestId: Option<&str>,
    body: &TargetSslProxiesSetBackendServiceRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/global/targetSslProxies/{}/setBackendService",
        project, targetSslProxy,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "requestId",
            requestId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET projects/{project}/global/targetSslProxies/{targetSslProxy}/setBackendService
/// Changes the BackendService for TargetSslProxy.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_target_ssl_proxies_set_backend_service_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_target_ssl_proxies_set_backend_service_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_target_ssl_proxies_set_backend_service_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/global/targetSslProxies/{targetSslProxy}/setBackendService
/// Changes the BackendService for TargetSslProxy.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_target_ssl_proxies_set_backend_service_builder()` + `compute_target_ssl_proxies_set_backend_service_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_target_ssl_proxies_set_backend_service(
    client: &SimpleHttpClient,
    project: &str,
    targetSslProxy: &str,
    requestId: Option<&str>,
    body: &TargetSslProxiesSetBackendServiceRequest,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_target_ssl_proxies_set_backend_service_builder(
        client,
        project,
        targetSslProxy,
        requestId,
        body,
    )?;
    compute_target_ssl_proxies_set_backend_service_execute(builder)
}

/// GET projects/{project}/global/targetSslProxies/{targetSslProxy}/setCertificateMap
/// Changes the Certificate Map for TargetSslProxy.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_target_ssl_proxies_set_certificate_map_execute()` to send, or `compute_target_ssl_proxies_set_certificate_map` for simplest API.

pub fn compute_target_ssl_proxies_set_certificate_map_builder(
    client: &SimpleHttpClient,
    project: &str,
    targetSslProxy: &str,
    requestId: Option<&str>,
    body: &TargetSslProxiesSetCertificateMapRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/global/targetSslProxies/{}/setCertificateMap",
        project, targetSslProxy,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "requestId",
            requestId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET projects/{project}/global/targetSslProxies/{targetSslProxy}/setCertificateMap
/// Changes the Certificate Map for TargetSslProxy.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_target_ssl_proxies_set_certificate_map_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_target_ssl_proxies_set_certificate_map_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_target_ssl_proxies_set_certificate_map_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/global/targetSslProxies/{targetSslProxy}/setCertificateMap
/// Changes the Certificate Map for TargetSslProxy.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_target_ssl_proxies_set_certificate_map_builder()` + `compute_target_ssl_proxies_set_certificate_map_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_target_ssl_proxies_set_certificate_map(
    client: &SimpleHttpClient,
    project: &str,
    targetSslProxy: &str,
    requestId: Option<&str>,
    body: &TargetSslProxiesSetCertificateMapRequest,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_target_ssl_proxies_set_certificate_map_builder(
        client,
        project,
        targetSslProxy,
        requestId,
        body,
    )?;
    compute_target_ssl_proxies_set_certificate_map_execute(builder)
}

/// GET projects/{project}/global/targetSslProxies/{targetSslProxy}/setProxyHeader
/// Changes the ProxyHeaderType for TargetSslProxy.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_target_ssl_proxies_set_proxy_header_execute()` to send, or `compute_target_ssl_proxies_set_proxy_header` for simplest API.

pub fn compute_target_ssl_proxies_set_proxy_header_builder(
    client: &SimpleHttpClient,
    project: &str,
    targetSslProxy: &str,
    requestId: Option<&str>,
    body: &TargetSslProxiesSetProxyHeaderRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/global/targetSslProxies/{}/setProxyHeader",
        project, targetSslProxy,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "requestId",
            requestId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET projects/{project}/global/targetSslProxies/{targetSslProxy}/setProxyHeader
/// Changes the ProxyHeaderType for TargetSslProxy.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_target_ssl_proxies_set_proxy_header_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_target_ssl_proxies_set_proxy_header_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_target_ssl_proxies_set_proxy_header_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/global/targetSslProxies/{targetSslProxy}/setProxyHeader
/// Changes the ProxyHeaderType for TargetSslProxy.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_target_ssl_proxies_set_proxy_header_builder()` + `compute_target_ssl_proxies_set_proxy_header_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_target_ssl_proxies_set_proxy_header(
    client: &SimpleHttpClient,
    project: &str,
    targetSslProxy: &str,
    requestId: Option<&str>,
    body: &TargetSslProxiesSetProxyHeaderRequest,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_target_ssl_proxies_set_proxy_header_builder(
        client,
        project,
        targetSslProxy,
        requestId,
        body,
    )?;
    compute_target_ssl_proxies_set_proxy_header_execute(builder)
}

/// GET projects/{project}/global/targetSslProxies/{targetSslProxy}/setSslCertificates
/// Changes SslCertificates for TargetSslProxy.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_target_ssl_proxies_set_ssl_certificates_execute()` to send, or `compute_target_ssl_proxies_set_ssl_certificates` for simplest API.

pub fn compute_target_ssl_proxies_set_ssl_certificates_builder(
    client: &SimpleHttpClient,
    project: &str,
    targetSslProxy: &str,
    requestId: Option<&str>,
    body: &TargetSslProxiesSetSslCertificatesRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/global/targetSslProxies/{}/setSslCertificates",
        project, targetSslProxy,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "requestId",
            requestId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET projects/{project}/global/targetSslProxies/{targetSslProxy}/setSslCertificates
/// Changes SslCertificates for TargetSslProxy.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_target_ssl_proxies_set_ssl_certificates_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_target_ssl_proxies_set_ssl_certificates_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_target_ssl_proxies_set_ssl_certificates_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/global/targetSslProxies/{targetSslProxy}/setSslCertificates
/// Changes SslCertificates for TargetSslProxy.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_target_ssl_proxies_set_ssl_certificates_builder()` + `compute_target_ssl_proxies_set_ssl_certificates_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_target_ssl_proxies_set_ssl_certificates(
    client: &SimpleHttpClient,
    project: &str,
    targetSslProxy: &str,
    requestId: Option<&str>,
    body: &TargetSslProxiesSetSslCertificatesRequest,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_target_ssl_proxies_set_ssl_certificates_builder(
        client,
        project,
        targetSslProxy,
        requestId,
        body,
    )?;
    compute_target_ssl_proxies_set_ssl_certificates_execute(builder)
}

/// GET projects/{project}/global/targetSslProxies/{targetSslProxy}/setSslPolicy
/// Sets the SSL policy for TargetSslProxy. The SSL policy specifies the server-side support for SSL features. This affects connections between clients and the load balancer. They do not affect the connection between the load balancer and the backends.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_target_ssl_proxies_set_ssl_policy_execute()` to send, or `compute_target_ssl_proxies_set_ssl_policy` for simplest API.

pub fn compute_target_ssl_proxies_set_ssl_policy_builder(
    client: &SimpleHttpClient,
    project: &str,
    targetSslProxy: &str,
    requestId: Option<&str>,
    body: &SslPolicyReference,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/global/targetSslProxies/{}/setSslPolicy",
        project, targetSslProxy,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "requestId",
            requestId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET projects/{project}/global/targetSslProxies/{targetSslProxy}/setSslPolicy
/// Sets the SSL policy for TargetSslProxy. The SSL policy specifies the server-side support for SSL features. This affects connections between clients and the load balancer. They do not affect the connection between the load balancer and the backends.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_target_ssl_proxies_set_ssl_policy_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_target_ssl_proxies_set_ssl_policy_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_target_ssl_proxies_set_ssl_policy_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/global/targetSslProxies/{targetSslProxy}/setSslPolicy
/// Sets the SSL policy for TargetSslProxy. The SSL policy specifies the server-side support for SSL features. This affects connections between clients and the load balancer. They do not affect the connection between the load balancer and the backends.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_target_ssl_proxies_set_ssl_policy_builder()` + `compute_target_ssl_proxies_set_ssl_policy_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_target_ssl_proxies_set_ssl_policy(
    client: &SimpleHttpClient,
    project: &str,
    targetSslProxy: &str,
    requestId: Option<&str>,
    body: &SslPolicyReference,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_target_ssl_proxies_set_ssl_policy_builder(
        client,
        project,
        targetSslProxy,
        requestId,
        body,
    )?;
    compute_target_ssl_proxies_set_ssl_policy_execute(builder)
}

/// GET projects/{project}/global/targetSslProxies/{resource}/testIamPermissions
/// Returns permissions that a caller has on the specified resource.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_target_ssl_proxies_test_iam_permissions_execute()` to send, or `compute_target_ssl_proxies_test_iam_permissions` for simplest API.

pub fn compute_target_ssl_proxies_test_iam_permissions_builder(
    client: &SimpleHttpClient,
    project: &str,
    resource: &str,
    body: &TestPermissionsRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/global/targetSslProxies/{}/testIamPermissions",
        project, resource,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET projects/{project}/global/targetSslProxies/{resource}/testIamPermissions
/// Returns permissions that a caller has on the specified resource.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_target_ssl_proxies_test_iam_permissions_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_target_ssl_proxies_test_iam_permissions_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_target_ssl_proxies_test_iam_permissions_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<TestPermissionsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: TestPermissionsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/global/targetSslProxies/{resource}/testIamPermissions
/// Returns permissions that a caller has on the specified resource.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_target_ssl_proxies_test_iam_permissions_builder()` + `compute_target_ssl_proxies_test_iam_permissions_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_target_ssl_proxies_test_iam_permissions(
    client: &SimpleHttpClient,
    project: &str,
    resource: &str,
    body: &TestPermissionsRequest,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<TestPermissionsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        compute_target_ssl_proxies_test_iam_permissions_builder(client, project, resource, body)?;
    compute_target_ssl_proxies_test_iam_permissions_execute(builder)
}

/// GET projects/{project}/aggregated/targetTcpProxies
/// Retrieves the list of all TargetTcpProxy resources, regional and global, available to the specified project.  To prevent failure, Google recommends that you set the `returnPartialSuccess` parameter to `true`.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_target_tcp_proxies_aggregated_list_execute()` to send, or `compute_target_tcp_proxies_aggregated_list` for simplest API.

pub fn compute_target_tcp_proxies_aggregated_list_builder(
    client: &SimpleHttpClient,
    project: &str,
    filter: Option<&str>,
    includeAllScopes: Option<bool>,
    maxResults: Option<i32>,
    orderBy: Option<&str>,
    pageToken: Option<&str>,
    returnPartialSuccess: Option<bool>,
    serviceProjectNumber: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/aggregated/targetTcpProxies",
        project,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "filter",
            filter
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "includeAllScopes",
            includeAllScopes
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "maxResults",
            maxResults
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "orderBy",
            orderBy
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageToken",
            pageToken
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "returnPartialSuccess",
            returnPartialSuccess
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "serviceProjectNumber",
            serviceProjectNumber
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET projects/{project}/aggregated/targetTcpProxies
/// Retrieves the list of all TargetTcpProxy resources, regional and global, available to the specified project.  To prevent failure, Google recommends that you set the `returnPartialSuccess` parameter to `true`.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_target_tcp_proxies_aggregated_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_target_tcp_proxies_aggregated_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_target_tcp_proxies_aggregated_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<TargetTcpProxyAggregatedList>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: TargetTcpProxyAggregatedList = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/aggregated/targetTcpProxies
/// Retrieves the list of all TargetTcpProxy resources, regional and global, available to the specified project.  To prevent failure, Google recommends that you set the `returnPartialSuccess` parameter to `true`.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_target_tcp_proxies_aggregated_list_builder()` + `compute_target_tcp_proxies_aggregated_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_target_tcp_proxies_aggregated_list(
    client: &SimpleHttpClient,
    project: &str,
    filter: Option<&str>,
    includeAllScopes: Option<bool>,
    maxResults: Option<i32>,
    orderBy: Option<&str>,
    pageToken: Option<&str>,
    returnPartialSuccess: Option<bool>,
    serviceProjectNumber: Option<&str>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<TargetTcpProxyAggregatedList>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = compute_target_tcp_proxies_aggregated_list_builder(
        client,
        project,
        filter,
        includeAllScopes,
        maxResults,
        orderBy,
        pageToken,
        returnPartialSuccess,
        serviceProjectNumber,
    )?;
    compute_target_tcp_proxies_aggregated_list_execute(builder)
}

/// GET projects/{project}/global/targetTcpProxies/{targetTcpProxy}
/// Deletes the specified TargetTcpProxy resource.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_target_tcp_proxies_delete_execute()` to send, or `compute_target_tcp_proxies_delete` for simplest API.

pub fn compute_target_tcp_proxies_delete_builder(
    client: &SimpleHttpClient,
    project: &str,
    targetTcpProxy: &str,
    requestId: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/global/targetTcpProxies/{}",
        project, targetTcpProxy,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "requestId",
            requestId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET projects/{project}/global/targetTcpProxies/{targetTcpProxy}
/// Deletes the specified TargetTcpProxy resource.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_target_tcp_proxies_delete_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_target_tcp_proxies_delete_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_target_tcp_proxies_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/global/targetTcpProxies/{targetTcpProxy}
/// Deletes the specified TargetTcpProxy resource.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_target_tcp_proxies_delete_builder()` + `compute_target_tcp_proxies_delete_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_target_tcp_proxies_delete(
    client: &SimpleHttpClient,
    project: &str,
    targetTcpProxy: &str,
    requestId: Option<&str>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        compute_target_tcp_proxies_delete_builder(client, project, targetTcpProxy, requestId)?;
    compute_target_tcp_proxies_delete_execute(builder)
}

/// GET projects/{project}/global/targetTcpProxies/{targetTcpProxy}
/// Returns the specified TargetTcpProxy resource.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_target_tcp_proxies_get_execute()` to send, or `compute_target_tcp_proxies_get` for simplest API.

pub fn compute_target_tcp_proxies_get_builder(
    client: &SimpleHttpClient,
    project: &str,
    targetTcpProxy: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/global/targetTcpProxies/{}",
        project, targetTcpProxy,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/global/targetTcpProxies/{targetTcpProxy}
/// Returns the specified TargetTcpProxy resource.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_target_tcp_proxies_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_target_tcp_proxies_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_target_tcp_proxies_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<TargetTcpProxy>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: TargetTcpProxy = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/global/targetTcpProxies/{targetTcpProxy}
/// Returns the specified TargetTcpProxy resource.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_target_tcp_proxies_get_builder()` + `compute_target_tcp_proxies_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_target_tcp_proxies_get(
    client: &SimpleHttpClient,
    project: &str,
    targetTcpProxy: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<TargetTcpProxy>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = compute_target_tcp_proxies_get_builder(client, project, targetTcpProxy)?;
    compute_target_tcp_proxies_get_execute(builder)
}

/// GET projects/{project}/global/targetTcpProxies
/// Creates a TargetTcpProxy resource in the specified project using the data included in the request.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_target_tcp_proxies_insert_execute()` to send, or `compute_target_tcp_proxies_insert` for simplest API.

pub fn compute_target_tcp_proxies_insert_builder(
    client: &SimpleHttpClient,
    project: &str,
    requestId: Option<&str>,
    body: &TargetTcpProxy,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/global/targetTcpProxies",
        project,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "requestId",
            requestId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET projects/{project}/global/targetTcpProxies
/// Creates a TargetTcpProxy resource in the specified project using the data included in the request.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_target_tcp_proxies_insert_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_target_tcp_proxies_insert_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_target_tcp_proxies_insert_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/global/targetTcpProxies
/// Creates a TargetTcpProxy resource in the specified project using the data included in the request.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_target_tcp_proxies_insert_builder()` + `compute_target_tcp_proxies_insert_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_target_tcp_proxies_insert(
    client: &SimpleHttpClient,
    project: &str,
    requestId: Option<&str>,
    body: &TargetTcpProxy,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_target_tcp_proxies_insert_builder(client, project, requestId, body)?;
    compute_target_tcp_proxies_insert_execute(builder)
}

/// GET projects/{project}/global/targetTcpProxies
/// Retrieves the list of TargetTcpProxy resources available to the specified project.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_target_tcp_proxies_list_execute()` to send, or `compute_target_tcp_proxies_list` for simplest API.

pub fn compute_target_tcp_proxies_list_builder(
    client: &SimpleHttpClient,
    project: &str,
    filter: Option<&str>,
    maxResults: Option<i32>,
    orderBy: Option<&str>,
    pageToken: Option<&str>,
    returnPartialSuccess: Option<bool>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/global/targetTcpProxies",
        project,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "filter",
            filter
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "maxResults",
            maxResults
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "orderBy",
            orderBy
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageToken",
            pageToken
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "returnPartialSuccess",
            returnPartialSuccess
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET projects/{project}/global/targetTcpProxies
/// Retrieves the list of TargetTcpProxy resources available to the specified project.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_target_tcp_proxies_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_target_tcp_proxies_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_target_tcp_proxies_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<TargetTcpProxyList>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: TargetTcpProxyList = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/global/targetTcpProxies
/// Retrieves the list of TargetTcpProxy resources available to the specified project.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_target_tcp_proxies_list_builder()` + `compute_target_tcp_proxies_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_target_tcp_proxies_list(
    client: &SimpleHttpClient,
    project: &str,
    filter: Option<&str>,
    maxResults: Option<i32>,
    orderBy: Option<&str>,
    pageToken: Option<&str>,
    returnPartialSuccess: Option<bool>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<TargetTcpProxyList>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = compute_target_tcp_proxies_list_builder(
        client,
        project,
        filter,
        maxResults,
        orderBy,
        pageToken,
        returnPartialSuccess,
    )?;
    compute_target_tcp_proxies_list_execute(builder)
}

/// GET projects/{project}/global/targetTcpProxies/{targetTcpProxy}/setBackendService
/// Changes the BackendService for TargetTcpProxy.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_target_tcp_proxies_set_backend_service_execute()` to send, or `compute_target_tcp_proxies_set_backend_service` for simplest API.

pub fn compute_target_tcp_proxies_set_backend_service_builder(
    client: &SimpleHttpClient,
    project: &str,
    targetTcpProxy: &str,
    requestId: Option<&str>,
    body: &TargetTcpProxiesSetBackendServiceRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/global/targetTcpProxies/{}/setBackendService",
        project, targetTcpProxy,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "requestId",
            requestId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET projects/{project}/global/targetTcpProxies/{targetTcpProxy}/setBackendService
/// Changes the BackendService for TargetTcpProxy.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_target_tcp_proxies_set_backend_service_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_target_tcp_proxies_set_backend_service_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_target_tcp_proxies_set_backend_service_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/global/targetTcpProxies/{targetTcpProxy}/setBackendService
/// Changes the BackendService for TargetTcpProxy.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_target_tcp_proxies_set_backend_service_builder()` + `compute_target_tcp_proxies_set_backend_service_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_target_tcp_proxies_set_backend_service(
    client: &SimpleHttpClient,
    project: &str,
    targetTcpProxy: &str,
    requestId: Option<&str>,
    body: &TargetTcpProxiesSetBackendServiceRequest,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_target_tcp_proxies_set_backend_service_builder(
        client,
        project,
        targetTcpProxy,
        requestId,
        body,
    )?;
    compute_target_tcp_proxies_set_backend_service_execute(builder)
}

/// GET projects/{project}/global/targetTcpProxies/{targetTcpProxy}/setProxyHeader
/// Changes the ProxyHeaderType for TargetTcpProxy.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_target_tcp_proxies_set_proxy_header_execute()` to send, or `compute_target_tcp_proxies_set_proxy_header` for simplest API.

pub fn compute_target_tcp_proxies_set_proxy_header_builder(
    client: &SimpleHttpClient,
    project: &str,
    targetTcpProxy: &str,
    requestId: Option<&str>,
    body: &TargetTcpProxiesSetProxyHeaderRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/global/targetTcpProxies/{}/setProxyHeader",
        project, targetTcpProxy,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "requestId",
            requestId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET projects/{project}/global/targetTcpProxies/{targetTcpProxy}/setProxyHeader
/// Changes the ProxyHeaderType for TargetTcpProxy.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_target_tcp_proxies_set_proxy_header_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_target_tcp_proxies_set_proxy_header_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_target_tcp_proxies_set_proxy_header_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/global/targetTcpProxies/{targetTcpProxy}/setProxyHeader
/// Changes the ProxyHeaderType for TargetTcpProxy.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_target_tcp_proxies_set_proxy_header_builder()` + `compute_target_tcp_proxies_set_proxy_header_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_target_tcp_proxies_set_proxy_header(
    client: &SimpleHttpClient,
    project: &str,
    targetTcpProxy: &str,
    requestId: Option<&str>,
    body: &TargetTcpProxiesSetProxyHeaderRequest,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_target_tcp_proxies_set_proxy_header_builder(
        client,
        project,
        targetTcpProxy,
        requestId,
        body,
    )?;
    compute_target_tcp_proxies_set_proxy_header_execute(builder)
}

/// GET projects/{project}/global/targetTcpProxies/{resource}/testIamPermissions
/// Returns permissions that a caller has on the specified resource.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_target_tcp_proxies_test_iam_permissions_execute()` to send, or `compute_target_tcp_proxies_test_iam_permissions` for simplest API.

pub fn compute_target_tcp_proxies_test_iam_permissions_builder(
    client: &SimpleHttpClient,
    project: &str,
    resource: &str,
    body: &TestPermissionsRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/global/targetTcpProxies/{}/testIamPermissions",
        project, resource,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET projects/{project}/global/targetTcpProxies/{resource}/testIamPermissions
/// Returns permissions that a caller has on the specified resource.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_target_tcp_proxies_test_iam_permissions_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_target_tcp_proxies_test_iam_permissions_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_target_tcp_proxies_test_iam_permissions_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<TestPermissionsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: TestPermissionsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/global/targetTcpProxies/{resource}/testIamPermissions
/// Returns permissions that a caller has on the specified resource.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_target_tcp_proxies_test_iam_permissions_builder()` + `compute_target_tcp_proxies_test_iam_permissions_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_target_tcp_proxies_test_iam_permissions(
    client: &SimpleHttpClient,
    project: &str,
    resource: &str,
    body: &TestPermissionsRequest,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<TestPermissionsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        compute_target_tcp_proxies_test_iam_permissions_builder(client, project, resource, body)?;
    compute_target_tcp_proxies_test_iam_permissions_execute(builder)
}

/// GET projects/{project}/aggregated/targetVpnGateways
/// Retrieves an aggregated list of target VPN gateways.  To prevent failure, Google recommends that you set the `returnPartialSuccess` parameter to `true`.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_target_vpn_gateways_aggregated_list_execute()` to send, or `compute_target_vpn_gateways_aggregated_list` for simplest API.

pub fn compute_target_vpn_gateways_aggregated_list_builder(
    client: &SimpleHttpClient,
    project: &str,
    filter: Option<&str>,
    includeAllScopes: Option<bool>,
    maxResults: Option<i32>,
    orderBy: Option<&str>,
    pageToken: Option<&str>,
    returnPartialSuccess: Option<bool>,
    serviceProjectNumber: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/aggregated/targetVpnGateways",
        project,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "filter",
            filter
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "includeAllScopes",
            includeAllScopes
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "maxResults",
            maxResults
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "orderBy",
            orderBy
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageToken",
            pageToken
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "returnPartialSuccess",
            returnPartialSuccess
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "serviceProjectNumber",
            serviceProjectNumber
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET projects/{project}/aggregated/targetVpnGateways
/// Retrieves an aggregated list of target VPN gateways.  To prevent failure, Google recommends that you set the `returnPartialSuccess` parameter to `true`.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_target_vpn_gateways_aggregated_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_target_vpn_gateways_aggregated_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_target_vpn_gateways_aggregated_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<TargetVpnGatewayAggregatedList>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: TargetVpnGatewayAggregatedList = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/aggregated/targetVpnGateways
/// Retrieves an aggregated list of target VPN gateways.  To prevent failure, Google recommends that you set the `returnPartialSuccess` parameter to `true`.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_target_vpn_gateways_aggregated_list_builder()` + `compute_target_vpn_gateways_aggregated_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_target_vpn_gateways_aggregated_list(
    client: &SimpleHttpClient,
    project: &str,
    filter: Option<&str>,
    includeAllScopes: Option<bool>,
    maxResults: Option<i32>,
    orderBy: Option<&str>,
    pageToken: Option<&str>,
    returnPartialSuccess: Option<bool>,
    serviceProjectNumber: Option<&str>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<TargetVpnGatewayAggregatedList>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = compute_target_vpn_gateways_aggregated_list_builder(
        client,
        project,
        filter,
        includeAllScopes,
        maxResults,
        orderBy,
        pageToken,
        returnPartialSuccess,
        serviceProjectNumber,
    )?;
    compute_target_vpn_gateways_aggregated_list_execute(builder)
}

/// GET projects/{project}/regions/{region}/targetVpnGateways/{targetVpnGateway}
/// Deletes the specified target VPN gateway.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_target_vpn_gateways_delete_execute()` to send, or `compute_target_vpn_gateways_delete` for simplest API.

pub fn compute_target_vpn_gateways_delete_builder(
    client: &SimpleHttpClient,
    project: &str,
    region: &str,
    targetVpnGateway: &str,
    requestId: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/regions/{}/targetVpnGateways/{}",
        project, region, targetVpnGateway,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "requestId",
            requestId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET projects/{project}/regions/{region}/targetVpnGateways/{targetVpnGateway}
/// Deletes the specified target VPN gateway.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_target_vpn_gateways_delete_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_target_vpn_gateways_delete_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_target_vpn_gateways_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/regions/{region}/targetVpnGateways/{targetVpnGateway}
/// Deletes the specified target VPN gateway.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_target_vpn_gateways_delete_builder()` + `compute_target_vpn_gateways_delete_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_target_vpn_gateways_delete(
    client: &SimpleHttpClient,
    project: &str,
    region: &str,
    targetVpnGateway: &str,
    requestId: Option<&str>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_target_vpn_gateways_delete_builder(
        client,
        project,
        region,
        targetVpnGateway,
        requestId,
    )?;
    compute_target_vpn_gateways_delete_execute(builder)
}

/// GET projects/{project}/regions/{region}/targetVpnGateways/{targetVpnGateway}
/// Returns the specified target VPN gateway.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_target_vpn_gateways_get_execute()` to send, or `compute_target_vpn_gateways_get` for simplest API.

pub fn compute_target_vpn_gateways_get_builder(
    client: &SimpleHttpClient,
    project: &str,
    region: &str,
    targetVpnGateway: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/regions/{}/targetVpnGateways/{}",
        project, region, targetVpnGateway,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/regions/{region}/targetVpnGateways/{targetVpnGateway}
/// Returns the specified target VPN gateway.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_target_vpn_gateways_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_target_vpn_gateways_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_target_vpn_gateways_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<TargetVpnGateway>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: TargetVpnGateway = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/regions/{region}/targetVpnGateways/{targetVpnGateway}
/// Returns the specified target VPN gateway.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_target_vpn_gateways_get_builder()` + `compute_target_vpn_gateways_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_target_vpn_gateways_get(
    client: &SimpleHttpClient,
    project: &str,
    region: &str,
    targetVpnGateway: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<TargetVpnGateway>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        compute_target_vpn_gateways_get_builder(client, project, region, targetVpnGateway)?;
    compute_target_vpn_gateways_get_execute(builder)
}

/// GET projects/{project}/regions/{region}/targetVpnGateways
/// Creates a target VPN gateway in the specified project and region using the data included in the request.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_target_vpn_gateways_insert_execute()` to send, or `compute_target_vpn_gateways_insert` for simplest API.

pub fn compute_target_vpn_gateways_insert_builder(
    client: &SimpleHttpClient,
    project: &str,
    region: &str,
    requestId: Option<&str>,
    body: &TargetVpnGateway,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/regions/{}/targetVpnGateways",
        project, region,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "requestId",
            requestId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET projects/{project}/regions/{region}/targetVpnGateways
/// Creates a target VPN gateway in the specified project and region using the data included in the request.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_target_vpn_gateways_insert_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_target_vpn_gateways_insert_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_target_vpn_gateways_insert_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/regions/{region}/targetVpnGateways
/// Creates a target VPN gateway in the specified project and region using the data included in the request.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_target_vpn_gateways_insert_builder()` + `compute_target_vpn_gateways_insert_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_target_vpn_gateways_insert(
    client: &SimpleHttpClient,
    project: &str,
    region: &str,
    requestId: Option<&str>,
    body: &TargetVpnGateway,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        compute_target_vpn_gateways_insert_builder(client, project, region, requestId, body)?;
    compute_target_vpn_gateways_insert_execute(builder)
}

/// GET projects/{project}/regions/{region}/targetVpnGateways
/// Retrieves a list of target VPN gateways available to the specified project and region.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_target_vpn_gateways_list_execute()` to send, or `compute_target_vpn_gateways_list` for simplest API.

pub fn compute_target_vpn_gateways_list_builder(
    client: &SimpleHttpClient,
    project: &str,
    region: &str,
    filter: Option<&str>,
    maxResults: Option<i32>,
    orderBy: Option<&str>,
    pageToken: Option<&str>,
    returnPartialSuccess: Option<bool>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/regions/{}/targetVpnGateways",
        project, region,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "filter",
            filter
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "maxResults",
            maxResults
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "orderBy",
            orderBy
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageToken",
            pageToken
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "returnPartialSuccess",
            returnPartialSuccess
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET projects/{project}/regions/{region}/targetVpnGateways
/// Retrieves a list of target VPN gateways available to the specified project and region.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_target_vpn_gateways_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_target_vpn_gateways_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_target_vpn_gateways_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<TargetVpnGatewayList>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: TargetVpnGatewayList = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/regions/{region}/targetVpnGateways
/// Retrieves a list of target VPN gateways available to the specified project and region.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_target_vpn_gateways_list_builder()` + `compute_target_vpn_gateways_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_target_vpn_gateways_list(
    client: &SimpleHttpClient,
    project: &str,
    region: &str,
    filter: Option<&str>,
    maxResults: Option<i32>,
    orderBy: Option<&str>,
    pageToken: Option<&str>,
    returnPartialSuccess: Option<bool>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<TargetVpnGatewayList>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = compute_target_vpn_gateways_list_builder(
        client,
        project,
        region,
        filter,
        maxResults,
        orderBy,
        pageToken,
        returnPartialSuccess,
    )?;
    compute_target_vpn_gateways_list_execute(builder)
}

/// GET projects/{project}/regions/{region}/targetVpnGateways/{resource}/setLabels
/// Sets the labels on a TargetVpnGateway. To learn more about labels, read `theLabeling` Resources documentation.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_target_vpn_gateways_set_labels_execute()` to send, or `compute_target_vpn_gateways_set_labels` for simplest API.

pub fn compute_target_vpn_gateways_set_labels_builder(
    client: &SimpleHttpClient,
    project: &str,
    region: &str,
    resource: &str,
    requestId: Option<&str>,
    body: &RegionSetLabelsRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/regions/{}/targetVpnGateways/{}/setLabels",
        project, region, resource,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "requestId",
            requestId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET projects/{project}/regions/{region}/targetVpnGateways/{resource}/setLabels
/// Sets the labels on a TargetVpnGateway. To learn more about labels, read `theLabeling` Resources documentation.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_target_vpn_gateways_set_labels_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_target_vpn_gateways_set_labels_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_target_vpn_gateways_set_labels_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/regions/{region}/targetVpnGateways/{resource}/setLabels
/// Sets the labels on a TargetVpnGateway. To learn more about labels, read `theLabeling` Resources documentation.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_target_vpn_gateways_set_labels_builder()` + `compute_target_vpn_gateways_set_labels_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_target_vpn_gateways_set_labels(
    client: &SimpleHttpClient,
    project: &str,
    region: &str,
    resource: &str,
    requestId: Option<&str>,
    body: &RegionSetLabelsRequest,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_target_vpn_gateways_set_labels_builder(
        client, project, region, resource, requestId, body,
    )?;
    compute_target_vpn_gateways_set_labels_execute(builder)
}

/// GET projects/{project}/aggregated/urlMaps
/// Retrieves the list of all UrlMap resources, regional and global, available to the specified project.  To prevent failure, Google recommends that you set the `returnPartialSuccess` parameter to `true`.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_url_maps_aggregated_list_execute()` to send, or `compute_url_maps_aggregated_list` for simplest API.

pub fn compute_url_maps_aggregated_list_builder(
    client: &SimpleHttpClient,
    project: &str,
    filter: Option<&str>,
    includeAllScopes: Option<bool>,
    maxResults: Option<i32>,
    orderBy: Option<&str>,
    pageToken: Option<&str>,
    returnPartialSuccess: Option<bool>,
    serviceProjectNumber: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/aggregated/urlMaps",
        project,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "filter",
            filter
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "includeAllScopes",
            includeAllScopes
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "maxResults",
            maxResults
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "orderBy",
            orderBy
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageToken",
            pageToken
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "returnPartialSuccess",
            returnPartialSuccess
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "serviceProjectNumber",
            serviceProjectNumber
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET projects/{project}/aggregated/urlMaps
/// Retrieves the list of all UrlMap resources, regional and global, available to the specified project.  To prevent failure, Google recommends that you set the `returnPartialSuccess` parameter to `true`.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_url_maps_aggregated_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_url_maps_aggregated_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_url_maps_aggregated_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<UrlMapsAggregatedList>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: UrlMapsAggregatedList = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/aggregated/urlMaps
/// Retrieves the list of all UrlMap resources, regional and global, available to the specified project.  To prevent failure, Google recommends that you set the `returnPartialSuccess` parameter to `true`.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_url_maps_aggregated_list_builder()` + `compute_url_maps_aggregated_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_url_maps_aggregated_list(
    client: &SimpleHttpClient,
    project: &str,
    filter: Option<&str>,
    includeAllScopes: Option<bool>,
    maxResults: Option<i32>,
    orderBy: Option<&str>,
    pageToken: Option<&str>,
    returnPartialSuccess: Option<bool>,
    serviceProjectNumber: Option<&str>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<UrlMapsAggregatedList>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = compute_url_maps_aggregated_list_builder(
        client,
        project,
        filter,
        includeAllScopes,
        maxResults,
        orderBy,
        pageToken,
        returnPartialSuccess,
        serviceProjectNumber,
    )?;
    compute_url_maps_aggregated_list_execute(builder)
}

/// GET projects/{project}/global/urlMaps/{urlMap}
/// Deletes the specified UrlMap resource.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_url_maps_delete_execute()` to send, or `compute_url_maps_delete` for simplest API.

pub fn compute_url_maps_delete_builder(
    client: &SimpleHttpClient,
    project: &str,
    urlMap: &str,
    requestId: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/global/urlMaps/{}",
        project, urlMap,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "requestId",
            requestId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET projects/{project}/global/urlMaps/{urlMap}
/// Deletes the specified UrlMap resource.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_url_maps_delete_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_url_maps_delete_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_url_maps_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/global/urlMaps/{urlMap}
/// Deletes the specified UrlMap resource.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_url_maps_delete_builder()` + `compute_url_maps_delete_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_url_maps_delete(
    client: &SimpleHttpClient,
    project: &str,
    urlMap: &str,
    requestId: Option<&str>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_url_maps_delete_builder(client, project, urlMap, requestId)?;
    compute_url_maps_delete_execute(builder)
}

/// GET projects/{project}/global/urlMaps/{urlMap}
/// Returns the specified UrlMap resource.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_url_maps_get_execute()` to send, or `compute_url_maps_get` for simplest API.

pub fn compute_url_maps_get_builder(
    client: &SimpleHttpClient,
    project: &str,
    urlMap: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/global/urlMaps/{}",
        project, urlMap,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/global/urlMaps/{urlMap}
/// Returns the specified UrlMap resource.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_url_maps_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_url_maps_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_url_maps_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<UrlMap>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: UrlMap = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/global/urlMaps/{urlMap}
/// Returns the specified UrlMap resource.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_url_maps_get_builder()` + `compute_url_maps_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_url_maps_get(
    client: &SimpleHttpClient,
    project: &str,
    urlMap: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<UrlMap>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_url_maps_get_builder(client, project, urlMap)?;
    compute_url_maps_get_execute(builder)
}

/// GET projects/{project}/global/urlMaps
/// Creates a UrlMap resource in the specified project using the data included in the request.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_url_maps_insert_execute()` to send, or `compute_url_maps_insert` for simplest API.

pub fn compute_url_maps_insert_builder(
    client: &SimpleHttpClient,
    project: &str,
    requestId: Option<&str>,
    body: &UrlMap,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comprojects/{}/global/urlMaps", project,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "requestId",
            requestId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET projects/{project}/global/urlMaps
/// Creates a UrlMap resource in the specified project using the data included in the request.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_url_maps_insert_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_url_maps_insert_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_url_maps_insert_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/global/urlMaps
/// Creates a UrlMap resource in the specified project using the data included in the request.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_url_maps_insert_builder()` + `compute_url_maps_insert_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_url_maps_insert(
    client: &SimpleHttpClient,
    project: &str,
    requestId: Option<&str>,
    body: &UrlMap,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_url_maps_insert_builder(client, project, requestId, body)?;
    compute_url_maps_insert_execute(builder)
}

/// GET projects/{project}/global/urlMaps/{urlMap}/invalidateCache
/// Initiates a cache invalidation operation, invalidating the specified path, scoped to the specified UrlMap.  For more information, see [Invalidating cached content](/`cdn/docs/invalidating-cached-content`).
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_url_maps_invalidate_cache_execute()` to send, or `compute_url_maps_invalidate_cache` for simplest API.

pub fn compute_url_maps_invalidate_cache_builder(
    client: &SimpleHttpClient,
    project: &str,
    urlMap: &str,
    requestId: Option<&str>,
    body: &CacheInvalidationRule,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/global/urlMaps/{}/invalidateCache",
        project, urlMap,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "requestId",
            requestId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET projects/{project}/global/urlMaps/{urlMap}/invalidateCache
/// Initiates a cache invalidation operation, invalidating the specified path, scoped to the specified UrlMap.  For more information, see [Invalidating cached content](/`cdn/docs/invalidating-cached-content`).
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_url_maps_invalidate_cache_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_url_maps_invalidate_cache_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_url_maps_invalidate_cache_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/global/urlMaps/{urlMap}/invalidateCache
/// Initiates a cache invalidation operation, invalidating the specified path, scoped to the specified UrlMap.  For more information, see [Invalidating cached content](/`cdn/docs/invalidating-cached-content`).
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_url_maps_invalidate_cache_builder()` + `compute_url_maps_invalidate_cache_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_url_maps_invalidate_cache(
    client: &SimpleHttpClient,
    project: &str,
    urlMap: &str,
    requestId: Option<&str>,
    body: &CacheInvalidationRule,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        compute_url_maps_invalidate_cache_builder(client, project, urlMap, requestId, body)?;
    compute_url_maps_invalidate_cache_execute(builder)
}

/// GET projects/{project}/global/urlMaps
/// Retrieves the list of UrlMap resources available to the specified project.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_url_maps_list_execute()` to send, or `compute_url_maps_list` for simplest API.

pub fn compute_url_maps_list_builder(
    client: &SimpleHttpClient,
    project: &str,
    filter: Option<&str>,
    maxResults: Option<i32>,
    orderBy: Option<&str>,
    pageToken: Option<&str>,
    returnPartialSuccess: Option<bool>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comprojects/{}/global/urlMaps", project,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "filter",
            filter
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "maxResults",
            maxResults
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "orderBy",
            orderBy
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageToken",
            pageToken
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "returnPartialSuccess",
            returnPartialSuccess
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET projects/{project}/global/urlMaps
/// Retrieves the list of UrlMap resources available to the specified project.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_url_maps_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_url_maps_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_url_maps_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<UrlMapList>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: UrlMapList = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/global/urlMaps
/// Retrieves the list of UrlMap resources available to the specified project.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_url_maps_list_builder()` + `compute_url_maps_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_url_maps_list(
    client: &SimpleHttpClient,
    project: &str,
    filter: Option<&str>,
    maxResults: Option<i32>,
    orderBy: Option<&str>,
    pageToken: Option<&str>,
    returnPartialSuccess: Option<bool>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<UrlMapList>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_url_maps_list_builder(
        client,
        project,
        filter,
        maxResults,
        orderBy,
        pageToken,
        returnPartialSuccess,
    )?;
    compute_url_maps_list_execute(builder)
}

/// GET projects/{project}/global/urlMaps/{urlMap}
/// Patches the specified UrlMap resource with the data included in the request. This method `supportsPATCH` semantics and uses `theJSON` merge patch format and processing rules.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_url_maps_patch_execute()` to send, or `compute_url_maps_patch` for simplest API.

pub fn compute_url_maps_patch_builder(
    client: &SimpleHttpClient,
    project: &str,
    urlMap: &str,
    requestId: Option<&str>,
    body: &UrlMap,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/global/urlMaps/{}",
        project, urlMap,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "requestId",
            requestId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET projects/{project}/global/urlMaps/{urlMap}
/// Patches the specified UrlMap resource with the data included in the request. This method `supportsPATCH` semantics and uses `theJSON` merge patch format and processing rules.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_url_maps_patch_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_url_maps_patch_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_url_maps_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/global/urlMaps/{urlMap}
/// Patches the specified UrlMap resource with the data included in the request. This method `supportsPATCH` semantics and uses `theJSON` merge patch format and processing rules.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_url_maps_patch_builder()` + `compute_url_maps_patch_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_url_maps_patch(
    client: &SimpleHttpClient,
    project: &str,
    urlMap: &str,
    requestId: Option<&str>,
    body: &UrlMap,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_url_maps_patch_builder(client, project, urlMap, requestId, body)?;
    compute_url_maps_patch_execute(builder)
}

/// GET projects/{project}/global/urlMaps/{resource}/testIamPermissions
/// Returns permissions that a caller has on the specified resource.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_url_maps_test_iam_permissions_execute()` to send, or `compute_url_maps_test_iam_permissions` for simplest API.

pub fn compute_url_maps_test_iam_permissions_builder(
    client: &SimpleHttpClient,
    project: &str,
    resource: &str,
    body: &TestPermissionsRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/global/urlMaps/{}/testIamPermissions",
        project, resource,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET projects/{project}/global/urlMaps/{resource}/testIamPermissions
/// Returns permissions that a caller has on the specified resource.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_url_maps_test_iam_permissions_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_url_maps_test_iam_permissions_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_url_maps_test_iam_permissions_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<TestPermissionsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: TestPermissionsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/global/urlMaps/{resource}/testIamPermissions
/// Returns permissions that a caller has on the specified resource.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_url_maps_test_iam_permissions_builder()` + `compute_url_maps_test_iam_permissions_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_url_maps_test_iam_permissions(
    client: &SimpleHttpClient,
    project: &str,
    resource: &str,
    body: &TestPermissionsRequest,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<TestPermissionsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = compute_url_maps_test_iam_permissions_builder(client, project, resource, body)?;
    compute_url_maps_test_iam_permissions_execute(builder)
}

/// GET projects/{project}/global/urlMaps/{urlMap}
/// Updates the specified UrlMap resource with the data included in the request.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_url_maps_update_execute()` to send, or `compute_url_maps_update` for simplest API.

pub fn compute_url_maps_update_builder(
    client: &SimpleHttpClient,
    project: &str,
    urlMap: &str,
    requestId: Option<&str>,
    body: &UrlMap,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/global/urlMaps/{}",
        project, urlMap,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "requestId",
            requestId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET projects/{project}/global/urlMaps/{urlMap}
/// Updates the specified UrlMap resource with the data included in the request.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_url_maps_update_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_url_maps_update_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_url_maps_update_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/global/urlMaps/{urlMap}
/// Updates the specified UrlMap resource with the data included in the request.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_url_maps_update_builder()` + `compute_url_maps_update_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_url_maps_update(
    client: &SimpleHttpClient,
    project: &str,
    urlMap: &str,
    requestId: Option<&str>,
    body: &UrlMap,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_url_maps_update_builder(client, project, urlMap, requestId, body)?;
    compute_url_maps_update_execute(builder)
}

/// GET projects/{project}/global/urlMaps/{urlMap}/validate
/// Runs static validation for the UrlMap. In particular, the tests of the provided UrlMap will be run. Calling this method does NOT create the UrlMap.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_url_maps_validate_execute()` to send, or `compute_url_maps_validate` for simplest API.

pub fn compute_url_maps_validate_builder(
    client: &SimpleHttpClient,
    project: &str,
    urlMap: &str,
    body: &UrlMapsValidateRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/global/urlMaps/{}/validate",
        project, urlMap,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET projects/{project}/global/urlMaps/{urlMap}/validate
/// Runs static validation for the UrlMap. In particular, the tests of the provided UrlMap will be run. Calling this method does NOT create the UrlMap.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_url_maps_validate_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_url_maps_validate_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_url_maps_validate_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<UrlMapsValidateResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: UrlMapsValidateResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/global/urlMaps/{urlMap}/validate
/// Runs static validation for the UrlMap. In particular, the tests of the provided UrlMap will be run. Calling this method does NOT create the UrlMap.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_url_maps_validate_builder()` + `compute_url_maps_validate_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_url_maps_validate(
    client: &SimpleHttpClient,
    project: &str,
    urlMap: &str,
    body: &UrlMapsValidateRequest,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<UrlMapsValidateResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = compute_url_maps_validate_builder(client, project, urlMap, body)?;
    compute_url_maps_validate_execute(builder)
}

/// GET projects/{project}/aggregated/vpnGateways
/// Retrieves an aggregated list of VPN gateways.  To prevent failure, Google recommends that you set the `returnPartialSuccess` parameter to `true`.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_vpn_gateways_aggregated_list_execute()` to send, or `compute_vpn_gateways_aggregated_list` for simplest API.

pub fn compute_vpn_gateways_aggregated_list_builder(
    client: &SimpleHttpClient,
    project: &str,
    filter: Option<&str>,
    includeAllScopes: Option<bool>,
    maxResults: Option<i32>,
    orderBy: Option<&str>,
    pageToken: Option<&str>,
    returnPartialSuccess: Option<bool>,
    serviceProjectNumber: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/aggregated/vpnGateways",
        project,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "filter",
            filter
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "includeAllScopes",
            includeAllScopes
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "maxResults",
            maxResults
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "orderBy",
            orderBy
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageToken",
            pageToken
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "returnPartialSuccess",
            returnPartialSuccess
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "serviceProjectNumber",
            serviceProjectNumber
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET projects/{project}/aggregated/vpnGateways
/// Retrieves an aggregated list of VPN gateways.  To prevent failure, Google recommends that you set the `returnPartialSuccess` parameter to `true`.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_vpn_gateways_aggregated_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_vpn_gateways_aggregated_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_vpn_gateways_aggregated_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<VpnGatewayAggregatedList>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: VpnGatewayAggregatedList = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/aggregated/vpnGateways
/// Retrieves an aggregated list of VPN gateways.  To prevent failure, Google recommends that you set the `returnPartialSuccess` parameter to `true`.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_vpn_gateways_aggregated_list_builder()` + `compute_vpn_gateways_aggregated_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_vpn_gateways_aggregated_list(
    client: &SimpleHttpClient,
    project: &str,
    filter: Option<&str>,
    includeAllScopes: Option<bool>,
    maxResults: Option<i32>,
    orderBy: Option<&str>,
    pageToken: Option<&str>,
    returnPartialSuccess: Option<bool>,
    serviceProjectNumber: Option<&str>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<VpnGatewayAggregatedList>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = compute_vpn_gateways_aggregated_list_builder(
        client,
        project,
        filter,
        includeAllScopes,
        maxResults,
        orderBy,
        pageToken,
        returnPartialSuccess,
        serviceProjectNumber,
    )?;
    compute_vpn_gateways_aggregated_list_execute(builder)
}

/// GET projects/{project}/regions/{region}/vpnGateways/{vpnGateway}
/// Deletes the specified VPN gateway.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_vpn_gateways_delete_execute()` to send, or `compute_vpn_gateways_delete` for simplest API.

pub fn compute_vpn_gateways_delete_builder(
    client: &SimpleHttpClient,
    project: &str,
    region: &str,
    vpnGateway: &str,
    requestId: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/regions/{}/vpnGateways/{}",
        project, region, vpnGateway,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "requestId",
            requestId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET projects/{project}/regions/{region}/vpnGateways/{vpnGateway}
/// Deletes the specified VPN gateway.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_vpn_gateways_delete_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_vpn_gateways_delete_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_vpn_gateways_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/regions/{region}/vpnGateways/{vpnGateway}
/// Deletes the specified VPN gateway.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_vpn_gateways_delete_builder()` + `compute_vpn_gateways_delete_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_vpn_gateways_delete(
    client: &SimpleHttpClient,
    project: &str,
    region: &str,
    vpnGateway: &str,
    requestId: Option<&str>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        compute_vpn_gateways_delete_builder(client, project, region, vpnGateway, requestId)?;
    compute_vpn_gateways_delete_execute(builder)
}

/// GET projects/{project}/regions/{region}/vpnGateways/{vpnGateway}
/// Returns the specified VPN gateway.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_vpn_gateways_get_execute()` to send, or `compute_vpn_gateways_get` for simplest API.

pub fn compute_vpn_gateways_get_builder(
    client: &SimpleHttpClient,
    project: &str,
    region: &str,
    vpnGateway: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/regions/{}/vpnGateways/{}",
        project, region, vpnGateway,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/regions/{region}/vpnGateways/{vpnGateway}
/// Returns the specified VPN gateway.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_vpn_gateways_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_vpn_gateways_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_vpn_gateways_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<VpnGateway>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: VpnGateway = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/regions/{region}/vpnGateways/{vpnGateway}
/// Returns the specified VPN gateway.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_vpn_gateways_get_builder()` + `compute_vpn_gateways_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_vpn_gateways_get(
    client: &SimpleHttpClient,
    project: &str,
    region: &str,
    vpnGateway: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<VpnGateway>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_vpn_gateways_get_builder(client, project, region, vpnGateway)?;
    compute_vpn_gateways_get_execute(builder)
}

/// GET projects/{project}/regions/{region}/vpnGateways/{vpnGateway}/getStatus
/// Returns the status for the specified VPN gateway.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_vpn_gateways_get_status_execute()` to send, or `compute_vpn_gateways_get_status` for simplest API.

pub fn compute_vpn_gateways_get_status_builder(
    client: &SimpleHttpClient,
    project: &str,
    region: &str,
    vpnGateway: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/regions/{}/vpnGateways/{}/getStatus",
        project, region, vpnGateway,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/regions/{region}/vpnGateways/{vpnGateway}/getStatus
/// Returns the status for the specified VPN gateway.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_vpn_gateways_get_status_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_vpn_gateways_get_status_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_vpn_gateways_get_status_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<VpnGatewaysGetStatusResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: VpnGatewaysGetStatusResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/regions/{region}/vpnGateways/{vpnGateway}/getStatus
/// Returns the status for the specified VPN gateway.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_vpn_gateways_get_status_builder()` + `compute_vpn_gateways_get_status_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_vpn_gateways_get_status(
    client: &SimpleHttpClient,
    project: &str,
    region: &str,
    vpnGateway: &str,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<VpnGatewaysGetStatusResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = compute_vpn_gateways_get_status_builder(client, project, region, vpnGateway)?;
    compute_vpn_gateways_get_status_execute(builder)
}

/// GET projects/{project}/regions/{region}/vpnGateways
/// Creates a VPN gateway in the specified project and region using the data included in the request.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_vpn_gateways_insert_execute()` to send, or `compute_vpn_gateways_insert` for simplest API.

pub fn compute_vpn_gateways_insert_builder(
    client: &SimpleHttpClient,
    project: &str,
    region: &str,
    requestId: Option<&str>,
    body: &VpnGateway,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/regions/{}/vpnGateways",
        project, region,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "requestId",
            requestId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET projects/{project}/regions/{region}/vpnGateways
/// Creates a VPN gateway in the specified project and region using the data included in the request.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_vpn_gateways_insert_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_vpn_gateways_insert_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_vpn_gateways_insert_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/regions/{region}/vpnGateways
/// Creates a VPN gateway in the specified project and region using the data included in the request.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_vpn_gateways_insert_builder()` + `compute_vpn_gateways_insert_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_vpn_gateways_insert(
    client: &SimpleHttpClient,
    project: &str,
    region: &str,
    requestId: Option<&str>,
    body: &VpnGateway,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_vpn_gateways_insert_builder(client, project, region, requestId, body)?;
    compute_vpn_gateways_insert_execute(builder)
}

/// GET projects/{project}/regions/{region}/vpnGateways
/// Retrieves a list of VPN gateways available to the specified project and region.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_vpn_gateways_list_execute()` to send, or `compute_vpn_gateways_list` for simplest API.

pub fn compute_vpn_gateways_list_builder(
    client: &SimpleHttpClient,
    project: &str,
    region: &str,
    filter: Option<&str>,
    maxResults: Option<i32>,
    orderBy: Option<&str>,
    pageToken: Option<&str>,
    returnPartialSuccess: Option<bool>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/regions/{}/vpnGateways",
        project, region,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "filter",
            filter
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "maxResults",
            maxResults
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "orderBy",
            orderBy
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageToken",
            pageToken
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "returnPartialSuccess",
            returnPartialSuccess
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET projects/{project}/regions/{region}/vpnGateways
/// Retrieves a list of VPN gateways available to the specified project and region.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_vpn_gateways_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_vpn_gateways_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_vpn_gateways_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<VpnGatewayList>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: VpnGatewayList = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/regions/{region}/vpnGateways
/// Retrieves a list of VPN gateways available to the specified project and region.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_vpn_gateways_list_builder()` + `compute_vpn_gateways_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_vpn_gateways_list(
    client: &SimpleHttpClient,
    project: &str,
    region: &str,
    filter: Option<&str>,
    maxResults: Option<i32>,
    orderBy: Option<&str>,
    pageToken: Option<&str>,
    returnPartialSuccess: Option<bool>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<VpnGatewayList>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = compute_vpn_gateways_list_builder(
        client,
        project,
        region,
        filter,
        maxResults,
        orderBy,
        pageToken,
        returnPartialSuccess,
    )?;
    compute_vpn_gateways_list_execute(builder)
}

/// GET projects/{project}/regions/{region}/vpnGateways/{resource}/setLabels
/// Sets the labels on a VpnGateway. To learn more about labels, read `theLabeling` Resources documentation.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_vpn_gateways_set_labels_execute()` to send, or `compute_vpn_gateways_set_labels` for simplest API.

pub fn compute_vpn_gateways_set_labels_builder(
    client: &SimpleHttpClient,
    project: &str,
    region: &str,
    resource: &str,
    requestId: Option<&str>,
    body: &RegionSetLabelsRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/regions/{}/vpnGateways/{}/setLabels",
        project, region, resource,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "requestId",
            requestId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET projects/{project}/regions/{region}/vpnGateways/{resource}/setLabels
/// Sets the labels on a VpnGateway. To learn more about labels, read `theLabeling` Resources documentation.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_vpn_gateways_set_labels_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_vpn_gateways_set_labels_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_vpn_gateways_set_labels_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/regions/{region}/vpnGateways/{resource}/setLabels
/// Sets the labels on a VpnGateway. To learn more about labels, read `theLabeling` Resources documentation.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_vpn_gateways_set_labels_builder()` + `compute_vpn_gateways_set_labels_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_vpn_gateways_set_labels(
    client: &SimpleHttpClient,
    project: &str,
    region: &str,
    resource: &str,
    requestId: Option<&str>,
    body: &RegionSetLabelsRequest,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_vpn_gateways_set_labels_builder(
        client, project, region, resource, requestId, body,
    )?;
    compute_vpn_gateways_set_labels_execute(builder)
}

/// GET projects/{project}/regions/{region}/vpnGateways/{resource}/testIamPermissions
/// Returns permissions that a caller has on the specified resource.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_vpn_gateways_test_iam_permissions_execute()` to send, or `compute_vpn_gateways_test_iam_permissions` for simplest API.

pub fn compute_vpn_gateways_test_iam_permissions_builder(
    client: &SimpleHttpClient,
    project: &str,
    region: &str,
    resource: &str,
    body: &TestPermissionsRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/regions/{}/vpnGateways/{}/testIamPermissions",
        project, region, resource,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET projects/{project}/regions/{region}/vpnGateways/{resource}/testIamPermissions
/// Returns permissions that a caller has on the specified resource.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_vpn_gateways_test_iam_permissions_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_vpn_gateways_test_iam_permissions_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_vpn_gateways_test_iam_permissions_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<TestPermissionsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: TestPermissionsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/regions/{region}/vpnGateways/{resource}/testIamPermissions
/// Returns permissions that a caller has on the specified resource.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_vpn_gateways_test_iam_permissions_builder()` + `compute_vpn_gateways_test_iam_permissions_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_vpn_gateways_test_iam_permissions(
    client: &SimpleHttpClient,
    project: &str,
    region: &str,
    resource: &str,
    body: &TestPermissionsRequest,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<TestPermissionsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        compute_vpn_gateways_test_iam_permissions_builder(client, project, region, resource, body)?;
    compute_vpn_gateways_test_iam_permissions_execute(builder)
}

/// GET projects/{project}/aggregated/vpnTunnels
/// Retrieves an aggregated list of VPN tunnels.  To prevent failure, Google recommends that you set the `returnPartialSuccess` parameter to `true`.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_vpn_tunnels_aggregated_list_execute()` to send, or `compute_vpn_tunnels_aggregated_list` for simplest API.

pub fn compute_vpn_tunnels_aggregated_list_builder(
    client: &SimpleHttpClient,
    project: &str,
    filter: Option<&str>,
    includeAllScopes: Option<bool>,
    maxResults: Option<i32>,
    orderBy: Option<&str>,
    pageToken: Option<&str>,
    returnPartialSuccess: Option<bool>,
    serviceProjectNumber: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/aggregated/vpnTunnels",
        project,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "filter",
            filter
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "includeAllScopes",
            includeAllScopes
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "maxResults",
            maxResults
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "orderBy",
            orderBy
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageToken",
            pageToken
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "returnPartialSuccess",
            returnPartialSuccess
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "serviceProjectNumber",
            serviceProjectNumber
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET projects/{project}/aggregated/vpnTunnels
/// Retrieves an aggregated list of VPN tunnels.  To prevent failure, Google recommends that you set the `returnPartialSuccess` parameter to `true`.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_vpn_tunnels_aggregated_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_vpn_tunnels_aggregated_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_vpn_tunnels_aggregated_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<VpnTunnelAggregatedList>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: VpnTunnelAggregatedList = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/aggregated/vpnTunnels
/// Retrieves an aggregated list of VPN tunnels.  To prevent failure, Google recommends that you set the `returnPartialSuccess` parameter to `true`.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_vpn_tunnels_aggregated_list_builder()` + `compute_vpn_tunnels_aggregated_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_vpn_tunnels_aggregated_list(
    client: &SimpleHttpClient,
    project: &str,
    filter: Option<&str>,
    includeAllScopes: Option<bool>,
    maxResults: Option<i32>,
    orderBy: Option<&str>,
    pageToken: Option<&str>,
    returnPartialSuccess: Option<bool>,
    serviceProjectNumber: Option<&str>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<VpnTunnelAggregatedList>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = compute_vpn_tunnels_aggregated_list_builder(
        client,
        project,
        filter,
        includeAllScopes,
        maxResults,
        orderBy,
        pageToken,
        returnPartialSuccess,
        serviceProjectNumber,
    )?;
    compute_vpn_tunnels_aggregated_list_execute(builder)
}

/// GET projects/{project}/regions/{region}/vpnTunnels/{vpnTunnel}
/// Deletes the specified VpnTunnel resource.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_vpn_tunnels_delete_execute()` to send, or `compute_vpn_tunnels_delete` for simplest API.

pub fn compute_vpn_tunnels_delete_builder(
    client: &SimpleHttpClient,
    project: &str,
    region: &str,
    vpnTunnel: &str,
    requestId: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/regions/{}/vpnTunnels/{}",
        project, region, vpnTunnel,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "requestId",
            requestId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET projects/{project}/regions/{region}/vpnTunnels/{vpnTunnel}
/// Deletes the specified VpnTunnel resource.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_vpn_tunnels_delete_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_vpn_tunnels_delete_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_vpn_tunnels_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/regions/{region}/vpnTunnels/{vpnTunnel}
/// Deletes the specified VpnTunnel resource.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_vpn_tunnels_delete_builder()` + `compute_vpn_tunnels_delete_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_vpn_tunnels_delete(
    client: &SimpleHttpClient,
    project: &str,
    region: &str,
    vpnTunnel: &str,
    requestId: Option<&str>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        compute_vpn_tunnels_delete_builder(client, project, region, vpnTunnel, requestId)?;
    compute_vpn_tunnels_delete_execute(builder)
}

/// GET projects/{project}/regions/{region}/vpnTunnels/{vpnTunnel}
/// Returns the specified VpnTunnel resource.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_vpn_tunnels_get_execute()` to send, or `compute_vpn_tunnels_get` for simplest API.

pub fn compute_vpn_tunnels_get_builder(
    client: &SimpleHttpClient,
    project: &str,
    region: &str,
    vpnTunnel: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/regions/{}/vpnTunnels/{}",
        project, region, vpnTunnel,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/regions/{region}/vpnTunnels/{vpnTunnel}
/// Returns the specified VpnTunnel resource.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_vpn_tunnels_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_vpn_tunnels_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_vpn_tunnels_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<VpnTunnel>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: VpnTunnel = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/regions/{region}/vpnTunnels/{vpnTunnel}
/// Returns the specified VpnTunnel resource.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_vpn_tunnels_get_builder()` + `compute_vpn_tunnels_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_vpn_tunnels_get(
    client: &SimpleHttpClient,
    project: &str,
    region: &str,
    vpnTunnel: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<VpnTunnel>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_vpn_tunnels_get_builder(client, project, region, vpnTunnel)?;
    compute_vpn_tunnels_get_execute(builder)
}

/// GET projects/{project}/regions/{region}/vpnTunnels
/// Creates a VpnTunnel resource in the specified project and region using the data included in the request.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_vpn_tunnels_insert_execute()` to send, or `compute_vpn_tunnels_insert` for simplest API.

pub fn compute_vpn_tunnels_insert_builder(
    client: &SimpleHttpClient,
    project: &str,
    region: &str,
    requestId: Option<&str>,
    body: &VpnTunnel,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/regions/{}/vpnTunnels",
        project, region,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "requestId",
            requestId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET projects/{project}/regions/{region}/vpnTunnels
/// Creates a VpnTunnel resource in the specified project and region using the data included in the request.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_vpn_tunnels_insert_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_vpn_tunnels_insert_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_vpn_tunnels_insert_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/regions/{region}/vpnTunnels
/// Creates a VpnTunnel resource in the specified project and region using the data included in the request.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_vpn_tunnels_insert_builder()` + `compute_vpn_tunnels_insert_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_vpn_tunnels_insert(
    client: &SimpleHttpClient,
    project: &str,
    region: &str,
    requestId: Option<&str>,
    body: &VpnTunnel,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_vpn_tunnels_insert_builder(client, project, region, requestId, body)?;
    compute_vpn_tunnels_insert_execute(builder)
}

/// GET projects/{project}/regions/{region}/vpnTunnels
/// Retrieves a list of VpnTunnel resources contained in the specified project and region.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_vpn_tunnels_list_execute()` to send, or `compute_vpn_tunnels_list` for simplest API.

pub fn compute_vpn_tunnels_list_builder(
    client: &SimpleHttpClient,
    project: &str,
    region: &str,
    filter: Option<&str>,
    maxResults: Option<i32>,
    orderBy: Option<&str>,
    pageToken: Option<&str>,
    returnPartialSuccess: Option<bool>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/regions/{}/vpnTunnels",
        project, region,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "filter",
            filter
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "maxResults",
            maxResults
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "orderBy",
            orderBy
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageToken",
            pageToken
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "returnPartialSuccess",
            returnPartialSuccess
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET projects/{project}/regions/{region}/vpnTunnels
/// Retrieves a list of VpnTunnel resources contained in the specified project and region.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_vpn_tunnels_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_vpn_tunnels_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_vpn_tunnels_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<VpnTunnelList>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: VpnTunnelList = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/regions/{region}/vpnTunnels
/// Retrieves a list of VpnTunnel resources contained in the specified project and region.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_vpn_tunnels_list_builder()` + `compute_vpn_tunnels_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_vpn_tunnels_list(
    client: &SimpleHttpClient,
    project: &str,
    region: &str,
    filter: Option<&str>,
    maxResults: Option<i32>,
    orderBy: Option<&str>,
    pageToken: Option<&str>,
    returnPartialSuccess: Option<bool>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<VpnTunnelList>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = compute_vpn_tunnels_list_builder(
        client,
        project,
        region,
        filter,
        maxResults,
        orderBy,
        pageToken,
        returnPartialSuccess,
    )?;
    compute_vpn_tunnels_list_execute(builder)
}

/// GET projects/{project}/regions/{region}/vpnTunnels/{resource}/setLabels
/// Sets the labels on a VpnTunnel. To learn more about labels, read `theLabeling` Resources documentation.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_vpn_tunnels_set_labels_execute()` to send, or `compute_vpn_tunnels_set_labels` for simplest API.

pub fn compute_vpn_tunnels_set_labels_builder(
    client: &SimpleHttpClient,
    project: &str,
    region: &str,
    resource: &str,
    requestId: Option<&str>,
    body: &RegionSetLabelsRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/regions/{}/vpnTunnels/{}/setLabels",
        project, region, resource,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "requestId",
            requestId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET projects/{project}/regions/{region}/vpnTunnels/{resource}/setLabels
/// Sets the labels on a VpnTunnel. To learn more about labels, read `theLabeling` Resources documentation.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_vpn_tunnels_set_labels_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_vpn_tunnels_set_labels_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_vpn_tunnels_set_labels_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/regions/{region}/vpnTunnels/{resource}/setLabels
/// Sets the labels on a VpnTunnel. To learn more about labels, read `theLabeling` Resources documentation.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_vpn_tunnels_set_labels_builder()` + `compute_vpn_tunnels_set_labels_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_vpn_tunnels_set_labels(
    client: &SimpleHttpClient,
    project: &str,
    region: &str,
    resource: &str,
    requestId: Option<&str>,
    body: &RegionSetLabelsRequest,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        compute_vpn_tunnels_set_labels_builder(client, project, region, resource, requestId, body)?;
    compute_vpn_tunnels_set_labels_execute(builder)
}

/// GET projects/{project}/global/crossSiteNetworks/{crossSiteNetwork}/wireGroups/{wireGroup}
/// Deletes the specified wire group in the given scope.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_wire_groups_delete_execute()` to send, or `compute_wire_groups_delete` for simplest API.

pub fn compute_wire_groups_delete_builder(
    client: &SimpleHttpClient,
    crossSiteNetwork: &str,
    project: &str,
    wireGroup: &str,
    requestId: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/global/crossSiteNetworks/{}/wireGroups/{}",
        crossSiteNetwork, project, wireGroup,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "requestId",
            requestId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET projects/{project}/global/crossSiteNetworks/{crossSiteNetwork}/wireGroups/{wireGroup}
/// Deletes the specified wire group in the given scope.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_wire_groups_delete_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_wire_groups_delete_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_wire_groups_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/global/crossSiteNetworks/{crossSiteNetwork}/wireGroups/{wireGroup}
/// Deletes the specified wire group in the given scope.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_wire_groups_delete_builder()` + `compute_wire_groups_delete_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_wire_groups_delete(
    client: &SimpleHttpClient,
    crossSiteNetwork: &str,
    project: &str,
    wireGroup: &str,
    requestId: Option<&str>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_wire_groups_delete_builder(
        client,
        crossSiteNetwork,
        project,
        wireGroup,
        requestId,
    )?;
    compute_wire_groups_delete_execute(builder)
}

/// GET projects/{project}/global/crossSiteNetworks/{crossSiteNetwork}/wireGroups/{wireGroup}
/// Gets the specified wire group resource in the given scope.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_wire_groups_get_execute()` to send, or `compute_wire_groups_get` for simplest API.

pub fn compute_wire_groups_get_builder(
    client: &SimpleHttpClient,
    crossSiteNetwork: &str,
    project: &str,
    wireGroup: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/global/crossSiteNetworks/{}/wireGroups/{}",
        crossSiteNetwork, project, wireGroup,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/global/crossSiteNetworks/{crossSiteNetwork}/wireGroups/{wireGroup}
/// Gets the specified wire group resource in the given scope.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_wire_groups_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_wire_groups_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_wire_groups_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<WireGroup>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: WireGroup = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/global/crossSiteNetworks/{crossSiteNetwork}/wireGroups/{wireGroup}
/// Gets the specified wire group resource in the given scope.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_wire_groups_get_builder()` + `compute_wire_groups_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_wire_groups_get(
    client: &SimpleHttpClient,
    crossSiteNetwork: &str,
    project: &str,
    wireGroup: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<WireGroup>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_wire_groups_get_builder(client, crossSiteNetwork, project, wireGroup)?;
    compute_wire_groups_get_execute(builder)
}

/// GET projects/{project}/global/crossSiteNetworks/{crossSiteNetwork}/wireGroups
/// Creates a wire group in the specified project in the given scope using the parameters that are included in the request.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_wire_groups_insert_execute()` to send, or `compute_wire_groups_insert` for simplest API.

pub fn compute_wire_groups_insert_builder(
    client: &SimpleHttpClient,
    crossSiteNetwork: &str,
    project: &str,
    requestId: Option<&str>,
    validateOnly: Option<bool>,
    body: &WireGroup,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/global/crossSiteNetworks/{}/wireGroups",
        crossSiteNetwork, project,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "requestId",
            requestId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "validateOnly",
            validateOnly
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET projects/{project}/global/crossSiteNetworks/{crossSiteNetwork}/wireGroups
/// Creates a wire group in the specified project in the given scope using the parameters that are included in the request.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_wire_groups_insert_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_wire_groups_insert_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_wire_groups_insert_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/global/crossSiteNetworks/{crossSiteNetwork}/wireGroups
/// Creates a wire group in the specified project in the given scope using the parameters that are included in the request.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_wire_groups_insert_builder()` + `compute_wire_groups_insert_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_wire_groups_insert(
    client: &SimpleHttpClient,
    crossSiteNetwork: &str,
    project: &str,
    requestId: Option<&str>,
    validateOnly: Option<bool>,
    body: &WireGroup,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_wire_groups_insert_builder(
        client,
        crossSiteNetwork,
        project,
        requestId,
        validateOnly,
        body,
    )?;
    compute_wire_groups_insert_execute(builder)
}

/// GET projects/{project}/global/crossSiteNetworks/{crossSiteNetwork}/wireGroups
/// Lists the wire groups for a project in the given scope.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_wire_groups_list_execute()` to send, or `compute_wire_groups_list` for simplest API.

pub fn compute_wire_groups_list_builder(
    client: &SimpleHttpClient,
    crossSiteNetwork: &str,
    project: &str,
    filter: Option<&str>,
    maxResults: Option<i32>,
    orderBy: Option<&str>,
    pageToken: Option<&str>,
    returnPartialSuccess: Option<bool>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/global/crossSiteNetworks/{}/wireGroups",
        crossSiteNetwork, project,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "filter",
            filter
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "maxResults",
            maxResults
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "orderBy",
            orderBy
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageToken",
            pageToken
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "returnPartialSuccess",
            returnPartialSuccess
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET projects/{project}/global/crossSiteNetworks/{crossSiteNetwork}/wireGroups
/// Lists the wire groups for a project in the given scope.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_wire_groups_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_wire_groups_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_wire_groups_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<WireGroupList>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: WireGroupList = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/global/crossSiteNetworks/{crossSiteNetwork}/wireGroups
/// Lists the wire groups for a project in the given scope.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_wire_groups_list_builder()` + `compute_wire_groups_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_wire_groups_list(
    client: &SimpleHttpClient,
    crossSiteNetwork: &str,
    project: &str,
    filter: Option<&str>,
    maxResults: Option<i32>,
    orderBy: Option<&str>,
    pageToken: Option<&str>,
    returnPartialSuccess: Option<bool>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<WireGroupList>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = compute_wire_groups_list_builder(
        client,
        crossSiteNetwork,
        project,
        filter,
        maxResults,
        orderBy,
        pageToken,
        returnPartialSuccess,
    )?;
    compute_wire_groups_list_execute(builder)
}

/// GET projects/{project}/global/crossSiteNetworks/{crossSiteNetwork}/wireGroups/{wireGroup}
/// Updates the specified wire group resource with the data included in the request. This method `supportsPATCH` semantics and `usesJSON` merge patch format and processing rules.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_wire_groups_patch_execute()` to send, or `compute_wire_groups_patch` for simplest API.

pub fn compute_wire_groups_patch_builder(
    client: &SimpleHttpClient,
    crossSiteNetwork: &str,
    project: &str,
    wireGroup: &str,
    requestId: Option<&str>,
    updateMask: Option<&str>,
    validateOnly: Option<bool>,
    body: &WireGroup,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/global/crossSiteNetworks/{}/wireGroups/{}",
        crossSiteNetwork, project, wireGroup,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "requestId",
            requestId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "updateMask",
            updateMask
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "validateOnly",
            validateOnly
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET projects/{project}/global/crossSiteNetworks/{crossSiteNetwork}/wireGroups/{wireGroup}
/// Updates the specified wire group resource with the data included in the request. This method `supportsPATCH` semantics and `usesJSON` merge patch format and processing rules.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_wire_groups_patch_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_wire_groups_patch_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_wire_groups_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/global/crossSiteNetworks/{crossSiteNetwork}/wireGroups/{wireGroup}
/// Updates the specified wire group resource with the data included in the request. This method `supportsPATCH` semantics and `usesJSON` merge patch format and processing rules.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_wire_groups_patch_builder()` + `compute_wire_groups_patch_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_wire_groups_patch(
    client: &SimpleHttpClient,
    crossSiteNetwork: &str,
    project: &str,
    wireGroup: &str,
    requestId: Option<&str>,
    updateMask: Option<&str>,
    validateOnly: Option<bool>,
    body: &WireGroup,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_wire_groups_patch_builder(
        client,
        crossSiteNetwork,
        project,
        wireGroup,
        requestId,
        updateMask,
        validateOnly,
        body,
    )?;
    compute_wire_groups_patch_execute(builder)
}

/// GET projects/{project}/zones/{zone}/operations/{operation}
/// Deletes the specified zone-specific Operations resource.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_zone_operations_delete_execute()` to send, or `compute_zone_operations_delete` for simplest API.

pub fn compute_zone_operations_delete_builder(
    client: &SimpleHttpClient,
    operation: &str,
    project: &str,
    zone: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/zones/{}/operations/{}",
        operation, project, zone,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/zones/{zone}/operations/{operation}
/// Deletes the specified zone-specific Operations resource.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_zone_operations_delete_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_zone_operations_delete_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_zone_operations_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/zones/{zone}/operations/{operation}
/// Deletes the specified zone-specific Operations resource.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_zone_operations_delete_builder()` + `compute_zone_operations_delete_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_zone_operations_delete(
    client: &SimpleHttpClient,
    operation: &str,
    project: &str,
    zone: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_zone_operations_delete_builder(client, operation, project, zone)?;
    compute_zone_operations_delete_execute(builder)
}

/// GET projects/{project}/zones/{zone}/operations/{operation}
/// Retrieves the specified zone-specific Operations resource.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_zone_operations_get_execute()` to send, or `compute_zone_operations_get` for simplest API.

pub fn compute_zone_operations_get_builder(
    client: &SimpleHttpClient,
    operation: &str,
    project: &str,
    zone: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/zones/{}/operations/{}",
        operation, project, zone,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/zones/{zone}/operations/{operation}
/// Retrieves the specified zone-specific Operations resource.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_zone_operations_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_zone_operations_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_zone_operations_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/zones/{zone}/operations/{operation}
/// Retrieves the specified zone-specific Operations resource.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_zone_operations_get_builder()` + `compute_zone_operations_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_zone_operations_get(
    client: &SimpleHttpClient,
    operation: &str,
    project: &str,
    zone: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_zone_operations_get_builder(client, operation, project, zone)?;
    compute_zone_operations_get_execute(builder)
}

/// GET projects/{project}/zones/{zone}/operations
/// Retrieves a list of Operation resources contained within the specified zone.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_zone_operations_list_execute()` to send, or `compute_zone_operations_list` for simplest API.

pub fn compute_zone_operations_list_builder(
    client: &SimpleHttpClient,
    project: &str,
    zone: &str,
    filter: Option<&str>,
    maxResults: Option<i32>,
    orderBy: Option<&str>,
    pageToken: Option<&str>,
    returnPartialSuccess: Option<bool>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/zones/{}/operations",
        project, zone,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "filter",
            filter
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "maxResults",
            maxResults
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "orderBy",
            orderBy
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageToken",
            pageToken
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "returnPartialSuccess",
            returnPartialSuccess
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET projects/{project}/zones/{zone}/operations
/// Retrieves a list of Operation resources contained within the specified zone.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_zone_operations_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_zone_operations_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_zone_operations_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<OperationList>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: OperationList = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/zones/{zone}/operations
/// Retrieves a list of Operation resources contained within the specified zone.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_zone_operations_list_builder()` + `compute_zone_operations_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_zone_operations_list(
    client: &SimpleHttpClient,
    project: &str,
    zone: &str,
    filter: Option<&str>,
    maxResults: Option<i32>,
    orderBy: Option<&str>,
    pageToken: Option<&str>,
    returnPartialSuccess: Option<bool>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<OperationList>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = compute_zone_operations_list_builder(
        client,
        project,
        zone,
        filter,
        maxResults,
        orderBy,
        pageToken,
        returnPartialSuccess,
    )?;
    compute_zone_operations_list_execute(builder)
}

/// GET projects/{project}/zones/{zone}/operations/{operation}/wait
/// Waits for the specified Operation resource to return as DONE or for the request to approach the 2 minute deadline, and retrieves the specified Operation resource. This method waits for no more than the 2 minutes and then returns the current state of the operation, which might be DONE or still in progress.  This method is called on a best-effort basis. Specifically:   - In uncommon cases, when the server is overloaded, the request might return before the default deadline is reached, or might return after zero seconds. - If the default deadline is reached, there is no guarantee that the operation is actually done when the method returns. Be prepared to retry if the operation is not DONE.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_zone_operations_wait_execute()` to send, or `compute_zone_operations_wait` for simplest API.

pub fn compute_zone_operations_wait_builder(
    client: &SimpleHttpClient,
    operation: &str,
    project: &str,
    zone: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comprojects/{}/zones/{}/operations/{}/wait",
        operation, project, zone,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/zones/{zone}/operations/{operation}/wait
/// Waits for the specified Operation resource to return as DONE or for the request to approach the 2 minute deadline, and retrieves the specified Operation resource. This method waits for no more than the 2 minutes and then returns the current state of the operation, which might be DONE or still in progress.  This method is called on a best-effort basis. Specifically:   - In uncommon cases, when the server is overloaded, the request might return before the default deadline is reached, or might return after zero seconds. - If the default deadline is reached, there is no guarantee that the operation is actually done when the method returns. Be prepared to retry if the operation is not DONE.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_zone_operations_wait_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_zone_operations_wait_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_zone_operations_wait_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/zones/{zone}/operations/{operation}/wait
/// Waits for the specified Operation resource to return as DONE or for the request to approach the 2 minute deadline, and retrieves the specified Operation resource. This method waits for no more than the 2 minutes and then returns the current state of the operation, which might be DONE or still in progress.  This method is called on a best-effort basis. Specifically:   - In uncommon cases, when the server is overloaded, the request might return before the default deadline is reached, or might return after zero seconds. - If the default deadline is reached, there is no guarantee that the operation is actually done when the method returns. Be prepared to retry if the operation is not DONE.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_zone_operations_wait_builder()` + `compute_zone_operations_wait_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_zone_operations_wait(
    client: &SimpleHttpClient,
    operation: &str,
    project: &str,
    zone: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_zone_operations_wait_builder(client, operation, project, zone)?;
    compute_zone_operations_wait_execute(builder)
}

/// GET projects/{project}/zones/{zone}
/// Returns the specified Zone resource.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_zones_get_execute()` to send, or `compute_zones_get` for simplest API.

pub fn compute_zones_get_builder(
    client: &SimpleHttpClient,
    project: &str,
    zone: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comprojects/{}/zones/{}", project, zone,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/zones/{zone}
/// Returns the specified Zone resource.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_zones_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_zones_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_zones_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Zone>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Zone = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/zones/{zone}
/// Returns the specified Zone resource.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_zones_get_builder()` + `compute_zones_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_zones_get(
    client: &SimpleHttpClient,
    project: &str,
    zone: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Zone>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_zones_get_builder(client, project, zone)?;
    compute_zones_get_execute(builder)
}

/// GET projects/{project}/zones
/// Retrieves the list of Zone resources available to the specified project.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `compute_zones_list_execute()` to send, or `compute_zones_list` for simplest API.

pub fn compute_zones_list_builder(
    client: &SimpleHttpClient,
    project: &str,
    filter: Option<&str>,
    maxResults: Option<i32>,
    orderBy: Option<&str>,
    pageToken: Option<&str>,
    returnPartialSuccess: Option<bool>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comprojects/{}/zones", project,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "filter",
            filter
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "maxResults",
            maxResults
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "orderBy",
            orderBy
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageToken",
            pageToken
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "returnPartialSuccess",
            returnPartialSuccess
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET projects/{project}/zones
/// Retrieves the list of Zone resources available to the specified project.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `compute_zones_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from compute_zones_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_zones_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ZoneList>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ZoneList = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET projects/{project}/zones
/// Retrieves the list of Zone resources available to the specified project.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `compute_zones_list_builder()` + `compute_zones_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn compute_zones_list(
    client: &SimpleHttpClient,
    project: &str,
    filter: Option<&str>,
    maxResults: Option<i32>,
    orderBy: Option<&str>,
    pageToken: Option<&str>,
    returnPartialSuccess: Option<bool>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ZoneList>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = compute_zones_list_builder(
        client,
        project,
        filter,
        maxResults,
        orderBy,
        pageToken,
        returnPartialSuccess,
    )?;
    compute_zones_list_execute(builder)
}
