//! Auto-generated API clients for gcp/customsearch.
//!
//! Generated by `cargo run --bin ewe_platform gen_provider_clients`.
//! DO NOT EDIT MANUALLY.

use crate::providers::gcp::clients::types::*;
use crate::providers::gcp::resources::*;
use foundation_core::valtron::{execute, StreamIterator, StreamIteratorExt};
use foundation_core::wire::simple_http::client::{
    body_reader, ClientRequestBuilder, RequestIntro, SimpleHttpClient, SystemDnsResolver,
};

/// GET customsearch/v1
/// Returns metadata about the search performed, metadata about the engine used for the search, and the search results.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `search_cse_list_execute()` to send, or `search_cse_list` for simplest API.

pub fn search_cse_list_builder(
    client: &SimpleHttpClient,
    c2coff: Option<&str>,
    cr: Option<&str>,
    cx: Option<&str>,
    dateRestrict: Option<&str>,
    enableAlternateSearchHandler: Option<bool>,
    exactTerms: Option<&str>,
    excludeTerms: Option<&str>,
    fileType: Option<&str>,
    filter: Option<&str>,
    gl: Option<&str>,
    googlehost: Option<&str>,
    highRange: Option<&str>,
    hl: Option<&str>,
    hq: Option<&str>,
    imgColorType: Option<&str>,
    imgDominantColor: Option<&str>,
    imgSize: Option<&str>,
    imgType: Option<&str>,
    linkSite: Option<&str>,
    lowRange: Option<&str>,
    lr: Option<&str>,
    num: Option<i32>,
    orTerms: Option<&str>,
    q: Option<&str>,
    relatedSite: Option<&str>,
    rights: Option<&str>,
    safe: Option<&str>,
    searchType: Option<&str>,
    siteSearch: Option<&str>,
    siteSearchFilter: Option<&str>,
    snippetLength: Option<i32>,
    sort: Option<&str>,
    start: Option<i32>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comcustomsearch/v1",);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "c2coff",
            c2coff
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "cr",
            cr.as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "cx",
            cx.as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "dateRestrict",
            dateRestrict
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "enableAlternateSearchHandler",
            enableAlternateSearchHandler
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "exactTerms",
            exactTerms
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "excludeTerms",
            excludeTerms
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "fileType",
            fileType
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "filter",
            filter
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "gl",
            gl.as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "googlehost",
            googlehost
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "highRange",
            highRange
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "hl",
            hl.as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "hq",
            hq.as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "imgColorType",
            imgColorType
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "imgDominantColor",
            imgDominantColor
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "imgSize",
            imgSize
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "imgType",
            imgType
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "linkSite",
            linkSite
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "lowRange",
            lowRange
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "lr",
            lr.as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "num",
            num.as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "orTerms",
            orTerms
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "q",
            q.as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "relatedSite",
            relatedSite
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "rights",
            rights
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "safe",
            safe.as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "searchType",
            searchType
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "siteSearch",
            siteSearch
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "siteSearchFilter",
            siteSearchFilter
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "snippetLength",
            snippetLength
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "sort",
            sort.as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "start",
            start
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET customsearch/v1
/// Returns metadata about the search performed, metadata about the engine used for the search, and the search results.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `search_cse_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from search_cse_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn search_cse_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Search>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Search = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET customsearch/v1
/// Returns metadata about the search performed, metadata about the engine used for the search, and the search results.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `search_cse_list_builder()` + `search_cse_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn search_cse_list(
    client: &SimpleHttpClient,
    c2coff: Option<&str>,
    cr: Option<&str>,
    cx: Option<&str>,
    dateRestrict: Option<&str>,
    enableAlternateSearchHandler: Option<bool>,
    exactTerms: Option<&str>,
    excludeTerms: Option<&str>,
    fileType: Option<&str>,
    filter: Option<&str>,
    gl: Option<&str>,
    googlehost: Option<&str>,
    highRange: Option<&str>,
    hl: Option<&str>,
    hq: Option<&str>,
    imgColorType: Option<&str>,
    imgDominantColor: Option<&str>,
    imgSize: Option<&str>,
    imgType: Option<&str>,
    linkSite: Option<&str>,
    lowRange: Option<&str>,
    lr: Option<&str>,
    num: Option<i32>,
    orTerms: Option<&str>,
    q: Option<&str>,
    relatedSite: Option<&str>,
    rights: Option<&str>,
    safe: Option<&str>,
    searchType: Option<&str>,
    siteSearch: Option<&str>,
    siteSearchFilter: Option<&str>,
    snippetLength: Option<i32>,
    sort: Option<&str>,
    start: Option<i32>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Search>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = search_cse_list_builder(
        client,
        c2coff,
        cr,
        cx,
        dateRestrict,
        enableAlternateSearchHandler,
        exactTerms,
        excludeTerms,
        fileType,
        filter,
        gl,
        googlehost,
        highRange,
        hl,
        hq,
        imgColorType,
        imgDominantColor,
        imgSize,
        imgType,
        linkSite,
        lowRange,
        lr,
        num,
        orTerms,
        q,
        relatedSite,
        rights,
        safe,
        searchType,
        siteSearch,
        siteSearchFilter,
        snippetLength,
        sort,
        start,
    )?;
    search_cse_list_execute(builder)
}

/// GET customsearch/v1/siterestrict
/// Returns metadata about the search performed, metadata about the engine used for the search, and the search results. Uses a small set of url patterns.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `search_cse_siterestrict_list_execute()` to send, or `search_cse_siterestrict_list` for simplest API.

pub fn search_cse_siterestrict_list_builder(
    client: &SimpleHttpClient,
    c2coff: Option<&str>,
    cr: Option<&str>,
    cx: Option<&str>,
    dateRestrict: Option<&str>,
    enableAlternateSearchHandler: Option<bool>,
    exactTerms: Option<&str>,
    excludeTerms: Option<&str>,
    fileType: Option<&str>,
    filter: Option<&str>,
    gl: Option<&str>,
    googlehost: Option<&str>,
    highRange: Option<&str>,
    hl: Option<&str>,
    hq: Option<&str>,
    imgColorType: Option<&str>,
    imgDominantColor: Option<&str>,
    imgSize: Option<&str>,
    imgType: Option<&str>,
    linkSite: Option<&str>,
    lowRange: Option<&str>,
    lr: Option<&str>,
    num: Option<i32>,
    orTerms: Option<&str>,
    q: Option<&str>,
    relatedSite: Option<&str>,
    rights: Option<&str>,
    safe: Option<&str>,
    searchType: Option<&str>,
    siteSearch: Option<&str>,
    siteSearchFilter: Option<&str>,
    snippetLength: Option<i32>,
    sort: Option<&str>,
    start: Option<i32>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comcustomsearch/v1/siterestrict",);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "c2coff",
            c2coff
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "cr",
            cr.as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "cx",
            cx.as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "dateRestrict",
            dateRestrict
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "enableAlternateSearchHandler",
            enableAlternateSearchHandler
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "exactTerms",
            exactTerms
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "excludeTerms",
            excludeTerms
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "fileType",
            fileType
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "filter",
            filter
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "gl",
            gl.as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "googlehost",
            googlehost
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "highRange",
            highRange
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "hl",
            hl.as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "hq",
            hq.as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "imgColorType",
            imgColorType
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "imgDominantColor",
            imgDominantColor
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "imgSize",
            imgSize
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "imgType",
            imgType
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "linkSite",
            linkSite
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "lowRange",
            lowRange
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "lr",
            lr.as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "num",
            num.as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "orTerms",
            orTerms
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "q",
            q.as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "relatedSite",
            relatedSite
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "rights",
            rights
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "safe",
            safe.as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "searchType",
            searchType
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "siteSearch",
            siteSearch
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "siteSearchFilter",
            siteSearchFilter
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "snippetLength",
            snippetLength
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "sort",
            sort.as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "start",
            start
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET customsearch/v1/siterestrict
/// Returns metadata about the search performed, metadata about the engine used for the search, and the search results. Uses a small set of url patterns.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `search_cse_siterestrict_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from search_cse_siterestrict_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn search_cse_siterestrict_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Search>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Search = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET customsearch/v1/siterestrict
/// Returns metadata about the search performed, metadata about the engine used for the search, and the search results. Uses a small set of url patterns.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `search_cse_siterestrict_list_builder()` + `search_cse_siterestrict_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn search_cse_siterestrict_list(
    client: &SimpleHttpClient,
    c2coff: Option<&str>,
    cr: Option<&str>,
    cx: Option<&str>,
    dateRestrict: Option<&str>,
    enableAlternateSearchHandler: Option<bool>,
    exactTerms: Option<&str>,
    excludeTerms: Option<&str>,
    fileType: Option<&str>,
    filter: Option<&str>,
    gl: Option<&str>,
    googlehost: Option<&str>,
    highRange: Option<&str>,
    hl: Option<&str>,
    hq: Option<&str>,
    imgColorType: Option<&str>,
    imgDominantColor: Option<&str>,
    imgSize: Option<&str>,
    imgType: Option<&str>,
    linkSite: Option<&str>,
    lowRange: Option<&str>,
    lr: Option<&str>,
    num: Option<i32>,
    orTerms: Option<&str>,
    q: Option<&str>,
    relatedSite: Option<&str>,
    rights: Option<&str>,
    safe: Option<&str>,
    searchType: Option<&str>,
    siteSearch: Option<&str>,
    siteSearchFilter: Option<&str>,
    snippetLength: Option<i32>,
    sort: Option<&str>,
    start: Option<i32>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Search>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = search_cse_siterestrict_list_builder(
        client,
        c2coff,
        cr,
        cx,
        dateRestrict,
        enableAlternateSearchHandler,
        exactTerms,
        excludeTerms,
        fileType,
        filter,
        gl,
        googlehost,
        highRange,
        hl,
        hq,
        imgColorType,
        imgDominantColor,
        imgSize,
        imgType,
        linkSite,
        lowRange,
        lr,
        num,
        orTerms,
        q,
        relatedSite,
        rights,
        safe,
        searchType,
        siteSearch,
        siteSearchFilter,
        snippetLength,
        sort,
        start,
    )?;
    search_cse_siterestrict_list_execute(builder)
}
