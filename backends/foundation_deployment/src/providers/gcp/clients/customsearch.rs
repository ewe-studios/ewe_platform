//! Auto-generated API clients for gcp/customsearch.
//!
//! Generated by `cargo run --bin ewe_platform gen_provider_clients`.
//! DO NOT EDIT MANUALLY.
//!
//! Feature flag: `gcp`

#![cfg(feature = "gcp")]

pub mod types;

use crate::providers::gcp::clients::types::*;
use crate::providers::gcp::resources::*;
use foundation_core::valtron::{
    execute, BoxedSendExecutionAction, StreamIterator, StreamIteratorExt, TaskIterator,
    TaskIteratorExt,
};
use foundation_core::wire::simple_http::client::{
    body_reader, ClientRequestBuilder, DnsResolver, RequestIntro, SimpleHttpClient,
    SystemDnsResolver,
};
use foundation_db::state::resource_identifier::ResourceIdentifier;
use foundation_macros::JsonHash;
use serde::Serialize;

/// GET customsearch/v1
/// Returns metadata about the search performed, metadata about the engine used for the search, and the search results.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `search_cse_list_execute()` to send, or `search_cse_list` for simplest API.

pub fn search_cse_list_builder<R>(
    client: &SimpleHttpClient<R>,
    c2coff: &Option<String>,
    cr: &Option<String>,
    cx: &Option<String>,
    dateRestrict: &Option<String>,
    enableAlternateSearchHandler: &Option<String>,
    exactTerms: &Option<String>,
    excludeTerms: &Option<String>,
    fileType: &Option<String>,
    filter: &Option<String>,
    gl: &Option<String>,
    googlehost: &Option<String>,
    highRange: &Option<String>,
    hl: &Option<String>,
    hq: &Option<String>,
    imgColorType: &Option<String>,
    imgDominantColor: &Option<String>,
    imgSize: &Option<String>,
    imgType: &Option<String>,
    linkSite: &Option<String>,
    lowRange: &Option<String>,
    lr: &Option<String>,
    num: &Option<String>,
    orTerms: &Option<String>,
    q: &Option<String>,
    relatedSite: &Option<String>,
    rights: &Option<String>,
    safe: &Option<String>,
    searchType: &Option<String>,
    siteSearch: &Option<String>,
    siteSearchFilter: &Option<String>,
    snippetLength: &Option<String>,
    sort: &Option<String>,
    start: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!("https://customsearch.googleapis.com/customsearch/v1",);

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = c2coff.as_ref() {
        query_parts.push(format!("c2coff={}", val));
    }
    if let Some(val) = cr.as_ref() {
        query_parts.push(format!("cr={}", val));
    }
    if let Some(val) = cx.as_ref() {
        query_parts.push(format!("cx={}", val));
    }
    if let Some(val) = dateRestrict.as_ref() {
        query_parts.push(format!("dateRestrict={}", val));
    }
    if let Some(val) = enableAlternateSearchHandler.as_ref() {
        query_parts.push(format!("enableAlternateSearchHandler={}", val));
    }
    if let Some(val) = exactTerms.as_ref() {
        query_parts.push(format!("exactTerms={}", val));
    }
    if let Some(val) = excludeTerms.as_ref() {
        query_parts.push(format!("excludeTerms={}", val));
    }
    if let Some(val) = fileType.as_ref() {
        query_parts.push(format!("fileType={}", val));
    }
    if let Some(val) = filter.as_ref() {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = gl.as_ref() {
        query_parts.push(format!("gl={}", val));
    }
    if let Some(val) = googlehost.as_ref() {
        query_parts.push(format!("googlehost={}", val));
    }
    if let Some(val) = highRange.as_ref() {
        query_parts.push(format!("highRange={}", val));
    }
    if let Some(val) = hl.as_ref() {
        query_parts.push(format!("hl={}", val));
    }
    if let Some(val) = hq.as_ref() {
        query_parts.push(format!("hq={}", val));
    }
    if let Some(val) = imgColorType.as_ref() {
        query_parts.push(format!("imgColorType={}", val));
    }
    if let Some(val) = imgDominantColor.as_ref() {
        query_parts.push(format!("imgDominantColor={}", val));
    }
    if let Some(val) = imgSize.as_ref() {
        query_parts.push(format!("imgSize={}", val));
    }
    if let Some(val) = imgType.as_ref() {
        query_parts.push(format!("imgType={}", val));
    }
    if let Some(val) = linkSite.as_ref() {
        query_parts.push(format!("linkSite={}", val));
    }
    if let Some(val) = lowRange.as_ref() {
        query_parts.push(format!("lowRange={}", val));
    }
    if let Some(val) = lr.as_ref() {
        query_parts.push(format!("lr={}", val));
    }
    if let Some(val) = num.as_ref() {
        query_parts.push(format!("num={}", val));
    }
    if let Some(val) = orTerms.as_ref() {
        query_parts.push(format!("orTerms={}", val));
    }
    if let Some(val) = q.as_ref() {
        query_parts.push(format!("q={}", val));
    }
    if let Some(val) = relatedSite.as_ref() {
        query_parts.push(format!("relatedSite={}", val));
    }
    if let Some(val) = rights.as_ref() {
        query_parts.push(format!("rights={}", val));
    }
    if let Some(val) = safe.as_ref() {
        query_parts.push(format!("safe={}", val));
    }
    if let Some(val) = searchType.as_ref() {
        query_parts.push(format!("searchType={}", val));
    }
    if let Some(val) = siteSearch.as_ref() {
        query_parts.push(format!("siteSearch={}", val));
    }
    if let Some(val) = siteSearchFilter.as_ref() {
        query_parts.push(format!("siteSearchFilter={}", val));
    }
    if let Some(val) = snippetLength.as_ref() {
        query_parts.push(format!("snippetLength={}", val));
    }
    if let Some(val) = sort.as_ref() {
        query_parts.push(format!("sort={}", val));
    }
    if let Some(val) = start.as_ref() {
        query_parts.push(format!("start={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET customsearch/v1
/// Returns metadata about the search performed, metadata about the engine used for the search, and the search results.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `search_cse_list_execute()` or `search_cse_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `search_cse_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn search_cse_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Search>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Search = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET customsearch/v1
/// Returns metadata about the search performed, metadata about the engine used for the search, and the search results.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `search_cse_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `search_cse_list_task()`.
/// For the simplest API, use `search_cse_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `search_cse_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn search_cse_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Search>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = search_cse_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`search_cse_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct SearchCseListArgs {
    /// Query parameter: c2coff
    pub c2coff: Option<String>,
    /// Query parameter: cr
    pub cr: Option<String>,
    /// Query parameter: cx
    pub cx: Option<String>,
    /// Query parameter: dateRestrict
    pub dateRestrict: Option<String>,
    /// Query parameter: enableAlternateSearchHandler
    pub enableAlternateSearchHandler: Option<String>,
    /// Query parameter: exactTerms
    pub exactTerms: Option<String>,
    /// Query parameter: excludeTerms
    pub excludeTerms: Option<String>,
    /// Query parameter: fileType
    pub fileType: Option<String>,
    /// Query parameter: filter
    pub filter: Option<String>,
    /// Query parameter: gl
    pub gl: Option<String>,
    /// Query parameter: googlehost
    pub googlehost: Option<String>,
    /// Query parameter: highRange
    pub highRange: Option<String>,
    /// Query parameter: hl
    pub hl: Option<String>,
    /// Query parameter: hq
    pub hq: Option<String>,
    /// Query parameter: imgColorType
    pub imgColorType: Option<String>,
    /// Query parameter: imgDominantColor
    pub imgDominantColor: Option<String>,
    /// Query parameter: imgSize
    pub imgSize: Option<String>,
    /// Query parameter: imgType
    pub imgType: Option<String>,
    /// Query parameter: linkSite
    pub linkSite: Option<String>,
    /// Query parameter: lowRange
    pub lowRange: Option<String>,
    /// Query parameter: lr
    pub lr: Option<String>,
    /// Query parameter: num
    pub num: Option<String>,
    /// Query parameter: orTerms
    pub orTerms: Option<String>,
    /// Query parameter: q
    pub q: Option<String>,
    /// Query parameter: relatedSite
    pub relatedSite: Option<String>,
    /// Query parameter: rights
    pub rights: Option<String>,
    /// Query parameter: safe
    pub safe: Option<String>,
    /// Query parameter: searchType
    pub searchType: Option<String>,
    /// Query parameter: siteSearch
    pub siteSearch: Option<String>,
    /// Query parameter: siteSearchFilter
    pub siteSearchFilter: Option<String>,
    /// Query parameter: snippetLength
    pub snippetLength: Option<String>,
    /// Query parameter: sort
    pub sort: Option<String>,
    /// Query parameter: start
    pub start: Option<String>,
}

/// GET customsearch/v1
/// Returns metadata about the search performed, metadata about the engine used for the search, and the search results.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `search_cse_list_builder()` + `search_cse_list_execute()`.
/// For task-level control, use `search_cse_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn search_cse_list(
    client: &SimpleHttpClient,
    args: &SearchCseListArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Search>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = search_cse_list_builder(
        client,
        &args.c2coff,
        &args.cr,
        &args.cx,
        &args.dateRestrict,
        &args.enableAlternateSearchHandler,
        &args.exactTerms,
        &args.excludeTerms,
        &args.fileType,
        &args.filter,
        &args.gl,
        &args.googlehost,
        &args.highRange,
        &args.hl,
        &args.hq,
        &args.imgColorType,
        &args.imgDominantColor,
        &args.imgSize,
        &args.imgType,
        &args.linkSite,
        &args.lowRange,
        &args.lr,
        &args.num,
        &args.orTerms,
        &args.q,
        &args.relatedSite,
        &args.rights,
        &args.safe,
        &args.searchType,
        &args.siteSearch,
        &args.siteSearchFilter,
        &args.snippetLength,
        &args.sort,
        &args.start,
    )?;
    search_cse_list_execute(builder)
}

/// GET customsearch/v1/siterestrict
/// Returns metadata about the search performed, metadata about the engine used for the search, and the search results. Uses a small set of url patterns.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `search_cse_siterestrict_list_execute()` to send, or `search_cse_siterestrict_list` for simplest API.

pub fn search_cse_siterestrict_list_builder<R>(
    client: &SimpleHttpClient<R>,
    c2coff: &Option<String>,
    cr: &Option<String>,
    cx: &Option<String>,
    dateRestrict: &Option<String>,
    enableAlternateSearchHandler: &Option<String>,
    exactTerms: &Option<String>,
    excludeTerms: &Option<String>,
    fileType: &Option<String>,
    filter: &Option<String>,
    gl: &Option<String>,
    googlehost: &Option<String>,
    highRange: &Option<String>,
    hl: &Option<String>,
    hq: &Option<String>,
    imgColorType: &Option<String>,
    imgDominantColor: &Option<String>,
    imgSize: &Option<String>,
    imgType: &Option<String>,
    linkSite: &Option<String>,
    lowRange: &Option<String>,
    lr: &Option<String>,
    num: &Option<String>,
    orTerms: &Option<String>,
    q: &Option<String>,
    relatedSite: &Option<String>,
    rights: &Option<String>,
    safe: &Option<String>,
    searchType: &Option<String>,
    siteSearch: &Option<String>,
    siteSearchFilter: &Option<String>,
    snippetLength: &Option<String>,
    sort: &Option<String>,
    start: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!("https://customsearch.googleapis.com/customsearch/v1/siterestrict",);

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = c2coff.as_ref() {
        query_parts.push(format!("c2coff={}", val));
    }
    if let Some(val) = cr.as_ref() {
        query_parts.push(format!("cr={}", val));
    }
    if let Some(val) = cx.as_ref() {
        query_parts.push(format!("cx={}", val));
    }
    if let Some(val) = dateRestrict.as_ref() {
        query_parts.push(format!("dateRestrict={}", val));
    }
    if let Some(val) = enableAlternateSearchHandler.as_ref() {
        query_parts.push(format!("enableAlternateSearchHandler={}", val));
    }
    if let Some(val) = exactTerms.as_ref() {
        query_parts.push(format!("exactTerms={}", val));
    }
    if let Some(val) = excludeTerms.as_ref() {
        query_parts.push(format!("excludeTerms={}", val));
    }
    if let Some(val) = fileType.as_ref() {
        query_parts.push(format!("fileType={}", val));
    }
    if let Some(val) = filter.as_ref() {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = gl.as_ref() {
        query_parts.push(format!("gl={}", val));
    }
    if let Some(val) = googlehost.as_ref() {
        query_parts.push(format!("googlehost={}", val));
    }
    if let Some(val) = highRange.as_ref() {
        query_parts.push(format!("highRange={}", val));
    }
    if let Some(val) = hl.as_ref() {
        query_parts.push(format!("hl={}", val));
    }
    if let Some(val) = hq.as_ref() {
        query_parts.push(format!("hq={}", val));
    }
    if let Some(val) = imgColorType.as_ref() {
        query_parts.push(format!("imgColorType={}", val));
    }
    if let Some(val) = imgDominantColor.as_ref() {
        query_parts.push(format!("imgDominantColor={}", val));
    }
    if let Some(val) = imgSize.as_ref() {
        query_parts.push(format!("imgSize={}", val));
    }
    if let Some(val) = imgType.as_ref() {
        query_parts.push(format!("imgType={}", val));
    }
    if let Some(val) = linkSite.as_ref() {
        query_parts.push(format!("linkSite={}", val));
    }
    if let Some(val) = lowRange.as_ref() {
        query_parts.push(format!("lowRange={}", val));
    }
    if let Some(val) = lr.as_ref() {
        query_parts.push(format!("lr={}", val));
    }
    if let Some(val) = num.as_ref() {
        query_parts.push(format!("num={}", val));
    }
    if let Some(val) = orTerms.as_ref() {
        query_parts.push(format!("orTerms={}", val));
    }
    if let Some(val) = q.as_ref() {
        query_parts.push(format!("q={}", val));
    }
    if let Some(val) = relatedSite.as_ref() {
        query_parts.push(format!("relatedSite={}", val));
    }
    if let Some(val) = rights.as_ref() {
        query_parts.push(format!("rights={}", val));
    }
    if let Some(val) = safe.as_ref() {
        query_parts.push(format!("safe={}", val));
    }
    if let Some(val) = searchType.as_ref() {
        query_parts.push(format!("searchType={}", val));
    }
    if let Some(val) = siteSearch.as_ref() {
        query_parts.push(format!("siteSearch={}", val));
    }
    if let Some(val) = siteSearchFilter.as_ref() {
        query_parts.push(format!("siteSearchFilter={}", val));
    }
    if let Some(val) = snippetLength.as_ref() {
        query_parts.push(format!("snippetLength={}", val));
    }
    if let Some(val) = sort.as_ref() {
        query_parts.push(format!("sort={}", val));
    }
    if let Some(val) = start.as_ref() {
        query_parts.push(format!("start={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET customsearch/v1/siterestrict
/// Returns metadata about the search performed, metadata about the engine used for the search, and the search results. Uses a small set of url patterns.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `search_cse_siterestrict_list_execute()` or `search_cse_siterestrict_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `search_cse_siterestrict_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn search_cse_siterestrict_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Search>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Search = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET customsearch/v1/siterestrict
/// Returns metadata about the search performed, metadata about the engine used for the search, and the search results. Uses a small set of url patterns.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `search_cse_siterestrict_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `search_cse_siterestrict_list_task()`.
/// For the simplest API, use `search_cse_siterestrict_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `search_cse_siterestrict_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn search_cse_siterestrict_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Search>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = search_cse_siterestrict_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`search_cse_siterestrict_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct SearchCseSiterestrictListArgs {
    /// Query parameter: c2coff
    pub c2coff: Option<String>,
    /// Query parameter: cr
    pub cr: Option<String>,
    /// Query parameter: cx
    pub cx: Option<String>,
    /// Query parameter: dateRestrict
    pub dateRestrict: Option<String>,
    /// Query parameter: enableAlternateSearchHandler
    pub enableAlternateSearchHandler: Option<String>,
    /// Query parameter: exactTerms
    pub exactTerms: Option<String>,
    /// Query parameter: excludeTerms
    pub excludeTerms: Option<String>,
    /// Query parameter: fileType
    pub fileType: Option<String>,
    /// Query parameter: filter
    pub filter: Option<String>,
    /// Query parameter: gl
    pub gl: Option<String>,
    /// Query parameter: googlehost
    pub googlehost: Option<String>,
    /// Query parameter: highRange
    pub highRange: Option<String>,
    /// Query parameter: hl
    pub hl: Option<String>,
    /// Query parameter: hq
    pub hq: Option<String>,
    /// Query parameter: imgColorType
    pub imgColorType: Option<String>,
    /// Query parameter: imgDominantColor
    pub imgDominantColor: Option<String>,
    /// Query parameter: imgSize
    pub imgSize: Option<String>,
    /// Query parameter: imgType
    pub imgType: Option<String>,
    /// Query parameter: linkSite
    pub linkSite: Option<String>,
    /// Query parameter: lowRange
    pub lowRange: Option<String>,
    /// Query parameter: lr
    pub lr: Option<String>,
    /// Query parameter: num
    pub num: Option<String>,
    /// Query parameter: orTerms
    pub orTerms: Option<String>,
    /// Query parameter: q
    pub q: Option<String>,
    /// Query parameter: relatedSite
    pub relatedSite: Option<String>,
    /// Query parameter: rights
    pub rights: Option<String>,
    /// Query parameter: safe
    pub safe: Option<String>,
    /// Query parameter: searchType
    pub searchType: Option<String>,
    /// Query parameter: siteSearch
    pub siteSearch: Option<String>,
    /// Query parameter: siteSearchFilter
    pub siteSearchFilter: Option<String>,
    /// Query parameter: snippetLength
    pub snippetLength: Option<String>,
    /// Query parameter: sort
    pub sort: Option<String>,
    /// Query parameter: start
    pub start: Option<String>,
}

/// GET customsearch/v1/siterestrict
/// Returns metadata about the search performed, metadata about the engine used for the search, and the search results. Uses a small set of url patterns.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `search_cse_siterestrict_list_builder()` + `search_cse_siterestrict_list_execute()`.
/// For task-level control, use `search_cse_siterestrict_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn search_cse_siterestrict_list(
    client: &SimpleHttpClient,
    args: &SearchCseSiterestrictListArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Search>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = search_cse_siterestrict_list_builder(
        client,
        &args.c2coff,
        &args.cr,
        &args.cx,
        &args.dateRestrict,
        &args.enableAlternateSearchHandler,
        &args.exactTerms,
        &args.excludeTerms,
        &args.fileType,
        &args.filter,
        &args.gl,
        &args.googlehost,
        &args.highRange,
        &args.hl,
        &args.hq,
        &args.imgColorType,
        &args.imgDominantColor,
        &args.imgSize,
        &args.imgType,
        &args.linkSite,
        &args.lowRange,
        &args.lr,
        &args.num,
        &args.orTerms,
        &args.q,
        &args.relatedSite,
        &args.rights,
        &args.safe,
        &args.searchType,
        &args.siteSearch,
        &args.siteSearchFilter,
        &args.snippetLength,
        &args.sort,
        &args.start,
    )?;
    search_cse_siterestrict_list_execute(builder)
}

// =============================================================================
// ResourceIdentifier implementation for Search
// =============================================================================

/// ResourceIdentifier implementation for Search with SearchCseListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<SearchCseListArgs> for Search {
    fn generate_resource_id(&self, input: &SearchCseListArgs) -> String {
        "gcp::customsearch::Search".to_string()
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::customsearch::Search"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Search
// =============================================================================

/// ResourceIdentifier implementation for Search with SearchCseSiterestrictListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<SearchCseSiterestrictListArgs> for Search {
    fn generate_resource_id(&self, input: &SearchCseSiterestrictListArgs) -> String {
        "gcp::customsearch::Search".to_string()
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::customsearch::Search"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}
