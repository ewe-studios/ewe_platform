//! Auto-generated API clients for Google Cloud Platform - mybusinessverifications.
//!
//! Generated by `cargo run --bin ewe_platform gen_provider_clients`.
//! DO NOT EDIT MANUALLY.
//!
//! Feature flag: `gcp`


#![cfg(feature = "gcp")]

use crate::providers::gcp::clients::types::*;
use crate::providers::gcp::resources::*;
use foundation_core::valtron::{execute, StreamIterator, StreamIteratorExt};
use foundation_core::wire::simple_http::client::{
    body_reader, ClientRequestBuilder, RequestIntro, SimpleHttpClient, SystemDnsResolver,
};

/// GET v1/{+location}:fetchVerificationOptions
/// Reports all eligible verification options for a location in a specific language.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `mybusinessverifications_locations_fetch_verification_options_execute()` to send, or `mybusinessverifications_locations_fetch_verification_options` for simplest API.

pub fn mybusinessverifications_locations_fetch_verification_options_builder(
    client: &SimpleHttpClient,
    location: &str,
    body: &FetchVerificationOptionsRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comv1/{+location}:fetchVerificationOptions",
        location,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v1/{+location}:fetchVerificationOptions
/// Reports all eligible verification options for a location in a specific language.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `mybusinessverifications_locations_fetch_verification_options_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from mybusinessverifications_locations_fetch_verification_options_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn mybusinessverifications_locations_fetch_verification_options_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<FetchVerificationOptionsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: FetchVerificationOptionsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+location}:fetchVerificationOptions
/// Reports all eligible verification options for a location in a specific language.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `mybusinessverifications_locations_fetch_verification_options_builder()` + `mybusinessverifications_locations_fetch_verification_options_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn mybusinessverifications_locations_fetch_verification_options(
    client: &SimpleHttpClient,
    location: &str,
    body: &FetchVerificationOptionsRequest,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<FetchVerificationOptionsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = mybusinessverifications_locations_fetch_verification_options_builder(
        client, location, body,
    )?;
    mybusinessverifications_locations_fetch_verification_options_execute(builder)
}

/// GET v1/{+name}/VoiceOfMerchantState
/// Gets the VoiceOfMerchant state.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `mybusinessverifications_locations_get_voice_of_merchant_state_execute()` to send, or `mybusinessverifications_locations_get_voice_of_merchant_state` for simplest API.

pub fn mybusinessverifications_locations_get_voice_of_merchant_state_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comv1/{+name}/VoiceOfMerchantState",
        name,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+name}/VoiceOfMerchantState
/// Gets the VoiceOfMerchant state.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `mybusinessverifications_locations_get_voice_of_merchant_state_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from mybusinessverifications_locations_get_voice_of_merchant_state_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn mybusinessverifications_locations_get_voice_of_merchant_state_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<VoiceOfMerchantState>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: VoiceOfMerchantState = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+name}/VoiceOfMerchantState
/// Gets the VoiceOfMerchant state.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `mybusinessverifications_locations_get_voice_of_merchant_state_builder()` + `mybusinessverifications_locations_get_voice_of_merchant_state_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn mybusinessverifications_locations_get_voice_of_merchant_state(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<VoiceOfMerchantState>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        mybusinessverifications_locations_get_voice_of_merchant_state_builder(client, name)?;
    mybusinessverifications_locations_get_voice_of_merchant_state_execute(builder)
}

/// GET v1/{+name}:verify
/// Starts the verification process for a location.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `mybusinessverifications_locations_verify_execute()` to send, or `mybusinessverifications_locations_verify` for simplest API.

pub fn mybusinessverifications_locations_verify_builder(
    client: &SimpleHttpClient,
    name: &str,
    body: &VerifyLocationRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/{+name}:verify", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v1/{+name}:verify
/// Starts the verification process for a location.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `mybusinessverifications_locations_verify_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from mybusinessverifications_locations_verify_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn mybusinessverifications_locations_verify_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<VerifyLocationResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: VerifyLocationResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+name}:verify
/// Starts the verification process for a location.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `mybusinessverifications_locations_verify_builder()` + `mybusinessverifications_locations_verify_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn mybusinessverifications_locations_verify(
    client: &SimpleHttpClient,
    name: &str,
    body: &VerifyLocationRequest,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<VerifyLocationResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = mybusinessverifications_locations_verify_builder(client, name, body)?;
    mybusinessverifications_locations_verify_execute(builder)
}

/// GET v1/{+name}:complete
/// Completes a `PENDING` verification. It is only necessary for non AUTO verification methods. AUTO verification request is instantly VERIFIED upon creation.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `mybusinessverifications_locations_verifications_complete_execute()` to send, or `mybusinessverifications_locations_verifications_complete` for simplest API.

pub fn mybusinessverifications_locations_verifications_complete_builder(
    client: &SimpleHttpClient,
    name: &str,
    body: &CompleteVerificationRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/{+name}:complete", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v1/{+name}:complete
/// Completes a `PENDING` verification. It is only necessary for non AUTO verification methods. AUTO verification request is instantly VERIFIED upon creation.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `mybusinessverifications_locations_verifications_complete_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from mybusinessverifications_locations_verifications_complete_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn mybusinessverifications_locations_verifications_complete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<CompleteVerificationResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: CompleteVerificationResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+name}:complete
/// Completes a `PENDING` verification. It is only necessary for non AUTO verification methods. AUTO verification request is instantly VERIFIED upon creation.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `mybusinessverifications_locations_verifications_complete_builder()` + `mybusinessverifications_locations_verifications_complete_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn mybusinessverifications_locations_verifications_complete(
    client: &SimpleHttpClient,
    name: &str,
    body: &CompleteVerificationRequest,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<CompleteVerificationResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        mybusinessverifications_locations_verifications_complete_builder(client, name, body)?;
    mybusinessverifications_locations_verifications_complete_execute(builder)
}

/// GET v1/{+parent}/verifications
/// List verifications of a location, ordered by create time.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `mybusinessverifications_locations_verifications_list_execute()` to send, or `mybusinessverifications_locations_verifications_list` for simplest API.

pub fn mybusinessverifications_locations_verifications_list_builder(
    client: &SimpleHttpClient,
    parent: &str,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/{+parent}/verifications", parent,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "pageSize",
            pageSize
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageToken",
            pageToken
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET v1/{+parent}/verifications
/// List verifications of a location, ordered by create time.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `mybusinessverifications_locations_verifications_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from mybusinessverifications_locations_verifications_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn mybusinessverifications_locations_verifications_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListVerificationsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListVerificationsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+parent}/verifications
/// List verifications of a location, ordered by create time.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `mybusinessverifications_locations_verifications_list_builder()` + `mybusinessverifications_locations_verifications_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn mybusinessverifications_locations_verifications_list(
    client: &SimpleHttpClient,
    parent: &str,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListVerificationsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = mybusinessverifications_locations_verifications_list_builder(
        client, parent, pageSize, pageToken,
    )?;
    mybusinessverifications_locations_verifications_list_execute(builder)
}

/// GET v1/verificationTokens:generate
/// Generate a token for the provided location data to verify the location.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `mybusinessverifications_verification_tokens_generate_execute()` to send, or `mybusinessverifications_verification_tokens_generate` for simplest API.

pub fn mybusinessverifications_verification_tokens_generate_builder(
    client: &SimpleHttpClient,
    body: &GenerateInstantVerificationTokenRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/verificationTokens:generate",);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v1/verificationTokens:generate
/// Generate a token for the provided location data to verify the location.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `mybusinessverifications_verification_tokens_generate_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from mybusinessverifications_verification_tokens_generate_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn mybusinessverifications_verification_tokens_generate_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GenerateInstantVerificationTokenResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GenerateInstantVerificationTokenResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/verificationTokens:generate
/// Generate a token for the provided location data to verify the location.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `mybusinessverifications_verification_tokens_generate_builder()` + `mybusinessverifications_verification_tokens_generate_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn mybusinessverifications_verification_tokens_generate(
    client: &SimpleHttpClient,
    body: &GenerateInstantVerificationTokenRequest,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GenerateInstantVerificationTokenResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = mybusinessverifications_verification_tokens_generate_builder(client, body)?;
    mybusinessverifications_verification_tokens_generate_execute(builder)
}
