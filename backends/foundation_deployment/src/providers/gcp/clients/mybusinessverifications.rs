//! Auto-generated API clients for gcp/mybusinessverifications.
//!
//! Generated by `cargo run --bin ewe_platform gen_provider_clients`.
//! DO NOT EDIT MANUALLY.
//!
//! Feature flag: `gcp`

#![cfg(feature = "gcp")]

pub mod types;

use crate::providers::gcp::clients::types::*;
use crate::providers::gcp::resources::*;
use foundation_core::valtron::{
    execute, BoxedSendExecutionAction, StreamIterator, StreamIteratorExt, TaskIterator,
    TaskIteratorExt,
};
use foundation_core::wire::simple_http::client::{
    body_reader, ClientRequestBuilder, RequestIntro, SimpleHttpClient, SystemDnsResolver,
};
use foundation_macros::JsonHash;
use serde::Serialize;

/// GET v1/locations/{locationsId}:fetchVerificationOptions
/// Reports all eligible verification options for a location in a specific language.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `mybusinessverifications_locations_fetch_verification_options_execute()` to send, or `mybusinessverifications_locations_fetch_verification_options` for simplest API.

pub fn mybusinessverifications_locations_fetch_verification_options_builder(
    client: &SimpleHttpClient,
    location: &String,
    body: &FetchVerificationOptionsRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://mybusinessverifications.googleapis.com/v1/locations/{}:fetchVerificationOptions",
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/locations/{locationsId}:fetchVerificationOptions
/// Reports all eligible verification options for a location in a specific language.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `mybusinessverifications_locations_fetch_verification_options_execute()` or `mybusinessverifications_locations_fetch_verification_options`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `mybusinessverifications_locations_fetch_verification_options_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn mybusinessverifications_locations_fetch_verification_options_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<FetchVerificationOptionsResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: FetchVerificationOptionsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/locations/{locationsId}:fetchVerificationOptions
/// Reports all eligible verification options for a location in a specific language.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `mybusinessverifications_locations_fetch_verification_options_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `mybusinessverifications_locations_fetch_verification_options_task()`.
/// For the simplest API, use `mybusinessverifications_locations_fetch_verification_options()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `mybusinessverifications_locations_fetch_verification_options_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn mybusinessverifications_locations_fetch_verification_options_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<FetchVerificationOptionsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = mybusinessverifications_locations_fetch_verification_options_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`mybusinessverifications_locations_fetch_verification_options`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct MybusinessverificationsLocationsFetchVerificationOptionsArgs {
    /// Path parameter: location
    pub location: String,
    /// Request body.
    pub body: FetchVerificationOptionsRequest,
}

/// GET v1/locations/{locationsId}:fetchVerificationOptions
/// Reports all eligible verification options for a location in a specific language.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `mybusinessverifications_locations_fetch_verification_options_builder()` + `mybusinessverifications_locations_fetch_verification_options_execute()`.
/// For task-level control, use `mybusinessverifications_locations_fetch_verification_options_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn mybusinessverifications_locations_fetch_verification_options(
    client: &SimpleHttpClient,
    args: &MybusinessverificationsLocationsFetchVerificationOptionsArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<FetchVerificationOptionsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = mybusinessverifications_locations_fetch_verification_options_builder(
        client,
        &args.location,
        &args.body,
    )?;
    mybusinessverifications_locations_fetch_verification_options_execute(builder)
}

/// GET v1/locations/{locationsId}/VoiceOfMerchantState
/// Gets the VoiceOfMerchant state.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `mybusinessverifications_locations_get_voice_of_merchant_state_execute()` to send, or `mybusinessverifications_locations_get_voice_of_merchant_state` for simplest API.

pub fn mybusinessverifications_locations_get_voice_of_merchant_state_builder(
    client: &SimpleHttpClient,
    name: &String,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://mybusinessverifications.googleapis.com/v1/locations/{}/VoiceOfMerchantState",
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/locations/{locationsId}/VoiceOfMerchantState
/// Gets the VoiceOfMerchant state.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `mybusinessverifications_locations_get_voice_of_merchant_state_execute()` or `mybusinessverifications_locations_get_voice_of_merchant_state`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `mybusinessverifications_locations_get_voice_of_merchant_state_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn mybusinessverifications_locations_get_voice_of_merchant_state_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<VoiceOfMerchantState>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: VoiceOfMerchantState = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/locations/{locationsId}/VoiceOfMerchantState
/// Gets the VoiceOfMerchant state.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `mybusinessverifications_locations_get_voice_of_merchant_state_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `mybusinessverifications_locations_get_voice_of_merchant_state_task()`.
/// For the simplest API, use `mybusinessverifications_locations_get_voice_of_merchant_state()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `mybusinessverifications_locations_get_voice_of_merchant_state_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn mybusinessverifications_locations_get_voice_of_merchant_state_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<VoiceOfMerchantState>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = mybusinessverifications_locations_get_voice_of_merchant_state_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`mybusinessverifications_locations_get_voice_of_merchant_state`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct MybusinessverificationsLocationsGetVoiceOfMerchantStateArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/locations/{locationsId}/VoiceOfMerchantState
/// Gets the VoiceOfMerchant state.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `mybusinessverifications_locations_get_voice_of_merchant_state_builder()` + `mybusinessverifications_locations_get_voice_of_merchant_state_execute()`.
/// For task-level control, use `mybusinessverifications_locations_get_voice_of_merchant_state_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn mybusinessverifications_locations_get_voice_of_merchant_state(
    client: &SimpleHttpClient,
    args: &MybusinessverificationsLocationsGetVoiceOfMerchantStateArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<VoiceOfMerchantState>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        mybusinessverifications_locations_get_voice_of_merchant_state_builder(client, &args.name)?;
    mybusinessverifications_locations_get_voice_of_merchant_state_execute(builder)
}

/// GET v1/locations/{locationsId}:verify
/// Starts the verification process for a location.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `mybusinessverifications_locations_verify_execute()` to send, or `mybusinessverifications_locations_verify` for simplest API.

pub fn mybusinessverifications_locations_verify_builder(
    client: &SimpleHttpClient,
    name: &String,
    body: &VerifyLocationRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url =
        format!("https://mybusinessverifications.googleapis.com/v1/locations/{}:verify",);

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/locations/{locationsId}:verify
/// Starts the verification process for a location.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `mybusinessverifications_locations_verify_execute()` or `mybusinessverifications_locations_verify`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `mybusinessverifications_locations_verify_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn mybusinessverifications_locations_verify_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<VerifyLocationResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: VerifyLocationResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/locations/{locationsId}:verify
/// Starts the verification process for a location.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `mybusinessverifications_locations_verify_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `mybusinessverifications_locations_verify_task()`.
/// For the simplest API, use `mybusinessverifications_locations_verify()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `mybusinessverifications_locations_verify_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn mybusinessverifications_locations_verify_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<VerifyLocationResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = mybusinessverifications_locations_verify_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`mybusinessverifications_locations_verify`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct MybusinessverificationsLocationsVerifyArgs {
    /// Path parameter: name
    pub name: String,
    /// Request body.
    pub body: VerifyLocationRequest,
}

/// GET v1/locations/{locationsId}:verify
/// Starts the verification process for a location.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `mybusinessverifications_locations_verify_builder()` + `mybusinessverifications_locations_verify_execute()`.
/// For task-level control, use `mybusinessverifications_locations_verify_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn mybusinessverifications_locations_verify(
    client: &SimpleHttpClient,
    args: &MybusinessverificationsLocationsVerifyArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<VerifyLocationResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = mybusinessverifications_locations_verify_builder(client, &args.name, &args.body)?;
    mybusinessverifications_locations_verify_execute(builder)
}

/// GET v1/locations/{locationsId}/verifications
/// List verifications of a location, ordered by create time.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `mybusinessverifications_locations_verifications_list_execute()` to send, or `mybusinessverifications_locations_verifications_list` for simplest API.

pub fn mybusinessverifications_locations_verifications_list_builder(
    client: &SimpleHttpClient,
    parent: &String,
    pageSize: &Option<i32>,
    pageToken: &Option<String>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url =
        format!("https://mybusinessverifications.googleapis.com/v1/locations/{}/verifications",);

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = pageSize.as_ref() {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/locations/{locationsId}/verifications
/// List verifications of a location, ordered by create time.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `mybusinessverifications_locations_verifications_list_execute()` or `mybusinessverifications_locations_verifications_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `mybusinessverifications_locations_verifications_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn mybusinessverifications_locations_verifications_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ListVerificationsResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListVerificationsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/locations/{locationsId}/verifications
/// List verifications of a location, ordered by create time.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `mybusinessverifications_locations_verifications_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `mybusinessverifications_locations_verifications_list_task()`.
/// For the simplest API, use `mybusinessverifications_locations_verifications_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `mybusinessverifications_locations_verifications_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn mybusinessverifications_locations_verifications_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListVerificationsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = mybusinessverifications_locations_verifications_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`mybusinessverifications_locations_verifications_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct MybusinessverificationsLocationsVerificationsListArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: pageSize
    pub pageSize: Option<i32>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
}

/// GET v1/locations/{locationsId}/verifications
/// List verifications of a location, ordered by create time.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `mybusinessverifications_locations_verifications_list_builder()` + `mybusinessverifications_locations_verifications_list_execute()`.
/// For task-level control, use `mybusinessverifications_locations_verifications_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn mybusinessverifications_locations_verifications_list(
    client: &SimpleHttpClient,
    args: &MybusinessverificationsLocationsVerificationsListArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListVerificationsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = mybusinessverifications_locations_verifications_list_builder(
        client,
        &args.parent,
        &args.pageSize,
        &args.pageToken,
    )?;
    mybusinessverifications_locations_verifications_list_execute(builder)
}

/// GET v1/verificationTokens:generate
/// Generate a token for the provided location data to verify the location.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `mybusinessverifications_verification_tokens_generate_execute()` to send, or `mybusinessverifications_verification_tokens_generate` for simplest API.

pub fn mybusinessverifications_verification_tokens_generate_builder(
    client: &SimpleHttpClient,
    body: &GenerateInstantVerificationTokenRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url =
        format!("https://mybusinessverifications.googleapis.com/v1/verificationTokens:generate",);

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/verificationTokens:generate
/// Generate a token for the provided location data to verify the location.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `mybusinessverifications_verification_tokens_generate_execute()` or `mybusinessverifications_verification_tokens_generate`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `mybusinessverifications_verification_tokens_generate_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn mybusinessverifications_verification_tokens_generate_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GenerateInstantVerificationTokenResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GenerateInstantVerificationTokenResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/verificationTokens:generate
/// Generate a token for the provided location data to verify the location.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `mybusinessverifications_verification_tokens_generate_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `mybusinessverifications_verification_tokens_generate_task()`.
/// For the simplest API, use `mybusinessverifications_verification_tokens_generate()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `mybusinessverifications_verification_tokens_generate_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn mybusinessverifications_verification_tokens_generate_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GenerateInstantVerificationTokenResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = mybusinessverifications_verification_tokens_generate_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`mybusinessverifications_verification_tokens_generate`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct MybusinessverificationsVerificationTokensGenerateArgs {
    /// Request body.
    pub body: GenerateInstantVerificationTokenRequest,
}

/// GET v1/verificationTokens:generate
/// Generate a token for the provided location data to verify the location.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `mybusinessverifications_verification_tokens_generate_builder()` + `mybusinessverifications_verification_tokens_generate_execute()`.
/// For task-level control, use `mybusinessverifications_verification_tokens_generate_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn mybusinessverifications_verification_tokens_generate(
    client: &SimpleHttpClient,
    args: &MybusinessverificationsVerificationTokensGenerateArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GenerateInstantVerificationTokenResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = mybusinessverifications_verification_tokens_generate_builder(client, &args.body)?;
    mybusinessverifications_verification_tokens_generate_execute(builder)
}
