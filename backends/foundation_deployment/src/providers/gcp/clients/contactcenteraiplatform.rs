//! Auto-generated API clients for gcp/contactcenteraiplatform.
//!
//! Generated by `cargo run --bin ewe_platform gen_provider_clients`.
//! DO NOT EDIT MANUALLY.
//!
//! Feature flag: `gcp`

#![cfg(feature = "gcp")]

pub mod types;

use crate::providers::gcp::clients::types::*;
use crate::providers::gcp::resources::*;
use foundation_core::valtron::{
    execute, StreamIterator, StreamIteratorExt, TaskIterator, TaskIteratorExt,
};
use foundation_core::wire::simple_http::client::{
    body_reader, ClientRequestBuilder, RequestIntro, SimpleHttpClient, SystemDnsResolver,
};
use foundation_macros::JsonHash;
use serde::Serialize;

/// GET v1alpha1/projects/{projectsId}/locations/{locationsId}:generateShifts
/// Generates shifts constrained by various parameters.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `contactcenteraiplatform_projects_locations_generate_shifts_execute()` to send, or `contactcenteraiplatform_projects_locations_generate_shifts` for simplest API.

pub fn contactcenteraiplatform_projects_locations_generate_shifts_builder(
    client: &SimpleHttpClient,
    parent: &str,
    body: &GenerateShiftsRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://contactcenteraiplatform.googleapis.com/v1alpha1/projects/{}/locations/{}:generateShifts",
        parent,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1alpha1/projects/{projectsId}/locations/{locationsId}:generateShifts
/// Generates shifts constrained by various parameters.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `contactcenteraiplatform_projects_locations_generate_shifts_execute()` or `contactcenteraiplatform_projects_locations_generate_shifts`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `contactcenteraiplatform_projects_locations_generate_shifts_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn contactcenteraiplatform_projects_locations_generate_shifts_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1alpha1/projects/{projectsId}/locations/{locationsId}:generateShifts
/// Generates shifts constrained by various parameters.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `contactcenteraiplatform_projects_locations_generate_shifts_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `contactcenteraiplatform_projects_locations_generate_shifts_task()`.
/// For the simplest API, use `contactcenteraiplatform_projects_locations_generate_shifts()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `contactcenteraiplatform_projects_locations_generate_shifts_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn contactcenteraiplatform_projects_locations_generate_shifts_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = contactcenteraiplatform_projects_locations_generate_shifts_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`contactcenteraiplatform_projects_locations_generate_shifts`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ContactcenteraiplatformProjectsLocationsGenerateShiftsArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Request body.
    pub body: GenerateShiftsRequest,
}

/// GET v1alpha1/projects/{projectsId}/locations/{locationsId}:generateShifts
/// Generates shifts constrained by various parameters.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `contactcenteraiplatform_projects_locations_generate_shifts_builder()` + `contactcenteraiplatform_projects_locations_generate_shifts_execute()`.
/// For task-level control, use `contactcenteraiplatform_projects_locations_generate_shifts_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn contactcenteraiplatform_projects_locations_generate_shifts(
    client: &SimpleHttpClient,
    args: &ContactcenteraiplatformProjectsLocationsGenerateShiftsArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = contactcenteraiplatform_projects_locations_generate_shifts_builder(
        client,
        &args.parent,
        &args.body,
    )?;
    contactcenteraiplatform_projects_locations_generate_shifts_execute(builder)
}

/// GET v1alpha1/projects/{projectsId}/locations/{locationsId}
/// Gets information about a location.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `contactcenteraiplatform_projects_locations_get_execute()` to send, or `contactcenteraiplatform_projects_locations_get` for simplest API.

pub fn contactcenteraiplatform_projects_locations_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://contactcenteraiplatform.googleapis.com/v1alpha1/projects/{}/locations/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1alpha1/projects/{projectsId}/locations/{locationsId}
/// Gets information about a location.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `contactcenteraiplatform_projects_locations_get_execute()` or `contactcenteraiplatform_projects_locations_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `contactcenteraiplatform_projects_locations_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn contactcenteraiplatform_projects_locations_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<Location>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Location = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1alpha1/projects/{projectsId}/locations/{locationsId}
/// Gets information about a location.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `contactcenteraiplatform_projects_locations_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `contactcenteraiplatform_projects_locations_get_task()`.
/// For the simplest API, use `contactcenteraiplatform_projects_locations_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `contactcenteraiplatform_projects_locations_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn contactcenteraiplatform_projects_locations_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Location>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = contactcenteraiplatform_projects_locations_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`contactcenteraiplatform_projects_locations_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ContactcenteraiplatformProjectsLocationsGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1alpha1/projects/{projectsId}/locations/{locationsId}
/// Gets information about a location.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `contactcenteraiplatform_projects_locations_get_builder()` + `contactcenteraiplatform_projects_locations_get_execute()`.
/// For task-level control, use `contactcenteraiplatform_projects_locations_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn contactcenteraiplatform_projects_locations_get(
    client: &SimpleHttpClient,
    args: &ContactcenteraiplatformProjectsLocationsGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Location>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = contactcenteraiplatform_projects_locations_get_builder(client, &args.name)?;
    contactcenteraiplatform_projects_locations_get_execute(builder)
}

/// GET v1alpha1/projects/{projectsId}/locations
/// Lists information about the supported locations for this service. This method lists locations based on the resource scope provided in the [ListLocationsRequest.name] field: * **Global locations**: If name is empty, the method lists the public locations available to all projects. * **Project-specific locations**: If name follows the format `projects/{project}`, the method lists locations visible to that specific project. This includes public, private, or other project-specific locations enabled for the project. For `gRPC` and client library implementations, the resource name is passed as the name field. For direct service calls, the resource name is incorporated into the request path based on the specific service implementation and version.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `contactcenteraiplatform_projects_locations_list_execute()` to send, or `contactcenteraiplatform_projects_locations_list` for simplest API.

pub fn contactcenteraiplatform_projects_locations_list_builder(
    client: &SimpleHttpClient,
    name: &str,
    extraLocationTypes: Option<&str>,
    filter: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://contactcenteraiplatform.googleapis.com/v1alpha1/projects/{}/locations",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = extraLocationTypes {
        query_parts.push(format!("extraLocationTypes={}", val));
    }
    if let Some(val) = filter {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1alpha1/projects/{projectsId}/locations
/// Lists information about the supported locations for this service. This method lists locations based on the resource scope provided in the [ListLocationsRequest.name] field: * **Global locations**: If name is empty, the method lists the public locations available to all projects. * **Project-specific locations**: If name follows the format `projects/{project}`, the method lists locations visible to that specific project. This includes public, private, or other project-specific locations enabled for the project. For `gRPC` and client library implementations, the resource name is passed as the name field. For direct service calls, the resource name is incorporated into the request path based on the specific service implementation and version.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `contactcenteraiplatform_projects_locations_list_execute()` or `contactcenteraiplatform_projects_locations_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `contactcenteraiplatform_projects_locations_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn contactcenteraiplatform_projects_locations_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<ListLocationsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListLocationsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1alpha1/projects/{projectsId}/locations
/// Lists information about the supported locations for this service. This method lists locations based on the resource scope provided in the [ListLocationsRequest.name] field: * **Global locations**: If name is empty, the method lists the public locations available to all projects. * **Project-specific locations**: If name follows the format `projects/{project}`, the method lists locations visible to that specific project. This includes public, private, or other project-specific locations enabled for the project. For `gRPC` and client library implementations, the resource name is passed as the name field. For direct service calls, the resource name is incorporated into the request path based on the specific service implementation and version.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `contactcenteraiplatform_projects_locations_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `contactcenteraiplatform_projects_locations_list_task()`.
/// For the simplest API, use `contactcenteraiplatform_projects_locations_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `contactcenteraiplatform_projects_locations_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn contactcenteraiplatform_projects_locations_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListLocationsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = contactcenteraiplatform_projects_locations_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`contactcenteraiplatform_projects_locations_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ContactcenteraiplatformProjectsLocationsListArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: extraLocationTypes
    pub extraLocationTypes: Option<String>,
    /// Query parameter: filter
    pub filter: Option<String>,
    /// Query parameter: pageSize
    pub pageSize: Option<i32>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
}

/// GET v1alpha1/projects/{projectsId}/locations
/// Lists information about the supported locations for this service. This method lists locations based on the resource scope provided in the [ListLocationsRequest.name] field: * **Global locations**: If name is empty, the method lists the public locations available to all projects. * **Project-specific locations**: If name follows the format `projects/{project}`, the method lists locations visible to that specific project. This includes public, private, or other project-specific locations enabled for the project. For `gRPC` and client library implementations, the resource name is passed as the name field. For direct service calls, the resource name is incorporated into the request path based on the specific service implementation and version.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `contactcenteraiplatform_projects_locations_list_builder()` + `contactcenteraiplatform_projects_locations_list_execute()`.
/// For task-level control, use `contactcenteraiplatform_projects_locations_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn contactcenteraiplatform_projects_locations_list(
    client: &SimpleHttpClient,
    args: &ContactcenteraiplatformProjectsLocationsListArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListLocationsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = contactcenteraiplatform_projects_locations_list_builder(
        client,
        &args.name,
        args.extraLocationTypes.as_deref(),
        args.filter.as_deref(),
        args.pageSize,
        args.pageToken.as_deref(),
    )?;
    contactcenteraiplatform_projects_locations_list_execute(builder)
}

/// GET v1alpha1/projects/{projectsId}/locations/{locationsId}:queryContactCenterQuota
/// Queries the contact center quota, an aggregation over all the projects, that belongs to the billing account, which the input project belongs to.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `contactcenteraiplatform_projects_locations_query_contact_center_quota_execute()` to send, or `contactcenteraiplatform_projects_locations_query_contact_center_quota` for simplest API.

pub fn contactcenteraiplatform_projects_locations_query_contact_center_quota_builder(
    client: &SimpleHttpClient,
    parent: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://contactcenteraiplatform.googleapis.com/v1alpha1/projects/{}/locations/{}:queryContactCenterQuota",
        parent,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1alpha1/projects/{projectsId}/locations/{locationsId}:queryContactCenterQuota
/// Queries the contact center quota, an aggregation over all the projects, that belongs to the billing account, which the input project belongs to.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `contactcenteraiplatform_projects_locations_query_contact_center_quota_execute()` or `contactcenteraiplatform_projects_locations_query_contact_center_quota`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `contactcenteraiplatform_projects_locations_query_contact_center_quota_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn contactcenteraiplatform_projects_locations_query_contact_center_quota_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<ContactCenterQuota>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ContactCenterQuota = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1alpha1/projects/{projectsId}/locations/{locationsId}:queryContactCenterQuota
/// Queries the contact center quota, an aggregation over all the projects, that belongs to the billing account, which the input project belongs to.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `contactcenteraiplatform_projects_locations_query_contact_center_quota_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `contactcenteraiplatform_projects_locations_query_contact_center_quota_task()`.
/// For the simplest API, use `contactcenteraiplatform_projects_locations_query_contact_center_quota()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `contactcenteraiplatform_projects_locations_query_contact_center_quota_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn contactcenteraiplatform_projects_locations_query_contact_center_quota_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ContactCenterQuota>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = contactcenteraiplatform_projects_locations_query_contact_center_quota_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`contactcenteraiplatform_projects_locations_query_contact_center_quota`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ContactcenteraiplatformProjectsLocationsQueryContactCenterQuotaArgs {
    /// Path parameter: parent
    pub parent: String,
}

/// GET v1alpha1/projects/{projectsId}/locations/{locationsId}:queryContactCenterQuota
/// Queries the contact center quota, an aggregation over all the projects, that belongs to the billing account, which the input project belongs to.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `contactcenteraiplatform_projects_locations_query_contact_center_quota_builder()` + `contactcenteraiplatform_projects_locations_query_contact_center_quota_execute()`.
/// For task-level control, use `contactcenteraiplatform_projects_locations_query_contact_center_quota_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn contactcenteraiplatform_projects_locations_query_contact_center_quota(
    client: &SimpleHttpClient,
    args: &ContactcenteraiplatformProjectsLocationsQueryContactCenterQuotaArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ContactCenterQuota>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = contactcenteraiplatform_projects_locations_query_contact_center_quota_builder(
        client,
        &args.parent,
    )?;
    contactcenteraiplatform_projects_locations_query_contact_center_quota_execute(builder)
}

/// GET v1alpha1/projects/{projectsId}/locations/{locationsId}/contactCenters
/// Creates a new ContactCenter in a given project and location.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `contactcenteraiplatform_projects_locations_contact_centers_create_execute()` to send, or `contactcenteraiplatform_projects_locations_contact_centers_create` for simplest API.

pub fn contactcenteraiplatform_projects_locations_contact_centers_create_builder(
    client: &SimpleHttpClient,
    parent: &str,
    contactCenterId: Option<&str>,
    requestId: Option<&str>,
    body: &ContactCenter,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://contactcenteraiplatform.googleapis.com/v1alpha1/projects/{}/locations/{}/contactCenters",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = contactCenterId {
        query_parts.push(format!("contactCenterId={}", val));
    }
    if let Some(val) = requestId {
        query_parts.push(format!("requestId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1alpha1/projects/{projectsId}/locations/{locationsId}/contactCenters
/// Creates a new ContactCenter in a given project and location.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `contactcenteraiplatform_projects_locations_contact_centers_create_execute()` or `contactcenteraiplatform_projects_locations_contact_centers_create`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `contactcenteraiplatform_projects_locations_contact_centers_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn contactcenteraiplatform_projects_locations_contact_centers_create_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1alpha1/projects/{projectsId}/locations/{locationsId}/contactCenters
/// Creates a new ContactCenter in a given project and location.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `contactcenteraiplatform_projects_locations_contact_centers_create_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `contactcenteraiplatform_projects_locations_contact_centers_create_task()`.
/// For the simplest API, use `contactcenteraiplatform_projects_locations_contact_centers_create()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `contactcenteraiplatform_projects_locations_contact_centers_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn contactcenteraiplatform_projects_locations_contact_centers_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = contactcenteraiplatform_projects_locations_contact_centers_create_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`contactcenteraiplatform_projects_locations_contact_centers_create`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ContactcenteraiplatformProjectsLocationsContactCentersCreateArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: contactCenterId
    pub contactCenterId: Option<String>,
    /// Query parameter: requestId
    pub requestId: Option<String>,
    /// Request body.
    pub body: ContactCenter,
}

/// GET v1alpha1/projects/{projectsId}/locations/{locationsId}/contactCenters
/// Creates a new ContactCenter in a given project and location.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `contactcenteraiplatform_projects_locations_contact_centers_create_builder()` + `contactcenteraiplatform_projects_locations_contact_centers_create_execute()`.
/// For task-level control, use `contactcenteraiplatform_projects_locations_contact_centers_create_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn contactcenteraiplatform_projects_locations_contact_centers_create(
    client: &SimpleHttpClient,
    args: &ContactcenteraiplatformProjectsLocationsContactCentersCreateArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = contactcenteraiplatform_projects_locations_contact_centers_create_builder(
        client,
        &args.parent,
        args.contactCenterId.as_deref(),
        args.requestId.as_deref(),
        &args.body,
    )?;
    contactcenteraiplatform_projects_locations_contact_centers_create_execute(builder)
}

/// GET v1alpha1/projects/{projectsId}/locations/{locationsId}/contactCenters/{contactCentersId}
/// Deletes a single ContactCenter.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `contactcenteraiplatform_projects_locations_contact_centers_delete_execute()` to send, or `contactcenteraiplatform_projects_locations_contact_centers_delete` for simplest API.

pub fn contactcenteraiplatform_projects_locations_contact_centers_delete_builder(
    client: &SimpleHttpClient,
    name: &str,
    requestId: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://contactcenteraiplatform.googleapis.com/v1alpha1/projects/{}/locations/{}/contactCenters/{}",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = requestId {
        query_parts.push(format!("requestId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1alpha1/projects/{projectsId}/locations/{locationsId}/contactCenters/{contactCentersId}
/// Deletes a single ContactCenter.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `contactcenteraiplatform_projects_locations_contact_centers_delete_execute()` or `contactcenteraiplatform_projects_locations_contact_centers_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `contactcenteraiplatform_projects_locations_contact_centers_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn contactcenteraiplatform_projects_locations_contact_centers_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1alpha1/projects/{projectsId}/locations/{locationsId}/contactCenters/{contactCentersId}
/// Deletes a single ContactCenter.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `contactcenteraiplatform_projects_locations_contact_centers_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `contactcenteraiplatform_projects_locations_contact_centers_delete_task()`.
/// For the simplest API, use `contactcenteraiplatform_projects_locations_contact_centers_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `contactcenteraiplatform_projects_locations_contact_centers_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn contactcenteraiplatform_projects_locations_contact_centers_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = contactcenteraiplatform_projects_locations_contact_centers_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`contactcenteraiplatform_projects_locations_contact_centers_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ContactcenteraiplatformProjectsLocationsContactCentersDeleteArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: requestId
    pub requestId: Option<String>,
}

/// GET v1alpha1/projects/{projectsId}/locations/{locationsId}/contactCenters/{contactCentersId}
/// Deletes a single ContactCenter.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `contactcenteraiplatform_projects_locations_contact_centers_delete_builder()` + `contactcenteraiplatform_projects_locations_contact_centers_delete_execute()`.
/// For task-level control, use `contactcenteraiplatform_projects_locations_contact_centers_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn contactcenteraiplatform_projects_locations_contact_centers_delete(
    client: &SimpleHttpClient,
    args: &ContactcenteraiplatformProjectsLocationsContactCentersDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = contactcenteraiplatform_projects_locations_contact_centers_delete_builder(
        client,
        &args.name,
        args.requestId.as_deref(),
    )?;
    contactcenteraiplatform_projects_locations_contact_centers_delete_execute(builder)
}

/// GET v1alpha1/projects/{projectsId}/locations/{locationsId}/contactCenters/{contactCentersId}
/// Gets details of a single ContactCenter.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `contactcenteraiplatform_projects_locations_contact_centers_get_execute()` to send, or `contactcenteraiplatform_projects_locations_contact_centers_get` for simplest API.

pub fn contactcenteraiplatform_projects_locations_contact_centers_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://contactcenteraiplatform.googleapis.com/v1alpha1/projects/{}/locations/{}/contactCenters/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1alpha1/projects/{projectsId}/locations/{locationsId}/contactCenters/{contactCentersId}
/// Gets details of a single ContactCenter.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `contactcenteraiplatform_projects_locations_contact_centers_get_execute()` or `contactcenteraiplatform_projects_locations_contact_centers_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `contactcenteraiplatform_projects_locations_contact_centers_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn contactcenteraiplatform_projects_locations_contact_centers_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<ContactCenter>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ContactCenter = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1alpha1/projects/{projectsId}/locations/{locationsId}/contactCenters/{contactCentersId}
/// Gets details of a single ContactCenter.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `contactcenteraiplatform_projects_locations_contact_centers_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `contactcenteraiplatform_projects_locations_contact_centers_get_task()`.
/// For the simplest API, use `contactcenteraiplatform_projects_locations_contact_centers_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `contactcenteraiplatform_projects_locations_contact_centers_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn contactcenteraiplatform_projects_locations_contact_centers_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ContactCenter>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = contactcenteraiplatform_projects_locations_contact_centers_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`contactcenteraiplatform_projects_locations_contact_centers_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ContactcenteraiplatformProjectsLocationsContactCentersGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1alpha1/projects/{projectsId}/locations/{locationsId}/contactCenters/{contactCentersId}
/// Gets details of a single ContactCenter.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `contactcenteraiplatform_projects_locations_contact_centers_get_builder()` + `contactcenteraiplatform_projects_locations_contact_centers_get_execute()`.
/// For task-level control, use `contactcenteraiplatform_projects_locations_contact_centers_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn contactcenteraiplatform_projects_locations_contact_centers_get(
    client: &SimpleHttpClient,
    args: &ContactcenteraiplatformProjectsLocationsContactCentersGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ContactCenter>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        contactcenteraiplatform_projects_locations_contact_centers_get_builder(client, &args.name)?;
    contactcenteraiplatform_projects_locations_contact_centers_get_execute(builder)
}

/// GET v1alpha1/projects/{projectsId}/locations/{locationsId}/contactCenters
/// Lists ContactCenters in a given project and location.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `contactcenteraiplatform_projects_locations_contact_centers_list_execute()` to send, or `contactcenteraiplatform_projects_locations_contact_centers_list` for simplest API.

pub fn contactcenteraiplatform_projects_locations_contact_centers_list_builder(
    client: &SimpleHttpClient,
    parent: &str,
    filter: Option<&str>,
    orderBy: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://contactcenteraiplatform.googleapis.com/v1alpha1/projects/{}/locations/{}/contactCenters",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = orderBy {
        query_parts.push(format!("orderBy={}", val));
    }
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1alpha1/projects/{projectsId}/locations/{locationsId}/contactCenters
/// Lists ContactCenters in a given project and location.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `contactcenteraiplatform_projects_locations_contact_centers_list_execute()` or `contactcenteraiplatform_projects_locations_contact_centers_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `contactcenteraiplatform_projects_locations_contact_centers_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn contactcenteraiplatform_projects_locations_contact_centers_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<ListContactCentersResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListContactCentersResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1alpha1/projects/{projectsId}/locations/{locationsId}/contactCenters
/// Lists ContactCenters in a given project and location.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `contactcenteraiplatform_projects_locations_contact_centers_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `contactcenteraiplatform_projects_locations_contact_centers_list_task()`.
/// For the simplest API, use `contactcenteraiplatform_projects_locations_contact_centers_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `contactcenteraiplatform_projects_locations_contact_centers_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn contactcenteraiplatform_projects_locations_contact_centers_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ListContactCentersResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = contactcenteraiplatform_projects_locations_contact_centers_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`contactcenteraiplatform_projects_locations_contact_centers_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ContactcenteraiplatformProjectsLocationsContactCentersListArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: filter
    pub filter: Option<String>,
    /// Query parameter: orderBy
    pub orderBy: Option<String>,
    /// Query parameter: pageSize
    pub pageSize: Option<i32>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
}

/// GET v1alpha1/projects/{projectsId}/locations/{locationsId}/contactCenters
/// Lists ContactCenters in a given project and location.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `contactcenteraiplatform_projects_locations_contact_centers_list_builder()` + `contactcenteraiplatform_projects_locations_contact_centers_list_execute()`.
/// For task-level control, use `contactcenteraiplatform_projects_locations_contact_centers_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn contactcenteraiplatform_projects_locations_contact_centers_list(
    client: &SimpleHttpClient,
    args: &ContactcenteraiplatformProjectsLocationsContactCentersListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ListContactCentersResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = contactcenteraiplatform_projects_locations_contact_centers_list_builder(
        client,
        &args.parent,
        args.filter.as_deref(),
        args.orderBy.as_deref(),
        args.pageSize,
        args.pageToken.as_deref(),
    )?;
    contactcenteraiplatform_projects_locations_contact_centers_list_execute(builder)
}

/// GET v1alpha1/projects/{projectsId}/locations/{locationsId}/contactCenters/{contactCentersId}
/// Updates the parameters of a single ContactCenter.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `contactcenteraiplatform_projects_locations_contact_centers_patch_execute()` to send, or `contactcenteraiplatform_projects_locations_contact_centers_patch` for simplest API.

pub fn contactcenteraiplatform_projects_locations_contact_centers_patch_builder(
    client: &SimpleHttpClient,
    name: &str,
    requestId: Option<&str>,
    updateMask: Option<&str>,
    body: &ContactCenter,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://contactcenteraiplatform.googleapis.com/v1alpha1/projects/{}/locations/{}/contactCenters/{}",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = requestId {
        query_parts.push(format!("requestId={}", val));
    }
    if let Some(val) = updateMask {
        query_parts.push(format!("updateMask={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1alpha1/projects/{projectsId}/locations/{locationsId}/contactCenters/{contactCentersId}
/// Updates the parameters of a single ContactCenter.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `contactcenteraiplatform_projects_locations_contact_centers_patch_execute()` or `contactcenteraiplatform_projects_locations_contact_centers_patch`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `contactcenteraiplatform_projects_locations_contact_centers_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn contactcenteraiplatform_projects_locations_contact_centers_patch_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1alpha1/projects/{projectsId}/locations/{locationsId}/contactCenters/{contactCentersId}
/// Updates the parameters of a single ContactCenter.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `contactcenteraiplatform_projects_locations_contact_centers_patch_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `contactcenteraiplatform_projects_locations_contact_centers_patch_task()`.
/// For the simplest API, use `contactcenteraiplatform_projects_locations_contact_centers_patch()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `contactcenteraiplatform_projects_locations_contact_centers_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn contactcenteraiplatform_projects_locations_contact_centers_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = contactcenteraiplatform_projects_locations_contact_centers_patch_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`contactcenteraiplatform_projects_locations_contact_centers_patch`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ContactcenteraiplatformProjectsLocationsContactCentersPatchArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: requestId
    pub requestId: Option<String>,
    /// Query parameter: updateMask
    pub updateMask: Option<String>,
    /// Request body.
    pub body: ContactCenter,
}

/// GET v1alpha1/projects/{projectsId}/locations/{locationsId}/contactCenters/{contactCentersId}
/// Updates the parameters of a single ContactCenter.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `contactcenteraiplatform_projects_locations_contact_centers_patch_builder()` + `contactcenteraiplatform_projects_locations_contact_centers_patch_execute()`.
/// For task-level control, use `contactcenteraiplatform_projects_locations_contact_centers_patch_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn contactcenteraiplatform_projects_locations_contact_centers_patch(
    client: &SimpleHttpClient,
    args: &ContactcenteraiplatformProjectsLocationsContactCentersPatchArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = contactcenteraiplatform_projects_locations_contact_centers_patch_builder(
        client,
        &args.name,
        args.requestId.as_deref(),
        args.updateMask.as_deref(),
        &args.body,
    )?;
    contactcenteraiplatform_projects_locations_contact_centers_patch_execute(builder)
}

/// GET v1alpha1/projects/{projectsId}/locations/{locationsId}/operations/{operationsId}:cancel
/// Starts asynchronous cancellation on a long-running operation. The server makes a best effort to cancel the operation, but success is not guaranteed. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED. Clients can use Operations.GetOperation or other methods to check whether the cancellation succeeded or whether the operation completed despite cancellation. On successful cancellation, the operation is not deleted; instead, it becomes an operation with an Operation.error value with a google.rpc.Status.code of 1, corresponding to Code.CANCELLED.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `contactcenteraiplatform_projects_locations_operations_cancel_execute()` to send, or `contactcenteraiplatform_projects_locations_operations_cancel` for simplest API.

pub fn contactcenteraiplatform_projects_locations_operations_cancel_builder(
    client: &SimpleHttpClient,
    name: &str,
    body: &CancelOperationRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://contactcenteraiplatform.googleapis.com/v1alpha1/projects/{}/locations/{}/operations/{}:cancel",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1alpha1/projects/{projectsId}/locations/{locationsId}/operations/{operationsId}:cancel
/// Starts asynchronous cancellation on a long-running operation. The server makes a best effort to cancel the operation, but success is not guaranteed. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED. Clients can use Operations.GetOperation or other methods to check whether the cancellation succeeded or whether the operation completed despite cancellation. On successful cancellation, the operation is not deleted; instead, it becomes an operation with an Operation.error value with a google.rpc.Status.code of 1, corresponding to Code.CANCELLED.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `contactcenteraiplatform_projects_locations_operations_cancel_execute()` or `contactcenteraiplatform_projects_locations_operations_cancel`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `contactcenteraiplatform_projects_locations_operations_cancel_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn contactcenteraiplatform_projects_locations_operations_cancel_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Empty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1alpha1/projects/{projectsId}/locations/{locationsId}/operations/{operationsId}:cancel
/// Starts asynchronous cancellation on a long-running operation. The server makes a best effort to cancel the operation, but success is not guaranteed. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED. Clients can use Operations.GetOperation or other methods to check whether the cancellation succeeded or whether the operation completed despite cancellation. On successful cancellation, the operation is not deleted; instead, it becomes an operation with an Operation.error value with a google.rpc.Status.code of 1, corresponding to Code.CANCELLED.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `contactcenteraiplatform_projects_locations_operations_cancel_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `contactcenteraiplatform_projects_locations_operations_cancel_task()`.
/// For the simplest API, use `contactcenteraiplatform_projects_locations_operations_cancel()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `contactcenteraiplatform_projects_locations_operations_cancel_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn contactcenteraiplatform_projects_locations_operations_cancel_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = contactcenteraiplatform_projects_locations_operations_cancel_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`contactcenteraiplatform_projects_locations_operations_cancel`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ContactcenteraiplatformProjectsLocationsOperationsCancelArgs {
    /// Path parameter: name
    pub name: String,
    /// Request body.
    pub body: CancelOperationRequest,
}

/// GET v1alpha1/projects/{projectsId}/locations/{locationsId}/operations/{operationsId}:cancel
/// Starts asynchronous cancellation on a long-running operation. The server makes a best effort to cancel the operation, but success is not guaranteed. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED. Clients can use Operations.GetOperation or other methods to check whether the cancellation succeeded or whether the operation completed despite cancellation. On successful cancellation, the operation is not deleted; instead, it becomes an operation with an Operation.error value with a google.rpc.Status.code of 1, corresponding to Code.CANCELLED.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `contactcenteraiplatform_projects_locations_operations_cancel_builder()` + `contactcenteraiplatform_projects_locations_operations_cancel_execute()`.
/// For task-level control, use `contactcenteraiplatform_projects_locations_operations_cancel_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn contactcenteraiplatform_projects_locations_operations_cancel(
    client: &SimpleHttpClient,
    args: &ContactcenteraiplatformProjectsLocationsOperationsCancelArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = contactcenteraiplatform_projects_locations_operations_cancel_builder(
        client, &args.name, &args.body,
    )?;
    contactcenteraiplatform_projects_locations_operations_cancel_execute(builder)
}

/// GET v1alpha1/projects/{projectsId}/locations/{locationsId}/operations/{operationsId}
/// Deletes a long-running operation. This method indicates that the client is no longer interested in the operation result. It does not cancel the operation. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `contactcenteraiplatform_projects_locations_operations_delete_execute()` to send, or `contactcenteraiplatform_projects_locations_operations_delete` for simplest API.

pub fn contactcenteraiplatform_projects_locations_operations_delete_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://contactcenteraiplatform.googleapis.com/v1alpha1/projects/{}/locations/{}/operations/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1alpha1/projects/{projectsId}/locations/{locationsId}/operations/{operationsId}
/// Deletes a long-running operation. This method indicates that the client is no longer interested in the operation result. It does not cancel the operation. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `contactcenteraiplatform_projects_locations_operations_delete_execute()` or `contactcenteraiplatform_projects_locations_operations_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `contactcenteraiplatform_projects_locations_operations_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn contactcenteraiplatform_projects_locations_operations_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Empty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1alpha1/projects/{projectsId}/locations/{locationsId}/operations/{operationsId}
/// Deletes a long-running operation. This method indicates that the client is no longer interested in the operation result. It does not cancel the operation. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `contactcenteraiplatform_projects_locations_operations_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `contactcenteraiplatform_projects_locations_operations_delete_task()`.
/// For the simplest API, use `contactcenteraiplatform_projects_locations_operations_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `contactcenteraiplatform_projects_locations_operations_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn contactcenteraiplatform_projects_locations_operations_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = contactcenteraiplatform_projects_locations_operations_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`contactcenteraiplatform_projects_locations_operations_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ContactcenteraiplatformProjectsLocationsOperationsDeleteArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1alpha1/projects/{projectsId}/locations/{locationsId}/operations/{operationsId}
/// Deletes a long-running operation. This method indicates that the client is no longer interested in the operation result. It does not cancel the operation. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `contactcenteraiplatform_projects_locations_operations_delete_builder()` + `contactcenteraiplatform_projects_locations_operations_delete_execute()`.
/// For task-level control, use `contactcenteraiplatform_projects_locations_operations_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn contactcenteraiplatform_projects_locations_operations_delete(
    client: &SimpleHttpClient,
    args: &ContactcenteraiplatformProjectsLocationsOperationsDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        contactcenteraiplatform_projects_locations_operations_delete_builder(client, &args.name)?;
    contactcenteraiplatform_projects_locations_operations_delete_execute(builder)
}

/// GET v1alpha1/projects/{projectsId}/locations/{locationsId}/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `contactcenteraiplatform_projects_locations_operations_get_execute()` to send, or `contactcenteraiplatform_projects_locations_operations_get` for simplest API.

pub fn contactcenteraiplatform_projects_locations_operations_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://contactcenteraiplatform.googleapis.com/v1alpha1/projects/{}/locations/{}/operations/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1alpha1/projects/{projectsId}/locations/{locationsId}/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `contactcenteraiplatform_projects_locations_operations_get_execute()` or `contactcenteraiplatform_projects_locations_operations_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `contactcenteraiplatform_projects_locations_operations_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn contactcenteraiplatform_projects_locations_operations_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1alpha1/projects/{projectsId}/locations/{locationsId}/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `contactcenteraiplatform_projects_locations_operations_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `contactcenteraiplatform_projects_locations_operations_get_task()`.
/// For the simplest API, use `contactcenteraiplatform_projects_locations_operations_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `contactcenteraiplatform_projects_locations_operations_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn contactcenteraiplatform_projects_locations_operations_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = contactcenteraiplatform_projects_locations_operations_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`contactcenteraiplatform_projects_locations_operations_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ContactcenteraiplatformProjectsLocationsOperationsGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1alpha1/projects/{projectsId}/locations/{locationsId}/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `contactcenteraiplatform_projects_locations_operations_get_builder()` + `contactcenteraiplatform_projects_locations_operations_get_execute()`.
/// For task-level control, use `contactcenteraiplatform_projects_locations_operations_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn contactcenteraiplatform_projects_locations_operations_get(
    client: &SimpleHttpClient,
    args: &ContactcenteraiplatformProjectsLocationsOperationsGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        contactcenteraiplatform_projects_locations_operations_get_builder(client, &args.name)?;
    contactcenteraiplatform_projects_locations_operations_get_execute(builder)
}

/// GET v1alpha1/projects/{projectsId}/locations/{locationsId}/operations
/// Lists operations that match the specified filter in the request. If the server doesn't support this method, it returns UNIMPLEMENTED.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `contactcenteraiplatform_projects_locations_operations_list_execute()` to send, or `contactcenteraiplatform_projects_locations_operations_list` for simplest API.

pub fn contactcenteraiplatform_projects_locations_operations_list_builder(
    client: &SimpleHttpClient,
    name: &str,
    filter: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
    returnPartialSuccess: Option<bool>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://contactcenteraiplatform.googleapis.com/v1alpha1/projects/{}/locations/{}/operations",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = returnPartialSuccess {
        query_parts.push(format!("returnPartialSuccess={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1alpha1/projects/{projectsId}/locations/{locationsId}/operations
/// Lists operations that match the specified filter in the request. If the server doesn't support this method, it returns UNIMPLEMENTED.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `contactcenteraiplatform_projects_locations_operations_list_execute()` or `contactcenteraiplatform_projects_locations_operations_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `contactcenteraiplatform_projects_locations_operations_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn contactcenteraiplatform_projects_locations_operations_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<ListOperationsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListOperationsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1alpha1/projects/{projectsId}/locations/{locationsId}/operations
/// Lists operations that match the specified filter in the request. If the server doesn't support this method, it returns UNIMPLEMENTED.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `contactcenteraiplatform_projects_locations_operations_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `contactcenteraiplatform_projects_locations_operations_list_task()`.
/// For the simplest API, use `contactcenteraiplatform_projects_locations_operations_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `contactcenteraiplatform_projects_locations_operations_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn contactcenteraiplatform_projects_locations_operations_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListOperationsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = contactcenteraiplatform_projects_locations_operations_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`contactcenteraiplatform_projects_locations_operations_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ContactcenteraiplatformProjectsLocationsOperationsListArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: filter
    pub filter: Option<String>,
    /// Query parameter: pageSize
    pub pageSize: Option<i32>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
    /// Query parameter: returnPartialSuccess
    pub returnPartialSuccess: Option<bool>,
}

/// GET v1alpha1/projects/{projectsId}/locations/{locationsId}/operations
/// Lists operations that match the specified filter in the request. If the server doesn't support this method, it returns UNIMPLEMENTED.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `contactcenteraiplatform_projects_locations_operations_list_builder()` + `contactcenteraiplatform_projects_locations_operations_list_execute()`.
/// For task-level control, use `contactcenteraiplatform_projects_locations_operations_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn contactcenteraiplatform_projects_locations_operations_list(
    client: &SimpleHttpClient,
    args: &ContactcenteraiplatformProjectsLocationsOperationsListArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListOperationsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = contactcenteraiplatform_projects_locations_operations_list_builder(
        client,
        &args.name,
        args.filter.as_deref(),
        args.pageSize,
        args.pageToken.as_deref(),
        args.returnPartialSuccess,
    )?;
    contactcenteraiplatform_projects_locations_operations_list_execute(builder)
}
