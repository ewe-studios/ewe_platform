//! Auto-generated API clients for Google Cloud Platform - kmsinventory.
//!
//! Generated by `cargo run --bin ewe_platform gen_provider_clients`.
//! DO NOT EDIT MANUALLY.
//!
//! Feature flag: `gcp`


#![cfg(feature = "gcp")]

use crate::providers::gcp::clients::types::*;
use crate::providers::gcp::resources::*;
use foundation_core::valtron::{execute, StreamIterator, StreamIteratorExt};
use foundation_core::wire::simple_http::client::{
    body_reader, ClientRequestBuilder, RequestIntro, SimpleHttpClient, SystemDnsResolver,
};

/// GET v1/{+scope}/protectedResources:search
/// Returns metadata about the resources protected by the given Cloud KMS CryptoKey in the given Cloud `organization/project`.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `kmsinventory_organizations_protected_resources_search_execute()` to send, or `kmsinventory_organizations_protected_resources_search` for simplest API.

pub fn kmsinventory_organizations_protected_resources_search_builder(
    client: &SimpleHttpClient,
    scope: &str,
    cryptoKey: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
    resourceTypes: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comv1/{+scope}/protectedResources:search",
        scope,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "cryptoKey",
            cryptoKey
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageSize",
            pageSize
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageToken",
            pageToken
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "resourceTypes",
            resourceTypes
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET v1/{+scope}/protectedResources:search
/// Returns metadata about the resources protected by the given Cloud KMS CryptoKey in the given Cloud `organization/project`.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `kmsinventory_organizations_protected_resources_search_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from kmsinventory_organizations_protected_resources_search_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn kmsinventory_organizations_protected_resources_search_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleCloudKmsInventoryV1SearchProtectedResourcesResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudKmsInventoryV1SearchProtectedResourcesResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+scope}/protectedResources:search
/// Returns metadata about the resources protected by the given Cloud KMS CryptoKey in the given Cloud `organization/project`.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `kmsinventory_organizations_protected_resources_search_builder()` + `kmsinventory_organizations_protected_resources_search_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn kmsinventory_organizations_protected_resources_search(
    client: &SimpleHttpClient,
    scope: &str,
    cryptoKey: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
    resourceTypes: Option<&str>,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleCloudKmsInventoryV1SearchProtectedResourcesResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = kmsinventory_organizations_protected_resources_search_builder(
        client,
        scope,
        cryptoKey,
        pageSize,
        pageToken,
        resourceTypes,
    )?;
    kmsinventory_organizations_protected_resources_search_execute(builder)
}

/// GET v1/{+parent}/cryptoKeys
/// Returns cryptographic keys managed by Cloud KMS in a given Cloud project. Note that this data is sourced from snapshots, meaning it may not completely reflect the actual state of key metadata at call time.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `kmsinventory_projects_crypto_keys_list_execute()` to send, or `kmsinventory_projects_crypto_keys_list` for simplest API.

pub fn kmsinventory_projects_crypto_keys_list_builder(
    client: &SimpleHttpClient,
    parent: &str,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/{+parent}/cryptoKeys", parent,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "pageSize",
            pageSize
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageToken",
            pageToken
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET v1/{+parent}/cryptoKeys
/// Returns cryptographic keys managed by Cloud KMS in a given Cloud project. Note that this data is sourced from snapshots, meaning it may not completely reflect the actual state of key metadata at call time.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `kmsinventory_projects_crypto_keys_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from kmsinventory_projects_crypto_keys_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn kmsinventory_projects_crypto_keys_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudKmsInventoryV1ListCryptoKeysResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudKmsInventoryV1ListCryptoKeysResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+parent}/cryptoKeys
/// Returns cryptographic keys managed by Cloud KMS in a given Cloud project. Note that this data is sourced from snapshots, meaning it may not completely reflect the actual state of key metadata at call time.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `kmsinventory_projects_crypto_keys_list_builder()` + `kmsinventory_projects_crypto_keys_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn kmsinventory_projects_crypto_keys_list(
    client: &SimpleHttpClient,
    parent: &str,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudKmsInventoryV1ListCryptoKeysResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        kmsinventory_projects_crypto_keys_list_builder(client, parent, pageSize, pageToken)?;
    kmsinventory_projects_crypto_keys_list_execute(builder)
}

/// GET v1/{+name}/protectedResourcesSummary
/// Returns aggregate information about the resources protected by the given Cloud KMS CryptoKey. By default, summary of resources within the same Cloud organization as the key will be returned, which requires the KMS organization service account to be configured(refer <https://docs.cloud.google.`com/kms/docs/view-key-usage`#required-roles>). If the KMS organization service account is not configured or key's project is not part of an organization, set fallback_scope to FALLBACK_SCOPE_PROJECT to retrieve a summary of protected resources within the key's project.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `kmsinventory_projects_locations_key_rings_crypto_keys_get_protected_resources_summary_execute()` to send, or `kmsinventory_projects_locations_key_rings_crypto_keys_get_protected_resources_summary` for simplest API.

pub fn kmsinventory_projects_locations_key_rings_crypto_keys_get_protected_resources_summary_builder(
    client: &SimpleHttpClient,
    name: &str,
    fallbackScope: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comv1/{+name}/protectedResourcesSummary",
        name,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "fallbackScope",
            fallbackScope
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET v1/{+name}/protectedResourcesSummary
/// Returns aggregate information about the resources protected by the given Cloud KMS CryptoKey. By default, summary of resources within the same Cloud organization as the key will be returned, which requires the KMS organization service account to be configured(refer <https://docs.cloud.google.`com/kms/docs/view-key-usage`#required-roles>). If the KMS organization service account is not configured or key's project is not part of an organization, set fallback_scope to FALLBACK_SCOPE_PROJECT to retrieve a summary of protected resources within the key's project.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `kmsinventory_projects_locations_key_rings_crypto_keys_get_protected_resources_summary_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from kmsinventory_projects_locations_key_rings_crypto_keys_get_protected_resources_summary_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn kmsinventory_projects_locations_key_rings_crypto_keys_get_protected_resources_summary_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudKmsInventoryV1ProtectedResourcesSummary>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudKmsInventoryV1ProtectedResourcesSummary =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+name}/protectedResourcesSummary
/// Returns aggregate information about the resources protected by the given Cloud KMS CryptoKey. By default, summary of resources within the same Cloud organization as the key will be returned, which requires the KMS organization service account to be configured(refer <https://docs.cloud.google.`com/kms/docs/view-key-usage`#required-roles>). If the KMS organization service account is not configured or key's project is not part of an organization, set fallback_scope to FALLBACK_SCOPE_PROJECT to retrieve a summary of protected resources within the key's project.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `kmsinventory_projects_locations_key_rings_crypto_keys_get_protected_resources_summary_builder()` + `kmsinventory_projects_locations_key_rings_crypto_keys_get_protected_resources_summary_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn kmsinventory_projects_locations_key_rings_crypto_keys_get_protected_resources_summary(
    client: &SimpleHttpClient,
    name: &str,
    fallbackScope: Option<&str>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudKmsInventoryV1ProtectedResourcesSummary>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = kmsinventory_projects_locations_key_rings_crypto_keys_get_protected_resources_summary_builder(client, name, fallbackScope)?;
    kmsinventory_projects_locations_key_rings_crypto_keys_get_protected_resources_summary_execute(
        builder,
    )
}

/// GET v1/{+scope}/protectedResources:search
/// Returns metadata about the resources protected by the given Cloud KMS CryptoKey in the given Cloud `organization/project`.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `kmsinventory_projects_protected_resources_search_execute()` to send, or `kmsinventory_projects_protected_resources_search` for simplest API.

pub fn kmsinventory_projects_protected_resources_search_builder(
    client: &SimpleHttpClient,
    scope: &str,
    cryptoKey: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
    resourceTypes: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comv1/{+scope}/protectedResources:search",
        scope,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "cryptoKey",
            cryptoKey
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageSize",
            pageSize
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageToken",
            pageToken
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "resourceTypes",
            resourceTypes
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET v1/{+scope}/protectedResources:search
/// Returns metadata about the resources protected by the given Cloud KMS CryptoKey in the given Cloud `organization/project`.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `kmsinventory_projects_protected_resources_search_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from kmsinventory_projects_protected_resources_search_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn kmsinventory_projects_protected_resources_search_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleCloudKmsInventoryV1SearchProtectedResourcesResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudKmsInventoryV1SearchProtectedResourcesResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+scope}/protectedResources:search
/// Returns metadata about the resources protected by the given Cloud KMS CryptoKey in the given Cloud `organization/project`.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `kmsinventory_projects_protected_resources_search_builder()` + `kmsinventory_projects_protected_resources_search_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn kmsinventory_projects_protected_resources_search(
    client: &SimpleHttpClient,
    scope: &str,
    cryptoKey: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
    resourceTypes: Option<&str>,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleCloudKmsInventoryV1SearchProtectedResourcesResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = kmsinventory_projects_protected_resources_search_builder(
        client,
        scope,
        cryptoKey,
        pageSize,
        pageToken,
        resourceTypes,
    )?;
    kmsinventory_projects_protected_resources_search_execute(builder)
}
