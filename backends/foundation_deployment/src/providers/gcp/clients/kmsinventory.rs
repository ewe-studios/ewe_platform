//! Auto-generated API clients for gcp/kmsinventory.
//!
//! Generated by `cargo run --bin ewe_platform gen_provider_clients`.
//! DO NOT EDIT MANUALLY.
//!
//! Feature flag: `gcp`

#![cfg(feature = "gcp")]


use crate::providers::gcp::clients::types::*;
use crate::providers::gcp::resources::*;
use foundation_core::valtron::{
    execute, BoxedSendExecutionAction, StreamIterator, StreamIteratorExt, TaskIterator,
    TaskIteratorExt,
};
use foundation_core::wire::simple_http::client::{
    body_reader, ClientRequestBuilder, RequestIntro, SimpleHttpClient, SystemDnsResolver,
};
use foundation_macros::JsonHash;
use serde::Serialize;

/// GET v1/organizations/{organizationsId}/protectedResources:search
/// Returns metadata about the resources protected by the given Cloud KMS CryptoKey in the given Cloud `organization/project`.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `kmsinventory_organizations_protected_resources_search_execute()` to send, or `kmsinventory_organizations_protected_resources_search` for simplest API.

pub fn kmsinventory_organizations_protected_resources_search_builder(
    client: &SimpleHttpClient,
    scope: &String,
    cryptoKey: &Option<String>,
    pageSize: &Option<i32>,
    pageToken: &Option<String>,
    resourceTypes: &Option<String>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://kmsinventory.googleapis.com/v1/organizations/{}/protectedResources:search",
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = cryptoKey.as_ref() {
        query_parts.push(format!("cryptoKey={}", val));
    }
    if let Some(val) = pageSize.as_ref() {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = resourceTypes.as_ref() {
        query_parts.push(format!("resourceTypes={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/organizations/{organizationsId}/protectedResources:search
/// Returns metadata about the resources protected by the given Cloud KMS CryptoKey in the given Cloud `organization/project`.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `kmsinventory_organizations_protected_resources_search_execute()` or `kmsinventory_organizations_protected_resources_search`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `kmsinventory_organizations_protected_resources_search_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn kmsinventory_organizations_protected_resources_search_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<GoogleCloudKmsInventoryV1SearchProtectedResourcesResponse>,
                ApiError,
            >,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudKmsInventoryV1SearchProtectedResourcesResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/organizations/{organizationsId}/protectedResources:search
/// Returns metadata about the resources protected by the given Cloud KMS CryptoKey in the given Cloud `organization/project`.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `kmsinventory_organizations_protected_resources_search_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `kmsinventory_organizations_protected_resources_search_task()`.
/// For the simplest API, use `kmsinventory_organizations_protected_resources_search()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `kmsinventory_organizations_protected_resources_search_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn kmsinventory_organizations_protected_resources_search_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleCloudKmsInventoryV1SearchProtectedResourcesResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = kmsinventory_organizations_protected_resources_search_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`kmsinventory_organizations_protected_resources_search`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct KmsinventoryOrganizationsProtectedResourcesSearchArgs {
    /// Path parameter: scope
    pub scope: String,
    /// Query parameter: cryptoKey
    pub cryptoKey: Option<String>,
    /// Query parameter: pageSize
    pub pageSize: Option<i32>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
    /// Query parameter: resourceTypes
    pub resourceTypes: Option<String>,
}

/// GET v1/organizations/{organizationsId}/protectedResources:search
/// Returns metadata about the resources protected by the given Cloud KMS CryptoKey in the given Cloud `organization/project`.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `kmsinventory_organizations_protected_resources_search_builder()` + `kmsinventory_organizations_protected_resources_search_execute()`.
/// For task-level control, use `kmsinventory_organizations_protected_resources_search_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn kmsinventory_organizations_protected_resources_search(
    client: &SimpleHttpClient,
    args: &KmsinventoryOrganizationsProtectedResourcesSearchArgs,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleCloudKmsInventoryV1SearchProtectedResourcesResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = kmsinventory_organizations_protected_resources_search_builder(
        client,
        &args.scope,
        &args.cryptoKey,
        &args.pageSize,
        &args.pageToken,
        &args.resourceTypes,
    )?;
    kmsinventory_organizations_protected_resources_search_execute(builder)
}

/// GET v1/projects/{projectsId}/cryptoKeys
/// Returns cryptographic keys managed by Cloud KMS in a given Cloud project. Note that this data is sourced from snapshots, meaning it may not completely reflect the actual state of key metadata at call time.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `kmsinventory_projects_crypto_keys_list_execute()` to send, or `kmsinventory_projects_crypto_keys_list` for simplest API.

pub fn kmsinventory_projects_crypto_keys_list_builder(
    client: &SimpleHttpClient,
    parent: &String,
    pageSize: &Option<i32>,
    pageToken: &Option<String>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!("https://kmsinventory.googleapis.com/v1/projects/{}/cryptoKeys",);

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = pageSize.as_ref() {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/cryptoKeys
/// Returns cryptographic keys managed by Cloud KMS in a given Cloud project. Note that this data is sourced from snapshots, meaning it may not completely reflect the actual state of key metadata at call time.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `kmsinventory_projects_crypto_keys_list_execute()` or `kmsinventory_projects_crypto_keys_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `kmsinventory_projects_crypto_keys_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn kmsinventory_projects_crypto_keys_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleCloudKmsInventoryV1ListCryptoKeysResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudKmsInventoryV1ListCryptoKeysResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/cryptoKeys
/// Returns cryptographic keys managed by Cloud KMS in a given Cloud project. Note that this data is sourced from snapshots, meaning it may not completely reflect the actual state of key metadata at call time.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `kmsinventory_projects_crypto_keys_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `kmsinventory_projects_crypto_keys_list_task()`.
/// For the simplest API, use `kmsinventory_projects_crypto_keys_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `kmsinventory_projects_crypto_keys_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn kmsinventory_projects_crypto_keys_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudKmsInventoryV1ListCryptoKeysResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = kmsinventory_projects_crypto_keys_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`kmsinventory_projects_crypto_keys_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct KmsinventoryProjectsCryptoKeysListArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: pageSize
    pub pageSize: Option<i32>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
}

/// GET v1/projects/{projectsId}/cryptoKeys
/// Returns cryptographic keys managed by Cloud KMS in a given Cloud project. Note that this data is sourced from snapshots, meaning it may not completely reflect the actual state of key metadata at call time.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `kmsinventory_projects_crypto_keys_list_builder()` + `kmsinventory_projects_crypto_keys_list_execute()`.
/// For task-level control, use `kmsinventory_projects_crypto_keys_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn kmsinventory_projects_crypto_keys_list(
    client: &SimpleHttpClient,
    args: &KmsinventoryProjectsCryptoKeysListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudKmsInventoryV1ListCryptoKeysResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = kmsinventory_projects_crypto_keys_list_builder(
        client,
        &args.parent,
        &args.pageSize,
        &args.pageToken,
    )?;
    kmsinventory_projects_crypto_keys_list_execute(builder)
}

/// GET v1/projects/{projectsId}/protectedResources:search
/// Returns metadata about the resources protected by the given Cloud KMS CryptoKey in the given Cloud `organization/project`.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `kmsinventory_projects_protected_resources_search_execute()` to send, or `kmsinventory_projects_protected_resources_search` for simplest API.

pub fn kmsinventory_projects_protected_resources_search_builder(
    client: &SimpleHttpClient,
    scope: &String,
    cryptoKey: &Option<String>,
    pageSize: &Option<i32>,
    pageToken: &Option<String>,
    resourceTypes: &Option<String>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url =
        format!("https://kmsinventory.googleapis.com/v1/projects/{}/protectedResources:search",);

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = cryptoKey.as_ref() {
        query_parts.push(format!("cryptoKey={}", val));
    }
    if let Some(val) = pageSize.as_ref() {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = resourceTypes.as_ref() {
        query_parts.push(format!("resourceTypes={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/protectedResources:search
/// Returns metadata about the resources protected by the given Cloud KMS CryptoKey in the given Cloud `organization/project`.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `kmsinventory_projects_protected_resources_search_execute()` or `kmsinventory_projects_protected_resources_search`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `kmsinventory_projects_protected_resources_search_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn kmsinventory_projects_protected_resources_search_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<GoogleCloudKmsInventoryV1SearchProtectedResourcesResponse>,
                ApiError,
            >,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudKmsInventoryV1SearchProtectedResourcesResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/protectedResources:search
/// Returns metadata about the resources protected by the given Cloud KMS CryptoKey in the given Cloud `organization/project`.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `kmsinventory_projects_protected_resources_search_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `kmsinventory_projects_protected_resources_search_task()`.
/// For the simplest API, use `kmsinventory_projects_protected_resources_search()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `kmsinventory_projects_protected_resources_search_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn kmsinventory_projects_protected_resources_search_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleCloudKmsInventoryV1SearchProtectedResourcesResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = kmsinventory_projects_protected_resources_search_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`kmsinventory_projects_protected_resources_search`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct KmsinventoryProjectsProtectedResourcesSearchArgs {
    /// Path parameter: scope
    pub scope: String,
    /// Query parameter: cryptoKey
    pub cryptoKey: Option<String>,
    /// Query parameter: pageSize
    pub pageSize: Option<i32>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
    /// Query parameter: resourceTypes
    pub resourceTypes: Option<String>,
}

/// GET v1/projects/{projectsId}/protectedResources:search
/// Returns metadata about the resources protected by the given Cloud KMS CryptoKey in the given Cloud `organization/project`.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `kmsinventory_projects_protected_resources_search_builder()` + `kmsinventory_projects_protected_resources_search_execute()`.
/// For task-level control, use `kmsinventory_projects_protected_resources_search_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn kmsinventory_projects_protected_resources_search(
    client: &SimpleHttpClient,
    args: &KmsinventoryProjectsProtectedResourcesSearchArgs,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleCloudKmsInventoryV1SearchProtectedResourcesResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = kmsinventory_projects_protected_resources_search_builder(
        client,
        &args.scope,
        &args.cryptoKey,
        &args.pageSize,
        &args.pageToken,
        &args.resourceTypes,
    )?;
    kmsinventory_projects_protected_resources_search_execute(builder)
}
