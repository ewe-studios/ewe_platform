//! Auto-generated API clients for gcp/bigquerydatatransfer.
//!
//! Generated by `cargo run --bin ewe_platform gen_provider_clients`.
//! DO NOT EDIT MANUALLY.

use crate::providers::gcp::clients::types::*;
use crate::providers::gcp::resources::*;
use foundation_core::valtron::{execute, StreamIterator, StreamIteratorExt};
use foundation_core::wire::simple_http::client::{
    body_reader, ClientRequestBuilder, RequestIntro, SimpleHttpClient, SystemDnsResolver,
};

/// GET v1/{+name}:enrollDataSources
/// Enroll data sources in a user project. This allows users to create transfer configurations for these data sources. They will also appear in the ListDataSources RPC and as such, will appear in the [BigQuery UI](<https://console.cloud.google.`com/bigquery`>), and the documents can be found in the public guide for [BigQuery Web UI](<https://cloud.google.`com/bigquery/bigquery-web-ui`>) and [Data Transfer Service](<https://cloud.google.`com/bigquery/docs/working-with-transfers`>).
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `bigquerydatatransfer_projects_enroll_data_sources_execute()` to send, or `bigquerydatatransfer_projects_enroll_data_sources` for simplest API.

pub fn bigquerydatatransfer_projects_enroll_data_sources_builder(
    client: &SimpleHttpClient,
    name: &str,
    body: &EnrollDataSourcesRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/{+name}:enrollDataSources", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v1/{+name}:enrollDataSources
/// Enroll data sources in a user project. This allows users to create transfer configurations for these data sources. They will also appear in the ListDataSources RPC and as such, will appear in the [BigQuery UI](<https://console.cloud.google.`com/bigquery`>), and the documents can be found in the public guide for [BigQuery Web UI](<https://cloud.google.`com/bigquery/bigquery-web-ui`>) and [Data Transfer Service](<https://cloud.google.`com/bigquery/docs/working-with-transfers`>).
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `bigquerydatatransfer_projects_enroll_data_sources_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from bigquerydatatransfer_projects_enroll_data_sources_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn bigquerydatatransfer_projects_enroll_data_sources_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Empty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+name}:enrollDataSources
/// Enroll data sources in a user project. This allows users to create transfer configurations for these data sources. They will also appear in the ListDataSources RPC and as such, will appear in the [BigQuery UI](<https://console.cloud.google.`com/bigquery`>), and the documents can be found in the public guide for [BigQuery Web UI](<https://cloud.google.`com/bigquery/bigquery-web-ui`>) and [Data Transfer Service](<https://cloud.google.`com/bigquery/docs/working-with-transfers`>).
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `bigquerydatatransfer_projects_enroll_data_sources_builder()` + `bigquerydatatransfer_projects_enroll_data_sources_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn bigquerydatatransfer_projects_enroll_data_sources(
    client: &SimpleHttpClient,
    name: &str,
    body: &EnrollDataSourcesRequest,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = bigquerydatatransfer_projects_enroll_data_sources_builder(client, name, body)?;
    bigquerydatatransfer_projects_enroll_data_sources_execute(builder)
}

/// GET v1/{+name}:checkValidCreds
/// Returns `true` if valid credentials exist for the given data source and requesting user.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `bigquerydatatransfer_projects_data_sources_check_valid_creds_execute()` to send, or `bigquerydatatransfer_projects_data_sources_check_valid_creds` for simplest API.

pub fn bigquerydatatransfer_projects_data_sources_check_valid_creds_builder(
    client: &SimpleHttpClient,
    name: &str,
    body: &CheckValidCredsRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/{+name}:checkValidCreds", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v1/{+name}:checkValidCreds
/// Returns `true` if valid credentials exist for the given data source and requesting user.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `bigquerydatatransfer_projects_data_sources_check_valid_creds_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from bigquerydatatransfer_projects_data_sources_check_valid_creds_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn bigquerydatatransfer_projects_data_sources_check_valid_creds_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<CheckValidCredsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: CheckValidCredsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+name}:checkValidCreds
/// Returns `true` if valid credentials exist for the given data source and requesting user.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `bigquerydatatransfer_projects_data_sources_check_valid_creds_builder()` + `bigquerydatatransfer_projects_data_sources_check_valid_creds_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn bigquerydatatransfer_projects_data_sources_check_valid_creds(
    client: &SimpleHttpClient,
    name: &str,
    body: &CheckValidCredsRequest,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<CheckValidCredsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        bigquerydatatransfer_projects_data_sources_check_valid_creds_builder(client, name, body)?;
    bigquerydatatransfer_projects_data_sources_check_valid_creds_execute(builder)
}

/// GET v1/{+name}
/// Retrieves a supported data source and returns its settings.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `bigquerydatatransfer_projects_data_sources_get_execute()` to send, or `bigquerydatatransfer_projects_data_sources_get` for simplest API.

pub fn bigquerydatatransfer_projects_data_sources_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/{+name}", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+name}
/// Retrieves a supported data source and returns its settings.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `bigquerydatatransfer_projects_data_sources_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from bigquerydatatransfer_projects_data_sources_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn bigquerydatatransfer_projects_data_sources_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<DataSource>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: DataSource = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+name}
/// Retrieves a supported data source and returns its settings.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `bigquerydatatransfer_projects_data_sources_get_builder()` + `bigquerydatatransfer_projects_data_sources_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn bigquerydatatransfer_projects_data_sources_get(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<DataSource>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = bigquerydatatransfer_projects_data_sources_get_builder(client, name)?;
    bigquerydatatransfer_projects_data_sources_get_execute(builder)
}

/// GET v1/{+parent}/dataSources
/// Lists supported data sources and returns their settings.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `bigquerydatatransfer_projects_data_sources_list_execute()` to send, or `bigquerydatatransfer_projects_data_sources_list` for simplest API.

pub fn bigquerydatatransfer_projects_data_sources_list_builder(
    client: &SimpleHttpClient,
    parent: &str,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/{+parent}/dataSources", parent,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "pageSize",
            pageSize
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageToken",
            pageToken
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET v1/{+parent}/dataSources
/// Lists supported data sources and returns their settings.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `bigquerydatatransfer_projects_data_sources_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from bigquerydatatransfer_projects_data_sources_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn bigquerydatatransfer_projects_data_sources_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListDataSourcesResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListDataSourcesResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+parent}/dataSources
/// Lists supported data sources and returns their settings.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `bigquerydatatransfer_projects_data_sources_list_builder()` + `bigquerydatatransfer_projects_data_sources_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn bigquerydatatransfer_projects_data_sources_list(
    client: &SimpleHttpClient,
    parent: &str,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListDataSourcesResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = bigquerydatatransfer_projects_data_sources_list_builder(
        client, parent, pageSize, pageToken,
    )?;
    bigquerydatatransfer_projects_data_sources_list_execute(builder)
}

/// GET v1/{+name}:enrollDataSources
/// Enroll data sources in a user project. This allows users to create transfer configurations for these data sources. They will also appear in the ListDataSources RPC and as such, will appear in the [BigQuery UI](<https://console.cloud.google.`com/bigquery`>), and the documents can be found in the public guide for [BigQuery Web UI](<https://cloud.google.`com/bigquery/bigquery-web-ui`>) and [Data Transfer Service](<https://cloud.google.`com/bigquery/docs/working-with-transfers`>).
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `bigquerydatatransfer_projects_locations_enroll_data_sources_execute()` to send, or `bigquerydatatransfer_projects_locations_enroll_data_sources` for simplest API.

pub fn bigquerydatatransfer_projects_locations_enroll_data_sources_builder(
    client: &SimpleHttpClient,
    name: &str,
    body: &EnrollDataSourcesRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/{+name}:enrollDataSources", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v1/{+name}:enrollDataSources
/// Enroll data sources in a user project. This allows users to create transfer configurations for these data sources. They will also appear in the ListDataSources RPC and as such, will appear in the [BigQuery UI](<https://console.cloud.google.`com/bigquery`>), and the documents can be found in the public guide for [BigQuery Web UI](<https://cloud.google.`com/bigquery/bigquery-web-ui`>) and [Data Transfer Service](<https://cloud.google.`com/bigquery/docs/working-with-transfers`>).
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `bigquerydatatransfer_projects_locations_enroll_data_sources_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from bigquerydatatransfer_projects_locations_enroll_data_sources_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn bigquerydatatransfer_projects_locations_enroll_data_sources_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Empty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+name}:enrollDataSources
/// Enroll data sources in a user project. This allows users to create transfer configurations for these data sources. They will also appear in the ListDataSources RPC and as such, will appear in the [BigQuery UI](<https://console.cloud.google.`com/bigquery`>), and the documents can be found in the public guide for [BigQuery Web UI](<https://cloud.google.`com/bigquery/bigquery-web-ui`>) and [Data Transfer Service](<https://cloud.google.`com/bigquery/docs/working-with-transfers`>).
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `bigquerydatatransfer_projects_locations_enroll_data_sources_builder()` + `bigquerydatatransfer_projects_locations_enroll_data_sources_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn bigquerydatatransfer_projects_locations_enroll_data_sources(
    client: &SimpleHttpClient,
    name: &str,
    body: &EnrollDataSourcesRequest,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        bigquerydatatransfer_projects_locations_enroll_data_sources_builder(client, name, body)?;
    bigquerydatatransfer_projects_locations_enroll_data_sources_execute(builder)
}

/// GET v1/{+name}
/// Gets information about a location.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `bigquerydatatransfer_projects_locations_get_execute()` to send, or `bigquerydatatransfer_projects_locations_get` for simplest API.

pub fn bigquerydatatransfer_projects_locations_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/{+name}", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+name}
/// Gets information about a location.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `bigquerydatatransfer_projects_locations_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from bigquerydatatransfer_projects_locations_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn bigquerydatatransfer_projects_locations_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Location>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Location = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+name}
/// Gets information about a location.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `bigquerydatatransfer_projects_locations_get_builder()` + `bigquerydatatransfer_projects_locations_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn bigquerydatatransfer_projects_locations_get(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Location>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = bigquerydatatransfer_projects_locations_get_builder(client, name)?;
    bigquerydatatransfer_projects_locations_get_execute(builder)
}

/// GET v1/{+name}/locations
/// Lists information about the supported locations for this service. This method lists locations based on the resource scope provided in the [ListLocationsRequest.name] field: * **Global locations**: If name is empty, the method lists the public locations available to all projects. * **Project-specific locations**: If name follows the format `projects/{project}`, the method lists locations visible to that specific project. This includes public, private, or other project-specific locations enabled for the project. For `gRPC` and client library implementations, the resource name is passed as the name field. For direct service calls, the resource name is incorporated into the request path based on the specific service implementation and version.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `bigquerydatatransfer_projects_locations_list_execute()` to send, or `bigquerydatatransfer_projects_locations_list` for simplest API.

pub fn bigquerydatatransfer_projects_locations_list_builder(
    client: &SimpleHttpClient,
    name: &str,
    extraLocationTypes: Option<&str>,
    filter: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/{+name}/locations", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "extraLocationTypes",
            extraLocationTypes
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "filter",
            filter
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageSize",
            pageSize
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageToken",
            pageToken
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET v1/{+name}/locations
/// Lists information about the supported locations for this service. This method lists locations based on the resource scope provided in the [ListLocationsRequest.name] field: * **Global locations**: If name is empty, the method lists the public locations available to all projects. * **Project-specific locations**: If name follows the format `projects/{project}`, the method lists locations visible to that specific project. This includes public, private, or other project-specific locations enabled for the project. For `gRPC` and client library implementations, the resource name is passed as the name field. For direct service calls, the resource name is incorporated into the request path based on the specific service implementation and version.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `bigquerydatatransfer_projects_locations_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from bigquerydatatransfer_projects_locations_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn bigquerydatatransfer_projects_locations_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListLocationsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListLocationsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+name}/locations
/// Lists information about the supported locations for this service. This method lists locations based on the resource scope provided in the [ListLocationsRequest.name] field: * **Global locations**: If name is empty, the method lists the public locations available to all projects. * **Project-specific locations**: If name follows the format `projects/{project}`, the method lists locations visible to that specific project. This includes public, private, or other project-specific locations enabled for the project. For `gRPC` and client library implementations, the resource name is passed as the name field. For direct service calls, the resource name is incorporated into the request path based on the specific service implementation and version.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `bigquerydatatransfer_projects_locations_list_builder()` + `bigquerydatatransfer_projects_locations_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn bigquerydatatransfer_projects_locations_list(
    client: &SimpleHttpClient,
    name: &str,
    extraLocationTypes: Option<&str>,
    filter: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListLocationsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = bigquerydatatransfer_projects_locations_list_builder(
        client,
        name,
        extraLocationTypes,
        filter,
        pageSize,
        pageToken,
    )?;
    bigquerydatatransfer_projects_locations_list_execute(builder)
}

/// GET v1/{+name}:unenrollDataSources
/// Unenroll data sources in a user project. This allows users to remove transfer configurations for these data sources. They will no longer appear in the ListDataSources RPC and will also no longer appear in the [BigQuery UI](<https://console.cloud.google.`com/bigquery`>). Data transfers configurations of unenrolled data sources will not be scheduled.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `bigquerydatatransfer_projects_locations_unenroll_data_sources_execute()` to send, or `bigquerydatatransfer_projects_locations_unenroll_data_sources` for simplest API.

pub fn bigquerydatatransfer_projects_locations_unenroll_data_sources_builder(
    client: &SimpleHttpClient,
    name: &str,
    body: &UnenrollDataSourcesRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comv1/{+name}:unenrollDataSources",
        name,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v1/{+name}:unenrollDataSources
/// Unenroll data sources in a user project. This allows users to remove transfer configurations for these data sources. They will no longer appear in the ListDataSources RPC and will also no longer appear in the [BigQuery UI](<https://console.cloud.google.`com/bigquery`>). Data transfers configurations of unenrolled data sources will not be scheduled.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `bigquerydatatransfer_projects_locations_unenroll_data_sources_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from bigquerydatatransfer_projects_locations_unenroll_data_sources_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn bigquerydatatransfer_projects_locations_unenroll_data_sources_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Empty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+name}:unenrollDataSources
/// Unenroll data sources in a user project. This allows users to remove transfer configurations for these data sources. They will no longer appear in the ListDataSources RPC and will also no longer appear in the [BigQuery UI](<https://console.cloud.google.`com/bigquery`>). Data transfers configurations of unenrolled data sources will not be scheduled.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `bigquerydatatransfer_projects_locations_unenroll_data_sources_builder()` + `bigquerydatatransfer_projects_locations_unenroll_data_sources_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn bigquerydatatransfer_projects_locations_unenroll_data_sources(
    client: &SimpleHttpClient,
    name: &str,
    body: &UnenrollDataSourcesRequest,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        bigquerydatatransfer_projects_locations_unenroll_data_sources_builder(client, name, body)?;
    bigquerydatatransfer_projects_locations_unenroll_data_sources_execute(builder)
}

/// GET v1/{+name}:checkValidCreds
/// Returns `true` if valid credentials exist for the given data source and requesting user.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `bigquerydatatransfer_projects_locations_data_sources_check_valid_creds_execute()` to send, or `bigquerydatatransfer_projects_locations_data_sources_check_valid_creds` for simplest API.

pub fn bigquerydatatransfer_projects_locations_data_sources_check_valid_creds_builder(
    client: &SimpleHttpClient,
    name: &str,
    body: &CheckValidCredsRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/{+name}:checkValidCreds", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v1/{+name}:checkValidCreds
/// Returns `true` if valid credentials exist for the given data source and requesting user.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `bigquerydatatransfer_projects_locations_data_sources_check_valid_creds_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from bigquerydatatransfer_projects_locations_data_sources_check_valid_creds_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn bigquerydatatransfer_projects_locations_data_sources_check_valid_creds_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<CheckValidCredsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: CheckValidCredsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+name}:checkValidCreds
/// Returns `true` if valid credentials exist for the given data source and requesting user.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `bigquerydatatransfer_projects_locations_data_sources_check_valid_creds_builder()` + `bigquerydatatransfer_projects_locations_data_sources_check_valid_creds_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn bigquerydatatransfer_projects_locations_data_sources_check_valid_creds(
    client: &SimpleHttpClient,
    name: &str,
    body: &CheckValidCredsRequest,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<CheckValidCredsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = bigquerydatatransfer_projects_locations_data_sources_check_valid_creds_builder(
        client, name, body,
    )?;
    bigquerydatatransfer_projects_locations_data_sources_check_valid_creds_execute(builder)
}

/// GET v1/{+name}
/// Retrieves a supported data source and returns its settings.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `bigquerydatatransfer_projects_locations_data_sources_get_execute()` to send, or `bigquerydatatransfer_projects_locations_data_sources_get` for simplest API.

pub fn bigquerydatatransfer_projects_locations_data_sources_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/{+name}", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+name}
/// Retrieves a supported data source and returns its settings.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `bigquerydatatransfer_projects_locations_data_sources_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from bigquerydatatransfer_projects_locations_data_sources_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn bigquerydatatransfer_projects_locations_data_sources_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<DataSource>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: DataSource = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+name}
/// Retrieves a supported data source and returns its settings.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `bigquerydatatransfer_projects_locations_data_sources_get_builder()` + `bigquerydatatransfer_projects_locations_data_sources_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn bigquerydatatransfer_projects_locations_data_sources_get(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<DataSource>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = bigquerydatatransfer_projects_locations_data_sources_get_builder(client, name)?;
    bigquerydatatransfer_projects_locations_data_sources_get_execute(builder)
}

/// GET v1/{+parent}/dataSources
/// Lists supported data sources and returns their settings.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `bigquerydatatransfer_projects_locations_data_sources_list_execute()` to send, or `bigquerydatatransfer_projects_locations_data_sources_list` for simplest API.

pub fn bigquerydatatransfer_projects_locations_data_sources_list_builder(
    client: &SimpleHttpClient,
    parent: &str,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/{+parent}/dataSources", parent,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "pageSize",
            pageSize
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageToken",
            pageToken
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET v1/{+parent}/dataSources
/// Lists supported data sources and returns their settings.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `bigquerydatatransfer_projects_locations_data_sources_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from bigquerydatatransfer_projects_locations_data_sources_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn bigquerydatatransfer_projects_locations_data_sources_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListDataSourcesResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListDataSourcesResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+parent}/dataSources
/// Lists supported data sources and returns their settings.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `bigquerydatatransfer_projects_locations_data_sources_list_builder()` + `bigquerydatatransfer_projects_locations_data_sources_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn bigquerydatatransfer_projects_locations_data_sources_list(
    client: &SimpleHttpClient,
    parent: &str,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListDataSourcesResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = bigquerydatatransfer_projects_locations_data_sources_list_builder(
        client, parent, pageSize, pageToken,
    )?;
    bigquerydatatransfer_projects_locations_data_sources_list_execute(builder)
}

/// GET v1/{+parent}/transferConfigs
/// Creates a new data transfer configuration.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `bigquerydatatransfer_projects_locations_transfer_configs_create_execute()` to send, or `bigquerydatatransfer_projects_locations_transfer_configs_create` for simplest API.

pub fn bigquerydatatransfer_projects_locations_transfer_configs_create_builder(
    client: &SimpleHttpClient,
    parent: &str,
    authorizationCode: Option<&str>,
    serviceAccountName: Option<&str>,
    versionInfo: Option<&str>,
    body: &TransferConfig,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comv1/{+parent}/transferConfigs",
        parent,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "authorizationCode",
            authorizationCode
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "serviceAccountName",
            serviceAccountName
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "versionInfo",
            versionInfo
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v1/{+parent}/transferConfigs
/// Creates a new data transfer configuration.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `bigquerydatatransfer_projects_locations_transfer_configs_create_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from bigquerydatatransfer_projects_locations_transfer_configs_create_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn bigquerydatatransfer_projects_locations_transfer_configs_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<TransferConfig>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: TransferConfig = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+parent}/transferConfigs
/// Creates a new data transfer configuration.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `bigquerydatatransfer_projects_locations_transfer_configs_create_builder()` + `bigquerydatatransfer_projects_locations_transfer_configs_create_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn bigquerydatatransfer_projects_locations_transfer_configs_create(
    client: &SimpleHttpClient,
    parent: &str,
    authorizationCode: Option<&str>,
    serviceAccountName: Option<&str>,
    versionInfo: Option<&str>,
    body: &TransferConfig,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<TransferConfig>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = bigquerydatatransfer_projects_locations_transfer_configs_create_builder(
        client,
        parent,
        authorizationCode,
        serviceAccountName,
        versionInfo,
        body,
    )?;
    bigquerydatatransfer_projects_locations_transfer_configs_create_execute(builder)
}

/// GET v1/{+name}
/// Deletes a data transfer configuration, including any associated transfer runs and logs.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `bigquerydatatransfer_projects_locations_transfer_configs_delete_execute()` to send, or `bigquerydatatransfer_projects_locations_transfer_configs_delete` for simplest API.

pub fn bigquerydatatransfer_projects_locations_transfer_configs_delete_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/{+name}", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+name}
/// Deletes a data transfer configuration, including any associated transfer runs and logs.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `bigquerydatatransfer_projects_locations_transfer_configs_delete_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from bigquerydatatransfer_projects_locations_transfer_configs_delete_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn bigquerydatatransfer_projects_locations_transfer_configs_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Empty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+name}
/// Deletes a data transfer configuration, including any associated transfer runs and logs.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `bigquerydatatransfer_projects_locations_transfer_configs_delete_builder()` + `bigquerydatatransfer_projects_locations_transfer_configs_delete_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn bigquerydatatransfer_projects_locations_transfer_configs_delete(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        bigquerydatatransfer_projects_locations_transfer_configs_delete_builder(client, name)?;
    bigquerydatatransfer_projects_locations_transfer_configs_delete_execute(builder)
}

/// GET v1/{+name}
/// Returns information about a data transfer config.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `bigquerydatatransfer_projects_locations_transfer_configs_get_execute()` to send, or `bigquerydatatransfer_projects_locations_transfer_configs_get` for simplest API.

pub fn bigquerydatatransfer_projects_locations_transfer_configs_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/{+name}", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+name}
/// Returns information about a data transfer config.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `bigquerydatatransfer_projects_locations_transfer_configs_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from bigquerydatatransfer_projects_locations_transfer_configs_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn bigquerydatatransfer_projects_locations_transfer_configs_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<TransferConfig>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: TransferConfig = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+name}
/// Returns information about a data transfer config.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `bigquerydatatransfer_projects_locations_transfer_configs_get_builder()` + `bigquerydatatransfer_projects_locations_transfer_configs_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn bigquerydatatransfer_projects_locations_transfer_configs_get(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<TransferConfig>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        bigquerydatatransfer_projects_locations_transfer_configs_get_builder(client, name)?;
    bigquerydatatransfer_projects_locations_transfer_configs_get_execute(builder)
}

/// GET v1/{+parent}/transferConfigs
/// Returns information about all transfer configs owned by a project in the specified location.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `bigquerydatatransfer_projects_locations_transfer_configs_list_execute()` to send, or `bigquerydatatransfer_projects_locations_transfer_configs_list` for simplest API.

pub fn bigquerydatatransfer_projects_locations_transfer_configs_list_builder(
    client: &SimpleHttpClient,
    parent: &str,
    dataSourceIds: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comv1/{+parent}/transferConfigs",
        parent,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "dataSourceIds",
            dataSourceIds
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageSize",
            pageSize
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageToken",
            pageToken
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET v1/{+parent}/transferConfigs
/// Returns information about all transfer configs owned by a project in the specified location.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `bigquerydatatransfer_projects_locations_transfer_configs_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from bigquerydatatransfer_projects_locations_transfer_configs_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn bigquerydatatransfer_projects_locations_transfer_configs_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ListTransferConfigsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListTransferConfigsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+parent}/transferConfigs
/// Returns information about all transfer configs owned by a project in the specified location.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `bigquerydatatransfer_projects_locations_transfer_configs_list_builder()` + `bigquerydatatransfer_projects_locations_transfer_configs_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn bigquerydatatransfer_projects_locations_transfer_configs_list(
    client: &SimpleHttpClient,
    parent: &str,
    dataSourceIds: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ListTransferConfigsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = bigquerydatatransfer_projects_locations_transfer_configs_list_builder(
        client,
        parent,
        dataSourceIds,
        pageSize,
        pageToken,
    )?;
    bigquerydatatransfer_projects_locations_transfer_configs_list_execute(builder)
}

/// GET v1/{+name}
/// Updates a data transfer configuration. All fields must be set, even if they are not updated.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `bigquerydatatransfer_projects_locations_transfer_configs_patch_execute()` to send, or `bigquerydatatransfer_projects_locations_transfer_configs_patch` for simplest API.

pub fn bigquerydatatransfer_projects_locations_transfer_configs_patch_builder(
    client: &SimpleHttpClient,
    name: &str,
    authorizationCode: Option<&str>,
    serviceAccountName: Option<&str>,
    updateMask: Option<&str>,
    versionInfo: Option<&str>,
    body: &TransferConfig,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/{+name}", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "authorizationCode",
            authorizationCode
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "serviceAccountName",
            serviceAccountName
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "updateMask",
            updateMask
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "versionInfo",
            versionInfo
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v1/{+name}
/// Updates a data transfer configuration. All fields must be set, even if they are not updated.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `bigquerydatatransfer_projects_locations_transfer_configs_patch_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from bigquerydatatransfer_projects_locations_transfer_configs_patch_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn bigquerydatatransfer_projects_locations_transfer_configs_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<TransferConfig>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: TransferConfig = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+name}
/// Updates a data transfer configuration. All fields must be set, even if they are not updated.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `bigquerydatatransfer_projects_locations_transfer_configs_patch_builder()` + `bigquerydatatransfer_projects_locations_transfer_configs_patch_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn bigquerydatatransfer_projects_locations_transfer_configs_patch(
    client: &SimpleHttpClient,
    name: &str,
    authorizationCode: Option<&str>,
    serviceAccountName: Option<&str>,
    updateMask: Option<&str>,
    versionInfo: Option<&str>,
    body: &TransferConfig,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<TransferConfig>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = bigquerydatatransfer_projects_locations_transfer_configs_patch_builder(
        client,
        name,
        authorizationCode,
        serviceAccountName,
        updateMask,
        versionInfo,
        body,
    )?;
    bigquerydatatransfer_projects_locations_transfer_configs_patch_execute(builder)
}

/// GET v1/{+parent}:scheduleRuns
/// Creates transfer runs for a time range [start_time, end_time]. For each date - or whatever granularity the data source supports - in the range, one transfer run is created. Note that runs are created per UTC time in the time range. DEPRECATED: use StartManualTransferRuns instead.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `bigquerydatatransfer_projects_locations_transfer_configs_schedule_runs_execute()` to send, or `bigquerydatatransfer_projects_locations_transfer_configs_schedule_runs` for simplest API.

pub fn bigquerydatatransfer_projects_locations_transfer_configs_schedule_runs_builder(
    client: &SimpleHttpClient,
    parent: &str,
    body: &ScheduleTransferRunsRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/{+parent}:scheduleRuns", parent,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v1/{+parent}:scheduleRuns
/// Creates transfer runs for a time range [start_time, end_time]. For each date - or whatever granularity the data source supports - in the range, one transfer run is created. Note that runs are created per UTC time in the time range. DEPRECATED: use StartManualTransferRuns instead.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `bigquerydatatransfer_projects_locations_transfer_configs_schedule_runs_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from bigquerydatatransfer_projects_locations_transfer_configs_schedule_runs_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn bigquerydatatransfer_projects_locations_transfer_configs_schedule_runs_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ScheduleTransferRunsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ScheduleTransferRunsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+parent}:scheduleRuns
/// Creates transfer runs for a time range [start_time, end_time]. For each date - or whatever granularity the data source supports - in the range, one transfer run is created. Note that runs are created per UTC time in the time range. DEPRECATED: use StartManualTransferRuns instead.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `bigquerydatatransfer_projects_locations_transfer_configs_schedule_runs_builder()` + `bigquerydatatransfer_projects_locations_transfer_configs_schedule_runs_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn bigquerydatatransfer_projects_locations_transfer_configs_schedule_runs(
    client: &SimpleHttpClient,
    parent: &str,
    body: &ScheduleTransferRunsRequest,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ScheduleTransferRunsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = bigquerydatatransfer_projects_locations_transfer_configs_schedule_runs_builder(
        client, parent, body,
    )?;
    bigquerydatatransfer_projects_locations_transfer_configs_schedule_runs_execute(builder)
}

/// GET v1/{+parent}:startManualRuns
/// Manually initiates transfer runs. You can schedule these runs in two ways: 1. For a specific point in time using the 'requested_run_time' parameter. 2. For a period between 'start_time' (inclusive) and 'end_time' (exclusive). If scheduling a single run, it is set to execute immediately (schedule_time equals the current time). When scheduling multiple runs within a time range, the first run starts now, and subsequent runs are delayed by 15 seconds each.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `bigquerydatatransfer_projects_locations_transfer_configs_start_manual_runs_execute()` to send, or `bigquerydatatransfer_projects_locations_transfer_configs_start_manual_runs` for simplest API.

pub fn bigquerydatatransfer_projects_locations_transfer_configs_start_manual_runs_builder(
    client: &SimpleHttpClient,
    parent: &str,
    body: &StartManualTransferRunsRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comv1/{+parent}:startManualRuns",
        parent,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v1/{+parent}:startManualRuns
/// Manually initiates transfer runs. You can schedule these runs in two ways: 1. For a specific point in time using the 'requested_run_time' parameter. 2. For a period between 'start_time' (inclusive) and 'end_time' (exclusive). If scheduling a single run, it is set to execute immediately (schedule_time equals the current time). When scheduling multiple runs within a time range, the first run starts now, and subsequent runs are delayed by 15 seconds each.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `bigquerydatatransfer_projects_locations_transfer_configs_start_manual_runs_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from bigquerydatatransfer_projects_locations_transfer_configs_start_manual_runs_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn bigquerydatatransfer_projects_locations_transfer_configs_start_manual_runs_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<StartManualTransferRunsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: StartManualTransferRunsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+parent}:startManualRuns
/// Manually initiates transfer runs. You can schedule these runs in two ways: 1. For a specific point in time using the 'requested_run_time' parameter. 2. For a period between 'start_time' (inclusive) and 'end_time' (exclusive). If scheduling a single run, it is set to execute immediately (schedule_time equals the current time). When scheduling multiple runs within a time range, the first run starts now, and subsequent runs are delayed by 15 seconds each.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `bigquerydatatransfer_projects_locations_transfer_configs_start_manual_runs_builder()` + `bigquerydatatransfer_projects_locations_transfer_configs_start_manual_runs_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn bigquerydatatransfer_projects_locations_transfer_configs_start_manual_runs(
    client: &SimpleHttpClient,
    parent: &str,
    body: &StartManualTransferRunsRequest,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<StartManualTransferRunsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        bigquerydatatransfer_projects_locations_transfer_configs_start_manual_runs_builder(
            client, parent, body,
        )?;
    bigquerydatatransfer_projects_locations_transfer_configs_start_manual_runs_execute(builder)
}

/// GET v1/{+name}
/// Deletes the specified transfer run.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `bigquerydatatransfer_projects_locations_transfer_configs_runs_delete_execute()` to send, or `bigquerydatatransfer_projects_locations_transfer_configs_runs_delete` for simplest API.

pub fn bigquerydatatransfer_projects_locations_transfer_configs_runs_delete_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/{+name}", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+name}
/// Deletes the specified transfer run.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `bigquerydatatransfer_projects_locations_transfer_configs_runs_delete_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from bigquerydatatransfer_projects_locations_transfer_configs_runs_delete_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn bigquerydatatransfer_projects_locations_transfer_configs_runs_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Empty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+name}
/// Deletes the specified transfer run.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `bigquerydatatransfer_projects_locations_transfer_configs_runs_delete_builder()` + `bigquerydatatransfer_projects_locations_transfer_configs_runs_delete_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn bigquerydatatransfer_projects_locations_transfer_configs_runs_delete(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        bigquerydatatransfer_projects_locations_transfer_configs_runs_delete_builder(client, name)?;
    bigquerydatatransfer_projects_locations_transfer_configs_runs_delete_execute(builder)
}

/// GET v1/{+name}
/// Returns information about the particular transfer run.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `bigquerydatatransfer_projects_locations_transfer_configs_runs_get_execute()` to send, or `bigquerydatatransfer_projects_locations_transfer_configs_runs_get` for simplest API.

pub fn bigquerydatatransfer_projects_locations_transfer_configs_runs_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/{+name}", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+name}
/// Returns information about the particular transfer run.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `bigquerydatatransfer_projects_locations_transfer_configs_runs_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from bigquerydatatransfer_projects_locations_transfer_configs_runs_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn bigquerydatatransfer_projects_locations_transfer_configs_runs_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<TransferRun>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: TransferRun = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+name}
/// Returns information about the particular transfer run.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `bigquerydatatransfer_projects_locations_transfer_configs_runs_get_builder()` + `bigquerydatatransfer_projects_locations_transfer_configs_runs_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn bigquerydatatransfer_projects_locations_transfer_configs_runs_get(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<TransferRun>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        bigquerydatatransfer_projects_locations_transfer_configs_runs_get_builder(client, name)?;
    bigquerydatatransfer_projects_locations_transfer_configs_runs_get_execute(builder)
}

/// GET v1/{+parent}/runs
/// Returns information about running and completed transfer runs.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `bigquerydatatransfer_projects_locations_transfer_configs_runs_list_execute()` to send, or `bigquerydatatransfer_projects_locations_transfer_configs_runs_list` for simplest API.

pub fn bigquerydatatransfer_projects_locations_transfer_configs_runs_list_builder(
    client: &SimpleHttpClient,
    parent: &str,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
    runAttempt: Option<&str>,
    states: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/{+parent}/runs", parent,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "pageSize",
            pageSize
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageToken",
            pageToken
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "runAttempt",
            runAttempt
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "states",
            states
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET v1/{+parent}/runs
/// Returns information about running and completed transfer runs.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `bigquerydatatransfer_projects_locations_transfer_configs_runs_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from bigquerydatatransfer_projects_locations_transfer_configs_runs_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn bigquerydatatransfer_projects_locations_transfer_configs_runs_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListTransferRunsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListTransferRunsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+parent}/runs
/// Returns information about running and completed transfer runs.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `bigquerydatatransfer_projects_locations_transfer_configs_runs_list_builder()` + `bigquerydatatransfer_projects_locations_transfer_configs_runs_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn bigquerydatatransfer_projects_locations_transfer_configs_runs_list(
    client: &SimpleHttpClient,
    parent: &str,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
    runAttempt: Option<&str>,
    states: Option<&str>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListTransferRunsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = bigquerydatatransfer_projects_locations_transfer_configs_runs_list_builder(
        client, parent, pageSize, pageToken, runAttempt, states,
    )?;
    bigquerydatatransfer_projects_locations_transfer_configs_runs_list_execute(builder)
}

/// GET v1/{+parent}/transferLogs
/// Returns log messages for the transfer run.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `bigquerydatatransfer_projects_locations_transfer_configs_runs_transfer_logs_list_execute()` to send, or `bigquerydatatransfer_projects_locations_transfer_configs_runs_transfer_logs_list` for simplest API.

pub fn bigquerydatatransfer_projects_locations_transfer_configs_runs_transfer_logs_list_builder(
    client: &SimpleHttpClient,
    parent: &str,
    messageTypes: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/{+parent}/transferLogs", parent,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "messageTypes",
            messageTypes
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageSize",
            pageSize
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageToken",
            pageToken
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET v1/{+parent}/transferLogs
/// Returns log messages for the transfer run.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `bigquerydatatransfer_projects_locations_transfer_configs_runs_transfer_logs_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from bigquerydatatransfer_projects_locations_transfer_configs_runs_transfer_logs_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn bigquerydatatransfer_projects_locations_transfer_configs_runs_transfer_logs_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListTransferLogsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListTransferLogsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+parent}/transferLogs
/// Returns log messages for the transfer run.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `bigquerydatatransfer_projects_locations_transfer_configs_runs_transfer_logs_list_builder()` + `bigquerydatatransfer_projects_locations_transfer_configs_runs_transfer_logs_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn bigquerydatatransfer_projects_locations_transfer_configs_runs_transfer_logs_list(
    client: &SimpleHttpClient,
    parent: &str,
    messageTypes: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListTransferLogsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        bigquerydatatransfer_projects_locations_transfer_configs_runs_transfer_logs_list_builder(
            client,
            parent,
            messageTypes,
            pageSize,
            pageToken,
        )?;
    bigquerydatatransfer_projects_locations_transfer_configs_runs_transfer_logs_list_execute(
        builder,
    )
}

/// GET v1/{+name}
/// Returns a transfer resource.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `bigquerydatatransfer_projects_locations_transfer_configs_transfer_resources_get_execute()` to send, or `bigquerydatatransfer_projects_locations_transfer_configs_transfer_resources_get` for simplest API.

pub fn bigquerydatatransfer_projects_locations_transfer_configs_transfer_resources_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/{+name}", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+name}
/// Returns a transfer resource.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `bigquerydatatransfer_projects_locations_transfer_configs_transfer_resources_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from bigquerydatatransfer_projects_locations_transfer_configs_transfer_resources_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn bigquerydatatransfer_projects_locations_transfer_configs_transfer_resources_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<TransferResource>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: TransferResource = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+name}
/// Returns a transfer resource.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `bigquerydatatransfer_projects_locations_transfer_configs_transfer_resources_get_builder()` + `bigquerydatatransfer_projects_locations_transfer_configs_transfer_resources_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn bigquerydatatransfer_projects_locations_transfer_configs_transfer_resources_get(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<TransferResource>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        bigquerydatatransfer_projects_locations_transfer_configs_transfer_resources_get_builder(
            client, name,
        )?;
    bigquerydatatransfer_projects_locations_transfer_configs_transfer_resources_get_execute(builder)
}

/// GET v1/{+parent}/transferResources
/// Returns information about transfer resources.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `bigquerydatatransfer_projects_locations_transfer_configs_transfer_resources_list_execute()` to send, or `bigquerydatatransfer_projects_locations_transfer_configs_transfer_resources_list` for simplest API.

pub fn bigquerydatatransfer_projects_locations_transfer_configs_transfer_resources_list_builder(
    client: &SimpleHttpClient,
    parent: &str,
    filter: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comv1/{+parent}/transferResources",
        parent,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "filter",
            filter
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageSize",
            pageSize
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageToken",
            pageToken
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET v1/{+parent}/transferResources
/// Returns information about transfer resources.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `bigquerydatatransfer_projects_locations_transfer_configs_transfer_resources_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from bigquerydatatransfer_projects_locations_transfer_configs_transfer_resources_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn bigquerydatatransfer_projects_locations_transfer_configs_transfer_resources_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ListTransferResourcesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListTransferResourcesResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+parent}/transferResources
/// Returns information about transfer resources.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `bigquerydatatransfer_projects_locations_transfer_configs_transfer_resources_list_builder()` + `bigquerydatatransfer_projects_locations_transfer_configs_transfer_resources_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn bigquerydatatransfer_projects_locations_transfer_configs_transfer_resources_list(
    client: &SimpleHttpClient,
    parent: &str,
    filter: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ListTransferResourcesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        bigquerydatatransfer_projects_locations_transfer_configs_transfer_resources_list_builder(
            client, parent, filter, pageSize, pageToken,
        )?;
    bigquerydatatransfer_projects_locations_transfer_configs_transfer_resources_list_execute(
        builder,
    )
}

/// GET v1/{+parent}/transferConfigs
/// Creates a new data transfer configuration.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `bigquerydatatransfer_projects_transfer_configs_create_execute()` to send, or `bigquerydatatransfer_projects_transfer_configs_create` for simplest API.

pub fn bigquerydatatransfer_projects_transfer_configs_create_builder(
    client: &SimpleHttpClient,
    parent: &str,
    authorizationCode: Option<&str>,
    serviceAccountName: Option<&str>,
    versionInfo: Option<&str>,
    body: &TransferConfig,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comv1/{+parent}/transferConfigs",
        parent,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "authorizationCode",
            authorizationCode
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "serviceAccountName",
            serviceAccountName
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "versionInfo",
            versionInfo
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v1/{+parent}/transferConfigs
/// Creates a new data transfer configuration.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `bigquerydatatransfer_projects_transfer_configs_create_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from bigquerydatatransfer_projects_transfer_configs_create_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn bigquerydatatransfer_projects_transfer_configs_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<TransferConfig>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: TransferConfig = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+parent}/transferConfigs
/// Creates a new data transfer configuration.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `bigquerydatatransfer_projects_transfer_configs_create_builder()` + `bigquerydatatransfer_projects_transfer_configs_create_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn bigquerydatatransfer_projects_transfer_configs_create(
    client: &SimpleHttpClient,
    parent: &str,
    authorizationCode: Option<&str>,
    serviceAccountName: Option<&str>,
    versionInfo: Option<&str>,
    body: &TransferConfig,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<TransferConfig>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = bigquerydatatransfer_projects_transfer_configs_create_builder(
        client,
        parent,
        authorizationCode,
        serviceAccountName,
        versionInfo,
        body,
    )?;
    bigquerydatatransfer_projects_transfer_configs_create_execute(builder)
}

/// GET v1/{+name}
/// Deletes a data transfer configuration, including any associated transfer runs and logs.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `bigquerydatatransfer_projects_transfer_configs_delete_execute()` to send, or `bigquerydatatransfer_projects_transfer_configs_delete` for simplest API.

pub fn bigquerydatatransfer_projects_transfer_configs_delete_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/{+name}", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+name}
/// Deletes a data transfer configuration, including any associated transfer runs and logs.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `bigquerydatatransfer_projects_transfer_configs_delete_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from bigquerydatatransfer_projects_transfer_configs_delete_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn bigquerydatatransfer_projects_transfer_configs_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Empty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+name}
/// Deletes a data transfer configuration, including any associated transfer runs and logs.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `bigquerydatatransfer_projects_transfer_configs_delete_builder()` + `bigquerydatatransfer_projects_transfer_configs_delete_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn bigquerydatatransfer_projects_transfer_configs_delete(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = bigquerydatatransfer_projects_transfer_configs_delete_builder(client, name)?;
    bigquerydatatransfer_projects_transfer_configs_delete_execute(builder)
}

/// GET v1/{+name}
/// Returns information about a data transfer config.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `bigquerydatatransfer_projects_transfer_configs_get_execute()` to send, or `bigquerydatatransfer_projects_transfer_configs_get` for simplest API.

pub fn bigquerydatatransfer_projects_transfer_configs_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/{+name}", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+name}
/// Returns information about a data transfer config.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `bigquerydatatransfer_projects_transfer_configs_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from bigquerydatatransfer_projects_transfer_configs_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn bigquerydatatransfer_projects_transfer_configs_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<TransferConfig>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: TransferConfig = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+name}
/// Returns information about a data transfer config.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `bigquerydatatransfer_projects_transfer_configs_get_builder()` + `bigquerydatatransfer_projects_transfer_configs_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn bigquerydatatransfer_projects_transfer_configs_get(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<TransferConfig>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = bigquerydatatransfer_projects_transfer_configs_get_builder(client, name)?;
    bigquerydatatransfer_projects_transfer_configs_get_execute(builder)
}

/// GET v1/{+parent}/transferConfigs
/// Returns information about all transfer configs owned by a project in the specified location.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `bigquerydatatransfer_projects_transfer_configs_list_execute()` to send, or `bigquerydatatransfer_projects_transfer_configs_list` for simplest API.

pub fn bigquerydatatransfer_projects_transfer_configs_list_builder(
    client: &SimpleHttpClient,
    parent: &str,
    dataSourceIds: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comv1/{+parent}/transferConfigs",
        parent,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "dataSourceIds",
            dataSourceIds
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageSize",
            pageSize
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageToken",
            pageToken
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET v1/{+parent}/transferConfigs
/// Returns information about all transfer configs owned by a project in the specified location.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `bigquerydatatransfer_projects_transfer_configs_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from bigquerydatatransfer_projects_transfer_configs_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn bigquerydatatransfer_projects_transfer_configs_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ListTransferConfigsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListTransferConfigsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+parent}/transferConfigs
/// Returns information about all transfer configs owned by a project in the specified location.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `bigquerydatatransfer_projects_transfer_configs_list_builder()` + `bigquerydatatransfer_projects_transfer_configs_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn bigquerydatatransfer_projects_transfer_configs_list(
    client: &SimpleHttpClient,
    parent: &str,
    dataSourceIds: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ListTransferConfigsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = bigquerydatatransfer_projects_transfer_configs_list_builder(
        client,
        parent,
        dataSourceIds,
        pageSize,
        pageToken,
    )?;
    bigquerydatatransfer_projects_transfer_configs_list_execute(builder)
}

/// GET v1/{+name}
/// Updates a data transfer configuration. All fields must be set, even if they are not updated.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `bigquerydatatransfer_projects_transfer_configs_patch_execute()` to send, or `bigquerydatatransfer_projects_transfer_configs_patch` for simplest API.

pub fn bigquerydatatransfer_projects_transfer_configs_patch_builder(
    client: &SimpleHttpClient,
    name: &str,
    authorizationCode: Option<&str>,
    serviceAccountName: Option<&str>,
    updateMask: Option<&str>,
    versionInfo: Option<&str>,
    body: &TransferConfig,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/{+name}", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "authorizationCode",
            authorizationCode
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "serviceAccountName",
            serviceAccountName
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "updateMask",
            updateMask
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "versionInfo",
            versionInfo
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v1/{+name}
/// Updates a data transfer configuration. All fields must be set, even if they are not updated.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `bigquerydatatransfer_projects_transfer_configs_patch_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from bigquerydatatransfer_projects_transfer_configs_patch_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn bigquerydatatransfer_projects_transfer_configs_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<TransferConfig>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: TransferConfig = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+name}
/// Updates a data transfer configuration. All fields must be set, even if they are not updated.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `bigquerydatatransfer_projects_transfer_configs_patch_builder()` + `bigquerydatatransfer_projects_transfer_configs_patch_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn bigquerydatatransfer_projects_transfer_configs_patch(
    client: &SimpleHttpClient,
    name: &str,
    authorizationCode: Option<&str>,
    serviceAccountName: Option<&str>,
    updateMask: Option<&str>,
    versionInfo: Option<&str>,
    body: &TransferConfig,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<TransferConfig>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = bigquerydatatransfer_projects_transfer_configs_patch_builder(
        client,
        name,
        authorizationCode,
        serviceAccountName,
        updateMask,
        versionInfo,
        body,
    )?;
    bigquerydatatransfer_projects_transfer_configs_patch_execute(builder)
}

/// GET v1/{+parent}:scheduleRuns
/// Creates transfer runs for a time range [start_time, end_time]. For each date - or whatever granularity the data source supports - in the range, one transfer run is created. Note that runs are created per UTC time in the time range. DEPRECATED: use StartManualTransferRuns instead.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `bigquerydatatransfer_projects_transfer_configs_schedule_runs_execute()` to send, or `bigquerydatatransfer_projects_transfer_configs_schedule_runs` for simplest API.

pub fn bigquerydatatransfer_projects_transfer_configs_schedule_runs_builder(
    client: &SimpleHttpClient,
    parent: &str,
    body: &ScheduleTransferRunsRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/{+parent}:scheduleRuns", parent,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v1/{+parent}:scheduleRuns
/// Creates transfer runs for a time range [start_time, end_time]. For each date - or whatever granularity the data source supports - in the range, one transfer run is created. Note that runs are created per UTC time in the time range. DEPRECATED: use StartManualTransferRuns instead.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `bigquerydatatransfer_projects_transfer_configs_schedule_runs_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from bigquerydatatransfer_projects_transfer_configs_schedule_runs_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn bigquerydatatransfer_projects_transfer_configs_schedule_runs_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ScheduleTransferRunsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ScheduleTransferRunsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+parent}:scheduleRuns
/// Creates transfer runs for a time range [start_time, end_time]. For each date - or whatever granularity the data source supports - in the range, one transfer run is created. Note that runs are created per UTC time in the time range. DEPRECATED: use StartManualTransferRuns instead.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `bigquerydatatransfer_projects_transfer_configs_schedule_runs_builder()` + `bigquerydatatransfer_projects_transfer_configs_schedule_runs_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn bigquerydatatransfer_projects_transfer_configs_schedule_runs(
    client: &SimpleHttpClient,
    parent: &str,
    body: &ScheduleTransferRunsRequest,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ScheduleTransferRunsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        bigquerydatatransfer_projects_transfer_configs_schedule_runs_builder(client, parent, body)?;
    bigquerydatatransfer_projects_transfer_configs_schedule_runs_execute(builder)
}

/// GET v1/{+parent}:startManualRuns
/// Manually initiates transfer runs. You can schedule these runs in two ways: 1. For a specific point in time using the 'requested_run_time' parameter. 2. For a period between 'start_time' (inclusive) and 'end_time' (exclusive). If scheduling a single run, it is set to execute immediately (schedule_time equals the current time). When scheduling multiple runs within a time range, the first run starts now, and subsequent runs are delayed by 15 seconds each.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `bigquerydatatransfer_projects_transfer_configs_start_manual_runs_execute()` to send, or `bigquerydatatransfer_projects_transfer_configs_start_manual_runs` for simplest API.

pub fn bigquerydatatransfer_projects_transfer_configs_start_manual_runs_builder(
    client: &SimpleHttpClient,
    parent: &str,
    body: &StartManualTransferRunsRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comv1/{+parent}:startManualRuns",
        parent,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v1/{+parent}:startManualRuns
/// Manually initiates transfer runs. You can schedule these runs in two ways: 1. For a specific point in time using the 'requested_run_time' parameter. 2. For a period between 'start_time' (inclusive) and 'end_time' (exclusive). If scheduling a single run, it is set to execute immediately (schedule_time equals the current time). When scheduling multiple runs within a time range, the first run starts now, and subsequent runs are delayed by 15 seconds each.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `bigquerydatatransfer_projects_transfer_configs_start_manual_runs_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from bigquerydatatransfer_projects_transfer_configs_start_manual_runs_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn bigquerydatatransfer_projects_transfer_configs_start_manual_runs_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<StartManualTransferRunsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: StartManualTransferRunsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+parent}:startManualRuns
/// Manually initiates transfer runs. You can schedule these runs in two ways: 1. For a specific point in time using the 'requested_run_time' parameter. 2. For a period between 'start_time' (inclusive) and 'end_time' (exclusive). If scheduling a single run, it is set to execute immediately (schedule_time equals the current time). When scheduling multiple runs within a time range, the first run starts now, and subsequent runs are delayed by 15 seconds each.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `bigquerydatatransfer_projects_transfer_configs_start_manual_runs_builder()` + `bigquerydatatransfer_projects_transfer_configs_start_manual_runs_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn bigquerydatatransfer_projects_transfer_configs_start_manual_runs(
    client: &SimpleHttpClient,
    parent: &str,
    body: &StartManualTransferRunsRequest,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<StartManualTransferRunsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = bigquerydatatransfer_projects_transfer_configs_start_manual_runs_builder(
        client, parent, body,
    )?;
    bigquerydatatransfer_projects_transfer_configs_start_manual_runs_execute(builder)
}

/// GET v1/{+name}
/// Deletes the specified transfer run.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `bigquerydatatransfer_projects_transfer_configs_runs_delete_execute()` to send, or `bigquerydatatransfer_projects_transfer_configs_runs_delete` for simplest API.

pub fn bigquerydatatransfer_projects_transfer_configs_runs_delete_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/{+name}", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+name}
/// Deletes the specified transfer run.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `bigquerydatatransfer_projects_transfer_configs_runs_delete_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from bigquerydatatransfer_projects_transfer_configs_runs_delete_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn bigquerydatatransfer_projects_transfer_configs_runs_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Empty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+name}
/// Deletes the specified transfer run.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `bigquerydatatransfer_projects_transfer_configs_runs_delete_builder()` + `bigquerydatatransfer_projects_transfer_configs_runs_delete_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn bigquerydatatransfer_projects_transfer_configs_runs_delete(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = bigquerydatatransfer_projects_transfer_configs_runs_delete_builder(client, name)?;
    bigquerydatatransfer_projects_transfer_configs_runs_delete_execute(builder)
}

/// GET v1/{+name}
/// Returns information about the particular transfer run.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `bigquerydatatransfer_projects_transfer_configs_runs_get_execute()` to send, or `bigquerydatatransfer_projects_transfer_configs_runs_get` for simplest API.

pub fn bigquerydatatransfer_projects_transfer_configs_runs_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/{+name}", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+name}
/// Returns information about the particular transfer run.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `bigquerydatatransfer_projects_transfer_configs_runs_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from bigquerydatatransfer_projects_transfer_configs_runs_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn bigquerydatatransfer_projects_transfer_configs_runs_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<TransferRun>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: TransferRun = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+name}
/// Returns information about the particular transfer run.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `bigquerydatatransfer_projects_transfer_configs_runs_get_builder()` + `bigquerydatatransfer_projects_transfer_configs_runs_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn bigquerydatatransfer_projects_transfer_configs_runs_get(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<TransferRun>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = bigquerydatatransfer_projects_transfer_configs_runs_get_builder(client, name)?;
    bigquerydatatransfer_projects_transfer_configs_runs_get_execute(builder)
}

/// GET v1/{+parent}/runs
/// Returns information about running and completed transfer runs.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `bigquerydatatransfer_projects_transfer_configs_runs_list_execute()` to send, or `bigquerydatatransfer_projects_transfer_configs_runs_list` for simplest API.

pub fn bigquerydatatransfer_projects_transfer_configs_runs_list_builder(
    client: &SimpleHttpClient,
    parent: &str,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
    runAttempt: Option<&str>,
    states: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/{+parent}/runs", parent,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "pageSize",
            pageSize
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageToken",
            pageToken
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "runAttempt",
            runAttempt
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "states",
            states
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET v1/{+parent}/runs
/// Returns information about running and completed transfer runs.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `bigquerydatatransfer_projects_transfer_configs_runs_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from bigquerydatatransfer_projects_transfer_configs_runs_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn bigquerydatatransfer_projects_transfer_configs_runs_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListTransferRunsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListTransferRunsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+parent}/runs
/// Returns information about running and completed transfer runs.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `bigquerydatatransfer_projects_transfer_configs_runs_list_builder()` + `bigquerydatatransfer_projects_transfer_configs_runs_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn bigquerydatatransfer_projects_transfer_configs_runs_list(
    client: &SimpleHttpClient,
    parent: &str,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
    runAttempt: Option<&str>,
    states: Option<&str>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListTransferRunsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = bigquerydatatransfer_projects_transfer_configs_runs_list_builder(
        client, parent, pageSize, pageToken, runAttempt, states,
    )?;
    bigquerydatatransfer_projects_transfer_configs_runs_list_execute(builder)
}

/// GET v1/{+parent}/transferLogs
/// Returns log messages for the transfer run.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `bigquerydatatransfer_projects_transfer_configs_runs_transfer_logs_list_execute()` to send, or `bigquerydatatransfer_projects_transfer_configs_runs_transfer_logs_list` for simplest API.

pub fn bigquerydatatransfer_projects_transfer_configs_runs_transfer_logs_list_builder(
    client: &SimpleHttpClient,
    parent: &str,
    messageTypes: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/{+parent}/transferLogs", parent,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "messageTypes",
            messageTypes
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageSize",
            pageSize
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageToken",
            pageToken
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET v1/{+parent}/transferLogs
/// Returns log messages for the transfer run.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `bigquerydatatransfer_projects_transfer_configs_runs_transfer_logs_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from bigquerydatatransfer_projects_transfer_configs_runs_transfer_logs_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn bigquerydatatransfer_projects_transfer_configs_runs_transfer_logs_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListTransferLogsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListTransferLogsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+parent}/transferLogs
/// Returns log messages for the transfer run.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `bigquerydatatransfer_projects_transfer_configs_runs_transfer_logs_list_builder()` + `bigquerydatatransfer_projects_transfer_configs_runs_transfer_logs_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn bigquerydatatransfer_projects_transfer_configs_runs_transfer_logs_list(
    client: &SimpleHttpClient,
    parent: &str,
    messageTypes: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListTransferLogsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = bigquerydatatransfer_projects_transfer_configs_runs_transfer_logs_list_builder(
        client,
        parent,
        messageTypes,
        pageSize,
        pageToken,
    )?;
    bigquerydatatransfer_projects_transfer_configs_runs_transfer_logs_list_execute(builder)
}

/// GET v1/{+name}
/// Returns a transfer resource.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `bigquerydatatransfer_projects_transfer_configs_transfer_resources_get_execute()` to send, or `bigquerydatatransfer_projects_transfer_configs_transfer_resources_get` for simplest API.

pub fn bigquerydatatransfer_projects_transfer_configs_transfer_resources_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/{+name}", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+name}
/// Returns a transfer resource.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `bigquerydatatransfer_projects_transfer_configs_transfer_resources_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from bigquerydatatransfer_projects_transfer_configs_transfer_resources_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn bigquerydatatransfer_projects_transfer_configs_transfer_resources_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<TransferResource>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: TransferResource = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+name}
/// Returns a transfer resource.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `bigquerydatatransfer_projects_transfer_configs_transfer_resources_get_builder()` + `bigquerydatatransfer_projects_transfer_configs_transfer_resources_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn bigquerydatatransfer_projects_transfer_configs_transfer_resources_get(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<TransferResource>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = bigquerydatatransfer_projects_transfer_configs_transfer_resources_get_builder(
        client, name,
    )?;
    bigquerydatatransfer_projects_transfer_configs_transfer_resources_get_execute(builder)
}

/// GET v1/{+parent}/transferResources
/// Returns information about transfer resources.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `bigquerydatatransfer_projects_transfer_configs_transfer_resources_list_execute()` to send, or `bigquerydatatransfer_projects_transfer_configs_transfer_resources_list` for simplest API.

pub fn bigquerydatatransfer_projects_transfer_configs_transfer_resources_list_builder(
    client: &SimpleHttpClient,
    parent: &str,
    filter: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comv1/{+parent}/transferResources",
        parent,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "filter",
            filter
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageSize",
            pageSize
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageToken",
            pageToken
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET v1/{+parent}/transferResources
/// Returns information about transfer resources.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `bigquerydatatransfer_projects_transfer_configs_transfer_resources_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from bigquerydatatransfer_projects_transfer_configs_transfer_resources_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn bigquerydatatransfer_projects_transfer_configs_transfer_resources_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ListTransferResourcesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListTransferResourcesResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+parent}/transferResources
/// Returns information about transfer resources.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `bigquerydatatransfer_projects_transfer_configs_transfer_resources_list_builder()` + `bigquerydatatransfer_projects_transfer_configs_transfer_resources_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn bigquerydatatransfer_projects_transfer_configs_transfer_resources_list(
    client: &SimpleHttpClient,
    parent: &str,
    filter: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ListTransferResourcesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = bigquerydatatransfer_projects_transfer_configs_transfer_resources_list_builder(
        client, parent, filter, pageSize, pageToken,
    )?;
    bigquerydatatransfer_projects_transfer_configs_transfer_resources_list_execute(builder)
}
