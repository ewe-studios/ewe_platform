//! Auto-generated API clients for gcp/bigquerydatatransfer.
//!
//! Generated by `cargo run --bin ewe_platform gen_provider_clients`.
//! DO NOT EDIT MANUALLY.
//!
//! Feature flag: `gcp`

#![cfg(feature = "gcp")]

pub mod types;

use crate::providers::gcp::clients::types::*;
use crate::providers::gcp::resources::*;
use foundation_core::valtron::{
    execute, BoxedSendExecutionAction, StreamIterator, StreamIteratorExt, TaskIterator,
    TaskIteratorExt,
};
use foundation_core::wire::simple_http::client::{
    body_reader, ClientRequestBuilder, RequestIntro, SimpleHttpClient, SystemDnsResolver,
};
use foundation_macros::JsonHash;
use serde::Serialize;

/// GET v1/projects/{projectsId}:enrollDataSources
/// Enroll data sources in a user project. This allows users to create transfer configurations for these data sources. They will also appear in the ListDataSources RPC and as such, will appear in the [BigQuery UI](<https://console.cloud.google.`com/bigquery`>), and the documents can be found in the public guide for [BigQuery Web UI](<https://cloud.google.`com/bigquery/bigquery-web-ui`>) and [Data Transfer Service](<https://cloud.google.`com/bigquery/docs/working-with-transfers`>).
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `bigquerydatatransfer_projects_enroll_data_sources_execute()` to send, or `bigquerydatatransfer_projects_enroll_data_sources` for simplest API.

pub fn bigquerydatatransfer_projects_enroll_data_sources_builder(
    client: &SimpleHttpClient,
    name: String,
    body: &EnrollDataSourcesRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url =
        format!("https://bigquerydatatransfer.googleapis.com/v1/projects/{}:enrollDataSources",);

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}:enrollDataSources
/// Enroll data sources in a user project. This allows users to create transfer configurations for these data sources. They will also appear in the ListDataSources RPC and as such, will appear in the [BigQuery UI](<https://console.cloud.google.`com/bigquery`>), and the documents can be found in the public guide for [BigQuery Web UI](<https://cloud.google.`com/bigquery/bigquery-web-ui`>) and [Data Transfer Service](<https://cloud.google.`com/bigquery/docs/working-with-transfers`>).
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `bigquerydatatransfer_projects_enroll_data_sources_execute()` or `bigquerydatatransfer_projects_enroll_data_sources`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `bigquerydatatransfer_projects_enroll_data_sources_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn bigquerydatatransfer_projects_enroll_data_sources_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Empty>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Empty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}:enrollDataSources
/// Enroll data sources in a user project. This allows users to create transfer configurations for these data sources. They will also appear in the ListDataSources RPC and as such, will appear in the [BigQuery UI](<https://console.cloud.google.`com/bigquery`>), and the documents can be found in the public guide for [BigQuery Web UI](<https://cloud.google.`com/bigquery/bigquery-web-ui`>) and [Data Transfer Service](<https://cloud.google.`com/bigquery/docs/working-with-transfers`>).
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `bigquerydatatransfer_projects_enroll_data_sources_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `bigquerydatatransfer_projects_enroll_data_sources_task()`.
/// For the simplest API, use `bigquerydatatransfer_projects_enroll_data_sources()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `bigquerydatatransfer_projects_enroll_data_sources_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn bigquerydatatransfer_projects_enroll_data_sources_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = bigquerydatatransfer_projects_enroll_data_sources_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`bigquerydatatransfer_projects_enroll_data_sources`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct BigquerydatatransferProjectsEnrollDataSourcesArgs {
    /// Path parameter: name
    pub name: String,
    /// Request body.
    pub body: EnrollDataSourcesRequest,
}

/// GET v1/projects/{projectsId}:enrollDataSources
/// Enroll data sources in a user project. This allows users to create transfer configurations for these data sources. They will also appear in the ListDataSources RPC and as such, will appear in the [BigQuery UI](<https://console.cloud.google.`com/bigquery`>), and the documents can be found in the public guide for [BigQuery Web UI](<https://cloud.google.`com/bigquery/bigquery-web-ui`>) and [Data Transfer Service](<https://cloud.google.`com/bigquery/docs/working-with-transfers`>).
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `bigquerydatatransfer_projects_enroll_data_sources_builder()` + `bigquerydatatransfer_projects_enroll_data_sources_execute()`.
/// For task-level control, use `bigquerydatatransfer_projects_enroll_data_sources_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn bigquerydatatransfer_projects_enroll_data_sources(
    client: &SimpleHttpClient,
    args: &BigquerydatatransferProjectsEnrollDataSourcesArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = bigquerydatatransfer_projects_enroll_data_sources_builder(
        client,
        args.name.clone(),
        &args.body,
    )?;
    bigquerydatatransfer_projects_enroll_data_sources_execute(builder)
}

/// GET v1/projects/{projectsId}/dataSources
/// Lists supported data sources and returns their settings.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `bigquerydatatransfer_projects_data_sources_list_execute()` to send, or `bigquerydatatransfer_projects_data_sources_list` for simplest API.

pub fn bigquerydatatransfer_projects_data_sources_list_builder(
    client: &SimpleHttpClient,
    parent: String,
    pageSize: Option<i32>,
    pageToken: Option<String>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url =
        format!("https://bigquerydatatransfer.googleapis.com/v1/projects/{}/dataSources",);

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/dataSources
/// Lists supported data sources and returns their settings.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `bigquerydatatransfer_projects_data_sources_list_execute()` or `bigquerydatatransfer_projects_data_sources_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `bigquerydatatransfer_projects_data_sources_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn bigquerydatatransfer_projects_data_sources_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ListDataSourcesResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListDataSourcesResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/dataSources
/// Lists supported data sources and returns their settings.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `bigquerydatatransfer_projects_data_sources_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `bigquerydatatransfer_projects_data_sources_list_task()`.
/// For the simplest API, use `bigquerydatatransfer_projects_data_sources_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `bigquerydatatransfer_projects_data_sources_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn bigquerydatatransfer_projects_data_sources_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListDataSourcesResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = bigquerydatatransfer_projects_data_sources_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`bigquerydatatransfer_projects_data_sources_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct BigquerydatatransferProjectsDataSourcesListArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: pageSize
    pub pageSize: Option<i32>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
}

/// GET v1/projects/{projectsId}/dataSources
/// Lists supported data sources and returns their settings.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `bigquerydatatransfer_projects_data_sources_list_builder()` + `bigquerydatatransfer_projects_data_sources_list_execute()`.
/// For task-level control, use `bigquerydatatransfer_projects_data_sources_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn bigquerydatatransfer_projects_data_sources_list(
    client: &SimpleHttpClient,
    args: &BigquerydatatransferProjectsDataSourcesListArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListDataSourcesResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = bigquerydatatransfer_projects_data_sources_list_builder(
        client,
        args.parent.clone(),
        args.pageSize.clone(),
        args.pageToken.clone(),
    )?;
    bigquerydatatransfer_projects_data_sources_list_execute(builder)
}

/// GET v1/projects/{projectsId}/locations
/// Lists information about the supported locations for this service. This method lists locations based on the resource scope provided in the [ListLocationsRequest.name] field: * **Global locations**: If name is empty, the method lists the public locations available to all projects. * **Project-specific locations**: If name follows the format `projects/{project}`, the method lists locations visible to that specific project. This includes public, private, or other project-specific locations enabled for the project. For `gRPC` and client library implementations, the resource name is passed as the name field. For direct service calls, the resource name is incorporated into the request path based on the specific service implementation and version.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `bigquerydatatransfer_projects_locations_list_execute()` to send, or `bigquerydatatransfer_projects_locations_list` for simplest API.

pub fn bigquerydatatransfer_projects_locations_list_builder(
    client: &SimpleHttpClient,
    name: String,
    extraLocationTypes: Option<String>,
    filter: Option<String>,
    pageSize: Option<i32>,
    pageToken: Option<String>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url =
        format!("https://bigquerydatatransfer.googleapis.com/v1/projects/{}/locations",);

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = extraLocationTypes {
        query_parts.push(format!("extraLocationTypes={}", val));
    }
    if let Some(val) = filter {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations
/// Lists information about the supported locations for this service. This method lists locations based on the resource scope provided in the [ListLocationsRequest.name] field: * **Global locations**: If name is empty, the method lists the public locations available to all projects. * **Project-specific locations**: If name follows the format `projects/{project}`, the method lists locations visible to that specific project. This includes public, private, or other project-specific locations enabled for the project. For `gRPC` and client library implementations, the resource name is passed as the name field. For direct service calls, the resource name is incorporated into the request path based on the specific service implementation and version.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `bigquerydatatransfer_projects_locations_list_execute()` or `bigquerydatatransfer_projects_locations_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `bigquerydatatransfer_projects_locations_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn bigquerydatatransfer_projects_locations_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ListLocationsResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListLocationsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations
/// Lists information about the supported locations for this service. This method lists locations based on the resource scope provided in the [ListLocationsRequest.name] field: * **Global locations**: If name is empty, the method lists the public locations available to all projects. * **Project-specific locations**: If name follows the format `projects/{project}`, the method lists locations visible to that specific project. This includes public, private, or other project-specific locations enabled for the project. For `gRPC` and client library implementations, the resource name is passed as the name field. For direct service calls, the resource name is incorporated into the request path based on the specific service implementation and version.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `bigquerydatatransfer_projects_locations_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `bigquerydatatransfer_projects_locations_list_task()`.
/// For the simplest API, use `bigquerydatatransfer_projects_locations_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `bigquerydatatransfer_projects_locations_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn bigquerydatatransfer_projects_locations_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListLocationsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = bigquerydatatransfer_projects_locations_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`bigquerydatatransfer_projects_locations_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct BigquerydatatransferProjectsLocationsListArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: extraLocationTypes
    pub extraLocationTypes: Option<String>,
    /// Query parameter: filter
    pub filter: Option<String>,
    /// Query parameter: pageSize
    pub pageSize: Option<i32>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
}

/// GET v1/projects/{projectsId}/locations
/// Lists information about the supported locations for this service. This method lists locations based on the resource scope provided in the [ListLocationsRequest.name] field: * **Global locations**: If name is empty, the method lists the public locations available to all projects. * **Project-specific locations**: If name follows the format `projects/{project}`, the method lists locations visible to that specific project. This includes public, private, or other project-specific locations enabled for the project. For `gRPC` and client library implementations, the resource name is passed as the name field. For direct service calls, the resource name is incorporated into the request path based on the specific service implementation and version.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `bigquerydatatransfer_projects_locations_list_builder()` + `bigquerydatatransfer_projects_locations_list_execute()`.
/// For task-level control, use `bigquerydatatransfer_projects_locations_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn bigquerydatatransfer_projects_locations_list(
    client: &SimpleHttpClient,
    args: &BigquerydatatransferProjectsLocationsListArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListLocationsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = bigquerydatatransfer_projects_locations_list_builder(
        client,
        args.name.clone(),
        args.extraLocationTypes.clone(),
        args.filter.clone(),
        args.pageSize.clone(),
        args.pageToken.clone(),
    )?;
    bigquerydatatransfer_projects_locations_list_execute(builder)
}

/// GET v1/projects/{projectsId}/transferConfigs
/// Creates a new data transfer configuration.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `bigquerydatatransfer_projects_transfer_configs_create_execute()` to send, or `bigquerydatatransfer_projects_transfer_configs_create` for simplest API.

pub fn bigquerydatatransfer_projects_transfer_configs_create_builder(
    client: &SimpleHttpClient,
    parent: String,
    authorizationCode: Option<String>,
    serviceAccountName: Option<String>,
    versionInfo: Option<String>,
    body: &TransferConfig,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url =
        format!("https://bigquerydatatransfer.googleapis.com/v1/projects/{}/transferConfigs",);

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = authorizationCode {
        query_parts.push(format!("authorizationCode={}", val));
    }
    if let Some(val) = serviceAccountName {
        query_parts.push(format!("serviceAccountName={}", val));
    }
    if let Some(val) = versionInfo {
        query_parts.push(format!("versionInfo={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/transferConfigs
/// Creates a new data transfer configuration.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `bigquerydatatransfer_projects_transfer_configs_create_execute()` or `bigquerydatatransfer_projects_transfer_configs_create`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `bigquerydatatransfer_projects_transfer_configs_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn bigquerydatatransfer_projects_transfer_configs_create_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<TransferConfig>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: TransferConfig = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/transferConfigs
/// Creates a new data transfer configuration.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `bigquerydatatransfer_projects_transfer_configs_create_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `bigquerydatatransfer_projects_transfer_configs_create_task()`.
/// For the simplest API, use `bigquerydatatransfer_projects_transfer_configs_create()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `bigquerydatatransfer_projects_transfer_configs_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn bigquerydatatransfer_projects_transfer_configs_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<TransferConfig>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = bigquerydatatransfer_projects_transfer_configs_create_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`bigquerydatatransfer_projects_transfer_configs_create`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct BigquerydatatransferProjectsTransferConfigsCreateArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: authorizationCode
    pub authorizationCode: Option<String>,
    /// Query parameter: serviceAccountName
    pub serviceAccountName: Option<String>,
    /// Query parameter: versionInfo
    pub versionInfo: Option<String>,
    /// Request body.
    pub body: TransferConfig,
}

/// GET v1/projects/{projectsId}/transferConfigs
/// Creates a new data transfer configuration.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `bigquerydatatransfer_projects_transfer_configs_create_builder()` + `bigquerydatatransfer_projects_transfer_configs_create_execute()`.
/// For task-level control, use `bigquerydatatransfer_projects_transfer_configs_create_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn bigquerydatatransfer_projects_transfer_configs_create(
    client: &SimpleHttpClient,
    args: &BigquerydatatransferProjectsTransferConfigsCreateArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<TransferConfig>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = bigquerydatatransfer_projects_transfer_configs_create_builder(
        client,
        args.parent.clone(),
        args.authorizationCode.clone(),
        args.serviceAccountName.clone(),
        args.versionInfo.clone(),
        &args.body,
    )?;
    bigquerydatatransfer_projects_transfer_configs_create_execute(builder)
}
