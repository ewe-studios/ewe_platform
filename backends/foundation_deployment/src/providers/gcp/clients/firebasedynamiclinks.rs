//! Auto-generated API clients for gcp/firebasedynamiclinks.
//!
//! Generated by `cargo run --bin ewe_platform gen_provider_clients`.
//! DO NOT EDIT MANUALLY.
//!
//! Feature flag: `gcp`

#![cfg(feature = "gcp")]


use crate::providers::gcp::clients::types::*;
use crate::providers::gcp::resources::*;
use foundation_core::valtron::{
    execute, BoxedSendExecutionAction, StreamIterator, StreamIteratorExt, TaskIterator,
    TaskIteratorExt,
};
use foundation_core::wire::simple_http::client::{
    body_reader, ClientRequestBuilder, DnsResolver, RequestIntro, SimpleHttpClient,
    SystemDnsResolver,
};
use foundation_db::state::resource_identifier::ResourceIdentifier;
use foundation_macros::JsonHash;
use serde::Serialize;

/// POST v1/managedShortLinks:create
/// Creates a managed short Dynamic Link given either a valid long Dynamic Link or details such as Dynamic Link domain, Android and iOS app information. The created short Dynamic Link will not expire. This differs from CreateShortDynamicLink in the following ways: - The request will also contain a name for the link (non unique name for the front end). - The response must be authenticated with an auth token (generated with the admin service account). - The link will appear in the FDL list of links in the console front end. The Dynamic Link domain in the request must be owned by requester''s Firebase project.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `firebasedynamiclinks_managed_short_links_create_execute()` to send, or `firebasedynamiclinks_managed_short_links_create` for simplest API.

pub fn firebasedynamiclinks_managed_short_links_create_builder<R>(
    client: &SimpleHttpClient<R>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url =
        format!("https://firebasedynamiclinks.googleapis.com/v1/managedShortLinks:create",);

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1/managedShortLinks:create
/// Creates a managed short Dynamic Link given either a valid long Dynamic Link or details such as Dynamic Link domain, Android and iOS app information. The created short Dynamic Link will not expire. This differs from CreateShortDynamicLink in the following ways: - The request will also contain a name for the link (non unique name for the front end). - The response must be authenticated with an auth token (generated with the admin service account). - The link will appear in the FDL list of links in the console front end. The Dynamic Link domain in the request must be owned by requester''s Firebase project.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `firebasedynamiclinks_managed_short_links_create_execute()` or `firebasedynamiclinks_managed_short_links_create`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `firebasedynamiclinks_managed_short_links_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn firebasedynamiclinks_managed_short_links_create_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<CreateManagedShortLinkResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: CreateManagedShortLinkResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1/managedShortLinks:create
/// Creates a managed short Dynamic Link given either a valid long Dynamic Link or details such as Dynamic Link domain, Android and iOS app information. The created short Dynamic Link will not expire. This differs from CreateShortDynamicLink in the following ways: - The request will also contain a name for the link (non unique name for the front end). - The response must be authenticated with an auth token (generated with the admin service account). - The link will appear in the FDL list of links in the console front end. The Dynamic Link domain in the request must be owned by requester''s Firebase project.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `firebasedynamiclinks_managed_short_links_create_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `firebasedynamiclinks_managed_short_links_create_task()`.
/// For the simplest API, use `firebasedynamiclinks_managed_short_links_create()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `firebasedynamiclinks_managed_short_links_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn firebasedynamiclinks_managed_short_links_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<CreateManagedShortLinkResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = firebasedynamiclinks_managed_short_links_create_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// POST v1/managedShortLinks:create
/// Creates a managed short Dynamic Link given either a valid long Dynamic Link or details such as Dynamic Link domain, Android and iOS app information. The created short Dynamic Link will not expire. This differs from CreateShortDynamicLink in the following ways: - The request will also contain a name for the link (non unique name for the front end). - The response must be authenticated with an auth token (generated with the admin service account). - The link will appear in the FDL list of links in the console front end. The Dynamic Link domain in the request must be owned by requester''s Firebase project.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `firebasedynamiclinks_managed_short_links_create_builder()` + `firebasedynamiclinks_managed_short_links_create_execute()`.
/// For task-level control, use `firebasedynamiclinks_managed_short_links_create_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn firebasedynamiclinks_managed_short_links_create(
    client: &SimpleHttpClient,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<CreateManagedShortLinkResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = firebasedynamiclinks_managed_short_links_create_builder(client)?;
    firebasedynamiclinks_managed_short_links_create_execute(builder)
}

/// POST v1/shortLinks
/// Creates a short Dynamic Link given either a valid long Dynamic Link or details such as Dynamic Link domain, Android and iOS app information. The created short Dynamic Link will not expire. Repeated calls with the same long Dynamic Link or Dynamic Link information will produce the same short Dynamic Link. The Dynamic Link domain in the request must be owned by requester''s Firebase project.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `firebasedynamiclinks_short_links_create_execute()` to send, or `firebasedynamiclinks_short_links_create` for simplest API.

pub fn firebasedynamiclinks_short_links_create_builder<R>(
    client: &SimpleHttpClient<R>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!("https://firebasedynamiclinks.googleapis.com/v1/shortLinks",);

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1/shortLinks
/// Creates a short Dynamic Link given either a valid long Dynamic Link or details such as Dynamic Link domain, Android and iOS app information. The created short Dynamic Link will not expire. Repeated calls with the same long Dynamic Link or Dynamic Link information will produce the same short Dynamic Link. The Dynamic Link domain in the request must be owned by requester''s Firebase project.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `firebasedynamiclinks_short_links_create_execute()` or `firebasedynamiclinks_short_links_create`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `firebasedynamiclinks_short_links_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn firebasedynamiclinks_short_links_create_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<CreateShortDynamicLinkResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: CreateShortDynamicLinkResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1/shortLinks
/// Creates a short Dynamic Link given either a valid long Dynamic Link or details such as Dynamic Link domain, Android and iOS app information. The created short Dynamic Link will not expire. Repeated calls with the same long Dynamic Link or Dynamic Link information will produce the same short Dynamic Link. The Dynamic Link domain in the request must be owned by requester''s Firebase project.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `firebasedynamiclinks_short_links_create_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `firebasedynamiclinks_short_links_create_task()`.
/// For the simplest API, use `firebasedynamiclinks_short_links_create()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `firebasedynamiclinks_short_links_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn firebasedynamiclinks_short_links_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<CreateShortDynamicLinkResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = firebasedynamiclinks_short_links_create_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// POST v1/shortLinks
/// Creates a short Dynamic Link given either a valid long Dynamic Link or details such as Dynamic Link domain, Android and iOS app information. The created short Dynamic Link will not expire. Repeated calls with the same long Dynamic Link or Dynamic Link information will produce the same short Dynamic Link. The Dynamic Link domain in the request must be owned by requester''s Firebase project.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `firebasedynamiclinks_short_links_create_builder()` + `firebasedynamiclinks_short_links_create_execute()`.
/// For task-level control, use `firebasedynamiclinks_short_links_create_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn firebasedynamiclinks_short_links_create(
    client: &SimpleHttpClient,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<CreateShortDynamicLinkResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = firebasedynamiclinks_short_links_create_builder(client)?;
    firebasedynamiclinks_short_links_create_execute(builder)
}

/// GET v1/{dynamicLink}/linkStats
/// Fetches analytics stats of a short Dynamic Link for a given duration. Metrics include number of clicks, redirects, installs, app first opens, and app reopens.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `firebasedynamiclinks_get_link_stats_execute()` to send, or `firebasedynamiclinks_get_link_stats` for simplest API.

pub fn firebasedynamiclinks_get_link_stats_builder<R>(
    client: &SimpleHttpClient<R>,
    dynamicLink: &String,
    durationDays: &Option<String>,
    sdkVersion: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://firebasedynamiclinks.googleapis.com/v1/{}/linkStats",
        dynamicLink,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = durationDays.as_ref() {
        query_parts.push(format!("durationDays={}", val));
    }
    if let Some(val) = sdkVersion.as_ref() {
        query_parts.push(format!("sdkVersion={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/{dynamicLink}/linkStats
/// Fetches analytics stats of a short Dynamic Link for a given duration. Metrics include number of clicks, redirects, installs, app first opens, and app reopens.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `firebasedynamiclinks_get_link_stats_execute()` or `firebasedynamiclinks_get_link_stats`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `firebasedynamiclinks_get_link_stats_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn firebasedynamiclinks_get_link_stats_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<DynamicLinkStats>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: DynamicLinkStats = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/{dynamicLink}/linkStats
/// Fetches analytics stats of a short Dynamic Link for a given duration. Metrics include number of clicks, redirects, installs, app first opens, and app reopens.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `firebasedynamiclinks_get_link_stats_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `firebasedynamiclinks_get_link_stats_task()`.
/// For the simplest API, use `firebasedynamiclinks_get_link_stats()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `firebasedynamiclinks_get_link_stats_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn firebasedynamiclinks_get_link_stats_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<DynamicLinkStats>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = firebasedynamiclinks_get_link_stats_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`firebasedynamiclinks_get_link_stats`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct FirebasedynamiclinksGetLinkStatsArgs {
    /// Path parameter: dynamicLink
    pub dynamicLink: String,
    /// Query parameter: durationDays
    pub durationDays: Option<String>,
    /// Query parameter: sdkVersion
    pub sdkVersion: Option<String>,
}

/// GET v1/{dynamicLink}/linkStats
/// Fetches analytics stats of a short Dynamic Link for a given duration. Metrics include number of clicks, redirects, installs, app first opens, and app reopens.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `firebasedynamiclinks_get_link_stats_builder()` + `firebasedynamiclinks_get_link_stats_execute()`.
/// For task-level control, use `firebasedynamiclinks_get_link_stats_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn firebasedynamiclinks_get_link_stats(
    client: &SimpleHttpClient,
    args: &FirebasedynamiclinksGetLinkStatsArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<DynamicLinkStats>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = firebasedynamiclinks_get_link_stats_builder(
        client,
        &args.dynamicLink,
        &args.durationDays,
        &args.sdkVersion,
    )?;
    firebasedynamiclinks_get_link_stats_execute(builder)
}

/// POST v1/installAttribution
/// Get iOS `strong/weak-match` info for post-install attribution.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `firebasedynamiclinks_install_attribution_execute()` to send, or `firebasedynamiclinks_install_attribution` for simplest API.

pub fn firebasedynamiclinks_install_attribution_builder<R>(
    client: &SimpleHttpClient<R>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url =
        format!("https://firebasedynamiclinks.googleapis.com/v1/installAttribution",);

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1/installAttribution
/// Get iOS `strong/weak-match` info for post-install attribution.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `firebasedynamiclinks_install_attribution_execute()` or `firebasedynamiclinks_install_attribution`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `firebasedynamiclinks_install_attribution_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn firebasedynamiclinks_install_attribution_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GetIosPostInstallAttributionResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GetIosPostInstallAttributionResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1/installAttribution
/// Get iOS `strong/weak-match` info for post-install attribution.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `firebasedynamiclinks_install_attribution_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `firebasedynamiclinks_install_attribution_task()`.
/// For the simplest API, use `firebasedynamiclinks_install_attribution()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `firebasedynamiclinks_install_attribution_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn firebasedynamiclinks_install_attribution_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GetIosPostInstallAttributionResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = firebasedynamiclinks_install_attribution_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// POST v1/installAttribution
/// Get iOS `strong/weak-match` info for post-install attribution.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `firebasedynamiclinks_install_attribution_builder()` + `firebasedynamiclinks_install_attribution_execute()`.
/// For task-level control, use `firebasedynamiclinks_install_attribution_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn firebasedynamiclinks_install_attribution(
    client: &SimpleHttpClient,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GetIosPostInstallAttributionResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = firebasedynamiclinks_install_attribution_builder(client)?;
    firebasedynamiclinks_install_attribution_execute(builder)
}

/// POST v1/reopenAttribution
/// Get iOS reopen attribution for app universal link open deeplinking.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `firebasedynamiclinks_reopen_attribution_execute()` to send, or `firebasedynamiclinks_reopen_attribution` for simplest API.

pub fn firebasedynamiclinks_reopen_attribution_builder<R>(
    client: &SimpleHttpClient<R>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!("https://firebasedynamiclinks.googleapis.com/v1/reopenAttribution",);

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1/reopenAttribution
/// Get iOS reopen attribution for app universal link open deeplinking.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `firebasedynamiclinks_reopen_attribution_execute()` or `firebasedynamiclinks_reopen_attribution`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `firebasedynamiclinks_reopen_attribution_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn firebasedynamiclinks_reopen_attribution_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GetIosReopenAttributionResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GetIosReopenAttributionResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1/reopenAttribution
/// Get iOS reopen attribution for app universal link open deeplinking.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `firebasedynamiclinks_reopen_attribution_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `firebasedynamiclinks_reopen_attribution_task()`.
/// For the simplest API, use `firebasedynamiclinks_reopen_attribution()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `firebasedynamiclinks_reopen_attribution_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn firebasedynamiclinks_reopen_attribution_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GetIosReopenAttributionResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = firebasedynamiclinks_reopen_attribution_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// POST v1/reopenAttribution
/// Get iOS reopen attribution for app universal link open deeplinking.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `firebasedynamiclinks_reopen_attribution_builder()` + `firebasedynamiclinks_reopen_attribution_execute()`.
/// For task-level control, use `firebasedynamiclinks_reopen_attribution_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn firebasedynamiclinks_reopen_attribution(
    client: &SimpleHttpClient,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GetIosReopenAttributionResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = firebasedynamiclinks_reopen_attribution_builder(client)?;
    firebasedynamiclinks_reopen_attribution_execute(builder)
}

// =============================================================================
// ResourceIdentifier implementation for DynamicLinkStats
// =============================================================================

/// ResourceIdentifier implementation for DynamicLinkStats with FirebasedynamiclinksGetLinkStatsArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<FirebasedynamiclinksGetLinkStatsArgs> for DynamicLinkStats {
    fn generate_resource_id(&self, input: &FirebasedynamiclinksGetLinkStatsArgs) -> String {
        format!(
            "gcp::firebasedynamiclinks::DynamicLinkStats/{}",
            input.dynamicLink
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::firebasedynamiclinks::DynamicLinkStats"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}
