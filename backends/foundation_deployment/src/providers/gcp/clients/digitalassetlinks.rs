//! Auto-generated API clients for Google Cloud Platform - digitalassetlinks.
//!
//! Generated by `cargo run --bin ewe_platform gen_provider_clients`.
//! DO NOT EDIT MANUALLY.
//!
//! Feature flag: `gcp`


#![cfg(feature = "gcp")]

use crate::providers::gcp::clients::types::*;
use crate::providers::gcp::resources::*;
use foundation_core::valtron::{execute, StreamIterator, StreamIteratorExt};
use foundation_core::wire::simple_http::client::{
    body_reader, ClientRequestBuilder, RequestIntro, SimpleHttpClient, SystemDnsResolver,
};

/// GET v1/assetlinks:bulkCheck
/// Send a bundle of statement checks in a single RPC to minimize latency and service load. Statements need not be all for the same source `and/or` target. We recommend using this method when you need to check more than one statement in a short period of time.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `digitalassetlinks_assetlinks_bulk_check_execute()` to send, or `digitalassetlinks_assetlinks_bulk_check` for simplest API.

pub fn digitalassetlinks_assetlinks_bulk_check_builder(
    client: &SimpleHttpClient,
    body: &BulkCheckRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/assetlinks:bulkCheck",);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v1/assetlinks:bulkCheck
/// Send a bundle of statement checks in a single RPC to minimize latency and service load. Statements need not be all for the same source `and/or` target. We recommend using this method when you need to check more than one statement in a short period of time.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `digitalassetlinks_assetlinks_bulk_check_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from digitalassetlinks_assetlinks_bulk_check_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn digitalassetlinks_assetlinks_bulk_check_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<BulkCheckResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: BulkCheckResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/assetlinks:bulkCheck
/// Send a bundle of statement checks in a single RPC to minimize latency and service load. Statements need not be all for the same source `and/or` target. We recommend using this method when you need to check more than one statement in a short period of time.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `digitalassetlinks_assetlinks_bulk_check_builder()` + `digitalassetlinks_assetlinks_bulk_check_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn digitalassetlinks_assetlinks_bulk_check(
    client: &SimpleHttpClient,
    body: &BulkCheckRequest,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<BulkCheckResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = digitalassetlinks_assetlinks_bulk_check_builder(client, body)?;
    digitalassetlinks_assetlinks_bulk_check_execute(builder)
}

/// GET v1/assetlinks:check
/// Determines whether the specified (directional) relationship exists between the specified source and target assets. The relation describes the intent of the link between the two assets as claimed by the source asset. An example for such relationships is the delegation of privileges or permissions. This command is most often used by infrastructure systems to check preconditions for an action. For example, a client may want to know if it is `OK` to send a web URL to a particular mobile app instead. The client can check for the relevant asset link from the website to the mobile app to decide if the operation should be allowed. A note about security: if you specify a secure asset as the source, such as an HTTPS website or an Android app, the API will ensure that any statements used to generate the response have been made in a secure way by the owner of that asset. Conversely, if the source asset is an insecure HTTP website (that is, the URL starts with http:// instead of https://), the API cannot verify its statements securely, and it is not possible to ensure that the website's statements have not been altered by a third party. For more information, see the [Digital Asset Links technical design specification](<https://github.`com/google/digitalassetlinks/blob/master/well-known/details`.md>).
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `digitalassetlinks_assetlinks_check_execute()` to send, or `digitalassetlinks_assetlinks_check` for simplest API.

pub fn digitalassetlinks_assetlinks_check_builder(
    client: &SimpleHttpClient,
    relation: Option<&str>,
    returnRelationExtensions: Option<bool>,
    source_androidApp_certificate_sha256Fingerprint: Option<&str>,
    source_androidApp_packageName: Option<&str>,
    source_web_site: Option<&str>,
    target_androidApp_certificate_sha256Fingerprint: Option<&str>,
    target_androidApp_packageName: Option<&str>,
    target_web_site: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/assetlinks:check",);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "relation",
            relation
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "returnRelationExtensions",
            returnRelationExtensions
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "source_androidApp_certificate_sha256Fingerprint",
            source_androidApp_certificate_sha256Fingerprint
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "source_androidApp_packageName",
            source_androidApp_packageName
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "source_web_site",
            source_web_site
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "target_androidApp_certificate_sha256Fingerprint",
            target_androidApp_certificate_sha256Fingerprint
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "target_androidApp_packageName",
            target_androidApp_packageName
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "target_web_site",
            target_web_site
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET v1/assetlinks:check
/// Determines whether the specified (directional) relationship exists between the specified source and target assets. The relation describes the intent of the link between the two assets as claimed by the source asset. An example for such relationships is the delegation of privileges or permissions. This command is most often used by infrastructure systems to check preconditions for an action. For example, a client may want to know if it is `OK` to send a web URL to a particular mobile app instead. The client can check for the relevant asset link from the website to the mobile app to decide if the operation should be allowed. A note about security: if you specify a secure asset as the source, such as an HTTPS website or an Android app, the API will ensure that any statements used to generate the response have been made in a secure way by the owner of that asset. Conversely, if the source asset is an insecure HTTP website (that is, the URL starts with http:// instead of https://), the API cannot verify its statements securely, and it is not possible to ensure that the website's statements have not been altered by a third party. For more information, see the [Digital Asset Links technical design specification](<https://github.`com/google/digitalassetlinks/blob/master/well-known/details`.md>).
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `digitalassetlinks_assetlinks_check_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from digitalassetlinks_assetlinks_check_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn digitalassetlinks_assetlinks_check_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<CheckResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: CheckResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/assetlinks:check
/// Determines whether the specified (directional) relationship exists between the specified source and target assets. The relation describes the intent of the link between the two assets as claimed by the source asset. An example for such relationships is the delegation of privileges or permissions. This command is most often used by infrastructure systems to check preconditions for an action. For example, a client may want to know if it is `OK` to send a web URL to a particular mobile app instead. The client can check for the relevant asset link from the website to the mobile app to decide if the operation should be allowed. A note about security: if you specify a secure asset as the source, such as an HTTPS website or an Android app, the API will ensure that any statements used to generate the response have been made in a secure way by the owner of that asset. Conversely, if the source asset is an insecure HTTP website (that is, the URL starts with http:// instead of https://), the API cannot verify its statements securely, and it is not possible to ensure that the website's statements have not been altered by a third party. For more information, see the [Digital Asset Links technical design specification](<https://github.`com/google/digitalassetlinks/blob/master/well-known/details`.md>).
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `digitalassetlinks_assetlinks_check_builder()` + `digitalassetlinks_assetlinks_check_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn digitalassetlinks_assetlinks_check(
    client: &SimpleHttpClient,
    relation: Option<&str>,
    returnRelationExtensions: Option<bool>,
    source_androidApp_certificate_sha256Fingerprint: Option<&str>,
    source_androidApp_packageName: Option<&str>,
    source_web_site: Option<&str>,
    target_androidApp_certificate_sha256Fingerprint: Option<&str>,
    target_androidApp_packageName: Option<&str>,
    target_web_site: Option<&str>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<CheckResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = digitalassetlinks_assetlinks_check_builder(
        client,
        relation,
        returnRelationExtensions,
        source_androidApp_certificate_sha256Fingerprint,
        source_androidApp_packageName,
        source_web_site,
        target_androidApp_certificate_sha256Fingerprint,
        target_androidApp_packageName,
        target_web_site,
    )?;
    digitalassetlinks_assetlinks_check_execute(builder)
}

/// GET v1/statements:list
/// Retrieves a list of all statements from a given source that match the specified target and statement string. The API guarantees that all statements with secure source assets, such as HTTPS websites or Android apps, have been made in a secure way by the owner of those assets, as described in the [Digital Asset Links technical design specification](<https://github.`com/google/digitalassetlinks/blob/master/well-known/details`.md>). Specifically, you should consider that for insecure websites (that is, where the URL starts with http:// instead of https://), this guarantee cannot be made. The List command is most useful in cases where the API client wants to know all the ways in which two assets are related, or enumerate all the relationships from a particular source asset. Example: a feature that helps users navigate to related items. When a mobile app is running on a device, the feature would make it easy to navigate to the corresponding web site or Google+ profile.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `digitalassetlinks_statements_list_execute()` to send, or `digitalassetlinks_statements_list` for simplest API.

pub fn digitalassetlinks_statements_list_builder(
    client: &SimpleHttpClient,
    relation: Option<&str>,
    returnRelationExtensions: Option<bool>,
    source_androidApp_certificate_sha256Fingerprint: Option<&str>,
    source_androidApp_packageName: Option<&str>,
    source_web_site: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/statements:list",);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "relation",
            relation
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "returnRelationExtensions",
            returnRelationExtensions
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "source_androidApp_certificate_sha256Fingerprint",
            source_androidApp_certificate_sha256Fingerprint
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "source_androidApp_packageName",
            source_androidApp_packageName
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "source_web_site",
            source_web_site
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET v1/statements:list
/// Retrieves a list of all statements from a given source that match the specified target and statement string. The API guarantees that all statements with secure source assets, such as HTTPS websites or Android apps, have been made in a secure way by the owner of those assets, as described in the [Digital Asset Links technical design specification](<https://github.`com/google/digitalassetlinks/blob/master/well-known/details`.md>). Specifically, you should consider that for insecure websites (that is, where the URL starts with http:// instead of https://), this guarantee cannot be made. The List command is most useful in cases where the API client wants to know all the ways in which two assets are related, or enumerate all the relationships from a particular source asset. Example: a feature that helps users navigate to related items. When a mobile app is running on a device, the feature would make it easy to navigate to the corresponding web site or Google+ profile.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `digitalassetlinks_statements_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from digitalassetlinks_statements_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn digitalassetlinks_statements_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/statements:list
/// Retrieves a list of all statements from a given source that match the specified target and statement string. The API guarantees that all statements with secure source assets, such as HTTPS websites or Android apps, have been made in a secure way by the owner of those assets, as described in the [Digital Asset Links technical design specification](<https://github.`com/google/digitalassetlinks/blob/master/well-known/details`.md>). Specifically, you should consider that for insecure websites (that is, where the URL starts with http:// instead of https://), this guarantee cannot be made. The List command is most useful in cases where the API client wants to know all the ways in which two assets are related, or enumerate all the relationships from a particular source asset. Example: a feature that helps users navigate to related items. When a mobile app is running on a device, the feature would make it easy to navigate to the corresponding web site or Google+ profile.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `digitalassetlinks_statements_list_builder()` + `digitalassetlinks_statements_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn digitalassetlinks_statements_list(
    client: &SimpleHttpClient,
    relation: Option<&str>,
    returnRelationExtensions: Option<bool>,
    source_androidApp_certificate_sha256Fingerprint: Option<&str>,
    source_androidApp_packageName: Option<&str>,
    source_web_site: Option<&str>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = digitalassetlinks_statements_list_builder(
        client,
        relation,
        returnRelationExtensions,
        source_androidApp_certificate_sha256Fingerprint,
        source_androidApp_packageName,
        source_web_site,
    )?;
    digitalassetlinks_statements_list_execute(builder)
}
