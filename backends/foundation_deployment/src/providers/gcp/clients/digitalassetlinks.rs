//! Auto-generated API clients for gcp/digitalassetlinks.
//!
//! Generated by `cargo run --bin ewe_platform gen_provider_clients`.
//! DO NOT EDIT MANUALLY.
//!
//! Feature flag: `gcp`

#![cfg(feature = "gcp")]

use crate::providers::gcp::clients::types::*;
use crate::providers::gcp::resources::*;
use foundation_core::valtron::{
    execute, BoxedSendExecutionAction, StreamIterator, StreamIteratorExt, TaskIterator,
    TaskIteratorExt,
};
use foundation_core::wire::simple_http::client::{
    body_reader, ClientRequestBuilder, DnsResolver, RequestIntro, SimpleHttpClient,
    SystemDnsResolver,
};
use foundation_db::state::resource_identifier::ResourceIdentifier;
use foundation_macros::JsonHash;
use serde::Serialize;

/// POST v1/assetlinks:bulkCheck
/// Send a bundle of statement checks in a single RPC to minimize latency and service load. Statements need not be all for the same source `and/or` target. We recommend using this method when you need to check more than one statement in a short period of time.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `digitalassetlinks_assetlinks_bulk_check_execute()` to send, or `digitalassetlinks_assetlinks_bulk_check` for simplest API.

pub fn digitalassetlinks_assetlinks_bulk_check_builder<R>(
    client: &SimpleHttpClient<R>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!("https://digitalassetlinks.googleapis.com/v1/assetlinks:bulkCheck",);

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1/assetlinks:bulkCheck
/// Send a bundle of statement checks in a single RPC to minimize latency and service load. Statements need not be all for the same source `and/or` target. We recommend using this method when you need to check more than one statement in a short period of time.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `digitalassetlinks_assetlinks_bulk_check_execute()` or `digitalassetlinks_assetlinks_bulk_check`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `digitalassetlinks_assetlinks_bulk_check_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn digitalassetlinks_assetlinks_bulk_check_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<BulkCheckResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: BulkCheckResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1/assetlinks:bulkCheck
/// Send a bundle of statement checks in a single RPC to minimize latency and service load. Statements need not be all for the same source `and/or` target. We recommend using this method when you need to check more than one statement in a short period of time.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `digitalassetlinks_assetlinks_bulk_check_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `digitalassetlinks_assetlinks_bulk_check_task()`.
/// For the simplest API, use `digitalassetlinks_assetlinks_bulk_check()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `digitalassetlinks_assetlinks_bulk_check_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn digitalassetlinks_assetlinks_bulk_check_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<BulkCheckResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = digitalassetlinks_assetlinks_bulk_check_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// POST v1/assetlinks:bulkCheck
/// Send a bundle of statement checks in a single RPC to minimize latency and service load. Statements need not be all for the same source `and/or` target. We recommend using this method when you need to check more than one statement in a short period of time.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `digitalassetlinks_assetlinks_bulk_check_builder()` + `digitalassetlinks_assetlinks_bulk_check_execute()`.
/// For task-level control, use `digitalassetlinks_assetlinks_bulk_check_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn digitalassetlinks_assetlinks_bulk_check(
    client: &SimpleHttpClient,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<BulkCheckResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = digitalassetlinks_assetlinks_bulk_check_builder(client)?;
    digitalassetlinks_assetlinks_bulk_check_execute(builder)
}

/// GET v1/assetlinks:check
/// Determines whether the specified (directional) relationship exists between the specified source and target assets. The relation describes the intent of the link between the two assets as claimed by the source asset. An example for such relationships is the delegation of privileges or permissions. This command is most often used by infrastructure systems to check preconditions for an action. For example, a client may want to know if it is `OK` to send a web URL to a particular mobile app instead. The client can check for the relevant asset link from the website to the mobile app to decide if the operation should be allowed. A note about security: if you specify a secure asset as the source, such as an HTTPS website or an Android app, the API will ensure that any statements used to generate the response have been made in a secure way by the owner of that asset. Conversely, if the source asset is an insecure HTTP website (that is, the URL starts with http:// instead of https://), the API cannot verify its statements securely, and it is not possible to ensure that the website''s statements have not been altered by a third party. For more information, see the [Digital Asset Links technical design specification](<https://github.`com/google/digitalassetlinks/blob/master/well-known/details`.md>).
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `digitalassetlinks_assetlinks_check_execute()` to send, or `digitalassetlinks_assetlinks_check` for simplest API.

pub fn digitalassetlinks_assetlinks_check_builder<R>(
    client: &SimpleHttpClient<R>,
    relation: &Option<Option<String>>,
    returnRelationExtensions: &Option<Option<String>>,
    source_androidApp_certificate_sha256Fingerprint: &Option<Option<String>>,
    source_androidApp_packageName: &Option<Option<String>>,
    source_web_site: &Option<Option<String>>,
    target_androidApp_certificate_sha256Fingerprint: &Option<Option<String>>,
    target_androidApp_packageName: &Option<Option<String>>,
    target_web_site: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!("https://digitalassetlinks.googleapis.com/v1/assetlinks:check",);

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = relation.as_ref() {
        query_parts.push(format!("relation={}", val));
    }
    if let Some(val) = returnRelationExtensions.as_ref() {
        query_parts.push(format!("returnRelationExtensions={}", val));
    }
    if let Some(val) = source_androidApp_certificate_sha256Fingerprint.as_ref() {
        query_parts.push(format!(
            "source.androidApp.certificate.sha256Fingerprint={}",
            val
        ));
    }
    if let Some(val) = source_androidApp_packageName.as_ref() {
        query_parts.push(format!("source.androidApp.packageName={}", val));
    }
    if let Some(val) = source_web_site.as_ref() {
        query_parts.push(format!("source.web.site={}", val));
    }
    if let Some(val) = target_androidApp_certificate_sha256Fingerprint.as_ref() {
        query_parts.push(format!(
            "target.androidApp.certificate.sha256Fingerprint={}",
            val
        ));
    }
    if let Some(val) = target_androidApp_packageName.as_ref() {
        query_parts.push(format!("target.androidApp.packageName={}", val));
    }
    if let Some(val) = target_web_site.as_ref() {
        query_parts.push(format!("target.web.site={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/assetlinks:check
/// Determines whether the specified (directional) relationship exists between the specified source and target assets. The relation describes the intent of the link between the two assets as claimed by the source asset. An example for such relationships is the delegation of privileges or permissions. This command is most often used by infrastructure systems to check preconditions for an action. For example, a client may want to know if it is `OK` to send a web URL to a particular mobile app instead. The client can check for the relevant asset link from the website to the mobile app to decide if the operation should be allowed. A note about security: if you specify a secure asset as the source, such as an HTTPS website or an Android app, the API will ensure that any statements used to generate the response have been made in a secure way by the owner of that asset. Conversely, if the source asset is an insecure HTTP website (that is, the URL starts with http:// instead of https://), the API cannot verify its statements securely, and it is not possible to ensure that the website''s statements have not been altered by a third party. For more information, see the [Digital Asset Links technical design specification](<https://github.`com/google/digitalassetlinks/blob/master/well-known/details`.md>).
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `digitalassetlinks_assetlinks_check_execute()` or `digitalassetlinks_assetlinks_check`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `digitalassetlinks_assetlinks_check_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn digitalassetlinks_assetlinks_check_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<CheckResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: CheckResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/assetlinks:check
/// Determines whether the specified (directional) relationship exists between the specified source and target assets. The relation describes the intent of the link between the two assets as claimed by the source asset. An example for such relationships is the delegation of privileges or permissions. This command is most often used by infrastructure systems to check preconditions for an action. For example, a client may want to know if it is `OK` to send a web URL to a particular mobile app instead. The client can check for the relevant asset link from the website to the mobile app to decide if the operation should be allowed. A note about security: if you specify a secure asset as the source, such as an HTTPS website or an Android app, the API will ensure that any statements used to generate the response have been made in a secure way by the owner of that asset. Conversely, if the source asset is an insecure HTTP website (that is, the URL starts with http:// instead of https://), the API cannot verify its statements securely, and it is not possible to ensure that the website''s statements have not been altered by a third party. For more information, see the [Digital Asset Links technical design specification](<https://github.`com/google/digitalassetlinks/blob/master/well-known/details`.md>).
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `digitalassetlinks_assetlinks_check_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `digitalassetlinks_assetlinks_check_task()`.
/// For the simplest API, use `digitalassetlinks_assetlinks_check()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `digitalassetlinks_assetlinks_check_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn digitalassetlinks_assetlinks_check_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<CheckResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = digitalassetlinks_assetlinks_check_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`digitalassetlinks_assetlinks_check`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DigitalassetlinksAssetlinksCheckArgs {
    /// Query parameter: relation
    pub relation: Option<Option<String>>,
    /// Query parameter: returnRelationExtensions
    pub returnRelationExtensions: Option<Option<String>>,
    /// Query parameter: source_androidApp_certificate_sha256Fingerprint
    pub source_androidApp_certificate_sha256Fingerprint: Option<Option<String>>,
    /// Query parameter: source_androidApp_packageName
    pub source_androidApp_packageName: Option<Option<String>>,
    /// Query parameter: source_web_site
    pub source_web_site: Option<Option<String>>,
    /// Query parameter: target_androidApp_certificate_sha256Fingerprint
    pub target_androidApp_certificate_sha256Fingerprint: Option<Option<String>>,
    /// Query parameter: target_androidApp_packageName
    pub target_androidApp_packageName: Option<Option<String>>,
    /// Query parameter: target_web_site
    pub target_web_site: Option<Option<String>>,
}

/// GET v1/assetlinks:check
/// Determines whether the specified (directional) relationship exists between the specified source and target assets. The relation describes the intent of the link between the two assets as claimed by the source asset. An example for such relationships is the delegation of privileges or permissions. This command is most often used by infrastructure systems to check preconditions for an action. For example, a client may want to know if it is `OK` to send a web URL to a particular mobile app instead. The client can check for the relevant asset link from the website to the mobile app to decide if the operation should be allowed. A note about security: if you specify a secure asset as the source, such as an HTTPS website or an Android app, the API will ensure that any statements used to generate the response have been made in a secure way by the owner of that asset. Conversely, if the source asset is an insecure HTTP website (that is, the URL starts with http:// instead of https://), the API cannot verify its statements securely, and it is not possible to ensure that the website''s statements have not been altered by a third party. For more information, see the [Digital Asset Links technical design specification](<https://github.`com/google/digitalassetlinks/blob/master/well-known/details`.md>).
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `digitalassetlinks_assetlinks_check_builder()` + `digitalassetlinks_assetlinks_check_execute()`.
/// For task-level control, use `digitalassetlinks_assetlinks_check_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn digitalassetlinks_assetlinks_check(
    client: &SimpleHttpClient,
    args: &DigitalassetlinksAssetlinksCheckArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<CheckResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = digitalassetlinks_assetlinks_check_builder(
        client,
        &args.relation,
        &args.returnRelationExtensions,
        &args.source_androidApp_certificate_sha256Fingerprint,
        &args.source_androidApp_packageName,
        &args.source_web_site,
        &args.target_androidApp_certificate_sha256Fingerprint,
        &args.target_androidApp_packageName,
        &args.target_web_site,
    )?;
    digitalassetlinks_assetlinks_check_execute(builder)
}

/// GET v1/statements:list
/// Retrieves a list of all statements from a given source that match the specified target and statement string. The API guarantees that all statements with secure source assets, such as HTTPS websites or Android apps, have been made in a secure way by the owner of those assets, as described in the [Digital Asset Links technical design specification](<https://github.`com/google/digitalassetlinks/blob/master/well-known/details`.md>). Specifically, you should consider that for insecure websites (that is, where the URL starts with http:// instead of https://), this guarantee cannot be made. The List command is most useful in cases where the API client wants to know all the ways in which two assets are related, or enumerate all the relationships from a particular source asset. Example: a feature that helps users navigate to related items. When a mobile app is running on a device, the feature would make it easy to navigate to the corresponding web site or Google+ profile.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `digitalassetlinks_statements_list_execute()` to send, or `digitalassetlinks_statements_list` for simplest API.

pub fn digitalassetlinks_statements_list_builder<R>(
    client: &SimpleHttpClient<R>,
    relation: &Option<Option<String>>,
    returnRelationExtensions: &Option<Option<String>>,
    source_androidApp_certificate_sha256Fingerprint: &Option<Option<String>>,
    source_androidApp_packageName: &Option<Option<String>>,
    source_web_site: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!("https://digitalassetlinks.googleapis.com/v1/statements:list",);

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = relation.as_ref() {
        query_parts.push(format!("relation={}", val));
    }
    if let Some(val) = returnRelationExtensions.as_ref() {
        query_parts.push(format!("returnRelationExtensions={}", val));
    }
    if let Some(val) = source_androidApp_certificate_sha256Fingerprint.as_ref() {
        query_parts.push(format!(
            "source.androidApp.certificate.sha256Fingerprint={}",
            val
        ));
    }
    if let Some(val) = source_androidApp_packageName.as_ref() {
        query_parts.push(format!("source.androidApp.packageName={}", val));
    }
    if let Some(val) = source_web_site.as_ref() {
        query_parts.push(format!("source.web.site={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/statements:list
/// Retrieves a list of all statements from a given source that match the specified target and statement string. The API guarantees that all statements with secure source assets, such as HTTPS websites or Android apps, have been made in a secure way by the owner of those assets, as described in the [Digital Asset Links technical design specification](<https://github.`com/google/digitalassetlinks/blob/master/well-known/details`.md>). Specifically, you should consider that for insecure websites (that is, where the URL starts with http:// instead of https://), this guarantee cannot be made. The List command is most useful in cases where the API client wants to know all the ways in which two assets are related, or enumerate all the relationships from a particular source asset. Example: a feature that helps users navigate to related items. When a mobile app is running on a device, the feature would make it easy to navigate to the corresponding web site or Google+ profile.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `digitalassetlinks_statements_list_execute()` or `digitalassetlinks_statements_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `digitalassetlinks_statements_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn digitalassetlinks_statements_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ListResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/statements:list
/// Retrieves a list of all statements from a given source that match the specified target and statement string. The API guarantees that all statements with secure source assets, such as HTTPS websites or Android apps, have been made in a secure way by the owner of those assets, as described in the [Digital Asset Links technical design specification](<https://github.`com/google/digitalassetlinks/blob/master/well-known/details`.md>). Specifically, you should consider that for insecure websites (that is, where the URL starts with http:// instead of https://), this guarantee cannot be made. The List command is most useful in cases where the API client wants to know all the ways in which two assets are related, or enumerate all the relationships from a particular source asset. Example: a feature that helps users navigate to related items. When a mobile app is running on a device, the feature would make it easy to navigate to the corresponding web site or Google+ profile.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `digitalassetlinks_statements_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `digitalassetlinks_statements_list_task()`.
/// For the simplest API, use `digitalassetlinks_statements_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `digitalassetlinks_statements_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn digitalassetlinks_statements_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = digitalassetlinks_statements_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`digitalassetlinks_statements_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DigitalassetlinksStatementsListArgs {
    /// Query parameter: relation
    pub relation: Option<Option<String>>,
    /// Query parameter: returnRelationExtensions
    pub returnRelationExtensions: Option<Option<String>>,
    /// Query parameter: source_androidApp_certificate_sha256Fingerprint
    pub source_androidApp_certificate_sha256Fingerprint: Option<Option<String>>,
    /// Query parameter: source_androidApp_packageName
    pub source_androidApp_packageName: Option<Option<String>>,
    /// Query parameter: source_web_site
    pub source_web_site: Option<Option<String>>,
}

/// GET v1/statements:list
/// Retrieves a list of all statements from a given source that match the specified target and statement string. The API guarantees that all statements with secure source assets, such as HTTPS websites or Android apps, have been made in a secure way by the owner of those assets, as described in the [Digital Asset Links technical design specification](<https://github.`com/google/digitalassetlinks/blob/master/well-known/details`.md>). Specifically, you should consider that for insecure websites (that is, where the URL starts with http:// instead of https://), this guarantee cannot be made. The List command is most useful in cases where the API client wants to know all the ways in which two assets are related, or enumerate all the relationships from a particular source asset. Example: a feature that helps users navigate to related items. When a mobile app is running on a device, the feature would make it easy to navigate to the corresponding web site or Google+ profile.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `digitalassetlinks_statements_list_builder()` + `digitalassetlinks_statements_list_execute()`.
/// For task-level control, use `digitalassetlinks_statements_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn digitalassetlinks_statements_list(
    client: &SimpleHttpClient,
    args: &DigitalassetlinksStatementsListArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = digitalassetlinks_statements_list_builder(
        client,
        &args.relation,
        &args.returnRelationExtensions,
        &args.source_androidApp_certificate_sha256Fingerprint,
        &args.source_androidApp_packageName,
        &args.source_web_site,
    )?;
    digitalassetlinks_statements_list_execute(builder)
}

// =============================================================================
// ResourceIdentifier implementation for BulkCheckResponse
// =============================================================================

/// ResourceIdentifier implementation for BulkCheckResponse with DigitalassetlinksAssetlinksBulkCheckArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DigitalassetlinksAssetlinksBulkCheckArgs> for BulkCheckResponse {
    fn generate_resource_id(&self, input: &DigitalassetlinksAssetlinksBulkCheckArgs) -> String {
        "gcp::digitalassetlinks::BulkCheckResponse".to_string()
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::digitalassetlinks::BulkCheckResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for CheckResponse
// =============================================================================

/// ResourceIdentifier implementation for CheckResponse with DigitalassetlinksAssetlinksCheckArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DigitalassetlinksAssetlinksCheckArgs> for CheckResponse {
    fn generate_resource_id(&self, input: &DigitalassetlinksAssetlinksCheckArgs) -> String {
        "gcp::digitalassetlinks::CheckResponse".to_string()
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::digitalassetlinks::CheckResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for ListResponse
// =============================================================================

/// ResourceIdentifier implementation for ListResponse with DigitalassetlinksStatementsListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DigitalassetlinksStatementsListArgs> for ListResponse {
    fn generate_resource_id(&self, input: &DigitalassetlinksStatementsListArgs) -> String {
        "gcp::digitalassetlinks::ListResponse".to_string()
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::digitalassetlinks::ListResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}
