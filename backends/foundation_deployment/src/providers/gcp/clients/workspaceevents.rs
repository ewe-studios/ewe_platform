//! Auto-generated API clients for gcp/workspaceevents.
//!
//! Generated by `cargo run --bin ewe_platform gen_provider_clients`.
//! DO NOT EDIT MANUALLY.

use crate::providers::gcp::clients::types::*;
use crate::providers::gcp::resources::*;
use foundation_core::valtron::{execute, StreamIterator, StreamIteratorExt};
use foundation_core::wire::simple_http::client::{
    body_reader, ClientRequestBuilder, RequestIntro, SimpleHttpClient, SystemDnsResolver,
};

/// GET v1/message:stream
/// SendStreamingMessage is a streaming call that will return a stream of task update events until the Task is in an interrupted or terminal state.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `workspaceevents_message_stream_execute()` to send, or `workspaceevents_message_stream` for simplest API.

pub fn workspaceevents_message_stream_builder(
    client: &SimpleHttpClient,
    body: &SendMessageRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/message:stream",);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v1/message:stream
/// SendStreamingMessage is a streaming call that will return a stream of task update events until the Task is in an interrupted or terminal state.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `workspaceevents_message_stream_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from workspaceevents_message_stream_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn workspaceevents_message_stream_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<StreamResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: StreamResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/message:stream
/// SendStreamingMessage is a streaming call that will return a stream of task update events until the Task is in an interrupted or terminal state.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `workspaceevents_message_stream_builder()` + `workspaceevents_message_stream_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn workspaceevents_message_stream(
    client: &SimpleHttpClient,
    body: &SendMessageRequest,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<StreamResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = workspaceevents_message_stream_builder(client, body)?;
    workspaceevents_message_stream_execute(builder)
}

/// GET v1/{+name}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `workspaceevents_operations_get_execute()` to send, or `workspaceevents_operations_get` for simplest API.

pub fn workspaceevents_operations_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/{+name}", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+name}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `workspaceevents_operations_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from workspaceevents_operations_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn workspaceevents_operations_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+name}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `workspaceevents_operations_get_builder()` + `workspaceevents_operations_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn workspaceevents_operations_get(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = workspaceevents_operations_get_builder(client, name)?;
    workspaceevents_operations_get_execute(builder)
}

/// GET v1/subscriptions
/// Creates a Google Workspace subscription. To learn how to use this method, see [Create a Google Workspace subscription](<https://developers.google.`com/workspace/events/guides/create-subscription`>). For a subscription on a [Chat target resource](<https://developers.google.`com/workspace/events/guides/events-chat`>), you can create a subscription as: - A Chat app by specifying an authorization scope that begins with chat.app and getting one-time administrator approval ([Developer Preview](<https://developers.google.`com/workspace/preview`>)). To learn more, see [Authorize as a Chat app with administrator approval](<https://developers.google.`com/workspace/chat/authenticate-authorize-chat-app`>). - A user by specifying an authorization scope that doesn't include app in its name. To learn more, see [Authorize as a Chat user](<https://developers.google.`com/workspace/chat/authenticate-authorize-chat-user`>).
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `workspaceevents_subscriptions_create_execute()` to send, or `workspaceevents_subscriptions_create` for simplest API.

pub fn workspaceevents_subscriptions_create_builder(
    client: &SimpleHttpClient,
    validateOnly: Option<bool>,
    body: &Subscription,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/subscriptions",);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "validateOnly",
            validateOnly
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v1/subscriptions
/// Creates a Google Workspace subscription. To learn how to use this method, see [Create a Google Workspace subscription](<https://developers.google.`com/workspace/events/guides/create-subscription`>). For a subscription on a [Chat target resource](<https://developers.google.`com/workspace/events/guides/events-chat`>), you can create a subscription as: - A Chat app by specifying an authorization scope that begins with chat.app and getting one-time administrator approval ([Developer Preview](<https://developers.google.`com/workspace/preview`>)). To learn more, see [Authorize as a Chat app with administrator approval](<https://developers.google.`com/workspace/chat/authenticate-authorize-chat-app`>). - A user by specifying an authorization scope that doesn't include app in its name. To learn more, see [Authorize as a Chat user](<https://developers.google.`com/workspace/chat/authenticate-authorize-chat-user`>).
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `workspaceevents_subscriptions_create_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from workspaceevents_subscriptions_create_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn workspaceevents_subscriptions_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/subscriptions
/// Creates a Google Workspace subscription. To learn how to use this method, see [Create a Google Workspace subscription](<https://developers.google.`com/workspace/events/guides/create-subscription`>). For a subscription on a [Chat target resource](<https://developers.google.`com/workspace/events/guides/events-chat`>), you can create a subscription as: - A Chat app by specifying an authorization scope that begins with chat.app and getting one-time administrator approval ([Developer Preview](<https://developers.google.`com/workspace/preview`>)). To learn more, see [Authorize as a Chat app with administrator approval](<https://developers.google.`com/workspace/chat/authenticate-authorize-chat-app`>). - A user by specifying an authorization scope that doesn't include app in its name. To learn more, see [Authorize as a Chat user](<https://developers.google.`com/workspace/chat/authenticate-authorize-chat-user`>).
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `workspaceevents_subscriptions_create_builder()` + `workspaceevents_subscriptions_create_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn workspaceevents_subscriptions_create(
    client: &SimpleHttpClient,
    validateOnly: Option<bool>,
    body: &Subscription,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = workspaceevents_subscriptions_create_builder(client, validateOnly, body)?;
    workspaceevents_subscriptions_create_execute(builder)
}

/// GET v1/{+name}
/// Deletes a Google Workspace subscription. To learn how to use this method, see [Delete a Google Workspace subscription](<https://developers.google.`com/workspace/events/guides/delete-subscription`>).
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `workspaceevents_subscriptions_delete_execute()` to send, or `workspaceevents_subscriptions_delete` for simplest API.

pub fn workspaceevents_subscriptions_delete_builder(
    client: &SimpleHttpClient,
    name: &str,
    allowMissing: Option<bool>,
    etag: Option<&str>,
    validateOnly: Option<bool>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/{+name}", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "allowMissing",
            allowMissing
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "etag",
            etag.as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "validateOnly",
            validateOnly
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET v1/{+name}
/// Deletes a Google Workspace subscription. To learn how to use this method, see [Delete a Google Workspace subscription](<https://developers.google.`com/workspace/events/guides/delete-subscription`>).
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `workspaceevents_subscriptions_delete_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from workspaceevents_subscriptions_delete_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn workspaceevents_subscriptions_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+name}
/// Deletes a Google Workspace subscription. To learn how to use this method, see [Delete a Google Workspace subscription](<https://developers.google.`com/workspace/events/guides/delete-subscription`>).
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `workspaceevents_subscriptions_delete_builder()` + `workspaceevents_subscriptions_delete_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn workspaceevents_subscriptions_delete(
    client: &SimpleHttpClient,
    name: &str,
    allowMissing: Option<bool>,
    etag: Option<&str>,
    validateOnly: Option<bool>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = workspaceevents_subscriptions_delete_builder(
        client,
        name,
        allowMissing,
        etag,
        validateOnly,
    )?;
    workspaceevents_subscriptions_delete_execute(builder)
}

/// GET v1/{+name}
/// Gets details about a Google Workspace subscription. To learn how to use this method, see [Get details about a Google Workspace subscription](<https://developers.google.`com/workspace/events/guides/get-subscription`>).
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `workspaceevents_subscriptions_get_execute()` to send, or `workspaceevents_subscriptions_get` for simplest API.

pub fn workspaceevents_subscriptions_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/{+name}", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+name}
/// Gets details about a Google Workspace subscription. To learn how to use this method, see [Get details about a Google Workspace subscription](<https://developers.google.`com/workspace/events/guides/get-subscription`>).
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `workspaceevents_subscriptions_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from workspaceevents_subscriptions_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn workspaceevents_subscriptions_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Subscription>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Subscription = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+name}
/// Gets details about a Google Workspace subscription. To learn how to use this method, see [Get details about a Google Workspace subscription](<https://developers.google.`com/workspace/events/guides/get-subscription`>).
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `workspaceevents_subscriptions_get_builder()` + `workspaceevents_subscriptions_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn workspaceevents_subscriptions_get(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Subscription>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = workspaceevents_subscriptions_get_builder(client, name)?;
    workspaceevents_subscriptions_get_execute(builder)
}

/// GET v1/subscriptions
/// Lists Google Workspace subscriptions. To learn how to use this method, see [List Google Workspace subscriptions](<https://developers.google.`com/workspace/events/guides/list-subscriptions`>).
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `workspaceevents_subscriptions_list_execute()` to send, or `workspaceevents_subscriptions_list` for simplest API.

pub fn workspaceevents_subscriptions_list_builder(
    client: &SimpleHttpClient,
    filter: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/subscriptions",);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "filter",
            filter
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageSize",
            pageSize
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageToken",
            pageToken
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET v1/subscriptions
/// Lists Google Workspace subscriptions. To learn how to use this method, see [List Google Workspace subscriptions](<https://developers.google.`com/workspace/events/guides/list-subscriptions`>).
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `workspaceevents_subscriptions_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from workspaceevents_subscriptions_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn workspaceevents_subscriptions_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListSubscriptionsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListSubscriptionsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/subscriptions
/// Lists Google Workspace subscriptions. To learn how to use this method, see [List Google Workspace subscriptions](<https://developers.google.`com/workspace/events/guides/list-subscriptions`>).
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `workspaceevents_subscriptions_list_builder()` + `workspaceevents_subscriptions_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn workspaceevents_subscriptions_list(
    client: &SimpleHttpClient,
    filter: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListSubscriptionsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = workspaceevents_subscriptions_list_builder(client, filter, pageSize, pageToken)?;
    workspaceevents_subscriptions_list_execute(builder)
}

/// GET v1/{+name}
/// Updates or renews a Google Workspace subscription. To learn how to use this method, see [Update or renew a Google Workspace subscription](<https://developers.google.`com/workspace/events/guides/update-subscription`>). For a subscription on a [Chat target resource](<https://developers.google.`com/workspace/events/guides/events-chat`>), you can update a subscription as: - A Chat app by specifying an authorization scope that begins with chat.app andgetting one-time administrator approval ([Developer Preview](<https://developers.google.`com/workspace/preview`>)). To learn more, see [Authorize as a Chat app with administrator approval](<https://developers.google.`com/workspace/chat/authenticate-authorize-chat-app`>). - A user by specifying an authorization scope that doesn't include app in its name. To learn more, see [Authorize as a Chat user](<https://developers.google.`com/workspace/chat/authenticate-authorize-chat-user`>).
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `workspaceevents_subscriptions_patch_execute()` to send, or `workspaceevents_subscriptions_patch` for simplest API.

pub fn workspaceevents_subscriptions_patch_builder(
    client: &SimpleHttpClient,
    name: &str,
    updateMask: Option<&str>,
    validateOnly: Option<bool>,
    body: &Subscription,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/{+name}", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "updateMask",
            updateMask
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "validateOnly",
            validateOnly
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v1/{+name}
/// Updates or renews a Google Workspace subscription. To learn how to use this method, see [Update or renew a Google Workspace subscription](<https://developers.google.`com/workspace/events/guides/update-subscription`>). For a subscription on a [Chat target resource](<https://developers.google.`com/workspace/events/guides/events-chat`>), you can update a subscription as: - A Chat app by specifying an authorization scope that begins with chat.app andgetting one-time administrator approval ([Developer Preview](<https://developers.google.`com/workspace/preview`>)). To learn more, see [Authorize as a Chat app with administrator approval](<https://developers.google.`com/workspace/chat/authenticate-authorize-chat-app`>). - A user by specifying an authorization scope that doesn't include app in its name. To learn more, see [Authorize as a Chat user](<https://developers.google.`com/workspace/chat/authenticate-authorize-chat-user`>).
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `workspaceevents_subscriptions_patch_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from workspaceevents_subscriptions_patch_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn workspaceevents_subscriptions_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+name}
/// Updates or renews a Google Workspace subscription. To learn how to use this method, see [Update or renew a Google Workspace subscription](<https://developers.google.`com/workspace/events/guides/update-subscription`>). For a subscription on a [Chat target resource](<https://developers.google.`com/workspace/events/guides/events-chat`>), you can update a subscription as: - A Chat app by specifying an authorization scope that begins with chat.app andgetting one-time administrator approval ([Developer Preview](<https://developers.google.`com/workspace/preview`>)). To learn more, see [Authorize as a Chat app with administrator approval](<https://developers.google.`com/workspace/chat/authenticate-authorize-chat-app`>). - A user by specifying an authorization scope that doesn't include app in its name. To learn more, see [Authorize as a Chat user](<https://developers.google.`com/workspace/chat/authenticate-authorize-chat-user`>).
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `workspaceevents_subscriptions_patch_builder()` + `workspaceevents_subscriptions_patch_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn workspaceevents_subscriptions_patch(
    client: &SimpleHttpClient,
    name: &str,
    updateMask: Option<&str>,
    validateOnly: Option<bool>,
    body: &Subscription,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        workspaceevents_subscriptions_patch_builder(client, name, updateMask, validateOnly, body)?;
    workspaceevents_subscriptions_patch_execute(builder)
}

/// GET v1/{+name}:reactivate
/// Reactivates a suspended Google Workspace subscription. This method resets your subscription's State field to `ACTIVE`. Before you use this method, you must fix the error that suspended the subscription. This method will ignore or reject any subscription that isn't currently in a suspended state. To learn how to use this method, see [Reactivate a Google Workspace subscription](<https://developers.google.`com/workspace/events/guides/reactivate-subscription`>). For a subscription on a [Chat target resource](<https://developers.google.`com/workspace/events/guides/events-chat`>), you can reactivate a subscription as: - A Chat app by specifying an authorization scope that begins with chat.app andgetting one-time administrator approval ([Developer Preview](<https://developers.google.`com/workspace/preview`>)). To learn more, see [Authorize as a Chat app with administrator approval](<https://developers.google.`com/workspace/chat/authenticate-authorize-chat-app`>). - A user by specifying an authorization scope that doesn't include app in its name. To learn more, see [Authorize as a Chat user](<https://developers.google.`com/workspace/chat/authenticate-authorize-chat-user`>).
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `workspaceevents_subscriptions_reactivate_execute()` to send, or `workspaceevents_subscriptions_reactivate` for simplest API.

pub fn workspaceevents_subscriptions_reactivate_builder(
    client: &SimpleHttpClient,
    name: &str,
    body: &ReactivateSubscriptionRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/{+name}:reactivate", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v1/{+name}:reactivate
/// Reactivates a suspended Google Workspace subscription. This method resets your subscription's State field to `ACTIVE`. Before you use this method, you must fix the error that suspended the subscription. This method will ignore or reject any subscription that isn't currently in a suspended state. To learn how to use this method, see [Reactivate a Google Workspace subscription](<https://developers.google.`com/workspace/events/guides/reactivate-subscription`>). For a subscription on a [Chat target resource](<https://developers.google.`com/workspace/events/guides/events-chat`>), you can reactivate a subscription as: - A Chat app by specifying an authorization scope that begins with chat.app andgetting one-time administrator approval ([Developer Preview](<https://developers.google.`com/workspace/preview`>)). To learn more, see [Authorize as a Chat app with administrator approval](<https://developers.google.`com/workspace/chat/authenticate-authorize-chat-app`>). - A user by specifying an authorization scope that doesn't include app in its name. To learn more, see [Authorize as a Chat user](<https://developers.google.`com/workspace/chat/authenticate-authorize-chat-user`>).
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `workspaceevents_subscriptions_reactivate_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from workspaceevents_subscriptions_reactivate_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn workspaceevents_subscriptions_reactivate_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+name}:reactivate
/// Reactivates a suspended Google Workspace subscription. This method resets your subscription's State field to `ACTIVE`. Before you use this method, you must fix the error that suspended the subscription. This method will ignore or reject any subscription that isn't currently in a suspended state. To learn how to use this method, see [Reactivate a Google Workspace subscription](<https://developers.google.`com/workspace/events/guides/reactivate-subscription`>). For a subscription on a [Chat target resource](<https://developers.google.`com/workspace/events/guides/events-chat`>), you can reactivate a subscription as: - A Chat app by specifying an authorization scope that begins with chat.app andgetting one-time administrator approval ([Developer Preview](<https://developers.google.`com/workspace/preview`>)). To learn more, see [Authorize as a Chat app with administrator approval](<https://developers.google.`com/workspace/chat/authenticate-authorize-chat-app`>). - A user by specifying an authorization scope that doesn't include app in its name. To learn more, see [Authorize as a Chat user](<https://developers.google.`com/workspace/chat/authenticate-authorize-chat-user`>).
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `workspaceevents_subscriptions_reactivate_builder()` + `workspaceevents_subscriptions_reactivate_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn workspaceevents_subscriptions_reactivate(
    client: &SimpleHttpClient,
    name: &str,
    body: &ReactivateSubscriptionRequest,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = workspaceevents_subscriptions_reactivate_builder(client, name, body)?;
    workspaceevents_subscriptions_reactivate_execute(builder)
}

/// GET v1/{+name}:cancel
/// Cancel a task from the agent. If supported one should expect no more task updates for the task.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `workspaceevents_tasks_cancel_execute()` to send, or `workspaceevents_tasks_cancel` for simplest API.

pub fn workspaceevents_tasks_cancel_builder(
    client: &SimpleHttpClient,
    name: &str,
    body: &CancelTaskRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/{+name}:cancel", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v1/{+name}:cancel
/// Cancel a task from the agent. If supported one should expect no more task updates for the task.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `workspaceevents_tasks_cancel_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from workspaceevents_tasks_cancel_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn workspaceevents_tasks_cancel_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Task>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Task = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+name}:cancel
/// Cancel a task from the agent. If supported one should expect no more task updates for the task.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `workspaceevents_tasks_cancel_builder()` + `workspaceevents_tasks_cancel_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn workspaceevents_tasks_cancel(
    client: &SimpleHttpClient,
    name: &str,
    body: &CancelTaskRequest,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Task>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = workspaceevents_tasks_cancel_builder(client, name, body)?;
    workspaceevents_tasks_cancel_execute(builder)
}

/// GET v1/{+name}
/// Get the current state of a task from the agent.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `workspaceevents_tasks_get_execute()` to send, or `workspaceevents_tasks_get` for simplest API.

pub fn workspaceevents_tasks_get_builder(
    client: &SimpleHttpClient,
    name: &str,
    historyLength: Option<i32>,
    tenant: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/{+name}", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "historyLength",
            historyLength
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "tenant",
            tenant
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET v1/{+name}
/// Get the current state of a task from the agent.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `workspaceevents_tasks_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from workspaceevents_tasks_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn workspaceevents_tasks_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Task>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Task = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+name}
/// Get the current state of a task from the agent.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `workspaceevents_tasks_get_builder()` + `workspaceevents_tasks_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn workspaceevents_tasks_get(
    client: &SimpleHttpClient,
    name: &str,
    historyLength: Option<i32>,
    tenant: Option<&str>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Task>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = workspaceevents_tasks_get_builder(client, name, historyLength, tenant)?;
    workspaceevents_tasks_get_execute(builder)
}

/// GET v1/{+name}:subscribe
/// TaskSubscription is a streaming call that will return a stream of task update events. This attaches the stream to an existing in process task. If the task is complete the stream will return the completed task (like GetTask) and close the stream.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `workspaceevents_tasks_subscribe_execute()` to send, or `workspaceevents_tasks_subscribe` for simplest API.

pub fn workspaceevents_tasks_subscribe_builder(
    client: &SimpleHttpClient,
    name: &str,
    tenant: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/{+name}:subscribe", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "tenant",
            tenant
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET v1/{+name}:subscribe
/// TaskSubscription is a streaming call that will return a stream of task update events. This attaches the stream to an existing in process task. If the task is complete the stream will return the completed task (like GetTask) and close the stream.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `workspaceevents_tasks_subscribe_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from workspaceevents_tasks_subscribe_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn workspaceevents_tasks_subscribe_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<StreamResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: StreamResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+name}:subscribe
/// TaskSubscription is a streaming call that will return a stream of task update events. This attaches the stream to an existing in process task. If the task is complete the stream will return the completed task (like GetTask) and close the stream.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `workspaceevents_tasks_subscribe_builder()` + `workspaceevents_tasks_subscribe_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn workspaceevents_tasks_subscribe(
    client: &SimpleHttpClient,
    name: &str,
    tenant: Option<&str>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<StreamResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = workspaceevents_tasks_subscribe_builder(client, name, tenant)?;
    workspaceevents_tasks_subscribe_execute(builder)
}

/// GET v1/{+parent}
/// Set a push notification config for a task.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `workspaceevents_tasks_push_notification_configs_create_execute()` to send, or `workspaceevents_tasks_push_notification_configs_create` for simplest API.

pub fn workspaceevents_tasks_push_notification_configs_create_builder(
    client: &SimpleHttpClient,
    parent: &str,
    configId: Option<&str>,
    tenant: Option<&str>,
    body: &TaskPushNotificationConfig,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/{+parent}", parent,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "configId",
            configId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "tenant",
            tenant
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v1/{+parent}
/// Set a push notification config for a task.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `workspaceevents_tasks_push_notification_configs_create_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from workspaceevents_tasks_push_notification_configs_create_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn workspaceevents_tasks_push_notification_configs_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<TaskPushNotificationConfig>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: TaskPushNotificationConfig = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+parent}
/// Set a push notification config for a task.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `workspaceevents_tasks_push_notification_configs_create_builder()` + `workspaceevents_tasks_push_notification_configs_create_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn workspaceevents_tasks_push_notification_configs_create(
    client: &SimpleHttpClient,
    parent: &str,
    configId: Option<&str>,
    tenant: Option<&str>,
    body: &TaskPushNotificationConfig,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<TaskPushNotificationConfig>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = workspaceevents_tasks_push_notification_configs_create_builder(
        client, parent, configId, tenant, body,
    )?;
    workspaceevents_tasks_push_notification_configs_create_execute(builder)
}

/// GET v1/{+name}
/// Delete a push notification config for a task.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `workspaceevents_tasks_push_notification_configs_delete_execute()` to send, or `workspaceevents_tasks_push_notification_configs_delete` for simplest API.

pub fn workspaceevents_tasks_push_notification_configs_delete_builder(
    client: &SimpleHttpClient,
    name: &str,
    tenant: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/{+name}", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "tenant",
            tenant
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET v1/{+name}
/// Delete a push notification config for a task.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `workspaceevents_tasks_push_notification_configs_delete_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from workspaceevents_tasks_push_notification_configs_delete_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn workspaceevents_tasks_push_notification_configs_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Empty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+name}
/// Delete a push notification config for a task.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `workspaceevents_tasks_push_notification_configs_delete_builder()` + `workspaceevents_tasks_push_notification_configs_delete_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn workspaceevents_tasks_push_notification_configs_delete(
    client: &SimpleHttpClient,
    name: &str,
    tenant: Option<&str>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        workspaceevents_tasks_push_notification_configs_delete_builder(client, name, tenant)?;
    workspaceevents_tasks_push_notification_configs_delete_execute(builder)
}

/// GET v1/{+name}
/// Get a push notification config for a task.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `workspaceevents_tasks_push_notification_configs_get_execute()` to send, or `workspaceevents_tasks_push_notification_configs_get` for simplest API.

pub fn workspaceevents_tasks_push_notification_configs_get_builder(
    client: &SimpleHttpClient,
    name: &str,
    tenant: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/{+name}", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "tenant",
            tenant
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET v1/{+name}
/// Get a push notification config for a task.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `workspaceevents_tasks_push_notification_configs_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from workspaceevents_tasks_push_notification_configs_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn workspaceevents_tasks_push_notification_configs_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<TaskPushNotificationConfig>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: TaskPushNotificationConfig = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+name}
/// Get a push notification config for a task.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `workspaceevents_tasks_push_notification_configs_get_builder()` + `workspaceevents_tasks_push_notification_configs_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn workspaceevents_tasks_push_notification_configs_get(
    client: &SimpleHttpClient,
    name: &str,
    tenant: Option<&str>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<TaskPushNotificationConfig>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        workspaceevents_tasks_push_notification_configs_get_builder(client, name, tenant)?;
    workspaceevents_tasks_push_notification_configs_get_execute(builder)
}

/// GET v1/{+parent}/pushNotificationConfigs
/// Get a list of push notifications configured for a task.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `workspaceevents_tasks_push_notification_configs_list_execute()` to send, or `workspaceevents_tasks_push_notification_configs_list` for simplest API.

pub fn workspaceevents_tasks_push_notification_configs_list_builder(
    client: &SimpleHttpClient,
    parent: &str,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
    tenant: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comv1/{+parent}/pushNotificationConfigs",
        parent,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "pageSize",
            pageSize
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageToken",
            pageToken
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "tenant",
            tenant
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET v1/{+parent}/pushNotificationConfigs
/// Get a list of push notifications configured for a task.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `workspaceevents_tasks_push_notification_configs_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from workspaceevents_tasks_push_notification_configs_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn workspaceevents_tasks_push_notification_configs_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ListTaskPushNotificationConfigResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListTaskPushNotificationConfigResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+parent}/pushNotificationConfigs
/// Get a list of push notifications configured for a task.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `workspaceevents_tasks_push_notification_configs_list_builder()` + `workspaceevents_tasks_push_notification_configs_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn workspaceevents_tasks_push_notification_configs_list(
    client: &SimpleHttpClient,
    parent: &str,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
    tenant: Option<&str>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ListTaskPushNotificationConfigResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = workspaceevents_tasks_push_notification_configs_list_builder(
        client, parent, pageSize, pageToken, tenant,
    )?;
    workspaceevents_tasks_push_notification_configs_list_execute(builder)
}
