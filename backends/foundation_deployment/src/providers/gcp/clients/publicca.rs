//! Auto-generated API clients for gcp/publicca.
//!
//! Generated by `cargo run --bin ewe_platform gen_provider_clients`.
//! DO NOT EDIT MANUALLY.
//!
//! Feature flag: `gcp`

#![cfg(feature = "gcp")]

use crate::providers::gcp::clients::types::*;
use crate::providers::gcp::resources::*;
use foundation_core::valtron::{
    execute, BoxedSendExecutionAction, StreamIterator, StreamIteratorExt, TaskIterator,
    TaskIteratorExt,
};
use foundation_core::wire::simple_http::client::{
    body_reader, ClientRequestBuilder, DnsResolver, RequestIntro, SimpleHttpClient,
    SystemDnsResolver,
};
use foundation_db::state::resource_identifier::ResourceIdentifier;
use foundation_macros::JsonHash;
use serde::Serialize;

/// POST v1/projects/{projectsId}/locations/{locationsId}/externalAccountKeys
/// Creates a new ExternalAccountKey bound to the project.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `publicca_projects_locations_external_account_keys_create_execute()` to send, or `publicca_projects_locations_external_account_keys_create` for simplest API.

pub fn publicca_projects_locations_external_account_keys_create_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://publicca.googleapis.com/v1/projects/{}/locations/{locationsId}/externalAccountKeys",
        parent,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/externalAccountKeys
/// Creates a new ExternalAccountKey bound to the project.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `publicca_projects_locations_external_account_keys_create_execute()` or `publicca_projects_locations_external_account_keys_create`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `publicca_projects_locations_external_account_keys_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn publicca_projects_locations_external_account_keys_create_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ExternalAccountKey>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ExternalAccountKey = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/externalAccountKeys
/// Creates a new ExternalAccountKey bound to the project.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `publicca_projects_locations_external_account_keys_create_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `publicca_projects_locations_external_account_keys_create_task()`.
/// For the simplest API, use `publicca_projects_locations_external_account_keys_create()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `publicca_projects_locations_external_account_keys_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn publicca_projects_locations_external_account_keys_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ExternalAccountKey>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = publicca_projects_locations_external_account_keys_create_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`publicca_projects_locations_external_account_keys_create`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct PubliccaProjectsLocationsExternalAccountKeysCreateArgs {
    /// Path parameter: parent
    pub parent: String,
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/externalAccountKeys
/// Creates a new ExternalAccountKey bound to the project.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `publicca_projects_locations_external_account_keys_create_builder()` + `publicca_projects_locations_external_account_keys_create_execute()`.
/// For task-level control, use `publicca_projects_locations_external_account_keys_create_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn publicca_projects_locations_external_account_keys_create(
    client: &SimpleHttpClient,
    args: &PubliccaProjectsLocationsExternalAccountKeysCreateArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ExternalAccountKey>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        publicca_projects_locations_external_account_keys_create_builder(client, &args.parent)?;
    publicca_projects_locations_external_account_keys_create_execute(builder)
}

// =============================================================================
// ResourceIdentifier implementation for ExternalAccountKey
// =============================================================================

/// ResourceIdentifier implementation for ExternalAccountKey with PubliccaProjectsLocationsExternalAccountKeysCreateArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<PubliccaProjectsLocationsExternalAccountKeysCreateArgs>
    for ExternalAccountKey
{
    fn generate_resource_id(
        &self,
        input: &PubliccaProjectsLocationsExternalAccountKeysCreateArgs,
    ) -> String {
        format!("gcp::publicca::ExternalAccountKey/{}", input.parent)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::publicca::ExternalAccountKey"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}
