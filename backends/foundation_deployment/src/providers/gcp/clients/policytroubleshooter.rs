//! Auto-generated API clients for gcp/policytroubleshooter.
//!
//! Generated by `cargo run --bin ewe_platform gen_provider_clients`.
//! DO NOT EDIT MANUALLY.
//!
//! Feature flag: `gcp`

#![cfg(feature = "gcp")]


use crate::providers::gcp::clients::types::*;
use crate::providers::gcp::resources::*;
use foundation_core::valtron::{
    execute, BoxedSendExecutionAction, StreamIterator, StreamIteratorExt, TaskIterator,
    TaskIteratorExt,
};
use foundation_core::wire::simple_http::client::{
    body_reader, ClientRequestBuilder, RequestIntro, SimpleHttpClient, SystemDnsResolver,
};
use foundation_macros::JsonHash;
use serde::Serialize;

/// GET v3/iam:troubleshoot
/// Checks whether a principal has a specific permission for a specific resource, and explains why the principal does or doesn't have that permission.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `policytroubleshooter_iam_troubleshoot_execute()` to send, or `policytroubleshooter_iam_troubleshoot` for simplest API.

pub fn policytroubleshooter_iam_troubleshoot_builder(
    client: &SimpleHttpClient,
    body: &GoogleCloudPolicytroubleshooterIamV3TroubleshootIamPolicyRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!("https://policytroubleshooter.googleapis.com/v3/iam:troubleshoot",);

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v3/iam:troubleshoot
/// Checks whether a principal has a specific permission for a specific resource, and explains why the principal does or doesn't have that permission.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `policytroubleshooter_iam_troubleshoot_execute()` or `policytroubleshooter_iam_troubleshoot`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `policytroubleshooter_iam_troubleshoot_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn policytroubleshooter_iam_troubleshoot_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<GoogleCloudPolicytroubleshooterIamV3TroubleshootIamPolicyResponse>,
                ApiError,
            >,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudPolicytroubleshooterIamV3TroubleshootIamPolicyResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v3/iam:troubleshoot
/// Checks whether a principal has a specific permission for a specific resource, and explains why the principal does or doesn't have that permission.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `policytroubleshooter_iam_troubleshoot_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `policytroubleshooter_iam_troubleshoot_task()`.
/// For the simplest API, use `policytroubleshooter_iam_troubleshoot()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `policytroubleshooter_iam_troubleshoot_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn policytroubleshooter_iam_troubleshoot_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleCloudPolicytroubleshooterIamV3TroubleshootIamPolicyResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = policytroubleshooter_iam_troubleshoot_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`policytroubleshooter_iam_troubleshoot`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct PolicytroubleshooterIamTroubleshootArgs {
    /// Request body.
    pub body: GoogleCloudPolicytroubleshooterIamV3TroubleshootIamPolicyRequest,
}

/// GET v3/iam:troubleshoot
/// Checks whether a principal has a specific permission for a specific resource, and explains why the principal does or doesn't have that permission.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `policytroubleshooter_iam_troubleshoot_builder()` + `policytroubleshooter_iam_troubleshoot_execute()`.
/// For task-level control, use `policytroubleshooter_iam_troubleshoot_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn policytroubleshooter_iam_troubleshoot(
    client: &SimpleHttpClient,
    args: &PolicytroubleshooterIamTroubleshootArgs,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleCloudPolicytroubleshooterIamV3TroubleshootIamPolicyResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = policytroubleshooter_iam_troubleshoot_builder(client, &args.body)?;
    policytroubleshooter_iam_troubleshoot_execute(builder)
}
