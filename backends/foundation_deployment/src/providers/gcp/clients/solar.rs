//! Auto-generated API clients for gcp/solar.
//!
//! Generated by `cargo run --bin ewe_platform gen_provider_clients`.
//! DO NOT EDIT MANUALLY.
//!
//! Feature flag: `gcp`

#![cfg(feature = "gcp")]


use crate::providers::gcp::clients::types::*;
use crate::providers::gcp::resources::*;
use foundation_core::valtron::{
    execute, BoxedSendExecutionAction, StreamIterator, StreamIteratorExt, TaskIterator,
    TaskIteratorExt,
};
use foundation_core::wire::simple_http::client::{
    body_reader, ClientRequestBuilder, DnsResolver, RequestIntro, SimpleHttpClient,
    SystemDnsResolver,
};
use foundation_db::state::resource_identifier::ResourceIdentifier;
use foundation_macros::JsonHash;
use serde::Serialize;

/// GET v1/buildingInsights:findClosest
/// Locates the building whose centroid is closest to a query point. Returns an error with code NOT_FOUND if there are no buildings within approximately 50m of the query point.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `solar_building_insights_find_closest_execute()` to send, or `solar_building_insights_find_closest` for simplest API.

pub fn solar_building_insights_find_closest_builder<R>(
    client: &SimpleHttpClient<R>,
    exactQualityRequired: &Option<String>,
    experiments: &Option<String>,
    location_latitude: &Option<String>,
    location_longitude: &Option<String>,
    requiredQuality: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!("https://solar.googleapis.com/v1/buildingInsights:findClosest",);

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = exactQualityRequired.as_ref() {
        query_parts.push(format!("exactQualityRequired={}", val));
    }
    if let Some(val) = experiments.as_ref() {
        query_parts.push(format!("experiments={}", val));
    }
    if let Some(val) = location_latitude.as_ref() {
        query_parts.push(format!("location.latitude={}", val));
    }
    if let Some(val) = location_longitude.as_ref() {
        query_parts.push(format!("location.longitude={}", val));
    }
    if let Some(val) = requiredQuality.as_ref() {
        query_parts.push(format!("requiredQuality={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/buildingInsights:findClosest
/// Locates the building whose centroid is closest to a query point. Returns an error with code NOT_FOUND if there are no buildings within approximately 50m of the query point.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `solar_building_insights_find_closest_execute()` or `solar_building_insights_find_closest`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `solar_building_insights_find_closest_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn solar_building_insights_find_closest_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<BuildingInsights>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: BuildingInsights = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/buildingInsights:findClosest
/// Locates the building whose centroid is closest to a query point. Returns an error with code NOT_FOUND if there are no buildings within approximately 50m of the query point.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `solar_building_insights_find_closest_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `solar_building_insights_find_closest_task()`.
/// For the simplest API, use `solar_building_insights_find_closest()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `solar_building_insights_find_closest_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn solar_building_insights_find_closest_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<BuildingInsights>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = solar_building_insights_find_closest_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`solar_building_insights_find_closest`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct SolarBuildingInsightsFindClosestArgs {
    /// Query parameter: exactQualityRequired
    pub exactQualityRequired: Option<String>,
    /// Query parameter: experiments
    pub experiments: Option<String>,
    /// Query parameter: location_latitude
    pub location_latitude: Option<String>,
    /// Query parameter: location_longitude
    pub location_longitude: Option<String>,
    /// Query parameter: requiredQuality
    pub requiredQuality: Option<String>,
}

/// GET v1/buildingInsights:findClosest
/// Locates the building whose centroid is closest to a query point. Returns an error with code NOT_FOUND if there are no buildings within approximately 50m of the query point.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `solar_building_insights_find_closest_builder()` + `solar_building_insights_find_closest_execute()`.
/// For task-level control, use `solar_building_insights_find_closest_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn solar_building_insights_find_closest(
    client: &SimpleHttpClient,
    args: &SolarBuildingInsightsFindClosestArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<BuildingInsights>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = solar_building_insights_find_closest_builder(
        client,
        &args.exactQualityRequired,
        &args.experiments,
        &args.location_latitude,
        &args.location_longitude,
        &args.requiredQuality,
    )?;
    solar_building_insights_find_closest_execute(builder)
}

/// GET v1/dataLayers:get
/// Gets solar information for a region surrounding a location. Returns an error with code NOT_FOUND if the location is outside the coverage area.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `solar_data_layers_get_execute()` to send, or `solar_data_layers_get` for simplest API.

pub fn solar_data_layers_get_builder<R>(
    client: &SimpleHttpClient<R>,
    exactQualityRequired: &Option<String>,
    experiments: &Option<String>,
    location_latitude: &Option<String>,
    location_longitude: &Option<String>,
    pixelSizeMeters: &Option<String>,
    radiusMeters: &Option<String>,
    requiredQuality: &Option<String>,
    view: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!("https://solar.googleapis.com/v1/dataLayers:get",);

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = exactQualityRequired.as_ref() {
        query_parts.push(format!("exactQualityRequired={}", val));
    }
    if let Some(val) = experiments.as_ref() {
        query_parts.push(format!("experiments={}", val));
    }
    if let Some(val) = location_latitude.as_ref() {
        query_parts.push(format!("location.latitude={}", val));
    }
    if let Some(val) = location_longitude.as_ref() {
        query_parts.push(format!("location.longitude={}", val));
    }
    if let Some(val) = pixelSizeMeters.as_ref() {
        query_parts.push(format!("pixelSizeMeters={}", val));
    }
    if let Some(val) = radiusMeters.as_ref() {
        query_parts.push(format!("radiusMeters={}", val));
    }
    if let Some(val) = requiredQuality.as_ref() {
        query_parts.push(format!("requiredQuality={}", val));
    }
    if let Some(val) = view.as_ref() {
        query_parts.push(format!("view={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/dataLayers:get
/// Gets solar information for a region surrounding a location. Returns an error with code NOT_FOUND if the location is outside the coverage area.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `solar_data_layers_get_execute()` or `solar_data_layers_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `solar_data_layers_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn solar_data_layers_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<DataLayers>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: DataLayers = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/dataLayers:get
/// Gets solar information for a region surrounding a location. Returns an error with code NOT_FOUND if the location is outside the coverage area.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `solar_data_layers_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `solar_data_layers_get_task()`.
/// For the simplest API, use `solar_data_layers_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `solar_data_layers_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn solar_data_layers_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<DataLayers>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = solar_data_layers_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`solar_data_layers_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct SolarDataLayersGetArgs {
    /// Query parameter: exactQualityRequired
    pub exactQualityRequired: Option<String>,
    /// Query parameter: experiments
    pub experiments: Option<String>,
    /// Query parameter: location_latitude
    pub location_latitude: Option<String>,
    /// Query parameter: location_longitude
    pub location_longitude: Option<String>,
    /// Query parameter: pixelSizeMeters
    pub pixelSizeMeters: Option<String>,
    /// Query parameter: radiusMeters
    pub radiusMeters: Option<String>,
    /// Query parameter: requiredQuality
    pub requiredQuality: Option<String>,
    /// Query parameter: view
    pub view: Option<String>,
}

/// GET v1/dataLayers:get
/// Gets solar information for a region surrounding a location. Returns an error with code NOT_FOUND if the location is outside the coverage area.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `solar_data_layers_get_builder()` + `solar_data_layers_get_execute()`.
/// For task-level control, use `solar_data_layers_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn solar_data_layers_get(
    client: &SimpleHttpClient,
    args: &SolarDataLayersGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<DataLayers>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = solar_data_layers_get_builder(
        client,
        &args.exactQualityRequired,
        &args.experiments,
        &args.location_latitude,
        &args.location_longitude,
        &args.pixelSizeMeters,
        &args.radiusMeters,
        &args.requiredQuality,
        &args.view,
    )?;
    solar_data_layers_get_execute(builder)
}

/// GET v1/geoTiff:get
/// Returns an image by its ID.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `solar_geo_tiff_get_execute()` to send, or `solar_geo_tiff_get` for simplest API.

pub fn solar_geo_tiff_get_builder<R>(
    client: &SimpleHttpClient<R>,
    id: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!("https://solar.googleapis.com/v1/geoTiff:get",);

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = id.as_ref() {
        query_parts.push(format!("id={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/geoTiff:get
/// Returns an image by its ID.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `solar_geo_tiff_get_execute()` or `solar_geo_tiff_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `solar_geo_tiff_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn solar_geo_tiff_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<HttpBody>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: HttpBody = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/geoTiff:get
/// Returns an image by its ID.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `solar_geo_tiff_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `solar_geo_tiff_get_task()`.
/// For the simplest API, use `solar_geo_tiff_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `solar_geo_tiff_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn solar_geo_tiff_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<HttpBody>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = solar_geo_tiff_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`solar_geo_tiff_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct SolarGeoTiffGetArgs {
    /// Query parameter: id
    pub id: Option<String>,
}

/// GET v1/geoTiff:get
/// Returns an image by its ID.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `solar_geo_tiff_get_builder()` + `solar_geo_tiff_get_execute()`.
/// For task-level control, use `solar_geo_tiff_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn solar_geo_tiff_get(
    client: &SimpleHttpClient,
    args: &SolarGeoTiffGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<HttpBody>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = solar_geo_tiff_get_builder(client, &args.id)?;
    solar_geo_tiff_get_execute(builder)
}

// =============================================================================
// ResourceIdentifier implementation for BuildingInsights
// =============================================================================

/// ResourceIdentifier implementation for BuildingInsights with SolarBuildingInsightsFindClosestArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<SolarBuildingInsightsFindClosestArgs> for BuildingInsights {
    fn generate_resource_id(&self, input: &SolarBuildingInsightsFindClosestArgs) -> String {
        "gcp::solar::BuildingInsights".to_string()
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::solar::BuildingInsights"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for DataLayers
// =============================================================================

/// ResourceIdentifier implementation for DataLayers with SolarDataLayersGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<SolarDataLayersGetArgs> for DataLayers {
    fn generate_resource_id(&self, input: &SolarDataLayersGetArgs) -> String {
        "gcp::solar::DataLayers".to_string()
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::solar::DataLayers"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for HttpBody
// =============================================================================

/// ResourceIdentifier implementation for HttpBody with SolarGeoTiffGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<SolarGeoTiffGetArgs> for HttpBody {
    fn generate_resource_id(&self, input: &SolarGeoTiffGetArgs) -> String {
        "gcp::solar::HttpBody".to_string()
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::solar::HttpBody"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}
