//! Auto-generated API clients for gcp/solar.
//!
//! Generated by `cargo run --bin ewe_platform gen_provider_clients`.
//! DO NOT EDIT MANUALLY.
//!
//! Feature flag: `gcp/solar`

#![cfg(feature = "gcp/solar")]

pub mod types;

use crate::providers::gcp::clients::types::*;
use crate::providers::gcp::resources::*;
use foundation_core::valtron::{execute, StreamIterator, StreamIteratorExt, TaskIteratorExt};
use foundation_core::wire::simple_http::client::{
    body_reader, ClientRequestBuilder, RequestIntro, SimpleHttpClient, SystemDnsResolver,
};
use foundation_macros::JsonHash;
use serde::Serialize;

/// GET v1/buildingInsights:findClosest
/// Locates the building whose centroid is closest to a query point. Returns an error with code NOT_FOUND if there are no buildings within approximately 50m of the query point.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `solar_building_insights_find_closest_execute()` to send, or `solar_building_insights_find_closest` for simplest API.

pub fn solar_building_insights_find_closest_builder(
    client: &SimpleHttpClient,
    exactQualityRequired: Option<bool>,
    experiments: Option<&str>,
    location_latitude: Option<f64>,
    location_longitude: Option<f64>,
    requiredQuality: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://solar.googleapis.com/v1/buildingInsights:findClosest",);

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = exactQualityRequired {
        query_parts.push(format!("exactQualityRequired={}", val));
    }
    if let Some(val) = experiments {
        query_parts.push(format!("experiments={}", val));
    }
    if let Some(val) = location_latitude {
        query_parts.push(format!("location_latitude={}", val));
    }
    if let Some(val) = location_longitude {
        query_parts.push(format!("location_longitude={}", val));
    }
    if let Some(val) = requiredQuality {
        query_parts.push(format!("requiredQuality={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/buildingInsights:findClosest
/// Locates the building whose centroid is closest to a query point. Returns an error with code NOT_FOUND if there are no buildings within approximately 50m of the query point.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `solar_building_insights_find_closest_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from solar_building_insights_find_closest_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn solar_building_insights_find_closest_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<BuildingInsights>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: BuildingInsights = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`solar_building_insights_find_closest`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct SolarBuildingInsightsFindClosestArgs {
    /// Query parameter: exactQualityRequired
    pub exactQualityRequired: Option<bool>,
    /// Query parameter: experiments
    pub experiments: Option<String>,
    /// Query parameter: location_latitude
    pub location_latitude: Option<f64>,
    /// Query parameter: location_longitude
    pub location_longitude: Option<f64>,
    /// Query parameter: requiredQuality
    pub requiredQuality: Option<String>,
}

/// GET v1/buildingInsights:findClosest
/// Locates the building whose centroid is closest to a query point. Returns an error with code NOT_FOUND if there are no buildings within approximately 50m of the query point.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `solar_building_insights_find_closest_builder()` + `solar_building_insights_find_closest_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn solar_building_insights_find_closest(
    client: &SimpleHttpClient,
    args: &SolarBuildingInsightsFindClosestArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<BuildingInsights>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = solar_building_insights_find_closest_builder(
        client,
        args.exactQualityRequired,
        args.experiments.as_deref(),
        args.location_latitude,
        args.location_longitude,
        args.requiredQuality.as_deref(),
    )?;
    solar_building_insights_find_closest_execute(builder)
}

/// GET v1/dataLayers:get
/// Gets solar information for a region surrounding a location. Returns an error with code NOT_FOUND if the location is outside the coverage area.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `solar_data_layers_get_execute()` to send, or `solar_data_layers_get` for simplest API.

pub fn solar_data_layers_get_builder(
    client: &SimpleHttpClient,
    exactQualityRequired: Option<bool>,
    experiments: Option<&str>,
    location_latitude: Option<f64>,
    location_longitude: Option<f64>,
    pixelSizeMeters: Option<f32>,
    radiusMeters: Option<f32>,
    requiredQuality: Option<&str>,
    view: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://solar.googleapis.com/v1/dataLayers:get",);

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = exactQualityRequired {
        query_parts.push(format!("exactQualityRequired={}", val));
    }
    if let Some(val) = experiments {
        query_parts.push(format!("experiments={}", val));
    }
    if let Some(val) = location_latitude {
        query_parts.push(format!("location_latitude={}", val));
    }
    if let Some(val) = location_longitude {
        query_parts.push(format!("location_longitude={}", val));
    }
    if let Some(val) = pixelSizeMeters {
        query_parts.push(format!("pixelSizeMeters={}", val));
    }
    if let Some(val) = radiusMeters {
        query_parts.push(format!("radiusMeters={}", val));
    }
    if let Some(val) = requiredQuality {
        query_parts.push(format!("requiredQuality={}", val));
    }
    if let Some(val) = view {
        query_parts.push(format!("view={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/dataLayers:get
/// Gets solar information for a region surrounding a location. Returns an error with code NOT_FOUND if the location is outside the coverage area.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `solar_data_layers_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from solar_data_layers_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn solar_data_layers_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<DataLayers>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: DataLayers = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`solar_data_layers_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct SolarDataLayersGetArgs {
    /// Query parameter: exactQualityRequired
    pub exactQualityRequired: Option<bool>,
    /// Query parameter: experiments
    pub experiments: Option<String>,
    /// Query parameter: location_latitude
    pub location_latitude: Option<f64>,
    /// Query parameter: location_longitude
    pub location_longitude: Option<f64>,
    /// Query parameter: pixelSizeMeters
    pub pixelSizeMeters: Option<f32>,
    /// Query parameter: radiusMeters
    pub radiusMeters: Option<f32>,
    /// Query parameter: requiredQuality
    pub requiredQuality: Option<String>,
    /// Query parameter: view
    pub view: Option<String>,
}

/// GET v1/dataLayers:get
/// Gets solar information for a region surrounding a location. Returns an error with code NOT_FOUND if the location is outside the coverage area.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `solar_data_layers_get_builder()` + `solar_data_layers_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn solar_data_layers_get(
    client: &SimpleHttpClient,
    args: &SolarDataLayersGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<DataLayers>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = solar_data_layers_get_builder(
        client,
        args.exactQualityRequired,
        args.experiments.as_deref(),
        args.location_latitude,
        args.location_longitude,
        args.pixelSizeMeters,
        args.radiusMeters,
        args.requiredQuality.as_deref(),
        args.view.as_deref(),
    )?;
    solar_data_layers_get_execute(builder)
}

/// GET v1/geoTiff:get
/// Returns an image by its ID.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `solar_geo_tiff_get_execute()` to send, or `solar_geo_tiff_get` for simplest API.

pub fn solar_geo_tiff_get_builder(
    client: &SimpleHttpClient,
    id: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://solar.googleapis.com/v1/geoTiff:get",);

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = id {
        query_parts.push(format!("id={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/geoTiff:get
/// Returns an image by its ID.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `solar_geo_tiff_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from solar_geo_tiff_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn solar_geo_tiff_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<HttpBody>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: HttpBody = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`solar_geo_tiff_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct SolarGeoTiffGetArgs {
    /// Query parameter: id
    pub id: Option<String>,
}

/// GET v1/geoTiff:get
/// Returns an image by its ID.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `solar_geo_tiff_get_builder()` + `solar_geo_tiff_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn solar_geo_tiff_get(
    client: &SimpleHttpClient,
    args: &SolarGeoTiffGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<HttpBody>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = solar_geo_tiff_get_builder(client, args.id.as_deref())?;
    solar_geo_tiff_get_execute(builder)
}
