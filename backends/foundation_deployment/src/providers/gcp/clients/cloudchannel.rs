//! Auto-generated API clients for gcp/cloudchannel.
//!
//! Generated by `cargo run --bin ewe_platform gen_provider_clients`.
//! DO NOT EDIT MANUALLY.
//!
//! Feature flag: `gcp`

#![cfg(feature = "gcp")]

pub mod types;

use crate::providers::gcp::clients::types::*;
use crate::providers::gcp::resources::*;
use foundation_core::valtron::{
    execute, BoxedSendExecutionAction, StreamIterator, StreamIteratorExt, TaskIterator,
    TaskIteratorExt,
};
use foundation_core::wire::simple_http::client::{
    body_reader, ClientRequestBuilder, RequestIntro, SimpleHttpClient, SystemDnsResolver,
};
use foundation_macros::JsonHash;
use serde::Serialize;

/// GET v1/accounts/{accountsId}:checkCloudIdentityAccountsExist
/// Confirms the existence of Cloud Identity accounts based on the domain and if the Cloud Identity accounts are owned by the reseller. Possible error codes: * PERMISSION_DENIED: The reseller account making the request is different from the reseller account in the API request. * INVALID_ARGUMENT: Required request parameters are missing or invalid. * INVALID_VALUE: Invalid domain value in the request. Return value: A list of CloudIdentityCustomerAccount resources for the domain (may be empty) Note: in the v1alpha1 version of the API, a NOT_FOUND error returns if no CloudIdentityCustomerAccount resources match the domain.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `cloudchannel_accounts_check_cloud_identity_accounts_exist_execute()` to send, or `cloudchannel_accounts_check_cloud_identity_accounts_exist` for simplest API.

pub fn cloudchannel_accounts_check_cloud_identity_accounts_exist_builder(
    client: &SimpleHttpClient,
    parent: String,
    body: &GoogleCloudChannelV1CheckCloudIdentityAccountsExistRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://cloudchannel.googleapis.com/v1/accounts/{}:checkCloudIdentityAccountsExist",
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/accounts/{accountsId}:checkCloudIdentityAccountsExist
/// Confirms the existence of Cloud Identity accounts based on the domain and if the Cloud Identity accounts are owned by the reseller. Possible error codes: * PERMISSION_DENIED: The reseller account making the request is different from the reseller account in the API request. * INVALID_ARGUMENT: Required request parameters are missing or invalid. * INVALID_VALUE: Invalid domain value in the request. Return value: A list of CloudIdentityCustomerAccount resources for the domain (may be empty) Note: in the v1alpha1 version of the API, a NOT_FOUND error returns if no CloudIdentityCustomerAccount resources match the domain.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `cloudchannel_accounts_check_cloud_identity_accounts_exist_execute()` or `cloudchannel_accounts_check_cloud_identity_accounts_exist`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `cloudchannel_accounts_check_cloud_identity_accounts_exist_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudchannel_accounts_check_cloud_identity_accounts_exist_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<GoogleCloudChannelV1CheckCloudIdentityAccountsExistResponse>,
                ApiError,
            >,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudChannelV1CheckCloudIdentityAccountsExistResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/accounts/{accountsId}:checkCloudIdentityAccountsExist
/// Confirms the existence of Cloud Identity accounts based on the domain and if the Cloud Identity accounts are owned by the reseller. Possible error codes: * PERMISSION_DENIED: The reseller account making the request is different from the reseller account in the API request. * INVALID_ARGUMENT: Required request parameters are missing or invalid. * INVALID_VALUE: Invalid domain value in the request. Return value: A list of CloudIdentityCustomerAccount resources for the domain (may be empty) Note: in the v1alpha1 version of the API, a NOT_FOUND error returns if no CloudIdentityCustomerAccount resources match the domain.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `cloudchannel_accounts_check_cloud_identity_accounts_exist_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `cloudchannel_accounts_check_cloud_identity_accounts_exist_task()`.
/// For the simplest API, use `cloudchannel_accounts_check_cloud_identity_accounts_exist()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `cloudchannel_accounts_check_cloud_identity_accounts_exist_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn cloudchannel_accounts_check_cloud_identity_accounts_exist_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleCloudChannelV1CheckCloudIdentityAccountsExistResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = cloudchannel_accounts_check_cloud_identity_accounts_exist_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`cloudchannel_accounts_check_cloud_identity_accounts_exist`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct CloudchannelAccountsCheckCloudIdentityAccountsExistArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Request body.
    pub body: GoogleCloudChannelV1CheckCloudIdentityAccountsExistRequest,
}

/// GET v1/accounts/{accountsId}:checkCloudIdentityAccountsExist
/// Confirms the existence of Cloud Identity accounts based on the domain and if the Cloud Identity accounts are owned by the reseller. Possible error codes: * PERMISSION_DENIED: The reseller account making the request is different from the reseller account in the API request. * INVALID_ARGUMENT: Required request parameters are missing or invalid. * INVALID_VALUE: Invalid domain value in the request. Return value: A list of CloudIdentityCustomerAccount resources for the domain (may be empty) Note: in the v1alpha1 version of the API, a NOT_FOUND error returns if no CloudIdentityCustomerAccount resources match the domain.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `cloudchannel_accounts_check_cloud_identity_accounts_exist_builder()` + `cloudchannel_accounts_check_cloud_identity_accounts_exist_execute()`.
/// For task-level control, use `cloudchannel_accounts_check_cloud_identity_accounts_exist_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudchannel_accounts_check_cloud_identity_accounts_exist(
    client: &SimpleHttpClient,
    args: &CloudchannelAccountsCheckCloudIdentityAccountsExistArgs,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleCloudChannelV1CheckCloudIdentityAccountsExistResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = cloudchannel_accounts_check_cloud_identity_accounts_exist_builder(
        client,
        args.parent.clone(),
        &args.body,
    )?;
    cloudchannel_accounts_check_cloud_identity_accounts_exist_execute(builder)
}

/// GET v1/accounts/{accountsId}:listSubscribers
/// Lists service accounts with subscriber privileges on the P`ub/Sub` topic created for this Channel Services account or integrator. Possible error codes: * PERMISSION_DENIED: The reseller account making the request and the provided reseller account are different, or the impersonated user is not a super admin. * INVALID_ARGUMENT: Required request parameters are missing or invalid. * NOT_FOUND: The topic resource doesn't exist. * INTERNAL: Any non-user error related to a technical issue in the backend. Contact Cloud Channel support. * UNKNOWN: Any non-user error related to a technical issue in the backend. Contact Cloud Channel support. Return value: A list of service email addresses.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `cloudchannel_accounts_list_subscribers_execute()` to send, or `cloudchannel_accounts_list_subscribers` for simplest API.

pub fn cloudchannel_accounts_list_subscribers_builder(
    client: &SimpleHttpClient,
    account: String,
    integrator: Option<String>,
    pageSize: Option<i32>,
    pageToken: Option<String>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url =
        format!("https://cloudchannel.googleapis.com/v1/accounts/{}:listSubscribers",);

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = integrator {
        query_parts.push(format!("integrator={}", val));
    }
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/accounts/{accountsId}:listSubscribers
/// Lists service accounts with subscriber privileges on the P`ub/Sub` topic created for this Channel Services account or integrator. Possible error codes: * PERMISSION_DENIED: The reseller account making the request and the provided reseller account are different, or the impersonated user is not a super admin. * INVALID_ARGUMENT: Required request parameters are missing or invalid. * NOT_FOUND: The topic resource doesn't exist. * INTERNAL: Any non-user error related to a technical issue in the backend. Contact Cloud Channel support. * UNKNOWN: Any non-user error related to a technical issue in the backend. Contact Cloud Channel support. Return value: A list of service email addresses.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `cloudchannel_accounts_list_subscribers_execute()` or `cloudchannel_accounts_list_subscribers`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `cloudchannel_accounts_list_subscribers_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudchannel_accounts_list_subscribers_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleCloudChannelV1ListSubscribersResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudChannelV1ListSubscribersResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/accounts/{accountsId}:listSubscribers
/// Lists service accounts with subscriber privileges on the P`ub/Sub` topic created for this Channel Services account or integrator. Possible error codes: * PERMISSION_DENIED: The reseller account making the request and the provided reseller account are different, or the impersonated user is not a super admin. * INVALID_ARGUMENT: Required request parameters are missing or invalid. * NOT_FOUND: The topic resource doesn't exist. * INTERNAL: Any non-user error related to a technical issue in the backend. Contact Cloud Channel support. * UNKNOWN: Any non-user error related to a technical issue in the backend. Contact Cloud Channel support. Return value: A list of service email addresses.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `cloudchannel_accounts_list_subscribers_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `cloudchannel_accounts_list_subscribers_task()`.
/// For the simplest API, use `cloudchannel_accounts_list_subscribers()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `cloudchannel_accounts_list_subscribers_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn cloudchannel_accounts_list_subscribers_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudChannelV1ListSubscribersResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = cloudchannel_accounts_list_subscribers_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`cloudchannel_accounts_list_subscribers`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct CloudchannelAccountsListSubscribersArgs {
    /// Path parameter: account
    pub account: String,
    /// Query parameter: integrator
    pub integrator: Option<String>,
    /// Query parameter: pageSize
    pub pageSize: Option<i32>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
}

/// GET v1/accounts/{accountsId}:listSubscribers
/// Lists service accounts with subscriber privileges on the P`ub/Sub` topic created for this Channel Services account or integrator. Possible error codes: * PERMISSION_DENIED: The reseller account making the request and the provided reseller account are different, or the impersonated user is not a super admin. * INVALID_ARGUMENT: Required request parameters are missing or invalid. * NOT_FOUND: The topic resource doesn't exist. * INTERNAL: Any non-user error related to a technical issue in the backend. Contact Cloud Channel support. * UNKNOWN: Any non-user error related to a technical issue in the backend. Contact Cloud Channel support. Return value: A list of service email addresses.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `cloudchannel_accounts_list_subscribers_builder()` + `cloudchannel_accounts_list_subscribers_execute()`.
/// For task-level control, use `cloudchannel_accounts_list_subscribers_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudchannel_accounts_list_subscribers(
    client: &SimpleHttpClient,
    args: &CloudchannelAccountsListSubscribersArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudChannelV1ListSubscribersResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = cloudchannel_accounts_list_subscribers_builder(
        client,
        args.account.clone(),
        args.integrator.clone(),
        args.pageSize.clone(),
        args.pageToken.clone(),
    )?;
    cloudchannel_accounts_list_subscribers_execute(builder)
}

/// GET v1/accounts/{accountsId}:listTransferableOffers
/// List TransferableOffers of a customer based on Cloud Identity ID or Customer Name in the request. Use this method when a reseller gets the entitlement information of an unowned customer. The reseller should provide the customer's Cloud Identity ID or Customer Name. Possible error codes: * PERMISSION_DENIED: * The customer doesn't belong to the reseller and has no auth token. * The customer provided incorrect reseller information when generating auth token. * The reseller account making the request is different from the reseller account in the query. * The reseller is not authorized to transact on this Product. See <https://support.google.`com/channelservices/answer/9759265`> * INVALID_ARGUMENT: Required request parameters are missing or invalid. Return value: List of TransferableOffer for the given customer and SKU.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `cloudchannel_accounts_list_transferable_offers_execute()` to send, or `cloudchannel_accounts_list_transferable_offers` for simplest API.

pub fn cloudchannel_accounts_list_transferable_offers_builder(
    client: &SimpleHttpClient,
    parent: String,
    body: &GoogleCloudChannelV1ListTransferableOffersRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url =
        format!("https://cloudchannel.googleapis.com/v1/accounts/{}:listTransferableOffers",);

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/accounts/{accountsId}:listTransferableOffers
/// List TransferableOffers of a customer based on Cloud Identity ID or Customer Name in the request. Use this method when a reseller gets the entitlement information of an unowned customer. The reseller should provide the customer's Cloud Identity ID or Customer Name. Possible error codes: * PERMISSION_DENIED: * The customer doesn't belong to the reseller and has no auth token. * The customer provided incorrect reseller information when generating auth token. * The reseller account making the request is different from the reseller account in the query. * The reseller is not authorized to transact on this Product. See <https://support.google.`com/channelservices/answer/9759265`> * INVALID_ARGUMENT: Required request parameters are missing or invalid. Return value: List of TransferableOffer for the given customer and SKU.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `cloudchannel_accounts_list_transferable_offers_execute()` or `cloudchannel_accounts_list_transferable_offers`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `cloudchannel_accounts_list_transferable_offers_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudchannel_accounts_list_transferable_offers_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<GoogleCloudChannelV1ListTransferableOffersResponse>,
                ApiError,
            >,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudChannelV1ListTransferableOffersResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/accounts/{accountsId}:listTransferableOffers
/// List TransferableOffers of a customer based on Cloud Identity ID or Customer Name in the request. Use this method when a reseller gets the entitlement information of an unowned customer. The reseller should provide the customer's Cloud Identity ID or Customer Name. Possible error codes: * PERMISSION_DENIED: * The customer doesn't belong to the reseller and has no auth token. * The customer provided incorrect reseller information when generating auth token. * The reseller account making the request is different from the reseller account in the query. * The reseller is not authorized to transact on this Product. See <https://support.google.`com/channelservices/answer/9759265`> * INVALID_ARGUMENT: Required request parameters are missing or invalid. Return value: List of TransferableOffer for the given customer and SKU.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `cloudchannel_accounts_list_transferable_offers_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `cloudchannel_accounts_list_transferable_offers_task()`.
/// For the simplest API, use `cloudchannel_accounts_list_transferable_offers()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `cloudchannel_accounts_list_transferable_offers_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn cloudchannel_accounts_list_transferable_offers_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudChannelV1ListTransferableOffersResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = cloudchannel_accounts_list_transferable_offers_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`cloudchannel_accounts_list_transferable_offers`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct CloudchannelAccountsListTransferableOffersArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Request body.
    pub body: GoogleCloudChannelV1ListTransferableOffersRequest,
}

/// GET v1/accounts/{accountsId}:listTransferableOffers
/// List TransferableOffers of a customer based on Cloud Identity ID or Customer Name in the request. Use this method when a reseller gets the entitlement information of an unowned customer. The reseller should provide the customer's Cloud Identity ID or Customer Name. Possible error codes: * PERMISSION_DENIED: * The customer doesn't belong to the reseller and has no auth token. * The customer provided incorrect reseller information when generating auth token. * The reseller account making the request is different from the reseller account in the query. * The reseller is not authorized to transact on this Product. See <https://support.google.`com/channelservices/answer/9759265`> * INVALID_ARGUMENT: Required request parameters are missing or invalid. Return value: List of TransferableOffer for the given customer and SKU.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `cloudchannel_accounts_list_transferable_offers_builder()` + `cloudchannel_accounts_list_transferable_offers_execute()`.
/// For task-level control, use `cloudchannel_accounts_list_transferable_offers_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudchannel_accounts_list_transferable_offers(
    client: &SimpleHttpClient,
    args: &CloudchannelAccountsListTransferableOffersArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudChannelV1ListTransferableOffersResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = cloudchannel_accounts_list_transferable_offers_builder(
        client,
        args.parent.clone(),
        &args.body,
    )?;
    cloudchannel_accounts_list_transferable_offers_execute(builder)
}

/// GET v1/accounts/{accountsId}:listTransferableSkus
/// List TransferableSkus of a customer based on the Cloud Identity ID or Customer Name in the request. Use this method to list the entitlements information of an unowned customer. You should provide the customer's Cloud Identity ID or Customer Name. Possible error codes: * PERMISSION_DENIED: * The customer doesn't belong to the reseller and has no auth token. * The supplied auth token is invalid. * The reseller account making the request is different from the reseller account in the query. * INVALID_ARGUMENT: Required request parameters are missing or invalid. Return value: A list of the customer's TransferableSku.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `cloudchannel_accounts_list_transferable_skus_execute()` to send, or `cloudchannel_accounts_list_transferable_skus` for simplest API.

pub fn cloudchannel_accounts_list_transferable_skus_builder(
    client: &SimpleHttpClient,
    parent: String,
    body: &GoogleCloudChannelV1ListTransferableSkusRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url =
        format!("https://cloudchannel.googleapis.com/v1/accounts/{}:listTransferableSkus",);

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/accounts/{accountsId}:listTransferableSkus
/// List TransferableSkus of a customer based on the Cloud Identity ID or Customer Name in the request. Use this method to list the entitlements information of an unowned customer. You should provide the customer's Cloud Identity ID or Customer Name. Possible error codes: * PERMISSION_DENIED: * The customer doesn't belong to the reseller and has no auth token. * The supplied auth token is invalid. * The reseller account making the request is different from the reseller account in the query. * INVALID_ARGUMENT: Required request parameters are missing or invalid. Return value: A list of the customer's TransferableSku.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `cloudchannel_accounts_list_transferable_skus_execute()` or `cloudchannel_accounts_list_transferable_skus`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `cloudchannel_accounts_list_transferable_skus_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudchannel_accounts_list_transferable_skus_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleCloudChannelV1ListTransferableSkusResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudChannelV1ListTransferableSkusResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/accounts/{accountsId}:listTransferableSkus
/// List TransferableSkus of a customer based on the Cloud Identity ID or Customer Name in the request. Use this method to list the entitlements information of an unowned customer. You should provide the customer's Cloud Identity ID or Customer Name. Possible error codes: * PERMISSION_DENIED: * The customer doesn't belong to the reseller and has no auth token. * The supplied auth token is invalid. * The reseller account making the request is different from the reseller account in the query. * INVALID_ARGUMENT: Required request parameters are missing or invalid. Return value: A list of the customer's TransferableSku.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `cloudchannel_accounts_list_transferable_skus_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `cloudchannel_accounts_list_transferable_skus_task()`.
/// For the simplest API, use `cloudchannel_accounts_list_transferable_skus()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `cloudchannel_accounts_list_transferable_skus_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn cloudchannel_accounts_list_transferable_skus_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudChannelV1ListTransferableSkusResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = cloudchannel_accounts_list_transferable_skus_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`cloudchannel_accounts_list_transferable_skus`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct CloudchannelAccountsListTransferableSkusArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Request body.
    pub body: GoogleCloudChannelV1ListTransferableSkusRequest,
}

/// GET v1/accounts/{accountsId}:listTransferableSkus
/// List TransferableSkus of a customer based on the Cloud Identity ID or Customer Name in the request. Use this method to list the entitlements information of an unowned customer. You should provide the customer's Cloud Identity ID or Customer Name. Possible error codes: * PERMISSION_DENIED: * The customer doesn't belong to the reseller and has no auth token. * The supplied auth token is invalid. * The reseller account making the request is different from the reseller account in the query. * INVALID_ARGUMENT: Required request parameters are missing or invalid. Return value: A list of the customer's TransferableSku.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `cloudchannel_accounts_list_transferable_skus_builder()` + `cloudchannel_accounts_list_transferable_skus_execute()`.
/// For task-level control, use `cloudchannel_accounts_list_transferable_skus_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudchannel_accounts_list_transferable_skus(
    client: &SimpleHttpClient,
    args: &CloudchannelAccountsListTransferableSkusArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudChannelV1ListTransferableSkusResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = cloudchannel_accounts_list_transferable_skus_builder(
        client,
        args.parent.clone(),
        &args.body,
    )?;
    cloudchannel_accounts_list_transferable_skus_execute(builder)
}

/// GET v1/accounts/{accountsId}:register
/// Registers a service account with subscriber privileges on the P`ub/Sub` topic for this Channel Services account or integrator. After you create a subscriber, you get the events through SubscriberEvent Possible error codes: * PERMISSION_DENIED: The reseller account making the request and the provided reseller account are different, or the impersonated user is not a super admin. * INVALID_ARGUMENT: Required request parameters are missing or invalid. * INTERNAL: Any non-user error related to a technical issue in the backend. Contact Cloud Channel support. * UNKNOWN: Any non-user error related to a technical issue in the backend. Contact Cloud Channel support. Return value: The topic name with the registered service email address.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `cloudchannel_accounts_register_execute()` to send, or `cloudchannel_accounts_register` for simplest API.

pub fn cloudchannel_accounts_register_builder(
    client: &SimpleHttpClient,
    account: String,
    body: &GoogleCloudChannelV1RegisterSubscriberRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!("https://cloudchannel.googleapis.com/v1/accounts/{}:register",);

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/accounts/{accountsId}:register
/// Registers a service account with subscriber privileges on the P`ub/Sub` topic for this Channel Services account or integrator. After you create a subscriber, you get the events through SubscriberEvent Possible error codes: * PERMISSION_DENIED: The reseller account making the request and the provided reseller account are different, or the impersonated user is not a super admin. * INVALID_ARGUMENT: Required request parameters are missing or invalid. * INTERNAL: Any non-user error related to a technical issue in the backend. Contact Cloud Channel support. * UNKNOWN: Any non-user error related to a technical issue in the backend. Contact Cloud Channel support. Return value: The topic name with the registered service email address.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `cloudchannel_accounts_register_execute()` or `cloudchannel_accounts_register`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `cloudchannel_accounts_register_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudchannel_accounts_register_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleCloudChannelV1RegisterSubscriberResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudChannelV1RegisterSubscriberResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/accounts/{accountsId}:register
/// Registers a service account with subscriber privileges on the P`ub/Sub` topic for this Channel Services account or integrator. After you create a subscriber, you get the events through SubscriberEvent Possible error codes: * PERMISSION_DENIED: The reseller account making the request and the provided reseller account are different, or the impersonated user is not a super admin. * INVALID_ARGUMENT: Required request parameters are missing or invalid. * INTERNAL: Any non-user error related to a technical issue in the backend. Contact Cloud Channel support. * UNKNOWN: Any non-user error related to a technical issue in the backend. Contact Cloud Channel support. Return value: The topic name with the registered service email address.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `cloudchannel_accounts_register_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `cloudchannel_accounts_register_task()`.
/// For the simplest API, use `cloudchannel_accounts_register()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `cloudchannel_accounts_register_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn cloudchannel_accounts_register_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudChannelV1RegisterSubscriberResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = cloudchannel_accounts_register_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`cloudchannel_accounts_register`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct CloudchannelAccountsRegisterArgs {
    /// Path parameter: account
    pub account: String,
    /// Request body.
    pub body: GoogleCloudChannelV1RegisterSubscriberRequest,
}

/// GET v1/accounts/{accountsId}:register
/// Registers a service account with subscriber privileges on the P`ub/Sub` topic for this Channel Services account or integrator. After you create a subscriber, you get the events through SubscriberEvent Possible error codes: * PERMISSION_DENIED: The reseller account making the request and the provided reseller account are different, or the impersonated user is not a super admin. * INVALID_ARGUMENT: Required request parameters are missing or invalid. * INTERNAL: Any non-user error related to a technical issue in the backend. Contact Cloud Channel support. * UNKNOWN: Any non-user error related to a technical issue in the backend. Contact Cloud Channel support. Return value: The topic name with the registered service email address.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `cloudchannel_accounts_register_builder()` + `cloudchannel_accounts_register_execute()`.
/// For task-level control, use `cloudchannel_accounts_register_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudchannel_accounts_register(
    client: &SimpleHttpClient,
    args: &CloudchannelAccountsRegisterArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudChannelV1RegisterSubscriberResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = cloudchannel_accounts_register_builder(client, args.account.clone(), &args.body)?;
    cloudchannel_accounts_register_execute(builder)
}

/// GET v1/accounts/{accountsId}:unregister
/// Unregisters a service account with subscriber privileges on the P`ub/Sub` topic created for this Channel Services account or integrator. If there are no service accounts left with subscriber privileges, this deletes the topic. You can call ListSubscribers to check for these accounts. Possible error codes: * PERMISSION_DENIED: The reseller account making the request and the provided reseller account are different, or the impersonated user is not a super admin. * INVALID_ARGUMENT: Required request parameters are missing or invalid. * NOT_FOUND: The topic resource doesn't exist. * INTERNAL: Any non-user error related to a technical issue in the backend. Contact Cloud Channel support. * UNKNOWN: Any non-user error related to a technical issue in the backend. Contact Cloud Channel support. Return value: The topic name that unregistered the service email address. Returns a success response if the service email address wasn't registered with the topic.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `cloudchannel_accounts_unregister_execute()` to send, or `cloudchannel_accounts_unregister` for simplest API.

pub fn cloudchannel_accounts_unregister_builder(
    client: &SimpleHttpClient,
    account: String,
    body: &GoogleCloudChannelV1UnregisterSubscriberRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!("https://cloudchannel.googleapis.com/v1/accounts/{}:unregister",);

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/accounts/{accountsId}:unregister
/// Unregisters a service account with subscriber privileges on the P`ub/Sub` topic created for this Channel Services account or integrator. If there are no service accounts left with subscriber privileges, this deletes the topic. You can call ListSubscribers to check for these accounts. Possible error codes: * PERMISSION_DENIED: The reseller account making the request and the provided reseller account are different, or the impersonated user is not a super admin. * INVALID_ARGUMENT: Required request parameters are missing or invalid. * NOT_FOUND: The topic resource doesn't exist. * INTERNAL: Any non-user error related to a technical issue in the backend. Contact Cloud Channel support. * UNKNOWN: Any non-user error related to a technical issue in the backend. Contact Cloud Channel support. Return value: The topic name that unregistered the service email address. Returns a success response if the service email address wasn't registered with the topic.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `cloudchannel_accounts_unregister_execute()` or `cloudchannel_accounts_unregister`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `cloudchannel_accounts_unregister_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudchannel_accounts_unregister_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleCloudChannelV1UnregisterSubscriberResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudChannelV1UnregisterSubscriberResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/accounts/{accountsId}:unregister
/// Unregisters a service account with subscriber privileges on the P`ub/Sub` topic created for this Channel Services account or integrator. If there are no service accounts left with subscriber privileges, this deletes the topic. You can call ListSubscribers to check for these accounts. Possible error codes: * PERMISSION_DENIED: The reseller account making the request and the provided reseller account are different, or the impersonated user is not a super admin. * INVALID_ARGUMENT: Required request parameters are missing or invalid. * NOT_FOUND: The topic resource doesn't exist. * INTERNAL: Any non-user error related to a technical issue in the backend. Contact Cloud Channel support. * UNKNOWN: Any non-user error related to a technical issue in the backend. Contact Cloud Channel support. Return value: The topic name that unregistered the service email address. Returns a success response if the service email address wasn't registered with the topic.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `cloudchannel_accounts_unregister_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `cloudchannel_accounts_unregister_task()`.
/// For the simplest API, use `cloudchannel_accounts_unregister()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `cloudchannel_accounts_unregister_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn cloudchannel_accounts_unregister_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudChannelV1UnregisterSubscriberResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = cloudchannel_accounts_unregister_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`cloudchannel_accounts_unregister`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct CloudchannelAccountsUnregisterArgs {
    /// Path parameter: account
    pub account: String,
    /// Request body.
    pub body: GoogleCloudChannelV1UnregisterSubscriberRequest,
}

/// GET v1/accounts/{accountsId}:unregister
/// Unregisters a service account with subscriber privileges on the P`ub/Sub` topic created for this Channel Services account or integrator. If there are no service accounts left with subscriber privileges, this deletes the topic. You can call ListSubscribers to check for these accounts. Possible error codes: * PERMISSION_DENIED: The reseller account making the request and the provided reseller account are different, or the impersonated user is not a super admin. * INVALID_ARGUMENT: Required request parameters are missing or invalid. * NOT_FOUND: The topic resource doesn't exist. * INTERNAL: Any non-user error related to a technical issue in the backend. Contact Cloud Channel support. * UNKNOWN: Any non-user error related to a technical issue in the backend. Contact Cloud Channel support. Return value: The topic name that unregistered the service email address. Returns a success response if the service email address wasn't registered with the topic.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `cloudchannel_accounts_unregister_builder()` + `cloudchannel_accounts_unregister_execute()`.
/// For task-level control, use `cloudchannel_accounts_unregister_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudchannel_accounts_unregister(
    client: &SimpleHttpClient,
    args: &CloudchannelAccountsUnregisterArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudChannelV1UnregisterSubscriberResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        cloudchannel_accounts_unregister_builder(client, args.account.clone(), &args.body)?;
    cloudchannel_accounts_unregister_execute(builder)
}

/// GET v1/accounts/{accountsId}/channelPartnerLinks
/// Initiates a channel partner link between a distributor and a reseller, or between resellers in an n-tier reseller channel. Invited partners need to follow the invite_link_uri provided in the response to accept. After accepting the invitation, a link is set up between the two parties. You must be a distributor to call this method. Possible error codes: * PERMISSION_DENIED: The reseller account making the request is different from the reseller account in the API request. * INVALID_ARGUMENT: Required request parameters are missing or invalid. * ALREADY_EXISTS: The ChannelPartnerLink sent in the request already exists. * NOT_FOUND: No Cloud Identity customer exists for provided domain. * INTERNAL: Any non-user error related to a technical issue in the backend. Contact Cloud Channel support. * UNKNOWN: Any non-user error related to a technical issue in the backend. Contact Cloud Channel support. Return value: The new ChannelPartnerLink resource.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `cloudchannel_accounts_channel_partner_links_create_execute()` to send, or `cloudchannel_accounts_channel_partner_links_create` for simplest API.

pub fn cloudchannel_accounts_channel_partner_links_create_builder(
    client: &SimpleHttpClient,
    parent: String,
    body: &GoogleCloudChannelV1ChannelPartnerLink,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url =
        format!("https://cloudchannel.googleapis.com/v1/accounts/{}/channelPartnerLinks",);

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/accounts/{accountsId}/channelPartnerLinks
/// Initiates a channel partner link between a distributor and a reseller, or between resellers in an n-tier reseller channel. Invited partners need to follow the invite_link_uri provided in the response to accept. After accepting the invitation, a link is set up between the two parties. You must be a distributor to call this method. Possible error codes: * PERMISSION_DENIED: The reseller account making the request is different from the reseller account in the API request. * INVALID_ARGUMENT: Required request parameters are missing or invalid. * ALREADY_EXISTS: The ChannelPartnerLink sent in the request already exists. * NOT_FOUND: No Cloud Identity customer exists for provided domain. * INTERNAL: Any non-user error related to a technical issue in the backend. Contact Cloud Channel support. * UNKNOWN: Any non-user error related to a technical issue in the backend. Contact Cloud Channel support. Return value: The new ChannelPartnerLink resource.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `cloudchannel_accounts_channel_partner_links_create_execute()` or `cloudchannel_accounts_channel_partner_links_create`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `cloudchannel_accounts_channel_partner_links_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudchannel_accounts_channel_partner_links_create_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleCloudChannelV1ChannelPartnerLink>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudChannelV1ChannelPartnerLink = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/accounts/{accountsId}/channelPartnerLinks
/// Initiates a channel partner link between a distributor and a reseller, or between resellers in an n-tier reseller channel. Invited partners need to follow the invite_link_uri provided in the response to accept. After accepting the invitation, a link is set up between the two parties. You must be a distributor to call this method. Possible error codes: * PERMISSION_DENIED: The reseller account making the request is different from the reseller account in the API request. * INVALID_ARGUMENT: Required request parameters are missing or invalid. * ALREADY_EXISTS: The ChannelPartnerLink sent in the request already exists. * NOT_FOUND: No Cloud Identity customer exists for provided domain. * INTERNAL: Any non-user error related to a technical issue in the backend. Contact Cloud Channel support. * UNKNOWN: Any non-user error related to a technical issue in the backend. Contact Cloud Channel support. Return value: The new ChannelPartnerLink resource.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `cloudchannel_accounts_channel_partner_links_create_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `cloudchannel_accounts_channel_partner_links_create_task()`.
/// For the simplest API, use `cloudchannel_accounts_channel_partner_links_create()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `cloudchannel_accounts_channel_partner_links_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn cloudchannel_accounts_channel_partner_links_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudChannelV1ChannelPartnerLink>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = cloudchannel_accounts_channel_partner_links_create_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`cloudchannel_accounts_channel_partner_links_create`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct CloudchannelAccountsChannelPartnerLinksCreateArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Request body.
    pub body: GoogleCloudChannelV1ChannelPartnerLink,
}

/// GET v1/accounts/{accountsId}/channelPartnerLinks
/// Initiates a channel partner link between a distributor and a reseller, or between resellers in an n-tier reseller channel. Invited partners need to follow the invite_link_uri provided in the response to accept. After accepting the invitation, a link is set up between the two parties. You must be a distributor to call this method. Possible error codes: * PERMISSION_DENIED: The reseller account making the request is different from the reseller account in the API request. * INVALID_ARGUMENT: Required request parameters are missing or invalid. * ALREADY_EXISTS: The ChannelPartnerLink sent in the request already exists. * NOT_FOUND: No Cloud Identity customer exists for provided domain. * INTERNAL: Any non-user error related to a technical issue in the backend. Contact Cloud Channel support. * UNKNOWN: Any non-user error related to a technical issue in the backend. Contact Cloud Channel support. Return value: The new ChannelPartnerLink resource.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `cloudchannel_accounts_channel_partner_links_create_builder()` + `cloudchannel_accounts_channel_partner_links_create_execute()`.
/// For task-level control, use `cloudchannel_accounts_channel_partner_links_create_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudchannel_accounts_channel_partner_links_create(
    client: &SimpleHttpClient,
    args: &CloudchannelAccountsChannelPartnerLinksCreateArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudChannelV1ChannelPartnerLink>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = cloudchannel_accounts_channel_partner_links_create_builder(
        client,
        args.parent.clone(),
        &args.body,
    )?;
    cloudchannel_accounts_channel_partner_links_create_execute(builder)
}

/// GET v1/accounts/{accountsId}/customers
/// Creates a new Customer resource under the reseller or distributor account. Possible error codes: * PERMISSION_DENIED: * The reseller account making the request is different from the reseller account in the API request. * You are not authorized to create a customer. See <https://support.google.`com/channelservices/answer/9759265`> * INVALID_ARGUMENT: * Required request parameters are missing or invalid. * Domain field value doesn't match the primary email domain. Return value: The newly created Customer resource.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `cloudchannel_accounts_customers_create_execute()` to send, or `cloudchannel_accounts_customers_create` for simplest API.

pub fn cloudchannel_accounts_customers_create_builder(
    client: &SimpleHttpClient,
    parent: String,
    body: &GoogleCloudChannelV1Customer,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!("https://cloudchannel.googleapis.com/v1/accounts/{}/customers",);

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/accounts/{accountsId}/customers
/// Creates a new Customer resource under the reseller or distributor account. Possible error codes: * PERMISSION_DENIED: * The reseller account making the request is different from the reseller account in the API request. * You are not authorized to create a customer. See <https://support.google.`com/channelservices/answer/9759265`> * INVALID_ARGUMENT: * Required request parameters are missing or invalid. * Domain field value doesn't match the primary email domain. Return value: The newly created Customer resource.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `cloudchannel_accounts_customers_create_execute()` or `cloudchannel_accounts_customers_create`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `cloudchannel_accounts_customers_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudchannel_accounts_customers_create_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleCloudChannelV1Customer>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudChannelV1Customer = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/accounts/{accountsId}/customers
/// Creates a new Customer resource under the reseller or distributor account. Possible error codes: * PERMISSION_DENIED: * The reseller account making the request is different from the reseller account in the API request. * You are not authorized to create a customer. See <https://support.google.`com/channelservices/answer/9759265`> * INVALID_ARGUMENT: * Required request parameters are missing or invalid. * Domain field value doesn't match the primary email domain. Return value: The newly created Customer resource.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `cloudchannel_accounts_customers_create_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `cloudchannel_accounts_customers_create_task()`.
/// For the simplest API, use `cloudchannel_accounts_customers_create()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `cloudchannel_accounts_customers_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn cloudchannel_accounts_customers_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudChannelV1Customer>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = cloudchannel_accounts_customers_create_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`cloudchannel_accounts_customers_create`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct CloudchannelAccountsCustomersCreateArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Request body.
    pub body: GoogleCloudChannelV1Customer,
}

/// GET v1/accounts/{accountsId}/customers
/// Creates a new Customer resource under the reseller or distributor account. Possible error codes: * PERMISSION_DENIED: * The reseller account making the request is different from the reseller account in the API request. * You are not authorized to create a customer. See <https://support.google.`com/channelservices/answer/9759265`> * INVALID_ARGUMENT: * Required request parameters are missing or invalid. * Domain field value doesn't match the primary email domain. Return value: The newly created Customer resource.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `cloudchannel_accounts_customers_create_builder()` + `cloudchannel_accounts_customers_create_execute()`.
/// For task-level control, use `cloudchannel_accounts_customers_create_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudchannel_accounts_customers_create(
    client: &SimpleHttpClient,
    args: &CloudchannelAccountsCustomersCreateArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudChannelV1Customer>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        cloudchannel_accounts_customers_create_builder(client, args.parent.clone(), &args.body)?;
    cloudchannel_accounts_customers_create_execute(builder)
}

/// GET v1/accounts/{accountsId}/customers:import
/// Imports a Customer from the Cloud Identity associated with the provided Cloud Identity ID or domain before a TransferEntitlements call. If a linked Customer already exists and overwrite_if_exists is `true`, it will update that Customer's data. Possible error codes: * PERMISSION_DENIED: * The reseller account making the request is different from the reseller account in the API request. * You are not authorized to import the customer. See <https://support.google.`com/channelservices/answer/9759265`> * NOT_FOUND: Cloud Identity doesn't exist or was deleted. * INVALID_ARGUMENT: Required parameters are missing, or the auth_token is expired or invalid. * ALREADY_EXISTS: A customer already exists and has conflicting critical fields. Requires an overwrite. Return value: The Customer.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `cloudchannel_accounts_customers_import_execute()` to send, or `cloudchannel_accounts_customers_import` for simplest API.

pub fn cloudchannel_accounts_customers_import_builder(
    client: &SimpleHttpClient,
    parent: String,
    body: &GoogleCloudChannelV1ImportCustomerRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url =
        format!("https://cloudchannel.googleapis.com/v1/accounts/{}/customers:import",);

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/accounts/{accountsId}/customers:import
/// Imports a Customer from the Cloud Identity associated with the provided Cloud Identity ID or domain before a TransferEntitlements call. If a linked Customer already exists and overwrite_if_exists is `true`, it will update that Customer's data. Possible error codes: * PERMISSION_DENIED: * The reseller account making the request is different from the reseller account in the API request. * You are not authorized to import the customer. See <https://support.google.`com/channelservices/answer/9759265`> * NOT_FOUND: Cloud Identity doesn't exist or was deleted. * INVALID_ARGUMENT: Required parameters are missing, or the auth_token is expired or invalid. * ALREADY_EXISTS: A customer already exists and has conflicting critical fields. Requires an overwrite. Return value: The Customer.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `cloudchannel_accounts_customers_import_execute()` or `cloudchannel_accounts_customers_import`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `cloudchannel_accounts_customers_import_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudchannel_accounts_customers_import_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleCloudChannelV1Customer>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudChannelV1Customer = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/accounts/{accountsId}/customers:import
/// Imports a Customer from the Cloud Identity associated with the provided Cloud Identity ID or domain before a TransferEntitlements call. If a linked Customer already exists and overwrite_if_exists is `true`, it will update that Customer's data. Possible error codes: * PERMISSION_DENIED: * The reseller account making the request is different from the reseller account in the API request. * You are not authorized to import the customer. See <https://support.google.`com/channelservices/answer/9759265`> * NOT_FOUND: Cloud Identity doesn't exist or was deleted. * INVALID_ARGUMENT: Required parameters are missing, or the auth_token is expired or invalid. * ALREADY_EXISTS: A customer already exists and has conflicting critical fields. Requires an overwrite. Return value: The Customer.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `cloudchannel_accounts_customers_import_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `cloudchannel_accounts_customers_import_task()`.
/// For the simplest API, use `cloudchannel_accounts_customers_import()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `cloudchannel_accounts_customers_import_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn cloudchannel_accounts_customers_import_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudChannelV1Customer>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = cloudchannel_accounts_customers_import_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`cloudchannel_accounts_customers_import`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct CloudchannelAccountsCustomersImportArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Request body.
    pub body: GoogleCloudChannelV1ImportCustomerRequest,
}

/// GET v1/accounts/{accountsId}/customers:import
/// Imports a Customer from the Cloud Identity associated with the provided Cloud Identity ID or domain before a TransferEntitlements call. If a linked Customer already exists and overwrite_if_exists is `true`, it will update that Customer's data. Possible error codes: * PERMISSION_DENIED: * The reseller account making the request is different from the reseller account in the API request. * You are not authorized to import the customer. See <https://support.google.`com/channelservices/answer/9759265`> * NOT_FOUND: Cloud Identity doesn't exist or was deleted. * INVALID_ARGUMENT: Required parameters are missing, or the auth_token is expired or invalid. * ALREADY_EXISTS: A customer already exists and has conflicting critical fields. Requires an overwrite. Return value: The Customer.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `cloudchannel_accounts_customers_import_builder()` + `cloudchannel_accounts_customers_import_execute()`.
/// For task-level control, use `cloudchannel_accounts_customers_import_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudchannel_accounts_customers_import(
    client: &SimpleHttpClient,
    args: &CloudchannelAccountsCustomersImportArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudChannelV1Customer>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        cloudchannel_accounts_customers_import_builder(client, args.parent.clone(), &args.body)?;
    cloudchannel_accounts_customers_import_execute(builder)
}

/// GET v1/accounts/{accountsId}/offers
/// Lists the Offers the reseller can sell. Possible error codes: * INVALID_ARGUMENT: Required request parameters are missing or invalid.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `cloudchannel_accounts_offers_list_execute()` to send, or `cloudchannel_accounts_offers_list` for simplest API.

pub fn cloudchannel_accounts_offers_list_builder(
    client: &SimpleHttpClient,
    parent: String,
    filter: Option<String>,
    languageCode: Option<String>,
    pageSize: Option<i32>,
    pageToken: Option<String>,
    showFutureOffers: Option<bool>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!("https://cloudchannel.googleapis.com/v1/accounts/{}/offers",);

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = languageCode {
        query_parts.push(format!("languageCode={}", val));
    }
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = showFutureOffers {
        query_parts.push(format!("showFutureOffers={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/accounts/{accountsId}/offers
/// Lists the Offers the reseller can sell. Possible error codes: * INVALID_ARGUMENT: Required request parameters are missing or invalid.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `cloudchannel_accounts_offers_list_execute()` or `cloudchannel_accounts_offers_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `cloudchannel_accounts_offers_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudchannel_accounts_offers_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleCloudChannelV1ListOffersResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudChannelV1ListOffersResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/accounts/{accountsId}/offers
/// Lists the Offers the reseller can sell. Possible error codes: * INVALID_ARGUMENT: Required request parameters are missing or invalid.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `cloudchannel_accounts_offers_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `cloudchannel_accounts_offers_list_task()`.
/// For the simplest API, use `cloudchannel_accounts_offers_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `cloudchannel_accounts_offers_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn cloudchannel_accounts_offers_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudChannelV1ListOffersResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = cloudchannel_accounts_offers_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`cloudchannel_accounts_offers_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct CloudchannelAccountsOffersListArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: filter
    pub filter: Option<String>,
    /// Query parameter: languageCode
    pub languageCode: Option<String>,
    /// Query parameter: pageSize
    pub pageSize: Option<i32>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
    /// Query parameter: showFutureOffers
    pub showFutureOffers: Option<bool>,
}

/// GET v1/accounts/{accountsId}/offers
/// Lists the Offers the reseller can sell. Possible error codes: * INVALID_ARGUMENT: Required request parameters are missing or invalid.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `cloudchannel_accounts_offers_list_builder()` + `cloudchannel_accounts_offers_list_execute()`.
/// For task-level control, use `cloudchannel_accounts_offers_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudchannel_accounts_offers_list(
    client: &SimpleHttpClient,
    args: &CloudchannelAccountsOffersListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudChannelV1ListOffersResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = cloudchannel_accounts_offers_list_builder(
        client,
        args.parent.clone(),
        args.filter.clone(),
        args.languageCode.clone(),
        args.pageSize.clone(),
        args.pageToken.clone(),
        args.showFutureOffers.clone(),
    )?;
    cloudchannel_accounts_offers_list_execute(builder)
}

/// GET v1/accounts/{accountsId}/reports
/// Lists the reports that RunReportJob can run. These reports include an ID, a description, and the list of columns that will be in the result. Deprecated: Please use [Export Channel Services data to BigQuery](<https://cloud.google.`com/channel/docs/rebilling/export-data-to-bigquery`>) instead.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `cloudchannel_accounts_reports_list_execute()` to send, or `cloudchannel_accounts_reports_list` for simplest API.

pub fn cloudchannel_accounts_reports_list_builder(
    client: &SimpleHttpClient,
    parent: String,
    languageCode: Option<String>,
    pageSize: Option<i32>,
    pageToken: Option<String>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!("https://cloudchannel.googleapis.com/v1/accounts/{}/reports",);

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = languageCode {
        query_parts.push(format!("languageCode={}", val));
    }
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/accounts/{accountsId}/reports
/// Lists the reports that RunReportJob can run. These reports include an ID, a description, and the list of columns that will be in the result. Deprecated: Please use [Export Channel Services data to BigQuery](<https://cloud.google.`com/channel/docs/rebilling/export-data-to-bigquery`>) instead.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `cloudchannel_accounts_reports_list_execute()` or `cloudchannel_accounts_reports_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `cloudchannel_accounts_reports_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudchannel_accounts_reports_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleCloudChannelV1ListReportsResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudChannelV1ListReportsResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/accounts/{accountsId}/reports
/// Lists the reports that RunReportJob can run. These reports include an ID, a description, and the list of columns that will be in the result. Deprecated: Please use [Export Channel Services data to BigQuery](<https://cloud.google.`com/channel/docs/rebilling/export-data-to-bigquery`>) instead.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `cloudchannel_accounts_reports_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `cloudchannel_accounts_reports_list_task()`.
/// For the simplest API, use `cloudchannel_accounts_reports_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `cloudchannel_accounts_reports_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn cloudchannel_accounts_reports_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudChannelV1ListReportsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = cloudchannel_accounts_reports_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`cloudchannel_accounts_reports_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct CloudchannelAccountsReportsListArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: languageCode
    pub languageCode: Option<String>,
    /// Query parameter: pageSize
    pub pageSize: Option<i32>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
}

/// GET v1/accounts/{accountsId}/reports
/// Lists the reports that RunReportJob can run. These reports include an ID, a description, and the list of columns that will be in the result. Deprecated: Please use [Export Channel Services data to BigQuery](<https://cloud.google.`com/channel/docs/rebilling/export-data-to-bigquery`>) instead.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `cloudchannel_accounts_reports_list_builder()` + `cloudchannel_accounts_reports_list_execute()`.
/// For task-level control, use `cloudchannel_accounts_reports_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudchannel_accounts_reports_list(
    client: &SimpleHttpClient,
    args: &CloudchannelAccountsReportsListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudChannelV1ListReportsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = cloudchannel_accounts_reports_list_builder(
        client,
        args.parent.clone(),
        args.languageCode.clone(),
        args.pageSize.clone(),
        args.pageToken.clone(),
    )?;
    cloudchannel_accounts_reports_list_execute(builder)
}

/// GET v1/accounts/{accountsId}/skuGroups
/// Lists the Rebilling supported SKU groups the account is authorized to sell. Reference: <https://cloud.google.`com/skus/sku-groups`> Possible Error Codes: * PERMISSION_DENIED: If the account making the request and the account being queried are different, or the account doesn't exist. * INTERNAL: Any non-user error related to technical issues in the backend. In this case, contact Cloud Channel support. Return Value: If successful, the SkuGroup resources. The data for each resource is displayed in the alphabetical order of SKU group display name. The data for each resource is displayed in the ascending order of SkuGroup.display_name If unsuccessful, returns an error.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `cloudchannel_accounts_sku_groups_list_execute()` to send, or `cloudchannel_accounts_sku_groups_list` for simplest API.

pub fn cloudchannel_accounts_sku_groups_list_builder(
    client: &SimpleHttpClient,
    parent: String,
    pageSize: Option<i32>,
    pageToken: Option<String>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!("https://cloudchannel.googleapis.com/v1/accounts/{}/skuGroups",);

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/accounts/{accountsId}/skuGroups
/// Lists the Rebilling supported SKU groups the account is authorized to sell. Reference: <https://cloud.google.`com/skus/sku-groups`> Possible Error Codes: * PERMISSION_DENIED: If the account making the request and the account being queried are different, or the account doesn't exist. * INTERNAL: Any non-user error related to technical issues in the backend. In this case, contact Cloud Channel support. Return Value: If successful, the SkuGroup resources. The data for each resource is displayed in the alphabetical order of SKU group display name. The data for each resource is displayed in the ascending order of SkuGroup.display_name If unsuccessful, returns an error.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `cloudchannel_accounts_sku_groups_list_execute()` or `cloudchannel_accounts_sku_groups_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `cloudchannel_accounts_sku_groups_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudchannel_accounts_sku_groups_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleCloudChannelV1ListSkuGroupsResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudChannelV1ListSkuGroupsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/accounts/{accountsId}/skuGroups
/// Lists the Rebilling supported SKU groups the account is authorized to sell. Reference: <https://cloud.google.`com/skus/sku-groups`> Possible Error Codes: * PERMISSION_DENIED: If the account making the request and the account being queried are different, or the account doesn't exist. * INTERNAL: Any non-user error related to technical issues in the backend. In this case, contact Cloud Channel support. Return Value: If successful, the SkuGroup resources. The data for each resource is displayed in the alphabetical order of SKU group display name. The data for each resource is displayed in the ascending order of SkuGroup.display_name If unsuccessful, returns an error.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `cloudchannel_accounts_sku_groups_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `cloudchannel_accounts_sku_groups_list_task()`.
/// For the simplest API, use `cloudchannel_accounts_sku_groups_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `cloudchannel_accounts_sku_groups_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn cloudchannel_accounts_sku_groups_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudChannelV1ListSkuGroupsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = cloudchannel_accounts_sku_groups_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`cloudchannel_accounts_sku_groups_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct CloudchannelAccountsSkuGroupsListArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: pageSize
    pub pageSize: Option<i32>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
}

/// GET v1/accounts/{accountsId}/skuGroups
/// Lists the Rebilling supported SKU groups the account is authorized to sell. Reference: <https://cloud.google.`com/skus/sku-groups`> Possible Error Codes: * PERMISSION_DENIED: If the account making the request and the account being queried are different, or the account doesn't exist. * INTERNAL: Any non-user error related to technical issues in the backend. In this case, contact Cloud Channel support. Return Value: If successful, the SkuGroup resources. The data for each resource is displayed in the alphabetical order of SKU group display name. The data for each resource is displayed in the ascending order of SkuGroup.display_name If unsuccessful, returns an error.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `cloudchannel_accounts_sku_groups_list_builder()` + `cloudchannel_accounts_sku_groups_list_execute()`.
/// For task-level control, use `cloudchannel_accounts_sku_groups_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudchannel_accounts_sku_groups_list(
    client: &SimpleHttpClient,
    args: &CloudchannelAccountsSkuGroupsListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudChannelV1ListSkuGroupsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = cloudchannel_accounts_sku_groups_list_builder(
        client,
        args.parent.clone(),
        args.pageSize.clone(),
        args.pageToken.clone(),
    )?;
    cloudchannel_accounts_sku_groups_list_execute(builder)
}

/// GET v1/integrators/{integratorsId}:listSubscribers
/// Lists service accounts with subscriber privileges on the P`ub/Sub` topic created for this Channel Services account or integrator. Possible error codes: * PERMISSION_DENIED: The reseller account making the request and the provided reseller account are different, or the impersonated user is not a super admin. * INVALID_ARGUMENT: Required request parameters are missing or invalid. * NOT_FOUND: The topic resource doesn't exist. * INTERNAL: Any non-user error related to a technical issue in the backend. Contact Cloud Channel support. * UNKNOWN: Any non-user error related to a technical issue in the backend. Contact Cloud Channel support. Return value: A list of service email addresses.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `cloudchannel_integrators_list_subscribers_execute()` to send, or `cloudchannel_integrators_list_subscribers` for simplest API.

pub fn cloudchannel_integrators_list_subscribers_builder(
    client: &SimpleHttpClient,
    integrator: String,
    account: Option<String>,
    pageSize: Option<i32>,
    pageToken: Option<String>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url =
        format!("https://cloudchannel.googleapis.com/v1/integrators/{}:listSubscribers",);

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = account {
        query_parts.push(format!("account={}", val));
    }
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/integrators/{integratorsId}:listSubscribers
/// Lists service accounts with subscriber privileges on the P`ub/Sub` topic created for this Channel Services account or integrator. Possible error codes: * PERMISSION_DENIED: The reseller account making the request and the provided reseller account are different, or the impersonated user is not a super admin. * INVALID_ARGUMENT: Required request parameters are missing or invalid. * NOT_FOUND: The topic resource doesn't exist. * INTERNAL: Any non-user error related to a technical issue in the backend. Contact Cloud Channel support. * UNKNOWN: Any non-user error related to a technical issue in the backend. Contact Cloud Channel support. Return value: A list of service email addresses.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `cloudchannel_integrators_list_subscribers_execute()` or `cloudchannel_integrators_list_subscribers`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `cloudchannel_integrators_list_subscribers_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudchannel_integrators_list_subscribers_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleCloudChannelV1ListSubscribersResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudChannelV1ListSubscribersResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/integrators/{integratorsId}:listSubscribers
/// Lists service accounts with subscriber privileges on the P`ub/Sub` topic created for this Channel Services account or integrator. Possible error codes: * PERMISSION_DENIED: The reseller account making the request and the provided reseller account are different, or the impersonated user is not a super admin. * INVALID_ARGUMENT: Required request parameters are missing or invalid. * NOT_FOUND: The topic resource doesn't exist. * INTERNAL: Any non-user error related to a technical issue in the backend. Contact Cloud Channel support. * UNKNOWN: Any non-user error related to a technical issue in the backend. Contact Cloud Channel support. Return value: A list of service email addresses.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `cloudchannel_integrators_list_subscribers_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `cloudchannel_integrators_list_subscribers_task()`.
/// For the simplest API, use `cloudchannel_integrators_list_subscribers()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `cloudchannel_integrators_list_subscribers_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn cloudchannel_integrators_list_subscribers_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudChannelV1ListSubscribersResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = cloudchannel_integrators_list_subscribers_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`cloudchannel_integrators_list_subscribers`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct CloudchannelIntegratorsListSubscribersArgs {
    /// Path parameter: integrator
    pub integrator: String,
    /// Query parameter: account
    pub account: Option<String>,
    /// Query parameter: pageSize
    pub pageSize: Option<i32>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
}

/// GET v1/integrators/{integratorsId}:listSubscribers
/// Lists service accounts with subscriber privileges on the P`ub/Sub` topic created for this Channel Services account or integrator. Possible error codes: * PERMISSION_DENIED: The reseller account making the request and the provided reseller account are different, or the impersonated user is not a super admin. * INVALID_ARGUMENT: Required request parameters are missing or invalid. * NOT_FOUND: The topic resource doesn't exist. * INTERNAL: Any non-user error related to a technical issue in the backend. Contact Cloud Channel support. * UNKNOWN: Any non-user error related to a technical issue in the backend. Contact Cloud Channel support. Return value: A list of service email addresses.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `cloudchannel_integrators_list_subscribers_builder()` + `cloudchannel_integrators_list_subscribers_execute()`.
/// For task-level control, use `cloudchannel_integrators_list_subscribers_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudchannel_integrators_list_subscribers(
    client: &SimpleHttpClient,
    args: &CloudchannelIntegratorsListSubscribersArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudChannelV1ListSubscribersResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = cloudchannel_integrators_list_subscribers_builder(
        client,
        args.integrator.clone(),
        args.account.clone(),
        args.pageSize.clone(),
        args.pageToken.clone(),
    )?;
    cloudchannel_integrators_list_subscribers_execute(builder)
}

/// GET v1/integrators/{integratorsId}:registerSubscriber
/// Registers a service account with subscriber privileges on the P`ub/Sub` topic for this Channel Services account or integrator. After you create a subscriber, you get the events through SubscriberEvent Possible error codes: * PERMISSION_DENIED: The reseller account making the request and the provided reseller account are different, or the impersonated user is not a super admin. * INVALID_ARGUMENT: Required request parameters are missing or invalid. * INTERNAL: Any non-user error related to a technical issue in the backend. Contact Cloud Channel support. * UNKNOWN: Any non-user error related to a technical issue in the backend. Contact Cloud Channel support. Return value: The topic name with the registered service email address.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `cloudchannel_integrators_register_subscriber_execute()` to send, or `cloudchannel_integrators_register_subscriber` for simplest API.

pub fn cloudchannel_integrators_register_subscriber_builder(
    client: &SimpleHttpClient,
    integrator: String,
    body: &GoogleCloudChannelV1RegisterSubscriberRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url =
        format!("https://cloudchannel.googleapis.com/v1/integrators/{}:registerSubscriber",);

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/integrators/{integratorsId}:registerSubscriber
/// Registers a service account with subscriber privileges on the P`ub/Sub` topic for this Channel Services account or integrator. After you create a subscriber, you get the events through SubscriberEvent Possible error codes: * PERMISSION_DENIED: The reseller account making the request and the provided reseller account are different, or the impersonated user is not a super admin. * INVALID_ARGUMENT: Required request parameters are missing or invalid. * INTERNAL: Any non-user error related to a technical issue in the backend. Contact Cloud Channel support. * UNKNOWN: Any non-user error related to a technical issue in the backend. Contact Cloud Channel support. Return value: The topic name with the registered service email address.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `cloudchannel_integrators_register_subscriber_execute()` or `cloudchannel_integrators_register_subscriber`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `cloudchannel_integrators_register_subscriber_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudchannel_integrators_register_subscriber_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleCloudChannelV1RegisterSubscriberResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudChannelV1RegisterSubscriberResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/integrators/{integratorsId}:registerSubscriber
/// Registers a service account with subscriber privileges on the P`ub/Sub` topic for this Channel Services account or integrator. After you create a subscriber, you get the events through SubscriberEvent Possible error codes: * PERMISSION_DENIED: The reseller account making the request and the provided reseller account are different, or the impersonated user is not a super admin. * INVALID_ARGUMENT: Required request parameters are missing or invalid. * INTERNAL: Any non-user error related to a technical issue in the backend. Contact Cloud Channel support. * UNKNOWN: Any non-user error related to a technical issue in the backend. Contact Cloud Channel support. Return value: The topic name with the registered service email address.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `cloudchannel_integrators_register_subscriber_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `cloudchannel_integrators_register_subscriber_task()`.
/// For the simplest API, use `cloudchannel_integrators_register_subscriber()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `cloudchannel_integrators_register_subscriber_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn cloudchannel_integrators_register_subscriber_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudChannelV1RegisterSubscriberResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = cloudchannel_integrators_register_subscriber_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`cloudchannel_integrators_register_subscriber`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct CloudchannelIntegratorsRegisterSubscriberArgs {
    /// Path parameter: integrator
    pub integrator: String,
    /// Request body.
    pub body: GoogleCloudChannelV1RegisterSubscriberRequest,
}

/// GET v1/integrators/{integratorsId}:registerSubscriber
/// Registers a service account with subscriber privileges on the P`ub/Sub` topic for this Channel Services account or integrator. After you create a subscriber, you get the events through SubscriberEvent Possible error codes: * PERMISSION_DENIED: The reseller account making the request and the provided reseller account are different, or the impersonated user is not a super admin. * INVALID_ARGUMENT: Required request parameters are missing or invalid. * INTERNAL: Any non-user error related to a technical issue in the backend. Contact Cloud Channel support. * UNKNOWN: Any non-user error related to a technical issue in the backend. Contact Cloud Channel support. Return value: The topic name with the registered service email address.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `cloudchannel_integrators_register_subscriber_builder()` + `cloudchannel_integrators_register_subscriber_execute()`.
/// For task-level control, use `cloudchannel_integrators_register_subscriber_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudchannel_integrators_register_subscriber(
    client: &SimpleHttpClient,
    args: &CloudchannelIntegratorsRegisterSubscriberArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudChannelV1RegisterSubscriberResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = cloudchannel_integrators_register_subscriber_builder(
        client,
        args.integrator.clone(),
        &args.body,
    )?;
    cloudchannel_integrators_register_subscriber_execute(builder)
}

/// GET v1/integrators/{integratorsId}:unregisterSubscriber
/// Unregisters a service account with subscriber privileges on the P`ub/Sub` topic created for this Channel Services account or integrator. If there are no service accounts left with subscriber privileges, this deletes the topic. You can call ListSubscribers to check for these accounts. Possible error codes: * PERMISSION_DENIED: The reseller account making the request and the provided reseller account are different, or the impersonated user is not a super admin. * INVALID_ARGUMENT: Required request parameters are missing or invalid. * NOT_FOUND: The topic resource doesn't exist. * INTERNAL: Any non-user error related to a technical issue in the backend. Contact Cloud Channel support. * UNKNOWN: Any non-user error related to a technical issue in the backend. Contact Cloud Channel support. Return value: The topic name that unregistered the service email address. Returns a success response if the service email address wasn't registered with the topic.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `cloudchannel_integrators_unregister_subscriber_execute()` to send, or `cloudchannel_integrators_unregister_subscriber` for simplest API.

pub fn cloudchannel_integrators_unregister_subscriber_builder(
    client: &SimpleHttpClient,
    integrator: String,
    body: &GoogleCloudChannelV1UnregisterSubscriberRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url =
        format!("https://cloudchannel.googleapis.com/v1/integrators/{}:unregisterSubscriber",);

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/integrators/{integratorsId}:unregisterSubscriber
/// Unregisters a service account with subscriber privileges on the P`ub/Sub` topic created for this Channel Services account or integrator. If there are no service accounts left with subscriber privileges, this deletes the topic. You can call ListSubscribers to check for these accounts. Possible error codes: * PERMISSION_DENIED: The reseller account making the request and the provided reseller account are different, or the impersonated user is not a super admin. * INVALID_ARGUMENT: Required request parameters are missing or invalid. * NOT_FOUND: The topic resource doesn't exist. * INTERNAL: Any non-user error related to a technical issue in the backend. Contact Cloud Channel support. * UNKNOWN: Any non-user error related to a technical issue in the backend. Contact Cloud Channel support. Return value: The topic name that unregistered the service email address. Returns a success response if the service email address wasn't registered with the topic.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `cloudchannel_integrators_unregister_subscriber_execute()` or `cloudchannel_integrators_unregister_subscriber`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `cloudchannel_integrators_unregister_subscriber_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudchannel_integrators_unregister_subscriber_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleCloudChannelV1UnregisterSubscriberResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudChannelV1UnregisterSubscriberResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/integrators/{integratorsId}:unregisterSubscriber
/// Unregisters a service account with subscriber privileges on the P`ub/Sub` topic created for this Channel Services account or integrator. If there are no service accounts left with subscriber privileges, this deletes the topic. You can call ListSubscribers to check for these accounts. Possible error codes: * PERMISSION_DENIED: The reseller account making the request and the provided reseller account are different, or the impersonated user is not a super admin. * INVALID_ARGUMENT: Required request parameters are missing or invalid. * NOT_FOUND: The topic resource doesn't exist. * INTERNAL: Any non-user error related to a technical issue in the backend. Contact Cloud Channel support. * UNKNOWN: Any non-user error related to a technical issue in the backend. Contact Cloud Channel support. Return value: The topic name that unregistered the service email address. Returns a success response if the service email address wasn't registered with the topic.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `cloudchannel_integrators_unregister_subscriber_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `cloudchannel_integrators_unregister_subscriber_task()`.
/// For the simplest API, use `cloudchannel_integrators_unregister_subscriber()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `cloudchannel_integrators_unregister_subscriber_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn cloudchannel_integrators_unregister_subscriber_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudChannelV1UnregisterSubscriberResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = cloudchannel_integrators_unregister_subscriber_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`cloudchannel_integrators_unregister_subscriber`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct CloudchannelIntegratorsUnregisterSubscriberArgs {
    /// Path parameter: integrator
    pub integrator: String,
    /// Request body.
    pub body: GoogleCloudChannelV1UnregisterSubscriberRequest,
}

/// GET v1/integrators/{integratorsId}:unregisterSubscriber
/// Unregisters a service account with subscriber privileges on the P`ub/Sub` topic created for this Channel Services account or integrator. If there are no service accounts left with subscriber privileges, this deletes the topic. You can call ListSubscribers to check for these accounts. Possible error codes: * PERMISSION_DENIED: The reseller account making the request and the provided reseller account are different, or the impersonated user is not a super admin. * INVALID_ARGUMENT: Required request parameters are missing or invalid. * NOT_FOUND: The topic resource doesn't exist. * INTERNAL: Any non-user error related to a technical issue in the backend. Contact Cloud Channel support. * UNKNOWN: Any non-user error related to a technical issue in the backend. Contact Cloud Channel support. Return value: The topic name that unregistered the service email address. Returns a success response if the service email address wasn't registered with the topic.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `cloudchannel_integrators_unregister_subscriber_builder()` + `cloudchannel_integrators_unregister_subscriber_execute()`.
/// For task-level control, use `cloudchannel_integrators_unregister_subscriber_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudchannel_integrators_unregister_subscriber(
    client: &SimpleHttpClient,
    args: &CloudchannelIntegratorsUnregisterSubscriberArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudChannelV1UnregisterSubscriberResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = cloudchannel_integrators_unregister_subscriber_builder(
        client,
        args.integrator.clone(),
        &args.body,
    )?;
    cloudchannel_integrators_unregister_subscriber_execute(builder)
}

/// GET v1/operations/{operationsId}:cancel
/// Starts asynchronous cancellation on a long-running operation. The server makes a best effort to cancel the operation, but success is not guaranteed. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED. Clients can use Operations.GetOperation or other methods to check whether the cancellation succeeded or whether the operation completed despite cancellation. On successful cancellation, the operation is not deleted; instead, it becomes an operation with an Operation.error value with a google.rpc.Status.code of 1, corresponding to Code.CANCELLED.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `cloudchannel_operations_cancel_execute()` to send, or `cloudchannel_operations_cancel` for simplest API.

pub fn cloudchannel_operations_cancel_builder(
    client: &SimpleHttpClient,
    name: String,
    body: &GoogleLongrunningCancelOperationRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!("https://cloudchannel.googleapis.com/v1/operations/{}:cancel",);

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/operations/{operationsId}:cancel
/// Starts asynchronous cancellation on a long-running operation. The server makes a best effort to cancel the operation, but success is not guaranteed. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED. Clients can use Operations.GetOperation or other methods to check whether the cancellation succeeded or whether the operation completed despite cancellation. On successful cancellation, the operation is not deleted; instead, it becomes an operation with an Operation.error value with a google.rpc.Status.code of 1, corresponding to Code.CANCELLED.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `cloudchannel_operations_cancel_execute()` or `cloudchannel_operations_cancel`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `cloudchannel_operations_cancel_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudchannel_operations_cancel_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleProtobufEmpty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/operations/{operationsId}:cancel
/// Starts asynchronous cancellation on a long-running operation. The server makes a best effort to cancel the operation, but success is not guaranteed. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED. Clients can use Operations.GetOperation or other methods to check whether the cancellation succeeded or whether the operation completed despite cancellation. On successful cancellation, the operation is not deleted; instead, it becomes an operation with an Operation.error value with a google.rpc.Status.code of 1, corresponding to Code.CANCELLED.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `cloudchannel_operations_cancel_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `cloudchannel_operations_cancel_task()`.
/// For the simplest API, use `cloudchannel_operations_cancel()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `cloudchannel_operations_cancel_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn cloudchannel_operations_cancel_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = cloudchannel_operations_cancel_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`cloudchannel_operations_cancel`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct CloudchannelOperationsCancelArgs {
    /// Path parameter: name
    pub name: String,
    /// Request body.
    pub body: GoogleLongrunningCancelOperationRequest,
}

/// GET v1/operations/{operationsId}:cancel
/// Starts asynchronous cancellation on a long-running operation. The server makes a best effort to cancel the operation, but success is not guaranteed. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED. Clients can use Operations.GetOperation or other methods to check whether the cancellation succeeded or whether the operation completed despite cancellation. On successful cancellation, the operation is not deleted; instead, it becomes an operation with an Operation.error value with a google.rpc.Status.code of 1, corresponding to Code.CANCELLED.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `cloudchannel_operations_cancel_builder()` + `cloudchannel_operations_cancel_execute()`.
/// For task-level control, use `cloudchannel_operations_cancel_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudchannel_operations_cancel(
    client: &SimpleHttpClient,
    args: &CloudchannelOperationsCancelArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = cloudchannel_operations_cancel_builder(client, args.name.clone(), &args.body)?;
    cloudchannel_operations_cancel_execute(builder)
}

/// GET v1/operations/{operationsId}
/// Deletes a long-running operation. This method indicates that the client is no longer interested in the operation result. It does not cancel the operation. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `cloudchannel_operations_delete_execute()` to send, or `cloudchannel_operations_delete` for simplest API.

pub fn cloudchannel_operations_delete_builder(
    client: &SimpleHttpClient,
    name: String,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!("https://cloudchannel.googleapis.com/v1/operations/{}",);

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/operations/{operationsId}
/// Deletes a long-running operation. This method indicates that the client is no longer interested in the operation result. It does not cancel the operation. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `cloudchannel_operations_delete_execute()` or `cloudchannel_operations_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `cloudchannel_operations_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudchannel_operations_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleProtobufEmpty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/operations/{operationsId}
/// Deletes a long-running operation. This method indicates that the client is no longer interested in the operation result. It does not cancel the operation. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `cloudchannel_operations_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `cloudchannel_operations_delete_task()`.
/// For the simplest API, use `cloudchannel_operations_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `cloudchannel_operations_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn cloudchannel_operations_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = cloudchannel_operations_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`cloudchannel_operations_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct CloudchannelOperationsDeleteArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/operations/{operationsId}
/// Deletes a long-running operation. This method indicates that the client is no longer interested in the operation result. It does not cancel the operation. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `cloudchannel_operations_delete_builder()` + `cloudchannel_operations_delete_execute()`.
/// For task-level control, use `cloudchannel_operations_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudchannel_operations_delete(
    client: &SimpleHttpClient,
    args: &CloudchannelOperationsDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = cloudchannel_operations_delete_builder(client, args.name.clone())?;
    cloudchannel_operations_delete_execute(builder)
}

/// GET v1/products
/// Lists the Products the reseller is authorized to sell. Possible error codes: * INVALID_ARGUMENT: Required request parameters are missing or invalid.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `cloudchannel_products_list_execute()` to send, or `cloudchannel_products_list` for simplest API.

pub fn cloudchannel_products_list_builder(
    client: &SimpleHttpClient,
    account: Option<String>,
    languageCode: Option<String>,
    pageSize: Option<i32>,
    pageToken: Option<String>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!("https://cloudchannel.googleapis.com/v1/products",);

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = account {
        query_parts.push(format!("account={}", val));
    }
    if let Some(val) = languageCode {
        query_parts.push(format!("languageCode={}", val));
    }
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/products
/// Lists the Products the reseller is authorized to sell. Possible error codes: * INVALID_ARGUMENT: Required request parameters are missing or invalid.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `cloudchannel_products_list_execute()` or `cloudchannel_products_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `cloudchannel_products_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudchannel_products_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleCloudChannelV1ListProductsResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudChannelV1ListProductsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/products
/// Lists the Products the reseller is authorized to sell. Possible error codes: * INVALID_ARGUMENT: Required request parameters are missing or invalid.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `cloudchannel_products_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `cloudchannel_products_list_task()`.
/// For the simplest API, use `cloudchannel_products_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `cloudchannel_products_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn cloudchannel_products_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudChannelV1ListProductsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = cloudchannel_products_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`cloudchannel_products_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct CloudchannelProductsListArgs {
    /// Query parameter: account
    pub account: Option<String>,
    /// Query parameter: languageCode
    pub languageCode: Option<String>,
    /// Query parameter: pageSize
    pub pageSize: Option<i32>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
}

/// GET v1/products
/// Lists the Products the reseller is authorized to sell. Possible error codes: * INVALID_ARGUMENT: Required request parameters are missing or invalid.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `cloudchannel_products_list_builder()` + `cloudchannel_products_list_execute()`.
/// For task-level control, use `cloudchannel_products_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudchannel_products_list(
    client: &SimpleHttpClient,
    args: &CloudchannelProductsListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudChannelV1ListProductsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = cloudchannel_products_list_builder(
        client,
        args.account.clone(),
        args.languageCode.clone(),
        args.pageSize.clone(),
        args.pageToken.clone(),
    )?;
    cloudchannel_products_list_execute(builder)
}

/// GET v1/products/{productsId}/skus
/// Lists the SKUs for a product the reseller is authorized to sell. Possible error codes: * INVALID_ARGUMENT: Required request parameters are missing or invalid.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `cloudchannel_products_skus_list_execute()` to send, or `cloudchannel_products_skus_list` for simplest API.

pub fn cloudchannel_products_skus_list_builder(
    client: &SimpleHttpClient,
    parent: String,
    account: Option<String>,
    languageCode: Option<String>,
    pageSize: Option<i32>,
    pageToken: Option<String>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!("https://cloudchannel.googleapis.com/v1/products/{}/skus",);

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = account {
        query_parts.push(format!("account={}", val));
    }
    if let Some(val) = languageCode {
        query_parts.push(format!("languageCode={}", val));
    }
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/products/{productsId}/skus
/// Lists the SKUs for a product the reseller is authorized to sell. Possible error codes: * INVALID_ARGUMENT: Required request parameters are missing or invalid.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `cloudchannel_products_skus_list_execute()` or `cloudchannel_products_skus_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `cloudchannel_products_skus_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudchannel_products_skus_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleCloudChannelV1ListSkusResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudChannelV1ListSkusResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/products/{productsId}/skus
/// Lists the SKUs for a product the reseller is authorized to sell. Possible error codes: * INVALID_ARGUMENT: Required request parameters are missing or invalid.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `cloudchannel_products_skus_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `cloudchannel_products_skus_list_task()`.
/// For the simplest API, use `cloudchannel_products_skus_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `cloudchannel_products_skus_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn cloudchannel_products_skus_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudChannelV1ListSkusResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = cloudchannel_products_skus_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`cloudchannel_products_skus_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct CloudchannelProductsSkusListArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: account
    pub account: Option<String>,
    /// Query parameter: languageCode
    pub languageCode: Option<String>,
    /// Query parameter: pageSize
    pub pageSize: Option<i32>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
}

/// GET v1/products/{productsId}/skus
/// Lists the SKUs for a product the reseller is authorized to sell. Possible error codes: * INVALID_ARGUMENT: Required request parameters are missing or invalid.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `cloudchannel_products_skus_list_builder()` + `cloudchannel_products_skus_list_execute()`.
/// For task-level control, use `cloudchannel_products_skus_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudchannel_products_skus_list(
    client: &SimpleHttpClient,
    args: &CloudchannelProductsSkusListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudChannelV1ListSkusResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = cloudchannel_products_skus_list_builder(
        client,
        args.parent.clone(),
        args.account.clone(),
        args.languageCode.clone(),
        args.pageSize.clone(),
        args.pageToken.clone(),
    )?;
    cloudchannel_products_skus_list_execute(builder)
}
