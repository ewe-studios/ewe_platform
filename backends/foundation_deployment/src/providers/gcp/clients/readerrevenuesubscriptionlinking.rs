//! Auto-generated API clients for Google Cloud Platform - readerrevenuesubscriptionlinking.
//!
//! Generated by `cargo run --bin ewe_platform gen_provider_clients`.
//! DO NOT EDIT MANUALLY.
//!
//! Feature flag: `gcp`


#![cfg(feature = "gcp")]

use crate::providers::gcp::clients::types::*;
use crate::providers::gcp::resources::*;
use foundation_core::valtron::{execute, StreamIterator, StreamIteratorExt};
use foundation_core::wire::simple_http::client::{
    body_reader, ClientRequestBuilder, RequestIntro, SimpleHttpClient, SystemDnsResolver,
};

/// GET v1/{+name}
/// Removes a publication reader, effectively severing the association with a Google user. If force is set to `true`, any entitlements for this reader will also be deleted. (Otherwise, the request will only work if the reader has no entitlements.) - If the reader does not exist, return NOT_FOUND. - Return FAILED_PRECONDITION if the force field is `false` (or unset) and entitlements are present.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `readerrevenuesubscriptionlinking_publications_readers_delete_execute()` to send, or `readerrevenuesubscriptionlinking_publications_readers_delete` for simplest API.

pub fn readerrevenuesubscriptionlinking_publications_readers_delete_builder(
    client: &SimpleHttpClient,
    name: &str,
    force: Option<bool>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/{+name}", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "force",
            force
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET v1/{+name}
/// Removes a publication reader, effectively severing the association with a Google user. If force is set to `true`, any entitlements for this reader will also be deleted. (Otherwise, the request will only work if the reader has no entitlements.) - If the reader does not exist, return NOT_FOUND. - Return FAILED_PRECONDITION if the force field is `false` (or unset) and entitlements are present.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `readerrevenuesubscriptionlinking_publications_readers_delete_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from readerrevenuesubscriptionlinking_publications_readers_delete_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn readerrevenuesubscriptionlinking_publications_readers_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<DeleteReaderResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: DeleteReaderResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+name}
/// Removes a publication reader, effectively severing the association with a Google user. If force is set to `true`, any entitlements for this reader will also be deleted. (Otherwise, the request will only work if the reader has no entitlements.) - If the reader does not exist, return NOT_FOUND. - Return FAILED_PRECONDITION if the force field is `false` (or unset) and entitlements are present.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `readerrevenuesubscriptionlinking_publications_readers_delete_builder()` + `readerrevenuesubscriptionlinking_publications_readers_delete_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn readerrevenuesubscriptionlinking_publications_readers_delete(
    client: &SimpleHttpClient,
    name: &str,
    force: Option<bool>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<DeleteReaderResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        readerrevenuesubscriptionlinking_publications_readers_delete_builder(client, name, force)?;
    readerrevenuesubscriptionlinking_publications_readers_delete_execute(builder)
}

/// GET v1/{+name}
/// Gets a reader of a publication. Returns NOT_FOUND if the reader does not exist.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `readerrevenuesubscriptionlinking_publications_readers_get_execute()` to send, or `readerrevenuesubscriptionlinking_publications_readers_get` for simplest API.

pub fn readerrevenuesubscriptionlinking_publications_readers_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/{+name}", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+name}
/// Gets a reader of a publication. Returns NOT_FOUND if the reader does not exist.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `readerrevenuesubscriptionlinking_publications_readers_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from readerrevenuesubscriptionlinking_publications_readers_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn readerrevenuesubscriptionlinking_publications_readers_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Reader>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Reader = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+name}
/// Gets a reader of a publication. Returns NOT_FOUND if the reader does not exist.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `readerrevenuesubscriptionlinking_publications_readers_get_builder()` + `readerrevenuesubscriptionlinking_publications_readers_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn readerrevenuesubscriptionlinking_publications_readers_get(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Reader>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = readerrevenuesubscriptionlinking_publications_readers_get_builder(client, name)?;
    readerrevenuesubscriptionlinking_publications_readers_get_execute(builder)
}

/// GET v1/{+name}
/// Gets the reader entitlements for a publication reader. - Returns PERMISSION_DENIED if the caller does not have access. - Returns NOT_FOUND if the reader does not exist.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `readerrevenuesubscriptionlinking_publications_readers_get_entitlements_execute()` to send, or `readerrevenuesubscriptionlinking_publications_readers_get_entitlements` for simplest API.

pub fn readerrevenuesubscriptionlinking_publications_readers_get_entitlements_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/{+name}", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+name}
/// Gets the reader entitlements for a publication reader. - Returns PERMISSION_DENIED if the caller does not have access. - Returns NOT_FOUND if the reader does not exist.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `readerrevenuesubscriptionlinking_publications_readers_get_entitlements_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from readerrevenuesubscriptionlinking_publications_readers_get_entitlements_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn readerrevenuesubscriptionlinking_publications_readers_get_entitlements_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ReaderEntitlements>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ReaderEntitlements = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+name}
/// Gets the reader entitlements for a publication reader. - Returns PERMISSION_DENIED if the caller does not have access. - Returns NOT_FOUND if the reader does not exist.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `readerrevenuesubscriptionlinking_publications_readers_get_entitlements_builder()` + `readerrevenuesubscriptionlinking_publications_readers_get_entitlements_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn readerrevenuesubscriptionlinking_publications_readers_get_entitlements(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ReaderEntitlements>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = readerrevenuesubscriptionlinking_publications_readers_get_entitlements_builder(
        client, name,
    )?;
    readerrevenuesubscriptionlinking_publications_readers_get_entitlements_execute(builder)
}

/// GET v1/{+name}
/// Updates the reader entitlements for a publication reader. The entire reader entitlements will be overwritten by the new reader entitlements in the payload, like a PUT. - Returns PERMISSION_DENIED if the caller does not have access. - Returns NOT_FOUND if the reader does not exist.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `readerrevenuesubscriptionlinking_publications_readers_update_entitlements_execute()` to send, or `readerrevenuesubscriptionlinking_publications_readers_update_entitlements` for simplest API.

pub fn readerrevenuesubscriptionlinking_publications_readers_update_entitlements_builder(
    client: &SimpleHttpClient,
    name: &str,
    updateMask: Option<&str>,
    body: &ReaderEntitlements,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/{+name}", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "updateMask",
            updateMask
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v1/{+name}
/// Updates the reader entitlements for a publication reader. The entire reader entitlements will be overwritten by the new reader entitlements in the payload, like a PUT. - Returns PERMISSION_DENIED if the caller does not have access. - Returns NOT_FOUND if the reader does not exist.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `readerrevenuesubscriptionlinking_publications_readers_update_entitlements_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from readerrevenuesubscriptionlinking_publications_readers_update_entitlements_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn readerrevenuesubscriptionlinking_publications_readers_update_entitlements_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ReaderEntitlements>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ReaderEntitlements = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+name}
/// Updates the reader entitlements for a publication reader. The entire reader entitlements will be overwritten by the new reader entitlements in the payload, like a PUT. - Returns PERMISSION_DENIED if the caller does not have access. - Returns NOT_FOUND if the reader does not exist.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `readerrevenuesubscriptionlinking_publications_readers_update_entitlements_builder()` + `readerrevenuesubscriptionlinking_publications_readers_update_entitlements_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn readerrevenuesubscriptionlinking_publications_readers_update_entitlements(
    client: &SimpleHttpClient,
    name: &str,
    updateMask: Option<&str>,
    body: &ReaderEntitlements,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ReaderEntitlements>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        readerrevenuesubscriptionlinking_publications_readers_update_entitlements_builder(
            client, name, updateMask, body,
        )?;
    readerrevenuesubscriptionlinking_publications_readers_update_entitlements_execute(builder)
}
