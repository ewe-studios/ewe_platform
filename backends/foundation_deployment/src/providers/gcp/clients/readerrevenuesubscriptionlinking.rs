//! Auto-generated API clients for gcp/readerrevenuesubscriptionlinking.
//!
//! Generated by `cargo run --bin ewe_platform gen_provider_clients`.
//! DO NOT EDIT MANUALLY.
//!
//! Feature flag: `gcp`

#![cfg(feature = "gcp")]

use crate::providers::gcp::clients::types::*;
use crate::providers::gcp::resources::*;
use foundation_core::valtron::{
    execute, BoxedSendExecutionAction, StreamIterator, StreamIteratorExt, TaskIterator,
    TaskIteratorExt,
};
use foundation_core::wire::simple_http::client::{
    body_reader, ClientRequestBuilder, DnsResolver, RequestIntro, SimpleHttpClient,
    SystemDnsResolver,
};
use foundation_db::state::resource_identifier::ResourceIdentifier;
use foundation_macros::JsonHash;
use serde::Serialize;

/// DELETE v1/publications/{publicationsId}/readers/{readersId}
/// Removes a publication reader, effectively severing the association with a Google user. If force is set to `true`, any entitlements for this reader will also be deleted. (Otherwise, the request will only work if the reader has no entitlements.) - If the reader does not exist, return NOT_FOUND. - Return FAILED_PRECONDITION if the force field is `false` (or unset) and entitlements are present.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `readerrevenuesubscriptionlinking_publications_readers_delete_execute()` to send, or `readerrevenuesubscriptionlinking_publications_readers_delete` for simplest API.

pub fn readerrevenuesubscriptionlinking_publications_readers_delete_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
    force: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://readerrevenuesubscriptionlinking.googleapis.com/v1/publications/{}/readers/{readersId}",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = force.as_ref() {
        query_parts.push(format!("force={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .delete(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// DELETE v1/publications/{publicationsId}/readers/{readersId}
/// Removes a publication reader, effectively severing the association with a Google user. If force is set to `true`, any entitlements for this reader will also be deleted. (Otherwise, the request will only work if the reader has no entitlements.) - If the reader does not exist, return NOT_FOUND. - Return FAILED_PRECONDITION if the force field is `false` (or unset) and entitlements are present.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `readerrevenuesubscriptionlinking_publications_readers_delete_execute()` or `readerrevenuesubscriptionlinking_publications_readers_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `readerrevenuesubscriptionlinking_publications_readers_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn readerrevenuesubscriptionlinking_publications_readers_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<DeleteReaderResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: DeleteReaderResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// DELETE v1/publications/{publicationsId}/readers/{readersId}
/// Removes a publication reader, effectively severing the association with a Google user. If force is set to `true`, any entitlements for this reader will also be deleted. (Otherwise, the request will only work if the reader has no entitlements.) - If the reader does not exist, return NOT_FOUND. - Return FAILED_PRECONDITION if the force field is `false` (or unset) and entitlements are present.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `readerrevenuesubscriptionlinking_publications_readers_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `readerrevenuesubscriptionlinking_publications_readers_delete_task()`.
/// For the simplest API, use `readerrevenuesubscriptionlinking_publications_readers_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `readerrevenuesubscriptionlinking_publications_readers_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn readerrevenuesubscriptionlinking_publications_readers_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<DeleteReaderResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = readerrevenuesubscriptionlinking_publications_readers_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`readerrevenuesubscriptionlinking_publications_readers_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ReaderrevenuesubscriptionlinkingPublicationsReadersDeleteArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: force
    pub force: Option<Option<String>>,
}

/// DELETE v1/publications/{publicationsId}/readers/{readersId}
/// Removes a publication reader, effectively severing the association with a Google user. If force is set to `true`, any entitlements for this reader will also be deleted. (Otherwise, the request will only work if the reader has no entitlements.) - If the reader does not exist, return NOT_FOUND. - Return FAILED_PRECONDITION if the force field is `false` (or unset) and entitlements are present.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `readerrevenuesubscriptionlinking_publications_readers_delete_builder()` + `readerrevenuesubscriptionlinking_publications_readers_delete_execute()`.
/// For task-level control, use `readerrevenuesubscriptionlinking_publications_readers_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn readerrevenuesubscriptionlinking_publications_readers_delete(
    client: &SimpleHttpClient,
    args: &ReaderrevenuesubscriptionlinkingPublicationsReadersDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<DeleteReaderResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = readerrevenuesubscriptionlinking_publications_readers_delete_builder(
        client,
        &args.name,
        &args.force,
    )?;
    readerrevenuesubscriptionlinking_publications_readers_delete_execute(builder)
}

/// GET v1/publications/{publicationsId}/readers/{readersId}
/// Gets a reader of a publication. Returns NOT_FOUND if the reader does not exist.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `readerrevenuesubscriptionlinking_publications_readers_get_execute()` to send, or `readerrevenuesubscriptionlinking_publications_readers_get` for simplest API.

pub fn readerrevenuesubscriptionlinking_publications_readers_get_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://readerrevenuesubscriptionlinking.googleapis.com/v1/publications/{}/readers/{readersId}",
        name,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/publications/{publicationsId}/readers/{readersId}
/// Gets a reader of a publication. Returns NOT_FOUND if the reader does not exist.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `readerrevenuesubscriptionlinking_publications_readers_get_execute()` or `readerrevenuesubscriptionlinking_publications_readers_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `readerrevenuesubscriptionlinking_publications_readers_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn readerrevenuesubscriptionlinking_publications_readers_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Reader>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Reader = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/publications/{publicationsId}/readers/{readersId}
/// Gets a reader of a publication. Returns NOT_FOUND if the reader does not exist.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `readerrevenuesubscriptionlinking_publications_readers_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `readerrevenuesubscriptionlinking_publications_readers_get_task()`.
/// For the simplest API, use `readerrevenuesubscriptionlinking_publications_readers_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `readerrevenuesubscriptionlinking_publications_readers_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn readerrevenuesubscriptionlinking_publications_readers_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Reader>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = readerrevenuesubscriptionlinking_publications_readers_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`readerrevenuesubscriptionlinking_publications_readers_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ReaderrevenuesubscriptionlinkingPublicationsReadersGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/publications/{publicationsId}/readers/{readersId}
/// Gets a reader of a publication. Returns NOT_FOUND if the reader does not exist.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `readerrevenuesubscriptionlinking_publications_readers_get_builder()` + `readerrevenuesubscriptionlinking_publications_readers_get_execute()`.
/// For task-level control, use `readerrevenuesubscriptionlinking_publications_readers_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn readerrevenuesubscriptionlinking_publications_readers_get(
    client: &SimpleHttpClient,
    args: &ReaderrevenuesubscriptionlinkingPublicationsReadersGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Reader>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        readerrevenuesubscriptionlinking_publications_readers_get_builder(client, &args.name)?;
    readerrevenuesubscriptionlinking_publications_readers_get_execute(builder)
}

/// GET v1/publications/{publicationsId}/readers/{readersId}/entitlements
/// Gets the reader entitlements for a publication reader. - Returns PERMISSION_DENIED if the caller does not have access. - Returns NOT_FOUND if the reader does not exist.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `readerrevenuesubscriptionlinking_publications_readers_get_entitlements_execute()` to send, or `readerrevenuesubscriptionlinking_publications_readers_get_entitlements` for simplest API.

pub fn readerrevenuesubscriptionlinking_publications_readers_get_entitlements_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://readerrevenuesubscriptionlinking.googleapis.com/v1/publications/{}/readers/{readersId}/entitlements",
        name,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/publications/{publicationsId}/readers/{readersId}/entitlements
/// Gets the reader entitlements for a publication reader. - Returns PERMISSION_DENIED if the caller does not have access. - Returns NOT_FOUND if the reader does not exist.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `readerrevenuesubscriptionlinking_publications_readers_get_entitlements_execute()` or `readerrevenuesubscriptionlinking_publications_readers_get_entitlements`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `readerrevenuesubscriptionlinking_publications_readers_get_entitlements_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn readerrevenuesubscriptionlinking_publications_readers_get_entitlements_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ReaderEntitlements>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ReaderEntitlements = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/publications/{publicationsId}/readers/{readersId}/entitlements
/// Gets the reader entitlements for a publication reader. - Returns PERMISSION_DENIED if the caller does not have access. - Returns NOT_FOUND if the reader does not exist.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `readerrevenuesubscriptionlinking_publications_readers_get_entitlements_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `readerrevenuesubscriptionlinking_publications_readers_get_entitlements_task()`.
/// For the simplest API, use `readerrevenuesubscriptionlinking_publications_readers_get_entitlements()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `readerrevenuesubscriptionlinking_publications_readers_get_entitlements_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn readerrevenuesubscriptionlinking_publications_readers_get_entitlements_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ReaderEntitlements>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task =
        readerrevenuesubscriptionlinking_publications_readers_get_entitlements_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`readerrevenuesubscriptionlinking_publications_readers_get_entitlements`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ReaderrevenuesubscriptionlinkingPublicationsReadersGetEntitlementsArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/publications/{publicationsId}/readers/{readersId}/entitlements
/// Gets the reader entitlements for a publication reader. - Returns PERMISSION_DENIED if the caller does not have access. - Returns NOT_FOUND if the reader does not exist.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `readerrevenuesubscriptionlinking_publications_readers_get_entitlements_builder()` + `readerrevenuesubscriptionlinking_publications_readers_get_entitlements_execute()`.
/// For task-level control, use `readerrevenuesubscriptionlinking_publications_readers_get_entitlements_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn readerrevenuesubscriptionlinking_publications_readers_get_entitlements(
    client: &SimpleHttpClient,
    args: &ReaderrevenuesubscriptionlinkingPublicationsReadersGetEntitlementsArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ReaderEntitlements>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = readerrevenuesubscriptionlinking_publications_readers_get_entitlements_builder(
        client, &args.name,
    )?;
    readerrevenuesubscriptionlinking_publications_readers_get_entitlements_execute(builder)
}

/// PATCH v1/publications/{publicationsId}/readers/{readersId}/entitlements
/// Updates the reader entitlements for a publication reader. The entire reader entitlements will be overwritten by the new reader entitlements in the payload, like a PUT. - Returns PERMISSION_DENIED if the caller does not have access. - Returns NOT_FOUND if the reader does not exist.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `readerrevenuesubscriptionlinking_publications_readers_update_entitlements_execute()` to send, or `readerrevenuesubscriptionlinking_publications_readers_update_entitlements` for simplest API.

pub fn readerrevenuesubscriptionlinking_publications_readers_update_entitlements_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
    updateMask: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://readerrevenuesubscriptionlinking.googleapis.com/v1/publications/{}/readers/{readersId}/entitlements",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = updateMask.as_ref() {
        query_parts.push(format!("updateMask={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .patch(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// PATCH v1/publications/{publicationsId}/readers/{readersId}/entitlements
/// Updates the reader entitlements for a publication reader. The entire reader entitlements will be overwritten by the new reader entitlements in the payload, like a PUT. - Returns PERMISSION_DENIED if the caller does not have access. - Returns NOT_FOUND if the reader does not exist.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `readerrevenuesubscriptionlinking_publications_readers_update_entitlements_execute()` or `readerrevenuesubscriptionlinking_publications_readers_update_entitlements`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `readerrevenuesubscriptionlinking_publications_readers_update_entitlements_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn readerrevenuesubscriptionlinking_publications_readers_update_entitlements_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ReaderEntitlements>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ReaderEntitlements = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// PATCH v1/publications/{publicationsId}/readers/{readersId}/entitlements
/// Updates the reader entitlements for a publication reader. The entire reader entitlements will be overwritten by the new reader entitlements in the payload, like a PUT. - Returns PERMISSION_DENIED if the caller does not have access. - Returns NOT_FOUND if the reader does not exist.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `readerrevenuesubscriptionlinking_publications_readers_update_entitlements_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `readerrevenuesubscriptionlinking_publications_readers_update_entitlements_task()`.
/// For the simplest API, use `readerrevenuesubscriptionlinking_publications_readers_update_entitlements()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `readerrevenuesubscriptionlinking_publications_readers_update_entitlements_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn readerrevenuesubscriptionlinking_publications_readers_update_entitlements_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ReaderEntitlements>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task =
        readerrevenuesubscriptionlinking_publications_readers_update_entitlements_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`readerrevenuesubscriptionlinking_publications_readers_update_entitlements`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ReaderrevenuesubscriptionlinkingPublicationsReadersUpdateEntitlementsArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: updateMask
    pub updateMask: Option<Option<String>>,
}

/// PATCH v1/publications/{publicationsId}/readers/{readersId}/entitlements
/// Updates the reader entitlements for a publication reader. The entire reader entitlements will be overwritten by the new reader entitlements in the payload, like a PUT. - Returns PERMISSION_DENIED if the caller does not have access. - Returns NOT_FOUND if the reader does not exist.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `readerrevenuesubscriptionlinking_publications_readers_update_entitlements_builder()` + `readerrevenuesubscriptionlinking_publications_readers_update_entitlements_execute()`.
/// For task-level control, use `readerrevenuesubscriptionlinking_publications_readers_update_entitlements_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn readerrevenuesubscriptionlinking_publications_readers_update_entitlements(
    client: &SimpleHttpClient,
    args: &ReaderrevenuesubscriptionlinkingPublicationsReadersUpdateEntitlementsArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ReaderEntitlements>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        readerrevenuesubscriptionlinking_publications_readers_update_entitlements_builder(
            client,
            &args.name,
            &args.updateMask,
        )?;
    readerrevenuesubscriptionlinking_publications_readers_update_entitlements_execute(builder)
}

// =============================================================================
// ResourceIdentifier implementation for DeleteReaderResponse
// =============================================================================

/// ResourceIdentifier implementation for DeleteReaderResponse with ReaderrevenuesubscriptionlinkingPublicationsReadersDeleteArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ReaderrevenuesubscriptionlinkingPublicationsReadersDeleteArgs>
    for DeleteReaderResponse
{
    fn generate_resource_id(
        &self,
        input: &ReaderrevenuesubscriptionlinkingPublicationsReadersDeleteArgs,
    ) -> String {
        format!(
            "gcp::readerrevenuesubscriptionlinking::DeleteReaderResponse/{}",
            input.name
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::readerrevenuesubscriptionlinking::DeleteReaderResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Reader
// =============================================================================

/// ResourceIdentifier implementation for Reader with ReaderrevenuesubscriptionlinkingPublicationsReadersGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ReaderrevenuesubscriptionlinkingPublicationsReadersGetArgs> for Reader {
    fn generate_resource_id(
        &self,
        input: &ReaderrevenuesubscriptionlinkingPublicationsReadersGetArgs,
    ) -> String {
        format!(
            "gcp::readerrevenuesubscriptionlinking::Reader/{}",
            input.name
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::readerrevenuesubscriptionlinking::Reader"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for ReaderEntitlements
// =============================================================================

/// ResourceIdentifier implementation for ReaderEntitlements with ReaderrevenuesubscriptionlinkingPublicationsReadersGetEntitlementsArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ReaderrevenuesubscriptionlinkingPublicationsReadersGetEntitlementsArgs>
    for ReaderEntitlements
{
    fn generate_resource_id(
        &self,
        input: &ReaderrevenuesubscriptionlinkingPublicationsReadersGetEntitlementsArgs,
    ) -> String {
        format!(
            "gcp::readerrevenuesubscriptionlinking::ReaderEntitlements/{}",
            input.name
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::readerrevenuesubscriptionlinking::ReaderEntitlements"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for ReaderEntitlements
// =============================================================================

/// ResourceIdentifier implementation for ReaderEntitlements with ReaderrevenuesubscriptionlinkingPublicationsReadersUpdateEntitlementsArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ReaderrevenuesubscriptionlinkingPublicationsReadersUpdateEntitlementsArgs>
    for ReaderEntitlements
{
    fn generate_resource_id(
        &self,
        input: &ReaderrevenuesubscriptionlinkingPublicationsReadersUpdateEntitlementsArgs,
    ) -> String {
        format!(
            "gcp::readerrevenuesubscriptionlinking::ReaderEntitlements/{}",
            input.name
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::readerrevenuesubscriptionlinking::ReaderEntitlements"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}
