//! Auto-generated API clients for gcp/authorizedbuyersmarketplace.
//!
//! Generated by `cargo run --bin ewe_platform gen_provider_clients`.
//! DO NOT EDIT MANUALLY.
//!
//! Feature flag: `gcp`

#![cfg(feature = "gcp")]

use crate::providers::gcp::clients::types::*;
use crate::providers::gcp::resources::*;
use foundation_core::valtron::{
    execute, BoxedSendExecutionAction, StreamIterator, StreamIteratorExt, TaskIterator,
    TaskIteratorExt,
};
use foundation_core::wire::simple_http::client::{
    body_reader, ClientRequestBuilder, DnsResolver, RequestIntro, SimpleHttpClient,
    SystemDnsResolver,
};
use foundation_db::state::resource_identifier::ResourceIdentifier;
use foundation_macros::JsonHash;
use serde::Serialize;

/// GET v1/bidders/{biddersId}/auctionPackages
/// List the auction packages. Buyers can use the URL path "/v1/`buyers/{`accountId`}/`auctionPackages``" to list auction packages for the current buyer and its clients. Bidders can use the URL path "/v1/`bidders/{`accountId`}/`auctionPackages``" to list auction packages for the bidder, its media planners, its buyers, and all their clients.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `authorizedbuyersmarketplace_bidders_auction_packages_list_execute()` to send, or `authorizedbuyersmarketplace_bidders_auction_packages_list` for simplest API.

pub fn authorizedbuyersmarketplace_bidders_auction_packages_list_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
    filter: &Option<Option<String>>,
    orderBy: &Option<Option<String>>,
    pageSize: &Option<Option<String>>,
    pageToken: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://authorizedbuyersmarketplace.googleapis.com/v1/bidders/{}/auctionPackages",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter.as_ref() {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = orderBy.as_ref() {
        query_parts.push(format!("orderBy={}", val));
    }
    if let Some(val) = pageSize.as_ref() {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/bidders/{biddersId}/auctionPackages
/// List the auction packages. Buyers can use the URL path "/v1/`buyers/{`accountId`}/`auctionPackages``" to list auction packages for the current buyer and its clients. Bidders can use the URL path "/v1/`bidders/{`accountId`}/`auctionPackages``" to list auction packages for the bidder, its media planners, its buyers, and all their clients.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `authorizedbuyersmarketplace_bidders_auction_packages_list_execute()` or `authorizedbuyersmarketplace_bidders_auction_packages_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `authorizedbuyersmarketplace_bidders_auction_packages_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn authorizedbuyersmarketplace_bidders_auction_packages_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ListAuctionPackagesResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListAuctionPackagesResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/bidders/{biddersId}/auctionPackages
/// List the auction packages. Buyers can use the URL path "/v1/`buyers/{`accountId`}/`auctionPackages``" to list auction packages for the current buyer and its clients. Bidders can use the URL path "/v1/`bidders/{`accountId`}/`auctionPackages``" to list auction packages for the bidder, its media planners, its buyers, and all their clients.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `authorizedbuyersmarketplace_bidders_auction_packages_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `authorizedbuyersmarketplace_bidders_auction_packages_list_task()`.
/// For the simplest API, use `authorizedbuyersmarketplace_bidders_auction_packages_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `authorizedbuyersmarketplace_bidders_auction_packages_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn authorizedbuyersmarketplace_bidders_auction_packages_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ListAuctionPackagesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = authorizedbuyersmarketplace_bidders_auction_packages_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`authorizedbuyersmarketplace_bidders_auction_packages_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AuthorizedbuyersmarketplaceBiddersAuctionPackagesListArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: filter
    pub filter: Option<Option<String>>,
    /// Query parameter: orderBy
    pub orderBy: Option<Option<String>>,
    /// Query parameter: pageSize
    pub pageSize: Option<Option<String>>,
    /// Query parameter: pageToken
    pub pageToken: Option<Option<String>>,
}

/// GET v1/bidders/{biddersId}/auctionPackages
/// List the auction packages. Buyers can use the URL path "/v1/`buyers/{`accountId`}/`auctionPackages``" to list auction packages for the current buyer and its clients. Bidders can use the URL path "/v1/`bidders/{`accountId`}/`auctionPackages``" to list auction packages for the bidder, its media planners, its buyers, and all their clients.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `authorizedbuyersmarketplace_bidders_auction_packages_list_builder()` + `authorizedbuyersmarketplace_bidders_auction_packages_list_execute()`.
/// For task-level control, use `authorizedbuyersmarketplace_bidders_auction_packages_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn authorizedbuyersmarketplace_bidders_auction_packages_list(
    client: &SimpleHttpClient,
    args: &AuthorizedbuyersmarketplaceBiddersAuctionPackagesListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ListAuctionPackagesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = authorizedbuyersmarketplace_bidders_auction_packages_list_builder(
        client,
        &args.parent,
        &args.filter,
        &args.orderBy,
        &args.pageSize,
        &args.pageToken,
    )?;
    authorizedbuyersmarketplace_bidders_auction_packages_list_execute(builder)
}

/// GET v1/bidders/{biddersId}/finalizedDeals
/// Lists finalized deals. Use the URL path "/v1/`buyers/{`accountId`}/`finalizedDeals``" to list finalized deals for the current buyer and its clients. Bidders can use the URL path "/v1/`bidders/{`accountId`}/`finalizedDeals``" to list finalized deals for the bidder, its buyers and all their clients.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `authorizedbuyersmarketplace_bidders_finalized_deals_list_execute()` to send, or `authorizedbuyersmarketplace_bidders_finalized_deals_list` for simplest API.

pub fn authorizedbuyersmarketplace_bidders_finalized_deals_list_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
    filter: &Option<Option<String>>,
    orderBy: &Option<Option<String>>,
    pageSize: &Option<Option<String>>,
    pageToken: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://authorizedbuyersmarketplace.googleapis.com/v1/bidders/{}/finalizedDeals",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter.as_ref() {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = orderBy.as_ref() {
        query_parts.push(format!("orderBy={}", val));
    }
    if let Some(val) = pageSize.as_ref() {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/bidders/{biddersId}/finalizedDeals
/// Lists finalized deals. Use the URL path "/v1/`buyers/{`accountId`}/`finalizedDeals``" to list finalized deals for the current buyer and its clients. Bidders can use the URL path "/v1/`bidders/{`accountId`}/`finalizedDeals``" to list finalized deals for the bidder, its buyers and all their clients.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `authorizedbuyersmarketplace_bidders_finalized_deals_list_execute()` or `authorizedbuyersmarketplace_bidders_finalized_deals_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `authorizedbuyersmarketplace_bidders_finalized_deals_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn authorizedbuyersmarketplace_bidders_finalized_deals_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ListFinalizedDealsResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListFinalizedDealsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/bidders/{biddersId}/finalizedDeals
/// Lists finalized deals. Use the URL path "/v1/`buyers/{`accountId`}/`finalizedDeals``" to list finalized deals for the current buyer and its clients. Bidders can use the URL path "/v1/`bidders/{`accountId`}/`finalizedDeals``" to list finalized deals for the bidder, its buyers and all their clients.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `authorizedbuyersmarketplace_bidders_finalized_deals_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `authorizedbuyersmarketplace_bidders_finalized_deals_list_task()`.
/// For the simplest API, use `authorizedbuyersmarketplace_bidders_finalized_deals_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `authorizedbuyersmarketplace_bidders_finalized_deals_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn authorizedbuyersmarketplace_bidders_finalized_deals_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ListFinalizedDealsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = authorizedbuyersmarketplace_bidders_finalized_deals_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`authorizedbuyersmarketplace_bidders_finalized_deals_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AuthorizedbuyersmarketplaceBiddersFinalizedDealsListArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: filter
    pub filter: Option<Option<String>>,
    /// Query parameter: orderBy
    pub orderBy: Option<Option<String>>,
    /// Query parameter: pageSize
    pub pageSize: Option<Option<String>>,
    /// Query parameter: pageToken
    pub pageToken: Option<Option<String>>,
}

/// GET v1/bidders/{biddersId}/finalizedDeals
/// Lists finalized deals. Use the URL path "/v1/`buyers/{`accountId`}/`finalizedDeals``" to list finalized deals for the current buyer and its clients. Bidders can use the URL path "/v1/`bidders/{`accountId`}/`finalizedDeals``" to list finalized deals for the bidder, its buyers and all their clients.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `authorizedbuyersmarketplace_bidders_finalized_deals_list_builder()` + `authorizedbuyersmarketplace_bidders_finalized_deals_list_execute()`.
/// For task-level control, use `authorizedbuyersmarketplace_bidders_finalized_deals_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn authorizedbuyersmarketplace_bidders_finalized_deals_list(
    client: &SimpleHttpClient,
    args: &AuthorizedbuyersmarketplaceBiddersFinalizedDealsListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ListFinalizedDealsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = authorizedbuyersmarketplace_bidders_finalized_deals_list_builder(
        client,
        &args.parent,
        &args.filter,
        &args.orderBy,
        &args.pageSize,
        &args.pageToken,
    )?;
    authorizedbuyersmarketplace_bidders_finalized_deals_list_execute(builder)
}

/// POST v1/bidders/{biddersId}/finalizedDeals/{finalizedDealsId}:setReadyToServe
/// Sets the given finalized deal as ready to serve. By default, deals are set as ready to serve as soon as they're finalized. If you want to opt out of the default behavior, and manually indicate that deals are ready to serve, ask your Technical Account Manager to add you to the allowlist. If you choose to use this method, finalized deals belonging to the bidder and its child seats don't start serving until after you call `setReadyToServe`, and after the deals become active. For example, you can use this method to delay receiving bid requests until your creative is ready. In addition, bidders can use the URL path "/v1/`bidders/{`accountId`}/`finalizedDeals`/{`dealId`}`" to set ready to serve for the finalized deals belong to itself, its child seats and all their clients. This method only applies to programmatic guaranteed deals.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `authorizedbuyersmarketplace_bidders_finalized_deals_set_ready_to_serve_execute()` to send, or `authorizedbuyersmarketplace_bidders_finalized_deals_set_ready_to_serve` for simplest API.

pub fn authorizedbuyersmarketplace_bidders_finalized_deals_set_ready_to_serve_builder<R>(
    client: &SimpleHttpClient<R>,
    deal: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://authorizedbuyersmarketplace.googleapis.com/v1/bidders/{}/finalizedDeals/{finalizedDealsId}:setReadyToServe",
        deal,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1/bidders/{biddersId}/finalizedDeals/{finalizedDealsId}:setReadyToServe
/// Sets the given finalized deal as ready to serve. By default, deals are set as ready to serve as soon as they're finalized. If you want to opt out of the default behavior, and manually indicate that deals are ready to serve, ask your Technical Account Manager to add you to the allowlist. If you choose to use this method, finalized deals belonging to the bidder and its child seats don't start serving until after you call `setReadyToServe`, and after the deals become active. For example, you can use this method to delay receiving bid requests until your creative is ready. In addition, bidders can use the URL path "/v1/`bidders/{`accountId`}/`finalizedDeals`/{`dealId`}`" to set ready to serve for the finalized deals belong to itself, its child seats and all their clients. This method only applies to programmatic guaranteed deals.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `authorizedbuyersmarketplace_bidders_finalized_deals_set_ready_to_serve_execute()` or `authorizedbuyersmarketplace_bidders_finalized_deals_set_ready_to_serve`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `authorizedbuyersmarketplace_bidders_finalized_deals_set_ready_to_serve_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn authorizedbuyersmarketplace_bidders_finalized_deals_set_ready_to_serve_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<FinalizedDeal>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: FinalizedDeal = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1/bidders/{biddersId}/finalizedDeals/{finalizedDealsId}:setReadyToServe
/// Sets the given finalized deal as ready to serve. By default, deals are set as ready to serve as soon as they're finalized. If you want to opt out of the default behavior, and manually indicate that deals are ready to serve, ask your Technical Account Manager to add you to the allowlist. If you choose to use this method, finalized deals belonging to the bidder and its child seats don't start serving until after you call `setReadyToServe`, and after the deals become active. For example, you can use this method to delay receiving bid requests until your creative is ready. In addition, bidders can use the URL path "/v1/`bidders/{`accountId`}/`finalizedDeals`/{`dealId`}`" to set ready to serve for the finalized deals belong to itself, its child seats and all their clients. This method only applies to programmatic guaranteed deals.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `authorizedbuyersmarketplace_bidders_finalized_deals_set_ready_to_serve_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `authorizedbuyersmarketplace_bidders_finalized_deals_set_ready_to_serve_task()`.
/// For the simplest API, use `authorizedbuyersmarketplace_bidders_finalized_deals_set_ready_to_serve()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `authorizedbuyersmarketplace_bidders_finalized_deals_set_ready_to_serve_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn authorizedbuyersmarketplace_bidders_finalized_deals_set_ready_to_serve_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<FinalizedDeal>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task =
        authorizedbuyersmarketplace_bidders_finalized_deals_set_ready_to_serve_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`authorizedbuyersmarketplace_bidders_finalized_deals_set_ready_to_serve`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AuthorizedbuyersmarketplaceBiddersFinalizedDealsSetReadyToServeArgs {
    /// Path parameter: deal
    pub deal: String,
}

/// POST v1/bidders/{biddersId}/finalizedDeals/{finalizedDealsId}:setReadyToServe
/// Sets the given finalized deal as ready to serve. By default, deals are set as ready to serve as soon as they're finalized. If you want to opt out of the default behavior, and manually indicate that deals are ready to serve, ask your Technical Account Manager to add you to the allowlist. If you choose to use this method, finalized deals belonging to the bidder and its child seats don't start serving until after you call `setReadyToServe`, and after the deals become active. For example, you can use this method to delay receiving bid requests until your creative is ready. In addition, bidders can use the URL path "/v1/`bidders/{`accountId`}/`finalizedDeals`/{`dealId`}`" to set ready to serve for the finalized deals belong to itself, its child seats and all their clients. This method only applies to programmatic guaranteed deals.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `authorizedbuyersmarketplace_bidders_finalized_deals_set_ready_to_serve_builder()` + `authorizedbuyersmarketplace_bidders_finalized_deals_set_ready_to_serve_execute()`.
/// For task-level control, use `authorizedbuyersmarketplace_bidders_finalized_deals_set_ready_to_serve_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn authorizedbuyersmarketplace_bidders_finalized_deals_set_ready_to_serve(
    client: &SimpleHttpClient,
    args: &AuthorizedbuyersmarketplaceBiddersFinalizedDealsSetReadyToServeArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<FinalizedDeal>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = authorizedbuyersmarketplace_bidders_finalized_deals_set_ready_to_serve_builder(
        client, &args.deal,
    )?;
    authorizedbuyersmarketplace_bidders_finalized_deals_set_ready_to_serve_execute(builder)
}

/// GET v1/buyers/{buyersId}/auctionPackages/{auctionPackagesId}
/// Gets an auction package given its name.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `authorizedbuyersmarketplace_buyers_auction_packages_get_execute()` to send, or `authorizedbuyersmarketplace_buyers_auction_packages_get` for simplest API.

pub fn authorizedbuyersmarketplace_buyers_auction_packages_get_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://authorizedbuyersmarketplace.googleapis.com/v1/buyers/{}/auctionPackages/{auctionPackagesId}",
        name,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/buyers/{buyersId}/auctionPackages/{auctionPackagesId}
/// Gets an auction package given its name.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `authorizedbuyersmarketplace_buyers_auction_packages_get_execute()` or `authorizedbuyersmarketplace_buyers_auction_packages_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `authorizedbuyersmarketplace_buyers_auction_packages_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn authorizedbuyersmarketplace_buyers_auction_packages_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<AuctionPackage>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: AuctionPackage = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/buyers/{buyersId}/auctionPackages/{auctionPackagesId}
/// Gets an auction package given its name.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `authorizedbuyersmarketplace_buyers_auction_packages_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `authorizedbuyersmarketplace_buyers_auction_packages_get_task()`.
/// For the simplest API, use `authorizedbuyersmarketplace_buyers_auction_packages_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `authorizedbuyersmarketplace_buyers_auction_packages_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn authorizedbuyersmarketplace_buyers_auction_packages_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<AuctionPackage>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = authorizedbuyersmarketplace_buyers_auction_packages_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`authorizedbuyersmarketplace_buyers_auction_packages_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AuthorizedbuyersmarketplaceBuyersAuctionPackagesGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/buyers/{buyersId}/auctionPackages/{auctionPackagesId}
/// Gets an auction package given its name.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `authorizedbuyersmarketplace_buyers_auction_packages_get_builder()` + `authorizedbuyersmarketplace_buyers_auction_packages_get_execute()`.
/// For task-level control, use `authorizedbuyersmarketplace_buyers_auction_packages_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn authorizedbuyersmarketplace_buyers_auction_packages_get(
    client: &SimpleHttpClient,
    args: &AuthorizedbuyersmarketplaceBuyersAuctionPackagesGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<AuctionPackage>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        authorizedbuyersmarketplace_buyers_auction_packages_get_builder(client, &args.name)?;
    authorizedbuyersmarketplace_buyers_auction_packages_get_execute(builder)
}

/// GET v1/buyers/{buyersId}/auctionPackages
/// List the auction packages. Buyers can use the URL path "/v1/`buyers/{`accountId`}/`auctionPackages``" to list auction packages for the current buyer and its clients. Bidders can use the URL path "/v1/`bidders/{`accountId`}/`auctionPackages``" to list auction packages for the bidder, its media planners, its buyers, and all their clients.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `authorizedbuyersmarketplace_buyers_auction_packages_list_execute()` to send, or `authorizedbuyersmarketplace_buyers_auction_packages_list` for simplest API.

pub fn authorizedbuyersmarketplace_buyers_auction_packages_list_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
    filter: &Option<Option<String>>,
    orderBy: &Option<Option<String>>,
    pageSize: &Option<Option<String>>,
    pageToken: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://authorizedbuyersmarketplace.googleapis.com/v1/buyers/{}/auctionPackages",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter.as_ref() {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = orderBy.as_ref() {
        query_parts.push(format!("orderBy={}", val));
    }
    if let Some(val) = pageSize.as_ref() {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/buyers/{buyersId}/auctionPackages
/// List the auction packages. Buyers can use the URL path "/v1/`buyers/{`accountId`}/`auctionPackages``" to list auction packages for the current buyer and its clients. Bidders can use the URL path "/v1/`bidders/{`accountId`}/`auctionPackages``" to list auction packages for the bidder, its media planners, its buyers, and all their clients.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `authorizedbuyersmarketplace_buyers_auction_packages_list_execute()` or `authorizedbuyersmarketplace_buyers_auction_packages_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `authorizedbuyersmarketplace_buyers_auction_packages_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn authorizedbuyersmarketplace_buyers_auction_packages_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ListAuctionPackagesResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListAuctionPackagesResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/buyers/{buyersId}/auctionPackages
/// List the auction packages. Buyers can use the URL path "/v1/`buyers/{`accountId`}/`auctionPackages``" to list auction packages for the current buyer and its clients. Bidders can use the URL path "/v1/`bidders/{`accountId`}/`auctionPackages``" to list auction packages for the bidder, its media planners, its buyers, and all their clients.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `authorizedbuyersmarketplace_buyers_auction_packages_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `authorizedbuyersmarketplace_buyers_auction_packages_list_task()`.
/// For the simplest API, use `authorizedbuyersmarketplace_buyers_auction_packages_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `authorizedbuyersmarketplace_buyers_auction_packages_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn authorizedbuyersmarketplace_buyers_auction_packages_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ListAuctionPackagesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = authorizedbuyersmarketplace_buyers_auction_packages_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`authorizedbuyersmarketplace_buyers_auction_packages_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AuthorizedbuyersmarketplaceBuyersAuctionPackagesListArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: filter
    pub filter: Option<Option<String>>,
    /// Query parameter: orderBy
    pub orderBy: Option<Option<String>>,
    /// Query parameter: pageSize
    pub pageSize: Option<Option<String>>,
    /// Query parameter: pageToken
    pub pageToken: Option<Option<String>>,
}

/// GET v1/buyers/{buyersId}/auctionPackages
/// List the auction packages. Buyers can use the URL path "/v1/`buyers/{`accountId`}/`auctionPackages``" to list auction packages for the current buyer and its clients. Bidders can use the URL path "/v1/`bidders/{`accountId`}/`auctionPackages``" to list auction packages for the bidder, its media planners, its buyers, and all their clients.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `authorizedbuyersmarketplace_buyers_auction_packages_list_builder()` + `authorizedbuyersmarketplace_buyers_auction_packages_list_execute()`.
/// For task-level control, use `authorizedbuyersmarketplace_buyers_auction_packages_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn authorizedbuyersmarketplace_buyers_auction_packages_list(
    client: &SimpleHttpClient,
    args: &AuthorizedbuyersmarketplaceBuyersAuctionPackagesListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ListAuctionPackagesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = authorizedbuyersmarketplace_buyers_auction_packages_list_builder(
        client,
        &args.parent,
        &args.filter,
        &args.orderBy,
        &args.pageSize,
        &args.pageToken,
    )?;
    authorizedbuyersmarketplace_buyers_auction_packages_list_execute(builder)
}

/// POST v1/buyers/{buyersId}/auctionPackages/{auctionPackagesId}:subscribe
/// Subscribe to the auction package for the specified buyer. Once subscribed, the bidder will receive a call out for inventory matching the auction package targeting criteria with the auction package deal ID and the specified buyer.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `authorizedbuyersmarketplace_buyers_auction_packages_subscribe_execute()` to send, or `authorizedbuyersmarketplace_buyers_auction_packages_subscribe` for simplest API.

pub fn authorizedbuyersmarketplace_buyers_auction_packages_subscribe_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://authorizedbuyersmarketplace.googleapis.com/v1/buyers/{}/auctionPackages/{auctionPackagesId}:subscribe",
        name,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1/buyers/{buyersId}/auctionPackages/{auctionPackagesId}:subscribe
/// Subscribe to the auction package for the specified buyer. Once subscribed, the bidder will receive a call out for inventory matching the auction package targeting criteria with the auction package deal ID and the specified buyer.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `authorizedbuyersmarketplace_buyers_auction_packages_subscribe_execute()` or `authorizedbuyersmarketplace_buyers_auction_packages_subscribe`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `authorizedbuyersmarketplace_buyers_auction_packages_subscribe_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn authorizedbuyersmarketplace_buyers_auction_packages_subscribe_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<AuctionPackage>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: AuctionPackage = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1/buyers/{buyersId}/auctionPackages/{auctionPackagesId}:subscribe
/// Subscribe to the auction package for the specified buyer. Once subscribed, the bidder will receive a call out for inventory matching the auction package targeting criteria with the auction package deal ID and the specified buyer.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `authorizedbuyersmarketplace_buyers_auction_packages_subscribe_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `authorizedbuyersmarketplace_buyers_auction_packages_subscribe_task()`.
/// For the simplest API, use `authorizedbuyersmarketplace_buyers_auction_packages_subscribe()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `authorizedbuyersmarketplace_buyers_auction_packages_subscribe_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn authorizedbuyersmarketplace_buyers_auction_packages_subscribe_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<AuctionPackage>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = authorizedbuyersmarketplace_buyers_auction_packages_subscribe_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`authorizedbuyersmarketplace_buyers_auction_packages_subscribe`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AuthorizedbuyersmarketplaceBuyersAuctionPackagesSubscribeArgs {
    /// Path parameter: name
    pub name: String,
}

/// POST v1/buyers/{buyersId}/auctionPackages/{auctionPackagesId}:subscribe
/// Subscribe to the auction package for the specified buyer. Once subscribed, the bidder will receive a call out for inventory matching the auction package targeting criteria with the auction package deal ID and the specified buyer.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `authorizedbuyersmarketplace_buyers_auction_packages_subscribe_builder()` + `authorizedbuyersmarketplace_buyers_auction_packages_subscribe_execute()`.
/// For task-level control, use `authorizedbuyersmarketplace_buyers_auction_packages_subscribe_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn authorizedbuyersmarketplace_buyers_auction_packages_subscribe(
    client: &SimpleHttpClient,
    args: &AuthorizedbuyersmarketplaceBuyersAuctionPackagesSubscribeArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<AuctionPackage>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        authorizedbuyersmarketplace_buyers_auction_packages_subscribe_builder(client, &args.name)?;
    authorizedbuyersmarketplace_buyers_auction_packages_subscribe_execute(builder)
}

/// POST v1/buyers/{buyersId}/auctionPackages/{auctionPackagesId}:subscribeClients
/// Subscribe the specified clients of the buyer to the auction package. If a client in the list does not belong to the buyer, an error response will be returned, and all of the following clients in the list will not be subscribed. Subscribing an already subscribed client will have no effect.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `authorizedbuyersmarketplace_buyers_auction_packages_subscribe_clients_execute()` to send, or `authorizedbuyersmarketplace_buyers_auction_packages_subscribe_clients` for simplest API.

pub fn authorizedbuyersmarketplace_buyers_auction_packages_subscribe_clients_builder<R>(
    client: &SimpleHttpClient<R>,
    auctionPackage: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://authorizedbuyersmarketplace.googleapis.com/v1/buyers/{}/auctionPackages/{auctionPackagesId}:subscribeClients",
        auctionPackage,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1/buyers/{buyersId}/auctionPackages/{auctionPackagesId}:subscribeClients
/// Subscribe the specified clients of the buyer to the auction package. If a client in the list does not belong to the buyer, an error response will be returned, and all of the following clients in the list will not be subscribed. Subscribing an already subscribed client will have no effect.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `authorizedbuyersmarketplace_buyers_auction_packages_subscribe_clients_execute()` or `authorizedbuyersmarketplace_buyers_auction_packages_subscribe_clients`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `authorizedbuyersmarketplace_buyers_auction_packages_subscribe_clients_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn authorizedbuyersmarketplace_buyers_auction_packages_subscribe_clients_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<AuctionPackage>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: AuctionPackage = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1/buyers/{buyersId}/auctionPackages/{auctionPackagesId}:subscribeClients
/// Subscribe the specified clients of the buyer to the auction package. If a client in the list does not belong to the buyer, an error response will be returned, and all of the following clients in the list will not be subscribed. Subscribing an already subscribed client will have no effect.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `authorizedbuyersmarketplace_buyers_auction_packages_subscribe_clients_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `authorizedbuyersmarketplace_buyers_auction_packages_subscribe_clients_task()`.
/// For the simplest API, use `authorizedbuyersmarketplace_buyers_auction_packages_subscribe_clients()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `authorizedbuyersmarketplace_buyers_auction_packages_subscribe_clients_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn authorizedbuyersmarketplace_buyers_auction_packages_subscribe_clients_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<AuctionPackage>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = authorizedbuyersmarketplace_buyers_auction_packages_subscribe_clients_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`authorizedbuyersmarketplace_buyers_auction_packages_subscribe_clients`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AuthorizedbuyersmarketplaceBuyersAuctionPackagesSubscribeClientsArgs {
    /// Path parameter: auctionPackage
    pub auctionPackage: String,
}

/// POST v1/buyers/{buyersId}/auctionPackages/{auctionPackagesId}:subscribeClients
/// Subscribe the specified clients of the buyer to the auction package. If a client in the list does not belong to the buyer, an error response will be returned, and all of the following clients in the list will not be subscribed. Subscribing an already subscribed client will have no effect.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `authorizedbuyersmarketplace_buyers_auction_packages_subscribe_clients_builder()` + `authorizedbuyersmarketplace_buyers_auction_packages_subscribe_clients_execute()`.
/// For task-level control, use `authorizedbuyersmarketplace_buyers_auction_packages_subscribe_clients_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn authorizedbuyersmarketplace_buyers_auction_packages_subscribe_clients(
    client: &SimpleHttpClient,
    args: &AuthorizedbuyersmarketplaceBuyersAuctionPackagesSubscribeClientsArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<AuctionPackage>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = authorizedbuyersmarketplace_buyers_auction_packages_subscribe_clients_builder(
        client,
        &args.auctionPackage,
    )?;
    authorizedbuyersmarketplace_buyers_auction_packages_subscribe_clients_execute(builder)
}

/// POST v1/buyers/{buyersId}/auctionPackages/{auctionPackagesId}:unsubscribe
/// Unsubscribe from the auction package for the specified buyer. Once unsubscribed, the bidder will no longer receive a call out for the auction package deal ID and the specified buyer.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `authorizedbuyersmarketplace_buyers_auction_packages_unsubscribe_execute()` to send, or `authorizedbuyersmarketplace_buyers_auction_packages_unsubscribe` for simplest API.

pub fn authorizedbuyersmarketplace_buyers_auction_packages_unsubscribe_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://authorizedbuyersmarketplace.googleapis.com/v1/buyers/{}/auctionPackages/{auctionPackagesId}:unsubscribe",
        name,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1/buyers/{buyersId}/auctionPackages/{auctionPackagesId}:unsubscribe
/// Unsubscribe from the auction package for the specified buyer. Once unsubscribed, the bidder will no longer receive a call out for the auction package deal ID and the specified buyer.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `authorizedbuyersmarketplace_buyers_auction_packages_unsubscribe_execute()` or `authorizedbuyersmarketplace_buyers_auction_packages_unsubscribe`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `authorizedbuyersmarketplace_buyers_auction_packages_unsubscribe_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn authorizedbuyersmarketplace_buyers_auction_packages_unsubscribe_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<AuctionPackage>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: AuctionPackage = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1/buyers/{buyersId}/auctionPackages/{auctionPackagesId}:unsubscribe
/// Unsubscribe from the auction package for the specified buyer. Once unsubscribed, the bidder will no longer receive a call out for the auction package deal ID and the specified buyer.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `authorizedbuyersmarketplace_buyers_auction_packages_unsubscribe_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `authorizedbuyersmarketplace_buyers_auction_packages_unsubscribe_task()`.
/// For the simplest API, use `authorizedbuyersmarketplace_buyers_auction_packages_unsubscribe()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `authorizedbuyersmarketplace_buyers_auction_packages_unsubscribe_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn authorizedbuyersmarketplace_buyers_auction_packages_unsubscribe_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<AuctionPackage>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = authorizedbuyersmarketplace_buyers_auction_packages_unsubscribe_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`authorizedbuyersmarketplace_buyers_auction_packages_unsubscribe`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AuthorizedbuyersmarketplaceBuyersAuctionPackagesUnsubscribeArgs {
    /// Path parameter: name
    pub name: String,
}

/// POST v1/buyers/{buyersId}/auctionPackages/{auctionPackagesId}:unsubscribe
/// Unsubscribe from the auction package for the specified buyer. Once unsubscribed, the bidder will no longer receive a call out for the auction package deal ID and the specified buyer.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `authorizedbuyersmarketplace_buyers_auction_packages_unsubscribe_builder()` + `authorizedbuyersmarketplace_buyers_auction_packages_unsubscribe_execute()`.
/// For task-level control, use `authorizedbuyersmarketplace_buyers_auction_packages_unsubscribe_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn authorizedbuyersmarketplace_buyers_auction_packages_unsubscribe(
    client: &SimpleHttpClient,
    args: &AuthorizedbuyersmarketplaceBuyersAuctionPackagesUnsubscribeArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<AuctionPackage>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = authorizedbuyersmarketplace_buyers_auction_packages_unsubscribe_builder(
        client, &args.name,
    )?;
    authorizedbuyersmarketplace_buyers_auction_packages_unsubscribe_execute(builder)
}

/// POST v1/buyers/{buyersId}/auctionPackages/{auctionPackagesId}:unsubscribeClients
/// Unsubscribe from the auction package for the specified clients of the buyer. Unsubscribing a client that is not subscribed will have no effect.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `authorizedbuyersmarketplace_buyers_auction_packages_unsubscribe_clients_execute()` to send, or `authorizedbuyersmarketplace_buyers_auction_packages_unsubscribe_clients` for simplest API.

pub fn authorizedbuyersmarketplace_buyers_auction_packages_unsubscribe_clients_builder<R>(
    client: &SimpleHttpClient<R>,
    auctionPackage: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://authorizedbuyersmarketplace.googleapis.com/v1/buyers/{}/auctionPackages/{auctionPackagesId}:unsubscribeClients",
        auctionPackage,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1/buyers/{buyersId}/auctionPackages/{auctionPackagesId}:unsubscribeClients
/// Unsubscribe from the auction package for the specified clients of the buyer. Unsubscribing a client that is not subscribed will have no effect.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `authorizedbuyersmarketplace_buyers_auction_packages_unsubscribe_clients_execute()` or `authorizedbuyersmarketplace_buyers_auction_packages_unsubscribe_clients`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `authorizedbuyersmarketplace_buyers_auction_packages_unsubscribe_clients_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn authorizedbuyersmarketplace_buyers_auction_packages_unsubscribe_clients_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<AuctionPackage>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: AuctionPackage = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1/buyers/{buyersId}/auctionPackages/{auctionPackagesId}:unsubscribeClients
/// Unsubscribe from the auction package for the specified clients of the buyer. Unsubscribing a client that is not subscribed will have no effect.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `authorizedbuyersmarketplace_buyers_auction_packages_unsubscribe_clients_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `authorizedbuyersmarketplace_buyers_auction_packages_unsubscribe_clients_task()`.
/// For the simplest API, use `authorizedbuyersmarketplace_buyers_auction_packages_unsubscribe_clients()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `authorizedbuyersmarketplace_buyers_auction_packages_unsubscribe_clients_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn authorizedbuyersmarketplace_buyers_auction_packages_unsubscribe_clients_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<AuctionPackage>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task =
        authorizedbuyersmarketplace_buyers_auction_packages_unsubscribe_clients_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`authorizedbuyersmarketplace_buyers_auction_packages_unsubscribe_clients`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AuthorizedbuyersmarketplaceBuyersAuctionPackagesUnsubscribeClientsArgs {
    /// Path parameter: auctionPackage
    pub auctionPackage: String,
}

/// POST v1/buyers/{buyersId}/auctionPackages/{auctionPackagesId}:unsubscribeClients
/// Unsubscribe from the auction package for the specified clients of the buyer. Unsubscribing a client that is not subscribed will have no effect.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `authorizedbuyersmarketplace_buyers_auction_packages_unsubscribe_clients_builder()` + `authorizedbuyersmarketplace_buyers_auction_packages_unsubscribe_clients_execute()`.
/// For task-level control, use `authorizedbuyersmarketplace_buyers_auction_packages_unsubscribe_clients_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn authorizedbuyersmarketplace_buyers_auction_packages_unsubscribe_clients(
    client: &SimpleHttpClient,
    args: &AuthorizedbuyersmarketplaceBuyersAuctionPackagesUnsubscribeClientsArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<AuctionPackage>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = authorizedbuyersmarketplace_buyers_auction_packages_unsubscribe_clients_builder(
        client,
        &args.auctionPackage,
    )?;
    authorizedbuyersmarketplace_buyers_auction_packages_unsubscribe_clients_execute(builder)
}

/// POST v1/buyers/{buyersId}/clients/{clientsId}:activate
/// Activates an existing client. The state of the client will be updated to "`ACTIVE`". This method has no effect if the client is already in "`ACTIVE`" state.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `authorizedbuyersmarketplace_buyers_clients_activate_execute()` to send, or `authorizedbuyersmarketplace_buyers_clients_activate` for simplest API.

pub fn authorizedbuyersmarketplace_buyers_clients_activate_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://authorizedbuyersmarketplace.googleapis.com/v1/buyers/{}/clients/{clientsId}:activate",
        name,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1/buyers/{buyersId}/clients/{clientsId}:activate
/// Activates an existing client. The state of the client will be updated to "`ACTIVE`". This method has no effect if the client is already in "`ACTIVE`" state.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `authorizedbuyersmarketplace_buyers_clients_activate_execute()` or `authorizedbuyersmarketplace_buyers_clients_activate`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `authorizedbuyersmarketplace_buyers_clients_activate_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn authorizedbuyersmarketplace_buyers_clients_activate_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Client>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Client = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1/buyers/{buyersId}/clients/{clientsId}:activate
/// Activates an existing client. The state of the client will be updated to "`ACTIVE`". This method has no effect if the client is already in "`ACTIVE`" state.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `authorizedbuyersmarketplace_buyers_clients_activate_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `authorizedbuyersmarketplace_buyers_clients_activate_task()`.
/// For the simplest API, use `authorizedbuyersmarketplace_buyers_clients_activate()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `authorizedbuyersmarketplace_buyers_clients_activate_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn authorizedbuyersmarketplace_buyers_clients_activate_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Client>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = authorizedbuyersmarketplace_buyers_clients_activate_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`authorizedbuyersmarketplace_buyers_clients_activate`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AuthorizedbuyersmarketplaceBuyersClientsActivateArgs {
    /// Path parameter: name
    pub name: String,
}

/// POST v1/buyers/{buyersId}/clients/{clientsId}:activate
/// Activates an existing client. The state of the client will be updated to "`ACTIVE`". This method has no effect if the client is already in "`ACTIVE`" state.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `authorizedbuyersmarketplace_buyers_clients_activate_builder()` + `authorizedbuyersmarketplace_buyers_clients_activate_execute()`.
/// For task-level control, use `authorizedbuyersmarketplace_buyers_clients_activate_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn authorizedbuyersmarketplace_buyers_clients_activate(
    client: &SimpleHttpClient,
    args: &AuthorizedbuyersmarketplaceBuyersClientsActivateArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Client>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = authorizedbuyersmarketplace_buyers_clients_activate_builder(client, &args.name)?;
    authorizedbuyersmarketplace_buyers_clients_activate_execute(builder)
}

/// POST v1/buyers/{buyersId}/clients
/// Creates a new client.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `authorizedbuyersmarketplace_buyers_clients_create_execute()` to send, or `authorizedbuyersmarketplace_buyers_clients_create` for simplest API.

pub fn authorizedbuyersmarketplace_buyers_clients_create_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://authorizedbuyersmarketplace.googleapis.com/v1/buyers/{}/clients",
        parent,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1/buyers/{buyersId}/clients
/// Creates a new client.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `authorizedbuyersmarketplace_buyers_clients_create_execute()` or `authorizedbuyersmarketplace_buyers_clients_create`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `authorizedbuyersmarketplace_buyers_clients_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn authorizedbuyersmarketplace_buyers_clients_create_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Client>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Client = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1/buyers/{buyersId}/clients
/// Creates a new client.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `authorizedbuyersmarketplace_buyers_clients_create_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `authorizedbuyersmarketplace_buyers_clients_create_task()`.
/// For the simplest API, use `authorizedbuyersmarketplace_buyers_clients_create()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `authorizedbuyersmarketplace_buyers_clients_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn authorizedbuyersmarketplace_buyers_clients_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Client>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = authorizedbuyersmarketplace_buyers_clients_create_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`authorizedbuyersmarketplace_buyers_clients_create`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AuthorizedbuyersmarketplaceBuyersClientsCreateArgs {
    /// Path parameter: parent
    pub parent: String,
}

/// POST v1/buyers/{buyersId}/clients
/// Creates a new client.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `authorizedbuyersmarketplace_buyers_clients_create_builder()` + `authorizedbuyersmarketplace_buyers_clients_create_execute()`.
/// For task-level control, use `authorizedbuyersmarketplace_buyers_clients_create_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn authorizedbuyersmarketplace_buyers_clients_create(
    client: &SimpleHttpClient,
    args: &AuthorizedbuyersmarketplaceBuyersClientsCreateArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Client>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = authorizedbuyersmarketplace_buyers_clients_create_builder(client, &args.parent)?;
    authorizedbuyersmarketplace_buyers_clients_create_execute(builder)
}

/// POST v1/buyers/{buyersId}/clients/{clientsId}:deactivate
/// Deactivates an existing client. The state of the client will be updated to "`INACTIVE`". This method has no effect if the client is already in "`INACTIVE`" state.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `authorizedbuyersmarketplace_buyers_clients_deactivate_execute()` to send, or `authorizedbuyersmarketplace_buyers_clients_deactivate` for simplest API.

pub fn authorizedbuyersmarketplace_buyers_clients_deactivate_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://authorizedbuyersmarketplace.googleapis.com/v1/buyers/{}/clients/{clientsId}:deactivate",
        name,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1/buyers/{buyersId}/clients/{clientsId}:deactivate
/// Deactivates an existing client. The state of the client will be updated to "`INACTIVE`". This method has no effect if the client is already in "`INACTIVE`" state.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `authorizedbuyersmarketplace_buyers_clients_deactivate_execute()` or `authorizedbuyersmarketplace_buyers_clients_deactivate`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `authorizedbuyersmarketplace_buyers_clients_deactivate_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn authorizedbuyersmarketplace_buyers_clients_deactivate_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Client>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Client = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1/buyers/{buyersId}/clients/{clientsId}:deactivate
/// Deactivates an existing client. The state of the client will be updated to "`INACTIVE`". This method has no effect if the client is already in "`INACTIVE`" state.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `authorizedbuyersmarketplace_buyers_clients_deactivate_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `authorizedbuyersmarketplace_buyers_clients_deactivate_task()`.
/// For the simplest API, use `authorizedbuyersmarketplace_buyers_clients_deactivate()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `authorizedbuyersmarketplace_buyers_clients_deactivate_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn authorizedbuyersmarketplace_buyers_clients_deactivate_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Client>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = authorizedbuyersmarketplace_buyers_clients_deactivate_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`authorizedbuyersmarketplace_buyers_clients_deactivate`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AuthorizedbuyersmarketplaceBuyersClientsDeactivateArgs {
    /// Path parameter: name
    pub name: String,
}

/// POST v1/buyers/{buyersId}/clients/{clientsId}:deactivate
/// Deactivates an existing client. The state of the client will be updated to "`INACTIVE`". This method has no effect if the client is already in "`INACTIVE`" state.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `authorizedbuyersmarketplace_buyers_clients_deactivate_builder()` + `authorizedbuyersmarketplace_buyers_clients_deactivate_execute()`.
/// For task-level control, use `authorizedbuyersmarketplace_buyers_clients_deactivate_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn authorizedbuyersmarketplace_buyers_clients_deactivate(
    client: &SimpleHttpClient,
    args: &AuthorizedbuyersmarketplaceBuyersClientsDeactivateArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Client>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        authorizedbuyersmarketplace_buyers_clients_deactivate_builder(client, &args.name)?;
    authorizedbuyersmarketplace_buyers_clients_deactivate_execute(builder)
}

/// GET v1/buyers/{buyersId}/clients/{clientsId}
/// Gets a client with a given resource name.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `authorizedbuyersmarketplace_buyers_clients_get_execute()` to send, or `authorizedbuyersmarketplace_buyers_clients_get` for simplest API.

pub fn authorizedbuyersmarketplace_buyers_clients_get_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://authorizedbuyersmarketplace.googleapis.com/v1/buyers/{}/clients/{clientsId}",
        name,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/buyers/{buyersId}/clients/{clientsId}
/// Gets a client with a given resource name.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `authorizedbuyersmarketplace_buyers_clients_get_execute()` or `authorizedbuyersmarketplace_buyers_clients_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `authorizedbuyersmarketplace_buyers_clients_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn authorizedbuyersmarketplace_buyers_clients_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Client>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Client = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/buyers/{buyersId}/clients/{clientsId}
/// Gets a client with a given resource name.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `authorizedbuyersmarketplace_buyers_clients_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `authorizedbuyersmarketplace_buyers_clients_get_task()`.
/// For the simplest API, use `authorizedbuyersmarketplace_buyers_clients_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `authorizedbuyersmarketplace_buyers_clients_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn authorizedbuyersmarketplace_buyers_clients_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Client>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = authorizedbuyersmarketplace_buyers_clients_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`authorizedbuyersmarketplace_buyers_clients_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AuthorizedbuyersmarketplaceBuyersClientsGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/buyers/{buyersId}/clients/{clientsId}
/// Gets a client with a given resource name.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `authorizedbuyersmarketplace_buyers_clients_get_builder()` + `authorizedbuyersmarketplace_buyers_clients_get_execute()`.
/// For task-level control, use `authorizedbuyersmarketplace_buyers_clients_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn authorizedbuyersmarketplace_buyers_clients_get(
    client: &SimpleHttpClient,
    args: &AuthorizedbuyersmarketplaceBuyersClientsGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Client>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = authorizedbuyersmarketplace_buyers_clients_get_builder(client, &args.name)?;
    authorizedbuyersmarketplace_buyers_clients_get_execute(builder)
}

/// GET v1/buyers/{buyersId}/clients
/// Lists all the clients for the current buyer.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `authorizedbuyersmarketplace_buyers_clients_list_execute()` to send, or `authorizedbuyersmarketplace_buyers_clients_list` for simplest API.

pub fn authorizedbuyersmarketplace_buyers_clients_list_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
    filter: &Option<Option<String>>,
    pageSize: &Option<Option<String>>,
    pageToken: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://authorizedbuyersmarketplace.googleapis.com/v1/buyers/{}/clients",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter.as_ref() {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = pageSize.as_ref() {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/buyers/{buyersId}/clients
/// Lists all the clients for the current buyer.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `authorizedbuyersmarketplace_buyers_clients_list_execute()` or `authorizedbuyersmarketplace_buyers_clients_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `authorizedbuyersmarketplace_buyers_clients_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn authorizedbuyersmarketplace_buyers_clients_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ListClientsResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListClientsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/buyers/{buyersId}/clients
/// Lists all the clients for the current buyer.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `authorizedbuyersmarketplace_buyers_clients_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `authorizedbuyersmarketplace_buyers_clients_list_task()`.
/// For the simplest API, use `authorizedbuyersmarketplace_buyers_clients_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `authorizedbuyersmarketplace_buyers_clients_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn authorizedbuyersmarketplace_buyers_clients_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListClientsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = authorizedbuyersmarketplace_buyers_clients_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`authorizedbuyersmarketplace_buyers_clients_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AuthorizedbuyersmarketplaceBuyersClientsListArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: filter
    pub filter: Option<Option<String>>,
    /// Query parameter: pageSize
    pub pageSize: Option<Option<String>>,
    /// Query parameter: pageToken
    pub pageToken: Option<Option<String>>,
}

/// GET v1/buyers/{buyersId}/clients
/// Lists all the clients for the current buyer.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `authorizedbuyersmarketplace_buyers_clients_list_builder()` + `authorizedbuyersmarketplace_buyers_clients_list_execute()`.
/// For task-level control, use `authorizedbuyersmarketplace_buyers_clients_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn authorizedbuyersmarketplace_buyers_clients_list(
    client: &SimpleHttpClient,
    args: &AuthorizedbuyersmarketplaceBuyersClientsListArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListClientsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = authorizedbuyersmarketplace_buyers_clients_list_builder(
        client,
        &args.parent,
        &args.filter,
        &args.pageSize,
        &args.pageToken,
    )?;
    authorizedbuyersmarketplace_buyers_clients_list_execute(builder)
}

/// PATCH v1/buyers/{buyersId}/clients/{clientsId}
/// Updates an existing client.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `authorizedbuyersmarketplace_buyers_clients_patch_execute()` to send, or `authorizedbuyersmarketplace_buyers_clients_patch` for simplest API.

pub fn authorizedbuyersmarketplace_buyers_clients_patch_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
    updateMask: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://authorizedbuyersmarketplace.googleapis.com/v1/buyers/{}/clients/{clientsId}",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = updateMask.as_ref() {
        query_parts.push(format!("updateMask={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .patch(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// PATCH v1/buyers/{buyersId}/clients/{clientsId}
/// Updates an existing client.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `authorizedbuyersmarketplace_buyers_clients_patch_execute()` or `authorizedbuyersmarketplace_buyers_clients_patch`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `authorizedbuyersmarketplace_buyers_clients_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn authorizedbuyersmarketplace_buyers_clients_patch_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Client>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Client = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// PATCH v1/buyers/{buyersId}/clients/{clientsId}
/// Updates an existing client.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `authorizedbuyersmarketplace_buyers_clients_patch_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `authorizedbuyersmarketplace_buyers_clients_patch_task()`.
/// For the simplest API, use `authorizedbuyersmarketplace_buyers_clients_patch()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `authorizedbuyersmarketplace_buyers_clients_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn authorizedbuyersmarketplace_buyers_clients_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Client>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = authorizedbuyersmarketplace_buyers_clients_patch_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`authorizedbuyersmarketplace_buyers_clients_patch`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AuthorizedbuyersmarketplaceBuyersClientsPatchArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: updateMask
    pub updateMask: Option<Option<String>>,
}

/// PATCH v1/buyers/{buyersId}/clients/{clientsId}
/// Updates an existing client.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `authorizedbuyersmarketplace_buyers_clients_patch_builder()` + `authorizedbuyersmarketplace_buyers_clients_patch_execute()`.
/// For task-level control, use `authorizedbuyersmarketplace_buyers_clients_patch_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn authorizedbuyersmarketplace_buyers_clients_patch(
    client: &SimpleHttpClient,
    args: &AuthorizedbuyersmarketplaceBuyersClientsPatchArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Client>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = authorizedbuyersmarketplace_buyers_clients_patch_builder(
        client,
        &args.name,
        &args.updateMask,
    )?;
    authorizedbuyersmarketplace_buyers_clients_patch_execute(builder)
}

/// POST v1/buyers/{buyersId}/clients/{clientsId}/users/{usersId}:activate
/// Activates an existing client user. The state of the client user will be updated from "`INACTIVE`" to "`ACTIVE`". This method has no effect if the client user is already in "`ACTIVE`" state. An error will be returned if the client user to activate is still in "INVITED" state.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `authorizedbuyersmarketplace_buyers_clients_users_activate_execute()` to send, or `authorizedbuyersmarketplace_buyers_clients_users_activate` for simplest API.

pub fn authorizedbuyersmarketplace_buyers_clients_users_activate_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://authorizedbuyersmarketplace.googleapis.com/v1/buyers/{}/clients/{clientsId}/users/{usersId}:activate",
        name,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1/buyers/{buyersId}/clients/{clientsId}/users/{usersId}:activate
/// Activates an existing client user. The state of the client user will be updated from "`INACTIVE`" to "`ACTIVE`". This method has no effect if the client user is already in "`ACTIVE`" state. An error will be returned if the client user to activate is still in "INVITED" state.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `authorizedbuyersmarketplace_buyers_clients_users_activate_execute()` or `authorizedbuyersmarketplace_buyers_clients_users_activate`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `authorizedbuyersmarketplace_buyers_clients_users_activate_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn authorizedbuyersmarketplace_buyers_clients_users_activate_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ClientUser>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ClientUser = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1/buyers/{buyersId}/clients/{clientsId}/users/{usersId}:activate
/// Activates an existing client user. The state of the client user will be updated from "`INACTIVE`" to "`ACTIVE`". This method has no effect if the client user is already in "`ACTIVE`" state. An error will be returned if the client user to activate is still in "INVITED" state.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `authorizedbuyersmarketplace_buyers_clients_users_activate_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `authorizedbuyersmarketplace_buyers_clients_users_activate_task()`.
/// For the simplest API, use `authorizedbuyersmarketplace_buyers_clients_users_activate()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `authorizedbuyersmarketplace_buyers_clients_users_activate_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn authorizedbuyersmarketplace_buyers_clients_users_activate_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ClientUser>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = authorizedbuyersmarketplace_buyers_clients_users_activate_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`authorizedbuyersmarketplace_buyers_clients_users_activate`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AuthorizedbuyersmarketplaceBuyersClientsUsersActivateArgs {
    /// Path parameter: name
    pub name: String,
}

/// POST v1/buyers/{buyersId}/clients/{clientsId}/users/{usersId}:activate
/// Activates an existing client user. The state of the client user will be updated from "`INACTIVE`" to "`ACTIVE`". This method has no effect if the client user is already in "`ACTIVE`" state. An error will be returned if the client user to activate is still in "INVITED" state.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `authorizedbuyersmarketplace_buyers_clients_users_activate_builder()` + `authorizedbuyersmarketplace_buyers_clients_users_activate_execute()`.
/// For task-level control, use `authorizedbuyersmarketplace_buyers_clients_users_activate_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn authorizedbuyersmarketplace_buyers_clients_users_activate(
    client: &SimpleHttpClient,
    args: &AuthorizedbuyersmarketplaceBuyersClientsUsersActivateArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ClientUser>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        authorizedbuyersmarketplace_buyers_clients_users_activate_builder(client, &args.name)?;
    authorizedbuyersmarketplace_buyers_clients_users_activate_execute(builder)
}

/// POST v1/buyers/{buyersId}/clients/{clientsId}/users
/// Creates a new client user in "INVITED" state. An email invitation will be sent to the new user, once accepted the user will become active.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `authorizedbuyersmarketplace_buyers_clients_users_create_execute()` to send, or `authorizedbuyersmarketplace_buyers_clients_users_create` for simplest API.

pub fn authorizedbuyersmarketplace_buyers_clients_users_create_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://authorizedbuyersmarketplace.googleapis.com/v1/buyers/{}/clients/{clientsId}/users",
        parent,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1/buyers/{buyersId}/clients/{clientsId}/users
/// Creates a new client user in "INVITED" state. An email invitation will be sent to the new user, once accepted the user will become active.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `authorizedbuyersmarketplace_buyers_clients_users_create_execute()` or `authorizedbuyersmarketplace_buyers_clients_users_create`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `authorizedbuyersmarketplace_buyers_clients_users_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn authorizedbuyersmarketplace_buyers_clients_users_create_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ClientUser>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ClientUser = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1/buyers/{buyersId}/clients/{clientsId}/users
/// Creates a new client user in "INVITED" state. An email invitation will be sent to the new user, once accepted the user will become active.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `authorizedbuyersmarketplace_buyers_clients_users_create_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `authorizedbuyersmarketplace_buyers_clients_users_create_task()`.
/// For the simplest API, use `authorizedbuyersmarketplace_buyers_clients_users_create()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `authorizedbuyersmarketplace_buyers_clients_users_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn authorizedbuyersmarketplace_buyers_clients_users_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ClientUser>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = authorizedbuyersmarketplace_buyers_clients_users_create_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`authorizedbuyersmarketplace_buyers_clients_users_create`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AuthorizedbuyersmarketplaceBuyersClientsUsersCreateArgs {
    /// Path parameter: parent
    pub parent: String,
}

/// POST v1/buyers/{buyersId}/clients/{clientsId}/users
/// Creates a new client user in "INVITED" state. An email invitation will be sent to the new user, once accepted the user will become active.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `authorizedbuyersmarketplace_buyers_clients_users_create_builder()` + `authorizedbuyersmarketplace_buyers_clients_users_create_execute()`.
/// For task-level control, use `authorizedbuyersmarketplace_buyers_clients_users_create_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn authorizedbuyersmarketplace_buyers_clients_users_create(
    client: &SimpleHttpClient,
    args: &AuthorizedbuyersmarketplaceBuyersClientsUsersCreateArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ClientUser>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        authorizedbuyersmarketplace_buyers_clients_users_create_builder(client, &args.parent)?;
    authorizedbuyersmarketplace_buyers_clients_users_create_execute(builder)
}

/// POST v1/buyers/{buyersId}/clients/{clientsId}/users/{usersId}:deactivate
/// Deactivates an existing client user. The state of the client user will be updated from "`ACTIVE`" to "`INACTIVE`". This method has no effect if the client user is already in "`INACTIVE`" state. An error will be returned if the client user to deactivate is still in "INVITED" state.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `authorizedbuyersmarketplace_buyers_clients_users_deactivate_execute()` to send, or `authorizedbuyersmarketplace_buyers_clients_users_deactivate` for simplest API.

pub fn authorizedbuyersmarketplace_buyers_clients_users_deactivate_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://authorizedbuyersmarketplace.googleapis.com/v1/buyers/{}/clients/{clientsId}/users/{usersId}:deactivate",
        name,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1/buyers/{buyersId}/clients/{clientsId}/users/{usersId}:deactivate
/// Deactivates an existing client user. The state of the client user will be updated from "`ACTIVE`" to "`INACTIVE`". This method has no effect if the client user is already in "`INACTIVE`" state. An error will be returned if the client user to deactivate is still in "INVITED" state.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `authorizedbuyersmarketplace_buyers_clients_users_deactivate_execute()` or `authorizedbuyersmarketplace_buyers_clients_users_deactivate`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `authorizedbuyersmarketplace_buyers_clients_users_deactivate_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn authorizedbuyersmarketplace_buyers_clients_users_deactivate_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ClientUser>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ClientUser = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1/buyers/{buyersId}/clients/{clientsId}/users/{usersId}:deactivate
/// Deactivates an existing client user. The state of the client user will be updated from "`ACTIVE`" to "`INACTIVE`". This method has no effect if the client user is already in "`INACTIVE`" state. An error will be returned if the client user to deactivate is still in "INVITED" state.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `authorizedbuyersmarketplace_buyers_clients_users_deactivate_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `authorizedbuyersmarketplace_buyers_clients_users_deactivate_task()`.
/// For the simplest API, use `authorizedbuyersmarketplace_buyers_clients_users_deactivate()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `authorizedbuyersmarketplace_buyers_clients_users_deactivate_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn authorizedbuyersmarketplace_buyers_clients_users_deactivate_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ClientUser>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = authorizedbuyersmarketplace_buyers_clients_users_deactivate_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`authorizedbuyersmarketplace_buyers_clients_users_deactivate`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AuthorizedbuyersmarketplaceBuyersClientsUsersDeactivateArgs {
    /// Path parameter: name
    pub name: String,
}

/// POST v1/buyers/{buyersId}/clients/{clientsId}/users/{usersId}:deactivate
/// Deactivates an existing client user. The state of the client user will be updated from "`ACTIVE`" to "`INACTIVE`". This method has no effect if the client user is already in "`INACTIVE`" state. An error will be returned if the client user to deactivate is still in "INVITED" state.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `authorizedbuyersmarketplace_buyers_clients_users_deactivate_builder()` + `authorizedbuyersmarketplace_buyers_clients_users_deactivate_execute()`.
/// For task-level control, use `authorizedbuyersmarketplace_buyers_clients_users_deactivate_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn authorizedbuyersmarketplace_buyers_clients_users_deactivate(
    client: &SimpleHttpClient,
    args: &AuthorizedbuyersmarketplaceBuyersClientsUsersDeactivateArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ClientUser>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        authorizedbuyersmarketplace_buyers_clients_users_deactivate_builder(client, &args.name)?;
    authorizedbuyersmarketplace_buyers_clients_users_deactivate_execute(builder)
}

/// DELETE v1/buyers/{buyersId}/clients/{clientsId}/users/{usersId}
/// Deletes an existing client user. The client user will lose access to the Authorized Buyers UI. Note that if a client user is deleted, the user's access to the UI can't be restored unless a new client user is created and activated.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `authorizedbuyersmarketplace_buyers_clients_users_delete_execute()` to send, or `authorizedbuyersmarketplace_buyers_clients_users_delete` for simplest API.

pub fn authorizedbuyersmarketplace_buyers_clients_users_delete_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://authorizedbuyersmarketplace.googleapis.com/v1/buyers/{}/clients/{clientsId}/users/{usersId}",
        name,
    );

    // Build request
    let builder = client
        .delete(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// DELETE v1/buyers/{buyersId}/clients/{clientsId}/users/{usersId}
/// Deletes an existing client user. The client user will lose access to the Authorized Buyers UI. Note that if a client user is deleted, the user's access to the UI can't be restored unless a new client user is created and activated.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `authorizedbuyersmarketplace_buyers_clients_users_delete_execute()` or `authorizedbuyersmarketplace_buyers_clients_users_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `authorizedbuyersmarketplace_buyers_clients_users_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn authorizedbuyersmarketplace_buyers_clients_users_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Empty>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Empty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// DELETE v1/buyers/{buyersId}/clients/{clientsId}/users/{usersId}
/// Deletes an existing client user. The client user will lose access to the Authorized Buyers UI. Note that if a client user is deleted, the user's access to the UI can't be restored unless a new client user is created and activated.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `authorizedbuyersmarketplace_buyers_clients_users_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `authorizedbuyersmarketplace_buyers_clients_users_delete_task()`.
/// For the simplest API, use `authorizedbuyersmarketplace_buyers_clients_users_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `authorizedbuyersmarketplace_buyers_clients_users_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn authorizedbuyersmarketplace_buyers_clients_users_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = authorizedbuyersmarketplace_buyers_clients_users_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`authorizedbuyersmarketplace_buyers_clients_users_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AuthorizedbuyersmarketplaceBuyersClientsUsersDeleteArgs {
    /// Path parameter: name
    pub name: String,
}

/// DELETE v1/buyers/{buyersId}/clients/{clientsId}/users/{usersId}
/// Deletes an existing client user. The client user will lose access to the Authorized Buyers UI. Note that if a client user is deleted, the user's access to the UI can't be restored unless a new client user is created and activated.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `authorizedbuyersmarketplace_buyers_clients_users_delete_builder()` + `authorizedbuyersmarketplace_buyers_clients_users_delete_execute()`.
/// For task-level control, use `authorizedbuyersmarketplace_buyers_clients_users_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn authorizedbuyersmarketplace_buyers_clients_users_delete(
    client: &SimpleHttpClient,
    args: &AuthorizedbuyersmarketplaceBuyersClientsUsersDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        authorizedbuyersmarketplace_buyers_clients_users_delete_builder(client, &args.name)?;
    authorizedbuyersmarketplace_buyers_clients_users_delete_execute(builder)
}

/// GET v1/buyers/{buyersId}/clients/{clientsId}/users/{usersId}
/// Retrieves an existing client user.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `authorizedbuyersmarketplace_buyers_clients_users_get_execute()` to send, or `authorizedbuyersmarketplace_buyers_clients_users_get` for simplest API.

pub fn authorizedbuyersmarketplace_buyers_clients_users_get_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://authorizedbuyersmarketplace.googleapis.com/v1/buyers/{}/clients/{clientsId}/users/{usersId}",
        name,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/buyers/{buyersId}/clients/{clientsId}/users/{usersId}
/// Retrieves an existing client user.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `authorizedbuyersmarketplace_buyers_clients_users_get_execute()` or `authorizedbuyersmarketplace_buyers_clients_users_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `authorizedbuyersmarketplace_buyers_clients_users_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn authorizedbuyersmarketplace_buyers_clients_users_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ClientUser>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ClientUser = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/buyers/{buyersId}/clients/{clientsId}/users/{usersId}
/// Retrieves an existing client user.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `authorizedbuyersmarketplace_buyers_clients_users_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `authorizedbuyersmarketplace_buyers_clients_users_get_task()`.
/// For the simplest API, use `authorizedbuyersmarketplace_buyers_clients_users_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `authorizedbuyersmarketplace_buyers_clients_users_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn authorizedbuyersmarketplace_buyers_clients_users_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ClientUser>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = authorizedbuyersmarketplace_buyers_clients_users_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`authorizedbuyersmarketplace_buyers_clients_users_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AuthorizedbuyersmarketplaceBuyersClientsUsersGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/buyers/{buyersId}/clients/{clientsId}/users/{usersId}
/// Retrieves an existing client user.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `authorizedbuyersmarketplace_buyers_clients_users_get_builder()` + `authorizedbuyersmarketplace_buyers_clients_users_get_execute()`.
/// For task-level control, use `authorizedbuyersmarketplace_buyers_clients_users_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn authorizedbuyersmarketplace_buyers_clients_users_get(
    client: &SimpleHttpClient,
    args: &AuthorizedbuyersmarketplaceBuyersClientsUsersGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ClientUser>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = authorizedbuyersmarketplace_buyers_clients_users_get_builder(client, &args.name)?;
    authorizedbuyersmarketplace_buyers_clients_users_get_execute(builder)
}

/// GET v1/buyers/{buyersId}/clients/{clientsId}/users
/// Lists all client users for a specified client.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `authorizedbuyersmarketplace_buyers_clients_users_list_execute()` to send, or `authorizedbuyersmarketplace_buyers_clients_users_list` for simplest API.

pub fn authorizedbuyersmarketplace_buyers_clients_users_list_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
    pageSize: &Option<Option<String>>,
    pageToken: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://authorizedbuyersmarketplace.googleapis.com/v1/buyers/{}/clients/{clientsId}/users",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = pageSize.as_ref() {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/buyers/{buyersId}/clients/{clientsId}/users
/// Lists all client users for a specified client.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `authorizedbuyersmarketplace_buyers_clients_users_list_execute()` or `authorizedbuyersmarketplace_buyers_clients_users_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `authorizedbuyersmarketplace_buyers_clients_users_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn authorizedbuyersmarketplace_buyers_clients_users_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ListClientUsersResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListClientUsersResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/buyers/{buyersId}/clients/{clientsId}/users
/// Lists all client users for a specified client.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `authorizedbuyersmarketplace_buyers_clients_users_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `authorizedbuyersmarketplace_buyers_clients_users_list_task()`.
/// For the simplest API, use `authorizedbuyersmarketplace_buyers_clients_users_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `authorizedbuyersmarketplace_buyers_clients_users_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn authorizedbuyersmarketplace_buyers_clients_users_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListClientUsersResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = authorizedbuyersmarketplace_buyers_clients_users_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`authorizedbuyersmarketplace_buyers_clients_users_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AuthorizedbuyersmarketplaceBuyersClientsUsersListArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: pageSize
    pub pageSize: Option<Option<String>>,
    /// Query parameter: pageToken
    pub pageToken: Option<Option<String>>,
}

/// GET v1/buyers/{buyersId}/clients/{clientsId}/users
/// Lists all client users for a specified client.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `authorizedbuyersmarketplace_buyers_clients_users_list_builder()` + `authorizedbuyersmarketplace_buyers_clients_users_list_execute()`.
/// For task-level control, use `authorizedbuyersmarketplace_buyers_clients_users_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn authorizedbuyersmarketplace_buyers_clients_users_list(
    client: &SimpleHttpClient,
    args: &AuthorizedbuyersmarketplaceBuyersClientsUsersListArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListClientUsersResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = authorizedbuyersmarketplace_buyers_clients_users_list_builder(
        client,
        &args.parent,
        &args.pageSize,
        &args.pageToken,
    )?;
    authorizedbuyersmarketplace_buyers_clients_users_list_execute(builder)
}

/// POST v1/buyers/{buyersId}/finalizedDeals/{finalizedDealsId}:addCreative
/// Add creative to be used in the bidding process for a finalized deal. For programmatic guaranteed deals, it's recommended that you associate at least one approved creative with the deal before calling SetReadyToServe, to help reduce the number of bid responses filtered because they don't contain approved creatives. Creatives successfully added to a deal can be found in the Realtime-bidding Creatives API creative.deal_ids. This method only applies to programmatic guaranteed deals. Maximum number of 1000 creatives can be added to a finalized deal.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `authorizedbuyersmarketplace_buyers_finalized_deals_add_creative_execute()` to send, or `authorizedbuyersmarketplace_buyers_finalized_deals_add_creative` for simplest API.

pub fn authorizedbuyersmarketplace_buyers_finalized_deals_add_creative_builder<R>(
    client: &SimpleHttpClient<R>,
    deal: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://authorizedbuyersmarketplace.googleapis.com/v1/buyers/{}/finalizedDeals/{finalizedDealsId}:addCreative",
        deal,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1/buyers/{buyersId}/finalizedDeals/{finalizedDealsId}:addCreative
/// Add creative to be used in the bidding process for a finalized deal. For programmatic guaranteed deals, it's recommended that you associate at least one approved creative with the deal before calling SetReadyToServe, to help reduce the number of bid responses filtered because they don't contain approved creatives. Creatives successfully added to a deal can be found in the Realtime-bidding Creatives API creative.deal_ids. This method only applies to programmatic guaranteed deals. Maximum number of 1000 creatives can be added to a finalized deal.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `authorizedbuyersmarketplace_buyers_finalized_deals_add_creative_execute()` or `authorizedbuyersmarketplace_buyers_finalized_deals_add_creative`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `authorizedbuyersmarketplace_buyers_finalized_deals_add_creative_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn authorizedbuyersmarketplace_buyers_finalized_deals_add_creative_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<FinalizedDeal>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: FinalizedDeal = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1/buyers/{buyersId}/finalizedDeals/{finalizedDealsId}:addCreative
/// Add creative to be used in the bidding process for a finalized deal. For programmatic guaranteed deals, it's recommended that you associate at least one approved creative with the deal before calling SetReadyToServe, to help reduce the number of bid responses filtered because they don't contain approved creatives. Creatives successfully added to a deal can be found in the Realtime-bidding Creatives API creative.deal_ids. This method only applies to programmatic guaranteed deals. Maximum number of 1000 creatives can be added to a finalized deal.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `authorizedbuyersmarketplace_buyers_finalized_deals_add_creative_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `authorizedbuyersmarketplace_buyers_finalized_deals_add_creative_task()`.
/// For the simplest API, use `authorizedbuyersmarketplace_buyers_finalized_deals_add_creative()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `authorizedbuyersmarketplace_buyers_finalized_deals_add_creative_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn authorizedbuyersmarketplace_buyers_finalized_deals_add_creative_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<FinalizedDeal>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = authorizedbuyersmarketplace_buyers_finalized_deals_add_creative_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`authorizedbuyersmarketplace_buyers_finalized_deals_add_creative`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AuthorizedbuyersmarketplaceBuyersFinalizedDealsAddCreativeArgs {
    /// Path parameter: deal
    pub deal: String,
}

/// POST v1/buyers/{buyersId}/finalizedDeals/{finalizedDealsId}:addCreative
/// Add creative to be used in the bidding process for a finalized deal. For programmatic guaranteed deals, it's recommended that you associate at least one approved creative with the deal before calling SetReadyToServe, to help reduce the number of bid responses filtered because they don't contain approved creatives. Creatives successfully added to a deal can be found in the Realtime-bidding Creatives API creative.deal_ids. This method only applies to programmatic guaranteed deals. Maximum number of 1000 creatives can be added to a finalized deal.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `authorizedbuyersmarketplace_buyers_finalized_deals_add_creative_builder()` + `authorizedbuyersmarketplace_buyers_finalized_deals_add_creative_execute()`.
/// For task-level control, use `authorizedbuyersmarketplace_buyers_finalized_deals_add_creative_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn authorizedbuyersmarketplace_buyers_finalized_deals_add_creative(
    client: &SimpleHttpClient,
    args: &AuthorizedbuyersmarketplaceBuyersFinalizedDealsAddCreativeArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<FinalizedDeal>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = authorizedbuyersmarketplace_buyers_finalized_deals_add_creative_builder(
        client, &args.deal,
    )?;
    authorizedbuyersmarketplace_buyers_finalized_deals_add_creative_execute(builder)
}

/// GET v1/buyers/{buyersId}/finalizedDeals/{finalizedDealsId}
/// Gets a finalized deal given its name.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `authorizedbuyersmarketplace_buyers_finalized_deals_get_execute()` to send, or `authorizedbuyersmarketplace_buyers_finalized_deals_get` for simplest API.

pub fn authorizedbuyersmarketplace_buyers_finalized_deals_get_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://authorizedbuyersmarketplace.googleapis.com/v1/buyers/{}/finalizedDeals/{finalizedDealsId}",
        name,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/buyers/{buyersId}/finalizedDeals/{finalizedDealsId}
/// Gets a finalized deal given its name.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `authorizedbuyersmarketplace_buyers_finalized_deals_get_execute()` or `authorizedbuyersmarketplace_buyers_finalized_deals_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `authorizedbuyersmarketplace_buyers_finalized_deals_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn authorizedbuyersmarketplace_buyers_finalized_deals_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<FinalizedDeal>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: FinalizedDeal = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/buyers/{buyersId}/finalizedDeals/{finalizedDealsId}
/// Gets a finalized deal given its name.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `authorizedbuyersmarketplace_buyers_finalized_deals_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `authorizedbuyersmarketplace_buyers_finalized_deals_get_task()`.
/// For the simplest API, use `authorizedbuyersmarketplace_buyers_finalized_deals_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `authorizedbuyersmarketplace_buyers_finalized_deals_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn authorizedbuyersmarketplace_buyers_finalized_deals_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<FinalizedDeal>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = authorizedbuyersmarketplace_buyers_finalized_deals_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`authorizedbuyersmarketplace_buyers_finalized_deals_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AuthorizedbuyersmarketplaceBuyersFinalizedDealsGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/buyers/{buyersId}/finalizedDeals/{finalizedDealsId}
/// Gets a finalized deal given its name.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `authorizedbuyersmarketplace_buyers_finalized_deals_get_builder()` + `authorizedbuyersmarketplace_buyers_finalized_deals_get_execute()`.
/// For task-level control, use `authorizedbuyersmarketplace_buyers_finalized_deals_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn authorizedbuyersmarketplace_buyers_finalized_deals_get(
    client: &SimpleHttpClient,
    args: &AuthorizedbuyersmarketplaceBuyersFinalizedDealsGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<FinalizedDeal>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        authorizedbuyersmarketplace_buyers_finalized_deals_get_builder(client, &args.name)?;
    authorizedbuyersmarketplace_buyers_finalized_deals_get_execute(builder)
}

/// GET v1/buyers/{buyersId}/finalizedDeals
/// Lists finalized deals. Use the URL path "/v1/`buyers/{`accountId`}/`finalizedDeals``" to list finalized deals for the current buyer and its clients. Bidders can use the URL path "/v1/`bidders/{`accountId`}/`finalizedDeals``" to list finalized deals for the bidder, its buyers and all their clients.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `authorizedbuyersmarketplace_buyers_finalized_deals_list_execute()` to send, or `authorizedbuyersmarketplace_buyers_finalized_deals_list` for simplest API.

pub fn authorizedbuyersmarketplace_buyers_finalized_deals_list_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
    filter: &Option<Option<String>>,
    orderBy: &Option<Option<String>>,
    pageSize: &Option<Option<String>>,
    pageToken: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://authorizedbuyersmarketplace.googleapis.com/v1/buyers/{}/finalizedDeals",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter.as_ref() {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = orderBy.as_ref() {
        query_parts.push(format!("orderBy={}", val));
    }
    if let Some(val) = pageSize.as_ref() {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/buyers/{buyersId}/finalizedDeals
/// Lists finalized deals. Use the URL path "/v1/`buyers/{`accountId`}/`finalizedDeals``" to list finalized deals for the current buyer and its clients. Bidders can use the URL path "/v1/`bidders/{`accountId`}/`finalizedDeals``" to list finalized deals for the bidder, its buyers and all their clients.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `authorizedbuyersmarketplace_buyers_finalized_deals_list_execute()` or `authorizedbuyersmarketplace_buyers_finalized_deals_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `authorizedbuyersmarketplace_buyers_finalized_deals_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn authorizedbuyersmarketplace_buyers_finalized_deals_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ListFinalizedDealsResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListFinalizedDealsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/buyers/{buyersId}/finalizedDeals
/// Lists finalized deals. Use the URL path "/v1/`buyers/{`accountId`}/`finalizedDeals``" to list finalized deals for the current buyer and its clients. Bidders can use the URL path "/v1/`bidders/{`accountId`}/`finalizedDeals``" to list finalized deals for the bidder, its buyers and all their clients.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `authorizedbuyersmarketplace_buyers_finalized_deals_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `authorizedbuyersmarketplace_buyers_finalized_deals_list_task()`.
/// For the simplest API, use `authorizedbuyersmarketplace_buyers_finalized_deals_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `authorizedbuyersmarketplace_buyers_finalized_deals_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn authorizedbuyersmarketplace_buyers_finalized_deals_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ListFinalizedDealsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = authorizedbuyersmarketplace_buyers_finalized_deals_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`authorizedbuyersmarketplace_buyers_finalized_deals_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AuthorizedbuyersmarketplaceBuyersFinalizedDealsListArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: filter
    pub filter: Option<Option<String>>,
    /// Query parameter: orderBy
    pub orderBy: Option<Option<String>>,
    /// Query parameter: pageSize
    pub pageSize: Option<Option<String>>,
    /// Query parameter: pageToken
    pub pageToken: Option<Option<String>>,
}

/// GET v1/buyers/{buyersId}/finalizedDeals
/// Lists finalized deals. Use the URL path "/v1/`buyers/{`accountId`}/`finalizedDeals``" to list finalized deals for the current buyer and its clients. Bidders can use the URL path "/v1/`bidders/{`accountId`}/`finalizedDeals``" to list finalized deals for the bidder, its buyers and all their clients.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `authorizedbuyersmarketplace_buyers_finalized_deals_list_builder()` + `authorizedbuyersmarketplace_buyers_finalized_deals_list_execute()`.
/// For task-level control, use `authorizedbuyersmarketplace_buyers_finalized_deals_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn authorizedbuyersmarketplace_buyers_finalized_deals_list(
    client: &SimpleHttpClient,
    args: &AuthorizedbuyersmarketplaceBuyersFinalizedDealsListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ListFinalizedDealsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = authorizedbuyersmarketplace_buyers_finalized_deals_list_builder(
        client,
        &args.parent,
        &args.filter,
        &args.orderBy,
        &args.pageSize,
        &args.pageToken,
    )?;
    authorizedbuyersmarketplace_buyers_finalized_deals_list_execute(builder)
}

/// POST v1/buyers/{buyersId}/finalizedDeals/{finalizedDealsId}:pause
/// Pauses serving of the given finalized deal. This call only pauses the serving status, and does not affect other fields of the finalized deal. Calling this method for an already paused deal has no effect. This method only applies to programmatic guaranteed deals and preferred deals.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `authorizedbuyersmarketplace_buyers_finalized_deals_pause_execute()` to send, or `authorizedbuyersmarketplace_buyers_finalized_deals_pause` for simplest API.

pub fn authorizedbuyersmarketplace_buyers_finalized_deals_pause_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://authorizedbuyersmarketplace.googleapis.com/v1/buyers/{}/finalizedDeals/{finalizedDealsId}:pause",
        name,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1/buyers/{buyersId}/finalizedDeals/{finalizedDealsId}:pause
/// Pauses serving of the given finalized deal. This call only pauses the serving status, and does not affect other fields of the finalized deal. Calling this method for an already paused deal has no effect. This method only applies to programmatic guaranteed deals and preferred deals.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `authorizedbuyersmarketplace_buyers_finalized_deals_pause_execute()` or `authorizedbuyersmarketplace_buyers_finalized_deals_pause`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `authorizedbuyersmarketplace_buyers_finalized_deals_pause_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn authorizedbuyersmarketplace_buyers_finalized_deals_pause_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<FinalizedDeal>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: FinalizedDeal = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1/buyers/{buyersId}/finalizedDeals/{finalizedDealsId}:pause
/// Pauses serving of the given finalized deal. This call only pauses the serving status, and does not affect other fields of the finalized deal. Calling this method for an already paused deal has no effect. This method only applies to programmatic guaranteed deals and preferred deals.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `authorizedbuyersmarketplace_buyers_finalized_deals_pause_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `authorizedbuyersmarketplace_buyers_finalized_deals_pause_task()`.
/// For the simplest API, use `authorizedbuyersmarketplace_buyers_finalized_deals_pause()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `authorizedbuyersmarketplace_buyers_finalized_deals_pause_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn authorizedbuyersmarketplace_buyers_finalized_deals_pause_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<FinalizedDeal>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = authorizedbuyersmarketplace_buyers_finalized_deals_pause_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`authorizedbuyersmarketplace_buyers_finalized_deals_pause`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AuthorizedbuyersmarketplaceBuyersFinalizedDealsPauseArgs {
    /// Path parameter: name
    pub name: String,
}

/// POST v1/buyers/{buyersId}/finalizedDeals/{finalizedDealsId}:pause
/// Pauses serving of the given finalized deal. This call only pauses the serving status, and does not affect other fields of the finalized deal. Calling this method for an already paused deal has no effect. This method only applies to programmatic guaranteed deals and preferred deals.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `authorizedbuyersmarketplace_buyers_finalized_deals_pause_builder()` + `authorizedbuyersmarketplace_buyers_finalized_deals_pause_execute()`.
/// For task-level control, use `authorizedbuyersmarketplace_buyers_finalized_deals_pause_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn authorizedbuyersmarketplace_buyers_finalized_deals_pause(
    client: &SimpleHttpClient,
    args: &AuthorizedbuyersmarketplaceBuyersFinalizedDealsPauseArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<FinalizedDeal>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        authorizedbuyersmarketplace_buyers_finalized_deals_pause_builder(client, &args.name)?;
    authorizedbuyersmarketplace_buyers_finalized_deals_pause_execute(builder)
}

/// POST v1/buyers/{buyersId}/finalizedDeals/{finalizedDealsId}:resume
/// Resumes serving of the given finalized deal. Calling this method for an running deal has no effect. If a deal is initially paused by the seller, calling this method will not resume serving of the deal until the seller also resumes the deal. This method only applies to programmatic guaranteed deals and preferred deals.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `authorizedbuyersmarketplace_buyers_finalized_deals_resume_execute()` to send, or `authorizedbuyersmarketplace_buyers_finalized_deals_resume` for simplest API.

pub fn authorizedbuyersmarketplace_buyers_finalized_deals_resume_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://authorizedbuyersmarketplace.googleapis.com/v1/buyers/{}/finalizedDeals/{finalizedDealsId}:resume",
        name,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1/buyers/{buyersId}/finalizedDeals/{finalizedDealsId}:resume
/// Resumes serving of the given finalized deal. Calling this method for an running deal has no effect. If a deal is initially paused by the seller, calling this method will not resume serving of the deal until the seller also resumes the deal. This method only applies to programmatic guaranteed deals and preferred deals.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `authorizedbuyersmarketplace_buyers_finalized_deals_resume_execute()` or `authorizedbuyersmarketplace_buyers_finalized_deals_resume`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `authorizedbuyersmarketplace_buyers_finalized_deals_resume_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn authorizedbuyersmarketplace_buyers_finalized_deals_resume_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<FinalizedDeal>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: FinalizedDeal = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1/buyers/{buyersId}/finalizedDeals/{finalizedDealsId}:resume
/// Resumes serving of the given finalized deal. Calling this method for an running deal has no effect. If a deal is initially paused by the seller, calling this method will not resume serving of the deal until the seller also resumes the deal. This method only applies to programmatic guaranteed deals and preferred deals.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `authorizedbuyersmarketplace_buyers_finalized_deals_resume_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `authorizedbuyersmarketplace_buyers_finalized_deals_resume_task()`.
/// For the simplest API, use `authorizedbuyersmarketplace_buyers_finalized_deals_resume()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `authorizedbuyersmarketplace_buyers_finalized_deals_resume_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn authorizedbuyersmarketplace_buyers_finalized_deals_resume_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<FinalizedDeal>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = authorizedbuyersmarketplace_buyers_finalized_deals_resume_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`authorizedbuyersmarketplace_buyers_finalized_deals_resume`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AuthorizedbuyersmarketplaceBuyersFinalizedDealsResumeArgs {
    /// Path parameter: name
    pub name: String,
}

/// POST v1/buyers/{buyersId}/finalizedDeals/{finalizedDealsId}:resume
/// Resumes serving of the given finalized deal. Calling this method for an running deal has no effect. If a deal is initially paused by the seller, calling this method will not resume serving of the deal until the seller also resumes the deal. This method only applies to programmatic guaranteed deals and preferred deals.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `authorizedbuyersmarketplace_buyers_finalized_deals_resume_builder()` + `authorizedbuyersmarketplace_buyers_finalized_deals_resume_execute()`.
/// For task-level control, use `authorizedbuyersmarketplace_buyers_finalized_deals_resume_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn authorizedbuyersmarketplace_buyers_finalized_deals_resume(
    client: &SimpleHttpClient,
    args: &AuthorizedbuyersmarketplaceBuyersFinalizedDealsResumeArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<FinalizedDeal>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        authorizedbuyersmarketplace_buyers_finalized_deals_resume_builder(client, &args.name)?;
    authorizedbuyersmarketplace_buyers_finalized_deals_resume_execute(builder)
}

/// POST v1/buyers/{buyersId}/finalizedDeals/{finalizedDealsId}:setReadyToServe
/// Sets the given finalized deal as ready to serve. By default, deals are set as ready to serve as soon as they're finalized. If you want to opt out of the default behavior, and manually indicate that deals are ready to serve, ask your Technical Account Manager to add you to the allowlist. If you choose to use this method, finalized deals belonging to the bidder and its child seats don't start serving until after you call `setReadyToServe`, and after the deals become active. For example, you can use this method to delay receiving bid requests until your creative is ready. In addition, bidders can use the URL path "/v1/`bidders/{`accountId`}/`finalizedDeals`/{`dealId`}`" to set ready to serve for the finalized deals belong to itself, its child seats and all their clients. This method only applies to programmatic guaranteed deals.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `authorizedbuyersmarketplace_buyers_finalized_deals_set_ready_to_serve_execute()` to send, or `authorizedbuyersmarketplace_buyers_finalized_deals_set_ready_to_serve` for simplest API.

pub fn authorizedbuyersmarketplace_buyers_finalized_deals_set_ready_to_serve_builder<R>(
    client: &SimpleHttpClient<R>,
    deal: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://authorizedbuyersmarketplace.googleapis.com/v1/buyers/{}/finalizedDeals/{finalizedDealsId}:setReadyToServe",
        deal,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1/buyers/{buyersId}/finalizedDeals/{finalizedDealsId}:setReadyToServe
/// Sets the given finalized deal as ready to serve. By default, deals are set as ready to serve as soon as they're finalized. If you want to opt out of the default behavior, and manually indicate that deals are ready to serve, ask your Technical Account Manager to add you to the allowlist. If you choose to use this method, finalized deals belonging to the bidder and its child seats don't start serving until after you call `setReadyToServe`, and after the deals become active. For example, you can use this method to delay receiving bid requests until your creative is ready. In addition, bidders can use the URL path "/v1/`bidders/{`accountId`}/`finalizedDeals`/{`dealId`}`" to set ready to serve for the finalized deals belong to itself, its child seats and all their clients. This method only applies to programmatic guaranteed deals.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `authorizedbuyersmarketplace_buyers_finalized_deals_set_ready_to_serve_execute()` or `authorizedbuyersmarketplace_buyers_finalized_deals_set_ready_to_serve`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `authorizedbuyersmarketplace_buyers_finalized_deals_set_ready_to_serve_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn authorizedbuyersmarketplace_buyers_finalized_deals_set_ready_to_serve_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<FinalizedDeal>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: FinalizedDeal = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1/buyers/{buyersId}/finalizedDeals/{finalizedDealsId}:setReadyToServe
/// Sets the given finalized deal as ready to serve. By default, deals are set as ready to serve as soon as they're finalized. If you want to opt out of the default behavior, and manually indicate that deals are ready to serve, ask your Technical Account Manager to add you to the allowlist. If you choose to use this method, finalized deals belonging to the bidder and its child seats don't start serving until after you call `setReadyToServe`, and after the deals become active. For example, you can use this method to delay receiving bid requests until your creative is ready. In addition, bidders can use the URL path "/v1/`bidders/{`accountId`}/`finalizedDeals`/{`dealId`}`" to set ready to serve for the finalized deals belong to itself, its child seats and all their clients. This method only applies to programmatic guaranteed deals.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `authorizedbuyersmarketplace_buyers_finalized_deals_set_ready_to_serve_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `authorizedbuyersmarketplace_buyers_finalized_deals_set_ready_to_serve_task()`.
/// For the simplest API, use `authorizedbuyersmarketplace_buyers_finalized_deals_set_ready_to_serve()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `authorizedbuyersmarketplace_buyers_finalized_deals_set_ready_to_serve_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn authorizedbuyersmarketplace_buyers_finalized_deals_set_ready_to_serve_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<FinalizedDeal>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = authorizedbuyersmarketplace_buyers_finalized_deals_set_ready_to_serve_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`authorizedbuyersmarketplace_buyers_finalized_deals_set_ready_to_serve`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AuthorizedbuyersmarketplaceBuyersFinalizedDealsSetReadyToServeArgs {
    /// Path parameter: deal
    pub deal: String,
}

/// POST v1/buyers/{buyersId}/finalizedDeals/{finalizedDealsId}:setReadyToServe
/// Sets the given finalized deal as ready to serve. By default, deals are set as ready to serve as soon as they're finalized. If you want to opt out of the default behavior, and manually indicate that deals are ready to serve, ask your Technical Account Manager to add you to the allowlist. If you choose to use this method, finalized deals belonging to the bidder and its child seats don't start serving until after you call `setReadyToServe`, and after the deals become active. For example, you can use this method to delay receiving bid requests until your creative is ready. In addition, bidders can use the URL path "/v1/`bidders/{`accountId`}/`finalizedDeals`/{`dealId`}`" to set ready to serve for the finalized deals belong to itself, its child seats and all their clients. This method only applies to programmatic guaranteed deals.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `authorizedbuyersmarketplace_buyers_finalized_deals_set_ready_to_serve_builder()` + `authorizedbuyersmarketplace_buyers_finalized_deals_set_ready_to_serve_execute()`.
/// For task-level control, use `authorizedbuyersmarketplace_buyers_finalized_deals_set_ready_to_serve_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn authorizedbuyersmarketplace_buyers_finalized_deals_set_ready_to_serve(
    client: &SimpleHttpClient,
    args: &AuthorizedbuyersmarketplaceBuyersFinalizedDealsSetReadyToServeArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<FinalizedDeal>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = authorizedbuyersmarketplace_buyers_finalized_deals_set_ready_to_serve_builder(
        client, &args.deal,
    )?;
    authorizedbuyersmarketplace_buyers_finalized_deals_set_ready_to_serve_execute(builder)
}

/// POST v1/buyers/{buyersId}/proposals/{proposalsId}:accept
/// Accepts the proposal at the given revision number. If the revision number in the request is behind the latest from the server, an error message will be returned. This call updates the Proposal.state from BUYER_ACCEPTANCE_REQUESTED to FINALIZED; it has no side effect if the Proposal.state is already FINALIZED and throws exception if the Proposal.state is not either BUYER_ACCEPTANCE_REQUESTED or FINALIZED. Accepting a proposal means the buyer understands and accepts the Proposal.terms_and_conditions proposed by the seller.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `authorizedbuyersmarketplace_buyers_proposals_accept_execute()` to send, or `authorizedbuyersmarketplace_buyers_proposals_accept` for simplest API.

pub fn authorizedbuyersmarketplace_buyers_proposals_accept_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://authorizedbuyersmarketplace.googleapis.com/v1/buyers/{}/proposals/{proposalsId}:accept",
        name,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1/buyers/{buyersId}/proposals/{proposalsId}:accept
/// Accepts the proposal at the given revision number. If the revision number in the request is behind the latest from the server, an error message will be returned. This call updates the Proposal.state from BUYER_ACCEPTANCE_REQUESTED to FINALIZED; it has no side effect if the Proposal.state is already FINALIZED and throws exception if the Proposal.state is not either BUYER_ACCEPTANCE_REQUESTED or FINALIZED. Accepting a proposal means the buyer understands and accepts the Proposal.terms_and_conditions proposed by the seller.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `authorizedbuyersmarketplace_buyers_proposals_accept_execute()` or `authorizedbuyersmarketplace_buyers_proposals_accept`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `authorizedbuyersmarketplace_buyers_proposals_accept_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn authorizedbuyersmarketplace_buyers_proposals_accept_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Proposal>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Proposal = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1/buyers/{buyersId}/proposals/{proposalsId}:accept
/// Accepts the proposal at the given revision number. If the revision number in the request is behind the latest from the server, an error message will be returned. This call updates the Proposal.state from BUYER_ACCEPTANCE_REQUESTED to FINALIZED; it has no side effect if the Proposal.state is already FINALIZED and throws exception if the Proposal.state is not either BUYER_ACCEPTANCE_REQUESTED or FINALIZED. Accepting a proposal means the buyer understands and accepts the Proposal.terms_and_conditions proposed by the seller.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `authorizedbuyersmarketplace_buyers_proposals_accept_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `authorizedbuyersmarketplace_buyers_proposals_accept_task()`.
/// For the simplest API, use `authorizedbuyersmarketplace_buyers_proposals_accept()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `authorizedbuyersmarketplace_buyers_proposals_accept_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn authorizedbuyersmarketplace_buyers_proposals_accept_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Proposal>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = authorizedbuyersmarketplace_buyers_proposals_accept_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`authorizedbuyersmarketplace_buyers_proposals_accept`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AuthorizedbuyersmarketplaceBuyersProposalsAcceptArgs {
    /// Path parameter: name
    pub name: String,
}

/// POST v1/buyers/{buyersId}/proposals/{proposalsId}:accept
/// Accepts the proposal at the given revision number. If the revision number in the request is behind the latest from the server, an error message will be returned. This call updates the Proposal.state from BUYER_ACCEPTANCE_REQUESTED to FINALIZED; it has no side effect if the Proposal.state is already FINALIZED and throws exception if the Proposal.state is not either BUYER_ACCEPTANCE_REQUESTED or FINALIZED. Accepting a proposal means the buyer understands and accepts the Proposal.terms_and_conditions proposed by the seller.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `authorizedbuyersmarketplace_buyers_proposals_accept_builder()` + `authorizedbuyersmarketplace_buyers_proposals_accept_execute()`.
/// For task-level control, use `authorizedbuyersmarketplace_buyers_proposals_accept_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn authorizedbuyersmarketplace_buyers_proposals_accept(
    client: &SimpleHttpClient,
    args: &AuthorizedbuyersmarketplaceBuyersProposalsAcceptArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Proposal>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = authorizedbuyersmarketplace_buyers_proposals_accept_builder(client, &args.name)?;
    authorizedbuyersmarketplace_buyers_proposals_accept_execute(builder)
}

/// POST v1/buyers/{buyersId}/proposals/{proposalsId}:addNote
/// Creates a note for this proposal and sends to the seller. This method is not supported for proposals with DealType set to 'PRIVATE_AUCTION'.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `authorizedbuyersmarketplace_buyers_proposals_add_note_execute()` to send, or `authorizedbuyersmarketplace_buyers_proposals_add_note` for simplest API.

pub fn authorizedbuyersmarketplace_buyers_proposals_add_note_builder<R>(
    client: &SimpleHttpClient<R>,
    proposal: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://authorizedbuyersmarketplace.googleapis.com/v1/buyers/{}/proposals/{proposalsId}:addNote",
        proposal,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1/buyers/{buyersId}/proposals/{proposalsId}:addNote
/// Creates a note for this proposal and sends to the seller. This method is not supported for proposals with DealType set to 'PRIVATE_AUCTION'.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `authorizedbuyersmarketplace_buyers_proposals_add_note_execute()` or `authorizedbuyersmarketplace_buyers_proposals_add_note`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `authorizedbuyersmarketplace_buyers_proposals_add_note_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn authorizedbuyersmarketplace_buyers_proposals_add_note_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Proposal>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Proposal = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1/buyers/{buyersId}/proposals/{proposalsId}:addNote
/// Creates a note for this proposal and sends to the seller. This method is not supported for proposals with DealType set to 'PRIVATE_AUCTION'.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `authorizedbuyersmarketplace_buyers_proposals_add_note_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `authorizedbuyersmarketplace_buyers_proposals_add_note_task()`.
/// For the simplest API, use `authorizedbuyersmarketplace_buyers_proposals_add_note()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `authorizedbuyersmarketplace_buyers_proposals_add_note_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn authorizedbuyersmarketplace_buyers_proposals_add_note_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Proposal>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = authorizedbuyersmarketplace_buyers_proposals_add_note_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`authorizedbuyersmarketplace_buyers_proposals_add_note`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AuthorizedbuyersmarketplaceBuyersProposalsAddNoteArgs {
    /// Path parameter: proposal
    pub proposal: String,
}

/// POST v1/buyers/{buyersId}/proposals/{proposalsId}:addNote
/// Creates a note for this proposal and sends to the seller. This method is not supported for proposals with DealType set to 'PRIVATE_AUCTION'.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `authorizedbuyersmarketplace_buyers_proposals_add_note_builder()` + `authorizedbuyersmarketplace_buyers_proposals_add_note_execute()`.
/// For task-level control, use `authorizedbuyersmarketplace_buyers_proposals_add_note_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn authorizedbuyersmarketplace_buyers_proposals_add_note(
    client: &SimpleHttpClient,
    args: &AuthorizedbuyersmarketplaceBuyersProposalsAddNoteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Proposal>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        authorizedbuyersmarketplace_buyers_proposals_add_note_builder(client, &args.proposal)?;
    authorizedbuyersmarketplace_buyers_proposals_add_note_execute(builder)
}

/// POST v1/buyers/{buyersId}/proposals/{proposalsId}:cancelNegotiation
/// Cancels an ongoing negotiation on a proposal. This does not cancel or end serving for the deals if the proposal has been finalized. If the proposal has not been finalized before, calling this method will set the Proposal.state to TERMINATED and increment the Proposal.proposal_revision. If the proposal has been finalized before and is under renegotiation now, calling this method will reset the Proposal.state to FINALIZED and increment the Proposal.proposal_revision. This method does not support private auction proposals whose Proposal.deal_type is 'PRIVATE_AUCTION'.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `authorizedbuyersmarketplace_buyers_proposals_cancel_negotiation_execute()` to send, or `authorizedbuyersmarketplace_buyers_proposals_cancel_negotiation` for simplest API.

pub fn authorizedbuyersmarketplace_buyers_proposals_cancel_negotiation_builder<R>(
    client: &SimpleHttpClient<R>,
    proposal: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://authorizedbuyersmarketplace.googleapis.com/v1/buyers/{}/proposals/{proposalsId}:cancelNegotiation",
        proposal,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1/buyers/{buyersId}/proposals/{proposalsId}:cancelNegotiation
/// Cancels an ongoing negotiation on a proposal. This does not cancel or end serving for the deals if the proposal has been finalized. If the proposal has not been finalized before, calling this method will set the Proposal.state to TERMINATED and increment the Proposal.proposal_revision. If the proposal has been finalized before and is under renegotiation now, calling this method will reset the Proposal.state to FINALIZED and increment the Proposal.proposal_revision. This method does not support private auction proposals whose Proposal.deal_type is 'PRIVATE_AUCTION'.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `authorizedbuyersmarketplace_buyers_proposals_cancel_negotiation_execute()` or `authorizedbuyersmarketplace_buyers_proposals_cancel_negotiation`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `authorizedbuyersmarketplace_buyers_proposals_cancel_negotiation_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn authorizedbuyersmarketplace_buyers_proposals_cancel_negotiation_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Proposal>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Proposal = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1/buyers/{buyersId}/proposals/{proposalsId}:cancelNegotiation
/// Cancels an ongoing negotiation on a proposal. This does not cancel or end serving for the deals if the proposal has been finalized. If the proposal has not been finalized before, calling this method will set the Proposal.state to TERMINATED and increment the Proposal.proposal_revision. If the proposal has been finalized before and is under renegotiation now, calling this method will reset the Proposal.state to FINALIZED and increment the Proposal.proposal_revision. This method does not support private auction proposals whose Proposal.deal_type is 'PRIVATE_AUCTION'.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `authorizedbuyersmarketplace_buyers_proposals_cancel_negotiation_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `authorizedbuyersmarketplace_buyers_proposals_cancel_negotiation_task()`.
/// For the simplest API, use `authorizedbuyersmarketplace_buyers_proposals_cancel_negotiation()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `authorizedbuyersmarketplace_buyers_proposals_cancel_negotiation_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn authorizedbuyersmarketplace_buyers_proposals_cancel_negotiation_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Proposal>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = authorizedbuyersmarketplace_buyers_proposals_cancel_negotiation_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`authorizedbuyersmarketplace_buyers_proposals_cancel_negotiation`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AuthorizedbuyersmarketplaceBuyersProposalsCancelNegotiationArgs {
    /// Path parameter: proposal
    pub proposal: String,
}

/// POST v1/buyers/{buyersId}/proposals/{proposalsId}:cancelNegotiation
/// Cancels an ongoing negotiation on a proposal. This does not cancel or end serving for the deals if the proposal has been finalized. If the proposal has not been finalized before, calling this method will set the Proposal.state to TERMINATED and increment the Proposal.proposal_revision. If the proposal has been finalized before and is under renegotiation now, calling this method will reset the Proposal.state to FINALIZED and increment the Proposal.proposal_revision. This method does not support private auction proposals whose Proposal.deal_type is 'PRIVATE_AUCTION'.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `authorizedbuyersmarketplace_buyers_proposals_cancel_negotiation_builder()` + `authorizedbuyersmarketplace_buyers_proposals_cancel_negotiation_execute()`.
/// For task-level control, use `authorizedbuyersmarketplace_buyers_proposals_cancel_negotiation_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn authorizedbuyersmarketplace_buyers_proposals_cancel_negotiation(
    client: &SimpleHttpClient,
    args: &AuthorizedbuyersmarketplaceBuyersProposalsCancelNegotiationArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Proposal>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = authorizedbuyersmarketplace_buyers_proposals_cancel_negotiation_builder(
        client,
        &args.proposal,
    )?;
    authorizedbuyersmarketplace_buyers_proposals_cancel_negotiation_execute(builder)
}

/// GET v1/buyers/{buyersId}/proposals/{proposalsId}
/// Gets a proposal using its resource name. The proposal is returned at the latest revision.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `authorizedbuyersmarketplace_buyers_proposals_get_execute()` to send, or `authorizedbuyersmarketplace_buyers_proposals_get` for simplest API.

pub fn authorizedbuyersmarketplace_buyers_proposals_get_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://authorizedbuyersmarketplace.googleapis.com/v1/buyers/{}/proposals/{proposalsId}",
        name,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/buyers/{buyersId}/proposals/{proposalsId}
/// Gets a proposal using its resource name. The proposal is returned at the latest revision.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `authorizedbuyersmarketplace_buyers_proposals_get_execute()` or `authorizedbuyersmarketplace_buyers_proposals_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `authorizedbuyersmarketplace_buyers_proposals_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn authorizedbuyersmarketplace_buyers_proposals_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Proposal>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Proposal = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/buyers/{buyersId}/proposals/{proposalsId}
/// Gets a proposal using its resource name. The proposal is returned at the latest revision.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `authorizedbuyersmarketplace_buyers_proposals_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `authorizedbuyersmarketplace_buyers_proposals_get_task()`.
/// For the simplest API, use `authorizedbuyersmarketplace_buyers_proposals_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `authorizedbuyersmarketplace_buyers_proposals_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn authorizedbuyersmarketplace_buyers_proposals_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Proposal>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = authorizedbuyersmarketplace_buyers_proposals_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`authorizedbuyersmarketplace_buyers_proposals_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AuthorizedbuyersmarketplaceBuyersProposalsGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/buyers/{buyersId}/proposals/{proposalsId}
/// Gets a proposal using its resource name. The proposal is returned at the latest revision.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `authorizedbuyersmarketplace_buyers_proposals_get_builder()` + `authorizedbuyersmarketplace_buyers_proposals_get_execute()`.
/// For task-level control, use `authorizedbuyersmarketplace_buyers_proposals_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn authorizedbuyersmarketplace_buyers_proposals_get(
    client: &SimpleHttpClient,
    args: &AuthorizedbuyersmarketplaceBuyersProposalsGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Proposal>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = authorizedbuyersmarketplace_buyers_proposals_get_builder(client, &args.name)?;
    authorizedbuyersmarketplace_buyers_proposals_get_execute(builder)
}

/// GET v1/buyers/{buyersId}/proposals
/// Lists proposals. A filter expression using [Cloud API list filtering syntax](<https://developers.google.`com/authorized-buyers/apis/guides/list-filters`>) may be specified to filter the results.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `authorizedbuyersmarketplace_buyers_proposals_list_execute()` to send, or `authorizedbuyersmarketplace_buyers_proposals_list` for simplest API.

pub fn authorizedbuyersmarketplace_buyers_proposals_list_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
    filter: &Option<Option<String>>,
    pageSize: &Option<Option<String>>,
    pageToken: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://authorizedbuyersmarketplace.googleapis.com/v1/buyers/{}/proposals",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter.as_ref() {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = pageSize.as_ref() {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/buyers/{buyersId}/proposals
/// Lists proposals. A filter expression using [Cloud API list filtering syntax](<https://developers.google.`com/authorized-buyers/apis/guides/list-filters`>) may be specified to filter the results.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `authorizedbuyersmarketplace_buyers_proposals_list_execute()` or `authorizedbuyersmarketplace_buyers_proposals_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `authorizedbuyersmarketplace_buyers_proposals_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn authorizedbuyersmarketplace_buyers_proposals_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ListProposalsResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListProposalsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/buyers/{buyersId}/proposals
/// Lists proposals. A filter expression using [Cloud API list filtering syntax](<https://developers.google.`com/authorized-buyers/apis/guides/list-filters`>) may be specified to filter the results.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `authorizedbuyersmarketplace_buyers_proposals_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `authorizedbuyersmarketplace_buyers_proposals_list_task()`.
/// For the simplest API, use `authorizedbuyersmarketplace_buyers_proposals_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `authorizedbuyersmarketplace_buyers_proposals_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn authorizedbuyersmarketplace_buyers_proposals_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListProposalsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = authorizedbuyersmarketplace_buyers_proposals_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`authorizedbuyersmarketplace_buyers_proposals_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AuthorizedbuyersmarketplaceBuyersProposalsListArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: filter
    pub filter: Option<Option<String>>,
    /// Query parameter: pageSize
    pub pageSize: Option<Option<String>>,
    /// Query parameter: pageToken
    pub pageToken: Option<Option<String>>,
}

/// GET v1/buyers/{buyersId}/proposals
/// Lists proposals. A filter expression using [Cloud API list filtering syntax](<https://developers.google.`com/authorized-buyers/apis/guides/list-filters`>) may be specified to filter the results.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `authorizedbuyersmarketplace_buyers_proposals_list_builder()` + `authorizedbuyersmarketplace_buyers_proposals_list_execute()`.
/// For task-level control, use `authorizedbuyersmarketplace_buyers_proposals_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn authorizedbuyersmarketplace_buyers_proposals_list(
    client: &SimpleHttpClient,
    args: &AuthorizedbuyersmarketplaceBuyersProposalsListArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListProposalsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = authorizedbuyersmarketplace_buyers_proposals_list_builder(
        client,
        &args.parent,
        &args.filter,
        &args.pageSize,
        &args.pageToken,
    )?;
    authorizedbuyersmarketplace_buyers_proposals_list_execute(builder)
}

/// PATCH v1/buyers/{buyersId}/proposals/{proposalsId}
/// Updates the proposal at the given revision number. If the revision number in the request is behind the latest one kept in the server, an error message will be returned. See FieldMask for how to use FieldMask. Only fields specified in the UpdateProposalRequest.update_mask will be updated; Fields noted as 'Immutable' or 'Output only' yet specified in the UpdateProposalRequest.update_mask will be ignored and left unchanged. Updating a private auction proposal is only allowed for buyer private data, all other fields are immutable.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `authorizedbuyersmarketplace_buyers_proposals_patch_execute()` to send, or `authorizedbuyersmarketplace_buyers_proposals_patch` for simplest API.

pub fn authorizedbuyersmarketplace_buyers_proposals_patch_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
    updateMask: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://authorizedbuyersmarketplace.googleapis.com/v1/buyers/{}/proposals/{proposalsId}",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = updateMask.as_ref() {
        query_parts.push(format!("updateMask={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .patch(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// PATCH v1/buyers/{buyersId}/proposals/{proposalsId}
/// Updates the proposal at the given revision number. If the revision number in the request is behind the latest one kept in the server, an error message will be returned. See FieldMask for how to use FieldMask. Only fields specified in the UpdateProposalRequest.update_mask will be updated; Fields noted as 'Immutable' or 'Output only' yet specified in the UpdateProposalRequest.update_mask will be ignored and left unchanged. Updating a private auction proposal is only allowed for buyer private data, all other fields are immutable.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `authorizedbuyersmarketplace_buyers_proposals_patch_execute()` or `authorizedbuyersmarketplace_buyers_proposals_patch`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `authorizedbuyersmarketplace_buyers_proposals_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn authorizedbuyersmarketplace_buyers_proposals_patch_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Proposal>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Proposal = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// PATCH v1/buyers/{buyersId}/proposals/{proposalsId}
/// Updates the proposal at the given revision number. If the revision number in the request is behind the latest one kept in the server, an error message will be returned. See FieldMask for how to use FieldMask. Only fields specified in the UpdateProposalRequest.update_mask will be updated; Fields noted as 'Immutable' or 'Output only' yet specified in the UpdateProposalRequest.update_mask will be ignored and left unchanged. Updating a private auction proposal is only allowed for buyer private data, all other fields are immutable.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `authorizedbuyersmarketplace_buyers_proposals_patch_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `authorizedbuyersmarketplace_buyers_proposals_patch_task()`.
/// For the simplest API, use `authorizedbuyersmarketplace_buyers_proposals_patch()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `authorizedbuyersmarketplace_buyers_proposals_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn authorizedbuyersmarketplace_buyers_proposals_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Proposal>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = authorizedbuyersmarketplace_buyers_proposals_patch_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`authorizedbuyersmarketplace_buyers_proposals_patch`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AuthorizedbuyersmarketplaceBuyersProposalsPatchArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: updateMask
    pub updateMask: Option<Option<String>>,
}

/// PATCH v1/buyers/{buyersId}/proposals/{proposalsId}
/// Updates the proposal at the given revision number. If the revision number in the request is behind the latest one kept in the server, an error message will be returned. See FieldMask for how to use FieldMask. Only fields specified in the UpdateProposalRequest.update_mask will be updated; Fields noted as 'Immutable' or 'Output only' yet specified in the UpdateProposalRequest.update_mask will be ignored and left unchanged. Updating a private auction proposal is only allowed for buyer private data, all other fields are immutable.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `authorizedbuyersmarketplace_buyers_proposals_patch_builder()` + `authorizedbuyersmarketplace_buyers_proposals_patch_execute()`.
/// For task-level control, use `authorizedbuyersmarketplace_buyers_proposals_patch_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn authorizedbuyersmarketplace_buyers_proposals_patch(
    client: &SimpleHttpClient,
    args: &AuthorizedbuyersmarketplaceBuyersProposalsPatchArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Proposal>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = authorizedbuyersmarketplace_buyers_proposals_patch_builder(
        client,
        &args.name,
        &args.updateMask,
    )?;
    authorizedbuyersmarketplace_buyers_proposals_patch_execute(builder)
}

/// POST v1/buyers/{buyersId}/proposals:sendRfp
/// Sends a request for proposal (RFP) to a publisher to initiate the negotiation regarding certain inventory. In the RFP, buyers can specify the deal type, deal terms, start and end dates, targeting, and a message to the publisher. Once the RFP is sent, a proposal in SELLER_REVIEW_REQUESTED state will be created and returned in the response. The publisher may review your request and respond with detailed deals in the proposal.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `authorizedbuyersmarketplace_buyers_proposals_send_rfp_execute()` to send, or `authorizedbuyersmarketplace_buyers_proposals_send_rfp` for simplest API.

pub fn authorizedbuyersmarketplace_buyers_proposals_send_rfp_builder<R>(
    client: &SimpleHttpClient<R>,
    buyer: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://authorizedbuyersmarketplace.googleapis.com/v1/buyers/{}/proposals:sendRfp",
        buyer,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1/buyers/{buyersId}/proposals:sendRfp
/// Sends a request for proposal (RFP) to a publisher to initiate the negotiation regarding certain inventory. In the RFP, buyers can specify the deal type, deal terms, start and end dates, targeting, and a message to the publisher. Once the RFP is sent, a proposal in SELLER_REVIEW_REQUESTED state will be created and returned in the response. The publisher may review your request and respond with detailed deals in the proposal.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `authorizedbuyersmarketplace_buyers_proposals_send_rfp_execute()` or `authorizedbuyersmarketplace_buyers_proposals_send_rfp`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `authorizedbuyersmarketplace_buyers_proposals_send_rfp_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn authorizedbuyersmarketplace_buyers_proposals_send_rfp_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Proposal>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Proposal = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1/buyers/{buyersId}/proposals:sendRfp
/// Sends a request for proposal (RFP) to a publisher to initiate the negotiation regarding certain inventory. In the RFP, buyers can specify the deal type, deal terms, start and end dates, targeting, and a message to the publisher. Once the RFP is sent, a proposal in SELLER_REVIEW_REQUESTED state will be created and returned in the response. The publisher may review your request and respond with detailed deals in the proposal.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `authorizedbuyersmarketplace_buyers_proposals_send_rfp_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `authorizedbuyersmarketplace_buyers_proposals_send_rfp_task()`.
/// For the simplest API, use `authorizedbuyersmarketplace_buyers_proposals_send_rfp()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `authorizedbuyersmarketplace_buyers_proposals_send_rfp_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn authorizedbuyersmarketplace_buyers_proposals_send_rfp_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Proposal>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = authorizedbuyersmarketplace_buyers_proposals_send_rfp_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`authorizedbuyersmarketplace_buyers_proposals_send_rfp`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AuthorizedbuyersmarketplaceBuyersProposalsSendRfpArgs {
    /// Path parameter: buyer
    pub buyer: String,
}

/// POST v1/buyers/{buyersId}/proposals:sendRfp
/// Sends a request for proposal (RFP) to a publisher to initiate the negotiation regarding certain inventory. In the RFP, buyers can specify the deal type, deal terms, start and end dates, targeting, and a message to the publisher. Once the RFP is sent, a proposal in SELLER_REVIEW_REQUESTED state will be created and returned in the response. The publisher may review your request and respond with detailed deals in the proposal.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `authorizedbuyersmarketplace_buyers_proposals_send_rfp_builder()` + `authorizedbuyersmarketplace_buyers_proposals_send_rfp_execute()`.
/// For task-level control, use `authorizedbuyersmarketplace_buyers_proposals_send_rfp_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn authorizedbuyersmarketplace_buyers_proposals_send_rfp(
    client: &SimpleHttpClient,
    args: &AuthorizedbuyersmarketplaceBuyersProposalsSendRfpArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Proposal>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        authorizedbuyersmarketplace_buyers_proposals_send_rfp_builder(client, &args.buyer)?;
    authorizedbuyersmarketplace_buyers_proposals_send_rfp_execute(builder)
}

/// POST v1/buyers/{buyersId}/proposals/{proposalsId}/deals:batchUpdate
/// Batch updates multiple deals in the same proposal.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `authorizedbuyersmarketplace_buyers_proposals_deals_batch_update_execute()` to send, or `authorizedbuyersmarketplace_buyers_proposals_deals_batch_update` for simplest API.

pub fn authorizedbuyersmarketplace_buyers_proposals_deals_batch_update_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://authorizedbuyersmarketplace.googleapis.com/v1/buyers/{}/proposals/{proposalsId}/deals:batchUpdate",
        parent,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1/buyers/{buyersId}/proposals/{proposalsId}/deals:batchUpdate
/// Batch updates multiple deals in the same proposal.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `authorizedbuyersmarketplace_buyers_proposals_deals_batch_update_execute()` or `authorizedbuyersmarketplace_buyers_proposals_deals_batch_update`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `authorizedbuyersmarketplace_buyers_proposals_deals_batch_update_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn authorizedbuyersmarketplace_buyers_proposals_deals_batch_update_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<BatchUpdateDealsResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: BatchUpdateDealsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1/buyers/{buyersId}/proposals/{proposalsId}/deals:batchUpdate
/// Batch updates multiple deals in the same proposal.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `authorizedbuyersmarketplace_buyers_proposals_deals_batch_update_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `authorizedbuyersmarketplace_buyers_proposals_deals_batch_update_task()`.
/// For the simplest API, use `authorizedbuyersmarketplace_buyers_proposals_deals_batch_update()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `authorizedbuyersmarketplace_buyers_proposals_deals_batch_update_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn authorizedbuyersmarketplace_buyers_proposals_deals_batch_update_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<BatchUpdateDealsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = authorizedbuyersmarketplace_buyers_proposals_deals_batch_update_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`authorizedbuyersmarketplace_buyers_proposals_deals_batch_update`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AuthorizedbuyersmarketplaceBuyersProposalsDealsBatchUpdateArgs {
    /// Path parameter: parent
    pub parent: String,
}

/// POST v1/buyers/{buyersId}/proposals/{proposalsId}/deals:batchUpdate
/// Batch updates multiple deals in the same proposal.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `authorizedbuyersmarketplace_buyers_proposals_deals_batch_update_builder()` + `authorizedbuyersmarketplace_buyers_proposals_deals_batch_update_execute()`.
/// For task-level control, use `authorizedbuyersmarketplace_buyers_proposals_deals_batch_update_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn authorizedbuyersmarketplace_buyers_proposals_deals_batch_update(
    client: &SimpleHttpClient,
    args: &AuthorizedbuyersmarketplaceBuyersProposalsDealsBatchUpdateArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<BatchUpdateDealsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = authorizedbuyersmarketplace_buyers_proposals_deals_batch_update_builder(
        client,
        &args.parent,
    )?;
    authorizedbuyersmarketplace_buyers_proposals_deals_batch_update_execute(builder)
}

/// GET v1/buyers/{buyersId}/proposals/{proposalsId}/deals/{dealsId}
/// Gets a deal given its name. The deal is returned at its head revision.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `authorizedbuyersmarketplace_buyers_proposals_deals_get_execute()` to send, or `authorizedbuyersmarketplace_buyers_proposals_deals_get` for simplest API.

pub fn authorizedbuyersmarketplace_buyers_proposals_deals_get_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://authorizedbuyersmarketplace.googleapis.com/v1/buyers/{}/proposals/{proposalsId}/deals/{dealsId}",
        name,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/buyers/{buyersId}/proposals/{proposalsId}/deals/{dealsId}
/// Gets a deal given its name. The deal is returned at its head revision.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `authorizedbuyersmarketplace_buyers_proposals_deals_get_execute()` or `authorizedbuyersmarketplace_buyers_proposals_deals_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `authorizedbuyersmarketplace_buyers_proposals_deals_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn authorizedbuyersmarketplace_buyers_proposals_deals_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Deal>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Deal = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/buyers/{buyersId}/proposals/{proposalsId}/deals/{dealsId}
/// Gets a deal given its name. The deal is returned at its head revision.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `authorizedbuyersmarketplace_buyers_proposals_deals_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `authorizedbuyersmarketplace_buyers_proposals_deals_get_task()`.
/// For the simplest API, use `authorizedbuyersmarketplace_buyers_proposals_deals_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `authorizedbuyersmarketplace_buyers_proposals_deals_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn authorizedbuyersmarketplace_buyers_proposals_deals_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Deal>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = authorizedbuyersmarketplace_buyers_proposals_deals_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`authorizedbuyersmarketplace_buyers_proposals_deals_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AuthorizedbuyersmarketplaceBuyersProposalsDealsGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/buyers/{buyersId}/proposals/{proposalsId}/deals/{dealsId}
/// Gets a deal given its name. The deal is returned at its head revision.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `authorizedbuyersmarketplace_buyers_proposals_deals_get_builder()` + `authorizedbuyersmarketplace_buyers_proposals_deals_get_execute()`.
/// For task-level control, use `authorizedbuyersmarketplace_buyers_proposals_deals_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn authorizedbuyersmarketplace_buyers_proposals_deals_get(
    client: &SimpleHttpClient,
    args: &AuthorizedbuyersmarketplaceBuyersProposalsDealsGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Deal>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        authorizedbuyersmarketplace_buyers_proposals_deals_get_builder(client, &args.name)?;
    authorizedbuyersmarketplace_buyers_proposals_deals_get_execute(builder)
}

/// GET v1/buyers/{buyersId}/proposals/{proposalsId}/deals
/// Lists all deals in a proposal. To retrieve only the finalized revision deals regardless if a deal is being renegotiated, see the FinalizedDeals resource.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `authorizedbuyersmarketplace_buyers_proposals_deals_list_execute()` to send, or `authorizedbuyersmarketplace_buyers_proposals_deals_list` for simplest API.

pub fn authorizedbuyersmarketplace_buyers_proposals_deals_list_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
    pageSize: &Option<Option<String>>,
    pageToken: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://authorizedbuyersmarketplace.googleapis.com/v1/buyers/{}/proposals/{proposalsId}/deals",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = pageSize.as_ref() {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/buyers/{buyersId}/proposals/{proposalsId}/deals
/// Lists all deals in a proposal. To retrieve only the finalized revision deals regardless if a deal is being renegotiated, see the FinalizedDeals resource.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `authorizedbuyersmarketplace_buyers_proposals_deals_list_execute()` or `authorizedbuyersmarketplace_buyers_proposals_deals_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `authorizedbuyersmarketplace_buyers_proposals_deals_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn authorizedbuyersmarketplace_buyers_proposals_deals_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ListDealsResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListDealsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/buyers/{buyersId}/proposals/{proposalsId}/deals
/// Lists all deals in a proposal. To retrieve only the finalized revision deals regardless if a deal is being renegotiated, see the FinalizedDeals resource.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `authorizedbuyersmarketplace_buyers_proposals_deals_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `authorizedbuyersmarketplace_buyers_proposals_deals_list_task()`.
/// For the simplest API, use `authorizedbuyersmarketplace_buyers_proposals_deals_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `authorizedbuyersmarketplace_buyers_proposals_deals_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn authorizedbuyersmarketplace_buyers_proposals_deals_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListDealsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = authorizedbuyersmarketplace_buyers_proposals_deals_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`authorizedbuyersmarketplace_buyers_proposals_deals_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AuthorizedbuyersmarketplaceBuyersProposalsDealsListArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: pageSize
    pub pageSize: Option<Option<String>>,
    /// Query parameter: pageToken
    pub pageToken: Option<Option<String>>,
}

/// GET v1/buyers/{buyersId}/proposals/{proposalsId}/deals
/// Lists all deals in a proposal. To retrieve only the finalized revision deals regardless if a deal is being renegotiated, see the FinalizedDeals resource.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `authorizedbuyersmarketplace_buyers_proposals_deals_list_builder()` + `authorizedbuyersmarketplace_buyers_proposals_deals_list_execute()`.
/// For task-level control, use `authorizedbuyersmarketplace_buyers_proposals_deals_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn authorizedbuyersmarketplace_buyers_proposals_deals_list(
    client: &SimpleHttpClient,
    args: &AuthorizedbuyersmarketplaceBuyersProposalsDealsListArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListDealsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = authorizedbuyersmarketplace_buyers_proposals_deals_list_builder(
        client,
        &args.parent,
        &args.pageSize,
        &args.pageToken,
    )?;
    authorizedbuyersmarketplace_buyers_proposals_deals_list_execute(builder)
}

/// PATCH v1/buyers/{buyersId}/proposals/{proposalsId}/deals/{dealsId}
/// Updates the given deal at the buyer known revision number. If the server revision has advanced since the passed-in proposal.proposal_revision an ABORTED error message will be returned. The revision number is incremented by the server whenever the proposal or its constituent deals are updated. Note: The revision number is kept at a proposal level. The buyer of the API is expected to keep track of the revision number after the last update operation and send it in as part of the next update request. This way, if there are further changes on the server (for example, seller making new updates), then the server can detect conflicts and reject the proposed changes.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `authorizedbuyersmarketplace_buyers_proposals_deals_patch_execute()` to send, or `authorizedbuyersmarketplace_buyers_proposals_deals_patch` for simplest API.

pub fn authorizedbuyersmarketplace_buyers_proposals_deals_patch_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
    updateMask: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://authorizedbuyersmarketplace.googleapis.com/v1/buyers/{}/proposals/{proposalsId}/deals/{dealsId}",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = updateMask.as_ref() {
        query_parts.push(format!("updateMask={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .patch(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// PATCH v1/buyers/{buyersId}/proposals/{proposalsId}/deals/{dealsId}
/// Updates the given deal at the buyer known revision number. If the server revision has advanced since the passed-in proposal.proposal_revision an ABORTED error message will be returned. The revision number is incremented by the server whenever the proposal or its constituent deals are updated. Note: The revision number is kept at a proposal level. The buyer of the API is expected to keep track of the revision number after the last update operation and send it in as part of the next update request. This way, if there are further changes on the server (for example, seller making new updates), then the server can detect conflicts and reject the proposed changes.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `authorizedbuyersmarketplace_buyers_proposals_deals_patch_execute()` or `authorizedbuyersmarketplace_buyers_proposals_deals_patch`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `authorizedbuyersmarketplace_buyers_proposals_deals_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn authorizedbuyersmarketplace_buyers_proposals_deals_patch_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Deal>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Deal = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// PATCH v1/buyers/{buyersId}/proposals/{proposalsId}/deals/{dealsId}
/// Updates the given deal at the buyer known revision number. If the server revision has advanced since the passed-in proposal.proposal_revision an ABORTED error message will be returned. The revision number is incremented by the server whenever the proposal or its constituent deals are updated. Note: The revision number is kept at a proposal level. The buyer of the API is expected to keep track of the revision number after the last update operation and send it in as part of the next update request. This way, if there are further changes on the server (for example, seller making new updates), then the server can detect conflicts and reject the proposed changes.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `authorizedbuyersmarketplace_buyers_proposals_deals_patch_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `authorizedbuyersmarketplace_buyers_proposals_deals_patch_task()`.
/// For the simplest API, use `authorizedbuyersmarketplace_buyers_proposals_deals_patch()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `authorizedbuyersmarketplace_buyers_proposals_deals_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn authorizedbuyersmarketplace_buyers_proposals_deals_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Deal>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = authorizedbuyersmarketplace_buyers_proposals_deals_patch_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`authorizedbuyersmarketplace_buyers_proposals_deals_patch`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AuthorizedbuyersmarketplaceBuyersProposalsDealsPatchArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: updateMask
    pub updateMask: Option<Option<String>>,
}

/// PATCH v1/buyers/{buyersId}/proposals/{proposalsId}/deals/{dealsId}
/// Updates the given deal at the buyer known revision number. If the server revision has advanced since the passed-in proposal.proposal_revision an ABORTED error message will be returned. The revision number is incremented by the server whenever the proposal or its constituent deals are updated. Note: The revision number is kept at a proposal level. The buyer of the API is expected to keep track of the revision number after the last update operation and send it in as part of the next update request. This way, if there are further changes on the server (for example, seller making new updates), then the server can detect conflicts and reject the proposed changes.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `authorizedbuyersmarketplace_buyers_proposals_deals_patch_builder()` + `authorizedbuyersmarketplace_buyers_proposals_deals_patch_execute()`.
/// For task-level control, use `authorizedbuyersmarketplace_buyers_proposals_deals_patch_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn authorizedbuyersmarketplace_buyers_proposals_deals_patch(
    client: &SimpleHttpClient,
    args: &AuthorizedbuyersmarketplaceBuyersProposalsDealsPatchArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Deal>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = authorizedbuyersmarketplace_buyers_proposals_deals_patch_builder(
        client,
        &args.name,
        &args.updateMask,
    )?;
    authorizedbuyersmarketplace_buyers_proposals_deals_patch_execute(builder)
}

/// GET v1/buyers/{buyersId}/publisherProfiles/{publisherProfilesId}
/// Gets the requested publisher profile by name.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `authorizedbuyersmarketplace_buyers_publisher_profiles_get_execute()` to send, or `authorizedbuyersmarketplace_buyers_publisher_profiles_get` for simplest API.

pub fn authorizedbuyersmarketplace_buyers_publisher_profiles_get_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://authorizedbuyersmarketplace.googleapis.com/v1/buyers/{}/publisherProfiles/{publisherProfilesId}",
        name,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/buyers/{buyersId}/publisherProfiles/{publisherProfilesId}
/// Gets the requested publisher profile by name.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `authorizedbuyersmarketplace_buyers_publisher_profiles_get_execute()` or `authorizedbuyersmarketplace_buyers_publisher_profiles_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `authorizedbuyersmarketplace_buyers_publisher_profiles_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn authorizedbuyersmarketplace_buyers_publisher_profiles_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<PublisherProfile>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: PublisherProfile = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/buyers/{buyersId}/publisherProfiles/{publisherProfilesId}
/// Gets the requested publisher profile by name.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `authorizedbuyersmarketplace_buyers_publisher_profiles_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `authorizedbuyersmarketplace_buyers_publisher_profiles_get_task()`.
/// For the simplest API, use `authorizedbuyersmarketplace_buyers_publisher_profiles_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `authorizedbuyersmarketplace_buyers_publisher_profiles_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn authorizedbuyersmarketplace_buyers_publisher_profiles_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<PublisherProfile>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = authorizedbuyersmarketplace_buyers_publisher_profiles_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`authorizedbuyersmarketplace_buyers_publisher_profiles_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AuthorizedbuyersmarketplaceBuyersPublisherProfilesGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/buyers/{buyersId}/publisherProfiles/{publisherProfilesId}
/// Gets the requested publisher profile by name.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `authorizedbuyersmarketplace_buyers_publisher_profiles_get_builder()` + `authorizedbuyersmarketplace_buyers_publisher_profiles_get_execute()`.
/// For task-level control, use `authorizedbuyersmarketplace_buyers_publisher_profiles_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn authorizedbuyersmarketplace_buyers_publisher_profiles_get(
    client: &SimpleHttpClient,
    args: &AuthorizedbuyersmarketplaceBuyersPublisherProfilesGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<PublisherProfile>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        authorizedbuyersmarketplace_buyers_publisher_profiles_get_builder(client, &args.name)?;
    authorizedbuyersmarketplace_buyers_publisher_profiles_get_execute(builder)
}

/// GET v1/buyers/{buyersId}/publisherProfiles
/// Lists publisher profiles. The returned publisher profiles aren't in any defined order. The order of the results might change. A new publisher profile can appear in any place in the list of returned results.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `authorizedbuyersmarketplace_buyers_publisher_profiles_list_execute()` to send, or `authorizedbuyersmarketplace_buyers_publisher_profiles_list` for simplest API.

pub fn authorizedbuyersmarketplace_buyers_publisher_profiles_list_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
    filter: &Option<Option<String>>,
    pageSize: &Option<Option<String>>,
    pageToken: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://authorizedbuyersmarketplace.googleapis.com/v1/buyers/{}/publisherProfiles",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter.as_ref() {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = pageSize.as_ref() {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/buyers/{buyersId}/publisherProfiles
/// Lists publisher profiles. The returned publisher profiles aren't in any defined order. The order of the results might change. A new publisher profile can appear in any place in the list of returned results.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `authorizedbuyersmarketplace_buyers_publisher_profiles_list_execute()` or `authorizedbuyersmarketplace_buyers_publisher_profiles_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `authorizedbuyersmarketplace_buyers_publisher_profiles_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn authorizedbuyersmarketplace_buyers_publisher_profiles_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ListPublisherProfilesResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListPublisherProfilesResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/buyers/{buyersId}/publisherProfiles
/// Lists publisher profiles. The returned publisher profiles aren't in any defined order. The order of the results might change. A new publisher profile can appear in any place in the list of returned results.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `authorizedbuyersmarketplace_buyers_publisher_profiles_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `authorizedbuyersmarketplace_buyers_publisher_profiles_list_task()`.
/// For the simplest API, use `authorizedbuyersmarketplace_buyers_publisher_profiles_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `authorizedbuyersmarketplace_buyers_publisher_profiles_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn authorizedbuyersmarketplace_buyers_publisher_profiles_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ListPublisherProfilesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = authorizedbuyersmarketplace_buyers_publisher_profiles_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`authorizedbuyersmarketplace_buyers_publisher_profiles_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AuthorizedbuyersmarketplaceBuyersPublisherProfilesListArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: filter
    pub filter: Option<Option<String>>,
    /// Query parameter: pageSize
    pub pageSize: Option<Option<String>>,
    /// Query parameter: pageToken
    pub pageToken: Option<Option<String>>,
}

/// GET v1/buyers/{buyersId}/publisherProfiles
/// Lists publisher profiles. The returned publisher profiles aren't in any defined order. The order of the results might change. A new publisher profile can appear in any place in the list of returned results.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `authorizedbuyersmarketplace_buyers_publisher_profiles_list_builder()` + `authorizedbuyersmarketplace_buyers_publisher_profiles_list_execute()`.
/// For task-level control, use `authorizedbuyersmarketplace_buyers_publisher_profiles_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn authorizedbuyersmarketplace_buyers_publisher_profiles_list(
    client: &SimpleHttpClient,
    args: &AuthorizedbuyersmarketplaceBuyersPublisherProfilesListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ListPublisherProfilesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = authorizedbuyersmarketplace_buyers_publisher_profiles_list_builder(
        client,
        &args.parent,
        &args.filter,
        &args.pageSize,
        &args.pageToken,
    )?;
    authorizedbuyersmarketplace_buyers_publisher_profiles_list_execute(builder)
}

// =============================================================================
// ResourceIdentifier implementation for ListAuctionPackagesResponse
// =============================================================================

/// ResourceIdentifier implementation for ListAuctionPackagesResponse with AuthorizedbuyersmarketplaceBiddersAuctionPackagesListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<AuthorizedbuyersmarketplaceBiddersAuctionPackagesListArgs>
    for ListAuctionPackagesResponse
{
    fn generate_resource_id(
        &self,
        input: &AuthorizedbuyersmarketplaceBiddersAuctionPackagesListArgs,
    ) -> String {
        format!(
            "gcp::authorizedbuyersmarketplace::ListAuctionPackagesResponse/{}",
            input.parent
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::authorizedbuyersmarketplace::ListAuctionPackagesResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for ListFinalizedDealsResponse
// =============================================================================

/// ResourceIdentifier implementation for ListFinalizedDealsResponse with AuthorizedbuyersmarketplaceBiddersFinalizedDealsListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<AuthorizedbuyersmarketplaceBiddersFinalizedDealsListArgs>
    for ListFinalizedDealsResponse
{
    fn generate_resource_id(
        &self,
        input: &AuthorizedbuyersmarketplaceBiddersFinalizedDealsListArgs,
    ) -> String {
        format!(
            "gcp::authorizedbuyersmarketplace::ListFinalizedDealsResponse/{}",
            input.parent
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::authorizedbuyersmarketplace::ListFinalizedDealsResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for FinalizedDeal
// =============================================================================

/// ResourceIdentifier implementation for FinalizedDeal with AuthorizedbuyersmarketplaceBiddersFinalizedDealsSetReadyToServeArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<AuthorizedbuyersmarketplaceBiddersFinalizedDealsSetReadyToServeArgs>
    for FinalizedDeal
{
    fn generate_resource_id(
        &self,
        input: &AuthorizedbuyersmarketplaceBiddersFinalizedDealsSetReadyToServeArgs,
    ) -> String {
        format!(
            "gcp::authorizedbuyersmarketplace::FinalizedDeal/{}",
            input.deal
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::authorizedbuyersmarketplace::FinalizedDeal"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for AuctionPackage
// =============================================================================

/// ResourceIdentifier implementation for AuctionPackage with AuthorizedbuyersmarketplaceBuyersAuctionPackagesGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<AuthorizedbuyersmarketplaceBuyersAuctionPackagesGetArgs>
    for AuctionPackage
{
    fn generate_resource_id(
        &self,
        input: &AuthorizedbuyersmarketplaceBuyersAuctionPackagesGetArgs,
    ) -> String {
        format!(
            "gcp::authorizedbuyersmarketplace::AuctionPackage/{}",
            input.name
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::authorizedbuyersmarketplace::AuctionPackage"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for ListAuctionPackagesResponse
// =============================================================================

/// ResourceIdentifier implementation for ListAuctionPackagesResponse with AuthorizedbuyersmarketplaceBuyersAuctionPackagesListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<AuthorizedbuyersmarketplaceBuyersAuctionPackagesListArgs>
    for ListAuctionPackagesResponse
{
    fn generate_resource_id(
        &self,
        input: &AuthorizedbuyersmarketplaceBuyersAuctionPackagesListArgs,
    ) -> String {
        format!(
            "gcp::authorizedbuyersmarketplace::ListAuctionPackagesResponse/{}",
            input.parent
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::authorizedbuyersmarketplace::ListAuctionPackagesResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for AuctionPackage
// =============================================================================

/// ResourceIdentifier implementation for AuctionPackage with AuthorizedbuyersmarketplaceBuyersAuctionPackagesSubscribeArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<AuthorizedbuyersmarketplaceBuyersAuctionPackagesSubscribeArgs>
    for AuctionPackage
{
    fn generate_resource_id(
        &self,
        input: &AuthorizedbuyersmarketplaceBuyersAuctionPackagesSubscribeArgs,
    ) -> String {
        format!(
            "gcp::authorizedbuyersmarketplace::AuctionPackage/{}",
            input.name
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::authorizedbuyersmarketplace::AuctionPackage"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for AuctionPackage
// =============================================================================

/// ResourceIdentifier implementation for AuctionPackage with AuthorizedbuyersmarketplaceBuyersAuctionPackagesSubscribeClientsArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<AuthorizedbuyersmarketplaceBuyersAuctionPackagesSubscribeClientsArgs>
    for AuctionPackage
{
    fn generate_resource_id(
        &self,
        input: &AuthorizedbuyersmarketplaceBuyersAuctionPackagesSubscribeClientsArgs,
    ) -> String {
        format!(
            "gcp::authorizedbuyersmarketplace::AuctionPackage/{}",
            input.auctionPackage
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::authorizedbuyersmarketplace::AuctionPackage"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for AuctionPackage
// =============================================================================

/// ResourceIdentifier implementation for AuctionPackage with AuthorizedbuyersmarketplaceBuyersAuctionPackagesUnsubscribeArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<AuthorizedbuyersmarketplaceBuyersAuctionPackagesUnsubscribeArgs>
    for AuctionPackage
{
    fn generate_resource_id(
        &self,
        input: &AuthorizedbuyersmarketplaceBuyersAuctionPackagesUnsubscribeArgs,
    ) -> String {
        format!(
            "gcp::authorizedbuyersmarketplace::AuctionPackage/{}",
            input.name
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::authorizedbuyersmarketplace::AuctionPackage"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for AuctionPackage
// =============================================================================

/// ResourceIdentifier implementation for AuctionPackage with AuthorizedbuyersmarketplaceBuyersAuctionPackagesUnsubscribeClientsArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<AuthorizedbuyersmarketplaceBuyersAuctionPackagesUnsubscribeClientsArgs>
    for AuctionPackage
{
    fn generate_resource_id(
        &self,
        input: &AuthorizedbuyersmarketplaceBuyersAuctionPackagesUnsubscribeClientsArgs,
    ) -> String {
        format!(
            "gcp::authorizedbuyersmarketplace::AuctionPackage/{}",
            input.auctionPackage
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::authorizedbuyersmarketplace::AuctionPackage"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Client
// =============================================================================

/// ResourceIdentifier implementation for Client with AuthorizedbuyersmarketplaceBuyersClientsActivateArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<AuthorizedbuyersmarketplaceBuyersClientsActivateArgs> for Client {
    fn generate_resource_id(
        &self,
        input: &AuthorizedbuyersmarketplaceBuyersClientsActivateArgs,
    ) -> String {
        format!("gcp::authorizedbuyersmarketplace::Client/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::authorizedbuyersmarketplace::Client"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Client
// =============================================================================

/// ResourceIdentifier implementation for Client with AuthorizedbuyersmarketplaceBuyersClientsCreateArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<AuthorizedbuyersmarketplaceBuyersClientsCreateArgs> for Client {
    fn generate_resource_id(
        &self,
        input: &AuthorizedbuyersmarketplaceBuyersClientsCreateArgs,
    ) -> String {
        format!("gcp::authorizedbuyersmarketplace::Client/{}", input.parent)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::authorizedbuyersmarketplace::Client"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Client
// =============================================================================

/// ResourceIdentifier implementation for Client with AuthorizedbuyersmarketplaceBuyersClientsDeactivateArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<AuthorizedbuyersmarketplaceBuyersClientsDeactivateArgs> for Client {
    fn generate_resource_id(
        &self,
        input: &AuthorizedbuyersmarketplaceBuyersClientsDeactivateArgs,
    ) -> String {
        format!("gcp::authorizedbuyersmarketplace::Client/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::authorizedbuyersmarketplace::Client"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Client
// =============================================================================

/// ResourceIdentifier implementation for Client with AuthorizedbuyersmarketplaceBuyersClientsGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<AuthorizedbuyersmarketplaceBuyersClientsGetArgs> for Client {
    fn generate_resource_id(
        &self,
        input: &AuthorizedbuyersmarketplaceBuyersClientsGetArgs,
    ) -> String {
        format!("gcp::authorizedbuyersmarketplace::Client/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::authorizedbuyersmarketplace::Client"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for ListClientsResponse
// =============================================================================

/// ResourceIdentifier implementation for ListClientsResponse with AuthorizedbuyersmarketplaceBuyersClientsListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<AuthorizedbuyersmarketplaceBuyersClientsListArgs> for ListClientsResponse {
    fn generate_resource_id(
        &self,
        input: &AuthorizedbuyersmarketplaceBuyersClientsListArgs,
    ) -> String {
        format!(
            "gcp::authorizedbuyersmarketplace::ListClientsResponse/{}",
            input.parent
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::authorizedbuyersmarketplace::ListClientsResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Client
// =============================================================================

/// ResourceIdentifier implementation for Client with AuthorizedbuyersmarketplaceBuyersClientsPatchArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<AuthorizedbuyersmarketplaceBuyersClientsPatchArgs> for Client {
    fn generate_resource_id(
        &self,
        input: &AuthorizedbuyersmarketplaceBuyersClientsPatchArgs,
    ) -> String {
        format!("gcp::authorizedbuyersmarketplace::Client/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::authorizedbuyersmarketplace::Client"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for ClientUser
// =============================================================================

/// ResourceIdentifier implementation for ClientUser with AuthorizedbuyersmarketplaceBuyersClientsUsersActivateArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<AuthorizedbuyersmarketplaceBuyersClientsUsersActivateArgs> for ClientUser {
    fn generate_resource_id(
        &self,
        input: &AuthorizedbuyersmarketplaceBuyersClientsUsersActivateArgs,
    ) -> String {
        format!(
            "gcp::authorizedbuyersmarketplace::ClientUser/{}",
            input.name
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::authorizedbuyersmarketplace::ClientUser"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for ClientUser
// =============================================================================

/// ResourceIdentifier implementation for ClientUser with AuthorizedbuyersmarketplaceBuyersClientsUsersCreateArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<AuthorizedbuyersmarketplaceBuyersClientsUsersCreateArgs> for ClientUser {
    fn generate_resource_id(
        &self,
        input: &AuthorizedbuyersmarketplaceBuyersClientsUsersCreateArgs,
    ) -> String {
        format!(
            "gcp::authorizedbuyersmarketplace::ClientUser/{}",
            input.parent
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::authorizedbuyersmarketplace::ClientUser"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for ClientUser
// =============================================================================

/// ResourceIdentifier implementation for ClientUser with AuthorizedbuyersmarketplaceBuyersClientsUsersDeactivateArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<AuthorizedbuyersmarketplaceBuyersClientsUsersDeactivateArgs>
    for ClientUser
{
    fn generate_resource_id(
        &self,
        input: &AuthorizedbuyersmarketplaceBuyersClientsUsersDeactivateArgs,
    ) -> String {
        format!(
            "gcp::authorizedbuyersmarketplace::ClientUser/{}",
            input.name
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::authorizedbuyersmarketplace::ClientUser"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Empty
// =============================================================================

/// ResourceIdentifier implementation for Empty with AuthorizedbuyersmarketplaceBuyersClientsUsersDeleteArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<AuthorizedbuyersmarketplaceBuyersClientsUsersDeleteArgs> for Empty {
    fn generate_resource_id(
        &self,
        input: &AuthorizedbuyersmarketplaceBuyersClientsUsersDeleteArgs,
    ) -> String {
        format!("gcp::authorizedbuyersmarketplace::Empty/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::authorizedbuyersmarketplace::Empty"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for ClientUser
// =============================================================================

/// ResourceIdentifier implementation for ClientUser with AuthorizedbuyersmarketplaceBuyersClientsUsersGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<AuthorizedbuyersmarketplaceBuyersClientsUsersGetArgs> for ClientUser {
    fn generate_resource_id(
        &self,
        input: &AuthorizedbuyersmarketplaceBuyersClientsUsersGetArgs,
    ) -> String {
        format!(
            "gcp::authorizedbuyersmarketplace::ClientUser/{}",
            input.name
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::authorizedbuyersmarketplace::ClientUser"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for ListClientUsersResponse
// =============================================================================

/// ResourceIdentifier implementation for ListClientUsersResponse with AuthorizedbuyersmarketplaceBuyersClientsUsersListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<AuthorizedbuyersmarketplaceBuyersClientsUsersListArgs>
    for ListClientUsersResponse
{
    fn generate_resource_id(
        &self,
        input: &AuthorizedbuyersmarketplaceBuyersClientsUsersListArgs,
    ) -> String {
        format!(
            "gcp::authorizedbuyersmarketplace::ListClientUsersResponse/{}",
            input.parent
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::authorizedbuyersmarketplace::ListClientUsersResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for FinalizedDeal
// =============================================================================

/// ResourceIdentifier implementation for FinalizedDeal with AuthorizedbuyersmarketplaceBuyersFinalizedDealsAddCreativeArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<AuthorizedbuyersmarketplaceBuyersFinalizedDealsAddCreativeArgs>
    for FinalizedDeal
{
    fn generate_resource_id(
        &self,
        input: &AuthorizedbuyersmarketplaceBuyersFinalizedDealsAddCreativeArgs,
    ) -> String {
        format!(
            "gcp::authorizedbuyersmarketplace::FinalizedDeal/{}",
            input.deal
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::authorizedbuyersmarketplace::FinalizedDeal"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for FinalizedDeal
// =============================================================================

/// ResourceIdentifier implementation for FinalizedDeal with AuthorizedbuyersmarketplaceBuyersFinalizedDealsGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<AuthorizedbuyersmarketplaceBuyersFinalizedDealsGetArgs> for FinalizedDeal {
    fn generate_resource_id(
        &self,
        input: &AuthorizedbuyersmarketplaceBuyersFinalizedDealsGetArgs,
    ) -> String {
        format!(
            "gcp::authorizedbuyersmarketplace::FinalizedDeal/{}",
            input.name
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::authorizedbuyersmarketplace::FinalizedDeal"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for ListFinalizedDealsResponse
// =============================================================================

/// ResourceIdentifier implementation for ListFinalizedDealsResponse with AuthorizedbuyersmarketplaceBuyersFinalizedDealsListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<AuthorizedbuyersmarketplaceBuyersFinalizedDealsListArgs>
    for ListFinalizedDealsResponse
{
    fn generate_resource_id(
        &self,
        input: &AuthorizedbuyersmarketplaceBuyersFinalizedDealsListArgs,
    ) -> String {
        format!(
            "gcp::authorizedbuyersmarketplace::ListFinalizedDealsResponse/{}",
            input.parent
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::authorizedbuyersmarketplace::ListFinalizedDealsResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for FinalizedDeal
// =============================================================================

/// ResourceIdentifier implementation for FinalizedDeal with AuthorizedbuyersmarketplaceBuyersFinalizedDealsPauseArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<AuthorizedbuyersmarketplaceBuyersFinalizedDealsPauseArgs>
    for FinalizedDeal
{
    fn generate_resource_id(
        &self,
        input: &AuthorizedbuyersmarketplaceBuyersFinalizedDealsPauseArgs,
    ) -> String {
        format!(
            "gcp::authorizedbuyersmarketplace::FinalizedDeal/{}",
            input.name
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::authorizedbuyersmarketplace::FinalizedDeal"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for FinalizedDeal
// =============================================================================

/// ResourceIdentifier implementation for FinalizedDeal with AuthorizedbuyersmarketplaceBuyersFinalizedDealsResumeArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<AuthorizedbuyersmarketplaceBuyersFinalizedDealsResumeArgs>
    for FinalizedDeal
{
    fn generate_resource_id(
        &self,
        input: &AuthorizedbuyersmarketplaceBuyersFinalizedDealsResumeArgs,
    ) -> String {
        format!(
            "gcp::authorizedbuyersmarketplace::FinalizedDeal/{}",
            input.name
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::authorizedbuyersmarketplace::FinalizedDeal"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for FinalizedDeal
// =============================================================================

/// ResourceIdentifier implementation for FinalizedDeal with AuthorizedbuyersmarketplaceBuyersFinalizedDealsSetReadyToServeArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<AuthorizedbuyersmarketplaceBuyersFinalizedDealsSetReadyToServeArgs>
    for FinalizedDeal
{
    fn generate_resource_id(
        &self,
        input: &AuthorizedbuyersmarketplaceBuyersFinalizedDealsSetReadyToServeArgs,
    ) -> String {
        format!(
            "gcp::authorizedbuyersmarketplace::FinalizedDeal/{}",
            input.deal
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::authorizedbuyersmarketplace::FinalizedDeal"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Proposal
// =============================================================================

/// ResourceIdentifier implementation for Proposal with AuthorizedbuyersmarketplaceBuyersProposalsAcceptArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<AuthorizedbuyersmarketplaceBuyersProposalsAcceptArgs> for Proposal {
    fn generate_resource_id(
        &self,
        input: &AuthorizedbuyersmarketplaceBuyersProposalsAcceptArgs,
    ) -> String {
        format!("gcp::authorizedbuyersmarketplace::Proposal/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::authorizedbuyersmarketplace::Proposal"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Proposal
// =============================================================================

/// ResourceIdentifier implementation for Proposal with AuthorizedbuyersmarketplaceBuyersProposalsAddNoteArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<AuthorizedbuyersmarketplaceBuyersProposalsAddNoteArgs> for Proposal {
    fn generate_resource_id(
        &self,
        input: &AuthorizedbuyersmarketplaceBuyersProposalsAddNoteArgs,
    ) -> String {
        format!(
            "gcp::authorizedbuyersmarketplace::Proposal/{}",
            input.proposal
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::authorizedbuyersmarketplace::Proposal"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Proposal
// =============================================================================

/// ResourceIdentifier implementation for Proposal with AuthorizedbuyersmarketplaceBuyersProposalsCancelNegotiationArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<AuthorizedbuyersmarketplaceBuyersProposalsCancelNegotiationArgs>
    for Proposal
{
    fn generate_resource_id(
        &self,
        input: &AuthorizedbuyersmarketplaceBuyersProposalsCancelNegotiationArgs,
    ) -> String {
        format!(
            "gcp::authorizedbuyersmarketplace::Proposal/{}",
            input.proposal
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::authorizedbuyersmarketplace::Proposal"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Proposal
// =============================================================================

/// ResourceIdentifier implementation for Proposal with AuthorizedbuyersmarketplaceBuyersProposalsGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<AuthorizedbuyersmarketplaceBuyersProposalsGetArgs> for Proposal {
    fn generate_resource_id(
        &self,
        input: &AuthorizedbuyersmarketplaceBuyersProposalsGetArgs,
    ) -> String {
        format!("gcp::authorizedbuyersmarketplace::Proposal/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::authorizedbuyersmarketplace::Proposal"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for ListProposalsResponse
// =============================================================================

/// ResourceIdentifier implementation for ListProposalsResponse with AuthorizedbuyersmarketplaceBuyersProposalsListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<AuthorizedbuyersmarketplaceBuyersProposalsListArgs>
    for ListProposalsResponse
{
    fn generate_resource_id(
        &self,
        input: &AuthorizedbuyersmarketplaceBuyersProposalsListArgs,
    ) -> String {
        format!(
            "gcp::authorizedbuyersmarketplace::ListProposalsResponse/{}",
            input.parent
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::authorizedbuyersmarketplace::ListProposalsResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Proposal
// =============================================================================

/// ResourceIdentifier implementation for Proposal with AuthorizedbuyersmarketplaceBuyersProposalsPatchArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<AuthorizedbuyersmarketplaceBuyersProposalsPatchArgs> for Proposal {
    fn generate_resource_id(
        &self,
        input: &AuthorizedbuyersmarketplaceBuyersProposalsPatchArgs,
    ) -> String {
        format!("gcp::authorizedbuyersmarketplace::Proposal/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::authorizedbuyersmarketplace::Proposal"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Proposal
// =============================================================================

/// ResourceIdentifier implementation for Proposal with AuthorizedbuyersmarketplaceBuyersProposalsSendRfpArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<AuthorizedbuyersmarketplaceBuyersProposalsSendRfpArgs> for Proposal {
    fn generate_resource_id(
        &self,
        input: &AuthorizedbuyersmarketplaceBuyersProposalsSendRfpArgs,
    ) -> String {
        format!("gcp::authorizedbuyersmarketplace::Proposal/{}", input.buyer)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::authorizedbuyersmarketplace::Proposal"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for BatchUpdateDealsResponse
// =============================================================================

/// ResourceIdentifier implementation for BatchUpdateDealsResponse with AuthorizedbuyersmarketplaceBuyersProposalsDealsBatchUpdateArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<AuthorizedbuyersmarketplaceBuyersProposalsDealsBatchUpdateArgs>
    for BatchUpdateDealsResponse
{
    fn generate_resource_id(
        &self,
        input: &AuthorizedbuyersmarketplaceBuyersProposalsDealsBatchUpdateArgs,
    ) -> String {
        format!(
            "gcp::authorizedbuyersmarketplace::BatchUpdateDealsResponse/{}",
            input.parent
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::authorizedbuyersmarketplace::BatchUpdateDealsResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Deal
// =============================================================================

/// ResourceIdentifier implementation for Deal with AuthorizedbuyersmarketplaceBuyersProposalsDealsGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<AuthorizedbuyersmarketplaceBuyersProposalsDealsGetArgs> for Deal {
    fn generate_resource_id(
        &self,
        input: &AuthorizedbuyersmarketplaceBuyersProposalsDealsGetArgs,
    ) -> String {
        format!("gcp::authorizedbuyersmarketplace::Deal/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::authorizedbuyersmarketplace::Deal"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for ListDealsResponse
// =============================================================================

/// ResourceIdentifier implementation for ListDealsResponse with AuthorizedbuyersmarketplaceBuyersProposalsDealsListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<AuthorizedbuyersmarketplaceBuyersProposalsDealsListArgs>
    for ListDealsResponse
{
    fn generate_resource_id(
        &self,
        input: &AuthorizedbuyersmarketplaceBuyersProposalsDealsListArgs,
    ) -> String {
        format!(
            "gcp::authorizedbuyersmarketplace::ListDealsResponse/{}",
            input.parent
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::authorizedbuyersmarketplace::ListDealsResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Deal
// =============================================================================

/// ResourceIdentifier implementation for Deal with AuthorizedbuyersmarketplaceBuyersProposalsDealsPatchArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<AuthorizedbuyersmarketplaceBuyersProposalsDealsPatchArgs> for Deal {
    fn generate_resource_id(
        &self,
        input: &AuthorizedbuyersmarketplaceBuyersProposalsDealsPatchArgs,
    ) -> String {
        format!("gcp::authorizedbuyersmarketplace::Deal/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::authorizedbuyersmarketplace::Deal"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for PublisherProfile
// =============================================================================

/// ResourceIdentifier implementation for PublisherProfile with AuthorizedbuyersmarketplaceBuyersPublisherProfilesGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<AuthorizedbuyersmarketplaceBuyersPublisherProfilesGetArgs>
    for PublisherProfile
{
    fn generate_resource_id(
        &self,
        input: &AuthorizedbuyersmarketplaceBuyersPublisherProfilesGetArgs,
    ) -> String {
        format!(
            "gcp::authorizedbuyersmarketplace::PublisherProfile/{}",
            input.name
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::authorizedbuyersmarketplace::PublisherProfile"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for ListPublisherProfilesResponse
// =============================================================================

/// ResourceIdentifier implementation for ListPublisherProfilesResponse with AuthorizedbuyersmarketplaceBuyersPublisherProfilesListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<AuthorizedbuyersmarketplaceBuyersPublisherProfilesListArgs>
    for ListPublisherProfilesResponse
{
    fn generate_resource_id(
        &self,
        input: &AuthorizedbuyersmarketplaceBuyersPublisherProfilesListArgs,
    ) -> String {
        format!(
            "gcp::authorizedbuyersmarketplace::ListPublisherProfilesResponse/{}",
            input.parent
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::authorizedbuyersmarketplace::ListPublisherProfilesResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}
