//! Auto-generated API clients for gcp/authorizedbuyersmarketplace.
//!
//! Generated by `cargo run --bin ewe_platform gen_provider_clients`.
//! DO NOT EDIT MANUALLY.

use crate::providers::gcp::clients::types::*;
use crate::providers::gcp::resources::*;
use foundation_core::valtron::{execute, StreamIterator, StreamIteratorExt};
use foundation_core::wire::simple_http::client::{
    body_reader, ClientRequestBuilder, RequestIntro, SimpleHttpClient, SystemDnsResolver,
};

/// GET v1/{+parent}/auctionPackages
/// List the auction packages. Buyers can use the URL path "/v1/`buyers/{`accountId`}/`auctionPackages``" to list auction packages for the current buyer and its clients. Bidders can use the URL path "/v1/`bidders/{`accountId`}/`auctionPackages``" to list auction packages for the bidder, its media planners, its buyers, and all their clients.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `authorizedbuyersmarketplace_bidders_auction_packages_list_execute()` to send, or `authorizedbuyersmarketplace_bidders_auction_packages_list` for simplest API.

pub fn authorizedbuyersmarketplace_bidders_auction_packages_list_builder(
    client: &SimpleHttpClient,
    parent: &str,
    filter: Option<&str>,
    orderBy: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comv1/{+parent}/auctionPackages",
        parent,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "filter",
            filter
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "orderBy",
            orderBy
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageSize",
            pageSize
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageToken",
            pageToken
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET v1/{+parent}/auctionPackages
/// List the auction packages. Buyers can use the URL path "/v1/`buyers/{`accountId`}/`auctionPackages``" to list auction packages for the current buyer and its clients. Bidders can use the URL path "/v1/`bidders/{`accountId`}/`auctionPackages``" to list auction packages for the bidder, its media planners, its buyers, and all their clients.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `authorizedbuyersmarketplace_bidders_auction_packages_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from authorizedbuyersmarketplace_bidders_auction_packages_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn authorizedbuyersmarketplace_bidders_auction_packages_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ListAuctionPackagesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListAuctionPackagesResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+parent}/auctionPackages
/// List the auction packages. Buyers can use the URL path "/v1/`buyers/{`accountId`}/`auctionPackages``" to list auction packages for the current buyer and its clients. Bidders can use the URL path "/v1/`bidders/{`accountId`}/`auctionPackages``" to list auction packages for the bidder, its media planners, its buyers, and all their clients.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `authorizedbuyersmarketplace_bidders_auction_packages_list_builder()` + `authorizedbuyersmarketplace_bidders_auction_packages_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn authorizedbuyersmarketplace_bidders_auction_packages_list(
    client: &SimpleHttpClient,
    parent: &str,
    filter: Option<&str>,
    orderBy: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ListAuctionPackagesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = authorizedbuyersmarketplace_bidders_auction_packages_list_builder(
        client, parent, filter, orderBy, pageSize, pageToken,
    )?;
    authorizedbuyersmarketplace_bidders_auction_packages_list_execute(builder)
}

/// GET v1/{+parent}/finalizedDeals
/// Lists finalized deals. Use the URL path "/v1/`buyers/{`accountId`}/`finalizedDeals``" to list finalized deals for the current buyer and its clients. Bidders can use the URL path "/v1/`bidders/{`accountId`}/`finalizedDeals``" to list finalized deals for the bidder, its buyers and all their clients.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `authorizedbuyersmarketplace_bidders_finalized_deals_list_execute()` to send, or `authorizedbuyersmarketplace_bidders_finalized_deals_list` for simplest API.

pub fn authorizedbuyersmarketplace_bidders_finalized_deals_list_builder(
    client: &SimpleHttpClient,
    parent: &str,
    filter: Option<&str>,
    orderBy: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/{+parent}/finalizedDeals", parent,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "filter",
            filter
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "orderBy",
            orderBy
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageSize",
            pageSize
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageToken",
            pageToken
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET v1/{+parent}/finalizedDeals
/// Lists finalized deals. Use the URL path "/v1/`buyers/{`accountId`}/`finalizedDeals``" to list finalized deals for the current buyer and its clients. Bidders can use the URL path "/v1/`bidders/{`accountId`}/`finalizedDeals``" to list finalized deals for the bidder, its buyers and all their clients.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `authorizedbuyersmarketplace_bidders_finalized_deals_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from authorizedbuyersmarketplace_bidders_finalized_deals_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn authorizedbuyersmarketplace_bidders_finalized_deals_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ListFinalizedDealsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListFinalizedDealsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+parent}/finalizedDeals
/// Lists finalized deals. Use the URL path "/v1/`buyers/{`accountId`}/`finalizedDeals``" to list finalized deals for the current buyer and its clients. Bidders can use the URL path "/v1/`bidders/{`accountId`}/`finalizedDeals``" to list finalized deals for the bidder, its buyers and all their clients.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `authorizedbuyersmarketplace_bidders_finalized_deals_list_builder()` + `authorizedbuyersmarketplace_bidders_finalized_deals_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn authorizedbuyersmarketplace_bidders_finalized_deals_list(
    client: &SimpleHttpClient,
    parent: &str,
    filter: Option<&str>,
    orderBy: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ListFinalizedDealsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = authorizedbuyersmarketplace_bidders_finalized_deals_list_builder(
        client, parent, filter, orderBy, pageSize, pageToken,
    )?;
    authorizedbuyersmarketplace_bidders_finalized_deals_list_execute(builder)
}

/// GET v1/{+deal}:setReadyToServe
/// Sets the given finalized deal as ready to serve. By default, deals are set as ready to serve as soon as they're finalized. If you want to opt out of the default behavior, and manually indicate that deals are ready to serve, ask your Technical Account Manager to add you to the allowlist. If you choose to use this method, finalized deals belonging to the bidder and its child seats don't start serving until after you call `setReadyToServe`, and after the deals become active. For example, you can use this method to delay receiving bid requests until your creative is ready. In addition, bidders can use the URL path "/v1/`bidders/{`accountId`}/`finalizedDeals`/{`dealId`}`" to set ready to serve for the finalized deals belong to itself, its child seats and all their clients. This method only applies to programmatic guaranteed deals.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `authorizedbuyersmarketplace_bidders_finalized_deals_set_ready_to_serve_execute()` to send, or `authorizedbuyersmarketplace_bidders_finalized_deals_set_ready_to_serve` for simplest API.

pub fn authorizedbuyersmarketplace_bidders_finalized_deals_set_ready_to_serve_builder(
    client: &SimpleHttpClient,
    deal: &str,
    body: &SetReadyToServeRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/{+deal}:setReadyToServe", deal,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v1/{+deal}:setReadyToServe
/// Sets the given finalized deal as ready to serve. By default, deals are set as ready to serve as soon as they're finalized. If you want to opt out of the default behavior, and manually indicate that deals are ready to serve, ask your Technical Account Manager to add you to the allowlist. If you choose to use this method, finalized deals belonging to the bidder and its child seats don't start serving until after you call `setReadyToServe`, and after the deals become active. For example, you can use this method to delay receiving bid requests until your creative is ready. In addition, bidders can use the URL path "/v1/`bidders/{`accountId`}/`finalizedDeals`/{`dealId`}`" to set ready to serve for the finalized deals belong to itself, its child seats and all their clients. This method only applies to programmatic guaranteed deals.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `authorizedbuyersmarketplace_bidders_finalized_deals_set_ready_to_serve_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from authorizedbuyersmarketplace_bidders_finalized_deals_set_ready_to_serve_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn authorizedbuyersmarketplace_bidders_finalized_deals_set_ready_to_serve_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<FinalizedDeal>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: FinalizedDeal = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+deal}:setReadyToServe
/// Sets the given finalized deal as ready to serve. By default, deals are set as ready to serve as soon as they're finalized. If you want to opt out of the default behavior, and manually indicate that deals are ready to serve, ask your Technical Account Manager to add you to the allowlist. If you choose to use this method, finalized deals belonging to the bidder and its child seats don't start serving until after you call `setReadyToServe`, and after the deals become active. For example, you can use this method to delay receiving bid requests until your creative is ready. In addition, bidders can use the URL path "/v1/`bidders/{`accountId`}/`finalizedDeals`/{`dealId`}`" to set ready to serve for the finalized deals belong to itself, its child seats and all their clients. This method only applies to programmatic guaranteed deals.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `authorizedbuyersmarketplace_bidders_finalized_deals_set_ready_to_serve_builder()` + `authorizedbuyersmarketplace_bidders_finalized_deals_set_ready_to_serve_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn authorizedbuyersmarketplace_bidders_finalized_deals_set_ready_to_serve(
    client: &SimpleHttpClient,
    deal: &str,
    body: &SetReadyToServeRequest,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<FinalizedDeal>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = authorizedbuyersmarketplace_bidders_finalized_deals_set_ready_to_serve_builder(
        client, deal, body,
    )?;
    authorizedbuyersmarketplace_bidders_finalized_deals_set_ready_to_serve_execute(builder)
}

/// GET v1/{+name}
/// Gets an auction package given its name.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `authorizedbuyersmarketplace_buyers_auction_packages_get_execute()` to send, or `authorizedbuyersmarketplace_buyers_auction_packages_get` for simplest API.

pub fn authorizedbuyersmarketplace_buyers_auction_packages_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/{+name}", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+name}
/// Gets an auction package given its name.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `authorizedbuyersmarketplace_buyers_auction_packages_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from authorizedbuyersmarketplace_buyers_auction_packages_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn authorizedbuyersmarketplace_buyers_auction_packages_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<AuctionPackage>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: AuctionPackage = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+name}
/// Gets an auction package given its name.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `authorizedbuyersmarketplace_buyers_auction_packages_get_builder()` + `authorizedbuyersmarketplace_buyers_auction_packages_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn authorizedbuyersmarketplace_buyers_auction_packages_get(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<AuctionPackage>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = authorizedbuyersmarketplace_buyers_auction_packages_get_builder(client, name)?;
    authorizedbuyersmarketplace_buyers_auction_packages_get_execute(builder)
}

/// GET v1/{+parent}/auctionPackages
/// List the auction packages. Buyers can use the URL path "/v1/`buyers/{`accountId`}/`auctionPackages``" to list auction packages for the current buyer and its clients. Bidders can use the URL path "/v1/`bidders/{`accountId`}/`auctionPackages``" to list auction packages for the bidder, its media planners, its buyers, and all their clients.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `authorizedbuyersmarketplace_buyers_auction_packages_list_execute()` to send, or `authorizedbuyersmarketplace_buyers_auction_packages_list` for simplest API.

pub fn authorizedbuyersmarketplace_buyers_auction_packages_list_builder(
    client: &SimpleHttpClient,
    parent: &str,
    filter: Option<&str>,
    orderBy: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comv1/{+parent}/auctionPackages",
        parent,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "filter",
            filter
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "orderBy",
            orderBy
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageSize",
            pageSize
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageToken",
            pageToken
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET v1/{+parent}/auctionPackages
/// List the auction packages. Buyers can use the URL path "/v1/`buyers/{`accountId`}/`auctionPackages``" to list auction packages for the current buyer and its clients. Bidders can use the URL path "/v1/`bidders/{`accountId`}/`auctionPackages``" to list auction packages for the bidder, its media planners, its buyers, and all their clients.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `authorizedbuyersmarketplace_buyers_auction_packages_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from authorizedbuyersmarketplace_buyers_auction_packages_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn authorizedbuyersmarketplace_buyers_auction_packages_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ListAuctionPackagesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListAuctionPackagesResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+parent}/auctionPackages
/// List the auction packages. Buyers can use the URL path "/v1/`buyers/{`accountId`}/`auctionPackages``" to list auction packages for the current buyer and its clients. Bidders can use the URL path "/v1/`bidders/{`accountId`}/`auctionPackages``" to list auction packages for the bidder, its media planners, its buyers, and all their clients.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `authorizedbuyersmarketplace_buyers_auction_packages_list_builder()` + `authorizedbuyersmarketplace_buyers_auction_packages_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn authorizedbuyersmarketplace_buyers_auction_packages_list(
    client: &SimpleHttpClient,
    parent: &str,
    filter: Option<&str>,
    orderBy: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ListAuctionPackagesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = authorizedbuyersmarketplace_buyers_auction_packages_list_builder(
        client, parent, filter, orderBy, pageSize, pageToken,
    )?;
    authorizedbuyersmarketplace_buyers_auction_packages_list_execute(builder)
}

/// GET v1/{+name}:subscribe
/// Subscribe to the auction package for the specified buyer. Once subscribed, the bidder will receive a call out for inventory matching the auction package targeting criteria with the auction package deal ID and the specified buyer.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `authorizedbuyersmarketplace_buyers_auction_packages_subscribe_execute()` to send, or `authorizedbuyersmarketplace_buyers_auction_packages_subscribe` for simplest API.

pub fn authorizedbuyersmarketplace_buyers_auction_packages_subscribe_builder(
    client: &SimpleHttpClient,
    name: &str,
    body: &SubscribeAuctionPackageRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/{+name}:subscribe", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v1/{+name}:subscribe
/// Subscribe to the auction package for the specified buyer. Once subscribed, the bidder will receive a call out for inventory matching the auction package targeting criteria with the auction package deal ID and the specified buyer.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `authorizedbuyersmarketplace_buyers_auction_packages_subscribe_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from authorizedbuyersmarketplace_buyers_auction_packages_subscribe_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn authorizedbuyersmarketplace_buyers_auction_packages_subscribe_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<AuctionPackage>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: AuctionPackage = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+name}:subscribe
/// Subscribe to the auction package for the specified buyer. Once subscribed, the bidder will receive a call out for inventory matching the auction package targeting criteria with the auction package deal ID and the specified buyer.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `authorizedbuyersmarketplace_buyers_auction_packages_subscribe_builder()` + `authorizedbuyersmarketplace_buyers_auction_packages_subscribe_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn authorizedbuyersmarketplace_buyers_auction_packages_subscribe(
    client: &SimpleHttpClient,
    name: &str,
    body: &SubscribeAuctionPackageRequest,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<AuctionPackage>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        authorizedbuyersmarketplace_buyers_auction_packages_subscribe_builder(client, name, body)?;
    authorizedbuyersmarketplace_buyers_auction_packages_subscribe_execute(builder)
}

/// GET v1/{+auctionPackage}:subscribeClients
/// Subscribe the specified clients of the buyer to the auction package. If a client in the list does not belong to the buyer, an error response will be returned, and all of the following clients in the list will not be subscribed. Subscribing an already subscribed client will have no effect.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `authorizedbuyersmarketplace_buyers_auction_packages_subscribe_clients_execute()` to send, or `authorizedbuyersmarketplace_buyers_auction_packages_subscribe_clients` for simplest API.

pub fn authorizedbuyersmarketplace_buyers_auction_packages_subscribe_clients_builder(
    client: &SimpleHttpClient,
    auctionPackage: &str,
    body: &SubscribeClientsRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comv1/{+auctionPackage}:subscribeClients",
        auctionPackage,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v1/{+auctionPackage}:subscribeClients
/// Subscribe the specified clients of the buyer to the auction package. If a client in the list does not belong to the buyer, an error response will be returned, and all of the following clients in the list will not be subscribed. Subscribing an already subscribed client will have no effect.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `authorizedbuyersmarketplace_buyers_auction_packages_subscribe_clients_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from authorizedbuyersmarketplace_buyers_auction_packages_subscribe_clients_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn authorizedbuyersmarketplace_buyers_auction_packages_subscribe_clients_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<AuctionPackage>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: AuctionPackage = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+auctionPackage}:subscribeClients
/// Subscribe the specified clients of the buyer to the auction package. If a client in the list does not belong to the buyer, an error response will be returned, and all of the following clients in the list will not be subscribed. Subscribing an already subscribed client will have no effect.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `authorizedbuyersmarketplace_buyers_auction_packages_subscribe_clients_builder()` + `authorizedbuyersmarketplace_buyers_auction_packages_subscribe_clients_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn authorizedbuyersmarketplace_buyers_auction_packages_subscribe_clients(
    client: &SimpleHttpClient,
    auctionPackage: &str,
    body: &SubscribeClientsRequest,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<AuctionPackage>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = authorizedbuyersmarketplace_buyers_auction_packages_subscribe_clients_builder(
        client,
        auctionPackage,
        body,
    )?;
    authorizedbuyersmarketplace_buyers_auction_packages_subscribe_clients_execute(builder)
}

/// GET v1/{+name}:unsubscribe
/// Unsubscribe from the auction package for the specified buyer. Once unsubscribed, the bidder will no longer receive a call out for the auction package deal ID and the specified buyer.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `authorizedbuyersmarketplace_buyers_auction_packages_unsubscribe_execute()` to send, or `authorizedbuyersmarketplace_buyers_auction_packages_unsubscribe` for simplest API.

pub fn authorizedbuyersmarketplace_buyers_auction_packages_unsubscribe_builder(
    client: &SimpleHttpClient,
    name: &str,
    body: &UnsubscribeAuctionPackageRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/{+name}:unsubscribe", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v1/{+name}:unsubscribe
/// Unsubscribe from the auction package for the specified buyer. Once unsubscribed, the bidder will no longer receive a call out for the auction package deal ID and the specified buyer.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `authorizedbuyersmarketplace_buyers_auction_packages_unsubscribe_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from authorizedbuyersmarketplace_buyers_auction_packages_unsubscribe_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn authorizedbuyersmarketplace_buyers_auction_packages_unsubscribe_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<AuctionPackage>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: AuctionPackage = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+name}:unsubscribe
/// Unsubscribe from the auction package for the specified buyer. Once unsubscribed, the bidder will no longer receive a call out for the auction package deal ID and the specified buyer.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `authorizedbuyersmarketplace_buyers_auction_packages_unsubscribe_builder()` + `authorizedbuyersmarketplace_buyers_auction_packages_unsubscribe_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn authorizedbuyersmarketplace_buyers_auction_packages_unsubscribe(
    client: &SimpleHttpClient,
    name: &str,
    body: &UnsubscribeAuctionPackageRequest,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<AuctionPackage>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = authorizedbuyersmarketplace_buyers_auction_packages_unsubscribe_builder(
        client, name, body,
    )?;
    authorizedbuyersmarketplace_buyers_auction_packages_unsubscribe_execute(builder)
}

/// GET v1/{+auctionPackage}:unsubscribeClients
/// Unsubscribe from the auction package for the specified clients of the buyer. Unsubscribing a client that is not subscribed will have no effect.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `authorizedbuyersmarketplace_buyers_auction_packages_unsubscribe_clients_execute()` to send, or `authorizedbuyersmarketplace_buyers_auction_packages_unsubscribe_clients` for simplest API.

pub fn authorizedbuyersmarketplace_buyers_auction_packages_unsubscribe_clients_builder(
    client: &SimpleHttpClient,
    auctionPackage: &str,
    body: &UnsubscribeClientsRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comv1/{+auctionPackage}:unsubscribeClients",
        auctionPackage,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v1/{+auctionPackage}:unsubscribeClients
/// Unsubscribe from the auction package for the specified clients of the buyer. Unsubscribing a client that is not subscribed will have no effect.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `authorizedbuyersmarketplace_buyers_auction_packages_unsubscribe_clients_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from authorizedbuyersmarketplace_buyers_auction_packages_unsubscribe_clients_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn authorizedbuyersmarketplace_buyers_auction_packages_unsubscribe_clients_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<AuctionPackage>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: AuctionPackage = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+auctionPackage}:unsubscribeClients
/// Unsubscribe from the auction package for the specified clients of the buyer. Unsubscribing a client that is not subscribed will have no effect.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `authorizedbuyersmarketplace_buyers_auction_packages_unsubscribe_clients_builder()` + `authorizedbuyersmarketplace_buyers_auction_packages_unsubscribe_clients_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn authorizedbuyersmarketplace_buyers_auction_packages_unsubscribe_clients(
    client: &SimpleHttpClient,
    auctionPackage: &str,
    body: &UnsubscribeClientsRequest,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<AuctionPackage>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = authorizedbuyersmarketplace_buyers_auction_packages_unsubscribe_clients_builder(
        client,
        auctionPackage,
        body,
    )?;
    authorizedbuyersmarketplace_buyers_auction_packages_unsubscribe_clients_execute(builder)
}

/// GET v1/{+name}:activate
/// Activates an existing client. The state of the client will be updated to "`ACTIVE`". This method has no effect if the client is already in "`ACTIVE`" state.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `authorizedbuyersmarketplace_buyers_clients_activate_execute()` to send, or `authorizedbuyersmarketplace_buyers_clients_activate` for simplest API.

pub fn authorizedbuyersmarketplace_buyers_clients_activate_builder(
    client: &SimpleHttpClient,
    name: &str,
    body: &ActivateClientRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/{+name}:activate", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v1/{+name}:activate
/// Activates an existing client. The state of the client will be updated to "`ACTIVE`". This method has no effect if the client is already in "`ACTIVE`" state.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `authorizedbuyersmarketplace_buyers_clients_activate_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from authorizedbuyersmarketplace_buyers_clients_activate_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn authorizedbuyersmarketplace_buyers_clients_activate_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Client>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Client = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+name}:activate
/// Activates an existing client. The state of the client will be updated to "`ACTIVE`". This method has no effect if the client is already in "`ACTIVE`" state.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `authorizedbuyersmarketplace_buyers_clients_activate_builder()` + `authorizedbuyersmarketplace_buyers_clients_activate_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn authorizedbuyersmarketplace_buyers_clients_activate(
    client: &SimpleHttpClient,
    name: &str,
    body: &ActivateClientRequest,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Client>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = authorizedbuyersmarketplace_buyers_clients_activate_builder(client, name, body)?;
    authorizedbuyersmarketplace_buyers_clients_activate_execute(builder)
}

/// GET v1/{+parent}/clients
/// Creates a new client.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `authorizedbuyersmarketplace_buyers_clients_create_execute()` to send, or `authorizedbuyersmarketplace_buyers_clients_create` for simplest API.

pub fn authorizedbuyersmarketplace_buyers_clients_create_builder(
    client: &SimpleHttpClient,
    parent: &str,
    body: &Client,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/{+parent}/clients", parent,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v1/{+parent}/clients
/// Creates a new client.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `authorizedbuyersmarketplace_buyers_clients_create_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from authorizedbuyersmarketplace_buyers_clients_create_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn authorizedbuyersmarketplace_buyers_clients_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Client>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Client = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+parent}/clients
/// Creates a new client.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `authorizedbuyersmarketplace_buyers_clients_create_builder()` + `authorizedbuyersmarketplace_buyers_clients_create_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn authorizedbuyersmarketplace_buyers_clients_create(
    client: &SimpleHttpClient,
    parent: &str,
    body: &Client,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Client>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = authorizedbuyersmarketplace_buyers_clients_create_builder(client, parent, body)?;
    authorizedbuyersmarketplace_buyers_clients_create_execute(builder)
}

/// GET v1/{+name}:deactivate
/// Deactivates an existing client. The state of the client will be updated to "`INACTIVE`". This method has no effect if the client is already in "`INACTIVE`" state.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `authorizedbuyersmarketplace_buyers_clients_deactivate_execute()` to send, or `authorizedbuyersmarketplace_buyers_clients_deactivate` for simplest API.

pub fn authorizedbuyersmarketplace_buyers_clients_deactivate_builder(
    client: &SimpleHttpClient,
    name: &str,
    body: &DeactivateClientRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/{+name}:deactivate", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v1/{+name}:deactivate
/// Deactivates an existing client. The state of the client will be updated to "`INACTIVE`". This method has no effect if the client is already in "`INACTIVE`" state.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `authorizedbuyersmarketplace_buyers_clients_deactivate_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from authorizedbuyersmarketplace_buyers_clients_deactivate_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn authorizedbuyersmarketplace_buyers_clients_deactivate_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Client>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Client = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+name}:deactivate
/// Deactivates an existing client. The state of the client will be updated to "`INACTIVE`". This method has no effect if the client is already in "`INACTIVE`" state.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `authorizedbuyersmarketplace_buyers_clients_deactivate_builder()` + `authorizedbuyersmarketplace_buyers_clients_deactivate_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn authorizedbuyersmarketplace_buyers_clients_deactivate(
    client: &SimpleHttpClient,
    name: &str,
    body: &DeactivateClientRequest,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Client>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        authorizedbuyersmarketplace_buyers_clients_deactivate_builder(client, name, body)?;
    authorizedbuyersmarketplace_buyers_clients_deactivate_execute(builder)
}

/// GET v1/{+name}
/// Gets a client with a given resource name.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `authorizedbuyersmarketplace_buyers_clients_get_execute()` to send, or `authorizedbuyersmarketplace_buyers_clients_get` for simplest API.

pub fn authorizedbuyersmarketplace_buyers_clients_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/{+name}", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+name}
/// Gets a client with a given resource name.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `authorizedbuyersmarketplace_buyers_clients_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from authorizedbuyersmarketplace_buyers_clients_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn authorizedbuyersmarketplace_buyers_clients_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Client>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Client = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+name}
/// Gets a client with a given resource name.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `authorizedbuyersmarketplace_buyers_clients_get_builder()` + `authorizedbuyersmarketplace_buyers_clients_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn authorizedbuyersmarketplace_buyers_clients_get(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Client>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = authorizedbuyersmarketplace_buyers_clients_get_builder(client, name)?;
    authorizedbuyersmarketplace_buyers_clients_get_execute(builder)
}

/// GET v1/{+parent}/clients
/// Lists all the clients for the current buyer.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `authorizedbuyersmarketplace_buyers_clients_list_execute()` to send, or `authorizedbuyersmarketplace_buyers_clients_list` for simplest API.

pub fn authorizedbuyersmarketplace_buyers_clients_list_builder(
    client: &SimpleHttpClient,
    parent: &str,
    filter: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/{+parent}/clients", parent,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "filter",
            filter
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageSize",
            pageSize
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageToken",
            pageToken
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET v1/{+parent}/clients
/// Lists all the clients for the current buyer.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `authorizedbuyersmarketplace_buyers_clients_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from authorizedbuyersmarketplace_buyers_clients_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn authorizedbuyersmarketplace_buyers_clients_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListClientsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListClientsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+parent}/clients
/// Lists all the clients for the current buyer.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `authorizedbuyersmarketplace_buyers_clients_list_builder()` + `authorizedbuyersmarketplace_buyers_clients_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn authorizedbuyersmarketplace_buyers_clients_list(
    client: &SimpleHttpClient,
    parent: &str,
    filter: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListClientsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = authorizedbuyersmarketplace_buyers_clients_list_builder(
        client, parent, filter, pageSize, pageToken,
    )?;
    authorizedbuyersmarketplace_buyers_clients_list_execute(builder)
}

/// GET v1/{+name}
/// Updates an existing client.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `authorizedbuyersmarketplace_buyers_clients_patch_execute()` to send, or `authorizedbuyersmarketplace_buyers_clients_patch` for simplest API.

pub fn authorizedbuyersmarketplace_buyers_clients_patch_builder(
    client: &SimpleHttpClient,
    name: &str,
    updateMask: Option<&str>,
    body: &Client,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/{+name}", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "updateMask",
            updateMask
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v1/{+name}
/// Updates an existing client.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `authorizedbuyersmarketplace_buyers_clients_patch_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from authorizedbuyersmarketplace_buyers_clients_patch_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn authorizedbuyersmarketplace_buyers_clients_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Client>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Client = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+name}
/// Updates an existing client.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `authorizedbuyersmarketplace_buyers_clients_patch_builder()` + `authorizedbuyersmarketplace_buyers_clients_patch_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn authorizedbuyersmarketplace_buyers_clients_patch(
    client: &SimpleHttpClient,
    name: &str,
    updateMask: Option<&str>,
    body: &Client,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Client>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        authorizedbuyersmarketplace_buyers_clients_patch_builder(client, name, updateMask, body)?;
    authorizedbuyersmarketplace_buyers_clients_patch_execute(builder)
}

/// GET v1/{+name}:activate
/// Activates an existing client user. The state of the client user will be updated from "`INACTIVE`" to "`ACTIVE`". This method has no effect if the client user is already in "`ACTIVE`" state. An error will be returned if the client user to activate is still in "INVITED" state.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `authorizedbuyersmarketplace_buyers_clients_users_activate_execute()` to send, or `authorizedbuyersmarketplace_buyers_clients_users_activate` for simplest API.

pub fn authorizedbuyersmarketplace_buyers_clients_users_activate_builder(
    client: &SimpleHttpClient,
    name: &str,
    body: &ActivateClientUserRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/{+name}:activate", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v1/{+name}:activate
/// Activates an existing client user. The state of the client user will be updated from "`INACTIVE`" to "`ACTIVE`". This method has no effect if the client user is already in "`ACTIVE`" state. An error will be returned if the client user to activate is still in "INVITED" state.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `authorizedbuyersmarketplace_buyers_clients_users_activate_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from authorizedbuyersmarketplace_buyers_clients_users_activate_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn authorizedbuyersmarketplace_buyers_clients_users_activate_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ClientUser>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ClientUser = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+name}:activate
/// Activates an existing client user. The state of the client user will be updated from "`INACTIVE`" to "`ACTIVE`". This method has no effect if the client user is already in "`ACTIVE`" state. An error will be returned if the client user to activate is still in "INVITED" state.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `authorizedbuyersmarketplace_buyers_clients_users_activate_builder()` + `authorizedbuyersmarketplace_buyers_clients_users_activate_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn authorizedbuyersmarketplace_buyers_clients_users_activate(
    client: &SimpleHttpClient,
    name: &str,
    body: &ActivateClientUserRequest,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ClientUser>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        authorizedbuyersmarketplace_buyers_clients_users_activate_builder(client, name, body)?;
    authorizedbuyersmarketplace_buyers_clients_users_activate_execute(builder)
}

/// GET v1/{+parent}/users
/// Creates a new client user in "INVITED" state. An email invitation will be sent to the new user, once accepted the user will become active.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `authorizedbuyersmarketplace_buyers_clients_users_create_execute()` to send, or `authorizedbuyersmarketplace_buyers_clients_users_create` for simplest API.

pub fn authorizedbuyersmarketplace_buyers_clients_users_create_builder(
    client: &SimpleHttpClient,
    parent: &str,
    body: &ClientUser,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/{+parent}/users", parent,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v1/{+parent}/users
/// Creates a new client user in "INVITED" state. An email invitation will be sent to the new user, once accepted the user will become active.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `authorizedbuyersmarketplace_buyers_clients_users_create_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from authorizedbuyersmarketplace_buyers_clients_users_create_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn authorizedbuyersmarketplace_buyers_clients_users_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ClientUser>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ClientUser = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+parent}/users
/// Creates a new client user in "INVITED" state. An email invitation will be sent to the new user, once accepted the user will become active.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `authorizedbuyersmarketplace_buyers_clients_users_create_builder()` + `authorizedbuyersmarketplace_buyers_clients_users_create_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn authorizedbuyersmarketplace_buyers_clients_users_create(
    client: &SimpleHttpClient,
    parent: &str,
    body: &ClientUser,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ClientUser>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        authorizedbuyersmarketplace_buyers_clients_users_create_builder(client, parent, body)?;
    authorizedbuyersmarketplace_buyers_clients_users_create_execute(builder)
}

/// GET v1/{+name}:deactivate
/// Deactivates an existing client user. The state of the client user will be updated from "`ACTIVE`" to "`INACTIVE`". This method has no effect if the client user is already in "`INACTIVE`" state. An error will be returned if the client user to deactivate is still in "INVITED" state.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `authorizedbuyersmarketplace_buyers_clients_users_deactivate_execute()` to send, or `authorizedbuyersmarketplace_buyers_clients_users_deactivate` for simplest API.

pub fn authorizedbuyersmarketplace_buyers_clients_users_deactivate_builder(
    client: &SimpleHttpClient,
    name: &str,
    body: &DeactivateClientUserRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/{+name}:deactivate", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v1/{+name}:deactivate
/// Deactivates an existing client user. The state of the client user will be updated from "`ACTIVE`" to "`INACTIVE`". This method has no effect if the client user is already in "`INACTIVE`" state. An error will be returned if the client user to deactivate is still in "INVITED" state.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `authorizedbuyersmarketplace_buyers_clients_users_deactivate_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from authorizedbuyersmarketplace_buyers_clients_users_deactivate_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn authorizedbuyersmarketplace_buyers_clients_users_deactivate_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ClientUser>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ClientUser = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+name}:deactivate
/// Deactivates an existing client user. The state of the client user will be updated from "`ACTIVE`" to "`INACTIVE`". This method has no effect if the client user is already in "`INACTIVE`" state. An error will be returned if the client user to deactivate is still in "INVITED" state.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `authorizedbuyersmarketplace_buyers_clients_users_deactivate_builder()` + `authorizedbuyersmarketplace_buyers_clients_users_deactivate_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn authorizedbuyersmarketplace_buyers_clients_users_deactivate(
    client: &SimpleHttpClient,
    name: &str,
    body: &DeactivateClientUserRequest,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ClientUser>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        authorizedbuyersmarketplace_buyers_clients_users_deactivate_builder(client, name, body)?;
    authorizedbuyersmarketplace_buyers_clients_users_deactivate_execute(builder)
}

/// GET v1/{+name}
/// Deletes an existing client user. The client user will lose access to the Authorized Buyers UI. Note that if a client user is deleted, the user's access to the UI can't be restored unless a new client user is created and activated.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `authorizedbuyersmarketplace_buyers_clients_users_delete_execute()` to send, or `authorizedbuyersmarketplace_buyers_clients_users_delete` for simplest API.

pub fn authorizedbuyersmarketplace_buyers_clients_users_delete_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/{+name}", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+name}
/// Deletes an existing client user. The client user will lose access to the Authorized Buyers UI. Note that if a client user is deleted, the user's access to the UI can't be restored unless a new client user is created and activated.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `authorizedbuyersmarketplace_buyers_clients_users_delete_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from authorizedbuyersmarketplace_buyers_clients_users_delete_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn authorizedbuyersmarketplace_buyers_clients_users_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Empty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+name}
/// Deletes an existing client user. The client user will lose access to the Authorized Buyers UI. Note that if a client user is deleted, the user's access to the UI can't be restored unless a new client user is created and activated.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `authorizedbuyersmarketplace_buyers_clients_users_delete_builder()` + `authorizedbuyersmarketplace_buyers_clients_users_delete_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn authorizedbuyersmarketplace_buyers_clients_users_delete(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = authorizedbuyersmarketplace_buyers_clients_users_delete_builder(client, name)?;
    authorizedbuyersmarketplace_buyers_clients_users_delete_execute(builder)
}

/// GET v1/{+name}
/// Retrieves an existing client user.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `authorizedbuyersmarketplace_buyers_clients_users_get_execute()` to send, or `authorizedbuyersmarketplace_buyers_clients_users_get` for simplest API.

pub fn authorizedbuyersmarketplace_buyers_clients_users_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/{+name}", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+name}
/// Retrieves an existing client user.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `authorizedbuyersmarketplace_buyers_clients_users_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from authorizedbuyersmarketplace_buyers_clients_users_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn authorizedbuyersmarketplace_buyers_clients_users_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ClientUser>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ClientUser = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+name}
/// Retrieves an existing client user.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `authorizedbuyersmarketplace_buyers_clients_users_get_builder()` + `authorizedbuyersmarketplace_buyers_clients_users_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn authorizedbuyersmarketplace_buyers_clients_users_get(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ClientUser>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = authorizedbuyersmarketplace_buyers_clients_users_get_builder(client, name)?;
    authorizedbuyersmarketplace_buyers_clients_users_get_execute(builder)
}

/// GET v1/{+parent}/users
/// Lists all client users for a specified client.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `authorizedbuyersmarketplace_buyers_clients_users_list_execute()` to send, or `authorizedbuyersmarketplace_buyers_clients_users_list` for simplest API.

pub fn authorizedbuyersmarketplace_buyers_clients_users_list_builder(
    client: &SimpleHttpClient,
    parent: &str,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/{+parent}/users", parent,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "pageSize",
            pageSize
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageToken",
            pageToken
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET v1/{+parent}/users
/// Lists all client users for a specified client.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `authorizedbuyersmarketplace_buyers_clients_users_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from authorizedbuyersmarketplace_buyers_clients_users_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn authorizedbuyersmarketplace_buyers_clients_users_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListClientUsersResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListClientUsersResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+parent}/users
/// Lists all client users for a specified client.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `authorizedbuyersmarketplace_buyers_clients_users_list_builder()` + `authorizedbuyersmarketplace_buyers_clients_users_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn authorizedbuyersmarketplace_buyers_clients_users_list(
    client: &SimpleHttpClient,
    parent: &str,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListClientUsersResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = authorizedbuyersmarketplace_buyers_clients_users_list_builder(
        client, parent, pageSize, pageToken,
    )?;
    authorizedbuyersmarketplace_buyers_clients_users_list_execute(builder)
}

/// GET v1/{+deal}:addCreative
/// Add creative to be used in the bidding process for a finalized deal. For programmatic guaranteed deals, it's recommended that you associate at least one approved creative with the deal before calling SetReadyToServe, to help reduce the number of bid responses filtered because they don't contain approved creatives. Creatives successfully added to a deal can be found in the Realtime-bidding Creatives API creative.deal_ids. This method only applies to programmatic guaranteed deals. Maximum number of 1000 creatives can be added to a finalized deal.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `authorizedbuyersmarketplace_buyers_finalized_deals_add_creative_execute()` to send, or `authorizedbuyersmarketplace_buyers_finalized_deals_add_creative` for simplest API.

pub fn authorizedbuyersmarketplace_buyers_finalized_deals_add_creative_builder(
    client: &SimpleHttpClient,
    deal: &str,
    body: &AddCreativeRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/{+deal}:addCreative", deal,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v1/{+deal}:addCreative
/// Add creative to be used in the bidding process for a finalized deal. For programmatic guaranteed deals, it's recommended that you associate at least one approved creative with the deal before calling SetReadyToServe, to help reduce the number of bid responses filtered because they don't contain approved creatives. Creatives successfully added to a deal can be found in the Realtime-bidding Creatives API creative.deal_ids. This method only applies to programmatic guaranteed deals. Maximum number of 1000 creatives can be added to a finalized deal.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `authorizedbuyersmarketplace_buyers_finalized_deals_add_creative_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from authorizedbuyersmarketplace_buyers_finalized_deals_add_creative_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn authorizedbuyersmarketplace_buyers_finalized_deals_add_creative_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<FinalizedDeal>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: FinalizedDeal = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+deal}:addCreative
/// Add creative to be used in the bidding process for a finalized deal. For programmatic guaranteed deals, it's recommended that you associate at least one approved creative with the deal before calling SetReadyToServe, to help reduce the number of bid responses filtered because they don't contain approved creatives. Creatives successfully added to a deal can be found in the Realtime-bidding Creatives API creative.deal_ids. This method only applies to programmatic guaranteed deals. Maximum number of 1000 creatives can be added to a finalized deal.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `authorizedbuyersmarketplace_buyers_finalized_deals_add_creative_builder()` + `authorizedbuyersmarketplace_buyers_finalized_deals_add_creative_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn authorizedbuyersmarketplace_buyers_finalized_deals_add_creative(
    client: &SimpleHttpClient,
    deal: &str,
    body: &AddCreativeRequest,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<FinalizedDeal>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = authorizedbuyersmarketplace_buyers_finalized_deals_add_creative_builder(
        client, deal, body,
    )?;
    authorizedbuyersmarketplace_buyers_finalized_deals_add_creative_execute(builder)
}

/// GET v1/{+name}
/// Gets a finalized deal given its name.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `authorizedbuyersmarketplace_buyers_finalized_deals_get_execute()` to send, or `authorizedbuyersmarketplace_buyers_finalized_deals_get` for simplest API.

pub fn authorizedbuyersmarketplace_buyers_finalized_deals_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/{+name}", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+name}
/// Gets a finalized deal given its name.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `authorizedbuyersmarketplace_buyers_finalized_deals_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from authorizedbuyersmarketplace_buyers_finalized_deals_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn authorizedbuyersmarketplace_buyers_finalized_deals_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<FinalizedDeal>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: FinalizedDeal = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+name}
/// Gets a finalized deal given its name.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `authorizedbuyersmarketplace_buyers_finalized_deals_get_builder()` + `authorizedbuyersmarketplace_buyers_finalized_deals_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn authorizedbuyersmarketplace_buyers_finalized_deals_get(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<FinalizedDeal>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = authorizedbuyersmarketplace_buyers_finalized_deals_get_builder(client, name)?;
    authorizedbuyersmarketplace_buyers_finalized_deals_get_execute(builder)
}

/// GET v1/{+parent}/finalizedDeals
/// Lists finalized deals. Use the URL path "/v1/`buyers/{`accountId`}/`finalizedDeals``" to list finalized deals for the current buyer and its clients. Bidders can use the URL path "/v1/`bidders/{`accountId`}/`finalizedDeals``" to list finalized deals for the bidder, its buyers and all their clients.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `authorizedbuyersmarketplace_buyers_finalized_deals_list_execute()` to send, or `authorizedbuyersmarketplace_buyers_finalized_deals_list` for simplest API.

pub fn authorizedbuyersmarketplace_buyers_finalized_deals_list_builder(
    client: &SimpleHttpClient,
    parent: &str,
    filter: Option<&str>,
    orderBy: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/{+parent}/finalizedDeals", parent,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "filter",
            filter
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "orderBy",
            orderBy
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageSize",
            pageSize
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageToken",
            pageToken
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET v1/{+parent}/finalizedDeals
/// Lists finalized deals. Use the URL path "/v1/`buyers/{`accountId`}/`finalizedDeals``" to list finalized deals for the current buyer and its clients. Bidders can use the URL path "/v1/`bidders/{`accountId`}/`finalizedDeals``" to list finalized deals for the bidder, its buyers and all their clients.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `authorizedbuyersmarketplace_buyers_finalized_deals_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from authorizedbuyersmarketplace_buyers_finalized_deals_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn authorizedbuyersmarketplace_buyers_finalized_deals_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ListFinalizedDealsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListFinalizedDealsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+parent}/finalizedDeals
/// Lists finalized deals. Use the URL path "/v1/`buyers/{`accountId`}/`finalizedDeals``" to list finalized deals for the current buyer and its clients. Bidders can use the URL path "/v1/`bidders/{`accountId`}/`finalizedDeals``" to list finalized deals for the bidder, its buyers and all their clients.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `authorizedbuyersmarketplace_buyers_finalized_deals_list_builder()` + `authorizedbuyersmarketplace_buyers_finalized_deals_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn authorizedbuyersmarketplace_buyers_finalized_deals_list(
    client: &SimpleHttpClient,
    parent: &str,
    filter: Option<&str>,
    orderBy: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ListFinalizedDealsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = authorizedbuyersmarketplace_buyers_finalized_deals_list_builder(
        client, parent, filter, orderBy, pageSize, pageToken,
    )?;
    authorizedbuyersmarketplace_buyers_finalized_deals_list_execute(builder)
}

/// GET v1/{+name}:pause
/// Pauses serving of the given finalized deal. This call only pauses the serving status, and does not affect other fields of the finalized deal. Calling this method for an already paused deal has no effect. This method only applies to programmatic guaranteed deals and preferred deals.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `authorizedbuyersmarketplace_buyers_finalized_deals_pause_execute()` to send, or `authorizedbuyersmarketplace_buyers_finalized_deals_pause` for simplest API.

pub fn authorizedbuyersmarketplace_buyers_finalized_deals_pause_builder(
    client: &SimpleHttpClient,
    name: &str,
    body: &PauseFinalizedDealRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/{+name}:pause", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v1/{+name}:pause
/// Pauses serving of the given finalized deal. This call only pauses the serving status, and does not affect other fields of the finalized deal. Calling this method for an already paused deal has no effect. This method only applies to programmatic guaranteed deals and preferred deals.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `authorizedbuyersmarketplace_buyers_finalized_deals_pause_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from authorizedbuyersmarketplace_buyers_finalized_deals_pause_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn authorizedbuyersmarketplace_buyers_finalized_deals_pause_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<FinalizedDeal>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: FinalizedDeal = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+name}:pause
/// Pauses serving of the given finalized deal. This call only pauses the serving status, and does not affect other fields of the finalized deal. Calling this method for an already paused deal has no effect. This method only applies to programmatic guaranteed deals and preferred deals.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `authorizedbuyersmarketplace_buyers_finalized_deals_pause_builder()` + `authorizedbuyersmarketplace_buyers_finalized_deals_pause_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn authorizedbuyersmarketplace_buyers_finalized_deals_pause(
    client: &SimpleHttpClient,
    name: &str,
    body: &PauseFinalizedDealRequest,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<FinalizedDeal>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        authorizedbuyersmarketplace_buyers_finalized_deals_pause_builder(client, name, body)?;
    authorizedbuyersmarketplace_buyers_finalized_deals_pause_execute(builder)
}

/// GET v1/{+name}:resume
/// Resumes serving of the given finalized deal. Calling this method for an running deal has no effect. If a deal is initially paused by the seller, calling this method will not resume serving of the deal until the seller also resumes the deal. This method only applies to programmatic guaranteed deals and preferred deals.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `authorizedbuyersmarketplace_buyers_finalized_deals_resume_execute()` to send, or `authorizedbuyersmarketplace_buyers_finalized_deals_resume` for simplest API.

pub fn authorizedbuyersmarketplace_buyers_finalized_deals_resume_builder(
    client: &SimpleHttpClient,
    name: &str,
    body: &ResumeFinalizedDealRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/{+name}:resume", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v1/{+name}:resume
/// Resumes serving of the given finalized deal. Calling this method for an running deal has no effect. If a deal is initially paused by the seller, calling this method will not resume serving of the deal until the seller also resumes the deal. This method only applies to programmatic guaranteed deals and preferred deals.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `authorizedbuyersmarketplace_buyers_finalized_deals_resume_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from authorizedbuyersmarketplace_buyers_finalized_deals_resume_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn authorizedbuyersmarketplace_buyers_finalized_deals_resume_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<FinalizedDeal>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: FinalizedDeal = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+name}:resume
/// Resumes serving of the given finalized deal. Calling this method for an running deal has no effect. If a deal is initially paused by the seller, calling this method will not resume serving of the deal until the seller also resumes the deal. This method only applies to programmatic guaranteed deals and preferred deals.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `authorizedbuyersmarketplace_buyers_finalized_deals_resume_builder()` + `authorizedbuyersmarketplace_buyers_finalized_deals_resume_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn authorizedbuyersmarketplace_buyers_finalized_deals_resume(
    client: &SimpleHttpClient,
    name: &str,
    body: &ResumeFinalizedDealRequest,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<FinalizedDeal>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        authorizedbuyersmarketplace_buyers_finalized_deals_resume_builder(client, name, body)?;
    authorizedbuyersmarketplace_buyers_finalized_deals_resume_execute(builder)
}

/// GET v1/{+deal}:setReadyToServe
/// Sets the given finalized deal as ready to serve. By default, deals are set as ready to serve as soon as they're finalized. If you want to opt out of the default behavior, and manually indicate that deals are ready to serve, ask your Technical Account Manager to add you to the allowlist. If you choose to use this method, finalized deals belonging to the bidder and its child seats don't start serving until after you call `setReadyToServe`, and after the deals become active. For example, you can use this method to delay receiving bid requests until your creative is ready. In addition, bidders can use the URL path "/v1/`bidders/{`accountId`}/`finalizedDeals`/{`dealId`}`" to set ready to serve for the finalized deals belong to itself, its child seats and all their clients. This method only applies to programmatic guaranteed deals.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `authorizedbuyersmarketplace_buyers_finalized_deals_set_ready_to_serve_execute()` to send, or `authorizedbuyersmarketplace_buyers_finalized_deals_set_ready_to_serve` for simplest API.

pub fn authorizedbuyersmarketplace_buyers_finalized_deals_set_ready_to_serve_builder(
    client: &SimpleHttpClient,
    deal: &str,
    body: &SetReadyToServeRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/{+deal}:setReadyToServe", deal,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v1/{+deal}:setReadyToServe
/// Sets the given finalized deal as ready to serve. By default, deals are set as ready to serve as soon as they're finalized. If you want to opt out of the default behavior, and manually indicate that deals are ready to serve, ask your Technical Account Manager to add you to the allowlist. If you choose to use this method, finalized deals belonging to the bidder and its child seats don't start serving until after you call `setReadyToServe`, and after the deals become active. For example, you can use this method to delay receiving bid requests until your creative is ready. In addition, bidders can use the URL path "/v1/`bidders/{`accountId`}/`finalizedDeals`/{`dealId`}`" to set ready to serve for the finalized deals belong to itself, its child seats and all their clients. This method only applies to programmatic guaranteed deals.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `authorizedbuyersmarketplace_buyers_finalized_deals_set_ready_to_serve_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from authorizedbuyersmarketplace_buyers_finalized_deals_set_ready_to_serve_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn authorizedbuyersmarketplace_buyers_finalized_deals_set_ready_to_serve_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<FinalizedDeal>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: FinalizedDeal = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+deal}:setReadyToServe
/// Sets the given finalized deal as ready to serve. By default, deals are set as ready to serve as soon as they're finalized. If you want to opt out of the default behavior, and manually indicate that deals are ready to serve, ask your Technical Account Manager to add you to the allowlist. If you choose to use this method, finalized deals belonging to the bidder and its child seats don't start serving until after you call `setReadyToServe`, and after the deals become active. For example, you can use this method to delay receiving bid requests until your creative is ready. In addition, bidders can use the URL path "/v1/`bidders/{`accountId`}/`finalizedDeals`/{`dealId`}`" to set ready to serve for the finalized deals belong to itself, its child seats and all their clients. This method only applies to programmatic guaranteed deals.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `authorizedbuyersmarketplace_buyers_finalized_deals_set_ready_to_serve_builder()` + `authorizedbuyersmarketplace_buyers_finalized_deals_set_ready_to_serve_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn authorizedbuyersmarketplace_buyers_finalized_deals_set_ready_to_serve(
    client: &SimpleHttpClient,
    deal: &str,
    body: &SetReadyToServeRequest,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<FinalizedDeal>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = authorizedbuyersmarketplace_buyers_finalized_deals_set_ready_to_serve_builder(
        client, deal, body,
    )?;
    authorizedbuyersmarketplace_buyers_finalized_deals_set_ready_to_serve_execute(builder)
}

/// GET v1/{+name}:accept
/// Accepts the proposal at the given revision number. If the revision number in the request is behind the latest from the server, an error message will be returned. This call updates the Proposal.state from BUYER_ACCEPTANCE_REQUESTED to FINALIZED; it has no side effect if the Proposal.state is already FINALIZED and throws exception if the Proposal.state is not either BUYER_ACCEPTANCE_REQUESTED or FINALIZED. Accepting a proposal means the buyer understands and accepts the Proposal.terms_and_conditions proposed by the seller.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `authorizedbuyersmarketplace_buyers_proposals_accept_execute()` to send, or `authorizedbuyersmarketplace_buyers_proposals_accept` for simplest API.

pub fn authorizedbuyersmarketplace_buyers_proposals_accept_builder(
    client: &SimpleHttpClient,
    name: &str,
    body: &AcceptProposalRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/{+name}:accept", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v1/{+name}:accept
/// Accepts the proposal at the given revision number. If the revision number in the request is behind the latest from the server, an error message will be returned. This call updates the Proposal.state from BUYER_ACCEPTANCE_REQUESTED to FINALIZED; it has no side effect if the Proposal.state is already FINALIZED and throws exception if the Proposal.state is not either BUYER_ACCEPTANCE_REQUESTED or FINALIZED. Accepting a proposal means the buyer understands and accepts the Proposal.terms_and_conditions proposed by the seller.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `authorizedbuyersmarketplace_buyers_proposals_accept_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from authorizedbuyersmarketplace_buyers_proposals_accept_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn authorizedbuyersmarketplace_buyers_proposals_accept_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Proposal>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Proposal = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+name}:accept
/// Accepts the proposal at the given revision number. If the revision number in the request is behind the latest from the server, an error message will be returned. This call updates the Proposal.state from BUYER_ACCEPTANCE_REQUESTED to FINALIZED; it has no side effect if the Proposal.state is already FINALIZED and throws exception if the Proposal.state is not either BUYER_ACCEPTANCE_REQUESTED or FINALIZED. Accepting a proposal means the buyer understands and accepts the Proposal.terms_and_conditions proposed by the seller.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `authorizedbuyersmarketplace_buyers_proposals_accept_builder()` + `authorizedbuyersmarketplace_buyers_proposals_accept_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn authorizedbuyersmarketplace_buyers_proposals_accept(
    client: &SimpleHttpClient,
    name: &str,
    body: &AcceptProposalRequest,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Proposal>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = authorizedbuyersmarketplace_buyers_proposals_accept_builder(client, name, body)?;
    authorizedbuyersmarketplace_buyers_proposals_accept_execute(builder)
}

/// GET v1/{+proposal}:addNote
/// Creates a note for this proposal and sends to the seller. This method is not supported for proposals with DealType set to 'PRIVATE_AUCTION'.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `authorizedbuyersmarketplace_buyers_proposals_add_note_execute()` to send, or `authorizedbuyersmarketplace_buyers_proposals_add_note` for simplest API.

pub fn authorizedbuyersmarketplace_buyers_proposals_add_note_builder(
    client: &SimpleHttpClient,
    proposal: &str,
    body: &AddNoteRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/{+proposal}:addNote", proposal,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v1/{+proposal}:addNote
/// Creates a note for this proposal and sends to the seller. This method is not supported for proposals with DealType set to 'PRIVATE_AUCTION'.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `authorizedbuyersmarketplace_buyers_proposals_add_note_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from authorizedbuyersmarketplace_buyers_proposals_add_note_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn authorizedbuyersmarketplace_buyers_proposals_add_note_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Proposal>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Proposal = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+proposal}:addNote
/// Creates a note for this proposal and sends to the seller. This method is not supported for proposals with DealType set to 'PRIVATE_AUCTION'.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `authorizedbuyersmarketplace_buyers_proposals_add_note_builder()` + `authorizedbuyersmarketplace_buyers_proposals_add_note_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn authorizedbuyersmarketplace_buyers_proposals_add_note(
    client: &SimpleHttpClient,
    proposal: &str,
    body: &AddNoteRequest,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Proposal>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        authorizedbuyersmarketplace_buyers_proposals_add_note_builder(client, proposal, body)?;
    authorizedbuyersmarketplace_buyers_proposals_add_note_execute(builder)
}

/// GET v1/{+proposal}:cancelNegotiation
/// Cancels an ongoing negotiation on a proposal. This does not cancel or end serving for the deals if the proposal has been finalized. If the proposal has not been finalized before, calling this method will set the Proposal.state to TERMINATED and increment the Proposal.proposal_revision. If the proposal has been finalized before and is under renegotiation now, calling this method will reset the Proposal.state to FINALIZED and increment the Proposal.proposal_revision. This method does not support private auction proposals whose Proposal.deal_type is 'PRIVATE_AUCTION'.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `authorizedbuyersmarketplace_buyers_proposals_cancel_negotiation_execute()` to send, or `authorizedbuyersmarketplace_buyers_proposals_cancel_negotiation` for simplest API.

pub fn authorizedbuyersmarketplace_buyers_proposals_cancel_negotiation_builder(
    client: &SimpleHttpClient,
    proposal: &str,
    body: &CancelNegotiationRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comv1/{+proposal}:cancelNegotiation",
        proposal,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v1/{+proposal}:cancelNegotiation
/// Cancels an ongoing negotiation on a proposal. This does not cancel or end serving for the deals if the proposal has been finalized. If the proposal has not been finalized before, calling this method will set the Proposal.state to TERMINATED and increment the Proposal.proposal_revision. If the proposal has been finalized before and is under renegotiation now, calling this method will reset the Proposal.state to FINALIZED and increment the Proposal.proposal_revision. This method does not support private auction proposals whose Proposal.deal_type is 'PRIVATE_AUCTION'.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `authorizedbuyersmarketplace_buyers_proposals_cancel_negotiation_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from authorizedbuyersmarketplace_buyers_proposals_cancel_negotiation_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn authorizedbuyersmarketplace_buyers_proposals_cancel_negotiation_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Proposal>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Proposal = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+proposal}:cancelNegotiation
/// Cancels an ongoing negotiation on a proposal. This does not cancel or end serving for the deals if the proposal has been finalized. If the proposal has not been finalized before, calling this method will set the Proposal.state to TERMINATED and increment the Proposal.proposal_revision. If the proposal has been finalized before and is under renegotiation now, calling this method will reset the Proposal.state to FINALIZED and increment the Proposal.proposal_revision. This method does not support private auction proposals whose Proposal.deal_type is 'PRIVATE_AUCTION'.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `authorizedbuyersmarketplace_buyers_proposals_cancel_negotiation_builder()` + `authorizedbuyersmarketplace_buyers_proposals_cancel_negotiation_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn authorizedbuyersmarketplace_buyers_proposals_cancel_negotiation(
    client: &SimpleHttpClient,
    proposal: &str,
    body: &CancelNegotiationRequest,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Proposal>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = authorizedbuyersmarketplace_buyers_proposals_cancel_negotiation_builder(
        client, proposal, body,
    )?;
    authorizedbuyersmarketplace_buyers_proposals_cancel_negotiation_execute(builder)
}

/// GET v1/{+name}
/// Gets a proposal using its resource name. The proposal is returned at the latest revision.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `authorizedbuyersmarketplace_buyers_proposals_get_execute()` to send, or `authorizedbuyersmarketplace_buyers_proposals_get` for simplest API.

pub fn authorizedbuyersmarketplace_buyers_proposals_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/{+name}", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+name}
/// Gets a proposal using its resource name. The proposal is returned at the latest revision.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `authorizedbuyersmarketplace_buyers_proposals_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from authorizedbuyersmarketplace_buyers_proposals_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn authorizedbuyersmarketplace_buyers_proposals_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Proposal>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Proposal = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+name}
/// Gets a proposal using its resource name. The proposal is returned at the latest revision.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `authorizedbuyersmarketplace_buyers_proposals_get_builder()` + `authorizedbuyersmarketplace_buyers_proposals_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn authorizedbuyersmarketplace_buyers_proposals_get(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Proposal>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = authorizedbuyersmarketplace_buyers_proposals_get_builder(client, name)?;
    authorizedbuyersmarketplace_buyers_proposals_get_execute(builder)
}

/// GET v1/{+parent}/proposals
/// Lists proposals. A filter expression using [Cloud API list filtering syntax](<https://developers.google.`com/authorized-buyers/apis/guides/list-filters`>) may be specified to filter the results.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `authorizedbuyersmarketplace_buyers_proposals_list_execute()` to send, or `authorizedbuyersmarketplace_buyers_proposals_list` for simplest API.

pub fn authorizedbuyersmarketplace_buyers_proposals_list_builder(
    client: &SimpleHttpClient,
    parent: &str,
    filter: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/{+parent}/proposals", parent,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "filter",
            filter
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageSize",
            pageSize
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageToken",
            pageToken
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET v1/{+parent}/proposals
/// Lists proposals. A filter expression using [Cloud API list filtering syntax](<https://developers.google.`com/authorized-buyers/apis/guides/list-filters`>) may be specified to filter the results.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `authorizedbuyersmarketplace_buyers_proposals_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from authorizedbuyersmarketplace_buyers_proposals_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn authorizedbuyersmarketplace_buyers_proposals_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListProposalsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListProposalsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+parent}/proposals
/// Lists proposals. A filter expression using [Cloud API list filtering syntax](<https://developers.google.`com/authorized-buyers/apis/guides/list-filters`>) may be specified to filter the results.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `authorizedbuyersmarketplace_buyers_proposals_list_builder()` + `authorizedbuyersmarketplace_buyers_proposals_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn authorizedbuyersmarketplace_buyers_proposals_list(
    client: &SimpleHttpClient,
    parent: &str,
    filter: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListProposalsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = authorizedbuyersmarketplace_buyers_proposals_list_builder(
        client, parent, filter, pageSize, pageToken,
    )?;
    authorizedbuyersmarketplace_buyers_proposals_list_execute(builder)
}

/// GET v1/{+name}
/// Updates the proposal at the given revision number. If the revision number in the request is behind the latest one kept in the server, an error message will be returned. See FieldMask for how to use FieldMask. Only fields specified in the UpdateProposalRequest.update_mask will be updated; Fields noted as 'Immutable' or 'Output only' yet specified in the UpdateProposalRequest.update_mask will be ignored and left unchanged. Updating a private auction proposal is only allowed for buyer private data, all other fields are immutable.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `authorizedbuyersmarketplace_buyers_proposals_patch_execute()` to send, or `authorizedbuyersmarketplace_buyers_proposals_patch` for simplest API.

pub fn authorizedbuyersmarketplace_buyers_proposals_patch_builder(
    client: &SimpleHttpClient,
    name: &str,
    updateMask: Option<&str>,
    body: &Proposal,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/{+name}", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "updateMask",
            updateMask
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v1/{+name}
/// Updates the proposal at the given revision number. If the revision number in the request is behind the latest one kept in the server, an error message will be returned. See FieldMask for how to use FieldMask. Only fields specified in the UpdateProposalRequest.update_mask will be updated; Fields noted as 'Immutable' or 'Output only' yet specified in the UpdateProposalRequest.update_mask will be ignored and left unchanged. Updating a private auction proposal is only allowed for buyer private data, all other fields are immutable.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `authorizedbuyersmarketplace_buyers_proposals_patch_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from authorizedbuyersmarketplace_buyers_proposals_patch_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn authorizedbuyersmarketplace_buyers_proposals_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Proposal>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Proposal = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+name}
/// Updates the proposal at the given revision number. If the revision number in the request is behind the latest one kept in the server, an error message will be returned. See FieldMask for how to use FieldMask. Only fields specified in the UpdateProposalRequest.update_mask will be updated; Fields noted as 'Immutable' or 'Output only' yet specified in the UpdateProposalRequest.update_mask will be ignored and left unchanged. Updating a private auction proposal is only allowed for buyer private data, all other fields are immutable.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `authorizedbuyersmarketplace_buyers_proposals_patch_builder()` + `authorizedbuyersmarketplace_buyers_proposals_patch_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn authorizedbuyersmarketplace_buyers_proposals_patch(
    client: &SimpleHttpClient,
    name: &str,
    updateMask: Option<&str>,
    body: &Proposal,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Proposal>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        authorizedbuyersmarketplace_buyers_proposals_patch_builder(client, name, updateMask, body)?;
    authorizedbuyersmarketplace_buyers_proposals_patch_execute(builder)
}

/// GET v1/{+buyer}/proposals:sendRfp
/// Sends a request for proposal (RFP) to a publisher to initiate the negotiation regarding certain inventory. In the RFP, buyers can specify the deal type, deal terms, start and end dates, targeting, and a message to the publisher. Once the RFP is sent, a proposal in SELLER_REVIEW_REQUESTED state will be created and returned in the response. The publisher may review your request and respond with detailed deals in the proposal.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `authorizedbuyersmarketplace_buyers_proposals_send_rfp_execute()` to send, or `authorizedbuyersmarketplace_buyers_proposals_send_rfp` for simplest API.

pub fn authorizedbuyersmarketplace_buyers_proposals_send_rfp_builder(
    client: &SimpleHttpClient,
    buyer: &str,
    body: &SendRfpRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comv1/{+buyer}/proposals:sendRfp",
        buyer,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v1/{+buyer}/proposals:sendRfp
/// Sends a request for proposal (RFP) to a publisher to initiate the negotiation regarding certain inventory. In the RFP, buyers can specify the deal type, deal terms, start and end dates, targeting, and a message to the publisher. Once the RFP is sent, a proposal in SELLER_REVIEW_REQUESTED state will be created and returned in the response. The publisher may review your request and respond with detailed deals in the proposal.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `authorizedbuyersmarketplace_buyers_proposals_send_rfp_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from authorizedbuyersmarketplace_buyers_proposals_send_rfp_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn authorizedbuyersmarketplace_buyers_proposals_send_rfp_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Proposal>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Proposal = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+buyer}/proposals:sendRfp
/// Sends a request for proposal (RFP) to a publisher to initiate the negotiation regarding certain inventory. In the RFP, buyers can specify the deal type, deal terms, start and end dates, targeting, and a message to the publisher. Once the RFP is sent, a proposal in SELLER_REVIEW_REQUESTED state will be created and returned in the response. The publisher may review your request and respond with detailed deals in the proposal.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `authorizedbuyersmarketplace_buyers_proposals_send_rfp_builder()` + `authorizedbuyersmarketplace_buyers_proposals_send_rfp_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn authorizedbuyersmarketplace_buyers_proposals_send_rfp(
    client: &SimpleHttpClient,
    buyer: &str,
    body: &SendRfpRequest,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Proposal>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        authorizedbuyersmarketplace_buyers_proposals_send_rfp_builder(client, buyer, body)?;
    authorizedbuyersmarketplace_buyers_proposals_send_rfp_execute(builder)
}

/// GET v1/{+parent}/deals:batchUpdate
/// Batch updates multiple deals in the same proposal.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `authorizedbuyersmarketplace_buyers_proposals_deals_batch_update_execute()` to send, or `authorizedbuyersmarketplace_buyers_proposals_deals_batch_update` for simplest API.

pub fn authorizedbuyersmarketplace_buyers_proposals_deals_batch_update_builder(
    client: &SimpleHttpClient,
    parent: &str,
    body: &BatchUpdateDealsRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comv1/{+parent}/deals:batchUpdate",
        parent,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v1/{+parent}/deals:batchUpdate
/// Batch updates multiple deals in the same proposal.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `authorizedbuyersmarketplace_buyers_proposals_deals_batch_update_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from authorizedbuyersmarketplace_buyers_proposals_deals_batch_update_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn authorizedbuyersmarketplace_buyers_proposals_deals_batch_update_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<BatchUpdateDealsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: BatchUpdateDealsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+parent}/deals:batchUpdate
/// Batch updates multiple deals in the same proposal.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `authorizedbuyersmarketplace_buyers_proposals_deals_batch_update_builder()` + `authorizedbuyersmarketplace_buyers_proposals_deals_batch_update_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn authorizedbuyersmarketplace_buyers_proposals_deals_batch_update(
    client: &SimpleHttpClient,
    parent: &str,
    body: &BatchUpdateDealsRequest,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<BatchUpdateDealsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = authorizedbuyersmarketplace_buyers_proposals_deals_batch_update_builder(
        client, parent, body,
    )?;
    authorizedbuyersmarketplace_buyers_proposals_deals_batch_update_execute(builder)
}

/// GET v1/{+name}
/// Gets a deal given its name. The deal is returned at its head revision.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `authorizedbuyersmarketplace_buyers_proposals_deals_get_execute()` to send, or `authorizedbuyersmarketplace_buyers_proposals_deals_get` for simplest API.

pub fn authorizedbuyersmarketplace_buyers_proposals_deals_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/{+name}", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+name}
/// Gets a deal given its name. The deal is returned at its head revision.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `authorizedbuyersmarketplace_buyers_proposals_deals_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from authorizedbuyersmarketplace_buyers_proposals_deals_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn authorizedbuyersmarketplace_buyers_proposals_deals_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Deal>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Deal = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+name}
/// Gets a deal given its name. The deal is returned at its head revision.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `authorizedbuyersmarketplace_buyers_proposals_deals_get_builder()` + `authorizedbuyersmarketplace_buyers_proposals_deals_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn authorizedbuyersmarketplace_buyers_proposals_deals_get(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Deal>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = authorizedbuyersmarketplace_buyers_proposals_deals_get_builder(client, name)?;
    authorizedbuyersmarketplace_buyers_proposals_deals_get_execute(builder)
}

/// GET v1/{+parent}/deals
/// Lists all deals in a proposal. To retrieve only the finalized revision deals regardless if a deal is being renegotiated, see the FinalizedDeals resource.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `authorizedbuyersmarketplace_buyers_proposals_deals_list_execute()` to send, or `authorizedbuyersmarketplace_buyers_proposals_deals_list` for simplest API.

pub fn authorizedbuyersmarketplace_buyers_proposals_deals_list_builder(
    client: &SimpleHttpClient,
    parent: &str,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/{+parent}/deals", parent,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "pageSize",
            pageSize
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageToken",
            pageToken
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET v1/{+parent}/deals
/// Lists all deals in a proposal. To retrieve only the finalized revision deals regardless if a deal is being renegotiated, see the FinalizedDeals resource.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `authorizedbuyersmarketplace_buyers_proposals_deals_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from authorizedbuyersmarketplace_buyers_proposals_deals_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn authorizedbuyersmarketplace_buyers_proposals_deals_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListDealsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListDealsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+parent}/deals
/// Lists all deals in a proposal. To retrieve only the finalized revision deals regardless if a deal is being renegotiated, see the FinalizedDeals resource.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `authorizedbuyersmarketplace_buyers_proposals_deals_list_builder()` + `authorizedbuyersmarketplace_buyers_proposals_deals_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn authorizedbuyersmarketplace_buyers_proposals_deals_list(
    client: &SimpleHttpClient,
    parent: &str,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListDealsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = authorizedbuyersmarketplace_buyers_proposals_deals_list_builder(
        client, parent, pageSize, pageToken,
    )?;
    authorizedbuyersmarketplace_buyers_proposals_deals_list_execute(builder)
}

/// GET v1/{+name}
/// Updates the given deal at the buyer known revision number. If the server revision has advanced since the passed-in proposal.proposal_revision an ABORTED error message will be returned. The revision number is incremented by the server whenever the proposal or its constituent deals are updated. Note: The revision number is kept at a proposal level. The buyer of the API is expected to keep track of the revision number after the last update operation and send it in as part of the next update request. This way, if there are further changes on the server (for example, seller making new updates), then the server can detect conflicts and reject the proposed changes.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `authorizedbuyersmarketplace_buyers_proposals_deals_patch_execute()` to send, or `authorizedbuyersmarketplace_buyers_proposals_deals_patch` for simplest API.

pub fn authorizedbuyersmarketplace_buyers_proposals_deals_patch_builder(
    client: &SimpleHttpClient,
    name: &str,
    updateMask: Option<&str>,
    body: &Deal,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/{+name}", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "updateMask",
            updateMask
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v1/{+name}
/// Updates the given deal at the buyer known revision number. If the server revision has advanced since the passed-in proposal.proposal_revision an ABORTED error message will be returned. The revision number is incremented by the server whenever the proposal or its constituent deals are updated. Note: The revision number is kept at a proposal level. The buyer of the API is expected to keep track of the revision number after the last update operation and send it in as part of the next update request. This way, if there are further changes on the server (for example, seller making new updates), then the server can detect conflicts and reject the proposed changes.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `authorizedbuyersmarketplace_buyers_proposals_deals_patch_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from authorizedbuyersmarketplace_buyers_proposals_deals_patch_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn authorizedbuyersmarketplace_buyers_proposals_deals_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Deal>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Deal = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+name}
/// Updates the given deal at the buyer known revision number. If the server revision has advanced since the passed-in proposal.proposal_revision an ABORTED error message will be returned. The revision number is incremented by the server whenever the proposal or its constituent deals are updated. Note: The revision number is kept at a proposal level. The buyer of the API is expected to keep track of the revision number after the last update operation and send it in as part of the next update request. This way, if there are further changes on the server (for example, seller making new updates), then the server can detect conflicts and reject the proposed changes.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `authorizedbuyersmarketplace_buyers_proposals_deals_patch_builder()` + `authorizedbuyersmarketplace_buyers_proposals_deals_patch_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn authorizedbuyersmarketplace_buyers_proposals_deals_patch(
    client: &SimpleHttpClient,
    name: &str,
    updateMask: Option<&str>,
    body: &Deal,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Deal>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = authorizedbuyersmarketplace_buyers_proposals_deals_patch_builder(
        client, name, updateMask, body,
    )?;
    authorizedbuyersmarketplace_buyers_proposals_deals_patch_execute(builder)
}

/// GET v1/{+name}
/// Gets the requested publisher profile by name.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `authorizedbuyersmarketplace_buyers_publisher_profiles_get_execute()` to send, or `authorizedbuyersmarketplace_buyers_publisher_profiles_get` for simplest API.

pub fn authorizedbuyersmarketplace_buyers_publisher_profiles_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/{+name}", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+name}
/// Gets the requested publisher profile by name.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `authorizedbuyersmarketplace_buyers_publisher_profiles_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from authorizedbuyersmarketplace_buyers_publisher_profiles_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn authorizedbuyersmarketplace_buyers_publisher_profiles_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<PublisherProfile>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: PublisherProfile = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+name}
/// Gets the requested publisher profile by name.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `authorizedbuyersmarketplace_buyers_publisher_profiles_get_builder()` + `authorizedbuyersmarketplace_buyers_publisher_profiles_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn authorizedbuyersmarketplace_buyers_publisher_profiles_get(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<PublisherProfile>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = authorizedbuyersmarketplace_buyers_publisher_profiles_get_builder(client, name)?;
    authorizedbuyersmarketplace_buyers_publisher_profiles_get_execute(builder)
}

/// GET v1/{+parent}/publisherProfiles
/// Lists publisher profiles. The returned publisher profiles aren't in any defined order. The order of the results might change. A new publisher profile can appear in any place in the list of returned results.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `authorizedbuyersmarketplace_buyers_publisher_profiles_list_execute()` to send, or `authorizedbuyersmarketplace_buyers_publisher_profiles_list` for simplest API.

pub fn authorizedbuyersmarketplace_buyers_publisher_profiles_list_builder(
    client: &SimpleHttpClient,
    parent: &str,
    filter: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comv1/{+parent}/publisherProfiles",
        parent,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "filter",
            filter
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageSize",
            pageSize
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageToken",
            pageToken
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET v1/{+parent}/publisherProfiles
/// Lists publisher profiles. The returned publisher profiles aren't in any defined order. The order of the results might change. A new publisher profile can appear in any place in the list of returned results.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `authorizedbuyersmarketplace_buyers_publisher_profiles_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from authorizedbuyersmarketplace_buyers_publisher_profiles_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn authorizedbuyersmarketplace_buyers_publisher_profiles_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ListPublisherProfilesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListPublisherProfilesResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+parent}/publisherProfiles
/// Lists publisher profiles. The returned publisher profiles aren't in any defined order. The order of the results might change. A new publisher profile can appear in any place in the list of returned results.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `authorizedbuyersmarketplace_buyers_publisher_profiles_list_builder()` + `authorizedbuyersmarketplace_buyers_publisher_profiles_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn authorizedbuyersmarketplace_buyers_publisher_profiles_list(
    client: &SimpleHttpClient,
    parent: &str,
    filter: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ListPublisherProfilesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = authorizedbuyersmarketplace_buyers_publisher_profiles_list_builder(
        client, parent, filter, pageSize, pageToken,
    )?;
    authorizedbuyersmarketplace_buyers_publisher_profiles_list_execute(builder)
}
