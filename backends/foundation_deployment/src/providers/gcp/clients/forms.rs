//! Auto-generated API clients for gcp/forms.
//!
//! Generated by `cargo run --bin ewe_platform gen_provider_clients`.
//! DO NOT EDIT MANUALLY.

use crate::providers::gcp::clients::types::*;
use crate::providers::gcp::resources::*;
use foundation_core::valtron::{execute, StreamIterator, StreamIteratorExt};
use foundation_core::wire::simple_http::client::{
    body_reader, ClientRequestBuilder, RequestIntro, SimpleHttpClient, SystemDnsResolver,
};

/// GET v1/forms/{formId}:batchUpdate
/// Change the form with a batch of updates.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `forms_forms_batch_update_execute()` to send, or `forms_forms_batch_update` for simplest API.

pub fn forms_forms_batch_update_builder(
    client: &SimpleHttpClient,
    formId: &str,
    body: &BatchUpdateFormRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/forms/{}:batchUpdate", formId,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v1/forms/{formId}:batchUpdate
/// Change the form with a batch of updates.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `forms_forms_batch_update_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from forms_forms_batch_update_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn forms_forms_batch_update_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<BatchUpdateFormResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: BatchUpdateFormResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/forms/{formId}:batchUpdate
/// Change the form with a batch of updates.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `forms_forms_batch_update_builder()` + `forms_forms_batch_update_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn forms_forms_batch_update(
    client: &SimpleHttpClient,
    formId: &str,
    body: &BatchUpdateFormRequest,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<BatchUpdateFormResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = forms_forms_batch_update_builder(client, formId, body)?;
    forms_forms_batch_update_execute(builder)
}

/// GET v1/forms
/// Create a new form using the title given in the provided form message in the request. *Important:* Only the form.info.title and form.info.document_title fields are copied to the new form. All other fields including the form description, items and settings are disallowed. To create a new form and add items, you must first call forms.create to create an empty form with a title and (optional) document title, and then call forms.update to add the items.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `forms_forms_create_execute()` to send, or `forms_forms_create` for simplest API.

pub fn forms_forms_create_builder(
    client: &SimpleHttpClient,
    unpublished: Option<bool>,
    body: &Form,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/forms",);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "unpublished",
            unpublished
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v1/forms
/// Create a new form using the title given in the provided form message in the request. *Important:* Only the form.info.title and form.info.document_title fields are copied to the new form. All other fields including the form description, items and settings are disallowed. To create a new form and add items, you must first call forms.create to create an empty form with a title and (optional) document title, and then call forms.update to add the items.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `forms_forms_create_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from forms_forms_create_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn forms_forms_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Form>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Form = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/forms
/// Create a new form using the title given in the provided form message in the request. *Important:* Only the form.info.title and form.info.document_title fields are copied to the new form. All other fields including the form description, items and settings are disallowed. To create a new form and add items, you must first call forms.create to create an empty form with a title and (optional) document title, and then call forms.update to add the items.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `forms_forms_create_builder()` + `forms_forms_create_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn forms_forms_create(
    client: &SimpleHttpClient,
    unpublished: Option<bool>,
    body: &Form,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Form>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = forms_forms_create_builder(client, unpublished, body)?;
    forms_forms_create_execute(builder)
}

/// GET v1/forms/{formId}
/// Get a form.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `forms_forms_get_execute()` to send, or `forms_forms_get` for simplest API.

pub fn forms_forms_get_builder(
    client: &SimpleHttpClient,
    formId: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/forms/{}", formId,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/forms/{formId}
/// Get a form.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `forms_forms_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from forms_forms_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn forms_forms_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Form>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Form = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/forms/{formId}
/// Get a form.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `forms_forms_get_builder()` + `forms_forms_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn forms_forms_get(
    client: &SimpleHttpClient,
    formId: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Form>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = forms_forms_get_builder(client, formId)?;
    forms_forms_get_execute(builder)
}

/// GET v1/forms/{formId}:setPublishSettings
/// Updates the publish settings of a form. Legacy forms aren't supported because they don't have the publish_settings field.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `forms_forms_set_publish_settings_execute()` to send, or `forms_forms_set_publish_settings` for simplest API.

pub fn forms_forms_set_publish_settings_builder(
    client: &SimpleHttpClient,
    formId: &str,
    body: &SetPublishSettingsRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comv1/forms/{}:setPublishSettings",
        formId,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v1/forms/{formId}:setPublishSettings
/// Updates the publish settings of a form. Legacy forms aren't supported because they don't have the publish_settings field.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `forms_forms_set_publish_settings_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from forms_forms_set_publish_settings_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn forms_forms_set_publish_settings_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<SetPublishSettingsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: SetPublishSettingsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/forms/{formId}:setPublishSettings
/// Updates the publish settings of a form. Legacy forms aren't supported because they don't have the publish_settings field.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `forms_forms_set_publish_settings_builder()` + `forms_forms_set_publish_settings_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn forms_forms_set_publish_settings(
    client: &SimpleHttpClient,
    formId: &str,
    body: &SetPublishSettingsRequest,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<SetPublishSettingsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = forms_forms_set_publish_settings_builder(client, formId, body)?;
    forms_forms_set_publish_settings_execute(builder)
}

/// GET v1/forms/{formId}/responses/{responseId}
/// Get one response from the form.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `forms_forms_responses_get_execute()` to send, or `forms_forms_responses_get` for simplest API.

pub fn forms_forms_responses_get_builder(
    client: &SimpleHttpClient,
    formId: &str,
    responseId: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comv1/forms/{}/responses/{}",
        formId, responseId,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/forms/{formId}/responses/{responseId}
/// Get one response from the form.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `forms_forms_responses_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from forms_forms_responses_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn forms_forms_responses_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<FormResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: FormResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/forms/{formId}/responses/{responseId}
/// Get one response from the form.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `forms_forms_responses_get_builder()` + `forms_forms_responses_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn forms_forms_responses_get(
    client: &SimpleHttpClient,
    formId: &str,
    responseId: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<FormResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = forms_forms_responses_get_builder(client, formId, responseId)?;
    forms_forms_responses_get_execute(builder)
}

/// GET v1/forms/{formId}/responses
/// List a form's responses.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `forms_forms_responses_list_execute()` to send, or `forms_forms_responses_list` for simplest API.

pub fn forms_forms_responses_list_builder(
    client: &SimpleHttpClient,
    formId: &str,
    filter: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/forms/{}/responses", formId,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "filter",
            filter
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageSize",
            pageSize
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageToken",
            pageToken
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET v1/forms/{formId}/responses
/// List a form's responses.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `forms_forms_responses_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from forms_forms_responses_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn forms_forms_responses_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListFormResponsesResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListFormResponsesResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/forms/{formId}/responses
/// List a form's responses.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `forms_forms_responses_list_builder()` + `forms_forms_responses_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn forms_forms_responses_list(
    client: &SimpleHttpClient,
    formId: &str,
    filter: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListFormResponsesResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = forms_forms_responses_list_builder(client, formId, filter, pageSize, pageToken)?;
    forms_forms_responses_list_execute(builder)
}

/// GET v1/forms/{formId}/watches
/// Create a new watch. If a watch ID is provided, it must be unused. For each invoking project, the per form limit is one watch per Watch.EventType. A watch expires seven days after it is created (see Watch.expire_time).
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `forms_forms_watches_create_execute()` to send, or `forms_forms_watches_create` for simplest API.

pub fn forms_forms_watches_create_builder(
    client: &SimpleHttpClient,
    formId: &str,
    body: &CreateWatchRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/forms/{}/watches", formId,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v1/forms/{formId}/watches
/// Create a new watch. If a watch ID is provided, it must be unused. For each invoking project, the per form limit is one watch per Watch.EventType. A watch expires seven days after it is created (see Watch.expire_time).
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `forms_forms_watches_create_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from forms_forms_watches_create_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn forms_forms_watches_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Watch>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Watch = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/forms/{formId}/watches
/// Create a new watch. If a watch ID is provided, it must be unused. For each invoking project, the per form limit is one watch per Watch.EventType. A watch expires seven days after it is created (see Watch.expire_time).
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `forms_forms_watches_create_builder()` + `forms_forms_watches_create_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn forms_forms_watches_create(
    client: &SimpleHttpClient,
    formId: &str,
    body: &CreateWatchRequest,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Watch>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = forms_forms_watches_create_builder(client, formId, body)?;
    forms_forms_watches_create_execute(builder)
}

/// GET v1/forms/{formId}/watches/{watchId}
/// Delete a watch.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `forms_forms_watches_delete_execute()` to send, or `forms_forms_watches_delete` for simplest API.

pub fn forms_forms_watches_delete_builder(
    client: &SimpleHttpClient,
    formId: &str,
    watchId: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comv1/forms/{}/watches/{}",
        formId, watchId,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/forms/{formId}/watches/{watchId}
/// Delete a watch.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `forms_forms_watches_delete_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from forms_forms_watches_delete_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn forms_forms_watches_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Empty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/forms/{formId}/watches/{watchId}
/// Delete a watch.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `forms_forms_watches_delete_builder()` + `forms_forms_watches_delete_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn forms_forms_watches_delete(
    client: &SimpleHttpClient,
    formId: &str,
    watchId: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = forms_forms_watches_delete_builder(client, formId, watchId)?;
    forms_forms_watches_delete_execute(builder)
}

/// GET v1/forms/{formId}/watches
/// Return a list of the watches owned by the invoking project. The maximum number of watches is two: For each invoker, the limit is one for each event type per form.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `forms_forms_watches_list_execute()` to send, or `forms_forms_watches_list` for simplest API.

pub fn forms_forms_watches_list_builder(
    client: &SimpleHttpClient,
    formId: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/forms/{}/watches", formId,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/forms/{formId}/watches
/// Return a list of the watches owned by the invoking project. The maximum number of watches is two: For each invoker, the limit is one for each event type per form.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `forms_forms_watches_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from forms_forms_watches_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn forms_forms_watches_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListWatchesResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListWatchesResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/forms/{formId}/watches
/// Return a list of the watches owned by the invoking project. The maximum number of watches is two: For each invoker, the limit is one for each event type per form.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `forms_forms_watches_list_builder()` + `forms_forms_watches_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn forms_forms_watches_list(
    client: &SimpleHttpClient,
    formId: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListWatchesResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = forms_forms_watches_list_builder(client, formId)?;
    forms_forms_watches_list_execute(builder)
}

/// GET v1/forms/{formId}/watches/{watchId}:renew
/// Renew an existing watch for seven days. The state of the watch after renewal is `ACTIVE`, and the expire_time is seven days from the renewal. Renewing a watch in an error state (e.g. SUSPENDED) succeeds if the error is no longer present, but fail otherwise. After a watch has expired, RenewWatch returns NOT_FOUND.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `forms_forms_watches_renew_execute()` to send, or `forms_forms_watches_renew` for simplest API.

pub fn forms_forms_watches_renew_builder(
    client: &SimpleHttpClient,
    formId: &str,
    watchId: &str,
    body: &RenewWatchRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comv1/forms/{}/watches/{}:renew",
        formId, watchId,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v1/forms/{formId}/watches/{watchId}:renew
/// Renew an existing watch for seven days. The state of the watch after renewal is `ACTIVE`, and the expire_time is seven days from the renewal. Renewing a watch in an error state (e.g. SUSPENDED) succeeds if the error is no longer present, but fail otherwise. After a watch has expired, RenewWatch returns NOT_FOUND.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `forms_forms_watches_renew_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from forms_forms_watches_renew_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn forms_forms_watches_renew_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Watch>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Watch = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/forms/{formId}/watches/{watchId}:renew
/// Renew an existing watch for seven days. The state of the watch after renewal is `ACTIVE`, and the expire_time is seven days from the renewal. Renewing a watch in an error state (e.g. SUSPENDED) succeeds if the error is no longer present, but fail otherwise. After a watch has expired, RenewWatch returns NOT_FOUND.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `forms_forms_watches_renew_builder()` + `forms_forms_watches_renew_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn forms_forms_watches_renew(
    client: &SimpleHttpClient,
    formId: &str,
    watchId: &str,
    body: &RenewWatchRequest,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Watch>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = forms_forms_watches_renew_builder(client, formId, watchId, body)?;
    forms_forms_watches_renew_execute(builder)
}
