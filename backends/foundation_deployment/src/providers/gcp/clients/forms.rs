//! Auto-generated API clients for gcp/forms.
//!
//! Generated by `cargo run --bin ewe_platform gen_provider_clients`.
//! DO NOT EDIT MANUALLY.
//!
//! Feature flag: `gcp`

#![cfg(feature = "gcp")]

use crate::providers::gcp::clients::types::*;
use crate::providers::gcp::resources::*;
use foundation_core::valtron::{
    execute, BoxedSendExecutionAction, StreamIterator, StreamIteratorExt, TaskIterator,
    TaskIteratorExt,
};
use foundation_core::wire::simple_http::client::{
    body_reader, ClientRequestBuilder, DnsResolver, RequestIntro, SimpleHttpClient,
    SystemDnsResolver,
};
use foundation_db::state::resource_identifier::ResourceIdentifier;
use foundation_macros::JsonHash;
use serde::Serialize;

/// POST v1/forms/{formId}:batchUpdate
/// Change the form with a batch of updates.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `forms_forms_batch_update_execute()` to send, or `forms_forms_batch_update` for simplest API.

pub fn forms_forms_batch_update_builder<R>(
    client: &SimpleHttpClient<R>,
    formId: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://forms.googleapis.com/v1/forms/{}:batchUpdate",
        formId,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1/forms/{formId}:batchUpdate
/// Change the form with a batch of updates.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `forms_forms_batch_update_execute()` or `forms_forms_batch_update`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `forms_forms_batch_update_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn forms_forms_batch_update_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<BatchUpdateFormResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: BatchUpdateFormResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1/forms/{formId}:batchUpdate
/// Change the form with a batch of updates.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `forms_forms_batch_update_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `forms_forms_batch_update_task()`.
/// For the simplest API, use `forms_forms_batch_update()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `forms_forms_batch_update_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn forms_forms_batch_update_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<BatchUpdateFormResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = forms_forms_batch_update_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`forms_forms_batch_update`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct FormsFormsBatchUpdateArgs {
    /// Path parameter: formId
    pub formId: String,
}

/// POST v1/forms/{formId}:batchUpdate
/// Change the form with a batch of updates.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `forms_forms_batch_update_builder()` + `forms_forms_batch_update_execute()`.
/// For task-level control, use `forms_forms_batch_update_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn forms_forms_batch_update(
    client: &SimpleHttpClient,
    args: &FormsFormsBatchUpdateArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<BatchUpdateFormResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = forms_forms_batch_update_builder(client, &args.formId)?;
    forms_forms_batch_update_execute(builder)
}

/// POST v1/forms
/// Create a new form using the title given in the provided form message in the request. *Important:* Only the form.info.title and form.info.document_title fields are copied to the new form. All other fields including the form description, items and settings are disallowed. To create a new form and add items, you must first call forms.create to create an empty form with a title and (optional) document title, and then call forms.update to add the items.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `forms_forms_create_execute()` to send, or `forms_forms_create` for simplest API.

pub fn forms_forms_create_builder<R>(
    client: &SimpleHttpClient<R>,
    unpublished: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!("https://forms.googleapis.com/v1/forms",);

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = unpublished.as_ref() {
        query_parts.push(format!("unpublished={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .post(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1/forms
/// Create a new form using the title given in the provided form message in the request. *Important:* Only the form.info.title and form.info.document_title fields are copied to the new form. All other fields including the form description, items and settings are disallowed. To create a new form and add items, you must first call forms.create to create an empty form with a title and (optional) document title, and then call forms.update to add the items.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `forms_forms_create_execute()` or `forms_forms_create`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `forms_forms_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn forms_forms_create_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Form>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Form = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1/forms
/// Create a new form using the title given in the provided form message in the request. *Important:* Only the form.info.title and form.info.document_title fields are copied to the new form. All other fields including the form description, items and settings are disallowed. To create a new form and add items, you must first call forms.create to create an empty form with a title and (optional) document title, and then call forms.update to add the items.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `forms_forms_create_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `forms_forms_create_task()`.
/// For the simplest API, use `forms_forms_create()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `forms_forms_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn forms_forms_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Form>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = forms_forms_create_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`forms_forms_create`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct FormsFormsCreateArgs {
    /// Query parameter: unpublished
    pub unpublished: Option<Option<String>>,
}

/// POST v1/forms
/// Create a new form using the title given in the provided form message in the request. *Important:* Only the form.info.title and form.info.document_title fields are copied to the new form. All other fields including the form description, items and settings are disallowed. To create a new form and add items, you must first call forms.create to create an empty form with a title and (optional) document title, and then call forms.update to add the items.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `forms_forms_create_builder()` + `forms_forms_create_execute()`.
/// For task-level control, use `forms_forms_create_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn forms_forms_create(
    client: &SimpleHttpClient,
    args: &FormsFormsCreateArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Form>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = forms_forms_create_builder(client, &args.unpublished)?;
    forms_forms_create_execute(builder)
}

/// GET v1/forms/{formId}
/// Get a form.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `forms_forms_get_execute()` to send, or `forms_forms_get` for simplest API.

pub fn forms_forms_get_builder<R>(
    client: &SimpleHttpClient<R>,
    formId: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!("https://forms.googleapis.com/v1/forms/{}", formId,);

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/forms/{formId}
/// Get a form.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `forms_forms_get_execute()` or `forms_forms_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `forms_forms_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn forms_forms_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Form>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Form = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/forms/{formId}
/// Get a form.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `forms_forms_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `forms_forms_get_task()`.
/// For the simplest API, use `forms_forms_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `forms_forms_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn forms_forms_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Form>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = forms_forms_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`forms_forms_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct FormsFormsGetArgs {
    /// Path parameter: formId
    pub formId: String,
}

/// GET v1/forms/{formId}
/// Get a form.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `forms_forms_get_builder()` + `forms_forms_get_execute()`.
/// For task-level control, use `forms_forms_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn forms_forms_get(
    client: &SimpleHttpClient,
    args: &FormsFormsGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Form>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = forms_forms_get_builder(client, &args.formId)?;
    forms_forms_get_execute(builder)
}

/// POST v1/forms/{formId}:setPublishSettings
/// Updates the publish settings of a form. Legacy forms aren''t supported because they don''t have the publish_settings field.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `forms_forms_set_publish_settings_execute()` to send, or `forms_forms_set_publish_settings` for simplest API.

pub fn forms_forms_set_publish_settings_builder<R>(
    client: &SimpleHttpClient<R>,
    formId: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://forms.googleapis.com/v1/forms/{}:setPublishSettings",
        formId,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1/forms/{formId}:setPublishSettings
/// Updates the publish settings of a form. Legacy forms aren''t supported because they don''t have the publish_settings field.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `forms_forms_set_publish_settings_execute()` or `forms_forms_set_publish_settings`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `forms_forms_set_publish_settings_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn forms_forms_set_publish_settings_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<SetPublishSettingsResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: SetPublishSettingsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1/forms/{formId}:setPublishSettings
/// Updates the publish settings of a form. Legacy forms aren''t supported because they don''t have the publish_settings field.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `forms_forms_set_publish_settings_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `forms_forms_set_publish_settings_task()`.
/// For the simplest API, use `forms_forms_set_publish_settings()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `forms_forms_set_publish_settings_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn forms_forms_set_publish_settings_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<SetPublishSettingsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = forms_forms_set_publish_settings_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`forms_forms_set_publish_settings`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct FormsFormsSetPublishSettingsArgs {
    /// Path parameter: formId
    pub formId: String,
}

/// POST v1/forms/{formId}:setPublishSettings
/// Updates the publish settings of a form. Legacy forms aren''t supported because they don''t have the publish_settings field.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `forms_forms_set_publish_settings_builder()` + `forms_forms_set_publish_settings_execute()`.
/// For task-level control, use `forms_forms_set_publish_settings_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn forms_forms_set_publish_settings(
    client: &SimpleHttpClient,
    args: &FormsFormsSetPublishSettingsArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<SetPublishSettingsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = forms_forms_set_publish_settings_builder(client, &args.formId)?;
    forms_forms_set_publish_settings_execute(builder)
}

/// GET v1/forms/{formId}/responses/{responseId}
/// Get one response from the form.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `forms_forms_responses_get_execute()` to send, or `forms_forms_responses_get` for simplest API.

pub fn forms_forms_responses_get_builder<R>(
    client: &SimpleHttpClient<R>,
    formId: &String,
    responseId: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://forms.googleapis.com/v1/forms/{}/responses/{}",
        formId, responseId,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/forms/{formId}/responses/{responseId}
/// Get one response from the form.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `forms_forms_responses_get_execute()` or `forms_forms_responses_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `forms_forms_responses_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn forms_forms_responses_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<FormResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: FormResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/forms/{formId}/responses/{responseId}
/// Get one response from the form.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `forms_forms_responses_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `forms_forms_responses_get_task()`.
/// For the simplest API, use `forms_forms_responses_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `forms_forms_responses_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn forms_forms_responses_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<FormResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = forms_forms_responses_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`forms_forms_responses_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct FormsFormsResponsesGetArgs {
    /// Path parameter: formId
    pub formId: String,
    /// Path parameter: responseId
    pub responseId: String,
}

/// GET v1/forms/{formId}/responses/{responseId}
/// Get one response from the form.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `forms_forms_responses_get_builder()` + `forms_forms_responses_get_execute()`.
/// For task-level control, use `forms_forms_responses_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn forms_forms_responses_get(
    client: &SimpleHttpClient,
    args: &FormsFormsResponsesGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<FormResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = forms_forms_responses_get_builder(client, &args.formId, &args.responseId)?;
    forms_forms_responses_get_execute(builder)
}

/// GET v1/forms/{formId}/responses
/// List a form''s responses.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `forms_forms_responses_list_execute()` to send, or `forms_forms_responses_list` for simplest API.

pub fn forms_forms_responses_list_builder<R>(
    client: &SimpleHttpClient<R>,
    formId: &String,
    filter: &Option<Option<String>>,
    pageSize: &Option<Option<String>>,
    pageToken: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!("https://forms.googleapis.com/v1/forms/{}/responses", formId,);

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter.as_ref() {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = pageSize.as_ref() {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/forms/{formId}/responses
/// List a form''s responses.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `forms_forms_responses_list_execute()` or `forms_forms_responses_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `forms_forms_responses_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn forms_forms_responses_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ListFormResponsesResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListFormResponsesResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/forms/{formId}/responses
/// List a form''s responses.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `forms_forms_responses_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `forms_forms_responses_list_task()`.
/// For the simplest API, use `forms_forms_responses_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `forms_forms_responses_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn forms_forms_responses_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListFormResponsesResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = forms_forms_responses_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`forms_forms_responses_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct FormsFormsResponsesListArgs {
    /// Path parameter: formId
    pub formId: String,
    /// Query parameter: filter
    pub filter: Option<Option<String>>,
    /// Query parameter: pageSize
    pub pageSize: Option<Option<String>>,
    /// Query parameter: pageToken
    pub pageToken: Option<Option<String>>,
}

/// GET v1/forms/{formId}/responses
/// List a form''s responses.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `forms_forms_responses_list_builder()` + `forms_forms_responses_list_execute()`.
/// For task-level control, use `forms_forms_responses_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn forms_forms_responses_list(
    client: &SimpleHttpClient,
    args: &FormsFormsResponsesListArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListFormResponsesResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = forms_forms_responses_list_builder(
        client,
        &args.formId,
        &args.filter,
        &args.pageSize,
        &args.pageToken,
    )?;
    forms_forms_responses_list_execute(builder)
}

/// POST v1/forms/{formId}/watches
/// Create a new watch. If a watch ID is provided, it must be unused. For each invoking project, the per form limit is one watch per Watch.EventType. A watch expires seven days after it is created (see Watch.expire_time).
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `forms_forms_watches_create_execute()` to send, or `forms_forms_watches_create` for simplest API.

pub fn forms_forms_watches_create_builder<R>(
    client: &SimpleHttpClient<R>,
    formId: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!("https://forms.googleapis.com/v1/forms/{}/watches", formId,);

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1/forms/{formId}/watches
/// Create a new watch. If a watch ID is provided, it must be unused. For each invoking project, the per form limit is one watch per Watch.EventType. A watch expires seven days after it is created (see Watch.expire_time).
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `forms_forms_watches_create_execute()` or `forms_forms_watches_create`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `forms_forms_watches_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn forms_forms_watches_create_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Watch>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Watch = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1/forms/{formId}/watches
/// Create a new watch. If a watch ID is provided, it must be unused. For each invoking project, the per form limit is one watch per Watch.EventType. A watch expires seven days after it is created (see Watch.expire_time).
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `forms_forms_watches_create_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `forms_forms_watches_create_task()`.
/// For the simplest API, use `forms_forms_watches_create()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `forms_forms_watches_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn forms_forms_watches_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Watch>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = forms_forms_watches_create_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`forms_forms_watches_create`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct FormsFormsWatchesCreateArgs {
    /// Path parameter: formId
    pub formId: String,
}

/// POST v1/forms/{formId}/watches
/// Create a new watch. If a watch ID is provided, it must be unused. For each invoking project, the per form limit is one watch per Watch.EventType. A watch expires seven days after it is created (see Watch.expire_time).
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `forms_forms_watches_create_builder()` + `forms_forms_watches_create_execute()`.
/// For task-level control, use `forms_forms_watches_create_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn forms_forms_watches_create(
    client: &SimpleHttpClient,
    args: &FormsFormsWatchesCreateArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Watch>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = forms_forms_watches_create_builder(client, &args.formId)?;
    forms_forms_watches_create_execute(builder)
}

/// DELETE v1/forms/{formId}/watches/{watchId}
/// Delete a watch.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `forms_forms_watches_delete_execute()` to send, or `forms_forms_watches_delete` for simplest API.

pub fn forms_forms_watches_delete_builder<R>(
    client: &SimpleHttpClient<R>,
    formId: &String,
    watchId: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://forms.googleapis.com/v1/forms/{}/watches/{}",
        formId, watchId,
    );

    // Build request
    let builder = client
        .delete(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// DELETE v1/forms/{formId}/watches/{watchId}
/// Delete a watch.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `forms_forms_watches_delete_execute()` or `forms_forms_watches_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `forms_forms_watches_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn forms_forms_watches_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Empty>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Empty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// DELETE v1/forms/{formId}/watches/{watchId}
/// Delete a watch.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `forms_forms_watches_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `forms_forms_watches_delete_task()`.
/// For the simplest API, use `forms_forms_watches_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `forms_forms_watches_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn forms_forms_watches_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = forms_forms_watches_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`forms_forms_watches_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct FormsFormsWatchesDeleteArgs {
    /// Path parameter: formId
    pub formId: String,
    /// Path parameter: watchId
    pub watchId: String,
}

/// DELETE v1/forms/{formId}/watches/{watchId}
/// Delete a watch.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `forms_forms_watches_delete_builder()` + `forms_forms_watches_delete_execute()`.
/// For task-level control, use `forms_forms_watches_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn forms_forms_watches_delete(
    client: &SimpleHttpClient,
    args: &FormsFormsWatchesDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = forms_forms_watches_delete_builder(client, &args.formId, &args.watchId)?;
    forms_forms_watches_delete_execute(builder)
}

/// GET v1/forms/{formId}/watches
/// Return a list of the watches owned by the invoking project. The maximum number of watches is two: For each invoker, the limit is one for each event type per form.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `forms_forms_watches_list_execute()` to send, or `forms_forms_watches_list` for simplest API.

pub fn forms_forms_watches_list_builder<R>(
    client: &SimpleHttpClient<R>,
    formId: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!("https://forms.googleapis.com/v1/forms/{}/watches", formId,);

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/forms/{formId}/watches
/// Return a list of the watches owned by the invoking project. The maximum number of watches is two: For each invoker, the limit is one for each event type per form.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `forms_forms_watches_list_execute()` or `forms_forms_watches_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `forms_forms_watches_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn forms_forms_watches_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ListWatchesResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListWatchesResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/forms/{formId}/watches
/// Return a list of the watches owned by the invoking project. The maximum number of watches is two: For each invoker, the limit is one for each event type per form.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `forms_forms_watches_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `forms_forms_watches_list_task()`.
/// For the simplest API, use `forms_forms_watches_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `forms_forms_watches_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn forms_forms_watches_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListWatchesResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = forms_forms_watches_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`forms_forms_watches_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct FormsFormsWatchesListArgs {
    /// Path parameter: formId
    pub formId: String,
}

/// GET v1/forms/{formId}/watches
/// Return a list of the watches owned by the invoking project. The maximum number of watches is two: For each invoker, the limit is one for each event type per form.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `forms_forms_watches_list_builder()` + `forms_forms_watches_list_execute()`.
/// For task-level control, use `forms_forms_watches_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn forms_forms_watches_list(
    client: &SimpleHttpClient,
    args: &FormsFormsWatchesListArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListWatchesResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = forms_forms_watches_list_builder(client, &args.formId)?;
    forms_forms_watches_list_execute(builder)
}

/// POST v1/forms/{formId}/watches/{watchId}:renew
/// Renew an existing watch for seven days. The state of the watch after renewal is `ACTIVE`, and the expire_time is seven days from the renewal. Renewing a watch in an error state (e.g. SUSPENDED) succeeds if the error is no longer present, but fail otherwise. After a watch has expired, RenewWatch returns NOT_FOUND.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `forms_forms_watches_renew_execute()` to send, or `forms_forms_watches_renew` for simplest API.

pub fn forms_forms_watches_renew_builder<R>(
    client: &SimpleHttpClient<R>,
    formId: &String,
    watchId: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://forms.googleapis.com/v1/forms/{}/watches/{}:renew",
        formId, watchId,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1/forms/{formId}/watches/{watchId}:renew
/// Renew an existing watch for seven days. The state of the watch after renewal is `ACTIVE`, and the expire_time is seven days from the renewal. Renewing a watch in an error state (e.g. SUSPENDED) succeeds if the error is no longer present, but fail otherwise. After a watch has expired, RenewWatch returns NOT_FOUND.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `forms_forms_watches_renew_execute()` or `forms_forms_watches_renew`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `forms_forms_watches_renew_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn forms_forms_watches_renew_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Watch>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Watch = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1/forms/{formId}/watches/{watchId}:renew
/// Renew an existing watch for seven days. The state of the watch after renewal is `ACTIVE`, and the expire_time is seven days from the renewal. Renewing a watch in an error state (e.g. SUSPENDED) succeeds if the error is no longer present, but fail otherwise. After a watch has expired, RenewWatch returns NOT_FOUND.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `forms_forms_watches_renew_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `forms_forms_watches_renew_task()`.
/// For the simplest API, use `forms_forms_watches_renew()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `forms_forms_watches_renew_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn forms_forms_watches_renew_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Watch>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = forms_forms_watches_renew_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`forms_forms_watches_renew`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct FormsFormsWatchesRenewArgs {
    /// Path parameter: formId
    pub formId: String,
    /// Path parameter: watchId
    pub watchId: String,
}

/// POST v1/forms/{formId}/watches/{watchId}:renew
/// Renew an existing watch for seven days. The state of the watch after renewal is `ACTIVE`, and the expire_time is seven days from the renewal. Renewing a watch in an error state (e.g. SUSPENDED) succeeds if the error is no longer present, but fail otherwise. After a watch has expired, RenewWatch returns NOT_FOUND.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `forms_forms_watches_renew_builder()` + `forms_forms_watches_renew_execute()`.
/// For task-level control, use `forms_forms_watches_renew_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn forms_forms_watches_renew(
    client: &SimpleHttpClient,
    args: &FormsFormsWatchesRenewArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Watch>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = forms_forms_watches_renew_builder(client, &args.formId, &args.watchId)?;
    forms_forms_watches_renew_execute(builder)
}

// =============================================================================
// ResourceIdentifier implementation for BatchUpdateFormResponse
// =============================================================================

/// ResourceIdentifier implementation for BatchUpdateFormResponse with FormsFormsBatchUpdateArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<FormsFormsBatchUpdateArgs> for BatchUpdateFormResponse {
    fn generate_resource_id(&self, input: &FormsFormsBatchUpdateArgs) -> String {
        format!("gcp::forms::BatchUpdateFormResponse/{}", input.formId)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::forms::BatchUpdateFormResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Form
// =============================================================================

/// ResourceIdentifier implementation for Form with FormsFormsCreateArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<FormsFormsCreateArgs> for Form {
    fn generate_resource_id(&self, input: &FormsFormsCreateArgs) -> String {
        "gcp::forms::Form".to_string()
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::forms::Form"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Form
// =============================================================================

/// ResourceIdentifier implementation for Form with FormsFormsGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<FormsFormsGetArgs> for Form {
    fn generate_resource_id(&self, input: &FormsFormsGetArgs) -> String {
        format!("gcp::forms::Form/{}", input.formId)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::forms::Form"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for SetPublishSettingsResponse
// =============================================================================

/// ResourceIdentifier implementation for SetPublishSettingsResponse with FormsFormsSetPublishSettingsArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<FormsFormsSetPublishSettingsArgs> for SetPublishSettingsResponse {
    fn generate_resource_id(&self, input: &FormsFormsSetPublishSettingsArgs) -> String {
        format!("gcp::forms::SetPublishSettingsResponse/{}", input.formId)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::forms::SetPublishSettingsResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for FormResponse
// =============================================================================

/// ResourceIdentifier implementation for FormResponse with FormsFormsResponsesGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<FormsFormsResponsesGetArgs> for FormResponse {
    fn generate_resource_id(&self, input: &FormsFormsResponsesGetArgs) -> String {
        format!(
            "gcp::forms::FormResponse/{}/{}",
            input.formId, input.responseId
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::forms::FormResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for ListFormResponsesResponse
// =============================================================================

/// ResourceIdentifier implementation for ListFormResponsesResponse with FormsFormsResponsesListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<FormsFormsResponsesListArgs> for ListFormResponsesResponse {
    fn generate_resource_id(&self, input: &FormsFormsResponsesListArgs) -> String {
        format!("gcp::forms::ListFormResponsesResponse/{}", input.formId)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::forms::ListFormResponsesResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Watch
// =============================================================================

/// ResourceIdentifier implementation for Watch with FormsFormsWatchesCreateArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<FormsFormsWatchesCreateArgs> for Watch {
    fn generate_resource_id(&self, input: &FormsFormsWatchesCreateArgs) -> String {
        format!("gcp::forms::Watch/{}", input.formId)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::forms::Watch"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Empty
// =============================================================================

/// ResourceIdentifier implementation for Empty with FormsFormsWatchesDeleteArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<FormsFormsWatchesDeleteArgs> for Empty {
    fn generate_resource_id(&self, input: &FormsFormsWatchesDeleteArgs) -> String {
        format!("gcp::forms::Empty/{}/{}", input.formId, input.watchId)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::forms::Empty"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for ListWatchesResponse
// =============================================================================

/// ResourceIdentifier implementation for ListWatchesResponse with FormsFormsWatchesListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<FormsFormsWatchesListArgs> for ListWatchesResponse {
    fn generate_resource_id(&self, input: &FormsFormsWatchesListArgs) -> String {
        format!("gcp::forms::ListWatchesResponse/{}", input.formId)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::forms::ListWatchesResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Watch
// =============================================================================

/// ResourceIdentifier implementation for Watch with FormsFormsWatchesRenewArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<FormsFormsWatchesRenewArgs> for Watch {
    fn generate_resource_id(&self, input: &FormsFormsWatchesRenewArgs) -> String {
        format!("gcp::forms::Watch/{}/{}", input.formId, input.watchId)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::forms::Watch"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}
