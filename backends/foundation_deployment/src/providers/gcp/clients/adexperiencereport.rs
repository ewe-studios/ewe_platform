//! Auto-generated API clients for gcp/adexperiencereport.
//!
//! Generated by `cargo run --bin ewe_platform gen_provider_clients`.
//! DO NOT EDIT MANUALLY.

use crate::providers::gcp::clients::types::*;
use crate::providers::gcp::resources::*;
use foundation_core::valtron::{execute, StreamIterator, StreamIteratorExt};
use foundation_core::wire::simple_http::client::{
    body_reader, ClientRequestBuilder, RequestIntro, SimpleHttpClient, SystemDnsResolver,
};

/// GET v1/{+name}
/// Gets a site's Ad Experience Report summary.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `adexperiencereport_sites_get_execute()` to send, or `adexperiencereport_sites_get` for simplest API.

pub fn adexperiencereport_sites_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/{+name}", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+name}
/// Gets a site's Ad Experience Report summary.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `adexperiencereport_sites_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from adexperiencereport_sites_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn adexperiencereport_sites_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SiteSummaryResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: SiteSummaryResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+name}
/// Gets a site's Ad Experience Report summary.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `adexperiencereport_sites_get_builder()` + `adexperiencereport_sites_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn adexperiencereport_sites_get(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SiteSummaryResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = adexperiencereport_sites_get_builder(client, name)?;
    adexperiencereport_sites_get_execute(builder)
}

/// GET v1/violatingSites
/// Lists sites that are failing in the Ad Experience Report on at least one platform.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `adexperiencereport_violating_sites_list_execute()` to send, or `adexperiencereport_violating_sites_list` for simplest API.

pub fn adexperiencereport_violating_sites_list_builder(
    client: &SimpleHttpClient,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/violatingSites",);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/violatingSites
/// Lists sites that are failing in the Ad Experience Report on at least one platform.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `adexperiencereport_violating_sites_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from adexperiencereport_violating_sites_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn adexperiencereport_violating_sites_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ViolatingSitesResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ViolatingSitesResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/violatingSites
/// Lists sites that are failing in the Ad Experience Report on at least one platform.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `adexperiencereport_violating_sites_list_builder()` + `adexperiencereport_violating_sites_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn adexperiencereport_violating_sites_list(
    client: &SimpleHttpClient,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ViolatingSitesResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = adexperiencereport_violating_sites_list_builder(client)?;
    adexperiencereport_violating_sites_list_execute(builder)
}
