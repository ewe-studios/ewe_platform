//! Auto-generated API clients for gcp/gamesConfiguration.
//!
//! Generated by `cargo run --bin ewe_platform gen_provider_clients`.
//! DO NOT EDIT MANUALLY.
//!
//! Feature flag: `gcp`

#![cfg(feature = "gcp")]

pub mod types;

use crate::providers::gcp::clients::types::*;
use crate::providers::gcp::resources::*;
use foundation_core::valtron::{
    execute, BoxedSendExecutionAction, StreamIterator, StreamIteratorExt, TaskIterator,
    TaskIteratorExt,
};
use foundation_core::wire::simple_http::client::{
    body_reader, ClientRequestBuilder, RequestIntro, SimpleHttpClient, SystemDnsResolver,
};
use foundation_macros::JsonHash;
use serde::Serialize;

/// GET games/v1configuration/achievements/{achievementId}
/// Delete the achievement configuration with the given ID.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `games_configuration_achievement_configurations_delete_execute()` to send, or `games_configuration_achievement_configurations_delete` for simplest API.

pub fn games_configuration_achievement_configurations_delete_builder(
    client: &SimpleHttpClient,
    achievementId: &String,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://gamesconfiguration.googleapis.com/games/v1configuration/achievements/{}",
        achievementId,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET games/v1configuration/achievements/{achievementId}
/// Delete the achievement configuration with the given ID.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `games_configuration_achievement_configurations_delete_execute()` or `games_configuration_achievement_configurations_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `games_configuration_achievement_configurations_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn games_configuration_achievement_configurations_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<()>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET games/v1configuration/achievements/{achievementId}
/// Delete the achievement configuration with the given ID.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `games_configuration_achievement_configurations_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `games_configuration_achievement_configurations_delete_task()`.
/// For the simplest API, use `games_configuration_achievement_configurations_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `games_configuration_achievement_configurations_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn games_configuration_achievement_configurations_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = games_configuration_achievement_configurations_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`games_configuration_achievement_configurations_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GamesConfigurationAchievementConfigurationsDeleteArgs {
    /// Path parameter: achievementId
    pub achievementId: String,
}

/// GET games/v1configuration/achievements/{achievementId}
/// Delete the achievement configuration with the given ID.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `games_configuration_achievement_configurations_delete_builder()` + `games_configuration_achievement_configurations_delete_execute()`.
/// For task-level control, use `games_configuration_achievement_configurations_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn games_configuration_achievement_configurations_delete(
    client: &SimpleHttpClient,
    args: &GamesConfigurationAchievementConfigurationsDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        games_configuration_achievement_configurations_delete_builder(client, &args.achievementId)?;
    games_configuration_achievement_configurations_delete_execute(builder)
}

/// GET games/v1configuration/applications/{applicationId}/achievements
/// Insert a new achievement configuration in this application.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `games_configuration_achievement_configurations_insert_execute()` to send, or `games_configuration_achievement_configurations_insert` for simplest API.

pub fn games_configuration_achievement_configurations_insert_builder(
    client: &SimpleHttpClient,
    applicationId: &String,
    body: &AchievementConfiguration,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://gamesconfiguration.googleapis.com/games/v1configuration/applications/{}/achievements",
        applicationId,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET games/v1configuration/applications/{applicationId}/achievements
/// Insert a new achievement configuration in this application.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `games_configuration_achievement_configurations_insert_execute()` or `games_configuration_achievement_configurations_insert`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `games_configuration_achievement_configurations_insert_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn games_configuration_achievement_configurations_insert_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<AchievementConfiguration>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: AchievementConfiguration = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET games/v1configuration/applications/{applicationId}/achievements
/// Insert a new achievement configuration in this application.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `games_configuration_achievement_configurations_insert_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `games_configuration_achievement_configurations_insert_task()`.
/// For the simplest API, use `games_configuration_achievement_configurations_insert()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `games_configuration_achievement_configurations_insert_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn games_configuration_achievement_configurations_insert_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<AchievementConfiguration>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = games_configuration_achievement_configurations_insert_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`games_configuration_achievement_configurations_insert`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GamesConfigurationAchievementConfigurationsInsertArgs {
    /// Path parameter: applicationId
    pub applicationId: String,
    /// Request body.
    pub body: AchievementConfiguration,
}

/// GET games/v1configuration/applications/{applicationId}/achievements
/// Insert a new achievement configuration in this application.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `games_configuration_achievement_configurations_insert_builder()` + `games_configuration_achievement_configurations_insert_execute()`.
/// For task-level control, use `games_configuration_achievement_configurations_insert_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn games_configuration_achievement_configurations_insert(
    client: &SimpleHttpClient,
    args: &GamesConfigurationAchievementConfigurationsInsertArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<AchievementConfiguration>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = games_configuration_achievement_configurations_insert_builder(
        client,
        &args.applicationId,
        &args.body,
    )?;
    games_configuration_achievement_configurations_insert_execute(builder)
}

/// GET games/v1configuration/leaderboards/{leaderboardId}
/// Delete the leaderboard configuration with the given ID.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `games_configuration_leaderboard_configurations_delete_execute()` to send, or `games_configuration_leaderboard_configurations_delete` for simplest API.

pub fn games_configuration_leaderboard_configurations_delete_builder(
    client: &SimpleHttpClient,
    leaderboardId: &String,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://gamesconfiguration.googleapis.com/games/v1configuration/leaderboards/{}",
        leaderboardId,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET games/v1configuration/leaderboards/{leaderboardId}
/// Delete the leaderboard configuration with the given ID.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `games_configuration_leaderboard_configurations_delete_execute()` or `games_configuration_leaderboard_configurations_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `games_configuration_leaderboard_configurations_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn games_configuration_leaderboard_configurations_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<()>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET games/v1configuration/leaderboards/{leaderboardId}
/// Delete the leaderboard configuration with the given ID.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `games_configuration_leaderboard_configurations_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `games_configuration_leaderboard_configurations_delete_task()`.
/// For the simplest API, use `games_configuration_leaderboard_configurations_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `games_configuration_leaderboard_configurations_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn games_configuration_leaderboard_configurations_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = games_configuration_leaderboard_configurations_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`games_configuration_leaderboard_configurations_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GamesConfigurationLeaderboardConfigurationsDeleteArgs {
    /// Path parameter: leaderboardId
    pub leaderboardId: String,
}

/// GET games/v1configuration/leaderboards/{leaderboardId}
/// Delete the leaderboard configuration with the given ID.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `games_configuration_leaderboard_configurations_delete_builder()` + `games_configuration_leaderboard_configurations_delete_execute()`.
/// For task-level control, use `games_configuration_leaderboard_configurations_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn games_configuration_leaderboard_configurations_delete(
    client: &SimpleHttpClient,
    args: &GamesConfigurationLeaderboardConfigurationsDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        games_configuration_leaderboard_configurations_delete_builder(client, &args.leaderboardId)?;
    games_configuration_leaderboard_configurations_delete_execute(builder)
}

/// GET games/v1configuration/applications/{applicationId}/leaderboards
/// Insert a new leaderboard configuration in this application.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `games_configuration_leaderboard_configurations_insert_execute()` to send, or `games_configuration_leaderboard_configurations_insert` for simplest API.

pub fn games_configuration_leaderboard_configurations_insert_builder(
    client: &SimpleHttpClient,
    applicationId: &String,
    body: &LeaderboardConfiguration,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://gamesconfiguration.googleapis.com/games/v1configuration/applications/{}/leaderboards",
        applicationId,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET games/v1configuration/applications/{applicationId}/leaderboards
/// Insert a new leaderboard configuration in this application.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `games_configuration_leaderboard_configurations_insert_execute()` or `games_configuration_leaderboard_configurations_insert`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `games_configuration_leaderboard_configurations_insert_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn games_configuration_leaderboard_configurations_insert_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<LeaderboardConfiguration>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: LeaderboardConfiguration = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET games/v1configuration/applications/{applicationId}/leaderboards
/// Insert a new leaderboard configuration in this application.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `games_configuration_leaderboard_configurations_insert_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `games_configuration_leaderboard_configurations_insert_task()`.
/// For the simplest API, use `games_configuration_leaderboard_configurations_insert()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `games_configuration_leaderboard_configurations_insert_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn games_configuration_leaderboard_configurations_insert_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<LeaderboardConfiguration>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = games_configuration_leaderboard_configurations_insert_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`games_configuration_leaderboard_configurations_insert`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GamesConfigurationLeaderboardConfigurationsInsertArgs {
    /// Path parameter: applicationId
    pub applicationId: String,
    /// Request body.
    pub body: LeaderboardConfiguration,
}

/// GET games/v1configuration/applications/{applicationId}/leaderboards
/// Insert a new leaderboard configuration in this application.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `games_configuration_leaderboard_configurations_insert_builder()` + `games_configuration_leaderboard_configurations_insert_execute()`.
/// For task-level control, use `games_configuration_leaderboard_configurations_insert_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn games_configuration_leaderboard_configurations_insert(
    client: &SimpleHttpClient,
    args: &GamesConfigurationLeaderboardConfigurationsInsertArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<LeaderboardConfiguration>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = games_configuration_leaderboard_configurations_insert_builder(
        client,
        &args.applicationId,
        &args.body,
    )?;
    games_configuration_leaderboard_configurations_insert_execute(builder)
}
