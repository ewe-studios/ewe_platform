//! Auto-generated API clients for gcp/gamesConfiguration.
//!
//! Generated by `cargo run --bin ewe_platform gen_provider_clients`.
//! DO NOT EDIT MANUALLY.
//!
//! Feature flag: `gcp/gamesConfiguration`

#![cfg(feature = "gcp/gamesConfiguration")]

pub mod types;

use crate::providers::gcp::clients::types::*;
use crate::providers::gcp::resources::*;
use foundation_core::valtron::{execute, StreamIterator, StreamIteratorExt, TaskIteratorExt};
use foundation_core::wire::simple_http::client::{
    body_reader, ClientRequestBuilder, RequestIntro, SimpleHttpClient, SystemDnsResolver,
};

/// GET games/v1configuration/achievements/{achievementId}
/// Delete the achievement configuration with the given ID.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `games_configuration_achievement_configurations_delete_execute()` to send, or `games_configuration_achievement_configurations_delete` for simplest API.

pub fn games_configuration_achievement_configurations_delete_builder(
    client: &SimpleHttpClient,
    achievementId: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://gamesconfiguration.googleapis.com/games/v1configuration/achievements/{}",
        achievementId,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET games/v1configuration/achievements/{achievementId}
/// Delete the achievement configuration with the given ID.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `games_configuration_achievement_configurations_delete_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from games_configuration_achievement_configurations_delete_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn games_configuration_achievement_configurations_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET games/v1configuration/achievements/{achievementId}
/// Delete the achievement configuration with the given ID.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `games_configuration_achievement_configurations_delete_builder()` + `games_configuration_achievement_configurations_delete_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn games_configuration_achievement_configurations_delete(
    client: &SimpleHttpClient,
    achievementId: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        games_configuration_achievement_configurations_delete_builder(client, achievementId)?;
    games_configuration_achievement_configurations_delete_execute(builder)
}

/// GET games/v1configuration/achievements/{achievementId}
/// Retrieves the metadata of the achievement configuration with the given ID.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `games_configuration_achievement_configurations_get_execute()` to send, or `games_configuration_achievement_configurations_get` for simplest API.

pub fn games_configuration_achievement_configurations_get_builder(
    client: &SimpleHttpClient,
    achievementId: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://gamesconfiguration.googleapis.com/games/v1configuration/achievements/{}",
        achievementId,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET games/v1configuration/achievements/{achievementId}
/// Retrieves the metadata of the achievement configuration with the given ID.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `games_configuration_achievement_configurations_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from games_configuration_achievement_configurations_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn games_configuration_achievement_configurations_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<AchievementConfiguration>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: AchievementConfiguration = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET games/v1configuration/achievements/{achievementId}
/// Retrieves the metadata of the achievement configuration with the given ID.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `games_configuration_achievement_configurations_get_builder()` + `games_configuration_achievement_configurations_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn games_configuration_achievement_configurations_get(
    client: &SimpleHttpClient,
    achievementId: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<AchievementConfiguration>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        games_configuration_achievement_configurations_get_builder(client, achievementId)?;
    games_configuration_achievement_configurations_get_execute(builder)
}

/// GET games/v1configuration/applications/{applicationId}/achievements
/// Insert a new achievement configuration in this application.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `games_configuration_achievement_configurations_insert_execute()` to send, or `games_configuration_achievement_configurations_insert` for simplest API.

pub fn games_configuration_achievement_configurations_insert_builder(
    client: &SimpleHttpClient,
    applicationId: &str,
    body: &AchievementConfiguration,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://gamesconfiguration.googleapis.com/games/v1configuration/applications/{}/achievements",
        applicationId,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET games/v1configuration/applications/{applicationId}/achievements
/// Insert a new achievement configuration in this application.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `games_configuration_achievement_configurations_insert_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from games_configuration_achievement_configurations_insert_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn games_configuration_achievement_configurations_insert_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<AchievementConfiguration>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: AchievementConfiguration = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET games/v1configuration/applications/{applicationId}/achievements
/// Insert a new achievement configuration in this application.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `games_configuration_achievement_configurations_insert_builder()` + `games_configuration_achievement_configurations_insert_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn games_configuration_achievement_configurations_insert(
    client: &SimpleHttpClient,
    applicationId: &str,
    body: &AchievementConfiguration,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<AchievementConfiguration>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        games_configuration_achievement_configurations_insert_builder(client, applicationId, body)?;
    games_configuration_achievement_configurations_insert_execute(builder)
}

/// GET games/v1configuration/applications/{applicationId}/achievements
/// Returns a list of the achievement configurations in this application.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `games_configuration_achievement_configurations_list_execute()` to send, or `games_configuration_achievement_configurations_list` for simplest API.

pub fn games_configuration_achievement_configurations_list_builder(
    client: &SimpleHttpClient,
    applicationId: &str,
    maxResults: Option<i32>,
    pageToken: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://gamesconfiguration.googleapis.com/games/v1configuration/applications/{}/achievements",
        applicationId,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = maxResults {
        query_parts.push(format!("maxResults={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET games/v1configuration/applications/{applicationId}/achievements
/// Returns a list of the achievement configurations in this application.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `games_configuration_achievement_configurations_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from games_configuration_achievement_configurations_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn games_configuration_achievement_configurations_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<AchievementConfigurationListResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: AchievementConfigurationListResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET games/v1configuration/applications/{applicationId}/achievements
/// Returns a list of the achievement configurations in this application.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `games_configuration_achievement_configurations_list_builder()` + `games_configuration_achievement_configurations_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn games_configuration_achievement_configurations_list(
    client: &SimpleHttpClient,
    applicationId: &str,
    maxResults: Option<i32>,
    pageToken: Option<&str>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<AchievementConfigurationListResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = games_configuration_achievement_configurations_list_builder(
        client,
        applicationId,
        maxResults,
        pageToken,
    )?;
    games_configuration_achievement_configurations_list_execute(builder)
}

/// GET games/v1configuration/achievements/{achievementId}
/// Update the metadata of the achievement configuration with the given ID.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `games_configuration_achievement_configurations_update_execute()` to send, or `games_configuration_achievement_configurations_update` for simplest API.

pub fn games_configuration_achievement_configurations_update_builder(
    client: &SimpleHttpClient,
    achievementId: &str,
    body: &AchievementConfiguration,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://gamesconfiguration.googleapis.com/games/v1configuration/achievements/{}",
        achievementId,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET games/v1configuration/achievements/{achievementId}
/// Update the metadata of the achievement configuration with the given ID.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `games_configuration_achievement_configurations_update_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from games_configuration_achievement_configurations_update_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn games_configuration_achievement_configurations_update_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<AchievementConfiguration>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: AchievementConfiguration = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET games/v1configuration/achievements/{achievementId}
/// Update the metadata of the achievement configuration with the given ID.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `games_configuration_achievement_configurations_update_builder()` + `games_configuration_achievement_configurations_update_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn games_configuration_achievement_configurations_update(
    client: &SimpleHttpClient,
    achievementId: &str,
    body: &AchievementConfiguration,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<AchievementConfiguration>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        games_configuration_achievement_configurations_update_builder(client, achievementId, body)?;
    games_configuration_achievement_configurations_update_execute(builder)
}

/// GET games/v1configuration/leaderboards/{leaderboardId}
/// Delete the leaderboard configuration with the given ID.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `games_configuration_leaderboard_configurations_delete_execute()` to send, or `games_configuration_leaderboard_configurations_delete` for simplest API.

pub fn games_configuration_leaderboard_configurations_delete_builder(
    client: &SimpleHttpClient,
    leaderboardId: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://gamesconfiguration.googleapis.com/games/v1configuration/leaderboards/{}",
        leaderboardId,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET games/v1configuration/leaderboards/{leaderboardId}
/// Delete the leaderboard configuration with the given ID.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `games_configuration_leaderboard_configurations_delete_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from games_configuration_leaderboard_configurations_delete_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn games_configuration_leaderboard_configurations_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET games/v1configuration/leaderboards/{leaderboardId}
/// Delete the leaderboard configuration with the given ID.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `games_configuration_leaderboard_configurations_delete_builder()` + `games_configuration_leaderboard_configurations_delete_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn games_configuration_leaderboard_configurations_delete(
    client: &SimpleHttpClient,
    leaderboardId: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        games_configuration_leaderboard_configurations_delete_builder(client, leaderboardId)?;
    games_configuration_leaderboard_configurations_delete_execute(builder)
}

/// GET games/v1configuration/leaderboards/{leaderboardId}
/// Retrieves the metadata of the leaderboard configuration with the given ID.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `games_configuration_leaderboard_configurations_get_execute()` to send, or `games_configuration_leaderboard_configurations_get` for simplest API.

pub fn games_configuration_leaderboard_configurations_get_builder(
    client: &SimpleHttpClient,
    leaderboardId: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://gamesconfiguration.googleapis.com/games/v1configuration/leaderboards/{}",
        leaderboardId,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET games/v1configuration/leaderboards/{leaderboardId}
/// Retrieves the metadata of the leaderboard configuration with the given ID.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `games_configuration_leaderboard_configurations_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from games_configuration_leaderboard_configurations_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn games_configuration_leaderboard_configurations_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<LeaderboardConfiguration>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: LeaderboardConfiguration = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET games/v1configuration/leaderboards/{leaderboardId}
/// Retrieves the metadata of the leaderboard configuration with the given ID.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `games_configuration_leaderboard_configurations_get_builder()` + `games_configuration_leaderboard_configurations_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn games_configuration_leaderboard_configurations_get(
    client: &SimpleHttpClient,
    leaderboardId: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<LeaderboardConfiguration>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        games_configuration_leaderboard_configurations_get_builder(client, leaderboardId)?;
    games_configuration_leaderboard_configurations_get_execute(builder)
}

/// GET games/v1configuration/applications/{applicationId}/leaderboards
/// Insert a new leaderboard configuration in this application.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `games_configuration_leaderboard_configurations_insert_execute()` to send, or `games_configuration_leaderboard_configurations_insert` for simplest API.

pub fn games_configuration_leaderboard_configurations_insert_builder(
    client: &SimpleHttpClient,
    applicationId: &str,
    body: &LeaderboardConfiguration,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://gamesconfiguration.googleapis.com/games/v1configuration/applications/{}/leaderboards",
        applicationId,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET games/v1configuration/applications/{applicationId}/leaderboards
/// Insert a new leaderboard configuration in this application.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `games_configuration_leaderboard_configurations_insert_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from games_configuration_leaderboard_configurations_insert_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn games_configuration_leaderboard_configurations_insert_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<LeaderboardConfiguration>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: LeaderboardConfiguration = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET games/v1configuration/applications/{applicationId}/leaderboards
/// Insert a new leaderboard configuration in this application.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `games_configuration_leaderboard_configurations_insert_builder()` + `games_configuration_leaderboard_configurations_insert_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn games_configuration_leaderboard_configurations_insert(
    client: &SimpleHttpClient,
    applicationId: &str,
    body: &LeaderboardConfiguration,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<LeaderboardConfiguration>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        games_configuration_leaderboard_configurations_insert_builder(client, applicationId, body)?;
    games_configuration_leaderboard_configurations_insert_execute(builder)
}

/// GET games/v1configuration/applications/{applicationId}/leaderboards
/// Returns a list of the leaderboard configurations in this application.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `games_configuration_leaderboard_configurations_list_execute()` to send, or `games_configuration_leaderboard_configurations_list` for simplest API.

pub fn games_configuration_leaderboard_configurations_list_builder(
    client: &SimpleHttpClient,
    applicationId: &str,
    maxResults: Option<i32>,
    pageToken: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://gamesconfiguration.googleapis.com/games/v1configuration/applications/{}/leaderboards",
        applicationId,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = maxResults {
        query_parts.push(format!("maxResults={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET games/v1configuration/applications/{applicationId}/leaderboards
/// Returns a list of the leaderboard configurations in this application.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `games_configuration_leaderboard_configurations_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from games_configuration_leaderboard_configurations_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn games_configuration_leaderboard_configurations_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<LeaderboardConfigurationListResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: LeaderboardConfigurationListResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET games/v1configuration/applications/{applicationId}/leaderboards
/// Returns a list of the leaderboard configurations in this application.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `games_configuration_leaderboard_configurations_list_builder()` + `games_configuration_leaderboard_configurations_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn games_configuration_leaderboard_configurations_list(
    client: &SimpleHttpClient,
    applicationId: &str,
    maxResults: Option<i32>,
    pageToken: Option<&str>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<LeaderboardConfigurationListResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = games_configuration_leaderboard_configurations_list_builder(
        client,
        applicationId,
        maxResults,
        pageToken,
    )?;
    games_configuration_leaderboard_configurations_list_execute(builder)
}

/// GET games/v1configuration/leaderboards/{leaderboardId}
/// Update the metadata of the leaderboard configuration with the given ID.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `games_configuration_leaderboard_configurations_update_execute()` to send, or `games_configuration_leaderboard_configurations_update` for simplest API.

pub fn games_configuration_leaderboard_configurations_update_builder(
    client: &SimpleHttpClient,
    leaderboardId: &str,
    body: &LeaderboardConfiguration,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://gamesconfiguration.googleapis.com/games/v1configuration/leaderboards/{}",
        leaderboardId,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET games/v1configuration/leaderboards/{leaderboardId}
/// Update the metadata of the leaderboard configuration with the given ID.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `games_configuration_leaderboard_configurations_update_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from games_configuration_leaderboard_configurations_update_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn games_configuration_leaderboard_configurations_update_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<LeaderboardConfiguration>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: LeaderboardConfiguration = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET games/v1configuration/leaderboards/{leaderboardId}
/// Update the metadata of the leaderboard configuration with the given ID.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `games_configuration_leaderboard_configurations_update_builder()` + `games_configuration_leaderboard_configurations_update_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn games_configuration_leaderboard_configurations_update(
    client: &SimpleHttpClient,
    leaderboardId: &str,
    body: &LeaderboardConfiguration,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<LeaderboardConfiguration>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        games_configuration_leaderboard_configurations_update_builder(client, leaderboardId, body)?;
    games_configuration_leaderboard_configurations_update_execute(builder)
}
