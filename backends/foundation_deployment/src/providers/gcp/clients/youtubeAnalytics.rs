//! Auto-generated API clients for gcp/youtubeAnalytics.
//!
//! Generated by `cargo run --bin ewe_platform gen_provider_clients`.
//! DO NOT EDIT MANUALLY.
//!
//! Feature flag: `gcp`

#![cfg(feature = "gcp")]

pub mod types;

use crate::providers::gcp::clients::types::*;
use crate::providers::gcp::resources::*;
use foundation_core::valtron::{
    execute, BoxedSendExecutionAction, StreamIterator, StreamIteratorExt, TaskIterator,
    TaskIteratorExt,
};
use foundation_core::wire::simple_http::client::{
    body_reader, ClientRequestBuilder, RequestIntro, SimpleHttpClient, SystemDnsResolver,
};
use foundation_macros::JsonHash;
use serde::Serialize;

/// GET v2/groupItems
/// Removes an item from a group.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `youtube_analytics_group_items_delete_execute()` to send, or `youtube_analytics_group_items_delete` for simplest API.

pub fn youtube_analytics_group_items_delete_builder(
    client: &SimpleHttpClient,
    id: Option<String>,
    onBehalfOfContentOwner: Option<String>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!("https://youtubeanalytics.googleapis.com/v2/groupItems",);

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = id {
        query_parts.push(format!("id={}", val));
    }
    if let Some(val) = onBehalfOfContentOwner {
        query_parts.push(format!("onBehalfOfContentOwner={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v2/groupItems
/// Removes an item from a group.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `youtube_analytics_group_items_delete_execute()` or `youtube_analytics_group_items_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `youtube_analytics_group_items_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn youtube_analytics_group_items_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<EmptyResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: EmptyResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v2/groupItems
/// Removes an item from a group.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `youtube_analytics_group_items_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `youtube_analytics_group_items_delete_task()`.
/// For the simplest API, use `youtube_analytics_group_items_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `youtube_analytics_group_items_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn youtube_analytics_group_items_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<EmptyResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = youtube_analytics_group_items_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`youtube_analytics_group_items_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct YoutubeAnalyticsGroupItemsDeleteArgs {
    /// Query parameter: id
    pub id: Option<String>,
    /// Query parameter: onBehalfOfContentOwner
    pub onBehalfOfContentOwner: Option<String>,
}

/// GET v2/groupItems
/// Removes an item from a group.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `youtube_analytics_group_items_delete_builder()` + `youtube_analytics_group_items_delete_execute()`.
/// For task-level control, use `youtube_analytics_group_items_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn youtube_analytics_group_items_delete(
    client: &SimpleHttpClient,
    args: &YoutubeAnalyticsGroupItemsDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<EmptyResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = youtube_analytics_group_items_delete_builder(
        client,
        args.id.clone(),
        args.onBehalfOfContentOwner.clone(),
    )?;
    youtube_analytics_group_items_delete_execute(builder)
}

/// GET v2/groups
/// Deletes a group.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `youtube_analytics_groups_delete_execute()` to send, or `youtube_analytics_groups_delete` for simplest API.

pub fn youtube_analytics_groups_delete_builder(
    client: &SimpleHttpClient,
    id: Option<String>,
    onBehalfOfContentOwner: Option<String>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!("https://youtubeanalytics.googleapis.com/v2/groups",);

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = id {
        query_parts.push(format!("id={}", val));
    }
    if let Some(val) = onBehalfOfContentOwner {
        query_parts.push(format!("onBehalfOfContentOwner={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v2/groups
/// Deletes a group.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `youtube_analytics_groups_delete_execute()` or `youtube_analytics_groups_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `youtube_analytics_groups_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn youtube_analytics_groups_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<EmptyResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: EmptyResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v2/groups
/// Deletes a group.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `youtube_analytics_groups_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `youtube_analytics_groups_delete_task()`.
/// For the simplest API, use `youtube_analytics_groups_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `youtube_analytics_groups_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn youtube_analytics_groups_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<EmptyResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = youtube_analytics_groups_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`youtube_analytics_groups_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct YoutubeAnalyticsGroupsDeleteArgs {
    /// Query parameter: id
    pub id: Option<String>,
    /// Query parameter: onBehalfOfContentOwner
    pub onBehalfOfContentOwner: Option<String>,
}

/// GET v2/groups
/// Deletes a group.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `youtube_analytics_groups_delete_builder()` + `youtube_analytics_groups_delete_execute()`.
/// For task-level control, use `youtube_analytics_groups_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn youtube_analytics_groups_delete(
    client: &SimpleHttpClient,
    args: &YoutubeAnalyticsGroupsDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<EmptyResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = youtube_analytics_groups_delete_builder(
        client,
        args.id.clone(),
        args.onBehalfOfContentOwner.clone(),
    )?;
    youtube_analytics_groups_delete_execute(builder)
}

/// GET v2/reports
/// Retrieve your YouTube Analytics reports.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `youtube_analytics_reports_query_execute()` to send, or `youtube_analytics_reports_query` for simplest API.

pub fn youtube_analytics_reports_query_builder(
    client: &SimpleHttpClient,
    currency: Option<String>,
    dimensions: Option<String>,
    endDate: Option<String>,
    filters: Option<String>,
    ids: Option<String>,
    includeHistoricalChannelData: Option<bool>,
    maxResults: Option<i32>,
    metrics: Option<String>,
    sort: Option<String>,
    startDate: Option<String>,
    startIndex: Option<i32>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!("https://youtubeanalytics.googleapis.com/v2/reports",);

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = currency {
        query_parts.push(format!("currency={}", val));
    }
    if let Some(val) = dimensions {
        query_parts.push(format!("dimensions={}", val));
    }
    if let Some(val) = endDate {
        query_parts.push(format!("endDate={}", val));
    }
    if let Some(val) = filters {
        query_parts.push(format!("filters={}", val));
    }
    if let Some(val) = ids {
        query_parts.push(format!("ids={}", val));
    }
    if let Some(val) = includeHistoricalChannelData {
        query_parts.push(format!("includeHistoricalChannelData={}", val));
    }
    if let Some(val) = maxResults {
        query_parts.push(format!("maxResults={}", val));
    }
    if let Some(val) = metrics {
        query_parts.push(format!("metrics={}", val));
    }
    if let Some(val) = sort {
        query_parts.push(format!("sort={}", val));
    }
    if let Some(val) = startDate {
        query_parts.push(format!("startDate={}", val));
    }
    if let Some(val) = startIndex {
        query_parts.push(format!("startIndex={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v2/reports
/// Retrieve your YouTube Analytics reports.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `youtube_analytics_reports_query_execute()` or `youtube_analytics_reports_query`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `youtube_analytics_reports_query_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn youtube_analytics_reports_query_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<QueryResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: QueryResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v2/reports
/// Retrieve your YouTube Analytics reports.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `youtube_analytics_reports_query_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `youtube_analytics_reports_query_task()`.
/// For the simplest API, use `youtube_analytics_reports_query()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `youtube_analytics_reports_query_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn youtube_analytics_reports_query_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<QueryResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = youtube_analytics_reports_query_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`youtube_analytics_reports_query`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct YoutubeAnalyticsReportsQueryArgs {
    /// Query parameter: currency
    pub currency: Option<String>,
    /// Query parameter: dimensions
    pub dimensions: Option<String>,
    /// Query parameter: endDate
    pub endDate: Option<String>,
    /// Query parameter: filters
    pub filters: Option<String>,
    /// Query parameter: ids
    pub ids: Option<String>,
    /// Query parameter: includeHistoricalChannelData
    pub includeHistoricalChannelData: Option<bool>,
    /// Query parameter: maxResults
    pub maxResults: Option<i32>,
    /// Query parameter: metrics
    pub metrics: Option<String>,
    /// Query parameter: sort
    pub sort: Option<String>,
    /// Query parameter: startDate
    pub startDate: Option<String>,
    /// Query parameter: startIndex
    pub startIndex: Option<i32>,
}

/// GET v2/reports
/// Retrieve your YouTube Analytics reports.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `youtube_analytics_reports_query_builder()` + `youtube_analytics_reports_query_execute()`.
/// For task-level control, use `youtube_analytics_reports_query_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn youtube_analytics_reports_query(
    client: &SimpleHttpClient,
    args: &YoutubeAnalyticsReportsQueryArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<QueryResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = youtube_analytics_reports_query_builder(
        client,
        args.currency.clone(),
        args.dimensions.clone(),
        args.endDate.clone(),
        args.filters.clone(),
        args.ids.clone(),
        args.includeHistoricalChannelData.clone(),
        args.maxResults.clone(),
        args.metrics.clone(),
        args.sort.clone(),
        args.startDate.clone(),
        args.startIndex.clone(),
    )?;
    youtube_analytics_reports_query_execute(builder)
}
