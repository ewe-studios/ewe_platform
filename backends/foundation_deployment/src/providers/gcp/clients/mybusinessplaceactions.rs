//! Auto-generated API clients for gcp/mybusinessplaceactions.
//!
//! Generated by `cargo run --bin ewe_platform gen_provider_clients`.
//! DO NOT EDIT MANUALLY.
//!
//! Feature flag: `gcp/mybusinessplaceactions`

#![cfg(feature = "gcp/mybusinessplaceactions")]

pub mod types;

use crate::providers::gcp::clients::types::*;
use crate::providers::gcp::resources::*;
use foundation_core::valtron::{execute, StreamIterator, StreamIteratorExt, TaskIteratorExt};
use foundation_core::wire::simple_http::client::{
    body_reader, ClientRequestBuilder, RequestIntro, SimpleHttpClient, SystemDnsResolver,
};
use foundation_macros::JsonHash;
use serde::Serialize;

/// GET v1/locations/{locationsId}/placeActionLinks
/// Creates a place action link associated with the specified location, and returns it. The request is considered duplicate if the parent, place_action_link.uri and place_action_link.place_action_type are the same as a previous request.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `mybusinessplaceactions_locations_place_action_links_create_execute()` to send, or `mybusinessplaceactions_locations_place_action_links_create` for simplest API.

pub fn mybusinessplaceactions_locations_place_action_links_create_builder(
    client: &SimpleHttpClient,
    parent: &str,
    body: &PlaceActionLink,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://mybusinessplaceactions.googleapis.com/v1/locations/{}/placeActionLinks",
        parent,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/locations/{locationsId}/placeActionLinks
/// Creates a place action link associated with the specified location, and returns it. The request is considered duplicate if the parent, place_action_link.uri and place_action_link.place_action_type are the same as a previous request.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `mybusinessplaceactions_locations_place_action_links_create_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from mybusinessplaceactions_locations_place_action_links_create_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn mybusinessplaceactions_locations_place_action_links_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<PlaceActionLink>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: PlaceActionLink = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`mybusinessplaceactions_locations_place_action_links_create`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct MybusinessplaceactionsLocationsPlaceActionLinksCreateArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Request body.
    pub body: PlaceActionLink,
}

/// GET v1/locations/{locationsId}/placeActionLinks
/// Creates a place action link associated with the specified location, and returns it. The request is considered duplicate if the parent, place_action_link.uri and place_action_link.place_action_type are the same as a previous request.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `mybusinessplaceactions_locations_place_action_links_create_builder()` + `mybusinessplaceactions_locations_place_action_links_create_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn mybusinessplaceactions_locations_place_action_links_create(
    client: &SimpleHttpClient,
    args: &MybusinessplaceactionsLocationsPlaceActionLinksCreateArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<PlaceActionLink>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = mybusinessplaceactions_locations_place_action_links_create_builder(
        client,
        &args.parent,
        &args.body,
    )?;
    mybusinessplaceactions_locations_place_action_links_create_execute(builder)
}

/// GET v1/locations/{locationsId}/placeActionLinks/{placeActionLinksId}
/// Deletes a place action link from the specified location.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `mybusinessplaceactions_locations_place_action_links_delete_execute()` to send, or `mybusinessplaceactions_locations_place_action_links_delete` for simplest API.

pub fn mybusinessplaceactions_locations_place_action_links_delete_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://mybusinessplaceactions.googleapis.com/v1/locations/{}/placeActionLinks/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/locations/{locationsId}/placeActionLinks/{placeActionLinksId}
/// Deletes a place action link from the specified location.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `mybusinessplaceactions_locations_place_action_links_delete_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from mybusinessplaceactions_locations_place_action_links_delete_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn mybusinessplaceactions_locations_place_action_links_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Empty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`mybusinessplaceactions_locations_place_action_links_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct MybusinessplaceactionsLocationsPlaceActionLinksDeleteArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/locations/{locationsId}/placeActionLinks/{placeActionLinksId}
/// Deletes a place action link from the specified location.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `mybusinessplaceactions_locations_place_action_links_delete_builder()` + `mybusinessplaceactions_locations_place_action_links_delete_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn mybusinessplaceactions_locations_place_action_links_delete(
    client: &SimpleHttpClient,
    args: &MybusinessplaceactionsLocationsPlaceActionLinksDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        mybusinessplaceactions_locations_place_action_links_delete_builder(client, &args.name)?;
    mybusinessplaceactions_locations_place_action_links_delete_execute(builder)
}

/// GET v1/locations/{locationsId}/placeActionLinks/{placeActionLinksId}
/// Gets the specified place action link.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `mybusinessplaceactions_locations_place_action_links_get_execute()` to send, or `mybusinessplaceactions_locations_place_action_links_get` for simplest API.

pub fn mybusinessplaceactions_locations_place_action_links_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://mybusinessplaceactions.googleapis.com/v1/locations/{}/placeActionLinks/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/locations/{locationsId}/placeActionLinks/{placeActionLinksId}
/// Gets the specified place action link.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `mybusinessplaceactions_locations_place_action_links_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from mybusinessplaceactions_locations_place_action_links_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn mybusinessplaceactions_locations_place_action_links_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<PlaceActionLink>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: PlaceActionLink = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`mybusinessplaceactions_locations_place_action_links_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct MybusinessplaceactionsLocationsPlaceActionLinksGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/locations/{locationsId}/placeActionLinks/{placeActionLinksId}
/// Gets the specified place action link.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `mybusinessplaceactions_locations_place_action_links_get_builder()` + `mybusinessplaceactions_locations_place_action_links_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn mybusinessplaceactions_locations_place_action_links_get(
    client: &SimpleHttpClient,
    args: &MybusinessplaceactionsLocationsPlaceActionLinksGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<PlaceActionLink>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        mybusinessplaceactions_locations_place_action_links_get_builder(client, &args.name)?;
    mybusinessplaceactions_locations_place_action_links_get_execute(builder)
}

/// GET v1/locations/{locationsId}/placeActionLinks
/// Lists the place action links for the specified location.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `mybusinessplaceactions_locations_place_action_links_list_execute()` to send, or `mybusinessplaceactions_locations_place_action_links_list` for simplest API.

pub fn mybusinessplaceactions_locations_place_action_links_list_builder(
    client: &SimpleHttpClient,
    parent: &str,
    filter: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://mybusinessplaceactions.googleapis.com/v1/locations/{}/placeActionLinks",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/locations/{locationsId}/placeActionLinks
/// Lists the place action links for the specified location.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `mybusinessplaceactions_locations_place_action_links_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from mybusinessplaceactions_locations_place_action_links_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn mybusinessplaceactions_locations_place_action_links_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ListPlaceActionLinksResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListPlaceActionLinksResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`mybusinessplaceactions_locations_place_action_links_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct MybusinessplaceactionsLocationsPlaceActionLinksListArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: filter
    pub filter: Option<String>,
    /// Query parameter: pageSize
    pub pageSize: Option<i32>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
}

/// GET v1/locations/{locationsId}/placeActionLinks
/// Lists the place action links for the specified location.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `mybusinessplaceactions_locations_place_action_links_list_builder()` + `mybusinessplaceactions_locations_place_action_links_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn mybusinessplaceactions_locations_place_action_links_list(
    client: &SimpleHttpClient,
    args: &MybusinessplaceactionsLocationsPlaceActionLinksListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ListPlaceActionLinksResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = mybusinessplaceactions_locations_place_action_links_list_builder(
        client,
        &args.parent,
        args.filter.as_deref(),
        args.pageSize,
        args.pageToken.as_deref(),
    )?;
    mybusinessplaceactions_locations_place_action_links_list_execute(builder)
}

/// GET v1/locations/{locationsId}/placeActionLinks/{placeActionLinksId}
/// Updates the specified place action link and returns it.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `mybusinessplaceactions_locations_place_action_links_patch_execute()` to send, or `mybusinessplaceactions_locations_place_action_links_patch` for simplest API.

pub fn mybusinessplaceactions_locations_place_action_links_patch_builder(
    client: &SimpleHttpClient,
    name: &str,
    updateMask: Option<&str>,
    body: &PlaceActionLink,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://mybusinessplaceactions.googleapis.com/v1/locations/{}/placeActionLinks/{}",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = updateMask {
        query_parts.push(format!("updateMask={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/locations/{locationsId}/placeActionLinks/{placeActionLinksId}
/// Updates the specified place action link and returns it.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `mybusinessplaceactions_locations_place_action_links_patch_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from mybusinessplaceactions_locations_place_action_links_patch_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn mybusinessplaceactions_locations_place_action_links_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<PlaceActionLink>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: PlaceActionLink = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`mybusinessplaceactions_locations_place_action_links_patch`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct MybusinessplaceactionsLocationsPlaceActionLinksPatchArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: updateMask
    pub updateMask: Option<String>,
    /// Request body.
    pub body: PlaceActionLink,
}

/// GET v1/locations/{locationsId}/placeActionLinks/{placeActionLinksId}
/// Updates the specified place action link and returns it.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `mybusinessplaceactions_locations_place_action_links_patch_builder()` + `mybusinessplaceactions_locations_place_action_links_patch_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn mybusinessplaceactions_locations_place_action_links_patch(
    client: &SimpleHttpClient,
    args: &MybusinessplaceactionsLocationsPlaceActionLinksPatchArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<PlaceActionLink>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = mybusinessplaceactions_locations_place_action_links_patch_builder(
        client,
        &args.name,
        args.updateMask.as_deref(),
        &args.body,
    )?;
    mybusinessplaceactions_locations_place_action_links_patch_execute(builder)
}

/// GET v1/placeActionTypeMetadata
/// Returns the list of available place action types for a location or country.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `mybusinessplaceactions_place_action_type_metadata_list_execute()` to send, or `mybusinessplaceactions_place_action_type_metadata_list` for simplest API.

pub fn mybusinessplaceactions_place_action_type_metadata_list_builder(
    client: &SimpleHttpClient,
    filter: Option<&str>,
    languageCode: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://mybusinessplaceactions.googleapis.com/v1/placeActionTypeMetadata",);

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = languageCode {
        query_parts.push(format!("languageCode={}", val));
    }
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/placeActionTypeMetadata
/// Returns the list of available place action types for a location or country.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `mybusinessplaceactions_place_action_type_metadata_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from mybusinessplaceactions_place_action_type_metadata_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn mybusinessplaceactions_place_action_type_metadata_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ListPlaceActionTypeMetadataResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListPlaceActionTypeMetadataResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`mybusinessplaceactions_place_action_type_metadata_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct MybusinessplaceactionsPlaceActionTypeMetadataListArgs {
    /// Query parameter: filter
    pub filter: Option<String>,
    /// Query parameter: languageCode
    pub languageCode: Option<String>,
    /// Query parameter: pageSize
    pub pageSize: Option<i32>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
}

/// GET v1/placeActionTypeMetadata
/// Returns the list of available place action types for a location or country.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `mybusinessplaceactions_place_action_type_metadata_list_builder()` + `mybusinessplaceactions_place_action_type_metadata_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn mybusinessplaceactions_place_action_type_metadata_list(
    client: &SimpleHttpClient,
    args: &MybusinessplaceactionsPlaceActionTypeMetadataListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ListPlaceActionTypeMetadataResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = mybusinessplaceactions_place_action_type_metadata_list_builder(
        client,
        args.filter.as_deref(),
        args.languageCode.as_deref(),
        args.pageSize,
        args.pageToken.as_deref(),
    )?;
    mybusinessplaceactions_place_action_type_metadata_list_execute(builder)
}
