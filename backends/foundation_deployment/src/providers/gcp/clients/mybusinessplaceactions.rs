//! Auto-generated API clients for gcp/mybusinessplaceactions.
//!
//! Generated by `cargo run --bin ewe_platform gen_provider_clients`.
//! DO NOT EDIT MANUALLY.
//!
//! Feature flag: `gcp`

#![cfg(feature = "gcp")]

pub mod types;

use crate::providers::gcp::clients::types::*;
use crate::providers::gcp::resources::*;
use foundation_core::valtron::{
    execute, BoxedSendExecutionAction, StreamIterator, StreamIteratorExt, TaskIterator,
    TaskIteratorExt,
};
use foundation_core::wire::simple_http::client::{
    body_reader, ClientRequestBuilder, RequestIntro, SimpleHttpClient, SystemDnsResolver,
};
use foundation_macros::JsonHash;
use serde::Serialize;

/// GET v1/locations/{locationsId}/placeActionLinks
/// Creates a place action link associated with the specified location, and returns it. The request is considered duplicate if the parent, place_action_link.uri and place_action_link.place_action_type are the same as a previous request.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `mybusinessplaceactions_locations_place_action_links_create_execute()` to send, or `mybusinessplaceactions_locations_place_action_links_create` for simplest API.

pub fn mybusinessplaceactions_locations_place_action_links_create_builder(
    client: &SimpleHttpClient,
    parent: &String,
    body: &PlaceActionLink,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url =
        format!("https://mybusinessplaceactions.googleapis.com/v1/locations/{}/placeActionLinks",);

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/locations/{locationsId}/placeActionLinks
/// Creates a place action link associated with the specified location, and returns it. The request is considered duplicate if the parent, place_action_link.uri and place_action_link.place_action_type are the same as a previous request.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `mybusinessplaceactions_locations_place_action_links_create_execute()` or `mybusinessplaceactions_locations_place_action_links_create`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `mybusinessplaceactions_locations_place_action_links_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn mybusinessplaceactions_locations_place_action_links_create_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<PlaceActionLink>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: PlaceActionLink = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/locations/{locationsId}/placeActionLinks
/// Creates a place action link associated with the specified location, and returns it. The request is considered duplicate if the parent, place_action_link.uri and place_action_link.place_action_type are the same as a previous request.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `mybusinessplaceactions_locations_place_action_links_create_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `mybusinessplaceactions_locations_place_action_links_create_task()`.
/// For the simplest API, use `mybusinessplaceactions_locations_place_action_links_create()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `mybusinessplaceactions_locations_place_action_links_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn mybusinessplaceactions_locations_place_action_links_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<PlaceActionLink>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = mybusinessplaceactions_locations_place_action_links_create_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`mybusinessplaceactions_locations_place_action_links_create`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct MybusinessplaceactionsLocationsPlaceActionLinksCreateArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Request body.
    pub body: PlaceActionLink,
}

/// GET v1/locations/{locationsId}/placeActionLinks
/// Creates a place action link associated with the specified location, and returns it. The request is considered duplicate if the parent, place_action_link.uri and place_action_link.place_action_type are the same as a previous request.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `mybusinessplaceactions_locations_place_action_links_create_builder()` + `mybusinessplaceactions_locations_place_action_links_create_execute()`.
/// For task-level control, use `mybusinessplaceactions_locations_place_action_links_create_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn mybusinessplaceactions_locations_place_action_links_create(
    client: &SimpleHttpClient,
    args: &MybusinessplaceactionsLocationsPlaceActionLinksCreateArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<PlaceActionLink>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = mybusinessplaceactions_locations_place_action_links_create_builder(
        client,
        &args.parent,
        &args.body,
    )?;
    mybusinessplaceactions_locations_place_action_links_create_execute(builder)
}

/// GET v1/placeActionTypeMetadata
/// Returns the list of available place action types for a location or country.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `mybusinessplaceactions_place_action_type_metadata_list_execute()` to send, or `mybusinessplaceactions_place_action_type_metadata_list` for simplest API.

pub fn mybusinessplaceactions_place_action_type_metadata_list_builder(
    client: &SimpleHttpClient,
    filter: &Option<String>,
    languageCode: &Option<String>,
    pageSize: &Option<i32>,
    pageToken: &Option<String>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url =
        format!("https://mybusinessplaceactions.googleapis.com/v1/placeActionTypeMetadata",);

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter.as_ref() {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = languageCode.as_ref() {
        query_parts.push(format!("languageCode={}", val));
    }
    if let Some(val) = pageSize.as_ref() {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/placeActionTypeMetadata
/// Returns the list of available place action types for a location or country.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `mybusinessplaceactions_place_action_type_metadata_list_execute()` or `mybusinessplaceactions_place_action_type_metadata_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `mybusinessplaceactions_place_action_type_metadata_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn mybusinessplaceactions_place_action_type_metadata_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ListPlaceActionTypeMetadataResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListPlaceActionTypeMetadataResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/placeActionTypeMetadata
/// Returns the list of available place action types for a location or country.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `mybusinessplaceactions_place_action_type_metadata_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `mybusinessplaceactions_place_action_type_metadata_list_task()`.
/// For the simplest API, use `mybusinessplaceactions_place_action_type_metadata_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `mybusinessplaceactions_place_action_type_metadata_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn mybusinessplaceactions_place_action_type_metadata_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ListPlaceActionTypeMetadataResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = mybusinessplaceactions_place_action_type_metadata_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`mybusinessplaceactions_place_action_type_metadata_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct MybusinessplaceactionsPlaceActionTypeMetadataListArgs {
    /// Query parameter: filter
    pub filter: Option<String>,
    /// Query parameter: languageCode
    pub languageCode: Option<String>,
    /// Query parameter: pageSize
    pub pageSize: Option<i32>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
}

/// GET v1/placeActionTypeMetadata
/// Returns the list of available place action types for a location or country.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `mybusinessplaceactions_place_action_type_metadata_list_builder()` + `mybusinessplaceactions_place_action_type_metadata_list_execute()`.
/// For task-level control, use `mybusinessplaceactions_place_action_type_metadata_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn mybusinessplaceactions_place_action_type_metadata_list(
    client: &SimpleHttpClient,
    args: &MybusinessplaceactionsPlaceActionTypeMetadataListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ListPlaceActionTypeMetadataResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = mybusinessplaceactions_place_action_type_metadata_list_builder(
        client,
        &args.filter,
        &args.languageCode,
        &args.pageSize,
        &args.pageToken,
    )?;
    mybusinessplaceactions_place_action_type_metadata_list_execute(builder)
}
