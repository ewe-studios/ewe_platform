//! Auto-generated API clients for gcp/searchads360.
//!
//! Generated by `cargo run --bin ewe_platform gen_provider_clients`.
//! DO NOT EDIT MANUALLY.
//!
//! Feature flag: `gcp`

#![cfg(feature = "gcp")]

use crate::providers::gcp::clients::types::*;
use crate::providers::gcp::resources::*;
use foundation_core::valtron::{
    execute, BoxedSendExecutionAction, StreamIterator, StreamIteratorExt, TaskIterator,
    TaskIteratorExt,
};
use foundation_core::wire::simple_http::client::{
    body_reader, ClientRequestBuilder, DnsResolver, RequestIntro, SimpleHttpClient,
    SystemDnsResolver,
};
use foundation_db::state::resource_identifier::ResourceIdentifier;
use foundation_macros::JsonHash;
use serde::Serialize;

/// GET v0/customers:listAccessibleCustomers
/// Returns resource names of customers directly accessible by the user authenticating the call. List of thrown errors: [AuthenticationError]() [AuthorizationError]() [HeaderError]() [InternalError]() [QuotaError]() [RequestError]()
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `searchads360_customers_list_accessible_customers_execute()` to send, or `searchads360_customers_list_accessible_customers` for simplest API.

pub fn searchads360_customers_list_accessible_customers_builder<R>(
    client: &SimpleHttpClient<R>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url =
        format!("https://searchads360.googleapis.com/v0/customers:listAccessibleCustomers",);

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v0/customers:listAccessibleCustomers
/// Returns resource names of customers directly accessible by the user authenticating the call. List of thrown errors: [AuthenticationError]() [AuthorizationError]() [HeaderError]() [InternalError]() [QuotaError]() [RequestError]()
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `searchads360_customers_list_accessible_customers_execute()` or `searchads360_customers_list_accessible_customers`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `searchads360_customers_list_accessible_customers_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn searchads360_customers_list_accessible_customers_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<GoogleAdsSearchads360V0ServicesListAccessibleCustomersResponse>,
                ApiError,
            >,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleAdsSearchads360V0ServicesListAccessibleCustomersResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v0/customers:listAccessibleCustomers
/// Returns resource names of customers directly accessible by the user authenticating the call. List of thrown errors: [AuthenticationError]() [AuthorizationError]() [HeaderError]() [InternalError]() [QuotaError]() [RequestError]()
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `searchads360_customers_list_accessible_customers_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `searchads360_customers_list_accessible_customers_task()`.
/// For the simplest API, use `searchads360_customers_list_accessible_customers()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `searchads360_customers_list_accessible_customers_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn searchads360_customers_list_accessible_customers_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleAdsSearchads360V0ServicesListAccessibleCustomersResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = searchads360_customers_list_accessible_customers_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v0/customers:listAccessibleCustomers
/// Returns resource names of customers directly accessible by the user authenticating the call. List of thrown errors: [AuthenticationError]() [AuthorizationError]() [HeaderError]() [InternalError]() [QuotaError]() [RequestError]()
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `searchads360_customers_list_accessible_customers_builder()` + `searchads360_customers_list_accessible_customers_execute()`.
/// For task-level control, use `searchads360_customers_list_accessible_customers_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn searchads360_customers_list_accessible_customers(
    client: &SimpleHttpClient,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleAdsSearchads360V0ServicesListAccessibleCustomersResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = searchads360_customers_list_accessible_customers_builder(client)?;
    searchads360_customers_list_accessible_customers_execute(builder)
}

/// GET v0/customers/{customersId}/customColumns/{customColumnsId}
/// Returns the requested custom column in full detail.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `searchads360_customers_custom_columns_get_execute()` to send, or `searchads360_customers_custom_columns_get` for simplest API.

pub fn searchads360_customers_custom_columns_get_builder<R>(
    client: &SimpleHttpClient<R>,
    resourceName: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://searchads360.googleapis.com/v0/customers/{}/customColumns/{customColumnsId}",
        resourceName,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v0/customers/{customersId}/customColumns/{customColumnsId}
/// Returns the requested custom column in full detail.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `searchads360_customers_custom_columns_get_execute()` or `searchads360_customers_custom_columns_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `searchads360_customers_custom_columns_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn searchads360_customers_custom_columns_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleAdsSearchads360V0ResourcesCustomColumn>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleAdsSearchads360V0ResourcesCustomColumn =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v0/customers/{customersId}/customColumns/{customColumnsId}
/// Returns the requested custom column in full detail.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `searchads360_customers_custom_columns_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `searchads360_customers_custom_columns_get_task()`.
/// For the simplest API, use `searchads360_customers_custom_columns_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `searchads360_customers_custom_columns_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn searchads360_customers_custom_columns_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleAdsSearchads360V0ResourcesCustomColumn>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = searchads360_customers_custom_columns_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`searchads360_customers_custom_columns_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct Searchads360CustomersCustomColumnsGetArgs {
    /// Path parameter: resourceName
    pub resourceName: String,
}

/// GET v0/customers/{customersId}/customColumns/{customColumnsId}
/// Returns the requested custom column in full detail.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `searchads360_customers_custom_columns_get_builder()` + `searchads360_customers_custom_columns_get_execute()`.
/// For task-level control, use `searchads360_customers_custom_columns_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn searchads360_customers_custom_columns_get(
    client: &SimpleHttpClient,
    args: &Searchads360CustomersCustomColumnsGetArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleAdsSearchads360V0ResourcesCustomColumn>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = searchads360_customers_custom_columns_get_builder(client, &args.resourceName)?;
    searchads360_customers_custom_columns_get_execute(builder)
}

/// GET v0/customers/{customersId}/customColumns
/// Returns all the custom columns associated with the customer in full detail.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `searchads360_customers_custom_columns_list_execute()` to send, or `searchads360_customers_custom_columns_list` for simplest API.

pub fn searchads360_customers_custom_columns_list_builder<R>(
    client: &SimpleHttpClient<R>,
    customerId: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://searchads360.googleapis.com/v0/customers/{}/customColumns",
        customerId,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v0/customers/{customersId}/customColumns
/// Returns all the custom columns associated with the customer in full detail.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `searchads360_customers_custom_columns_list_execute()` or `searchads360_customers_custom_columns_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `searchads360_customers_custom_columns_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn searchads360_customers_custom_columns_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<GoogleAdsSearchads360V0ServicesListCustomColumnsResponse>,
                ApiError,
            >,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleAdsSearchads360V0ServicesListCustomColumnsResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v0/customers/{customersId}/customColumns
/// Returns all the custom columns associated with the customer in full detail.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `searchads360_customers_custom_columns_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `searchads360_customers_custom_columns_list_task()`.
/// For the simplest API, use `searchads360_customers_custom_columns_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `searchads360_customers_custom_columns_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn searchads360_customers_custom_columns_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleAdsSearchads360V0ServicesListCustomColumnsResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = searchads360_customers_custom_columns_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`searchads360_customers_custom_columns_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct Searchads360CustomersCustomColumnsListArgs {
    /// Path parameter: customerId
    pub customerId: String,
}

/// GET v0/customers/{customersId}/customColumns
/// Returns all the custom columns associated with the customer in full detail.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `searchads360_customers_custom_columns_list_builder()` + `searchads360_customers_custom_columns_list_execute()`.
/// For task-level control, use `searchads360_customers_custom_columns_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn searchads360_customers_custom_columns_list(
    client: &SimpleHttpClient,
    args: &Searchads360CustomersCustomColumnsListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleAdsSearchads360V0ServicesListCustomColumnsResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = searchads360_customers_custom_columns_list_builder(client, &args.customerId)?;
    searchads360_customers_custom_columns_list_execute(builder)
}

/// POST v0/customers/{customersId}/searchAds360:search
/// Returns all rows that match the search query. List of thrown errors: [AuthenticationError]() [AuthorizationError]() [HeaderError]() [InternalError]() [QueryError]() [QuotaError]() [RequestError]()
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `searchads360_customers_search_ads360_search_execute()` to send, or `searchads360_customers_search_ads360_search` for simplest API.

pub fn searchads360_customers_search_ads360_search_builder<R>(
    client: &SimpleHttpClient<R>,
    customerId: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://searchads360.googleapis.com/v0/customers/{}/searchAds360:search",
        customerId,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v0/customers/{customersId}/searchAds360:search
/// Returns all rows that match the search query. List of thrown errors: [AuthenticationError]() [AuthorizationError]() [HeaderError]() [InternalError]() [QueryError]() [QuotaError]() [RequestError]()
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `searchads360_customers_search_ads360_search_execute()` or `searchads360_customers_search_ads360_search`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `searchads360_customers_search_ads360_search_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn searchads360_customers_search_ads360_search_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<GoogleAdsSearchads360V0ServicesSearchSearchAds360Response>,
                ApiError,
            >,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleAdsSearchads360V0ServicesSearchSearchAds360Response =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v0/customers/{customersId}/searchAds360:search
/// Returns all rows that match the search query. List of thrown errors: [AuthenticationError]() [AuthorizationError]() [HeaderError]() [InternalError]() [QueryError]() [QuotaError]() [RequestError]()
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `searchads360_customers_search_ads360_search_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `searchads360_customers_search_ads360_search_task()`.
/// For the simplest API, use `searchads360_customers_search_ads360_search()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `searchads360_customers_search_ads360_search_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn searchads360_customers_search_ads360_search_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleAdsSearchads360V0ServicesSearchSearchAds360Response>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = searchads360_customers_search_ads360_search_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`searchads360_customers_search_ads360_search`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct Searchads360CustomersSearchAds360SearchArgs {
    /// Path parameter: customerId
    pub customerId: String,
}

/// POST v0/customers/{customersId}/searchAds360:search
/// Returns all rows that match the search query. List of thrown errors: [AuthenticationError]() [AuthorizationError]() [HeaderError]() [InternalError]() [QueryError]() [QuotaError]() [RequestError]()
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `searchads360_customers_search_ads360_search_builder()` + `searchads360_customers_search_ads360_search_execute()`.
/// For task-level control, use `searchads360_customers_search_ads360_search_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn searchads360_customers_search_ads360_search(
    client: &SimpleHttpClient,
    args: &Searchads360CustomersSearchAds360SearchArgs,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleAdsSearchads360V0ServicesSearchSearchAds360Response>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = searchads360_customers_search_ads360_search_builder(client, &args.customerId)?;
    searchads360_customers_search_ads360_search_execute(builder)
}

/// GET v0/searchAds360Fields/{searchAds360FieldsId}
/// Returns just the requested field. List of thrown errors: [AuthenticationError]() [AuthorizationError]() [HeaderError]() [InternalError]() [QuotaError]() [RequestError]()
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `searchads360_search_ads360_fields_get_execute()` to send, or `searchads360_search_ads360_fields_get` for simplest API.

pub fn searchads360_search_ads360_fields_get_builder<R>(
    client: &SimpleHttpClient<R>,
    resourceName: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://searchads360.googleapis.com/v0/searchAds360Fields/{}",
        resourceName,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v0/searchAds360Fields/{searchAds360FieldsId}
/// Returns just the requested field. List of thrown errors: [AuthenticationError]() [AuthorizationError]() [HeaderError]() [InternalError]() [QuotaError]() [RequestError]()
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `searchads360_search_ads360_fields_get_execute()` or `searchads360_search_ads360_fields_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `searchads360_search_ads360_fields_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn searchads360_search_ads360_fields_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<GoogleAdsSearchads360V0ResourcesSearchAds360Field>,
                ApiError,
            >,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleAdsSearchads360V0ResourcesSearchAds360Field =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v0/searchAds360Fields/{searchAds360FieldsId}
/// Returns just the requested field. List of thrown errors: [AuthenticationError]() [AuthorizationError]() [HeaderError]() [InternalError]() [QuotaError]() [RequestError]()
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `searchads360_search_ads360_fields_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `searchads360_search_ads360_fields_get_task()`.
/// For the simplest API, use `searchads360_search_ads360_fields_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `searchads360_search_ads360_fields_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn searchads360_search_ads360_fields_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleAdsSearchads360V0ResourcesSearchAds360Field>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = searchads360_search_ads360_fields_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`searchads360_search_ads360_fields_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct Searchads360SearchAds360FieldsGetArgs {
    /// Path parameter: resourceName
    pub resourceName: String,
}

/// GET v0/searchAds360Fields/{searchAds360FieldsId}
/// Returns just the requested field. List of thrown errors: [AuthenticationError]() [AuthorizationError]() [HeaderError]() [InternalError]() [QuotaError]() [RequestError]()
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `searchads360_search_ads360_fields_get_builder()` + `searchads360_search_ads360_fields_get_execute()`.
/// For task-level control, use `searchads360_search_ads360_fields_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn searchads360_search_ads360_fields_get(
    client: &SimpleHttpClient,
    args: &Searchads360SearchAds360FieldsGetArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleAdsSearchads360V0ResourcesSearchAds360Field>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = searchads360_search_ads360_fields_get_builder(client, &args.resourceName)?;
    searchads360_search_ads360_fields_get_execute(builder)
}

/// POST v0/searchAds360Fields:search
/// Returns all fields that match the search [query](/search-`ads/reporting/concepts/field-service`#use_a_query_to_get_field_details). List of thrown errors: [AuthenticationError]() [AuthorizationError]() [HeaderError]() [InternalError]() [QueryError]() [QuotaError]() [RequestError]()
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `searchads360_search_ads360_fields_search_execute()` to send, or `searchads360_search_ads360_fields_search` for simplest API.

pub fn searchads360_search_ads360_fields_search_builder<R>(
    client: &SimpleHttpClient<R>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!("https://searchads360.googleapis.com/v0/searchAds360Fields:search",);

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v0/searchAds360Fields:search
/// Returns all fields that match the search [query](/search-`ads/reporting/concepts/field-service`#use_a_query_to_get_field_details). List of thrown errors: [AuthenticationError]() [AuthorizationError]() [HeaderError]() [InternalError]() [QueryError]() [QuotaError]() [RequestError]()
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `searchads360_search_ads360_fields_search_execute()` or `searchads360_search_ads360_fields_search`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `searchads360_search_ads360_fields_search_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn searchads360_search_ads360_fields_search_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<GoogleAdsSearchads360V0ServicesSearchSearchAds360FieldsResponse>,
                ApiError,
            >,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleAdsSearchads360V0ServicesSearchSearchAds360FieldsResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v0/searchAds360Fields:search
/// Returns all fields that match the search [query](/search-`ads/reporting/concepts/field-service`#use_a_query_to_get_field_details). List of thrown errors: [AuthenticationError]() [AuthorizationError]() [HeaderError]() [InternalError]() [QueryError]() [QuotaError]() [RequestError]()
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `searchads360_search_ads360_fields_search_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `searchads360_search_ads360_fields_search_task()`.
/// For the simplest API, use `searchads360_search_ads360_fields_search()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `searchads360_search_ads360_fields_search_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn searchads360_search_ads360_fields_search_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleAdsSearchads360V0ServicesSearchSearchAds360FieldsResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = searchads360_search_ads360_fields_search_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// POST v0/searchAds360Fields:search
/// Returns all fields that match the search [query](/search-`ads/reporting/concepts/field-service`#use_a_query_to_get_field_details). List of thrown errors: [AuthenticationError]() [AuthorizationError]() [HeaderError]() [InternalError]() [QueryError]() [QuotaError]() [RequestError]()
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `searchads360_search_ads360_fields_search_builder()` + `searchads360_search_ads360_fields_search_execute()`.
/// For task-level control, use `searchads360_search_ads360_fields_search_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn searchads360_search_ads360_fields_search(
    client: &SimpleHttpClient,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleAdsSearchads360V0ServicesSearchSearchAds360FieldsResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = searchads360_search_ads360_fields_search_builder(client)?;
    searchads360_search_ads360_fields_search_execute(builder)
}

// =============================================================================
// ResourceIdentifier implementation for GoogleAdsSearchads360V0ServicesListAccessibleCustomersResponse
// =============================================================================

/// ResourceIdentifier implementation for GoogleAdsSearchads360V0ServicesListAccessibleCustomersResponse with Searchads360CustomersListAccessibleCustomersArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<Searchads360CustomersListAccessibleCustomersArgs>
    for GoogleAdsSearchads360V0ServicesListAccessibleCustomersResponse
{
    fn generate_resource_id(
        &self,
        input: &Searchads360CustomersListAccessibleCustomersArgs,
    ) -> String {
        "gcp::searchads360::GoogleAdsSearchads360V0ServicesListAccessibleCustomersResponse"
            .to_string()
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::searchads360::GoogleAdsSearchads360V0ServicesListAccessibleCustomersResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleAdsSearchads360V0ResourcesCustomColumn
// =============================================================================

/// ResourceIdentifier implementation for GoogleAdsSearchads360V0ResourcesCustomColumn with Searchads360CustomersCustomColumnsGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<Searchads360CustomersCustomColumnsGetArgs>
    for GoogleAdsSearchads360V0ResourcesCustomColumn
{
    fn generate_resource_id(&self, input: &Searchads360CustomersCustomColumnsGetArgs) -> String {
        format!(
            "gcp::searchads360::GoogleAdsSearchads360V0ResourcesCustomColumn/{}",
            input.resourceName
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::searchads360::GoogleAdsSearchads360V0ResourcesCustomColumn"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleAdsSearchads360V0ServicesListCustomColumnsResponse
// =============================================================================

/// ResourceIdentifier implementation for GoogleAdsSearchads360V0ServicesListCustomColumnsResponse with Searchads360CustomersCustomColumnsListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<Searchads360CustomersCustomColumnsListArgs>
    for GoogleAdsSearchads360V0ServicesListCustomColumnsResponse
{
    fn generate_resource_id(&self, input: &Searchads360CustomersCustomColumnsListArgs) -> String {
        format!(
            "gcp::searchads360::GoogleAdsSearchads360V0ServicesListCustomColumnsResponse/{}",
            input.customerId
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::searchads360::GoogleAdsSearchads360V0ServicesListCustomColumnsResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleAdsSearchads360V0ServicesSearchSearchAds360Response
// =============================================================================

/// ResourceIdentifier implementation for GoogleAdsSearchads360V0ServicesSearchSearchAds360Response with Searchads360CustomersSearchAds360SearchArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<Searchads360CustomersSearchAds360SearchArgs>
    for GoogleAdsSearchads360V0ServicesSearchSearchAds360Response
{
    fn generate_resource_id(&self, input: &Searchads360CustomersSearchAds360SearchArgs) -> String {
        format!(
            "gcp::searchads360::GoogleAdsSearchads360V0ServicesSearchSearchAds360Response/{}",
            input.customerId
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::searchads360::GoogleAdsSearchads360V0ServicesSearchSearchAds360Response"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleAdsSearchads360V0ResourcesSearchAds360Field
// =============================================================================

/// ResourceIdentifier implementation for GoogleAdsSearchads360V0ResourcesSearchAds360Field with Searchads360SearchAds360FieldsGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<Searchads360SearchAds360FieldsGetArgs>
    for GoogleAdsSearchads360V0ResourcesSearchAds360Field
{
    fn generate_resource_id(&self, input: &Searchads360SearchAds360FieldsGetArgs) -> String {
        format!(
            "gcp::searchads360::GoogleAdsSearchads360V0ResourcesSearchAds360Field/{}",
            input.resourceName
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::searchads360::GoogleAdsSearchads360V0ResourcesSearchAds360Field"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleAdsSearchads360V0ServicesSearchSearchAds360FieldsResponse
// =============================================================================

/// ResourceIdentifier implementation for GoogleAdsSearchads360V0ServicesSearchSearchAds360FieldsResponse with Searchads360SearchAds360FieldsSearchArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<Searchads360SearchAds360FieldsSearchArgs>
    for GoogleAdsSearchads360V0ServicesSearchSearchAds360FieldsResponse
{
    fn generate_resource_id(&self, input: &Searchads360SearchAds360FieldsSearchArgs) -> String {
        "gcp::searchads360::GoogleAdsSearchads360V0ServicesSearchSearchAds360FieldsResponse"
            .to_string()
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::searchads360::GoogleAdsSearchads360V0ServicesSearchSearchAds360FieldsResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}
