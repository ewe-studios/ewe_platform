//! Auto-generated API clients for gcp/searchconsole.
//!
//! Generated by `cargo run --bin ewe_platform gen_provider_clients`.
//! DO NOT EDIT MANUALLY.

use crate::providers::gcp::clients::types::*;
use crate::providers::gcp::resources::*;
use foundation_core::valtron::{execute, StreamIterator, StreamIteratorExt};
use foundation_core::wire::simple_http::client::{
    body_reader, ClientRequestBuilder, RequestIntro, SimpleHttpClient, SystemDnsResolver,
};

/// GET webmasters/v3/sites/{siteUrl}/searchAnalytics/query
/// Query your data with filters and parameters that you define. Returns zero or more rows grouped by the row keys that you define. You must define a date range of one or more days. When date is one of the group by values, any days without data are omitted from the result list. If you need to know which days have data, issue a broad date range query grouped by date for any metric, and see which day rows are returned.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `webmasters_searchanalytics_query_execute()` to send, or `webmasters_searchanalytics_query` for simplest API.

pub fn webmasters_searchanalytics_query_builder(
    client: &SimpleHttpClient,
    siteUrl: &str,
    body: &SearchAnalyticsQueryRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comwebmasters/v3/sites/{}/searchAnalytics/query",
        siteUrl,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET webmasters/v3/sites/{siteUrl}/searchAnalytics/query
/// Query your data with filters and parameters that you define. Returns zero or more rows grouped by the row keys that you define. You must define a date range of one or more days. When date is one of the group by values, any days without data are omitted from the result list. If you need to know which days have data, issue a broad date range query grouped by date for any metric, and see which day rows are returned.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `webmasters_searchanalytics_query_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from webmasters_searchanalytics_query_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn webmasters_searchanalytics_query_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<SearchAnalyticsQueryResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: SearchAnalyticsQueryResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET webmasters/v3/sites/{siteUrl}/searchAnalytics/query
/// Query your data with filters and parameters that you define. Returns zero or more rows grouped by the row keys that you define. You must define a date range of one or more days. When date is one of the group by values, any days without data are omitted from the result list. If you need to know which days have data, issue a broad date range query grouped by date for any metric, and see which day rows are returned.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `webmasters_searchanalytics_query_builder()` + `webmasters_searchanalytics_query_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn webmasters_searchanalytics_query(
    client: &SimpleHttpClient,
    siteUrl: &str,
    body: &SearchAnalyticsQueryRequest,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<SearchAnalyticsQueryResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = webmasters_searchanalytics_query_builder(client, siteUrl, body)?;
    webmasters_searchanalytics_query_execute(builder)
}

/// GET webmasters/v3/sites/{siteUrl}/sitemaps/{feedpath}
/// Deletes a sitemap from the Sitemaps report. Does not stop Google from crawling this sitemap or the URLs that were previously crawled in the deleted sitemap.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `webmasters_sitemaps_delete_execute()` to send, or `webmasters_sitemaps_delete` for simplest API.

pub fn webmasters_sitemaps_delete_builder(
    client: &SimpleHttpClient,
    feedpath: &str,
    siteUrl: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comwebmasters/v3/sites/{}/sitemaps/{}",
        feedpath, siteUrl,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET webmasters/v3/sites/{siteUrl}/sitemaps/{feedpath}
/// Deletes a sitemap from the Sitemaps report. Does not stop Google from crawling this sitemap or the URLs that were previously crawled in the deleted sitemap.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `webmasters_sitemaps_delete_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from webmasters_sitemaps_delete_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn webmasters_sitemaps_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET webmasters/v3/sites/{siteUrl}/sitemaps/{feedpath}
/// Deletes a sitemap from the Sitemaps report. Does not stop Google from crawling this sitemap or the URLs that were previously crawled in the deleted sitemap.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `webmasters_sitemaps_delete_builder()` + `webmasters_sitemaps_delete_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn webmasters_sitemaps_delete(
    client: &SimpleHttpClient,
    feedpath: &str,
    siteUrl: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = webmasters_sitemaps_delete_builder(client, feedpath, siteUrl)?;
    webmasters_sitemaps_delete_execute(builder)
}

/// GET webmasters/v3/sites/{siteUrl}/sitemaps/{feedpath}
/// Retrieves information about a specific sitemap.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `webmasters_sitemaps_get_execute()` to send, or `webmasters_sitemaps_get` for simplest API.

pub fn webmasters_sitemaps_get_builder(
    client: &SimpleHttpClient,
    feedpath: &str,
    siteUrl: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comwebmasters/v3/sites/{}/sitemaps/{}",
        feedpath, siteUrl,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET webmasters/v3/sites/{siteUrl}/sitemaps/{feedpath}
/// Retrieves information about a specific sitemap.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `webmasters_sitemaps_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from webmasters_sitemaps_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn webmasters_sitemaps_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<WmxSitemap>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: WmxSitemap = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET webmasters/v3/sites/{siteUrl}/sitemaps/{feedpath}
/// Retrieves information about a specific sitemap.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `webmasters_sitemaps_get_builder()` + `webmasters_sitemaps_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn webmasters_sitemaps_get(
    client: &SimpleHttpClient,
    feedpath: &str,
    siteUrl: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<WmxSitemap>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = webmasters_sitemaps_get_builder(client, feedpath, siteUrl)?;
    webmasters_sitemaps_get_execute(builder)
}

/// GET webmasters/v3/sites/{siteUrl}/sitemaps
/// Lists the [sitemaps-entries](/webmaster-`tools/v3/sitemaps`) submitted for this site, or included in the sitemap index file (if `sitemapIndex` is specified in the request).
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `webmasters_sitemaps_list_execute()` to send, or `webmasters_sitemaps_list` for simplest API.

pub fn webmasters_sitemaps_list_builder(
    client: &SimpleHttpClient,
    siteUrl: &str,
    sitemapIndex: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comwebmasters/v3/sites/{}/sitemaps",
        siteUrl,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "sitemapIndex",
            sitemapIndex
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET webmasters/v3/sites/{siteUrl}/sitemaps
/// Lists the [sitemaps-entries](/webmaster-`tools/v3/sitemaps`) submitted for this site, or included in the sitemap index file (if `sitemapIndex` is specified in the request).
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `webmasters_sitemaps_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from webmasters_sitemaps_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn webmasters_sitemaps_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SitemapsListResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: SitemapsListResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET webmasters/v3/sites/{siteUrl}/sitemaps
/// Lists the [sitemaps-entries](/webmaster-`tools/v3/sitemaps`) submitted for this site, or included in the sitemap index file (if `sitemapIndex` is specified in the request).
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `webmasters_sitemaps_list_builder()` + `webmasters_sitemaps_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn webmasters_sitemaps_list(
    client: &SimpleHttpClient,
    siteUrl: &str,
    sitemapIndex: Option<&str>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SitemapsListResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = webmasters_sitemaps_list_builder(client, siteUrl, sitemapIndex)?;
    webmasters_sitemaps_list_execute(builder)
}

/// GET webmasters/v3/sites/{siteUrl}/sitemaps/{feedpath}
/// Submits a sitemap for a site.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `webmasters_sitemaps_submit_execute()` to send, or `webmasters_sitemaps_submit` for simplest API.

pub fn webmasters_sitemaps_submit_builder(
    client: &SimpleHttpClient,
    feedpath: &str,
    siteUrl: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comwebmasters/v3/sites/{}/sitemaps/{}",
        feedpath, siteUrl,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET webmasters/v3/sites/{siteUrl}/sitemaps/{feedpath}
/// Submits a sitemap for a site.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `webmasters_sitemaps_submit_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from webmasters_sitemaps_submit_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn webmasters_sitemaps_submit_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET webmasters/v3/sites/{siteUrl}/sitemaps/{feedpath}
/// Submits a sitemap for a site.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `webmasters_sitemaps_submit_builder()` + `webmasters_sitemaps_submit_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn webmasters_sitemaps_submit(
    client: &SimpleHttpClient,
    feedpath: &str,
    siteUrl: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = webmasters_sitemaps_submit_builder(client, feedpath, siteUrl)?;
    webmasters_sitemaps_submit_execute(builder)
}

/// GET webmasters/v3/sites/{siteUrl}
/// Adds a site to the set of the user's sites in Search Console.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `webmasters_sites_add_execute()` to send, or `webmasters_sites_add` for simplest API.

pub fn webmasters_sites_add_builder(
    client: &SimpleHttpClient,
    siteUrl: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comwebmasters/v3/sites/{}", siteUrl,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET webmasters/v3/sites/{siteUrl}
/// Adds a site to the set of the user's sites in Search Console.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `webmasters_sites_add_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from webmasters_sites_add_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn webmasters_sites_add_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET webmasters/v3/sites/{siteUrl}
/// Adds a site to the set of the user's sites in Search Console.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `webmasters_sites_add_builder()` + `webmasters_sites_add_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn webmasters_sites_add(
    client: &SimpleHttpClient,
    siteUrl: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = webmasters_sites_add_builder(client, siteUrl)?;
    webmasters_sites_add_execute(builder)
}

/// GET webmasters/v3/sites/{siteUrl}
/// Removes a site from the set of the user's Search Console sites.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `webmasters_sites_delete_execute()` to send, or `webmasters_sites_delete` for simplest API.

pub fn webmasters_sites_delete_builder(
    client: &SimpleHttpClient,
    siteUrl: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comwebmasters/v3/sites/{}", siteUrl,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET webmasters/v3/sites/{siteUrl}
/// Removes a site from the set of the user's Search Console sites.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `webmasters_sites_delete_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from webmasters_sites_delete_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn webmasters_sites_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET webmasters/v3/sites/{siteUrl}
/// Removes a site from the set of the user's Search Console sites.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `webmasters_sites_delete_builder()` + `webmasters_sites_delete_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn webmasters_sites_delete(
    client: &SimpleHttpClient,
    siteUrl: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = webmasters_sites_delete_builder(client, siteUrl)?;
    webmasters_sites_delete_execute(builder)
}

/// GET webmasters/v3/sites/{siteUrl}
/// Retrieves information about specific site.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `webmasters_sites_get_execute()` to send, or `webmasters_sites_get` for simplest API.

pub fn webmasters_sites_get_builder(
    client: &SimpleHttpClient,
    siteUrl: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comwebmasters/v3/sites/{}", siteUrl,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET webmasters/v3/sites/{siteUrl}
/// Retrieves information about specific site.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `webmasters_sites_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from webmasters_sites_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn webmasters_sites_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<WmxSite>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: WmxSite = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET webmasters/v3/sites/{siteUrl}
/// Retrieves information about specific site.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `webmasters_sites_get_builder()` + `webmasters_sites_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn webmasters_sites_get(
    client: &SimpleHttpClient,
    siteUrl: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<WmxSite>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = webmasters_sites_get_builder(client, siteUrl)?;
    webmasters_sites_get_execute(builder)
}

/// GET webmasters/v3/sites
/// Lists the user's Search Console sites.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `webmasters_sites_list_execute()` to send, or `webmasters_sites_list` for simplest API.

pub fn webmasters_sites_list_builder(
    client: &SimpleHttpClient,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comwebmasters/v3/sites",);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET webmasters/v3/sites
/// Lists the user's Search Console sites.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `webmasters_sites_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from webmasters_sites_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn webmasters_sites_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SitesListResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: SitesListResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET webmasters/v3/sites
/// Lists the user's Search Console sites.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `webmasters_sites_list_builder()` + `webmasters_sites_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn webmasters_sites_list(
    client: &SimpleHttpClient,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SitesListResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = webmasters_sites_list_builder(client)?;
    webmasters_sites_list_execute(builder)
}

/// GET v1/urlInspection/index:inspect
/// Index inspection.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `searchconsole_url_inspection_index_inspect_execute()` to send, or `searchconsole_url_inspection_index_inspect` for simplest API.

pub fn searchconsole_url_inspection_index_inspect_builder(
    client: &SimpleHttpClient,
    body: &InspectUrlIndexRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/urlInspection/index:inspect",);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v1/urlInspection/index:inspect
/// Index inspection.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `searchconsole_url_inspection_index_inspect_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from searchconsole_url_inspection_index_inspect_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn searchconsole_url_inspection_index_inspect_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<InspectUrlIndexResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: InspectUrlIndexResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/urlInspection/index:inspect
/// Index inspection.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `searchconsole_url_inspection_index_inspect_builder()` + `searchconsole_url_inspection_index_inspect_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn searchconsole_url_inspection_index_inspect(
    client: &SimpleHttpClient,
    body: &InspectUrlIndexRequest,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<InspectUrlIndexResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = searchconsole_url_inspection_index_inspect_builder(client, body)?;
    searchconsole_url_inspection_index_inspect_execute(builder)
}

/// GET v1/urlTestingTools/mobileFriendlyTest:run
/// Runs Mobile-Friendly Test for a given URL.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `searchconsole_url_testing_tools_mobile_friendly_test_run_execute()` to send, or `searchconsole_url_testing_tools_mobile_friendly_test_run` for simplest API.

pub fn searchconsole_url_testing_tools_mobile_friendly_test_run_builder(
    client: &SimpleHttpClient,
    body: &RunMobileFriendlyTestRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/urlTestingTools/mobileFriendlyTest:run",);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v1/urlTestingTools/mobileFriendlyTest:run
/// Runs Mobile-Friendly Test for a given URL.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `searchconsole_url_testing_tools_mobile_friendly_test_run_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from searchconsole_url_testing_tools_mobile_friendly_test_run_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn searchconsole_url_testing_tools_mobile_friendly_test_run_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<RunMobileFriendlyTestResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: RunMobileFriendlyTestResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/urlTestingTools/mobileFriendlyTest:run
/// Runs Mobile-Friendly Test for a given URL.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `searchconsole_url_testing_tools_mobile_friendly_test_run_builder()` + `searchconsole_url_testing_tools_mobile_friendly_test_run_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn searchconsole_url_testing_tools_mobile_friendly_test_run(
    client: &SimpleHttpClient,
    body: &RunMobileFriendlyTestRequest,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<RunMobileFriendlyTestResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = searchconsole_url_testing_tools_mobile_friendly_test_run_builder(client, body)?;
    searchconsole_url_testing_tools_mobile_friendly_test_run_execute(builder)
}
