//! Auto-generated API clients for gcp/searchconsole.
//!
//! Generated by `cargo run --bin ewe_platform gen_provider_clients`.
//! DO NOT EDIT MANUALLY.
//!
//! Feature flag: `gcp`

#![cfg(feature = "gcp")]

pub mod types;

use crate::providers::gcp::clients::types::*;
use crate::providers::gcp::resources::*;
use foundation_core::valtron::{
    execute, BoxedSendExecutionAction, StreamIterator, StreamIteratorExt, TaskIterator,
    TaskIteratorExt,
};
use foundation_core::wire::simple_http::client::{
    body_reader, ClientRequestBuilder, RequestIntro, SimpleHttpClient, SystemDnsResolver,
};
use foundation_macros::JsonHash;
use serde::Serialize;

/// GET webmasters/v3/sites/{siteUrl}/searchAnalytics/query
/// Query your data with filters and parameters that you define. Returns zero or more rows grouped by the row keys that you define. You must define a date range of one or more days. When date is one of the group by values, any days without data are omitted from the result list. If you need to know which days have data, issue a broad date range query grouped by date for any metric, and see which day rows are returned.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `webmasters_searchanalytics_query_execute()` to send, or `webmasters_searchanalytics_query` for simplest API.

pub fn webmasters_searchanalytics_query_builder(
    client: &SimpleHttpClient,
    siteUrl: &String,
    body: &SearchAnalyticsQueryRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://searchconsole.googleapis.com/webmasters/v3/sites/{}/searchAnalytics/query",
        siteUrl,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET webmasters/v3/sites/{siteUrl}/searchAnalytics/query
/// Query your data with filters and parameters that you define. Returns zero or more rows grouped by the row keys that you define. You must define a date range of one or more days. When date is one of the group by values, any days without data are omitted from the result list. If you need to know which days have data, issue a broad date range query grouped by date for any metric, and see which day rows are returned.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `webmasters_searchanalytics_query_execute()` or `webmasters_searchanalytics_query`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `webmasters_searchanalytics_query_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn webmasters_searchanalytics_query_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<SearchAnalyticsQueryResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: SearchAnalyticsQueryResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET webmasters/v3/sites/{siteUrl}/searchAnalytics/query
/// Query your data with filters and parameters that you define. Returns zero or more rows grouped by the row keys that you define. You must define a date range of one or more days. When date is one of the group by values, any days without data are omitted from the result list. If you need to know which days have data, issue a broad date range query grouped by date for any metric, and see which day rows are returned.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `webmasters_searchanalytics_query_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `webmasters_searchanalytics_query_task()`.
/// For the simplest API, use `webmasters_searchanalytics_query()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `webmasters_searchanalytics_query_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn webmasters_searchanalytics_query_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<SearchAnalyticsQueryResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = webmasters_searchanalytics_query_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`webmasters_searchanalytics_query`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct WebmastersSearchanalyticsQueryArgs {
    /// Path parameter: siteUrl
    pub siteUrl: String,
    /// Request body.
    pub body: SearchAnalyticsQueryRequest,
}

/// GET webmasters/v3/sites/{siteUrl}/searchAnalytics/query
/// Query your data with filters and parameters that you define. Returns zero or more rows grouped by the row keys that you define. You must define a date range of one or more days. When date is one of the group by values, any days without data are omitted from the result list. If you need to know which days have data, issue a broad date range query grouped by date for any metric, and see which day rows are returned.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `webmasters_searchanalytics_query_builder()` + `webmasters_searchanalytics_query_execute()`.
/// For task-level control, use `webmasters_searchanalytics_query_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn webmasters_searchanalytics_query(
    client: &SimpleHttpClient,
    args: &WebmastersSearchanalyticsQueryArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<SearchAnalyticsQueryResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = webmasters_searchanalytics_query_builder(client, &args.siteUrl, &args.body)?;
    webmasters_searchanalytics_query_execute(builder)
}

/// GET webmasters/v3/sites/{siteUrl}/sitemaps/{feedpath}
/// Deletes a sitemap from the Sitemaps report. Does not stop Google from crawling this sitemap or the URLs that were previously crawled in the deleted sitemap.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `webmasters_sitemaps_delete_execute()` to send, or `webmasters_sitemaps_delete` for simplest API.

pub fn webmasters_sitemaps_delete_builder(
    client: &SimpleHttpClient,
    siteUrl: &String,
    feedpath: &String,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://searchconsole.googleapis.com/webmasters/v3/sites/{}/sitemaps/{}",
        siteUrl, feedpath,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET webmasters/v3/sites/{siteUrl}/sitemaps/{feedpath}
/// Deletes a sitemap from the Sitemaps report. Does not stop Google from crawling this sitemap or the URLs that were previously crawled in the deleted sitemap.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `webmasters_sitemaps_delete_execute()` or `webmasters_sitemaps_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `webmasters_sitemaps_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn webmasters_sitemaps_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<()>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET webmasters/v3/sites/{siteUrl}/sitemaps/{feedpath}
/// Deletes a sitemap from the Sitemaps report. Does not stop Google from crawling this sitemap or the URLs that were previously crawled in the deleted sitemap.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `webmasters_sitemaps_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `webmasters_sitemaps_delete_task()`.
/// For the simplest API, use `webmasters_sitemaps_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `webmasters_sitemaps_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn webmasters_sitemaps_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = webmasters_sitemaps_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`webmasters_sitemaps_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct WebmastersSitemapsDeleteArgs {
    /// Path parameter: siteUrl
    pub siteUrl: String,
    /// Path parameter: feedpath
    pub feedpath: String,
}

/// GET webmasters/v3/sites/{siteUrl}/sitemaps/{feedpath}
/// Deletes a sitemap from the Sitemaps report. Does not stop Google from crawling this sitemap or the URLs that were previously crawled in the deleted sitemap.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `webmasters_sitemaps_delete_builder()` + `webmasters_sitemaps_delete_execute()`.
/// For task-level control, use `webmasters_sitemaps_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn webmasters_sitemaps_delete(
    client: &SimpleHttpClient,
    args: &WebmastersSitemapsDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = webmasters_sitemaps_delete_builder(client, &args.siteUrl, &args.feedpath)?;
    webmasters_sitemaps_delete_execute(builder)
}

/// GET webmasters/v3/sites/{siteUrl}/sitemaps
/// Lists the [sitemaps-entries](/webmaster-`tools/v3/sitemaps`) submitted for this site, or included in the sitemap index file (if `sitemapIndex` is specified in the request).
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `webmasters_sitemaps_list_execute()` to send, or `webmasters_sitemaps_list` for simplest API.

pub fn webmasters_sitemaps_list_builder(
    client: &SimpleHttpClient,
    siteUrl: &String,
    sitemapIndex: &Option<String>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://searchconsole.googleapis.com/webmasters/v3/sites/{}/sitemaps",
        siteUrl,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = sitemapIndex.as_ref() {
        query_parts.push(format!("sitemapIndex={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET webmasters/v3/sites/{siteUrl}/sitemaps
/// Lists the [sitemaps-entries](/webmaster-`tools/v3/sitemaps`) submitted for this site, or included in the sitemap index file (if `sitemapIndex` is specified in the request).
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `webmasters_sitemaps_list_execute()` or `webmasters_sitemaps_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `webmasters_sitemaps_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn webmasters_sitemaps_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<SitemapsListResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: SitemapsListResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET webmasters/v3/sites/{siteUrl}/sitemaps
/// Lists the [sitemaps-entries](/webmaster-`tools/v3/sitemaps`) submitted for this site, or included in the sitemap index file (if `sitemapIndex` is specified in the request).
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `webmasters_sitemaps_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `webmasters_sitemaps_list_task()`.
/// For the simplest API, use `webmasters_sitemaps_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `webmasters_sitemaps_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn webmasters_sitemaps_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SitemapsListResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = webmasters_sitemaps_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`webmasters_sitemaps_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct WebmastersSitemapsListArgs {
    /// Path parameter: siteUrl
    pub siteUrl: String,
    /// Query parameter: sitemapIndex
    pub sitemapIndex: Option<String>,
}

/// GET webmasters/v3/sites/{siteUrl}/sitemaps
/// Lists the [sitemaps-entries](/webmaster-`tools/v3/sitemaps`) submitted for this site, or included in the sitemap index file (if `sitemapIndex` is specified in the request).
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `webmasters_sitemaps_list_builder()` + `webmasters_sitemaps_list_execute()`.
/// For task-level control, use `webmasters_sitemaps_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn webmasters_sitemaps_list(
    client: &SimpleHttpClient,
    args: &WebmastersSitemapsListArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SitemapsListResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = webmasters_sitemaps_list_builder(client, &args.siteUrl, &args.sitemapIndex)?;
    webmasters_sitemaps_list_execute(builder)
}

/// GET webmasters/v3/sites/{siteUrl}
/// Adds a site to the set of the user's sites in Search Console.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `webmasters_sites_add_execute()` to send, or `webmasters_sites_add` for simplest API.

pub fn webmasters_sites_add_builder(
    client: &SimpleHttpClient,
    siteUrl: &String,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://searchconsole.googleapis.com/webmasters/v3/sites/{}",
        siteUrl,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET webmasters/v3/sites/{siteUrl}
/// Adds a site to the set of the user's sites in Search Console.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `webmasters_sites_add_execute()` or `webmasters_sites_add`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `webmasters_sites_add_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn webmasters_sites_add_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<()>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET webmasters/v3/sites/{siteUrl}
/// Adds a site to the set of the user's sites in Search Console.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `webmasters_sites_add_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `webmasters_sites_add_task()`.
/// For the simplest API, use `webmasters_sites_add()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `webmasters_sites_add_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn webmasters_sites_add_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = webmasters_sites_add_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`webmasters_sites_add`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct WebmastersSitesAddArgs {
    /// Path parameter: siteUrl
    pub siteUrl: String,
}

/// GET webmasters/v3/sites/{siteUrl}
/// Adds a site to the set of the user's sites in Search Console.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `webmasters_sites_add_builder()` + `webmasters_sites_add_execute()`.
/// For task-level control, use `webmasters_sites_add_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn webmasters_sites_add(
    client: &SimpleHttpClient,
    args: &WebmastersSitesAddArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = webmasters_sites_add_builder(client, &args.siteUrl)?;
    webmasters_sites_add_execute(builder)
}

/// GET webmasters/v3/sites
/// Lists the user's Search Console sites.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `webmasters_sites_list_execute()` to send, or `webmasters_sites_list` for simplest API.

pub fn webmasters_sites_list_builder(
    client: &SimpleHttpClient,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!("https://searchconsole.googleapis.com/webmasters/v3/sites",);

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET webmasters/v3/sites
/// Lists the user's Search Console sites.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `webmasters_sites_list_execute()` or `webmasters_sites_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `webmasters_sites_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn webmasters_sites_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<SitesListResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: SitesListResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET webmasters/v3/sites
/// Lists the user's Search Console sites.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `webmasters_sites_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `webmasters_sites_list_task()`.
/// For the simplest API, use `webmasters_sites_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `webmasters_sites_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn webmasters_sites_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SitesListResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = webmasters_sites_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET webmasters/v3/sites
/// Lists the user's Search Console sites.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `webmasters_sites_list_builder()` + `webmasters_sites_list_execute()`.
/// For task-level control, use `webmasters_sites_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn webmasters_sites_list(
    client: &SimpleHttpClient,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SitesListResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = webmasters_sites_list_builder(client)?;
    webmasters_sites_list_execute(builder)
}

/// GET v1/urlInspection/index:inspect
/// Index inspection.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `searchconsole_url_inspection_index_inspect_execute()` to send, or `searchconsole_url_inspection_index_inspect` for simplest API.

pub fn searchconsole_url_inspection_index_inspect_builder(
    client: &SimpleHttpClient,
    body: &InspectUrlIndexRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url =
        format!("https://searchconsole.googleapis.com/v1/urlInspection/index:inspect",);

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/urlInspection/index:inspect
/// Index inspection.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `searchconsole_url_inspection_index_inspect_execute()` or `searchconsole_url_inspection_index_inspect`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `searchconsole_url_inspection_index_inspect_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn searchconsole_url_inspection_index_inspect_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<InspectUrlIndexResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: InspectUrlIndexResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/urlInspection/index:inspect
/// Index inspection.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `searchconsole_url_inspection_index_inspect_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `searchconsole_url_inspection_index_inspect_task()`.
/// For the simplest API, use `searchconsole_url_inspection_index_inspect()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `searchconsole_url_inspection_index_inspect_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn searchconsole_url_inspection_index_inspect_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<InspectUrlIndexResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = searchconsole_url_inspection_index_inspect_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`searchconsole_url_inspection_index_inspect`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct SearchconsoleUrlInspectionIndexInspectArgs {
    /// Request body.
    pub body: InspectUrlIndexRequest,
}

/// GET v1/urlInspection/index:inspect
/// Index inspection.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `searchconsole_url_inspection_index_inspect_builder()` + `searchconsole_url_inspection_index_inspect_execute()`.
/// For task-level control, use `searchconsole_url_inspection_index_inspect_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn searchconsole_url_inspection_index_inspect(
    client: &SimpleHttpClient,
    args: &SearchconsoleUrlInspectionIndexInspectArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<InspectUrlIndexResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = searchconsole_url_inspection_index_inspect_builder(client, &args.body)?;
    searchconsole_url_inspection_index_inspect_execute(builder)
}

/// GET v1/urlTestingTools/mobileFriendlyTest:run
/// Runs Mobile-Friendly Test for a given URL.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `searchconsole_url_testing_tools_mobile_friendly_test_run_execute()` to send, or `searchconsole_url_testing_tools_mobile_friendly_test_run` for simplest API.

pub fn searchconsole_url_testing_tools_mobile_friendly_test_run_builder(
    client: &SimpleHttpClient,
    body: &RunMobileFriendlyTestRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url =
        format!("https://searchconsole.googleapis.com/v1/urlTestingTools/mobileFriendlyTest:run",);

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/urlTestingTools/mobileFriendlyTest:run
/// Runs Mobile-Friendly Test for a given URL.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `searchconsole_url_testing_tools_mobile_friendly_test_run_execute()` or `searchconsole_url_testing_tools_mobile_friendly_test_run`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `searchconsole_url_testing_tools_mobile_friendly_test_run_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn searchconsole_url_testing_tools_mobile_friendly_test_run_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<RunMobileFriendlyTestResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: RunMobileFriendlyTestResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/urlTestingTools/mobileFriendlyTest:run
/// Runs Mobile-Friendly Test for a given URL.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `searchconsole_url_testing_tools_mobile_friendly_test_run_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `searchconsole_url_testing_tools_mobile_friendly_test_run_task()`.
/// For the simplest API, use `searchconsole_url_testing_tools_mobile_friendly_test_run()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `searchconsole_url_testing_tools_mobile_friendly_test_run_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn searchconsole_url_testing_tools_mobile_friendly_test_run_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<RunMobileFriendlyTestResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = searchconsole_url_testing_tools_mobile_friendly_test_run_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`searchconsole_url_testing_tools_mobile_friendly_test_run`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct SearchconsoleUrlTestingToolsMobileFriendlyTestRunArgs {
    /// Request body.
    pub body: RunMobileFriendlyTestRequest,
}

/// GET v1/urlTestingTools/mobileFriendlyTest:run
/// Runs Mobile-Friendly Test for a given URL.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `searchconsole_url_testing_tools_mobile_friendly_test_run_builder()` + `searchconsole_url_testing_tools_mobile_friendly_test_run_execute()`.
/// For task-level control, use `searchconsole_url_testing_tools_mobile_friendly_test_run_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn searchconsole_url_testing_tools_mobile_friendly_test_run(
    client: &SimpleHttpClient,
    args: &SearchconsoleUrlTestingToolsMobileFriendlyTestRunArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<RunMobileFriendlyTestResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        searchconsole_url_testing_tools_mobile_friendly_test_run_builder(client, &args.body)?;
    searchconsole_url_testing_tools_mobile_friendly_test_run_execute(builder)
}
