//! Auto-generated API clients for gcp/agentregistry.
//!
//! Generated by `cargo run --bin ewe_platform gen_provider_clients`.
//! DO NOT EDIT MANUALLY.
//!
//! Feature flag: `gcp`

#![cfg(feature = "gcp")]

use crate::providers::gcp::clients::types::*;
use crate::providers::gcp::resources::*;
use foundation_core::valtron::{
    execute, BoxedSendExecutionAction, StreamIterator, StreamIteratorExt, TaskIterator,
    TaskIteratorExt,
};
use foundation_core::wire::simple_http::client::{
    body_reader, ClientRequestBuilder, DnsResolver, RequestIntro, SimpleHttpClient,
    SystemDnsResolver,
};
use foundation_db::state::resource_identifier::ResourceIdentifier;
use foundation_macros::JsonHash;
use serde::Serialize;

/// GET v1alpha/projects/{projectsId}/locations/{locationsId}
/// Gets information about a location.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `agentregistry_projects_locations_get_execute()` to send, or `agentregistry_projects_locations_get` for simplest API.

pub fn agentregistry_projects_locations_get_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://agentregistry.googleapis.com/v1alpha/projects/{}/locations/{locationsId}",
        name,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1alpha/projects/{projectsId}/locations/{locationsId}
/// Gets information about a location.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `agentregistry_projects_locations_get_execute()` or `agentregistry_projects_locations_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `agentregistry_projects_locations_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn agentregistry_projects_locations_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Location>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Location = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1alpha/projects/{projectsId}/locations/{locationsId}
/// Gets information about a location.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `agentregistry_projects_locations_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `agentregistry_projects_locations_get_task()`.
/// For the simplest API, use `agentregistry_projects_locations_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `agentregistry_projects_locations_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn agentregistry_projects_locations_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Location>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = agentregistry_projects_locations_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`agentregistry_projects_locations_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AgentregistryProjectsLocationsGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1alpha/projects/{projectsId}/locations/{locationsId}
/// Gets information about a location.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `agentregistry_projects_locations_get_builder()` + `agentregistry_projects_locations_get_execute()`.
/// For task-level control, use `agentregistry_projects_locations_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn agentregistry_projects_locations_get(
    client: &SimpleHttpClient,
    args: &AgentregistryProjectsLocationsGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Location>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = agentregistry_projects_locations_get_builder(client, &args.name)?;
    agentregistry_projects_locations_get_execute(builder)
}

/// GET v1alpha/projects/{projectsId}/locations
/// Lists information about the supported locations for this service. This method lists locations based on the resource scope provided in the [ListLocationsRequest.name] field: * **Global locations**: If name is empty, the method lists the public locations available to all projects. * **Project-specific locations**: If name follows the format `projects/{project}`, the method lists locations visible to that specific project. This includes public, private, or other project-specific locations enabled for the project. For `gRPC` and client library implementations, the resource name is passed as the name field. For direct service calls, the resource name is incorporated into the request path based on the specific service implementation and version.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `agentregistry_projects_locations_list_execute()` to send, or `agentregistry_projects_locations_list` for simplest API.

pub fn agentregistry_projects_locations_list_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
    extraLocationTypes: &Option<Option<String>>,
    filter: &Option<Option<String>>,
    pageSize: &Option<Option<String>>,
    pageToken: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://agentregistry.googleapis.com/v1alpha/projects/{}/locations",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = extraLocationTypes.as_ref() {
        query_parts.push(format!("extraLocationTypes={}", val));
    }
    if let Some(val) = filter.as_ref() {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = pageSize.as_ref() {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1alpha/projects/{projectsId}/locations
/// Lists information about the supported locations for this service. This method lists locations based on the resource scope provided in the [ListLocationsRequest.name] field: * **Global locations**: If name is empty, the method lists the public locations available to all projects. * **Project-specific locations**: If name follows the format `projects/{project}`, the method lists locations visible to that specific project. This includes public, private, or other project-specific locations enabled for the project. For `gRPC` and client library implementations, the resource name is passed as the name field. For direct service calls, the resource name is incorporated into the request path based on the specific service implementation and version.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `agentregistry_projects_locations_list_execute()` or `agentregistry_projects_locations_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `agentregistry_projects_locations_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn agentregistry_projects_locations_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ListLocationsResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListLocationsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1alpha/projects/{projectsId}/locations
/// Lists information about the supported locations for this service. This method lists locations based on the resource scope provided in the [ListLocationsRequest.name] field: * **Global locations**: If name is empty, the method lists the public locations available to all projects. * **Project-specific locations**: If name follows the format `projects/{project}`, the method lists locations visible to that specific project. This includes public, private, or other project-specific locations enabled for the project. For `gRPC` and client library implementations, the resource name is passed as the name field. For direct service calls, the resource name is incorporated into the request path based on the specific service implementation and version.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `agentregistry_projects_locations_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `agentregistry_projects_locations_list_task()`.
/// For the simplest API, use `agentregistry_projects_locations_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `agentregistry_projects_locations_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn agentregistry_projects_locations_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListLocationsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = agentregistry_projects_locations_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`agentregistry_projects_locations_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AgentregistryProjectsLocationsListArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: extraLocationTypes
    pub extraLocationTypes: Option<Option<String>>,
    /// Query parameter: filter
    pub filter: Option<Option<String>>,
    /// Query parameter: pageSize
    pub pageSize: Option<Option<String>>,
    /// Query parameter: pageToken
    pub pageToken: Option<Option<String>>,
}

/// GET v1alpha/projects/{projectsId}/locations
/// Lists information about the supported locations for this service. This method lists locations based on the resource scope provided in the [ListLocationsRequest.name] field: * **Global locations**: If name is empty, the method lists the public locations available to all projects. * **Project-specific locations**: If name follows the format `projects/{project}`, the method lists locations visible to that specific project. This includes public, private, or other project-specific locations enabled for the project. For `gRPC` and client library implementations, the resource name is passed as the name field. For direct service calls, the resource name is incorporated into the request path based on the specific service implementation and version.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `agentregistry_projects_locations_list_builder()` + `agentregistry_projects_locations_list_execute()`.
/// For task-level control, use `agentregistry_projects_locations_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn agentregistry_projects_locations_list(
    client: &SimpleHttpClient,
    args: &AgentregistryProjectsLocationsListArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListLocationsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = agentregistry_projects_locations_list_builder(
        client,
        &args.name,
        &args.extraLocationTypes,
        &args.filter,
        &args.pageSize,
        &args.pageToken,
    )?;
    agentregistry_projects_locations_list_execute(builder)
}

/// GET v1alpha/projects/{projectsId}/locations/{locationsId}/agents/{agentsId}
/// Gets details of a single Agent.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `agentregistry_projects_locations_agents_get_execute()` to send, or `agentregistry_projects_locations_agents_get` for simplest API.

pub fn agentregistry_projects_locations_agents_get_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://agentregistry.googleapis.com/v1alpha/projects/{}/locations/{locationsId}/agents/{agentsId}",
        name,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1alpha/projects/{projectsId}/locations/{locationsId}/agents/{agentsId}
/// Gets details of a single Agent.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `agentregistry_projects_locations_agents_get_execute()` or `agentregistry_projects_locations_agents_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `agentregistry_projects_locations_agents_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn agentregistry_projects_locations_agents_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Agent>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Agent = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1alpha/projects/{projectsId}/locations/{locationsId}/agents/{agentsId}
/// Gets details of a single Agent.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `agentregistry_projects_locations_agents_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `agentregistry_projects_locations_agents_get_task()`.
/// For the simplest API, use `agentregistry_projects_locations_agents_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `agentregistry_projects_locations_agents_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn agentregistry_projects_locations_agents_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Agent>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = agentregistry_projects_locations_agents_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`agentregistry_projects_locations_agents_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AgentregistryProjectsLocationsAgentsGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1alpha/projects/{projectsId}/locations/{locationsId}/agents/{agentsId}
/// Gets details of a single Agent.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `agentregistry_projects_locations_agents_get_builder()` + `agentregistry_projects_locations_agents_get_execute()`.
/// For task-level control, use `agentregistry_projects_locations_agents_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn agentregistry_projects_locations_agents_get(
    client: &SimpleHttpClient,
    args: &AgentregistryProjectsLocationsAgentsGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Agent>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = agentregistry_projects_locations_agents_get_builder(client, &args.name)?;
    agentregistry_projects_locations_agents_get_execute(builder)
}

/// GET v1alpha/projects/{projectsId}/locations/{locationsId}/agents
/// Lists Agents in a given project and location.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `agentregistry_projects_locations_agents_list_execute()` to send, or `agentregistry_projects_locations_agents_list` for simplest API.

pub fn agentregistry_projects_locations_agents_list_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
    filter: &Option<Option<String>>,
    orderBy: &Option<Option<String>>,
    pageSize: &Option<Option<String>>,
    pageToken: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://agentregistry.googleapis.com/v1alpha/projects/{}/locations/{locationsId}/agents",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter.as_ref() {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = orderBy.as_ref() {
        query_parts.push(format!("orderBy={}", val));
    }
    if let Some(val) = pageSize.as_ref() {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1alpha/projects/{projectsId}/locations/{locationsId}/agents
/// Lists Agents in a given project and location.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `agentregistry_projects_locations_agents_list_execute()` or `agentregistry_projects_locations_agents_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `agentregistry_projects_locations_agents_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn agentregistry_projects_locations_agents_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ListAgentsResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListAgentsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1alpha/projects/{projectsId}/locations/{locationsId}/agents
/// Lists Agents in a given project and location.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `agentregistry_projects_locations_agents_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `agentregistry_projects_locations_agents_list_task()`.
/// For the simplest API, use `agentregistry_projects_locations_agents_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `agentregistry_projects_locations_agents_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn agentregistry_projects_locations_agents_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListAgentsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = agentregistry_projects_locations_agents_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`agentregistry_projects_locations_agents_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AgentregistryProjectsLocationsAgentsListArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: filter
    pub filter: Option<Option<String>>,
    /// Query parameter: orderBy
    pub orderBy: Option<Option<String>>,
    /// Query parameter: pageSize
    pub pageSize: Option<Option<String>>,
    /// Query parameter: pageToken
    pub pageToken: Option<Option<String>>,
}

/// GET v1alpha/projects/{projectsId}/locations/{locationsId}/agents
/// Lists Agents in a given project and location.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `agentregistry_projects_locations_agents_list_builder()` + `agentregistry_projects_locations_agents_list_execute()`.
/// For task-level control, use `agentregistry_projects_locations_agents_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn agentregistry_projects_locations_agents_list(
    client: &SimpleHttpClient,
    args: &AgentregistryProjectsLocationsAgentsListArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListAgentsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = agentregistry_projects_locations_agents_list_builder(
        client,
        &args.parent,
        &args.filter,
        &args.orderBy,
        &args.pageSize,
        &args.pageToken,
    )?;
    agentregistry_projects_locations_agents_list_execute(builder)
}

/// POST v1alpha/projects/{projectsId}/locations/{locationsId}/agents:search
/// Searches Agents in a given project and location.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `agentregistry_projects_locations_agents_search_execute()` to send, or `agentregistry_projects_locations_agents_search` for simplest API.

pub fn agentregistry_projects_locations_agents_search_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://agentregistry.googleapis.com/v1alpha/projects/{}/locations/{locationsId}/agents:search",
        parent,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1alpha/projects/{projectsId}/locations/{locationsId}/agents:search
/// Searches Agents in a given project and location.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `agentregistry_projects_locations_agents_search_execute()` or `agentregistry_projects_locations_agents_search`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `agentregistry_projects_locations_agents_search_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn agentregistry_projects_locations_agents_search_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<SearchAgentsResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: SearchAgentsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1alpha/projects/{projectsId}/locations/{locationsId}/agents:search
/// Searches Agents in a given project and location.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `agentregistry_projects_locations_agents_search_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `agentregistry_projects_locations_agents_search_task()`.
/// For the simplest API, use `agentregistry_projects_locations_agents_search()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `agentregistry_projects_locations_agents_search_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn agentregistry_projects_locations_agents_search_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SearchAgentsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = agentregistry_projects_locations_agents_search_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`agentregistry_projects_locations_agents_search`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AgentregistryProjectsLocationsAgentsSearchArgs {
    /// Path parameter: parent
    pub parent: String,
}

/// POST v1alpha/projects/{projectsId}/locations/{locationsId}/agents:search
/// Searches Agents in a given project and location.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `agentregistry_projects_locations_agents_search_builder()` + `agentregistry_projects_locations_agents_search_execute()`.
/// For task-level control, use `agentregistry_projects_locations_agents_search_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn agentregistry_projects_locations_agents_search(
    client: &SimpleHttpClient,
    args: &AgentregistryProjectsLocationsAgentsSearchArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SearchAgentsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = agentregistry_projects_locations_agents_search_builder(client, &args.parent)?;
    agentregistry_projects_locations_agents_search_execute(builder)
}

/// POST v1alpha/projects/{projectsId}/locations/{locationsId}/bindings
/// Creates a new Binding in a given project and location.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `agentregistry_projects_locations_bindings_create_execute()` to send, or `agentregistry_projects_locations_bindings_create` for simplest API.

pub fn agentregistry_projects_locations_bindings_create_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
    bindingId: &Option<Option<String>>,
    requestId: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://agentregistry.googleapis.com/v1alpha/projects/{}/locations/{locationsId}/bindings",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = bindingId.as_ref() {
        query_parts.push(format!("bindingId={}", val));
    }
    if let Some(val) = requestId.as_ref() {
        query_parts.push(format!("requestId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .post(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1alpha/projects/{projectsId}/locations/{locationsId}/bindings
/// Creates a new Binding in a given project and location.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `agentregistry_projects_locations_bindings_create_execute()` or `agentregistry_projects_locations_bindings_create`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `agentregistry_projects_locations_bindings_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn agentregistry_projects_locations_bindings_create_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1alpha/projects/{projectsId}/locations/{locationsId}/bindings
/// Creates a new Binding in a given project and location.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `agentregistry_projects_locations_bindings_create_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `agentregistry_projects_locations_bindings_create_task()`.
/// For the simplest API, use `agentregistry_projects_locations_bindings_create()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `agentregistry_projects_locations_bindings_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn agentregistry_projects_locations_bindings_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = agentregistry_projects_locations_bindings_create_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`agentregistry_projects_locations_bindings_create`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AgentregistryProjectsLocationsBindingsCreateArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: bindingId
    pub bindingId: Option<Option<String>>,
    /// Query parameter: requestId
    pub requestId: Option<Option<String>>,
}

/// POST v1alpha/projects/{projectsId}/locations/{locationsId}/bindings
/// Creates a new Binding in a given project and location.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `agentregistry_projects_locations_bindings_create_builder()` + `agentregistry_projects_locations_bindings_create_execute()`.
/// For task-level control, use `agentregistry_projects_locations_bindings_create_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn agentregistry_projects_locations_bindings_create(
    client: &SimpleHttpClient,
    args: &AgentregistryProjectsLocationsBindingsCreateArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = agentregistry_projects_locations_bindings_create_builder(
        client,
        &args.parent,
        &args.bindingId,
        &args.requestId,
    )?;
    agentregistry_projects_locations_bindings_create_execute(builder)
}

/// DELETE v1alpha/projects/{projectsId}/locations/{locationsId}/bindings/{bindingsId}
/// Deletes a single Binding.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `agentregistry_projects_locations_bindings_delete_execute()` to send, or `agentregistry_projects_locations_bindings_delete` for simplest API.

pub fn agentregistry_projects_locations_bindings_delete_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
    requestId: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://agentregistry.googleapis.com/v1alpha/projects/{}/locations/{locationsId}/bindings/{bindingsId}",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = requestId.as_ref() {
        query_parts.push(format!("requestId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .delete(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// DELETE v1alpha/projects/{projectsId}/locations/{locationsId}/bindings/{bindingsId}
/// Deletes a single Binding.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `agentregistry_projects_locations_bindings_delete_execute()` or `agentregistry_projects_locations_bindings_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `agentregistry_projects_locations_bindings_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn agentregistry_projects_locations_bindings_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// DELETE v1alpha/projects/{projectsId}/locations/{locationsId}/bindings/{bindingsId}
/// Deletes a single Binding.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `agentregistry_projects_locations_bindings_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `agentregistry_projects_locations_bindings_delete_task()`.
/// For the simplest API, use `agentregistry_projects_locations_bindings_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `agentregistry_projects_locations_bindings_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn agentregistry_projects_locations_bindings_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = agentregistry_projects_locations_bindings_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`agentregistry_projects_locations_bindings_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AgentregistryProjectsLocationsBindingsDeleteArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: requestId
    pub requestId: Option<Option<String>>,
}

/// DELETE v1alpha/projects/{projectsId}/locations/{locationsId}/bindings/{bindingsId}
/// Deletes a single Binding.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `agentregistry_projects_locations_bindings_delete_builder()` + `agentregistry_projects_locations_bindings_delete_execute()`.
/// For task-level control, use `agentregistry_projects_locations_bindings_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn agentregistry_projects_locations_bindings_delete(
    client: &SimpleHttpClient,
    args: &AgentregistryProjectsLocationsBindingsDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = agentregistry_projects_locations_bindings_delete_builder(
        client,
        &args.name,
        &args.requestId,
    )?;
    agentregistry_projects_locations_bindings_delete_execute(builder)
}

/// GET v1alpha/projects/{projectsId}/locations/{locationsId}/bindings:fetchAvailable
/// Fetches available Bindings.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `agentregistry_projects_locations_bindings_fetch_available_execute()` to send, or `agentregistry_projects_locations_bindings_fetch_available` for simplest API.

pub fn agentregistry_projects_locations_bindings_fetch_available_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
    pageSize: &Option<Option<String>>,
    pageToken: &Option<Option<String>>,
    sourceIdentifier: &Option<Option<String>>,
    targetIdentifier: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://agentregistry.googleapis.com/v1alpha/projects/{}/locations/{locationsId}/bindings:fetchAvailable",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = pageSize.as_ref() {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = sourceIdentifier.as_ref() {
        query_parts.push(format!("sourceIdentifier={}", val));
    }
    if let Some(val) = targetIdentifier.as_ref() {
        query_parts.push(format!("targetIdentifier={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1alpha/projects/{projectsId}/locations/{locationsId}/bindings:fetchAvailable
/// Fetches available Bindings.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `agentregistry_projects_locations_bindings_fetch_available_execute()` or `agentregistry_projects_locations_bindings_fetch_available`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `agentregistry_projects_locations_bindings_fetch_available_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn agentregistry_projects_locations_bindings_fetch_available_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<FetchAvailableBindingsResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: FetchAvailableBindingsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1alpha/projects/{projectsId}/locations/{locationsId}/bindings:fetchAvailable
/// Fetches available Bindings.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `agentregistry_projects_locations_bindings_fetch_available_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `agentregistry_projects_locations_bindings_fetch_available_task()`.
/// For the simplest API, use `agentregistry_projects_locations_bindings_fetch_available()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `agentregistry_projects_locations_bindings_fetch_available_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn agentregistry_projects_locations_bindings_fetch_available_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<FetchAvailableBindingsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = agentregistry_projects_locations_bindings_fetch_available_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`agentregistry_projects_locations_bindings_fetch_available`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AgentregistryProjectsLocationsBindingsFetchAvailableArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: pageSize
    pub pageSize: Option<Option<String>>,
    /// Query parameter: pageToken
    pub pageToken: Option<Option<String>>,
    /// Query parameter: sourceIdentifier
    pub sourceIdentifier: Option<Option<String>>,
    /// Query parameter: targetIdentifier
    pub targetIdentifier: Option<Option<String>>,
}

/// GET v1alpha/projects/{projectsId}/locations/{locationsId}/bindings:fetchAvailable
/// Fetches available Bindings.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `agentregistry_projects_locations_bindings_fetch_available_builder()` + `agentregistry_projects_locations_bindings_fetch_available_execute()`.
/// For task-level control, use `agentregistry_projects_locations_bindings_fetch_available_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn agentregistry_projects_locations_bindings_fetch_available(
    client: &SimpleHttpClient,
    args: &AgentregistryProjectsLocationsBindingsFetchAvailableArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<FetchAvailableBindingsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = agentregistry_projects_locations_bindings_fetch_available_builder(
        client,
        &args.parent,
        &args.pageSize,
        &args.pageToken,
        &args.sourceIdentifier,
        &args.targetIdentifier,
    )?;
    agentregistry_projects_locations_bindings_fetch_available_execute(builder)
}

/// GET v1alpha/projects/{projectsId}/locations/{locationsId}/bindings/{bindingsId}
/// Gets details of a single Binding.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `agentregistry_projects_locations_bindings_get_execute()` to send, or `agentregistry_projects_locations_bindings_get` for simplest API.

pub fn agentregistry_projects_locations_bindings_get_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://agentregistry.googleapis.com/v1alpha/projects/{}/locations/{locationsId}/bindings/{bindingsId}",
        name,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1alpha/projects/{projectsId}/locations/{locationsId}/bindings/{bindingsId}
/// Gets details of a single Binding.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `agentregistry_projects_locations_bindings_get_execute()` or `agentregistry_projects_locations_bindings_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `agentregistry_projects_locations_bindings_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn agentregistry_projects_locations_bindings_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Binding>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Binding = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1alpha/projects/{projectsId}/locations/{locationsId}/bindings/{bindingsId}
/// Gets details of a single Binding.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `agentregistry_projects_locations_bindings_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `agentregistry_projects_locations_bindings_get_task()`.
/// For the simplest API, use `agentregistry_projects_locations_bindings_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `agentregistry_projects_locations_bindings_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn agentregistry_projects_locations_bindings_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Binding>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = agentregistry_projects_locations_bindings_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`agentregistry_projects_locations_bindings_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AgentregistryProjectsLocationsBindingsGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1alpha/projects/{projectsId}/locations/{locationsId}/bindings/{bindingsId}
/// Gets details of a single Binding.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `agentregistry_projects_locations_bindings_get_builder()` + `agentregistry_projects_locations_bindings_get_execute()`.
/// For task-level control, use `agentregistry_projects_locations_bindings_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn agentregistry_projects_locations_bindings_get(
    client: &SimpleHttpClient,
    args: &AgentregistryProjectsLocationsBindingsGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Binding>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = agentregistry_projects_locations_bindings_get_builder(client, &args.name)?;
    agentregistry_projects_locations_bindings_get_execute(builder)
}

/// GET v1alpha/projects/{projectsId}/locations/{locationsId}/bindings
/// Lists Bindings in a given project and location.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `agentregistry_projects_locations_bindings_list_execute()` to send, or `agentregistry_projects_locations_bindings_list` for simplest API.

pub fn agentregistry_projects_locations_bindings_list_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
    filter: &Option<Option<String>>,
    orderBy: &Option<Option<String>>,
    pageSize: &Option<Option<String>>,
    pageToken: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://agentregistry.googleapis.com/v1alpha/projects/{}/locations/{locationsId}/bindings",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter.as_ref() {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = orderBy.as_ref() {
        query_parts.push(format!("orderBy={}", val));
    }
    if let Some(val) = pageSize.as_ref() {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1alpha/projects/{projectsId}/locations/{locationsId}/bindings
/// Lists Bindings in a given project and location.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `agentregistry_projects_locations_bindings_list_execute()` or `agentregistry_projects_locations_bindings_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `agentregistry_projects_locations_bindings_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn agentregistry_projects_locations_bindings_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ListBindingsResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListBindingsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1alpha/projects/{projectsId}/locations/{locationsId}/bindings
/// Lists Bindings in a given project and location.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `agentregistry_projects_locations_bindings_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `agentregistry_projects_locations_bindings_list_task()`.
/// For the simplest API, use `agentregistry_projects_locations_bindings_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `agentregistry_projects_locations_bindings_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn agentregistry_projects_locations_bindings_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListBindingsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = agentregistry_projects_locations_bindings_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`agentregistry_projects_locations_bindings_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AgentregistryProjectsLocationsBindingsListArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: filter
    pub filter: Option<Option<String>>,
    /// Query parameter: orderBy
    pub orderBy: Option<Option<String>>,
    /// Query parameter: pageSize
    pub pageSize: Option<Option<String>>,
    /// Query parameter: pageToken
    pub pageToken: Option<Option<String>>,
}

/// GET v1alpha/projects/{projectsId}/locations/{locationsId}/bindings
/// Lists Bindings in a given project and location.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `agentregistry_projects_locations_bindings_list_builder()` + `agentregistry_projects_locations_bindings_list_execute()`.
/// For task-level control, use `agentregistry_projects_locations_bindings_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn agentregistry_projects_locations_bindings_list(
    client: &SimpleHttpClient,
    args: &AgentregistryProjectsLocationsBindingsListArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListBindingsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = agentregistry_projects_locations_bindings_list_builder(
        client,
        &args.parent,
        &args.filter,
        &args.orderBy,
        &args.pageSize,
        &args.pageToken,
    )?;
    agentregistry_projects_locations_bindings_list_execute(builder)
}

/// PATCH v1alpha/projects/{projectsId}/locations/{locationsId}/bindings/{bindingsId}
/// Updates the parameters of a single Binding.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `agentregistry_projects_locations_bindings_patch_execute()` to send, or `agentregistry_projects_locations_bindings_patch` for simplest API.

pub fn agentregistry_projects_locations_bindings_patch_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
    requestId: &Option<Option<String>>,
    updateMask: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://agentregistry.googleapis.com/v1alpha/projects/{}/locations/{locationsId}/bindings/{bindingsId}",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = requestId.as_ref() {
        query_parts.push(format!("requestId={}", val));
    }
    if let Some(val) = updateMask.as_ref() {
        query_parts.push(format!("updateMask={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .patch(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// PATCH v1alpha/projects/{projectsId}/locations/{locationsId}/bindings/{bindingsId}
/// Updates the parameters of a single Binding.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `agentregistry_projects_locations_bindings_patch_execute()` or `agentregistry_projects_locations_bindings_patch`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `agentregistry_projects_locations_bindings_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn agentregistry_projects_locations_bindings_patch_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// PATCH v1alpha/projects/{projectsId}/locations/{locationsId}/bindings/{bindingsId}
/// Updates the parameters of a single Binding.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `agentregistry_projects_locations_bindings_patch_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `agentregistry_projects_locations_bindings_patch_task()`.
/// For the simplest API, use `agentregistry_projects_locations_bindings_patch()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `agentregistry_projects_locations_bindings_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn agentregistry_projects_locations_bindings_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = agentregistry_projects_locations_bindings_patch_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`agentregistry_projects_locations_bindings_patch`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AgentregistryProjectsLocationsBindingsPatchArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: requestId
    pub requestId: Option<Option<String>>,
    /// Query parameter: updateMask
    pub updateMask: Option<Option<String>>,
}

/// PATCH v1alpha/projects/{projectsId}/locations/{locationsId}/bindings/{bindingsId}
/// Updates the parameters of a single Binding.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `agentregistry_projects_locations_bindings_patch_builder()` + `agentregistry_projects_locations_bindings_patch_execute()`.
/// For task-level control, use `agentregistry_projects_locations_bindings_patch_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn agentregistry_projects_locations_bindings_patch(
    client: &SimpleHttpClient,
    args: &AgentregistryProjectsLocationsBindingsPatchArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = agentregistry_projects_locations_bindings_patch_builder(
        client,
        &args.name,
        &args.requestId,
        &args.updateMask,
    )?;
    agentregistry_projects_locations_bindings_patch_execute(builder)
}

/// GET v1alpha/projects/{projectsId}/locations/{locationsId}/endpoints/{endpointsId}
/// Gets details of a single Endpoint.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `agentregistry_projects_locations_endpoints_get_execute()` to send, or `agentregistry_projects_locations_endpoints_get` for simplest API.

pub fn agentregistry_projects_locations_endpoints_get_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://agentregistry.googleapis.com/v1alpha/projects/{}/locations/{locationsId}/endpoints/{endpointsId}",
        name,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1alpha/projects/{projectsId}/locations/{locationsId}/endpoints/{endpointsId}
/// Gets details of a single Endpoint.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `agentregistry_projects_locations_endpoints_get_execute()` or `agentregistry_projects_locations_endpoints_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `agentregistry_projects_locations_endpoints_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn agentregistry_projects_locations_endpoints_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Endpoint>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Endpoint = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1alpha/projects/{projectsId}/locations/{locationsId}/endpoints/{endpointsId}
/// Gets details of a single Endpoint.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `agentregistry_projects_locations_endpoints_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `agentregistry_projects_locations_endpoints_get_task()`.
/// For the simplest API, use `agentregistry_projects_locations_endpoints_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `agentregistry_projects_locations_endpoints_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn agentregistry_projects_locations_endpoints_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Endpoint>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = agentregistry_projects_locations_endpoints_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`agentregistry_projects_locations_endpoints_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AgentregistryProjectsLocationsEndpointsGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1alpha/projects/{projectsId}/locations/{locationsId}/endpoints/{endpointsId}
/// Gets details of a single Endpoint.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `agentregistry_projects_locations_endpoints_get_builder()` + `agentregistry_projects_locations_endpoints_get_execute()`.
/// For task-level control, use `agentregistry_projects_locations_endpoints_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn agentregistry_projects_locations_endpoints_get(
    client: &SimpleHttpClient,
    args: &AgentregistryProjectsLocationsEndpointsGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Endpoint>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = agentregistry_projects_locations_endpoints_get_builder(client, &args.name)?;
    agentregistry_projects_locations_endpoints_get_execute(builder)
}

/// GET v1alpha/projects/{projectsId}/locations/{locationsId}/endpoints
/// Lists Endpoints in a given project and location.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `agentregistry_projects_locations_endpoints_list_execute()` to send, or `agentregistry_projects_locations_endpoints_list` for simplest API.

pub fn agentregistry_projects_locations_endpoints_list_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
    filter: &Option<Option<String>>,
    pageSize: &Option<Option<String>>,
    pageToken: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://agentregistry.googleapis.com/v1alpha/projects/{}/locations/{locationsId}/endpoints",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter.as_ref() {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = pageSize.as_ref() {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1alpha/projects/{projectsId}/locations/{locationsId}/endpoints
/// Lists Endpoints in a given project and location.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `agentregistry_projects_locations_endpoints_list_execute()` or `agentregistry_projects_locations_endpoints_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `agentregistry_projects_locations_endpoints_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn agentregistry_projects_locations_endpoints_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ListEndpointsResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListEndpointsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1alpha/projects/{projectsId}/locations/{locationsId}/endpoints
/// Lists Endpoints in a given project and location.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `agentregistry_projects_locations_endpoints_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `agentregistry_projects_locations_endpoints_list_task()`.
/// For the simplest API, use `agentregistry_projects_locations_endpoints_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `agentregistry_projects_locations_endpoints_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn agentregistry_projects_locations_endpoints_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListEndpointsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = agentregistry_projects_locations_endpoints_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`agentregistry_projects_locations_endpoints_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AgentregistryProjectsLocationsEndpointsListArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: filter
    pub filter: Option<Option<String>>,
    /// Query parameter: pageSize
    pub pageSize: Option<Option<String>>,
    /// Query parameter: pageToken
    pub pageToken: Option<Option<String>>,
}

/// GET v1alpha/projects/{projectsId}/locations/{locationsId}/endpoints
/// Lists Endpoints in a given project and location.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `agentregistry_projects_locations_endpoints_list_builder()` + `agentregistry_projects_locations_endpoints_list_execute()`.
/// For task-level control, use `agentregistry_projects_locations_endpoints_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn agentregistry_projects_locations_endpoints_list(
    client: &SimpleHttpClient,
    args: &AgentregistryProjectsLocationsEndpointsListArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListEndpointsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = agentregistry_projects_locations_endpoints_list_builder(
        client,
        &args.parent,
        &args.filter,
        &args.pageSize,
        &args.pageToken,
    )?;
    agentregistry_projects_locations_endpoints_list_execute(builder)
}

/// GET v1alpha/projects/{projectsId}/locations/{locationsId}/mcpServers/{mcpServersId}
/// Gets details of a single McpServer.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `agentregistry_projects_locations_mcp_servers_get_execute()` to send, or `agentregistry_projects_locations_mcp_servers_get` for simplest API.

pub fn agentregistry_projects_locations_mcp_servers_get_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://agentregistry.googleapis.com/v1alpha/projects/{}/locations/{locationsId}/mcpServers/{mcpServersId}",
        name,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1alpha/projects/{projectsId}/locations/{locationsId}/mcpServers/{mcpServersId}
/// Gets details of a single McpServer.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `agentregistry_projects_locations_mcp_servers_get_execute()` or `agentregistry_projects_locations_mcp_servers_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `agentregistry_projects_locations_mcp_servers_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn agentregistry_projects_locations_mcp_servers_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<McpServer>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: McpServer = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1alpha/projects/{projectsId}/locations/{locationsId}/mcpServers/{mcpServersId}
/// Gets details of a single McpServer.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `agentregistry_projects_locations_mcp_servers_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `agentregistry_projects_locations_mcp_servers_get_task()`.
/// For the simplest API, use `agentregistry_projects_locations_mcp_servers_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `agentregistry_projects_locations_mcp_servers_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn agentregistry_projects_locations_mcp_servers_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<McpServer>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = agentregistry_projects_locations_mcp_servers_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`agentregistry_projects_locations_mcp_servers_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AgentregistryProjectsLocationsMcpServersGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1alpha/projects/{projectsId}/locations/{locationsId}/mcpServers/{mcpServersId}
/// Gets details of a single McpServer.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `agentregistry_projects_locations_mcp_servers_get_builder()` + `agentregistry_projects_locations_mcp_servers_get_execute()`.
/// For task-level control, use `agentregistry_projects_locations_mcp_servers_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn agentregistry_projects_locations_mcp_servers_get(
    client: &SimpleHttpClient,
    args: &AgentregistryProjectsLocationsMcpServersGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<McpServer>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = agentregistry_projects_locations_mcp_servers_get_builder(client, &args.name)?;
    agentregistry_projects_locations_mcp_servers_get_execute(builder)
}

/// GET v1alpha/projects/{projectsId}/locations/{locationsId}/mcpServers
/// Lists McpServers in a given project and location.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `agentregistry_projects_locations_mcp_servers_list_execute()` to send, or `agentregistry_projects_locations_mcp_servers_list` for simplest API.

pub fn agentregistry_projects_locations_mcp_servers_list_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
    filter: &Option<Option<String>>,
    orderBy: &Option<Option<String>>,
    pageSize: &Option<Option<String>>,
    pageToken: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://agentregistry.googleapis.com/v1alpha/projects/{}/locations/{locationsId}/mcpServers",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter.as_ref() {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = orderBy.as_ref() {
        query_parts.push(format!("orderBy={}", val));
    }
    if let Some(val) = pageSize.as_ref() {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1alpha/projects/{projectsId}/locations/{locationsId}/mcpServers
/// Lists McpServers in a given project and location.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `agentregistry_projects_locations_mcp_servers_list_execute()` or `agentregistry_projects_locations_mcp_servers_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `agentregistry_projects_locations_mcp_servers_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn agentregistry_projects_locations_mcp_servers_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ListMcpServersResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListMcpServersResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1alpha/projects/{projectsId}/locations/{locationsId}/mcpServers
/// Lists McpServers in a given project and location.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `agentregistry_projects_locations_mcp_servers_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `agentregistry_projects_locations_mcp_servers_list_task()`.
/// For the simplest API, use `agentregistry_projects_locations_mcp_servers_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `agentregistry_projects_locations_mcp_servers_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn agentregistry_projects_locations_mcp_servers_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListMcpServersResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = agentregistry_projects_locations_mcp_servers_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`agentregistry_projects_locations_mcp_servers_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AgentregistryProjectsLocationsMcpServersListArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: filter
    pub filter: Option<Option<String>>,
    /// Query parameter: orderBy
    pub orderBy: Option<Option<String>>,
    /// Query parameter: pageSize
    pub pageSize: Option<Option<String>>,
    /// Query parameter: pageToken
    pub pageToken: Option<Option<String>>,
}

/// GET v1alpha/projects/{projectsId}/locations/{locationsId}/mcpServers
/// Lists McpServers in a given project and location.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `agentregistry_projects_locations_mcp_servers_list_builder()` + `agentregistry_projects_locations_mcp_servers_list_execute()`.
/// For task-level control, use `agentregistry_projects_locations_mcp_servers_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn agentregistry_projects_locations_mcp_servers_list(
    client: &SimpleHttpClient,
    args: &AgentregistryProjectsLocationsMcpServersListArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListMcpServersResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = agentregistry_projects_locations_mcp_servers_list_builder(
        client,
        &args.parent,
        &args.filter,
        &args.orderBy,
        &args.pageSize,
        &args.pageToken,
    )?;
    agentregistry_projects_locations_mcp_servers_list_execute(builder)
}

/// POST v1alpha/projects/{projectsId}/locations/{locationsId}/mcpServers:search
/// Searches McpServers in a given project and location.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `agentregistry_projects_locations_mcp_servers_search_execute()` to send, or `agentregistry_projects_locations_mcp_servers_search` for simplest API.

pub fn agentregistry_projects_locations_mcp_servers_search_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://agentregistry.googleapis.com/v1alpha/projects/{}/locations/{locationsId}/mcpServers:search",
        parent,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1alpha/projects/{projectsId}/locations/{locationsId}/mcpServers:search
/// Searches McpServers in a given project and location.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `agentregistry_projects_locations_mcp_servers_search_execute()` or `agentregistry_projects_locations_mcp_servers_search`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `agentregistry_projects_locations_mcp_servers_search_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn agentregistry_projects_locations_mcp_servers_search_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<SearchMcpServersResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: SearchMcpServersResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1alpha/projects/{projectsId}/locations/{locationsId}/mcpServers:search
/// Searches McpServers in a given project and location.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `agentregistry_projects_locations_mcp_servers_search_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `agentregistry_projects_locations_mcp_servers_search_task()`.
/// For the simplest API, use `agentregistry_projects_locations_mcp_servers_search()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `agentregistry_projects_locations_mcp_servers_search_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn agentregistry_projects_locations_mcp_servers_search_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SearchMcpServersResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = agentregistry_projects_locations_mcp_servers_search_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`agentregistry_projects_locations_mcp_servers_search`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AgentregistryProjectsLocationsMcpServersSearchArgs {
    /// Path parameter: parent
    pub parent: String,
}

/// POST v1alpha/projects/{projectsId}/locations/{locationsId}/mcpServers:search
/// Searches McpServers in a given project and location.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `agentregistry_projects_locations_mcp_servers_search_builder()` + `agentregistry_projects_locations_mcp_servers_search_execute()`.
/// For task-level control, use `agentregistry_projects_locations_mcp_servers_search_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn agentregistry_projects_locations_mcp_servers_search(
    client: &SimpleHttpClient,
    args: &AgentregistryProjectsLocationsMcpServersSearchArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SearchMcpServersResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        agentregistry_projects_locations_mcp_servers_search_builder(client, &args.parent)?;
    agentregistry_projects_locations_mcp_servers_search_execute(builder)
}

/// POST v1alpha/projects/{projectsId}/locations/{locationsId}/operations/{operationsId}:cancel
/// Starts asynchronous cancellation on a long-running operation. The server makes a best effort to cancel the operation, but success is not guaranteed. If the server doesn''t support this method, it returns google.rpc.Code.UNIMPLEMENTED. Clients can use Operations.GetOperation or other methods to check whether the cancellation succeeded or whether the operation completed despite cancellation. On successful cancellation, the operation is not deleted; instead, it becomes an operation with an Operation.error value with a google.rpc.Status.code of 1, corresponding to Code.CANCELLED.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `agentregistry_projects_locations_operations_cancel_execute()` to send, or `agentregistry_projects_locations_operations_cancel` for simplest API.

pub fn agentregistry_projects_locations_operations_cancel_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://agentregistry.googleapis.com/v1alpha/projects/{}/locations/{locationsId}/operations/{operationsId}:cancel",
        name,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1alpha/projects/{projectsId}/locations/{locationsId}/operations/{operationsId}:cancel
/// Starts asynchronous cancellation on a long-running operation. The server makes a best effort to cancel the operation, but success is not guaranteed. If the server doesn''t support this method, it returns google.rpc.Code.UNIMPLEMENTED. Clients can use Operations.GetOperation or other methods to check whether the cancellation succeeded or whether the operation completed despite cancellation. On successful cancellation, the operation is not deleted; instead, it becomes an operation with an Operation.error value with a google.rpc.Status.code of 1, corresponding to Code.CANCELLED.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `agentregistry_projects_locations_operations_cancel_execute()` or `agentregistry_projects_locations_operations_cancel`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `agentregistry_projects_locations_operations_cancel_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn agentregistry_projects_locations_operations_cancel_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Empty>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Empty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1alpha/projects/{projectsId}/locations/{locationsId}/operations/{operationsId}:cancel
/// Starts asynchronous cancellation on a long-running operation. The server makes a best effort to cancel the operation, but success is not guaranteed. If the server doesn''t support this method, it returns google.rpc.Code.UNIMPLEMENTED. Clients can use Operations.GetOperation or other methods to check whether the cancellation succeeded or whether the operation completed despite cancellation. On successful cancellation, the operation is not deleted; instead, it becomes an operation with an Operation.error value with a google.rpc.Status.code of 1, corresponding to Code.CANCELLED.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `agentregistry_projects_locations_operations_cancel_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `agentregistry_projects_locations_operations_cancel_task()`.
/// For the simplest API, use `agentregistry_projects_locations_operations_cancel()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `agentregistry_projects_locations_operations_cancel_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn agentregistry_projects_locations_operations_cancel_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = agentregistry_projects_locations_operations_cancel_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`agentregistry_projects_locations_operations_cancel`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AgentregistryProjectsLocationsOperationsCancelArgs {
    /// Path parameter: name
    pub name: String,
}

/// POST v1alpha/projects/{projectsId}/locations/{locationsId}/operations/{operationsId}:cancel
/// Starts asynchronous cancellation on a long-running operation. The server makes a best effort to cancel the operation, but success is not guaranteed. If the server doesn''t support this method, it returns google.rpc.Code.UNIMPLEMENTED. Clients can use Operations.GetOperation or other methods to check whether the cancellation succeeded or whether the operation completed despite cancellation. On successful cancellation, the operation is not deleted; instead, it becomes an operation with an Operation.error value with a google.rpc.Status.code of 1, corresponding to Code.CANCELLED.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `agentregistry_projects_locations_operations_cancel_builder()` + `agentregistry_projects_locations_operations_cancel_execute()`.
/// For task-level control, use `agentregistry_projects_locations_operations_cancel_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn agentregistry_projects_locations_operations_cancel(
    client: &SimpleHttpClient,
    args: &AgentregistryProjectsLocationsOperationsCancelArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = agentregistry_projects_locations_operations_cancel_builder(client, &args.name)?;
    agentregistry_projects_locations_operations_cancel_execute(builder)
}

/// DELETE v1alpha/projects/{projectsId}/locations/{locationsId}/operations/{operationsId}
/// Deletes a long-running operation. This method indicates that the client is no longer interested in the operation result. It does not cancel the operation. If the server doesn''t support this method, it returns google.rpc.Code.UNIMPLEMENTED.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `agentregistry_projects_locations_operations_delete_execute()` to send, or `agentregistry_projects_locations_operations_delete` for simplest API.

pub fn agentregistry_projects_locations_operations_delete_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://agentregistry.googleapis.com/v1alpha/projects/{}/locations/{locationsId}/operations/{operationsId}",
        name,
    );

    // Build request
    let builder = client
        .delete(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// DELETE v1alpha/projects/{projectsId}/locations/{locationsId}/operations/{operationsId}
/// Deletes a long-running operation. This method indicates that the client is no longer interested in the operation result. It does not cancel the operation. If the server doesn''t support this method, it returns google.rpc.Code.UNIMPLEMENTED.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `agentregistry_projects_locations_operations_delete_execute()` or `agentregistry_projects_locations_operations_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `agentregistry_projects_locations_operations_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn agentregistry_projects_locations_operations_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Empty>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Empty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// DELETE v1alpha/projects/{projectsId}/locations/{locationsId}/operations/{operationsId}
/// Deletes a long-running operation. This method indicates that the client is no longer interested in the operation result. It does not cancel the operation. If the server doesn''t support this method, it returns google.rpc.Code.UNIMPLEMENTED.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `agentregistry_projects_locations_operations_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `agentregistry_projects_locations_operations_delete_task()`.
/// For the simplest API, use `agentregistry_projects_locations_operations_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `agentregistry_projects_locations_operations_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn agentregistry_projects_locations_operations_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = agentregistry_projects_locations_operations_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`agentregistry_projects_locations_operations_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AgentregistryProjectsLocationsOperationsDeleteArgs {
    /// Path parameter: name
    pub name: String,
}

/// DELETE v1alpha/projects/{projectsId}/locations/{locationsId}/operations/{operationsId}
/// Deletes a long-running operation. This method indicates that the client is no longer interested in the operation result. It does not cancel the operation. If the server doesn''t support this method, it returns google.rpc.Code.UNIMPLEMENTED.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `agentregistry_projects_locations_operations_delete_builder()` + `agentregistry_projects_locations_operations_delete_execute()`.
/// For task-level control, use `agentregistry_projects_locations_operations_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn agentregistry_projects_locations_operations_delete(
    client: &SimpleHttpClient,
    args: &AgentregistryProjectsLocationsOperationsDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = agentregistry_projects_locations_operations_delete_builder(client, &args.name)?;
    agentregistry_projects_locations_operations_delete_execute(builder)
}

/// GET v1alpha/projects/{projectsId}/locations/{locationsId}/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `agentregistry_projects_locations_operations_get_execute()` to send, or `agentregistry_projects_locations_operations_get` for simplest API.

pub fn agentregistry_projects_locations_operations_get_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://agentregistry.googleapis.com/v1alpha/projects/{}/locations/{locationsId}/operations/{operationsId}",
        name,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1alpha/projects/{projectsId}/locations/{locationsId}/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `agentregistry_projects_locations_operations_get_execute()` or `agentregistry_projects_locations_operations_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `agentregistry_projects_locations_operations_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn agentregistry_projects_locations_operations_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1alpha/projects/{projectsId}/locations/{locationsId}/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `agentregistry_projects_locations_operations_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `agentregistry_projects_locations_operations_get_task()`.
/// For the simplest API, use `agentregistry_projects_locations_operations_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `agentregistry_projects_locations_operations_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn agentregistry_projects_locations_operations_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = agentregistry_projects_locations_operations_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`agentregistry_projects_locations_operations_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AgentregistryProjectsLocationsOperationsGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1alpha/projects/{projectsId}/locations/{locationsId}/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `agentregistry_projects_locations_operations_get_builder()` + `agentregistry_projects_locations_operations_get_execute()`.
/// For task-level control, use `agentregistry_projects_locations_operations_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn agentregistry_projects_locations_operations_get(
    client: &SimpleHttpClient,
    args: &AgentregistryProjectsLocationsOperationsGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = agentregistry_projects_locations_operations_get_builder(client, &args.name)?;
    agentregistry_projects_locations_operations_get_execute(builder)
}

/// GET v1alpha/projects/{projectsId}/locations/{locationsId}/operations
/// Lists operations that match the specified filter in the request. If the server doesn''t support this method, it returns UNIMPLEMENTED.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `agentregistry_projects_locations_operations_list_execute()` to send, or `agentregistry_projects_locations_operations_list` for simplest API.

pub fn agentregistry_projects_locations_operations_list_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
    filter: &Option<Option<String>>,
    pageSize: &Option<Option<String>>,
    pageToken: &Option<Option<String>>,
    returnPartialSuccess: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://agentregistry.googleapis.com/v1alpha/projects/{}/locations/{locationsId}/operations",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter.as_ref() {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = pageSize.as_ref() {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = returnPartialSuccess.as_ref() {
        query_parts.push(format!("returnPartialSuccess={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1alpha/projects/{projectsId}/locations/{locationsId}/operations
/// Lists operations that match the specified filter in the request. If the server doesn''t support this method, it returns UNIMPLEMENTED.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `agentregistry_projects_locations_operations_list_execute()` or `agentregistry_projects_locations_operations_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `agentregistry_projects_locations_operations_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn agentregistry_projects_locations_operations_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ListOperationsResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListOperationsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1alpha/projects/{projectsId}/locations/{locationsId}/operations
/// Lists operations that match the specified filter in the request. If the server doesn''t support this method, it returns UNIMPLEMENTED.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `agentregistry_projects_locations_operations_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `agentregistry_projects_locations_operations_list_task()`.
/// For the simplest API, use `agentregistry_projects_locations_operations_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `agentregistry_projects_locations_operations_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn agentregistry_projects_locations_operations_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListOperationsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = agentregistry_projects_locations_operations_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`agentregistry_projects_locations_operations_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AgentregistryProjectsLocationsOperationsListArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: filter
    pub filter: Option<Option<String>>,
    /// Query parameter: pageSize
    pub pageSize: Option<Option<String>>,
    /// Query parameter: pageToken
    pub pageToken: Option<Option<String>>,
    /// Query parameter: returnPartialSuccess
    pub returnPartialSuccess: Option<Option<String>>,
}

/// GET v1alpha/projects/{projectsId}/locations/{locationsId}/operations
/// Lists operations that match the specified filter in the request. If the server doesn''t support this method, it returns UNIMPLEMENTED.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `agentregistry_projects_locations_operations_list_builder()` + `agentregistry_projects_locations_operations_list_execute()`.
/// For task-level control, use `agentregistry_projects_locations_operations_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn agentregistry_projects_locations_operations_list(
    client: &SimpleHttpClient,
    args: &AgentregistryProjectsLocationsOperationsListArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListOperationsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = agentregistry_projects_locations_operations_list_builder(
        client,
        &args.name,
        &args.filter,
        &args.pageSize,
        &args.pageToken,
        &args.returnPartialSuccess,
    )?;
    agentregistry_projects_locations_operations_list_execute(builder)
}

/// POST v1alpha/projects/{projectsId}/locations/{locationsId}/services
/// Creates a new Service in a given project and location.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `agentregistry_projects_locations_services_create_execute()` to send, or `agentregistry_projects_locations_services_create` for simplest API.

pub fn agentregistry_projects_locations_services_create_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
    requestId: &Option<Option<String>>,
    serviceId: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://agentregistry.googleapis.com/v1alpha/projects/{}/locations/{locationsId}/services",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = requestId.as_ref() {
        query_parts.push(format!("requestId={}", val));
    }
    if let Some(val) = serviceId.as_ref() {
        query_parts.push(format!("serviceId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .post(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1alpha/projects/{projectsId}/locations/{locationsId}/services
/// Creates a new Service in a given project and location.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `agentregistry_projects_locations_services_create_execute()` or `agentregistry_projects_locations_services_create`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `agentregistry_projects_locations_services_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn agentregistry_projects_locations_services_create_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1alpha/projects/{projectsId}/locations/{locationsId}/services
/// Creates a new Service in a given project and location.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `agentregistry_projects_locations_services_create_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `agentregistry_projects_locations_services_create_task()`.
/// For the simplest API, use `agentregistry_projects_locations_services_create()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `agentregistry_projects_locations_services_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn agentregistry_projects_locations_services_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = agentregistry_projects_locations_services_create_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`agentregistry_projects_locations_services_create`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AgentregistryProjectsLocationsServicesCreateArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: requestId
    pub requestId: Option<Option<String>>,
    /// Query parameter: serviceId
    pub serviceId: Option<Option<String>>,
}

/// POST v1alpha/projects/{projectsId}/locations/{locationsId}/services
/// Creates a new Service in a given project and location.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `agentregistry_projects_locations_services_create_builder()` + `agentregistry_projects_locations_services_create_execute()`.
/// For task-level control, use `agentregistry_projects_locations_services_create_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn agentregistry_projects_locations_services_create(
    client: &SimpleHttpClient,
    args: &AgentregistryProjectsLocationsServicesCreateArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = agentregistry_projects_locations_services_create_builder(
        client,
        &args.parent,
        &args.requestId,
        &args.serviceId,
    )?;
    agentregistry_projects_locations_services_create_execute(builder)
}

/// DELETE v1alpha/projects/{projectsId}/locations/{locationsId}/services/{servicesId}
/// Deletes a single Service.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `agentregistry_projects_locations_services_delete_execute()` to send, or `agentregistry_projects_locations_services_delete` for simplest API.

pub fn agentregistry_projects_locations_services_delete_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
    requestId: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://agentregistry.googleapis.com/v1alpha/projects/{}/locations/{locationsId}/services/{servicesId}",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = requestId.as_ref() {
        query_parts.push(format!("requestId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .delete(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// DELETE v1alpha/projects/{projectsId}/locations/{locationsId}/services/{servicesId}
/// Deletes a single Service.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `agentregistry_projects_locations_services_delete_execute()` or `agentregistry_projects_locations_services_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `agentregistry_projects_locations_services_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn agentregistry_projects_locations_services_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// DELETE v1alpha/projects/{projectsId}/locations/{locationsId}/services/{servicesId}
/// Deletes a single Service.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `agentregistry_projects_locations_services_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `agentregistry_projects_locations_services_delete_task()`.
/// For the simplest API, use `agentregistry_projects_locations_services_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `agentregistry_projects_locations_services_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn agentregistry_projects_locations_services_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = agentregistry_projects_locations_services_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`agentregistry_projects_locations_services_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AgentregistryProjectsLocationsServicesDeleteArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: requestId
    pub requestId: Option<Option<String>>,
}

/// DELETE v1alpha/projects/{projectsId}/locations/{locationsId}/services/{servicesId}
/// Deletes a single Service.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `agentregistry_projects_locations_services_delete_builder()` + `agentregistry_projects_locations_services_delete_execute()`.
/// For task-level control, use `agentregistry_projects_locations_services_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn agentregistry_projects_locations_services_delete(
    client: &SimpleHttpClient,
    args: &AgentregistryProjectsLocationsServicesDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = agentregistry_projects_locations_services_delete_builder(
        client,
        &args.name,
        &args.requestId,
    )?;
    agentregistry_projects_locations_services_delete_execute(builder)
}

/// GET v1alpha/projects/{projectsId}/locations/{locationsId}/services/{servicesId}
/// Gets details of a single Service.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `agentregistry_projects_locations_services_get_execute()` to send, or `agentregistry_projects_locations_services_get` for simplest API.

pub fn agentregistry_projects_locations_services_get_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://agentregistry.googleapis.com/v1alpha/projects/{}/locations/{locationsId}/services/{servicesId}",
        name,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1alpha/projects/{projectsId}/locations/{locationsId}/services/{servicesId}
/// Gets details of a single Service.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `agentregistry_projects_locations_services_get_execute()` or `agentregistry_projects_locations_services_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `agentregistry_projects_locations_services_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn agentregistry_projects_locations_services_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Service>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Service = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1alpha/projects/{projectsId}/locations/{locationsId}/services/{servicesId}
/// Gets details of a single Service.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `agentregistry_projects_locations_services_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `agentregistry_projects_locations_services_get_task()`.
/// For the simplest API, use `agentregistry_projects_locations_services_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `agentregistry_projects_locations_services_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn agentregistry_projects_locations_services_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Service>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = agentregistry_projects_locations_services_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`agentregistry_projects_locations_services_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AgentregistryProjectsLocationsServicesGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1alpha/projects/{projectsId}/locations/{locationsId}/services/{servicesId}
/// Gets details of a single Service.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `agentregistry_projects_locations_services_get_builder()` + `agentregistry_projects_locations_services_get_execute()`.
/// For task-level control, use `agentregistry_projects_locations_services_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn agentregistry_projects_locations_services_get(
    client: &SimpleHttpClient,
    args: &AgentregistryProjectsLocationsServicesGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Service>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = agentregistry_projects_locations_services_get_builder(client, &args.name)?;
    agentregistry_projects_locations_services_get_execute(builder)
}

/// GET v1alpha/projects/{projectsId}/locations/{locationsId}/services
/// Lists Services in a given project and location.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `agentregistry_projects_locations_services_list_execute()` to send, or `agentregistry_projects_locations_services_list` for simplest API.

pub fn agentregistry_projects_locations_services_list_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
    filter: &Option<Option<String>>,
    pageSize: &Option<Option<String>>,
    pageToken: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://agentregistry.googleapis.com/v1alpha/projects/{}/locations/{locationsId}/services",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter.as_ref() {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = pageSize.as_ref() {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1alpha/projects/{projectsId}/locations/{locationsId}/services
/// Lists Services in a given project and location.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `agentregistry_projects_locations_services_list_execute()` or `agentregistry_projects_locations_services_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `agentregistry_projects_locations_services_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn agentregistry_projects_locations_services_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ListServicesResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListServicesResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1alpha/projects/{projectsId}/locations/{locationsId}/services
/// Lists Services in a given project and location.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `agentregistry_projects_locations_services_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `agentregistry_projects_locations_services_list_task()`.
/// For the simplest API, use `agentregistry_projects_locations_services_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `agentregistry_projects_locations_services_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn agentregistry_projects_locations_services_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListServicesResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = agentregistry_projects_locations_services_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`agentregistry_projects_locations_services_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AgentregistryProjectsLocationsServicesListArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: filter
    pub filter: Option<Option<String>>,
    /// Query parameter: pageSize
    pub pageSize: Option<Option<String>>,
    /// Query parameter: pageToken
    pub pageToken: Option<Option<String>>,
}

/// GET v1alpha/projects/{projectsId}/locations/{locationsId}/services
/// Lists Services in a given project and location.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `agentregistry_projects_locations_services_list_builder()` + `agentregistry_projects_locations_services_list_execute()`.
/// For task-level control, use `agentregistry_projects_locations_services_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn agentregistry_projects_locations_services_list(
    client: &SimpleHttpClient,
    args: &AgentregistryProjectsLocationsServicesListArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListServicesResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = agentregistry_projects_locations_services_list_builder(
        client,
        &args.parent,
        &args.filter,
        &args.pageSize,
        &args.pageToken,
    )?;
    agentregistry_projects_locations_services_list_execute(builder)
}

/// PATCH v1alpha/projects/{projectsId}/locations/{locationsId}/services/{servicesId}
/// Updates the parameters of a single Service.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `agentregistry_projects_locations_services_patch_execute()` to send, or `agentregistry_projects_locations_services_patch` for simplest API.

pub fn agentregistry_projects_locations_services_patch_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
    requestId: &Option<Option<String>>,
    updateMask: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://agentregistry.googleapis.com/v1alpha/projects/{}/locations/{locationsId}/services/{servicesId}",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = requestId.as_ref() {
        query_parts.push(format!("requestId={}", val));
    }
    if let Some(val) = updateMask.as_ref() {
        query_parts.push(format!("updateMask={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .patch(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// PATCH v1alpha/projects/{projectsId}/locations/{locationsId}/services/{servicesId}
/// Updates the parameters of a single Service.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `agentregistry_projects_locations_services_patch_execute()` or `agentregistry_projects_locations_services_patch`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `agentregistry_projects_locations_services_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn agentregistry_projects_locations_services_patch_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// PATCH v1alpha/projects/{projectsId}/locations/{locationsId}/services/{servicesId}
/// Updates the parameters of a single Service.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `agentregistry_projects_locations_services_patch_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `agentregistry_projects_locations_services_patch_task()`.
/// For the simplest API, use `agentregistry_projects_locations_services_patch()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `agentregistry_projects_locations_services_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn agentregistry_projects_locations_services_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = agentregistry_projects_locations_services_patch_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`agentregistry_projects_locations_services_patch`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AgentregistryProjectsLocationsServicesPatchArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: requestId
    pub requestId: Option<Option<String>>,
    /// Query parameter: updateMask
    pub updateMask: Option<Option<String>>,
}

/// PATCH v1alpha/projects/{projectsId}/locations/{locationsId}/services/{servicesId}
/// Updates the parameters of a single Service.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `agentregistry_projects_locations_services_patch_builder()` + `agentregistry_projects_locations_services_patch_execute()`.
/// For task-level control, use `agentregistry_projects_locations_services_patch_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn agentregistry_projects_locations_services_patch(
    client: &SimpleHttpClient,
    args: &AgentregistryProjectsLocationsServicesPatchArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = agentregistry_projects_locations_services_patch_builder(
        client,
        &args.name,
        &args.requestId,
        &args.updateMask,
    )?;
    agentregistry_projects_locations_services_patch_execute(builder)
}

// =============================================================================
// ResourceIdentifier implementation for Location
// =============================================================================

/// ResourceIdentifier implementation for Location with AgentregistryProjectsLocationsGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<AgentregistryProjectsLocationsGetArgs> for Location {
    fn generate_resource_id(&self, input: &AgentregistryProjectsLocationsGetArgs) -> String {
        format!("gcp::agentregistry::Location/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::agentregistry::Location"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for ListLocationsResponse
// =============================================================================

/// ResourceIdentifier implementation for ListLocationsResponse with AgentregistryProjectsLocationsListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<AgentregistryProjectsLocationsListArgs> for ListLocationsResponse {
    fn generate_resource_id(&self, input: &AgentregistryProjectsLocationsListArgs) -> String {
        format!("gcp::agentregistry::ListLocationsResponse/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::agentregistry::ListLocationsResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Agent
// =============================================================================

/// ResourceIdentifier implementation for Agent with AgentregistryProjectsLocationsAgentsGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<AgentregistryProjectsLocationsAgentsGetArgs> for Agent {
    fn generate_resource_id(&self, input: &AgentregistryProjectsLocationsAgentsGetArgs) -> String {
        format!("gcp::agentregistry::Agent/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::agentregistry::Agent"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for ListAgentsResponse
// =============================================================================

/// ResourceIdentifier implementation for ListAgentsResponse with AgentregistryProjectsLocationsAgentsListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<AgentregistryProjectsLocationsAgentsListArgs> for ListAgentsResponse {
    fn generate_resource_id(&self, input: &AgentregistryProjectsLocationsAgentsListArgs) -> String {
        format!("gcp::agentregistry::ListAgentsResponse/{}", input.parent)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::agentregistry::ListAgentsResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for SearchAgentsResponse
// =============================================================================

/// ResourceIdentifier implementation for SearchAgentsResponse with AgentregistryProjectsLocationsAgentsSearchArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<AgentregistryProjectsLocationsAgentsSearchArgs> for SearchAgentsResponse {
    fn generate_resource_id(
        &self,
        input: &AgentregistryProjectsLocationsAgentsSearchArgs,
    ) -> String {
        format!("gcp::agentregistry::SearchAgentsResponse/{}", input.parent)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::agentregistry::SearchAgentsResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with AgentregistryProjectsLocationsBindingsCreateArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<AgentregistryProjectsLocationsBindingsCreateArgs> for Operation {
    fn generate_resource_id(
        &self,
        input: &AgentregistryProjectsLocationsBindingsCreateArgs,
    ) -> String {
        format!("gcp::agentregistry::Operation/{}", input.parent)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::agentregistry::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with AgentregistryProjectsLocationsBindingsDeleteArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<AgentregistryProjectsLocationsBindingsDeleteArgs> for Operation {
    fn generate_resource_id(
        &self,
        input: &AgentregistryProjectsLocationsBindingsDeleteArgs,
    ) -> String {
        format!("gcp::agentregistry::Operation/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::agentregistry::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for FetchAvailableBindingsResponse
// =============================================================================

/// ResourceIdentifier implementation for FetchAvailableBindingsResponse with AgentregistryProjectsLocationsBindingsFetchAvailableArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<AgentregistryProjectsLocationsBindingsFetchAvailableArgs>
    for FetchAvailableBindingsResponse
{
    fn generate_resource_id(
        &self,
        input: &AgentregistryProjectsLocationsBindingsFetchAvailableArgs,
    ) -> String {
        format!(
            "gcp::agentregistry::FetchAvailableBindingsResponse/{}",
            input.parent
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::agentregistry::FetchAvailableBindingsResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Binding
// =============================================================================

/// ResourceIdentifier implementation for Binding with AgentregistryProjectsLocationsBindingsGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<AgentregistryProjectsLocationsBindingsGetArgs> for Binding {
    fn generate_resource_id(
        &self,
        input: &AgentregistryProjectsLocationsBindingsGetArgs,
    ) -> String {
        format!("gcp::agentregistry::Binding/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::agentregistry::Binding"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for ListBindingsResponse
// =============================================================================

/// ResourceIdentifier implementation for ListBindingsResponse with AgentregistryProjectsLocationsBindingsListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<AgentregistryProjectsLocationsBindingsListArgs> for ListBindingsResponse {
    fn generate_resource_id(
        &self,
        input: &AgentregistryProjectsLocationsBindingsListArgs,
    ) -> String {
        format!("gcp::agentregistry::ListBindingsResponse/{}", input.parent)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::agentregistry::ListBindingsResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with AgentregistryProjectsLocationsBindingsPatchArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<AgentregistryProjectsLocationsBindingsPatchArgs> for Operation {
    fn generate_resource_id(
        &self,
        input: &AgentregistryProjectsLocationsBindingsPatchArgs,
    ) -> String {
        format!("gcp::agentregistry::Operation/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::agentregistry::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Endpoint
// =============================================================================

/// ResourceIdentifier implementation for Endpoint with AgentregistryProjectsLocationsEndpointsGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<AgentregistryProjectsLocationsEndpointsGetArgs> for Endpoint {
    fn generate_resource_id(
        &self,
        input: &AgentregistryProjectsLocationsEndpointsGetArgs,
    ) -> String {
        format!("gcp::agentregistry::Endpoint/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::agentregistry::Endpoint"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for ListEndpointsResponse
// =============================================================================

/// ResourceIdentifier implementation for ListEndpointsResponse with AgentregistryProjectsLocationsEndpointsListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<AgentregistryProjectsLocationsEndpointsListArgs> for ListEndpointsResponse {
    fn generate_resource_id(
        &self,
        input: &AgentregistryProjectsLocationsEndpointsListArgs,
    ) -> String {
        format!("gcp::agentregistry::ListEndpointsResponse/{}", input.parent)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::agentregistry::ListEndpointsResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for McpServer
// =============================================================================

/// ResourceIdentifier implementation for McpServer with AgentregistryProjectsLocationsMcpServersGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<AgentregistryProjectsLocationsMcpServersGetArgs> for McpServer {
    fn generate_resource_id(
        &self,
        input: &AgentregistryProjectsLocationsMcpServersGetArgs,
    ) -> String {
        format!("gcp::agentregistry::McpServer/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::agentregistry::McpServer"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for ListMcpServersResponse
// =============================================================================

/// ResourceIdentifier implementation for ListMcpServersResponse with AgentregistryProjectsLocationsMcpServersListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<AgentregistryProjectsLocationsMcpServersListArgs>
    for ListMcpServersResponse
{
    fn generate_resource_id(
        &self,
        input: &AgentregistryProjectsLocationsMcpServersListArgs,
    ) -> String {
        format!(
            "gcp::agentregistry::ListMcpServersResponse/{}",
            input.parent
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::agentregistry::ListMcpServersResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for SearchMcpServersResponse
// =============================================================================

/// ResourceIdentifier implementation for SearchMcpServersResponse with AgentregistryProjectsLocationsMcpServersSearchArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<AgentregistryProjectsLocationsMcpServersSearchArgs>
    for SearchMcpServersResponse
{
    fn generate_resource_id(
        &self,
        input: &AgentregistryProjectsLocationsMcpServersSearchArgs,
    ) -> String {
        format!(
            "gcp::agentregistry::SearchMcpServersResponse/{}",
            input.parent
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::agentregistry::SearchMcpServersResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Empty
// =============================================================================

/// ResourceIdentifier implementation for Empty with AgentregistryProjectsLocationsOperationsCancelArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<AgentregistryProjectsLocationsOperationsCancelArgs> for Empty {
    fn generate_resource_id(
        &self,
        input: &AgentregistryProjectsLocationsOperationsCancelArgs,
    ) -> String {
        format!("gcp::agentregistry::Empty/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::agentregistry::Empty"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Empty
// =============================================================================

/// ResourceIdentifier implementation for Empty with AgentregistryProjectsLocationsOperationsDeleteArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<AgentregistryProjectsLocationsOperationsDeleteArgs> for Empty {
    fn generate_resource_id(
        &self,
        input: &AgentregistryProjectsLocationsOperationsDeleteArgs,
    ) -> String {
        format!("gcp::agentregistry::Empty/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::agentregistry::Empty"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with AgentregistryProjectsLocationsOperationsGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<AgentregistryProjectsLocationsOperationsGetArgs> for Operation {
    fn generate_resource_id(
        &self,
        input: &AgentregistryProjectsLocationsOperationsGetArgs,
    ) -> String {
        format!("gcp::agentregistry::Operation/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::agentregistry::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for ListOperationsResponse
// =============================================================================

/// ResourceIdentifier implementation for ListOperationsResponse with AgentregistryProjectsLocationsOperationsListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<AgentregistryProjectsLocationsOperationsListArgs>
    for ListOperationsResponse
{
    fn generate_resource_id(
        &self,
        input: &AgentregistryProjectsLocationsOperationsListArgs,
    ) -> String {
        format!("gcp::agentregistry::ListOperationsResponse/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::agentregistry::ListOperationsResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with AgentregistryProjectsLocationsServicesCreateArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<AgentregistryProjectsLocationsServicesCreateArgs> for Operation {
    fn generate_resource_id(
        &self,
        input: &AgentregistryProjectsLocationsServicesCreateArgs,
    ) -> String {
        format!("gcp::agentregistry::Operation/{}", input.parent)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::agentregistry::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with AgentregistryProjectsLocationsServicesDeleteArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<AgentregistryProjectsLocationsServicesDeleteArgs> for Operation {
    fn generate_resource_id(
        &self,
        input: &AgentregistryProjectsLocationsServicesDeleteArgs,
    ) -> String {
        format!("gcp::agentregistry::Operation/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::agentregistry::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Service
// =============================================================================

/// ResourceIdentifier implementation for Service with AgentregistryProjectsLocationsServicesGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<AgentregistryProjectsLocationsServicesGetArgs> for Service {
    fn generate_resource_id(
        &self,
        input: &AgentregistryProjectsLocationsServicesGetArgs,
    ) -> String {
        format!("gcp::agentregistry::Service/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::agentregistry::Service"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for ListServicesResponse
// =============================================================================

/// ResourceIdentifier implementation for ListServicesResponse with AgentregistryProjectsLocationsServicesListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<AgentregistryProjectsLocationsServicesListArgs> for ListServicesResponse {
    fn generate_resource_id(
        &self,
        input: &AgentregistryProjectsLocationsServicesListArgs,
    ) -> String {
        format!("gcp::agentregistry::ListServicesResponse/{}", input.parent)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::agentregistry::ListServicesResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with AgentregistryProjectsLocationsServicesPatchArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<AgentregistryProjectsLocationsServicesPatchArgs> for Operation {
    fn generate_resource_id(
        &self,
        input: &AgentregistryProjectsLocationsServicesPatchArgs,
    ) -> String {
        format!("gcp::agentregistry::Operation/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::agentregistry::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}
