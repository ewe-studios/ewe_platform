//! Auto-generated API clients for gcp/pollen.
//!
//! Generated by `cargo run --bin ewe_platform gen_provider_clients`.
//! DO NOT EDIT MANUALLY.
//!
//! Feature flag: `gcp`

#![cfg(feature = "gcp")]

use crate::providers::gcp::clients::types::*;
use crate::providers::gcp::resources::*;
use foundation_core::valtron::{
    execute, BoxedSendExecutionAction, StreamIterator, StreamIteratorExt, TaskIterator,
    TaskIteratorExt,
};
use foundation_core::wire::simple_http::client::{
    body_reader, ClientRequestBuilder, DnsResolver, RequestIntro, SimpleHttpClient,
    SystemDnsResolver,
};
use foundation_db::state::resource_identifier::ResourceIdentifier;
use foundation_macros::JsonHash;
use serde::Serialize;

/// GET v1/forecast:lookup
/// Returns up to 5 days of daily pollen information in more than 65 countries, up to 1km resolution.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `pollen_forecast_lookup_execute()` to send, or `pollen_forecast_lookup` for simplest API.

pub fn pollen_forecast_lookup_builder<R>(
    client: &SimpleHttpClient<R>,
    days: &Option<Option<String>>,
    languageCode: &Option<Option<String>>,
    location_latitude: &Option<Option<String>>,
    location_longitude: &Option<Option<String>>,
    pageSize: &Option<Option<String>>,
    pageToken: &Option<Option<String>>,
    plantsDescription: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!("https://pollen.googleapis.com/v1/forecast:lookup",);

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = days.as_ref() {
        query_parts.push(format!("days={}", val));
    }
    if let Some(val) = languageCode.as_ref() {
        query_parts.push(format!("languageCode={}", val));
    }
    if let Some(val) = location_latitude.as_ref() {
        query_parts.push(format!("location.latitude={}", val));
    }
    if let Some(val) = location_longitude.as_ref() {
        query_parts.push(format!("location.longitude={}", val));
    }
    if let Some(val) = pageSize.as_ref() {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = plantsDescription.as_ref() {
        query_parts.push(format!("plantsDescription={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/forecast:lookup
/// Returns up to 5 days of daily pollen information in more than 65 countries, up to 1km resolution.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `pollen_forecast_lookup_execute()` or `pollen_forecast_lookup`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `pollen_forecast_lookup_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn pollen_forecast_lookup_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<LookupForecastResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: LookupForecastResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/forecast:lookup
/// Returns up to 5 days of daily pollen information in more than 65 countries, up to 1km resolution.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `pollen_forecast_lookup_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `pollen_forecast_lookup_task()`.
/// For the simplest API, use `pollen_forecast_lookup()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `pollen_forecast_lookup_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn pollen_forecast_lookup_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<LookupForecastResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = pollen_forecast_lookup_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`pollen_forecast_lookup`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct PollenForecastLookupArgs {
    /// Query parameter: days
    pub days: Option<Option<String>>,
    /// Query parameter: languageCode
    pub languageCode: Option<Option<String>>,
    /// Query parameter: location_latitude
    pub location_latitude: Option<Option<String>>,
    /// Query parameter: location_longitude
    pub location_longitude: Option<Option<String>>,
    /// Query parameter: pageSize
    pub pageSize: Option<Option<String>>,
    /// Query parameter: pageToken
    pub pageToken: Option<Option<String>>,
    /// Query parameter: plantsDescription
    pub plantsDescription: Option<Option<String>>,
}

/// GET v1/forecast:lookup
/// Returns up to 5 days of daily pollen information in more than 65 countries, up to 1km resolution.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `pollen_forecast_lookup_builder()` + `pollen_forecast_lookup_execute()`.
/// For task-level control, use `pollen_forecast_lookup_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn pollen_forecast_lookup(
    client: &SimpleHttpClient,
    args: &PollenForecastLookupArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<LookupForecastResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = pollen_forecast_lookup_builder(
        client,
        &args.days,
        &args.languageCode,
        &args.location_latitude,
        &args.location_longitude,
        &args.pageSize,
        &args.pageToken,
        &args.plantsDescription,
    )?;
    pollen_forecast_lookup_execute(builder)
}

/// GET v1/mapTypes/{mapType}/heatmapTiles/{zoom}/{x}/{y}
/// Returns a byte array containing the data of the tile PNG image.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `pollen_map_types_heatmap_tiles_lookup_heatmap_tile_execute()` to send, or `pollen_map_types_heatmap_tiles_lookup_heatmap_tile` for simplest API.

pub fn pollen_map_types_heatmap_tiles_lookup_heatmap_tile_builder<R>(
    client: &SimpleHttpClient<R>,
    mapType: &String,
    zoom: &String,
    x: &String,
    y: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://pollen.googleapis.com/v1/mapTypes/{}/heatmapTiles/{}/{}/{}",
        mapType, zoom, x, y,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/mapTypes/{mapType}/heatmapTiles/{zoom}/{x}/{y}
/// Returns a byte array containing the data of the tile PNG image.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `pollen_map_types_heatmap_tiles_lookup_heatmap_tile_execute()` or `pollen_map_types_heatmap_tiles_lookup_heatmap_tile`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `pollen_map_types_heatmap_tiles_lookup_heatmap_tile_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn pollen_map_types_heatmap_tiles_lookup_heatmap_tile_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<HttpBody>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: HttpBody = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/mapTypes/{mapType}/heatmapTiles/{zoom}/{x}/{y}
/// Returns a byte array containing the data of the tile PNG image.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `pollen_map_types_heatmap_tiles_lookup_heatmap_tile_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `pollen_map_types_heatmap_tiles_lookup_heatmap_tile_task()`.
/// For the simplest API, use `pollen_map_types_heatmap_tiles_lookup_heatmap_tile()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `pollen_map_types_heatmap_tiles_lookup_heatmap_tile_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn pollen_map_types_heatmap_tiles_lookup_heatmap_tile_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<HttpBody>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = pollen_map_types_heatmap_tiles_lookup_heatmap_tile_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`pollen_map_types_heatmap_tiles_lookup_heatmap_tile`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct PollenMapTypesHeatmapTilesLookupHeatmapTileArgs {
    /// Path parameter: mapType
    pub mapType: String,
    /// Path parameter: zoom
    pub zoom: String,
    /// Path parameter: x
    pub x: String,
    /// Path parameter: y
    pub y: String,
}

/// GET v1/mapTypes/{mapType}/heatmapTiles/{zoom}/{x}/{y}
/// Returns a byte array containing the data of the tile PNG image.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `pollen_map_types_heatmap_tiles_lookup_heatmap_tile_builder()` + `pollen_map_types_heatmap_tiles_lookup_heatmap_tile_execute()`.
/// For task-level control, use `pollen_map_types_heatmap_tiles_lookup_heatmap_tile_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn pollen_map_types_heatmap_tiles_lookup_heatmap_tile(
    client: &SimpleHttpClient,
    args: &PollenMapTypesHeatmapTilesLookupHeatmapTileArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<HttpBody>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = pollen_map_types_heatmap_tiles_lookup_heatmap_tile_builder(
        client,
        &args.mapType,
        &args.zoom,
        &args.x,
        &args.y,
    )?;
    pollen_map_types_heatmap_tiles_lookup_heatmap_tile_execute(builder)
}

// =============================================================================
// ResourceIdentifier implementation for LookupForecastResponse
// =============================================================================

/// ResourceIdentifier implementation for LookupForecastResponse with PollenForecastLookupArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<PollenForecastLookupArgs> for LookupForecastResponse {
    fn generate_resource_id(&self, input: &PollenForecastLookupArgs) -> String {
        "gcp::pollen::LookupForecastResponse".to_string()
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::pollen::LookupForecastResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for HttpBody
// =============================================================================

/// ResourceIdentifier implementation for HttpBody with PollenMapTypesHeatmapTilesLookupHeatmapTileArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<PollenMapTypesHeatmapTilesLookupHeatmapTileArgs> for HttpBody {
    fn generate_resource_id(
        &self,
        input: &PollenMapTypesHeatmapTilesLookupHeatmapTileArgs,
    ) -> String {
        format!(
            "gcp::pollen::HttpBody/{}/{}/{}/{}",
            input.mapType, input.zoom, input.x, input.y
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::pollen::HttpBody"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}
