//! Auto-generated API clients for gcp/pollen.
//!
//! Generated by `cargo run --bin ewe_platform gen_provider_clients`.
//! DO NOT EDIT MANUALLY.

use crate::providers::gcp::clients::types::*;
use crate::providers::gcp::resources::*;
use foundation_core::valtron::{execute, StreamIterator, StreamIteratorExt};
use foundation_core::wire::simple_http::client::{
    body_reader, ClientRequestBuilder, RequestIntro, SimpleHttpClient, SystemDnsResolver,
};

/// GET v1/forecast:lookup
/// Returns up to 5 days of daily pollen information in more than 65 countries, up to 1km resolution.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `pollen_forecast_lookup_execute()` to send, or `pollen_forecast_lookup` for simplest API.

pub fn pollen_forecast_lookup_builder(
    client: &SimpleHttpClient,
    days: Option<i32>,
    languageCode: Option<&str>,
    location_latitude: Option<f64>,
    location_longitude: Option<f64>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
    plantsDescription: Option<bool>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/forecast:lookup",);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "days",
            days.as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "languageCode",
            languageCode
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "location_latitude",
            location_latitude
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "location_longitude",
            location_longitude
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageSize",
            pageSize
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageToken",
            pageToken
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "plantsDescription",
            plantsDescription
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET v1/forecast:lookup
/// Returns up to 5 days of daily pollen information in more than 65 countries, up to 1km resolution.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `pollen_forecast_lookup_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from pollen_forecast_lookup_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn pollen_forecast_lookup_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<LookupForecastResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: LookupForecastResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/forecast:lookup
/// Returns up to 5 days of daily pollen information in more than 65 countries, up to 1km resolution.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `pollen_forecast_lookup_builder()` + `pollen_forecast_lookup_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn pollen_forecast_lookup(
    client: &SimpleHttpClient,
    days: Option<i32>,
    languageCode: Option<&str>,
    location_latitude: Option<f64>,
    location_longitude: Option<f64>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
    plantsDescription: Option<bool>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<LookupForecastResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = pollen_forecast_lookup_builder(
        client,
        days,
        languageCode,
        location_latitude,
        location_longitude,
        pageSize,
        pageToken,
        plantsDescription,
    )?;
    pollen_forecast_lookup_execute(builder)
}

/// GET v1/mapTypes/{mapType}/heatmapTiles/{zoom}/{x}/{y}
/// Returns a byte array containing the data of the tile PNG image.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `pollen_map_types_heatmap_tiles_lookup_heatmap_tile_execute()` to send, or `pollen_map_types_heatmap_tiles_lookup_heatmap_tile` for simplest API.

pub fn pollen_map_types_heatmap_tiles_lookup_heatmap_tile_builder(
    client: &SimpleHttpClient,
    mapType: &str,
    x: &str,
    y: &str,
    zoom: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comv1/mapTypes/{}/heatmapTiles/{}/{}/{}",
        mapType, x, y, zoom,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/mapTypes/{mapType}/heatmapTiles/{zoom}/{x}/{y}
/// Returns a byte array containing the data of the tile PNG image.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `pollen_map_types_heatmap_tiles_lookup_heatmap_tile_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from pollen_map_types_heatmap_tiles_lookup_heatmap_tile_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn pollen_map_types_heatmap_tiles_lookup_heatmap_tile_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<HttpBody>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: HttpBody = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/mapTypes/{mapType}/heatmapTiles/{zoom}/{x}/{y}
/// Returns a byte array containing the data of the tile PNG image.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `pollen_map_types_heatmap_tiles_lookup_heatmap_tile_builder()` + `pollen_map_types_heatmap_tiles_lookup_heatmap_tile_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn pollen_map_types_heatmap_tiles_lookup_heatmap_tile(
    client: &SimpleHttpClient,
    mapType: &str,
    x: &str,
    y: &str,
    zoom: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<HttpBody>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        pollen_map_types_heatmap_tiles_lookup_heatmap_tile_builder(client, mapType, x, y, zoom)?;
    pollen_map_types_heatmap_tiles_lookup_heatmap_tile_execute(builder)
}
