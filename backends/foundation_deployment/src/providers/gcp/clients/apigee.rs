//! Auto-generated API clients for gcp/apigee.
//!
//! Generated by `cargo run --bin ewe_platform gen_provider_clients`.
//! DO NOT EDIT MANUALLY.
//!
//! Feature flag: `gcp/apigee`

#![cfg(feature = "gcp/apigee")]

pub mod types;

use crate::providers::gcp::clients::types::*;
use crate::providers::gcp::resources::*;
use foundation_core::valtron::{execute, StreamIterator, StreamIteratorExt, TaskIteratorExt};
use foundation_core::wire::simple_http::client::{
    body_reader, ClientRequestBuilder, RequestIntro, SimpleHttpClient, SystemDnsResolver,
};

/// GET v1/hybrid/issuers
/// Lists hybrid services and its trusted issuers service account ids. This api is authenticated and unauthorized(allow all the users) and used by runtime authn-authz service to query control plane's issuer service account ids.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `apigee_hybrid_issuers_list_execute()` to send, or `apigee_hybrid_issuers_list` for simplest API.

pub fn apigee_hybrid_issuers_list_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://apigee.googleapis.com/v1/hybrid/issuers", name,);

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/hybrid/issuers
/// Lists hybrid services and its trusted issuers service account ids. This api is authenticated and unauthorized(allow all the users) and used by runtime authn-authz service to query control plane's issuer service account ids.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `apigee_hybrid_issuers_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from apigee_hybrid_issuers_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_hybrid_issuers_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudApigeeV1ListHybridIssuersResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudApigeeV1ListHybridIssuersResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/hybrid/issuers
/// Lists hybrid services and its trusted issuers service account ids. This api is authenticated and unauthorized(allow all the users) and used by runtime authn-authz service to query control plane's issuer service account ids.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `apigee_hybrid_issuers_list_builder()` + `apigee_hybrid_issuers_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_hybrid_issuers_list(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudApigeeV1ListHybridIssuersResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = apigee_hybrid_issuers_list_builder(client, name)?;
    apigee_hybrid_issuers_list_execute(builder)
}

/// GET v1/organizations
/// Creates an Apigee organization. See [Create an Apigee organization](<https://cloud.google.`com/apigee/docs/api-platform/get-started/create-org`>).
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `apigee_organizations_create_execute()` to send, or `apigee_organizations_create` for simplest API.

pub fn apigee_organizations_create_builder(
    client: &SimpleHttpClient,
    parent: Option<&str>,
    body: &GoogleCloudApigeeV1Organization,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://apigee.googleapis.com/v1/organizations",);

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = parent {
        query_parts.push(format!("parent={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/organizations
/// Creates an Apigee organization. See [Create an Apigee organization](<https://cloud.google.`com/apigee/docs/api-platform/get-started/create-org`>).
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `apigee_organizations_create_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from apigee_organizations_create_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/organizations
/// Creates an Apigee organization. See [Create an Apigee organization](<https://cloud.google.`com/apigee/docs/api-platform/get-started/create-org`>).
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `apigee_organizations_create_builder()` + `apigee_organizations_create_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_create(
    client: &SimpleHttpClient,
    parent: Option<&str>,
    body: &GoogleCloudApigeeV1Organization,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = apigee_organizations_create_builder(client, parent, body)?;
    apigee_organizations_create_execute(builder)
}

/// GET v1/organizations/{organizationsId}
/// Delete an Apigee organization. For organizations with BillingType EVALUATION, an immediate deletion is performed. For paid organizations (Subscription or Pay-as-you-go), a soft-deletion is performed. The organization can be restored within the soft-deletion period, which is specified using the retention field in the request or by filing a support ticket with Apigee. During the data retention period specified in the request, the Apigee organization cannot be recreated in the same Google Cloud project. **IMPORTANT: The default data retention setting for this operation is 7 days. To permanently delete the organization in 24 hours, set the retention parameter to MINIMUM.**
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `apigee_organizations_delete_execute()` to send, or `apigee_organizations_delete` for simplest API.

pub fn apigee_organizations_delete_builder(
    client: &SimpleHttpClient,
    name: &str,
    retention: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://apigee.googleapis.com/v1/organizations/{}", name,);

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = retention {
        query_parts.push(format!("retention={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/organizations/{organizationsId}
/// Delete an Apigee organization. For organizations with BillingType EVALUATION, an immediate deletion is performed. For paid organizations (Subscription or Pay-as-you-go), a soft-deletion is performed. The organization can be restored within the soft-deletion period, which is specified using the retention field in the request or by filing a support ticket with Apigee. During the data retention period specified in the request, the Apigee organization cannot be recreated in the same Google Cloud project. **IMPORTANT: The default data retention setting for this operation is 7 days. To permanently delete the organization in 24 hours, set the retention parameter to MINIMUM.**
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `apigee_organizations_delete_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from apigee_organizations_delete_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/organizations/{organizationsId}
/// Delete an Apigee organization. For organizations with BillingType EVALUATION, an immediate deletion is performed. For paid organizations (Subscription or Pay-as-you-go), a soft-deletion is performed. The organization can be restored within the soft-deletion period, which is specified using the retention field in the request or by filing a support ticket with Apigee. During the data retention period specified in the request, the Apigee organization cannot be recreated in the same Google Cloud project. **IMPORTANT: The default data retention setting for this operation is 7 days. To permanently delete the organization in 24 hours, set the retention parameter to MINIMUM.**
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `apigee_organizations_delete_builder()` + `apigee_organizations_delete_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_delete(
    client: &SimpleHttpClient,
    name: &str,
    retention: Option<&str>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = apigee_organizations_delete_builder(client, name, retention)?;
    apigee_organizations_delete_execute(builder)
}

/// GET v1/organizations/{organizationsId}
/// Gets the profile for an Apigee organization. See [Understanding organizations](<https://cloud.google.`com/apigee/docs/api-platform/fundamentals/organization-structure`>).
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `apigee_organizations_get_execute()` to send, or `apigee_organizations_get` for simplest API.

pub fn apigee_organizations_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://apigee.googleapis.com/v1/organizations/{}", name,);

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/organizations/{organizationsId}
/// Gets the profile for an Apigee organization. See [Understanding organizations](<https://cloud.google.`com/apigee/docs/api-platform/fundamentals/organization-structure`>).
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `apigee_organizations_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from apigee_organizations_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudApigeeV1Organization>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudApigeeV1Organization = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/organizations/{organizationsId}
/// Gets the profile for an Apigee organization. See [Understanding organizations](<https://cloud.google.`com/apigee/docs/api-platform/fundamentals/organization-structure`>).
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `apigee_organizations_get_builder()` + `apigee_organizations_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_get(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudApigeeV1Organization>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = apigee_organizations_get_builder(client, name)?;
    apigee_organizations_get_execute(builder)
}

/// GET v1/organizations/{organizationsId}/controlPlaneAccess
/// Lists the service accounts allowed to access Apigee control plane directly for limited functionality. **Note**: Available to Apigee hybrid only.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `apigee_organizations_get_control_plane_access_execute()` to send, or `apigee_organizations_get_control_plane_access` for simplest API.

pub fn apigee_organizations_get_control_plane_access_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://apigee.googleapis.com/v1/organizations/{}/controlPlaneAccess",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/organizations/{organizationsId}/controlPlaneAccess
/// Lists the service accounts allowed to access Apigee control plane directly for limited functionality. **Note**: Available to Apigee hybrid only.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `apigee_organizations_get_control_plane_access_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from apigee_organizations_get_control_plane_access_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_get_control_plane_access_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudApigeeV1ControlPlaneAccess>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudApigeeV1ControlPlaneAccess = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/organizations/{organizationsId}/controlPlaneAccess
/// Lists the service accounts allowed to access Apigee control plane directly for limited functionality. **Note**: Available to Apigee hybrid only.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `apigee_organizations_get_control_plane_access_builder()` + `apigee_organizations_get_control_plane_access_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_get_control_plane_access(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudApigeeV1ControlPlaneAccess>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = apigee_organizations_get_control_plane_access_builder(client, name)?;
    apigee_organizations_get_control_plane_access_execute(builder)
}

/// GET v1/organizations/{organizationsId}/deployedIngressConfig
/// Gets the deployed ingress configuration for an organization.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `apigee_organizations_get_deployed_ingress_config_execute()` to send, or `apigee_organizations_get_deployed_ingress_config` for simplest API.

pub fn apigee_organizations_get_deployed_ingress_config_builder(
    client: &SimpleHttpClient,
    name: &str,
    view: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://apigee.googleapis.com/v1/organizations/{}/deployedIngressConfig",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = view {
        query_parts.push(format!("view={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/organizations/{organizationsId}/deployedIngressConfig
/// Gets the deployed ingress configuration for an organization.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `apigee_organizations_get_deployed_ingress_config_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from apigee_organizations_get_deployed_ingress_config_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_get_deployed_ingress_config_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudApigeeV1IngressConfig>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudApigeeV1IngressConfig = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/organizations/{organizationsId}/deployedIngressConfig
/// Gets the deployed ingress configuration for an organization.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `apigee_organizations_get_deployed_ingress_config_builder()` + `apigee_organizations_get_deployed_ingress_config_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_get_deployed_ingress_config(
    client: &SimpleHttpClient,
    name: &str,
    view: Option<&str>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudApigeeV1IngressConfig>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = apigee_organizations_get_deployed_ingress_config_builder(client, name, view)?;
    apigee_organizations_get_deployed_ingress_config_execute(builder)
}

/// GET v1/organizations/{organizationsId}:getProjectMapping
/// Gets the project ID and region for an Apigee organization.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `apigee_organizations_get_project_mapping_execute()` to send, or `apigee_organizations_get_project_mapping` for simplest API.

pub fn apigee_organizations_get_project_mapping_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://apigee.googleapis.com/v1/organizations/{}:getProjectMapping",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/organizations/{organizationsId}:getProjectMapping
/// Gets the project ID and region for an Apigee organization.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `apigee_organizations_get_project_mapping_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from apigee_organizations_get_project_mapping_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_get_project_mapping_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudApigeeV1OrganizationProjectMapping>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudApigeeV1OrganizationProjectMapping =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/organizations/{organizationsId}:getProjectMapping
/// Gets the project ID and region for an Apigee organization.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `apigee_organizations_get_project_mapping_builder()` + `apigee_organizations_get_project_mapping_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_get_project_mapping(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudApigeeV1OrganizationProjectMapping>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = apigee_organizations_get_project_mapping_builder(client, name)?;
    apigee_organizations_get_project_mapping_execute(builder)
}

/// GET v1/organizations/{organizationsId}/runtimeConfig
/// Get runtime config for an organization.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `apigee_organizations_get_runtime_config_execute()` to send, or `apigee_organizations_get_runtime_config` for simplest API.

pub fn apigee_organizations_get_runtime_config_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://apigee.googleapis.com/v1/organizations/{}/runtimeConfig",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/organizations/{organizationsId}/runtimeConfig
/// Get runtime config for an organization.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `apigee_organizations_get_runtime_config_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from apigee_organizations_get_runtime_config_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_get_runtime_config_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudApigeeV1RuntimeConfig>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudApigeeV1RuntimeConfig = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/organizations/{organizationsId}/runtimeConfig
/// Get runtime config for an organization.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `apigee_organizations_get_runtime_config_builder()` + `apigee_organizations_get_runtime_config_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_get_runtime_config(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudApigeeV1RuntimeConfig>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = apigee_organizations_get_runtime_config_builder(client, name)?;
    apigee_organizations_get_runtime_config_execute(builder)
}

/// GET v1/organizations/{organizationsId}/securitySettings
/// GetSecuritySettings gets the security settings for API Security.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `apigee_organizations_get_security_settings_execute()` to send, or `apigee_organizations_get_security_settings` for simplest API.

pub fn apigee_organizations_get_security_settings_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://apigee.googleapis.com/v1/organizations/{}/securitySettings",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/organizations/{organizationsId}/securitySettings
/// GetSecuritySettings gets the security settings for API Security.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `apigee_organizations_get_security_settings_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from apigee_organizations_get_security_settings_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_get_security_settings_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudApigeeV1SecuritySettings>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudApigeeV1SecuritySettings = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/organizations/{organizationsId}/securitySettings
/// GetSecuritySettings gets the security settings for API Security.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `apigee_organizations_get_security_settings_builder()` + `apigee_organizations_get_security_settings_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_get_security_settings(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudApigeeV1SecuritySettings>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = apigee_organizations_get_security_settings_builder(client, name)?;
    apigee_organizations_get_security_settings_execute(builder)
}

/// GET v1/organizations/{organizationsId}:getSyncAuthorization
/// Lists the service accounts with the permissions required to allow the Synchronizer to download environment data from the control plane. An ETag is returned in the response to `getSyncAuthorization`. Pass that ETag when calling [`setSyncAuthorization`](`setSyncAuthorization`) to ensure that you are updating the correct version. If you don't pass the ETag in the call to `setSyncAuthorization`, then the existing authorization is overwritten indiscriminately. For more information, see [Configure the Synchronizer](<https://cloud.google.`com/apigee/docs/hybrid/latest/synchronizer-access`>). **Note**: Available to Apigee hybrid only.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `apigee_organizations_get_sync_authorization_execute()` to send, or `apigee_organizations_get_sync_authorization` for simplest API.

pub fn apigee_organizations_get_sync_authorization_builder(
    client: &SimpleHttpClient,
    name: &str,
    body: &GoogleCloudApigeeV1GetSyncAuthorizationRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://apigee.googleapis.com/v1/organizations/{}:getSyncAuthorization",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/organizations/{organizationsId}:getSyncAuthorization
/// Lists the service accounts with the permissions required to allow the Synchronizer to download environment data from the control plane. An ETag is returned in the response to `getSyncAuthorization`. Pass that ETag when calling [`setSyncAuthorization`](`setSyncAuthorization`) to ensure that you are updating the correct version. If you don't pass the ETag in the call to `setSyncAuthorization`, then the existing authorization is overwritten indiscriminately. For more information, see [Configure the Synchronizer](<https://cloud.google.`com/apigee/docs/hybrid/latest/synchronizer-access`>). **Note**: Available to Apigee hybrid only.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `apigee_organizations_get_sync_authorization_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from apigee_organizations_get_sync_authorization_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_get_sync_authorization_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudApigeeV1SyncAuthorization>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudApigeeV1SyncAuthorization = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/organizations/{organizationsId}:getSyncAuthorization
/// Lists the service accounts with the permissions required to allow the Synchronizer to download environment data from the control plane. An ETag is returned in the response to `getSyncAuthorization`. Pass that ETag when calling [`setSyncAuthorization`](`setSyncAuthorization`) to ensure that you are updating the correct version. If you don't pass the ETag in the call to `setSyncAuthorization`, then the existing authorization is overwritten indiscriminately. For more information, see [Configure the Synchronizer](<https://cloud.google.`com/apigee/docs/hybrid/latest/synchronizer-access`>). **Note**: Available to Apigee hybrid only.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `apigee_organizations_get_sync_authorization_builder()` + `apigee_organizations_get_sync_authorization_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_get_sync_authorization(
    client: &SimpleHttpClient,
    name: &str,
    body: &GoogleCloudApigeeV1GetSyncAuthorizationRequest,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudApigeeV1SyncAuthorization>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = apigee_organizations_get_sync_authorization_builder(client, name, body)?;
    apigee_organizations_get_sync_authorization_execute(builder)
}

/// GET v1/organizations
/// Lists the Apigee organizations and associated Google Cloud projects that you have permission to access. See [Understanding organizations](<https://cloud.google.`com/apigee/docs/api-platform/fundamentals/organization-structure`>).
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `apigee_organizations_list_execute()` to send, or `apigee_organizations_list` for simplest API.

pub fn apigee_organizations_list_builder(
    client: &SimpleHttpClient,
    parent: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://apigee.googleapis.com/v1/organizations", parent,);

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/organizations
/// Lists the Apigee organizations and associated Google Cloud projects that you have permission to access. See [Understanding organizations](<https://cloud.google.`com/apigee/docs/api-platform/fundamentals/organization-structure`>).
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `apigee_organizations_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from apigee_organizations_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudApigeeV1ListOrganizationsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudApigeeV1ListOrganizationsResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/organizations
/// Lists the Apigee organizations and associated Google Cloud projects that you have permission to access. See [Understanding organizations](<https://cloud.google.`com/apigee/docs/api-platform/fundamentals/organization-structure`>).
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `apigee_organizations_list_builder()` + `apigee_organizations_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_list(
    client: &SimpleHttpClient,
    parent: &str,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudApigeeV1ListOrganizationsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = apigee_organizations_list_builder(client, parent)?;
    apigee_organizations_list_execute(builder)
}

/// GET v1/organizations/{organizationsId}:setAddons
/// Configures the add-ons for the Apigee organization. The existing add-on configuration will be fully replaced.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `apigee_organizations_set_addons_execute()` to send, or `apigee_organizations_set_addons` for simplest API.

pub fn apigee_organizations_set_addons_builder(
    client: &SimpleHttpClient,
    org: &str,
    body: &GoogleCloudApigeeV1SetAddonsRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://apigee.googleapis.com/v1/organizations/{}:setAddons",
        org,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/organizations/{organizationsId}:setAddons
/// Configures the add-ons for the Apigee organization. The existing add-on configuration will be fully replaced.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `apigee_organizations_set_addons_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from apigee_organizations_set_addons_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_set_addons_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/organizations/{organizationsId}:setAddons
/// Configures the add-ons for the Apigee organization. The existing add-on configuration will be fully replaced.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `apigee_organizations_set_addons_builder()` + `apigee_organizations_set_addons_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_set_addons(
    client: &SimpleHttpClient,
    org: &str,
    body: &GoogleCloudApigeeV1SetAddonsRequest,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = apigee_organizations_set_addons_builder(client, org, body)?;
    apigee_organizations_set_addons_execute(builder)
}

/// GET v1/organizations/{organizationsId}:setSyncAuthorization
/// Sets the permissions required to allow the Synchronizer to download environment data from the control plane. You must call this API to enable proper functioning of hybrid. Pass the ETag when calling `setSyncAuthorization` to ensure that you are updating the correct version. To get an ETag, call [`getSyncAuthorization`](`getSyncAuthorization`). If you don't pass the ETag in the call to `setSyncAuthorization`, then the existing authorization is overwritten indiscriminately. For more information, see [Configure the Synchronizer](<https://cloud.google.`com/apigee/docs/hybrid/latest/synchronizer-access`>). **Note**: Available to Apigee hybrid only.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `apigee_organizations_set_sync_authorization_execute()` to send, or `apigee_organizations_set_sync_authorization` for simplest API.

pub fn apigee_organizations_set_sync_authorization_builder(
    client: &SimpleHttpClient,
    name: &str,
    body: &GoogleCloudApigeeV1SyncAuthorization,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://apigee.googleapis.com/v1/organizations/{}:setSyncAuthorization",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/organizations/{organizationsId}:setSyncAuthorization
/// Sets the permissions required to allow the Synchronizer to download environment data from the control plane. You must call this API to enable proper functioning of hybrid. Pass the ETag when calling `setSyncAuthorization` to ensure that you are updating the correct version. To get an ETag, call [`getSyncAuthorization`](`getSyncAuthorization`). If you don't pass the ETag in the call to `setSyncAuthorization`, then the existing authorization is overwritten indiscriminately. For more information, see [Configure the Synchronizer](<https://cloud.google.`com/apigee/docs/hybrid/latest/synchronizer-access`>). **Note**: Available to Apigee hybrid only.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `apigee_organizations_set_sync_authorization_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from apigee_organizations_set_sync_authorization_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_set_sync_authorization_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudApigeeV1SyncAuthorization>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudApigeeV1SyncAuthorization = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/organizations/{organizationsId}:setSyncAuthorization
/// Sets the permissions required to allow the Synchronizer to download environment data from the control plane. You must call this API to enable proper functioning of hybrid. Pass the ETag when calling `setSyncAuthorization` to ensure that you are updating the correct version. To get an ETag, call [`getSyncAuthorization`](`getSyncAuthorization`). If you don't pass the ETag in the call to `setSyncAuthorization`, then the existing authorization is overwritten indiscriminately. For more information, see [Configure the Synchronizer](<https://cloud.google.`com/apigee/docs/hybrid/latest/synchronizer-access`>). **Note**: Available to Apigee hybrid only.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `apigee_organizations_set_sync_authorization_builder()` + `apigee_organizations_set_sync_authorization_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_set_sync_authorization(
    client: &SimpleHttpClient,
    name: &str,
    body: &GoogleCloudApigeeV1SyncAuthorization,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudApigeeV1SyncAuthorization>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = apigee_organizations_set_sync_authorization_builder(client, name, body)?;
    apigee_organizations_set_sync_authorization_execute(builder)
}

/// GET v1/organizations/{organizationsId}
/// Updates the properties for an Apigee organization. No other fields in the organization profile will be updated.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `apigee_organizations_update_execute()` to send, or `apigee_organizations_update` for simplest API.

pub fn apigee_organizations_update_builder(
    client: &SimpleHttpClient,
    name: &str,
    body: &GoogleCloudApigeeV1Organization,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://apigee.googleapis.com/v1/organizations/{}", name,);

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/organizations/{organizationsId}
/// Updates the properties for an Apigee organization. No other fields in the organization profile will be updated.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `apigee_organizations_update_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from apigee_organizations_update_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_update_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudApigeeV1Organization>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudApigeeV1Organization = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/organizations/{organizationsId}
/// Updates the properties for an Apigee organization. No other fields in the organization profile will be updated.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `apigee_organizations_update_builder()` + `apigee_organizations_update_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_update(
    client: &SimpleHttpClient,
    name: &str,
    body: &GoogleCloudApigeeV1Organization,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudApigeeV1Organization>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = apigee_organizations_update_builder(client, name, body)?;
    apigee_organizations_update_execute(builder)
}

/// GET v1/organizations/{organizationsId}/controlPlaneAccess
/// Updates the permissions required to allow Apigee runtime-plane components access to the control plane. Currently, the permissions required are to: 1. Allow runtime components to publish analytics data to the control plane. **Note**: Available to Apigee hybrid only.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `apigee_organizations_update_control_plane_access_execute()` to send, or `apigee_organizations_update_control_plane_access` for simplest API.

pub fn apigee_organizations_update_control_plane_access_builder(
    client: &SimpleHttpClient,
    name: &str,
    updateMask: Option<&str>,
    body: &GoogleCloudApigeeV1ControlPlaneAccess,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://apigee.googleapis.com/v1/organizations/{}/controlPlaneAccess",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = updateMask {
        query_parts.push(format!("updateMask={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/organizations/{organizationsId}/controlPlaneAccess
/// Updates the permissions required to allow Apigee runtime-plane components access to the control plane. Currently, the permissions required are to: 1. Allow runtime components to publish analytics data to the control plane. **Note**: Available to Apigee hybrid only.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `apigee_organizations_update_control_plane_access_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from apigee_organizations_update_control_plane_access_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_update_control_plane_access_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/organizations/{organizationsId}/controlPlaneAccess
/// Updates the permissions required to allow Apigee runtime-plane components access to the control plane. Currently, the permissions required are to: 1. Allow runtime components to publish analytics data to the control plane. **Note**: Available to Apigee hybrid only.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `apigee_organizations_update_control_plane_access_builder()` + `apigee_organizations_update_control_plane_access_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_update_control_plane_access(
    client: &SimpleHttpClient,
    name: &str,
    updateMask: Option<&str>,
    body: &GoogleCloudApigeeV1ControlPlaneAccess,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        apigee_organizations_update_control_plane_access_builder(client, name, updateMask, body)?;
    apigee_organizations_update_control_plane_access_execute(builder)
}

/// GET v1/organizations/{organizationsId}/securitySettings
/// UpdateSecuritySettings updates the current security settings for API Security.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `apigee_organizations_update_security_settings_execute()` to send, or `apigee_organizations_update_security_settings` for simplest API.

pub fn apigee_organizations_update_security_settings_builder(
    client: &SimpleHttpClient,
    name: &str,
    updateMask: Option<&str>,
    body: &GoogleCloudApigeeV1SecuritySettings,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://apigee.googleapis.com/v1/organizations/{}/securitySettings",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = updateMask {
        query_parts.push(format!("updateMask={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/organizations/{organizationsId}/securitySettings
/// UpdateSecuritySettings updates the current security settings for API Security.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `apigee_organizations_update_security_settings_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from apigee_organizations_update_security_settings_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_update_security_settings_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudApigeeV1SecuritySettings>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudApigeeV1SecuritySettings = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/organizations/{organizationsId}/securitySettings
/// UpdateSecuritySettings updates the current security settings for API Security.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `apigee_organizations_update_security_settings_builder()` + `apigee_organizations_update_security_settings_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_update_security_settings(
    client: &SimpleHttpClient,
    name: &str,
    updateMask: Option<&str>,
    body: &GoogleCloudApigeeV1SecuritySettings,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudApigeeV1SecuritySettings>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        apigee_organizations_update_security_settings_builder(client, name, updateMask, body)?;
    apigee_organizations_update_security_settings_execute(builder)
}

/// GET v1/organizations/{organizationsId}/analytics/datastores
/// Create a Datastore for an org
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `apigee_organizations_analytics_datastores_create_execute()` to send, or `apigee_organizations_analytics_datastores_create` for simplest API.

pub fn apigee_organizations_analytics_datastores_create_builder(
    client: &SimpleHttpClient,
    parent: &str,
    body: &GoogleCloudApigeeV1Datastore,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://apigee.googleapis.com/v1/organizations/{}/analytics/datastores",
        parent,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/organizations/{organizationsId}/analytics/datastores
/// Create a Datastore for an org
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `apigee_organizations_analytics_datastores_create_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from apigee_organizations_analytics_datastores_create_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_analytics_datastores_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudApigeeV1Datastore>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudApigeeV1Datastore = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/organizations/{organizationsId}/analytics/datastores
/// Create a Datastore for an org
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `apigee_organizations_analytics_datastores_create_builder()` + `apigee_organizations_analytics_datastores_create_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_analytics_datastores_create(
    client: &SimpleHttpClient,
    parent: &str,
    body: &GoogleCloudApigeeV1Datastore,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudApigeeV1Datastore>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = apigee_organizations_analytics_datastores_create_builder(client, parent, body)?;
    apigee_organizations_analytics_datastores_create_execute(builder)
}

/// GET v1/organizations/{organizationsId}/analytics/datastores/{datastoresId}
/// Delete a Datastore from an org.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `apigee_organizations_analytics_datastores_delete_execute()` to send, or `apigee_organizations_analytics_datastores_delete` for simplest API.

pub fn apigee_organizations_analytics_datastores_delete_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://apigee.googleapis.com/v1/organizations/{}/analytics/datastores/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/organizations/{organizationsId}/analytics/datastores/{datastoresId}
/// Delete a Datastore from an org.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `apigee_organizations_analytics_datastores_delete_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from apigee_organizations_analytics_datastores_delete_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_analytics_datastores_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleProtobufEmpty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/organizations/{organizationsId}/analytics/datastores/{datastoresId}
/// Delete a Datastore from an org.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `apigee_organizations_analytics_datastores_delete_builder()` + `apigee_organizations_analytics_datastores_delete_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_analytics_datastores_delete(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = apigee_organizations_analytics_datastores_delete_builder(client, name)?;
    apigee_organizations_analytics_datastores_delete_execute(builder)
}

/// GET v1/organizations/{organizationsId}/analytics/datastores/{datastoresId}
/// Get a Datastore
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `apigee_organizations_analytics_datastores_get_execute()` to send, or `apigee_organizations_analytics_datastores_get` for simplest API.

pub fn apigee_organizations_analytics_datastores_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://apigee.googleapis.com/v1/organizations/{}/analytics/datastores/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/organizations/{organizationsId}/analytics/datastores/{datastoresId}
/// Get a Datastore
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `apigee_organizations_analytics_datastores_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from apigee_organizations_analytics_datastores_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_analytics_datastores_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudApigeeV1Datastore>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudApigeeV1Datastore = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/organizations/{organizationsId}/analytics/datastores/{datastoresId}
/// Get a Datastore
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `apigee_organizations_analytics_datastores_get_builder()` + `apigee_organizations_analytics_datastores_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_analytics_datastores_get(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudApigeeV1Datastore>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = apigee_organizations_analytics_datastores_get_builder(client, name)?;
    apigee_organizations_analytics_datastores_get_execute(builder)
}

/// GET v1/organizations/{organizationsId}/analytics/datastores
/// List Datastores
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `apigee_organizations_analytics_datastores_list_execute()` to send, or `apigee_organizations_analytics_datastores_list` for simplest API.

pub fn apigee_organizations_analytics_datastores_list_builder(
    client: &SimpleHttpClient,
    parent: &str,
    targetType: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://apigee.googleapis.com/v1/organizations/{}/analytics/datastores",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = targetType {
        query_parts.push(format!("targetType={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/organizations/{organizationsId}/analytics/datastores
/// List Datastores
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `apigee_organizations_analytics_datastores_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from apigee_organizations_analytics_datastores_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_analytics_datastores_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudApigeeV1ListDatastoresResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudApigeeV1ListDatastoresResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/organizations/{organizationsId}/analytics/datastores
/// List Datastores
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `apigee_organizations_analytics_datastores_list_builder()` + `apigee_organizations_analytics_datastores_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_analytics_datastores_list(
    client: &SimpleHttpClient,
    parent: &str,
    targetType: Option<&str>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudApigeeV1ListDatastoresResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        apigee_organizations_analytics_datastores_list_builder(client, parent, targetType)?;
    apigee_organizations_analytics_datastores_list_execute(builder)
}

/// GET v1/organizations/{organizationsId}/analytics/datastores:test
/// Test if Datastore configuration is correct. This includes checking if credentials provided by customer have required permissions in target destination storage
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `apigee_organizations_analytics_datastores_test_execute()` to send, or `apigee_organizations_analytics_datastores_test` for simplest API.

pub fn apigee_organizations_analytics_datastores_test_builder(
    client: &SimpleHttpClient,
    parent: &str,
    body: &GoogleCloudApigeeV1Datastore,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://apigee.googleapis.com/v1/organizations/{}/analytics/datastores:test",
        parent,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/organizations/{organizationsId}/analytics/datastores:test
/// Test if Datastore configuration is correct. This includes checking if credentials provided by customer have required permissions in target destination storage
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `apigee_organizations_analytics_datastores_test_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from apigee_organizations_analytics_datastores_test_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_analytics_datastores_test_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudApigeeV1TestDatastoreResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudApigeeV1TestDatastoreResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/organizations/{organizationsId}/analytics/datastores:test
/// Test if Datastore configuration is correct. This includes checking if credentials provided by customer have required permissions in target destination storage
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `apigee_organizations_analytics_datastores_test_builder()` + `apigee_organizations_analytics_datastores_test_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_analytics_datastores_test(
    client: &SimpleHttpClient,
    parent: &str,
    body: &GoogleCloudApigeeV1Datastore,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudApigeeV1TestDatastoreResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = apigee_organizations_analytics_datastores_test_builder(client, parent, body)?;
    apigee_organizations_analytics_datastores_test_execute(builder)
}

/// GET v1/organizations/{organizationsId}/analytics/datastores/{datastoresId}
/// Update a Datastore
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `apigee_organizations_analytics_datastores_update_execute()` to send, or `apigee_organizations_analytics_datastores_update` for simplest API.

pub fn apigee_organizations_analytics_datastores_update_builder(
    client: &SimpleHttpClient,
    name: &str,
    body: &GoogleCloudApigeeV1Datastore,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://apigee.googleapis.com/v1/organizations/{}/analytics/datastores/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/organizations/{organizationsId}/analytics/datastores/{datastoresId}
/// Update a Datastore
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `apigee_organizations_analytics_datastores_update_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from apigee_organizations_analytics_datastores_update_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_analytics_datastores_update_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudApigeeV1Datastore>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudApigeeV1Datastore = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/organizations/{organizationsId}/analytics/datastores/{datastoresId}
/// Update a Datastore
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `apigee_organizations_analytics_datastores_update_builder()` + `apigee_organizations_analytics_datastores_update_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_analytics_datastores_update(
    client: &SimpleHttpClient,
    name: &str,
    body: &GoogleCloudApigeeV1Datastore,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudApigeeV1Datastore>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = apigee_organizations_analytics_datastores_update_builder(client, name, body)?;
    apigee_organizations_analytics_datastores_update_execute(builder)
}

/// GET v1/organizations/{organizationsId}/apimServiceExtensions
/// Creates an APIM ServiceExtension in an organization.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `apigee_organizations_apim_service_extensions_create_execute()` to send, or `apigee_organizations_apim_service_extensions_create` for simplest API.

pub fn apigee_organizations_apim_service_extensions_create_builder(
    client: &SimpleHttpClient,
    parent: &str,
    apimServiceExtensionId: Option<&str>,
    body: &GoogleCloudApigeeV1ApimServiceExtension,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://apigee.googleapis.com/v1/organizations/{}/apimServiceExtensions",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = apimServiceExtensionId {
        query_parts.push(format!("apimServiceExtensionId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/organizations/{organizationsId}/apimServiceExtensions
/// Creates an APIM ServiceExtension in an organization.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `apigee_organizations_apim_service_extensions_create_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from apigee_organizations_apim_service_extensions_create_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_apim_service_extensions_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/organizations/{organizationsId}/apimServiceExtensions
/// Creates an APIM ServiceExtension in an organization.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `apigee_organizations_apim_service_extensions_create_builder()` + `apigee_organizations_apim_service_extensions_create_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_apim_service_extensions_create(
    client: &SimpleHttpClient,
    parent: &str,
    apimServiceExtensionId: Option<&str>,
    body: &GoogleCloudApigeeV1ApimServiceExtension,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = apigee_organizations_apim_service_extensions_create_builder(
        client,
        parent,
        apimServiceExtensionId,
        body,
    )?;
    apigee_organizations_apim_service_extensions_create_execute(builder)
}

/// GET v1/organizations/{organizationsId}/apimServiceExtensions/{apimServiceExtensionsId}
/// Deletes APIM service extension from an organization.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `apigee_organizations_apim_service_extensions_delete_execute()` to send, or `apigee_organizations_apim_service_extensions_delete` for simplest API.

pub fn apigee_organizations_apim_service_extensions_delete_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://apigee.googleapis.com/v1/organizations/{}/apimServiceExtensions/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/organizations/{organizationsId}/apimServiceExtensions/{apimServiceExtensionsId}
/// Deletes APIM service extension from an organization.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `apigee_organizations_apim_service_extensions_delete_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from apigee_organizations_apim_service_extensions_delete_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_apim_service_extensions_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/organizations/{organizationsId}/apimServiceExtensions/{apimServiceExtensionsId}
/// Deletes APIM service extension from an organization.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `apigee_organizations_apim_service_extensions_delete_builder()` + `apigee_organizations_apim_service_extensions_delete_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_apim_service_extensions_delete(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = apigee_organizations_apim_service_extensions_delete_builder(client, name)?;
    apigee_organizations_apim_service_extensions_delete_execute(builder)
}

/// GET v1/organizations/{organizationsId}/apimServiceExtensions/{apimServiceExtensionsId}
/// Gets APIM service extension details.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `apigee_organizations_apim_service_extensions_get_execute()` to send, or `apigee_organizations_apim_service_extensions_get` for simplest API.

pub fn apigee_organizations_apim_service_extensions_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://apigee.googleapis.com/v1/organizations/{}/apimServiceExtensions/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/organizations/{organizationsId}/apimServiceExtensions/{apimServiceExtensionsId}
/// Gets APIM service extension details.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `apigee_organizations_apim_service_extensions_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from apigee_organizations_apim_service_extensions_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_apim_service_extensions_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudApigeeV1ApimServiceExtension>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudApigeeV1ApimServiceExtension =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/organizations/{organizationsId}/apimServiceExtensions/{apimServiceExtensionsId}
/// Gets APIM service extension details.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `apigee_organizations_apim_service_extensions_get_builder()` + `apigee_organizations_apim_service_extensions_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_apim_service_extensions_get(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudApigeeV1ApimServiceExtension>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = apigee_organizations_apim_service_extensions_get_builder(client, name)?;
    apigee_organizations_apim_service_extensions_get_execute(builder)
}

/// GET v1/organizations/{organizationsId}/apimServiceExtensions
/// Lists all APIM service extensions in an organization.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `apigee_organizations_apim_service_extensions_list_execute()` to send, or `apigee_organizations_apim_service_extensions_list` for simplest API.

pub fn apigee_organizations_apim_service_extensions_list_builder(
    client: &SimpleHttpClient,
    parent: &str,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://apigee.googleapis.com/v1/organizations/{}/apimServiceExtensions",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/organizations/{organizationsId}/apimServiceExtensions
/// Lists all APIM service extensions in an organization.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `apigee_organizations_apim_service_extensions_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from apigee_organizations_apim_service_extensions_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_apim_service_extensions_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudApigeeV1ListApimServiceExtensionsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudApigeeV1ListApimServiceExtensionsResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/organizations/{organizationsId}/apimServiceExtensions
/// Lists all APIM service extensions in an organization.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `apigee_organizations_apim_service_extensions_list_builder()` + `apigee_organizations_apim_service_extensions_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_apim_service_extensions_list(
    client: &SimpleHttpClient,
    parent: &str,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudApigeeV1ListApimServiceExtensionsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = apigee_organizations_apim_service_extensions_list_builder(
        client, parent, pageSize, pageToken,
    )?;
    apigee_organizations_apim_service_extensions_list_execute(builder)
}

/// GET v1/organizations/{organizationsId}/apimServiceExtensions/{apimServiceExtensionsId}
/// Updates an APIM service extension in an organization.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `apigee_organizations_apim_service_extensions_patch_execute()` to send, or `apigee_organizations_apim_service_extensions_patch` for simplest API.

pub fn apigee_organizations_apim_service_extensions_patch_builder(
    client: &SimpleHttpClient,
    name: &str,
    allowMissing: Option<bool>,
    updateMask: Option<&str>,
    body: &GoogleCloudApigeeV1ApimServiceExtension,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://apigee.googleapis.com/v1/organizations/{}/apimServiceExtensions/{}",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = allowMissing {
        query_parts.push(format!("allowMissing={}", val));
    }
    if let Some(val) = updateMask {
        query_parts.push(format!("updateMask={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/organizations/{organizationsId}/apimServiceExtensions/{apimServiceExtensionsId}
/// Updates an APIM service extension in an organization.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `apigee_organizations_apim_service_extensions_patch_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from apigee_organizations_apim_service_extensions_patch_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_apim_service_extensions_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/organizations/{organizationsId}/apimServiceExtensions/{apimServiceExtensionsId}
/// Updates an APIM service extension in an organization.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `apigee_organizations_apim_service_extensions_patch_builder()` + `apigee_organizations_apim_service_extensions_patch_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_apim_service_extensions_patch(
    client: &SimpleHttpClient,
    name: &str,
    allowMissing: Option<bool>,
    updateMask: Option<&str>,
    body: &GoogleCloudApigeeV1ApimServiceExtension,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = apigee_organizations_apim_service_extensions_patch_builder(
        client,
        name,
        allowMissing,
        updateMask,
        body,
    )?;
    apigee_organizations_apim_service_extensions_patch_execute(builder)
}

/// GET v1/organizations/{organizationsId}/apiproducts/{apiproductsId}/attributes
/// Updates or creates API product attributes. This API **replaces** the current list of attributes with the attributes specified in the request body. In this way, you can update existing attributes, add new attributes, or delete existing attributes by omitting them from the request body. **Note**: OAuth access tokens and Key Management Service (KMS) entities (apps, developers, and API products) are cached for 180 seconds (current default). Any custom attributes associated with entities also get cached for at least 180 seconds after entity is accessed during runtime. In this case, the ExpiresIn element on the OAuthV2 policy won't be able to expire an access token in less than 180 seconds.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `apigee_organizations_apiproducts_attributes_execute()` to send, or `apigee_organizations_apiproducts_attributes` for simplest API.

pub fn apigee_organizations_apiproducts_attributes_builder(
    client: &SimpleHttpClient,
    name: &str,
    body: &GoogleCloudApigeeV1Attributes,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://apigee.googleapis.com/v1/organizations/{}/apiproducts/{}/attributes",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/organizations/{organizationsId}/apiproducts/{apiproductsId}/attributes
/// Updates or creates API product attributes. This API **replaces** the current list of attributes with the attributes specified in the request body. In this way, you can update existing attributes, add new attributes, or delete existing attributes by omitting them from the request body. **Note**: OAuth access tokens and Key Management Service (KMS) entities (apps, developers, and API products) are cached for 180 seconds (current default). Any custom attributes associated with entities also get cached for at least 180 seconds after entity is accessed during runtime. In this case, the ExpiresIn element on the OAuthV2 policy won't be able to expire an access token in less than 180 seconds.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `apigee_organizations_apiproducts_attributes_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from apigee_organizations_apiproducts_attributes_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_apiproducts_attributes_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudApigeeV1Attributes>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudApigeeV1Attributes = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/organizations/{organizationsId}/apiproducts/{apiproductsId}/attributes
/// Updates or creates API product attributes. This API **replaces** the current list of attributes with the attributes specified in the request body. In this way, you can update existing attributes, add new attributes, or delete existing attributes by omitting them from the request body. **Note**: OAuth access tokens and Key Management Service (KMS) entities (apps, developers, and API products) are cached for 180 seconds (current default). Any custom attributes associated with entities also get cached for at least 180 seconds after entity is accessed during runtime. In this case, the ExpiresIn element on the OAuthV2 policy won't be able to expire an access token in less than 180 seconds.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `apigee_organizations_apiproducts_attributes_builder()` + `apigee_organizations_apiproducts_attributes_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_apiproducts_attributes(
    client: &SimpleHttpClient,
    name: &str,
    body: &GoogleCloudApigeeV1Attributes,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudApigeeV1Attributes>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = apigee_organizations_apiproducts_attributes_builder(client, name, body)?;
    apigee_organizations_apiproducts_attributes_execute(builder)
}

/// GET v1/organizations/{organizationsId}/apiproducts
/// Creates an API product in an organization. You create API products after you have proxied backend services using API proxies. An API product is a collection of API resources combined with quota settings and metadata that you can use to deliver customized and productized API bundles to your developer community. This metadata can include: - Scope - Environments - API proxies - Extensible profile API products enable you repackage APIs on the fly, without having to do any additional coding or configuration. Apigee recommends that you start with a simple API product including only required elements. You then provision credentials to apps to enable them to start testing your APIs. After you have authentication and authorization working against a simple API product, you can iterate to create finer-grained API products, defining different sets of API resources for each API product. **WARNING:** - If you don't specify an API proxy in the request body, *any* app associated with the product can make calls to *any* API in your entire organization. - If you don't specify an environment in the request body, the product allows access to all environments. For more information, see What is an API product?
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `apigee_organizations_apiproducts_create_execute()` to send, or `apigee_organizations_apiproducts_create` for simplest API.

pub fn apigee_organizations_apiproducts_create_builder(
    client: &SimpleHttpClient,
    parent: &str,
    body: &GoogleCloudApigeeV1ApiProduct,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://apigee.googleapis.com/v1/organizations/{}/apiproducts",
        parent,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/organizations/{organizationsId}/apiproducts
/// Creates an API product in an organization. You create API products after you have proxied backend services using API proxies. An API product is a collection of API resources combined with quota settings and metadata that you can use to deliver customized and productized API bundles to your developer community. This metadata can include: - Scope - Environments - API proxies - Extensible profile API products enable you repackage APIs on the fly, without having to do any additional coding or configuration. Apigee recommends that you start with a simple API product including only required elements. You then provision credentials to apps to enable them to start testing your APIs. After you have authentication and authorization working against a simple API product, you can iterate to create finer-grained API products, defining different sets of API resources for each API product. **WARNING:** - If you don't specify an API proxy in the request body, *any* app associated with the product can make calls to *any* API in your entire organization. - If you don't specify an environment in the request body, the product allows access to all environments. For more information, see What is an API product?
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `apigee_organizations_apiproducts_create_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from apigee_organizations_apiproducts_create_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_apiproducts_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudApigeeV1ApiProduct>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudApigeeV1ApiProduct = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/organizations/{organizationsId}/apiproducts
/// Creates an API product in an organization. You create API products after you have proxied backend services using API proxies. An API product is a collection of API resources combined with quota settings and metadata that you can use to deliver customized and productized API bundles to your developer community. This metadata can include: - Scope - Environments - API proxies - Extensible profile API products enable you repackage APIs on the fly, without having to do any additional coding or configuration. Apigee recommends that you start with a simple API product including only required elements. You then provision credentials to apps to enable them to start testing your APIs. After you have authentication and authorization working against a simple API product, you can iterate to create finer-grained API products, defining different sets of API resources for each API product. **WARNING:** - If you don't specify an API proxy in the request body, *any* app associated with the product can make calls to *any* API in your entire organization. - If you don't specify an environment in the request body, the product allows access to all environments. For more information, see What is an API product?
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `apigee_organizations_apiproducts_create_builder()` + `apigee_organizations_apiproducts_create_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_apiproducts_create(
    client: &SimpleHttpClient,
    parent: &str,
    body: &GoogleCloudApigeeV1ApiProduct,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudApigeeV1ApiProduct>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = apigee_organizations_apiproducts_create_builder(client, parent, body)?;
    apigee_organizations_apiproducts_create_execute(builder)
}

/// GET v1/organizations/{organizationsId}/apiproducts/{apiproductsId}
/// Deletes an API product from an organization. Deleting an API product causes app requests to the resource URIs defined in the API product to fail. Ensure that you create a new API product to serve existing apps, unless your intention is to disable access to the resources defined in the API product. The API product name required in the request URL is the internal name of the product, not the display name. While they may be the same, it depends on whether the API product was created via the UI or the API. View the list of API products to verify the internal name.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `apigee_organizations_apiproducts_delete_execute()` to send, or `apigee_organizations_apiproducts_delete` for simplest API.

pub fn apigee_organizations_apiproducts_delete_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://apigee.googleapis.com/v1/organizations/{}/apiproducts/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/organizations/{organizationsId}/apiproducts/{apiproductsId}
/// Deletes an API product from an organization. Deleting an API product causes app requests to the resource URIs defined in the API product to fail. Ensure that you create a new API product to serve existing apps, unless your intention is to disable access to the resources defined in the API product. The API product name required in the request URL is the internal name of the product, not the display name. While they may be the same, it depends on whether the API product was created via the UI or the API. View the list of API products to verify the internal name.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `apigee_organizations_apiproducts_delete_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from apigee_organizations_apiproducts_delete_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_apiproducts_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudApigeeV1ApiProduct>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudApigeeV1ApiProduct = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/organizations/{organizationsId}/apiproducts/{apiproductsId}
/// Deletes an API product from an organization. Deleting an API product causes app requests to the resource URIs defined in the API product to fail. Ensure that you create a new API product to serve existing apps, unless your intention is to disable access to the resources defined in the API product. The API product name required in the request URL is the internal name of the product, not the display name. While they may be the same, it depends on whether the API product was created via the UI or the API. View the list of API products to verify the internal name.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `apigee_organizations_apiproducts_delete_builder()` + `apigee_organizations_apiproducts_delete_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_apiproducts_delete(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudApigeeV1ApiProduct>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = apigee_organizations_apiproducts_delete_builder(client, name)?;
    apigee_organizations_apiproducts_delete_execute(builder)
}

/// GET v1/organizations/{organizationsId}/apiproducts/{apiproductsId}
/// Gets configuration details for an API product. The API product name required in the request URL is the internal name of the product, not the display name. While they may be the same, it depends on whether the API product was created via the UI or the API. View the list of API products to verify the internal name.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `apigee_organizations_apiproducts_get_execute()` to send, or `apigee_organizations_apiproducts_get` for simplest API.

pub fn apigee_organizations_apiproducts_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://apigee.googleapis.com/v1/organizations/{}/apiproducts/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/organizations/{organizationsId}/apiproducts/{apiproductsId}
/// Gets configuration details for an API product. The API product name required in the request URL is the internal name of the product, not the display name. While they may be the same, it depends on whether the API product was created via the UI or the API. View the list of API products to verify the internal name.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `apigee_organizations_apiproducts_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from apigee_organizations_apiproducts_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_apiproducts_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudApigeeV1ApiProduct>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudApigeeV1ApiProduct = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/organizations/{organizationsId}/apiproducts/{apiproductsId}
/// Gets configuration details for an API product. The API product name required in the request URL is the internal name of the product, not the display name. While they may be the same, it depends on whether the API product was created via the UI or the API. View the list of API products to verify the internal name.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `apigee_organizations_apiproducts_get_builder()` + `apigee_organizations_apiproducts_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_apiproducts_get(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudApigeeV1ApiProduct>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = apigee_organizations_apiproducts_get_builder(client, name)?;
    apigee_organizations_apiproducts_get_execute(builder)
}

/// GET v1/organizations/{organizationsId}/apiproducts
/// Lists all API product names for an organization. Filter the list by passing an attributename and attibutevalue. The maximum number of API products returned is 1000. You can paginate the list of API products returned using the `startKey` and count query parameters. If the resource has the space attribute set, the response may not return all resources. To learn more, read the [Apigee Spaces Overview](<https://cloud.google.`com/apigee/docs/api-platform/system-administration/spaces/apigee-spaces-overview`>).
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `apigee_organizations_apiproducts_list_execute()` to send, or `apigee_organizations_apiproducts_list` for simplest API.

pub fn apigee_organizations_apiproducts_list_builder(
    client: &SimpleHttpClient,
    parent: &str,
    attributename: Option<&str>,
    attributevalue: Option<&str>,
    count: Option<&str>,
    expand: Option<bool>,
    space: Option<&str>,
    startKey: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://apigee.googleapis.com/v1/organizations/{}/apiproducts",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = attributename {
        query_parts.push(format!("attributename={}", val));
    }
    if let Some(val) = attributevalue {
        query_parts.push(format!("attributevalue={}", val));
    }
    if let Some(val) = count {
        query_parts.push(format!("count={}", val));
    }
    if let Some(val) = expand {
        query_parts.push(format!("expand={}", val));
    }
    if let Some(val) = space {
        query_parts.push(format!("space={}", val));
    }
    if let Some(val) = startKey {
        query_parts.push(format!("startKey={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/organizations/{organizationsId}/apiproducts
/// Lists all API product names for an organization. Filter the list by passing an attributename and attibutevalue. The maximum number of API products returned is 1000. You can paginate the list of API products returned using the `startKey` and count query parameters. If the resource has the space attribute set, the response may not return all resources. To learn more, read the [Apigee Spaces Overview](<https://cloud.google.`com/apigee/docs/api-platform/system-administration/spaces/apigee-spaces-overview`>).
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `apigee_organizations_apiproducts_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from apigee_organizations_apiproducts_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_apiproducts_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudApigeeV1ListApiProductsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudApigeeV1ListApiProductsResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/organizations/{organizationsId}/apiproducts
/// Lists all API product names for an organization. Filter the list by passing an attributename and attibutevalue. The maximum number of API products returned is 1000. You can paginate the list of API products returned using the `startKey` and count query parameters. If the resource has the space attribute set, the response may not return all resources. To learn more, read the [Apigee Spaces Overview](<https://cloud.google.`com/apigee/docs/api-platform/system-administration/spaces/apigee-spaces-overview`>).
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `apigee_organizations_apiproducts_list_builder()` + `apigee_organizations_apiproducts_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_apiproducts_list(
    client: &SimpleHttpClient,
    parent: &str,
    attributename: Option<&str>,
    attributevalue: Option<&str>,
    count: Option<&str>,
    expand: Option<bool>,
    space: Option<&str>,
    startKey: Option<&str>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudApigeeV1ListApiProductsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = apigee_organizations_apiproducts_list_builder(
        client,
        parent,
        attributename,
        attributevalue,
        count,
        expand,
        space,
        startKey,
    )?;
    apigee_organizations_apiproducts_list_execute(builder)
}

/// GET v1/organizations/{organizationsId}/apiproducts/{apiproductsId}:move
/// Moves an API product to a different space.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `apigee_organizations_apiproducts_move_execute()` to send, or `apigee_organizations_apiproducts_move` for simplest API.

pub fn apigee_organizations_apiproducts_move_builder(
    client: &SimpleHttpClient,
    name: &str,
    body: &GoogleCloudApigeeV1MoveApiProductRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://apigee.googleapis.com/v1/organizations/{}/apiproducts/{}:move",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/organizations/{organizationsId}/apiproducts/{apiproductsId}:move
/// Moves an API product to a different space.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `apigee_organizations_apiproducts_move_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from apigee_organizations_apiproducts_move_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_apiproducts_move_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudApigeeV1ApiProduct>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudApigeeV1ApiProduct = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/organizations/{organizationsId}/apiproducts/{apiproductsId}:move
/// Moves an API product to a different space.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `apigee_organizations_apiproducts_move_builder()` + `apigee_organizations_apiproducts_move_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_apiproducts_move(
    client: &SimpleHttpClient,
    name: &str,
    body: &GoogleCloudApigeeV1MoveApiProductRequest,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudApigeeV1ApiProduct>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = apigee_organizations_apiproducts_move_builder(client, name, body)?;
    apigee_organizations_apiproducts_move_execute(builder)
}

/// GET v1/organizations/{organizationsId}/apiproducts/{apiproductsId}
/// Updates an existing API product. You must include all required values, whether or not you are updating them, as well as any optional values that you are updating. The API product name required in the request URL is the internal name of the product, not the display name. While they may be the same, it depends on whether the API product was created via UI or API. View the list of API products to identify their internal names.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `apigee_organizations_apiproducts_update_execute()` to send, or `apigee_organizations_apiproducts_update` for simplest API.

pub fn apigee_organizations_apiproducts_update_builder(
    client: &SimpleHttpClient,
    name: &str,
    body: &GoogleCloudApigeeV1ApiProduct,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://apigee.googleapis.com/v1/organizations/{}/apiproducts/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/organizations/{organizationsId}/apiproducts/{apiproductsId}
/// Updates an existing API product. You must include all required values, whether or not you are updating them, as well as any optional values that you are updating. The API product name required in the request URL is the internal name of the product, not the display name. While they may be the same, it depends on whether the API product was created via UI or API. View the list of API products to identify their internal names.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `apigee_organizations_apiproducts_update_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from apigee_organizations_apiproducts_update_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_apiproducts_update_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudApigeeV1ApiProduct>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudApigeeV1ApiProduct = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/organizations/{organizationsId}/apiproducts/{apiproductsId}
/// Updates an existing API product. You must include all required values, whether or not you are updating them, as well as any optional values that you are updating. The API product name required in the request URL is the internal name of the product, not the display name. While they may be the same, it depends on whether the API product was created via UI or API. View the list of API products to identify their internal names.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `apigee_organizations_apiproducts_update_builder()` + `apigee_organizations_apiproducts_update_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_apiproducts_update(
    client: &SimpleHttpClient,
    name: &str,
    body: &GoogleCloudApigeeV1ApiProduct,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudApigeeV1ApiProduct>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = apigee_organizations_apiproducts_update_builder(client, name, body)?;
    apigee_organizations_apiproducts_update_execute(builder)
}

/// GET v1/organizations/{organizationsId}/apiproducts/{apiproductsId}/attributes/{attributesId}
/// Deletes an API product attribute.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `apigee_organizations_apiproducts_attributes_delete_execute()` to send, or `apigee_organizations_apiproducts_attributes_delete` for simplest API.

pub fn apigee_organizations_apiproducts_attributes_delete_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://apigee.googleapis.com/v1/organizations/{}/apiproducts/{}/attributes/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/organizations/{organizationsId}/apiproducts/{apiproductsId}/attributes/{attributesId}
/// Deletes an API product attribute.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `apigee_organizations_apiproducts_attributes_delete_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from apigee_organizations_apiproducts_attributes_delete_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_apiproducts_attributes_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudApigeeV1Attribute>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudApigeeV1Attribute = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/organizations/{organizationsId}/apiproducts/{apiproductsId}/attributes/{attributesId}
/// Deletes an API product attribute.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `apigee_organizations_apiproducts_attributes_delete_builder()` + `apigee_organizations_apiproducts_attributes_delete_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_apiproducts_attributes_delete(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudApigeeV1Attribute>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = apigee_organizations_apiproducts_attributes_delete_builder(client, name)?;
    apigee_organizations_apiproducts_attributes_delete_execute(builder)
}

/// GET v1/organizations/{organizationsId}/apiproducts/{apiproductsId}/attributes/{attributesId}
/// Gets the value of an API product attribute.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `apigee_organizations_apiproducts_attributes_get_execute()` to send, or `apigee_organizations_apiproducts_attributes_get` for simplest API.

pub fn apigee_organizations_apiproducts_attributes_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://apigee.googleapis.com/v1/organizations/{}/apiproducts/{}/attributes/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/organizations/{organizationsId}/apiproducts/{apiproductsId}/attributes/{attributesId}
/// Gets the value of an API product attribute.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `apigee_organizations_apiproducts_attributes_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from apigee_organizations_apiproducts_attributes_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_apiproducts_attributes_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudApigeeV1Attribute>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudApigeeV1Attribute = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/organizations/{organizationsId}/apiproducts/{apiproductsId}/attributes/{attributesId}
/// Gets the value of an API product attribute.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `apigee_organizations_apiproducts_attributes_get_builder()` + `apigee_organizations_apiproducts_attributes_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_apiproducts_attributes_get(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudApigeeV1Attribute>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = apigee_organizations_apiproducts_attributes_get_builder(client, name)?;
    apigee_organizations_apiproducts_attributes_get_execute(builder)
}

/// GET v1/organizations/{organizationsId}/apiproducts/{apiproductsId}/attributes
/// Lists all API product attributes.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `apigee_organizations_apiproducts_attributes_list_execute()` to send, or `apigee_organizations_apiproducts_attributes_list` for simplest API.

pub fn apigee_organizations_apiproducts_attributes_list_builder(
    client: &SimpleHttpClient,
    parent: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://apigee.googleapis.com/v1/organizations/{}/apiproducts/{}/attributes",
        parent,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/organizations/{organizationsId}/apiproducts/{apiproductsId}/attributes
/// Lists all API product attributes.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `apigee_organizations_apiproducts_attributes_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from apigee_organizations_apiproducts_attributes_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_apiproducts_attributes_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudApigeeV1Attributes>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudApigeeV1Attributes = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/organizations/{organizationsId}/apiproducts/{apiproductsId}/attributes
/// Lists all API product attributes.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `apigee_organizations_apiproducts_attributes_list_builder()` + `apigee_organizations_apiproducts_attributes_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_apiproducts_attributes_list(
    client: &SimpleHttpClient,
    parent: &str,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudApigeeV1Attributes>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = apigee_organizations_apiproducts_attributes_list_builder(client, parent)?;
    apigee_organizations_apiproducts_attributes_list_execute(builder)
}

/// GET v1/organizations/{organizationsId}/apiproducts/{apiproductsId}/attributes/{attributesId}
/// Updates the value of an API product attribute. **Note**: OAuth access tokens and Key Management Service (KMS) entities (apps, developers, and API products) are cached for 180 seconds (current default). Any custom attributes associated with entities also get cached for at least 180 seconds after entity is accessed during runtime. In this case, the ExpiresIn element on the OAuthV2 policy won't be able to expire an access token in less than 180 seconds.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `apigee_organizations_apiproducts_attributes_update_api_product_attribute_execute()` to send, or `apigee_organizations_apiproducts_attributes_update_api_product_attribute` for simplest API.

pub fn apigee_organizations_apiproducts_attributes_update_api_product_attribute_builder(
    client: &SimpleHttpClient,
    name: &str,
    body: &GoogleCloudApigeeV1Attribute,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://apigee.googleapis.com/v1/organizations/{}/apiproducts/{}/attributes/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/organizations/{organizationsId}/apiproducts/{apiproductsId}/attributes/{attributesId}
/// Updates the value of an API product attribute. **Note**: OAuth access tokens and Key Management Service (KMS) entities (apps, developers, and API products) are cached for 180 seconds (current default). Any custom attributes associated with entities also get cached for at least 180 seconds after entity is accessed during runtime. In this case, the ExpiresIn element on the OAuthV2 policy won't be able to expire an access token in less than 180 seconds.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `apigee_organizations_apiproducts_attributes_update_api_product_attribute_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from apigee_organizations_apiproducts_attributes_update_api_product_attribute_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_apiproducts_attributes_update_api_product_attribute_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudApigeeV1Attribute>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudApigeeV1Attribute = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/organizations/{organizationsId}/apiproducts/{apiproductsId}/attributes/{attributesId}
/// Updates the value of an API product attribute. **Note**: OAuth access tokens and Key Management Service (KMS) entities (apps, developers, and API products) are cached for 180 seconds (current default). Any custom attributes associated with entities also get cached for at least 180 seconds after entity is accessed during runtime. In this case, the ExpiresIn element on the OAuthV2 policy won't be able to expire an access token in less than 180 seconds.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `apigee_organizations_apiproducts_attributes_update_api_product_attribute_builder()` + `apigee_organizations_apiproducts_attributes_update_api_product_attribute_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_apiproducts_attributes_update_api_product_attribute(
    client: &SimpleHttpClient,
    name: &str,
    body: &GoogleCloudApigeeV1Attribute,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudApigeeV1Attribute>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = apigee_organizations_apiproducts_attributes_update_api_product_attribute_builder(
        client, name, body,
    )?;
    apigee_organizations_apiproducts_attributes_update_api_product_attribute_execute(builder)
}

/// GET v1/organizations/{organizationsId}/apiproducts/{apiproductsId}/rateplans
/// Create a rate plan that is associated with an API product in an organization. Using rate plans, API product owners can monetize their API products by configuring one or more of the following: - Billing frequency - Initial setup fees for using an API product - Payment funding model (postpaid only) - Fixed recurring or consumption-based charges for using an API product - Revenue sharing with developer partners An API product can have multiple rate plans associated with it but *only one* rate plan can be active at any point of time. **Note: From the developer's perspective, they purchase API products not rate plans.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `apigee_organizations_apiproducts_rateplans_create_execute()` to send, or `apigee_organizations_apiproducts_rateplans_create` for simplest API.

pub fn apigee_organizations_apiproducts_rateplans_create_builder(
    client: &SimpleHttpClient,
    parent: &str,
    body: &GoogleCloudApigeeV1RatePlan,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://apigee.googleapis.com/v1/organizations/{}/apiproducts/{}/rateplans",
        parent,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/organizations/{organizationsId}/apiproducts/{apiproductsId}/rateplans
/// Create a rate plan that is associated with an API product in an organization. Using rate plans, API product owners can monetize their API products by configuring one or more of the following: - Billing frequency - Initial setup fees for using an API product - Payment funding model (postpaid only) - Fixed recurring or consumption-based charges for using an API product - Revenue sharing with developer partners An API product can have multiple rate plans associated with it but *only one* rate plan can be active at any point of time. **Note: From the developer's perspective, they purchase API products not rate plans.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `apigee_organizations_apiproducts_rateplans_create_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from apigee_organizations_apiproducts_rateplans_create_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_apiproducts_rateplans_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudApigeeV1RatePlan>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudApigeeV1RatePlan = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/organizations/{organizationsId}/apiproducts/{apiproductsId}/rateplans
/// Create a rate plan that is associated with an API product in an organization. Using rate plans, API product owners can monetize their API products by configuring one or more of the following: - Billing frequency - Initial setup fees for using an API product - Payment funding model (postpaid only) - Fixed recurring or consumption-based charges for using an API product - Revenue sharing with developer partners An API product can have multiple rate plans associated with it but *only one* rate plan can be active at any point of time. **Note: From the developer's perspective, they purchase API products not rate plans.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `apigee_organizations_apiproducts_rateplans_create_builder()` + `apigee_organizations_apiproducts_rateplans_create_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_apiproducts_rateplans_create(
    client: &SimpleHttpClient,
    parent: &str,
    body: &GoogleCloudApigeeV1RatePlan,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudApigeeV1RatePlan>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = apigee_organizations_apiproducts_rateplans_create_builder(client, parent, body)?;
    apigee_organizations_apiproducts_rateplans_create_execute(builder)
}

/// GET v1/organizations/{organizationsId}/apiproducts/{apiproductsId}/rateplans/{rateplansId}
/// Deletes a rate plan.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `apigee_organizations_apiproducts_rateplans_delete_execute()` to send, or `apigee_organizations_apiproducts_rateplans_delete` for simplest API.

pub fn apigee_organizations_apiproducts_rateplans_delete_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://apigee.googleapis.com/v1/organizations/{}/apiproducts/{}/rateplans/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/organizations/{organizationsId}/apiproducts/{apiproductsId}/rateplans/{rateplansId}
/// Deletes a rate plan.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `apigee_organizations_apiproducts_rateplans_delete_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from apigee_organizations_apiproducts_rateplans_delete_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_apiproducts_rateplans_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudApigeeV1RatePlan>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudApigeeV1RatePlan = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/organizations/{organizationsId}/apiproducts/{apiproductsId}/rateplans/{rateplansId}
/// Deletes a rate plan.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `apigee_organizations_apiproducts_rateplans_delete_builder()` + `apigee_organizations_apiproducts_rateplans_delete_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_apiproducts_rateplans_delete(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudApigeeV1RatePlan>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = apigee_organizations_apiproducts_rateplans_delete_builder(client, name)?;
    apigee_organizations_apiproducts_rateplans_delete_execute(builder)
}

/// GET v1/organizations/{organizationsId}/apiproducts/{apiproductsId}/rateplans/{rateplansId}
/// Gets the details of a rate plan.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `apigee_organizations_apiproducts_rateplans_get_execute()` to send, or `apigee_organizations_apiproducts_rateplans_get` for simplest API.

pub fn apigee_organizations_apiproducts_rateplans_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://apigee.googleapis.com/v1/organizations/{}/apiproducts/{}/rateplans/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/organizations/{organizationsId}/apiproducts/{apiproductsId}/rateplans/{rateplansId}
/// Gets the details of a rate plan.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `apigee_organizations_apiproducts_rateplans_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from apigee_organizations_apiproducts_rateplans_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_apiproducts_rateplans_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudApigeeV1RatePlan>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudApigeeV1RatePlan = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/organizations/{organizationsId}/apiproducts/{apiproductsId}/rateplans/{rateplansId}
/// Gets the details of a rate plan.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `apigee_organizations_apiproducts_rateplans_get_builder()` + `apigee_organizations_apiproducts_rateplans_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_apiproducts_rateplans_get(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudApigeeV1RatePlan>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = apigee_organizations_apiproducts_rateplans_get_builder(client, name)?;
    apigee_organizations_apiproducts_rateplans_get_execute(builder)
}

/// GET v1/organizations/{organizationsId}/apiproducts/{apiproductsId}/rateplans
/// Lists all the rate plans for an API product.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `apigee_organizations_apiproducts_rateplans_list_execute()` to send, or `apigee_organizations_apiproducts_rateplans_list` for simplest API.

pub fn apigee_organizations_apiproducts_rateplans_list_builder(
    client: &SimpleHttpClient,
    parent: &str,
    count: Option<i32>,
    expand: Option<bool>,
    orderBy: Option<&str>,
    startKey: Option<&str>,
    state: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://apigee.googleapis.com/v1/organizations/{}/apiproducts/{}/rateplans",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = count {
        query_parts.push(format!("count={}", val));
    }
    if let Some(val) = expand {
        query_parts.push(format!("expand={}", val));
    }
    if let Some(val) = orderBy {
        query_parts.push(format!("orderBy={}", val));
    }
    if let Some(val) = startKey {
        query_parts.push(format!("startKey={}", val));
    }
    if let Some(val) = state {
        query_parts.push(format!("state={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/organizations/{organizationsId}/apiproducts/{apiproductsId}/rateplans
/// Lists all the rate plans for an API product.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `apigee_organizations_apiproducts_rateplans_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from apigee_organizations_apiproducts_rateplans_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_apiproducts_rateplans_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudApigeeV1ListRatePlansResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudApigeeV1ListRatePlansResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/organizations/{organizationsId}/apiproducts/{apiproductsId}/rateplans
/// Lists all the rate plans for an API product.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `apigee_organizations_apiproducts_rateplans_list_builder()` + `apigee_organizations_apiproducts_rateplans_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_apiproducts_rateplans_list(
    client: &SimpleHttpClient,
    parent: &str,
    count: Option<i32>,
    expand: Option<bool>,
    orderBy: Option<&str>,
    startKey: Option<&str>,
    state: Option<&str>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudApigeeV1ListRatePlansResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = apigee_organizations_apiproducts_rateplans_list_builder(
        client, parent, count, expand, orderBy, startKey, state,
    )?;
    apigee_organizations_apiproducts_rateplans_list_execute(builder)
}

/// GET v1/organizations/{organizationsId}/apiproducts/{apiproductsId}/rateplans/{rateplansId}
/// Updates an existing rate plan.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `apigee_organizations_apiproducts_rateplans_update_execute()` to send, or `apigee_organizations_apiproducts_rateplans_update` for simplest API.

pub fn apigee_organizations_apiproducts_rateplans_update_builder(
    client: &SimpleHttpClient,
    name: &str,
    body: &GoogleCloudApigeeV1RatePlan,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://apigee.googleapis.com/v1/organizations/{}/apiproducts/{}/rateplans/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/organizations/{organizationsId}/apiproducts/{apiproductsId}/rateplans/{rateplansId}
/// Updates an existing rate plan.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `apigee_organizations_apiproducts_rateplans_update_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from apigee_organizations_apiproducts_rateplans_update_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_apiproducts_rateplans_update_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudApigeeV1RatePlan>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudApigeeV1RatePlan = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/organizations/{organizationsId}/apiproducts/{apiproductsId}/rateplans/{rateplansId}
/// Updates an existing rate plan.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `apigee_organizations_apiproducts_rateplans_update_builder()` + `apigee_organizations_apiproducts_rateplans_update_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_apiproducts_rateplans_update(
    client: &SimpleHttpClient,
    name: &str,
    body: &GoogleCloudApigeeV1RatePlan,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudApigeeV1RatePlan>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = apigee_organizations_apiproducts_rateplans_update_builder(client, name, body)?;
    apigee_organizations_apiproducts_rateplans_update_execute(builder)
}

/// GET v1/organizations/{organizationsId}/apis
/// Creates an API proxy. The API proxy created will not be accessible at runtime until it is deployed to an environment. Create a new API proxy by setting the name query parameter to the name of the API proxy. Import an API proxy configuration bundle stored in zip format on your local machine to your organization by doing the following: * Set the name query parameter to the name of the API proxy. * Set the action query parameter to import. * Set the Content-Type header to `multipart/form-data`. * Pass as a file the name of API proxy configuration bundle stored in zip format on your local machine using the file form field. **Note**: To validate the API proxy configuration bundle only without importing it, set the action query parameter to validate. When importing an API proxy configuration bundle, if the API proxy does not exist, it will be created. If the API proxy exists, then a new revision is created. Invalid API proxy configurations are rejected, and a list of validation errors is returned to the client.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `apigee_organizations_apis_create_execute()` to send, or `apigee_organizations_apis_create` for simplest API.

pub fn apigee_organizations_apis_create_builder(
    client: &SimpleHttpClient,
    parent: &str,
    action: Option<&str>,
    name: Option<&str>,
    space: Option<&str>,
    validate: Option<bool>,
    body: &GoogleApiHttpBody,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://apigee.googleapis.com/v1/organizations/{}/apis",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = action {
        query_parts.push(format!("action={}", val));
    }
    if let Some(val) = name {
        query_parts.push(format!("name={}", val));
    }
    if let Some(val) = space {
        query_parts.push(format!("space={}", val));
    }
    if let Some(val) = validate {
        query_parts.push(format!("validate={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/organizations/{organizationsId}/apis
/// Creates an API proxy. The API proxy created will not be accessible at runtime until it is deployed to an environment. Create a new API proxy by setting the name query parameter to the name of the API proxy. Import an API proxy configuration bundle stored in zip format on your local machine to your organization by doing the following: * Set the name query parameter to the name of the API proxy. * Set the action query parameter to import. * Set the Content-Type header to `multipart/form-data`. * Pass as a file the name of API proxy configuration bundle stored in zip format on your local machine using the file form field. **Note**: To validate the API proxy configuration bundle only without importing it, set the action query parameter to validate. When importing an API proxy configuration bundle, if the API proxy does not exist, it will be created. If the API proxy exists, then a new revision is created. Invalid API proxy configurations are rejected, and a list of validation errors is returned to the client.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `apigee_organizations_apis_create_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from apigee_organizations_apis_create_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_apis_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudApigeeV1ApiProxyRevision>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudApigeeV1ApiProxyRevision = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/organizations/{organizationsId}/apis
/// Creates an API proxy. The API proxy created will not be accessible at runtime until it is deployed to an environment. Create a new API proxy by setting the name query parameter to the name of the API proxy. Import an API proxy configuration bundle stored in zip format on your local machine to your organization by doing the following: * Set the name query parameter to the name of the API proxy. * Set the action query parameter to import. * Set the Content-Type header to `multipart/form-data`. * Pass as a file the name of API proxy configuration bundle stored in zip format on your local machine using the file form field. **Note**: To validate the API proxy configuration bundle only without importing it, set the action query parameter to validate. When importing an API proxy configuration bundle, if the API proxy does not exist, it will be created. If the API proxy exists, then a new revision is created. Invalid API proxy configurations are rejected, and a list of validation errors is returned to the client.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `apigee_organizations_apis_create_builder()` + `apigee_organizations_apis_create_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_apis_create(
    client: &SimpleHttpClient,
    parent: &str,
    action: Option<&str>,
    name: Option<&str>,
    space: Option<&str>,
    validate: Option<bool>,
    body: &GoogleApiHttpBody,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudApigeeV1ApiProxyRevision>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = apigee_organizations_apis_create_builder(
        client, parent, action, name, space, validate, body,
    )?;
    apigee_organizations_apis_create_execute(builder)
}

/// GET v1/organizations/{organizationsId}/apis/{apisId}
/// Deletes an API proxy and all associated endpoints, policies, resources, and revisions. The API proxy must be undeployed before you can delete it.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `apigee_organizations_apis_delete_execute()` to send, or `apigee_organizations_apis_delete` for simplest API.

pub fn apigee_organizations_apis_delete_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://apigee.googleapis.com/v1/organizations/{}/apis/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/organizations/{organizationsId}/apis/{apisId}
/// Deletes an API proxy and all associated endpoints, policies, resources, and revisions. The API proxy must be undeployed before you can delete it.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `apigee_organizations_apis_delete_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from apigee_organizations_apis_delete_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_apis_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudApigeeV1ApiProxy>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudApigeeV1ApiProxy = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/organizations/{organizationsId}/apis/{apisId}
/// Deletes an API proxy and all associated endpoints, policies, resources, and revisions. The API proxy must be undeployed before you can delete it.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `apigee_organizations_apis_delete_builder()` + `apigee_organizations_apis_delete_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_apis_delete(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudApigeeV1ApiProxy>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = apigee_organizations_apis_delete_builder(client, name)?;
    apigee_organizations_apis_delete_execute(builder)
}

/// GET v1/organizations/{organizationsId}/apis/{apisId}
/// Gets an API proxy including a list of existing revisions.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `apigee_organizations_apis_get_execute()` to send, or `apigee_organizations_apis_get` for simplest API.

pub fn apigee_organizations_apis_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://apigee.googleapis.com/v1/organizations/{}/apis/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/organizations/{organizationsId}/apis/{apisId}
/// Gets an API proxy including a list of existing revisions.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `apigee_organizations_apis_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from apigee_organizations_apis_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_apis_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudApigeeV1ApiProxy>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudApigeeV1ApiProxy = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/organizations/{organizationsId}/apis/{apisId}
/// Gets an API proxy including a list of existing revisions.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `apigee_organizations_apis_get_builder()` + `apigee_organizations_apis_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_apis_get(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudApigeeV1ApiProxy>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = apigee_organizations_apis_get_builder(client, name)?;
    apigee_organizations_apis_get_execute(builder)
}

/// GET v1/organizations/{organizationsId}/apis
/// Lists the names of all API proxies in an organization. The names returned correspond to the names defined in the configuration files for each API proxy. If the resource has the space attribute set, the response may not return all resources. To learn more, read the [Apigee Spaces Overview](<https://cloud.google.`com/apigee/docs/api-platform/system-administration/spaces/apigee-spaces-overview`>).
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `apigee_organizations_apis_list_execute()` to send, or `apigee_organizations_apis_list` for simplest API.

pub fn apigee_organizations_apis_list_builder(
    client: &SimpleHttpClient,
    parent: &str,
    includeMetaData: Option<bool>,
    includeRevisions: Option<bool>,
    space: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://apigee.googleapis.com/v1/organizations/{}/apis",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = includeMetaData {
        query_parts.push(format!("includeMetaData={}", val));
    }
    if let Some(val) = includeRevisions {
        query_parts.push(format!("includeRevisions={}", val));
    }
    if let Some(val) = space {
        query_parts.push(format!("space={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/organizations/{organizationsId}/apis
/// Lists the names of all API proxies in an organization. The names returned correspond to the names defined in the configuration files for each API proxy. If the resource has the space attribute set, the response may not return all resources. To learn more, read the [Apigee Spaces Overview](<https://cloud.google.`com/apigee/docs/api-platform/system-administration/spaces/apigee-spaces-overview`>).
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `apigee_organizations_apis_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from apigee_organizations_apis_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_apis_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudApigeeV1ListApiProxiesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudApigeeV1ListApiProxiesResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/organizations/{organizationsId}/apis
/// Lists the names of all API proxies in an organization. The names returned correspond to the names defined in the configuration files for each API proxy. If the resource has the space attribute set, the response may not return all resources. To learn more, read the [Apigee Spaces Overview](<https://cloud.google.`com/apigee/docs/api-platform/system-administration/spaces/apigee-spaces-overview`>).
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `apigee_organizations_apis_list_builder()` + `apigee_organizations_apis_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_apis_list(
    client: &SimpleHttpClient,
    parent: &str,
    includeMetaData: Option<bool>,
    includeRevisions: Option<bool>,
    space: Option<&str>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudApigeeV1ListApiProxiesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = apigee_organizations_apis_list_builder(
        client,
        parent,
        includeMetaData,
        includeRevisions,
        space,
    )?;
    apigee_organizations_apis_list_execute(builder)
}

/// GET v1/organizations/{organizationsId}/apis/{apisId}:move
/// Moves an API proxy to a different space.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `apigee_organizations_apis_move_execute()` to send, or `apigee_organizations_apis_move` for simplest API.

pub fn apigee_organizations_apis_move_builder(
    client: &SimpleHttpClient,
    name: &str,
    body: &GoogleCloudApigeeV1MoveApiProxyRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://apigee.googleapis.com/v1/organizations/{}/apis/{}:move",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/organizations/{organizationsId}/apis/{apisId}:move
/// Moves an API proxy to a different space.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `apigee_organizations_apis_move_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from apigee_organizations_apis_move_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_apis_move_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudApigeeV1ApiProxy>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudApigeeV1ApiProxy = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/organizations/{organizationsId}/apis/{apisId}:move
/// Moves an API proxy to a different space.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `apigee_organizations_apis_move_builder()` + `apigee_organizations_apis_move_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_apis_move(
    client: &SimpleHttpClient,
    name: &str,
    body: &GoogleCloudApigeeV1MoveApiProxyRequest,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudApigeeV1ApiProxy>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = apigee_organizations_apis_move_builder(client, name, body)?;
    apigee_organizations_apis_move_execute(builder)
}

/// GET v1/organizations/{organizationsId}/apis/{apisId}
/// Updates an existing API proxy.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `apigee_organizations_apis_patch_execute()` to send, or `apigee_organizations_apis_patch` for simplest API.

pub fn apigee_organizations_apis_patch_builder(
    client: &SimpleHttpClient,
    name: &str,
    updateMask: Option<&str>,
    body: &GoogleCloudApigeeV1ApiProxy,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://apigee.googleapis.com/v1/organizations/{}/apis/{}",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = updateMask {
        query_parts.push(format!("updateMask={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/organizations/{organizationsId}/apis/{apisId}
/// Updates an existing API proxy.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `apigee_organizations_apis_patch_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from apigee_organizations_apis_patch_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_apis_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudApigeeV1ApiProxy>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudApigeeV1ApiProxy = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/organizations/{organizationsId}/apis/{apisId}
/// Updates an existing API proxy.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `apigee_organizations_apis_patch_builder()` + `apigee_organizations_apis_patch_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_apis_patch(
    client: &SimpleHttpClient,
    name: &str,
    updateMask: Option<&str>,
    body: &GoogleCloudApigeeV1ApiProxy,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudApigeeV1ApiProxy>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = apigee_organizations_apis_patch_builder(client, name, updateMask, body)?;
    apigee_organizations_apis_patch_execute(builder)
}

/// GET v1/organizations/{organizationsId}/apis/{apisId}/debugsessions
/// Lists debug sessions that are currently active in the given API Proxy.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `apigee_organizations_apis_debugsessions_list_execute()` to send, or `apigee_organizations_apis_debugsessions_list` for simplest API.

pub fn apigee_organizations_apis_debugsessions_list_builder(
    client: &SimpleHttpClient,
    parent: &str,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://apigee.googleapis.com/v1/organizations/{}/apis/{}/debugsessions",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/organizations/{organizationsId}/apis/{apisId}/debugsessions
/// Lists debug sessions that are currently active in the given API Proxy.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `apigee_organizations_apis_debugsessions_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from apigee_organizations_apis_debugsessions_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_apis_debugsessions_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudApigeeV1ListApiDebugSessionsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudApigeeV1ListApiDebugSessionsResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/organizations/{organizationsId}/apis/{apisId}/debugsessions
/// Lists debug sessions that are currently active in the given API Proxy.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `apigee_organizations_apis_debugsessions_list_builder()` + `apigee_organizations_apis_debugsessions_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_apis_debugsessions_list(
    client: &SimpleHttpClient,
    parent: &str,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudApigeeV1ListApiDebugSessionsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        apigee_organizations_apis_debugsessions_list_builder(client, parent, pageSize, pageToken)?;
    apigee_organizations_apis_debugsessions_list_execute(builder)
}

/// GET v1/organizations/{organizationsId}/apis/{apisId}/deployments
/// Lists all deployments of an API proxy.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `apigee_organizations_apis_deployments_list_execute()` to send, or `apigee_organizations_apis_deployments_list` for simplest API.

pub fn apigee_organizations_apis_deployments_list_builder(
    client: &SimpleHttpClient,
    parent: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://apigee.googleapis.com/v1/organizations/{}/apis/{}/deployments",
        parent,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/organizations/{organizationsId}/apis/{apisId}/deployments
/// Lists all deployments of an API proxy.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `apigee_organizations_apis_deployments_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from apigee_organizations_apis_deployments_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_apis_deployments_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudApigeeV1ListDeploymentsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudApigeeV1ListDeploymentsResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/organizations/{organizationsId}/apis/{apisId}/deployments
/// Lists all deployments of an API proxy.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `apigee_organizations_apis_deployments_list_builder()` + `apigee_organizations_apis_deployments_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_apis_deployments_list(
    client: &SimpleHttpClient,
    parent: &str,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudApigeeV1ListDeploymentsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = apigee_organizations_apis_deployments_list_builder(client, parent)?;
    apigee_organizations_apis_deployments_list_execute(builder)
}

/// GET v1/organizations/{organizationsId}/apis/{apisId}/keyvaluemaps
/// Creates a key value map in an API proxy.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `apigee_organizations_apis_keyvaluemaps_create_execute()` to send, or `apigee_organizations_apis_keyvaluemaps_create` for simplest API.

pub fn apigee_organizations_apis_keyvaluemaps_create_builder(
    client: &SimpleHttpClient,
    parent: &str,
    body: &GoogleCloudApigeeV1KeyValueMap,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://apigee.googleapis.com/v1/organizations/{}/apis/{}/keyvaluemaps",
        parent,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/organizations/{organizationsId}/apis/{apisId}/keyvaluemaps
/// Creates a key value map in an API proxy.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `apigee_organizations_apis_keyvaluemaps_create_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from apigee_organizations_apis_keyvaluemaps_create_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_apis_keyvaluemaps_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudApigeeV1KeyValueMap>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudApigeeV1KeyValueMap = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/organizations/{organizationsId}/apis/{apisId}/keyvaluemaps
/// Creates a key value map in an API proxy.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `apigee_organizations_apis_keyvaluemaps_create_builder()` + `apigee_organizations_apis_keyvaluemaps_create_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_apis_keyvaluemaps_create(
    client: &SimpleHttpClient,
    parent: &str,
    body: &GoogleCloudApigeeV1KeyValueMap,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudApigeeV1KeyValueMap>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = apigee_organizations_apis_keyvaluemaps_create_builder(client, parent, body)?;
    apigee_organizations_apis_keyvaluemaps_create_execute(builder)
}

/// GET v1/organizations/{organizationsId}/apis/{apisId}/keyvaluemaps/{keyvaluemapsId}
/// Deletes a key value map from an API proxy.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `apigee_organizations_apis_keyvaluemaps_delete_execute()` to send, or `apigee_organizations_apis_keyvaluemaps_delete` for simplest API.

pub fn apigee_organizations_apis_keyvaluemaps_delete_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://apigee.googleapis.com/v1/organizations/{}/apis/{}/keyvaluemaps/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/organizations/{organizationsId}/apis/{apisId}/keyvaluemaps/{keyvaluemapsId}
/// Deletes a key value map from an API proxy.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `apigee_organizations_apis_keyvaluemaps_delete_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from apigee_organizations_apis_keyvaluemaps_delete_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_apis_keyvaluemaps_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudApigeeV1KeyValueMap>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudApigeeV1KeyValueMap = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/organizations/{organizationsId}/apis/{apisId}/keyvaluemaps/{keyvaluemapsId}
/// Deletes a key value map from an API proxy.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `apigee_organizations_apis_keyvaluemaps_delete_builder()` + `apigee_organizations_apis_keyvaluemaps_delete_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_apis_keyvaluemaps_delete(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudApigeeV1KeyValueMap>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = apigee_organizations_apis_keyvaluemaps_delete_builder(client, name)?;
    apigee_organizations_apis_keyvaluemaps_delete_execute(builder)
}

/// GET v1/organizations/{organizationsId}/apis/{apisId}/keyvaluemaps/{keyvaluemapsId}
/// Get the key value map scoped to an organization, environment, or API proxy.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `apigee_organizations_apis_keyvaluemaps_get_execute()` to send, or `apigee_organizations_apis_keyvaluemaps_get` for simplest API.

pub fn apigee_organizations_apis_keyvaluemaps_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://apigee.googleapis.com/v1/organizations/{}/apis/{}/keyvaluemaps/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/organizations/{organizationsId}/apis/{apisId}/keyvaluemaps/{keyvaluemapsId}
/// Get the key value map scoped to an organization, environment, or API proxy.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `apigee_organizations_apis_keyvaluemaps_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from apigee_organizations_apis_keyvaluemaps_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_apis_keyvaluemaps_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudApigeeV1KeyValueMap>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudApigeeV1KeyValueMap = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/organizations/{organizationsId}/apis/{apisId}/keyvaluemaps/{keyvaluemapsId}
/// Get the key value map scoped to an organization, environment, or API proxy.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `apigee_organizations_apis_keyvaluemaps_get_builder()` + `apigee_organizations_apis_keyvaluemaps_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_apis_keyvaluemaps_get(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudApigeeV1KeyValueMap>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = apigee_organizations_apis_keyvaluemaps_get_builder(client, name)?;
    apigee_organizations_apis_keyvaluemaps_get_execute(builder)
}

/// GET v1/organizations/{organizationsId}/apis/{apisId}/keyvaluemaps/{keyvaluemapsId}
/// Update the key value map scoped to an organization, environment, or API proxy.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `apigee_organizations_apis_keyvaluemaps_update_execute()` to send, or `apigee_organizations_apis_keyvaluemaps_update` for simplest API.

pub fn apigee_organizations_apis_keyvaluemaps_update_builder(
    client: &SimpleHttpClient,
    name: &str,
    body: &GoogleCloudApigeeV1KeyValueMap,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://apigee.googleapis.com/v1/organizations/{}/apis/{}/keyvaluemaps/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/organizations/{organizationsId}/apis/{apisId}/keyvaluemaps/{keyvaluemapsId}
/// Update the key value map scoped to an organization, environment, or API proxy.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `apigee_organizations_apis_keyvaluemaps_update_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from apigee_organizations_apis_keyvaluemaps_update_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_apis_keyvaluemaps_update_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudApigeeV1KeyValueMap>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudApigeeV1KeyValueMap = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/organizations/{organizationsId}/apis/{apisId}/keyvaluemaps/{keyvaluemapsId}
/// Update the key value map scoped to an organization, environment, or API proxy.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `apigee_organizations_apis_keyvaluemaps_update_builder()` + `apigee_organizations_apis_keyvaluemaps_update_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_apis_keyvaluemaps_update(
    client: &SimpleHttpClient,
    name: &str,
    body: &GoogleCloudApigeeV1KeyValueMap,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudApigeeV1KeyValueMap>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = apigee_organizations_apis_keyvaluemaps_update_builder(client, name, body)?;
    apigee_organizations_apis_keyvaluemaps_update_execute(builder)
}

/// GET v1/organizations/{organizationsId}/apis/{apisId}/keyvaluemaps/{keyvaluemapsId}/entries
/// Creates key value entries in a key value map scoped to an organization, environment, or API proxy. **Note**: Supported for Apigee hybrid 1.8.x and higher.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `apigee_organizations_apis_keyvaluemaps_entries_create_execute()` to send, or `apigee_organizations_apis_keyvaluemaps_entries_create` for simplest API.

pub fn apigee_organizations_apis_keyvaluemaps_entries_create_builder(
    client: &SimpleHttpClient,
    parent: &str,
    body: &GoogleCloudApigeeV1KeyValueEntry,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://apigee.googleapis.com/v1/organizations/{}/apis/{}/keyvaluemaps/{}/entries",
        parent,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/organizations/{organizationsId}/apis/{apisId}/keyvaluemaps/{keyvaluemapsId}/entries
/// Creates key value entries in a key value map scoped to an organization, environment, or API proxy. **Note**: Supported for Apigee hybrid 1.8.x and higher.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `apigee_organizations_apis_keyvaluemaps_entries_create_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from apigee_organizations_apis_keyvaluemaps_entries_create_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_apis_keyvaluemaps_entries_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudApigeeV1KeyValueEntry>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudApigeeV1KeyValueEntry = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/organizations/{organizationsId}/apis/{apisId}/keyvaluemaps/{keyvaluemapsId}/entries
/// Creates key value entries in a key value map scoped to an organization, environment, or API proxy. **Note**: Supported for Apigee hybrid 1.8.x and higher.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `apigee_organizations_apis_keyvaluemaps_entries_create_builder()` + `apigee_organizations_apis_keyvaluemaps_entries_create_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_apis_keyvaluemaps_entries_create(
    client: &SimpleHttpClient,
    parent: &str,
    body: &GoogleCloudApigeeV1KeyValueEntry,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudApigeeV1KeyValueEntry>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        apigee_organizations_apis_keyvaluemaps_entries_create_builder(client, parent, body)?;
    apigee_organizations_apis_keyvaluemaps_entries_create_execute(builder)
}

/// GET v1/organizations/{organizationsId}/apis/{apisId}/keyvaluemaps/{keyvaluemapsId}/entries/{entriesId}
/// Deletes a key value entry from a key value map scoped to an organization, environment, or API proxy. **Notes:** * After you delete the key value entry, the policy consuming the entry will continue to function with its cached values for a few minutes. This is expected behavior. * Supported for Apigee hybrid 1.8.x and higher.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `apigee_organizations_apis_keyvaluemaps_entries_delete_execute()` to send, or `apigee_organizations_apis_keyvaluemaps_entries_delete` for simplest API.

pub fn apigee_organizations_apis_keyvaluemaps_entries_delete_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://apigee.googleapis.com/v1/organizations/{}/apis/{}/keyvaluemaps/{}/entries/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/organizations/{organizationsId}/apis/{apisId}/keyvaluemaps/{keyvaluemapsId}/entries/{entriesId}
/// Deletes a key value entry from a key value map scoped to an organization, environment, or API proxy. **Notes:** * After you delete the key value entry, the policy consuming the entry will continue to function with its cached values for a few minutes. This is expected behavior. * Supported for Apigee hybrid 1.8.x and higher.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `apigee_organizations_apis_keyvaluemaps_entries_delete_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from apigee_organizations_apis_keyvaluemaps_entries_delete_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_apis_keyvaluemaps_entries_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudApigeeV1KeyValueEntry>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudApigeeV1KeyValueEntry = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/organizations/{organizationsId}/apis/{apisId}/keyvaluemaps/{keyvaluemapsId}/entries/{entriesId}
/// Deletes a key value entry from a key value map scoped to an organization, environment, or API proxy. **Notes:** * After you delete the key value entry, the policy consuming the entry will continue to function with its cached values for a few minutes. This is expected behavior. * Supported for Apigee hybrid 1.8.x and higher.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `apigee_organizations_apis_keyvaluemaps_entries_delete_builder()` + `apigee_organizations_apis_keyvaluemaps_entries_delete_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_apis_keyvaluemaps_entries_delete(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudApigeeV1KeyValueEntry>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = apigee_organizations_apis_keyvaluemaps_entries_delete_builder(client, name)?;
    apigee_organizations_apis_keyvaluemaps_entries_delete_execute(builder)
}

/// GET v1/organizations/{organizationsId}/apis/{apisId}/keyvaluemaps/{keyvaluemapsId}/entries/{entriesId}
/// Get the key value entry value for a key value map scoped to an organization, environment, or API proxy. **Note**: Supported for Apigee hybrid 1.8.x and higher.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `apigee_organizations_apis_keyvaluemaps_entries_get_execute()` to send, or `apigee_organizations_apis_keyvaluemaps_entries_get` for simplest API.

pub fn apigee_organizations_apis_keyvaluemaps_entries_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://apigee.googleapis.com/v1/organizations/{}/apis/{}/keyvaluemaps/{}/entries/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/organizations/{organizationsId}/apis/{apisId}/keyvaluemaps/{keyvaluemapsId}/entries/{entriesId}
/// Get the key value entry value for a key value map scoped to an organization, environment, or API proxy. **Note**: Supported for Apigee hybrid 1.8.x and higher.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `apigee_organizations_apis_keyvaluemaps_entries_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from apigee_organizations_apis_keyvaluemaps_entries_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_apis_keyvaluemaps_entries_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudApigeeV1KeyValueEntry>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudApigeeV1KeyValueEntry = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/organizations/{organizationsId}/apis/{apisId}/keyvaluemaps/{keyvaluemapsId}/entries/{entriesId}
/// Get the key value entry value for a key value map scoped to an organization, environment, or API proxy. **Note**: Supported for Apigee hybrid 1.8.x and higher.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `apigee_organizations_apis_keyvaluemaps_entries_get_builder()` + `apigee_organizations_apis_keyvaluemaps_entries_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_apis_keyvaluemaps_entries_get(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudApigeeV1KeyValueEntry>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = apigee_organizations_apis_keyvaluemaps_entries_get_builder(client, name)?;
    apigee_organizations_apis_keyvaluemaps_entries_get_execute(builder)
}

/// GET v1/organizations/{organizationsId}/apis/{apisId}/keyvaluemaps/{keyvaluemapsId}/entries
/// Lists key value entries for key values maps scoped to an organization, environment, or API proxy. **Note**: Supported for Apigee hybrid 1.8.x and higher.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `apigee_organizations_apis_keyvaluemaps_entries_list_execute()` to send, or `apigee_organizations_apis_keyvaluemaps_entries_list` for simplest API.

pub fn apigee_organizations_apis_keyvaluemaps_entries_list_builder(
    client: &SimpleHttpClient,
    parent: &str,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://apigee.googleapis.com/v1/organizations/{}/apis/{}/keyvaluemaps/{}/entries",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/organizations/{organizationsId}/apis/{apisId}/keyvaluemaps/{keyvaluemapsId}/entries
/// Lists key value entries for key values maps scoped to an organization, environment, or API proxy. **Note**: Supported for Apigee hybrid 1.8.x and higher.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `apigee_organizations_apis_keyvaluemaps_entries_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from apigee_organizations_apis_keyvaluemaps_entries_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_apis_keyvaluemaps_entries_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudApigeeV1ListKeyValueEntriesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudApigeeV1ListKeyValueEntriesResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/organizations/{organizationsId}/apis/{apisId}/keyvaluemaps/{keyvaluemapsId}/entries
/// Lists key value entries for key values maps scoped to an organization, environment, or API proxy. **Note**: Supported for Apigee hybrid 1.8.x and higher.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `apigee_organizations_apis_keyvaluemaps_entries_list_builder()` + `apigee_organizations_apis_keyvaluemaps_entries_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_apis_keyvaluemaps_entries_list(
    client: &SimpleHttpClient,
    parent: &str,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudApigeeV1ListKeyValueEntriesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = apigee_organizations_apis_keyvaluemaps_entries_list_builder(
        client, parent, pageSize, pageToken,
    )?;
    apigee_organizations_apis_keyvaluemaps_entries_list_execute(builder)
}

/// GET v1/organizations/{organizationsId}/apis/{apisId}/keyvaluemaps/{keyvaluemapsId}/entries/{entriesId}
/// Update key value entry scoped to an organization, environment, or API proxy for an existing key.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `apigee_organizations_apis_keyvaluemaps_entries_update_execute()` to send, or `apigee_organizations_apis_keyvaluemaps_entries_update` for simplest API.

pub fn apigee_organizations_apis_keyvaluemaps_entries_update_builder(
    client: &SimpleHttpClient,
    name: &str,
    body: &GoogleCloudApigeeV1KeyValueEntry,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://apigee.googleapis.com/v1/organizations/{}/apis/{}/keyvaluemaps/{}/entries/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/organizations/{organizationsId}/apis/{apisId}/keyvaluemaps/{keyvaluemapsId}/entries/{entriesId}
/// Update key value entry scoped to an organization, environment, or API proxy for an existing key.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `apigee_organizations_apis_keyvaluemaps_entries_update_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from apigee_organizations_apis_keyvaluemaps_entries_update_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_apis_keyvaluemaps_entries_update_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudApigeeV1KeyValueEntry>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudApigeeV1KeyValueEntry = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/organizations/{organizationsId}/apis/{apisId}/keyvaluemaps/{keyvaluemapsId}/entries/{entriesId}
/// Update key value entry scoped to an organization, environment, or API proxy for an existing key.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `apigee_organizations_apis_keyvaluemaps_entries_update_builder()` + `apigee_organizations_apis_keyvaluemaps_entries_update_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_apis_keyvaluemaps_entries_update(
    client: &SimpleHttpClient,
    name: &str,
    body: &GoogleCloudApigeeV1KeyValueEntry,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudApigeeV1KeyValueEntry>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        apigee_organizations_apis_keyvaluemaps_entries_update_builder(client, name, body)?;
    apigee_organizations_apis_keyvaluemaps_entries_update_execute(builder)
}

/// GET v1/organizations/{organizationsId}/apis/{apisId}/revisions/{revisionsId}
/// Deletes an API proxy revision and all policies, resources, endpoints, and revisions associated with it. The API proxy revision must be undeployed before you can delete it.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `apigee_organizations_apis_revisions_delete_execute()` to send, or `apigee_organizations_apis_revisions_delete` for simplest API.

pub fn apigee_organizations_apis_revisions_delete_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://apigee.googleapis.com/v1/organizations/{}/apis/{}/revisions/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/organizations/{organizationsId}/apis/{apisId}/revisions/{revisionsId}
/// Deletes an API proxy revision and all policies, resources, endpoints, and revisions associated with it. The API proxy revision must be undeployed before you can delete it.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `apigee_organizations_apis_revisions_delete_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from apigee_organizations_apis_revisions_delete_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_apis_revisions_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudApigeeV1ApiProxyRevision>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudApigeeV1ApiProxyRevision = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/organizations/{organizationsId}/apis/{apisId}/revisions/{revisionsId}
/// Deletes an API proxy revision and all policies, resources, endpoints, and revisions associated with it. The API proxy revision must be undeployed before you can delete it.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `apigee_organizations_apis_revisions_delete_builder()` + `apigee_organizations_apis_revisions_delete_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_apis_revisions_delete(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudApigeeV1ApiProxyRevision>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = apigee_organizations_apis_revisions_delete_builder(client, name)?;
    apigee_organizations_apis_revisions_delete_execute(builder)
}

/// GET v1/organizations/{organizationsId}/apis/{apisId}/revisions/{revisionsId}
/// Gets an API proxy revision. To download the API proxy configuration bundle for the specified revision as a zip file, set the format query parameter to bundle. If you are using curl, specify -o filename.zip to save the output to a file; otherwise, it displays to stdout. Then, develop the API proxy configuration locally and upload the updated API proxy configuration revision, as described in [`updateApiProxyRevision`](`updateApiProxyRevision`).
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `apigee_organizations_apis_revisions_get_execute()` to send, or `apigee_organizations_apis_revisions_get` for simplest API.

pub fn apigee_organizations_apis_revisions_get_builder(
    client: &SimpleHttpClient,
    name: &str,
    format: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://apigee.googleapis.com/v1/organizations/{}/apis/{}/revisions/{}",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = format {
        query_parts.push(format!("format={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/organizations/{organizationsId}/apis/{apisId}/revisions/{revisionsId}
/// Gets an API proxy revision. To download the API proxy configuration bundle for the specified revision as a zip file, set the format query parameter to bundle. If you are using curl, specify -o filename.zip to save the output to a file; otherwise, it displays to stdout. Then, develop the API proxy configuration locally and upload the updated API proxy configuration revision, as described in [`updateApiProxyRevision`](`updateApiProxyRevision`).
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `apigee_organizations_apis_revisions_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from apigee_organizations_apis_revisions_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_apis_revisions_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleApiHttpBody>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleApiHttpBody = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/organizations/{organizationsId}/apis/{apisId}/revisions/{revisionsId}
/// Gets an API proxy revision. To download the API proxy configuration bundle for the specified revision as a zip file, set the format query parameter to bundle. If you are using curl, specify -o filename.zip to save the output to a file; otherwise, it displays to stdout. Then, develop the API proxy configuration locally and upload the updated API proxy configuration revision, as described in [`updateApiProxyRevision`](`updateApiProxyRevision`).
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `apigee_organizations_apis_revisions_get_builder()` + `apigee_organizations_apis_revisions_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_apis_revisions_get(
    client: &SimpleHttpClient,
    name: &str,
    format: Option<&str>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleApiHttpBody>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = apigee_organizations_apis_revisions_get_builder(client, name, format)?;
    apigee_organizations_apis_revisions_get_execute(builder)
}

/// GET v1/organizations/{organizationsId}/apis/{apisId}/revisions/{revisionsId}
/// Updates an existing API proxy revision by uploading the API proxy configuration bundle as a zip file from your local machine. You can update only API proxy revisions that have never been deployed. After deployment, an API proxy revision becomes immutable, even if it is undeployed. Set the Content-Type header to either `multipart/form-data` or `application/octet-stream`.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `apigee_organizations_apis_revisions_update_api_proxy_revision_execute()` to send, or `apigee_organizations_apis_revisions_update_api_proxy_revision` for simplest API.

pub fn apigee_organizations_apis_revisions_update_api_proxy_revision_builder(
    client: &SimpleHttpClient,
    name: &str,
    validate: Option<bool>,
    body: &GoogleApiHttpBody,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://apigee.googleapis.com/v1/organizations/{}/apis/{}/revisions/{}",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = validate {
        query_parts.push(format!("validate={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/organizations/{organizationsId}/apis/{apisId}/revisions/{revisionsId}
/// Updates an existing API proxy revision by uploading the API proxy configuration bundle as a zip file from your local machine. You can update only API proxy revisions that have never been deployed. After deployment, an API proxy revision becomes immutable, even if it is undeployed. Set the Content-Type header to either `multipart/form-data` or `application/octet-stream`.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `apigee_organizations_apis_revisions_update_api_proxy_revision_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from apigee_organizations_apis_revisions_update_api_proxy_revision_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_apis_revisions_update_api_proxy_revision_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudApigeeV1ApiProxyRevision>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudApigeeV1ApiProxyRevision = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/organizations/{organizationsId}/apis/{apisId}/revisions/{revisionsId}
/// Updates an existing API proxy revision by uploading the API proxy configuration bundle as a zip file from your local machine. You can update only API proxy revisions that have never been deployed. After deployment, an API proxy revision becomes immutable, even if it is undeployed. Set the Content-Type header to either `multipart/form-data` or `application/octet-stream`.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `apigee_organizations_apis_revisions_update_api_proxy_revision_builder()` + `apigee_organizations_apis_revisions_update_api_proxy_revision_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_apis_revisions_update_api_proxy_revision(
    client: &SimpleHttpClient,
    name: &str,
    validate: Option<bool>,
    body: &GoogleApiHttpBody,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudApigeeV1ApiProxyRevision>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = apigee_organizations_apis_revisions_update_api_proxy_revision_builder(
        client, name, validate, body,
    )?;
    apigee_organizations_apis_revisions_update_api_proxy_revision_execute(builder)
}

/// GET v1/organizations/{organizationsId}/apis/{apisId}/revisions/{revisionsId}/deployments
/// Lists all deployments of an API proxy revision.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `apigee_organizations_apis_revisions_deployments_list_execute()` to send, or `apigee_organizations_apis_revisions_deployments_list` for simplest API.

pub fn apigee_organizations_apis_revisions_deployments_list_builder(
    client: &SimpleHttpClient,
    parent: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://apigee.googleapis.com/v1/organizations/{}/apis/{}/revisions/{}/deployments",
        parent,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/organizations/{organizationsId}/apis/{apisId}/revisions/{revisionsId}/deployments
/// Lists all deployments of an API proxy revision.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `apigee_organizations_apis_revisions_deployments_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from apigee_organizations_apis_revisions_deployments_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_apis_revisions_deployments_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudApigeeV1ListDeploymentsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudApigeeV1ListDeploymentsResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/organizations/{organizationsId}/apis/{apisId}/revisions/{revisionsId}/deployments
/// Lists all deployments of an API proxy revision.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `apigee_organizations_apis_revisions_deployments_list_builder()` + `apigee_organizations_apis_revisions_deployments_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_apis_revisions_deployments_list(
    client: &SimpleHttpClient,
    parent: &str,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudApigeeV1ListDeploymentsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = apigee_organizations_apis_revisions_deployments_list_builder(client, parent)?;
    apigee_organizations_apis_revisions_deployments_list_execute(builder)
}

/// GET v1/organizations/{organizationsId}/appgroups
/// Creates an AppGroup. Once created, user can register apps under the AppGroup to obtain secret key and password. At creation time, the AppGroup's state is set as active.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `apigee_organizations_appgroups_create_execute()` to send, or `apigee_organizations_appgroups_create` for simplest API.

pub fn apigee_organizations_appgroups_create_builder(
    client: &SimpleHttpClient,
    parent: &str,
    body: &GoogleCloudApigeeV1AppGroup,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://apigee.googleapis.com/v1/organizations/{}/appgroups",
        parent,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/organizations/{organizationsId}/appgroups
/// Creates an AppGroup. Once created, user can register apps under the AppGroup to obtain secret key and password. At creation time, the AppGroup's state is set as active.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `apigee_organizations_appgroups_create_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from apigee_organizations_appgroups_create_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_appgroups_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudApigeeV1AppGroup>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudApigeeV1AppGroup = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/organizations/{organizationsId}/appgroups
/// Creates an AppGroup. Once created, user can register apps under the AppGroup to obtain secret key and password. At creation time, the AppGroup's state is set as active.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `apigee_organizations_appgroups_create_builder()` + `apigee_organizations_appgroups_create_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_appgroups_create(
    client: &SimpleHttpClient,
    parent: &str,
    body: &GoogleCloudApigeeV1AppGroup,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudApigeeV1AppGroup>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = apigee_organizations_appgroups_create_builder(client, parent, body)?;
    apigee_organizations_appgroups_create_execute(builder)
}

/// GET v1/organizations/{organizationsId}/appgroups/{appgroupsId}
/// Deletes an AppGroup. All app and API keys associations with the AppGroup are also removed. **Warning**: This API will permanently delete the AppGroup and related artifacts. **Note**: The delete operation is asynchronous. The AppGroup is deleted immediately, but its associated resources, such as apps and API keys, may take anywhere from a few seconds to a few minutes to be deleted.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `apigee_organizations_appgroups_delete_execute()` to send, or `apigee_organizations_appgroups_delete` for simplest API.

pub fn apigee_organizations_appgroups_delete_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://apigee.googleapis.com/v1/organizations/{}/appgroups/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/organizations/{organizationsId}/appgroups/{appgroupsId}
/// Deletes an AppGroup. All app and API keys associations with the AppGroup are also removed. **Warning**: This API will permanently delete the AppGroup and related artifacts. **Note**: The delete operation is asynchronous. The AppGroup is deleted immediately, but its associated resources, such as apps and API keys, may take anywhere from a few seconds to a few minutes to be deleted.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `apigee_organizations_appgroups_delete_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from apigee_organizations_appgroups_delete_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_appgroups_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudApigeeV1AppGroup>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudApigeeV1AppGroup = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/organizations/{organizationsId}/appgroups/{appgroupsId}
/// Deletes an AppGroup. All app and API keys associations with the AppGroup are also removed. **Warning**: This API will permanently delete the AppGroup and related artifacts. **Note**: The delete operation is asynchronous. The AppGroup is deleted immediately, but its associated resources, such as apps and API keys, may take anywhere from a few seconds to a few minutes to be deleted.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `apigee_organizations_appgroups_delete_builder()` + `apigee_organizations_appgroups_delete_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_appgroups_delete(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudApigeeV1AppGroup>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = apigee_organizations_appgroups_delete_builder(client, name)?;
    apigee_organizations_appgroups_delete_execute(builder)
}

/// GET v1/organizations/{organizationsId}/appgroups/{appgroupsId}
/// Returns the AppGroup details for the provided AppGroup name in the request URI.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `apigee_organizations_appgroups_get_execute()` to send, or `apigee_organizations_appgroups_get` for simplest API.

pub fn apigee_organizations_appgroups_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://apigee.googleapis.com/v1/organizations/{}/appgroups/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/organizations/{organizationsId}/appgroups/{appgroupsId}
/// Returns the AppGroup details for the provided AppGroup name in the request URI.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `apigee_organizations_appgroups_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from apigee_organizations_appgroups_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_appgroups_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudApigeeV1AppGroup>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudApigeeV1AppGroup = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/organizations/{organizationsId}/appgroups/{appgroupsId}
/// Returns the AppGroup details for the provided AppGroup name in the request URI.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `apigee_organizations_appgroups_get_builder()` + `apigee_organizations_appgroups_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_appgroups_get(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudApigeeV1AppGroup>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = apigee_organizations_appgroups_get_builder(client, name)?;
    apigee_organizations_appgroups_get_execute(builder)
}

/// GET v1/organizations/{organizationsId}/appgroups/{appgroupsId}/balance
/// Gets the account balance for the AppGroup.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `apigee_organizations_appgroups_get_balance_execute()` to send, or `apigee_organizations_appgroups_get_balance` for simplest API.

pub fn apigee_organizations_appgroups_get_balance_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://apigee.googleapis.com/v1/organizations/{}/appgroups/{}/balance",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/organizations/{organizationsId}/appgroups/{appgroupsId}/balance
/// Gets the account balance for the AppGroup.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `apigee_organizations_appgroups_get_balance_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from apigee_organizations_appgroups_get_balance_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_appgroups_get_balance_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudApigeeV1AppGroupBalance>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudApigeeV1AppGroupBalance = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/organizations/{organizationsId}/appgroups/{appgroupsId}/balance
/// Gets the account balance for the AppGroup.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `apigee_organizations_appgroups_get_balance_builder()` + `apigee_organizations_appgroups_get_balance_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_appgroups_get_balance(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudApigeeV1AppGroupBalance>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = apigee_organizations_appgroups_get_balance_builder(client, name)?;
    apigee_organizations_appgroups_get_balance_execute(builder)
}

/// GET v1/organizations/{organizationsId}/appgroups/{appgroupsId}/monetizationConfig
/// Gets the monetization configuration for the AppGroup.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `apigee_organizations_appgroups_get_monetization_config_execute()` to send, or `apigee_organizations_appgroups_get_monetization_config` for simplest API.

pub fn apigee_organizations_appgroups_get_monetization_config_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://apigee.googleapis.com/v1/organizations/{}/appgroups/{}/monetizationConfig",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/organizations/{organizationsId}/appgroups/{appgroupsId}/monetizationConfig
/// Gets the monetization configuration for the AppGroup.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `apigee_organizations_appgroups_get_monetization_config_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from apigee_organizations_appgroups_get_monetization_config_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_appgroups_get_monetization_config_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudApigeeV1AppGroupMonetizationConfig>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudApigeeV1AppGroupMonetizationConfig =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/organizations/{organizationsId}/appgroups/{appgroupsId}/monetizationConfig
/// Gets the monetization configuration for the AppGroup.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `apigee_organizations_appgroups_get_monetization_config_builder()` + `apigee_organizations_appgroups_get_monetization_config_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_appgroups_get_monetization_config(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudApigeeV1AppGroupMonetizationConfig>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = apigee_organizations_appgroups_get_monetization_config_builder(client, name)?;
    apigee_organizations_appgroups_get_monetization_config_execute(builder)
}

/// GET v1/organizations/{organizationsId}/appgroups
/// Lists all AppGroups in an organization. A maximum of 1000 AppGroups are returned in the response if PageSize is not specified, or if the PageSize is greater than 1000.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `apigee_organizations_appgroups_list_execute()` to send, or `apigee_organizations_appgroups_list` for simplest API.

pub fn apigee_organizations_appgroups_list_builder(
    client: &SimpleHttpClient,
    parent: &str,
    filter: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://apigee.googleapis.com/v1/organizations/{}/appgroups",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/organizations/{organizationsId}/appgroups
/// Lists all AppGroups in an organization. A maximum of 1000 AppGroups are returned in the response if PageSize is not specified, or if the PageSize is greater than 1000.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `apigee_organizations_appgroups_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from apigee_organizations_appgroups_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_appgroups_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudApigeeV1ListAppGroupsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudApigeeV1ListAppGroupsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/organizations/{organizationsId}/appgroups
/// Lists all AppGroups in an organization. A maximum of 1000 AppGroups are returned in the response if PageSize is not specified, or if the PageSize is greater than 1000.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `apigee_organizations_appgroups_list_builder()` + `apigee_organizations_appgroups_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_appgroups_list(
    client: &SimpleHttpClient,
    parent: &str,
    filter: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudApigeeV1ListAppGroupsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        apigee_organizations_appgroups_list_builder(client, parent, filter, pageSize, pageToken)?;
    apigee_organizations_appgroups_list_execute(builder)
}

/// GET v1/organizations/{organizationsId}/appgroups/{appgroupsId}
/// Updates an AppGroup. This API replaces the existing AppGroup details with those specified in the request. Include or exclude any existing details that you want to retain or delete, respectively. Note that the state of the AppGroup should be updated using action, and not via AppGroup.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `apigee_organizations_appgroups_update_execute()` to send, or `apigee_organizations_appgroups_update` for simplest API.

pub fn apigee_organizations_appgroups_update_builder(
    client: &SimpleHttpClient,
    name: &str,
    action: Option<&str>,
    body: &GoogleCloudApigeeV1AppGroup,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://apigee.googleapis.com/v1/organizations/{}/appgroups/{}",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = action {
        query_parts.push(format!("action={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/organizations/{organizationsId}/appgroups/{appgroupsId}
/// Updates an AppGroup. This API replaces the existing AppGroup details with those specified in the request. Include or exclude any existing details that you want to retain or delete, respectively. Note that the state of the AppGroup should be updated using action, and not via AppGroup.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `apigee_organizations_appgroups_update_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from apigee_organizations_appgroups_update_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_appgroups_update_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudApigeeV1AppGroup>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudApigeeV1AppGroup = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/organizations/{organizationsId}/appgroups/{appgroupsId}
/// Updates an AppGroup. This API replaces the existing AppGroup details with those specified in the request. Include or exclude any existing details that you want to retain or delete, respectively. Note that the state of the AppGroup should be updated using action, and not via AppGroup.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `apigee_organizations_appgroups_update_builder()` + `apigee_organizations_appgroups_update_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_appgroups_update(
    client: &SimpleHttpClient,
    name: &str,
    action: Option<&str>,
    body: &GoogleCloudApigeeV1AppGroup,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudApigeeV1AppGroup>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = apigee_organizations_appgroups_update_builder(client, name, action, body)?;
    apigee_organizations_appgroups_update_execute(builder)
}

/// GET v1/organizations/{organizationsId}/appgroups/{appgroupsId}/monetizationConfig
/// Updates the monetization configuration for the AppGroup.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `apigee_organizations_appgroups_update_monetization_config_execute()` to send, or `apigee_organizations_appgroups_update_monetization_config` for simplest API.

pub fn apigee_organizations_appgroups_update_monetization_config_builder(
    client: &SimpleHttpClient,
    name: &str,
    body: &GoogleCloudApigeeV1AppGroupMonetizationConfig,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://apigee.googleapis.com/v1/organizations/{}/appgroups/{}/monetizationConfig",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/organizations/{organizationsId}/appgroups/{appgroupsId}/monetizationConfig
/// Updates the monetization configuration for the AppGroup.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `apigee_organizations_appgroups_update_monetization_config_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from apigee_organizations_appgroups_update_monetization_config_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_appgroups_update_monetization_config_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudApigeeV1AppGroupMonetizationConfig>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudApigeeV1AppGroupMonetizationConfig =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/organizations/{organizationsId}/appgroups/{appgroupsId}/monetizationConfig
/// Updates the monetization configuration for the AppGroup.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `apigee_organizations_appgroups_update_monetization_config_builder()` + `apigee_organizations_appgroups_update_monetization_config_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_appgroups_update_monetization_config(
    client: &SimpleHttpClient,
    name: &str,
    body: &GoogleCloudApigeeV1AppGroupMonetizationConfig,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudApigeeV1AppGroupMonetizationConfig>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        apigee_organizations_appgroups_update_monetization_config_builder(client, name, body)?;
    apigee_organizations_appgroups_update_monetization_config_execute(builder)
}

/// GET v1/organizations/{organizationsId}/appgroups/{appgroupsId}/apps
/// Creates an app and associates it with an AppGroup. This API associates the AppGroup app with the specified API product and auto-generates an API key for the app to use in calls to API proxies inside that API product. The name is the unique ID of the app that you can use in API calls.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `apigee_organizations_appgroups_apps_create_execute()` to send, or `apigee_organizations_appgroups_apps_create` for simplest API.

pub fn apigee_organizations_appgroups_apps_create_builder(
    client: &SimpleHttpClient,
    parent: &str,
    body: &GoogleCloudApigeeV1AppGroupApp,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://apigee.googleapis.com/v1/organizations/{}/appgroups/{}/apps",
        parent,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/organizations/{organizationsId}/appgroups/{appgroupsId}/apps
/// Creates an app and associates it with an AppGroup. This API associates the AppGroup app with the specified API product and auto-generates an API key for the app to use in calls to API proxies inside that API product. The name is the unique ID of the app that you can use in API calls.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `apigee_organizations_appgroups_apps_create_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from apigee_organizations_appgroups_apps_create_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_appgroups_apps_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudApigeeV1AppGroupApp>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudApigeeV1AppGroupApp = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/organizations/{organizationsId}/appgroups/{appgroupsId}/apps
/// Creates an app and associates it with an AppGroup. This API associates the AppGroup app with the specified API product and auto-generates an API key for the app to use in calls to API proxies inside that API product. The name is the unique ID of the app that you can use in API calls.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `apigee_organizations_appgroups_apps_create_builder()` + `apigee_organizations_appgroups_apps_create_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_appgroups_apps_create(
    client: &SimpleHttpClient,
    parent: &str,
    body: &GoogleCloudApigeeV1AppGroupApp,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudApigeeV1AppGroupApp>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = apigee_organizations_appgroups_apps_create_builder(client, parent, body)?;
    apigee_organizations_appgroups_apps_create_execute(builder)
}

/// GET v1/organizations/{organizationsId}/appgroups/{appgroupsId}/apps/{appsId}
/// Deletes an AppGroup app. **Note**: The delete operation is asynchronous. The AppGroup app is deleted immediately, but its associated resources, such as app keys or access tokens, may take anywhere from a few seconds to a few minutes to be deleted.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `apigee_organizations_appgroups_apps_delete_execute()` to send, or `apigee_organizations_appgroups_apps_delete` for simplest API.

pub fn apigee_organizations_appgroups_apps_delete_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://apigee.googleapis.com/v1/organizations/{}/appgroups/{}/apps/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/organizations/{organizationsId}/appgroups/{appgroupsId}/apps/{appsId}
/// Deletes an AppGroup app. **Note**: The delete operation is asynchronous. The AppGroup app is deleted immediately, but its associated resources, such as app keys or access tokens, may take anywhere from a few seconds to a few minutes to be deleted.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `apigee_organizations_appgroups_apps_delete_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from apigee_organizations_appgroups_apps_delete_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_appgroups_apps_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudApigeeV1AppGroupApp>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudApigeeV1AppGroupApp = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/organizations/{organizationsId}/appgroups/{appgroupsId}/apps/{appsId}
/// Deletes an AppGroup app. **Note**: The delete operation is asynchronous. The AppGroup app is deleted immediately, but its associated resources, such as app keys or access tokens, may take anywhere from a few seconds to a few minutes to be deleted.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `apigee_organizations_appgroups_apps_delete_builder()` + `apigee_organizations_appgroups_apps_delete_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_appgroups_apps_delete(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudApigeeV1AppGroupApp>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = apigee_organizations_appgroups_apps_delete_builder(client, name)?;
    apigee_organizations_appgroups_apps_delete_execute(builder)
}

/// GET v1/organizations/{organizationsId}/appgroups/{appgroupsId}/apps/{appsId}
/// Returns the details for an AppGroup app.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `apigee_organizations_appgroups_apps_get_execute()` to send, or `apigee_organizations_appgroups_apps_get` for simplest API.

pub fn apigee_organizations_appgroups_apps_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://apigee.googleapis.com/v1/organizations/{}/appgroups/{}/apps/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/organizations/{organizationsId}/appgroups/{appgroupsId}/apps/{appsId}
/// Returns the details for an AppGroup app.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `apigee_organizations_appgroups_apps_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from apigee_organizations_appgroups_apps_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_appgroups_apps_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudApigeeV1AppGroupApp>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudApigeeV1AppGroupApp = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/organizations/{organizationsId}/appgroups/{appgroupsId}/apps/{appsId}
/// Returns the details for an AppGroup app.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `apigee_organizations_appgroups_apps_get_builder()` + `apigee_organizations_appgroups_apps_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_appgroups_apps_get(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudApigeeV1AppGroupApp>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = apigee_organizations_appgroups_apps_get_builder(client, name)?;
    apigee_organizations_appgroups_apps_get_execute(builder)
}

/// GET v1/organizations/{organizationsId}/appgroups/{appgroupsId}/apps
/// Lists all apps created by an AppGroup in an Apigee organization. Optionally, you can request an expanded view of the AppGroup apps. Lists all AppGroupApps in an AppGroup. A maximum of 1000 AppGroup apps are returned in the response if PageSize is not specified, or if the PageSize is greater than 1000.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `apigee_organizations_appgroups_apps_list_execute()` to send, or `apigee_organizations_appgroups_apps_list` for simplest API.

pub fn apigee_organizations_appgroups_apps_list_builder(
    client: &SimpleHttpClient,
    parent: &str,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://apigee.googleapis.com/v1/organizations/{}/appgroups/{}/apps",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/organizations/{organizationsId}/appgroups/{appgroupsId}/apps
/// Lists all apps created by an AppGroup in an Apigee organization. Optionally, you can request an expanded view of the AppGroup apps. Lists all AppGroupApps in an AppGroup. A maximum of 1000 AppGroup apps are returned in the response if PageSize is not specified, or if the PageSize is greater than 1000.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `apigee_organizations_appgroups_apps_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from apigee_organizations_appgroups_apps_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_appgroups_apps_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudApigeeV1ListAppGroupAppsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudApigeeV1ListAppGroupAppsResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/organizations/{organizationsId}/appgroups/{appgroupsId}/apps
/// Lists all apps created by an AppGroup in an Apigee organization. Optionally, you can request an expanded view of the AppGroup apps. Lists all AppGroupApps in an AppGroup. A maximum of 1000 AppGroup apps are returned in the response if PageSize is not specified, or if the PageSize is greater than 1000.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `apigee_organizations_appgroups_apps_list_builder()` + `apigee_organizations_appgroups_apps_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_appgroups_apps_list(
    client: &SimpleHttpClient,
    parent: &str,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudApigeeV1ListAppGroupAppsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        apigee_organizations_appgroups_apps_list_builder(client, parent, pageSize, pageToken)?;
    apigee_organizations_appgroups_apps_list_execute(builder)
}

/// GET v1/organizations/{organizationsId}/appgroups/{appgroupsId}/apps/{appsId}
/// Updates the details for an AppGroup app. In addition, you can add an API product to an AppGroup app and automatically generate an API key for the app to use when calling APIs in the API product. If you want to use an existing API key for the API product, add the API product to the API key using the UpdateAppGroupAppKey API. Using this API, you cannot update the app name, as it is the primary key used to identify the app and cannot be changed. This API replaces the existing attributes with those specified in the request. Include or exclude any existing attributes that you want to retain or delete, respectively.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `apigee_organizations_appgroups_apps_update_execute()` to send, or `apigee_organizations_appgroups_apps_update` for simplest API.

pub fn apigee_organizations_appgroups_apps_update_builder(
    client: &SimpleHttpClient,
    name: &str,
    action: Option<&str>,
    body: &GoogleCloudApigeeV1AppGroupApp,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://apigee.googleapis.com/v1/organizations/{}/appgroups/{}/apps/{}",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = action {
        query_parts.push(format!("action={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/organizations/{organizationsId}/appgroups/{appgroupsId}/apps/{appsId}
/// Updates the details for an AppGroup app. In addition, you can add an API product to an AppGroup app and automatically generate an API key for the app to use when calling APIs in the API product. If you want to use an existing API key for the API product, add the API product to the API key using the UpdateAppGroupAppKey API. Using this API, you cannot update the app name, as it is the primary key used to identify the app and cannot be changed. This API replaces the existing attributes with those specified in the request. Include or exclude any existing attributes that you want to retain or delete, respectively.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `apigee_organizations_appgroups_apps_update_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from apigee_organizations_appgroups_apps_update_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_appgroups_apps_update_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudApigeeV1AppGroupApp>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudApigeeV1AppGroupApp = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/organizations/{organizationsId}/appgroups/{appgroupsId}/apps/{appsId}
/// Updates the details for an AppGroup app. In addition, you can add an API product to an AppGroup app and automatically generate an API key for the app to use when calling APIs in the API product. If you want to use an existing API key for the API product, add the API product to the API key using the UpdateAppGroupAppKey API. Using this API, you cannot update the app name, as it is the primary key used to identify the app and cannot be changed. This API replaces the existing attributes with those specified in the request. Include or exclude any existing attributes that you want to retain or delete, respectively.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `apigee_organizations_appgroups_apps_update_builder()` + `apigee_organizations_appgroups_apps_update_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_appgroups_apps_update(
    client: &SimpleHttpClient,
    name: &str,
    action: Option<&str>,
    body: &GoogleCloudApigeeV1AppGroupApp,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudApigeeV1AppGroupApp>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = apigee_organizations_appgroups_apps_update_builder(client, name, action, body)?;
    apigee_organizations_appgroups_apps_update_execute(builder)
}

/// GET v1/organizations/{organizationsId}/appgroups/{appgroupsId}/apps/{appsId}/keys
/// Creates a custom consumer key and secret for a AppGroup app. This is particularly useful if you want to migrate existing consumer keys and secrets to Apigee from another system. Consumer keys and secrets can contain letters, numbers, underscores, and hyphens. No other special characters are allowed. To avoid service disruptions, a consumer key and secret should not exceed 2 KBs each. **Note**: When creating the consumer key and secret, an association to API products will not be made. Therefore, you should not specify the associated API products in your request. Instead, use the UpdateAppGroupAppKey API to make the association after the consumer key and secret are created. If a consumer key and secret already exist, you can keep them or delete them using the DeleteAppGroupAppKey API.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `apigee_organizations_appgroups_apps_keys_create_execute()` to send, or `apigee_organizations_appgroups_apps_keys_create` for simplest API.

pub fn apigee_organizations_appgroups_apps_keys_create_builder(
    client: &SimpleHttpClient,
    parent: &str,
    body: &GoogleCloudApigeeV1AppGroupAppKey,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://apigee.googleapis.com/v1/organizations/{}/appgroups/{}/apps/{}/keys",
        parent,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/organizations/{organizationsId}/appgroups/{appgroupsId}/apps/{appsId}/keys
/// Creates a custom consumer key and secret for a AppGroup app. This is particularly useful if you want to migrate existing consumer keys and secrets to Apigee from another system. Consumer keys and secrets can contain letters, numbers, underscores, and hyphens. No other special characters are allowed. To avoid service disruptions, a consumer key and secret should not exceed 2 KBs each. **Note**: When creating the consumer key and secret, an association to API products will not be made. Therefore, you should not specify the associated API products in your request. Instead, use the UpdateAppGroupAppKey API to make the association after the consumer key and secret are created. If a consumer key and secret already exist, you can keep them or delete them using the DeleteAppGroupAppKey API.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `apigee_organizations_appgroups_apps_keys_create_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from apigee_organizations_appgroups_apps_keys_create_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_appgroups_apps_keys_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudApigeeV1AppGroupAppKey>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudApigeeV1AppGroupAppKey = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/organizations/{organizationsId}/appgroups/{appgroupsId}/apps/{appsId}/keys
/// Creates a custom consumer key and secret for a AppGroup app. This is particularly useful if you want to migrate existing consumer keys and secrets to Apigee from another system. Consumer keys and secrets can contain letters, numbers, underscores, and hyphens. No other special characters are allowed. To avoid service disruptions, a consumer key and secret should not exceed 2 KBs each. **Note**: When creating the consumer key and secret, an association to API products will not be made. Therefore, you should not specify the associated API products in your request. Instead, use the UpdateAppGroupAppKey API to make the association after the consumer key and secret are created. If a consumer key and secret already exist, you can keep them or delete them using the DeleteAppGroupAppKey API.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `apigee_organizations_appgroups_apps_keys_create_builder()` + `apigee_organizations_appgroups_apps_keys_create_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_appgroups_apps_keys_create(
    client: &SimpleHttpClient,
    parent: &str,
    body: &GoogleCloudApigeeV1AppGroupAppKey,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudApigeeV1AppGroupAppKey>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = apigee_organizations_appgroups_apps_keys_create_builder(client, parent, body)?;
    apigee_organizations_appgroups_apps_keys_create_execute(builder)
}

/// GET v1/organizations/{organizationsId}/appgroups/{appgroupsId}/apps/{appsId}/keys/{keysId}
/// Deletes an app's consumer key and removes all API products associated with the app. After the consumer key is deleted, it cannot be used to access any APIs.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `apigee_organizations_appgroups_apps_keys_delete_execute()` to send, or `apigee_organizations_appgroups_apps_keys_delete` for simplest API.

pub fn apigee_organizations_appgroups_apps_keys_delete_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://apigee.googleapis.com/v1/organizations/{}/appgroups/{}/apps/{}/keys/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/organizations/{organizationsId}/appgroups/{appgroupsId}/apps/{appsId}/keys/{keysId}
/// Deletes an app's consumer key and removes all API products associated with the app. After the consumer key is deleted, it cannot be used to access any APIs.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `apigee_organizations_appgroups_apps_keys_delete_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from apigee_organizations_appgroups_apps_keys_delete_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_appgroups_apps_keys_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudApigeeV1AppGroupAppKey>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudApigeeV1AppGroupAppKey = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/organizations/{organizationsId}/appgroups/{appgroupsId}/apps/{appsId}/keys/{keysId}
/// Deletes an app's consumer key and removes all API products associated with the app. After the consumer key is deleted, it cannot be used to access any APIs.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `apigee_organizations_appgroups_apps_keys_delete_builder()` + `apigee_organizations_appgroups_apps_keys_delete_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_appgroups_apps_keys_delete(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudApigeeV1AppGroupAppKey>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = apigee_organizations_appgroups_apps_keys_delete_builder(client, name)?;
    apigee_organizations_appgroups_apps_keys_delete_execute(builder)
}

/// GET v1/organizations/{organizationsId}/appgroups/{appgroupsId}/apps/{appsId}/keys/{keysId}
/// Gets details for a consumer key for a AppGroup app, including the key and secret value, associated API products, and other information.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `apigee_organizations_appgroups_apps_keys_get_execute()` to send, or `apigee_organizations_appgroups_apps_keys_get` for simplest API.

pub fn apigee_organizations_appgroups_apps_keys_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://apigee.googleapis.com/v1/organizations/{}/appgroups/{}/apps/{}/keys/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/organizations/{organizationsId}/appgroups/{appgroupsId}/apps/{appsId}/keys/{keysId}
/// Gets details for a consumer key for a AppGroup app, including the key and secret value, associated API products, and other information.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `apigee_organizations_appgroups_apps_keys_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from apigee_organizations_appgroups_apps_keys_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_appgroups_apps_keys_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudApigeeV1AppGroupAppKey>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudApigeeV1AppGroupAppKey = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/organizations/{organizationsId}/appgroups/{appgroupsId}/apps/{appsId}/keys/{keysId}
/// Gets details for a consumer key for a AppGroup app, including the key and secret value, associated API products, and other information.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `apigee_organizations_appgroups_apps_keys_get_builder()` + `apigee_organizations_appgroups_apps_keys_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_appgroups_apps_keys_get(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudApigeeV1AppGroupAppKey>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = apigee_organizations_appgroups_apps_keys_get_builder(client, name)?;
    apigee_organizations_appgroups_apps_keys_get_execute(builder)
}

/// GET v1/organizations/{organizationsId}/appgroups/{appgroupsId}/apps/{appsId}/keys/{keysId}
/// Adds an API product to an AppGroupAppKey, enabling the app that holds the key to access the API resources bundled in the API product. In addition, you can add attributes and scopes to the AppGroupAppKey. This API replaces the existing attributes with those specified in the request. Include or exclude any existing attributes that you want to retain or delete, respectively. You can use the same key to access all API products associated with the app.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `apigee_organizations_appgroups_apps_keys_update_app_group_app_key_execute()` to send, or `apigee_organizations_appgroups_apps_keys_update_app_group_app_key` for simplest API.

pub fn apigee_organizations_appgroups_apps_keys_update_app_group_app_key_builder(
    client: &SimpleHttpClient,
    name: &str,
    body: &GoogleCloudApigeeV1UpdateAppGroupAppKeyRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://apigee.googleapis.com/v1/organizations/{}/appgroups/{}/apps/{}/keys/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/organizations/{organizationsId}/appgroups/{appgroupsId}/apps/{appsId}/keys/{keysId}
/// Adds an API product to an AppGroupAppKey, enabling the app that holds the key to access the API resources bundled in the API product. In addition, you can add attributes and scopes to the AppGroupAppKey. This API replaces the existing attributes with those specified in the request. Include or exclude any existing attributes that you want to retain or delete, respectively. You can use the same key to access all API products associated with the app.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `apigee_organizations_appgroups_apps_keys_update_app_group_app_key_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from apigee_organizations_appgroups_apps_keys_update_app_group_app_key_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_appgroups_apps_keys_update_app_group_app_key_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudApigeeV1AppGroupAppKey>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudApigeeV1AppGroupAppKey = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/organizations/{organizationsId}/appgroups/{appgroupsId}/apps/{appsId}/keys/{keysId}
/// Adds an API product to an AppGroupAppKey, enabling the app that holds the key to access the API resources bundled in the API product. In addition, you can add attributes and scopes to the AppGroupAppKey. This API replaces the existing attributes with those specified in the request. Include or exclude any existing attributes that you want to retain or delete, respectively. You can use the same key to access all API products associated with the app.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `apigee_organizations_appgroups_apps_keys_update_app_group_app_key_builder()` + `apigee_organizations_appgroups_apps_keys_update_app_group_app_key_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_appgroups_apps_keys_update_app_group_app_key(
    client: &SimpleHttpClient,
    name: &str,
    body: &GoogleCloudApigeeV1UpdateAppGroupAppKeyRequest,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudApigeeV1AppGroupAppKey>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = apigee_organizations_appgroups_apps_keys_update_app_group_app_key_builder(
        client, name, body,
    )?;
    apigee_organizations_appgroups_apps_keys_update_app_group_app_key_execute(builder)
}

/// GET v1/organizations/{organizationsId}/appgroups/{appgroupsId}/apps/{appsId}/keys/{keysId}/apiproducts/{apiproductsId}
/// Removes an API product from an app's consumer key. After the API product is removed, the app cannot access the API resources defined in that API product. **Note**: The consumer key is not removed, only its association with the API product.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `apigee_organizations_appgroups_apps_keys_apiproducts_delete_execute()` to send, or `apigee_organizations_appgroups_apps_keys_apiproducts_delete` for simplest API.

pub fn apigee_organizations_appgroups_apps_keys_apiproducts_delete_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://apigee.googleapis.com/v1/organizations/{}/appgroups/{}/apps/{}/keys/{}/apiproducts/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/organizations/{organizationsId}/appgroups/{appgroupsId}/apps/{appsId}/keys/{keysId}/apiproducts/{apiproductsId}
/// Removes an API product from an app's consumer key. After the API product is removed, the app cannot access the API resources defined in that API product. **Note**: The consumer key is not removed, only its association with the API product.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `apigee_organizations_appgroups_apps_keys_apiproducts_delete_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from apigee_organizations_appgroups_apps_keys_apiproducts_delete_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_appgroups_apps_keys_apiproducts_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudApigeeV1AppGroupAppKey>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudApigeeV1AppGroupAppKey = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/organizations/{organizationsId}/appgroups/{appgroupsId}/apps/{appsId}/keys/{keysId}/apiproducts/{apiproductsId}
/// Removes an API product from an app's consumer key. After the API product is removed, the app cannot access the API resources defined in that API product. **Note**: The consumer key is not removed, only its association with the API product.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `apigee_organizations_appgroups_apps_keys_apiproducts_delete_builder()` + `apigee_organizations_appgroups_apps_keys_apiproducts_delete_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_appgroups_apps_keys_apiproducts_delete(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudApigeeV1AppGroupAppKey>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        apigee_organizations_appgroups_apps_keys_apiproducts_delete_builder(client, name)?;
    apigee_organizations_appgroups_apps_keys_apiproducts_delete_execute(builder)
}

/// GET v1/organizations/{organizationsId}/appgroups/{appgroupsId}/apps/{appsId}/keys/{keysId}/apiproducts/{apiproductsId}
/// Approves or revokes the consumer key for an API product. After a consumer key is approved, the app can use it to access APIs. A consumer key that is revoked or pending cannot be used to access an API. Any access tokens associated with a revoked consumer key will remain active. However, Apigee checks the status of the consumer key and if set to revoked will not allow access to the API.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `apigee_organizations_appgroups_apps_keys_apiproducts_update_app_group_app_key_api_product_execute()` to send, or `apigee_organizations_appgroups_apps_keys_apiproducts_update_app_group_app_key_api_product` for simplest API.

pub fn apigee_organizations_appgroups_apps_keys_apiproducts_update_app_group_app_key_api_product_builder(
    client: &SimpleHttpClient,
    name: &str,
    action: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://apigee.googleapis.com/v1/organizations/{}/appgroups/{}/apps/{}/keys/{}/apiproducts/{}",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = action {
        query_parts.push(format!("action={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/organizations/{organizationsId}/appgroups/{appgroupsId}/apps/{appsId}/keys/{keysId}/apiproducts/{apiproductsId}
/// Approves or revokes the consumer key for an API product. After a consumer key is approved, the app can use it to access APIs. A consumer key that is revoked or pending cannot be used to access an API. Any access tokens associated with a revoked consumer key will remain active. However, Apigee checks the status of the consumer key and if set to revoked will not allow access to the API.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `apigee_organizations_appgroups_apps_keys_apiproducts_update_app_group_app_key_api_product_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from apigee_organizations_appgroups_apps_keys_apiproducts_update_app_group_app_key_api_product_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_appgroups_apps_keys_apiproducts_update_app_group_app_key_api_product_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleProtobufEmpty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/organizations/{organizationsId}/appgroups/{appgroupsId}/apps/{appsId}/keys/{keysId}/apiproducts/{apiproductsId}
/// Approves or revokes the consumer key for an API product. After a consumer key is approved, the app can use it to access APIs. A consumer key that is revoked or pending cannot be used to access an API. Any access tokens associated with a revoked consumer key will remain active. However, Apigee checks the status of the consumer key and if set to revoked will not allow access to the API.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `apigee_organizations_appgroups_apps_keys_apiproducts_update_app_group_app_key_api_product_builder()` + `apigee_organizations_appgroups_apps_keys_apiproducts_update_app_group_app_key_api_product_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_appgroups_apps_keys_apiproducts_update_app_group_app_key_api_product(
    client: &SimpleHttpClient,
    name: &str,
    action: Option<&str>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = apigee_organizations_appgroups_apps_keys_apiproducts_update_app_group_app_key_api_product_builder(client, name, action)?;
    apigee_organizations_appgroups_apps_keys_apiproducts_update_app_group_app_key_api_product_execute(builder)
}

/// GET v1/organizations/{organizationsId}/appgroups/{appgroupsId}/balance:adjust
/// Adjust the prepaid balance for the AppGroup. This API will be used in scenarios where the AppGroup has been under-charged or over-charged.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `apigee_organizations_appgroups_balance_adjust_execute()` to send, or `apigee_organizations_appgroups_balance_adjust` for simplest API.

pub fn apigee_organizations_appgroups_balance_adjust_builder(
    client: &SimpleHttpClient,
    name: &str,
    body: &GoogleCloudApigeeV1AdjustAppGroupBalanceRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://apigee.googleapis.com/v1/organizations/{}/appgroups/{}/balance:adjust",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/organizations/{organizationsId}/appgroups/{appgroupsId}/balance:adjust
/// Adjust the prepaid balance for the AppGroup. This API will be used in scenarios where the AppGroup has been under-charged or over-charged.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `apigee_organizations_appgroups_balance_adjust_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from apigee_organizations_appgroups_balance_adjust_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_appgroups_balance_adjust_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudApigeeV1AppGroupBalance>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudApigeeV1AppGroupBalance = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/organizations/{organizationsId}/appgroups/{appgroupsId}/balance:adjust
/// Adjust the prepaid balance for the AppGroup. This API will be used in scenarios where the AppGroup has been under-charged or over-charged.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `apigee_organizations_appgroups_balance_adjust_builder()` + `apigee_organizations_appgroups_balance_adjust_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_appgroups_balance_adjust(
    client: &SimpleHttpClient,
    name: &str,
    body: &GoogleCloudApigeeV1AdjustAppGroupBalanceRequest,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudApigeeV1AppGroupBalance>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = apigee_organizations_appgroups_balance_adjust_builder(client, name, body)?;
    apigee_organizations_appgroups_balance_adjust_execute(builder)
}

/// GET v1/organizations/{organizationsId}/appgroups/{appgroupsId}/balance:credit
/// Credits the account balance for the AppGroup.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `apigee_organizations_appgroups_balance_credit_execute()` to send, or `apigee_organizations_appgroups_balance_credit` for simplest API.

pub fn apigee_organizations_appgroups_balance_credit_builder(
    client: &SimpleHttpClient,
    name: &str,
    body: &GoogleCloudApigeeV1CreditAppGroupBalanceRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://apigee.googleapis.com/v1/organizations/{}/appgroups/{}/balance:credit",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/organizations/{organizationsId}/appgroups/{appgroupsId}/balance:credit
/// Credits the account balance for the AppGroup.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `apigee_organizations_appgroups_balance_credit_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from apigee_organizations_appgroups_balance_credit_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_appgroups_balance_credit_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudApigeeV1AppGroupBalance>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudApigeeV1AppGroupBalance = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/organizations/{organizationsId}/appgroups/{appgroupsId}/balance:credit
/// Credits the account balance for the AppGroup.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `apigee_organizations_appgroups_balance_credit_builder()` + `apigee_organizations_appgroups_balance_credit_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_appgroups_balance_credit(
    client: &SimpleHttpClient,
    name: &str,
    body: &GoogleCloudApigeeV1CreditAppGroupBalanceRequest,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudApigeeV1AppGroupBalance>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = apigee_organizations_appgroups_balance_credit_builder(client, name, body)?;
    apigee_organizations_appgroups_balance_credit_execute(builder)
}

/// GET v1/organizations/{organizationsId}/appgroups/{appgroupsId}/subscriptions
/// Creates a subscription to an API product.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `apigee_organizations_appgroups_subscriptions_create_execute()` to send, or `apigee_organizations_appgroups_subscriptions_create` for simplest API.

pub fn apigee_organizations_appgroups_subscriptions_create_builder(
    client: &SimpleHttpClient,
    parent: &str,
    body: &GoogleCloudApigeeV1AppGroupSubscription,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://apigee.googleapis.com/v1/organizations/{}/appgroups/{}/subscriptions",
        parent,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/organizations/{organizationsId}/appgroups/{appgroupsId}/subscriptions
/// Creates a subscription to an API product.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `apigee_organizations_appgroups_subscriptions_create_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from apigee_organizations_appgroups_subscriptions_create_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_appgroups_subscriptions_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudApigeeV1AppGroupSubscription>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudApigeeV1AppGroupSubscription =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/organizations/{organizationsId}/appgroups/{appgroupsId}/subscriptions
/// Creates a subscription to an API product.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `apigee_organizations_appgroups_subscriptions_create_builder()` + `apigee_organizations_appgroups_subscriptions_create_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_appgroups_subscriptions_create(
    client: &SimpleHttpClient,
    parent: &str,
    body: &GoogleCloudApigeeV1AppGroupSubscription,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudApigeeV1AppGroupSubscription>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        apigee_organizations_appgroups_subscriptions_create_builder(client, parent, body)?;
    apigee_organizations_appgroups_subscriptions_create_execute(builder)
}

/// GET v1/organizations/{organizationsId}/appgroups/{appgroupsId}/subscriptions/{subscriptionsId}:expire
/// Expires an API product subscription immediately.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `apigee_organizations_appgroups_subscriptions_expire_execute()` to send, or `apigee_organizations_appgroups_subscriptions_expire` for simplest API.

pub fn apigee_organizations_appgroups_subscriptions_expire_builder(
    client: &SimpleHttpClient,
    name: &str,
    body: &GoogleCloudApigeeV1ExpireAppGroupSubscriptionRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://apigee.googleapis.com/v1/organizations/{}/appgroups/{}/subscriptions/{}:expire",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/organizations/{organizationsId}/appgroups/{appgroupsId}/subscriptions/{subscriptionsId}:expire
/// Expires an API product subscription immediately.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `apigee_organizations_appgroups_subscriptions_expire_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from apigee_organizations_appgroups_subscriptions_expire_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_appgroups_subscriptions_expire_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudApigeeV1AppGroupSubscription>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudApigeeV1AppGroupSubscription =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/organizations/{organizationsId}/appgroups/{appgroupsId}/subscriptions/{subscriptionsId}:expire
/// Expires an API product subscription immediately.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `apigee_organizations_appgroups_subscriptions_expire_builder()` + `apigee_organizations_appgroups_subscriptions_expire_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_appgroups_subscriptions_expire(
    client: &SimpleHttpClient,
    name: &str,
    body: &GoogleCloudApigeeV1ExpireAppGroupSubscriptionRequest,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudApigeeV1AppGroupSubscription>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = apigee_organizations_appgroups_subscriptions_expire_builder(client, name, body)?;
    apigee_organizations_appgroups_subscriptions_expire_execute(builder)
}

/// GET v1/organizations/{organizationsId}/appgroups/{appgroupsId}/subscriptions/{subscriptionsId}
/// Get an api product subscription for an appgroup.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `apigee_organizations_appgroups_subscriptions_get_execute()` to send, or `apigee_organizations_appgroups_subscriptions_get` for simplest API.

pub fn apigee_organizations_appgroups_subscriptions_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://apigee.googleapis.com/v1/organizations/{}/appgroups/{}/subscriptions/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/organizations/{organizationsId}/appgroups/{appgroupsId}/subscriptions/{subscriptionsId}
/// Get an api product subscription for an appgroup.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `apigee_organizations_appgroups_subscriptions_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from apigee_organizations_appgroups_subscriptions_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_appgroups_subscriptions_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudApigeeV1AppGroupSubscription>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudApigeeV1AppGroupSubscription =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/organizations/{organizationsId}/appgroups/{appgroupsId}/subscriptions/{subscriptionsId}
/// Get an api product subscription for an appgroup.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `apigee_organizations_appgroups_subscriptions_get_builder()` + `apigee_organizations_appgroups_subscriptions_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_appgroups_subscriptions_get(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudApigeeV1AppGroupSubscription>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = apigee_organizations_appgroups_subscriptions_get_builder(client, name)?;
    apigee_organizations_appgroups_subscriptions_get_execute(builder)
}

/// GET v1/organizations/{organizationsId}/appgroups/{appgroupsId}/subscriptions
/// List all api product subscriptions for an appgroup.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `apigee_organizations_appgroups_subscriptions_list_execute()` to send, or `apigee_organizations_appgroups_subscriptions_list` for simplest API.

pub fn apigee_organizations_appgroups_subscriptions_list_builder(
    client: &SimpleHttpClient,
    parent: &str,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://apigee.googleapis.com/v1/organizations/{}/appgroups/{}/subscriptions",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/organizations/{organizationsId}/appgroups/{appgroupsId}/subscriptions
/// List all api product subscriptions for an appgroup.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `apigee_organizations_appgroups_subscriptions_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from apigee_organizations_appgroups_subscriptions_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_appgroups_subscriptions_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudApigeeV1ListAppGroupSubscriptionsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudApigeeV1ListAppGroupSubscriptionsResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/organizations/{organizationsId}/appgroups/{appgroupsId}/subscriptions
/// List all api product subscriptions for an appgroup.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `apigee_organizations_appgroups_subscriptions_list_builder()` + `apigee_organizations_appgroups_subscriptions_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_appgroups_subscriptions_list(
    client: &SimpleHttpClient,
    parent: &str,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudApigeeV1ListAppGroupSubscriptionsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = apigee_organizations_appgroups_subscriptions_list_builder(
        client, parent, pageSize, pageToken,
    )?;
    apigee_organizations_appgroups_subscriptions_list_execute(builder)
}

/// GET v1/organizations/{organizationsId}/apps/{appsId}
/// Gets the app profile for the specified app ID.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `apigee_organizations_apps_get_execute()` to send, or `apigee_organizations_apps_get` for simplest API.

pub fn apigee_organizations_apps_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://apigee.googleapis.com/v1/organizations/{}/apps/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/organizations/{organizationsId}/apps/{appsId}
/// Gets the app profile for the specified app ID.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `apigee_organizations_apps_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from apigee_organizations_apps_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_apps_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleCloudApigeeV1App>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudApigeeV1App = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/organizations/{organizationsId}/apps/{appsId}
/// Gets the app profile for the specified app ID.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `apigee_organizations_apps_get_builder()` + `apigee_organizations_apps_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_apps_get(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleCloudApigeeV1App>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = apigee_organizations_apps_get_builder(client, name)?;
    apigee_organizations_apps_get_execute(builder)
}

/// GET v1/organizations/{organizationsId}/apps
/// Lists IDs of apps within an organization that have the specified app status (approved or revoked) or are of the specified app type (developer or company).
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `apigee_organizations_apps_list_execute()` to send, or `apigee_organizations_apps_list` for simplest API.

pub fn apigee_organizations_apps_list_builder(
    client: &SimpleHttpClient,
    parent: &str,
    apiProduct: Option<&str>,
    apptype: Option<&str>,
    expand: Option<bool>,
    filter: Option<&str>,
    ids: Option<&str>,
    includeCred: Option<bool>,
    keyStatus: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
    rows: Option<&str>,
    startKey: Option<&str>,
    status: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://apigee.googleapis.com/v1/organizations/{}/apps",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = apiProduct {
        query_parts.push(format!("apiProduct={}", val));
    }
    if let Some(val) = apptype {
        query_parts.push(format!("apptype={}", val));
    }
    if let Some(val) = expand {
        query_parts.push(format!("expand={}", val));
    }
    if let Some(val) = filter {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = ids {
        query_parts.push(format!("ids={}", val));
    }
    if let Some(val) = includeCred {
        query_parts.push(format!("includeCred={}", val));
    }
    if let Some(val) = keyStatus {
        query_parts.push(format!("keyStatus={}", val));
    }
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = rows {
        query_parts.push(format!("rows={}", val));
    }
    if let Some(val) = startKey {
        query_parts.push(format!("startKey={}", val));
    }
    if let Some(val) = status {
        query_parts.push(format!("status={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/organizations/{organizationsId}/apps
/// Lists IDs of apps within an organization that have the specified app status (approved or revoked) or are of the specified app type (developer or company).
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `apigee_organizations_apps_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from apigee_organizations_apps_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_apps_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudApigeeV1ListAppsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudApigeeV1ListAppsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/organizations/{organizationsId}/apps
/// Lists IDs of apps within an organization that have the specified app status (approved or revoked) or are of the specified app type (developer or company).
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `apigee_organizations_apps_list_builder()` + `apigee_organizations_apps_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_apps_list(
    client: &SimpleHttpClient,
    parent: &str,
    apiProduct: Option<&str>,
    apptype: Option<&str>,
    expand: Option<bool>,
    filter: Option<&str>,
    ids: Option<&str>,
    includeCred: Option<bool>,
    keyStatus: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
    rows: Option<&str>,
    startKey: Option<&str>,
    status: Option<&str>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudApigeeV1ListAppsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = apigee_organizations_apps_list_builder(
        client,
        parent,
        apiProduct,
        apptype,
        expand,
        filter,
        ids,
        includeCred,
        keyStatus,
        pageSize,
        pageToken,
        rows,
        startKey,
        status,
    )?;
    apigee_organizations_apps_list_execute(builder)
}

/// GET v1/organizations/{organizationsId}/datacollectors
/// Creates a new data collector.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `apigee_organizations_datacollectors_create_execute()` to send, or `apigee_organizations_datacollectors_create` for simplest API.

pub fn apigee_organizations_datacollectors_create_builder(
    client: &SimpleHttpClient,
    parent: &str,
    dataCollectorId: Option<&str>,
    body: &GoogleCloudApigeeV1DataCollector,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://apigee.googleapis.com/v1/organizations/{}/datacollectors",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = dataCollectorId {
        query_parts.push(format!("dataCollectorId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/organizations/{organizationsId}/datacollectors
/// Creates a new data collector.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `apigee_organizations_datacollectors_create_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from apigee_organizations_datacollectors_create_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_datacollectors_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudApigeeV1DataCollector>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudApigeeV1DataCollector = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/organizations/{organizationsId}/datacollectors
/// Creates a new data collector.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `apigee_organizations_datacollectors_create_builder()` + `apigee_organizations_datacollectors_create_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_datacollectors_create(
    client: &SimpleHttpClient,
    parent: &str,
    dataCollectorId: Option<&str>,
    body: &GoogleCloudApigeeV1DataCollector,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudApigeeV1DataCollector>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        apigee_organizations_datacollectors_create_builder(client, parent, dataCollectorId, body)?;
    apigee_organizations_datacollectors_create_execute(builder)
}

/// GET v1/organizations/{organizationsId}/datacollectors/{datacollectorsId}
/// Deletes a data collector.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `apigee_organizations_datacollectors_delete_execute()` to send, or `apigee_organizations_datacollectors_delete` for simplest API.

pub fn apigee_organizations_datacollectors_delete_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://apigee.googleapis.com/v1/organizations/{}/datacollectors/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/organizations/{organizationsId}/datacollectors/{datacollectorsId}
/// Deletes a data collector.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `apigee_organizations_datacollectors_delete_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from apigee_organizations_datacollectors_delete_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_datacollectors_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleProtobufEmpty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/organizations/{organizationsId}/datacollectors/{datacollectorsId}
/// Deletes a data collector.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `apigee_organizations_datacollectors_delete_builder()` + `apigee_organizations_datacollectors_delete_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_datacollectors_delete(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = apigee_organizations_datacollectors_delete_builder(client, name)?;
    apigee_organizations_datacollectors_delete_execute(builder)
}

/// GET v1/organizations/{organizationsId}/datacollectors/{datacollectorsId}
/// Gets a data collector.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `apigee_organizations_datacollectors_get_execute()` to send, or `apigee_organizations_datacollectors_get` for simplest API.

pub fn apigee_organizations_datacollectors_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://apigee.googleapis.com/v1/organizations/{}/datacollectors/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/organizations/{organizationsId}/datacollectors/{datacollectorsId}
/// Gets a data collector.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `apigee_organizations_datacollectors_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from apigee_organizations_datacollectors_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_datacollectors_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudApigeeV1DataCollector>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudApigeeV1DataCollector = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/organizations/{organizationsId}/datacollectors/{datacollectorsId}
/// Gets a data collector.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `apigee_organizations_datacollectors_get_builder()` + `apigee_organizations_datacollectors_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_datacollectors_get(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudApigeeV1DataCollector>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = apigee_organizations_datacollectors_get_builder(client, name)?;
    apigee_organizations_datacollectors_get_execute(builder)
}

/// GET v1/organizations/{organizationsId}/datacollectors
/// Lists all data collectors.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `apigee_organizations_datacollectors_list_execute()` to send, or `apigee_organizations_datacollectors_list` for simplest API.

pub fn apigee_organizations_datacollectors_list_builder(
    client: &SimpleHttpClient,
    parent: &str,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://apigee.googleapis.com/v1/organizations/{}/datacollectors",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/organizations/{organizationsId}/datacollectors
/// Lists all data collectors.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `apigee_organizations_datacollectors_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from apigee_organizations_datacollectors_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_datacollectors_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudApigeeV1ListDataCollectorsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudApigeeV1ListDataCollectorsResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/organizations/{organizationsId}/datacollectors
/// Lists all data collectors.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `apigee_organizations_datacollectors_list_builder()` + `apigee_organizations_datacollectors_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_datacollectors_list(
    client: &SimpleHttpClient,
    parent: &str,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudApigeeV1ListDataCollectorsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        apigee_organizations_datacollectors_list_builder(client, parent, pageSize, pageToken)?;
    apigee_organizations_datacollectors_list_execute(builder)
}

/// GET v1/organizations/{organizationsId}/datacollectors/{datacollectorsId}
/// Updates a data collector.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `apigee_organizations_datacollectors_patch_execute()` to send, or `apigee_organizations_datacollectors_patch` for simplest API.

pub fn apigee_organizations_datacollectors_patch_builder(
    client: &SimpleHttpClient,
    name: &str,
    updateMask: Option<&str>,
    body: &GoogleCloudApigeeV1DataCollector,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://apigee.googleapis.com/v1/organizations/{}/datacollectors/{}",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = updateMask {
        query_parts.push(format!("updateMask={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/organizations/{organizationsId}/datacollectors/{datacollectorsId}
/// Updates a data collector.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `apigee_organizations_datacollectors_patch_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from apigee_organizations_datacollectors_patch_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_datacollectors_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudApigeeV1DataCollector>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudApigeeV1DataCollector = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/organizations/{organizationsId}/datacollectors/{datacollectorsId}
/// Updates a data collector.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `apigee_organizations_datacollectors_patch_builder()` + `apigee_organizations_datacollectors_patch_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_datacollectors_patch(
    client: &SimpleHttpClient,
    name: &str,
    updateMask: Option<&str>,
    body: &GoogleCloudApigeeV1DataCollector,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudApigeeV1DataCollector>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        apigee_organizations_datacollectors_patch_builder(client, name, updateMask, body)?;
    apigee_organizations_datacollectors_patch_execute(builder)
}

/// GET v1/organizations/{organizationsId}/deployments
/// Lists all deployments of API proxies or shared flows.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `apigee_organizations_deployments_list_execute()` to send, or `apigee_organizations_deployments_list` for simplest API.

pub fn apigee_organizations_deployments_list_builder(
    client: &SimpleHttpClient,
    parent: &str,
    sharedFlows: Option<bool>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://apigee.googleapis.com/v1/organizations/{}/deployments",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = sharedFlows {
        query_parts.push(format!("sharedFlows={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/organizations/{organizationsId}/deployments
/// Lists all deployments of API proxies or shared flows.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `apigee_organizations_deployments_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from apigee_organizations_deployments_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_deployments_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudApigeeV1ListDeploymentsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudApigeeV1ListDeploymentsResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/organizations/{organizationsId}/deployments
/// Lists all deployments of API proxies or shared flows.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `apigee_organizations_deployments_list_builder()` + `apigee_organizations_deployments_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_deployments_list(
    client: &SimpleHttpClient,
    parent: &str,
    sharedFlows: Option<bool>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudApigeeV1ListDeploymentsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = apigee_organizations_deployments_list_builder(client, parent, sharedFlows)?;
    apigee_organizations_deployments_list_execute(builder)
}

/// GET v1/organizations/{organizationsId}/developers/{developersId}/attributes
/// Updates developer attributes. This API replaces the existing attributes with those specified in the request. Add new attributes, and include or exclude any existing attributes that you want to retain or remove, respectively. The custom attribute limit is 18. **Note**: OAuth access tokens and Key Management Service (KMS) entities (apps, developers, and API products) are cached for 180 seconds (default). Any custom attributes associated with these entities are cached for at least 180 seconds after the entity is accessed at runtime. Therefore, an ExpiresIn element on the OAuthV2 policy won't be able to expire an access token in less than 180 seconds.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `apigee_organizations_developers_attributes_execute()` to send, or `apigee_organizations_developers_attributes` for simplest API.

pub fn apigee_organizations_developers_attributes_builder(
    client: &SimpleHttpClient,
    parent: &str,
    body: &GoogleCloudApigeeV1Attributes,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://apigee.googleapis.com/v1/organizations/{}/developers/{}/attributes",
        parent,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/organizations/{organizationsId}/developers/{developersId}/attributes
/// Updates developer attributes. This API replaces the existing attributes with those specified in the request. Add new attributes, and include or exclude any existing attributes that you want to retain or remove, respectively. The custom attribute limit is 18. **Note**: OAuth access tokens and Key Management Service (KMS) entities (apps, developers, and API products) are cached for 180 seconds (default). Any custom attributes associated with these entities are cached for at least 180 seconds after the entity is accessed at runtime. Therefore, an ExpiresIn element on the OAuthV2 policy won't be able to expire an access token in less than 180 seconds.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `apigee_organizations_developers_attributes_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from apigee_organizations_developers_attributes_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_developers_attributes_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudApigeeV1Attributes>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudApigeeV1Attributes = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/organizations/{organizationsId}/developers/{developersId}/attributes
/// Updates developer attributes. This API replaces the existing attributes with those specified in the request. Add new attributes, and include or exclude any existing attributes that you want to retain or remove, respectively. The custom attribute limit is 18. **Note**: OAuth access tokens and Key Management Service (KMS) entities (apps, developers, and API products) are cached for 180 seconds (default). Any custom attributes associated with these entities are cached for at least 180 seconds after the entity is accessed at runtime. Therefore, an ExpiresIn element on the OAuthV2 policy won't be able to expire an access token in less than 180 seconds.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `apigee_organizations_developers_attributes_builder()` + `apigee_organizations_developers_attributes_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_developers_attributes(
    client: &SimpleHttpClient,
    parent: &str,
    body: &GoogleCloudApigeeV1Attributes,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudApigeeV1Attributes>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = apigee_organizations_developers_attributes_builder(client, parent, body)?;
    apigee_organizations_developers_attributes_execute(builder)
}

/// GET v1/organizations/{organizationsId}/developers
/// Creates a developer. Once created, the developer can register an app and obtain an API key. At creation time, a developer is set as active. To change the developer status, use the SetDeveloperStatus API.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `apigee_organizations_developers_create_execute()` to send, or `apigee_organizations_developers_create` for simplest API.

pub fn apigee_organizations_developers_create_builder(
    client: &SimpleHttpClient,
    parent: &str,
    body: &GoogleCloudApigeeV1Developer,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://apigee.googleapis.com/v1/organizations/{}/developers",
        parent,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/organizations/{organizationsId}/developers
/// Creates a developer. Once created, the developer can register an app and obtain an API key. At creation time, a developer is set as active. To change the developer status, use the SetDeveloperStatus API.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `apigee_organizations_developers_create_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from apigee_organizations_developers_create_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_developers_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudApigeeV1Developer>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudApigeeV1Developer = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/organizations/{organizationsId}/developers
/// Creates a developer. Once created, the developer can register an app and obtain an API key. At creation time, a developer is set as active. To change the developer status, use the SetDeveloperStatus API.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `apigee_organizations_developers_create_builder()` + `apigee_organizations_developers_create_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_developers_create(
    client: &SimpleHttpClient,
    parent: &str,
    body: &GoogleCloudApigeeV1Developer,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudApigeeV1Developer>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = apigee_organizations_developers_create_builder(client, parent, body)?;
    apigee_organizations_developers_create_execute(builder)
}

/// GET v1/organizations/{organizationsId}/developers/{developersId}
/// Deletes a developer. All apps and API keys associated with the developer are also removed. **Warning**: This API will permanently delete the developer and related artifacts. To avoid permanently deleting developers and their artifacts, set the developer status to inactive using the SetDeveloperStatus API. **Note**: The delete operation is asynchronous. The developer is deleted immediately, but its associated resources, such as apps and API keys, may take anywhere from a few seconds to a few minutes to be deleted.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `apigee_organizations_developers_delete_execute()` to send, or `apigee_organizations_developers_delete` for simplest API.

pub fn apigee_organizations_developers_delete_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://apigee.googleapis.com/v1/organizations/{}/developers/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/organizations/{organizationsId}/developers/{developersId}
/// Deletes a developer. All apps and API keys associated with the developer are also removed. **Warning**: This API will permanently delete the developer and related artifacts. To avoid permanently deleting developers and their artifacts, set the developer status to inactive using the SetDeveloperStatus API. **Note**: The delete operation is asynchronous. The developer is deleted immediately, but its associated resources, such as apps and API keys, may take anywhere from a few seconds to a few minutes to be deleted.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `apigee_organizations_developers_delete_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from apigee_organizations_developers_delete_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_developers_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudApigeeV1Developer>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudApigeeV1Developer = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/organizations/{organizationsId}/developers/{developersId}
/// Deletes a developer. All apps and API keys associated with the developer are also removed. **Warning**: This API will permanently delete the developer and related artifacts. To avoid permanently deleting developers and their artifacts, set the developer status to inactive using the SetDeveloperStatus API. **Note**: The delete operation is asynchronous. The developer is deleted immediately, but its associated resources, such as apps and API keys, may take anywhere from a few seconds to a few minutes to be deleted.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `apigee_organizations_developers_delete_builder()` + `apigee_organizations_developers_delete_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_developers_delete(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudApigeeV1Developer>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = apigee_organizations_developers_delete_builder(client, name)?;
    apigee_organizations_developers_delete_execute(builder)
}

/// GET v1/organizations/{organizationsId}/developers/{developersId}
/// Returns the developer details, including the developer's name, email address, apps, and other information. **Note**: The response includes only the first 100 developer apps.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `apigee_organizations_developers_get_execute()` to send, or `apigee_organizations_developers_get` for simplest API.

pub fn apigee_organizations_developers_get_builder(
    client: &SimpleHttpClient,
    name: &str,
    action: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://apigee.googleapis.com/v1/organizations/{}/developers/{}",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = action {
        query_parts.push(format!("action={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/organizations/{organizationsId}/developers/{developersId}
/// Returns the developer details, including the developer's name, email address, apps, and other information. **Note**: The response includes only the first 100 developer apps.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `apigee_organizations_developers_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from apigee_organizations_developers_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_developers_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudApigeeV1Developer>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudApigeeV1Developer = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/organizations/{organizationsId}/developers/{developersId}
/// Returns the developer details, including the developer's name, email address, apps, and other information. **Note**: The response includes only the first 100 developer apps.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `apigee_organizations_developers_get_builder()` + `apigee_organizations_developers_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_developers_get(
    client: &SimpleHttpClient,
    name: &str,
    action: Option<&str>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudApigeeV1Developer>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = apigee_organizations_developers_get_builder(client, name, action)?;
    apigee_organizations_developers_get_execute(builder)
}

/// GET v1/organizations/{organizationsId}/developers/{developersId}/balance
/// Gets the account balance for the developer.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `apigee_organizations_developers_get_balance_execute()` to send, or `apigee_organizations_developers_get_balance` for simplest API.

pub fn apigee_organizations_developers_get_balance_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://apigee.googleapis.com/v1/organizations/{}/developers/{}/balance",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/organizations/{organizationsId}/developers/{developersId}/balance
/// Gets the account balance for the developer.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `apigee_organizations_developers_get_balance_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from apigee_organizations_developers_get_balance_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_developers_get_balance_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudApigeeV1DeveloperBalance>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudApigeeV1DeveloperBalance = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/organizations/{organizationsId}/developers/{developersId}/balance
/// Gets the account balance for the developer.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `apigee_organizations_developers_get_balance_builder()` + `apigee_organizations_developers_get_balance_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_developers_get_balance(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudApigeeV1DeveloperBalance>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = apigee_organizations_developers_get_balance_builder(client, name)?;
    apigee_organizations_developers_get_balance_execute(builder)
}

/// GET v1/organizations/{organizationsId}/developers/{developersId}/monetizationConfig
/// Gets the monetization configuration for the developer.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `apigee_organizations_developers_get_monetization_config_execute()` to send, or `apigee_organizations_developers_get_monetization_config` for simplest API.

pub fn apigee_organizations_developers_get_monetization_config_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://apigee.googleapis.com/v1/organizations/{}/developers/{}/monetizationConfig",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/organizations/{organizationsId}/developers/{developersId}/monetizationConfig
/// Gets the monetization configuration for the developer.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `apigee_organizations_developers_get_monetization_config_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from apigee_organizations_developers_get_monetization_config_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_developers_get_monetization_config_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudApigeeV1DeveloperMonetizationConfig>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudApigeeV1DeveloperMonetizationConfig =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/organizations/{organizationsId}/developers/{developersId}/monetizationConfig
/// Gets the monetization configuration for the developer.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `apigee_organizations_developers_get_monetization_config_builder()` + `apigee_organizations_developers_get_monetization_config_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_developers_get_monetization_config(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudApigeeV1DeveloperMonetizationConfig>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = apigee_organizations_developers_get_monetization_config_builder(client, name)?;
    apigee_organizations_developers_get_monetization_config_execute(builder)
}

/// GET v1/organizations/{organizationsId}/developers
/// Lists all developers in an organization by email address. By default, the response does not include company developers. Set the `includeCompany` query parameter to `true` to include company developers. **Note**: A maximum of 1000 developers are returned in the response. You paginate the list of developers returned using the `startKey` and count query parameters.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `apigee_organizations_developers_list_execute()` to send, or `apigee_organizations_developers_list` for simplest API.

pub fn apigee_organizations_developers_list_builder(
    client: &SimpleHttpClient,
    parent: &str,
    app: Option<&str>,
    count: Option<&str>,
    expand: Option<bool>,
    ids: Option<&str>,
    includeCompany: Option<bool>,
    startKey: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://apigee.googleapis.com/v1/organizations/{}/developers",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = app {
        query_parts.push(format!("app={}", val));
    }
    if let Some(val) = count {
        query_parts.push(format!("count={}", val));
    }
    if let Some(val) = expand {
        query_parts.push(format!("expand={}", val));
    }
    if let Some(val) = ids {
        query_parts.push(format!("ids={}", val));
    }
    if let Some(val) = includeCompany {
        query_parts.push(format!("includeCompany={}", val));
    }
    if let Some(val) = startKey {
        query_parts.push(format!("startKey={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/organizations/{organizationsId}/developers
/// Lists all developers in an organization by email address. By default, the response does not include company developers. Set the `includeCompany` query parameter to `true` to include company developers. **Note**: A maximum of 1000 developers are returned in the response. You paginate the list of developers returned using the `startKey` and count query parameters.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `apigee_organizations_developers_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from apigee_organizations_developers_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_developers_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudApigeeV1ListOfDevelopersResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudApigeeV1ListOfDevelopersResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/organizations/{organizationsId}/developers
/// Lists all developers in an organization by email address. By default, the response does not include company developers. Set the `includeCompany` query parameter to `true` to include company developers. **Note**: A maximum of 1000 developers are returned in the response. You paginate the list of developers returned using the `startKey` and count query parameters.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `apigee_organizations_developers_list_builder()` + `apigee_organizations_developers_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_developers_list(
    client: &SimpleHttpClient,
    parent: &str,
    app: Option<&str>,
    count: Option<&str>,
    expand: Option<bool>,
    ids: Option<&str>,
    includeCompany: Option<bool>,
    startKey: Option<&str>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudApigeeV1ListOfDevelopersResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = apigee_organizations_developers_list_builder(
        client,
        parent,
        app,
        count,
        expand,
        ids,
        includeCompany,
        startKey,
    )?;
    apigee_organizations_developers_list_execute(builder)
}

/// GET v1/organizations/{organizationsId}/developers/{developersId}
/// Sets the status of a developer. A developer is active by default. If you set a developer's status to inactive, the API keys assigned to the developer apps are no longer valid even though the API keys are set to approved. Inactive developers can still sign in to the developer portal and create apps; however, any new API keys generated during app creation won't work. To set the status of a developer, set the action query parameter to active or inactive, and the Content-Type header to `application/octet-stream`. If successful, the API call returns the following HTTP status code: 204 No Content
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `apigee_organizations_developers_set_developer_status_execute()` to send, or `apigee_organizations_developers_set_developer_status` for simplest API.

pub fn apigee_organizations_developers_set_developer_status_builder(
    client: &SimpleHttpClient,
    name: &str,
    action: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://apigee.googleapis.com/v1/organizations/{}/developers/{}",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = action {
        query_parts.push(format!("action={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/organizations/{organizationsId}/developers/{developersId}
/// Sets the status of a developer. A developer is active by default. If you set a developer's status to inactive, the API keys assigned to the developer apps are no longer valid even though the API keys are set to approved. Inactive developers can still sign in to the developer portal and create apps; however, any new API keys generated during app creation won't work. To set the status of a developer, set the action query parameter to active or inactive, and the Content-Type header to `application/octet-stream`. If successful, the API call returns the following HTTP status code: 204 No Content
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `apigee_organizations_developers_set_developer_status_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from apigee_organizations_developers_set_developer_status_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_developers_set_developer_status_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleProtobufEmpty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/organizations/{organizationsId}/developers/{developersId}
/// Sets the status of a developer. A developer is active by default. If you set a developer's status to inactive, the API keys assigned to the developer apps are no longer valid even though the API keys are set to approved. Inactive developers can still sign in to the developer portal and create apps; however, any new API keys generated during app creation won't work. To set the status of a developer, set the action query parameter to active or inactive, and the Content-Type header to `application/octet-stream`. If successful, the API call returns the following HTTP status code: 204 No Content
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `apigee_organizations_developers_set_developer_status_builder()` + `apigee_organizations_developers_set_developer_status_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_developers_set_developer_status(
    client: &SimpleHttpClient,
    name: &str,
    action: Option<&str>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        apigee_organizations_developers_set_developer_status_builder(client, name, action)?;
    apigee_organizations_developers_set_developer_status_execute(builder)
}

/// GET v1/organizations/{organizationsId}/developers/{developersId}
/// Updates a developer. This API replaces the existing developer details with those specified in the request. Include or exclude any existing details that you want to retain or delete, respectively. The custom attribute limit is 18. **Note**: OAuth access tokens and Key Management Service (KMS) entities (apps, developers, and API products) are cached for 180 seconds (current default). Any custom attributes associated with these entities are cached for at least 180 seconds after the entity is accessed at runtime. Therefore, an ExpiresIn element on the OAuthV2 policy won't be able to expire an access token in less than 180 seconds.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `apigee_organizations_developers_update_execute()` to send, or `apigee_organizations_developers_update` for simplest API.

pub fn apigee_organizations_developers_update_builder(
    client: &SimpleHttpClient,
    name: &str,
    body: &GoogleCloudApigeeV1Developer,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://apigee.googleapis.com/v1/organizations/{}/developers/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/organizations/{organizationsId}/developers/{developersId}
/// Updates a developer. This API replaces the existing developer details with those specified in the request. Include or exclude any existing details that you want to retain or delete, respectively. The custom attribute limit is 18. **Note**: OAuth access tokens and Key Management Service (KMS) entities (apps, developers, and API products) are cached for 180 seconds (current default). Any custom attributes associated with these entities are cached for at least 180 seconds after the entity is accessed at runtime. Therefore, an ExpiresIn element on the OAuthV2 policy won't be able to expire an access token in less than 180 seconds.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `apigee_organizations_developers_update_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from apigee_organizations_developers_update_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_developers_update_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudApigeeV1Developer>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudApigeeV1Developer = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/organizations/{organizationsId}/developers/{developersId}
/// Updates a developer. This API replaces the existing developer details with those specified in the request. Include or exclude any existing details that you want to retain or delete, respectively. The custom attribute limit is 18. **Note**: OAuth access tokens and Key Management Service (KMS) entities (apps, developers, and API products) are cached for 180 seconds (current default). Any custom attributes associated with these entities are cached for at least 180 seconds after the entity is accessed at runtime. Therefore, an ExpiresIn element on the OAuthV2 policy won't be able to expire an access token in less than 180 seconds.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `apigee_organizations_developers_update_builder()` + `apigee_organizations_developers_update_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_developers_update(
    client: &SimpleHttpClient,
    name: &str,
    body: &GoogleCloudApigeeV1Developer,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudApigeeV1Developer>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = apigee_organizations_developers_update_builder(client, name, body)?;
    apigee_organizations_developers_update_execute(builder)
}

/// GET v1/organizations/{organizationsId}/developers/{developersId}/monetizationConfig
/// Updates the monetization configuration for the developer.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `apigee_organizations_developers_update_monetization_config_execute()` to send, or `apigee_organizations_developers_update_monetization_config` for simplest API.

pub fn apigee_organizations_developers_update_monetization_config_builder(
    client: &SimpleHttpClient,
    name: &str,
    body: &GoogleCloudApigeeV1DeveloperMonetizationConfig,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://apigee.googleapis.com/v1/organizations/{}/developers/{}/monetizationConfig",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/organizations/{organizationsId}/developers/{developersId}/monetizationConfig
/// Updates the monetization configuration for the developer.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `apigee_organizations_developers_update_monetization_config_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from apigee_organizations_developers_update_monetization_config_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_developers_update_monetization_config_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudApigeeV1DeveloperMonetizationConfig>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudApigeeV1DeveloperMonetizationConfig =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/organizations/{organizationsId}/developers/{developersId}/monetizationConfig
/// Updates the monetization configuration for the developer.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `apigee_organizations_developers_update_monetization_config_builder()` + `apigee_organizations_developers_update_monetization_config_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_developers_update_monetization_config(
    client: &SimpleHttpClient,
    name: &str,
    body: &GoogleCloudApigeeV1DeveloperMonetizationConfig,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudApigeeV1DeveloperMonetizationConfig>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        apigee_organizations_developers_update_monetization_config_builder(client, name, body)?;
    apigee_organizations_developers_update_monetization_config_execute(builder)
}

/// GET v1/organizations/{organizationsId}/developers/{developersId}/apps/{appsId}/attributes
/// Updates attributes for a developer app. This API replaces the current attributes with those specified in the request.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `apigee_organizations_developers_apps_attributes_execute()` to send, or `apigee_organizations_developers_apps_attributes` for simplest API.

pub fn apigee_organizations_developers_apps_attributes_builder(
    client: &SimpleHttpClient,
    name: &str,
    body: &GoogleCloudApigeeV1Attributes,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://apigee.googleapis.com/v1/organizations/{}/developers/{}/apps/{}/attributes",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/organizations/{organizationsId}/developers/{developersId}/apps/{appsId}/attributes
/// Updates attributes for a developer app. This API replaces the current attributes with those specified in the request.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `apigee_organizations_developers_apps_attributes_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from apigee_organizations_developers_apps_attributes_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_developers_apps_attributes_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudApigeeV1Attributes>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudApigeeV1Attributes = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/organizations/{organizationsId}/developers/{developersId}/apps/{appsId}/attributes
/// Updates attributes for a developer app. This API replaces the current attributes with those specified in the request.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `apigee_organizations_developers_apps_attributes_builder()` + `apigee_organizations_developers_apps_attributes_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_developers_apps_attributes(
    client: &SimpleHttpClient,
    name: &str,
    body: &GoogleCloudApigeeV1Attributes,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudApigeeV1Attributes>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = apigee_organizations_developers_apps_attributes_builder(client, name, body)?;
    apigee_organizations_developers_apps_attributes_execute(builder)
}

/// GET v1/organizations/{organizationsId}/developers/{developersId}/apps
/// Creates an app associated with a developer. This API associates the developer app with the specified API product and auto-generates an API key for the app to use in calls to API proxies inside that API product. The name is the unique ID of the app that you can use in API calls. The DisplayName (set as an attribute) appears in the UI. If you don't set the DisplayName attribute, the name appears in the UI.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `apigee_organizations_developers_apps_create_execute()` to send, or `apigee_organizations_developers_apps_create` for simplest API.

pub fn apigee_organizations_developers_apps_create_builder(
    client: &SimpleHttpClient,
    parent: &str,
    body: &GoogleCloudApigeeV1DeveloperApp,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://apigee.googleapis.com/v1/organizations/{}/developers/{}/apps",
        parent,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/organizations/{organizationsId}/developers/{developersId}/apps
/// Creates an app associated with a developer. This API associates the developer app with the specified API product and auto-generates an API key for the app to use in calls to API proxies inside that API product. The name is the unique ID of the app that you can use in API calls. The DisplayName (set as an attribute) appears in the UI. If you don't set the DisplayName attribute, the name appears in the UI.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `apigee_organizations_developers_apps_create_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from apigee_organizations_developers_apps_create_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_developers_apps_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudApigeeV1DeveloperApp>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudApigeeV1DeveloperApp = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/organizations/{organizationsId}/developers/{developersId}/apps
/// Creates an app associated with a developer. This API associates the developer app with the specified API product and auto-generates an API key for the app to use in calls to API proxies inside that API product. The name is the unique ID of the app that you can use in API calls. The DisplayName (set as an attribute) appears in the UI. If you don't set the DisplayName attribute, the name appears in the UI.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `apigee_organizations_developers_apps_create_builder()` + `apigee_organizations_developers_apps_create_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_developers_apps_create(
    client: &SimpleHttpClient,
    parent: &str,
    body: &GoogleCloudApigeeV1DeveloperApp,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudApigeeV1DeveloperApp>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = apigee_organizations_developers_apps_create_builder(client, parent, body)?;
    apigee_organizations_developers_apps_create_execute(builder)
}

/// GET v1/organizations/{organizationsId}/developers/{developersId}/apps/{appsId}
/// Deletes a developer app. **Note**: The delete operation is asynchronous. The developer app is deleted immediately, but its associated resources, such as app keys or access tokens, may take anywhere from a few seconds to a few minutes to be deleted.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `apigee_organizations_developers_apps_delete_execute()` to send, or `apigee_organizations_developers_apps_delete` for simplest API.

pub fn apigee_organizations_developers_apps_delete_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://apigee.googleapis.com/v1/organizations/{}/developers/{}/apps/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/organizations/{organizationsId}/developers/{developersId}/apps/{appsId}
/// Deletes a developer app. **Note**: The delete operation is asynchronous. The developer app is deleted immediately, but its associated resources, such as app keys or access tokens, may take anywhere from a few seconds to a few minutes to be deleted.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `apigee_organizations_developers_apps_delete_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from apigee_organizations_developers_apps_delete_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_developers_apps_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudApigeeV1DeveloperApp>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudApigeeV1DeveloperApp = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/organizations/{organizationsId}/developers/{developersId}/apps/{appsId}
/// Deletes a developer app. **Note**: The delete operation is asynchronous. The developer app is deleted immediately, but its associated resources, such as app keys or access tokens, may take anywhere from a few seconds to a few minutes to be deleted.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `apigee_organizations_developers_apps_delete_builder()` + `apigee_organizations_developers_apps_delete_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_developers_apps_delete(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudApigeeV1DeveloperApp>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = apigee_organizations_developers_apps_delete_builder(client, name)?;
    apigee_organizations_developers_apps_delete_execute(builder)
}

/// GET v1/organizations/{organizationsId}/developers/{developersId}/apps/{appsId}
/// Manages access to a developer app by enabling you to: * Approve or revoke a developer app * Generate a new consumer key and secret for a developer app To approve or revoke a developer app, set the action query parameter to approve or revoke, respectively, and the Content-Type header to `application/octet-stream`. If a developer app is revoked, none of its API keys are valid for API calls even though the keys are still approved. If successful, the API call returns the following HTTP status code: 204 No Content To generate a new consumer key and secret for a developer app, pass the new `key/secret` details. Rather than replace an existing key, this API generates a new key. In this case, multiple key pairs may be associated with a single developer app. Each key pair has an independent status (approve or revoke) and expiration time. Any approved, non-expired key can be used in an API call. For example, if you're using API key rotation, you can generate new keys with expiration times that overlap keys that are going to expire. You might also generate a new consumer `key/secret` if the security of the original `key/secret` is compromised. The `keyExpiresIn` property defines the expiration time for the API key in milliseconds. If you don't set this property or set it to -1, the API key never expires. **Notes**: * When generating a new `key/secret`, this API replaces the existing attributes, notes, and callback URLs with those specified in the request. Include or exclude any existing information that you want to retain or delete, respectively. * To migrate existing consumer keys and secrets to hybrid from another system, see the CreateDeveloperAppKey API.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `apigee_organizations_developers_apps_generate_key_pair_or_update_developer_app_status_execute()` to send, or `apigee_organizations_developers_apps_generate_key_pair_or_update_developer_app_status` for simplest API.

pub fn apigee_organizations_developers_apps_generate_key_pair_or_update_developer_app_status_builder(
    client: &SimpleHttpClient,
    name: &str,
    action: Option<&str>,
    body: &GoogleCloudApigeeV1DeveloperApp,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://apigee.googleapis.com/v1/organizations/{}/developers/{}/apps/{}",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = action {
        query_parts.push(format!("action={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/organizations/{organizationsId}/developers/{developersId}/apps/{appsId}
/// Manages access to a developer app by enabling you to: * Approve or revoke a developer app * Generate a new consumer key and secret for a developer app To approve or revoke a developer app, set the action query parameter to approve or revoke, respectively, and the Content-Type header to `application/octet-stream`. If a developer app is revoked, none of its API keys are valid for API calls even though the keys are still approved. If successful, the API call returns the following HTTP status code: 204 No Content To generate a new consumer key and secret for a developer app, pass the new `key/secret` details. Rather than replace an existing key, this API generates a new key. In this case, multiple key pairs may be associated with a single developer app. Each key pair has an independent status (approve or revoke) and expiration time. Any approved, non-expired key can be used in an API call. For example, if you're using API key rotation, you can generate new keys with expiration times that overlap keys that are going to expire. You might also generate a new consumer `key/secret` if the security of the original `key/secret` is compromised. The `keyExpiresIn` property defines the expiration time for the API key in milliseconds. If you don't set this property or set it to -1, the API key never expires. **Notes**: * When generating a new `key/secret`, this API replaces the existing attributes, notes, and callback URLs with those specified in the request. Include or exclude any existing information that you want to retain or delete, respectively. * To migrate existing consumer keys and secrets to hybrid from another system, see the CreateDeveloperAppKey API.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `apigee_organizations_developers_apps_generate_key_pair_or_update_developer_app_status_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from apigee_organizations_developers_apps_generate_key_pair_or_update_developer_app_status_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_developers_apps_generate_key_pair_or_update_developer_app_status_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudApigeeV1DeveloperApp>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudApigeeV1DeveloperApp = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/organizations/{organizationsId}/developers/{developersId}/apps/{appsId}
/// Manages access to a developer app by enabling you to: * Approve or revoke a developer app * Generate a new consumer key and secret for a developer app To approve or revoke a developer app, set the action query parameter to approve or revoke, respectively, and the Content-Type header to `application/octet-stream`. If a developer app is revoked, none of its API keys are valid for API calls even though the keys are still approved. If successful, the API call returns the following HTTP status code: 204 No Content To generate a new consumer key and secret for a developer app, pass the new `key/secret` details. Rather than replace an existing key, this API generates a new key. In this case, multiple key pairs may be associated with a single developer app. Each key pair has an independent status (approve or revoke) and expiration time. Any approved, non-expired key can be used in an API call. For example, if you're using API key rotation, you can generate new keys with expiration times that overlap keys that are going to expire. You might also generate a new consumer `key/secret` if the security of the original `key/secret` is compromised. The `keyExpiresIn` property defines the expiration time for the API key in milliseconds. If you don't set this property or set it to -1, the API key never expires. **Notes**: * When generating a new `key/secret`, this API replaces the existing attributes, notes, and callback URLs with those specified in the request. Include or exclude any existing information that you want to retain or delete, respectively. * To migrate existing consumer keys and secrets to hybrid from another system, see the CreateDeveloperAppKey API.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `apigee_organizations_developers_apps_generate_key_pair_or_update_developer_app_status_builder()` + `apigee_organizations_developers_apps_generate_key_pair_or_update_developer_app_status_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_developers_apps_generate_key_pair_or_update_developer_app_status(
    client: &SimpleHttpClient,
    name: &str,
    action: Option<&str>,
    body: &GoogleCloudApigeeV1DeveloperApp,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudApigeeV1DeveloperApp>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = apigee_organizations_developers_apps_generate_key_pair_or_update_developer_app_status_builder(client, name, action, body)?;
    apigee_organizations_developers_apps_generate_key_pair_or_update_developer_app_status_execute(
        builder,
    )
}

/// GET v1/organizations/{organizationsId}/developers/{developersId}/apps/{appsId}
/// Returns the details for a developer app.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `apigee_organizations_developers_apps_get_execute()` to send, or `apigee_organizations_developers_apps_get` for simplest API.

pub fn apigee_organizations_developers_apps_get_builder(
    client: &SimpleHttpClient,
    name: &str,
    entity: Option<&str>,
    query: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://apigee.googleapis.com/v1/organizations/{}/developers/{}/apps/{}",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = entity {
        query_parts.push(format!("entity={}", val));
    }
    if let Some(val) = query {
        query_parts.push(format!("query={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/organizations/{organizationsId}/developers/{developersId}/apps/{appsId}
/// Returns the details for a developer app.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `apigee_organizations_developers_apps_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from apigee_organizations_developers_apps_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_developers_apps_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudApigeeV1DeveloperApp>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudApigeeV1DeveloperApp = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/organizations/{organizationsId}/developers/{developersId}/apps/{appsId}
/// Returns the details for a developer app.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `apigee_organizations_developers_apps_get_builder()` + `apigee_organizations_developers_apps_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_developers_apps_get(
    client: &SimpleHttpClient,
    name: &str,
    entity: Option<&str>,
    query: Option<&str>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudApigeeV1DeveloperApp>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = apigee_organizations_developers_apps_get_builder(client, name, entity, query)?;
    apigee_organizations_developers_apps_get_execute(builder)
}

/// GET v1/organizations/{organizationsId}/developers/{developersId}/apps
/// Lists all apps created by a developer in an Apigee organization. Optionally, you can request an expanded view of the developer apps. A maximum of 100 developer apps are returned per API call. You can paginate the list of deveoper apps returned using the `startKey` and count query parameters.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `apigee_organizations_developers_apps_list_execute()` to send, or `apigee_organizations_developers_apps_list` for simplest API.

pub fn apigee_organizations_developers_apps_list_builder(
    client: &SimpleHttpClient,
    parent: &str,
    count: Option<&str>,
    expand: Option<bool>,
    shallowExpand: Option<bool>,
    startKey: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://apigee.googleapis.com/v1/organizations/{}/developers/{}/apps",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = count {
        query_parts.push(format!("count={}", val));
    }
    if let Some(val) = expand {
        query_parts.push(format!("expand={}", val));
    }
    if let Some(val) = shallowExpand {
        query_parts.push(format!("shallowExpand={}", val));
    }
    if let Some(val) = startKey {
        query_parts.push(format!("startKey={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/organizations/{organizationsId}/developers/{developersId}/apps
/// Lists all apps created by a developer in an Apigee organization. Optionally, you can request an expanded view of the developer apps. A maximum of 100 developer apps are returned per API call. You can paginate the list of deveoper apps returned using the `startKey` and count query parameters.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `apigee_organizations_developers_apps_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from apigee_organizations_developers_apps_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_developers_apps_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudApigeeV1ListDeveloperAppsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudApigeeV1ListDeveloperAppsResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/organizations/{organizationsId}/developers/{developersId}/apps
/// Lists all apps created by a developer in an Apigee organization. Optionally, you can request an expanded view of the developer apps. A maximum of 100 developer apps are returned per API call. You can paginate the list of deveoper apps returned using the `startKey` and count query parameters.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `apigee_organizations_developers_apps_list_builder()` + `apigee_organizations_developers_apps_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_developers_apps_list(
    client: &SimpleHttpClient,
    parent: &str,
    count: Option<&str>,
    expand: Option<bool>,
    shallowExpand: Option<bool>,
    startKey: Option<&str>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudApigeeV1ListDeveloperAppsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = apigee_organizations_developers_apps_list_builder(
        client,
        parent,
        count,
        expand,
        shallowExpand,
        startKey,
    )?;
    apigee_organizations_developers_apps_list_execute(builder)
}

/// GET v1/organizations/{organizationsId}/developers/{developersId}/apps/{appsId}
/// Updates the details for a developer app. In addition, you can add an API product to a developer app and automatically generate an API key for the app to use when calling APIs in the API product. If you want to use an existing API key for the API product, add the API product to the API key using the UpdateDeveloperAppKey API. Using this API, you cannot update the following: * App name as it is the primary key used to identify the app and cannot be changed. * Scopes associated with the app. Instead, use the ReplaceDeveloperAppKey API. This API replaces the existing attributes with those specified in the request. Include or exclude any existing attributes that you want to retain or delete, respectively.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `apigee_organizations_developers_apps_update_execute()` to send, or `apigee_organizations_developers_apps_update` for simplest API.

pub fn apigee_organizations_developers_apps_update_builder(
    client: &SimpleHttpClient,
    name: &str,
    body: &GoogleCloudApigeeV1DeveloperApp,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://apigee.googleapis.com/v1/organizations/{}/developers/{}/apps/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/organizations/{organizationsId}/developers/{developersId}/apps/{appsId}
/// Updates the details for a developer app. In addition, you can add an API product to a developer app and automatically generate an API key for the app to use when calling APIs in the API product. If you want to use an existing API key for the API product, add the API product to the API key using the UpdateDeveloperAppKey API. Using this API, you cannot update the following: * App name as it is the primary key used to identify the app and cannot be changed. * Scopes associated with the app. Instead, use the ReplaceDeveloperAppKey API. This API replaces the existing attributes with those specified in the request. Include or exclude any existing attributes that you want to retain or delete, respectively.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `apigee_organizations_developers_apps_update_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from apigee_organizations_developers_apps_update_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_developers_apps_update_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudApigeeV1DeveloperApp>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudApigeeV1DeveloperApp = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/organizations/{organizationsId}/developers/{developersId}/apps/{appsId}
/// Updates the details for a developer app. In addition, you can add an API product to a developer app and automatically generate an API key for the app to use when calling APIs in the API product. If you want to use an existing API key for the API product, add the API product to the API key using the UpdateDeveloperAppKey API. Using this API, you cannot update the following: * App name as it is the primary key used to identify the app and cannot be changed. * Scopes associated with the app. Instead, use the ReplaceDeveloperAppKey API. This API replaces the existing attributes with those specified in the request. Include or exclude any existing attributes that you want to retain or delete, respectively.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `apigee_organizations_developers_apps_update_builder()` + `apigee_organizations_developers_apps_update_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_developers_apps_update(
    client: &SimpleHttpClient,
    name: &str,
    body: &GoogleCloudApigeeV1DeveloperApp,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudApigeeV1DeveloperApp>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = apigee_organizations_developers_apps_update_builder(client, name, body)?;
    apigee_organizations_developers_apps_update_execute(builder)
}

/// GET v1/organizations/{organizationsId}/developers/{developersId}/apps/{appsId}/attributes/{attributesId}
/// Deletes a developer app attribute.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `apigee_organizations_developers_apps_attributes_delete_execute()` to send, or `apigee_organizations_developers_apps_attributes_delete` for simplest API.

pub fn apigee_organizations_developers_apps_attributes_delete_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://apigee.googleapis.com/v1/organizations/{}/developers/{}/apps/{}/attributes/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/organizations/{organizationsId}/developers/{developersId}/apps/{appsId}/attributes/{attributesId}
/// Deletes a developer app attribute.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `apigee_organizations_developers_apps_attributes_delete_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from apigee_organizations_developers_apps_attributes_delete_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_developers_apps_attributes_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudApigeeV1Attribute>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudApigeeV1Attribute = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/organizations/{organizationsId}/developers/{developersId}/apps/{appsId}/attributes/{attributesId}
/// Deletes a developer app attribute.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `apigee_organizations_developers_apps_attributes_delete_builder()` + `apigee_organizations_developers_apps_attributes_delete_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_developers_apps_attributes_delete(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudApigeeV1Attribute>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = apigee_organizations_developers_apps_attributes_delete_builder(client, name)?;
    apigee_organizations_developers_apps_attributes_delete_execute(builder)
}

/// GET v1/organizations/{organizationsId}/developers/{developersId}/apps/{appsId}/attributes/{attributesId}
/// Returns a developer app attribute.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `apigee_organizations_developers_apps_attributes_get_execute()` to send, or `apigee_organizations_developers_apps_attributes_get` for simplest API.

pub fn apigee_organizations_developers_apps_attributes_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://apigee.googleapis.com/v1/organizations/{}/developers/{}/apps/{}/attributes/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/organizations/{organizationsId}/developers/{developersId}/apps/{appsId}/attributes/{attributesId}
/// Returns a developer app attribute.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `apigee_organizations_developers_apps_attributes_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from apigee_organizations_developers_apps_attributes_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_developers_apps_attributes_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudApigeeV1Attribute>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudApigeeV1Attribute = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/organizations/{organizationsId}/developers/{developersId}/apps/{appsId}/attributes/{attributesId}
/// Returns a developer app attribute.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `apigee_organizations_developers_apps_attributes_get_builder()` + `apigee_organizations_developers_apps_attributes_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_developers_apps_attributes_get(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudApigeeV1Attribute>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = apigee_organizations_developers_apps_attributes_get_builder(client, name)?;
    apigee_organizations_developers_apps_attributes_get_execute(builder)
}

/// GET v1/organizations/{organizationsId}/developers/{developersId}/apps/{appsId}/attributes
/// Returns a list of all developer app attributes.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `apigee_organizations_developers_apps_attributes_list_execute()` to send, or `apigee_organizations_developers_apps_attributes_list` for simplest API.

pub fn apigee_organizations_developers_apps_attributes_list_builder(
    client: &SimpleHttpClient,
    parent: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://apigee.googleapis.com/v1/organizations/{}/developers/{}/apps/{}/attributes",
        parent,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/organizations/{organizationsId}/developers/{developersId}/apps/{appsId}/attributes
/// Returns a list of all developer app attributes.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `apigee_organizations_developers_apps_attributes_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from apigee_organizations_developers_apps_attributes_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_developers_apps_attributes_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudApigeeV1Attributes>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudApigeeV1Attributes = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/organizations/{organizationsId}/developers/{developersId}/apps/{appsId}/attributes
/// Returns a list of all developer app attributes.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `apigee_organizations_developers_apps_attributes_list_builder()` + `apigee_organizations_developers_apps_attributes_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_developers_apps_attributes_list(
    client: &SimpleHttpClient,
    parent: &str,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudApigeeV1Attributes>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = apigee_organizations_developers_apps_attributes_list_builder(client, parent)?;
    apigee_organizations_developers_apps_attributes_list_execute(builder)
}

/// GET v1/organizations/{organizationsId}/developers/{developersId}/apps/{appsId}/attributes/{attributesId}
/// Updates a developer app attribute. **Note**: OAuth access tokens and Key Management Service (KMS) entities (apps, developers, and API products) are cached for 180 seconds (current default). Any custom attributes associated with these entities are cached for at least 180 seconds after the entity is accessed at runtime. Therefore, an ExpiresIn element on the OAuthV2 policy won't be able to expire an access token in less than 180 seconds.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `apigee_organizations_developers_apps_attributes_update_developer_app_attribute_execute()` to send, or `apigee_organizations_developers_apps_attributes_update_developer_app_attribute` for simplest API.

pub fn apigee_organizations_developers_apps_attributes_update_developer_app_attribute_builder(
    client: &SimpleHttpClient,
    name: &str,
    body: &GoogleCloudApigeeV1Attribute,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://apigee.googleapis.com/v1/organizations/{}/developers/{}/apps/{}/attributes/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/organizations/{organizationsId}/developers/{developersId}/apps/{appsId}/attributes/{attributesId}
/// Updates a developer app attribute. **Note**: OAuth access tokens and Key Management Service (KMS) entities (apps, developers, and API products) are cached for 180 seconds (current default). Any custom attributes associated with these entities are cached for at least 180 seconds after the entity is accessed at runtime. Therefore, an ExpiresIn element on the OAuthV2 policy won't be able to expire an access token in less than 180 seconds.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `apigee_organizations_developers_apps_attributes_update_developer_app_attribute_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from apigee_organizations_developers_apps_attributes_update_developer_app_attribute_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_developers_apps_attributes_update_developer_app_attribute_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudApigeeV1Attribute>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudApigeeV1Attribute = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/organizations/{organizationsId}/developers/{developersId}/apps/{appsId}/attributes/{attributesId}
/// Updates a developer app attribute. **Note**: OAuth access tokens and Key Management Service (KMS) entities (apps, developers, and API products) are cached for 180 seconds (current default). Any custom attributes associated with these entities are cached for at least 180 seconds after the entity is accessed at runtime. Therefore, an ExpiresIn element on the OAuthV2 policy won't be able to expire an access token in less than 180 seconds.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `apigee_organizations_developers_apps_attributes_update_developer_app_attribute_builder()` + `apigee_organizations_developers_apps_attributes_update_developer_app_attribute_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_developers_apps_attributes_update_developer_app_attribute(
    client: &SimpleHttpClient,
    name: &str,
    body: &GoogleCloudApigeeV1Attribute,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudApigeeV1Attribute>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        apigee_organizations_developers_apps_attributes_update_developer_app_attribute_builder(
            client, name, body,
        )?;
    apigee_organizations_developers_apps_attributes_update_developer_app_attribute_execute(builder)
}

/// GET v1/organizations/{organizationsId}/developers/{developersId}/apps/{appsId}/keys
/// Creates a custom consumer key and secret for a developer app. This is particularly useful if you want to migrate existing consumer keys and secrets to Apigee from another system. Consumer keys and secrets can contain letters, numbers, underscores, and hyphens. No other special characters are allowed. To avoid service disruptions, a consumer key and secret should not exceed 2 KBs each. **Note**: When creating the consumer key and secret, an association to API products will not be made. Therefore, you should not specify the associated API products in your request. Instead, use the UpdateDeveloperAppKey API to make the association after the consumer key and secret are created. If a consumer key and secret already exist, you can keep them or delete them using the DeleteDeveloperAppKey API. **Note**: All keys start out with status=approved, even if status=revoked is passed when the key is created. To revoke a key, use the UpdateDeveloperAppKey API.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `apigee_organizations_developers_apps_keys_create_execute()` to send, or `apigee_organizations_developers_apps_keys_create` for simplest API.

pub fn apigee_organizations_developers_apps_keys_create_builder(
    client: &SimpleHttpClient,
    parent: &str,
    body: &GoogleCloudApigeeV1DeveloperAppKey,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://apigee.googleapis.com/v1/organizations/{}/developers/{}/apps/{}/keys",
        parent,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/organizations/{organizationsId}/developers/{developersId}/apps/{appsId}/keys
/// Creates a custom consumer key and secret for a developer app. This is particularly useful if you want to migrate existing consumer keys and secrets to Apigee from another system. Consumer keys and secrets can contain letters, numbers, underscores, and hyphens. No other special characters are allowed. To avoid service disruptions, a consumer key and secret should not exceed 2 KBs each. **Note**: When creating the consumer key and secret, an association to API products will not be made. Therefore, you should not specify the associated API products in your request. Instead, use the UpdateDeveloperAppKey API to make the association after the consumer key and secret are created. If a consumer key and secret already exist, you can keep them or delete them using the DeleteDeveloperAppKey API. **Note**: All keys start out with status=approved, even if status=revoked is passed when the key is created. To revoke a key, use the UpdateDeveloperAppKey API.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `apigee_organizations_developers_apps_keys_create_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from apigee_organizations_developers_apps_keys_create_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_developers_apps_keys_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudApigeeV1DeveloperAppKey>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudApigeeV1DeveloperAppKey = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/organizations/{organizationsId}/developers/{developersId}/apps/{appsId}/keys
/// Creates a custom consumer key and secret for a developer app. This is particularly useful if you want to migrate existing consumer keys and secrets to Apigee from another system. Consumer keys and secrets can contain letters, numbers, underscores, and hyphens. No other special characters are allowed. To avoid service disruptions, a consumer key and secret should not exceed 2 KBs each. **Note**: When creating the consumer key and secret, an association to API products will not be made. Therefore, you should not specify the associated API products in your request. Instead, use the UpdateDeveloperAppKey API to make the association after the consumer key and secret are created. If a consumer key and secret already exist, you can keep them or delete them using the DeleteDeveloperAppKey API. **Note**: All keys start out with status=approved, even if status=revoked is passed when the key is created. To revoke a key, use the UpdateDeveloperAppKey API.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `apigee_organizations_developers_apps_keys_create_builder()` + `apigee_organizations_developers_apps_keys_create_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_developers_apps_keys_create(
    client: &SimpleHttpClient,
    parent: &str,
    body: &GoogleCloudApigeeV1DeveloperAppKey,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudApigeeV1DeveloperAppKey>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = apigee_organizations_developers_apps_keys_create_builder(client, parent, body)?;
    apigee_organizations_developers_apps_keys_create_execute(builder)
}

/// GET v1/organizations/{organizationsId}/developers/{developersId}/apps/{appsId}/keys/{keysId}
/// Deletes an app's consumer key and removes all API products associated with the app. After the consumer key is deleted, it cannot be used to access any APIs. **Note**: After you delete a consumer key, you may want to: 1. Create a new consumer key and secret for the developer app using the CreateDeveloperAppKey API, and subsequently add an API product to the key using the UpdateDeveloperAppKey API. 2. Delete the developer app, if it is no longer required.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `apigee_organizations_developers_apps_keys_delete_execute()` to send, or `apigee_organizations_developers_apps_keys_delete` for simplest API.

pub fn apigee_organizations_developers_apps_keys_delete_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://apigee.googleapis.com/v1/organizations/{}/developers/{}/apps/{}/keys/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/organizations/{organizationsId}/developers/{developersId}/apps/{appsId}/keys/{keysId}
/// Deletes an app's consumer key and removes all API products associated with the app. After the consumer key is deleted, it cannot be used to access any APIs. **Note**: After you delete a consumer key, you may want to: 1. Create a new consumer key and secret for the developer app using the CreateDeveloperAppKey API, and subsequently add an API product to the key using the UpdateDeveloperAppKey API. 2. Delete the developer app, if it is no longer required.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `apigee_organizations_developers_apps_keys_delete_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from apigee_organizations_developers_apps_keys_delete_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_developers_apps_keys_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudApigeeV1DeveloperAppKey>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudApigeeV1DeveloperAppKey = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/organizations/{organizationsId}/developers/{developersId}/apps/{appsId}/keys/{keysId}
/// Deletes an app's consumer key and removes all API products associated with the app. After the consumer key is deleted, it cannot be used to access any APIs. **Note**: After you delete a consumer key, you may want to: 1. Create a new consumer key and secret for the developer app using the CreateDeveloperAppKey API, and subsequently add an API product to the key using the UpdateDeveloperAppKey API. 2. Delete the developer app, if it is no longer required.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `apigee_organizations_developers_apps_keys_delete_builder()` + `apigee_organizations_developers_apps_keys_delete_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_developers_apps_keys_delete(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudApigeeV1DeveloperAppKey>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = apigee_organizations_developers_apps_keys_delete_builder(client, name)?;
    apigee_organizations_developers_apps_keys_delete_execute(builder)
}

/// GET v1/organizations/{organizationsId}/developers/{developersId}/apps/{appsId}/keys/{keysId}
/// Gets details for a consumer key for a developer app, including the key and secret value, associated API products, and other information.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `apigee_organizations_developers_apps_keys_get_execute()` to send, or `apigee_organizations_developers_apps_keys_get` for simplest API.

pub fn apigee_organizations_developers_apps_keys_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://apigee.googleapis.com/v1/organizations/{}/developers/{}/apps/{}/keys/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/organizations/{organizationsId}/developers/{developersId}/apps/{appsId}/keys/{keysId}
/// Gets details for a consumer key for a developer app, including the key and secret value, associated API products, and other information.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `apigee_organizations_developers_apps_keys_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from apigee_organizations_developers_apps_keys_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_developers_apps_keys_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudApigeeV1DeveloperAppKey>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudApigeeV1DeveloperAppKey = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/organizations/{organizationsId}/developers/{developersId}/apps/{appsId}/keys/{keysId}
/// Gets details for a consumer key for a developer app, including the key and secret value, associated API products, and other information.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `apigee_organizations_developers_apps_keys_get_builder()` + `apigee_organizations_developers_apps_keys_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_developers_apps_keys_get(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudApigeeV1DeveloperAppKey>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = apigee_organizations_developers_apps_keys_get_builder(client, name)?;
    apigee_organizations_developers_apps_keys_get_execute(builder)
}

/// GET v1/organizations/{organizationsId}/developers/{developersId}/apps/{appsId}/keys/{keysId}
/// Updates the scope of an app. This API replaces the existing scopes with those specified in the request. Include or exclude any existing scopes that you want to retain or delete, respectively. The specified scopes must already be defined for the API products associated with the app. This API sets the scopes element under the `apiProducts` element in the attributes of the app.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `apigee_organizations_developers_apps_keys_replace_developer_app_key_execute()` to send, or `apigee_organizations_developers_apps_keys_replace_developer_app_key` for simplest API.

pub fn apigee_organizations_developers_apps_keys_replace_developer_app_key_builder(
    client: &SimpleHttpClient,
    name: &str,
    body: &GoogleCloudApigeeV1DeveloperAppKey,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://apigee.googleapis.com/v1/organizations/{}/developers/{}/apps/{}/keys/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/organizations/{organizationsId}/developers/{developersId}/apps/{appsId}/keys/{keysId}
/// Updates the scope of an app. This API replaces the existing scopes with those specified in the request. Include or exclude any existing scopes that you want to retain or delete, respectively. The specified scopes must already be defined for the API products associated with the app. This API sets the scopes element under the `apiProducts` element in the attributes of the app.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `apigee_organizations_developers_apps_keys_replace_developer_app_key_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from apigee_organizations_developers_apps_keys_replace_developer_app_key_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_developers_apps_keys_replace_developer_app_key_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudApigeeV1DeveloperAppKey>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudApigeeV1DeveloperAppKey = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/organizations/{organizationsId}/developers/{developersId}/apps/{appsId}/keys/{keysId}
/// Updates the scope of an app. This API replaces the existing scopes with those specified in the request. Include or exclude any existing scopes that you want to retain or delete, respectively. The specified scopes must already be defined for the API products associated with the app. This API sets the scopes element under the `apiProducts` element in the attributes of the app.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `apigee_organizations_developers_apps_keys_replace_developer_app_key_builder()` + `apigee_organizations_developers_apps_keys_replace_developer_app_key_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_developers_apps_keys_replace_developer_app_key(
    client: &SimpleHttpClient,
    name: &str,
    body: &GoogleCloudApigeeV1DeveloperAppKey,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudApigeeV1DeveloperAppKey>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = apigee_organizations_developers_apps_keys_replace_developer_app_key_builder(
        client, name, body,
    )?;
    apigee_organizations_developers_apps_keys_replace_developer_app_key_execute(builder)
}

/// GET v1/organizations/{organizationsId}/developers/{developersId}/apps/{appsId}/keys/{keysId}
/// Adds an API product to a developer app key, enabling the app that holds the key to access the API resources bundled in the API product. In addition, you can add attributes and scopes associated with the API product to the developer app key. The status of the key can be updated via "action" Query Parameter. None of the other fields can be updated via this API. This API replaces the existing attributes with those specified in the request. Include or exclude any existing attributes that you want to retain or delete, respectively. None of the other fields can be updated. You can use the same key to access all API products associated with the app.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `apigee_organizations_developers_apps_keys_update_developer_app_key_execute()` to send, or `apigee_organizations_developers_apps_keys_update_developer_app_key` for simplest API.

pub fn apigee_organizations_developers_apps_keys_update_developer_app_key_builder(
    client: &SimpleHttpClient,
    name: &str,
    action: Option<&str>,
    body: &GoogleCloudApigeeV1DeveloperAppKey,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://apigee.googleapis.com/v1/organizations/{}/developers/{}/apps/{}/keys/{}",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = action {
        query_parts.push(format!("action={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/organizations/{organizationsId}/developers/{developersId}/apps/{appsId}/keys/{keysId}
/// Adds an API product to a developer app key, enabling the app that holds the key to access the API resources bundled in the API product. In addition, you can add attributes and scopes associated with the API product to the developer app key. The status of the key can be updated via "action" Query Parameter. None of the other fields can be updated via this API. This API replaces the existing attributes with those specified in the request. Include or exclude any existing attributes that you want to retain or delete, respectively. None of the other fields can be updated. You can use the same key to access all API products associated with the app.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `apigee_organizations_developers_apps_keys_update_developer_app_key_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from apigee_organizations_developers_apps_keys_update_developer_app_key_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_developers_apps_keys_update_developer_app_key_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudApigeeV1DeveloperAppKey>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudApigeeV1DeveloperAppKey = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/organizations/{organizationsId}/developers/{developersId}/apps/{appsId}/keys/{keysId}
/// Adds an API product to a developer app key, enabling the app that holds the key to access the API resources bundled in the API product. In addition, you can add attributes and scopes associated with the API product to the developer app key. The status of the key can be updated via "action" Query Parameter. None of the other fields can be updated via this API. This API replaces the existing attributes with those specified in the request. Include or exclude any existing attributes that you want to retain or delete, respectively. None of the other fields can be updated. You can use the same key to access all API products associated with the app.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `apigee_organizations_developers_apps_keys_update_developer_app_key_builder()` + `apigee_organizations_developers_apps_keys_update_developer_app_key_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_developers_apps_keys_update_developer_app_key(
    client: &SimpleHttpClient,
    name: &str,
    action: Option<&str>,
    body: &GoogleCloudApigeeV1DeveloperAppKey,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudApigeeV1DeveloperAppKey>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = apigee_organizations_developers_apps_keys_update_developer_app_key_builder(
        client, name, action, body,
    )?;
    apigee_organizations_developers_apps_keys_update_developer_app_key_execute(builder)
}

/// GET v1/organizations/{organizationsId}/developers/{developersId}/apps/{appsId}/keys/{keysId}/apiproducts/{apiproductsId}
/// Removes an API product from an app's consumer key. After the API product is removed, the app cannot access the API resources defined in that API product. **Note**: The consumer key is not removed, only its association with the API product.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `apigee_organizations_developers_apps_keys_apiproducts_delete_execute()` to send, or `apigee_organizations_developers_apps_keys_apiproducts_delete` for simplest API.

pub fn apigee_organizations_developers_apps_keys_apiproducts_delete_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://apigee.googleapis.com/v1/organizations/{}/developers/{}/apps/{}/keys/{}/apiproducts/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/organizations/{organizationsId}/developers/{developersId}/apps/{appsId}/keys/{keysId}/apiproducts/{apiproductsId}
/// Removes an API product from an app's consumer key. After the API product is removed, the app cannot access the API resources defined in that API product. **Note**: The consumer key is not removed, only its association with the API product.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `apigee_organizations_developers_apps_keys_apiproducts_delete_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from apigee_organizations_developers_apps_keys_apiproducts_delete_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_developers_apps_keys_apiproducts_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudApigeeV1DeveloperAppKey>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudApigeeV1DeveloperAppKey = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/organizations/{organizationsId}/developers/{developersId}/apps/{appsId}/keys/{keysId}/apiproducts/{apiproductsId}
/// Removes an API product from an app's consumer key. After the API product is removed, the app cannot access the API resources defined in that API product. **Note**: The consumer key is not removed, only its association with the API product.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `apigee_organizations_developers_apps_keys_apiproducts_delete_builder()` + `apigee_organizations_developers_apps_keys_apiproducts_delete_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_developers_apps_keys_apiproducts_delete(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudApigeeV1DeveloperAppKey>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        apigee_organizations_developers_apps_keys_apiproducts_delete_builder(client, name)?;
    apigee_organizations_developers_apps_keys_apiproducts_delete_execute(builder)
}

/// GET v1/organizations/{organizationsId}/developers/{developersId}/apps/{appsId}/keys/{keysId}/apiproducts/{apiproductsId}
/// Approves or revokes the consumer key for an API product. After a consumer key is approved, the app can use it to access APIs. A consumer key that is revoked or pending cannot be used to access an API. Any access tokens associated with a revoked consumer key will remain active. However, Apigee checks the status of the consumer key and if set to revoked will not allow access to the API.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `apigee_organizations_developers_apps_keys_apiproducts_update_developer_app_key_api_product_execute()` to send, or `apigee_organizations_developers_apps_keys_apiproducts_update_developer_app_key_api_product` for simplest API.

pub fn apigee_organizations_developers_apps_keys_apiproducts_update_developer_app_key_api_product_builder(
    client: &SimpleHttpClient,
    name: &str,
    action: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://apigee.googleapis.com/v1/organizations/{}/developers/{}/apps/{}/keys/{}/apiproducts/{}",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = action {
        query_parts.push(format!("action={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/organizations/{organizationsId}/developers/{developersId}/apps/{appsId}/keys/{keysId}/apiproducts/{apiproductsId}
/// Approves or revokes the consumer key for an API product. After a consumer key is approved, the app can use it to access APIs. A consumer key that is revoked or pending cannot be used to access an API. Any access tokens associated with a revoked consumer key will remain active. However, Apigee checks the status of the consumer key and if set to revoked will not allow access to the API.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `apigee_organizations_developers_apps_keys_apiproducts_update_developer_app_key_api_product_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from apigee_organizations_developers_apps_keys_apiproducts_update_developer_app_key_api_product_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_developers_apps_keys_apiproducts_update_developer_app_key_api_product_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleProtobufEmpty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/organizations/{organizationsId}/developers/{developersId}/apps/{appsId}/keys/{keysId}/apiproducts/{apiproductsId}
/// Approves or revokes the consumer key for an API product. After a consumer key is approved, the app can use it to access APIs. A consumer key that is revoked or pending cannot be used to access an API. Any access tokens associated with a revoked consumer key will remain active. However, Apigee checks the status of the consumer key and if set to revoked will not allow access to the API.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `apigee_organizations_developers_apps_keys_apiproducts_update_developer_app_key_api_product_builder()` + `apigee_organizations_developers_apps_keys_apiproducts_update_developer_app_key_api_product_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_developers_apps_keys_apiproducts_update_developer_app_key_api_product(
    client: &SimpleHttpClient,
    name: &str,
    action: Option<&str>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = apigee_organizations_developers_apps_keys_apiproducts_update_developer_app_key_api_product_builder(client, name, action)?;
    apigee_organizations_developers_apps_keys_apiproducts_update_developer_app_key_api_product_execute(builder)
}

/// GET v1/organizations/{organizationsId}/developers/{developersId}/apps/{appsId}/keys/create
/// Creates a custom consumer key and secret for a developer app. This is particularly useful if you want to migrate existing consumer keys and secrets to Apigee from another system. Consumer keys and secrets can contain letters, numbers, underscores, and hyphens. No other special characters are allowed. To avoid service disruptions, a consumer key and secret should not exceed 2 KBs each. **Note**: When creating the consumer key and secret, an association to API products will not be made. Therefore, you should not specify the associated API products in your request. Instead, use the UpdateDeveloperAppKey API to make the association after the consumer key and secret are created. If a consumer key and secret already exist, you can keep them or delete them using the DeleteDeveloperAppKey API. **Note**: All keys start out with status=approved, even if status=revoked is passed when the key is created. To revoke a key, use the UpdateDeveloperAppKey API.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `apigee_organizations_developers_apps_keys_create_create_execute()` to send, or `apigee_organizations_developers_apps_keys_create_create` for simplest API.

pub fn apigee_organizations_developers_apps_keys_create_create_builder(
    client: &SimpleHttpClient,
    parent: &str,
    body: &GoogleCloudApigeeV1DeveloperAppKey,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://apigee.googleapis.com/v1/organizations/{}/developers/{}/apps/{}/keys/create",
        parent,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/organizations/{organizationsId}/developers/{developersId}/apps/{appsId}/keys/create
/// Creates a custom consumer key and secret for a developer app. This is particularly useful if you want to migrate existing consumer keys and secrets to Apigee from another system. Consumer keys and secrets can contain letters, numbers, underscores, and hyphens. No other special characters are allowed. To avoid service disruptions, a consumer key and secret should not exceed 2 KBs each. **Note**: When creating the consumer key and secret, an association to API products will not be made. Therefore, you should not specify the associated API products in your request. Instead, use the UpdateDeveloperAppKey API to make the association after the consumer key and secret are created. If a consumer key and secret already exist, you can keep them or delete them using the DeleteDeveloperAppKey API. **Note**: All keys start out with status=approved, even if status=revoked is passed when the key is created. To revoke a key, use the UpdateDeveloperAppKey API.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `apigee_organizations_developers_apps_keys_create_create_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from apigee_organizations_developers_apps_keys_create_create_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_developers_apps_keys_create_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudApigeeV1DeveloperAppKey>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudApigeeV1DeveloperAppKey = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/organizations/{organizationsId}/developers/{developersId}/apps/{appsId}/keys/create
/// Creates a custom consumer key and secret for a developer app. This is particularly useful if you want to migrate existing consumer keys and secrets to Apigee from another system. Consumer keys and secrets can contain letters, numbers, underscores, and hyphens. No other special characters are allowed. To avoid service disruptions, a consumer key and secret should not exceed 2 KBs each. **Note**: When creating the consumer key and secret, an association to API products will not be made. Therefore, you should not specify the associated API products in your request. Instead, use the UpdateDeveloperAppKey API to make the association after the consumer key and secret are created. If a consumer key and secret already exist, you can keep them or delete them using the DeleteDeveloperAppKey API. **Note**: All keys start out with status=approved, even if status=revoked is passed when the key is created. To revoke a key, use the UpdateDeveloperAppKey API.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `apigee_organizations_developers_apps_keys_create_create_builder()` + `apigee_organizations_developers_apps_keys_create_create_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_developers_apps_keys_create_create(
    client: &SimpleHttpClient,
    parent: &str,
    body: &GoogleCloudApigeeV1DeveloperAppKey,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudApigeeV1DeveloperAppKey>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        apigee_organizations_developers_apps_keys_create_create_builder(client, parent, body)?;
    apigee_organizations_developers_apps_keys_create_create_execute(builder)
}

/// GET v1/organizations/{organizationsId}/developers/{developersId}/attributes/{attributesId}
/// Deletes a developer attribute.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `apigee_organizations_developers_attributes_delete_execute()` to send, or `apigee_organizations_developers_attributes_delete` for simplest API.

pub fn apigee_organizations_developers_attributes_delete_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://apigee.googleapis.com/v1/organizations/{}/developers/{}/attributes/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/organizations/{organizationsId}/developers/{developersId}/attributes/{attributesId}
/// Deletes a developer attribute.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `apigee_organizations_developers_attributes_delete_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from apigee_organizations_developers_attributes_delete_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_developers_attributes_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudApigeeV1Attribute>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudApigeeV1Attribute = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/organizations/{organizationsId}/developers/{developersId}/attributes/{attributesId}
/// Deletes a developer attribute.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `apigee_organizations_developers_attributes_delete_builder()` + `apigee_organizations_developers_attributes_delete_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_developers_attributes_delete(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudApigeeV1Attribute>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = apigee_organizations_developers_attributes_delete_builder(client, name)?;
    apigee_organizations_developers_attributes_delete_execute(builder)
}

/// GET v1/organizations/{organizationsId}/developers/{developersId}/attributes/{attributesId}
/// Returns the value of the specified developer attribute.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `apigee_organizations_developers_attributes_get_execute()` to send, or `apigee_organizations_developers_attributes_get` for simplest API.

pub fn apigee_organizations_developers_attributes_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://apigee.googleapis.com/v1/organizations/{}/developers/{}/attributes/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/organizations/{organizationsId}/developers/{developersId}/attributes/{attributesId}
/// Returns the value of the specified developer attribute.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `apigee_organizations_developers_attributes_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from apigee_organizations_developers_attributes_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_developers_attributes_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudApigeeV1Attribute>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudApigeeV1Attribute = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/organizations/{organizationsId}/developers/{developersId}/attributes/{attributesId}
/// Returns the value of the specified developer attribute.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `apigee_organizations_developers_attributes_get_builder()` + `apigee_organizations_developers_attributes_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_developers_attributes_get(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudApigeeV1Attribute>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = apigee_organizations_developers_attributes_get_builder(client, name)?;
    apigee_organizations_developers_attributes_get_execute(builder)
}

/// GET v1/organizations/{organizationsId}/developers/{developersId}/attributes
/// Returns a list of all developer attributes.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `apigee_organizations_developers_attributes_list_execute()` to send, or `apigee_organizations_developers_attributes_list` for simplest API.

pub fn apigee_organizations_developers_attributes_list_builder(
    client: &SimpleHttpClient,
    parent: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://apigee.googleapis.com/v1/organizations/{}/developers/{}/attributes",
        parent,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/organizations/{organizationsId}/developers/{developersId}/attributes
/// Returns a list of all developer attributes.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `apigee_organizations_developers_attributes_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from apigee_organizations_developers_attributes_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_developers_attributes_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudApigeeV1Attributes>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudApigeeV1Attributes = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/organizations/{organizationsId}/developers/{developersId}/attributes
/// Returns a list of all developer attributes.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `apigee_organizations_developers_attributes_list_builder()` + `apigee_organizations_developers_attributes_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_developers_attributes_list(
    client: &SimpleHttpClient,
    parent: &str,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudApigeeV1Attributes>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = apigee_organizations_developers_attributes_list_builder(client, parent)?;
    apigee_organizations_developers_attributes_list_execute(builder)
}

/// GET v1/organizations/{organizationsId}/developers/{developersId}/attributes/{attributesId}
/// Updates a developer attribute. **Note**: OAuth access tokens and Key Management Service (KMS) entities (apps, developers, and API products) are cached for 180 seconds (default). Any custom attributes associated with these entities are cached for at least 180 seconds after the entity is accessed at runtime. Therefore, an ExpiresIn element on the OAuthV2 policy won't be able to expire an access token in less than 180 seconds.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `apigee_organizations_developers_attributes_update_developer_attribute_execute()` to send, or `apigee_organizations_developers_attributes_update_developer_attribute` for simplest API.

pub fn apigee_organizations_developers_attributes_update_developer_attribute_builder(
    client: &SimpleHttpClient,
    name: &str,
    body: &GoogleCloudApigeeV1Attribute,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://apigee.googleapis.com/v1/organizations/{}/developers/{}/attributes/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/organizations/{organizationsId}/developers/{developersId}/attributes/{attributesId}
/// Updates a developer attribute. **Note**: OAuth access tokens and Key Management Service (KMS) entities (apps, developers, and API products) are cached for 180 seconds (default). Any custom attributes associated with these entities are cached for at least 180 seconds after the entity is accessed at runtime. Therefore, an ExpiresIn element on the OAuthV2 policy won't be able to expire an access token in less than 180 seconds.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `apigee_organizations_developers_attributes_update_developer_attribute_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from apigee_organizations_developers_attributes_update_developer_attribute_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_developers_attributes_update_developer_attribute_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudApigeeV1Attribute>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudApigeeV1Attribute = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/organizations/{organizationsId}/developers/{developersId}/attributes/{attributesId}
/// Updates a developer attribute. **Note**: OAuth access tokens and Key Management Service (KMS) entities (apps, developers, and API products) are cached for 180 seconds (default). Any custom attributes associated with these entities are cached for at least 180 seconds after the entity is accessed at runtime. Therefore, an ExpiresIn element on the OAuthV2 policy won't be able to expire an access token in less than 180 seconds.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `apigee_organizations_developers_attributes_update_developer_attribute_builder()` + `apigee_organizations_developers_attributes_update_developer_attribute_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_developers_attributes_update_developer_attribute(
    client: &SimpleHttpClient,
    name: &str,
    body: &GoogleCloudApigeeV1Attribute,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudApigeeV1Attribute>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = apigee_organizations_developers_attributes_update_developer_attribute_builder(
        client, name, body,
    )?;
    apigee_organizations_developers_attributes_update_developer_attribute_execute(builder)
}

/// GET v1/organizations/{organizationsId}/developers/{developersId}/balance:adjust
/// Adjust the prepaid balance for the developer. This API will be used in scenarios where the developer has been under-charged or over-charged.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `apigee_organizations_developers_balance_adjust_execute()` to send, or `apigee_organizations_developers_balance_adjust` for simplest API.

pub fn apigee_organizations_developers_balance_adjust_builder(
    client: &SimpleHttpClient,
    name: &str,
    body: &GoogleCloudApigeeV1AdjustDeveloperBalanceRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://apigee.googleapis.com/v1/organizations/{}/developers/{}/balance:adjust",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/organizations/{organizationsId}/developers/{developersId}/balance:adjust
/// Adjust the prepaid balance for the developer. This API will be used in scenarios where the developer has been under-charged or over-charged.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `apigee_organizations_developers_balance_adjust_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from apigee_organizations_developers_balance_adjust_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_developers_balance_adjust_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudApigeeV1DeveloperBalance>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudApigeeV1DeveloperBalance = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/organizations/{organizationsId}/developers/{developersId}/balance:adjust
/// Adjust the prepaid balance for the developer. This API will be used in scenarios where the developer has been under-charged or over-charged.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `apigee_organizations_developers_balance_adjust_builder()` + `apigee_organizations_developers_balance_adjust_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_developers_balance_adjust(
    client: &SimpleHttpClient,
    name: &str,
    body: &GoogleCloudApigeeV1AdjustDeveloperBalanceRequest,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudApigeeV1DeveloperBalance>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = apigee_organizations_developers_balance_adjust_builder(client, name, body)?;
    apigee_organizations_developers_balance_adjust_execute(builder)
}

/// GET v1/organizations/{organizationsId}/developers/{developersId}/balance:credit
/// Credits the account balance for the developer.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `apigee_organizations_developers_balance_credit_execute()` to send, or `apigee_organizations_developers_balance_credit` for simplest API.

pub fn apigee_organizations_developers_balance_credit_builder(
    client: &SimpleHttpClient,
    name: &str,
    body: &GoogleCloudApigeeV1CreditDeveloperBalanceRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://apigee.googleapis.com/v1/organizations/{}/developers/{}/balance:credit",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/organizations/{organizationsId}/developers/{developersId}/balance:credit
/// Credits the account balance for the developer.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `apigee_organizations_developers_balance_credit_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from apigee_organizations_developers_balance_credit_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_developers_balance_credit_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudApigeeV1DeveloperBalance>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudApigeeV1DeveloperBalance = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/organizations/{organizationsId}/developers/{developersId}/balance:credit
/// Credits the account balance for the developer.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `apigee_organizations_developers_balance_credit_builder()` + `apigee_organizations_developers_balance_credit_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_developers_balance_credit(
    client: &SimpleHttpClient,
    name: &str,
    body: &GoogleCloudApigeeV1CreditDeveloperBalanceRequest,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudApigeeV1DeveloperBalance>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = apigee_organizations_developers_balance_credit_builder(client, name, body)?;
    apigee_organizations_developers_balance_credit_execute(builder)
}

/// GET v1/organizations/{organizationsId}/developers/{developersId}/subscriptions
/// Creates a subscription to an API product.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `apigee_organizations_developers_subscriptions_create_execute()` to send, or `apigee_organizations_developers_subscriptions_create` for simplest API.

pub fn apigee_organizations_developers_subscriptions_create_builder(
    client: &SimpleHttpClient,
    parent: &str,
    body: &GoogleCloudApigeeV1DeveloperSubscription,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://apigee.googleapis.com/v1/organizations/{}/developers/{}/subscriptions",
        parent,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/organizations/{organizationsId}/developers/{developersId}/subscriptions
/// Creates a subscription to an API product.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `apigee_organizations_developers_subscriptions_create_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from apigee_organizations_developers_subscriptions_create_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_developers_subscriptions_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudApigeeV1DeveloperSubscription>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudApigeeV1DeveloperSubscription = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/organizations/{organizationsId}/developers/{developersId}/subscriptions
/// Creates a subscription to an API product.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `apigee_organizations_developers_subscriptions_create_builder()` + `apigee_organizations_developers_subscriptions_create_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_developers_subscriptions_create(
    client: &SimpleHttpClient,
    parent: &str,
    body: &GoogleCloudApigeeV1DeveloperSubscription,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudApigeeV1DeveloperSubscription>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        apigee_organizations_developers_subscriptions_create_builder(client, parent, body)?;
    apigee_organizations_developers_subscriptions_create_execute(builder)
}

/// GET v1/organizations/{organizationsId}/developers/{developersId}/subscriptions/{subscriptionsId}:expire
/// Expires an API product subscription immediately.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `apigee_organizations_developers_subscriptions_expire_execute()` to send, or `apigee_organizations_developers_subscriptions_expire` for simplest API.

pub fn apigee_organizations_developers_subscriptions_expire_builder(
    client: &SimpleHttpClient,
    name: &str,
    body: &GoogleCloudApigeeV1ExpireDeveloperSubscriptionRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://apigee.googleapis.com/v1/organizations/{}/developers/{}/subscriptions/{}:expire",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/organizations/{organizationsId}/developers/{developersId}/subscriptions/{subscriptionsId}:expire
/// Expires an API product subscription immediately.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `apigee_organizations_developers_subscriptions_expire_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from apigee_organizations_developers_subscriptions_expire_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_developers_subscriptions_expire_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudApigeeV1DeveloperSubscription>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudApigeeV1DeveloperSubscription = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/organizations/{organizationsId}/developers/{developersId}/subscriptions/{subscriptionsId}:expire
/// Expires an API product subscription immediately.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `apigee_organizations_developers_subscriptions_expire_builder()` + `apigee_organizations_developers_subscriptions_expire_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_developers_subscriptions_expire(
    client: &SimpleHttpClient,
    name: &str,
    body: &GoogleCloudApigeeV1ExpireDeveloperSubscriptionRequest,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudApigeeV1DeveloperSubscription>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = apigee_organizations_developers_subscriptions_expire_builder(client, name, body)?;
    apigee_organizations_developers_subscriptions_expire_execute(builder)
}

/// GET v1/organizations/{organizationsId}/developers/{developersId}/subscriptions/{subscriptionsId}
/// Gets details for an API product subscription.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `apigee_organizations_developers_subscriptions_get_execute()` to send, or `apigee_organizations_developers_subscriptions_get` for simplest API.

pub fn apigee_organizations_developers_subscriptions_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://apigee.googleapis.com/v1/organizations/{}/developers/{}/subscriptions/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/organizations/{organizationsId}/developers/{developersId}/subscriptions/{subscriptionsId}
/// Gets details for an API product subscription.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `apigee_organizations_developers_subscriptions_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from apigee_organizations_developers_subscriptions_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_developers_subscriptions_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudApigeeV1DeveloperSubscription>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudApigeeV1DeveloperSubscription = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/organizations/{organizationsId}/developers/{developersId}/subscriptions/{subscriptionsId}
/// Gets details for an API product subscription.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `apigee_organizations_developers_subscriptions_get_builder()` + `apigee_organizations_developers_subscriptions_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_developers_subscriptions_get(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudApigeeV1DeveloperSubscription>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = apigee_organizations_developers_subscriptions_get_builder(client, name)?;
    apigee_organizations_developers_subscriptions_get_execute(builder)
}

/// GET v1/organizations/{organizationsId}/developers/{developersId}/subscriptions
/// Lists all API product subscriptions for a developer.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `apigee_organizations_developers_subscriptions_list_execute()` to send, or `apigee_organizations_developers_subscriptions_list` for simplest API.

pub fn apigee_organizations_developers_subscriptions_list_builder(
    client: &SimpleHttpClient,
    parent: &str,
    count: Option<i32>,
    startKey: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://apigee.googleapis.com/v1/organizations/{}/developers/{}/subscriptions",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = count {
        query_parts.push(format!("count={}", val));
    }
    if let Some(val) = startKey {
        query_parts.push(format!("startKey={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/organizations/{organizationsId}/developers/{developersId}/subscriptions
/// Lists all API product subscriptions for a developer.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `apigee_organizations_developers_subscriptions_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from apigee_organizations_developers_subscriptions_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_developers_subscriptions_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleCloudApigeeV1ListDeveloperSubscriptionsResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudApigeeV1ListDeveloperSubscriptionsResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/organizations/{organizationsId}/developers/{developersId}/subscriptions
/// Lists all API product subscriptions for a developer.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `apigee_organizations_developers_subscriptions_list_builder()` + `apigee_organizations_developers_subscriptions_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_developers_subscriptions_list(
    client: &SimpleHttpClient,
    parent: &str,
    count: Option<i32>,
    startKey: Option<&str>,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleCloudApigeeV1ListDeveloperSubscriptionsResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = apigee_organizations_developers_subscriptions_list_builder(
        client, parent, count, startKey,
    )?;
    apigee_organizations_developers_subscriptions_list_execute(builder)
}

/// GET v1/organizations/{organizationsId}/dnsZones
/// Creates a new DNS zone.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `apigee_organizations_dns_zones_create_execute()` to send, or `apigee_organizations_dns_zones_create` for simplest API.

pub fn apigee_organizations_dns_zones_create_builder(
    client: &SimpleHttpClient,
    parent: &str,
    dnsZoneId: Option<&str>,
    body: &GoogleCloudApigeeV1DnsZone,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://apigee.googleapis.com/v1/organizations/{}/dnsZones",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = dnsZoneId {
        query_parts.push(format!("dnsZoneId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/organizations/{organizationsId}/dnsZones
/// Creates a new DNS zone.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `apigee_organizations_dns_zones_create_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from apigee_organizations_dns_zones_create_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_dns_zones_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/organizations/{organizationsId}/dnsZones
/// Creates a new DNS zone.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `apigee_organizations_dns_zones_create_builder()` + `apigee_organizations_dns_zones_create_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_dns_zones_create(
    client: &SimpleHttpClient,
    parent: &str,
    dnsZoneId: Option<&str>,
    body: &GoogleCloudApigeeV1DnsZone,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = apigee_organizations_dns_zones_create_builder(client, parent, dnsZoneId, body)?;
    apigee_organizations_dns_zones_create_execute(builder)
}

/// GET v1/organizations/{organizationsId}/dnsZones/{dnsZonesId}
/// Deletes a previously created DNS zone.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `apigee_organizations_dns_zones_delete_execute()` to send, or `apigee_organizations_dns_zones_delete` for simplest API.

pub fn apigee_organizations_dns_zones_delete_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://apigee.googleapis.com/v1/organizations/{}/dnsZones/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/organizations/{organizationsId}/dnsZones/{dnsZonesId}
/// Deletes a previously created DNS zone.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `apigee_organizations_dns_zones_delete_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from apigee_organizations_dns_zones_delete_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_dns_zones_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/organizations/{organizationsId}/dnsZones/{dnsZonesId}
/// Deletes a previously created DNS zone.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `apigee_organizations_dns_zones_delete_builder()` + `apigee_organizations_dns_zones_delete_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_dns_zones_delete(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = apigee_organizations_dns_zones_delete_builder(client, name)?;
    apigee_organizations_dns_zones_delete_execute(builder)
}

/// GET v1/organizations/{organizationsId}/dnsZones/{dnsZonesId}
/// Fetches the representation of an existing DNS zone.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `apigee_organizations_dns_zones_get_execute()` to send, or `apigee_organizations_dns_zones_get` for simplest API.

pub fn apigee_organizations_dns_zones_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://apigee.googleapis.com/v1/organizations/{}/dnsZones/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/organizations/{organizationsId}/dnsZones/{dnsZonesId}
/// Fetches the representation of an existing DNS zone.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `apigee_organizations_dns_zones_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from apigee_organizations_dns_zones_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_dns_zones_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudApigeeV1DnsZone>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudApigeeV1DnsZone = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/organizations/{organizationsId}/dnsZones/{dnsZonesId}
/// Fetches the representation of an existing DNS zone.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `apigee_organizations_dns_zones_get_builder()` + `apigee_organizations_dns_zones_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_dns_zones_get(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudApigeeV1DnsZone>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = apigee_organizations_dns_zones_get_builder(client, name)?;
    apigee_organizations_dns_zones_get_execute(builder)
}

/// GET v1/organizations/{organizationsId}/dnsZones
/// Enumerates DNS zones that have been created but not yet deleted.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `apigee_organizations_dns_zones_list_execute()` to send, or `apigee_organizations_dns_zones_list` for simplest API.

pub fn apigee_organizations_dns_zones_list_builder(
    client: &SimpleHttpClient,
    parent: &str,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://apigee.googleapis.com/v1/organizations/{}/dnsZones",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/organizations/{organizationsId}/dnsZones
/// Enumerates DNS zones that have been created but not yet deleted.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `apigee_organizations_dns_zones_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from apigee_organizations_dns_zones_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_dns_zones_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudApigeeV1ListDnsZonesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudApigeeV1ListDnsZonesResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/organizations/{organizationsId}/dnsZones
/// Enumerates DNS zones that have been created but not yet deleted.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `apigee_organizations_dns_zones_list_builder()` + `apigee_organizations_dns_zones_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_dns_zones_list(
    client: &SimpleHttpClient,
    parent: &str,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudApigeeV1ListDnsZonesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = apigee_organizations_dns_zones_list_builder(client, parent, pageSize, pageToken)?;
    apigee_organizations_dns_zones_list_execute(builder)
}

/// GET v1/organizations/{organizationsId}/endpointAttachments
/// Creates an endpoint attachment. **Note:** Not supported for Apigee hybrid.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `apigee_organizations_endpoint_attachments_create_execute()` to send, or `apigee_organizations_endpoint_attachments_create` for simplest API.

pub fn apigee_organizations_endpoint_attachments_create_builder(
    client: &SimpleHttpClient,
    parent: &str,
    endpointAttachmentId: Option<&str>,
    body: &GoogleCloudApigeeV1EndpointAttachment,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://apigee.googleapis.com/v1/organizations/{}/endpointAttachments",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = endpointAttachmentId {
        query_parts.push(format!("endpointAttachmentId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/organizations/{organizationsId}/endpointAttachments
/// Creates an endpoint attachment. **Note:** Not supported for Apigee hybrid.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `apigee_organizations_endpoint_attachments_create_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from apigee_organizations_endpoint_attachments_create_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_endpoint_attachments_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/organizations/{organizationsId}/endpointAttachments
/// Creates an endpoint attachment. **Note:** Not supported for Apigee hybrid.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `apigee_organizations_endpoint_attachments_create_builder()` + `apigee_organizations_endpoint_attachments_create_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_endpoint_attachments_create(
    client: &SimpleHttpClient,
    parent: &str,
    endpointAttachmentId: Option<&str>,
    body: &GoogleCloudApigeeV1EndpointAttachment,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = apigee_organizations_endpoint_attachments_create_builder(
        client,
        parent,
        endpointAttachmentId,
        body,
    )?;
    apigee_organizations_endpoint_attachments_create_execute(builder)
}

/// GET v1/organizations/{organizationsId}/endpointAttachments/{endpointAttachmentsId}
/// Deletes an endpoint attachment.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `apigee_organizations_endpoint_attachments_delete_execute()` to send, or `apigee_organizations_endpoint_attachments_delete` for simplest API.

pub fn apigee_organizations_endpoint_attachments_delete_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://apigee.googleapis.com/v1/organizations/{}/endpointAttachments/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/organizations/{organizationsId}/endpointAttachments/{endpointAttachmentsId}
/// Deletes an endpoint attachment.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `apigee_organizations_endpoint_attachments_delete_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from apigee_organizations_endpoint_attachments_delete_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_endpoint_attachments_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/organizations/{organizationsId}/endpointAttachments/{endpointAttachmentsId}
/// Deletes an endpoint attachment.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `apigee_organizations_endpoint_attachments_delete_builder()` + `apigee_organizations_endpoint_attachments_delete_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_endpoint_attachments_delete(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = apigee_organizations_endpoint_attachments_delete_builder(client, name)?;
    apigee_organizations_endpoint_attachments_delete_execute(builder)
}

/// GET v1/organizations/{organizationsId}/endpointAttachments/{endpointAttachmentsId}
/// Gets the endpoint attachment.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `apigee_organizations_endpoint_attachments_get_execute()` to send, or `apigee_organizations_endpoint_attachments_get` for simplest API.

pub fn apigee_organizations_endpoint_attachments_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://apigee.googleapis.com/v1/organizations/{}/endpointAttachments/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/organizations/{organizationsId}/endpointAttachments/{endpointAttachmentsId}
/// Gets the endpoint attachment.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `apigee_organizations_endpoint_attachments_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from apigee_organizations_endpoint_attachments_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_endpoint_attachments_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudApigeeV1EndpointAttachment>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudApigeeV1EndpointAttachment = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/organizations/{organizationsId}/endpointAttachments/{endpointAttachmentsId}
/// Gets the endpoint attachment.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `apigee_organizations_endpoint_attachments_get_builder()` + `apigee_organizations_endpoint_attachments_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_endpoint_attachments_get(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudApigeeV1EndpointAttachment>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = apigee_organizations_endpoint_attachments_get_builder(client, name)?;
    apigee_organizations_endpoint_attachments_get_execute(builder)
}

/// GET v1/organizations/{organizationsId}/endpointAttachments
/// Lists the endpoint attachments in an organization.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `apigee_organizations_endpoint_attachments_list_execute()` to send, or `apigee_organizations_endpoint_attachments_list` for simplest API.

pub fn apigee_organizations_endpoint_attachments_list_builder(
    client: &SimpleHttpClient,
    parent: &str,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://apigee.googleapis.com/v1/organizations/{}/endpointAttachments",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/organizations/{organizationsId}/endpointAttachments
/// Lists the endpoint attachments in an organization.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `apigee_organizations_endpoint_attachments_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from apigee_organizations_endpoint_attachments_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_endpoint_attachments_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudApigeeV1ListEndpointAttachmentsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudApigeeV1ListEndpointAttachmentsResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/organizations/{organizationsId}/endpointAttachments
/// Lists the endpoint attachments in an organization.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `apigee_organizations_endpoint_attachments_list_builder()` + `apigee_organizations_endpoint_attachments_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_endpoint_attachments_list(
    client: &SimpleHttpClient,
    parent: &str,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudApigeeV1ListEndpointAttachmentsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = apigee_organizations_endpoint_attachments_list_builder(
        client, parent, pageSize, pageToken,
    )?;
    apigee_organizations_endpoint_attachments_list_execute(builder)
}

/// GET v1/organizations/{organizationsId}/envgroups
/// Creates a new environment group.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `apigee_organizations_envgroups_create_execute()` to send, or `apigee_organizations_envgroups_create` for simplest API.

pub fn apigee_organizations_envgroups_create_builder(
    client: &SimpleHttpClient,
    parent: &str,
    name: Option<&str>,
    body: &GoogleCloudApigeeV1EnvironmentGroup,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://apigee.googleapis.com/v1/organizations/{}/envgroups",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = name {
        query_parts.push(format!("name={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/organizations/{organizationsId}/envgroups
/// Creates a new environment group.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `apigee_organizations_envgroups_create_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from apigee_organizations_envgroups_create_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_envgroups_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/organizations/{organizationsId}/envgroups
/// Creates a new environment group.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `apigee_organizations_envgroups_create_builder()` + `apigee_organizations_envgroups_create_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_envgroups_create(
    client: &SimpleHttpClient,
    parent: &str,
    name: Option<&str>,
    body: &GoogleCloudApigeeV1EnvironmentGroup,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = apigee_organizations_envgroups_create_builder(client, parent, name, body)?;
    apigee_organizations_envgroups_create_execute(builder)
}

/// GET v1/organizations/{organizationsId}/envgroups/{envgroupsId}
/// Deletes an environment group.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `apigee_organizations_envgroups_delete_execute()` to send, or `apigee_organizations_envgroups_delete` for simplest API.

pub fn apigee_organizations_envgroups_delete_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://apigee.googleapis.com/v1/organizations/{}/envgroups/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/organizations/{organizationsId}/envgroups/{envgroupsId}
/// Deletes an environment group.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `apigee_organizations_envgroups_delete_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from apigee_organizations_envgroups_delete_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_envgroups_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/organizations/{organizationsId}/envgroups/{envgroupsId}
/// Deletes an environment group.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `apigee_organizations_envgroups_delete_builder()` + `apigee_organizations_envgroups_delete_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_envgroups_delete(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = apigee_organizations_envgroups_delete_builder(client, name)?;
    apigee_organizations_envgroups_delete_execute(builder)
}

/// GET v1/organizations/{organizationsId}/envgroups/{envgroupsId}
/// Gets an environment group.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `apigee_organizations_envgroups_get_execute()` to send, or `apigee_organizations_envgroups_get` for simplest API.

pub fn apigee_organizations_envgroups_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://apigee.googleapis.com/v1/organizations/{}/envgroups/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/organizations/{organizationsId}/envgroups/{envgroupsId}
/// Gets an environment group.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `apigee_organizations_envgroups_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from apigee_organizations_envgroups_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_envgroups_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudApigeeV1EnvironmentGroup>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudApigeeV1EnvironmentGroup = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/organizations/{organizationsId}/envgroups/{envgroupsId}
/// Gets an environment group.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `apigee_organizations_envgroups_get_builder()` + `apigee_organizations_envgroups_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_envgroups_get(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudApigeeV1EnvironmentGroup>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = apigee_organizations_envgroups_get_builder(client, name)?;
    apigee_organizations_envgroups_get_execute(builder)
}

/// GET v1/organizations/{organizationsId}/envgroups/{envgroupsId}/deployedIngressConfig
/// Gets the deployed ingress configuration for an environment group.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `apigee_organizations_envgroups_get_deployed_ingress_config_execute()` to send, or `apigee_organizations_envgroups_get_deployed_ingress_config` for simplest API.

pub fn apigee_organizations_envgroups_get_deployed_ingress_config_builder(
    client: &SimpleHttpClient,
    name: &str,
    view: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://apigee.googleapis.com/v1/organizations/{}/envgroups/{}/deployedIngressConfig",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = view {
        query_parts.push(format!("view={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/organizations/{organizationsId}/envgroups/{envgroupsId}/deployedIngressConfig
/// Gets the deployed ingress configuration for an environment group.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `apigee_organizations_envgroups_get_deployed_ingress_config_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from apigee_organizations_envgroups_get_deployed_ingress_config_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_envgroups_get_deployed_ingress_config_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudApigeeV1EnvironmentGroupConfig>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudApigeeV1EnvironmentGroupConfig = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/organizations/{organizationsId}/envgroups/{envgroupsId}/deployedIngressConfig
/// Gets the deployed ingress configuration for an environment group.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `apigee_organizations_envgroups_get_deployed_ingress_config_builder()` + `apigee_organizations_envgroups_get_deployed_ingress_config_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_envgroups_get_deployed_ingress_config(
    client: &SimpleHttpClient,
    name: &str,
    view: Option<&str>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudApigeeV1EnvironmentGroupConfig>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        apigee_organizations_envgroups_get_deployed_ingress_config_builder(client, name, view)?;
    apigee_organizations_envgroups_get_deployed_ingress_config_execute(builder)
}

/// GET v1/organizations/{organizationsId}/envgroups
/// Lists all environment groups.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `apigee_organizations_envgroups_list_execute()` to send, or `apigee_organizations_envgroups_list` for simplest API.

pub fn apigee_organizations_envgroups_list_builder(
    client: &SimpleHttpClient,
    parent: &str,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://apigee.googleapis.com/v1/organizations/{}/envgroups",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/organizations/{organizationsId}/envgroups
/// Lists all environment groups.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `apigee_organizations_envgroups_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from apigee_organizations_envgroups_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_envgroups_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudApigeeV1ListEnvironmentGroupsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudApigeeV1ListEnvironmentGroupsResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/organizations/{organizationsId}/envgroups
/// Lists all environment groups.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `apigee_organizations_envgroups_list_builder()` + `apigee_organizations_envgroups_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_envgroups_list(
    client: &SimpleHttpClient,
    parent: &str,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudApigeeV1ListEnvironmentGroupsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = apigee_organizations_envgroups_list_builder(client, parent, pageSize, pageToken)?;
    apigee_organizations_envgroups_list_execute(builder)
}

/// GET v1/organizations/{organizationsId}/envgroups/{envgroupsId}
/// Updates an environment group.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `apigee_organizations_envgroups_patch_execute()` to send, or `apigee_organizations_envgroups_patch` for simplest API.

pub fn apigee_organizations_envgroups_patch_builder(
    client: &SimpleHttpClient,
    name: &str,
    updateMask: Option<&str>,
    body: &GoogleCloudApigeeV1EnvironmentGroup,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://apigee.googleapis.com/v1/organizations/{}/envgroups/{}",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = updateMask {
        query_parts.push(format!("updateMask={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/organizations/{organizationsId}/envgroups/{envgroupsId}
/// Updates an environment group.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `apigee_organizations_envgroups_patch_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from apigee_organizations_envgroups_patch_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_envgroups_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/organizations/{organizationsId}/envgroups/{envgroupsId}
/// Updates an environment group.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `apigee_organizations_envgroups_patch_builder()` + `apigee_organizations_envgroups_patch_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_envgroups_patch(
    client: &SimpleHttpClient,
    name: &str,
    updateMask: Option<&str>,
    body: &GoogleCloudApigeeV1EnvironmentGroup,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = apigee_organizations_envgroups_patch_builder(client, name, updateMask, body)?;
    apigee_organizations_envgroups_patch_execute(builder)
}

/// GET v1/organizations/{organizationsId}/envgroups/{envgroupsId}/attachments
/// Creates a new attachment of an environment to an environment group.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `apigee_organizations_envgroups_attachments_create_execute()` to send, or `apigee_organizations_envgroups_attachments_create` for simplest API.

pub fn apigee_organizations_envgroups_attachments_create_builder(
    client: &SimpleHttpClient,
    parent: &str,
    body: &GoogleCloudApigeeV1EnvironmentGroupAttachment,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://apigee.googleapis.com/v1/organizations/{}/envgroups/{}/attachments",
        parent,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/organizations/{organizationsId}/envgroups/{envgroupsId}/attachments
/// Creates a new attachment of an environment to an environment group.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `apigee_organizations_envgroups_attachments_create_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from apigee_organizations_envgroups_attachments_create_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_envgroups_attachments_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/organizations/{organizationsId}/envgroups/{envgroupsId}/attachments
/// Creates a new attachment of an environment to an environment group.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `apigee_organizations_envgroups_attachments_create_builder()` + `apigee_organizations_envgroups_attachments_create_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_envgroups_attachments_create(
    client: &SimpleHttpClient,
    parent: &str,
    body: &GoogleCloudApigeeV1EnvironmentGroupAttachment,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = apigee_organizations_envgroups_attachments_create_builder(client, parent, body)?;
    apigee_organizations_envgroups_attachments_create_execute(builder)
}

/// GET v1/organizations/{organizationsId}/envgroups/{envgroupsId}/attachments/{attachmentsId}
/// Deletes an environment group attachment.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `apigee_organizations_envgroups_attachments_delete_execute()` to send, or `apigee_organizations_envgroups_attachments_delete` for simplest API.

pub fn apigee_organizations_envgroups_attachments_delete_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://apigee.googleapis.com/v1/organizations/{}/envgroups/{}/attachments/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/organizations/{organizationsId}/envgroups/{envgroupsId}/attachments/{attachmentsId}
/// Deletes an environment group attachment.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `apigee_organizations_envgroups_attachments_delete_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from apigee_organizations_envgroups_attachments_delete_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_envgroups_attachments_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/organizations/{organizationsId}/envgroups/{envgroupsId}/attachments/{attachmentsId}
/// Deletes an environment group attachment.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `apigee_organizations_envgroups_attachments_delete_builder()` + `apigee_organizations_envgroups_attachments_delete_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_envgroups_attachments_delete(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = apigee_organizations_envgroups_attachments_delete_builder(client, name)?;
    apigee_organizations_envgroups_attachments_delete_execute(builder)
}

/// GET v1/organizations/{organizationsId}/envgroups/{envgroupsId}/attachments/{attachmentsId}
/// Gets an environment group attachment.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `apigee_organizations_envgroups_attachments_get_execute()` to send, or `apigee_organizations_envgroups_attachments_get` for simplest API.

pub fn apigee_organizations_envgroups_attachments_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://apigee.googleapis.com/v1/organizations/{}/envgroups/{}/attachments/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/organizations/{organizationsId}/envgroups/{envgroupsId}/attachments/{attachmentsId}
/// Gets an environment group attachment.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `apigee_organizations_envgroups_attachments_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from apigee_organizations_envgroups_attachments_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_envgroups_attachments_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudApigeeV1EnvironmentGroupAttachment>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudApigeeV1EnvironmentGroupAttachment =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/organizations/{organizationsId}/envgroups/{envgroupsId}/attachments/{attachmentsId}
/// Gets an environment group attachment.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `apigee_organizations_envgroups_attachments_get_builder()` + `apigee_organizations_envgroups_attachments_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_envgroups_attachments_get(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudApigeeV1EnvironmentGroupAttachment>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = apigee_organizations_envgroups_attachments_get_builder(client, name)?;
    apigee_organizations_envgroups_attachments_get_execute(builder)
}

/// GET v1/organizations/{organizationsId}/envgroups/{envgroupsId}/attachments
/// Lists all attachments of an environment group.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `apigee_organizations_envgroups_attachments_list_execute()` to send, or `apigee_organizations_envgroups_attachments_list` for simplest API.

pub fn apigee_organizations_envgroups_attachments_list_builder(
    client: &SimpleHttpClient,
    parent: &str,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://apigee.googleapis.com/v1/organizations/{}/envgroups/{}/attachments",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/organizations/{organizationsId}/envgroups/{envgroupsId}/attachments
/// Lists all attachments of an environment group.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `apigee_organizations_envgroups_attachments_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from apigee_organizations_envgroups_attachments_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_envgroups_attachments_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleCloudApigeeV1ListEnvironmentGroupAttachmentsResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudApigeeV1ListEnvironmentGroupAttachmentsResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/organizations/{organizationsId}/envgroups/{envgroupsId}/attachments
/// Lists all attachments of an environment group.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `apigee_organizations_envgroups_attachments_list_builder()` + `apigee_organizations_envgroups_attachments_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_envgroups_attachments_list(
    client: &SimpleHttpClient,
    parent: &str,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleCloudApigeeV1ListEnvironmentGroupAttachmentsResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = apigee_organizations_envgroups_attachments_list_builder(
        client, parent, pageSize, pageToken,
    )?;
    apigee_organizations_envgroups_attachments_list_execute(builder)
}

/// GET v1/organizations/{organizationsId}/environments
/// Creates an environment in an organization.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `apigee_organizations_environments_create_execute()` to send, or `apigee_organizations_environments_create` for simplest API.

pub fn apigee_organizations_environments_create_builder(
    client: &SimpleHttpClient,
    parent: &str,
    name: Option<&str>,
    body: &GoogleCloudApigeeV1Environment,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://apigee.googleapis.com/v1/organizations/{}/environments",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = name {
        query_parts.push(format!("name={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/organizations/{organizationsId}/environments
/// Creates an environment in an organization.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `apigee_organizations_environments_create_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from apigee_organizations_environments_create_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_environments_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/organizations/{organizationsId}/environments
/// Creates an environment in an organization.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `apigee_organizations_environments_create_builder()` + `apigee_organizations_environments_create_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_environments_create(
    client: &SimpleHttpClient,
    parent: &str,
    name: Option<&str>,
    body: &GoogleCloudApigeeV1Environment,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = apigee_organizations_environments_create_builder(client, parent, name, body)?;
    apigee_organizations_environments_create_execute(builder)
}

/// GET v1/organizations/{organizationsId}/environments/{environmentsId}
/// Deletes an environment from an organization. **Warning: You must delete all key value maps and key value entries before you delete an environment.** Otherwise, if you re-create the environment the key value map entry operations will encounter `encryption/decryption` discrepancies.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `apigee_organizations_environments_delete_execute()` to send, or `apigee_organizations_environments_delete` for simplest API.

pub fn apigee_organizations_environments_delete_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://apigee.googleapis.com/v1/organizations/{}/environments/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/organizations/{organizationsId}/environments/{environmentsId}
/// Deletes an environment from an organization. **Warning: You must delete all key value maps and key value entries before you delete an environment.** Otherwise, if you re-create the environment the key value map entry operations will encounter `encryption/decryption` discrepancies.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `apigee_organizations_environments_delete_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from apigee_organizations_environments_delete_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_environments_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/organizations/{organizationsId}/environments/{environmentsId}
/// Deletes an environment from an organization. **Warning: You must delete all key value maps and key value entries before you delete an environment.** Otherwise, if you re-create the environment the key value map entry operations will encounter `encryption/decryption` discrepancies.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `apigee_organizations_environments_delete_builder()` + `apigee_organizations_environments_delete_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_environments_delete(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = apigee_organizations_environments_delete_builder(client, name)?;
    apigee_organizations_environments_delete_execute(builder)
}

/// GET v1/organizations/{organizationsId}/environments/{environmentsId}
/// Gets environment details.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `apigee_organizations_environments_get_execute()` to send, or `apigee_organizations_environments_get` for simplest API.

pub fn apigee_organizations_environments_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://apigee.googleapis.com/v1/organizations/{}/environments/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/organizations/{organizationsId}/environments/{environmentsId}
/// Gets environment details.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `apigee_organizations_environments_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from apigee_organizations_environments_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_environments_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudApigeeV1Environment>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudApigeeV1Environment = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/organizations/{organizationsId}/environments/{environmentsId}
/// Gets environment details.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `apigee_organizations_environments_get_builder()` + `apigee_organizations_environments_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_environments_get(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudApigeeV1Environment>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = apigee_organizations_environments_get_builder(client, name)?;
    apigee_organizations_environments_get_execute(builder)
}

/// GET v1/organizations/{organizationsId}/environments/{environmentsId}/addonsConfig
/// Gets the add-ons config of an environment.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `apigee_organizations_environments_get_addons_config_execute()` to send, or `apigee_organizations_environments_get_addons_config` for simplest API.

pub fn apigee_organizations_environments_get_addons_config_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://apigee.googleapis.com/v1/organizations/{}/environments/{}/addonsConfig",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/organizations/{organizationsId}/environments/{environmentsId}/addonsConfig
/// Gets the add-ons config of an environment.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `apigee_organizations_environments_get_addons_config_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from apigee_organizations_environments_get_addons_config_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_environments_get_addons_config_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudApigeeV1AddonsConfig>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudApigeeV1AddonsConfig = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/organizations/{organizationsId}/environments/{environmentsId}/addonsConfig
/// Gets the add-ons config of an environment.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `apigee_organizations_environments_get_addons_config_builder()` + `apigee_organizations_environments_get_addons_config_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_environments_get_addons_config(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudApigeeV1AddonsConfig>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = apigee_organizations_environments_get_addons_config_builder(client, name)?;
    apigee_organizations_environments_get_addons_config_execute(builder)
}

/// GET v1/organizations/{organizationsId}/environments/{environmentsId}/apiSecurityRuntimeConfig
/// Gets the API Security runtime configuration for an environment. This named ApiSecurityRuntimeConfig to prevent conflicts with ApiSecurityConfig from addon config.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `apigee_organizations_environments_get_api_security_runtime_config_execute()` to send, or `apigee_organizations_environments_get_api_security_runtime_config` for simplest API.

pub fn apigee_organizations_environments_get_api_security_runtime_config_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://apigee.googleapis.com/v1/organizations/{}/environments/{}/apiSecurityRuntimeConfig",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/organizations/{organizationsId}/environments/{environmentsId}/apiSecurityRuntimeConfig
/// Gets the API Security runtime configuration for an environment. This named ApiSecurityRuntimeConfig to prevent conflicts with ApiSecurityConfig from addon config.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `apigee_organizations_environments_get_api_security_runtime_config_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from apigee_organizations_environments_get_api_security_runtime_config_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_environments_get_api_security_runtime_config_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudApigeeV1ApiSecurityRuntimeConfig>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudApigeeV1ApiSecurityRuntimeConfig =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/organizations/{organizationsId}/environments/{environmentsId}/apiSecurityRuntimeConfig
/// Gets the API Security runtime configuration for an environment. This named ApiSecurityRuntimeConfig to prevent conflicts with ApiSecurityConfig from addon config.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `apigee_organizations_environments_get_api_security_runtime_config_builder()` + `apigee_organizations_environments_get_api_security_runtime_config_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_environments_get_api_security_runtime_config(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudApigeeV1ApiSecurityRuntimeConfig>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        apigee_organizations_environments_get_api_security_runtime_config_builder(client, name)?;
    apigee_organizations_environments_get_api_security_runtime_config_execute(builder)
}

/// GET v1/organizations/{organizationsId}/environments/{environmentsId}/debugmask
/// Gets the debug mask singleton resource for an environment.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `apigee_organizations_environments_get_debugmask_execute()` to send, or `apigee_organizations_environments_get_debugmask` for simplest API.

pub fn apigee_organizations_environments_get_debugmask_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://apigee.googleapis.com/v1/organizations/{}/environments/{}/debugmask",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/organizations/{organizationsId}/environments/{environmentsId}/debugmask
/// Gets the debug mask singleton resource for an environment.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `apigee_organizations_environments_get_debugmask_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from apigee_organizations_environments_get_debugmask_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_environments_get_debugmask_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudApigeeV1DebugMask>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudApigeeV1DebugMask = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/organizations/{organizationsId}/environments/{environmentsId}/debugmask
/// Gets the debug mask singleton resource for an environment.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `apigee_organizations_environments_get_debugmask_builder()` + `apigee_organizations_environments_get_debugmask_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_environments_get_debugmask(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudApigeeV1DebugMask>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = apigee_organizations_environments_get_debugmask_builder(client, name)?;
    apigee_organizations_environments_get_debugmask_execute(builder)
}

/// GET v1/organizations/{organizationsId}/environments/{environmentsId}/deployedConfig
/// Gets the deployed configuration for an environment.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `apigee_organizations_environments_get_deployed_config_execute()` to send, or `apigee_organizations_environments_get_deployed_config` for simplest API.

pub fn apigee_organizations_environments_get_deployed_config_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://apigee.googleapis.com/v1/organizations/{}/environments/{}/deployedConfig",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/organizations/{organizationsId}/environments/{environmentsId}/deployedConfig
/// Gets the deployed configuration for an environment.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `apigee_organizations_environments_get_deployed_config_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from apigee_organizations_environments_get_deployed_config_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_environments_get_deployed_config_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudApigeeV1EnvironmentConfig>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudApigeeV1EnvironmentConfig = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/organizations/{organizationsId}/environments/{environmentsId}/deployedConfig
/// Gets the deployed configuration for an environment.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `apigee_organizations_environments_get_deployed_config_builder()` + `apigee_organizations_environments_get_deployed_config_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_environments_get_deployed_config(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudApigeeV1EnvironmentConfig>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = apigee_organizations_environments_get_deployed_config_builder(client, name)?;
    apigee_organizations_environments_get_deployed_config_execute(builder)
}

/// GET v1/organizations/{organizationsId}/environments/{environmentsId}:getIamPolicy
/// Gets the IAM policy on an environment. For more information, see [Manage users, roles, and permissions using the API](<https://cloud.google.`com/apigee/docs/api-platform/system-administration/manage-users-roles`>). You must have the apigee.environments.`getIamPolicy` permission to call this API.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `apigee_organizations_environments_get_iam_policy_execute()` to send, or `apigee_organizations_environments_get_iam_policy` for simplest API.

pub fn apigee_organizations_environments_get_iam_policy_builder(
    client: &SimpleHttpClient,
    resource: &str,
    options_requestedPolicyVersion: Option<i32>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://apigee.googleapis.com/v1/organizations/{}/environments/{}:getIamPolicy",
        resource,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = options_requestedPolicyVersion {
        query_parts.push(format!("options_requestedPolicyVersion={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/organizations/{organizationsId}/environments/{environmentsId}:getIamPolicy
/// Gets the IAM policy on an environment. For more information, see [Manage users, roles, and permissions using the API](<https://cloud.google.`com/apigee/docs/api-platform/system-administration/manage-users-roles`>). You must have the apigee.environments.`getIamPolicy` permission to call this API.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `apigee_organizations_environments_get_iam_policy_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from apigee_organizations_environments_get_iam_policy_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_environments_get_iam_policy_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleIamV1Policy>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleIamV1Policy = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/organizations/{organizationsId}/environments/{environmentsId}:getIamPolicy
/// Gets the IAM policy on an environment. For more information, see [Manage users, roles, and permissions using the API](<https://cloud.google.`com/apigee/docs/api-platform/system-administration/manage-users-roles`>). You must have the apigee.environments.`getIamPolicy` permission to call this API.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `apigee_organizations_environments_get_iam_policy_builder()` + `apigee_organizations_environments_get_iam_policy_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_environments_get_iam_policy(
    client: &SimpleHttpClient,
    resource: &str,
    options_requestedPolicyVersion: Option<i32>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleIamV1Policy>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = apigee_organizations_environments_get_iam_policy_builder(
        client,
        resource,
        options_requestedPolicyVersion,
    )?;
    apigee_organizations_environments_get_iam_policy_execute(builder)
}

/// GET v1/organizations/{organizationsId}/environments/{environmentsId}/securityActionsConfig
/// GetSecurityActionConfig returns the current SecurityActions configuration.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `apigee_organizations_environments_get_security_actions_config_execute()` to send, or `apigee_organizations_environments_get_security_actions_config` for simplest API.

pub fn apigee_organizations_environments_get_security_actions_config_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://apigee.googleapis.com/v1/organizations/{}/environments/{}/securityActionsConfig",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/organizations/{organizationsId}/environments/{environmentsId}/securityActionsConfig
/// GetSecurityActionConfig returns the current SecurityActions configuration.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `apigee_organizations_environments_get_security_actions_config_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from apigee_organizations_environments_get_security_actions_config_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_environments_get_security_actions_config_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudApigeeV1SecurityActionsConfig>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudApigeeV1SecurityActionsConfig = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/organizations/{organizationsId}/environments/{environmentsId}/securityActionsConfig
/// GetSecurityActionConfig returns the current SecurityActions configuration.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `apigee_organizations_environments_get_security_actions_config_builder()` + `apigee_organizations_environments_get_security_actions_config_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_environments_get_security_actions_config(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudApigeeV1SecurityActionsConfig>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        apigee_organizations_environments_get_security_actions_config_builder(client, name)?;
    apigee_organizations_environments_get_security_actions_config_execute(builder)
}

/// GET v1/organizations/{organizationsId}/environments/{environmentsId}/traceConfig
/// Get distributed trace configuration in an environment.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `apigee_organizations_environments_get_trace_config_execute()` to send, or `apigee_organizations_environments_get_trace_config` for simplest API.

pub fn apigee_organizations_environments_get_trace_config_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://apigee.googleapis.com/v1/organizations/{}/environments/{}/traceConfig",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/organizations/{organizationsId}/environments/{environmentsId}/traceConfig
/// Get distributed trace configuration in an environment.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `apigee_organizations_environments_get_trace_config_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from apigee_organizations_environments_get_trace_config_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_environments_get_trace_config_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudApigeeV1TraceConfig>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudApigeeV1TraceConfig = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/organizations/{organizationsId}/environments/{environmentsId}/traceConfig
/// Get distributed trace configuration in an environment.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `apigee_organizations_environments_get_trace_config_builder()` + `apigee_organizations_environments_get_trace_config_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_environments_get_trace_config(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudApigeeV1TraceConfig>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = apigee_organizations_environments_get_trace_config_builder(client, name)?;
    apigee_organizations_environments_get_trace_config_execute(builder)
}

/// GET v1/organizations/{organizationsId}/environments/{environmentsId}
/// Updates properties for an Apigee environment with patch semantics using a field mask. **Note:** Not supported for Apigee hybrid.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `apigee_organizations_environments_modify_environment_execute()` to send, or `apigee_organizations_environments_modify_environment` for simplest API.

pub fn apigee_organizations_environments_modify_environment_builder(
    client: &SimpleHttpClient,
    name: &str,
    updateMask: Option<&str>,
    body: &GoogleCloudApigeeV1Environment,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://apigee.googleapis.com/v1/organizations/{}/environments/{}",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = updateMask {
        query_parts.push(format!("updateMask={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/organizations/{organizationsId}/environments/{environmentsId}
/// Updates properties for an Apigee environment with patch semantics using a field mask. **Note:** Not supported for Apigee hybrid.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `apigee_organizations_environments_modify_environment_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from apigee_organizations_environments_modify_environment_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_environments_modify_environment_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/organizations/{organizationsId}/environments/{environmentsId}
/// Updates properties for an Apigee environment with patch semantics using a field mask. **Note:** Not supported for Apigee hybrid.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `apigee_organizations_environments_modify_environment_builder()` + `apigee_organizations_environments_modify_environment_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_environments_modify_environment(
    client: &SimpleHttpClient,
    name: &str,
    updateMask: Option<&str>,
    body: &GoogleCloudApigeeV1Environment,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = apigee_organizations_environments_modify_environment_builder(
        client, name, updateMask, body,
    )?;
    apigee_organizations_environments_modify_environment_execute(builder)
}

/// GET v1/organizations/{organizationsId}/environments/{environmentsId}:setIamPolicy
/// Sets the IAM policy on an environment, if the policy already exists it will be replaced. For more information, see [Manage users, roles, and permissions using the API](<https://cloud.google.`com/apigee/docs/api-platform/system-administration/manage-users-roles`>). You must have the apigee.environments.`setIamPolicy` permission to call this API.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `apigee_organizations_environments_set_iam_policy_execute()` to send, or `apigee_organizations_environments_set_iam_policy` for simplest API.

pub fn apigee_organizations_environments_set_iam_policy_builder(
    client: &SimpleHttpClient,
    resource: &str,
    body: &GoogleIamV1SetIamPolicyRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://apigee.googleapis.com/v1/organizations/{}/environments/{}:setIamPolicy",
        resource,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/organizations/{organizationsId}/environments/{environmentsId}:setIamPolicy
/// Sets the IAM policy on an environment, if the policy already exists it will be replaced. For more information, see [Manage users, roles, and permissions using the API](<https://cloud.google.`com/apigee/docs/api-platform/system-administration/manage-users-roles`>). You must have the apigee.environments.`setIamPolicy` permission to call this API.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `apigee_organizations_environments_set_iam_policy_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from apigee_organizations_environments_set_iam_policy_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_environments_set_iam_policy_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleIamV1Policy>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleIamV1Policy = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/organizations/{organizationsId}/environments/{environmentsId}:setIamPolicy
/// Sets the IAM policy on an environment, if the policy already exists it will be replaced. For more information, see [Manage users, roles, and permissions using the API](<https://cloud.google.`com/apigee/docs/api-platform/system-administration/manage-users-roles`>). You must have the apigee.environments.`setIamPolicy` permission to call this API.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `apigee_organizations_environments_set_iam_policy_builder()` + `apigee_organizations_environments_set_iam_policy_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_environments_set_iam_policy(
    client: &SimpleHttpClient,
    resource: &str,
    body: &GoogleIamV1SetIamPolicyRequest,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleIamV1Policy>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = apigee_organizations_environments_set_iam_policy_builder(client, resource, body)?;
    apigee_organizations_environments_set_iam_policy_execute(builder)
}

/// GET v1/organizations/{organizationsId}/environments/{environmentsId}:subscribe
/// Creates a subscription for the environment's P`ub/Sub` topic. The server will assign a random name for this subscription. The "name" and "push_config" must *not* be specified.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `apigee_organizations_environments_subscribe_execute()` to send, or `apigee_organizations_environments_subscribe` for simplest API.

pub fn apigee_organizations_environments_subscribe_builder(
    client: &SimpleHttpClient,
    parent: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://apigee.googleapis.com/v1/organizations/{}/environments/{}:subscribe",
        parent,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/organizations/{organizationsId}/environments/{environmentsId}:subscribe
/// Creates a subscription for the environment's P`ub/Sub` topic. The server will assign a random name for this subscription. The "name" and "push_config" must *not* be specified.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `apigee_organizations_environments_subscribe_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from apigee_organizations_environments_subscribe_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_environments_subscribe_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudApigeeV1Subscription>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudApigeeV1Subscription = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/organizations/{organizationsId}/environments/{environmentsId}:subscribe
/// Creates a subscription for the environment's P`ub/Sub` topic. The server will assign a random name for this subscription. The "name" and "push_config" must *not* be specified.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `apigee_organizations_environments_subscribe_builder()` + `apigee_organizations_environments_subscribe_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_environments_subscribe(
    client: &SimpleHttpClient,
    parent: &str,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudApigeeV1Subscription>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = apigee_organizations_environments_subscribe_builder(client, parent)?;
    apigee_organizations_environments_subscribe_execute(builder)
}

/// GET v1/organizations/{organizationsId}/environments/{environmentsId}:testIamPermissions
/// Tests the permissions of a user on an environment, and returns a subset of permissions that the user has on the environment. If the environment does not exist, an empty permission set is returned (a NOT_FOUND error is not returned).
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `apigee_organizations_environments_test_iam_permissions_execute()` to send, or `apigee_organizations_environments_test_iam_permissions` for simplest API.

pub fn apigee_organizations_environments_test_iam_permissions_builder(
    client: &SimpleHttpClient,
    resource: &str,
    body: &GoogleIamV1TestIamPermissionsRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://apigee.googleapis.com/v1/organizations/{}/environments/{}:testIamPermissions",
        resource,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/organizations/{organizationsId}/environments/{environmentsId}:testIamPermissions
/// Tests the permissions of a user on an environment, and returns a subset of permissions that the user has on the environment. If the environment does not exist, an empty permission set is returned (a NOT_FOUND error is not returned).
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `apigee_organizations_environments_test_iam_permissions_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from apigee_organizations_environments_test_iam_permissions_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_environments_test_iam_permissions_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleIamV1TestIamPermissionsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleIamV1TestIamPermissionsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/organizations/{organizationsId}/environments/{environmentsId}:testIamPermissions
/// Tests the permissions of a user on an environment, and returns a subset of permissions that the user has on the environment. If the environment does not exist, an empty permission set is returned (a NOT_FOUND error is not returned).
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `apigee_organizations_environments_test_iam_permissions_builder()` + `apigee_organizations_environments_test_iam_permissions_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_environments_test_iam_permissions(
    client: &SimpleHttpClient,
    resource: &str,
    body: &GoogleIamV1TestIamPermissionsRequest,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleIamV1TestIamPermissionsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        apigee_organizations_environments_test_iam_permissions_builder(client, resource, body)?;
    apigee_organizations_environments_test_iam_permissions_execute(builder)
}

/// GET v1/organizations/{organizationsId}/environments/{environmentsId}:unsubscribe
/// Deletes a subscription for the environment's P`ub/Sub` topic.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `apigee_organizations_environments_unsubscribe_execute()` to send, or `apigee_organizations_environments_unsubscribe` for simplest API.

pub fn apigee_organizations_environments_unsubscribe_builder(
    client: &SimpleHttpClient,
    parent: &str,
    body: &GoogleCloudApigeeV1Subscription,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://apigee.googleapis.com/v1/organizations/{}/environments/{}:unsubscribe",
        parent,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/organizations/{organizationsId}/environments/{environmentsId}:unsubscribe
/// Deletes a subscription for the environment's P`ub/Sub` topic.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `apigee_organizations_environments_unsubscribe_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from apigee_organizations_environments_unsubscribe_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_environments_unsubscribe_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleProtobufEmpty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/organizations/{organizationsId}/environments/{environmentsId}:unsubscribe
/// Deletes a subscription for the environment's P`ub/Sub` topic.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `apigee_organizations_environments_unsubscribe_builder()` + `apigee_organizations_environments_unsubscribe_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_environments_unsubscribe(
    client: &SimpleHttpClient,
    parent: &str,
    body: &GoogleCloudApigeeV1Subscription,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = apigee_organizations_environments_unsubscribe_builder(client, parent, body)?;
    apigee_organizations_environments_unsubscribe_execute(builder)
}

/// GET v1/organizations/{organizationsId}/environments/{environmentsId}
/// Updates an existing environment. When updating properties, you must pass all existing properties to the API, even if they are not being changed. If you omit properties from the payload, the properties are removed. To get the current list of properties for the environment, use the [Get Environment API](get). **Note**: Both PUT and POST methods are supported for updating an existing environment.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `apigee_organizations_environments_update_execute()` to send, or `apigee_organizations_environments_update` for simplest API.

pub fn apigee_organizations_environments_update_builder(
    client: &SimpleHttpClient,
    name: &str,
    body: &GoogleCloudApigeeV1Environment,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://apigee.googleapis.com/v1/organizations/{}/environments/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/organizations/{organizationsId}/environments/{environmentsId}
/// Updates an existing environment. When updating properties, you must pass all existing properties to the API, even if they are not being changed. If you omit properties from the payload, the properties are removed. To get the current list of properties for the environment, use the [Get Environment API](get). **Note**: Both PUT and POST methods are supported for updating an existing environment.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `apigee_organizations_environments_update_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from apigee_organizations_environments_update_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_environments_update_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudApigeeV1Environment>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudApigeeV1Environment = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/organizations/{organizationsId}/environments/{environmentsId}
/// Updates an existing environment. When updating properties, you must pass all existing properties to the API, even if they are not being changed. If you omit properties from the payload, the properties are removed. To get the current list of properties for the environment, use the [Get Environment API](get). **Note**: Both PUT and POST methods are supported for updating an existing environment.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `apigee_organizations_environments_update_builder()` + `apigee_organizations_environments_update_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_environments_update(
    client: &SimpleHttpClient,
    name: &str,
    body: &GoogleCloudApigeeV1Environment,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudApigeeV1Environment>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = apigee_organizations_environments_update_builder(client, name, body)?;
    apigee_organizations_environments_update_execute(builder)
}

/// GET v1/organizations/{organizationsId}/environments/{environmentsId}/debugmask
/// Updates the debug mask singleton resource for an environment.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `apigee_organizations_environments_update_debugmask_execute()` to send, or `apigee_organizations_environments_update_debugmask` for simplest API.

pub fn apigee_organizations_environments_update_debugmask_builder(
    client: &SimpleHttpClient,
    name: &str,
    replaceRepeatedFields: Option<bool>,
    updateMask: Option<&str>,
    body: &GoogleCloudApigeeV1DebugMask,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://apigee.googleapis.com/v1/organizations/{}/environments/{}/debugmask",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = replaceRepeatedFields {
        query_parts.push(format!("replaceRepeatedFields={}", val));
    }
    if let Some(val) = updateMask {
        query_parts.push(format!("updateMask={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/organizations/{organizationsId}/environments/{environmentsId}/debugmask
/// Updates the debug mask singleton resource for an environment.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `apigee_organizations_environments_update_debugmask_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from apigee_organizations_environments_update_debugmask_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_environments_update_debugmask_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudApigeeV1DebugMask>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudApigeeV1DebugMask = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/organizations/{organizationsId}/environments/{environmentsId}/debugmask
/// Updates the debug mask singleton resource for an environment.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `apigee_organizations_environments_update_debugmask_builder()` + `apigee_organizations_environments_update_debugmask_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_environments_update_debugmask(
    client: &SimpleHttpClient,
    name: &str,
    replaceRepeatedFields: Option<bool>,
    updateMask: Option<&str>,
    body: &GoogleCloudApigeeV1DebugMask,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudApigeeV1DebugMask>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = apigee_organizations_environments_update_debugmask_builder(
        client,
        name,
        replaceRepeatedFields,
        updateMask,
        body,
    )?;
    apigee_organizations_environments_update_debugmask_execute(builder)
}

/// GET v1/organizations/{organizationsId}/environments/{environmentsId}
/// Updates an existing environment. When updating properties, you must pass all existing properties to the API, even if they are not being changed. If you omit properties from the payload, the properties are removed. To get the current list of properties for the environment, use the [Get Environment API](get). **Note**: Both PUT and POST methods are supported for updating an existing environment.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `apigee_organizations_environments_update_environment_execute()` to send, or `apigee_organizations_environments_update_environment` for simplest API.

pub fn apigee_organizations_environments_update_environment_builder(
    client: &SimpleHttpClient,
    name: &str,
    body: &GoogleCloudApigeeV1Environment,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://apigee.googleapis.com/v1/organizations/{}/environments/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/organizations/{organizationsId}/environments/{environmentsId}
/// Updates an existing environment. When updating properties, you must pass all existing properties to the API, even if they are not being changed. If you omit properties from the payload, the properties are removed. To get the current list of properties for the environment, use the [Get Environment API](get). **Note**: Both PUT and POST methods are supported for updating an existing environment.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `apigee_organizations_environments_update_environment_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from apigee_organizations_environments_update_environment_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_environments_update_environment_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudApigeeV1Environment>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudApigeeV1Environment = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/organizations/{organizationsId}/environments/{environmentsId}
/// Updates an existing environment. When updating properties, you must pass all existing properties to the API, even if they are not being changed. If you omit properties from the payload, the properties are removed. To get the current list of properties for the environment, use the [Get Environment API](get). **Note**: Both PUT and POST methods are supported for updating an existing environment.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `apigee_organizations_environments_update_environment_builder()` + `apigee_organizations_environments_update_environment_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_environments_update_environment(
    client: &SimpleHttpClient,
    name: &str,
    body: &GoogleCloudApigeeV1Environment,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudApigeeV1Environment>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = apigee_organizations_environments_update_environment_builder(client, name, body)?;
    apigee_organizations_environments_update_environment_execute(builder)
}

/// GET v1/organizations/{organizationsId}/environments/{environmentsId}/securityActionsConfig
/// UpdateSecurityActionConfig updates the current SecurityActions configuration. This method is used to `enable/disable` the feature at the environment level.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `apigee_organizations_environments_update_security_actions_config_execute()` to send, or `apigee_organizations_environments_update_security_actions_config` for simplest API.

pub fn apigee_organizations_environments_update_security_actions_config_builder(
    client: &SimpleHttpClient,
    name: &str,
    updateMask: Option<&str>,
    body: &GoogleCloudApigeeV1SecurityActionsConfig,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://apigee.googleapis.com/v1/organizations/{}/environments/{}/securityActionsConfig",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = updateMask {
        query_parts.push(format!("updateMask={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/organizations/{organizationsId}/environments/{environmentsId}/securityActionsConfig
/// UpdateSecurityActionConfig updates the current SecurityActions configuration. This method is used to `enable/disable` the feature at the environment level.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `apigee_organizations_environments_update_security_actions_config_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from apigee_organizations_environments_update_security_actions_config_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_environments_update_security_actions_config_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudApigeeV1SecurityActionsConfig>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudApigeeV1SecurityActionsConfig = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/organizations/{organizationsId}/environments/{environmentsId}/securityActionsConfig
/// UpdateSecurityActionConfig updates the current SecurityActions configuration. This method is used to `enable/disable` the feature at the environment level.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `apigee_organizations_environments_update_security_actions_config_builder()` + `apigee_organizations_environments_update_security_actions_config_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_environments_update_security_actions_config(
    client: &SimpleHttpClient,
    name: &str,
    updateMask: Option<&str>,
    body: &GoogleCloudApigeeV1SecurityActionsConfig,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudApigeeV1SecurityActionsConfig>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = apigee_organizations_environments_update_security_actions_config_builder(
        client, name, updateMask, body,
    )?;
    apigee_organizations_environments_update_security_actions_config_execute(builder)
}

/// GET v1/organizations/{organizationsId}/environments/{environmentsId}/traceConfig
/// Updates the trace configurations in an environment. Note that the repeated fields have replace semantics when included in the field mask and that they will be overwritten by the value of the fields in the request body.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `apigee_organizations_environments_update_trace_config_execute()` to send, or `apigee_organizations_environments_update_trace_config` for simplest API.

pub fn apigee_organizations_environments_update_trace_config_builder(
    client: &SimpleHttpClient,
    name: &str,
    updateMask: Option<&str>,
    body: &GoogleCloudApigeeV1TraceConfig,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://apigee.googleapis.com/v1/organizations/{}/environments/{}/traceConfig",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = updateMask {
        query_parts.push(format!("updateMask={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/organizations/{organizationsId}/environments/{environmentsId}/traceConfig
/// Updates the trace configurations in an environment. Note that the repeated fields have replace semantics when included in the field mask and that they will be overwritten by the value of the fields in the request body.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `apigee_organizations_environments_update_trace_config_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from apigee_organizations_environments_update_trace_config_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_environments_update_trace_config_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudApigeeV1TraceConfig>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudApigeeV1TraceConfig = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/organizations/{organizationsId}/environments/{environmentsId}/traceConfig
/// Updates the trace configurations in an environment. Note that the repeated fields have replace semantics when included in the field mask and that they will be overwritten by the value of the fields in the request body.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `apigee_organizations_environments_update_trace_config_builder()` + `apigee_organizations_environments_update_trace_config_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_environments_update_trace_config(
    client: &SimpleHttpClient,
    name: &str,
    updateMask: Option<&str>,
    body: &GoogleCloudApigeeV1TraceConfig,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudApigeeV1TraceConfig>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = apigee_organizations_environments_update_trace_config_builder(
        client, name, updateMask, body,
    )?;
    apigee_organizations_environments_update_trace_config_execute(builder)
}

/// GET v1/organizations/{organizationsId}/environments/{environmentsId}/addonsConfig:setAddonEnablement
/// Updates an add-on enablement status of an environment.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `apigee_organizations_environments_addons_config_set_addon_enablement_execute()` to send, or `apigee_organizations_environments_addons_config_set_addon_enablement` for simplest API.

pub fn apigee_organizations_environments_addons_config_set_addon_enablement_builder(
    client: &SimpleHttpClient,
    name: &str,
    body: &GoogleCloudApigeeV1SetAddonEnablementRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://apigee.googleapis.com/v1/organizations/{}/environments/{}/addonsConfig:setAddonEnablement",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/organizations/{organizationsId}/environments/{environmentsId}/addonsConfig:setAddonEnablement
/// Updates an add-on enablement status of an environment.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `apigee_organizations_environments_addons_config_set_addon_enablement_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from apigee_organizations_environments_addons_config_set_addon_enablement_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_environments_addons_config_set_addon_enablement_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/organizations/{organizationsId}/environments/{environmentsId}/addonsConfig:setAddonEnablement
/// Updates an add-on enablement status of an environment.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `apigee_organizations_environments_addons_config_set_addon_enablement_builder()` + `apigee_organizations_environments_addons_config_set_addon_enablement_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_environments_addons_config_set_addon_enablement(
    client: &SimpleHttpClient,
    name: &str,
    body: &GoogleCloudApigeeV1SetAddonEnablementRequest,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = apigee_organizations_environments_addons_config_set_addon_enablement_builder(
        client, name, body,
    )?;
    apigee_organizations_environments_addons_config_set_addon_enablement_execute(builder)
}

/// GET v1/organizations/{organizationsId}/environments/{environmentsId}/analytics/admin/schemav2
/// Gets a list of metrics and dimensions that can be used to create analytics queries and reports. Each schema element contains the name of the field, its associated type, and a flag indicating whether it is a standard or custom field.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `apigee_organizations_environments_analytics_admin_get_schemav2_execute()` to send, or `apigee_organizations_environments_analytics_admin_get_schemav2` for simplest API.

pub fn apigee_organizations_environments_analytics_admin_get_schemav2_builder(
    client: &SimpleHttpClient,
    name: &str,
    disableCache: Option<bool>,
    type_rs: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://apigee.googleapis.com/v1/organizations/{}/environments/{}/analytics/admin/schemav2",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = disableCache {
        query_parts.push(format!("disableCache={}", val));
    }
    if let Some(val) = type_rs {
        query_parts.push(format!("type={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/organizations/{organizationsId}/environments/{environmentsId}/analytics/admin/schemav2
/// Gets a list of metrics and dimensions that can be used to create analytics queries and reports. Each schema element contains the name of the field, its associated type, and a flag indicating whether it is a standard or custom field.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `apigee_organizations_environments_analytics_admin_get_schemav2_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from apigee_organizations_environments_analytics_admin_get_schemav2_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_environments_analytics_admin_get_schemav2_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleCloudApigeeV1Schema>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudApigeeV1Schema = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/organizations/{organizationsId}/environments/{environmentsId}/analytics/admin/schemav2
/// Gets a list of metrics and dimensions that can be used to create analytics queries and reports. Each schema element contains the name of the field, its associated type, and a flag indicating whether it is a standard or custom field.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `apigee_organizations_environments_analytics_admin_get_schemav2_builder()` + `apigee_organizations_environments_analytics_admin_get_schemav2_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_environments_analytics_admin_get_schemav2(
    client: &SimpleHttpClient,
    name: &str,
    disableCache: Option<bool>,
    type_rs: Option<&str>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleCloudApigeeV1Schema>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = apigee_organizations_environments_analytics_admin_get_schemav2_builder(
        client,
        name,
        disableCache,
        type_rs,
    )?;
    apigee_organizations_environments_analytics_admin_get_schemav2_execute(builder)
}

/// GET v1/organizations/{organizationsId}/environments/{environmentsId}/analytics/exports
/// Submit a data export job to be processed in the background. If the request is successful, the API returns a 201 status, a URI that can be used to retrieve the status of the export job, and the state value of "enqueued".
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `apigee_organizations_environments_analytics_exports_create_execute()` to send, or `apigee_organizations_environments_analytics_exports_create` for simplest API.

pub fn apigee_organizations_environments_analytics_exports_create_builder(
    client: &SimpleHttpClient,
    parent: &str,
    body: &GoogleCloudApigeeV1ExportRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://apigee.googleapis.com/v1/organizations/{}/environments/{}/analytics/exports",
        parent,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/organizations/{organizationsId}/environments/{environmentsId}/analytics/exports
/// Submit a data export job to be processed in the background. If the request is successful, the API returns a 201 status, a URI that can be used to retrieve the status of the export job, and the state value of "enqueued".
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `apigee_organizations_environments_analytics_exports_create_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from apigee_organizations_environments_analytics_exports_create_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_environments_analytics_exports_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleCloudApigeeV1Export>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudApigeeV1Export = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/organizations/{organizationsId}/environments/{environmentsId}/analytics/exports
/// Submit a data export job to be processed in the background. If the request is successful, the API returns a 201 status, a URI that can be used to retrieve the status of the export job, and the state value of "enqueued".
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `apigee_organizations_environments_analytics_exports_create_builder()` + `apigee_organizations_environments_analytics_exports_create_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_environments_analytics_exports_create(
    client: &SimpleHttpClient,
    parent: &str,
    body: &GoogleCloudApigeeV1ExportRequest,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleCloudApigeeV1Export>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        apigee_organizations_environments_analytics_exports_create_builder(client, parent, body)?;
    apigee_organizations_environments_analytics_exports_create_execute(builder)
}

/// GET v1/organizations/{organizationsId}/environments/{environmentsId}/analytics/exports/{exportsId}
/// Gets the details and status of an analytics export job. If the export job is still in progress, its state is set to "running". After the export job has completed successfully, its state is set to "completed". If the export job fails, its state is set to failed.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `apigee_organizations_environments_analytics_exports_get_execute()` to send, or `apigee_organizations_environments_analytics_exports_get` for simplest API.

pub fn apigee_organizations_environments_analytics_exports_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://apigee.googleapis.com/v1/organizations/{}/environments/{}/analytics/exports/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/organizations/{organizationsId}/environments/{environmentsId}/analytics/exports/{exportsId}
/// Gets the details and status of an analytics export job. If the export job is still in progress, its state is set to "running". After the export job has completed successfully, its state is set to "completed". If the export job fails, its state is set to failed.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `apigee_organizations_environments_analytics_exports_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from apigee_organizations_environments_analytics_exports_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_environments_analytics_exports_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleCloudApigeeV1Export>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudApigeeV1Export = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/organizations/{organizationsId}/environments/{environmentsId}/analytics/exports/{exportsId}
/// Gets the details and status of an analytics export job. If the export job is still in progress, its state is set to "running". After the export job has completed successfully, its state is set to "completed". If the export job fails, its state is set to failed.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `apigee_organizations_environments_analytics_exports_get_builder()` + `apigee_organizations_environments_analytics_exports_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_environments_analytics_exports_get(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleCloudApigeeV1Export>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = apigee_organizations_environments_analytics_exports_get_builder(client, name)?;
    apigee_organizations_environments_analytics_exports_get_execute(builder)
}

/// GET v1/organizations/{organizationsId}/environments/{environmentsId}/analytics/exports
/// Lists the details and status of all analytics export jobs belonging to the parent organization and environment.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `apigee_organizations_environments_analytics_exports_list_execute()` to send, or `apigee_organizations_environments_analytics_exports_list` for simplest API.

pub fn apigee_organizations_environments_analytics_exports_list_builder(
    client: &SimpleHttpClient,
    parent: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://apigee.googleapis.com/v1/organizations/{}/environments/{}/analytics/exports",
        parent,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/organizations/{organizationsId}/environments/{environmentsId}/analytics/exports
/// Lists the details and status of all analytics export jobs belonging to the parent organization and environment.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `apigee_organizations_environments_analytics_exports_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from apigee_organizations_environments_analytics_exports_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_environments_analytics_exports_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudApigeeV1ListExportsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudApigeeV1ListExportsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/organizations/{organizationsId}/environments/{environmentsId}/analytics/exports
/// Lists the details and status of all analytics export jobs belonging to the parent organization and environment.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `apigee_organizations_environments_analytics_exports_list_builder()` + `apigee_organizations_environments_analytics_exports_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_environments_analytics_exports_list(
    client: &SimpleHttpClient,
    parent: &str,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudApigeeV1ListExportsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = apigee_organizations_environments_analytics_exports_list_builder(client, parent)?;
    apigee_organizations_environments_analytics_exports_list_execute(builder)
}

/// GET v1/organizations/{organizationsId}/environments/{environmentsId}/apis/{apisId}/deployments
/// Lists all deployments of an API proxy in an environment.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `apigee_organizations_environments_apis_deployments_list_execute()` to send, or `apigee_organizations_environments_apis_deployments_list` for simplest API.

pub fn apigee_organizations_environments_apis_deployments_list_builder(
    client: &SimpleHttpClient,
    parent: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://apigee.googleapis.com/v1/organizations/{}/environments/{}/apis/{}/deployments",
        parent,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/organizations/{organizationsId}/environments/{environmentsId}/apis/{apisId}/deployments
/// Lists all deployments of an API proxy in an environment.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `apigee_organizations_environments_apis_deployments_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from apigee_organizations_environments_apis_deployments_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_environments_apis_deployments_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudApigeeV1ListDeploymentsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudApigeeV1ListDeploymentsResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/organizations/{organizationsId}/environments/{environmentsId}/apis/{apisId}/deployments
/// Lists all deployments of an API proxy in an environment.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `apigee_organizations_environments_apis_deployments_list_builder()` + `apigee_organizations_environments_apis_deployments_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_environments_apis_deployments_list(
    client: &SimpleHttpClient,
    parent: &str,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudApigeeV1ListDeploymentsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = apigee_organizations_environments_apis_deployments_list_builder(client, parent)?;
    apigee_organizations_environments_apis_deployments_list_execute(builder)
}

/// GET v1/organizations/{organizationsId}/environments/{environmentsId}/apis/{apisId}/revisions/{revisionsId}/deployments
/// Deploys a revision of an API proxy. If another revision of the same API proxy revision is currently deployed, set the override parameter to `true` to have this revision replace the currently deployed revision. You cannot invoke an API proxy until it has been deployed to an environment. After you deploy an API proxy revision, you cannot edit it. To edit the API proxy, you must create and deploy a new revision. For a request path `organizations/{org}/environments/{env}/apis/{api}/revisions/{rev}/deployments`, two permissions are required: * apigee.deployments.create on the resource `organizations/{org}/environments/{env}` * apigee.proxyrevisions.deploy on the resource `organizations/{org}/apis/{api}/revisions/{rev}` All successful API proxy deployments to Apigee are [zero-downtime deployments](<https://cloud.google.`com/apigee/docs/api-platform/deploy/ui-deploy-overview`#zero-downtime-deployment>). Apigee hybrid validates the dependencies between shared flows and API proxies at deployment time. For example, if the Flow Callout policy in an API proxy references a shared flow that either doesn't exist or isn't deployed, the API proxy deployment fails.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `apigee_organizations_environments_apis_revisions_deploy_execute()` to send, or `apigee_organizations_environments_apis_revisions_deploy` for simplest API.

pub fn apigee_organizations_environments_apis_revisions_deploy_builder(
    client: &SimpleHttpClient,
    name: &str,
    override_rs: Option<bool>,
    sequencedRollout: Option<bool>,
    serviceAccount: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://apigee.googleapis.com/v1/organizations/{}/environments/{}/apis/{}/revisions/{}/deployments",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = override_rs {
        query_parts.push(format!("override={}", val));
    }
    if let Some(val) = sequencedRollout {
        query_parts.push(format!("sequencedRollout={}", val));
    }
    if let Some(val) = serviceAccount {
        query_parts.push(format!("serviceAccount={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/organizations/{organizationsId}/environments/{environmentsId}/apis/{apisId}/revisions/{revisionsId}/deployments
/// Deploys a revision of an API proxy. If another revision of the same API proxy revision is currently deployed, set the override parameter to `true` to have this revision replace the currently deployed revision. You cannot invoke an API proxy until it has been deployed to an environment. After you deploy an API proxy revision, you cannot edit it. To edit the API proxy, you must create and deploy a new revision. For a request path `organizations/{org}/environments/{env}/apis/{api}/revisions/{rev}/deployments`, two permissions are required: * apigee.deployments.create on the resource `organizations/{org}/environments/{env}` * apigee.proxyrevisions.deploy on the resource `organizations/{org}/apis/{api}/revisions/{rev}` All successful API proxy deployments to Apigee are [zero-downtime deployments](<https://cloud.google.`com/apigee/docs/api-platform/deploy/ui-deploy-overview`#zero-downtime-deployment>). Apigee hybrid validates the dependencies between shared flows and API proxies at deployment time. For example, if the Flow Callout policy in an API proxy references a shared flow that either doesn't exist or isn't deployed, the API proxy deployment fails.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `apigee_organizations_environments_apis_revisions_deploy_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from apigee_organizations_environments_apis_revisions_deploy_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_environments_apis_revisions_deploy_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudApigeeV1Deployment>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudApigeeV1Deployment = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/organizations/{organizationsId}/environments/{environmentsId}/apis/{apisId}/revisions/{revisionsId}/deployments
/// Deploys a revision of an API proxy. If another revision of the same API proxy revision is currently deployed, set the override parameter to `true` to have this revision replace the currently deployed revision. You cannot invoke an API proxy until it has been deployed to an environment. After you deploy an API proxy revision, you cannot edit it. To edit the API proxy, you must create and deploy a new revision. For a request path `organizations/{org}/environments/{env}/apis/{api}/revisions/{rev}/deployments`, two permissions are required: * apigee.deployments.create on the resource `organizations/{org}/environments/{env}` * apigee.proxyrevisions.deploy on the resource `organizations/{org}/apis/{api}/revisions/{rev}` All successful API proxy deployments to Apigee are [zero-downtime deployments](<https://cloud.google.`com/apigee/docs/api-platform/deploy/ui-deploy-overview`#zero-downtime-deployment>). Apigee hybrid validates the dependencies between shared flows and API proxies at deployment time. For example, if the Flow Callout policy in an API proxy references a shared flow that either doesn't exist or isn't deployed, the API proxy deployment fails.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `apigee_organizations_environments_apis_revisions_deploy_builder()` + `apigee_organizations_environments_apis_revisions_deploy_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_environments_apis_revisions_deploy(
    client: &SimpleHttpClient,
    name: &str,
    override_rs: Option<bool>,
    sequencedRollout: Option<bool>,
    serviceAccount: Option<&str>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudApigeeV1Deployment>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = apigee_organizations_environments_apis_revisions_deploy_builder(
        client,
        name,
        override_rs,
        sequencedRollout,
        serviceAccount,
    )?;
    apigee_organizations_environments_apis_revisions_deploy_execute(builder)
}

/// GET v1/organizations/{organizationsId}/environments/{environmentsId}/apis/{apisId}/revisions/{revisionsId}/deployments
/// Gets the deployment of an API proxy revision and actual state reported by runtime pods.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `apigee_organizations_environments_apis_revisions_get_deployments_execute()` to send, or `apigee_organizations_environments_apis_revisions_get_deployments` for simplest API.

pub fn apigee_organizations_environments_apis_revisions_get_deployments_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://apigee.googleapis.com/v1/organizations/{}/environments/{}/apis/{}/revisions/{}/deployments",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/organizations/{organizationsId}/environments/{environmentsId}/apis/{apisId}/revisions/{revisionsId}/deployments
/// Gets the deployment of an API proxy revision and actual state reported by runtime pods.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `apigee_organizations_environments_apis_revisions_get_deployments_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from apigee_organizations_environments_apis_revisions_get_deployments_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_environments_apis_revisions_get_deployments_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudApigeeV1Deployment>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudApigeeV1Deployment = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/organizations/{organizationsId}/environments/{environmentsId}/apis/{apisId}/revisions/{revisionsId}/deployments
/// Gets the deployment of an API proxy revision and actual state reported by runtime pods.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `apigee_organizations_environments_apis_revisions_get_deployments_builder()` + `apigee_organizations_environments_apis_revisions_get_deployments_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_environments_apis_revisions_get_deployments(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudApigeeV1Deployment>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        apigee_organizations_environments_apis_revisions_get_deployments_builder(client, name)?;
    apigee_organizations_environments_apis_revisions_get_deployments_execute(builder)
}

/// GET v1/organizations/{organizationsId}/environments/{environmentsId}/apis/{apisId}/revisions/{revisionsId}/deployments
/// Undeploys an API proxy revision from an environment. For a request path `organizations/{org}/environments/{env}/apis/{api}/revisions/{rev}/deployments`, two permissions are required: * apigee.deployments.delete on the resource `organizations/{org}/environments/{env}` * apigee.proxyrevisions.undeploy on the resource `organizations/{org}/apis/{api}/revisions/{rev}`
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `apigee_organizations_environments_apis_revisions_undeploy_execute()` to send, or `apigee_organizations_environments_apis_revisions_undeploy` for simplest API.

pub fn apigee_organizations_environments_apis_revisions_undeploy_builder(
    client: &SimpleHttpClient,
    name: &str,
    sequencedRollout: Option<bool>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://apigee.googleapis.com/v1/organizations/{}/environments/{}/apis/{}/revisions/{}/deployments",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = sequencedRollout {
        query_parts.push(format!("sequencedRollout={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/organizations/{organizationsId}/environments/{environmentsId}/apis/{apisId}/revisions/{revisionsId}/deployments
/// Undeploys an API proxy revision from an environment. For a request path `organizations/{org}/environments/{env}/apis/{api}/revisions/{rev}/deployments`, two permissions are required: * apigee.deployments.delete on the resource `organizations/{org}/environments/{env}` * apigee.proxyrevisions.undeploy on the resource `organizations/{org}/apis/{api}/revisions/{rev}`
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `apigee_organizations_environments_apis_revisions_undeploy_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from apigee_organizations_environments_apis_revisions_undeploy_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_environments_apis_revisions_undeploy_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleProtobufEmpty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/organizations/{organizationsId}/environments/{environmentsId}/apis/{apisId}/revisions/{revisionsId}/deployments
/// Undeploys an API proxy revision from an environment. For a request path `organizations/{org}/environments/{env}/apis/{api}/revisions/{rev}/deployments`, two permissions are required: * apigee.deployments.delete on the resource `organizations/{org}/environments/{env}` * apigee.proxyrevisions.undeploy on the resource `organizations/{org}/apis/{api}/revisions/{rev}`
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `apigee_organizations_environments_apis_revisions_undeploy_builder()` + `apigee_organizations_environments_apis_revisions_undeploy_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_environments_apis_revisions_undeploy(
    client: &SimpleHttpClient,
    name: &str,
    sequencedRollout: Option<bool>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = apigee_organizations_environments_apis_revisions_undeploy_builder(
        client,
        name,
        sequencedRollout,
    )?;
    apigee_organizations_environments_apis_revisions_undeploy_execute(builder)
}

/// GET v1/organizations/{organizationsId}/environments/{environmentsId}/apis/{apisId}/revisions/{revisionsId}/debugsessions
/// Creates a debug session for a deployed API Proxy revision.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `apigee_organizations_environments_apis_revisions_debugsessions_create_execute()` to send, or `apigee_organizations_environments_apis_revisions_debugsessions_create` for simplest API.

pub fn apigee_organizations_environments_apis_revisions_debugsessions_create_builder(
    client: &SimpleHttpClient,
    parent: &str,
    timeout: Option<&str>,
    body: &GoogleCloudApigeeV1DebugSession,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://apigee.googleapis.com/v1/organizations/{}/environments/{}/apis/{}/revisions/{}/debugsessions",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = timeout {
        query_parts.push(format!("timeout={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/organizations/{organizationsId}/environments/{environmentsId}/apis/{apisId}/revisions/{revisionsId}/debugsessions
/// Creates a debug session for a deployed API Proxy revision.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `apigee_organizations_environments_apis_revisions_debugsessions_create_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from apigee_organizations_environments_apis_revisions_debugsessions_create_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_environments_apis_revisions_debugsessions_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudApigeeV1DebugSession>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudApigeeV1DebugSession = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/organizations/{organizationsId}/environments/{environmentsId}/apis/{apisId}/revisions/{revisionsId}/debugsessions
/// Creates a debug session for a deployed API Proxy revision.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `apigee_organizations_environments_apis_revisions_debugsessions_create_builder()` + `apigee_organizations_environments_apis_revisions_debugsessions_create_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_environments_apis_revisions_debugsessions_create(
    client: &SimpleHttpClient,
    parent: &str,
    timeout: Option<&str>,
    body: &GoogleCloudApigeeV1DebugSession,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudApigeeV1DebugSession>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = apigee_organizations_environments_apis_revisions_debugsessions_create_builder(
        client, parent, timeout, body,
    )?;
    apigee_organizations_environments_apis_revisions_debugsessions_create_execute(builder)
}

/// GET v1/organizations/{organizationsId}/environments/{environmentsId}/apis/{apisId}/revisions/{revisionsId}/debugsessions/{debugsessionsId}/data
/// Deletes the data from a debug session. This does not cancel the debug session or prevent further data from being collected if the session is still active in runtime pods.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `apigee_organizations_environments_apis_revisions_debugsessions_delete_data_execute()` to send, or `apigee_organizations_environments_apis_revisions_debugsessions_delete_data` for simplest API.

pub fn apigee_organizations_environments_apis_revisions_debugsessions_delete_data_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://apigee.googleapis.com/v1/organizations/{}/environments/{}/apis/{}/revisions/{}/debugsessions/{}/data",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/organizations/{organizationsId}/environments/{environmentsId}/apis/{apisId}/revisions/{revisionsId}/debugsessions/{debugsessionsId}/data
/// Deletes the data from a debug session. This does not cancel the debug session or prevent further data from being collected if the session is still active in runtime pods.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `apigee_organizations_environments_apis_revisions_debugsessions_delete_data_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from apigee_organizations_environments_apis_revisions_debugsessions_delete_data_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_environments_apis_revisions_debugsessions_delete_data_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleProtobufEmpty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/organizations/{organizationsId}/environments/{environmentsId}/apis/{apisId}/revisions/{revisionsId}/debugsessions/{debugsessionsId}/data
/// Deletes the data from a debug session. This does not cancel the debug session or prevent further data from being collected if the session is still active in runtime pods.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `apigee_organizations_environments_apis_revisions_debugsessions_delete_data_builder()` + `apigee_organizations_environments_apis_revisions_debugsessions_delete_data_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_environments_apis_revisions_debugsessions_delete_data(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        apigee_organizations_environments_apis_revisions_debugsessions_delete_data_builder(
            client, name,
        )?;
    apigee_organizations_environments_apis_revisions_debugsessions_delete_data_execute(builder)
}

/// GET v1/organizations/{organizationsId}/environments/{environmentsId}/apis/{apisId}/revisions/{revisionsId}/debugsessions/{debugsessionsId}
/// Retrieves a debug session.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `apigee_organizations_environments_apis_revisions_debugsessions_get_execute()` to send, or `apigee_organizations_environments_apis_revisions_debugsessions_get` for simplest API.

pub fn apigee_organizations_environments_apis_revisions_debugsessions_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://apigee.googleapis.com/v1/organizations/{}/environments/{}/apis/{}/revisions/{}/debugsessions/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/organizations/{organizationsId}/environments/{environmentsId}/apis/{apisId}/revisions/{revisionsId}/debugsessions/{debugsessionsId}
/// Retrieves a debug session.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `apigee_organizations_environments_apis_revisions_debugsessions_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from apigee_organizations_environments_apis_revisions_debugsessions_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_environments_apis_revisions_debugsessions_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudApigeeV1DebugSession>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudApigeeV1DebugSession = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/organizations/{organizationsId}/environments/{environmentsId}/apis/{apisId}/revisions/{revisionsId}/debugsessions/{debugsessionsId}
/// Retrieves a debug session.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `apigee_organizations_environments_apis_revisions_debugsessions_get_builder()` + `apigee_organizations_environments_apis_revisions_debugsessions_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_environments_apis_revisions_debugsessions_get(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudApigeeV1DebugSession>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        apigee_organizations_environments_apis_revisions_debugsessions_get_builder(client, name)?;
    apigee_organizations_environments_apis_revisions_debugsessions_get_execute(builder)
}

/// GET v1/organizations/{organizationsId}/environments/{environmentsId}/apis/{apisId}/revisions/{revisionsId}/debugsessions
/// Lists debug sessions that are currently active in the given API Proxy revision.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `apigee_organizations_environments_apis_revisions_debugsessions_list_execute()` to send, or `apigee_organizations_environments_apis_revisions_debugsessions_list` for simplest API.

pub fn apigee_organizations_environments_apis_revisions_debugsessions_list_builder(
    client: &SimpleHttpClient,
    parent: &str,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://apigee.googleapis.com/v1/organizations/{}/environments/{}/apis/{}/revisions/{}/debugsessions",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/organizations/{organizationsId}/environments/{environmentsId}/apis/{apisId}/revisions/{revisionsId}/debugsessions
/// Lists debug sessions that are currently active in the given API Proxy revision.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `apigee_organizations_environments_apis_revisions_debugsessions_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from apigee_organizations_environments_apis_revisions_debugsessions_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_environments_apis_revisions_debugsessions_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudApigeeV1ListDebugSessionsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudApigeeV1ListDebugSessionsResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/organizations/{organizationsId}/environments/{environmentsId}/apis/{apisId}/revisions/{revisionsId}/debugsessions
/// Lists debug sessions that are currently active in the given API Proxy revision.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `apigee_organizations_environments_apis_revisions_debugsessions_list_builder()` + `apigee_organizations_environments_apis_revisions_debugsessions_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_environments_apis_revisions_debugsessions_list(
    client: &SimpleHttpClient,
    parent: &str,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudApigeeV1ListDebugSessionsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = apigee_organizations_environments_apis_revisions_debugsessions_list_builder(
        client, parent, pageSize, pageToken,
    )?;
    apigee_organizations_environments_apis_revisions_debugsessions_list_execute(builder)
}

/// GET v1/organizations/{organizationsId}/environments/{environmentsId}/apis/{apisId}/revisions/{revisionsId}/debugsessions/{debugsessionsId}/data/{dataId}
/// Gets the debug data from a transaction.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `apigee_organizations_environments_apis_revisions_debugsessions_data_get_execute()` to send, or `apigee_organizations_environments_apis_revisions_debugsessions_data_get` for simplest API.

pub fn apigee_organizations_environments_apis_revisions_debugsessions_data_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://apigee.googleapis.com/v1/organizations/{}/environments/{}/apis/{}/revisions/{}/debugsessions/{}/data/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/organizations/{organizationsId}/environments/{environmentsId}/apis/{apisId}/revisions/{revisionsId}/debugsessions/{debugsessionsId}/data/{dataId}
/// Gets the debug data from a transaction.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `apigee_organizations_environments_apis_revisions_debugsessions_data_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from apigee_organizations_environments_apis_revisions_debugsessions_data_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_environments_apis_revisions_debugsessions_data_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudApigeeV1DebugSessionTransaction>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudApigeeV1DebugSessionTransaction =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/organizations/{organizationsId}/environments/{environmentsId}/apis/{apisId}/revisions/{revisionsId}/debugsessions/{debugsessionsId}/data/{dataId}
/// Gets the debug data from a transaction.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `apigee_organizations_environments_apis_revisions_debugsessions_data_get_builder()` + `apigee_organizations_environments_apis_revisions_debugsessions_data_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_environments_apis_revisions_debugsessions_data_get(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudApigeeV1DebugSessionTransaction>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = apigee_organizations_environments_apis_revisions_debugsessions_data_get_builder(
        client, name,
    )?;
    apigee_organizations_environments_apis_revisions_debugsessions_data_get_execute(builder)
}

/// GET v1/organizations/{organizationsId}/environments/{environmentsId}/apis/{apisId}/revisions/{revisionsId}/deployments:generateDeployChangeReport
/// Generates a report for a dry run analysis of a DeployApiProxy request without committing the deployment. In addition to the standard validations performed when adding deployments, additional analysis will be done to detect possible traffic routing changes that would result from this deployment being created. Any potential routing conflicts or unsafe changes will be reported in the response. This routing analysis is not performed for a non-dry-run DeployApiProxy request. For a request path `organizations/{org}/environments/{env}/apis/{api}/revisions/{rev}/deployments`:`generateDeployChangeReport`, two permissions are required: * apigee.deployments.create on the resource `organizations/{org}/environments/{env}` * apigee.proxyrevisions.deploy on the resource `organizations/{org}/apis/{api}/revisions/{rev}`
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `apigee_organizations_environments_apis_revisions_deployments_generate_deploy_change_report_execute()` to send, or `apigee_organizations_environments_apis_revisions_deployments_generate_deploy_change_report` for simplest API.

pub fn apigee_organizations_environments_apis_revisions_deployments_generate_deploy_change_report_builder(
    client: &SimpleHttpClient,
    name: &str,
    override_rs: Option<bool>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://apigee.googleapis.com/v1/organizations/{}/environments/{}/apis/{}/revisions/{}/deployments:generateDeployChangeReport",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = override_rs {
        query_parts.push(format!("override={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/organizations/{organizationsId}/environments/{environmentsId}/apis/{apisId}/revisions/{revisionsId}/deployments:generateDeployChangeReport
/// Generates a report for a dry run analysis of a DeployApiProxy request without committing the deployment. In addition to the standard validations performed when adding deployments, additional analysis will be done to detect possible traffic routing changes that would result from this deployment being created. Any potential routing conflicts or unsafe changes will be reported in the response. This routing analysis is not performed for a non-dry-run DeployApiProxy request. For a request path `organizations/{org}/environments/{env}/apis/{api}/revisions/{rev}/deployments`:`generateDeployChangeReport`, two permissions are required: * apigee.deployments.create on the resource `organizations/{org}/environments/{env}` * apigee.proxyrevisions.deploy on the resource `organizations/{org}/apis/{api}/revisions/{rev}`
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `apigee_organizations_environments_apis_revisions_deployments_generate_deploy_change_report_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from apigee_organizations_environments_apis_revisions_deployments_generate_deploy_change_report_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_environments_apis_revisions_deployments_generate_deploy_change_report_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudApigeeV1DeploymentChangeReport>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudApigeeV1DeploymentChangeReport = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/organizations/{organizationsId}/environments/{environmentsId}/apis/{apisId}/revisions/{revisionsId}/deployments:generateDeployChangeReport
/// Generates a report for a dry run analysis of a DeployApiProxy request without committing the deployment. In addition to the standard validations performed when adding deployments, additional analysis will be done to detect possible traffic routing changes that would result from this deployment being created. Any potential routing conflicts or unsafe changes will be reported in the response. This routing analysis is not performed for a non-dry-run DeployApiProxy request. For a request path `organizations/{org}/environments/{env}/apis/{api}/revisions/{rev}/deployments`:`generateDeployChangeReport`, two permissions are required: * apigee.deployments.create on the resource `organizations/{org}/environments/{env}` * apigee.proxyrevisions.deploy on the resource `organizations/{org}/apis/{api}/revisions/{rev}`
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `apigee_organizations_environments_apis_revisions_deployments_generate_deploy_change_report_builder()` + `apigee_organizations_environments_apis_revisions_deployments_generate_deploy_change_report_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_environments_apis_revisions_deployments_generate_deploy_change_report(
    client: &SimpleHttpClient,
    name: &str,
    override_rs: Option<bool>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudApigeeV1DeploymentChangeReport>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = apigee_organizations_environments_apis_revisions_deployments_generate_deploy_change_report_builder(client, name, override_rs)?;
    apigee_organizations_environments_apis_revisions_deployments_generate_deploy_change_report_execute(builder)
}

/// GET v1/organizations/{organizationsId}/environments/{environmentsId}/apis/{apisId}/revisions/{revisionsId}/deployments:generateUndeployChangeReport
/// Generates a report for a dry run analysis of an UndeployApiProxy request without committing the undeploy. In addition to the standard validations performed when removing deployments, additional analysis will be done to detect possible traffic routing changes that would result from this deployment being removed. Any potential routing conflicts or unsafe changes will be reported in the response. This routing analysis is not performed for a non-dry-run UndeployApiProxy request. For a request path `organizations/{org}/environments/{env}/apis/{api}/revisions/{rev}/deployments`:`generateUndeployChangeReport`, two permissions are required: * apigee.deployments.delete on the resource `organizations/{org}/environments/{env}` * apigee.proxyrevisions.undeploy on the resource `organizations/{org}/apis/{api}/revisions/{rev}`
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `apigee_organizations_environments_apis_revisions_deployments_generate_undeploy_change_report_execute()` to send, or `apigee_organizations_environments_apis_revisions_deployments_generate_undeploy_change_report` for simplest API.

pub fn apigee_organizations_environments_apis_revisions_deployments_generate_undeploy_change_report_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://apigee.googleapis.com/v1/organizations/{}/environments/{}/apis/{}/revisions/{}/deployments:generateUndeployChangeReport",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/organizations/{organizationsId}/environments/{environmentsId}/apis/{apisId}/revisions/{revisionsId}/deployments:generateUndeployChangeReport
/// Generates a report for a dry run analysis of an UndeployApiProxy request without committing the undeploy. In addition to the standard validations performed when removing deployments, additional analysis will be done to detect possible traffic routing changes that would result from this deployment being removed. Any potential routing conflicts or unsafe changes will be reported in the response. This routing analysis is not performed for a non-dry-run UndeployApiProxy request. For a request path `organizations/{org}/environments/{env}/apis/{api}/revisions/{rev}/deployments`:`generateUndeployChangeReport`, two permissions are required: * apigee.deployments.delete on the resource `organizations/{org}/environments/{env}` * apigee.proxyrevisions.undeploy on the resource `organizations/{org}/apis/{api}/revisions/{rev}`
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `apigee_organizations_environments_apis_revisions_deployments_generate_undeploy_change_report_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from apigee_organizations_environments_apis_revisions_deployments_generate_undeploy_change_report_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_environments_apis_revisions_deployments_generate_undeploy_change_report_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudApigeeV1DeploymentChangeReport>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudApigeeV1DeploymentChangeReport = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/organizations/{organizationsId}/environments/{environmentsId}/apis/{apisId}/revisions/{revisionsId}/deployments:generateUndeployChangeReport
/// Generates a report for a dry run analysis of an UndeployApiProxy request without committing the undeploy. In addition to the standard validations performed when removing deployments, additional analysis will be done to detect possible traffic routing changes that would result from this deployment being removed. Any potential routing conflicts or unsafe changes will be reported in the response. This routing analysis is not performed for a non-dry-run UndeployApiProxy request. For a request path `organizations/{org}/environments/{env}/apis/{api}/revisions/{rev}/deployments`:`generateUndeployChangeReport`, two permissions are required: * apigee.deployments.delete on the resource `organizations/{org}/environments/{env}` * apigee.proxyrevisions.undeploy on the resource `organizations/{org}/apis/{api}/revisions/{rev}`
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `apigee_organizations_environments_apis_revisions_deployments_generate_undeploy_change_report_builder()` + `apigee_organizations_environments_apis_revisions_deployments_generate_undeploy_change_report_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_environments_apis_revisions_deployments_generate_undeploy_change_report(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudApigeeV1DeploymentChangeReport>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = apigee_organizations_environments_apis_revisions_deployments_generate_undeploy_change_report_builder(client, name)?;
    apigee_organizations_environments_apis_revisions_deployments_generate_undeploy_change_report_execute(builder)
}

/// GET v1/organizations/{organizationsId}/environments/{environmentsId}/archiveDeployments
/// Creates a new ArchiveDeployment.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `apigee_organizations_environments_archive_deployments_create_execute()` to send, or `apigee_organizations_environments_archive_deployments_create` for simplest API.

pub fn apigee_organizations_environments_archive_deployments_create_builder(
    client: &SimpleHttpClient,
    parent: &str,
    body: &GoogleCloudApigeeV1ArchiveDeployment,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://apigee.googleapis.com/v1/organizations/{}/environments/{}/archiveDeployments",
        parent,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/organizations/{organizationsId}/environments/{environmentsId}/archiveDeployments
/// Creates a new ArchiveDeployment.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `apigee_organizations_environments_archive_deployments_create_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from apigee_organizations_environments_archive_deployments_create_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_environments_archive_deployments_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/organizations/{organizationsId}/environments/{environmentsId}/archiveDeployments
/// Creates a new ArchiveDeployment.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `apigee_organizations_environments_archive_deployments_create_builder()` + `apigee_organizations_environments_archive_deployments_create_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_environments_archive_deployments_create(
    client: &SimpleHttpClient,
    parent: &str,
    body: &GoogleCloudApigeeV1ArchiveDeployment,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        apigee_organizations_environments_archive_deployments_create_builder(client, parent, body)?;
    apigee_organizations_environments_archive_deployments_create_execute(builder)
}

/// GET v1/organizations/{organizationsId}/environments/{environmentsId}/archiveDeployments/{archiveDeploymentsId}
/// Deletes an archive deployment.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `apigee_organizations_environments_archive_deployments_delete_execute()` to send, or `apigee_organizations_environments_archive_deployments_delete` for simplest API.

pub fn apigee_organizations_environments_archive_deployments_delete_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://apigee.googleapis.com/v1/organizations/{}/environments/{}/archiveDeployments/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/organizations/{organizationsId}/environments/{environmentsId}/archiveDeployments/{archiveDeploymentsId}
/// Deletes an archive deployment.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `apigee_organizations_environments_archive_deployments_delete_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from apigee_organizations_environments_archive_deployments_delete_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_environments_archive_deployments_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleProtobufEmpty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/organizations/{organizationsId}/environments/{environmentsId}/archiveDeployments/{archiveDeploymentsId}
/// Deletes an archive deployment.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `apigee_organizations_environments_archive_deployments_delete_builder()` + `apigee_organizations_environments_archive_deployments_delete_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_environments_archive_deployments_delete(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        apigee_organizations_environments_archive_deployments_delete_builder(client, name)?;
    apigee_organizations_environments_archive_deployments_delete_execute(builder)
}

/// GET v1/organizations/{organizationsId}/environments/{environmentsId}/archiveDeployments/{archiveDeploymentsId}:generateDownloadUrl
/// Generates a signed URL for downloading the original zip file used to create an Archive Deployment. The URL is only valid for a limited period and should be used within minutes after generation. Each call returns a new upload URL.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `apigee_organizations_environments_archive_deployments_generate_download_url_execute()` to send, or `apigee_organizations_environments_archive_deployments_generate_download_url` for simplest API.

pub fn apigee_organizations_environments_archive_deployments_generate_download_url_builder(
    client: &SimpleHttpClient,
    name: &str,
    body: &GoogleCloudApigeeV1GenerateDownloadUrlRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://apigee.googleapis.com/v1/organizations/{}/environments/{}/archiveDeployments/{}:generateDownloadUrl",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/organizations/{organizationsId}/environments/{environmentsId}/archiveDeployments/{archiveDeploymentsId}:generateDownloadUrl
/// Generates a signed URL for downloading the original zip file used to create an Archive Deployment. The URL is only valid for a limited period and should be used within minutes after generation. Each call returns a new upload URL.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `apigee_organizations_environments_archive_deployments_generate_download_url_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from apigee_organizations_environments_archive_deployments_generate_download_url_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_environments_archive_deployments_generate_download_url_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudApigeeV1GenerateDownloadUrlResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudApigeeV1GenerateDownloadUrlResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/organizations/{organizationsId}/environments/{environmentsId}/archiveDeployments/{archiveDeploymentsId}:generateDownloadUrl
/// Generates a signed URL for downloading the original zip file used to create an Archive Deployment. The URL is only valid for a limited period and should be used within minutes after generation. Each call returns a new upload URL.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `apigee_organizations_environments_archive_deployments_generate_download_url_builder()` + `apigee_organizations_environments_archive_deployments_generate_download_url_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_environments_archive_deployments_generate_download_url(
    client: &SimpleHttpClient,
    name: &str,
    body: &GoogleCloudApigeeV1GenerateDownloadUrlRequest,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudApigeeV1GenerateDownloadUrlResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        apigee_organizations_environments_archive_deployments_generate_download_url_builder(
            client, name, body,
        )?;
    apigee_organizations_environments_archive_deployments_generate_download_url_execute(builder)
}

/// GET v1/organizations/{organizationsId}/environments/{environmentsId}/archiveDeployments:generateUploadUrl
/// Generates a signed URL for uploading an Archive zip file to Google Cloud Storage. Once the upload is complete, the signed URL should be passed to CreateArchiveDeployment. When uploading to the generated signed URL, please follow these restrictions: * Source file type should be a zip file. * Source file size should not exceed 1GB limit. * No credentials should be attached - the signed URLs provide access to the target bucket using internal service identity; if credentials were attached, the identity from the credentials would be used, but that identity does not have permissions to upload files to the URL. When making a HTTP PUT request, these two headers need to be specified: * content-type: `application/zip` * x-goog-content-length-range: 0,1073741824 And this header SHOULD NOT be specified: * Authorization: Bearer YOUR_TOKEN
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `apigee_organizations_environments_archive_deployments_generate_upload_url_execute()` to send, or `apigee_organizations_environments_archive_deployments_generate_upload_url` for simplest API.

pub fn apigee_organizations_environments_archive_deployments_generate_upload_url_builder(
    client: &SimpleHttpClient,
    parent: &str,
    body: &GoogleCloudApigeeV1GenerateUploadUrlRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://apigee.googleapis.com/v1/organizations/{}/environments/{}/archiveDeployments:generateUploadUrl",
        parent,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/organizations/{organizationsId}/environments/{environmentsId}/archiveDeployments:generateUploadUrl
/// Generates a signed URL for uploading an Archive zip file to Google Cloud Storage. Once the upload is complete, the signed URL should be passed to CreateArchiveDeployment. When uploading to the generated signed URL, please follow these restrictions: * Source file type should be a zip file. * Source file size should not exceed 1GB limit. * No credentials should be attached - the signed URLs provide access to the target bucket using internal service identity; if credentials were attached, the identity from the credentials would be used, but that identity does not have permissions to upload files to the URL. When making a HTTP PUT request, these two headers need to be specified: * content-type: `application/zip` * x-goog-content-length-range: 0,1073741824 And this header SHOULD NOT be specified: * Authorization: Bearer YOUR_TOKEN
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `apigee_organizations_environments_archive_deployments_generate_upload_url_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from apigee_organizations_environments_archive_deployments_generate_upload_url_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_environments_archive_deployments_generate_upload_url_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudApigeeV1GenerateUploadUrlResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudApigeeV1GenerateUploadUrlResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/organizations/{organizationsId}/environments/{environmentsId}/archiveDeployments:generateUploadUrl
/// Generates a signed URL for uploading an Archive zip file to Google Cloud Storage. Once the upload is complete, the signed URL should be passed to CreateArchiveDeployment. When uploading to the generated signed URL, please follow these restrictions: * Source file type should be a zip file. * Source file size should not exceed 1GB limit. * No credentials should be attached - the signed URLs provide access to the target bucket using internal service identity; if credentials were attached, the identity from the credentials would be used, but that identity does not have permissions to upload files to the URL. When making a HTTP PUT request, these two headers need to be specified: * content-type: `application/zip` * x-goog-content-length-range: 0,1073741824 And this header SHOULD NOT be specified: * Authorization: Bearer YOUR_TOKEN
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `apigee_organizations_environments_archive_deployments_generate_upload_url_builder()` + `apigee_organizations_environments_archive_deployments_generate_upload_url_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_environments_archive_deployments_generate_upload_url(
    client: &SimpleHttpClient,
    parent: &str,
    body: &GoogleCloudApigeeV1GenerateUploadUrlRequest,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudApigeeV1GenerateUploadUrlResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        apigee_organizations_environments_archive_deployments_generate_upload_url_builder(
            client, parent, body,
        )?;
    apigee_organizations_environments_archive_deployments_generate_upload_url_execute(builder)
}

/// GET v1/organizations/{organizationsId}/environments/{environmentsId}/archiveDeployments/{archiveDeploymentsId}
/// Gets the specified ArchiveDeployment.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `apigee_organizations_environments_archive_deployments_get_execute()` to send, or `apigee_organizations_environments_archive_deployments_get` for simplest API.

pub fn apigee_organizations_environments_archive_deployments_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://apigee.googleapis.com/v1/organizations/{}/environments/{}/archiveDeployments/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/organizations/{organizationsId}/environments/{environmentsId}/archiveDeployments/{archiveDeploymentsId}
/// Gets the specified ArchiveDeployment.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `apigee_organizations_environments_archive_deployments_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from apigee_organizations_environments_archive_deployments_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_environments_archive_deployments_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudApigeeV1ArchiveDeployment>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudApigeeV1ArchiveDeployment = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/organizations/{organizationsId}/environments/{environmentsId}/archiveDeployments/{archiveDeploymentsId}
/// Gets the specified ArchiveDeployment.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `apigee_organizations_environments_archive_deployments_get_builder()` + `apigee_organizations_environments_archive_deployments_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_environments_archive_deployments_get(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudApigeeV1ArchiveDeployment>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = apigee_organizations_environments_archive_deployments_get_builder(client, name)?;
    apigee_organizations_environments_archive_deployments_get_execute(builder)
}

/// GET v1/organizations/{organizationsId}/environments/{environmentsId}/archiveDeployments
/// Lists the ArchiveDeployments in the specified Environment.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `apigee_organizations_environments_archive_deployments_list_execute()` to send, or `apigee_organizations_environments_archive_deployments_list` for simplest API.

pub fn apigee_organizations_environments_archive_deployments_list_builder(
    client: &SimpleHttpClient,
    parent: &str,
    filter: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://apigee.googleapis.com/v1/organizations/{}/environments/{}/archiveDeployments",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/organizations/{organizationsId}/environments/{environmentsId}/archiveDeployments
/// Lists the ArchiveDeployments in the specified Environment.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `apigee_organizations_environments_archive_deployments_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from apigee_organizations_environments_archive_deployments_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_environments_archive_deployments_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudApigeeV1ListArchiveDeploymentsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudApigeeV1ListArchiveDeploymentsResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/organizations/{organizationsId}/environments/{environmentsId}/archiveDeployments
/// Lists the ArchiveDeployments in the specified Environment.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `apigee_organizations_environments_archive_deployments_list_builder()` + `apigee_organizations_environments_archive_deployments_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_environments_archive_deployments_list(
    client: &SimpleHttpClient,
    parent: &str,
    filter: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudApigeeV1ListArchiveDeploymentsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = apigee_organizations_environments_archive_deployments_list_builder(
        client, parent, filter, pageSize, pageToken,
    )?;
    apigee_organizations_environments_archive_deployments_list_execute(builder)
}

/// GET v1/organizations/{organizationsId}/environments/{environmentsId}/archiveDeployments/{archiveDeploymentsId}
/// Updates an existing ArchiveDeployment. Labels can modified but most of the other fields are not modifiable.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `apigee_organizations_environments_archive_deployments_patch_execute()` to send, or `apigee_organizations_environments_archive_deployments_patch` for simplest API.

pub fn apigee_organizations_environments_archive_deployments_patch_builder(
    client: &SimpleHttpClient,
    name: &str,
    updateMask: Option<&str>,
    body: &GoogleCloudApigeeV1ArchiveDeployment,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://apigee.googleapis.com/v1/organizations/{}/environments/{}/archiveDeployments/{}",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = updateMask {
        query_parts.push(format!("updateMask={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/organizations/{organizationsId}/environments/{environmentsId}/archiveDeployments/{archiveDeploymentsId}
/// Updates an existing ArchiveDeployment. Labels can modified but most of the other fields are not modifiable.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `apigee_organizations_environments_archive_deployments_patch_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from apigee_organizations_environments_archive_deployments_patch_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_environments_archive_deployments_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudApigeeV1ArchiveDeployment>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudApigeeV1ArchiveDeployment = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/organizations/{organizationsId}/environments/{environmentsId}/archiveDeployments/{archiveDeploymentsId}
/// Updates an existing ArchiveDeployment. Labels can modified but most of the other fields are not modifiable.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `apigee_organizations_environments_archive_deployments_patch_builder()` + `apigee_organizations_environments_archive_deployments_patch_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_environments_archive_deployments_patch(
    client: &SimpleHttpClient,
    name: &str,
    updateMask: Option<&str>,
    body: &GoogleCloudApigeeV1ArchiveDeployment,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudApigeeV1ArchiveDeployment>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = apigee_organizations_environments_archive_deployments_patch_builder(
        client, name, updateMask, body,
    )?;
    apigee_organizations_environments_archive_deployments_patch_execute(builder)
}

/// GET v1/organizations/{organizationsId}/environments/{environmentsId}/caches/{cachesId}
/// Deletes a cache.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `apigee_organizations_environments_caches_delete_execute()` to send, or `apigee_organizations_environments_caches_delete` for simplest API.

pub fn apigee_organizations_environments_caches_delete_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://apigee.googleapis.com/v1/organizations/{}/environments/{}/caches/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/organizations/{organizationsId}/environments/{environmentsId}/caches/{cachesId}
/// Deletes a cache.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `apigee_organizations_environments_caches_delete_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from apigee_organizations_environments_caches_delete_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_environments_caches_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleProtobufEmpty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/organizations/{organizationsId}/environments/{environmentsId}/caches/{cachesId}
/// Deletes a cache.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `apigee_organizations_environments_caches_delete_builder()` + `apigee_organizations_environments_caches_delete_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_environments_caches_delete(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = apigee_organizations_environments_caches_delete_builder(client, name)?;
    apigee_organizations_environments_caches_delete_execute(builder)
}

/// GET v1/organizations/{organizationsId}/environments/{environmentsId}/deployments/{deploymentsId}
/// Gets a particular deployment of Api proxy or a shared flow in an environment
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `apigee_organizations_environments_deployments_get_execute()` to send, or `apigee_organizations_environments_deployments_get` for simplest API.

pub fn apigee_organizations_environments_deployments_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://apigee.googleapis.com/v1/organizations/{}/environments/{}/deployments/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/organizations/{organizationsId}/environments/{environmentsId}/deployments/{deploymentsId}
/// Gets a particular deployment of Api proxy or a shared flow in an environment
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `apigee_organizations_environments_deployments_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from apigee_organizations_environments_deployments_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_environments_deployments_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudApigeeV1Deployment>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudApigeeV1Deployment = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/organizations/{organizationsId}/environments/{environmentsId}/deployments/{deploymentsId}
/// Gets a particular deployment of Api proxy or a shared flow in an environment
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `apigee_organizations_environments_deployments_get_builder()` + `apigee_organizations_environments_deployments_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_environments_deployments_get(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudApigeeV1Deployment>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = apigee_organizations_environments_deployments_get_builder(client, name)?;
    apigee_organizations_environments_deployments_get_execute(builder)
}

/// GET v1/organizations/{organizationsId}/environments/{environmentsId}/deployments/{deploymentsId}:getIamPolicy
/// Gets the IAM policy on a deployment. For more information, see [Manage users, roles, and permissions using the API](<https://cloud.google.`com/apigee/docs/api-platform/system-administration/manage-users-roles`>). You must have the apigee.deployments.`getIamPolicy` permission to call this API.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `apigee_organizations_environments_deployments_get_iam_policy_execute()` to send, or `apigee_organizations_environments_deployments_get_iam_policy` for simplest API.

pub fn apigee_organizations_environments_deployments_get_iam_policy_builder(
    client: &SimpleHttpClient,
    resource: &str,
    options_requestedPolicyVersion: Option<i32>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://apigee.googleapis.com/v1/organizations/{}/environments/{}/deployments/{}:getIamPolicy",
        resource,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = options_requestedPolicyVersion {
        query_parts.push(format!("options_requestedPolicyVersion={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/organizations/{organizationsId}/environments/{environmentsId}/deployments/{deploymentsId}:getIamPolicy
/// Gets the IAM policy on a deployment. For more information, see [Manage users, roles, and permissions using the API](<https://cloud.google.`com/apigee/docs/api-platform/system-administration/manage-users-roles`>). You must have the apigee.deployments.`getIamPolicy` permission to call this API.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `apigee_organizations_environments_deployments_get_iam_policy_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from apigee_organizations_environments_deployments_get_iam_policy_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_environments_deployments_get_iam_policy_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleIamV1Policy>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleIamV1Policy = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/organizations/{organizationsId}/environments/{environmentsId}/deployments/{deploymentsId}:getIamPolicy
/// Gets the IAM policy on a deployment. For more information, see [Manage users, roles, and permissions using the API](<https://cloud.google.`com/apigee/docs/api-platform/system-administration/manage-users-roles`>). You must have the apigee.deployments.`getIamPolicy` permission to call this API.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `apigee_organizations_environments_deployments_get_iam_policy_builder()` + `apigee_organizations_environments_deployments_get_iam_policy_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_environments_deployments_get_iam_policy(
    client: &SimpleHttpClient,
    resource: &str,
    options_requestedPolicyVersion: Option<i32>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleIamV1Policy>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = apigee_organizations_environments_deployments_get_iam_policy_builder(
        client,
        resource,
        options_requestedPolicyVersion,
    )?;
    apigee_organizations_environments_deployments_get_iam_policy_execute(builder)
}

/// GET v1/organizations/{organizationsId}/environments/{environmentsId}/deployments
/// Lists all deployments of API proxies or shared flows in an environment.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `apigee_organizations_environments_deployments_list_execute()` to send, or `apigee_organizations_environments_deployments_list` for simplest API.

pub fn apigee_organizations_environments_deployments_list_builder(
    client: &SimpleHttpClient,
    parent: &str,
    sharedFlows: Option<bool>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://apigee.googleapis.com/v1/organizations/{}/environments/{}/deployments",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = sharedFlows {
        query_parts.push(format!("sharedFlows={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/organizations/{organizationsId}/environments/{environmentsId}/deployments
/// Lists all deployments of API proxies or shared flows in an environment.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `apigee_organizations_environments_deployments_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from apigee_organizations_environments_deployments_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_environments_deployments_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudApigeeV1ListDeploymentsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudApigeeV1ListDeploymentsResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/organizations/{organizationsId}/environments/{environmentsId}/deployments
/// Lists all deployments of API proxies or shared flows in an environment.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `apigee_organizations_environments_deployments_list_builder()` + `apigee_organizations_environments_deployments_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_environments_deployments_list(
    client: &SimpleHttpClient,
    parent: &str,
    sharedFlows: Option<bool>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudApigeeV1ListDeploymentsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        apigee_organizations_environments_deployments_list_builder(client, parent, sharedFlows)?;
    apigee_organizations_environments_deployments_list_execute(builder)
}

/// GET v1/organizations/{organizationsId}/environments/{environmentsId}/deployments/{deploymentsId}:setIamPolicy
/// Sets the IAM policy on a deployment, if the policy already exists it will be replaced. For more information, see [Manage users, roles, and permissions using the API](<https://cloud.google.`com/apigee/docs/api-platform/system-administration/manage-users-roles`>). You must have the apigee.deployments.`setIamPolicy` permission to call this API.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `apigee_organizations_environments_deployments_set_iam_policy_execute()` to send, or `apigee_organizations_environments_deployments_set_iam_policy` for simplest API.

pub fn apigee_organizations_environments_deployments_set_iam_policy_builder(
    client: &SimpleHttpClient,
    resource: &str,
    body: &GoogleIamV1SetIamPolicyRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://apigee.googleapis.com/v1/organizations/{}/environments/{}/deployments/{}:setIamPolicy",
        resource,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/organizations/{organizationsId}/environments/{environmentsId}/deployments/{deploymentsId}:setIamPolicy
/// Sets the IAM policy on a deployment, if the policy already exists it will be replaced. For more information, see [Manage users, roles, and permissions using the API](<https://cloud.google.`com/apigee/docs/api-platform/system-administration/manage-users-roles`>). You must have the apigee.deployments.`setIamPolicy` permission to call this API.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `apigee_organizations_environments_deployments_set_iam_policy_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from apigee_organizations_environments_deployments_set_iam_policy_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_environments_deployments_set_iam_policy_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleIamV1Policy>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleIamV1Policy = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/organizations/{organizationsId}/environments/{environmentsId}/deployments/{deploymentsId}:setIamPolicy
/// Sets the IAM policy on a deployment, if the policy already exists it will be replaced. For more information, see [Manage users, roles, and permissions using the API](<https://cloud.google.`com/apigee/docs/api-platform/system-administration/manage-users-roles`>). You must have the apigee.deployments.`setIamPolicy` permission to call this API.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `apigee_organizations_environments_deployments_set_iam_policy_builder()` + `apigee_organizations_environments_deployments_set_iam_policy_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_environments_deployments_set_iam_policy(
    client: &SimpleHttpClient,
    resource: &str,
    body: &GoogleIamV1SetIamPolicyRequest,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleIamV1Policy>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = apigee_organizations_environments_deployments_set_iam_policy_builder(
        client, resource, body,
    )?;
    apigee_organizations_environments_deployments_set_iam_policy_execute(builder)
}

/// GET v1/organizations/{organizationsId}/environments/{environmentsId}/deployments/{deploymentsId}:testIamPermissions
/// Tests the permissions of a user on a deployment, and returns a subset of permissions that the user has on the deployment. If the deployment does not exist, an empty permission set is returned (a NOT_FOUND error is not returned).
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `apigee_organizations_environments_deployments_test_iam_permissions_execute()` to send, or `apigee_organizations_environments_deployments_test_iam_permissions` for simplest API.

pub fn apigee_organizations_environments_deployments_test_iam_permissions_builder(
    client: &SimpleHttpClient,
    resource: &str,
    body: &GoogleIamV1TestIamPermissionsRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://apigee.googleapis.com/v1/organizations/{}/environments/{}/deployments/{}:testIamPermissions",
        resource,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/organizations/{organizationsId}/environments/{environmentsId}/deployments/{deploymentsId}:testIamPermissions
/// Tests the permissions of a user on a deployment, and returns a subset of permissions that the user has on the deployment. If the deployment does not exist, an empty permission set is returned (a NOT_FOUND error is not returned).
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `apigee_organizations_environments_deployments_test_iam_permissions_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from apigee_organizations_environments_deployments_test_iam_permissions_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_environments_deployments_test_iam_permissions_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleIamV1TestIamPermissionsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleIamV1TestIamPermissionsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/organizations/{organizationsId}/environments/{environmentsId}/deployments/{deploymentsId}:testIamPermissions
/// Tests the permissions of a user on a deployment, and returns a subset of permissions that the user has on the deployment. If the deployment does not exist, an empty permission set is returned (a NOT_FOUND error is not returned).
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `apigee_organizations_environments_deployments_test_iam_permissions_builder()` + `apigee_organizations_environments_deployments_test_iam_permissions_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_environments_deployments_test_iam_permissions(
    client: &SimpleHttpClient,
    resource: &str,
    body: &GoogleIamV1TestIamPermissionsRequest,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleIamV1TestIamPermissionsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = apigee_organizations_environments_deployments_test_iam_permissions_builder(
        client, resource, body,
    )?;
    apigee_organizations_environments_deployments_test_iam_permissions_execute(builder)
}

/// GET v1/organizations/{organizationsId}/environments/{environmentsId}/flowhooks/{flowhooksId}
/// Attaches a shared flow to a flow hook.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `apigee_organizations_environments_flowhooks_attach_shared_flow_to_flow_hook_execute()` to send, or `apigee_organizations_environments_flowhooks_attach_shared_flow_to_flow_hook` for simplest API.

pub fn apigee_organizations_environments_flowhooks_attach_shared_flow_to_flow_hook_builder(
    client: &SimpleHttpClient,
    name: &str,
    body: &GoogleCloudApigeeV1FlowHook,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://apigee.googleapis.com/v1/organizations/{}/environments/{}/flowhooks/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/organizations/{organizationsId}/environments/{environmentsId}/flowhooks/{flowhooksId}
/// Attaches a shared flow to a flow hook.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `apigee_organizations_environments_flowhooks_attach_shared_flow_to_flow_hook_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from apigee_organizations_environments_flowhooks_attach_shared_flow_to_flow_hook_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_environments_flowhooks_attach_shared_flow_to_flow_hook_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudApigeeV1FlowHook>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudApigeeV1FlowHook = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/organizations/{organizationsId}/environments/{environmentsId}/flowhooks/{flowhooksId}
/// Attaches a shared flow to a flow hook.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `apigee_organizations_environments_flowhooks_attach_shared_flow_to_flow_hook_builder()` + `apigee_organizations_environments_flowhooks_attach_shared_flow_to_flow_hook_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_environments_flowhooks_attach_shared_flow_to_flow_hook(
    client: &SimpleHttpClient,
    name: &str,
    body: &GoogleCloudApigeeV1FlowHook,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudApigeeV1FlowHook>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        apigee_organizations_environments_flowhooks_attach_shared_flow_to_flow_hook_builder(
            client, name, body,
        )?;
    apigee_organizations_environments_flowhooks_attach_shared_flow_to_flow_hook_execute(builder)
}

/// GET v1/organizations/{organizationsId}/environments/{environmentsId}/flowhooks/{flowhooksId}
/// Detaches a shared flow from a flow hook.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `apigee_organizations_environments_flowhooks_detach_shared_flow_from_flow_hook_execute()` to send, or `apigee_organizations_environments_flowhooks_detach_shared_flow_from_flow_hook` for simplest API.

pub fn apigee_organizations_environments_flowhooks_detach_shared_flow_from_flow_hook_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://apigee.googleapis.com/v1/organizations/{}/environments/{}/flowhooks/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/organizations/{organizationsId}/environments/{environmentsId}/flowhooks/{flowhooksId}
/// Detaches a shared flow from a flow hook.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `apigee_organizations_environments_flowhooks_detach_shared_flow_from_flow_hook_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from apigee_organizations_environments_flowhooks_detach_shared_flow_from_flow_hook_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_environments_flowhooks_detach_shared_flow_from_flow_hook_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudApigeeV1FlowHook>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudApigeeV1FlowHook = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/organizations/{organizationsId}/environments/{environmentsId}/flowhooks/{flowhooksId}
/// Detaches a shared flow from a flow hook.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `apigee_organizations_environments_flowhooks_detach_shared_flow_from_flow_hook_builder()` + `apigee_organizations_environments_flowhooks_detach_shared_flow_from_flow_hook_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_environments_flowhooks_detach_shared_flow_from_flow_hook(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudApigeeV1FlowHook>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        apigee_organizations_environments_flowhooks_detach_shared_flow_from_flow_hook_builder(
            client, name,
        )?;
    apigee_organizations_environments_flowhooks_detach_shared_flow_from_flow_hook_execute(builder)
}

/// GET v1/organizations/{organizationsId}/environments/{environmentsId}/flowhooks/{flowhooksId}
/// Returns the name of the shared flow attached to the specified flow hook. If there's no shared flow attached to the flow hook, the API does not return an error; it simply does not return a name in the response.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `apigee_organizations_environments_flowhooks_get_execute()` to send, or `apigee_organizations_environments_flowhooks_get` for simplest API.

pub fn apigee_organizations_environments_flowhooks_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://apigee.googleapis.com/v1/organizations/{}/environments/{}/flowhooks/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/organizations/{organizationsId}/environments/{environmentsId}/flowhooks/{flowhooksId}
/// Returns the name of the shared flow attached to the specified flow hook. If there's no shared flow attached to the flow hook, the API does not return an error; it simply does not return a name in the response.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `apigee_organizations_environments_flowhooks_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from apigee_organizations_environments_flowhooks_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_environments_flowhooks_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudApigeeV1FlowHook>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudApigeeV1FlowHook = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/organizations/{organizationsId}/environments/{environmentsId}/flowhooks/{flowhooksId}
/// Returns the name of the shared flow attached to the specified flow hook. If there's no shared flow attached to the flow hook, the API does not return an error; it simply does not return a name in the response.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `apigee_organizations_environments_flowhooks_get_builder()` + `apigee_organizations_environments_flowhooks_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_environments_flowhooks_get(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudApigeeV1FlowHook>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = apigee_organizations_environments_flowhooks_get_builder(client, name)?;
    apigee_organizations_environments_flowhooks_get_execute(builder)
}

/// GET v1/organizations/{organizationsId}/environments/{environmentsId}/keystores
/// Creates a keystore or truststore. - Keystore: Contains certificates and their associated keys. - Truststore: Contains trusted certificates used to validate a server's certificate. These certificates are typically self-signed certificates or certificates that are not signed by a trusted CA.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `apigee_organizations_environments_keystores_create_execute()` to send, or `apigee_organizations_environments_keystores_create` for simplest API.

pub fn apigee_organizations_environments_keystores_create_builder(
    client: &SimpleHttpClient,
    parent: &str,
    name: Option<&str>,
    body: &GoogleCloudApigeeV1Keystore,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://apigee.googleapis.com/v1/organizations/{}/environments/{}/keystores",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = name {
        query_parts.push(format!("name={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/organizations/{organizationsId}/environments/{environmentsId}/keystores
/// Creates a keystore or truststore. - Keystore: Contains certificates and their associated keys. - Truststore: Contains trusted certificates used to validate a server's certificate. These certificates are typically self-signed certificates or certificates that are not signed by a trusted CA.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `apigee_organizations_environments_keystores_create_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from apigee_organizations_environments_keystores_create_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_environments_keystores_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudApigeeV1Keystore>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudApigeeV1Keystore = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/organizations/{organizationsId}/environments/{environmentsId}/keystores
/// Creates a keystore or truststore. - Keystore: Contains certificates and their associated keys. - Truststore: Contains trusted certificates used to validate a server's certificate. These certificates are typically self-signed certificates or certificates that are not signed by a trusted CA.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `apigee_organizations_environments_keystores_create_builder()` + `apigee_organizations_environments_keystores_create_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_environments_keystores_create(
    client: &SimpleHttpClient,
    parent: &str,
    name: Option<&str>,
    body: &GoogleCloudApigeeV1Keystore,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudApigeeV1Keystore>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        apigee_organizations_environments_keystores_create_builder(client, parent, name, body)?;
    apigee_organizations_environments_keystores_create_execute(builder)
}

/// GET v1/organizations/{organizationsId}/environments/{environmentsId}/keystores/{keystoresId}
/// Deletes a keystore or truststore.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `apigee_organizations_environments_keystores_delete_execute()` to send, or `apigee_organizations_environments_keystores_delete` for simplest API.

pub fn apigee_organizations_environments_keystores_delete_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://apigee.googleapis.com/v1/organizations/{}/environments/{}/keystores/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/organizations/{organizationsId}/environments/{environmentsId}/keystores/{keystoresId}
/// Deletes a keystore or truststore.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `apigee_organizations_environments_keystores_delete_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from apigee_organizations_environments_keystores_delete_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_environments_keystores_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudApigeeV1Keystore>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudApigeeV1Keystore = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/organizations/{organizationsId}/environments/{environmentsId}/keystores/{keystoresId}
/// Deletes a keystore or truststore.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `apigee_organizations_environments_keystores_delete_builder()` + `apigee_organizations_environments_keystores_delete_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_environments_keystores_delete(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudApigeeV1Keystore>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = apigee_organizations_environments_keystores_delete_builder(client, name)?;
    apigee_organizations_environments_keystores_delete_execute(builder)
}

/// GET v1/organizations/{organizationsId}/environments/{environmentsId}/keystores/{keystoresId}
/// Gets a keystore or truststore.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `apigee_organizations_environments_keystores_get_execute()` to send, or `apigee_organizations_environments_keystores_get` for simplest API.

pub fn apigee_organizations_environments_keystores_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://apigee.googleapis.com/v1/organizations/{}/environments/{}/keystores/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/organizations/{organizationsId}/environments/{environmentsId}/keystores/{keystoresId}
/// Gets a keystore or truststore.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `apigee_organizations_environments_keystores_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from apigee_organizations_environments_keystores_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_environments_keystores_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudApigeeV1Keystore>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudApigeeV1Keystore = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/organizations/{organizationsId}/environments/{environmentsId}/keystores/{keystoresId}
/// Gets a keystore or truststore.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `apigee_organizations_environments_keystores_get_builder()` + `apigee_organizations_environments_keystores_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_environments_keystores_get(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudApigeeV1Keystore>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = apigee_organizations_environments_keystores_get_builder(client, name)?;
    apigee_organizations_environments_keystores_get_execute(builder)
}

/// GET v1/organizations/{organizationsId}/environments/{environmentsId}/keystores/{keystoresId}/aliases
/// Creates an alias from a `key/certificate` pair. The structure of the request is controlled by the format query parameter: - keycertfile - Separate PEM-encoded key and certificate files are uploaded. Set Content-Type: `multipart/form-data` and include the `keyFile`, `certFile`, and password (if keys are encrypted) fields in the request body. If uploading to a truststore, omit `keyFile`. - pkcs12 - A PKCS12 file is uploaded. Set Content-Type: `multipart/form-data`, provide the file in the file field, and include the password field if the file is encrypted in the request body. - selfsignedcert - A new private key and certificate are generated. Set Content-Type: `application/json` and include CertificateGenerationSpec in the request body.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `apigee_organizations_environments_keystores_aliases_create_execute()` to send, or `apigee_organizations_environments_keystores_aliases_create` for simplest API.

pub fn apigee_organizations_environments_keystores_aliases_create_builder(
    client: &SimpleHttpClient,
    parent: &str,
    _password: Option<&str>,
    alias: Option<&str>,
    format: Option<&str>,
    ignoreExpiryValidation: Option<bool>,
    ignoreNewlineValidation: Option<bool>,
    body: &GoogleApiHttpBody,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://apigee.googleapis.com/v1/organizations/{}/environments/{}/keystores/{}/aliases",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = _password {
        query_parts.push(format!("_password={}", val));
    }
    if let Some(val) = alias {
        query_parts.push(format!("alias={}", val));
    }
    if let Some(val) = format {
        query_parts.push(format!("format={}", val));
    }
    if let Some(val) = ignoreExpiryValidation {
        query_parts.push(format!("ignoreExpiryValidation={}", val));
    }
    if let Some(val) = ignoreNewlineValidation {
        query_parts.push(format!("ignoreNewlineValidation={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/organizations/{organizationsId}/environments/{environmentsId}/keystores/{keystoresId}/aliases
/// Creates an alias from a `key/certificate` pair. The structure of the request is controlled by the format query parameter: - keycertfile - Separate PEM-encoded key and certificate files are uploaded. Set Content-Type: `multipart/form-data` and include the `keyFile`, `certFile`, and password (if keys are encrypted) fields in the request body. If uploading to a truststore, omit `keyFile`. - pkcs12 - A PKCS12 file is uploaded. Set Content-Type: `multipart/form-data`, provide the file in the file field, and include the password field if the file is encrypted in the request body. - selfsignedcert - A new private key and certificate are generated. Set Content-Type: `application/json` and include CertificateGenerationSpec in the request body.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `apigee_organizations_environments_keystores_aliases_create_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from apigee_organizations_environments_keystores_aliases_create_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_environments_keystores_aliases_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleCloudApigeeV1Alias>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudApigeeV1Alias = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/organizations/{organizationsId}/environments/{environmentsId}/keystores/{keystoresId}/aliases
/// Creates an alias from a `key/certificate` pair. The structure of the request is controlled by the format query parameter: - keycertfile - Separate PEM-encoded key and certificate files are uploaded. Set Content-Type: `multipart/form-data` and include the `keyFile`, `certFile`, and password (if keys are encrypted) fields in the request body. If uploading to a truststore, omit `keyFile`. - pkcs12 - A PKCS12 file is uploaded. Set Content-Type: `multipart/form-data`, provide the file in the file field, and include the password field if the file is encrypted in the request body. - selfsignedcert - A new private key and certificate are generated. Set Content-Type: `application/json` and include CertificateGenerationSpec in the request body.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `apigee_organizations_environments_keystores_aliases_create_builder()` + `apigee_organizations_environments_keystores_aliases_create_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_environments_keystores_aliases_create(
    client: &SimpleHttpClient,
    parent: &str,
    _password: Option<&str>,
    alias: Option<&str>,
    format: Option<&str>,
    ignoreExpiryValidation: Option<bool>,
    ignoreNewlineValidation: Option<bool>,
    body: &GoogleApiHttpBody,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleCloudApigeeV1Alias>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = apigee_organizations_environments_keystores_aliases_create_builder(
        client,
        parent,
        _password,
        alias,
        format,
        ignoreExpiryValidation,
        ignoreNewlineValidation,
        body,
    )?;
    apigee_organizations_environments_keystores_aliases_create_execute(builder)
}

/// GET v1/organizations/{organizationsId}/environments/{environmentsId}/keystores/{keystoresId}/aliases/{aliasesId}/csr
/// Generates a PKCS #10 Certificate Signing Request for the private key in an alias.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `apigee_organizations_environments_keystores_aliases_csr_execute()` to send, or `apigee_organizations_environments_keystores_aliases_csr` for simplest API.

pub fn apigee_organizations_environments_keystores_aliases_csr_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://apigee.googleapis.com/v1/organizations/{}/environments/{}/keystores/{}/aliases/{}/csr",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/organizations/{organizationsId}/environments/{environmentsId}/keystores/{keystoresId}/aliases/{aliasesId}/csr
/// Generates a PKCS #10 Certificate Signing Request for the private key in an alias.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `apigee_organizations_environments_keystores_aliases_csr_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from apigee_organizations_environments_keystores_aliases_csr_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_environments_keystores_aliases_csr_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleApiHttpBody>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleApiHttpBody = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/organizations/{organizationsId}/environments/{environmentsId}/keystores/{keystoresId}/aliases/{aliasesId}/csr
/// Generates a PKCS #10 Certificate Signing Request for the private key in an alias.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `apigee_organizations_environments_keystores_aliases_csr_builder()` + `apigee_organizations_environments_keystores_aliases_csr_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_environments_keystores_aliases_csr(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleApiHttpBody>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = apigee_organizations_environments_keystores_aliases_csr_builder(client, name)?;
    apigee_organizations_environments_keystores_aliases_csr_execute(builder)
}

/// GET v1/organizations/{organizationsId}/environments/{environmentsId}/keystores/{keystoresId}/aliases/{aliasesId}
/// Deletes an alias.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `apigee_organizations_environments_keystores_aliases_delete_execute()` to send, or `apigee_organizations_environments_keystores_aliases_delete` for simplest API.

pub fn apigee_organizations_environments_keystores_aliases_delete_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://apigee.googleapis.com/v1/organizations/{}/environments/{}/keystores/{}/aliases/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/organizations/{organizationsId}/environments/{environmentsId}/keystores/{keystoresId}/aliases/{aliasesId}
/// Deletes an alias.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `apigee_organizations_environments_keystores_aliases_delete_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from apigee_organizations_environments_keystores_aliases_delete_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_environments_keystores_aliases_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleCloudApigeeV1Alias>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudApigeeV1Alias = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/organizations/{organizationsId}/environments/{environmentsId}/keystores/{keystoresId}/aliases/{aliasesId}
/// Deletes an alias.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `apigee_organizations_environments_keystores_aliases_delete_builder()` + `apigee_organizations_environments_keystores_aliases_delete_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_environments_keystores_aliases_delete(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleCloudApigeeV1Alias>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = apigee_organizations_environments_keystores_aliases_delete_builder(client, name)?;
    apigee_organizations_environments_keystores_aliases_delete_execute(builder)
}

/// GET v1/organizations/{organizationsId}/environments/{environmentsId}/keystores/{keystoresId}/aliases/{aliasesId}
/// Gets an alias.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `apigee_organizations_environments_keystores_aliases_get_execute()` to send, or `apigee_organizations_environments_keystores_aliases_get` for simplest API.

pub fn apigee_organizations_environments_keystores_aliases_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://apigee.googleapis.com/v1/organizations/{}/environments/{}/keystores/{}/aliases/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/organizations/{organizationsId}/environments/{environmentsId}/keystores/{keystoresId}/aliases/{aliasesId}
/// Gets an alias.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `apigee_organizations_environments_keystores_aliases_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from apigee_organizations_environments_keystores_aliases_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_environments_keystores_aliases_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleCloudApigeeV1Alias>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudApigeeV1Alias = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/organizations/{organizationsId}/environments/{environmentsId}/keystores/{keystoresId}/aliases/{aliasesId}
/// Gets an alias.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `apigee_organizations_environments_keystores_aliases_get_builder()` + `apigee_organizations_environments_keystores_aliases_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_environments_keystores_aliases_get(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleCloudApigeeV1Alias>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = apigee_organizations_environments_keystores_aliases_get_builder(client, name)?;
    apigee_organizations_environments_keystores_aliases_get_execute(builder)
}

/// GET v1/organizations/{organizationsId}/environments/{environmentsId}/keystores/{keystoresId}/aliases/{aliasesId}/certificate
/// Gets the certificate from an alias in PEM-encoded form.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `apigee_organizations_environments_keystores_aliases_get_certificate_execute()` to send, or `apigee_organizations_environments_keystores_aliases_get_certificate` for simplest API.

pub fn apigee_organizations_environments_keystores_aliases_get_certificate_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://apigee.googleapis.com/v1/organizations/{}/environments/{}/keystores/{}/aliases/{}/certificate",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/organizations/{organizationsId}/environments/{environmentsId}/keystores/{keystoresId}/aliases/{aliasesId}/certificate
/// Gets the certificate from an alias in PEM-encoded form.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `apigee_organizations_environments_keystores_aliases_get_certificate_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from apigee_organizations_environments_keystores_aliases_get_certificate_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_environments_keystores_aliases_get_certificate_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleApiHttpBody>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleApiHttpBody = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/organizations/{organizationsId}/environments/{environmentsId}/keystores/{keystoresId}/aliases/{aliasesId}/certificate
/// Gets the certificate from an alias in PEM-encoded form.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `apigee_organizations_environments_keystores_aliases_get_certificate_builder()` + `apigee_organizations_environments_keystores_aliases_get_certificate_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_environments_keystores_aliases_get_certificate(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleApiHttpBody>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        apigee_organizations_environments_keystores_aliases_get_certificate_builder(client, name)?;
    apigee_organizations_environments_keystores_aliases_get_certificate_execute(builder)
}

/// GET v1/organizations/{organizationsId}/environments/{environmentsId}/keystores/{keystoresId}/aliases/{aliasesId}
/// Updates the certificate in an alias. The updated certificate must be in PEM- or DER-encoded X.509 format.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `apigee_organizations_environments_keystores_aliases_update_execute()` to send, or `apigee_organizations_environments_keystores_aliases_update` for simplest API.

pub fn apigee_organizations_environments_keystores_aliases_update_builder(
    client: &SimpleHttpClient,
    name: &str,
    ignoreExpiryValidation: Option<bool>,
    ignoreNewlineValidation: Option<bool>,
    body: &GoogleApiHttpBody,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://apigee.googleapis.com/v1/organizations/{}/environments/{}/keystores/{}/aliases/{}",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = ignoreExpiryValidation {
        query_parts.push(format!("ignoreExpiryValidation={}", val));
    }
    if let Some(val) = ignoreNewlineValidation {
        query_parts.push(format!("ignoreNewlineValidation={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/organizations/{organizationsId}/environments/{environmentsId}/keystores/{keystoresId}/aliases/{aliasesId}
/// Updates the certificate in an alias. The updated certificate must be in PEM- or DER-encoded X.509 format.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `apigee_organizations_environments_keystores_aliases_update_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from apigee_organizations_environments_keystores_aliases_update_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_environments_keystores_aliases_update_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleCloudApigeeV1Alias>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudApigeeV1Alias = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/organizations/{organizationsId}/environments/{environmentsId}/keystores/{keystoresId}/aliases/{aliasesId}
/// Updates the certificate in an alias. The updated certificate must be in PEM- or DER-encoded X.509 format.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `apigee_organizations_environments_keystores_aliases_update_builder()` + `apigee_organizations_environments_keystores_aliases_update_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_environments_keystores_aliases_update(
    client: &SimpleHttpClient,
    name: &str,
    ignoreExpiryValidation: Option<bool>,
    ignoreNewlineValidation: Option<bool>,
    body: &GoogleApiHttpBody,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleCloudApigeeV1Alias>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = apigee_organizations_environments_keystores_aliases_update_builder(
        client,
        name,
        ignoreExpiryValidation,
        ignoreNewlineValidation,
        body,
    )?;
    apigee_organizations_environments_keystores_aliases_update_execute(builder)
}

/// GET v1/organizations/{organizationsId}/environments/{environmentsId}/keyvaluemaps
/// Creates a key value map in an environment.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `apigee_organizations_environments_keyvaluemaps_create_execute()` to send, or `apigee_organizations_environments_keyvaluemaps_create` for simplest API.

pub fn apigee_organizations_environments_keyvaluemaps_create_builder(
    client: &SimpleHttpClient,
    parent: &str,
    body: &GoogleCloudApigeeV1KeyValueMap,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://apigee.googleapis.com/v1/organizations/{}/environments/{}/keyvaluemaps",
        parent,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/organizations/{organizationsId}/environments/{environmentsId}/keyvaluemaps
/// Creates a key value map in an environment.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `apigee_organizations_environments_keyvaluemaps_create_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from apigee_organizations_environments_keyvaluemaps_create_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_environments_keyvaluemaps_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudApigeeV1KeyValueMap>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudApigeeV1KeyValueMap = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/organizations/{organizationsId}/environments/{environmentsId}/keyvaluemaps
/// Creates a key value map in an environment.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `apigee_organizations_environments_keyvaluemaps_create_builder()` + `apigee_organizations_environments_keyvaluemaps_create_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_environments_keyvaluemaps_create(
    client: &SimpleHttpClient,
    parent: &str,
    body: &GoogleCloudApigeeV1KeyValueMap,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudApigeeV1KeyValueMap>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        apigee_organizations_environments_keyvaluemaps_create_builder(client, parent, body)?;
    apigee_organizations_environments_keyvaluemaps_create_execute(builder)
}

/// GET v1/organizations/{organizationsId}/environments/{environmentsId}/keyvaluemaps/{keyvaluemapsId}
/// Deletes a key value map from an environment.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `apigee_organizations_environments_keyvaluemaps_delete_execute()` to send, or `apigee_organizations_environments_keyvaluemaps_delete` for simplest API.

pub fn apigee_organizations_environments_keyvaluemaps_delete_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://apigee.googleapis.com/v1/organizations/{}/environments/{}/keyvaluemaps/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/organizations/{organizationsId}/environments/{environmentsId}/keyvaluemaps/{keyvaluemapsId}
/// Deletes a key value map from an environment.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `apigee_organizations_environments_keyvaluemaps_delete_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from apigee_organizations_environments_keyvaluemaps_delete_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_environments_keyvaluemaps_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudApigeeV1KeyValueMap>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudApigeeV1KeyValueMap = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/organizations/{organizationsId}/environments/{environmentsId}/keyvaluemaps/{keyvaluemapsId}
/// Deletes a key value map from an environment.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `apigee_organizations_environments_keyvaluemaps_delete_builder()` + `apigee_organizations_environments_keyvaluemaps_delete_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_environments_keyvaluemaps_delete(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudApigeeV1KeyValueMap>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = apigee_organizations_environments_keyvaluemaps_delete_builder(client, name)?;
    apigee_organizations_environments_keyvaluemaps_delete_execute(builder)
}

/// GET v1/organizations/{organizationsId}/environments/{environmentsId}/keyvaluemaps/{keyvaluemapsId}
/// Get the key value map scoped to an organization, environment, or API proxy.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `apigee_organizations_environments_keyvaluemaps_get_execute()` to send, or `apigee_organizations_environments_keyvaluemaps_get` for simplest API.

pub fn apigee_organizations_environments_keyvaluemaps_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://apigee.googleapis.com/v1/organizations/{}/environments/{}/keyvaluemaps/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/organizations/{organizationsId}/environments/{environmentsId}/keyvaluemaps/{keyvaluemapsId}
/// Get the key value map scoped to an organization, environment, or API proxy.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `apigee_organizations_environments_keyvaluemaps_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from apigee_organizations_environments_keyvaluemaps_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_environments_keyvaluemaps_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudApigeeV1KeyValueMap>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudApigeeV1KeyValueMap = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/organizations/{organizationsId}/environments/{environmentsId}/keyvaluemaps/{keyvaluemapsId}
/// Get the key value map scoped to an organization, environment, or API proxy.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `apigee_organizations_environments_keyvaluemaps_get_builder()` + `apigee_organizations_environments_keyvaluemaps_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_environments_keyvaluemaps_get(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudApigeeV1KeyValueMap>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = apigee_organizations_environments_keyvaluemaps_get_builder(client, name)?;
    apigee_organizations_environments_keyvaluemaps_get_execute(builder)
}

/// GET v1/organizations/{organizationsId}/environments/{environmentsId}/keyvaluemaps/{keyvaluemapsId}
/// Update the key value map scoped to an organization, environment, or API proxy.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `apigee_organizations_environments_keyvaluemaps_update_execute()` to send, or `apigee_organizations_environments_keyvaluemaps_update` for simplest API.

pub fn apigee_organizations_environments_keyvaluemaps_update_builder(
    client: &SimpleHttpClient,
    name: &str,
    body: &GoogleCloudApigeeV1KeyValueMap,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://apigee.googleapis.com/v1/organizations/{}/environments/{}/keyvaluemaps/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/organizations/{organizationsId}/environments/{environmentsId}/keyvaluemaps/{keyvaluemapsId}
/// Update the key value map scoped to an organization, environment, or API proxy.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `apigee_organizations_environments_keyvaluemaps_update_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from apigee_organizations_environments_keyvaluemaps_update_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_environments_keyvaluemaps_update_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudApigeeV1KeyValueMap>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudApigeeV1KeyValueMap = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/organizations/{organizationsId}/environments/{environmentsId}/keyvaluemaps/{keyvaluemapsId}
/// Update the key value map scoped to an organization, environment, or API proxy.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `apigee_organizations_environments_keyvaluemaps_update_builder()` + `apigee_organizations_environments_keyvaluemaps_update_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_environments_keyvaluemaps_update(
    client: &SimpleHttpClient,
    name: &str,
    body: &GoogleCloudApigeeV1KeyValueMap,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudApigeeV1KeyValueMap>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        apigee_organizations_environments_keyvaluemaps_update_builder(client, name, body)?;
    apigee_organizations_environments_keyvaluemaps_update_execute(builder)
}

/// GET v1/organizations/{organizationsId}/environments/{environmentsId}/keyvaluemaps/{keyvaluemapsId}/entries
/// Creates key value entries in a key value map scoped to an organization, environment, or API proxy. **Note**: Supported for Apigee hybrid 1.8.x and higher.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `apigee_organizations_environments_keyvaluemaps_entries_create_execute()` to send, or `apigee_organizations_environments_keyvaluemaps_entries_create` for simplest API.

pub fn apigee_organizations_environments_keyvaluemaps_entries_create_builder(
    client: &SimpleHttpClient,
    parent: &str,
    body: &GoogleCloudApigeeV1KeyValueEntry,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://apigee.googleapis.com/v1/organizations/{}/environments/{}/keyvaluemaps/{}/entries",
        parent,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/organizations/{organizationsId}/environments/{environmentsId}/keyvaluemaps/{keyvaluemapsId}/entries
/// Creates key value entries in a key value map scoped to an organization, environment, or API proxy. **Note**: Supported for Apigee hybrid 1.8.x and higher.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `apigee_organizations_environments_keyvaluemaps_entries_create_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from apigee_organizations_environments_keyvaluemaps_entries_create_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_environments_keyvaluemaps_entries_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudApigeeV1KeyValueEntry>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudApigeeV1KeyValueEntry = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/organizations/{organizationsId}/environments/{environmentsId}/keyvaluemaps/{keyvaluemapsId}/entries
/// Creates key value entries in a key value map scoped to an organization, environment, or API proxy. **Note**: Supported for Apigee hybrid 1.8.x and higher.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `apigee_organizations_environments_keyvaluemaps_entries_create_builder()` + `apigee_organizations_environments_keyvaluemaps_entries_create_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_environments_keyvaluemaps_entries_create(
    client: &SimpleHttpClient,
    parent: &str,
    body: &GoogleCloudApigeeV1KeyValueEntry,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudApigeeV1KeyValueEntry>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = apigee_organizations_environments_keyvaluemaps_entries_create_builder(
        client, parent, body,
    )?;
    apigee_organizations_environments_keyvaluemaps_entries_create_execute(builder)
}

/// GET v1/organizations/{organizationsId}/environments/{environmentsId}/keyvaluemaps/{keyvaluemapsId}/entries/{entriesId}
/// Deletes a key value entry from a key value map scoped to an organization, environment, or API proxy. **Notes:** * After you delete the key value entry, the policy consuming the entry will continue to function with its cached values for a few minutes. This is expected behavior. * Supported for Apigee hybrid 1.8.x and higher.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `apigee_organizations_environments_keyvaluemaps_entries_delete_execute()` to send, or `apigee_organizations_environments_keyvaluemaps_entries_delete` for simplest API.

pub fn apigee_organizations_environments_keyvaluemaps_entries_delete_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://apigee.googleapis.com/v1/organizations/{}/environments/{}/keyvaluemaps/{}/entries/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/organizations/{organizationsId}/environments/{environmentsId}/keyvaluemaps/{keyvaluemapsId}/entries/{entriesId}
/// Deletes a key value entry from a key value map scoped to an organization, environment, or API proxy. **Notes:** * After you delete the key value entry, the policy consuming the entry will continue to function with its cached values for a few minutes. This is expected behavior. * Supported for Apigee hybrid 1.8.x and higher.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `apigee_organizations_environments_keyvaluemaps_entries_delete_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from apigee_organizations_environments_keyvaluemaps_entries_delete_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_environments_keyvaluemaps_entries_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudApigeeV1KeyValueEntry>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudApigeeV1KeyValueEntry = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/organizations/{organizationsId}/environments/{environmentsId}/keyvaluemaps/{keyvaluemapsId}/entries/{entriesId}
/// Deletes a key value entry from a key value map scoped to an organization, environment, or API proxy. **Notes:** * After you delete the key value entry, the policy consuming the entry will continue to function with its cached values for a few minutes. This is expected behavior. * Supported for Apigee hybrid 1.8.x and higher.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `apigee_organizations_environments_keyvaluemaps_entries_delete_builder()` + `apigee_organizations_environments_keyvaluemaps_entries_delete_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_environments_keyvaluemaps_entries_delete(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudApigeeV1KeyValueEntry>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        apigee_organizations_environments_keyvaluemaps_entries_delete_builder(client, name)?;
    apigee_organizations_environments_keyvaluemaps_entries_delete_execute(builder)
}

/// GET v1/organizations/{organizationsId}/environments/{environmentsId}/keyvaluemaps/{keyvaluemapsId}/entries/{entriesId}
/// Get the key value entry value for a key value map scoped to an organization, environment, or API proxy. **Note**: Supported for Apigee hybrid 1.8.x and higher.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `apigee_organizations_environments_keyvaluemaps_entries_get_execute()` to send, or `apigee_organizations_environments_keyvaluemaps_entries_get` for simplest API.

pub fn apigee_organizations_environments_keyvaluemaps_entries_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://apigee.googleapis.com/v1/organizations/{}/environments/{}/keyvaluemaps/{}/entries/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/organizations/{organizationsId}/environments/{environmentsId}/keyvaluemaps/{keyvaluemapsId}/entries/{entriesId}
/// Get the key value entry value for a key value map scoped to an organization, environment, or API proxy. **Note**: Supported for Apigee hybrid 1.8.x and higher.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `apigee_organizations_environments_keyvaluemaps_entries_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from apigee_organizations_environments_keyvaluemaps_entries_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_environments_keyvaluemaps_entries_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudApigeeV1KeyValueEntry>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudApigeeV1KeyValueEntry = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/organizations/{organizationsId}/environments/{environmentsId}/keyvaluemaps/{keyvaluemapsId}/entries/{entriesId}
/// Get the key value entry value for a key value map scoped to an organization, environment, or API proxy. **Note**: Supported for Apigee hybrid 1.8.x and higher.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `apigee_organizations_environments_keyvaluemaps_entries_get_builder()` + `apigee_organizations_environments_keyvaluemaps_entries_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_environments_keyvaluemaps_entries_get(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudApigeeV1KeyValueEntry>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = apigee_organizations_environments_keyvaluemaps_entries_get_builder(client, name)?;
    apigee_organizations_environments_keyvaluemaps_entries_get_execute(builder)
}

/// GET v1/organizations/{organizationsId}/environments/{environmentsId}/keyvaluemaps/{keyvaluemapsId}/entries
/// Lists key value entries for key values maps scoped to an organization, environment, or API proxy. **Note**: Supported for Apigee hybrid 1.8.x and higher.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `apigee_organizations_environments_keyvaluemaps_entries_list_execute()` to send, or `apigee_organizations_environments_keyvaluemaps_entries_list` for simplest API.

pub fn apigee_organizations_environments_keyvaluemaps_entries_list_builder(
    client: &SimpleHttpClient,
    parent: &str,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://apigee.googleapis.com/v1/organizations/{}/environments/{}/keyvaluemaps/{}/entries",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/organizations/{organizationsId}/environments/{environmentsId}/keyvaluemaps/{keyvaluemapsId}/entries
/// Lists key value entries for key values maps scoped to an organization, environment, or API proxy. **Note**: Supported for Apigee hybrid 1.8.x and higher.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `apigee_organizations_environments_keyvaluemaps_entries_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from apigee_organizations_environments_keyvaluemaps_entries_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_environments_keyvaluemaps_entries_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudApigeeV1ListKeyValueEntriesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudApigeeV1ListKeyValueEntriesResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/organizations/{organizationsId}/environments/{environmentsId}/keyvaluemaps/{keyvaluemapsId}/entries
/// Lists key value entries for key values maps scoped to an organization, environment, or API proxy. **Note**: Supported for Apigee hybrid 1.8.x and higher.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `apigee_organizations_environments_keyvaluemaps_entries_list_builder()` + `apigee_organizations_environments_keyvaluemaps_entries_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_environments_keyvaluemaps_entries_list(
    client: &SimpleHttpClient,
    parent: &str,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudApigeeV1ListKeyValueEntriesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = apigee_organizations_environments_keyvaluemaps_entries_list_builder(
        client, parent, pageSize, pageToken,
    )?;
    apigee_organizations_environments_keyvaluemaps_entries_list_execute(builder)
}

/// GET v1/organizations/{organizationsId}/environments/{environmentsId}/keyvaluemaps/{keyvaluemapsId}/entries/{entriesId}
/// Update key value entry scoped to an organization, environment, or API proxy for an existing key.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `apigee_organizations_environments_keyvaluemaps_entries_update_execute()` to send, or `apigee_organizations_environments_keyvaluemaps_entries_update` for simplest API.

pub fn apigee_organizations_environments_keyvaluemaps_entries_update_builder(
    client: &SimpleHttpClient,
    name: &str,
    body: &GoogleCloudApigeeV1KeyValueEntry,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://apigee.googleapis.com/v1/organizations/{}/environments/{}/keyvaluemaps/{}/entries/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/organizations/{organizationsId}/environments/{environmentsId}/keyvaluemaps/{keyvaluemapsId}/entries/{entriesId}
/// Update key value entry scoped to an organization, environment, or API proxy for an existing key.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `apigee_organizations_environments_keyvaluemaps_entries_update_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from apigee_organizations_environments_keyvaluemaps_entries_update_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_environments_keyvaluemaps_entries_update_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudApigeeV1KeyValueEntry>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudApigeeV1KeyValueEntry = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/organizations/{organizationsId}/environments/{environmentsId}/keyvaluemaps/{keyvaluemapsId}/entries/{entriesId}
/// Update key value entry scoped to an organization, environment, or API proxy for an existing key.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `apigee_organizations_environments_keyvaluemaps_entries_update_builder()` + `apigee_organizations_environments_keyvaluemaps_entries_update_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_environments_keyvaluemaps_entries_update(
    client: &SimpleHttpClient,
    name: &str,
    body: &GoogleCloudApigeeV1KeyValueEntry,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudApigeeV1KeyValueEntry>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        apigee_organizations_environments_keyvaluemaps_entries_update_builder(client, name, body)?;
    apigee_organizations_environments_keyvaluemaps_entries_update_execute(builder)
}

/// GET v1/organizations/{organizationsId}/environments/{environmentsId}/optimizedStats/{optimizedStatsId}
/// Similar to GetStats except that the response is less verbose.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `apigee_organizations_environments_optimized_stats_get_execute()` to send, or `apigee_organizations_environments_optimized_stats_get` for simplest API.

pub fn apigee_organizations_environments_optimized_stats_get_builder(
    client: &SimpleHttpClient,
    name: &str,
    accuracy: Option<&str>,
    aggTable: Option<&str>,
    filter: Option<&str>,
    limit: Option<&str>,
    offset: Option<&str>,
    realtime: Option<bool>,
    select: Option<&str>,
    sonar: Option<bool>,
    sort: Option<&str>,
    sortby: Option<&str>,
    timeRange: Option<&str>,
    timeUnit: Option<&str>,
    topk: Option<&str>,
    tsAscending: Option<bool>,
    tzo: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://apigee.googleapis.com/v1/organizations/{}/environments/{}/optimizedStats/{}",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = accuracy {
        query_parts.push(format!("accuracy={}", val));
    }
    if let Some(val) = aggTable {
        query_parts.push(format!("aggTable={}", val));
    }
    if let Some(val) = filter {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = limit {
        query_parts.push(format!("limit={}", val));
    }
    if let Some(val) = offset {
        query_parts.push(format!("offset={}", val));
    }
    if let Some(val) = realtime {
        query_parts.push(format!("realtime={}", val));
    }
    if let Some(val) = select {
        query_parts.push(format!("select={}", val));
    }
    if let Some(val) = sonar {
        query_parts.push(format!("sonar={}", val));
    }
    if let Some(val) = sort {
        query_parts.push(format!("sort={}", val));
    }
    if let Some(val) = sortby {
        query_parts.push(format!("sortby={}", val));
    }
    if let Some(val) = timeRange {
        query_parts.push(format!("timeRange={}", val));
    }
    if let Some(val) = timeUnit {
        query_parts.push(format!("timeUnit={}", val));
    }
    if let Some(val) = topk {
        query_parts.push(format!("topk={}", val));
    }
    if let Some(val) = tsAscending {
        query_parts.push(format!("tsAscending={}", val));
    }
    if let Some(val) = tzo {
        query_parts.push(format!("tzo={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/organizations/{organizationsId}/environments/{environmentsId}/optimizedStats/{optimizedStatsId}
/// Similar to GetStats except that the response is less verbose.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `apigee_organizations_environments_optimized_stats_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from apigee_organizations_environments_optimized_stats_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_environments_optimized_stats_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudApigeeV1OptimizedStats>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudApigeeV1OptimizedStats = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/organizations/{organizationsId}/environments/{environmentsId}/optimizedStats/{optimizedStatsId}
/// Similar to GetStats except that the response is less verbose.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `apigee_organizations_environments_optimized_stats_get_builder()` + `apigee_organizations_environments_optimized_stats_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_environments_optimized_stats_get(
    client: &SimpleHttpClient,
    name: &str,
    accuracy: Option<&str>,
    aggTable: Option<&str>,
    filter: Option<&str>,
    limit: Option<&str>,
    offset: Option<&str>,
    realtime: Option<bool>,
    select: Option<&str>,
    sonar: Option<bool>,
    sort: Option<&str>,
    sortby: Option<&str>,
    timeRange: Option<&str>,
    timeUnit: Option<&str>,
    topk: Option<&str>,
    tsAscending: Option<bool>,
    tzo: Option<&str>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudApigeeV1OptimizedStats>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = apigee_organizations_environments_optimized_stats_get_builder(
        client,
        name,
        accuracy,
        aggTable,
        filter,
        limit,
        offset,
        realtime,
        select,
        sonar,
        sort,
        sortby,
        timeRange,
        timeUnit,
        topk,
        tsAscending,
        tzo,
    )?;
    apigee_organizations_environments_optimized_stats_get_execute(builder)
}

/// GET v1/organizations/{organizationsId}/environments/{environmentsId}/queries
/// Submit a query to be processed in the background. If the submission of the query succeeds, the API returns a 201 status and an ID that refer to the query. In addition to the HTTP status 201, the state of "enqueued" means that the request succeeded.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `apigee_organizations_environments_queries_create_execute()` to send, or `apigee_organizations_environments_queries_create` for simplest API.

pub fn apigee_organizations_environments_queries_create_builder(
    client: &SimpleHttpClient,
    parent: &str,
    body: &GoogleCloudApigeeV1Query,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://apigee.googleapis.com/v1/organizations/{}/environments/{}/queries",
        parent,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/organizations/{organizationsId}/environments/{environmentsId}/queries
/// Submit a query to be processed in the background. If the submission of the query succeeds, the API returns a 201 status and an ID that refer to the query. In addition to the HTTP status 201, the state of "enqueued" means that the request succeeded.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `apigee_organizations_environments_queries_create_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from apigee_organizations_environments_queries_create_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_environments_queries_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudApigeeV1AsyncQuery>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudApigeeV1AsyncQuery = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/organizations/{organizationsId}/environments/{environmentsId}/queries
/// Submit a query to be processed in the background. If the submission of the query succeeds, the API returns a 201 status and an ID that refer to the query. In addition to the HTTP status 201, the state of "enqueued" means that the request succeeded.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `apigee_organizations_environments_queries_create_builder()` + `apigee_organizations_environments_queries_create_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_environments_queries_create(
    client: &SimpleHttpClient,
    parent: &str,
    body: &GoogleCloudApigeeV1Query,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudApigeeV1AsyncQuery>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = apigee_organizations_environments_queries_create_builder(client, parent, body)?;
    apigee_organizations_environments_queries_create_execute(builder)
}

/// GET v1/organizations/{organizationsId}/environments/{environmentsId}/queries/{queriesId}
/// Get query status If the query is still in progress, the state is set to "running" After the query has completed successfully, state is set to "completed"
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `apigee_organizations_environments_queries_get_execute()` to send, or `apigee_organizations_environments_queries_get` for simplest API.

pub fn apigee_organizations_environments_queries_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://apigee.googleapis.com/v1/organizations/{}/environments/{}/queries/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/organizations/{organizationsId}/environments/{environmentsId}/queries/{queriesId}
/// Get query status If the query is still in progress, the state is set to "running" After the query has completed successfully, state is set to "completed"
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `apigee_organizations_environments_queries_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from apigee_organizations_environments_queries_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_environments_queries_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudApigeeV1AsyncQuery>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudApigeeV1AsyncQuery = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/organizations/{organizationsId}/environments/{environmentsId}/queries/{queriesId}
/// Get query status If the query is still in progress, the state is set to "running" After the query has completed successfully, state is set to "completed"
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `apigee_organizations_environments_queries_get_builder()` + `apigee_organizations_environments_queries_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_environments_queries_get(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudApigeeV1AsyncQuery>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = apigee_organizations_environments_queries_get_builder(client, name)?;
    apigee_organizations_environments_queries_get_execute(builder)
}

/// GET v1/organizations/{organizationsId}/environments/{environmentsId}/queries/{queriesId}/result
/// After the query is completed, use this API to retrieve the results. If the request succeeds, and there is a non-zero result set, the result is downloaded to the client as a zipped JSON file. The name of the downloaded file will be: OfflineQueryResult-.zip Example: OfflineQueryResult-9cfc0d85-0f30-46d6-ae6f-318d0cb961bd.zip
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `apigee_organizations_environments_queries_get_result_execute()` to send, or `apigee_organizations_environments_queries_get_result` for simplest API.

pub fn apigee_organizations_environments_queries_get_result_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://apigee.googleapis.com/v1/organizations/{}/environments/{}/queries/{}/result",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/organizations/{organizationsId}/environments/{environmentsId}/queries/{queriesId}/result
/// After the query is completed, use this API to retrieve the results. If the request succeeds, and there is a non-zero result set, the result is downloaded to the client as a zipped JSON file. The name of the downloaded file will be: OfflineQueryResult-.zip Example: OfflineQueryResult-9cfc0d85-0f30-46d6-ae6f-318d0cb961bd.zip
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `apigee_organizations_environments_queries_get_result_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from apigee_organizations_environments_queries_get_result_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_environments_queries_get_result_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleApiHttpBody>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleApiHttpBody = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/organizations/{organizationsId}/environments/{environmentsId}/queries/{queriesId}/result
/// After the query is completed, use this API to retrieve the results. If the request succeeds, and there is a non-zero result set, the result is downloaded to the client as a zipped JSON file. The name of the downloaded file will be: OfflineQueryResult-.zip Example: OfflineQueryResult-9cfc0d85-0f30-46d6-ae6f-318d0cb961bd.zip
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `apigee_organizations_environments_queries_get_result_builder()` + `apigee_organizations_environments_queries_get_result_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_environments_queries_get_result(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleApiHttpBody>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = apigee_organizations_environments_queries_get_result_builder(client, name)?;
    apigee_organizations_environments_queries_get_result_execute(builder)
}

/// GET v1/organizations/{organizationsId}/environments/{environmentsId}/queries/{queriesId}/resulturl
/// After the query is completed, use this API to retrieve the results. If the request succeeds, and there is a non-zero result set, the result is sent to the client as a list of urls to JSON files.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `apigee_organizations_environments_queries_get_resulturl_execute()` to send, or `apigee_organizations_environments_queries_get_resulturl` for simplest API.

pub fn apigee_organizations_environments_queries_get_resulturl_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://apigee.googleapis.com/v1/organizations/{}/environments/{}/queries/{}/resulturl",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/organizations/{organizationsId}/environments/{environmentsId}/queries/{queriesId}/resulturl
/// After the query is completed, use this API to retrieve the results. If the request succeeds, and there is a non-zero result set, the result is sent to the client as a list of urls to JSON files.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `apigee_organizations_environments_queries_get_resulturl_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from apigee_organizations_environments_queries_get_resulturl_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_environments_queries_get_resulturl_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudApigeeV1GetAsyncQueryResultUrlResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudApigeeV1GetAsyncQueryResultUrlResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/organizations/{organizationsId}/environments/{environmentsId}/queries/{queriesId}/resulturl
/// After the query is completed, use this API to retrieve the results. If the request succeeds, and there is a non-zero result set, the result is sent to the client as a list of urls to JSON files.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `apigee_organizations_environments_queries_get_resulturl_builder()` + `apigee_organizations_environments_queries_get_resulturl_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_environments_queries_get_resulturl(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudApigeeV1GetAsyncQueryResultUrlResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = apigee_organizations_environments_queries_get_resulturl_builder(client, name)?;
    apigee_organizations_environments_queries_get_resulturl_execute(builder)
}

/// GET v1/organizations/{organizationsId}/environments/{environmentsId}/queries
/// Return a list of Asynchronous Queries
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `apigee_organizations_environments_queries_list_execute()` to send, or `apigee_organizations_environments_queries_list` for simplest API.

pub fn apigee_organizations_environments_queries_list_builder(
    client: &SimpleHttpClient,
    parent: &str,
    dataset: Option<&str>,
    from: Option<&str>,
    inclQueriesWithoutReport: Option<&str>,
    status: Option<&str>,
    submittedBy: Option<&str>,
    to: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://apigee.googleapis.com/v1/organizations/{}/environments/{}/queries",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = dataset {
        query_parts.push(format!("dataset={}", val));
    }
    if let Some(val) = from {
        query_parts.push(format!("from={}", val));
    }
    if let Some(val) = inclQueriesWithoutReport {
        query_parts.push(format!("inclQueriesWithoutReport={}", val));
    }
    if let Some(val) = status {
        query_parts.push(format!("status={}", val));
    }
    if let Some(val) = submittedBy {
        query_parts.push(format!("submittedBy={}", val));
    }
    if let Some(val) = to {
        query_parts.push(format!("to={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/organizations/{organizationsId}/environments/{environmentsId}/queries
/// Return a list of Asynchronous Queries
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `apigee_organizations_environments_queries_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from apigee_organizations_environments_queries_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_environments_queries_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudApigeeV1ListAsyncQueriesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudApigeeV1ListAsyncQueriesResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/organizations/{organizationsId}/environments/{environmentsId}/queries
/// Return a list of Asynchronous Queries
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `apigee_organizations_environments_queries_list_builder()` + `apigee_organizations_environments_queries_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_environments_queries_list(
    client: &SimpleHttpClient,
    parent: &str,
    dataset: Option<&str>,
    from: Option<&str>,
    inclQueriesWithoutReport: Option<&str>,
    status: Option<&str>,
    submittedBy: Option<&str>,
    to: Option<&str>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudApigeeV1ListAsyncQueriesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = apigee_organizations_environments_queries_list_builder(
        client,
        parent,
        dataset,
        from,
        inclQueriesWithoutReport,
        status,
        submittedBy,
        to,
    )?;
    apigee_organizations_environments_queries_list_execute(builder)
}

/// GET v1/organizations/{organizationsId}/environments/{environmentsId}/references
/// Creates a Reference in the specified environment.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `apigee_organizations_environments_references_create_execute()` to send, or `apigee_organizations_environments_references_create` for simplest API.

pub fn apigee_organizations_environments_references_create_builder(
    client: &SimpleHttpClient,
    parent: &str,
    body: &GoogleCloudApigeeV1Reference,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://apigee.googleapis.com/v1/organizations/{}/environments/{}/references",
        parent,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/organizations/{organizationsId}/environments/{environmentsId}/references
/// Creates a Reference in the specified environment.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `apigee_organizations_environments_references_create_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from apigee_organizations_environments_references_create_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_environments_references_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudApigeeV1Reference>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudApigeeV1Reference = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/organizations/{organizationsId}/environments/{environmentsId}/references
/// Creates a Reference in the specified environment.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `apigee_organizations_environments_references_create_builder()` + `apigee_organizations_environments_references_create_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_environments_references_create(
    client: &SimpleHttpClient,
    parent: &str,
    body: &GoogleCloudApigeeV1Reference,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudApigeeV1Reference>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        apigee_organizations_environments_references_create_builder(client, parent, body)?;
    apigee_organizations_environments_references_create_execute(builder)
}

/// GET v1/organizations/{organizationsId}/environments/{environmentsId}/references/{referencesId}
/// Deletes a Reference from an environment. Returns the deleted Reference resource.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `apigee_organizations_environments_references_delete_execute()` to send, or `apigee_organizations_environments_references_delete` for simplest API.

pub fn apigee_organizations_environments_references_delete_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://apigee.googleapis.com/v1/organizations/{}/environments/{}/references/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/organizations/{organizationsId}/environments/{environmentsId}/references/{referencesId}
/// Deletes a Reference from an environment. Returns the deleted Reference resource.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `apigee_organizations_environments_references_delete_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from apigee_organizations_environments_references_delete_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_environments_references_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudApigeeV1Reference>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudApigeeV1Reference = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/organizations/{organizationsId}/environments/{environmentsId}/references/{referencesId}
/// Deletes a Reference from an environment. Returns the deleted Reference resource.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `apigee_organizations_environments_references_delete_builder()` + `apigee_organizations_environments_references_delete_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_environments_references_delete(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudApigeeV1Reference>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = apigee_organizations_environments_references_delete_builder(client, name)?;
    apigee_organizations_environments_references_delete_execute(builder)
}

/// GET v1/organizations/{organizationsId}/environments/{environmentsId}/references/{referencesId}
/// Gets a Reference resource.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `apigee_organizations_environments_references_get_execute()` to send, or `apigee_organizations_environments_references_get` for simplest API.

pub fn apigee_organizations_environments_references_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://apigee.googleapis.com/v1/organizations/{}/environments/{}/references/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/organizations/{organizationsId}/environments/{environmentsId}/references/{referencesId}
/// Gets a Reference resource.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `apigee_organizations_environments_references_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from apigee_organizations_environments_references_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_environments_references_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudApigeeV1Reference>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudApigeeV1Reference = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/organizations/{organizationsId}/environments/{environmentsId}/references/{referencesId}
/// Gets a Reference resource.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `apigee_organizations_environments_references_get_builder()` + `apigee_organizations_environments_references_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_environments_references_get(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudApigeeV1Reference>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = apigee_organizations_environments_references_get_builder(client, name)?;
    apigee_organizations_environments_references_get_execute(builder)
}

/// GET v1/organizations/{organizationsId}/environments/{environmentsId}/references/{referencesId}
/// Updates an existing Reference. Note that this operation has PUT semantics; it will replace the entirety of the existing Reference with the resource in the request body.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `apigee_organizations_environments_references_update_execute()` to send, or `apigee_organizations_environments_references_update` for simplest API.

pub fn apigee_organizations_environments_references_update_builder(
    client: &SimpleHttpClient,
    name: &str,
    body: &GoogleCloudApigeeV1Reference,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://apigee.googleapis.com/v1/organizations/{}/environments/{}/references/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/organizations/{organizationsId}/environments/{environmentsId}/references/{referencesId}
/// Updates an existing Reference. Note that this operation has PUT semantics; it will replace the entirety of the existing Reference with the resource in the request body.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `apigee_organizations_environments_references_update_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from apigee_organizations_environments_references_update_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_environments_references_update_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudApigeeV1Reference>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudApigeeV1Reference = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/organizations/{organizationsId}/environments/{environmentsId}/references/{referencesId}
/// Updates an existing Reference. Note that this operation has PUT semantics; it will replace the entirety of the existing Reference with the resource in the request body.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `apigee_organizations_environments_references_update_builder()` + `apigee_organizations_environments_references_update_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_environments_references_update(
    client: &SimpleHttpClient,
    name: &str,
    body: &GoogleCloudApigeeV1Reference,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudApigeeV1Reference>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = apigee_organizations_environments_references_update_builder(client, name, body)?;
    apigee_organizations_environments_references_update_execute(builder)
}

/// GET v1/organizations/{organizationsId}/environments/{environmentsId}/resourcefiles
/// Creates a resource file. Specify the Content-Type as `application/octet-stream` or `multipart/form-data`. For more information about resource files, see [Resource files](<https://cloud.google.`com/apigee/docs/api-platform/develop/resource-files`>).
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `apigee_organizations_environments_resourcefiles_create_execute()` to send, or `apigee_organizations_environments_resourcefiles_create` for simplest API.

pub fn apigee_organizations_environments_resourcefiles_create_builder(
    client: &SimpleHttpClient,
    parent: &str,
    name: Option<&str>,
    type_rs: Option<&str>,
    body: &GoogleApiHttpBody,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://apigee.googleapis.com/v1/organizations/{}/environments/{}/resourcefiles",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = name {
        query_parts.push(format!("name={}", val));
    }
    if let Some(val) = type_rs {
        query_parts.push(format!("type={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/organizations/{organizationsId}/environments/{environmentsId}/resourcefiles
/// Creates a resource file. Specify the Content-Type as `application/octet-stream` or `multipart/form-data`. For more information about resource files, see [Resource files](<https://cloud.google.`com/apigee/docs/api-platform/develop/resource-files`>).
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `apigee_organizations_environments_resourcefiles_create_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from apigee_organizations_environments_resourcefiles_create_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_environments_resourcefiles_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudApigeeV1ResourceFile>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudApigeeV1ResourceFile = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/organizations/{organizationsId}/environments/{environmentsId}/resourcefiles
/// Creates a resource file. Specify the Content-Type as `application/octet-stream` or `multipart/form-data`. For more information about resource files, see [Resource files](<https://cloud.google.`com/apigee/docs/api-platform/develop/resource-files`>).
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `apigee_organizations_environments_resourcefiles_create_builder()` + `apigee_organizations_environments_resourcefiles_create_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_environments_resourcefiles_create(
    client: &SimpleHttpClient,
    parent: &str,
    name: Option<&str>,
    type_rs: Option<&str>,
    body: &GoogleApiHttpBody,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudApigeeV1ResourceFile>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = apigee_organizations_environments_resourcefiles_create_builder(
        client, parent, name, type_rs, body,
    )?;
    apigee_organizations_environments_resourcefiles_create_execute(builder)
}

/// GET v1/organizations/{organizationsId}/environments/{environmentsId}/resourcefiles/{type}/{name}
/// Deletes a resource file. For more information about resource files, see [Resource files](<https://cloud.google.`com/apigee/docs/api-platform/develop/resource-files`>).
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `apigee_organizations_environments_resourcefiles_delete_execute()` to send, or `apigee_organizations_environments_resourcefiles_delete` for simplest API.

pub fn apigee_organizations_environments_resourcefiles_delete_builder(
    client: &SimpleHttpClient,
    parent: &str,
    type_rs: &str,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://apigee.googleapis.com/v1/organizations/{}/environments/{}/resourcefiles/{}/{}",
        parent, type_rs, name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/organizations/{organizationsId}/environments/{environmentsId}/resourcefiles/{type}/{name}
/// Deletes a resource file. For more information about resource files, see [Resource files](<https://cloud.google.`com/apigee/docs/api-platform/develop/resource-files`>).
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `apigee_organizations_environments_resourcefiles_delete_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from apigee_organizations_environments_resourcefiles_delete_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_environments_resourcefiles_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudApigeeV1ResourceFile>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudApigeeV1ResourceFile = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/organizations/{organizationsId}/environments/{environmentsId}/resourcefiles/{type}/{name}
/// Deletes a resource file. For more information about resource files, see [Resource files](<https://cloud.google.`com/apigee/docs/api-platform/develop/resource-files`>).
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `apigee_organizations_environments_resourcefiles_delete_builder()` + `apigee_organizations_environments_resourcefiles_delete_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_environments_resourcefiles_delete(
    client: &SimpleHttpClient,
    parent: &str,
    type_rs: &str,
    name: &str,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudApigeeV1ResourceFile>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = apigee_organizations_environments_resourcefiles_delete_builder(
        client, parent, type_rs, name,
    )?;
    apigee_organizations_environments_resourcefiles_delete_execute(builder)
}

/// GET v1/organizations/{organizationsId}/environments/{environmentsId}/resourcefiles/{type}/{name}
/// Gets the contents of a resource file. For more information about resource files, see [Resource files](<https://cloud.google.`com/apigee/docs/api-platform/develop/resource-files`>).
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `apigee_organizations_environments_resourcefiles_get_execute()` to send, or `apigee_organizations_environments_resourcefiles_get` for simplest API.

pub fn apigee_organizations_environments_resourcefiles_get_builder(
    client: &SimpleHttpClient,
    parent: &str,
    type_rs: &str,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://apigee.googleapis.com/v1/organizations/{}/environments/{}/resourcefiles/{}/{}",
        parent, type_rs, name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/organizations/{organizationsId}/environments/{environmentsId}/resourcefiles/{type}/{name}
/// Gets the contents of a resource file. For more information about resource files, see [Resource files](<https://cloud.google.`com/apigee/docs/api-platform/develop/resource-files`>).
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `apigee_organizations_environments_resourcefiles_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from apigee_organizations_environments_resourcefiles_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_environments_resourcefiles_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleApiHttpBody>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleApiHttpBody = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/organizations/{organizationsId}/environments/{environmentsId}/resourcefiles/{type}/{name}
/// Gets the contents of a resource file. For more information about resource files, see [Resource files](<https://cloud.google.`com/apigee/docs/api-platform/develop/resource-files`>).
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `apigee_organizations_environments_resourcefiles_get_builder()` + `apigee_organizations_environments_resourcefiles_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_environments_resourcefiles_get(
    client: &SimpleHttpClient,
    parent: &str,
    type_rs: &str,
    name: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleApiHttpBody>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        apigee_organizations_environments_resourcefiles_get_builder(client, parent, type_rs, name)?;
    apigee_organizations_environments_resourcefiles_get_execute(builder)
}

/// GET v1/organizations/{organizationsId}/environments/{environmentsId}/resourcefiles
/// Lists all resource files, optionally filtering by type. For more information about resource files, see [Resource files](<https://cloud.google.`com/apigee/docs/api-platform/develop/resource-files`>).
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `apigee_organizations_environments_resourcefiles_list_execute()` to send, or `apigee_organizations_environments_resourcefiles_list` for simplest API.

pub fn apigee_organizations_environments_resourcefiles_list_builder(
    client: &SimpleHttpClient,
    parent: &str,
    type_rs: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://apigee.googleapis.com/v1/organizations/{}/environments/{}/resourcefiles",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = type_rs {
        query_parts.push(format!("type={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/organizations/{organizationsId}/environments/{environmentsId}/resourcefiles
/// Lists all resource files, optionally filtering by type. For more information about resource files, see [Resource files](<https://cloud.google.`com/apigee/docs/api-platform/develop/resource-files`>).
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `apigee_organizations_environments_resourcefiles_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from apigee_organizations_environments_resourcefiles_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_environments_resourcefiles_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudApigeeV1ListEnvironmentResourcesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudApigeeV1ListEnvironmentResourcesResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/organizations/{organizationsId}/environments/{environmentsId}/resourcefiles
/// Lists all resource files, optionally filtering by type. For more information about resource files, see [Resource files](<https://cloud.google.`com/apigee/docs/api-platform/develop/resource-files`>).
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `apigee_organizations_environments_resourcefiles_list_builder()` + `apigee_organizations_environments_resourcefiles_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_environments_resourcefiles_list(
    client: &SimpleHttpClient,
    parent: &str,
    type_rs: Option<&str>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudApigeeV1ListEnvironmentResourcesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        apigee_organizations_environments_resourcefiles_list_builder(client, parent, type_rs)?;
    apigee_organizations_environments_resourcefiles_list_execute(builder)
}

/// GET v1/organizations/{organizationsId}/environments/{environmentsId}/resourcefiles/{type}
/// Lists all resource files, optionally filtering by type. For more information about resource files, see [Resource files](<https://cloud.google.`com/apigee/docs/api-platform/develop/resource-files`>).
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `apigee_organizations_environments_resourcefiles_list_environment_resources_execute()` to send, or `apigee_organizations_environments_resourcefiles_list_environment_resources` for simplest API.

pub fn apigee_organizations_environments_resourcefiles_list_environment_resources_builder(
    client: &SimpleHttpClient,
    parent: &str,
    type_rs: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://apigee.googleapis.com/v1/organizations/{}/environments/{}/resourcefiles/{}",
        parent, type_rs,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/organizations/{organizationsId}/environments/{environmentsId}/resourcefiles/{type}
/// Lists all resource files, optionally filtering by type. For more information about resource files, see [Resource files](<https://cloud.google.`com/apigee/docs/api-platform/develop/resource-files`>).
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `apigee_organizations_environments_resourcefiles_list_environment_resources_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from apigee_organizations_environments_resourcefiles_list_environment_resources_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_environments_resourcefiles_list_environment_resources_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudApigeeV1ListEnvironmentResourcesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudApigeeV1ListEnvironmentResourcesResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/organizations/{organizationsId}/environments/{environmentsId}/resourcefiles/{type}
/// Lists all resource files, optionally filtering by type. For more information about resource files, see [Resource files](<https://cloud.google.`com/apigee/docs/api-platform/develop/resource-files`>).
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `apigee_organizations_environments_resourcefiles_list_environment_resources_builder()` + `apigee_organizations_environments_resourcefiles_list_environment_resources_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_environments_resourcefiles_list_environment_resources(
    client: &SimpleHttpClient,
    parent: &str,
    type_rs: &str,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudApigeeV1ListEnvironmentResourcesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        apigee_organizations_environments_resourcefiles_list_environment_resources_builder(
            client, parent, type_rs,
        )?;
    apigee_organizations_environments_resourcefiles_list_environment_resources_execute(builder)
}

/// GET v1/organizations/{organizationsId}/environments/{environmentsId}/resourcefiles/{type}/{name}
/// Updates a resource file. Specify the Content-Type as `application/octet-stream` or `multipart/form-data`. For more information about resource files, see [Resource files](<https://cloud.google.`com/apigee/docs/api-platform/develop/resource-files`>).
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `apigee_organizations_environments_resourcefiles_update_execute()` to send, or `apigee_organizations_environments_resourcefiles_update` for simplest API.

pub fn apigee_organizations_environments_resourcefiles_update_builder(
    client: &SimpleHttpClient,
    parent: &str,
    type_rs: &str,
    name: &str,
    body: &GoogleApiHttpBody,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://apigee.googleapis.com/v1/organizations/{}/environments/{}/resourcefiles/{}/{}",
        parent, type_rs, name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/organizations/{organizationsId}/environments/{environmentsId}/resourcefiles/{type}/{name}
/// Updates a resource file. Specify the Content-Type as `application/octet-stream` or `multipart/form-data`. For more information about resource files, see [Resource files](<https://cloud.google.`com/apigee/docs/api-platform/develop/resource-files`>).
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `apigee_organizations_environments_resourcefiles_update_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from apigee_organizations_environments_resourcefiles_update_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_environments_resourcefiles_update_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudApigeeV1ResourceFile>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudApigeeV1ResourceFile = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/organizations/{organizationsId}/environments/{environmentsId}/resourcefiles/{type}/{name}
/// Updates a resource file. Specify the Content-Type as `application/octet-stream` or `multipart/form-data`. For more information about resource files, see [Resource files](<https://cloud.google.`com/apigee/docs/api-platform/develop/resource-files`>).
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `apigee_organizations_environments_resourcefiles_update_builder()` + `apigee_organizations_environments_resourcefiles_update_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_environments_resourcefiles_update(
    client: &SimpleHttpClient,
    parent: &str,
    type_rs: &str,
    name: &str,
    body: &GoogleApiHttpBody,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudApigeeV1ResourceFile>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = apigee_organizations_environments_resourcefiles_update_builder(
        client, parent, type_rs, name, body,
    )?;
    apigee_organizations_environments_resourcefiles_update_execute(builder)
}

/// GET v1/organizations/{organizationsId}/environments/{environmentsId}/securityActions
/// CreateSecurityAction creates a SecurityAction.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `apigee_organizations_environments_security_actions_create_execute()` to send, or `apigee_organizations_environments_security_actions_create` for simplest API.

pub fn apigee_organizations_environments_security_actions_create_builder(
    client: &SimpleHttpClient,
    parent: &str,
    securityActionId: Option<&str>,
    body: &GoogleCloudApigeeV1SecurityAction,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://apigee.googleapis.com/v1/organizations/{}/environments/{}/securityActions",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = securityActionId {
        query_parts.push(format!("securityActionId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/organizations/{organizationsId}/environments/{environmentsId}/securityActions
/// CreateSecurityAction creates a SecurityAction.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `apigee_organizations_environments_security_actions_create_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from apigee_organizations_environments_security_actions_create_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_environments_security_actions_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudApigeeV1SecurityAction>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudApigeeV1SecurityAction = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/organizations/{organizationsId}/environments/{environmentsId}/securityActions
/// CreateSecurityAction creates a SecurityAction.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `apigee_organizations_environments_security_actions_create_builder()` + `apigee_organizations_environments_security_actions_create_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_environments_security_actions_create(
    client: &SimpleHttpClient,
    parent: &str,
    securityActionId: Option<&str>,
    body: &GoogleCloudApigeeV1SecurityAction,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudApigeeV1SecurityAction>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = apigee_organizations_environments_security_actions_create_builder(
        client,
        parent,
        securityActionId,
        body,
    )?;
    apigee_organizations_environments_security_actions_create_execute(builder)
}

/// GET v1/organizations/{organizationsId}/environments/{environmentsId}/securityActions/{securityActionsId}
/// Delete a SecurityAction.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `apigee_organizations_environments_security_actions_delete_execute()` to send, or `apigee_organizations_environments_security_actions_delete` for simplest API.

pub fn apigee_organizations_environments_security_actions_delete_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://apigee.googleapis.com/v1/organizations/{}/environments/{}/securityActions/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/organizations/{organizationsId}/environments/{environmentsId}/securityActions/{securityActionsId}
/// Delete a SecurityAction.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `apigee_organizations_environments_security_actions_delete_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from apigee_organizations_environments_security_actions_delete_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_environments_security_actions_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleProtobufEmpty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/organizations/{organizationsId}/environments/{environmentsId}/securityActions/{securityActionsId}
/// Delete a SecurityAction.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `apigee_organizations_environments_security_actions_delete_builder()` + `apigee_organizations_environments_security_actions_delete_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_environments_security_actions_delete(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = apigee_organizations_environments_security_actions_delete_builder(client, name)?;
    apigee_organizations_environments_security_actions_delete_execute(builder)
}

/// GET v1/organizations/{organizationsId}/environments/{environmentsId}/securityActions/{securityActionsId}:disable
/// Disable a SecurityAction. The state of the SecurityAction after disabling is DISABLED. DisableSecurityAction can be called on SecurityActions in the state ENABLED; SecurityActions in a different state (including DISABLED) return an error.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `apigee_organizations_environments_security_actions_disable_execute()` to send, or `apigee_organizations_environments_security_actions_disable` for simplest API.

pub fn apigee_organizations_environments_security_actions_disable_builder(
    client: &SimpleHttpClient,
    name: &str,
    body: &GoogleCloudApigeeV1DisableSecurityActionRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://apigee.googleapis.com/v1/organizations/{}/environments/{}/securityActions/{}:disable",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/organizations/{organizationsId}/environments/{environmentsId}/securityActions/{securityActionsId}:disable
/// Disable a SecurityAction. The state of the SecurityAction after disabling is DISABLED. DisableSecurityAction can be called on SecurityActions in the state ENABLED; SecurityActions in a different state (including DISABLED) return an error.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `apigee_organizations_environments_security_actions_disable_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from apigee_organizations_environments_security_actions_disable_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_environments_security_actions_disable_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudApigeeV1SecurityAction>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudApigeeV1SecurityAction = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/organizations/{organizationsId}/environments/{environmentsId}/securityActions/{securityActionsId}:disable
/// Disable a SecurityAction. The state of the SecurityAction after disabling is DISABLED. DisableSecurityAction can be called on SecurityActions in the state ENABLED; SecurityActions in a different state (including DISABLED) return an error.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `apigee_organizations_environments_security_actions_disable_builder()` + `apigee_organizations_environments_security_actions_disable_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_environments_security_actions_disable(
    client: &SimpleHttpClient,
    name: &str,
    body: &GoogleCloudApigeeV1DisableSecurityActionRequest,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudApigeeV1SecurityAction>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        apigee_organizations_environments_security_actions_disable_builder(client, name, body)?;
    apigee_organizations_environments_security_actions_disable_execute(builder)
}

/// GET v1/organizations/{organizationsId}/environments/{environmentsId}/securityActions/{securityActionsId}:enable
/// Enable a SecurityAction. The state of the SecurityAction after enabling is ENABLED. EnableSecurityAction can be called on SecurityActions in the state DISABLED; SecurityActions in a different state (including ENABLED) return an error.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `apigee_organizations_environments_security_actions_enable_execute()` to send, or `apigee_organizations_environments_security_actions_enable` for simplest API.

pub fn apigee_organizations_environments_security_actions_enable_builder(
    client: &SimpleHttpClient,
    name: &str,
    body: &GoogleCloudApigeeV1EnableSecurityActionRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://apigee.googleapis.com/v1/organizations/{}/environments/{}/securityActions/{}:enable",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/organizations/{organizationsId}/environments/{environmentsId}/securityActions/{securityActionsId}:enable
/// Enable a SecurityAction. The state of the SecurityAction after enabling is ENABLED. EnableSecurityAction can be called on SecurityActions in the state DISABLED; SecurityActions in a different state (including ENABLED) return an error.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `apigee_organizations_environments_security_actions_enable_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from apigee_organizations_environments_security_actions_enable_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_environments_security_actions_enable_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudApigeeV1SecurityAction>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudApigeeV1SecurityAction = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/organizations/{organizationsId}/environments/{environmentsId}/securityActions/{securityActionsId}:enable
/// Enable a SecurityAction. The state of the SecurityAction after enabling is ENABLED. EnableSecurityAction can be called on SecurityActions in the state DISABLED; SecurityActions in a different state (including ENABLED) return an error.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `apigee_organizations_environments_security_actions_enable_builder()` + `apigee_organizations_environments_security_actions_enable_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_environments_security_actions_enable(
    client: &SimpleHttpClient,
    name: &str,
    body: &GoogleCloudApigeeV1EnableSecurityActionRequest,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudApigeeV1SecurityAction>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        apigee_organizations_environments_security_actions_enable_builder(client, name, body)?;
    apigee_organizations_environments_security_actions_enable_execute(builder)
}

/// GET v1/organizations/{organizationsId}/environments/{environmentsId}/securityActions/{securityActionsId}
/// Get a SecurityAction by name.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `apigee_organizations_environments_security_actions_get_execute()` to send, or `apigee_organizations_environments_security_actions_get` for simplest API.

pub fn apigee_organizations_environments_security_actions_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://apigee.googleapis.com/v1/organizations/{}/environments/{}/securityActions/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/organizations/{organizationsId}/environments/{environmentsId}/securityActions/{securityActionsId}
/// Get a SecurityAction by name.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `apigee_organizations_environments_security_actions_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from apigee_organizations_environments_security_actions_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_environments_security_actions_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudApigeeV1SecurityAction>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudApigeeV1SecurityAction = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/organizations/{organizationsId}/environments/{environmentsId}/securityActions/{securityActionsId}
/// Get a SecurityAction by name.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `apigee_organizations_environments_security_actions_get_builder()` + `apigee_organizations_environments_security_actions_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_environments_security_actions_get(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudApigeeV1SecurityAction>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = apigee_organizations_environments_security_actions_get_builder(client, name)?;
    apigee_organizations_environments_security_actions_get_execute(builder)
}

/// GET v1/organizations/{organizationsId}/environments/{environmentsId}/securityActions
/// Returns a list of SecurityActions. This returns both enabled and disabled actions.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `apigee_organizations_environments_security_actions_list_execute()` to send, or `apigee_organizations_environments_security_actions_list` for simplest API.

pub fn apigee_organizations_environments_security_actions_list_builder(
    client: &SimpleHttpClient,
    parent: &str,
    filter: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://apigee.googleapis.com/v1/organizations/{}/environments/{}/securityActions",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/organizations/{organizationsId}/environments/{environmentsId}/securityActions
/// Returns a list of SecurityActions. This returns both enabled and disabled actions.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `apigee_organizations_environments_security_actions_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from apigee_organizations_environments_security_actions_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_environments_security_actions_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudApigeeV1ListSecurityActionsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudApigeeV1ListSecurityActionsResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/organizations/{organizationsId}/environments/{environmentsId}/securityActions
/// Returns a list of SecurityActions. This returns both enabled and disabled actions.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `apigee_organizations_environments_security_actions_list_builder()` + `apigee_organizations_environments_security_actions_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_environments_security_actions_list(
    client: &SimpleHttpClient,
    parent: &str,
    filter: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudApigeeV1ListSecurityActionsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = apigee_organizations_environments_security_actions_list_builder(
        client, parent, filter, pageSize, pageToken,
    )?;
    apigee_organizations_environments_security_actions_list_execute(builder)
}

/// GET v1/organizations/{organizationsId}/environments/{environmentsId}/securityActions/{securityActionsId}
/// Update a SecurityAction.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `apigee_organizations_environments_security_actions_patch_execute()` to send, or `apigee_organizations_environments_security_actions_patch` for simplest API.

pub fn apigee_organizations_environments_security_actions_patch_builder(
    client: &SimpleHttpClient,
    name: &str,
    updateMask: Option<&str>,
    body: &GoogleCloudApigeeV1SecurityAction,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://apigee.googleapis.com/v1/organizations/{}/environments/{}/securityActions/{}",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = updateMask {
        query_parts.push(format!("updateMask={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/organizations/{organizationsId}/environments/{environmentsId}/securityActions/{securityActionsId}
/// Update a SecurityAction.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `apigee_organizations_environments_security_actions_patch_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from apigee_organizations_environments_security_actions_patch_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_environments_security_actions_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudApigeeV1SecurityAction>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudApigeeV1SecurityAction = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/organizations/{organizationsId}/environments/{environmentsId}/securityActions/{securityActionsId}
/// Update a SecurityAction.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `apigee_organizations_environments_security_actions_patch_builder()` + `apigee_organizations_environments_security_actions_patch_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_environments_security_actions_patch(
    client: &SimpleHttpClient,
    name: &str,
    updateMask: Option<&str>,
    body: &GoogleCloudApigeeV1SecurityAction,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudApigeeV1SecurityAction>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = apigee_organizations_environments_security_actions_patch_builder(
        client, name, updateMask, body,
    )?;
    apigee_organizations_environments_security_actions_patch_execute(builder)
}

/// GET v1/organizations/{organizationsId}/environments/{environmentsId}/securityIncidents:batchUpdate
/// BatchUpdateSecurityIncident updates multiple existing security incidents.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `apigee_organizations_environments_security_incidents_batch_update_execute()` to send, or `apigee_organizations_environments_security_incidents_batch_update` for simplest API.

pub fn apigee_organizations_environments_security_incidents_batch_update_builder(
    client: &SimpleHttpClient,
    parent: &str,
    body: &GoogleCloudApigeeV1BatchUpdateSecurityIncidentsRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://apigee.googleapis.com/v1/organizations/{}/environments/{}/securityIncidents:batchUpdate",
        parent,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/organizations/{organizationsId}/environments/{environmentsId}/securityIncidents:batchUpdate
/// BatchUpdateSecurityIncident updates multiple existing security incidents.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `apigee_organizations_environments_security_incidents_batch_update_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from apigee_organizations_environments_security_incidents_batch_update_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_environments_security_incidents_batch_update_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleCloudApigeeV1BatchUpdateSecurityIncidentsResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudApigeeV1BatchUpdateSecurityIncidentsResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/organizations/{organizationsId}/environments/{environmentsId}/securityIncidents:batchUpdate
/// BatchUpdateSecurityIncident updates multiple existing security incidents.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `apigee_organizations_environments_security_incidents_batch_update_builder()` + `apigee_organizations_environments_security_incidents_batch_update_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_environments_security_incidents_batch_update(
    client: &SimpleHttpClient,
    parent: &str,
    body: &GoogleCloudApigeeV1BatchUpdateSecurityIncidentsRequest,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleCloudApigeeV1BatchUpdateSecurityIncidentsResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = apigee_organizations_environments_security_incidents_batch_update_builder(
        client, parent, body,
    )?;
    apigee_organizations_environments_security_incidents_batch_update_execute(builder)
}

/// GET v1/organizations/{organizationsId}/environments/{environmentsId}/securityIncidents/{securityIncidentsId}
/// GetSecurityIncident gets the specified security incident. Returns NOT_FOUND if security incident is not present for the specified organization and environment.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `apigee_organizations_environments_security_incidents_get_execute()` to send, or `apigee_organizations_environments_security_incidents_get` for simplest API.

pub fn apigee_organizations_environments_security_incidents_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://apigee.googleapis.com/v1/organizations/{}/environments/{}/securityIncidents/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/organizations/{organizationsId}/environments/{environmentsId}/securityIncidents/{securityIncidentsId}
/// GetSecurityIncident gets the specified security incident. Returns NOT_FOUND if security incident is not present for the specified organization and environment.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `apigee_organizations_environments_security_incidents_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from apigee_organizations_environments_security_incidents_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_environments_security_incidents_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudApigeeV1SecurityIncident>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudApigeeV1SecurityIncident = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/organizations/{organizationsId}/environments/{environmentsId}/securityIncidents/{securityIncidentsId}
/// GetSecurityIncident gets the specified security incident. Returns NOT_FOUND if security incident is not present for the specified organization and environment.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `apigee_organizations_environments_security_incidents_get_builder()` + `apigee_organizations_environments_security_incidents_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_environments_security_incidents_get(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudApigeeV1SecurityIncident>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = apigee_organizations_environments_security_incidents_get_builder(client, name)?;
    apigee_organizations_environments_security_incidents_get_execute(builder)
}

/// GET v1/organizations/{organizationsId}/environments/{environmentsId}/securityIncidents
/// ListSecurityIncidents lists all the security incident associated with the environment.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `apigee_organizations_environments_security_incidents_list_execute()` to send, or `apigee_organizations_environments_security_incidents_list` for simplest API.

pub fn apigee_organizations_environments_security_incidents_list_builder(
    client: &SimpleHttpClient,
    parent: &str,
    filter: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://apigee.googleapis.com/v1/organizations/{}/environments/{}/securityIncidents",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/organizations/{organizationsId}/environments/{environmentsId}/securityIncidents
/// ListSecurityIncidents lists all the security incident associated with the environment.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `apigee_organizations_environments_security_incidents_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from apigee_organizations_environments_security_incidents_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_environments_security_incidents_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudApigeeV1ListSecurityIncidentsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudApigeeV1ListSecurityIncidentsResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/organizations/{organizationsId}/environments/{environmentsId}/securityIncidents
/// ListSecurityIncidents lists all the security incident associated with the environment.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `apigee_organizations_environments_security_incidents_list_builder()` + `apigee_organizations_environments_security_incidents_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_environments_security_incidents_list(
    client: &SimpleHttpClient,
    parent: &str,
    filter: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudApigeeV1ListSecurityIncidentsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = apigee_organizations_environments_security_incidents_list_builder(
        client, parent, filter, pageSize, pageToken,
    )?;
    apigee_organizations_environments_security_incidents_list_execute(builder)
}

/// GET v1/organizations/{organizationsId}/environments/{environmentsId}/securityIncidents/{securityIncidentsId}
/// UpdateSecurityIncidents updates an existing security incident.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `apigee_organizations_environments_security_incidents_patch_execute()` to send, or `apigee_organizations_environments_security_incidents_patch` for simplest API.

pub fn apigee_organizations_environments_security_incidents_patch_builder(
    client: &SimpleHttpClient,
    name: &str,
    updateMask: Option<&str>,
    body: &GoogleCloudApigeeV1SecurityIncident,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://apigee.googleapis.com/v1/organizations/{}/environments/{}/securityIncidents/{}",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = updateMask {
        query_parts.push(format!("updateMask={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/organizations/{organizationsId}/environments/{environmentsId}/securityIncidents/{securityIncidentsId}
/// UpdateSecurityIncidents updates an existing security incident.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `apigee_organizations_environments_security_incidents_patch_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from apigee_organizations_environments_security_incidents_patch_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_environments_security_incidents_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudApigeeV1SecurityIncident>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudApigeeV1SecurityIncident = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/organizations/{organizationsId}/environments/{environmentsId}/securityIncidents/{securityIncidentsId}
/// UpdateSecurityIncidents updates an existing security incident.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `apigee_organizations_environments_security_incidents_patch_builder()` + `apigee_organizations_environments_security_incidents_patch_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_environments_security_incidents_patch(
    client: &SimpleHttpClient,
    name: &str,
    updateMask: Option<&str>,
    body: &GoogleCloudApigeeV1SecurityIncident,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudApigeeV1SecurityIncident>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = apigee_organizations_environments_security_incidents_patch_builder(
        client, name, updateMask, body,
    )?;
    apigee_organizations_environments_security_incidents_patch_execute(builder)
}

/// GET v1/organizations/{organizationsId}/environments/{environmentsId}/securityReports
/// Submit a report request to be processed in the background. If the submission succeeds, the API returns a 200 status and an ID that refer to the report request. In addition to the HTTP status 200, the state of "enqueued" means that the request succeeded.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `apigee_organizations_environments_security_reports_create_execute()` to send, or `apigee_organizations_environments_security_reports_create` for simplest API.

pub fn apigee_organizations_environments_security_reports_create_builder(
    client: &SimpleHttpClient,
    parent: &str,
    body: &GoogleCloudApigeeV1SecurityReportQuery,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://apigee.googleapis.com/v1/organizations/{}/environments/{}/securityReports",
        parent,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/organizations/{organizationsId}/environments/{environmentsId}/securityReports
/// Submit a report request to be processed in the background. If the submission succeeds, the API returns a 200 status and an ID that refer to the report request. In addition to the HTTP status 200, the state of "enqueued" means that the request succeeded.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `apigee_organizations_environments_security_reports_create_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from apigee_organizations_environments_security_reports_create_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_environments_security_reports_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudApigeeV1SecurityReport>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudApigeeV1SecurityReport = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/organizations/{organizationsId}/environments/{environmentsId}/securityReports
/// Submit a report request to be processed in the background. If the submission succeeds, the API returns a 200 status and an ID that refer to the report request. In addition to the HTTP status 200, the state of "enqueued" means that the request succeeded.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `apigee_organizations_environments_security_reports_create_builder()` + `apigee_organizations_environments_security_reports_create_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_environments_security_reports_create(
    client: &SimpleHttpClient,
    parent: &str,
    body: &GoogleCloudApigeeV1SecurityReportQuery,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudApigeeV1SecurityReport>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        apigee_organizations_environments_security_reports_create_builder(client, parent, body)?;
    apigee_organizations_environments_security_reports_create_execute(builder)
}

/// GET v1/organizations/{organizationsId}/environments/{environmentsId}/securityReports/{securityReportsId}
/// Get security report status If the query is still in progress, the state is set to "running" After the query has completed successfully, state is set to "completed"
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `apigee_organizations_environments_security_reports_get_execute()` to send, or `apigee_organizations_environments_security_reports_get` for simplest API.

pub fn apigee_organizations_environments_security_reports_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://apigee.googleapis.com/v1/organizations/{}/environments/{}/securityReports/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/organizations/{organizationsId}/environments/{environmentsId}/securityReports/{securityReportsId}
/// Get security report status If the query is still in progress, the state is set to "running" After the query has completed successfully, state is set to "completed"
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `apigee_organizations_environments_security_reports_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from apigee_organizations_environments_security_reports_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_environments_security_reports_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudApigeeV1SecurityReport>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudApigeeV1SecurityReport = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/organizations/{organizationsId}/environments/{environmentsId}/securityReports/{securityReportsId}
/// Get security report status If the query is still in progress, the state is set to "running" After the query has completed successfully, state is set to "completed"
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `apigee_organizations_environments_security_reports_get_builder()` + `apigee_organizations_environments_security_reports_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_environments_security_reports_get(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudApigeeV1SecurityReport>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = apigee_organizations_environments_security_reports_get_builder(client, name)?;
    apigee_organizations_environments_security_reports_get_execute(builder)
}

/// GET v1/organizations/{organizationsId}/environments/{environmentsId}/securityReports/{securityReportsId}/result
/// After the query is completed, use this API to retrieve the results as file. If the request succeeds, and there is a non-zero result set, the result is downloaded to the client as a zipped JSON file. The name of the downloaded file will be: OfflineQueryResult-.zip Example: OfflineQueryResult-9cfc0d85-0f30-46d6-ae6f-318d0cb961bd.zip
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `apigee_organizations_environments_security_reports_get_result_execute()` to send, or `apigee_organizations_environments_security_reports_get_result` for simplest API.

pub fn apigee_organizations_environments_security_reports_get_result_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://apigee.googleapis.com/v1/organizations/{}/environments/{}/securityReports/{}/result",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/organizations/{organizationsId}/environments/{environmentsId}/securityReports/{securityReportsId}/result
/// After the query is completed, use this API to retrieve the results as file. If the request succeeds, and there is a non-zero result set, the result is downloaded to the client as a zipped JSON file. The name of the downloaded file will be: OfflineQueryResult-.zip Example: OfflineQueryResult-9cfc0d85-0f30-46d6-ae6f-318d0cb961bd.zip
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `apigee_organizations_environments_security_reports_get_result_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from apigee_organizations_environments_security_reports_get_result_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_environments_security_reports_get_result_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleApiHttpBody>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleApiHttpBody = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/organizations/{organizationsId}/environments/{environmentsId}/securityReports/{securityReportsId}/result
/// After the query is completed, use this API to retrieve the results as file. If the request succeeds, and there is a non-zero result set, the result is downloaded to the client as a zipped JSON file. The name of the downloaded file will be: OfflineQueryResult-.zip Example: OfflineQueryResult-9cfc0d85-0f30-46d6-ae6f-318d0cb961bd.zip
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `apigee_organizations_environments_security_reports_get_result_builder()` + `apigee_organizations_environments_security_reports_get_result_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_environments_security_reports_get_result(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleApiHttpBody>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        apigee_organizations_environments_security_reports_get_result_builder(client, name)?;
    apigee_organizations_environments_security_reports_get_result_execute(builder)
}

/// GET v1/organizations/{organizationsId}/environments/{environmentsId}/securityReports/{securityReportsId}/resultView
/// After the query is completed, use this API to view the query result when result size is small.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `apigee_organizations_environments_security_reports_get_result_view_execute()` to send, or `apigee_organizations_environments_security_reports_get_result_view` for simplest API.

pub fn apigee_organizations_environments_security_reports_get_result_view_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://apigee.googleapis.com/v1/organizations/{}/environments/{}/securityReports/{}/resultView",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/organizations/{organizationsId}/environments/{environmentsId}/securityReports/{securityReportsId}/resultView
/// After the query is completed, use this API to view the query result when result size is small.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `apigee_organizations_environments_security_reports_get_result_view_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from apigee_organizations_environments_security_reports_get_result_view_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_environments_security_reports_get_result_view_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudApigeeV1SecurityReportResultView>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudApigeeV1SecurityReportResultView =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/organizations/{organizationsId}/environments/{environmentsId}/securityReports/{securityReportsId}/resultView
/// After the query is completed, use this API to view the query result when result size is small.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `apigee_organizations_environments_security_reports_get_result_view_builder()` + `apigee_organizations_environments_security_reports_get_result_view_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_environments_security_reports_get_result_view(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudApigeeV1SecurityReportResultView>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        apigee_organizations_environments_security_reports_get_result_view_builder(client, name)?;
    apigee_organizations_environments_security_reports_get_result_view_execute(builder)
}

/// GET v1/organizations/{organizationsId}/environments/{environmentsId}/securityReports
/// Return a list of Security Reports
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `apigee_organizations_environments_security_reports_list_execute()` to send, or `apigee_organizations_environments_security_reports_list` for simplest API.

pub fn apigee_organizations_environments_security_reports_list_builder(
    client: &SimpleHttpClient,
    parent: &str,
    dataset: Option<&str>,
    from: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
    status: Option<&str>,
    submittedBy: Option<&str>,
    to: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://apigee.googleapis.com/v1/organizations/{}/environments/{}/securityReports",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = dataset {
        query_parts.push(format!("dataset={}", val));
    }
    if let Some(val) = from {
        query_parts.push(format!("from={}", val));
    }
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = status {
        query_parts.push(format!("status={}", val));
    }
    if let Some(val) = submittedBy {
        query_parts.push(format!("submittedBy={}", val));
    }
    if let Some(val) = to {
        query_parts.push(format!("to={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/organizations/{organizationsId}/environments/{environmentsId}/securityReports
/// Return a list of Security Reports
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `apigee_organizations_environments_security_reports_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from apigee_organizations_environments_security_reports_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_environments_security_reports_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudApigeeV1ListSecurityReportsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudApigeeV1ListSecurityReportsResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/organizations/{organizationsId}/environments/{environmentsId}/securityReports
/// Return a list of Security Reports
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `apigee_organizations_environments_security_reports_list_builder()` + `apigee_organizations_environments_security_reports_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_environments_security_reports_list(
    client: &SimpleHttpClient,
    parent: &str,
    dataset: Option<&str>,
    from: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
    status: Option<&str>,
    submittedBy: Option<&str>,
    to: Option<&str>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudApigeeV1ListSecurityReportsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = apigee_organizations_environments_security_reports_list_builder(
        client,
        parent,
        dataset,
        from,
        pageSize,
        pageToken,
        status,
        submittedBy,
        to,
    )?;
    apigee_organizations_environments_security_reports_list_execute(builder)
}

/// GET v1/organizations/{organizationsId}/environments/{environmentsId}/securityStats:queryTabularStats
/// Retrieve security statistics as tabular rows.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `apigee_organizations_environments_security_stats_query_tabular_stats_execute()` to send, or `apigee_organizations_environments_security_stats_query_tabular_stats` for simplest API.

pub fn apigee_organizations_environments_security_stats_query_tabular_stats_builder(
    client: &SimpleHttpClient,
    orgenv: &str,
    body: &GoogleCloudApigeeV1QueryTabularStatsRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://apigee.googleapis.com/v1/organizations/{}/environments/{}/securityStats:queryTabularStats",
        orgenv,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/organizations/{organizationsId}/environments/{environmentsId}/securityStats:queryTabularStats
/// Retrieve security statistics as tabular rows.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `apigee_organizations_environments_security_stats_query_tabular_stats_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from apigee_organizations_environments_security_stats_query_tabular_stats_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_environments_security_stats_query_tabular_stats_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudApigeeV1QueryTabularStatsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudApigeeV1QueryTabularStatsResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/organizations/{organizationsId}/environments/{environmentsId}/securityStats:queryTabularStats
/// Retrieve security statistics as tabular rows.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `apigee_organizations_environments_security_stats_query_tabular_stats_builder()` + `apigee_organizations_environments_security_stats_query_tabular_stats_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_environments_security_stats_query_tabular_stats(
    client: &SimpleHttpClient,
    orgenv: &str,
    body: &GoogleCloudApigeeV1QueryTabularStatsRequest,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudApigeeV1QueryTabularStatsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = apigee_organizations_environments_security_stats_query_tabular_stats_builder(
        client, orgenv, body,
    )?;
    apigee_organizations_environments_security_stats_query_tabular_stats_execute(builder)
}

/// GET v1/organizations/{organizationsId}/environments/{environmentsId}/securityStats:queryTimeSeriesStats
/// Retrieve security statistics as a collection of time series.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `apigee_organizations_environments_security_stats_query_time_series_stats_execute()` to send, or `apigee_organizations_environments_security_stats_query_time_series_stats` for simplest API.

pub fn apigee_organizations_environments_security_stats_query_time_series_stats_builder(
    client: &SimpleHttpClient,
    orgenv: &str,
    body: &GoogleCloudApigeeV1QueryTimeSeriesStatsRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://apigee.googleapis.com/v1/organizations/{}/environments/{}/securityStats:queryTimeSeriesStats",
        orgenv,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/organizations/{organizationsId}/environments/{environmentsId}/securityStats:queryTimeSeriesStats
/// Retrieve security statistics as a collection of time series.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `apigee_organizations_environments_security_stats_query_time_series_stats_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from apigee_organizations_environments_security_stats_query_time_series_stats_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_environments_security_stats_query_time_series_stats_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudApigeeV1QueryTimeSeriesStatsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudApigeeV1QueryTimeSeriesStatsResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/organizations/{organizationsId}/environments/{environmentsId}/securityStats:queryTimeSeriesStats
/// Retrieve security statistics as a collection of time series.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `apigee_organizations_environments_security_stats_query_time_series_stats_builder()` + `apigee_organizations_environments_security_stats_query_time_series_stats_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_environments_security_stats_query_time_series_stats(
    client: &SimpleHttpClient,
    orgenv: &str,
    body: &GoogleCloudApigeeV1QueryTimeSeriesStatsRequest,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudApigeeV1QueryTimeSeriesStatsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = apigee_organizations_environments_security_stats_query_time_series_stats_builder(
        client, orgenv, body,
    )?;
    apigee_organizations_environments_security_stats_query_time_series_stats_execute(builder)
}

/// GET v1/organizations/{organizationsId}/environments/{environmentsId}/sharedflows/{sharedflowsId}/deployments
/// Lists all deployments of a shared flow in an environment.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `apigee_organizations_environments_sharedflows_deployments_list_execute()` to send, or `apigee_organizations_environments_sharedflows_deployments_list` for simplest API.

pub fn apigee_organizations_environments_sharedflows_deployments_list_builder(
    client: &SimpleHttpClient,
    parent: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://apigee.googleapis.com/v1/organizations/{}/environments/{}/sharedflows/{}/deployments",
        parent,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/organizations/{organizationsId}/environments/{environmentsId}/sharedflows/{sharedflowsId}/deployments
/// Lists all deployments of a shared flow in an environment.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `apigee_organizations_environments_sharedflows_deployments_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from apigee_organizations_environments_sharedflows_deployments_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_environments_sharedflows_deployments_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudApigeeV1ListDeploymentsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudApigeeV1ListDeploymentsResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/organizations/{organizationsId}/environments/{environmentsId}/sharedflows/{sharedflowsId}/deployments
/// Lists all deployments of a shared flow in an environment.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `apigee_organizations_environments_sharedflows_deployments_list_builder()` + `apigee_organizations_environments_sharedflows_deployments_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_environments_sharedflows_deployments_list(
    client: &SimpleHttpClient,
    parent: &str,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudApigeeV1ListDeploymentsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        apigee_organizations_environments_sharedflows_deployments_list_builder(client, parent)?;
    apigee_organizations_environments_sharedflows_deployments_list_execute(builder)
}

/// GET v1/organizations/{organizationsId}/environments/{environmentsId}/sharedflows/{sharedflowsId}/revisions/{revisionsId}/deployments
/// Deploys a revision of a shared flow. If another revision of the same shared flow is currently deployed, set the override parameter to `true` to have this revision replace the currently deployed revision. You cannot use a shared flow until it has been deployed to an environment. For a request path `organizations/{org}/environments/{env}/sharedflows/{sf}/revisions/{rev}/deployments`, two permissions are required: * apigee.deployments.create on the resource `organizations/{org}/environments/{env}` * apigee.sharedflowrevisions.deploy on the resource `organizations/{org}/sharedflows/{sf}/revisions/{rev}`
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `apigee_organizations_environments_sharedflows_revisions_deploy_execute()` to send, or `apigee_organizations_environments_sharedflows_revisions_deploy` for simplest API.

pub fn apigee_organizations_environments_sharedflows_revisions_deploy_builder(
    client: &SimpleHttpClient,
    name: &str,
    override_rs: Option<bool>,
    serviceAccount: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://apigee.googleapis.com/v1/organizations/{}/environments/{}/sharedflows/{}/revisions/{}/deployments",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = override_rs {
        query_parts.push(format!("override={}", val));
    }
    if let Some(val) = serviceAccount {
        query_parts.push(format!("serviceAccount={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/organizations/{organizationsId}/environments/{environmentsId}/sharedflows/{sharedflowsId}/revisions/{revisionsId}/deployments
/// Deploys a revision of a shared flow. If another revision of the same shared flow is currently deployed, set the override parameter to `true` to have this revision replace the currently deployed revision. You cannot use a shared flow until it has been deployed to an environment. For a request path `organizations/{org}/environments/{env}/sharedflows/{sf}/revisions/{rev}/deployments`, two permissions are required: * apigee.deployments.create on the resource `organizations/{org}/environments/{env}` * apigee.sharedflowrevisions.deploy on the resource `organizations/{org}/sharedflows/{sf}/revisions/{rev}`
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `apigee_organizations_environments_sharedflows_revisions_deploy_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from apigee_organizations_environments_sharedflows_revisions_deploy_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_environments_sharedflows_revisions_deploy_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudApigeeV1Deployment>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudApigeeV1Deployment = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/organizations/{organizationsId}/environments/{environmentsId}/sharedflows/{sharedflowsId}/revisions/{revisionsId}/deployments
/// Deploys a revision of a shared flow. If another revision of the same shared flow is currently deployed, set the override parameter to `true` to have this revision replace the currently deployed revision. You cannot use a shared flow until it has been deployed to an environment. For a request path `organizations/{org}/environments/{env}/sharedflows/{sf}/revisions/{rev}/deployments`, two permissions are required: * apigee.deployments.create on the resource `organizations/{org}/environments/{env}` * apigee.sharedflowrevisions.deploy on the resource `organizations/{org}/sharedflows/{sf}/revisions/{rev}`
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `apigee_organizations_environments_sharedflows_revisions_deploy_builder()` + `apigee_organizations_environments_sharedflows_revisions_deploy_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_environments_sharedflows_revisions_deploy(
    client: &SimpleHttpClient,
    name: &str,
    override_rs: Option<bool>,
    serviceAccount: Option<&str>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudApigeeV1Deployment>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = apigee_organizations_environments_sharedflows_revisions_deploy_builder(
        client,
        name,
        override_rs,
        serviceAccount,
    )?;
    apigee_organizations_environments_sharedflows_revisions_deploy_execute(builder)
}

/// GET v1/organizations/{organizationsId}/environments/{environmentsId}/sharedflows/{sharedflowsId}/revisions/{revisionsId}/deployments
/// Gets the deployment of a shared flow revision and actual state reported by runtime pods.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `apigee_organizations_environments_sharedflows_revisions_get_deployments_execute()` to send, or `apigee_organizations_environments_sharedflows_revisions_get_deployments` for simplest API.

pub fn apigee_organizations_environments_sharedflows_revisions_get_deployments_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://apigee.googleapis.com/v1/organizations/{}/environments/{}/sharedflows/{}/revisions/{}/deployments",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/organizations/{organizationsId}/environments/{environmentsId}/sharedflows/{sharedflowsId}/revisions/{revisionsId}/deployments
/// Gets the deployment of a shared flow revision and actual state reported by runtime pods.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `apigee_organizations_environments_sharedflows_revisions_get_deployments_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from apigee_organizations_environments_sharedflows_revisions_get_deployments_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_environments_sharedflows_revisions_get_deployments_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudApigeeV1Deployment>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudApigeeV1Deployment = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/organizations/{organizationsId}/environments/{environmentsId}/sharedflows/{sharedflowsId}/revisions/{revisionsId}/deployments
/// Gets the deployment of a shared flow revision and actual state reported by runtime pods.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `apigee_organizations_environments_sharedflows_revisions_get_deployments_builder()` + `apigee_organizations_environments_sharedflows_revisions_get_deployments_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_environments_sharedflows_revisions_get_deployments(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudApigeeV1Deployment>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = apigee_organizations_environments_sharedflows_revisions_get_deployments_builder(
        client, name,
    )?;
    apigee_organizations_environments_sharedflows_revisions_get_deployments_execute(builder)
}

/// GET v1/organizations/{organizationsId}/environments/{environmentsId}/sharedflows/{sharedflowsId}/revisions/{revisionsId}/deployments
/// Undeploys a shared flow revision from an environment. For a request path `organizations/{org}/environments/{env}/sharedflows/{sf}/revisions/{rev}/deployments`, two permissions are required: * apigee.deployments.delete on the resource `organizations/{org}/environments/{env}` * apigee.sharedflowrevisions.undeploy on the resource `organizations/{org}/sharedflows/{sf}/revisions/{rev}`
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `apigee_organizations_environments_sharedflows_revisions_undeploy_execute()` to send, or `apigee_organizations_environments_sharedflows_revisions_undeploy` for simplest API.

pub fn apigee_organizations_environments_sharedflows_revisions_undeploy_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://apigee.googleapis.com/v1/organizations/{}/environments/{}/sharedflows/{}/revisions/{}/deployments",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/organizations/{organizationsId}/environments/{environmentsId}/sharedflows/{sharedflowsId}/revisions/{revisionsId}/deployments
/// Undeploys a shared flow revision from an environment. For a request path `organizations/{org}/environments/{env}/sharedflows/{sf}/revisions/{rev}/deployments`, two permissions are required: * apigee.deployments.delete on the resource `organizations/{org}/environments/{env}` * apigee.sharedflowrevisions.undeploy on the resource `organizations/{org}/sharedflows/{sf}/revisions/{rev}`
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `apigee_organizations_environments_sharedflows_revisions_undeploy_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from apigee_organizations_environments_sharedflows_revisions_undeploy_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_environments_sharedflows_revisions_undeploy_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleProtobufEmpty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/organizations/{organizationsId}/environments/{environmentsId}/sharedflows/{sharedflowsId}/revisions/{revisionsId}/deployments
/// Undeploys a shared flow revision from an environment. For a request path `organizations/{org}/environments/{env}/sharedflows/{sf}/revisions/{rev}/deployments`, two permissions are required: * apigee.deployments.delete on the resource `organizations/{org}/environments/{env}` * apigee.sharedflowrevisions.undeploy on the resource `organizations/{org}/sharedflows/{sf}/revisions/{rev}`
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `apigee_organizations_environments_sharedflows_revisions_undeploy_builder()` + `apigee_organizations_environments_sharedflows_revisions_undeploy_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_environments_sharedflows_revisions_undeploy(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        apigee_organizations_environments_sharedflows_revisions_undeploy_builder(client, name)?;
    apigee_organizations_environments_sharedflows_revisions_undeploy_execute(builder)
}

/// GET v1/organizations/{organizationsId}/environments/{environmentsId}/stats/{statsId}
/// Retrieve metrics grouped by dimensions. The types of metrics you can retrieve include traffic, message counts, API call latency, response size, and cache hits and counts. Dimensions let you view metrics in meaningful groups. You can optionally pass dimensions as path parameters to the stats API. If dimensions are not specified, the metrics are computed on the entire set of data for the given time range.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `apigee_organizations_environments_stats_get_execute()` to send, or `apigee_organizations_environments_stats_get` for simplest API.

pub fn apigee_organizations_environments_stats_get_builder(
    client: &SimpleHttpClient,
    name: &str,
    accuracy: Option<&str>,
    aggTable: Option<&str>,
    filter: Option<&str>,
    limit: Option<&str>,
    offset: Option<&str>,
    realtime: Option<bool>,
    select: Option<&str>,
    sonar: Option<bool>,
    sort: Option<&str>,
    sortby: Option<&str>,
    timeRange: Option<&str>,
    timeUnit: Option<&str>,
    topk: Option<&str>,
    tsAscending: Option<bool>,
    tzo: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://apigee.googleapis.com/v1/organizations/{}/environments/{}/stats/{}",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = accuracy {
        query_parts.push(format!("accuracy={}", val));
    }
    if let Some(val) = aggTable {
        query_parts.push(format!("aggTable={}", val));
    }
    if let Some(val) = filter {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = limit {
        query_parts.push(format!("limit={}", val));
    }
    if let Some(val) = offset {
        query_parts.push(format!("offset={}", val));
    }
    if let Some(val) = realtime {
        query_parts.push(format!("realtime={}", val));
    }
    if let Some(val) = select {
        query_parts.push(format!("select={}", val));
    }
    if let Some(val) = sonar {
        query_parts.push(format!("sonar={}", val));
    }
    if let Some(val) = sort {
        query_parts.push(format!("sort={}", val));
    }
    if let Some(val) = sortby {
        query_parts.push(format!("sortby={}", val));
    }
    if let Some(val) = timeRange {
        query_parts.push(format!("timeRange={}", val));
    }
    if let Some(val) = timeUnit {
        query_parts.push(format!("timeUnit={}", val));
    }
    if let Some(val) = topk {
        query_parts.push(format!("topk={}", val));
    }
    if let Some(val) = tsAscending {
        query_parts.push(format!("tsAscending={}", val));
    }
    if let Some(val) = tzo {
        query_parts.push(format!("tzo={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/organizations/{organizationsId}/environments/{environmentsId}/stats/{statsId}
/// Retrieve metrics grouped by dimensions. The types of metrics you can retrieve include traffic, message counts, API call latency, response size, and cache hits and counts. Dimensions let you view metrics in meaningful groups. You can optionally pass dimensions as path parameters to the stats API. If dimensions are not specified, the metrics are computed on the entire set of data for the given time range.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `apigee_organizations_environments_stats_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from apigee_organizations_environments_stats_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_environments_stats_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleCloudApigeeV1Stats>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudApigeeV1Stats = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/organizations/{organizationsId}/environments/{environmentsId}/stats/{statsId}
/// Retrieve metrics grouped by dimensions. The types of metrics you can retrieve include traffic, message counts, API call latency, response size, and cache hits and counts. Dimensions let you view metrics in meaningful groups. You can optionally pass dimensions as path parameters to the stats API. If dimensions are not specified, the metrics are computed on the entire set of data for the given time range.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `apigee_organizations_environments_stats_get_builder()` + `apigee_organizations_environments_stats_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_environments_stats_get(
    client: &SimpleHttpClient,
    name: &str,
    accuracy: Option<&str>,
    aggTable: Option<&str>,
    filter: Option<&str>,
    limit: Option<&str>,
    offset: Option<&str>,
    realtime: Option<bool>,
    select: Option<&str>,
    sonar: Option<bool>,
    sort: Option<&str>,
    sortby: Option<&str>,
    timeRange: Option<&str>,
    timeUnit: Option<&str>,
    topk: Option<&str>,
    tsAscending: Option<bool>,
    tzo: Option<&str>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleCloudApigeeV1Stats>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = apigee_organizations_environments_stats_get_builder(
        client,
        name,
        accuracy,
        aggTable,
        filter,
        limit,
        offset,
        realtime,
        select,
        sonar,
        sort,
        sortby,
        timeRange,
        timeUnit,
        topk,
        tsAscending,
        tzo,
    )?;
    apigee_organizations_environments_stats_get_execute(builder)
}

/// GET v1/organizations/{organizationsId}/environments/{environmentsId}/targetservers
/// Creates a TargetServer in the specified environment.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `apigee_organizations_environments_targetservers_create_execute()` to send, or `apigee_organizations_environments_targetservers_create` for simplest API.

pub fn apigee_organizations_environments_targetservers_create_builder(
    client: &SimpleHttpClient,
    parent: &str,
    name: Option<&str>,
    body: &GoogleCloudApigeeV1TargetServer,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://apigee.googleapis.com/v1/organizations/{}/environments/{}/targetservers",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = name {
        query_parts.push(format!("name={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/organizations/{organizationsId}/environments/{environmentsId}/targetservers
/// Creates a TargetServer in the specified environment.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `apigee_organizations_environments_targetservers_create_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from apigee_organizations_environments_targetservers_create_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_environments_targetservers_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudApigeeV1TargetServer>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudApigeeV1TargetServer = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/organizations/{organizationsId}/environments/{environmentsId}/targetservers
/// Creates a TargetServer in the specified environment.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `apigee_organizations_environments_targetservers_create_builder()` + `apigee_organizations_environments_targetservers_create_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_environments_targetservers_create(
    client: &SimpleHttpClient,
    parent: &str,
    name: Option<&str>,
    body: &GoogleCloudApigeeV1TargetServer,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudApigeeV1TargetServer>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        apigee_organizations_environments_targetservers_create_builder(client, parent, name, body)?;
    apigee_organizations_environments_targetservers_create_execute(builder)
}

/// GET v1/organizations/{organizationsId}/environments/{environmentsId}/targetservers/{targetserversId}
/// Deletes a TargetServer from an environment. Returns the deleted TargetServer resource.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `apigee_organizations_environments_targetservers_delete_execute()` to send, or `apigee_organizations_environments_targetservers_delete` for simplest API.

pub fn apigee_organizations_environments_targetservers_delete_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://apigee.googleapis.com/v1/organizations/{}/environments/{}/targetservers/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/organizations/{organizationsId}/environments/{environmentsId}/targetservers/{targetserversId}
/// Deletes a TargetServer from an environment. Returns the deleted TargetServer resource.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `apigee_organizations_environments_targetservers_delete_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from apigee_organizations_environments_targetservers_delete_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_environments_targetservers_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudApigeeV1TargetServer>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudApigeeV1TargetServer = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/organizations/{organizationsId}/environments/{environmentsId}/targetservers/{targetserversId}
/// Deletes a TargetServer from an environment. Returns the deleted TargetServer resource.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `apigee_organizations_environments_targetservers_delete_builder()` + `apigee_organizations_environments_targetservers_delete_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_environments_targetservers_delete(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudApigeeV1TargetServer>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = apigee_organizations_environments_targetservers_delete_builder(client, name)?;
    apigee_organizations_environments_targetservers_delete_execute(builder)
}

/// GET v1/organizations/{organizationsId}/environments/{environmentsId}/targetservers/{targetserversId}
/// Gets a TargetServer resource.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `apigee_organizations_environments_targetservers_get_execute()` to send, or `apigee_organizations_environments_targetservers_get` for simplest API.

pub fn apigee_organizations_environments_targetservers_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://apigee.googleapis.com/v1/organizations/{}/environments/{}/targetservers/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/organizations/{organizationsId}/environments/{environmentsId}/targetservers/{targetserversId}
/// Gets a TargetServer resource.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `apigee_organizations_environments_targetservers_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from apigee_organizations_environments_targetservers_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_environments_targetservers_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudApigeeV1TargetServer>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudApigeeV1TargetServer = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/organizations/{organizationsId}/environments/{environmentsId}/targetservers/{targetserversId}
/// Gets a TargetServer resource.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `apigee_organizations_environments_targetservers_get_builder()` + `apigee_organizations_environments_targetservers_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_environments_targetservers_get(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudApigeeV1TargetServer>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = apigee_organizations_environments_targetservers_get_builder(client, name)?;
    apigee_organizations_environments_targetservers_get_execute(builder)
}

/// GET v1/organizations/{organizationsId}/environments/{environmentsId}/targetservers/{targetserversId}
/// Updates an existing TargetServer. Note that this operation has PUT semantics; it will replace the entirety of the existing TargetServer with the resource in the request body.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `apigee_organizations_environments_targetservers_update_execute()` to send, or `apigee_organizations_environments_targetservers_update` for simplest API.

pub fn apigee_organizations_environments_targetservers_update_builder(
    client: &SimpleHttpClient,
    name: &str,
    body: &GoogleCloudApigeeV1TargetServer,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://apigee.googleapis.com/v1/organizations/{}/environments/{}/targetservers/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/organizations/{organizationsId}/environments/{environmentsId}/targetservers/{targetserversId}
/// Updates an existing TargetServer. Note that this operation has PUT semantics; it will replace the entirety of the existing TargetServer with the resource in the request body.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `apigee_organizations_environments_targetservers_update_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from apigee_organizations_environments_targetservers_update_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_environments_targetservers_update_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudApigeeV1TargetServer>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudApigeeV1TargetServer = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/organizations/{organizationsId}/environments/{environmentsId}/targetservers/{targetserversId}
/// Updates an existing TargetServer. Note that this operation has PUT semantics; it will replace the entirety of the existing TargetServer with the resource in the request body.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `apigee_organizations_environments_targetservers_update_builder()` + `apigee_organizations_environments_targetservers_update_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_environments_targetservers_update(
    client: &SimpleHttpClient,
    name: &str,
    body: &GoogleCloudApigeeV1TargetServer,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudApigeeV1TargetServer>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        apigee_organizations_environments_targetservers_update_builder(client, name, body)?;
    apigee_organizations_environments_targetservers_update_execute(builder)
}

/// GET v1/organizations/{organizationsId}/environments/{environmentsId}/traceConfig/overrides
/// Creates a trace configuration override. The response contains a system-generated UUID, that can be used to view, update, or delete the configuration override. Use the List API to view the existing trace configuration overrides.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `apigee_organizations_environments_trace_config_overrides_create_execute()` to send, or `apigee_organizations_environments_trace_config_overrides_create` for simplest API.

pub fn apigee_organizations_environments_trace_config_overrides_create_builder(
    client: &SimpleHttpClient,
    parent: &str,
    body: &GoogleCloudApigeeV1TraceConfigOverride,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://apigee.googleapis.com/v1/organizations/{}/environments/{}/traceConfig/overrides",
        parent,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/organizations/{organizationsId}/environments/{environmentsId}/traceConfig/overrides
/// Creates a trace configuration override. The response contains a system-generated UUID, that can be used to view, update, or delete the configuration override. Use the List API to view the existing trace configuration overrides.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `apigee_organizations_environments_trace_config_overrides_create_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from apigee_organizations_environments_trace_config_overrides_create_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_environments_trace_config_overrides_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudApigeeV1TraceConfigOverride>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudApigeeV1TraceConfigOverride = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/organizations/{organizationsId}/environments/{environmentsId}/traceConfig/overrides
/// Creates a trace configuration override. The response contains a system-generated UUID, that can be used to view, update, or delete the configuration override. Use the List API to view the existing trace configuration overrides.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `apigee_organizations_environments_trace_config_overrides_create_builder()` + `apigee_organizations_environments_trace_config_overrides_create_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_environments_trace_config_overrides_create(
    client: &SimpleHttpClient,
    parent: &str,
    body: &GoogleCloudApigeeV1TraceConfigOverride,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudApigeeV1TraceConfigOverride>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = apigee_organizations_environments_trace_config_overrides_create_builder(
        client, parent, body,
    )?;
    apigee_organizations_environments_trace_config_overrides_create_execute(builder)
}

/// GET v1/organizations/{organizationsId}/environments/{environmentsId}/traceConfig/overrides/{overridesId}
/// Deletes a distributed trace configuration override.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `apigee_organizations_environments_trace_config_overrides_delete_execute()` to send, or `apigee_organizations_environments_trace_config_overrides_delete` for simplest API.

pub fn apigee_organizations_environments_trace_config_overrides_delete_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://apigee.googleapis.com/v1/organizations/{}/environments/{}/traceConfig/overrides/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/organizations/{organizationsId}/environments/{environmentsId}/traceConfig/overrides/{overridesId}
/// Deletes a distributed trace configuration override.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `apigee_organizations_environments_trace_config_overrides_delete_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from apigee_organizations_environments_trace_config_overrides_delete_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_environments_trace_config_overrides_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleProtobufEmpty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/organizations/{organizationsId}/environments/{environmentsId}/traceConfig/overrides/{overridesId}
/// Deletes a distributed trace configuration override.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `apigee_organizations_environments_trace_config_overrides_delete_builder()` + `apigee_organizations_environments_trace_config_overrides_delete_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_environments_trace_config_overrides_delete(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        apigee_organizations_environments_trace_config_overrides_delete_builder(client, name)?;
    apigee_organizations_environments_trace_config_overrides_delete_execute(builder)
}

/// GET v1/organizations/{organizationsId}/environments/{environmentsId}/traceConfig/overrides/{overridesId}
/// Gets a trace configuration override.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `apigee_organizations_environments_trace_config_overrides_get_execute()` to send, or `apigee_organizations_environments_trace_config_overrides_get` for simplest API.

pub fn apigee_organizations_environments_trace_config_overrides_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://apigee.googleapis.com/v1/organizations/{}/environments/{}/traceConfig/overrides/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/organizations/{organizationsId}/environments/{environmentsId}/traceConfig/overrides/{overridesId}
/// Gets a trace configuration override.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `apigee_organizations_environments_trace_config_overrides_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from apigee_organizations_environments_trace_config_overrides_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_environments_trace_config_overrides_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudApigeeV1TraceConfigOverride>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudApigeeV1TraceConfigOverride = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/organizations/{organizationsId}/environments/{environmentsId}/traceConfig/overrides/{overridesId}
/// Gets a trace configuration override.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `apigee_organizations_environments_trace_config_overrides_get_builder()` + `apigee_organizations_environments_trace_config_overrides_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_environments_trace_config_overrides_get(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudApigeeV1TraceConfigOverride>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        apigee_organizations_environments_trace_config_overrides_get_builder(client, name)?;
    apigee_organizations_environments_trace_config_overrides_get_execute(builder)
}

/// GET v1/organizations/{organizationsId}/environments/{environmentsId}/traceConfig/overrides
/// Lists all of the distributed trace configuration overrides in an environment.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `apigee_organizations_environments_trace_config_overrides_list_execute()` to send, or `apigee_organizations_environments_trace_config_overrides_list` for simplest API.

pub fn apigee_organizations_environments_trace_config_overrides_list_builder(
    client: &SimpleHttpClient,
    parent: &str,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://apigee.googleapis.com/v1/organizations/{}/environments/{}/traceConfig/overrides",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/organizations/{organizationsId}/environments/{environmentsId}/traceConfig/overrides
/// Lists all of the distributed trace configuration overrides in an environment.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `apigee_organizations_environments_trace_config_overrides_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from apigee_organizations_environments_trace_config_overrides_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_environments_trace_config_overrides_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudApigeeV1ListTraceConfigOverridesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudApigeeV1ListTraceConfigOverridesResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/organizations/{organizationsId}/environments/{environmentsId}/traceConfig/overrides
/// Lists all of the distributed trace configuration overrides in an environment.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `apigee_organizations_environments_trace_config_overrides_list_builder()` + `apigee_organizations_environments_trace_config_overrides_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_environments_trace_config_overrides_list(
    client: &SimpleHttpClient,
    parent: &str,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudApigeeV1ListTraceConfigOverridesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = apigee_organizations_environments_trace_config_overrides_list_builder(
        client, parent, pageSize, pageToken,
    )?;
    apigee_organizations_environments_trace_config_overrides_list_execute(builder)
}

/// GET v1/organizations/{organizationsId}/environments/{environmentsId}/traceConfig/overrides/{overridesId}
/// Updates a distributed trace configuration override. Note that the repeated fields have replace semantics when included in the field mask and that they will be overwritten by the value of the fields in the request body.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `apigee_organizations_environments_trace_config_overrides_patch_execute()` to send, or `apigee_organizations_environments_trace_config_overrides_patch` for simplest API.

pub fn apigee_organizations_environments_trace_config_overrides_patch_builder(
    client: &SimpleHttpClient,
    name: &str,
    updateMask: Option<&str>,
    body: &GoogleCloudApigeeV1TraceConfigOverride,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://apigee.googleapis.com/v1/organizations/{}/environments/{}/traceConfig/overrides/{}",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = updateMask {
        query_parts.push(format!("updateMask={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/organizations/{organizationsId}/environments/{environmentsId}/traceConfig/overrides/{overridesId}
/// Updates a distributed trace configuration override. Note that the repeated fields have replace semantics when included in the field mask and that they will be overwritten by the value of the fields in the request body.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `apigee_organizations_environments_trace_config_overrides_patch_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from apigee_organizations_environments_trace_config_overrides_patch_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_environments_trace_config_overrides_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudApigeeV1TraceConfigOverride>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudApigeeV1TraceConfigOverride = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/organizations/{organizationsId}/environments/{environmentsId}/traceConfig/overrides/{overridesId}
/// Updates a distributed trace configuration override. Note that the repeated fields have replace semantics when included in the field mask and that they will be overwritten by the value of the fields in the request body.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `apigee_organizations_environments_trace_config_overrides_patch_builder()` + `apigee_organizations_environments_trace_config_overrides_patch_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_environments_trace_config_overrides_patch(
    client: &SimpleHttpClient,
    name: &str,
    updateMask: Option<&str>,
    body: &GoogleCloudApigeeV1TraceConfigOverride,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudApigeeV1TraceConfigOverride>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = apigee_organizations_environments_trace_config_overrides_patch_builder(
        client, name, updateMask, body,
    )?;
    apigee_organizations_environments_trace_config_overrides_patch_execute(builder)
}

/// GET v1/organizations/{organizationsId}/hostQueries
/// Submit a query at host level to be processed in the background. If the submission of the query succeeds, the API returns a 201 status and an ID that refer to the query. In addition to the HTTP status 201, the state of "enqueued" means that the request succeeded.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `apigee_organizations_host_queries_create_execute()` to send, or `apigee_organizations_host_queries_create` for simplest API.

pub fn apigee_organizations_host_queries_create_builder(
    client: &SimpleHttpClient,
    parent: &str,
    body: &GoogleCloudApigeeV1Query,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://apigee.googleapis.com/v1/organizations/{}/hostQueries",
        parent,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/organizations/{organizationsId}/hostQueries
/// Submit a query at host level to be processed in the background. If the submission of the query succeeds, the API returns a 201 status and an ID that refer to the query. In addition to the HTTP status 201, the state of "enqueued" means that the request succeeded.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `apigee_organizations_host_queries_create_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from apigee_organizations_host_queries_create_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_host_queries_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudApigeeV1AsyncQuery>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudApigeeV1AsyncQuery = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/organizations/{organizationsId}/hostQueries
/// Submit a query at host level to be processed in the background. If the submission of the query succeeds, the API returns a 201 status and an ID that refer to the query. In addition to the HTTP status 201, the state of "enqueued" means that the request succeeded.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `apigee_organizations_host_queries_create_builder()` + `apigee_organizations_host_queries_create_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_host_queries_create(
    client: &SimpleHttpClient,
    parent: &str,
    body: &GoogleCloudApigeeV1Query,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudApigeeV1AsyncQuery>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = apigee_organizations_host_queries_create_builder(client, parent, body)?;
    apigee_organizations_host_queries_create_execute(builder)
}

/// GET v1/organizations/{organizationsId}/hostQueries/{hostQueriesId}
/// Get status of a query submitted at host level. If the query is still in progress, the state is set to "running" After the query has completed successfully, state is set to "completed"
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `apigee_organizations_host_queries_get_execute()` to send, or `apigee_organizations_host_queries_get` for simplest API.

pub fn apigee_organizations_host_queries_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://apigee.googleapis.com/v1/organizations/{}/hostQueries/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/organizations/{organizationsId}/hostQueries/{hostQueriesId}
/// Get status of a query submitted at host level. If the query is still in progress, the state is set to "running" After the query has completed successfully, state is set to "completed"
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `apigee_organizations_host_queries_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from apigee_organizations_host_queries_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_host_queries_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudApigeeV1AsyncQuery>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudApigeeV1AsyncQuery = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/organizations/{organizationsId}/hostQueries/{hostQueriesId}
/// Get status of a query submitted at host level. If the query is still in progress, the state is set to "running" After the query has completed successfully, state is set to "completed"
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `apigee_organizations_host_queries_get_builder()` + `apigee_organizations_host_queries_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_host_queries_get(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudApigeeV1AsyncQuery>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = apigee_organizations_host_queries_get_builder(client, name)?;
    apigee_organizations_host_queries_get_execute(builder)
}

/// GET v1/organizations/{organizationsId}/hostQueries/{hostQueriesId}/result
/// After the query is completed, use this API to retrieve the results. If the request succeeds, and there is a non-zero result set, the result is downloaded to the client as a zipped JSON file. The name of the downloaded file will be: OfflineQueryResult-.zip Example: OfflineQueryResult-9cfc0d85-0f30-46d6-ae6f-318d0cb961bd.zip
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `apigee_organizations_host_queries_get_result_execute()` to send, or `apigee_organizations_host_queries_get_result` for simplest API.

pub fn apigee_organizations_host_queries_get_result_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://apigee.googleapis.com/v1/organizations/{}/hostQueries/{}/result",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/organizations/{organizationsId}/hostQueries/{hostQueriesId}/result
/// After the query is completed, use this API to retrieve the results. If the request succeeds, and there is a non-zero result set, the result is downloaded to the client as a zipped JSON file. The name of the downloaded file will be: OfflineQueryResult-.zip Example: OfflineQueryResult-9cfc0d85-0f30-46d6-ae6f-318d0cb961bd.zip
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `apigee_organizations_host_queries_get_result_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from apigee_organizations_host_queries_get_result_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_host_queries_get_result_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleApiHttpBody>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleApiHttpBody = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/organizations/{organizationsId}/hostQueries/{hostQueriesId}/result
/// After the query is completed, use this API to retrieve the results. If the request succeeds, and there is a non-zero result set, the result is downloaded to the client as a zipped JSON file. The name of the downloaded file will be: OfflineQueryResult-.zip Example: OfflineQueryResult-9cfc0d85-0f30-46d6-ae6f-318d0cb961bd.zip
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `apigee_organizations_host_queries_get_result_builder()` + `apigee_organizations_host_queries_get_result_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_host_queries_get_result(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleApiHttpBody>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = apigee_organizations_host_queries_get_result_builder(client, name)?;
    apigee_organizations_host_queries_get_result_execute(builder)
}

/// GET v1/organizations/{organizationsId}/hostQueries/{hostQueriesId}/resultView
///
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `apigee_organizations_host_queries_get_result_view_execute()` to send, or `apigee_organizations_host_queries_get_result_view` for simplest API.

pub fn apigee_organizations_host_queries_get_result_view_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://apigee.googleapis.com/v1/organizations/{}/hostQueries/{}/resultView",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/organizations/{organizationsId}/hostQueries/{hostQueriesId}/resultView
///
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `apigee_organizations_host_queries_get_result_view_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from apigee_organizations_host_queries_get_result_view_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_host_queries_get_result_view_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudApigeeV1AsyncQueryResultView>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudApigeeV1AsyncQueryResultView =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/organizations/{organizationsId}/hostQueries/{hostQueriesId}/resultView
///
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `apigee_organizations_host_queries_get_result_view_builder()` + `apigee_organizations_host_queries_get_result_view_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_host_queries_get_result_view(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudApigeeV1AsyncQueryResultView>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = apigee_organizations_host_queries_get_result_view_builder(client, name)?;
    apigee_organizations_host_queries_get_result_view_execute(builder)
}

/// GET v1/organizations/{organizationsId}/hostQueries
/// Return a list of Asynchronous Queries at host level.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `apigee_organizations_host_queries_list_execute()` to send, or `apigee_organizations_host_queries_list` for simplest API.

pub fn apigee_organizations_host_queries_list_builder(
    client: &SimpleHttpClient,
    parent: &str,
    dataset: Option<&str>,
    envgroupHostname: Option<&str>,
    from: Option<&str>,
    inclQueriesWithoutReport: Option<&str>,
    status: Option<&str>,
    submittedBy: Option<&str>,
    to: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://apigee.googleapis.com/v1/organizations/{}/hostQueries",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = dataset {
        query_parts.push(format!("dataset={}", val));
    }
    if let Some(val) = envgroupHostname {
        query_parts.push(format!("envgroupHostname={}", val));
    }
    if let Some(val) = from {
        query_parts.push(format!("from={}", val));
    }
    if let Some(val) = inclQueriesWithoutReport {
        query_parts.push(format!("inclQueriesWithoutReport={}", val));
    }
    if let Some(val) = status {
        query_parts.push(format!("status={}", val));
    }
    if let Some(val) = submittedBy {
        query_parts.push(format!("submittedBy={}", val));
    }
    if let Some(val) = to {
        query_parts.push(format!("to={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/organizations/{organizationsId}/hostQueries
/// Return a list of Asynchronous Queries at host level.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `apigee_organizations_host_queries_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from apigee_organizations_host_queries_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_host_queries_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudApigeeV1ListAsyncQueriesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudApigeeV1ListAsyncQueriesResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/organizations/{organizationsId}/hostQueries
/// Return a list of Asynchronous Queries at host level.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `apigee_organizations_host_queries_list_builder()` + `apigee_organizations_host_queries_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_host_queries_list(
    client: &SimpleHttpClient,
    parent: &str,
    dataset: Option<&str>,
    envgroupHostname: Option<&str>,
    from: Option<&str>,
    inclQueriesWithoutReport: Option<&str>,
    status: Option<&str>,
    submittedBy: Option<&str>,
    to: Option<&str>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudApigeeV1ListAsyncQueriesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = apigee_organizations_host_queries_list_builder(
        client,
        parent,
        dataset,
        envgroupHostname,
        from,
        inclQueriesWithoutReport,
        status,
        submittedBy,
        to,
    )?;
    apigee_organizations_host_queries_list_execute(builder)
}

/// GET v1/organizations/{organizationsId}/hostSecurityReports
/// Submit a query at host level to be processed in the background. If the submission of the query succeeds, the API returns a 201 status and an ID that refer to the query. In addition to the HTTP status 201, the state of "enqueued" means that the request succeeded.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `apigee_organizations_host_security_reports_create_execute()` to send, or `apigee_organizations_host_security_reports_create` for simplest API.

pub fn apigee_organizations_host_security_reports_create_builder(
    client: &SimpleHttpClient,
    parent: &str,
    body: &GoogleCloudApigeeV1SecurityReportQuery,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://apigee.googleapis.com/v1/organizations/{}/hostSecurityReports",
        parent,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/organizations/{organizationsId}/hostSecurityReports
/// Submit a query at host level to be processed in the background. If the submission of the query succeeds, the API returns a 201 status and an ID that refer to the query. In addition to the HTTP status 201, the state of "enqueued" means that the request succeeded.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `apigee_organizations_host_security_reports_create_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from apigee_organizations_host_security_reports_create_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_host_security_reports_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudApigeeV1SecurityReport>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudApigeeV1SecurityReport = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/organizations/{organizationsId}/hostSecurityReports
/// Submit a query at host level to be processed in the background. If the submission of the query succeeds, the API returns a 201 status and an ID that refer to the query. In addition to the HTTP status 201, the state of "enqueued" means that the request succeeded.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `apigee_organizations_host_security_reports_create_builder()` + `apigee_organizations_host_security_reports_create_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_host_security_reports_create(
    client: &SimpleHttpClient,
    parent: &str,
    body: &GoogleCloudApigeeV1SecurityReportQuery,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudApigeeV1SecurityReport>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = apigee_organizations_host_security_reports_create_builder(client, parent, body)?;
    apigee_organizations_host_security_reports_create_execute(builder)
}

/// GET v1/organizations/{organizationsId}/hostSecurityReports/{hostSecurityReportsId}
/// Get status of a query submitted at host level. If the query is still in progress, the state is set to "running" After the query has completed successfully, state is set to "completed"
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `apigee_organizations_host_security_reports_get_execute()` to send, or `apigee_organizations_host_security_reports_get` for simplest API.

pub fn apigee_organizations_host_security_reports_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://apigee.googleapis.com/v1/organizations/{}/hostSecurityReports/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/organizations/{organizationsId}/hostSecurityReports/{hostSecurityReportsId}
/// Get status of a query submitted at host level. If the query is still in progress, the state is set to "running" After the query has completed successfully, state is set to "completed"
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `apigee_organizations_host_security_reports_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from apigee_organizations_host_security_reports_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_host_security_reports_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudApigeeV1SecurityReport>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudApigeeV1SecurityReport = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/organizations/{organizationsId}/hostSecurityReports/{hostSecurityReportsId}
/// Get status of a query submitted at host level. If the query is still in progress, the state is set to "running" After the query has completed successfully, state is set to "completed"
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `apigee_organizations_host_security_reports_get_builder()` + `apigee_organizations_host_security_reports_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_host_security_reports_get(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudApigeeV1SecurityReport>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = apigee_organizations_host_security_reports_get_builder(client, name)?;
    apigee_organizations_host_security_reports_get_execute(builder)
}

/// GET v1/organizations/{organizationsId}/hostSecurityReports/{hostSecurityReportsId}/result
/// After the query is completed, use this API to retrieve the results. If the request succeeds, and there is a non-zero result set, the result is downloaded to the client as a zipped JSON file. The name of the downloaded file will be: OfflineQueryResult-.zip Example: OfflineQueryResult-9cfc0d85-0f30-46d6-ae6f-318d0cb961bd.zip
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `apigee_organizations_host_security_reports_get_result_execute()` to send, or `apigee_organizations_host_security_reports_get_result` for simplest API.

pub fn apigee_organizations_host_security_reports_get_result_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://apigee.googleapis.com/v1/organizations/{}/hostSecurityReports/{}/result",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/organizations/{organizationsId}/hostSecurityReports/{hostSecurityReportsId}/result
/// After the query is completed, use this API to retrieve the results. If the request succeeds, and there is a non-zero result set, the result is downloaded to the client as a zipped JSON file. The name of the downloaded file will be: OfflineQueryResult-.zip Example: OfflineQueryResult-9cfc0d85-0f30-46d6-ae6f-318d0cb961bd.zip
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `apigee_organizations_host_security_reports_get_result_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from apigee_organizations_host_security_reports_get_result_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_host_security_reports_get_result_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleApiHttpBody>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleApiHttpBody = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/organizations/{organizationsId}/hostSecurityReports/{hostSecurityReportsId}/result
/// After the query is completed, use this API to retrieve the results. If the request succeeds, and there is a non-zero result set, the result is downloaded to the client as a zipped JSON file. The name of the downloaded file will be: OfflineQueryResult-.zip Example: OfflineQueryResult-9cfc0d85-0f30-46d6-ae6f-318d0cb961bd.zip
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `apigee_organizations_host_security_reports_get_result_builder()` + `apigee_organizations_host_security_reports_get_result_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_host_security_reports_get_result(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleApiHttpBody>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = apigee_organizations_host_security_reports_get_result_builder(client, name)?;
    apigee_organizations_host_security_reports_get_result_execute(builder)
}

/// GET v1/organizations/{organizationsId}/hostSecurityReports/{hostSecurityReportsId}/resultView
/// After the query is completed, use this API to view the query result when result size is small.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `apigee_organizations_host_security_reports_get_result_view_execute()` to send, or `apigee_organizations_host_security_reports_get_result_view` for simplest API.

pub fn apigee_organizations_host_security_reports_get_result_view_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://apigee.googleapis.com/v1/organizations/{}/hostSecurityReports/{}/resultView",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/organizations/{organizationsId}/hostSecurityReports/{hostSecurityReportsId}/resultView
/// After the query is completed, use this API to view the query result when result size is small.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `apigee_organizations_host_security_reports_get_result_view_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from apigee_organizations_host_security_reports_get_result_view_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_host_security_reports_get_result_view_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudApigeeV1SecurityReportResultView>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudApigeeV1SecurityReportResultView =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/organizations/{organizationsId}/hostSecurityReports/{hostSecurityReportsId}/resultView
/// After the query is completed, use this API to view the query result when result size is small.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `apigee_organizations_host_security_reports_get_result_view_builder()` + `apigee_organizations_host_security_reports_get_result_view_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_host_security_reports_get_result_view(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudApigeeV1SecurityReportResultView>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = apigee_organizations_host_security_reports_get_result_view_builder(client, name)?;
    apigee_organizations_host_security_reports_get_result_view_execute(builder)
}

/// GET v1/organizations/{organizationsId}/hostSecurityReports
/// Return a list of Security Reports at host level.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `apigee_organizations_host_security_reports_list_execute()` to send, or `apigee_organizations_host_security_reports_list` for simplest API.

pub fn apigee_organizations_host_security_reports_list_builder(
    client: &SimpleHttpClient,
    parent: &str,
    dataset: Option<&str>,
    envgroupHostname: Option<&str>,
    from: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
    status: Option<&str>,
    submittedBy: Option<&str>,
    to: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://apigee.googleapis.com/v1/organizations/{}/hostSecurityReports",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = dataset {
        query_parts.push(format!("dataset={}", val));
    }
    if let Some(val) = envgroupHostname {
        query_parts.push(format!("envgroupHostname={}", val));
    }
    if let Some(val) = from {
        query_parts.push(format!("from={}", val));
    }
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = status {
        query_parts.push(format!("status={}", val));
    }
    if let Some(val) = submittedBy {
        query_parts.push(format!("submittedBy={}", val));
    }
    if let Some(val) = to {
        query_parts.push(format!("to={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/organizations/{organizationsId}/hostSecurityReports
/// Return a list of Security Reports at host level.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `apigee_organizations_host_security_reports_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from apigee_organizations_host_security_reports_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_host_security_reports_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudApigeeV1ListSecurityReportsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudApigeeV1ListSecurityReportsResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/organizations/{organizationsId}/hostSecurityReports
/// Return a list of Security Reports at host level.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `apigee_organizations_host_security_reports_list_builder()` + `apigee_organizations_host_security_reports_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_host_security_reports_list(
    client: &SimpleHttpClient,
    parent: &str,
    dataset: Option<&str>,
    envgroupHostname: Option<&str>,
    from: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
    status: Option<&str>,
    submittedBy: Option<&str>,
    to: Option<&str>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudApigeeV1ListSecurityReportsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = apigee_organizations_host_security_reports_list_builder(
        client,
        parent,
        dataset,
        envgroupHostname,
        from,
        pageSize,
        pageToken,
        status,
        submittedBy,
        to,
    )?;
    apigee_organizations_host_security_reports_list_execute(builder)
}

/// GET v1/organizations/{organizationsId}/hostStats/{hostStatsId}
/// Retrieve metrics grouped by dimensions in host level. The types of metrics you can retrieve include traffic, message counts, API call latency, response size, and cache hits and counts. Dimensions let you view metrics in meaningful groups. You can optionally pass dimensions as path parameters to the stats API. If dimensions are not specified, the metrics are computed on the entire set of data for the given time range.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `apigee_organizations_host_stats_get_execute()` to send, or `apigee_organizations_host_stats_get` for simplest API.

pub fn apigee_organizations_host_stats_get_builder(
    client: &SimpleHttpClient,
    name: &str,
    accuracy: Option<&str>,
    envgroupHostname: Option<&str>,
    filter: Option<&str>,
    limit: Option<&str>,
    offset: Option<&str>,
    realtime: Option<bool>,
    select: Option<&str>,
    sort: Option<&str>,
    sortby: Option<&str>,
    timeRange: Option<&str>,
    timeUnit: Option<&str>,
    topk: Option<&str>,
    tsAscending: Option<bool>,
    tzo: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://apigee.googleapis.com/v1/organizations/{}/hostStats/{}",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = accuracy {
        query_parts.push(format!("accuracy={}", val));
    }
    if let Some(val) = envgroupHostname {
        query_parts.push(format!("envgroupHostname={}", val));
    }
    if let Some(val) = filter {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = limit {
        query_parts.push(format!("limit={}", val));
    }
    if let Some(val) = offset {
        query_parts.push(format!("offset={}", val));
    }
    if let Some(val) = realtime {
        query_parts.push(format!("realtime={}", val));
    }
    if let Some(val) = select {
        query_parts.push(format!("select={}", val));
    }
    if let Some(val) = sort {
        query_parts.push(format!("sort={}", val));
    }
    if let Some(val) = sortby {
        query_parts.push(format!("sortby={}", val));
    }
    if let Some(val) = timeRange {
        query_parts.push(format!("timeRange={}", val));
    }
    if let Some(val) = timeUnit {
        query_parts.push(format!("timeUnit={}", val));
    }
    if let Some(val) = topk {
        query_parts.push(format!("topk={}", val));
    }
    if let Some(val) = tsAscending {
        query_parts.push(format!("tsAscending={}", val));
    }
    if let Some(val) = tzo {
        query_parts.push(format!("tzo={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/organizations/{organizationsId}/hostStats/{hostStatsId}
/// Retrieve metrics grouped by dimensions in host level. The types of metrics you can retrieve include traffic, message counts, API call latency, response size, and cache hits and counts. Dimensions let you view metrics in meaningful groups. You can optionally pass dimensions as path parameters to the stats API. If dimensions are not specified, the metrics are computed on the entire set of data for the given time range.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `apigee_organizations_host_stats_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from apigee_organizations_host_stats_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_host_stats_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleCloudApigeeV1Stats>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudApigeeV1Stats = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/organizations/{organizationsId}/hostStats/{hostStatsId}
/// Retrieve metrics grouped by dimensions in host level. The types of metrics you can retrieve include traffic, message counts, API call latency, response size, and cache hits and counts. Dimensions let you view metrics in meaningful groups. You can optionally pass dimensions as path parameters to the stats API. If dimensions are not specified, the metrics are computed on the entire set of data for the given time range.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `apigee_organizations_host_stats_get_builder()` + `apigee_organizations_host_stats_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_host_stats_get(
    client: &SimpleHttpClient,
    name: &str,
    accuracy: Option<&str>,
    envgroupHostname: Option<&str>,
    filter: Option<&str>,
    limit: Option<&str>,
    offset: Option<&str>,
    realtime: Option<bool>,
    select: Option<&str>,
    sort: Option<&str>,
    sortby: Option<&str>,
    timeRange: Option<&str>,
    timeUnit: Option<&str>,
    topk: Option<&str>,
    tsAscending: Option<bool>,
    tzo: Option<&str>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleCloudApigeeV1Stats>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = apigee_organizations_host_stats_get_builder(
        client,
        name,
        accuracy,
        envgroupHostname,
        filter,
        limit,
        offset,
        realtime,
        select,
        sort,
        sortby,
        timeRange,
        timeUnit,
        topk,
        tsAscending,
        tzo,
    )?;
    apigee_organizations_host_stats_get_execute(builder)
}

/// GET v1/organizations/{organizationsId}/instances
/// Creates an Apigee runtime instance. The instance is accessible from the authorized network configured on the organization. **Note:** Not supported for Apigee hybrid.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `apigee_organizations_instances_create_execute()` to send, or `apigee_organizations_instances_create` for simplest API.

pub fn apigee_organizations_instances_create_builder(
    client: &SimpleHttpClient,
    parent: &str,
    body: &GoogleCloudApigeeV1Instance,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://apigee.googleapis.com/v1/organizations/{}/instances",
        parent,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/organizations/{organizationsId}/instances
/// Creates an Apigee runtime instance. The instance is accessible from the authorized network configured on the organization. **Note:** Not supported for Apigee hybrid.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `apigee_organizations_instances_create_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from apigee_organizations_instances_create_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_instances_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/organizations/{organizationsId}/instances
/// Creates an Apigee runtime instance. The instance is accessible from the authorized network configured on the organization. **Note:** Not supported for Apigee hybrid.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `apigee_organizations_instances_create_builder()` + `apigee_organizations_instances_create_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_instances_create(
    client: &SimpleHttpClient,
    parent: &str,
    body: &GoogleCloudApigeeV1Instance,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = apigee_organizations_instances_create_builder(client, parent, body)?;
    apigee_organizations_instances_create_execute(builder)
}

/// GET v1/organizations/{organizationsId}/instances/{instancesId}
/// Deletes an Apigee runtime instance. The instance stops serving requests and the runtime data is deleted. **Note:** Not supported for Apigee hybrid.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `apigee_organizations_instances_delete_execute()` to send, or `apigee_organizations_instances_delete` for simplest API.

pub fn apigee_organizations_instances_delete_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://apigee.googleapis.com/v1/organizations/{}/instances/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/organizations/{organizationsId}/instances/{instancesId}
/// Deletes an Apigee runtime instance. The instance stops serving requests and the runtime data is deleted. **Note:** Not supported for Apigee hybrid.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `apigee_organizations_instances_delete_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from apigee_organizations_instances_delete_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_instances_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/organizations/{organizationsId}/instances/{instancesId}
/// Deletes an Apigee runtime instance. The instance stops serving requests and the runtime data is deleted. **Note:** Not supported for Apigee hybrid.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `apigee_organizations_instances_delete_builder()` + `apigee_organizations_instances_delete_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_instances_delete(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = apigee_organizations_instances_delete_builder(client, name)?;
    apigee_organizations_instances_delete_execute(builder)
}

/// GET v1/organizations/{organizationsId}/instances/{instancesId}
/// Gets the details for an Apigee runtime instance. **Note:** Not supported for Apigee hybrid.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `apigee_organizations_instances_get_execute()` to send, or `apigee_organizations_instances_get` for simplest API.

pub fn apigee_organizations_instances_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://apigee.googleapis.com/v1/organizations/{}/instances/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/organizations/{organizationsId}/instances/{instancesId}
/// Gets the details for an Apigee runtime instance. **Note:** Not supported for Apigee hybrid.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `apigee_organizations_instances_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from apigee_organizations_instances_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_instances_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudApigeeV1Instance>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudApigeeV1Instance = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/organizations/{organizationsId}/instances/{instancesId}
/// Gets the details for an Apigee runtime instance. **Note:** Not supported for Apigee hybrid.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `apigee_organizations_instances_get_builder()` + `apigee_organizations_instances_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_instances_get(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudApigeeV1Instance>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = apigee_organizations_instances_get_builder(client, name)?;
    apigee_organizations_instances_get_execute(builder)
}

/// GET v1/organizations/{organizationsId}/instances
/// Lists all Apigee runtime instances for the organization. **Note:** Not supported for Apigee hybrid.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `apigee_organizations_instances_list_execute()` to send, or `apigee_organizations_instances_list` for simplest API.

pub fn apigee_organizations_instances_list_builder(
    client: &SimpleHttpClient,
    parent: &str,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://apigee.googleapis.com/v1/organizations/{}/instances",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/organizations/{organizationsId}/instances
/// Lists all Apigee runtime instances for the organization. **Note:** Not supported for Apigee hybrid.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `apigee_organizations_instances_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from apigee_organizations_instances_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_instances_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudApigeeV1ListInstancesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudApigeeV1ListInstancesResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/organizations/{organizationsId}/instances
/// Lists all Apigee runtime instances for the organization. **Note:** Not supported for Apigee hybrid.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `apigee_organizations_instances_list_builder()` + `apigee_organizations_instances_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_instances_list(
    client: &SimpleHttpClient,
    parent: &str,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudApigeeV1ListInstancesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = apigee_organizations_instances_list_builder(client, parent, pageSize, pageToken)?;
    apigee_organizations_instances_list_execute(builder)
}

/// GET v1/organizations/{organizationsId}/instances/{instancesId}
/// Updates an Apigee runtime instance. You can update the fields described in NodeConfig. No other fields will be updated. **Note:** Not supported for Apigee hybrid.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `apigee_organizations_instances_patch_execute()` to send, or `apigee_organizations_instances_patch` for simplest API.

pub fn apigee_organizations_instances_patch_builder(
    client: &SimpleHttpClient,
    name: &str,
    updateMask: Option<&str>,
    body: &GoogleCloudApigeeV1Instance,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://apigee.googleapis.com/v1/organizations/{}/instances/{}",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = updateMask {
        query_parts.push(format!("updateMask={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/organizations/{organizationsId}/instances/{instancesId}
/// Updates an Apigee runtime instance. You can update the fields described in NodeConfig. No other fields will be updated. **Note:** Not supported for Apigee hybrid.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `apigee_organizations_instances_patch_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from apigee_organizations_instances_patch_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_instances_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/organizations/{organizationsId}/instances/{instancesId}
/// Updates an Apigee runtime instance. You can update the fields described in NodeConfig. No other fields will be updated. **Note:** Not supported for Apigee hybrid.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `apigee_organizations_instances_patch_builder()` + `apigee_organizations_instances_patch_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_instances_patch(
    client: &SimpleHttpClient,
    name: &str,
    updateMask: Option<&str>,
    body: &GoogleCloudApigeeV1Instance,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = apigee_organizations_instances_patch_builder(client, name, updateMask, body)?;
    apigee_organizations_instances_patch_execute(builder)
}

/// GET v1/organizations/{organizationsId}/instances/{instancesId}:reportStatus
/// Reports the latest status for a runtime instance.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `apigee_organizations_instances_report_status_execute()` to send, or `apigee_organizations_instances_report_status` for simplest API.

pub fn apigee_organizations_instances_report_status_builder(
    client: &SimpleHttpClient,
    instance: &str,
    body: &GoogleCloudApigeeV1ReportInstanceStatusRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://apigee.googleapis.com/v1/organizations/{}/instances/{}:reportStatus",
        instance,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/organizations/{organizationsId}/instances/{instancesId}:reportStatus
/// Reports the latest status for a runtime instance.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `apigee_organizations_instances_report_status_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from apigee_organizations_instances_report_status_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_instances_report_status_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudApigeeV1ReportInstanceStatusResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudApigeeV1ReportInstanceStatusResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/organizations/{organizationsId}/instances/{instancesId}:reportStatus
/// Reports the latest status for a runtime instance.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `apigee_organizations_instances_report_status_builder()` + `apigee_organizations_instances_report_status_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_instances_report_status(
    client: &SimpleHttpClient,
    instance: &str,
    body: &GoogleCloudApigeeV1ReportInstanceStatusRequest,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudApigeeV1ReportInstanceStatusResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = apigee_organizations_instances_report_status_builder(client, instance, body)?;
    apigee_organizations_instances_report_status_execute(builder)
}

/// GET v1/organizations/{organizationsId}/instances/{instancesId}/attachments
/// Creates a new attachment of an environment to an instance. **Note:** Not supported for Apigee hybrid.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `apigee_organizations_instances_attachments_create_execute()` to send, or `apigee_organizations_instances_attachments_create` for simplest API.

pub fn apigee_organizations_instances_attachments_create_builder(
    client: &SimpleHttpClient,
    parent: &str,
    body: &GoogleCloudApigeeV1InstanceAttachment,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://apigee.googleapis.com/v1/organizations/{}/instances/{}/attachments",
        parent,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/organizations/{organizationsId}/instances/{instancesId}/attachments
/// Creates a new attachment of an environment to an instance. **Note:** Not supported for Apigee hybrid.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `apigee_organizations_instances_attachments_create_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from apigee_organizations_instances_attachments_create_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_instances_attachments_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/organizations/{organizationsId}/instances/{instancesId}/attachments
/// Creates a new attachment of an environment to an instance. **Note:** Not supported for Apigee hybrid.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `apigee_organizations_instances_attachments_create_builder()` + `apigee_organizations_instances_attachments_create_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_instances_attachments_create(
    client: &SimpleHttpClient,
    parent: &str,
    body: &GoogleCloudApigeeV1InstanceAttachment,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = apigee_organizations_instances_attachments_create_builder(client, parent, body)?;
    apigee_organizations_instances_attachments_create_execute(builder)
}

/// GET v1/organizations/{organizationsId}/instances/{instancesId}/attachments/{attachmentsId}
/// Deletes an attachment. **Note:** Not supported for Apigee hybrid.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `apigee_organizations_instances_attachments_delete_execute()` to send, or `apigee_organizations_instances_attachments_delete` for simplest API.

pub fn apigee_organizations_instances_attachments_delete_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://apigee.googleapis.com/v1/organizations/{}/instances/{}/attachments/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/organizations/{organizationsId}/instances/{instancesId}/attachments/{attachmentsId}
/// Deletes an attachment. **Note:** Not supported for Apigee hybrid.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `apigee_organizations_instances_attachments_delete_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from apigee_organizations_instances_attachments_delete_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_instances_attachments_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/organizations/{organizationsId}/instances/{instancesId}/attachments/{attachmentsId}
/// Deletes an attachment. **Note:** Not supported for Apigee hybrid.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `apigee_organizations_instances_attachments_delete_builder()` + `apigee_organizations_instances_attachments_delete_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_instances_attachments_delete(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = apigee_organizations_instances_attachments_delete_builder(client, name)?;
    apigee_organizations_instances_attachments_delete_execute(builder)
}

/// GET v1/organizations/{organizationsId}/instances/{instancesId}/attachments/{attachmentsId}
/// Gets an attachment. **Note:** Not supported for Apigee hybrid.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `apigee_organizations_instances_attachments_get_execute()` to send, or `apigee_organizations_instances_attachments_get` for simplest API.

pub fn apigee_organizations_instances_attachments_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://apigee.googleapis.com/v1/organizations/{}/instances/{}/attachments/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/organizations/{organizationsId}/instances/{instancesId}/attachments/{attachmentsId}
/// Gets an attachment. **Note:** Not supported for Apigee hybrid.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `apigee_organizations_instances_attachments_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from apigee_organizations_instances_attachments_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_instances_attachments_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudApigeeV1InstanceAttachment>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudApigeeV1InstanceAttachment = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/organizations/{organizationsId}/instances/{instancesId}/attachments/{attachmentsId}
/// Gets an attachment. **Note:** Not supported for Apigee hybrid.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `apigee_organizations_instances_attachments_get_builder()` + `apigee_organizations_instances_attachments_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_instances_attachments_get(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudApigeeV1InstanceAttachment>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = apigee_organizations_instances_attachments_get_builder(client, name)?;
    apigee_organizations_instances_attachments_get_execute(builder)
}

/// GET v1/organizations/{organizationsId}/instances/{instancesId}/attachments
/// Lists all attachments to an instance. **Note:** Not supported for Apigee hybrid.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `apigee_organizations_instances_attachments_list_execute()` to send, or `apigee_organizations_instances_attachments_list` for simplest API.

pub fn apigee_organizations_instances_attachments_list_builder(
    client: &SimpleHttpClient,
    parent: &str,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://apigee.googleapis.com/v1/organizations/{}/instances/{}/attachments",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/organizations/{organizationsId}/instances/{instancesId}/attachments
/// Lists all attachments to an instance. **Note:** Not supported for Apigee hybrid.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `apigee_organizations_instances_attachments_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from apigee_organizations_instances_attachments_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_instances_attachments_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudApigeeV1ListInstanceAttachmentsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudApigeeV1ListInstanceAttachmentsResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/organizations/{organizationsId}/instances/{instancesId}/attachments
/// Lists all attachments to an instance. **Note:** Not supported for Apigee hybrid.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `apigee_organizations_instances_attachments_list_builder()` + `apigee_organizations_instances_attachments_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_instances_attachments_list(
    client: &SimpleHttpClient,
    parent: &str,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudApigeeV1ListInstanceAttachmentsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = apigee_organizations_instances_attachments_list_builder(
        client, parent, pageSize, pageToken,
    )?;
    apigee_organizations_instances_attachments_list_execute(builder)
}

/// GET v1/organizations/{organizationsId}/instances/{instancesId}/canaryevaluations
/// Creates a new canary evaluation for an organization.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `apigee_organizations_instances_canaryevaluations_create_execute()` to send, or `apigee_organizations_instances_canaryevaluations_create` for simplest API.

pub fn apigee_organizations_instances_canaryevaluations_create_builder(
    client: &SimpleHttpClient,
    parent: &str,
    body: &GoogleCloudApigeeV1CanaryEvaluation,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://apigee.googleapis.com/v1/organizations/{}/instances/{}/canaryevaluations",
        parent,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/organizations/{organizationsId}/instances/{instancesId}/canaryevaluations
/// Creates a new canary evaluation for an organization.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `apigee_organizations_instances_canaryevaluations_create_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from apigee_organizations_instances_canaryevaluations_create_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_instances_canaryevaluations_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/organizations/{organizationsId}/instances/{instancesId}/canaryevaluations
/// Creates a new canary evaluation for an organization.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `apigee_organizations_instances_canaryevaluations_create_builder()` + `apigee_organizations_instances_canaryevaluations_create_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_instances_canaryevaluations_create(
    client: &SimpleHttpClient,
    parent: &str,
    body: &GoogleCloudApigeeV1CanaryEvaluation,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        apigee_organizations_instances_canaryevaluations_create_builder(client, parent, body)?;
    apigee_organizations_instances_canaryevaluations_create_execute(builder)
}

/// GET v1/organizations/{organizationsId}/instances/{instancesId}/canaryevaluations/{canaryevaluationsId}
/// Gets a CanaryEvaluation for an organization.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `apigee_organizations_instances_canaryevaluations_get_execute()` to send, or `apigee_organizations_instances_canaryevaluations_get` for simplest API.

pub fn apigee_organizations_instances_canaryevaluations_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://apigee.googleapis.com/v1/organizations/{}/instances/{}/canaryevaluations/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/organizations/{organizationsId}/instances/{instancesId}/canaryevaluations/{canaryevaluationsId}
/// Gets a CanaryEvaluation for an organization.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `apigee_organizations_instances_canaryevaluations_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from apigee_organizations_instances_canaryevaluations_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_instances_canaryevaluations_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudApigeeV1CanaryEvaluation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudApigeeV1CanaryEvaluation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/organizations/{organizationsId}/instances/{instancesId}/canaryevaluations/{canaryevaluationsId}
/// Gets a CanaryEvaluation for an organization.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `apigee_organizations_instances_canaryevaluations_get_builder()` + `apigee_organizations_instances_canaryevaluations_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_instances_canaryevaluations_get(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudApigeeV1CanaryEvaluation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = apigee_organizations_instances_canaryevaluations_get_builder(client, name)?;
    apigee_organizations_instances_canaryevaluations_get_execute(builder)
}

/// GET v1/organizations/{organizationsId}/instances/{instancesId}/natAddresses/{natAddressesId}:activate
/// Activates the NAT address. The Apigee instance can now use this for Internet egress traffic. **Note:** Not supported for Apigee hybrid.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `apigee_organizations_instances_nat_addresses_activate_execute()` to send, or `apigee_organizations_instances_nat_addresses_activate` for simplest API.

pub fn apigee_organizations_instances_nat_addresses_activate_builder(
    client: &SimpleHttpClient,
    name: &str,
    body: &GoogleCloudApigeeV1ActivateNatAddressRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://apigee.googleapis.com/v1/organizations/{}/instances/{}/natAddresses/{}:activate",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/organizations/{organizationsId}/instances/{instancesId}/natAddresses/{natAddressesId}:activate
/// Activates the NAT address. The Apigee instance can now use this for Internet egress traffic. **Note:** Not supported for Apigee hybrid.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `apigee_organizations_instances_nat_addresses_activate_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from apigee_organizations_instances_nat_addresses_activate_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_instances_nat_addresses_activate_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/organizations/{organizationsId}/instances/{instancesId}/natAddresses/{natAddressesId}:activate
/// Activates the NAT address. The Apigee instance can now use this for Internet egress traffic. **Note:** Not supported for Apigee hybrid.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `apigee_organizations_instances_nat_addresses_activate_builder()` + `apigee_organizations_instances_nat_addresses_activate_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_instances_nat_addresses_activate(
    client: &SimpleHttpClient,
    name: &str,
    body: &GoogleCloudApigeeV1ActivateNatAddressRequest,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        apigee_organizations_instances_nat_addresses_activate_builder(client, name, body)?;
    apigee_organizations_instances_nat_addresses_activate_execute(builder)
}

/// GET v1/organizations/{organizationsId}/instances/{instancesId}/natAddresses
/// Creates a NAT address. The address is created in the RESERVED state and a static external IP address will be provisioned. At this time, the instance will not use this IP address for Internet egress traffic. The address can be activated for use once any required firewall IP whitelisting has been completed. **Note:** Not supported for Apigee hybrid.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `apigee_organizations_instances_nat_addresses_create_execute()` to send, or `apigee_organizations_instances_nat_addresses_create` for simplest API.

pub fn apigee_organizations_instances_nat_addresses_create_builder(
    client: &SimpleHttpClient,
    parent: &str,
    body: &GoogleCloudApigeeV1NatAddress,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://apigee.googleapis.com/v1/organizations/{}/instances/{}/natAddresses",
        parent,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/organizations/{organizationsId}/instances/{instancesId}/natAddresses
/// Creates a NAT address. The address is created in the RESERVED state and a static external IP address will be provisioned. At this time, the instance will not use this IP address for Internet egress traffic. The address can be activated for use once any required firewall IP whitelisting has been completed. **Note:** Not supported for Apigee hybrid.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `apigee_organizations_instances_nat_addresses_create_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from apigee_organizations_instances_nat_addresses_create_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_instances_nat_addresses_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/organizations/{organizationsId}/instances/{instancesId}/natAddresses
/// Creates a NAT address. The address is created in the RESERVED state and a static external IP address will be provisioned. At this time, the instance will not use this IP address for Internet egress traffic. The address can be activated for use once any required firewall IP whitelisting has been completed. **Note:** Not supported for Apigee hybrid.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `apigee_organizations_instances_nat_addresses_create_builder()` + `apigee_organizations_instances_nat_addresses_create_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_instances_nat_addresses_create(
    client: &SimpleHttpClient,
    parent: &str,
    body: &GoogleCloudApigeeV1NatAddress,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        apigee_organizations_instances_nat_addresses_create_builder(client, parent, body)?;
    apigee_organizations_instances_nat_addresses_create_execute(builder)
}

/// GET v1/organizations/{organizationsId}/instances/{instancesId}/natAddresses/{natAddressesId}
/// Deletes the NAT address. Connections that are actively using the address are drained before it is removed. **Note:** Not supported for Apigee hybrid.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `apigee_organizations_instances_nat_addresses_delete_execute()` to send, or `apigee_organizations_instances_nat_addresses_delete` for simplest API.

pub fn apigee_organizations_instances_nat_addresses_delete_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://apigee.googleapis.com/v1/organizations/{}/instances/{}/natAddresses/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/organizations/{organizationsId}/instances/{instancesId}/natAddresses/{natAddressesId}
/// Deletes the NAT address. Connections that are actively using the address are drained before it is removed. **Note:** Not supported for Apigee hybrid.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `apigee_organizations_instances_nat_addresses_delete_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from apigee_organizations_instances_nat_addresses_delete_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_instances_nat_addresses_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/organizations/{organizationsId}/instances/{instancesId}/natAddresses/{natAddressesId}
/// Deletes the NAT address. Connections that are actively using the address are drained before it is removed. **Note:** Not supported for Apigee hybrid.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `apigee_organizations_instances_nat_addresses_delete_builder()` + `apigee_organizations_instances_nat_addresses_delete_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_instances_nat_addresses_delete(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = apigee_organizations_instances_nat_addresses_delete_builder(client, name)?;
    apigee_organizations_instances_nat_addresses_delete_execute(builder)
}

/// GET v1/organizations/{organizationsId}/instances/{instancesId}/natAddresses/{natAddressesId}
/// Gets the details of a NAT address. **Note:** Not supported for Apigee hybrid.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `apigee_organizations_instances_nat_addresses_get_execute()` to send, or `apigee_organizations_instances_nat_addresses_get` for simplest API.

pub fn apigee_organizations_instances_nat_addresses_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://apigee.googleapis.com/v1/organizations/{}/instances/{}/natAddresses/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/organizations/{organizationsId}/instances/{instancesId}/natAddresses/{natAddressesId}
/// Gets the details of a NAT address. **Note:** Not supported for Apigee hybrid.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `apigee_organizations_instances_nat_addresses_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from apigee_organizations_instances_nat_addresses_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_instances_nat_addresses_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudApigeeV1NatAddress>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudApigeeV1NatAddress = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/organizations/{organizationsId}/instances/{instancesId}/natAddresses/{natAddressesId}
/// Gets the details of a NAT address. **Note:** Not supported for Apigee hybrid.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `apigee_organizations_instances_nat_addresses_get_builder()` + `apigee_organizations_instances_nat_addresses_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_instances_nat_addresses_get(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudApigeeV1NatAddress>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = apigee_organizations_instances_nat_addresses_get_builder(client, name)?;
    apigee_organizations_instances_nat_addresses_get_execute(builder)
}

/// GET v1/organizations/{organizationsId}/instances/{instancesId}/natAddresses
/// Lists the NAT addresses for an Apigee instance. **Note:** Not supported for Apigee hybrid.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `apigee_organizations_instances_nat_addresses_list_execute()` to send, or `apigee_organizations_instances_nat_addresses_list` for simplest API.

pub fn apigee_organizations_instances_nat_addresses_list_builder(
    client: &SimpleHttpClient,
    parent: &str,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://apigee.googleapis.com/v1/organizations/{}/instances/{}/natAddresses",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/organizations/{organizationsId}/instances/{instancesId}/natAddresses
/// Lists the NAT addresses for an Apigee instance. **Note:** Not supported for Apigee hybrid.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `apigee_organizations_instances_nat_addresses_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from apigee_organizations_instances_nat_addresses_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_instances_nat_addresses_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudApigeeV1ListNatAddressesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudApigeeV1ListNatAddressesResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/organizations/{organizationsId}/instances/{instancesId}/natAddresses
/// Lists the NAT addresses for an Apigee instance. **Note:** Not supported for Apigee hybrid.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `apigee_organizations_instances_nat_addresses_list_builder()` + `apigee_organizations_instances_nat_addresses_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_instances_nat_addresses_list(
    client: &SimpleHttpClient,
    parent: &str,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudApigeeV1ListNatAddressesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = apigee_organizations_instances_nat_addresses_list_builder(
        client, parent, pageSize, pageToken,
    )?;
    apigee_organizations_instances_nat_addresses_list_execute(builder)
}

/// GET v1/organizations/{organizationsId}/keyvaluemaps
/// Creates a key value map in an organization.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `apigee_organizations_keyvaluemaps_create_execute()` to send, or `apigee_organizations_keyvaluemaps_create` for simplest API.

pub fn apigee_organizations_keyvaluemaps_create_builder(
    client: &SimpleHttpClient,
    parent: &str,
    body: &GoogleCloudApigeeV1KeyValueMap,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://apigee.googleapis.com/v1/organizations/{}/keyvaluemaps",
        parent,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/organizations/{organizationsId}/keyvaluemaps
/// Creates a key value map in an organization.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `apigee_organizations_keyvaluemaps_create_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from apigee_organizations_keyvaluemaps_create_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_keyvaluemaps_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudApigeeV1KeyValueMap>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudApigeeV1KeyValueMap = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/organizations/{organizationsId}/keyvaluemaps
/// Creates a key value map in an organization.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `apigee_organizations_keyvaluemaps_create_builder()` + `apigee_organizations_keyvaluemaps_create_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_keyvaluemaps_create(
    client: &SimpleHttpClient,
    parent: &str,
    body: &GoogleCloudApigeeV1KeyValueMap,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudApigeeV1KeyValueMap>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = apigee_organizations_keyvaluemaps_create_builder(client, parent, body)?;
    apigee_organizations_keyvaluemaps_create_execute(builder)
}

/// GET v1/organizations/{organizationsId}/keyvaluemaps/{keyvaluemapsId}
/// Deletes a key value map from an organization.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `apigee_organizations_keyvaluemaps_delete_execute()` to send, or `apigee_organizations_keyvaluemaps_delete` for simplest API.

pub fn apigee_organizations_keyvaluemaps_delete_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://apigee.googleapis.com/v1/organizations/{}/keyvaluemaps/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/organizations/{organizationsId}/keyvaluemaps/{keyvaluemapsId}
/// Deletes a key value map from an organization.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `apigee_organizations_keyvaluemaps_delete_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from apigee_organizations_keyvaluemaps_delete_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_keyvaluemaps_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudApigeeV1KeyValueMap>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudApigeeV1KeyValueMap = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/organizations/{organizationsId}/keyvaluemaps/{keyvaluemapsId}
/// Deletes a key value map from an organization.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `apigee_organizations_keyvaluemaps_delete_builder()` + `apigee_organizations_keyvaluemaps_delete_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_keyvaluemaps_delete(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudApigeeV1KeyValueMap>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = apigee_organizations_keyvaluemaps_delete_builder(client, name)?;
    apigee_organizations_keyvaluemaps_delete_execute(builder)
}

/// GET v1/organizations/{organizationsId}/keyvaluemaps/{keyvaluemapsId}
/// Get the key value map scoped to an organization, environment, or API proxy.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `apigee_organizations_keyvaluemaps_get_execute()` to send, or `apigee_organizations_keyvaluemaps_get` for simplest API.

pub fn apigee_organizations_keyvaluemaps_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://apigee.googleapis.com/v1/organizations/{}/keyvaluemaps/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/organizations/{organizationsId}/keyvaluemaps/{keyvaluemapsId}
/// Get the key value map scoped to an organization, environment, or API proxy.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `apigee_organizations_keyvaluemaps_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from apigee_organizations_keyvaluemaps_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_keyvaluemaps_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudApigeeV1KeyValueMap>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudApigeeV1KeyValueMap = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/organizations/{organizationsId}/keyvaluemaps/{keyvaluemapsId}
/// Get the key value map scoped to an organization, environment, or API proxy.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `apigee_organizations_keyvaluemaps_get_builder()` + `apigee_organizations_keyvaluemaps_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_keyvaluemaps_get(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudApigeeV1KeyValueMap>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = apigee_organizations_keyvaluemaps_get_builder(client, name)?;
    apigee_organizations_keyvaluemaps_get_execute(builder)
}

/// GET v1/organizations/{organizationsId}/keyvaluemaps/{keyvaluemapsId}
/// Update the key value map scoped to an organization, environment, or API proxy.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `apigee_organizations_keyvaluemaps_update_execute()` to send, or `apigee_organizations_keyvaluemaps_update` for simplest API.

pub fn apigee_organizations_keyvaluemaps_update_builder(
    client: &SimpleHttpClient,
    name: &str,
    body: &GoogleCloudApigeeV1KeyValueMap,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://apigee.googleapis.com/v1/organizations/{}/keyvaluemaps/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/organizations/{organizationsId}/keyvaluemaps/{keyvaluemapsId}
/// Update the key value map scoped to an organization, environment, or API proxy.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `apigee_organizations_keyvaluemaps_update_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from apigee_organizations_keyvaluemaps_update_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_keyvaluemaps_update_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudApigeeV1KeyValueMap>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudApigeeV1KeyValueMap = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/organizations/{organizationsId}/keyvaluemaps/{keyvaluemapsId}
/// Update the key value map scoped to an organization, environment, or API proxy.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `apigee_organizations_keyvaluemaps_update_builder()` + `apigee_organizations_keyvaluemaps_update_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_keyvaluemaps_update(
    client: &SimpleHttpClient,
    name: &str,
    body: &GoogleCloudApigeeV1KeyValueMap,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudApigeeV1KeyValueMap>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = apigee_organizations_keyvaluemaps_update_builder(client, name, body)?;
    apigee_organizations_keyvaluemaps_update_execute(builder)
}

/// GET v1/organizations/{organizationsId}/keyvaluemaps/{keyvaluemapsId}/entries
/// Creates key value entries in a key value map scoped to an organization, environment, or API proxy. **Note**: Supported for Apigee hybrid 1.8.x and higher.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `apigee_organizations_keyvaluemaps_entries_create_execute()` to send, or `apigee_organizations_keyvaluemaps_entries_create` for simplest API.

pub fn apigee_organizations_keyvaluemaps_entries_create_builder(
    client: &SimpleHttpClient,
    parent: &str,
    body: &GoogleCloudApigeeV1KeyValueEntry,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://apigee.googleapis.com/v1/organizations/{}/keyvaluemaps/{}/entries",
        parent,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/organizations/{organizationsId}/keyvaluemaps/{keyvaluemapsId}/entries
/// Creates key value entries in a key value map scoped to an organization, environment, or API proxy. **Note**: Supported for Apigee hybrid 1.8.x and higher.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `apigee_organizations_keyvaluemaps_entries_create_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from apigee_organizations_keyvaluemaps_entries_create_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_keyvaluemaps_entries_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudApigeeV1KeyValueEntry>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudApigeeV1KeyValueEntry = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/organizations/{organizationsId}/keyvaluemaps/{keyvaluemapsId}/entries
/// Creates key value entries in a key value map scoped to an organization, environment, or API proxy. **Note**: Supported for Apigee hybrid 1.8.x and higher.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `apigee_organizations_keyvaluemaps_entries_create_builder()` + `apigee_organizations_keyvaluemaps_entries_create_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_keyvaluemaps_entries_create(
    client: &SimpleHttpClient,
    parent: &str,
    body: &GoogleCloudApigeeV1KeyValueEntry,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudApigeeV1KeyValueEntry>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = apigee_organizations_keyvaluemaps_entries_create_builder(client, parent, body)?;
    apigee_organizations_keyvaluemaps_entries_create_execute(builder)
}

/// GET v1/organizations/{organizationsId}/keyvaluemaps/{keyvaluemapsId}/entries/{entriesId}
/// Deletes a key value entry from a key value map scoped to an organization, environment, or API proxy. **Notes:** * After you delete the key value entry, the policy consuming the entry will continue to function with its cached values for a few minutes. This is expected behavior. * Supported for Apigee hybrid 1.8.x and higher.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `apigee_organizations_keyvaluemaps_entries_delete_execute()` to send, or `apigee_organizations_keyvaluemaps_entries_delete` for simplest API.

pub fn apigee_organizations_keyvaluemaps_entries_delete_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://apigee.googleapis.com/v1/organizations/{}/keyvaluemaps/{}/entries/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/organizations/{organizationsId}/keyvaluemaps/{keyvaluemapsId}/entries/{entriesId}
/// Deletes a key value entry from a key value map scoped to an organization, environment, or API proxy. **Notes:** * After you delete the key value entry, the policy consuming the entry will continue to function with its cached values for a few minutes. This is expected behavior. * Supported for Apigee hybrid 1.8.x and higher.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `apigee_organizations_keyvaluemaps_entries_delete_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from apigee_organizations_keyvaluemaps_entries_delete_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_keyvaluemaps_entries_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudApigeeV1KeyValueEntry>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudApigeeV1KeyValueEntry = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/organizations/{organizationsId}/keyvaluemaps/{keyvaluemapsId}/entries/{entriesId}
/// Deletes a key value entry from a key value map scoped to an organization, environment, or API proxy. **Notes:** * After you delete the key value entry, the policy consuming the entry will continue to function with its cached values for a few minutes. This is expected behavior. * Supported for Apigee hybrid 1.8.x and higher.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `apigee_organizations_keyvaluemaps_entries_delete_builder()` + `apigee_organizations_keyvaluemaps_entries_delete_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_keyvaluemaps_entries_delete(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudApigeeV1KeyValueEntry>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = apigee_organizations_keyvaluemaps_entries_delete_builder(client, name)?;
    apigee_organizations_keyvaluemaps_entries_delete_execute(builder)
}

/// GET v1/organizations/{organizationsId}/keyvaluemaps/{keyvaluemapsId}/entries/{entriesId}
/// Get the key value entry value for a key value map scoped to an organization, environment, or API proxy. **Note**: Supported for Apigee hybrid 1.8.x and higher.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `apigee_organizations_keyvaluemaps_entries_get_execute()` to send, or `apigee_organizations_keyvaluemaps_entries_get` for simplest API.

pub fn apigee_organizations_keyvaluemaps_entries_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://apigee.googleapis.com/v1/organizations/{}/keyvaluemaps/{}/entries/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/organizations/{organizationsId}/keyvaluemaps/{keyvaluemapsId}/entries/{entriesId}
/// Get the key value entry value for a key value map scoped to an organization, environment, or API proxy. **Note**: Supported for Apigee hybrid 1.8.x and higher.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `apigee_organizations_keyvaluemaps_entries_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from apigee_organizations_keyvaluemaps_entries_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_keyvaluemaps_entries_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudApigeeV1KeyValueEntry>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudApigeeV1KeyValueEntry = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/organizations/{organizationsId}/keyvaluemaps/{keyvaluemapsId}/entries/{entriesId}
/// Get the key value entry value for a key value map scoped to an organization, environment, or API proxy. **Note**: Supported for Apigee hybrid 1.8.x and higher.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `apigee_organizations_keyvaluemaps_entries_get_builder()` + `apigee_organizations_keyvaluemaps_entries_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_keyvaluemaps_entries_get(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudApigeeV1KeyValueEntry>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = apigee_organizations_keyvaluemaps_entries_get_builder(client, name)?;
    apigee_organizations_keyvaluemaps_entries_get_execute(builder)
}

/// GET v1/organizations/{organizationsId}/keyvaluemaps/{keyvaluemapsId}/entries
/// Lists key value entries for key values maps scoped to an organization, environment, or API proxy. **Note**: Supported for Apigee hybrid 1.8.x and higher.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `apigee_organizations_keyvaluemaps_entries_list_execute()` to send, or `apigee_organizations_keyvaluemaps_entries_list` for simplest API.

pub fn apigee_organizations_keyvaluemaps_entries_list_builder(
    client: &SimpleHttpClient,
    parent: &str,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://apigee.googleapis.com/v1/organizations/{}/keyvaluemaps/{}/entries",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/organizations/{organizationsId}/keyvaluemaps/{keyvaluemapsId}/entries
/// Lists key value entries for key values maps scoped to an organization, environment, or API proxy. **Note**: Supported for Apigee hybrid 1.8.x and higher.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `apigee_organizations_keyvaluemaps_entries_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from apigee_organizations_keyvaluemaps_entries_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_keyvaluemaps_entries_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudApigeeV1ListKeyValueEntriesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudApigeeV1ListKeyValueEntriesResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/organizations/{organizationsId}/keyvaluemaps/{keyvaluemapsId}/entries
/// Lists key value entries for key values maps scoped to an organization, environment, or API proxy. **Note**: Supported for Apigee hybrid 1.8.x and higher.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `apigee_organizations_keyvaluemaps_entries_list_builder()` + `apigee_organizations_keyvaluemaps_entries_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_keyvaluemaps_entries_list(
    client: &SimpleHttpClient,
    parent: &str,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudApigeeV1ListKeyValueEntriesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = apigee_organizations_keyvaluemaps_entries_list_builder(
        client, parent, pageSize, pageToken,
    )?;
    apigee_organizations_keyvaluemaps_entries_list_execute(builder)
}

/// GET v1/organizations/{organizationsId}/keyvaluemaps/{keyvaluemapsId}/entries/{entriesId}
/// Update key value entry scoped to an organization, environment, or API proxy for an existing key.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `apigee_organizations_keyvaluemaps_entries_update_execute()` to send, or `apigee_organizations_keyvaluemaps_entries_update` for simplest API.

pub fn apigee_organizations_keyvaluemaps_entries_update_builder(
    client: &SimpleHttpClient,
    name: &str,
    body: &GoogleCloudApigeeV1KeyValueEntry,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://apigee.googleapis.com/v1/organizations/{}/keyvaluemaps/{}/entries/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/organizations/{organizationsId}/keyvaluemaps/{keyvaluemapsId}/entries/{entriesId}
/// Update key value entry scoped to an organization, environment, or API proxy for an existing key.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `apigee_organizations_keyvaluemaps_entries_update_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from apigee_organizations_keyvaluemaps_entries_update_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_keyvaluemaps_entries_update_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudApigeeV1KeyValueEntry>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudApigeeV1KeyValueEntry = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/organizations/{organizationsId}/keyvaluemaps/{keyvaluemapsId}/entries/{entriesId}
/// Update key value entry scoped to an organization, environment, or API proxy for an existing key.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `apigee_organizations_keyvaluemaps_entries_update_builder()` + `apigee_organizations_keyvaluemaps_entries_update_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_keyvaluemaps_entries_update(
    client: &SimpleHttpClient,
    name: &str,
    body: &GoogleCloudApigeeV1KeyValueEntry,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudApigeeV1KeyValueEntry>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = apigee_organizations_keyvaluemaps_entries_update_builder(client, name, body)?;
    apigee_organizations_keyvaluemaps_entries_update_execute(builder)
}

/// GET v1/organizations/{organizationsId}/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `apigee_organizations_operations_get_execute()` to send, or `apigee_organizations_operations_get` for simplest API.

pub fn apigee_organizations_operations_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://apigee.googleapis.com/v1/organizations/{}/operations/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/organizations/{organizationsId}/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `apigee_organizations_operations_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from apigee_organizations_operations_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_operations_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/organizations/{organizationsId}/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `apigee_organizations_operations_get_builder()` + `apigee_organizations_operations_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_operations_get(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = apigee_organizations_operations_get_builder(client, name)?;
    apigee_organizations_operations_get_execute(builder)
}

/// GET v1/organizations/{organizationsId}/operations
/// Lists operations that match the specified filter in the request. If the server doesn't support this method, it returns UNIMPLEMENTED.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `apigee_organizations_operations_list_execute()` to send, or `apigee_organizations_operations_list` for simplest API.

pub fn apigee_organizations_operations_list_builder(
    client: &SimpleHttpClient,
    name: &str,
    filter: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
    returnPartialSuccess: Option<bool>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://apigee.googleapis.com/v1/organizations/{}/operations",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = returnPartialSuccess {
        query_parts.push(format!("returnPartialSuccess={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/organizations/{organizationsId}/operations
/// Lists operations that match the specified filter in the request. If the server doesn't support this method, it returns UNIMPLEMENTED.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `apigee_organizations_operations_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from apigee_organizations_operations_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_operations_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningListOperationsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningListOperationsResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/organizations/{organizationsId}/operations
/// Lists operations that match the specified filter in the request. If the server doesn't support this method, it returns UNIMPLEMENTED.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `apigee_organizations_operations_list_builder()` + `apigee_organizations_operations_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_operations_list(
    client: &SimpleHttpClient,
    name: &str,
    filter: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
    returnPartialSuccess: Option<bool>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningListOperationsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = apigee_organizations_operations_list_builder(
        client,
        name,
        filter,
        pageSize,
        pageToken,
        returnPartialSuccess,
    )?;
    apigee_organizations_operations_list_execute(builder)
}

/// GET v1/organizations/{organizationsId}/optimizedHostStats/{optimizedHostStatsId}
/// Similar to GetHostStats except that the response is less verbose.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `apigee_organizations_optimized_host_stats_get_execute()` to send, or `apigee_organizations_optimized_host_stats_get` for simplest API.

pub fn apigee_organizations_optimized_host_stats_get_builder(
    client: &SimpleHttpClient,
    name: &str,
    accuracy: Option<&str>,
    envgroupHostname: Option<&str>,
    filter: Option<&str>,
    limit: Option<&str>,
    offset: Option<&str>,
    realtime: Option<bool>,
    select: Option<&str>,
    sort: Option<&str>,
    sortby: Option<&str>,
    timeRange: Option<&str>,
    timeUnit: Option<&str>,
    topk: Option<&str>,
    tsAscending: Option<bool>,
    tzo: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://apigee.googleapis.com/v1/organizations/{}/optimizedHostStats/{}",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = accuracy {
        query_parts.push(format!("accuracy={}", val));
    }
    if let Some(val) = envgroupHostname {
        query_parts.push(format!("envgroupHostname={}", val));
    }
    if let Some(val) = filter {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = limit {
        query_parts.push(format!("limit={}", val));
    }
    if let Some(val) = offset {
        query_parts.push(format!("offset={}", val));
    }
    if let Some(val) = realtime {
        query_parts.push(format!("realtime={}", val));
    }
    if let Some(val) = select {
        query_parts.push(format!("select={}", val));
    }
    if let Some(val) = sort {
        query_parts.push(format!("sort={}", val));
    }
    if let Some(val) = sortby {
        query_parts.push(format!("sortby={}", val));
    }
    if let Some(val) = timeRange {
        query_parts.push(format!("timeRange={}", val));
    }
    if let Some(val) = timeUnit {
        query_parts.push(format!("timeUnit={}", val));
    }
    if let Some(val) = topk {
        query_parts.push(format!("topk={}", val));
    }
    if let Some(val) = tsAscending {
        query_parts.push(format!("tsAscending={}", val));
    }
    if let Some(val) = tzo {
        query_parts.push(format!("tzo={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/organizations/{organizationsId}/optimizedHostStats/{optimizedHostStatsId}
/// Similar to GetHostStats except that the response is less verbose.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `apigee_organizations_optimized_host_stats_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from apigee_organizations_optimized_host_stats_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_optimized_host_stats_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudApigeeV1OptimizedStats>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudApigeeV1OptimizedStats = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/organizations/{organizationsId}/optimizedHostStats/{optimizedHostStatsId}
/// Similar to GetHostStats except that the response is less verbose.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `apigee_organizations_optimized_host_stats_get_builder()` + `apigee_organizations_optimized_host_stats_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_optimized_host_stats_get(
    client: &SimpleHttpClient,
    name: &str,
    accuracy: Option<&str>,
    envgroupHostname: Option<&str>,
    filter: Option<&str>,
    limit: Option<&str>,
    offset: Option<&str>,
    realtime: Option<bool>,
    select: Option<&str>,
    sort: Option<&str>,
    sortby: Option<&str>,
    timeRange: Option<&str>,
    timeUnit: Option<&str>,
    topk: Option<&str>,
    tsAscending: Option<bool>,
    tzo: Option<&str>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudApigeeV1OptimizedStats>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = apigee_organizations_optimized_host_stats_get_builder(
        client,
        name,
        accuracy,
        envgroupHostname,
        filter,
        limit,
        offset,
        realtime,
        select,
        sort,
        sortby,
        timeRange,
        timeUnit,
        topk,
        tsAscending,
        tzo,
    )?;
    apigee_organizations_optimized_host_stats_get_execute(builder)
}

/// GET v1/organizations/{organizationsId}/reports
/// Creates a Custom Report for an Organization. A Custom Report provides Apigee Customers to create custom dashboards in addition to the standard dashboards which are provided. The Custom Report in its simplest form contains specifications about metrics, dimensions and filters. It is important to note that the custom report by itself does not provide an executable entity. The Edge UI converts the custom report definition into an analytics query and displays the result in a chart.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `apigee_organizations_reports_create_execute()` to send, or `apigee_organizations_reports_create` for simplest API.

pub fn apigee_organizations_reports_create_builder(
    client: &SimpleHttpClient,
    parent: &str,
    body: &GoogleCloudApigeeV1CustomReport,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://apigee.googleapis.com/v1/organizations/{}/reports",
        parent,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/organizations/{organizationsId}/reports
/// Creates a Custom Report for an Organization. A Custom Report provides Apigee Customers to create custom dashboards in addition to the standard dashboards which are provided. The Custom Report in its simplest form contains specifications about metrics, dimensions and filters. It is important to note that the custom report by itself does not provide an executable entity. The Edge UI converts the custom report definition into an analytics query and displays the result in a chart.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `apigee_organizations_reports_create_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from apigee_organizations_reports_create_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_reports_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudApigeeV1CustomReport>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudApigeeV1CustomReport = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/organizations/{organizationsId}/reports
/// Creates a Custom Report for an Organization. A Custom Report provides Apigee Customers to create custom dashboards in addition to the standard dashboards which are provided. The Custom Report in its simplest form contains specifications about metrics, dimensions and filters. It is important to note that the custom report by itself does not provide an executable entity. The Edge UI converts the custom report definition into an analytics query and displays the result in a chart.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `apigee_organizations_reports_create_builder()` + `apigee_organizations_reports_create_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_reports_create(
    client: &SimpleHttpClient,
    parent: &str,
    body: &GoogleCloudApigeeV1CustomReport,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudApigeeV1CustomReport>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = apigee_organizations_reports_create_builder(client, parent, body)?;
    apigee_organizations_reports_create_execute(builder)
}

/// GET v1/organizations/{organizationsId}/reports/{reportsId}
/// Deletes an existing custom report definition
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `apigee_organizations_reports_delete_execute()` to send, or `apigee_organizations_reports_delete` for simplest API.

pub fn apigee_organizations_reports_delete_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://apigee.googleapis.com/v1/organizations/{}/reports/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/organizations/{organizationsId}/reports/{reportsId}
/// Deletes an existing custom report definition
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `apigee_organizations_reports_delete_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from apigee_organizations_reports_delete_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_reports_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudApigeeV1DeleteCustomReportResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudApigeeV1DeleteCustomReportResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/organizations/{organizationsId}/reports/{reportsId}
/// Deletes an existing custom report definition
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `apigee_organizations_reports_delete_builder()` + `apigee_organizations_reports_delete_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_reports_delete(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudApigeeV1DeleteCustomReportResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = apigee_organizations_reports_delete_builder(client, name)?;
    apigee_organizations_reports_delete_execute(builder)
}

/// GET v1/organizations/{organizationsId}/reports/{reportsId}
/// Retrieve a custom report definition.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `apigee_organizations_reports_get_execute()` to send, or `apigee_organizations_reports_get` for simplest API.

pub fn apigee_organizations_reports_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://apigee.googleapis.com/v1/organizations/{}/reports/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/organizations/{organizationsId}/reports/{reportsId}
/// Retrieve a custom report definition.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `apigee_organizations_reports_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from apigee_organizations_reports_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_reports_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudApigeeV1CustomReport>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudApigeeV1CustomReport = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/organizations/{organizationsId}/reports/{reportsId}
/// Retrieve a custom report definition.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `apigee_organizations_reports_get_builder()` + `apigee_organizations_reports_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_reports_get(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudApigeeV1CustomReport>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = apigee_organizations_reports_get_builder(client, name)?;
    apigee_organizations_reports_get_execute(builder)
}

/// GET v1/organizations/{organizationsId}/reports
/// Return a list of Custom Reports
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `apigee_organizations_reports_list_execute()` to send, or `apigee_organizations_reports_list` for simplest API.

pub fn apigee_organizations_reports_list_builder(
    client: &SimpleHttpClient,
    parent: &str,
    expand: Option<bool>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://apigee.googleapis.com/v1/organizations/{}/reports",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = expand {
        query_parts.push(format!("expand={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/organizations/{organizationsId}/reports
/// Return a list of Custom Reports
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `apigee_organizations_reports_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from apigee_organizations_reports_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_reports_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudApigeeV1ListCustomReportsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudApigeeV1ListCustomReportsResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/organizations/{organizationsId}/reports
/// Return a list of Custom Reports
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `apigee_organizations_reports_list_builder()` + `apigee_organizations_reports_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_reports_list(
    client: &SimpleHttpClient,
    parent: &str,
    expand: Option<bool>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudApigeeV1ListCustomReportsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = apigee_organizations_reports_list_builder(client, parent, expand)?;
    apigee_organizations_reports_list_execute(builder)
}

/// GET v1/organizations/{organizationsId}/reports/{reportsId}
/// Update an existing custom report definition
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `apigee_organizations_reports_update_execute()` to send, or `apigee_organizations_reports_update` for simplest API.

pub fn apigee_organizations_reports_update_builder(
    client: &SimpleHttpClient,
    name: &str,
    body: &GoogleCloudApigeeV1CustomReport,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://apigee.googleapis.com/v1/organizations/{}/reports/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/organizations/{organizationsId}/reports/{reportsId}
/// Update an existing custom report definition
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `apigee_organizations_reports_update_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from apigee_organizations_reports_update_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_reports_update_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudApigeeV1CustomReport>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudApigeeV1CustomReport = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/organizations/{organizationsId}/reports/{reportsId}
/// Update an existing custom report definition
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `apigee_organizations_reports_update_builder()` + `apigee_organizations_reports_update_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_reports_update(
    client: &SimpleHttpClient,
    name: &str,
    body: &GoogleCloudApigeeV1CustomReport,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudApigeeV1CustomReport>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = apigee_organizations_reports_update_builder(client, name, body)?;
    apigee_organizations_reports_update_execute(builder)
}

/// GET v1/organizations/{organizationsId}/securityAssessmentResults:batchCompute
/// Compute RAV2 security scores for a set of resources.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `apigee_organizations_security_assessment_results_batch_compute_execute()` to send, or `apigee_organizations_security_assessment_results_batch_compute` for simplest API.

pub fn apigee_organizations_security_assessment_results_batch_compute_builder(
    client: &SimpleHttpClient,
    name: &str,
    body: &GoogleCloudApigeeV1BatchComputeSecurityAssessmentResultsRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://apigee.googleapis.com/v1/organizations/{}/securityAssessmentResults:batchCompute",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/organizations/{organizationsId}/securityAssessmentResults:batchCompute
/// Compute RAV2 security scores for a set of resources.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `apigee_organizations_security_assessment_results_batch_compute_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from apigee_organizations_security_assessment_results_batch_compute_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_security_assessment_results_batch_compute_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleCloudApigeeV1BatchComputeSecurityAssessmentResultsResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudApigeeV1BatchComputeSecurityAssessmentResultsResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/organizations/{organizationsId}/securityAssessmentResults:batchCompute
/// Compute RAV2 security scores for a set of resources.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `apigee_organizations_security_assessment_results_batch_compute_builder()` + `apigee_organizations_security_assessment_results_batch_compute_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_security_assessment_results_batch_compute(
    client: &SimpleHttpClient,
    name: &str,
    body: &GoogleCloudApigeeV1BatchComputeSecurityAssessmentResultsRequest,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleCloudApigeeV1BatchComputeSecurityAssessmentResultsResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        apigee_organizations_security_assessment_results_batch_compute_builder(client, name, body)?;
    apigee_organizations_security_assessment_results_batch_compute_execute(builder)
}

/// GET v1/organizations/{organizationsId}/securityFeedback
/// Creates a new report containing customer feedback.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `apigee_organizations_security_feedback_create_execute()` to send, or `apigee_organizations_security_feedback_create` for simplest API.

pub fn apigee_organizations_security_feedback_create_builder(
    client: &SimpleHttpClient,
    parent: &str,
    securityFeedbackId: Option<&str>,
    body: &GoogleCloudApigeeV1SecurityFeedback,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://apigee.googleapis.com/v1/organizations/{}/securityFeedback",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = securityFeedbackId {
        query_parts.push(format!("securityFeedbackId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/organizations/{organizationsId}/securityFeedback
/// Creates a new report containing customer feedback.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `apigee_organizations_security_feedback_create_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from apigee_organizations_security_feedback_create_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_security_feedback_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudApigeeV1SecurityFeedback>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudApigeeV1SecurityFeedback = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/organizations/{organizationsId}/securityFeedback
/// Creates a new report containing customer feedback.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `apigee_organizations_security_feedback_create_builder()` + `apigee_organizations_security_feedback_create_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_security_feedback_create(
    client: &SimpleHttpClient,
    parent: &str,
    securityFeedbackId: Option<&str>,
    body: &GoogleCloudApigeeV1SecurityFeedback,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudApigeeV1SecurityFeedback>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = apigee_organizations_security_feedback_create_builder(
        client,
        parent,
        securityFeedbackId,
        body,
    )?;
    apigee_organizations_security_feedback_create_execute(builder)
}

/// GET v1/organizations/{organizationsId}/securityFeedback/{securityFeedbackId}
/// Deletes a specific feedback report. Used for "undo" of a feedback submission.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `apigee_organizations_security_feedback_delete_execute()` to send, or `apigee_organizations_security_feedback_delete` for simplest API.

pub fn apigee_organizations_security_feedback_delete_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://apigee.googleapis.com/v1/organizations/{}/securityFeedback/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/organizations/{organizationsId}/securityFeedback/{securityFeedbackId}
/// Deletes a specific feedback report. Used for "undo" of a feedback submission.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `apigee_organizations_security_feedback_delete_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from apigee_organizations_security_feedback_delete_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_security_feedback_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleProtobufEmpty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/organizations/{organizationsId}/securityFeedback/{securityFeedbackId}
/// Deletes a specific feedback report. Used for "undo" of a feedback submission.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `apigee_organizations_security_feedback_delete_builder()` + `apigee_organizations_security_feedback_delete_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_security_feedback_delete(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = apigee_organizations_security_feedback_delete_builder(client, name)?;
    apigee_organizations_security_feedback_delete_execute(builder)
}

/// GET v1/organizations/{organizationsId}/securityFeedback/{securityFeedbackId}
/// Gets a specific customer feedback report.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `apigee_organizations_security_feedback_get_execute()` to send, or `apigee_organizations_security_feedback_get` for simplest API.

pub fn apigee_organizations_security_feedback_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://apigee.googleapis.com/v1/organizations/{}/securityFeedback/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/organizations/{organizationsId}/securityFeedback/{securityFeedbackId}
/// Gets a specific customer feedback report.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `apigee_organizations_security_feedback_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from apigee_organizations_security_feedback_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_security_feedback_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudApigeeV1SecurityFeedback>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudApigeeV1SecurityFeedback = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/organizations/{organizationsId}/securityFeedback/{securityFeedbackId}
/// Gets a specific customer feedback report.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `apigee_organizations_security_feedback_get_builder()` + `apigee_organizations_security_feedback_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_security_feedback_get(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudApigeeV1SecurityFeedback>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = apigee_organizations_security_feedback_get_builder(client, name)?;
    apigee_organizations_security_feedback_get_execute(builder)
}

/// GET v1/organizations/{organizationsId}/securityFeedback
/// Lists all feedback reports which have already been submitted.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `apigee_organizations_security_feedback_list_execute()` to send, or `apigee_organizations_security_feedback_list` for simplest API.

pub fn apigee_organizations_security_feedback_list_builder(
    client: &SimpleHttpClient,
    parent: &str,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://apigee.googleapis.com/v1/organizations/{}/securityFeedback",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/organizations/{organizationsId}/securityFeedback
/// Lists all feedback reports which have already been submitted.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `apigee_organizations_security_feedback_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from apigee_organizations_security_feedback_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_security_feedback_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudApigeeV1ListSecurityFeedbackResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudApigeeV1ListSecurityFeedbackResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/organizations/{organizationsId}/securityFeedback
/// Lists all feedback reports which have already been submitted.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `apigee_organizations_security_feedback_list_builder()` + `apigee_organizations_security_feedback_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_security_feedback_list(
    client: &SimpleHttpClient,
    parent: &str,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudApigeeV1ListSecurityFeedbackResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        apigee_organizations_security_feedback_list_builder(client, parent, pageSize, pageToken)?;
    apigee_organizations_security_feedback_list_execute(builder)
}

/// GET v1/organizations/{organizationsId}/securityFeedback/{securityFeedbackId}
/// Updates a specific feedback report.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `apigee_organizations_security_feedback_patch_execute()` to send, or `apigee_organizations_security_feedback_patch` for simplest API.

pub fn apigee_organizations_security_feedback_patch_builder(
    client: &SimpleHttpClient,
    name: &str,
    updateMask: Option<&str>,
    body: &GoogleCloudApigeeV1SecurityFeedback,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://apigee.googleapis.com/v1/organizations/{}/securityFeedback/{}",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = updateMask {
        query_parts.push(format!("updateMask={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/organizations/{organizationsId}/securityFeedback/{securityFeedbackId}
/// Updates a specific feedback report.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `apigee_organizations_security_feedback_patch_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from apigee_organizations_security_feedback_patch_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_security_feedback_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudApigeeV1SecurityFeedback>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudApigeeV1SecurityFeedback = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/organizations/{organizationsId}/securityFeedback/{securityFeedbackId}
/// Updates a specific feedback report.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `apigee_organizations_security_feedback_patch_builder()` + `apigee_organizations_security_feedback_patch_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_security_feedback_patch(
    client: &SimpleHttpClient,
    name: &str,
    updateMask: Option<&str>,
    body: &GoogleCloudApigeeV1SecurityFeedback,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudApigeeV1SecurityFeedback>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        apigee_organizations_security_feedback_patch_builder(client, name, updateMask, body)?;
    apigee_organizations_security_feedback_patch_execute(builder)
}

/// GET v1/organizations/{organizationsId}/securityMonitoringConditions
/// Create a security monitoring condition.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `apigee_organizations_security_monitoring_conditions_create_execute()` to send, or `apigee_organizations_security_monitoring_conditions_create` for simplest API.

pub fn apigee_organizations_security_monitoring_conditions_create_builder(
    client: &SimpleHttpClient,
    parent: &str,
    securityMonitoringConditionId: Option<&str>,
    body: &GoogleCloudApigeeV1SecurityMonitoringCondition,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://apigee.googleapis.com/v1/organizations/{}/securityMonitoringConditions",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = securityMonitoringConditionId {
        query_parts.push(format!("securityMonitoringConditionId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/organizations/{organizationsId}/securityMonitoringConditions
/// Create a security monitoring condition.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `apigee_organizations_security_monitoring_conditions_create_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from apigee_organizations_security_monitoring_conditions_create_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_security_monitoring_conditions_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudApigeeV1SecurityMonitoringCondition>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudApigeeV1SecurityMonitoringCondition =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/organizations/{organizationsId}/securityMonitoringConditions
/// Create a security monitoring condition.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `apigee_organizations_security_monitoring_conditions_create_builder()` + `apigee_organizations_security_monitoring_conditions_create_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_security_monitoring_conditions_create(
    client: &SimpleHttpClient,
    parent: &str,
    securityMonitoringConditionId: Option<&str>,
    body: &GoogleCloudApigeeV1SecurityMonitoringCondition,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudApigeeV1SecurityMonitoringCondition>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = apigee_organizations_security_monitoring_conditions_create_builder(
        client,
        parent,
        securityMonitoringConditionId,
        body,
    )?;
    apigee_organizations_security_monitoring_conditions_create_execute(builder)
}

/// GET v1/organizations/{organizationsId}/securityMonitoringConditions/{securityMonitoringConditionsId}
/// Delete a security monitoring condition.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `apigee_organizations_security_monitoring_conditions_delete_execute()` to send, or `apigee_organizations_security_monitoring_conditions_delete` for simplest API.

pub fn apigee_organizations_security_monitoring_conditions_delete_builder(
    client: &SimpleHttpClient,
    name: &str,
    riskAssessmentType: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://apigee.googleapis.com/v1/organizations/{}/securityMonitoringConditions/{}",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = riskAssessmentType {
        query_parts.push(format!("riskAssessmentType={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/organizations/{organizationsId}/securityMonitoringConditions/{securityMonitoringConditionsId}
/// Delete a security monitoring condition.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `apigee_organizations_security_monitoring_conditions_delete_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from apigee_organizations_security_monitoring_conditions_delete_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_security_monitoring_conditions_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleProtobufEmpty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/organizations/{organizationsId}/securityMonitoringConditions/{securityMonitoringConditionsId}
/// Delete a security monitoring condition.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `apigee_organizations_security_monitoring_conditions_delete_builder()` + `apigee_organizations_security_monitoring_conditions_delete_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_security_monitoring_conditions_delete(
    client: &SimpleHttpClient,
    name: &str,
    riskAssessmentType: Option<&str>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = apigee_organizations_security_monitoring_conditions_delete_builder(
        client,
        name,
        riskAssessmentType,
    )?;
    apigee_organizations_security_monitoring_conditions_delete_execute(builder)
}

/// GET v1/organizations/{organizationsId}/securityMonitoringConditions/{securityMonitoringConditionsId}
/// Get a security monitoring condition.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `apigee_organizations_security_monitoring_conditions_get_execute()` to send, or `apigee_organizations_security_monitoring_conditions_get` for simplest API.

pub fn apigee_organizations_security_monitoring_conditions_get_builder(
    client: &SimpleHttpClient,
    name: &str,
    riskAssessmentType: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://apigee.googleapis.com/v1/organizations/{}/securityMonitoringConditions/{}",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = riskAssessmentType {
        query_parts.push(format!("riskAssessmentType={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/organizations/{organizationsId}/securityMonitoringConditions/{securityMonitoringConditionsId}
/// Get a security monitoring condition.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `apigee_organizations_security_monitoring_conditions_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from apigee_organizations_security_monitoring_conditions_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_security_monitoring_conditions_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudApigeeV1SecurityMonitoringCondition>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudApigeeV1SecurityMonitoringCondition =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/organizations/{organizationsId}/securityMonitoringConditions/{securityMonitoringConditionsId}
/// Get a security monitoring condition.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `apigee_organizations_security_monitoring_conditions_get_builder()` + `apigee_organizations_security_monitoring_conditions_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_security_monitoring_conditions_get(
    client: &SimpleHttpClient,
    name: &str,
    riskAssessmentType: Option<&str>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudApigeeV1SecurityMonitoringCondition>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = apigee_organizations_security_monitoring_conditions_get_builder(
        client,
        name,
        riskAssessmentType,
    )?;
    apigee_organizations_security_monitoring_conditions_get_execute(builder)
}

/// GET v1/organizations/{organizationsId}/securityMonitoringConditions
/// List security monitoring conditions.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `apigee_organizations_security_monitoring_conditions_list_execute()` to send, or `apigee_organizations_security_monitoring_conditions_list` for simplest API.

pub fn apigee_organizations_security_monitoring_conditions_list_builder(
    client: &SimpleHttpClient,
    parent: &str,
    filter: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
    riskAssessmentType: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://apigee.googleapis.com/v1/organizations/{}/securityMonitoringConditions",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = riskAssessmentType {
        query_parts.push(format!("riskAssessmentType={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/organizations/{organizationsId}/securityMonitoringConditions
/// List security monitoring conditions.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `apigee_organizations_security_monitoring_conditions_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from apigee_organizations_security_monitoring_conditions_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_security_monitoring_conditions_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleCloudApigeeV1ListSecurityMonitoringConditionsResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudApigeeV1ListSecurityMonitoringConditionsResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/organizations/{organizationsId}/securityMonitoringConditions
/// List security monitoring conditions.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `apigee_organizations_security_monitoring_conditions_list_builder()` + `apigee_organizations_security_monitoring_conditions_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_security_monitoring_conditions_list(
    client: &SimpleHttpClient,
    parent: &str,
    filter: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
    riskAssessmentType: Option<&str>,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleCloudApigeeV1ListSecurityMonitoringConditionsResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = apigee_organizations_security_monitoring_conditions_list_builder(
        client,
        parent,
        filter,
        pageSize,
        pageToken,
        riskAssessmentType,
    )?;
    apigee_organizations_security_monitoring_conditions_list_execute(builder)
}

/// GET v1/organizations/{organizationsId}/securityMonitoringConditions/{securityMonitoringConditionsId}
/// Update a security monitoring condition.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `apigee_organizations_security_monitoring_conditions_patch_execute()` to send, or `apigee_organizations_security_monitoring_conditions_patch` for simplest API.

pub fn apigee_organizations_security_monitoring_conditions_patch_builder(
    client: &SimpleHttpClient,
    name: &str,
    updateMask: Option<&str>,
    body: &GoogleCloudApigeeV1SecurityMonitoringCondition,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://apigee.googleapis.com/v1/organizations/{}/securityMonitoringConditions/{}",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = updateMask {
        query_parts.push(format!("updateMask={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/organizations/{organizationsId}/securityMonitoringConditions/{securityMonitoringConditionsId}
/// Update a security monitoring condition.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `apigee_organizations_security_monitoring_conditions_patch_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from apigee_organizations_security_monitoring_conditions_patch_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_security_monitoring_conditions_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudApigeeV1SecurityMonitoringCondition>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudApigeeV1SecurityMonitoringCondition =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/organizations/{organizationsId}/securityMonitoringConditions/{securityMonitoringConditionsId}
/// Update a security monitoring condition.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `apigee_organizations_security_monitoring_conditions_patch_builder()` + `apigee_organizations_security_monitoring_conditions_patch_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_security_monitoring_conditions_patch(
    client: &SimpleHttpClient,
    name: &str,
    updateMask: Option<&str>,
    body: &GoogleCloudApigeeV1SecurityMonitoringCondition,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudApigeeV1SecurityMonitoringCondition>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = apigee_organizations_security_monitoring_conditions_patch_builder(
        client, name, updateMask, body,
    )?;
    apigee_organizations_security_monitoring_conditions_patch_execute(builder)
}

/// GET v1/organizations/{organizationsId}/securityProfiles
/// CreateSecurityProfile create a new custom security profile.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `apigee_organizations_security_profiles_create_execute()` to send, or `apigee_organizations_security_profiles_create` for simplest API.

pub fn apigee_organizations_security_profiles_create_builder(
    client: &SimpleHttpClient,
    parent: &str,
    securityProfileId: Option<&str>,
    body: &GoogleCloudApigeeV1SecurityProfile,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://apigee.googleapis.com/v1/organizations/{}/securityProfiles",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = securityProfileId {
        query_parts.push(format!("securityProfileId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/organizations/{organizationsId}/securityProfiles
/// CreateSecurityProfile create a new custom security profile.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `apigee_organizations_security_profiles_create_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from apigee_organizations_security_profiles_create_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_security_profiles_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudApigeeV1SecurityProfile>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudApigeeV1SecurityProfile = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/organizations/{organizationsId}/securityProfiles
/// CreateSecurityProfile create a new custom security profile.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `apigee_organizations_security_profiles_create_builder()` + `apigee_organizations_security_profiles_create_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_security_profiles_create(
    client: &SimpleHttpClient,
    parent: &str,
    securityProfileId: Option<&str>,
    body: &GoogleCloudApigeeV1SecurityProfile,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudApigeeV1SecurityProfile>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = apigee_organizations_security_profiles_create_builder(
        client,
        parent,
        securityProfileId,
        body,
    )?;
    apigee_organizations_security_profiles_create_execute(builder)
}

/// GET v1/organizations/{organizationsId}/securityProfiles/{securityProfilesId}
/// DeleteSecurityProfile delete a profile with all its revisions.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `apigee_organizations_security_profiles_delete_execute()` to send, or `apigee_organizations_security_profiles_delete` for simplest API.

pub fn apigee_organizations_security_profiles_delete_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://apigee.googleapis.com/v1/organizations/{}/securityProfiles/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/organizations/{organizationsId}/securityProfiles/{securityProfilesId}
/// DeleteSecurityProfile delete a profile with all its revisions.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `apigee_organizations_security_profiles_delete_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from apigee_organizations_security_profiles_delete_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_security_profiles_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleProtobufEmpty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/organizations/{organizationsId}/securityProfiles/{securityProfilesId}
/// DeleteSecurityProfile delete a profile with all its revisions.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `apigee_organizations_security_profiles_delete_builder()` + `apigee_organizations_security_profiles_delete_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_security_profiles_delete(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = apigee_organizations_security_profiles_delete_builder(client, name)?;
    apigee_organizations_security_profiles_delete_execute(builder)
}

/// GET v1/organizations/{organizationsId}/securityProfiles/{securityProfilesId}
/// GetSecurityProfile gets the specified security profile. Returns NOT_FOUND if security profile is not present for the specified organization.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `apigee_organizations_security_profiles_get_execute()` to send, or `apigee_organizations_security_profiles_get` for simplest API.

pub fn apigee_organizations_security_profiles_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://apigee.googleapis.com/v1/organizations/{}/securityProfiles/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/organizations/{organizationsId}/securityProfiles/{securityProfilesId}
/// GetSecurityProfile gets the specified security profile. Returns NOT_FOUND if security profile is not present for the specified organization.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `apigee_organizations_security_profiles_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from apigee_organizations_security_profiles_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_security_profiles_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudApigeeV1SecurityProfile>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudApigeeV1SecurityProfile = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/organizations/{organizationsId}/securityProfiles/{securityProfilesId}
/// GetSecurityProfile gets the specified security profile. Returns NOT_FOUND if security profile is not present for the specified organization.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `apigee_organizations_security_profiles_get_builder()` + `apigee_organizations_security_profiles_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_security_profiles_get(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudApigeeV1SecurityProfile>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = apigee_organizations_security_profiles_get_builder(client, name)?;
    apigee_organizations_security_profiles_get_execute(builder)
}

/// GET v1/organizations/{organizationsId}/securityProfiles
/// ListSecurityProfiles lists all the security profiles associated with the org including attached and unattached profiles.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `apigee_organizations_security_profiles_list_execute()` to send, or `apigee_organizations_security_profiles_list` for simplest API.

pub fn apigee_organizations_security_profiles_list_builder(
    client: &SimpleHttpClient,
    parent: &str,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://apigee.googleapis.com/v1/organizations/{}/securityProfiles",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/organizations/{organizationsId}/securityProfiles
/// ListSecurityProfiles lists all the security profiles associated with the org including attached and unattached profiles.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `apigee_organizations_security_profiles_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from apigee_organizations_security_profiles_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_security_profiles_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudApigeeV1ListSecurityProfilesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudApigeeV1ListSecurityProfilesResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/organizations/{organizationsId}/securityProfiles
/// ListSecurityProfiles lists all the security profiles associated with the org including attached and unattached profiles.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `apigee_organizations_security_profiles_list_builder()` + `apigee_organizations_security_profiles_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_security_profiles_list(
    client: &SimpleHttpClient,
    parent: &str,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudApigeeV1ListSecurityProfilesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        apigee_organizations_security_profiles_list_builder(client, parent, pageSize, pageToken)?;
    apigee_organizations_security_profiles_list_execute(builder)
}

/// GET v1/organizations/{organizationsId}/securityProfiles/{securityProfilesId}:listRevisions
/// ListSecurityProfileRevisions lists all the revisions of the security profile.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `apigee_organizations_security_profiles_list_revisions_execute()` to send, or `apigee_organizations_security_profiles_list_revisions` for simplest API.

pub fn apigee_organizations_security_profiles_list_revisions_builder(
    client: &SimpleHttpClient,
    name: &str,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://apigee.googleapis.com/v1/organizations/{}/securityProfiles/{}:listRevisions",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/organizations/{organizationsId}/securityProfiles/{securityProfilesId}:listRevisions
/// ListSecurityProfileRevisions lists all the revisions of the security profile.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `apigee_organizations_security_profiles_list_revisions_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from apigee_organizations_security_profiles_list_revisions_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_security_profiles_list_revisions_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleCloudApigeeV1ListSecurityProfileRevisionsResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudApigeeV1ListSecurityProfileRevisionsResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/organizations/{organizationsId}/securityProfiles/{securityProfilesId}:listRevisions
/// ListSecurityProfileRevisions lists all the revisions of the security profile.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `apigee_organizations_security_profiles_list_revisions_builder()` + `apigee_organizations_security_profiles_list_revisions_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_security_profiles_list_revisions(
    client: &SimpleHttpClient,
    name: &str,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleCloudApigeeV1ListSecurityProfileRevisionsResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = apigee_organizations_security_profiles_list_revisions_builder(
        client, name, pageSize, pageToken,
    )?;
    apigee_organizations_security_profiles_list_revisions_execute(builder)
}

/// GET v1/organizations/{organizationsId}/securityProfiles/{securityProfilesId}
/// UpdateSecurityProfile update the metadata of security profile.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `apigee_organizations_security_profiles_patch_execute()` to send, or `apigee_organizations_security_profiles_patch` for simplest API.

pub fn apigee_organizations_security_profiles_patch_builder(
    client: &SimpleHttpClient,
    name: &str,
    updateMask: Option<&str>,
    body: &GoogleCloudApigeeV1SecurityProfile,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://apigee.googleapis.com/v1/organizations/{}/securityProfiles/{}",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = updateMask {
        query_parts.push(format!("updateMask={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/organizations/{organizationsId}/securityProfiles/{securityProfilesId}
/// UpdateSecurityProfile update the metadata of security profile.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `apigee_organizations_security_profiles_patch_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from apigee_organizations_security_profiles_patch_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_security_profiles_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudApigeeV1SecurityProfile>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudApigeeV1SecurityProfile = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/organizations/{organizationsId}/securityProfiles/{securityProfilesId}
/// UpdateSecurityProfile update the metadata of security profile.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `apigee_organizations_security_profiles_patch_builder()` + `apigee_organizations_security_profiles_patch_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_security_profiles_patch(
    client: &SimpleHttpClient,
    name: &str,
    updateMask: Option<&str>,
    body: &GoogleCloudApigeeV1SecurityProfile,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudApigeeV1SecurityProfile>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        apigee_organizations_security_profiles_patch_builder(client, name, updateMask, body)?;
    apigee_organizations_security_profiles_patch_execute(builder)
}

/// GET v1/organizations/{organizationsId}/securityProfiles/{securityProfilesId}/environments/{environmentsId}:computeEnvironmentScores
/// ComputeEnvironmentScores calculates scores for requested time range for the specified security profile and environment.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `apigee_organizations_security_profiles_environments_compute_environment_scores_execute()` to send, or `apigee_organizations_security_profiles_environments_compute_environment_scores` for simplest API.

pub fn apigee_organizations_security_profiles_environments_compute_environment_scores_builder(
    client: &SimpleHttpClient,
    profileEnvironment: &str,
    body: &GoogleCloudApigeeV1ComputeEnvironmentScoresRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://apigee.googleapis.com/v1/organizations/{}/securityProfiles/{}/environments/{}:computeEnvironmentScores",
        profileEnvironment,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/organizations/{organizationsId}/securityProfiles/{securityProfilesId}/environments/{environmentsId}:computeEnvironmentScores
/// ComputeEnvironmentScores calculates scores for requested time range for the specified security profile and environment.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `apigee_organizations_security_profiles_environments_compute_environment_scores_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from apigee_organizations_security_profiles_environments_compute_environment_scores_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_security_profiles_environments_compute_environment_scores_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudApigeeV1ComputeEnvironmentScoresResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudApigeeV1ComputeEnvironmentScoresResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/organizations/{organizationsId}/securityProfiles/{securityProfilesId}/environments/{environmentsId}:computeEnvironmentScores
/// ComputeEnvironmentScores calculates scores for requested time range for the specified security profile and environment.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `apigee_organizations_security_profiles_environments_compute_environment_scores_builder()` + `apigee_organizations_security_profiles_environments_compute_environment_scores_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_security_profiles_environments_compute_environment_scores(
    client: &SimpleHttpClient,
    profileEnvironment: &str,
    body: &GoogleCloudApigeeV1ComputeEnvironmentScoresRequest,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudApigeeV1ComputeEnvironmentScoresResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        apigee_organizations_security_profiles_environments_compute_environment_scores_builder(
            client,
            profileEnvironment,
            body,
        )?;
    apigee_organizations_security_profiles_environments_compute_environment_scores_execute(builder)
}

/// GET v1/organizations/{organizationsId}/securityProfiles/{securityProfilesId}/environments
/// CreateSecurityProfileEnvironmentAssociation creates profile environment association i.e. attaches environment to security profile.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `apigee_organizations_security_profiles_environments_create_execute()` to send, or `apigee_organizations_security_profiles_environments_create` for simplest API.

pub fn apigee_organizations_security_profiles_environments_create_builder(
    client: &SimpleHttpClient,
    parent: &str,
    body: &GoogleCloudApigeeV1SecurityProfileEnvironmentAssociation,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://apigee.googleapis.com/v1/organizations/{}/securityProfiles/{}/environments",
        parent,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/organizations/{organizationsId}/securityProfiles/{securityProfilesId}/environments
/// CreateSecurityProfileEnvironmentAssociation creates profile environment association i.e. attaches environment to security profile.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `apigee_organizations_security_profiles_environments_create_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from apigee_organizations_security_profiles_environments_create_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_security_profiles_environments_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleCloudApigeeV1SecurityProfileEnvironmentAssociation>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudApigeeV1SecurityProfileEnvironmentAssociation =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/organizations/{organizationsId}/securityProfiles/{securityProfilesId}/environments
/// CreateSecurityProfileEnvironmentAssociation creates profile environment association i.e. attaches environment to security profile.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `apigee_organizations_security_profiles_environments_create_builder()` + `apigee_organizations_security_profiles_environments_create_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_security_profiles_environments_create(
    client: &SimpleHttpClient,
    parent: &str,
    body: &GoogleCloudApigeeV1SecurityProfileEnvironmentAssociation,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleCloudApigeeV1SecurityProfileEnvironmentAssociation>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        apigee_organizations_security_profiles_environments_create_builder(client, parent, body)?;
    apigee_organizations_security_profiles_environments_create_execute(builder)
}

/// GET v1/organizations/{organizationsId}/securityProfiles/{securityProfilesId}/environments/{environmentsId}
/// DeleteSecurityProfileEnvironmentAssociation removes profile environment association i.e. detaches environment from security profile.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `apigee_organizations_security_profiles_environments_delete_execute()` to send, or `apigee_organizations_security_profiles_environments_delete` for simplest API.

pub fn apigee_organizations_security_profiles_environments_delete_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://apigee.googleapis.com/v1/organizations/{}/securityProfiles/{}/environments/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/organizations/{organizationsId}/securityProfiles/{securityProfilesId}/environments/{environmentsId}
/// DeleteSecurityProfileEnvironmentAssociation removes profile environment association i.e. detaches environment from security profile.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `apigee_organizations_security_profiles_environments_delete_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from apigee_organizations_security_profiles_environments_delete_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_security_profiles_environments_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleProtobufEmpty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/organizations/{organizationsId}/securityProfiles/{securityProfilesId}/environments/{environmentsId}
/// DeleteSecurityProfileEnvironmentAssociation removes profile environment association i.e. detaches environment from security profile.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `apigee_organizations_security_profiles_environments_delete_builder()` + `apigee_organizations_security_profiles_environments_delete_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_security_profiles_environments_delete(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = apigee_organizations_security_profiles_environments_delete_builder(client, name)?;
    apigee_organizations_security_profiles_environments_delete_execute(builder)
}

/// GET v1/organizations/{organizationsId}/securityProfilesV2
/// Create a security profile v2.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `apigee_organizations_security_profiles_v2_create_execute()` to send, or `apigee_organizations_security_profiles_v2_create` for simplest API.

pub fn apigee_organizations_security_profiles_v2_create_builder(
    client: &SimpleHttpClient,
    parent: &str,
    securityProfileV2Id: Option<&str>,
    body: &GoogleCloudApigeeV1SecurityProfileV2,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://apigee.googleapis.com/v1/organizations/{}/securityProfilesV2",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = securityProfileV2Id {
        query_parts.push(format!("securityProfileV2Id={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/organizations/{organizationsId}/securityProfilesV2
/// Create a security profile v2.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `apigee_organizations_security_profiles_v2_create_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from apigee_organizations_security_profiles_v2_create_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_security_profiles_v2_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudApigeeV1SecurityProfileV2>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudApigeeV1SecurityProfileV2 = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/organizations/{organizationsId}/securityProfilesV2
/// Create a security profile v2.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `apigee_organizations_security_profiles_v2_create_builder()` + `apigee_organizations_security_profiles_v2_create_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_security_profiles_v2_create(
    client: &SimpleHttpClient,
    parent: &str,
    securityProfileV2Id: Option<&str>,
    body: &GoogleCloudApigeeV1SecurityProfileV2,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudApigeeV1SecurityProfileV2>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = apigee_organizations_security_profiles_v2_create_builder(
        client,
        parent,
        securityProfileV2Id,
        body,
    )?;
    apigee_organizations_security_profiles_v2_create_execute(builder)
}

/// GET v1/organizations/{organizationsId}/securityProfilesV2/{securityProfilesV2Id}
/// Delete a security profile v2.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `apigee_organizations_security_profiles_v2_delete_execute()` to send, or `apigee_organizations_security_profiles_v2_delete` for simplest API.

pub fn apigee_organizations_security_profiles_v2_delete_builder(
    client: &SimpleHttpClient,
    name: &str,
    riskAssessmentType: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://apigee.googleapis.com/v1/organizations/{}/securityProfilesV2/{}",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = riskAssessmentType {
        query_parts.push(format!("riskAssessmentType={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/organizations/{organizationsId}/securityProfilesV2/{securityProfilesV2Id}
/// Delete a security profile v2.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `apigee_organizations_security_profiles_v2_delete_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from apigee_organizations_security_profiles_v2_delete_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_security_profiles_v2_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleProtobufEmpty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/organizations/{organizationsId}/securityProfilesV2/{securityProfilesV2Id}
/// Delete a security profile v2.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `apigee_organizations_security_profiles_v2_delete_builder()` + `apigee_organizations_security_profiles_v2_delete_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_security_profiles_v2_delete(
    client: &SimpleHttpClient,
    name: &str,
    riskAssessmentType: Option<&str>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        apigee_organizations_security_profiles_v2_delete_builder(client, name, riskAssessmentType)?;
    apigee_organizations_security_profiles_v2_delete_execute(builder)
}

/// GET v1/organizations/{organizationsId}/securityProfilesV2/{securityProfilesV2Id}
/// Get a security profile v2.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `apigee_organizations_security_profiles_v2_get_execute()` to send, or `apigee_organizations_security_profiles_v2_get` for simplest API.

pub fn apigee_organizations_security_profiles_v2_get_builder(
    client: &SimpleHttpClient,
    name: &str,
    riskAssessmentType: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://apigee.googleapis.com/v1/organizations/{}/securityProfilesV2/{}",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = riskAssessmentType {
        query_parts.push(format!("riskAssessmentType={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/organizations/{organizationsId}/securityProfilesV2/{securityProfilesV2Id}
/// Get a security profile v2.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `apigee_organizations_security_profiles_v2_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from apigee_organizations_security_profiles_v2_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_security_profiles_v2_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudApigeeV1SecurityProfileV2>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudApigeeV1SecurityProfileV2 = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/organizations/{organizationsId}/securityProfilesV2/{securityProfilesV2Id}
/// Get a security profile v2.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `apigee_organizations_security_profiles_v2_get_builder()` + `apigee_organizations_security_profiles_v2_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_security_profiles_v2_get(
    client: &SimpleHttpClient,
    name: &str,
    riskAssessmentType: Option<&str>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudApigeeV1SecurityProfileV2>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        apigee_organizations_security_profiles_v2_get_builder(client, name, riskAssessmentType)?;
    apigee_organizations_security_profiles_v2_get_execute(builder)
}

/// GET v1/organizations/{organizationsId}/securityProfilesV2
/// List security profiles v2.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `apigee_organizations_security_profiles_v2_list_execute()` to send, or `apigee_organizations_security_profiles_v2_list` for simplest API.

pub fn apigee_organizations_security_profiles_v2_list_builder(
    client: &SimpleHttpClient,
    parent: &str,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
    riskAssessmentType: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://apigee.googleapis.com/v1/organizations/{}/securityProfilesV2",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = riskAssessmentType {
        query_parts.push(format!("riskAssessmentType={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/organizations/{organizationsId}/securityProfilesV2
/// List security profiles v2.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `apigee_organizations_security_profiles_v2_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from apigee_organizations_security_profiles_v2_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_security_profiles_v2_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudApigeeV1ListSecurityProfilesV2Response>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudApigeeV1ListSecurityProfilesV2Response =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/organizations/{organizationsId}/securityProfilesV2
/// List security profiles v2.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `apigee_organizations_security_profiles_v2_list_builder()` + `apigee_organizations_security_profiles_v2_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_security_profiles_v2_list(
    client: &SimpleHttpClient,
    parent: &str,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
    riskAssessmentType: Option<&str>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudApigeeV1ListSecurityProfilesV2Response>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = apigee_organizations_security_profiles_v2_list_builder(
        client,
        parent,
        pageSize,
        pageToken,
        riskAssessmentType,
    )?;
    apigee_organizations_security_profiles_v2_list_execute(builder)
}

/// GET v1/organizations/{organizationsId}/securityProfilesV2/{securityProfilesV2Id}
/// Update a security profile V2.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `apigee_organizations_security_profiles_v2_patch_execute()` to send, or `apigee_organizations_security_profiles_v2_patch` for simplest API.

pub fn apigee_organizations_security_profiles_v2_patch_builder(
    client: &SimpleHttpClient,
    name: &str,
    updateMask: Option<&str>,
    body: &GoogleCloudApigeeV1SecurityProfileV2,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://apigee.googleapis.com/v1/organizations/{}/securityProfilesV2/{}",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = updateMask {
        query_parts.push(format!("updateMask={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/organizations/{organizationsId}/securityProfilesV2/{securityProfilesV2Id}
/// Update a security profile V2.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `apigee_organizations_security_profiles_v2_patch_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from apigee_organizations_security_profiles_v2_patch_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_security_profiles_v2_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudApigeeV1SecurityProfileV2>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudApigeeV1SecurityProfileV2 = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/organizations/{organizationsId}/securityProfilesV2/{securityProfilesV2Id}
/// Update a security profile V2.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `apigee_organizations_security_profiles_v2_patch_builder()` + `apigee_organizations_security_profiles_v2_patch_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_security_profiles_v2_patch(
    client: &SimpleHttpClient,
    name: &str,
    updateMask: Option<&str>,
    body: &GoogleCloudApigeeV1SecurityProfileV2,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudApigeeV1SecurityProfileV2>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        apigee_organizations_security_profiles_v2_patch_builder(client, name, updateMask, body)?;
    apigee_organizations_security_profiles_v2_patch_execute(builder)
}

/// GET v1/organizations/{organizationsId}/sharedflows
/// Uploads a ZIP-formatted shared flow configuration bundle to an organization. If the shared flow already exists, this creates a new revision of it. If the shared flow does not exist, this creates it. Once imported, the shared flow revision must be deployed before it can be accessed at runtime. The size limit of a shared flow bundle is 15 MB.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `apigee_organizations_sharedflows_create_execute()` to send, or `apigee_organizations_sharedflows_create` for simplest API.

pub fn apigee_organizations_sharedflows_create_builder(
    client: &SimpleHttpClient,
    parent: &str,
    action: Option<&str>,
    name: Option<&str>,
    space: Option<&str>,
    body: &GoogleApiHttpBody,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://apigee.googleapis.com/v1/organizations/{}/sharedflows",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = action {
        query_parts.push(format!("action={}", val));
    }
    if let Some(val) = name {
        query_parts.push(format!("name={}", val));
    }
    if let Some(val) = space {
        query_parts.push(format!("space={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/organizations/{organizationsId}/sharedflows
/// Uploads a ZIP-formatted shared flow configuration bundle to an organization. If the shared flow already exists, this creates a new revision of it. If the shared flow does not exist, this creates it. Once imported, the shared flow revision must be deployed before it can be accessed at runtime. The size limit of a shared flow bundle is 15 MB.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `apigee_organizations_sharedflows_create_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from apigee_organizations_sharedflows_create_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_sharedflows_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudApigeeV1SharedFlowRevision>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudApigeeV1SharedFlowRevision = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/organizations/{organizationsId}/sharedflows
/// Uploads a ZIP-formatted shared flow configuration bundle to an organization. If the shared flow already exists, this creates a new revision of it. If the shared flow does not exist, this creates it. Once imported, the shared flow revision must be deployed before it can be accessed at runtime. The size limit of a shared flow bundle is 15 MB.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `apigee_organizations_sharedflows_create_builder()` + `apigee_organizations_sharedflows_create_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_sharedflows_create(
    client: &SimpleHttpClient,
    parent: &str,
    action: Option<&str>,
    name: Option<&str>,
    space: Option<&str>,
    body: &GoogleApiHttpBody,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudApigeeV1SharedFlowRevision>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        apigee_organizations_sharedflows_create_builder(client, parent, action, name, space, body)?;
    apigee_organizations_sharedflows_create_execute(builder)
}

/// GET v1/organizations/{organizationsId}/sharedflows/{sharedflowsId}
/// Deletes a shared flow and all it's revisions. The shared flow must be undeployed before you can delete it.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `apigee_organizations_sharedflows_delete_execute()` to send, or `apigee_organizations_sharedflows_delete` for simplest API.

pub fn apigee_organizations_sharedflows_delete_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://apigee.googleapis.com/v1/organizations/{}/sharedflows/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/organizations/{organizationsId}/sharedflows/{sharedflowsId}
/// Deletes a shared flow and all it's revisions. The shared flow must be undeployed before you can delete it.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `apigee_organizations_sharedflows_delete_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from apigee_organizations_sharedflows_delete_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_sharedflows_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudApigeeV1SharedFlow>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudApigeeV1SharedFlow = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/organizations/{organizationsId}/sharedflows/{sharedflowsId}
/// Deletes a shared flow and all it's revisions. The shared flow must be undeployed before you can delete it.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `apigee_organizations_sharedflows_delete_builder()` + `apigee_organizations_sharedflows_delete_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_sharedflows_delete(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudApigeeV1SharedFlow>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = apigee_organizations_sharedflows_delete_builder(client, name)?;
    apigee_organizations_sharedflows_delete_execute(builder)
}

/// GET v1/organizations/{organizationsId}/sharedflows/{sharedflowsId}
/// Gets a shared flow by name, including a list of its revisions.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `apigee_organizations_sharedflows_get_execute()` to send, or `apigee_organizations_sharedflows_get` for simplest API.

pub fn apigee_organizations_sharedflows_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://apigee.googleapis.com/v1/organizations/{}/sharedflows/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/organizations/{organizationsId}/sharedflows/{sharedflowsId}
/// Gets a shared flow by name, including a list of its revisions.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `apigee_organizations_sharedflows_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from apigee_organizations_sharedflows_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_sharedflows_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudApigeeV1SharedFlow>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudApigeeV1SharedFlow = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/organizations/{organizationsId}/sharedflows/{sharedflowsId}
/// Gets a shared flow by name, including a list of its revisions.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `apigee_organizations_sharedflows_get_builder()` + `apigee_organizations_sharedflows_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_sharedflows_get(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudApigeeV1SharedFlow>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = apigee_organizations_sharedflows_get_builder(client, name)?;
    apigee_organizations_sharedflows_get_execute(builder)
}

/// GET v1/organizations/{organizationsId}/sharedflows
/// Lists all shared flows in the organization. If the resource has the space attribute set, the response may not return all resources. To learn more, read the [Apigee Spaces Overview](<https://cloud.google.`com/apigee/docs/api-platform/system-administration/spaces/apigee-spaces-overview`>).
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `apigee_organizations_sharedflows_list_execute()` to send, or `apigee_organizations_sharedflows_list` for simplest API.

pub fn apigee_organizations_sharedflows_list_builder(
    client: &SimpleHttpClient,
    parent: &str,
    includeMetaData: Option<bool>,
    includeRevisions: Option<bool>,
    space: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://apigee.googleapis.com/v1/organizations/{}/sharedflows",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = includeMetaData {
        query_parts.push(format!("includeMetaData={}", val));
    }
    if let Some(val) = includeRevisions {
        query_parts.push(format!("includeRevisions={}", val));
    }
    if let Some(val) = space {
        query_parts.push(format!("space={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/organizations/{organizationsId}/sharedflows
/// Lists all shared flows in the organization. If the resource has the space attribute set, the response may not return all resources. To learn more, read the [Apigee Spaces Overview](<https://cloud.google.`com/apigee/docs/api-platform/system-administration/spaces/apigee-spaces-overview`>).
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `apigee_organizations_sharedflows_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from apigee_organizations_sharedflows_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_sharedflows_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudApigeeV1ListSharedFlowsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudApigeeV1ListSharedFlowsResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/organizations/{organizationsId}/sharedflows
/// Lists all shared flows in the organization. If the resource has the space attribute set, the response may not return all resources. To learn more, read the [Apigee Spaces Overview](<https://cloud.google.`com/apigee/docs/api-platform/system-administration/spaces/apigee-spaces-overview`>).
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `apigee_organizations_sharedflows_list_builder()` + `apigee_organizations_sharedflows_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_sharedflows_list(
    client: &SimpleHttpClient,
    parent: &str,
    includeMetaData: Option<bool>,
    includeRevisions: Option<bool>,
    space: Option<&str>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudApigeeV1ListSharedFlowsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = apigee_organizations_sharedflows_list_builder(
        client,
        parent,
        includeMetaData,
        includeRevisions,
        space,
    )?;
    apigee_organizations_sharedflows_list_execute(builder)
}

/// GET v1/organizations/{organizationsId}/sharedflows/{sharedflowsId}:move
/// Moves an shared flow to a different space.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `apigee_organizations_sharedflows_move_execute()` to send, or `apigee_organizations_sharedflows_move` for simplest API.

pub fn apigee_organizations_sharedflows_move_builder(
    client: &SimpleHttpClient,
    name: &str,
    body: &GoogleCloudApigeeV1MoveSharedFlowRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://apigee.googleapis.com/v1/organizations/{}/sharedflows/{}:move",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/organizations/{organizationsId}/sharedflows/{sharedflowsId}:move
/// Moves an shared flow to a different space.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `apigee_organizations_sharedflows_move_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from apigee_organizations_sharedflows_move_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_sharedflows_move_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudApigeeV1SharedFlow>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudApigeeV1SharedFlow = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/organizations/{organizationsId}/sharedflows/{sharedflowsId}:move
/// Moves an shared flow to a different space.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `apigee_organizations_sharedflows_move_builder()` + `apigee_organizations_sharedflows_move_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_sharedflows_move(
    client: &SimpleHttpClient,
    name: &str,
    body: &GoogleCloudApigeeV1MoveSharedFlowRequest,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudApigeeV1SharedFlow>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = apigee_organizations_sharedflows_move_builder(client, name, body)?;
    apigee_organizations_sharedflows_move_execute(builder)
}

/// GET v1/organizations/{organizationsId}/sharedflows/{sharedflowsId}/deployments
/// Lists all deployments of a shared flow.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `apigee_organizations_sharedflows_deployments_list_execute()` to send, or `apigee_organizations_sharedflows_deployments_list` for simplest API.

pub fn apigee_organizations_sharedflows_deployments_list_builder(
    client: &SimpleHttpClient,
    parent: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://apigee.googleapis.com/v1/organizations/{}/sharedflows/{}/deployments",
        parent,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/organizations/{organizationsId}/sharedflows/{sharedflowsId}/deployments
/// Lists all deployments of a shared flow.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `apigee_organizations_sharedflows_deployments_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from apigee_organizations_sharedflows_deployments_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_sharedflows_deployments_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudApigeeV1ListDeploymentsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudApigeeV1ListDeploymentsResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/organizations/{organizationsId}/sharedflows/{sharedflowsId}/deployments
/// Lists all deployments of a shared flow.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `apigee_organizations_sharedflows_deployments_list_builder()` + `apigee_organizations_sharedflows_deployments_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_sharedflows_deployments_list(
    client: &SimpleHttpClient,
    parent: &str,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudApigeeV1ListDeploymentsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = apigee_organizations_sharedflows_deployments_list_builder(client, parent)?;
    apigee_organizations_sharedflows_deployments_list_execute(builder)
}

/// GET v1/organizations/{organizationsId}/sharedflows/{sharedflowsId}/revisions/{revisionsId}
/// Deletes a shared flow and all associated policies, resources, and revisions. You must undeploy the shared flow before deleting it.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `apigee_organizations_sharedflows_revisions_delete_execute()` to send, or `apigee_organizations_sharedflows_revisions_delete` for simplest API.

pub fn apigee_organizations_sharedflows_revisions_delete_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://apigee.googleapis.com/v1/organizations/{}/sharedflows/{}/revisions/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/organizations/{organizationsId}/sharedflows/{sharedflowsId}/revisions/{revisionsId}
/// Deletes a shared flow and all associated policies, resources, and revisions. You must undeploy the shared flow before deleting it.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `apigee_organizations_sharedflows_revisions_delete_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from apigee_organizations_sharedflows_revisions_delete_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_sharedflows_revisions_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudApigeeV1SharedFlowRevision>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudApigeeV1SharedFlowRevision = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/organizations/{organizationsId}/sharedflows/{sharedflowsId}/revisions/{revisionsId}
/// Deletes a shared flow and all associated policies, resources, and revisions. You must undeploy the shared flow before deleting it.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `apigee_organizations_sharedflows_revisions_delete_builder()` + `apigee_organizations_sharedflows_revisions_delete_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_sharedflows_revisions_delete(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudApigeeV1SharedFlowRevision>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = apigee_organizations_sharedflows_revisions_delete_builder(client, name)?;
    apigee_organizations_sharedflows_revisions_delete_execute(builder)
}

/// GET v1/organizations/{organizationsId}/sharedflows/{sharedflowsId}/revisions/{revisionsId}
/// Gets a revision of a shared flow. To download the shared flow configuration bundle for the specified revision as a zip file, set the format query parameter to bundle. If you are using curl, specify -o filename.zip to save the output to a file; otherwise, it displays to stdout. Then, develop the shared flow configuration locally and upload the updated `sharedFlow` configuration revision, as described in [`updateSharedFlowRevision`](`updateSharedFlowRevision`).
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `apigee_organizations_sharedflows_revisions_get_execute()` to send, or `apigee_organizations_sharedflows_revisions_get` for simplest API.

pub fn apigee_organizations_sharedflows_revisions_get_builder(
    client: &SimpleHttpClient,
    name: &str,
    format: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://apigee.googleapis.com/v1/organizations/{}/sharedflows/{}/revisions/{}",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = format {
        query_parts.push(format!("format={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/organizations/{organizationsId}/sharedflows/{sharedflowsId}/revisions/{revisionsId}
/// Gets a revision of a shared flow. To download the shared flow configuration bundle for the specified revision as a zip file, set the format query parameter to bundle. If you are using curl, specify -o filename.zip to save the output to a file; otherwise, it displays to stdout. Then, develop the shared flow configuration locally and upload the updated `sharedFlow` configuration revision, as described in [`updateSharedFlowRevision`](`updateSharedFlowRevision`).
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `apigee_organizations_sharedflows_revisions_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from apigee_organizations_sharedflows_revisions_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_sharedflows_revisions_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleApiHttpBody>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleApiHttpBody = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/organizations/{organizationsId}/sharedflows/{sharedflowsId}/revisions/{revisionsId}
/// Gets a revision of a shared flow. To download the shared flow configuration bundle for the specified revision as a zip file, set the format query parameter to bundle. If you are using curl, specify -o filename.zip to save the output to a file; otherwise, it displays to stdout. Then, develop the shared flow configuration locally and upload the updated `sharedFlow` configuration revision, as described in [`updateSharedFlowRevision`](`updateSharedFlowRevision`).
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `apigee_organizations_sharedflows_revisions_get_builder()` + `apigee_organizations_sharedflows_revisions_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_sharedflows_revisions_get(
    client: &SimpleHttpClient,
    name: &str,
    format: Option<&str>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleApiHttpBody>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = apigee_organizations_sharedflows_revisions_get_builder(client, name, format)?;
    apigee_organizations_sharedflows_revisions_get_execute(builder)
}

/// GET v1/organizations/{organizationsId}/sharedflows/{sharedflowsId}/revisions/{revisionsId}
/// Updates a shared flow revision. This operation is only allowed on revisions which have never been deployed. After deployment a revision becomes immutable, even if it becomes undeployed. The payload is a ZIP-formatted shared flow. Content type must be either `multipart/form-data` or `application/octet-stream`.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `apigee_organizations_sharedflows_revisions_update_shared_flow_revision_execute()` to send, or `apigee_organizations_sharedflows_revisions_update_shared_flow_revision` for simplest API.

pub fn apigee_organizations_sharedflows_revisions_update_shared_flow_revision_builder(
    client: &SimpleHttpClient,
    name: &str,
    validate: Option<bool>,
    body: &GoogleApiHttpBody,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://apigee.googleapis.com/v1/organizations/{}/sharedflows/{}/revisions/{}",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = validate {
        query_parts.push(format!("validate={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/organizations/{organizationsId}/sharedflows/{sharedflowsId}/revisions/{revisionsId}
/// Updates a shared flow revision. This operation is only allowed on revisions which have never been deployed. After deployment a revision becomes immutable, even if it becomes undeployed. The payload is a ZIP-formatted shared flow. Content type must be either `multipart/form-data` or `application/octet-stream`.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `apigee_organizations_sharedflows_revisions_update_shared_flow_revision_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from apigee_organizations_sharedflows_revisions_update_shared_flow_revision_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_sharedflows_revisions_update_shared_flow_revision_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudApigeeV1SharedFlowRevision>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudApigeeV1SharedFlowRevision = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/organizations/{organizationsId}/sharedflows/{sharedflowsId}/revisions/{revisionsId}
/// Updates a shared flow revision. This operation is only allowed on revisions which have never been deployed. After deployment a revision becomes immutable, even if it becomes undeployed. The payload is a ZIP-formatted shared flow. Content type must be either `multipart/form-data` or `application/octet-stream`.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `apigee_organizations_sharedflows_revisions_update_shared_flow_revision_builder()` + `apigee_organizations_sharedflows_revisions_update_shared_flow_revision_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_sharedflows_revisions_update_shared_flow_revision(
    client: &SimpleHttpClient,
    name: &str,
    validate: Option<bool>,
    body: &GoogleApiHttpBody,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudApigeeV1SharedFlowRevision>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = apigee_organizations_sharedflows_revisions_update_shared_flow_revision_builder(
        client, name, validate, body,
    )?;
    apigee_organizations_sharedflows_revisions_update_shared_flow_revision_execute(builder)
}

/// GET v1/organizations/{organizationsId}/sharedflows/{sharedflowsId}/revisions/{revisionsId}/deployments
/// Lists all deployments of a shared flow revision.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `apigee_organizations_sharedflows_revisions_deployments_list_execute()` to send, or `apigee_organizations_sharedflows_revisions_deployments_list` for simplest API.

pub fn apigee_organizations_sharedflows_revisions_deployments_list_builder(
    client: &SimpleHttpClient,
    parent: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://apigee.googleapis.com/v1/organizations/{}/sharedflows/{}/revisions/{}/deployments",
        parent,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/organizations/{organizationsId}/sharedflows/{sharedflowsId}/revisions/{revisionsId}/deployments
/// Lists all deployments of a shared flow revision.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `apigee_organizations_sharedflows_revisions_deployments_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from apigee_organizations_sharedflows_revisions_deployments_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_sharedflows_revisions_deployments_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudApigeeV1ListDeploymentsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudApigeeV1ListDeploymentsResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/organizations/{organizationsId}/sharedflows/{sharedflowsId}/revisions/{revisionsId}/deployments
/// Lists all deployments of a shared flow revision.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `apigee_organizations_sharedflows_revisions_deployments_list_builder()` + `apigee_organizations_sharedflows_revisions_deployments_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_sharedflows_revisions_deployments_list(
    client: &SimpleHttpClient,
    parent: &str,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudApigeeV1ListDeploymentsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        apigee_organizations_sharedflows_revisions_deployments_list_builder(client, parent)?;
    apigee_organizations_sharedflows_revisions_deployments_list_execute(builder)
}

/// GET v1/organizations/{organizationsId}/sites/{sitesId}/apicategories
/// Creates a new API category.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `apigee_organizations_sites_apicategories_create_execute()` to send, or `apigee_organizations_sites_apicategories_create` for simplest API.

pub fn apigee_organizations_sites_apicategories_create_builder(
    client: &SimpleHttpClient,
    parent: &str,
    body: &GoogleCloudApigeeV1ApiCategory,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://apigee.googleapis.com/v1/organizations/{}/sites/{}/apicategories",
        parent,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/organizations/{organizationsId}/sites/{sitesId}/apicategories
/// Creates a new API category.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `apigee_organizations_sites_apicategories_create_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from apigee_organizations_sites_apicategories_create_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_sites_apicategories_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudApigeeV1ApiCategoryResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudApigeeV1ApiCategoryResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/organizations/{organizationsId}/sites/{sitesId}/apicategories
/// Creates a new API category.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `apigee_organizations_sites_apicategories_create_builder()` + `apigee_organizations_sites_apicategories_create_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_sites_apicategories_create(
    client: &SimpleHttpClient,
    parent: &str,
    body: &GoogleCloudApigeeV1ApiCategory,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudApigeeV1ApiCategoryResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = apigee_organizations_sites_apicategories_create_builder(client, parent, body)?;
    apigee_organizations_sites_apicategories_create_execute(builder)
}

/// GET v1/organizations/{organizationsId}/sites/{sitesId}/apicategories/{apicategoriesId}
/// Deletes an API category.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `apigee_organizations_sites_apicategories_delete_execute()` to send, or `apigee_organizations_sites_apicategories_delete` for simplest API.

pub fn apigee_organizations_sites_apicategories_delete_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://apigee.googleapis.com/v1/organizations/{}/sites/{}/apicategories/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/organizations/{organizationsId}/sites/{sitesId}/apicategories/{apicategoriesId}
/// Deletes an API category.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `apigee_organizations_sites_apicategories_delete_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from apigee_organizations_sites_apicategories_delete_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_sites_apicategories_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudApigeeV1DeleteResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudApigeeV1DeleteResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/organizations/{organizationsId}/sites/{sitesId}/apicategories/{apicategoriesId}
/// Deletes an API category.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `apigee_organizations_sites_apicategories_delete_builder()` + `apigee_organizations_sites_apicategories_delete_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_sites_apicategories_delete(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudApigeeV1DeleteResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = apigee_organizations_sites_apicategories_delete_builder(client, name)?;
    apigee_organizations_sites_apicategories_delete_execute(builder)
}

/// GET v1/organizations/{organizationsId}/sites/{sitesId}/apicategories/{apicategoriesId}
/// Gets an API category.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `apigee_organizations_sites_apicategories_get_execute()` to send, or `apigee_organizations_sites_apicategories_get` for simplest API.

pub fn apigee_organizations_sites_apicategories_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://apigee.googleapis.com/v1/organizations/{}/sites/{}/apicategories/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/organizations/{organizationsId}/sites/{sitesId}/apicategories/{apicategoriesId}
/// Gets an API category.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `apigee_organizations_sites_apicategories_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from apigee_organizations_sites_apicategories_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_sites_apicategories_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudApigeeV1ApiCategoryResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudApigeeV1ApiCategoryResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/organizations/{organizationsId}/sites/{sitesId}/apicategories/{apicategoriesId}
/// Gets an API category.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `apigee_organizations_sites_apicategories_get_builder()` + `apigee_organizations_sites_apicategories_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_sites_apicategories_get(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudApigeeV1ApiCategoryResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = apigee_organizations_sites_apicategories_get_builder(client, name)?;
    apigee_organizations_sites_apicategories_get_execute(builder)
}

/// GET v1/organizations/{organizationsId}/sites/{sitesId}/apicategories
/// Returns the API categories associated with a portal.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `apigee_organizations_sites_apicategories_list_execute()` to send, or `apigee_organizations_sites_apicategories_list` for simplest API.

pub fn apigee_organizations_sites_apicategories_list_builder(
    client: &SimpleHttpClient,
    parent: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://apigee.googleapis.com/v1/organizations/{}/sites/{}/apicategories",
        parent,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/organizations/{organizationsId}/sites/{sitesId}/apicategories
/// Returns the API categories associated with a portal.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `apigee_organizations_sites_apicategories_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from apigee_organizations_sites_apicategories_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_sites_apicategories_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudApigeeV1ListApiCategoriesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudApigeeV1ListApiCategoriesResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/organizations/{organizationsId}/sites/{sitesId}/apicategories
/// Returns the API categories associated with a portal.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `apigee_organizations_sites_apicategories_list_builder()` + `apigee_organizations_sites_apicategories_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_sites_apicategories_list(
    client: &SimpleHttpClient,
    parent: &str,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudApigeeV1ListApiCategoriesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = apigee_organizations_sites_apicategories_list_builder(client, parent)?;
    apigee_organizations_sites_apicategories_list_execute(builder)
}

/// GET v1/organizations/{organizationsId}/sites/{sitesId}/apicategories/{apicategoriesId}
/// Updates an API category.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `apigee_organizations_sites_apicategories_patch_execute()` to send, or `apigee_organizations_sites_apicategories_patch` for simplest API.

pub fn apigee_organizations_sites_apicategories_patch_builder(
    client: &SimpleHttpClient,
    name: &str,
    body: &GoogleCloudApigeeV1ApiCategory,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://apigee.googleapis.com/v1/organizations/{}/sites/{}/apicategories/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/organizations/{organizationsId}/sites/{sitesId}/apicategories/{apicategoriesId}
/// Updates an API category.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `apigee_organizations_sites_apicategories_patch_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from apigee_organizations_sites_apicategories_patch_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_sites_apicategories_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudApigeeV1ApiCategoryResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudApigeeV1ApiCategoryResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/organizations/{organizationsId}/sites/{sitesId}/apicategories/{apicategoriesId}
/// Updates an API category.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `apigee_organizations_sites_apicategories_patch_builder()` + `apigee_organizations_sites_apicategories_patch_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_sites_apicategories_patch(
    client: &SimpleHttpClient,
    name: &str,
    body: &GoogleCloudApigeeV1ApiCategory,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudApigeeV1ApiCategoryResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = apigee_organizations_sites_apicategories_patch_builder(client, name, body)?;
    apigee_organizations_sites_apicategories_patch_execute(builder)
}

/// GET v1/organizations/{organizationsId}/sites/{sitesId}/apidocs
/// Creates a new catalog item.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `apigee_organizations_sites_apidocs_create_execute()` to send, or `apigee_organizations_sites_apidocs_create` for simplest API.

pub fn apigee_organizations_sites_apidocs_create_builder(
    client: &SimpleHttpClient,
    parent: &str,
    body: &GoogleCloudApigeeV1ApiDoc,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://apigee.googleapis.com/v1/organizations/{}/sites/{}/apidocs",
        parent,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/organizations/{organizationsId}/sites/{sitesId}/apidocs
/// Creates a new catalog item.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `apigee_organizations_sites_apidocs_create_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from apigee_organizations_sites_apidocs_create_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_sites_apidocs_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudApigeeV1ApiDocResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudApigeeV1ApiDocResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/organizations/{organizationsId}/sites/{sitesId}/apidocs
/// Creates a new catalog item.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `apigee_organizations_sites_apidocs_create_builder()` + `apigee_organizations_sites_apidocs_create_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_sites_apidocs_create(
    client: &SimpleHttpClient,
    parent: &str,
    body: &GoogleCloudApigeeV1ApiDoc,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudApigeeV1ApiDocResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = apigee_organizations_sites_apidocs_create_builder(client, parent, body)?;
    apigee_organizations_sites_apidocs_create_execute(builder)
}

/// GET v1/organizations/{organizationsId}/sites/{sitesId}/apidocs/{apidocsId}
/// Deletes a catalog item.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `apigee_organizations_sites_apidocs_delete_execute()` to send, or `apigee_organizations_sites_apidocs_delete` for simplest API.

pub fn apigee_organizations_sites_apidocs_delete_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://apigee.googleapis.com/v1/organizations/{}/sites/{}/apidocs/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/organizations/{organizationsId}/sites/{sitesId}/apidocs/{apidocsId}
/// Deletes a catalog item.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `apigee_organizations_sites_apidocs_delete_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from apigee_organizations_sites_apidocs_delete_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_sites_apidocs_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudApigeeV1DeleteResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudApigeeV1DeleteResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/organizations/{organizationsId}/sites/{sitesId}/apidocs/{apidocsId}
/// Deletes a catalog item.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `apigee_organizations_sites_apidocs_delete_builder()` + `apigee_organizations_sites_apidocs_delete_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_sites_apidocs_delete(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudApigeeV1DeleteResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = apigee_organizations_sites_apidocs_delete_builder(client, name)?;
    apigee_organizations_sites_apidocs_delete_execute(builder)
}

/// GET v1/organizations/{organizationsId}/sites/{sitesId}/apidocs/{apidocsId}
/// Gets a catalog item.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `apigee_organizations_sites_apidocs_get_execute()` to send, or `apigee_organizations_sites_apidocs_get` for simplest API.

pub fn apigee_organizations_sites_apidocs_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://apigee.googleapis.com/v1/organizations/{}/sites/{}/apidocs/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/organizations/{organizationsId}/sites/{sitesId}/apidocs/{apidocsId}
/// Gets a catalog item.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `apigee_organizations_sites_apidocs_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from apigee_organizations_sites_apidocs_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_sites_apidocs_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudApigeeV1ApiDocResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudApigeeV1ApiDocResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/organizations/{organizationsId}/sites/{sitesId}/apidocs/{apidocsId}
/// Gets a catalog item.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `apigee_organizations_sites_apidocs_get_builder()` + `apigee_organizations_sites_apidocs_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_sites_apidocs_get(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudApigeeV1ApiDocResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = apigee_organizations_sites_apidocs_get_builder(client, name)?;
    apigee_organizations_sites_apidocs_get_execute(builder)
}

/// GET v1/organizations/{organizationsId}/sites/{sitesId}/apidocs/{apidocsId}/documentation
/// Gets the documentation for the specified catalog item.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `apigee_organizations_sites_apidocs_get_documentation_execute()` to send, or `apigee_organizations_sites_apidocs_get_documentation` for simplest API.

pub fn apigee_organizations_sites_apidocs_get_documentation_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://apigee.googleapis.com/v1/organizations/{}/sites/{}/apidocs/{}/documentation",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/organizations/{organizationsId}/sites/{sitesId}/apidocs/{apidocsId}/documentation
/// Gets the documentation for the specified catalog item.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `apigee_organizations_sites_apidocs_get_documentation_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from apigee_organizations_sites_apidocs_get_documentation_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_sites_apidocs_get_documentation_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudApigeeV1ApiDocDocumentationResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudApigeeV1ApiDocDocumentationResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/organizations/{organizationsId}/sites/{sitesId}/apidocs/{apidocsId}/documentation
/// Gets the documentation for the specified catalog item.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `apigee_organizations_sites_apidocs_get_documentation_builder()` + `apigee_organizations_sites_apidocs_get_documentation_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_sites_apidocs_get_documentation(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudApigeeV1ApiDocDocumentationResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = apigee_organizations_sites_apidocs_get_documentation_builder(client, name)?;
    apigee_organizations_sites_apidocs_get_documentation_execute(builder)
}

/// GET v1/organizations/{organizationsId}/sites/{sitesId}/apidocs
/// Returns the catalog items associated with a portal.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `apigee_organizations_sites_apidocs_list_execute()` to send, or `apigee_organizations_sites_apidocs_list` for simplest API.

pub fn apigee_organizations_sites_apidocs_list_builder(
    client: &SimpleHttpClient,
    parent: &str,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://apigee.googleapis.com/v1/organizations/{}/sites/{}/apidocs",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/organizations/{organizationsId}/sites/{sitesId}/apidocs
/// Returns the catalog items associated with a portal.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `apigee_organizations_sites_apidocs_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from apigee_organizations_sites_apidocs_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_sites_apidocs_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudApigeeV1ListApiDocsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudApigeeV1ListApiDocsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/organizations/{organizationsId}/sites/{sitesId}/apidocs
/// Returns the catalog items associated with a portal.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `apigee_organizations_sites_apidocs_list_builder()` + `apigee_organizations_sites_apidocs_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_sites_apidocs_list(
    client: &SimpleHttpClient,
    parent: &str,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudApigeeV1ListApiDocsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        apigee_organizations_sites_apidocs_list_builder(client, parent, pageSize, pageToken)?;
    apigee_organizations_sites_apidocs_list_execute(builder)
}

/// GET v1/organizations/{organizationsId}/sites/{sitesId}/apidocs/{apidocsId}
/// Updates a catalog item.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `apigee_organizations_sites_apidocs_update_execute()` to send, or `apigee_organizations_sites_apidocs_update` for simplest API.

pub fn apigee_organizations_sites_apidocs_update_builder(
    client: &SimpleHttpClient,
    name: &str,
    body: &GoogleCloudApigeeV1ApiDoc,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://apigee.googleapis.com/v1/organizations/{}/sites/{}/apidocs/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/organizations/{organizationsId}/sites/{sitesId}/apidocs/{apidocsId}
/// Updates a catalog item.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `apigee_organizations_sites_apidocs_update_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from apigee_organizations_sites_apidocs_update_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_sites_apidocs_update_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudApigeeV1ApiDocResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudApigeeV1ApiDocResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/organizations/{organizationsId}/sites/{sitesId}/apidocs/{apidocsId}
/// Updates a catalog item.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `apigee_organizations_sites_apidocs_update_builder()` + `apigee_organizations_sites_apidocs_update_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_sites_apidocs_update(
    client: &SimpleHttpClient,
    name: &str,
    body: &GoogleCloudApigeeV1ApiDoc,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudApigeeV1ApiDocResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = apigee_organizations_sites_apidocs_update_builder(client, name, body)?;
    apigee_organizations_sites_apidocs_update_execute(builder)
}

/// GET v1/organizations/{organizationsId}/sites/{sitesId}/apidocs/{apidocsId}/documentation
/// Updates the documentation for the specified catalog item. Note that the documentation file contents will not be populated in the return message.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `apigee_organizations_sites_apidocs_update_documentation_execute()` to send, or `apigee_organizations_sites_apidocs_update_documentation` for simplest API.

pub fn apigee_organizations_sites_apidocs_update_documentation_builder(
    client: &SimpleHttpClient,
    name: &str,
    body: &GoogleCloudApigeeV1ApiDocDocumentation,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://apigee.googleapis.com/v1/organizations/{}/sites/{}/apidocs/{}/documentation",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/organizations/{organizationsId}/sites/{sitesId}/apidocs/{apidocsId}/documentation
/// Updates the documentation for the specified catalog item. Note that the documentation file contents will not be populated in the return message.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `apigee_organizations_sites_apidocs_update_documentation_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from apigee_organizations_sites_apidocs_update_documentation_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_sites_apidocs_update_documentation_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudApigeeV1ApiDocDocumentationResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudApigeeV1ApiDocDocumentationResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/organizations/{organizationsId}/sites/{sitesId}/apidocs/{apidocsId}/documentation
/// Updates the documentation for the specified catalog item. Note that the documentation file contents will not be populated in the return message.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `apigee_organizations_sites_apidocs_update_documentation_builder()` + `apigee_organizations_sites_apidocs_update_documentation_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_sites_apidocs_update_documentation(
    client: &SimpleHttpClient,
    name: &str,
    body: &GoogleCloudApigeeV1ApiDocDocumentation,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudApigeeV1ApiDocDocumentationResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        apigee_organizations_sites_apidocs_update_documentation_builder(client, name, body)?;
    apigee_organizations_sites_apidocs_update_documentation_execute(builder)
}

/// GET v1/organizations/{organizationsId}/spaces
/// Create a space under an organization.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `apigee_organizations_spaces_create_execute()` to send, or `apigee_organizations_spaces_create` for simplest API.

pub fn apigee_organizations_spaces_create_builder(
    client: &SimpleHttpClient,
    parent: &str,
    spaceId: Option<&str>,
    body: &GoogleCloudApigeeV1Space,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://apigee.googleapis.com/v1/organizations/{}/spaces",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = spaceId {
        query_parts.push(format!("spaceId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/organizations/{organizationsId}/spaces
/// Create a space under an organization.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `apigee_organizations_spaces_create_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from apigee_organizations_spaces_create_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_spaces_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleCloudApigeeV1Space>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudApigeeV1Space = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/organizations/{organizationsId}/spaces
/// Create a space under an organization.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `apigee_organizations_spaces_create_builder()` + `apigee_organizations_spaces_create_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_spaces_create(
    client: &SimpleHttpClient,
    parent: &str,
    spaceId: Option<&str>,
    body: &GoogleCloudApigeeV1Space,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleCloudApigeeV1Space>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = apigee_organizations_spaces_create_builder(client, parent, spaceId, body)?;
    apigee_organizations_spaces_create_execute(builder)
}

/// GET v1/organizations/{organizationsId}/spaces/{spacesId}
/// Deletes an organization space.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `apigee_organizations_spaces_delete_execute()` to send, or `apigee_organizations_spaces_delete` for simplest API.

pub fn apigee_organizations_spaces_delete_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://apigee.googleapis.com/v1/organizations/{}/spaces/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/organizations/{organizationsId}/spaces/{spacesId}
/// Deletes an organization space.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `apigee_organizations_spaces_delete_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from apigee_organizations_spaces_delete_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_spaces_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleProtobufEmpty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/organizations/{organizationsId}/spaces/{spacesId}
/// Deletes an organization space.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `apigee_organizations_spaces_delete_builder()` + `apigee_organizations_spaces_delete_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_spaces_delete(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = apigee_organizations_spaces_delete_builder(client, name)?;
    apigee_organizations_spaces_delete_execute(builder)
}

/// GET v1/organizations/{organizationsId}/spaces/{spacesId}
/// Get a space under an Organization.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `apigee_organizations_spaces_get_execute()` to send, or `apigee_organizations_spaces_get` for simplest API.

pub fn apigee_organizations_spaces_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://apigee.googleapis.com/v1/organizations/{}/spaces/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/organizations/{organizationsId}/spaces/{spacesId}
/// Get a space under an Organization.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `apigee_organizations_spaces_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from apigee_organizations_spaces_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_spaces_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleCloudApigeeV1Space>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudApigeeV1Space = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/organizations/{organizationsId}/spaces/{spacesId}
/// Get a space under an Organization.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `apigee_organizations_spaces_get_builder()` + `apigee_organizations_spaces_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_spaces_get(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleCloudApigeeV1Space>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = apigee_organizations_spaces_get_builder(client, name)?;
    apigee_organizations_spaces_get_execute(builder)
}

/// GET v1/organizations/{organizationsId}/spaces/{spacesId}:getIamPolicy
/// Callers must have apigee.spaces.`getIamPolicy`.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `apigee_organizations_spaces_get_iam_policy_execute()` to send, or `apigee_organizations_spaces_get_iam_policy` for simplest API.

pub fn apigee_organizations_spaces_get_iam_policy_builder(
    client: &SimpleHttpClient,
    resource: &str,
    options_requestedPolicyVersion: Option<i32>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://apigee.googleapis.com/v1/organizations/{}/spaces/{}:getIamPolicy",
        resource,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = options_requestedPolicyVersion {
        query_parts.push(format!("options_requestedPolicyVersion={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/organizations/{organizationsId}/spaces/{spacesId}:getIamPolicy
/// Callers must have apigee.spaces.`getIamPolicy`.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `apigee_organizations_spaces_get_iam_policy_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from apigee_organizations_spaces_get_iam_policy_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_spaces_get_iam_policy_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleIamV1Policy>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleIamV1Policy = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/organizations/{organizationsId}/spaces/{spacesId}:getIamPolicy
/// Callers must have apigee.spaces.`getIamPolicy`.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `apigee_organizations_spaces_get_iam_policy_builder()` + `apigee_organizations_spaces_get_iam_policy_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_spaces_get_iam_policy(
    client: &SimpleHttpClient,
    resource: &str,
    options_requestedPolicyVersion: Option<i32>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleIamV1Policy>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = apigee_organizations_spaces_get_iam_policy_builder(
        client,
        resource,
        options_requestedPolicyVersion,
    )?;
    apigee_organizations_spaces_get_iam_policy_execute(builder)
}

/// GET v1/organizations/{organizationsId}/spaces
/// Lists spaces under an organization.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `apigee_organizations_spaces_list_execute()` to send, or `apigee_organizations_spaces_list` for simplest API.

pub fn apigee_organizations_spaces_list_builder(
    client: &SimpleHttpClient,
    parent: &str,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://apigee.googleapis.com/v1/organizations/{}/spaces",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/organizations/{organizationsId}/spaces
/// Lists spaces under an organization.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `apigee_organizations_spaces_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from apigee_organizations_spaces_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_spaces_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudApigeeV1ListSpacesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudApigeeV1ListSpacesResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/organizations/{organizationsId}/spaces
/// Lists spaces under an organization.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `apigee_organizations_spaces_list_builder()` + `apigee_organizations_spaces_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_spaces_list(
    client: &SimpleHttpClient,
    parent: &str,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudApigeeV1ListSpacesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = apigee_organizations_spaces_list_builder(client, parent, pageSize, pageToken)?;
    apigee_organizations_spaces_list_execute(builder)
}

/// GET v1/organizations/{organizationsId}/spaces/{spacesId}
/// Updates a space.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `apigee_organizations_spaces_patch_execute()` to send, or `apigee_organizations_spaces_patch` for simplest API.

pub fn apigee_organizations_spaces_patch_builder(
    client: &SimpleHttpClient,
    name: &str,
    updateMask: Option<&str>,
    body: &GoogleCloudApigeeV1Space,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://apigee.googleapis.com/v1/organizations/{}/spaces/{}",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = updateMask {
        query_parts.push(format!("updateMask={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/organizations/{organizationsId}/spaces/{spacesId}
/// Updates a space.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `apigee_organizations_spaces_patch_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from apigee_organizations_spaces_patch_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_spaces_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleCloudApigeeV1Space>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudApigeeV1Space = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/organizations/{organizationsId}/spaces/{spacesId}
/// Updates a space.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `apigee_organizations_spaces_patch_builder()` + `apigee_organizations_spaces_patch_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_spaces_patch(
    client: &SimpleHttpClient,
    name: &str,
    updateMask: Option<&str>,
    body: &GoogleCloudApigeeV1Space,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleCloudApigeeV1Space>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = apigee_organizations_spaces_patch_builder(client, name, updateMask, body)?;
    apigee_organizations_spaces_patch_execute(builder)
}

/// GET v1/organizations/{organizationsId}/spaces/{spacesId}:setIamPolicy
/// IAM META APIs Callers must have apigee.spaces.`setIamPolicy`.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `apigee_organizations_spaces_set_iam_policy_execute()` to send, or `apigee_organizations_spaces_set_iam_policy` for simplest API.

pub fn apigee_organizations_spaces_set_iam_policy_builder(
    client: &SimpleHttpClient,
    resource: &str,
    body: &GoogleIamV1SetIamPolicyRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://apigee.googleapis.com/v1/organizations/{}/spaces/{}:setIamPolicy",
        resource,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/organizations/{organizationsId}/spaces/{spacesId}:setIamPolicy
/// IAM META APIs Callers must have apigee.spaces.`setIamPolicy`.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `apigee_organizations_spaces_set_iam_policy_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from apigee_organizations_spaces_set_iam_policy_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_spaces_set_iam_policy_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleIamV1Policy>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleIamV1Policy = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/organizations/{organizationsId}/spaces/{spacesId}:setIamPolicy
/// IAM META APIs Callers must have apigee.spaces.`setIamPolicy`.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `apigee_organizations_spaces_set_iam_policy_builder()` + `apigee_organizations_spaces_set_iam_policy_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_spaces_set_iam_policy(
    client: &SimpleHttpClient,
    resource: &str,
    body: &GoogleIamV1SetIamPolicyRequest,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleIamV1Policy>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = apigee_organizations_spaces_set_iam_policy_builder(client, resource, body)?;
    apigee_organizations_spaces_set_iam_policy_execute(builder)
}

/// GET v1/organizations/{organizationsId}/spaces/{spacesId}:testIamPermissions
/// Callers don't need any permissions.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `apigee_organizations_spaces_test_iam_permissions_execute()` to send, or `apigee_organizations_spaces_test_iam_permissions` for simplest API.

pub fn apigee_organizations_spaces_test_iam_permissions_builder(
    client: &SimpleHttpClient,
    resource: &str,
    body: &GoogleIamV1TestIamPermissionsRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://apigee.googleapis.com/v1/organizations/{}/spaces/{}:testIamPermissions",
        resource,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/organizations/{organizationsId}/spaces/{spacesId}:testIamPermissions
/// Callers don't need any permissions.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `apigee_organizations_spaces_test_iam_permissions_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from apigee_organizations_spaces_test_iam_permissions_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_spaces_test_iam_permissions_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleIamV1TestIamPermissionsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleIamV1TestIamPermissionsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/organizations/{organizationsId}/spaces/{spacesId}:testIamPermissions
/// Callers don't need any permissions.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `apigee_organizations_spaces_test_iam_permissions_builder()` + `apigee_organizations_spaces_test_iam_permissions_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_spaces_test_iam_permissions(
    client: &SimpleHttpClient,
    resource: &str,
    body: &GoogleIamV1TestIamPermissionsRequest,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleIamV1TestIamPermissionsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = apigee_organizations_spaces_test_iam_permissions_builder(client, resource, body)?;
    apigee_organizations_spaces_test_iam_permissions_execute(builder)
}

/// GET v1/projects/{projectsId}:provisionOrganization
/// Provisions a new Apigee organization with a functioning runtime. This is the standard way to create trial organizations for a free Apigee trial.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `apigee_projects_provision_organization_execute()` to send, or `apigee_projects_provision_organization` for simplest API.

pub fn apigee_projects_provision_organization_builder(
    client: &SimpleHttpClient,
    project: &str,
    body: &GoogleCloudApigeeV1ProvisionOrganizationRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://apigee.googleapis.com/v1/projects/{}:provisionOrganization",
        project,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}:provisionOrganization
/// Provisions a new Apigee organization with a functioning runtime. This is the standard way to create trial organizations for a free Apigee trial.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `apigee_projects_provision_organization_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from apigee_projects_provision_organization_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_projects_provision_organization_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}:provisionOrganization
/// Provisions a new Apigee organization with a functioning runtime. This is the standard way to create trial organizations for a free Apigee trial.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `apigee_projects_provision_organization_builder()` + `apigee_projects_provision_organization_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_projects_provision_organization(
    client: &SimpleHttpClient,
    project: &str,
    body: &GoogleCloudApigeeV1ProvisionOrganizationRequest,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = apigee_projects_provision_organization_builder(client, project, body)?;
    apigee_projects_provision_organization_execute(builder)
}
