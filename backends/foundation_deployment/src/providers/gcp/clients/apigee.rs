//! Auto-generated API clients for gcp/apigee.
//!
//! Generated by `cargo run --bin ewe_platform gen_provider_clients`.
//! DO NOT EDIT MANUALLY.
//!
//! Feature flag: `gcp`

#![cfg(feature = "gcp")]


use crate::providers::gcp::clients::types::*;
use crate::providers::gcp::resources::*;
use foundation_core::valtron::{
    execute, BoxedSendExecutionAction, StreamIterator, StreamIteratorExt, TaskIterator,
    TaskIteratorExt,
};
use foundation_core::wire::simple_http::client::{
    body_reader, ClientRequestBuilder, RequestIntro, SimpleHttpClient, SystemDnsResolver,
};
use foundation_macros::JsonHash;
use serde::Serialize;

/// GET v1/organizations
/// Creates an Apigee organization. See [Create an Apigee organization](<https://cloud.google.`com/apigee/docs/api-platform/get-started/create-org`>).
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `apigee_organizations_create_execute()` to send, or `apigee_organizations_create` for simplest API.

pub fn apigee_organizations_create_builder(
    client: &SimpleHttpClient,
    parent: &Option<String>,
    body: &GoogleCloudApigeeV1Organization,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!("https://apigee.googleapis.com/v1/organizations",);

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = parent.as_ref() {
        query_parts.push(format!("parent={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/organizations
/// Creates an Apigee organization. See [Create an Apigee organization](<https://cloud.google.`com/apigee/docs/api-platform/get-started/create-org`>).
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `apigee_organizations_create_execute()` or `apigee_organizations_create`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `apigee_organizations_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_create_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/organizations
/// Creates an Apigee organization. See [Create an Apigee organization](<https://cloud.google.`com/apigee/docs/api-platform/get-started/create-org`>).
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `apigee_organizations_create_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `apigee_organizations_create_task()`.
/// For the simplest API, use `apigee_organizations_create()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `apigee_organizations_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn apigee_organizations_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = apigee_organizations_create_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`apigee_organizations_create`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ApigeeOrganizationsCreateArgs {
    /// Query parameter: parent
    pub parent: Option<String>,
    /// Request body.
    pub body: GoogleCloudApigeeV1Organization,
}

/// GET v1/organizations
/// Creates an Apigee organization. See [Create an Apigee organization](<https://cloud.google.`com/apigee/docs/api-platform/get-started/create-org`>).
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `apigee_organizations_create_builder()` + `apigee_organizations_create_execute()`.
/// For task-level control, use `apigee_organizations_create_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_create(
    client: &SimpleHttpClient,
    args: &ApigeeOrganizationsCreateArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = apigee_organizations_create_builder(client, &args.parent, &args.body)?;
    apigee_organizations_create_execute(builder)
}

/// GET v1/organizations/{organizationsId}
/// Delete an Apigee organization. For organizations with BillingType EVALUATION, an immediate deletion is performed. For paid organizations (Subscription or Pay-as-you-go), a soft-deletion is performed. The organization can be restored within the soft-deletion period, which is specified using the retention field in the request or by filing a support ticket with Apigee. During the data retention period specified in the request, the Apigee organization cannot be recreated in the same Google Cloud project. **IMPORTANT: The default data retention setting for this operation is 7 days. To permanently delete the organization in 24 hours, set the retention parameter to MINIMUM.**
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `apigee_organizations_delete_execute()` to send, or `apigee_organizations_delete` for simplest API.

pub fn apigee_organizations_delete_builder(
    client: &SimpleHttpClient,
    name: &String,
    retention: &Option<String>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!("https://apigee.googleapis.com/v1/organizations/{}",);

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = retention.as_ref() {
        query_parts.push(format!("retention={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/organizations/{organizationsId}
/// Delete an Apigee organization. For organizations with BillingType EVALUATION, an immediate deletion is performed. For paid organizations (Subscription or Pay-as-you-go), a soft-deletion is performed. The organization can be restored within the soft-deletion period, which is specified using the retention field in the request or by filing a support ticket with Apigee. During the data retention period specified in the request, the Apigee organization cannot be recreated in the same Google Cloud project. **IMPORTANT: The default data retention setting for this operation is 7 days. To permanently delete the organization in 24 hours, set the retention parameter to MINIMUM.**
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `apigee_organizations_delete_execute()` or `apigee_organizations_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `apigee_organizations_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/organizations/{organizationsId}
/// Delete an Apigee organization. For organizations with BillingType EVALUATION, an immediate deletion is performed. For paid organizations (Subscription or Pay-as-you-go), a soft-deletion is performed. The organization can be restored within the soft-deletion period, which is specified using the retention field in the request or by filing a support ticket with Apigee. During the data retention period specified in the request, the Apigee organization cannot be recreated in the same Google Cloud project. **IMPORTANT: The default data retention setting for this operation is 7 days. To permanently delete the organization in 24 hours, set the retention parameter to MINIMUM.**
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `apigee_organizations_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `apigee_organizations_delete_task()`.
/// For the simplest API, use `apigee_organizations_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `apigee_organizations_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn apigee_organizations_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = apigee_organizations_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`apigee_organizations_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ApigeeOrganizationsDeleteArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: retention
    pub retention: Option<String>,
}

/// GET v1/organizations/{organizationsId}
/// Delete an Apigee organization. For organizations with BillingType EVALUATION, an immediate deletion is performed. For paid organizations (Subscription or Pay-as-you-go), a soft-deletion is performed. The organization can be restored within the soft-deletion period, which is specified using the retention field in the request or by filing a support ticket with Apigee. During the data retention period specified in the request, the Apigee organization cannot be recreated in the same Google Cloud project. **IMPORTANT: The default data retention setting for this operation is 7 days. To permanently delete the organization in 24 hours, set the retention parameter to MINIMUM.**
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `apigee_organizations_delete_builder()` + `apigee_organizations_delete_execute()`.
/// For task-level control, use `apigee_organizations_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_delete(
    client: &SimpleHttpClient,
    args: &ApigeeOrganizationsDeleteArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = apigee_organizations_delete_builder(client, &args.name, &args.retention)?;
    apigee_organizations_delete_execute(builder)
}

/// GET v1/organizations/{organizationsId}/controlPlaneAccess
/// Lists the service accounts allowed to access Apigee control plane directly for limited functionality. **Note**: Available to Apigee hybrid only.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `apigee_organizations_get_control_plane_access_execute()` to send, or `apigee_organizations_get_control_plane_access` for simplest API.

pub fn apigee_organizations_get_control_plane_access_builder(
    client: &SimpleHttpClient,
    name: &String,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url =
        format!("https://apigee.googleapis.com/v1/organizations/{}/controlPlaneAccess",);

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/organizations/{organizationsId}/controlPlaneAccess
/// Lists the service accounts allowed to access Apigee control plane directly for limited functionality. **Note**: Available to Apigee hybrid only.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `apigee_organizations_get_control_plane_access_execute()` or `apigee_organizations_get_control_plane_access`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `apigee_organizations_get_control_plane_access_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_get_control_plane_access_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleCloudApigeeV1ControlPlaneAccess>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudApigeeV1ControlPlaneAccess = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/organizations/{organizationsId}/controlPlaneAccess
/// Lists the service accounts allowed to access Apigee control plane directly for limited functionality. **Note**: Available to Apigee hybrid only.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `apigee_organizations_get_control_plane_access_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `apigee_organizations_get_control_plane_access_task()`.
/// For the simplest API, use `apigee_organizations_get_control_plane_access()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `apigee_organizations_get_control_plane_access_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn apigee_organizations_get_control_plane_access_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudApigeeV1ControlPlaneAccess>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = apigee_organizations_get_control_plane_access_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`apigee_organizations_get_control_plane_access`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ApigeeOrganizationsGetControlPlaneAccessArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/organizations/{organizationsId}/controlPlaneAccess
/// Lists the service accounts allowed to access Apigee control plane directly for limited functionality. **Note**: Available to Apigee hybrid only.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `apigee_organizations_get_control_plane_access_builder()` + `apigee_organizations_get_control_plane_access_execute()`.
/// For task-level control, use `apigee_organizations_get_control_plane_access_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_get_control_plane_access(
    client: &SimpleHttpClient,
    args: &ApigeeOrganizationsGetControlPlaneAccessArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudApigeeV1ControlPlaneAccess>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = apigee_organizations_get_control_plane_access_builder(client, &args.name)?;
    apigee_organizations_get_control_plane_access_execute(builder)
}

/// GET v1/organizations/{organizationsId}/deployedIngressConfig
/// Gets the deployed ingress configuration for an organization.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `apigee_organizations_get_deployed_ingress_config_execute()` to send, or `apigee_organizations_get_deployed_ingress_config` for simplest API.

pub fn apigee_organizations_get_deployed_ingress_config_builder(
    client: &SimpleHttpClient,
    name: &String,
    view: &Option<String>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url =
        format!("https://apigee.googleapis.com/v1/organizations/{}/deployedIngressConfig",);

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = view.as_ref() {
        query_parts.push(format!("view={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/organizations/{organizationsId}/deployedIngressConfig
/// Gets the deployed ingress configuration for an organization.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `apigee_organizations_get_deployed_ingress_config_execute()` or `apigee_organizations_get_deployed_ingress_config`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `apigee_organizations_get_deployed_ingress_config_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_get_deployed_ingress_config_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleCloudApigeeV1IngressConfig>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudApigeeV1IngressConfig = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/organizations/{organizationsId}/deployedIngressConfig
/// Gets the deployed ingress configuration for an organization.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `apigee_organizations_get_deployed_ingress_config_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `apigee_organizations_get_deployed_ingress_config_task()`.
/// For the simplest API, use `apigee_organizations_get_deployed_ingress_config()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `apigee_organizations_get_deployed_ingress_config_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn apigee_organizations_get_deployed_ingress_config_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudApigeeV1IngressConfig>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = apigee_organizations_get_deployed_ingress_config_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`apigee_organizations_get_deployed_ingress_config`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ApigeeOrganizationsGetDeployedIngressConfigArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: view
    pub view: Option<String>,
}

/// GET v1/organizations/{organizationsId}/deployedIngressConfig
/// Gets the deployed ingress configuration for an organization.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `apigee_organizations_get_deployed_ingress_config_builder()` + `apigee_organizations_get_deployed_ingress_config_execute()`.
/// For task-level control, use `apigee_organizations_get_deployed_ingress_config_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_get_deployed_ingress_config(
    client: &SimpleHttpClient,
    args: &ApigeeOrganizationsGetDeployedIngressConfigArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudApigeeV1IngressConfig>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        apigee_organizations_get_deployed_ingress_config_builder(client, &args.name, &args.view)?;
    apigee_organizations_get_deployed_ingress_config_execute(builder)
}

/// GET v1/organizations/{organizationsId}:getProjectMapping
/// Gets the project ID and region for an Apigee organization.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `apigee_organizations_get_project_mapping_execute()` to send, or `apigee_organizations_get_project_mapping` for simplest API.

pub fn apigee_organizations_get_project_mapping_builder(
    client: &SimpleHttpClient,
    name: &String,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url =
        format!("https://apigee.googleapis.com/v1/organizations/{}:getProjectMapping",);

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/organizations/{organizationsId}:getProjectMapping
/// Gets the project ID and region for an Apigee organization.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `apigee_organizations_get_project_mapping_execute()` or `apigee_organizations_get_project_mapping`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `apigee_organizations_get_project_mapping_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_get_project_mapping_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleCloudApigeeV1OrganizationProjectMapping>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudApigeeV1OrganizationProjectMapping =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/organizations/{organizationsId}:getProjectMapping
/// Gets the project ID and region for an Apigee organization.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `apigee_organizations_get_project_mapping_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `apigee_organizations_get_project_mapping_task()`.
/// For the simplest API, use `apigee_organizations_get_project_mapping()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `apigee_organizations_get_project_mapping_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn apigee_organizations_get_project_mapping_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudApigeeV1OrganizationProjectMapping>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = apigee_organizations_get_project_mapping_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`apigee_organizations_get_project_mapping`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ApigeeOrganizationsGetProjectMappingArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/organizations/{organizationsId}:getProjectMapping
/// Gets the project ID and region for an Apigee organization.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `apigee_organizations_get_project_mapping_builder()` + `apigee_organizations_get_project_mapping_execute()`.
/// For task-level control, use `apigee_organizations_get_project_mapping_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_get_project_mapping(
    client: &SimpleHttpClient,
    args: &ApigeeOrganizationsGetProjectMappingArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudApigeeV1OrganizationProjectMapping>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = apigee_organizations_get_project_mapping_builder(client, &args.name)?;
    apigee_organizations_get_project_mapping_execute(builder)
}

/// GET v1/organizations/{organizationsId}/runtimeConfig
/// Get runtime config for an organization.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `apigee_organizations_get_runtime_config_execute()` to send, or `apigee_organizations_get_runtime_config` for simplest API.

pub fn apigee_organizations_get_runtime_config_builder(
    client: &SimpleHttpClient,
    name: &String,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!("https://apigee.googleapis.com/v1/organizations/{}/runtimeConfig",);

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/organizations/{organizationsId}/runtimeConfig
/// Get runtime config for an organization.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `apigee_organizations_get_runtime_config_execute()` or `apigee_organizations_get_runtime_config`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `apigee_organizations_get_runtime_config_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_get_runtime_config_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleCloudApigeeV1RuntimeConfig>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudApigeeV1RuntimeConfig = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/organizations/{organizationsId}/runtimeConfig
/// Get runtime config for an organization.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `apigee_organizations_get_runtime_config_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `apigee_organizations_get_runtime_config_task()`.
/// For the simplest API, use `apigee_organizations_get_runtime_config()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `apigee_organizations_get_runtime_config_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn apigee_organizations_get_runtime_config_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudApigeeV1RuntimeConfig>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = apigee_organizations_get_runtime_config_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`apigee_organizations_get_runtime_config`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ApigeeOrganizationsGetRuntimeConfigArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/organizations/{organizationsId}/runtimeConfig
/// Get runtime config for an organization.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `apigee_organizations_get_runtime_config_builder()` + `apigee_organizations_get_runtime_config_execute()`.
/// For task-level control, use `apigee_organizations_get_runtime_config_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_get_runtime_config(
    client: &SimpleHttpClient,
    args: &ApigeeOrganizationsGetRuntimeConfigArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudApigeeV1RuntimeConfig>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = apigee_organizations_get_runtime_config_builder(client, &args.name)?;
    apigee_organizations_get_runtime_config_execute(builder)
}

/// GET v1/organizations/{organizationsId}/securitySettings
/// GetSecuritySettings gets the security settings for API Security.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `apigee_organizations_get_security_settings_execute()` to send, or `apigee_organizations_get_security_settings` for simplest API.

pub fn apigee_organizations_get_security_settings_builder(
    client: &SimpleHttpClient,
    name: &String,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url =
        format!("https://apigee.googleapis.com/v1/organizations/{}/securitySettings",);

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/organizations/{organizationsId}/securitySettings
/// GetSecuritySettings gets the security settings for API Security.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `apigee_organizations_get_security_settings_execute()` or `apigee_organizations_get_security_settings`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `apigee_organizations_get_security_settings_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_get_security_settings_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleCloudApigeeV1SecuritySettings>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudApigeeV1SecuritySettings = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/organizations/{organizationsId}/securitySettings
/// GetSecuritySettings gets the security settings for API Security.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `apigee_organizations_get_security_settings_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `apigee_organizations_get_security_settings_task()`.
/// For the simplest API, use `apigee_organizations_get_security_settings()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `apigee_organizations_get_security_settings_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn apigee_organizations_get_security_settings_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudApigeeV1SecuritySettings>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = apigee_organizations_get_security_settings_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`apigee_organizations_get_security_settings`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ApigeeOrganizationsGetSecuritySettingsArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/organizations/{organizationsId}/securitySettings
/// GetSecuritySettings gets the security settings for API Security.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `apigee_organizations_get_security_settings_builder()` + `apigee_organizations_get_security_settings_execute()`.
/// For task-level control, use `apigee_organizations_get_security_settings_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_get_security_settings(
    client: &SimpleHttpClient,
    args: &ApigeeOrganizationsGetSecuritySettingsArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudApigeeV1SecuritySettings>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = apigee_organizations_get_security_settings_builder(client, &args.name)?;
    apigee_organizations_get_security_settings_execute(builder)
}

/// GET v1/organizations/{organizationsId}:getSyncAuthorization
/// Lists the service accounts with the permissions required to allow the Synchronizer to download environment data from the control plane. An ETag is returned in the response to `getSyncAuthorization`. Pass that ETag when calling [`setSyncAuthorization`](`setSyncAuthorization`) to ensure that you are updating the correct version. If you don't pass the ETag in the call to `setSyncAuthorization`, then the existing authorization is overwritten indiscriminately. For more information, see [Configure the Synchronizer](<https://cloud.google.`com/apigee/docs/hybrid/latest/synchronizer-access`>). **Note**: Available to Apigee hybrid only.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `apigee_organizations_get_sync_authorization_execute()` to send, or `apigee_organizations_get_sync_authorization` for simplest API.

pub fn apigee_organizations_get_sync_authorization_builder(
    client: &SimpleHttpClient,
    name: &String,
    body: &GoogleCloudApigeeV1GetSyncAuthorizationRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url =
        format!("https://apigee.googleapis.com/v1/organizations/{}:getSyncAuthorization",);

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/organizations/{organizationsId}:getSyncAuthorization
/// Lists the service accounts with the permissions required to allow the Synchronizer to download environment data from the control plane. An ETag is returned in the response to `getSyncAuthorization`. Pass that ETag when calling [`setSyncAuthorization`](`setSyncAuthorization`) to ensure that you are updating the correct version. If you don't pass the ETag in the call to `setSyncAuthorization`, then the existing authorization is overwritten indiscriminately. For more information, see [Configure the Synchronizer](<https://cloud.google.`com/apigee/docs/hybrid/latest/synchronizer-access`>). **Note**: Available to Apigee hybrid only.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `apigee_organizations_get_sync_authorization_execute()` or `apigee_organizations_get_sync_authorization`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `apigee_organizations_get_sync_authorization_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_get_sync_authorization_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleCloudApigeeV1SyncAuthorization>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudApigeeV1SyncAuthorization = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/organizations/{organizationsId}:getSyncAuthorization
/// Lists the service accounts with the permissions required to allow the Synchronizer to download environment data from the control plane. An ETag is returned in the response to `getSyncAuthorization`. Pass that ETag when calling [`setSyncAuthorization`](`setSyncAuthorization`) to ensure that you are updating the correct version. If you don't pass the ETag in the call to `setSyncAuthorization`, then the existing authorization is overwritten indiscriminately. For more information, see [Configure the Synchronizer](<https://cloud.google.`com/apigee/docs/hybrid/latest/synchronizer-access`>). **Note**: Available to Apigee hybrid only.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `apigee_organizations_get_sync_authorization_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `apigee_organizations_get_sync_authorization_task()`.
/// For the simplest API, use `apigee_organizations_get_sync_authorization()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `apigee_organizations_get_sync_authorization_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn apigee_organizations_get_sync_authorization_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudApigeeV1SyncAuthorization>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = apigee_organizations_get_sync_authorization_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`apigee_organizations_get_sync_authorization`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ApigeeOrganizationsGetSyncAuthorizationArgs {
    /// Path parameter: name
    pub name: String,
    /// Request body.
    pub body: GoogleCloudApigeeV1GetSyncAuthorizationRequest,
}

/// GET v1/organizations/{organizationsId}:getSyncAuthorization
/// Lists the service accounts with the permissions required to allow the Synchronizer to download environment data from the control plane. An ETag is returned in the response to `getSyncAuthorization`. Pass that ETag when calling [`setSyncAuthorization`](`setSyncAuthorization`) to ensure that you are updating the correct version. If you don't pass the ETag in the call to `setSyncAuthorization`, then the existing authorization is overwritten indiscriminately. For more information, see [Configure the Synchronizer](<https://cloud.google.`com/apigee/docs/hybrid/latest/synchronizer-access`>). **Note**: Available to Apigee hybrid only.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `apigee_organizations_get_sync_authorization_builder()` + `apigee_organizations_get_sync_authorization_execute()`.
/// For task-level control, use `apigee_organizations_get_sync_authorization_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_get_sync_authorization(
    client: &SimpleHttpClient,
    args: &ApigeeOrganizationsGetSyncAuthorizationArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudApigeeV1SyncAuthorization>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        apigee_organizations_get_sync_authorization_builder(client, &args.name, &args.body)?;
    apigee_organizations_get_sync_authorization_execute(builder)
}

/// GET v1/organizations/{organizationsId}:setAddons
/// Configures the add-ons for the Apigee organization. The existing add-on configuration will be fully replaced.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `apigee_organizations_set_addons_execute()` to send, or `apigee_organizations_set_addons` for simplest API.

pub fn apigee_organizations_set_addons_builder(
    client: &SimpleHttpClient,
    org: &String,
    body: &GoogleCloudApigeeV1SetAddonsRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!("https://apigee.googleapis.com/v1/organizations/{}:setAddons",);

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/organizations/{organizationsId}:setAddons
/// Configures the add-ons for the Apigee organization. The existing add-on configuration will be fully replaced.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `apigee_organizations_set_addons_execute()` or `apigee_organizations_set_addons`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `apigee_organizations_set_addons_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_set_addons_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/organizations/{organizationsId}:setAddons
/// Configures the add-ons for the Apigee organization. The existing add-on configuration will be fully replaced.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `apigee_organizations_set_addons_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `apigee_organizations_set_addons_task()`.
/// For the simplest API, use `apigee_organizations_set_addons()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `apigee_organizations_set_addons_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn apigee_organizations_set_addons_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = apigee_organizations_set_addons_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`apigee_organizations_set_addons`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ApigeeOrganizationsSetAddonsArgs {
    /// Path parameter: org
    pub org: String,
    /// Request body.
    pub body: GoogleCloudApigeeV1SetAddonsRequest,
}

/// GET v1/organizations/{organizationsId}:setAddons
/// Configures the add-ons for the Apigee organization. The existing add-on configuration will be fully replaced.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `apigee_organizations_set_addons_builder()` + `apigee_organizations_set_addons_execute()`.
/// For task-level control, use `apigee_organizations_set_addons_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_set_addons(
    client: &SimpleHttpClient,
    args: &ApigeeOrganizationsSetAddonsArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = apigee_organizations_set_addons_builder(client, &args.org, &args.body)?;
    apigee_organizations_set_addons_execute(builder)
}

/// GET v1/organizations/{organizationsId}:setSyncAuthorization
/// Sets the permissions required to allow the Synchronizer to download environment data from the control plane. You must call this API to enable proper functioning of hybrid. Pass the ETag when calling `setSyncAuthorization` to ensure that you are updating the correct version. To get an ETag, call [`getSyncAuthorization`](`getSyncAuthorization`). If you don't pass the ETag in the call to `setSyncAuthorization`, then the existing authorization is overwritten indiscriminately. For more information, see [Configure the Synchronizer](<https://cloud.google.`com/apigee/docs/hybrid/latest/synchronizer-access`>). **Note**: Available to Apigee hybrid only.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `apigee_organizations_set_sync_authorization_execute()` to send, or `apigee_organizations_set_sync_authorization` for simplest API.

pub fn apigee_organizations_set_sync_authorization_builder(
    client: &SimpleHttpClient,
    name: &String,
    body: &GoogleCloudApigeeV1SyncAuthorization,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url =
        format!("https://apigee.googleapis.com/v1/organizations/{}:setSyncAuthorization",);

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/organizations/{organizationsId}:setSyncAuthorization
/// Sets the permissions required to allow the Synchronizer to download environment data from the control plane. You must call this API to enable proper functioning of hybrid. Pass the ETag when calling `setSyncAuthorization` to ensure that you are updating the correct version. To get an ETag, call [`getSyncAuthorization`](`getSyncAuthorization`). If you don't pass the ETag in the call to `setSyncAuthorization`, then the existing authorization is overwritten indiscriminately. For more information, see [Configure the Synchronizer](<https://cloud.google.`com/apigee/docs/hybrid/latest/synchronizer-access`>). **Note**: Available to Apigee hybrid only.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `apigee_organizations_set_sync_authorization_execute()` or `apigee_organizations_set_sync_authorization`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `apigee_organizations_set_sync_authorization_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_set_sync_authorization_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleCloudApigeeV1SyncAuthorization>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudApigeeV1SyncAuthorization = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/organizations/{organizationsId}:setSyncAuthorization
/// Sets the permissions required to allow the Synchronizer to download environment data from the control plane. You must call this API to enable proper functioning of hybrid. Pass the ETag when calling `setSyncAuthorization` to ensure that you are updating the correct version. To get an ETag, call [`getSyncAuthorization`](`getSyncAuthorization`). If you don't pass the ETag in the call to `setSyncAuthorization`, then the existing authorization is overwritten indiscriminately. For more information, see [Configure the Synchronizer](<https://cloud.google.`com/apigee/docs/hybrid/latest/synchronizer-access`>). **Note**: Available to Apigee hybrid only.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `apigee_organizations_set_sync_authorization_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `apigee_organizations_set_sync_authorization_task()`.
/// For the simplest API, use `apigee_organizations_set_sync_authorization()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `apigee_organizations_set_sync_authorization_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn apigee_organizations_set_sync_authorization_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudApigeeV1SyncAuthorization>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = apigee_organizations_set_sync_authorization_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`apigee_organizations_set_sync_authorization`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ApigeeOrganizationsSetSyncAuthorizationArgs {
    /// Path parameter: name
    pub name: String,
    /// Request body.
    pub body: GoogleCloudApigeeV1SyncAuthorization,
}

/// GET v1/organizations/{organizationsId}:setSyncAuthorization
/// Sets the permissions required to allow the Synchronizer to download environment data from the control plane. You must call this API to enable proper functioning of hybrid. Pass the ETag when calling `setSyncAuthorization` to ensure that you are updating the correct version. To get an ETag, call [`getSyncAuthorization`](`getSyncAuthorization`). If you don't pass the ETag in the call to `setSyncAuthorization`, then the existing authorization is overwritten indiscriminately. For more information, see [Configure the Synchronizer](<https://cloud.google.`com/apigee/docs/hybrid/latest/synchronizer-access`>). **Note**: Available to Apigee hybrid only.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `apigee_organizations_set_sync_authorization_builder()` + `apigee_organizations_set_sync_authorization_execute()`.
/// For task-level control, use `apigee_organizations_set_sync_authorization_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_set_sync_authorization(
    client: &SimpleHttpClient,
    args: &ApigeeOrganizationsSetSyncAuthorizationArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudApigeeV1SyncAuthorization>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        apigee_organizations_set_sync_authorization_builder(client, &args.name, &args.body)?;
    apigee_organizations_set_sync_authorization_execute(builder)
}

/// GET v1/organizations/{organizationsId}/analytics/datastores
/// Create a Datastore for an org
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `apigee_organizations_analytics_datastores_create_execute()` to send, or `apigee_organizations_analytics_datastores_create` for simplest API.

pub fn apigee_organizations_analytics_datastores_create_builder(
    client: &SimpleHttpClient,
    parent: &String,
    body: &GoogleCloudApigeeV1Datastore,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url =
        format!("https://apigee.googleapis.com/v1/organizations/{}/analytics/datastores",);

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/organizations/{organizationsId}/analytics/datastores
/// Create a Datastore for an org
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `apigee_organizations_analytics_datastores_create_execute()` or `apigee_organizations_analytics_datastores_create`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `apigee_organizations_analytics_datastores_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_analytics_datastores_create_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleCloudApigeeV1Datastore>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudApigeeV1Datastore = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/organizations/{organizationsId}/analytics/datastores
/// Create a Datastore for an org
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `apigee_organizations_analytics_datastores_create_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `apigee_organizations_analytics_datastores_create_task()`.
/// For the simplest API, use `apigee_organizations_analytics_datastores_create()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `apigee_organizations_analytics_datastores_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn apigee_organizations_analytics_datastores_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudApigeeV1Datastore>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = apigee_organizations_analytics_datastores_create_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`apigee_organizations_analytics_datastores_create`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ApigeeOrganizationsAnalyticsDatastoresCreateArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Request body.
    pub body: GoogleCloudApigeeV1Datastore,
}

/// GET v1/organizations/{organizationsId}/analytics/datastores
/// Create a Datastore for an org
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `apigee_organizations_analytics_datastores_create_builder()` + `apigee_organizations_analytics_datastores_create_execute()`.
/// For task-level control, use `apigee_organizations_analytics_datastores_create_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_analytics_datastores_create(
    client: &SimpleHttpClient,
    args: &ApigeeOrganizationsAnalyticsDatastoresCreateArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudApigeeV1Datastore>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        apigee_organizations_analytics_datastores_create_builder(client, &args.parent, &args.body)?;
    apigee_organizations_analytics_datastores_create_execute(builder)
}

/// GET v1/organizations/{organizationsId}/analytics/datastores:test
/// Test if Datastore configuration is correct. This includes checking if credentials provided by customer have required permissions in target destination storage
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `apigee_organizations_analytics_datastores_test_execute()` to send, or `apigee_organizations_analytics_datastores_test` for simplest API.

pub fn apigee_organizations_analytics_datastores_test_builder(
    client: &SimpleHttpClient,
    parent: &String,
    body: &GoogleCloudApigeeV1Datastore,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url =
        format!("https://apigee.googleapis.com/v1/organizations/{}/analytics/datastores:test",);

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/organizations/{organizationsId}/analytics/datastores:test
/// Test if Datastore configuration is correct. This includes checking if credentials provided by customer have required permissions in target destination storage
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `apigee_organizations_analytics_datastores_test_execute()` or `apigee_organizations_analytics_datastores_test`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `apigee_organizations_analytics_datastores_test_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_analytics_datastores_test_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleCloudApigeeV1TestDatastoreResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudApigeeV1TestDatastoreResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/organizations/{organizationsId}/analytics/datastores:test
/// Test if Datastore configuration is correct. This includes checking if credentials provided by customer have required permissions in target destination storage
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `apigee_organizations_analytics_datastores_test_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `apigee_organizations_analytics_datastores_test_task()`.
/// For the simplest API, use `apigee_organizations_analytics_datastores_test()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `apigee_organizations_analytics_datastores_test_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn apigee_organizations_analytics_datastores_test_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudApigeeV1TestDatastoreResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = apigee_organizations_analytics_datastores_test_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`apigee_organizations_analytics_datastores_test`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ApigeeOrganizationsAnalyticsDatastoresTestArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Request body.
    pub body: GoogleCloudApigeeV1Datastore,
}

/// GET v1/organizations/{organizationsId}/analytics/datastores:test
/// Test if Datastore configuration is correct. This includes checking if credentials provided by customer have required permissions in target destination storage
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `apigee_organizations_analytics_datastores_test_builder()` + `apigee_organizations_analytics_datastores_test_execute()`.
/// For task-level control, use `apigee_organizations_analytics_datastores_test_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_analytics_datastores_test(
    client: &SimpleHttpClient,
    args: &ApigeeOrganizationsAnalyticsDatastoresTestArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudApigeeV1TestDatastoreResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        apigee_organizations_analytics_datastores_test_builder(client, &args.parent, &args.body)?;
    apigee_organizations_analytics_datastores_test_execute(builder)
}

/// GET v1/organizations/{organizationsId}/apimServiceExtensions
/// Creates an APIM ServiceExtension in an organization.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `apigee_organizations_apim_service_extensions_create_execute()` to send, or `apigee_organizations_apim_service_extensions_create` for simplest API.

pub fn apigee_organizations_apim_service_extensions_create_builder(
    client: &SimpleHttpClient,
    parent: &String,
    apimServiceExtensionId: &Option<String>,
    body: &GoogleCloudApigeeV1ApimServiceExtension,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url =
        format!("https://apigee.googleapis.com/v1/organizations/{}/apimServiceExtensions",);

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = apimServiceExtensionId.as_ref() {
        query_parts.push(format!("apimServiceExtensionId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/organizations/{organizationsId}/apimServiceExtensions
/// Creates an APIM ServiceExtension in an organization.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `apigee_organizations_apim_service_extensions_create_execute()` or `apigee_organizations_apim_service_extensions_create`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `apigee_organizations_apim_service_extensions_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_apim_service_extensions_create_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/organizations/{organizationsId}/apimServiceExtensions
/// Creates an APIM ServiceExtension in an organization.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `apigee_organizations_apim_service_extensions_create_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `apigee_organizations_apim_service_extensions_create_task()`.
/// For the simplest API, use `apigee_organizations_apim_service_extensions_create()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `apigee_organizations_apim_service_extensions_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn apigee_organizations_apim_service_extensions_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = apigee_organizations_apim_service_extensions_create_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`apigee_organizations_apim_service_extensions_create`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ApigeeOrganizationsApimServiceExtensionsCreateArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: apimServiceExtensionId
    pub apimServiceExtensionId: Option<String>,
    /// Request body.
    pub body: GoogleCloudApigeeV1ApimServiceExtension,
}

/// GET v1/organizations/{organizationsId}/apimServiceExtensions
/// Creates an APIM ServiceExtension in an organization.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `apigee_organizations_apim_service_extensions_create_builder()` + `apigee_organizations_apim_service_extensions_create_execute()`.
/// For task-level control, use `apigee_organizations_apim_service_extensions_create_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_apim_service_extensions_create(
    client: &SimpleHttpClient,
    args: &ApigeeOrganizationsApimServiceExtensionsCreateArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = apigee_organizations_apim_service_extensions_create_builder(
        client,
        &args.parent,
        &args.apimServiceExtensionId,
        &args.body,
    )?;
    apigee_organizations_apim_service_extensions_create_execute(builder)
}

/// GET v1/organizations/{organizationsId}/apiproducts
/// Creates an API product in an organization. You create API products after you have proxied backend services using API proxies. An API product is a collection of API resources combined with quota settings and metadata that you can use to deliver customized and productized API bundles to your developer community. This metadata can include: - Scope - Environments - API proxies - Extensible profile API products enable you repackage APIs on the fly, without having to do any additional coding or configuration. Apigee recommends that you start with a simple API product including only required elements. You then provision credentials to apps to enable them to start testing your APIs. After you have authentication and authorization working against a simple API product, you can iterate to create finer-grained API products, defining different sets of API resources for each API product. **WARNING:** - If you don't specify an API proxy in the request body, *any* app associated with the product can make calls to *any* API in your entire organization. - If you don't specify an environment in the request body, the product allows access to all environments. For more information, see What is an API product?
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `apigee_organizations_apiproducts_create_execute()` to send, or `apigee_organizations_apiproducts_create` for simplest API.

pub fn apigee_organizations_apiproducts_create_builder(
    client: &SimpleHttpClient,
    parent: &String,
    body: &GoogleCloudApigeeV1ApiProduct,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!("https://apigee.googleapis.com/v1/organizations/{}/apiproducts",);

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/organizations/{organizationsId}/apiproducts
/// Creates an API product in an organization. You create API products after you have proxied backend services using API proxies. An API product is a collection of API resources combined with quota settings and metadata that you can use to deliver customized and productized API bundles to your developer community. This metadata can include: - Scope - Environments - API proxies - Extensible profile API products enable you repackage APIs on the fly, without having to do any additional coding or configuration. Apigee recommends that you start with a simple API product including only required elements. You then provision credentials to apps to enable them to start testing your APIs. After you have authentication and authorization working against a simple API product, you can iterate to create finer-grained API products, defining different sets of API resources for each API product. **WARNING:** - If you don't specify an API proxy in the request body, *any* app associated with the product can make calls to *any* API in your entire organization. - If you don't specify an environment in the request body, the product allows access to all environments. For more information, see What is an API product?
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `apigee_organizations_apiproducts_create_execute()` or `apigee_organizations_apiproducts_create`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `apigee_organizations_apiproducts_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_apiproducts_create_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleCloudApigeeV1ApiProduct>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudApigeeV1ApiProduct = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/organizations/{organizationsId}/apiproducts
/// Creates an API product in an organization. You create API products after you have proxied backend services using API proxies. An API product is a collection of API resources combined with quota settings and metadata that you can use to deliver customized and productized API bundles to your developer community. This metadata can include: - Scope - Environments - API proxies - Extensible profile API products enable you repackage APIs on the fly, without having to do any additional coding or configuration. Apigee recommends that you start with a simple API product including only required elements. You then provision credentials to apps to enable them to start testing your APIs. After you have authentication and authorization working against a simple API product, you can iterate to create finer-grained API products, defining different sets of API resources for each API product. **WARNING:** - If you don't specify an API proxy in the request body, *any* app associated with the product can make calls to *any* API in your entire organization. - If you don't specify an environment in the request body, the product allows access to all environments. For more information, see What is an API product?
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `apigee_organizations_apiproducts_create_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `apigee_organizations_apiproducts_create_task()`.
/// For the simplest API, use `apigee_organizations_apiproducts_create()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `apigee_organizations_apiproducts_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn apigee_organizations_apiproducts_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudApigeeV1ApiProduct>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = apigee_organizations_apiproducts_create_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`apigee_organizations_apiproducts_create`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ApigeeOrganizationsApiproductsCreateArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Request body.
    pub body: GoogleCloudApigeeV1ApiProduct,
}

/// GET v1/organizations/{organizationsId}/apiproducts
/// Creates an API product in an organization. You create API products after you have proxied backend services using API proxies. An API product is a collection of API resources combined with quota settings and metadata that you can use to deliver customized and productized API bundles to your developer community. This metadata can include: - Scope - Environments - API proxies - Extensible profile API products enable you repackage APIs on the fly, without having to do any additional coding or configuration. Apigee recommends that you start with a simple API product including only required elements. You then provision credentials to apps to enable them to start testing your APIs. After you have authentication and authorization working against a simple API product, you can iterate to create finer-grained API products, defining different sets of API resources for each API product. **WARNING:** - If you don't specify an API proxy in the request body, *any* app associated with the product can make calls to *any* API in your entire organization. - If you don't specify an environment in the request body, the product allows access to all environments. For more information, see What is an API product?
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `apigee_organizations_apiproducts_create_builder()` + `apigee_organizations_apiproducts_create_execute()`.
/// For task-level control, use `apigee_organizations_apiproducts_create_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_apiproducts_create(
    client: &SimpleHttpClient,
    args: &ApigeeOrganizationsApiproductsCreateArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudApigeeV1ApiProduct>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        apigee_organizations_apiproducts_create_builder(client, &args.parent, &args.body)?;
    apigee_organizations_apiproducts_create_execute(builder)
}

/// GET v1/organizations/{organizationsId}/apis
/// Creates an API proxy. The API proxy created will not be accessible at runtime until it is deployed to an environment. Create a new API proxy by setting the name query parameter to the name of the API proxy. Import an API proxy configuration bundle stored in zip format on your local machine to your organization by doing the following: * Set the name query parameter to the name of the API proxy. * Set the action query parameter to import. * Set the Content-Type header to `multipart/form-data`. * Pass as a file the name of API proxy configuration bundle stored in zip format on your local machine using the file form field. **Note**: To validate the API proxy configuration bundle only without importing it, set the action query parameter to validate. When importing an API proxy configuration bundle, if the API proxy does not exist, it will be created. If the API proxy exists, then a new revision is created. Invalid API proxy configurations are rejected, and a list of validation errors is returned to the client.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `apigee_organizations_apis_create_execute()` to send, or `apigee_organizations_apis_create` for simplest API.

pub fn apigee_organizations_apis_create_builder(
    client: &SimpleHttpClient,
    parent: &String,
    action: &Option<String>,
    name: &Option<String>,
    space: &Option<String>,
    validate: &Option<bool>,
    body: &GoogleApiHttpBody,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!("https://apigee.googleapis.com/v1/organizations/{}/apis",);

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = action.as_ref() {
        query_parts.push(format!("action={}", val));
    }
    if let Some(val) = name.as_ref() {
        query_parts.push(format!("name={}", val));
    }
    if let Some(val) = space.as_ref() {
        query_parts.push(format!("space={}", val));
    }
    if let Some(val) = validate.as_ref() {
        query_parts.push(format!("validate={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/organizations/{organizationsId}/apis
/// Creates an API proxy. The API proxy created will not be accessible at runtime until it is deployed to an environment. Create a new API proxy by setting the name query parameter to the name of the API proxy. Import an API proxy configuration bundle stored in zip format on your local machine to your organization by doing the following: * Set the name query parameter to the name of the API proxy. * Set the action query parameter to import. * Set the Content-Type header to `multipart/form-data`. * Pass as a file the name of API proxy configuration bundle stored in zip format on your local machine using the file form field. **Note**: To validate the API proxy configuration bundle only without importing it, set the action query parameter to validate. When importing an API proxy configuration bundle, if the API proxy does not exist, it will be created. If the API proxy exists, then a new revision is created. Invalid API proxy configurations are rejected, and a list of validation errors is returned to the client.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `apigee_organizations_apis_create_execute()` or `apigee_organizations_apis_create`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `apigee_organizations_apis_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_apis_create_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleCloudApigeeV1ApiProxyRevision>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudApigeeV1ApiProxyRevision = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/organizations/{organizationsId}/apis
/// Creates an API proxy. The API proxy created will not be accessible at runtime until it is deployed to an environment. Create a new API proxy by setting the name query parameter to the name of the API proxy. Import an API proxy configuration bundle stored in zip format on your local machine to your organization by doing the following: * Set the name query parameter to the name of the API proxy. * Set the action query parameter to import. * Set the Content-Type header to `multipart/form-data`. * Pass as a file the name of API proxy configuration bundle stored in zip format on your local machine using the file form field. **Note**: To validate the API proxy configuration bundle only without importing it, set the action query parameter to validate. When importing an API proxy configuration bundle, if the API proxy does not exist, it will be created. If the API proxy exists, then a new revision is created. Invalid API proxy configurations are rejected, and a list of validation errors is returned to the client.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `apigee_organizations_apis_create_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `apigee_organizations_apis_create_task()`.
/// For the simplest API, use `apigee_organizations_apis_create()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `apigee_organizations_apis_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn apigee_organizations_apis_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudApigeeV1ApiProxyRevision>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = apigee_organizations_apis_create_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`apigee_organizations_apis_create`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ApigeeOrganizationsApisCreateArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: action
    pub action: Option<String>,
    /// Query parameter: name
    pub name: Option<String>,
    /// Query parameter: space
    pub space: Option<String>,
    /// Query parameter: validate
    pub validate: Option<bool>,
    /// Request body.
    pub body: GoogleApiHttpBody,
}

/// GET v1/organizations/{organizationsId}/apis
/// Creates an API proxy. The API proxy created will not be accessible at runtime until it is deployed to an environment. Create a new API proxy by setting the name query parameter to the name of the API proxy. Import an API proxy configuration bundle stored in zip format on your local machine to your organization by doing the following: * Set the name query parameter to the name of the API proxy. * Set the action query parameter to import. * Set the Content-Type header to `multipart/form-data`. * Pass as a file the name of API proxy configuration bundle stored in zip format on your local machine using the file form field. **Note**: To validate the API proxy configuration bundle only without importing it, set the action query parameter to validate. When importing an API proxy configuration bundle, if the API proxy does not exist, it will be created. If the API proxy exists, then a new revision is created. Invalid API proxy configurations are rejected, and a list of validation errors is returned to the client.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `apigee_organizations_apis_create_builder()` + `apigee_organizations_apis_create_execute()`.
/// For task-level control, use `apigee_organizations_apis_create_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_apis_create(
    client: &SimpleHttpClient,
    args: &ApigeeOrganizationsApisCreateArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudApigeeV1ApiProxyRevision>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = apigee_organizations_apis_create_builder(
        client,
        &args.parent,
        &args.action,
        &args.name,
        &args.space,
        &args.validate,
        &args.body,
    )?;
    apigee_organizations_apis_create_execute(builder)
}

/// GET v1/organizations/{organizationsId}/appgroups
/// Creates an AppGroup. Once created, user can register apps under the AppGroup to obtain secret key and password. At creation time, the AppGroup's state is set as active.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `apigee_organizations_appgroups_create_execute()` to send, or `apigee_organizations_appgroups_create` for simplest API.

pub fn apigee_organizations_appgroups_create_builder(
    client: &SimpleHttpClient,
    parent: &String,
    body: &GoogleCloudApigeeV1AppGroup,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!("https://apigee.googleapis.com/v1/organizations/{}/appgroups",);

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/organizations/{organizationsId}/appgroups
/// Creates an AppGroup. Once created, user can register apps under the AppGroup to obtain secret key and password. At creation time, the AppGroup's state is set as active.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `apigee_organizations_appgroups_create_execute()` or `apigee_organizations_appgroups_create`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `apigee_organizations_appgroups_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_appgroups_create_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleCloudApigeeV1AppGroup>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudApigeeV1AppGroup = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/organizations/{organizationsId}/appgroups
/// Creates an AppGroup. Once created, user can register apps under the AppGroup to obtain secret key and password. At creation time, the AppGroup's state is set as active.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `apigee_organizations_appgroups_create_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `apigee_organizations_appgroups_create_task()`.
/// For the simplest API, use `apigee_organizations_appgroups_create()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `apigee_organizations_appgroups_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn apigee_organizations_appgroups_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudApigeeV1AppGroup>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = apigee_organizations_appgroups_create_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`apigee_organizations_appgroups_create`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ApigeeOrganizationsAppgroupsCreateArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Request body.
    pub body: GoogleCloudApigeeV1AppGroup,
}

/// GET v1/organizations/{organizationsId}/appgroups
/// Creates an AppGroup. Once created, user can register apps under the AppGroup to obtain secret key and password. At creation time, the AppGroup's state is set as active.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `apigee_organizations_appgroups_create_builder()` + `apigee_organizations_appgroups_create_execute()`.
/// For task-level control, use `apigee_organizations_appgroups_create_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_appgroups_create(
    client: &SimpleHttpClient,
    args: &ApigeeOrganizationsAppgroupsCreateArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudApigeeV1AppGroup>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = apigee_organizations_appgroups_create_builder(client, &args.parent, &args.body)?;
    apigee_organizations_appgroups_create_execute(builder)
}

/// GET v1/organizations/{organizationsId}/apps
/// Lists IDs of apps within an organization that have the specified app status (approved or revoked) or are of the specified app type (developer or company).
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `apigee_organizations_apps_list_execute()` to send, or `apigee_organizations_apps_list` for simplest API.

pub fn apigee_organizations_apps_list_builder(
    client: &SimpleHttpClient,
    parent: &String,
    apiProduct: &Option<String>,
    apptype: &Option<String>,
    expand: &Option<bool>,
    filter: &Option<String>,
    ids: &Option<String>,
    includeCred: &Option<bool>,
    keyStatus: &Option<String>,
    pageSize: &Option<i32>,
    pageToken: &Option<String>,
    rows: &Option<String>,
    startKey: &Option<String>,
    status: &Option<String>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!("https://apigee.googleapis.com/v1/organizations/{}/apps",);

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = apiProduct.as_ref() {
        query_parts.push(format!("apiProduct={}", val));
    }
    if let Some(val) = apptype.as_ref() {
        query_parts.push(format!("apptype={}", val));
    }
    if let Some(val) = expand.as_ref() {
        query_parts.push(format!("expand={}", val));
    }
    if let Some(val) = filter.as_ref() {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = ids.as_ref() {
        query_parts.push(format!("ids={}", val));
    }
    if let Some(val) = includeCred.as_ref() {
        query_parts.push(format!("includeCred={}", val));
    }
    if let Some(val) = keyStatus.as_ref() {
        query_parts.push(format!("keyStatus={}", val));
    }
    if let Some(val) = pageSize.as_ref() {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = rows.as_ref() {
        query_parts.push(format!("rows={}", val));
    }
    if let Some(val) = startKey.as_ref() {
        query_parts.push(format!("startKey={}", val));
    }
    if let Some(val) = status.as_ref() {
        query_parts.push(format!("status={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/organizations/{organizationsId}/apps
/// Lists IDs of apps within an organization that have the specified app status (approved or revoked) or are of the specified app type (developer or company).
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `apigee_organizations_apps_list_execute()` or `apigee_organizations_apps_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `apigee_organizations_apps_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_apps_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleCloudApigeeV1ListAppsResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudApigeeV1ListAppsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/organizations/{organizationsId}/apps
/// Lists IDs of apps within an organization that have the specified app status (approved or revoked) or are of the specified app type (developer or company).
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `apigee_organizations_apps_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `apigee_organizations_apps_list_task()`.
/// For the simplest API, use `apigee_organizations_apps_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `apigee_organizations_apps_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn apigee_organizations_apps_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudApigeeV1ListAppsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = apigee_organizations_apps_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`apigee_organizations_apps_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ApigeeOrganizationsAppsListArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: apiProduct
    pub apiProduct: Option<String>,
    /// Query parameter: apptype
    pub apptype: Option<String>,
    /// Query parameter: expand
    pub expand: Option<bool>,
    /// Query parameter: filter
    pub filter: Option<String>,
    /// Query parameter: ids
    pub ids: Option<String>,
    /// Query parameter: includeCred
    pub includeCred: Option<bool>,
    /// Query parameter: keyStatus
    pub keyStatus: Option<String>,
    /// Query parameter: pageSize
    pub pageSize: Option<i32>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
    /// Query parameter: rows
    pub rows: Option<String>,
    /// Query parameter: startKey
    pub startKey: Option<String>,
    /// Query parameter: status
    pub status: Option<String>,
}

/// GET v1/organizations/{organizationsId}/apps
/// Lists IDs of apps within an organization that have the specified app status (approved or revoked) or are of the specified app type (developer or company).
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `apigee_organizations_apps_list_builder()` + `apigee_organizations_apps_list_execute()`.
/// For task-level control, use `apigee_organizations_apps_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_apps_list(
    client: &SimpleHttpClient,
    args: &ApigeeOrganizationsAppsListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudApigeeV1ListAppsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = apigee_organizations_apps_list_builder(
        client,
        &args.parent,
        &args.apiProduct,
        &args.apptype,
        &args.expand,
        &args.filter,
        &args.ids,
        &args.includeCred,
        &args.keyStatus,
        &args.pageSize,
        &args.pageToken,
        &args.rows,
        &args.startKey,
        &args.status,
    )?;
    apigee_organizations_apps_list_execute(builder)
}

/// GET v1/organizations/{organizationsId}/datacollectors
/// Creates a new data collector.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `apigee_organizations_datacollectors_create_execute()` to send, or `apigee_organizations_datacollectors_create` for simplest API.

pub fn apigee_organizations_datacollectors_create_builder(
    client: &SimpleHttpClient,
    parent: &String,
    dataCollectorId: &Option<String>,
    body: &GoogleCloudApigeeV1DataCollector,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!("https://apigee.googleapis.com/v1/organizations/{}/datacollectors",);

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = dataCollectorId.as_ref() {
        query_parts.push(format!("dataCollectorId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/organizations/{organizationsId}/datacollectors
/// Creates a new data collector.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `apigee_organizations_datacollectors_create_execute()` or `apigee_organizations_datacollectors_create`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `apigee_organizations_datacollectors_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_datacollectors_create_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleCloudApigeeV1DataCollector>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudApigeeV1DataCollector = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/organizations/{organizationsId}/datacollectors
/// Creates a new data collector.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `apigee_organizations_datacollectors_create_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `apigee_organizations_datacollectors_create_task()`.
/// For the simplest API, use `apigee_organizations_datacollectors_create()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `apigee_organizations_datacollectors_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn apigee_organizations_datacollectors_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudApigeeV1DataCollector>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = apigee_organizations_datacollectors_create_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`apigee_organizations_datacollectors_create`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ApigeeOrganizationsDatacollectorsCreateArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: dataCollectorId
    pub dataCollectorId: Option<String>,
    /// Request body.
    pub body: GoogleCloudApigeeV1DataCollector,
}

/// GET v1/organizations/{organizationsId}/datacollectors
/// Creates a new data collector.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `apigee_organizations_datacollectors_create_builder()` + `apigee_organizations_datacollectors_create_execute()`.
/// For task-level control, use `apigee_organizations_datacollectors_create_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_datacollectors_create(
    client: &SimpleHttpClient,
    args: &ApigeeOrganizationsDatacollectorsCreateArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudApigeeV1DataCollector>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = apigee_organizations_datacollectors_create_builder(
        client,
        &args.parent,
        &args.dataCollectorId,
        &args.body,
    )?;
    apigee_organizations_datacollectors_create_execute(builder)
}

/// GET v1/organizations/{organizationsId}/deployments
/// Lists all deployments of API proxies or shared flows.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `apigee_organizations_deployments_list_execute()` to send, or `apigee_organizations_deployments_list` for simplest API.

pub fn apigee_organizations_deployments_list_builder(
    client: &SimpleHttpClient,
    parent: &String,
    sharedFlows: &Option<bool>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!("https://apigee.googleapis.com/v1/organizations/{}/deployments",);

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = sharedFlows.as_ref() {
        query_parts.push(format!("sharedFlows={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/organizations/{organizationsId}/deployments
/// Lists all deployments of API proxies or shared flows.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `apigee_organizations_deployments_list_execute()` or `apigee_organizations_deployments_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `apigee_organizations_deployments_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_deployments_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleCloudApigeeV1ListDeploymentsResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudApigeeV1ListDeploymentsResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/organizations/{organizationsId}/deployments
/// Lists all deployments of API proxies or shared flows.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `apigee_organizations_deployments_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `apigee_organizations_deployments_list_task()`.
/// For the simplest API, use `apigee_organizations_deployments_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `apigee_organizations_deployments_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn apigee_organizations_deployments_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudApigeeV1ListDeploymentsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = apigee_organizations_deployments_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`apigee_organizations_deployments_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ApigeeOrganizationsDeploymentsListArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: sharedFlows
    pub sharedFlows: Option<bool>,
}

/// GET v1/organizations/{organizationsId}/deployments
/// Lists all deployments of API proxies or shared flows.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `apigee_organizations_deployments_list_builder()` + `apigee_organizations_deployments_list_execute()`.
/// For task-level control, use `apigee_organizations_deployments_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_deployments_list(
    client: &SimpleHttpClient,
    args: &ApigeeOrganizationsDeploymentsListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudApigeeV1ListDeploymentsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        apigee_organizations_deployments_list_builder(client, &args.parent, &args.sharedFlows)?;
    apigee_organizations_deployments_list_execute(builder)
}

/// GET v1/organizations/{organizationsId}/developers
/// Creates a developer. Once created, the developer can register an app and obtain an API key. At creation time, a developer is set as active. To change the developer status, use the SetDeveloperStatus API.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `apigee_organizations_developers_create_execute()` to send, or `apigee_organizations_developers_create` for simplest API.

pub fn apigee_organizations_developers_create_builder(
    client: &SimpleHttpClient,
    parent: &String,
    body: &GoogleCloudApigeeV1Developer,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!("https://apigee.googleapis.com/v1/organizations/{}/developers",);

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/organizations/{organizationsId}/developers
/// Creates a developer. Once created, the developer can register an app and obtain an API key. At creation time, a developer is set as active. To change the developer status, use the SetDeveloperStatus API.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `apigee_organizations_developers_create_execute()` or `apigee_organizations_developers_create`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `apigee_organizations_developers_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_developers_create_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleCloudApigeeV1Developer>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudApigeeV1Developer = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/organizations/{organizationsId}/developers
/// Creates a developer. Once created, the developer can register an app and obtain an API key. At creation time, a developer is set as active. To change the developer status, use the SetDeveloperStatus API.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `apigee_organizations_developers_create_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `apigee_organizations_developers_create_task()`.
/// For the simplest API, use `apigee_organizations_developers_create()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `apigee_organizations_developers_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn apigee_organizations_developers_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudApigeeV1Developer>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = apigee_organizations_developers_create_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`apigee_organizations_developers_create`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ApigeeOrganizationsDevelopersCreateArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Request body.
    pub body: GoogleCloudApigeeV1Developer,
}

/// GET v1/organizations/{organizationsId}/developers
/// Creates a developer. Once created, the developer can register an app and obtain an API key. At creation time, a developer is set as active. To change the developer status, use the SetDeveloperStatus API.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `apigee_organizations_developers_create_builder()` + `apigee_organizations_developers_create_execute()`.
/// For task-level control, use `apigee_organizations_developers_create_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_developers_create(
    client: &SimpleHttpClient,
    args: &ApigeeOrganizationsDevelopersCreateArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudApigeeV1Developer>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = apigee_organizations_developers_create_builder(client, &args.parent, &args.body)?;
    apigee_organizations_developers_create_execute(builder)
}

/// GET v1/organizations/{organizationsId}/dnsZones
/// Creates a new DNS zone.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `apigee_organizations_dns_zones_create_execute()` to send, or `apigee_organizations_dns_zones_create` for simplest API.

pub fn apigee_organizations_dns_zones_create_builder(
    client: &SimpleHttpClient,
    parent: &String,
    dnsZoneId: &Option<String>,
    body: &GoogleCloudApigeeV1DnsZone,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!("https://apigee.googleapis.com/v1/organizations/{}/dnsZones",);

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = dnsZoneId.as_ref() {
        query_parts.push(format!("dnsZoneId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/organizations/{organizationsId}/dnsZones
/// Creates a new DNS zone.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `apigee_organizations_dns_zones_create_execute()` or `apigee_organizations_dns_zones_create`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `apigee_organizations_dns_zones_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_dns_zones_create_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/organizations/{organizationsId}/dnsZones
/// Creates a new DNS zone.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `apigee_organizations_dns_zones_create_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `apigee_organizations_dns_zones_create_task()`.
/// For the simplest API, use `apigee_organizations_dns_zones_create()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `apigee_organizations_dns_zones_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn apigee_organizations_dns_zones_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = apigee_organizations_dns_zones_create_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`apigee_organizations_dns_zones_create`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ApigeeOrganizationsDnsZonesCreateArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: dnsZoneId
    pub dnsZoneId: Option<String>,
    /// Request body.
    pub body: GoogleCloudApigeeV1DnsZone,
}

/// GET v1/organizations/{organizationsId}/dnsZones
/// Creates a new DNS zone.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `apigee_organizations_dns_zones_create_builder()` + `apigee_organizations_dns_zones_create_execute()`.
/// For task-level control, use `apigee_organizations_dns_zones_create_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_dns_zones_create(
    client: &SimpleHttpClient,
    args: &ApigeeOrganizationsDnsZonesCreateArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = apigee_organizations_dns_zones_create_builder(
        client,
        &args.parent,
        &args.dnsZoneId,
        &args.body,
    )?;
    apigee_organizations_dns_zones_create_execute(builder)
}

/// GET v1/organizations/{organizationsId}/endpointAttachments
/// Creates an endpoint attachment. **Note:** Not supported for Apigee hybrid.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `apigee_organizations_endpoint_attachments_create_execute()` to send, or `apigee_organizations_endpoint_attachments_create` for simplest API.

pub fn apigee_organizations_endpoint_attachments_create_builder(
    client: &SimpleHttpClient,
    parent: &String,
    endpointAttachmentId: &Option<String>,
    body: &GoogleCloudApigeeV1EndpointAttachment,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url =
        format!("https://apigee.googleapis.com/v1/organizations/{}/endpointAttachments",);

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = endpointAttachmentId.as_ref() {
        query_parts.push(format!("endpointAttachmentId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/organizations/{organizationsId}/endpointAttachments
/// Creates an endpoint attachment. **Note:** Not supported for Apigee hybrid.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `apigee_organizations_endpoint_attachments_create_execute()` or `apigee_organizations_endpoint_attachments_create`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `apigee_organizations_endpoint_attachments_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_endpoint_attachments_create_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/organizations/{organizationsId}/endpointAttachments
/// Creates an endpoint attachment. **Note:** Not supported for Apigee hybrid.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `apigee_organizations_endpoint_attachments_create_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `apigee_organizations_endpoint_attachments_create_task()`.
/// For the simplest API, use `apigee_organizations_endpoint_attachments_create()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `apigee_organizations_endpoint_attachments_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn apigee_organizations_endpoint_attachments_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = apigee_organizations_endpoint_attachments_create_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`apigee_organizations_endpoint_attachments_create`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ApigeeOrganizationsEndpointAttachmentsCreateArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: endpointAttachmentId
    pub endpointAttachmentId: Option<String>,
    /// Request body.
    pub body: GoogleCloudApigeeV1EndpointAttachment,
}

/// GET v1/organizations/{organizationsId}/endpointAttachments
/// Creates an endpoint attachment. **Note:** Not supported for Apigee hybrid.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `apigee_organizations_endpoint_attachments_create_builder()` + `apigee_organizations_endpoint_attachments_create_execute()`.
/// For task-level control, use `apigee_organizations_endpoint_attachments_create_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_endpoint_attachments_create(
    client: &SimpleHttpClient,
    args: &ApigeeOrganizationsEndpointAttachmentsCreateArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = apigee_organizations_endpoint_attachments_create_builder(
        client,
        &args.parent,
        &args.endpointAttachmentId,
        &args.body,
    )?;
    apigee_organizations_endpoint_attachments_create_execute(builder)
}

/// GET v1/organizations/{organizationsId}/envgroups
/// Creates a new environment group.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `apigee_organizations_envgroups_create_execute()` to send, or `apigee_organizations_envgroups_create` for simplest API.

pub fn apigee_organizations_envgroups_create_builder(
    client: &SimpleHttpClient,
    parent: &String,
    name: &Option<String>,
    body: &GoogleCloudApigeeV1EnvironmentGroup,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!("https://apigee.googleapis.com/v1/organizations/{}/envgroups",);

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = name.as_ref() {
        query_parts.push(format!("name={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/organizations/{organizationsId}/envgroups
/// Creates a new environment group.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `apigee_organizations_envgroups_create_execute()` or `apigee_organizations_envgroups_create`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `apigee_organizations_envgroups_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_envgroups_create_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/organizations/{organizationsId}/envgroups
/// Creates a new environment group.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `apigee_organizations_envgroups_create_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `apigee_organizations_envgroups_create_task()`.
/// For the simplest API, use `apigee_organizations_envgroups_create()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `apigee_organizations_envgroups_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn apigee_organizations_envgroups_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = apigee_organizations_envgroups_create_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`apigee_organizations_envgroups_create`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ApigeeOrganizationsEnvgroupsCreateArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: name
    pub name: Option<String>,
    /// Request body.
    pub body: GoogleCloudApigeeV1EnvironmentGroup,
}

/// GET v1/organizations/{organizationsId}/envgroups
/// Creates a new environment group.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `apigee_organizations_envgroups_create_builder()` + `apigee_organizations_envgroups_create_execute()`.
/// For task-level control, use `apigee_organizations_envgroups_create_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_envgroups_create(
    client: &SimpleHttpClient,
    args: &ApigeeOrganizationsEnvgroupsCreateArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = apigee_organizations_envgroups_create_builder(
        client,
        &args.parent,
        &args.name,
        &args.body,
    )?;
    apigee_organizations_envgroups_create_execute(builder)
}

/// GET v1/organizations/{organizationsId}/environments
/// Creates an environment in an organization.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `apigee_organizations_environments_create_execute()` to send, or `apigee_organizations_environments_create` for simplest API.

pub fn apigee_organizations_environments_create_builder(
    client: &SimpleHttpClient,
    parent: &String,
    name: &Option<String>,
    body: &GoogleCloudApigeeV1Environment,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!("https://apigee.googleapis.com/v1/organizations/{}/environments",);

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = name.as_ref() {
        query_parts.push(format!("name={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/organizations/{organizationsId}/environments
/// Creates an environment in an organization.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `apigee_organizations_environments_create_execute()` or `apigee_organizations_environments_create`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `apigee_organizations_environments_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_environments_create_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/organizations/{organizationsId}/environments
/// Creates an environment in an organization.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `apigee_organizations_environments_create_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `apigee_organizations_environments_create_task()`.
/// For the simplest API, use `apigee_organizations_environments_create()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `apigee_organizations_environments_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn apigee_organizations_environments_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = apigee_organizations_environments_create_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`apigee_organizations_environments_create`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ApigeeOrganizationsEnvironmentsCreateArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: name
    pub name: Option<String>,
    /// Request body.
    pub body: GoogleCloudApigeeV1Environment,
}

/// GET v1/organizations/{organizationsId}/environments
/// Creates an environment in an organization.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `apigee_organizations_environments_create_builder()` + `apigee_organizations_environments_create_execute()`.
/// For task-level control, use `apigee_organizations_environments_create_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_environments_create(
    client: &SimpleHttpClient,
    args: &ApigeeOrganizationsEnvironmentsCreateArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = apigee_organizations_environments_create_builder(
        client,
        &args.parent,
        &args.name,
        &args.body,
    )?;
    apigee_organizations_environments_create_execute(builder)
}

/// GET v1/organizations/{organizationsId}/hostQueries
/// Submit a query at host level to be processed in the background. If the submission of the query succeeds, the API returns a 201 status and an ID that refer to the query. In addition to the HTTP status 201, the state of "enqueued" means that the request succeeded.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `apigee_organizations_host_queries_create_execute()` to send, or `apigee_organizations_host_queries_create` for simplest API.

pub fn apigee_organizations_host_queries_create_builder(
    client: &SimpleHttpClient,
    parent: &String,
    body: &GoogleCloudApigeeV1Query,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!("https://apigee.googleapis.com/v1/organizations/{}/hostQueries",);

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/organizations/{organizationsId}/hostQueries
/// Submit a query at host level to be processed in the background. If the submission of the query succeeds, the API returns a 201 status and an ID that refer to the query. In addition to the HTTP status 201, the state of "enqueued" means that the request succeeded.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `apigee_organizations_host_queries_create_execute()` or `apigee_organizations_host_queries_create`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `apigee_organizations_host_queries_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_host_queries_create_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleCloudApigeeV1AsyncQuery>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudApigeeV1AsyncQuery = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/organizations/{organizationsId}/hostQueries
/// Submit a query at host level to be processed in the background. If the submission of the query succeeds, the API returns a 201 status and an ID that refer to the query. In addition to the HTTP status 201, the state of "enqueued" means that the request succeeded.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `apigee_organizations_host_queries_create_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `apigee_organizations_host_queries_create_task()`.
/// For the simplest API, use `apigee_organizations_host_queries_create()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `apigee_organizations_host_queries_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn apigee_organizations_host_queries_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudApigeeV1AsyncQuery>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = apigee_organizations_host_queries_create_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`apigee_organizations_host_queries_create`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ApigeeOrganizationsHostQueriesCreateArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Request body.
    pub body: GoogleCloudApigeeV1Query,
}

/// GET v1/organizations/{organizationsId}/hostQueries
/// Submit a query at host level to be processed in the background. If the submission of the query succeeds, the API returns a 201 status and an ID that refer to the query. In addition to the HTTP status 201, the state of "enqueued" means that the request succeeded.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `apigee_organizations_host_queries_create_builder()` + `apigee_organizations_host_queries_create_execute()`.
/// For task-level control, use `apigee_organizations_host_queries_create_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_host_queries_create(
    client: &SimpleHttpClient,
    args: &ApigeeOrganizationsHostQueriesCreateArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudApigeeV1AsyncQuery>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        apigee_organizations_host_queries_create_builder(client, &args.parent, &args.body)?;
    apigee_organizations_host_queries_create_execute(builder)
}

/// GET v1/organizations/{organizationsId}/hostSecurityReports
/// Submit a query at host level to be processed in the background. If the submission of the query succeeds, the API returns a 201 status and an ID that refer to the query. In addition to the HTTP status 201, the state of "enqueued" means that the request succeeded.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `apigee_organizations_host_security_reports_create_execute()` to send, or `apigee_organizations_host_security_reports_create` for simplest API.

pub fn apigee_organizations_host_security_reports_create_builder(
    client: &SimpleHttpClient,
    parent: &String,
    body: &GoogleCloudApigeeV1SecurityReportQuery,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url =
        format!("https://apigee.googleapis.com/v1/organizations/{}/hostSecurityReports",);

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/organizations/{organizationsId}/hostSecurityReports
/// Submit a query at host level to be processed in the background. If the submission of the query succeeds, the API returns a 201 status and an ID that refer to the query. In addition to the HTTP status 201, the state of "enqueued" means that the request succeeded.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `apigee_organizations_host_security_reports_create_execute()` or `apigee_organizations_host_security_reports_create`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `apigee_organizations_host_security_reports_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_host_security_reports_create_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleCloudApigeeV1SecurityReport>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudApigeeV1SecurityReport = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/organizations/{organizationsId}/hostSecurityReports
/// Submit a query at host level to be processed in the background. If the submission of the query succeeds, the API returns a 201 status and an ID that refer to the query. In addition to the HTTP status 201, the state of "enqueued" means that the request succeeded.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `apigee_organizations_host_security_reports_create_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `apigee_organizations_host_security_reports_create_task()`.
/// For the simplest API, use `apigee_organizations_host_security_reports_create()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `apigee_organizations_host_security_reports_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn apigee_organizations_host_security_reports_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudApigeeV1SecurityReport>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = apigee_organizations_host_security_reports_create_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`apigee_organizations_host_security_reports_create`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ApigeeOrganizationsHostSecurityReportsCreateArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Request body.
    pub body: GoogleCloudApigeeV1SecurityReportQuery,
}

/// GET v1/organizations/{organizationsId}/hostSecurityReports
/// Submit a query at host level to be processed in the background. If the submission of the query succeeds, the API returns a 201 status and an ID that refer to the query. In addition to the HTTP status 201, the state of "enqueued" means that the request succeeded.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `apigee_organizations_host_security_reports_create_builder()` + `apigee_organizations_host_security_reports_create_execute()`.
/// For task-level control, use `apigee_organizations_host_security_reports_create_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_host_security_reports_create(
    client: &SimpleHttpClient,
    args: &ApigeeOrganizationsHostSecurityReportsCreateArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudApigeeV1SecurityReport>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = apigee_organizations_host_security_reports_create_builder(
        client,
        &args.parent,
        &args.body,
    )?;
    apigee_organizations_host_security_reports_create_execute(builder)
}

/// GET v1/organizations/{organizationsId}/instances
/// Creates an Apigee runtime instance. The instance is accessible from the authorized network configured on the organization. **Note:** Not supported for Apigee hybrid.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `apigee_organizations_instances_create_execute()` to send, or `apigee_organizations_instances_create` for simplest API.

pub fn apigee_organizations_instances_create_builder(
    client: &SimpleHttpClient,
    parent: &String,
    body: &GoogleCloudApigeeV1Instance,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!("https://apigee.googleapis.com/v1/organizations/{}/instances",);

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/organizations/{organizationsId}/instances
/// Creates an Apigee runtime instance. The instance is accessible from the authorized network configured on the organization. **Note:** Not supported for Apigee hybrid.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `apigee_organizations_instances_create_execute()` or `apigee_organizations_instances_create`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `apigee_organizations_instances_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_instances_create_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/organizations/{organizationsId}/instances
/// Creates an Apigee runtime instance. The instance is accessible from the authorized network configured on the organization. **Note:** Not supported for Apigee hybrid.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `apigee_organizations_instances_create_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `apigee_organizations_instances_create_task()`.
/// For the simplest API, use `apigee_organizations_instances_create()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `apigee_organizations_instances_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn apigee_organizations_instances_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = apigee_organizations_instances_create_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`apigee_organizations_instances_create`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ApigeeOrganizationsInstancesCreateArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Request body.
    pub body: GoogleCloudApigeeV1Instance,
}

/// GET v1/organizations/{organizationsId}/instances
/// Creates an Apigee runtime instance. The instance is accessible from the authorized network configured on the organization. **Note:** Not supported for Apigee hybrid.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `apigee_organizations_instances_create_builder()` + `apigee_organizations_instances_create_execute()`.
/// For task-level control, use `apigee_organizations_instances_create_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_instances_create(
    client: &SimpleHttpClient,
    args: &ApigeeOrganizationsInstancesCreateArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = apigee_organizations_instances_create_builder(client, &args.parent, &args.body)?;
    apigee_organizations_instances_create_execute(builder)
}

/// GET v1/organizations/{organizationsId}/keyvaluemaps
/// Creates a key value map in an organization.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `apigee_organizations_keyvaluemaps_create_execute()` to send, or `apigee_organizations_keyvaluemaps_create` for simplest API.

pub fn apigee_organizations_keyvaluemaps_create_builder(
    client: &SimpleHttpClient,
    parent: &String,
    body: &GoogleCloudApigeeV1KeyValueMap,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!("https://apigee.googleapis.com/v1/organizations/{}/keyvaluemaps",);

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/organizations/{organizationsId}/keyvaluemaps
/// Creates a key value map in an organization.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `apigee_organizations_keyvaluemaps_create_execute()` or `apigee_organizations_keyvaluemaps_create`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `apigee_organizations_keyvaluemaps_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_keyvaluemaps_create_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleCloudApigeeV1KeyValueMap>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudApigeeV1KeyValueMap = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/organizations/{organizationsId}/keyvaluemaps
/// Creates a key value map in an organization.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `apigee_organizations_keyvaluemaps_create_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `apigee_organizations_keyvaluemaps_create_task()`.
/// For the simplest API, use `apigee_organizations_keyvaluemaps_create()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `apigee_organizations_keyvaluemaps_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn apigee_organizations_keyvaluemaps_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudApigeeV1KeyValueMap>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = apigee_organizations_keyvaluemaps_create_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`apigee_organizations_keyvaluemaps_create`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ApigeeOrganizationsKeyvaluemapsCreateArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Request body.
    pub body: GoogleCloudApigeeV1KeyValueMap,
}

/// GET v1/organizations/{organizationsId}/keyvaluemaps
/// Creates a key value map in an organization.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `apigee_organizations_keyvaluemaps_create_builder()` + `apigee_organizations_keyvaluemaps_create_execute()`.
/// For task-level control, use `apigee_organizations_keyvaluemaps_create_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_keyvaluemaps_create(
    client: &SimpleHttpClient,
    args: &ApigeeOrganizationsKeyvaluemapsCreateArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudApigeeV1KeyValueMap>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        apigee_organizations_keyvaluemaps_create_builder(client, &args.parent, &args.body)?;
    apigee_organizations_keyvaluemaps_create_execute(builder)
}

/// GET v1/organizations/{organizationsId}/operations
/// Lists operations that match the specified filter in the request. If the server doesn't support this method, it returns UNIMPLEMENTED.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `apigee_organizations_operations_list_execute()` to send, or `apigee_organizations_operations_list` for simplest API.

pub fn apigee_organizations_operations_list_builder(
    client: &SimpleHttpClient,
    name: &String,
    filter: &Option<String>,
    pageSize: &Option<i32>,
    pageToken: &Option<String>,
    returnPartialSuccess: &Option<bool>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!("https://apigee.googleapis.com/v1/organizations/{}/operations",);

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter.as_ref() {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = pageSize.as_ref() {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = returnPartialSuccess.as_ref() {
        query_parts.push(format!("returnPartialSuccess={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/organizations/{organizationsId}/operations
/// Lists operations that match the specified filter in the request. If the server doesn't support this method, it returns UNIMPLEMENTED.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `apigee_organizations_operations_list_execute()` or `apigee_organizations_operations_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `apigee_organizations_operations_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_operations_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleLongrunningListOperationsResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningListOperationsResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/organizations/{organizationsId}/operations
/// Lists operations that match the specified filter in the request. If the server doesn't support this method, it returns UNIMPLEMENTED.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `apigee_organizations_operations_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `apigee_organizations_operations_list_task()`.
/// For the simplest API, use `apigee_organizations_operations_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `apigee_organizations_operations_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn apigee_organizations_operations_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningListOperationsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = apigee_organizations_operations_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`apigee_organizations_operations_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ApigeeOrganizationsOperationsListArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: filter
    pub filter: Option<String>,
    /// Query parameter: pageSize
    pub pageSize: Option<i32>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
    /// Query parameter: returnPartialSuccess
    pub returnPartialSuccess: Option<bool>,
}

/// GET v1/organizations/{organizationsId}/operations
/// Lists operations that match the specified filter in the request. If the server doesn't support this method, it returns UNIMPLEMENTED.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `apigee_organizations_operations_list_builder()` + `apigee_organizations_operations_list_execute()`.
/// For task-level control, use `apigee_organizations_operations_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_operations_list(
    client: &SimpleHttpClient,
    args: &ApigeeOrganizationsOperationsListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningListOperationsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = apigee_organizations_operations_list_builder(
        client,
        &args.name,
        &args.filter,
        &args.pageSize,
        &args.pageToken,
        &args.returnPartialSuccess,
    )?;
    apigee_organizations_operations_list_execute(builder)
}

/// GET v1/organizations/{organizationsId}/reports
/// Creates a Custom Report for an Organization. A Custom Report provides Apigee Customers to create custom dashboards in addition to the standard dashboards which are provided. The Custom Report in its simplest form contains specifications about metrics, dimensions and filters. It is important to note that the custom report by itself does not provide an executable entity. The Edge UI converts the custom report definition into an analytics query and displays the result in a chart.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `apigee_organizations_reports_create_execute()` to send, or `apigee_organizations_reports_create` for simplest API.

pub fn apigee_organizations_reports_create_builder(
    client: &SimpleHttpClient,
    parent: &String,
    body: &GoogleCloudApigeeV1CustomReport,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!("https://apigee.googleapis.com/v1/organizations/{}/reports",);

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/organizations/{organizationsId}/reports
/// Creates a Custom Report for an Organization. A Custom Report provides Apigee Customers to create custom dashboards in addition to the standard dashboards which are provided. The Custom Report in its simplest form contains specifications about metrics, dimensions and filters. It is important to note that the custom report by itself does not provide an executable entity. The Edge UI converts the custom report definition into an analytics query and displays the result in a chart.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `apigee_organizations_reports_create_execute()` or `apigee_organizations_reports_create`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `apigee_organizations_reports_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_reports_create_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleCloudApigeeV1CustomReport>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudApigeeV1CustomReport = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/organizations/{organizationsId}/reports
/// Creates a Custom Report for an Organization. A Custom Report provides Apigee Customers to create custom dashboards in addition to the standard dashboards which are provided. The Custom Report in its simplest form contains specifications about metrics, dimensions and filters. It is important to note that the custom report by itself does not provide an executable entity. The Edge UI converts the custom report definition into an analytics query and displays the result in a chart.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `apigee_organizations_reports_create_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `apigee_organizations_reports_create_task()`.
/// For the simplest API, use `apigee_organizations_reports_create()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `apigee_organizations_reports_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn apigee_organizations_reports_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudApigeeV1CustomReport>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = apigee_organizations_reports_create_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`apigee_organizations_reports_create`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ApigeeOrganizationsReportsCreateArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Request body.
    pub body: GoogleCloudApigeeV1CustomReport,
}

/// GET v1/organizations/{organizationsId}/reports
/// Creates a Custom Report for an Organization. A Custom Report provides Apigee Customers to create custom dashboards in addition to the standard dashboards which are provided. The Custom Report in its simplest form contains specifications about metrics, dimensions and filters. It is important to note that the custom report by itself does not provide an executable entity. The Edge UI converts the custom report definition into an analytics query and displays the result in a chart.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `apigee_organizations_reports_create_builder()` + `apigee_organizations_reports_create_execute()`.
/// For task-level control, use `apigee_organizations_reports_create_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_reports_create(
    client: &SimpleHttpClient,
    args: &ApigeeOrganizationsReportsCreateArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudApigeeV1CustomReport>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = apigee_organizations_reports_create_builder(client, &args.parent, &args.body)?;
    apigee_organizations_reports_create_execute(builder)
}

/// GET v1/organizations/{organizationsId}/securityAssessmentResults:batchCompute
/// Compute RAV2 security scores for a set of resources.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `apigee_organizations_security_assessment_results_batch_compute_execute()` to send, or `apigee_organizations_security_assessment_results_batch_compute` for simplest API.

pub fn apigee_organizations_security_assessment_results_batch_compute_builder(
    client: &SimpleHttpClient,
    name: &String,
    body: &GoogleCloudApigeeV1BatchComputeSecurityAssessmentResultsRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://apigee.googleapis.com/v1/organizations/{}/securityAssessmentResults:batchCompute",
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/organizations/{organizationsId}/securityAssessmentResults:batchCompute
/// Compute RAV2 security scores for a set of resources.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `apigee_organizations_security_assessment_results_batch_compute_execute()` or `apigee_organizations_security_assessment_results_batch_compute`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `apigee_organizations_security_assessment_results_batch_compute_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_security_assessment_results_batch_compute_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<GoogleCloudApigeeV1BatchComputeSecurityAssessmentResultsResponse>,
                ApiError,
            >,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudApigeeV1BatchComputeSecurityAssessmentResultsResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/organizations/{organizationsId}/securityAssessmentResults:batchCompute
/// Compute RAV2 security scores for a set of resources.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `apigee_organizations_security_assessment_results_batch_compute_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `apigee_organizations_security_assessment_results_batch_compute_task()`.
/// For the simplest API, use `apigee_organizations_security_assessment_results_batch_compute()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `apigee_organizations_security_assessment_results_batch_compute_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn apigee_organizations_security_assessment_results_batch_compute_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleCloudApigeeV1BatchComputeSecurityAssessmentResultsResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = apigee_organizations_security_assessment_results_batch_compute_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`apigee_organizations_security_assessment_results_batch_compute`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ApigeeOrganizationsSecurityAssessmentResultsBatchComputeArgs {
    /// Path parameter: name
    pub name: String,
    /// Request body.
    pub body: GoogleCloudApigeeV1BatchComputeSecurityAssessmentResultsRequest,
}

/// GET v1/organizations/{organizationsId}/securityAssessmentResults:batchCompute
/// Compute RAV2 security scores for a set of resources.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `apigee_organizations_security_assessment_results_batch_compute_builder()` + `apigee_organizations_security_assessment_results_batch_compute_execute()`.
/// For task-level control, use `apigee_organizations_security_assessment_results_batch_compute_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_security_assessment_results_batch_compute(
    client: &SimpleHttpClient,
    args: &ApigeeOrganizationsSecurityAssessmentResultsBatchComputeArgs,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleCloudApigeeV1BatchComputeSecurityAssessmentResultsResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = apigee_organizations_security_assessment_results_batch_compute_builder(
        client, &args.name, &args.body,
    )?;
    apigee_organizations_security_assessment_results_batch_compute_execute(builder)
}

/// GET v1/organizations/{organizationsId}/securityFeedback
/// Creates a new report containing customer feedback.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `apigee_organizations_security_feedback_create_execute()` to send, or `apigee_organizations_security_feedback_create` for simplest API.

pub fn apigee_organizations_security_feedback_create_builder(
    client: &SimpleHttpClient,
    parent: &String,
    securityFeedbackId: &Option<String>,
    body: &GoogleCloudApigeeV1SecurityFeedback,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url =
        format!("https://apigee.googleapis.com/v1/organizations/{}/securityFeedback",);

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = securityFeedbackId.as_ref() {
        query_parts.push(format!("securityFeedbackId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/organizations/{organizationsId}/securityFeedback
/// Creates a new report containing customer feedback.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `apigee_organizations_security_feedback_create_execute()` or `apigee_organizations_security_feedback_create`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `apigee_organizations_security_feedback_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_security_feedback_create_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleCloudApigeeV1SecurityFeedback>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudApigeeV1SecurityFeedback = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/organizations/{organizationsId}/securityFeedback
/// Creates a new report containing customer feedback.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `apigee_organizations_security_feedback_create_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `apigee_organizations_security_feedback_create_task()`.
/// For the simplest API, use `apigee_organizations_security_feedback_create()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `apigee_organizations_security_feedback_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn apigee_organizations_security_feedback_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudApigeeV1SecurityFeedback>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = apigee_organizations_security_feedback_create_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`apigee_organizations_security_feedback_create`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ApigeeOrganizationsSecurityFeedbackCreateArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: securityFeedbackId
    pub securityFeedbackId: Option<String>,
    /// Request body.
    pub body: GoogleCloudApigeeV1SecurityFeedback,
}

/// GET v1/organizations/{organizationsId}/securityFeedback
/// Creates a new report containing customer feedback.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `apigee_organizations_security_feedback_create_builder()` + `apigee_organizations_security_feedback_create_execute()`.
/// For task-level control, use `apigee_organizations_security_feedback_create_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_security_feedback_create(
    client: &SimpleHttpClient,
    args: &ApigeeOrganizationsSecurityFeedbackCreateArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudApigeeV1SecurityFeedback>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = apigee_organizations_security_feedback_create_builder(
        client,
        &args.parent,
        &args.securityFeedbackId,
        &args.body,
    )?;
    apigee_organizations_security_feedback_create_execute(builder)
}

/// GET v1/organizations/{organizationsId}/securityMonitoringConditions
/// Create a security monitoring condition.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `apigee_organizations_security_monitoring_conditions_create_execute()` to send, or `apigee_organizations_security_monitoring_conditions_create` for simplest API.

pub fn apigee_organizations_security_monitoring_conditions_create_builder(
    client: &SimpleHttpClient,
    parent: &String,
    securityMonitoringConditionId: &Option<String>,
    body: &GoogleCloudApigeeV1SecurityMonitoringCondition,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url =
        format!("https://apigee.googleapis.com/v1/organizations/{}/securityMonitoringConditions",);

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = securityMonitoringConditionId.as_ref() {
        query_parts.push(format!("securityMonitoringConditionId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/organizations/{organizationsId}/securityMonitoringConditions
/// Create a security monitoring condition.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `apigee_organizations_security_monitoring_conditions_create_execute()` or `apigee_organizations_security_monitoring_conditions_create`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `apigee_organizations_security_monitoring_conditions_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_security_monitoring_conditions_create_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleCloudApigeeV1SecurityMonitoringCondition>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudApigeeV1SecurityMonitoringCondition =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/organizations/{organizationsId}/securityMonitoringConditions
/// Create a security monitoring condition.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `apigee_organizations_security_monitoring_conditions_create_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `apigee_organizations_security_monitoring_conditions_create_task()`.
/// For the simplest API, use `apigee_organizations_security_monitoring_conditions_create()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `apigee_organizations_security_monitoring_conditions_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn apigee_organizations_security_monitoring_conditions_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudApigeeV1SecurityMonitoringCondition>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = apigee_organizations_security_monitoring_conditions_create_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`apigee_organizations_security_monitoring_conditions_create`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ApigeeOrganizationsSecurityMonitoringConditionsCreateArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: securityMonitoringConditionId
    pub securityMonitoringConditionId: Option<String>,
    /// Request body.
    pub body: GoogleCloudApigeeV1SecurityMonitoringCondition,
}

/// GET v1/organizations/{organizationsId}/securityMonitoringConditions
/// Create a security monitoring condition.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `apigee_organizations_security_monitoring_conditions_create_builder()` + `apigee_organizations_security_monitoring_conditions_create_execute()`.
/// For task-level control, use `apigee_organizations_security_monitoring_conditions_create_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_security_monitoring_conditions_create(
    client: &SimpleHttpClient,
    args: &ApigeeOrganizationsSecurityMonitoringConditionsCreateArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudApigeeV1SecurityMonitoringCondition>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = apigee_organizations_security_monitoring_conditions_create_builder(
        client,
        &args.parent,
        &args.securityMonitoringConditionId,
        &args.body,
    )?;
    apigee_organizations_security_monitoring_conditions_create_execute(builder)
}

/// GET v1/organizations/{organizationsId}/securityProfiles
/// CreateSecurityProfile create a new custom security profile.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `apigee_organizations_security_profiles_create_execute()` to send, or `apigee_organizations_security_profiles_create` for simplest API.

pub fn apigee_organizations_security_profiles_create_builder(
    client: &SimpleHttpClient,
    parent: &String,
    securityProfileId: &Option<String>,
    body: &GoogleCloudApigeeV1SecurityProfile,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url =
        format!("https://apigee.googleapis.com/v1/organizations/{}/securityProfiles",);

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = securityProfileId.as_ref() {
        query_parts.push(format!("securityProfileId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/organizations/{organizationsId}/securityProfiles
/// CreateSecurityProfile create a new custom security profile.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `apigee_organizations_security_profiles_create_execute()` or `apigee_organizations_security_profiles_create`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `apigee_organizations_security_profiles_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_security_profiles_create_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleCloudApigeeV1SecurityProfile>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudApigeeV1SecurityProfile = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/organizations/{organizationsId}/securityProfiles
/// CreateSecurityProfile create a new custom security profile.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `apigee_organizations_security_profiles_create_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `apigee_organizations_security_profiles_create_task()`.
/// For the simplest API, use `apigee_organizations_security_profiles_create()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `apigee_organizations_security_profiles_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn apigee_organizations_security_profiles_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudApigeeV1SecurityProfile>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = apigee_organizations_security_profiles_create_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`apigee_organizations_security_profiles_create`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ApigeeOrganizationsSecurityProfilesCreateArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: securityProfileId
    pub securityProfileId: Option<String>,
    /// Request body.
    pub body: GoogleCloudApigeeV1SecurityProfile,
}

/// GET v1/organizations/{organizationsId}/securityProfiles
/// CreateSecurityProfile create a new custom security profile.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `apigee_organizations_security_profiles_create_builder()` + `apigee_organizations_security_profiles_create_execute()`.
/// For task-level control, use `apigee_organizations_security_profiles_create_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_security_profiles_create(
    client: &SimpleHttpClient,
    args: &ApigeeOrganizationsSecurityProfilesCreateArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudApigeeV1SecurityProfile>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = apigee_organizations_security_profiles_create_builder(
        client,
        &args.parent,
        &args.securityProfileId,
        &args.body,
    )?;
    apigee_organizations_security_profiles_create_execute(builder)
}

/// GET v1/organizations/{organizationsId}/securityProfilesV2
/// Create a security profile v2.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `apigee_organizations_security_profiles_v2_create_execute()` to send, or `apigee_organizations_security_profiles_v2_create` for simplest API.

pub fn apigee_organizations_security_profiles_v2_create_builder(
    client: &SimpleHttpClient,
    parent: &String,
    securityProfileV2Id: &Option<String>,
    body: &GoogleCloudApigeeV1SecurityProfileV2,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url =
        format!("https://apigee.googleapis.com/v1/organizations/{}/securityProfilesV2",);

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = securityProfileV2Id.as_ref() {
        query_parts.push(format!("securityProfileV2Id={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/organizations/{organizationsId}/securityProfilesV2
/// Create a security profile v2.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `apigee_organizations_security_profiles_v2_create_execute()` or `apigee_organizations_security_profiles_v2_create`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `apigee_organizations_security_profiles_v2_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_security_profiles_v2_create_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleCloudApigeeV1SecurityProfileV2>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudApigeeV1SecurityProfileV2 = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/organizations/{organizationsId}/securityProfilesV2
/// Create a security profile v2.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `apigee_organizations_security_profiles_v2_create_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `apigee_organizations_security_profiles_v2_create_task()`.
/// For the simplest API, use `apigee_organizations_security_profiles_v2_create()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `apigee_organizations_security_profiles_v2_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn apigee_organizations_security_profiles_v2_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudApigeeV1SecurityProfileV2>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = apigee_organizations_security_profiles_v2_create_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`apigee_organizations_security_profiles_v2_create`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ApigeeOrganizationsSecurityProfilesV2CreateArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: securityProfileV2Id
    pub securityProfileV2Id: Option<String>,
    /// Request body.
    pub body: GoogleCloudApigeeV1SecurityProfileV2,
}

/// GET v1/organizations/{organizationsId}/securityProfilesV2
/// Create a security profile v2.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `apigee_organizations_security_profiles_v2_create_builder()` + `apigee_organizations_security_profiles_v2_create_execute()`.
/// For task-level control, use `apigee_organizations_security_profiles_v2_create_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_security_profiles_v2_create(
    client: &SimpleHttpClient,
    args: &ApigeeOrganizationsSecurityProfilesV2CreateArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudApigeeV1SecurityProfileV2>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = apigee_organizations_security_profiles_v2_create_builder(
        client,
        &args.parent,
        &args.securityProfileV2Id,
        &args.body,
    )?;
    apigee_organizations_security_profiles_v2_create_execute(builder)
}

/// GET v1/organizations/{organizationsId}/sharedflows
/// Uploads a ZIP-formatted shared flow configuration bundle to an organization. If the shared flow already exists, this creates a new revision of it. If the shared flow does not exist, this creates it. Once imported, the shared flow revision must be deployed before it can be accessed at runtime. The size limit of a shared flow bundle is 15 MB.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `apigee_organizations_sharedflows_create_execute()` to send, or `apigee_organizations_sharedflows_create` for simplest API.

pub fn apigee_organizations_sharedflows_create_builder(
    client: &SimpleHttpClient,
    parent: &String,
    action: &Option<String>,
    name: &Option<String>,
    space: &Option<String>,
    body: &GoogleApiHttpBody,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!("https://apigee.googleapis.com/v1/organizations/{}/sharedflows",);

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = action.as_ref() {
        query_parts.push(format!("action={}", val));
    }
    if let Some(val) = name.as_ref() {
        query_parts.push(format!("name={}", val));
    }
    if let Some(val) = space.as_ref() {
        query_parts.push(format!("space={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/organizations/{organizationsId}/sharedflows
/// Uploads a ZIP-formatted shared flow configuration bundle to an organization. If the shared flow already exists, this creates a new revision of it. If the shared flow does not exist, this creates it. Once imported, the shared flow revision must be deployed before it can be accessed at runtime. The size limit of a shared flow bundle is 15 MB.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `apigee_organizations_sharedflows_create_execute()` or `apigee_organizations_sharedflows_create`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `apigee_organizations_sharedflows_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_sharedflows_create_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleCloudApigeeV1SharedFlowRevision>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudApigeeV1SharedFlowRevision = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/organizations/{organizationsId}/sharedflows
/// Uploads a ZIP-formatted shared flow configuration bundle to an organization. If the shared flow already exists, this creates a new revision of it. If the shared flow does not exist, this creates it. Once imported, the shared flow revision must be deployed before it can be accessed at runtime. The size limit of a shared flow bundle is 15 MB.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `apigee_organizations_sharedflows_create_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `apigee_organizations_sharedflows_create_task()`.
/// For the simplest API, use `apigee_organizations_sharedflows_create()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `apigee_organizations_sharedflows_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn apigee_organizations_sharedflows_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudApigeeV1SharedFlowRevision>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = apigee_organizations_sharedflows_create_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`apigee_organizations_sharedflows_create`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ApigeeOrganizationsSharedflowsCreateArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: action
    pub action: Option<String>,
    /// Query parameter: name
    pub name: Option<String>,
    /// Query parameter: space
    pub space: Option<String>,
    /// Request body.
    pub body: GoogleApiHttpBody,
}

/// GET v1/organizations/{organizationsId}/sharedflows
/// Uploads a ZIP-formatted shared flow configuration bundle to an organization. If the shared flow already exists, this creates a new revision of it. If the shared flow does not exist, this creates it. Once imported, the shared flow revision must be deployed before it can be accessed at runtime. The size limit of a shared flow bundle is 15 MB.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `apigee_organizations_sharedflows_create_builder()` + `apigee_organizations_sharedflows_create_execute()`.
/// For task-level control, use `apigee_organizations_sharedflows_create_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_sharedflows_create(
    client: &SimpleHttpClient,
    args: &ApigeeOrganizationsSharedflowsCreateArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudApigeeV1SharedFlowRevision>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = apigee_organizations_sharedflows_create_builder(
        client,
        &args.parent,
        &args.action,
        &args.name,
        &args.space,
        &args.body,
    )?;
    apigee_organizations_sharedflows_create_execute(builder)
}

/// GET v1/organizations/{organizationsId}/spaces
/// Create a space under an organization.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `apigee_organizations_spaces_create_execute()` to send, or `apigee_organizations_spaces_create` for simplest API.

pub fn apigee_organizations_spaces_create_builder(
    client: &SimpleHttpClient,
    parent: &String,
    spaceId: &Option<String>,
    body: &GoogleCloudApigeeV1Space,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!("https://apigee.googleapis.com/v1/organizations/{}/spaces",);

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = spaceId.as_ref() {
        query_parts.push(format!("spaceId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/organizations/{organizationsId}/spaces
/// Create a space under an organization.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `apigee_organizations_spaces_create_execute()` or `apigee_organizations_spaces_create`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `apigee_organizations_spaces_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_spaces_create_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleCloudApigeeV1Space>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudApigeeV1Space = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/organizations/{organizationsId}/spaces
/// Create a space under an organization.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `apigee_organizations_spaces_create_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `apigee_organizations_spaces_create_task()`.
/// For the simplest API, use `apigee_organizations_spaces_create()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `apigee_organizations_spaces_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn apigee_organizations_spaces_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleCloudApigeeV1Space>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = apigee_organizations_spaces_create_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`apigee_organizations_spaces_create`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ApigeeOrganizationsSpacesCreateArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: spaceId
    pub spaceId: Option<String>,
    /// Request body.
    pub body: GoogleCloudApigeeV1Space,
}

/// GET v1/organizations/{organizationsId}/spaces
/// Create a space under an organization.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `apigee_organizations_spaces_create_builder()` + `apigee_organizations_spaces_create_execute()`.
/// For task-level control, use `apigee_organizations_spaces_create_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_organizations_spaces_create(
    client: &SimpleHttpClient,
    args: &ApigeeOrganizationsSpacesCreateArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleCloudApigeeV1Space>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = apigee_organizations_spaces_create_builder(
        client,
        &args.parent,
        &args.spaceId,
        &args.body,
    )?;
    apigee_organizations_spaces_create_execute(builder)
}

/// GET v1/projects/{projectsId}:provisionOrganization
/// Provisions a new Apigee organization with a functioning runtime. This is the standard way to create trial organizations for a free Apigee trial.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `apigee_projects_provision_organization_execute()` to send, or `apigee_projects_provision_organization` for simplest API.

pub fn apigee_projects_provision_organization_builder(
    client: &SimpleHttpClient,
    project: &String,
    body: &GoogleCloudApigeeV1ProvisionOrganizationRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url =
        format!("https://apigee.googleapis.com/v1/projects/{}:provisionOrganization",);

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}:provisionOrganization
/// Provisions a new Apigee organization with a functioning runtime. This is the standard way to create trial organizations for a free Apigee trial.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `apigee_projects_provision_organization_execute()` or `apigee_projects_provision_organization`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `apigee_projects_provision_organization_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_projects_provision_organization_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}:provisionOrganization
/// Provisions a new Apigee organization with a functioning runtime. This is the standard way to create trial organizations for a free Apigee trial.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `apigee_projects_provision_organization_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `apigee_projects_provision_organization_task()`.
/// For the simplest API, use `apigee_projects_provision_organization()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `apigee_projects_provision_organization_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn apigee_projects_provision_organization_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = apigee_projects_provision_organization_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`apigee_projects_provision_organization`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ApigeeProjectsProvisionOrganizationArgs {
    /// Path parameter: project
    pub project: String,
    /// Request body.
    pub body: GoogleCloudApigeeV1ProvisionOrganizationRequest,
}

/// GET v1/projects/{projectsId}:provisionOrganization
/// Provisions a new Apigee organization with a functioning runtime. This is the standard way to create trial organizations for a free Apigee trial.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `apigee_projects_provision_organization_builder()` + `apigee_projects_provision_organization_execute()`.
/// For task-level control, use `apigee_projects_provision_organization_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigee_projects_provision_organization(
    client: &SimpleHttpClient,
    args: &ApigeeProjectsProvisionOrganizationArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        apigee_projects_provision_organization_builder(client, &args.project, &args.body)?;
    apigee_projects_provision_organization_execute(builder)
}
