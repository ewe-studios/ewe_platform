//! Auto-generated API clients for gcp/apigeeregistry.
//!
//! Generated by `cargo run --bin ewe_platform gen_provider_clients`.
//! DO NOT EDIT MANUALLY.
//!
//! Feature flag: `gcp`

#![cfg(feature = "gcp")]

use crate::providers::gcp::clients::types::*;
use crate::providers::gcp::resources::*;
use foundation_core::valtron::{
    execute, BoxedSendExecutionAction, StreamIterator, StreamIteratorExt, TaskIterator,
    TaskIteratorExt,
};
use foundation_core::wire::simple_http::client::{
    body_reader, ClientRequestBuilder, RequestIntro, SimpleHttpClient, SystemDnsResolver,
};
use foundation_db::state::resource_identifier::ResourceIdentifier;
use foundation_macros::JsonHash;
use serde::Serialize;

/// GET v1/projects/{projectsId}/locations/{locationsId}
/// Gets information about a location.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `apigeeregistry_projects_locations_get_execute()` to send, or `apigeeregistry_projects_locations_get` for simplest API.

pub fn apigeeregistry_projects_locations_get_builder(
    client: &SimpleHttpClient,
    name: &String,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://apigeeregistry.googleapis.com/v1/projects/{}/locations/{locationsId}",
        name,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}
/// Gets information about a location.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `apigeeregistry_projects_locations_get_execute()` or `apigeeregistry_projects_locations_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `apigeeregistry_projects_locations_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigeeregistry_projects_locations_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Location>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Location = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}
/// Gets information about a location.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `apigeeregistry_projects_locations_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `apigeeregistry_projects_locations_get_task()`.
/// For the simplest API, use `apigeeregistry_projects_locations_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `apigeeregistry_projects_locations_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn apigeeregistry_projects_locations_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Location>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = apigeeregistry_projects_locations_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`apigeeregistry_projects_locations_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ApigeeregistryProjectsLocationsGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}
/// Gets information about a location.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `apigeeregistry_projects_locations_get_builder()` + `apigeeregistry_projects_locations_get_execute()`.
/// For task-level control, use `apigeeregistry_projects_locations_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigeeregistry_projects_locations_get(
    client: &SimpleHttpClient,
    args: &ApigeeregistryProjectsLocationsGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Location>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = apigeeregistry_projects_locations_get_builder(client, &args.name)?;
    apigeeregistry_projects_locations_get_execute(builder)
}

/// GET v1/projects/{projectsId}/locations
/// Lists information about the supported locations for this service.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `apigeeregistry_projects_locations_list_execute()` to send, or `apigeeregistry_projects_locations_list` for simplest API.

pub fn apigeeregistry_projects_locations_list_builder(
    client: &SimpleHttpClient,
    name: &String,
    filter: &Option<Option<String>>,
    pageSize: &Option<Option<String>>,
    pageToken: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://apigeeregistry.googleapis.com/v1/projects/{}/locations",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter.as_ref() {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = pageSize.as_ref() {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations
/// Lists information about the supported locations for this service.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `apigeeregistry_projects_locations_list_execute()` or `apigeeregistry_projects_locations_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `apigeeregistry_projects_locations_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigeeregistry_projects_locations_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ListLocationsResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListLocationsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations
/// Lists information about the supported locations for this service.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `apigeeregistry_projects_locations_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `apigeeregistry_projects_locations_list_task()`.
/// For the simplest API, use `apigeeregistry_projects_locations_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `apigeeregistry_projects_locations_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn apigeeregistry_projects_locations_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListLocationsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = apigeeregistry_projects_locations_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`apigeeregistry_projects_locations_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ApigeeregistryProjectsLocationsListArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: filter
    pub filter: Option<Option<String>>,
    /// Query parameter: pageSize
    pub pageSize: Option<Option<String>>,
    /// Query parameter: pageToken
    pub pageToken: Option<Option<String>>,
}

/// GET v1/projects/{projectsId}/locations
/// Lists information about the supported locations for this service.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `apigeeregistry_projects_locations_list_builder()` + `apigeeregistry_projects_locations_list_execute()`.
/// For task-level control, use `apigeeregistry_projects_locations_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigeeregistry_projects_locations_list(
    client: &SimpleHttpClient,
    args: &ApigeeregistryProjectsLocationsListArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListLocationsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = apigeeregistry_projects_locations_list_builder(
        client,
        &args.name,
        &args.filter,
        &args.pageSize,
        &args.pageToken,
    )?;
    apigeeregistry_projects_locations_list_execute(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/apis
/// Creates a specified API.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `apigeeregistry_projects_locations_apis_create_execute()` to send, or `apigeeregistry_projects_locations_apis_create` for simplest API.

pub fn apigeeregistry_projects_locations_apis_create_builder(
    client: &SimpleHttpClient,
    parent: &String,
    apiId: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://apigeeregistry.googleapis.com/v1/projects/{}/locations/{locationsId}/apis",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = apiId.as_ref() {
        query_parts.push(format!("apiId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .post(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/apis
/// Creates a specified API.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `apigeeregistry_projects_locations_apis_create_execute()` or `apigeeregistry_projects_locations_apis_create`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `apigeeregistry_projects_locations_apis_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigeeregistry_projects_locations_apis_create_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Api>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Api = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/apis
/// Creates a specified API.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `apigeeregistry_projects_locations_apis_create_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `apigeeregistry_projects_locations_apis_create_task()`.
/// For the simplest API, use `apigeeregistry_projects_locations_apis_create()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `apigeeregistry_projects_locations_apis_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn apigeeregistry_projects_locations_apis_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Api>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = apigeeregistry_projects_locations_apis_create_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`apigeeregistry_projects_locations_apis_create`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ApigeeregistryProjectsLocationsApisCreateArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: apiId
    pub apiId: Option<Option<String>>,
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/apis
/// Creates a specified API.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `apigeeregistry_projects_locations_apis_create_builder()` + `apigeeregistry_projects_locations_apis_create_execute()`.
/// For task-level control, use `apigeeregistry_projects_locations_apis_create_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigeeregistry_projects_locations_apis_create(
    client: &SimpleHttpClient,
    args: &ApigeeregistryProjectsLocationsApisCreateArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Api>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        apigeeregistry_projects_locations_apis_create_builder(client, &args.parent, &args.apiId)?;
    apigeeregistry_projects_locations_apis_create_execute(builder)
}

/// DELETE v1/projects/{projectsId}/locations/{locationsId}/apis/{apisId}
/// Removes a specified API and all of the resources that it owns.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `apigeeregistry_projects_locations_apis_delete_execute()` to send, or `apigeeregistry_projects_locations_apis_delete` for simplest API.

pub fn apigeeregistry_projects_locations_apis_delete_builder(
    client: &SimpleHttpClient,
    name: &String,
    force: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://apigeeregistry.googleapis.com/v1/projects/{}/locations/{locationsId}/apis/{apisId}",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = force.as_ref() {
        query_parts.push(format!("force={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .delete(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// DELETE v1/projects/{projectsId}/locations/{locationsId}/apis/{apisId}
/// Removes a specified API and all of the resources that it owns.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `apigeeregistry_projects_locations_apis_delete_execute()` or `apigeeregistry_projects_locations_apis_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `apigeeregistry_projects_locations_apis_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigeeregistry_projects_locations_apis_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Empty>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Empty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// DELETE v1/projects/{projectsId}/locations/{locationsId}/apis/{apisId}
/// Removes a specified API and all of the resources that it owns.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `apigeeregistry_projects_locations_apis_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `apigeeregistry_projects_locations_apis_delete_task()`.
/// For the simplest API, use `apigeeregistry_projects_locations_apis_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `apigeeregistry_projects_locations_apis_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn apigeeregistry_projects_locations_apis_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = apigeeregistry_projects_locations_apis_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`apigeeregistry_projects_locations_apis_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ApigeeregistryProjectsLocationsApisDeleteArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: force
    pub force: Option<Option<String>>,
}

/// DELETE v1/projects/{projectsId}/locations/{locationsId}/apis/{apisId}
/// Removes a specified API and all of the resources that it owns.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `apigeeregistry_projects_locations_apis_delete_builder()` + `apigeeregistry_projects_locations_apis_delete_execute()`.
/// For task-level control, use `apigeeregistry_projects_locations_apis_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigeeregistry_projects_locations_apis_delete(
    client: &SimpleHttpClient,
    args: &ApigeeregistryProjectsLocationsApisDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        apigeeregistry_projects_locations_apis_delete_builder(client, &args.name, &args.force)?;
    apigeeregistry_projects_locations_apis_delete_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/apis/{apisId}
/// Returns a specified API.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `apigeeregistry_projects_locations_apis_get_execute()` to send, or `apigeeregistry_projects_locations_apis_get` for simplest API.

pub fn apigeeregistry_projects_locations_apis_get_builder(
    client: &SimpleHttpClient,
    name: &String,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://apigeeregistry.googleapis.com/v1/projects/{}/locations/{locationsId}/apis/{apisId}",
        name,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/apis/{apisId}
/// Returns a specified API.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `apigeeregistry_projects_locations_apis_get_execute()` or `apigeeregistry_projects_locations_apis_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `apigeeregistry_projects_locations_apis_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigeeregistry_projects_locations_apis_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Api>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Api = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/apis/{apisId}
/// Returns a specified API.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `apigeeregistry_projects_locations_apis_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `apigeeregistry_projects_locations_apis_get_task()`.
/// For the simplest API, use `apigeeregistry_projects_locations_apis_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `apigeeregistry_projects_locations_apis_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn apigeeregistry_projects_locations_apis_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Api>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = apigeeregistry_projects_locations_apis_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`apigeeregistry_projects_locations_apis_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ApigeeregistryProjectsLocationsApisGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/apis/{apisId}
/// Returns a specified API.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `apigeeregistry_projects_locations_apis_get_builder()` + `apigeeregistry_projects_locations_apis_get_execute()`.
/// For task-level control, use `apigeeregistry_projects_locations_apis_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigeeregistry_projects_locations_apis_get(
    client: &SimpleHttpClient,
    args: &ApigeeregistryProjectsLocationsApisGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Api>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = apigeeregistry_projects_locations_apis_get_builder(client, &args.name)?;
    apigeeregistry_projects_locations_apis_get_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/apis/{apisId}:getIamPolicy
/// Gets the access control policy for a resource. Returns an empty policy if the resource exists and does not have a policy set.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `apigeeregistry_projects_locations_apis_get_iam_policy_execute()` to send, or `apigeeregistry_projects_locations_apis_get_iam_policy` for simplest API.

pub fn apigeeregistry_projects_locations_apis_get_iam_policy_builder(
    client: &SimpleHttpClient,
    resource: &String,
    options_requestedPolicyVersion: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://apigeeregistry.googleapis.com/v1/projects/{}/locations/{locationsId}/apis/{apisId}:getIamPolicy",
        resource,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = options_requestedPolicyVersion.as_ref() {
        query_parts.push(format!("options.requestedPolicyVersion={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/apis/{apisId}:getIamPolicy
/// Gets the access control policy for a resource. Returns an empty policy if the resource exists and does not have a policy set.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `apigeeregistry_projects_locations_apis_get_iam_policy_execute()` or `apigeeregistry_projects_locations_apis_get_iam_policy`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `apigeeregistry_projects_locations_apis_get_iam_policy_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigeeregistry_projects_locations_apis_get_iam_policy_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Policy>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Policy = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/apis/{apisId}:getIamPolicy
/// Gets the access control policy for a resource. Returns an empty policy if the resource exists and does not have a policy set.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `apigeeregistry_projects_locations_apis_get_iam_policy_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `apigeeregistry_projects_locations_apis_get_iam_policy_task()`.
/// For the simplest API, use `apigeeregistry_projects_locations_apis_get_iam_policy()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `apigeeregistry_projects_locations_apis_get_iam_policy_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn apigeeregistry_projects_locations_apis_get_iam_policy_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Policy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = apigeeregistry_projects_locations_apis_get_iam_policy_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`apigeeregistry_projects_locations_apis_get_iam_policy`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ApigeeregistryProjectsLocationsApisGetIamPolicyArgs {
    /// Path parameter: resource
    pub resource: String,
    /// Query parameter: options_requestedPolicyVersion
    pub options_requestedPolicyVersion: Option<Option<String>>,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/apis/{apisId}:getIamPolicy
/// Gets the access control policy for a resource. Returns an empty policy if the resource exists and does not have a policy set.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `apigeeregistry_projects_locations_apis_get_iam_policy_builder()` + `apigeeregistry_projects_locations_apis_get_iam_policy_execute()`.
/// For task-level control, use `apigeeregistry_projects_locations_apis_get_iam_policy_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigeeregistry_projects_locations_apis_get_iam_policy(
    client: &SimpleHttpClient,
    args: &ApigeeregistryProjectsLocationsApisGetIamPolicyArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Policy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = apigeeregistry_projects_locations_apis_get_iam_policy_builder(
        client,
        &args.resource,
        &args.options_requestedPolicyVersion,
    )?;
    apigeeregistry_projects_locations_apis_get_iam_policy_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/apis
/// Returns matching APIs.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `apigeeregistry_projects_locations_apis_list_execute()` to send, or `apigeeregistry_projects_locations_apis_list` for simplest API.

pub fn apigeeregistry_projects_locations_apis_list_builder(
    client: &SimpleHttpClient,
    parent: &String,
    filter: &Option<Option<String>>,
    orderBy: &Option<Option<String>>,
    pageSize: &Option<Option<String>>,
    pageToken: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://apigeeregistry.googleapis.com/v1/projects/{}/locations/{locationsId}/apis",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter.as_ref() {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = orderBy.as_ref() {
        query_parts.push(format!("orderBy={}", val));
    }
    if let Some(val) = pageSize.as_ref() {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/apis
/// Returns matching APIs.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `apigeeregistry_projects_locations_apis_list_execute()` or `apigeeregistry_projects_locations_apis_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `apigeeregistry_projects_locations_apis_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigeeregistry_projects_locations_apis_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ListApisResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListApisResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/apis
/// Returns matching APIs.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `apigeeregistry_projects_locations_apis_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `apigeeregistry_projects_locations_apis_list_task()`.
/// For the simplest API, use `apigeeregistry_projects_locations_apis_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `apigeeregistry_projects_locations_apis_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn apigeeregistry_projects_locations_apis_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListApisResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = apigeeregistry_projects_locations_apis_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`apigeeregistry_projects_locations_apis_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ApigeeregistryProjectsLocationsApisListArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: filter
    pub filter: Option<Option<String>>,
    /// Query parameter: orderBy
    pub orderBy: Option<Option<String>>,
    /// Query parameter: pageSize
    pub pageSize: Option<Option<String>>,
    /// Query parameter: pageToken
    pub pageToken: Option<Option<String>>,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/apis
/// Returns matching APIs.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `apigeeregistry_projects_locations_apis_list_builder()` + `apigeeregistry_projects_locations_apis_list_execute()`.
/// For task-level control, use `apigeeregistry_projects_locations_apis_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigeeregistry_projects_locations_apis_list(
    client: &SimpleHttpClient,
    args: &ApigeeregistryProjectsLocationsApisListArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListApisResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = apigeeregistry_projects_locations_apis_list_builder(
        client,
        &args.parent,
        &args.filter,
        &args.orderBy,
        &args.pageSize,
        &args.pageToken,
    )?;
    apigeeregistry_projects_locations_apis_list_execute(builder)
}

/// PATCH v1/projects/{projectsId}/locations/{locationsId}/apis/{apisId}
/// Used to modify a specified API.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `apigeeregistry_projects_locations_apis_patch_execute()` to send, or `apigeeregistry_projects_locations_apis_patch` for simplest API.

pub fn apigeeregistry_projects_locations_apis_patch_builder(
    client: &SimpleHttpClient,
    name: &String,
    allowMissing: &Option<Option<String>>,
    updateMask: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://apigeeregistry.googleapis.com/v1/projects/{}/locations/{locationsId}/apis/{apisId}",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = allowMissing.as_ref() {
        query_parts.push(format!("allowMissing={}", val));
    }
    if let Some(val) = updateMask.as_ref() {
        query_parts.push(format!("updateMask={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .patch(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// PATCH v1/projects/{projectsId}/locations/{locationsId}/apis/{apisId}
/// Used to modify a specified API.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `apigeeregistry_projects_locations_apis_patch_execute()` or `apigeeregistry_projects_locations_apis_patch`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `apigeeregistry_projects_locations_apis_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigeeregistry_projects_locations_apis_patch_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Api>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Api = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// PATCH v1/projects/{projectsId}/locations/{locationsId}/apis/{apisId}
/// Used to modify a specified API.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `apigeeregistry_projects_locations_apis_patch_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `apigeeregistry_projects_locations_apis_patch_task()`.
/// For the simplest API, use `apigeeregistry_projects_locations_apis_patch()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `apigeeregistry_projects_locations_apis_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn apigeeregistry_projects_locations_apis_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Api>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = apigeeregistry_projects_locations_apis_patch_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`apigeeregistry_projects_locations_apis_patch`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ApigeeregistryProjectsLocationsApisPatchArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: allowMissing
    pub allowMissing: Option<Option<String>>,
    /// Query parameter: updateMask
    pub updateMask: Option<Option<String>>,
}

/// PATCH v1/projects/{projectsId}/locations/{locationsId}/apis/{apisId}
/// Used to modify a specified API.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `apigeeregistry_projects_locations_apis_patch_builder()` + `apigeeregistry_projects_locations_apis_patch_execute()`.
/// For task-level control, use `apigeeregistry_projects_locations_apis_patch_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigeeregistry_projects_locations_apis_patch(
    client: &SimpleHttpClient,
    args: &ApigeeregistryProjectsLocationsApisPatchArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Api>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = apigeeregistry_projects_locations_apis_patch_builder(
        client,
        &args.name,
        &args.allowMissing,
        &args.updateMask,
    )?;
    apigeeregistry_projects_locations_apis_patch_execute(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/apis/{apisId}:setIamPolicy
/// Sets the access control policy on the specified resource. Replaces any existing policy. Can return NOT_FOUND, INVALID_ARGUMENT, and PERMISSION_DENIED errors.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `apigeeregistry_projects_locations_apis_set_iam_policy_execute()` to send, or `apigeeregistry_projects_locations_apis_set_iam_policy` for simplest API.

pub fn apigeeregistry_projects_locations_apis_set_iam_policy_builder(
    client: &SimpleHttpClient,
    resource: &String,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://apigeeregistry.googleapis.com/v1/projects/{}/locations/{locationsId}/apis/{apisId}:setIamPolicy",
        resource,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/apis/{apisId}:setIamPolicy
/// Sets the access control policy on the specified resource. Replaces any existing policy. Can return NOT_FOUND, INVALID_ARGUMENT, and PERMISSION_DENIED errors.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `apigeeregistry_projects_locations_apis_set_iam_policy_execute()` or `apigeeregistry_projects_locations_apis_set_iam_policy`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `apigeeregistry_projects_locations_apis_set_iam_policy_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigeeregistry_projects_locations_apis_set_iam_policy_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Policy>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Policy = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/apis/{apisId}:setIamPolicy
/// Sets the access control policy on the specified resource. Replaces any existing policy. Can return NOT_FOUND, INVALID_ARGUMENT, and PERMISSION_DENIED errors.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `apigeeregistry_projects_locations_apis_set_iam_policy_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `apigeeregistry_projects_locations_apis_set_iam_policy_task()`.
/// For the simplest API, use `apigeeregistry_projects_locations_apis_set_iam_policy()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `apigeeregistry_projects_locations_apis_set_iam_policy_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn apigeeregistry_projects_locations_apis_set_iam_policy_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Policy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = apigeeregistry_projects_locations_apis_set_iam_policy_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`apigeeregistry_projects_locations_apis_set_iam_policy`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ApigeeregistryProjectsLocationsApisSetIamPolicyArgs {
    /// Path parameter: resource
    pub resource: String,
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/apis/{apisId}:setIamPolicy
/// Sets the access control policy on the specified resource. Replaces any existing policy. Can return NOT_FOUND, INVALID_ARGUMENT, and PERMISSION_DENIED errors.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `apigeeregistry_projects_locations_apis_set_iam_policy_builder()` + `apigeeregistry_projects_locations_apis_set_iam_policy_execute()`.
/// For task-level control, use `apigeeregistry_projects_locations_apis_set_iam_policy_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigeeregistry_projects_locations_apis_set_iam_policy(
    client: &SimpleHttpClient,
    args: &ApigeeregistryProjectsLocationsApisSetIamPolicyArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Policy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        apigeeregistry_projects_locations_apis_set_iam_policy_builder(client, &args.resource)?;
    apigeeregistry_projects_locations_apis_set_iam_policy_execute(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/apis/{apisId}:testIamPermissions
/// Returns permissions that a caller has on the specified resource. If the resource does not exist, this will return an empty set of permissions, not a NOT_FOUND error. Note: This operation is designed to be used for building permission-aware UIs and command-line tools, not for authorization checking. This operation may "fail open" without warning.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `apigeeregistry_projects_locations_apis_test_iam_permissions_execute()` to send, or `apigeeregistry_projects_locations_apis_test_iam_permissions` for simplest API.

pub fn apigeeregistry_projects_locations_apis_test_iam_permissions_builder(
    client: &SimpleHttpClient,
    resource: &String,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://apigeeregistry.googleapis.com/v1/projects/{}/locations/{locationsId}/apis/{apisId}:testIamPermissions",
        resource,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/apis/{apisId}:testIamPermissions
/// Returns permissions that a caller has on the specified resource. If the resource does not exist, this will return an empty set of permissions, not a NOT_FOUND error. Note: This operation is designed to be used for building permission-aware UIs and command-line tools, not for authorization checking. This operation may "fail open" without warning.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `apigeeregistry_projects_locations_apis_test_iam_permissions_execute()` or `apigeeregistry_projects_locations_apis_test_iam_permissions`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `apigeeregistry_projects_locations_apis_test_iam_permissions_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigeeregistry_projects_locations_apis_test_iam_permissions_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<TestIamPermissionsResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: TestIamPermissionsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/apis/{apisId}:testIamPermissions
/// Returns permissions that a caller has on the specified resource. If the resource does not exist, this will return an empty set of permissions, not a NOT_FOUND error. Note: This operation is designed to be used for building permission-aware UIs and command-line tools, not for authorization checking. This operation may "fail open" without warning.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `apigeeregistry_projects_locations_apis_test_iam_permissions_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `apigeeregistry_projects_locations_apis_test_iam_permissions_task()`.
/// For the simplest API, use `apigeeregistry_projects_locations_apis_test_iam_permissions()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `apigeeregistry_projects_locations_apis_test_iam_permissions_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn apigeeregistry_projects_locations_apis_test_iam_permissions_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<TestIamPermissionsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = apigeeregistry_projects_locations_apis_test_iam_permissions_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`apigeeregistry_projects_locations_apis_test_iam_permissions`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ApigeeregistryProjectsLocationsApisTestIamPermissionsArgs {
    /// Path parameter: resource
    pub resource: String,
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/apis/{apisId}:testIamPermissions
/// Returns permissions that a caller has on the specified resource. If the resource does not exist, this will return an empty set of permissions, not a NOT_FOUND error. Note: This operation is designed to be used for building permission-aware UIs and command-line tools, not for authorization checking. This operation may "fail open" without warning.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `apigeeregistry_projects_locations_apis_test_iam_permissions_builder()` + `apigeeregistry_projects_locations_apis_test_iam_permissions_execute()`.
/// For task-level control, use `apigeeregistry_projects_locations_apis_test_iam_permissions_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigeeregistry_projects_locations_apis_test_iam_permissions(
    client: &SimpleHttpClient,
    args: &ApigeeregistryProjectsLocationsApisTestIamPermissionsArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<TestIamPermissionsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = apigeeregistry_projects_locations_apis_test_iam_permissions_builder(
        client,
        &args.resource,
    )?;
    apigeeregistry_projects_locations_apis_test_iam_permissions_execute(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/apis/{apisId}/artifacts
/// Creates a specified artifact.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `apigeeregistry_projects_locations_apis_artifacts_create_execute()` to send, or `apigeeregistry_projects_locations_apis_artifacts_create` for simplest API.

pub fn apigeeregistry_projects_locations_apis_artifacts_create_builder(
    client: &SimpleHttpClient,
    parent: &String,
    artifactId: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://apigeeregistry.googleapis.com/v1/projects/{}/locations/{locationsId}/apis/{apisId}/artifacts",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = artifactId.as_ref() {
        query_parts.push(format!("artifactId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .post(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/apis/{apisId}/artifacts
/// Creates a specified artifact.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `apigeeregistry_projects_locations_apis_artifacts_create_execute()` or `apigeeregistry_projects_locations_apis_artifacts_create`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `apigeeregistry_projects_locations_apis_artifacts_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigeeregistry_projects_locations_apis_artifacts_create_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Artifact>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Artifact = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/apis/{apisId}/artifacts
/// Creates a specified artifact.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `apigeeregistry_projects_locations_apis_artifacts_create_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `apigeeregistry_projects_locations_apis_artifacts_create_task()`.
/// For the simplest API, use `apigeeregistry_projects_locations_apis_artifacts_create()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `apigeeregistry_projects_locations_apis_artifacts_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn apigeeregistry_projects_locations_apis_artifacts_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Artifact>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = apigeeregistry_projects_locations_apis_artifacts_create_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`apigeeregistry_projects_locations_apis_artifacts_create`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ApigeeregistryProjectsLocationsApisArtifactsCreateArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: artifactId
    pub artifactId: Option<Option<String>>,
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/apis/{apisId}/artifacts
/// Creates a specified artifact.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `apigeeregistry_projects_locations_apis_artifacts_create_builder()` + `apigeeregistry_projects_locations_apis_artifacts_create_execute()`.
/// For task-level control, use `apigeeregistry_projects_locations_apis_artifacts_create_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigeeregistry_projects_locations_apis_artifacts_create(
    client: &SimpleHttpClient,
    args: &ApigeeregistryProjectsLocationsApisArtifactsCreateArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Artifact>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = apigeeregistry_projects_locations_apis_artifacts_create_builder(
        client,
        &args.parent,
        &args.artifactId,
    )?;
    apigeeregistry_projects_locations_apis_artifacts_create_execute(builder)
}

/// DELETE v1/projects/{projectsId}/locations/{locationsId}/apis/{apisId}/artifacts/{artifactsId}
/// Removes a specified artifact.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `apigeeregistry_projects_locations_apis_artifacts_delete_execute()` to send, or `apigeeregistry_projects_locations_apis_artifacts_delete` for simplest API.

pub fn apigeeregistry_projects_locations_apis_artifacts_delete_builder(
    client: &SimpleHttpClient,
    name: &String,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://apigeeregistry.googleapis.com/v1/projects/{}/locations/{locationsId}/apis/{apisId}/artifacts/{artifactsId}",
        name,
    );

    // Build request
    let builder = client
        .delete(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// DELETE v1/projects/{projectsId}/locations/{locationsId}/apis/{apisId}/artifacts/{artifactsId}
/// Removes a specified artifact.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `apigeeregistry_projects_locations_apis_artifacts_delete_execute()` or `apigeeregistry_projects_locations_apis_artifacts_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `apigeeregistry_projects_locations_apis_artifacts_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigeeregistry_projects_locations_apis_artifacts_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Empty>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Empty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// DELETE v1/projects/{projectsId}/locations/{locationsId}/apis/{apisId}/artifacts/{artifactsId}
/// Removes a specified artifact.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `apigeeregistry_projects_locations_apis_artifacts_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `apigeeregistry_projects_locations_apis_artifacts_delete_task()`.
/// For the simplest API, use `apigeeregistry_projects_locations_apis_artifacts_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `apigeeregistry_projects_locations_apis_artifacts_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn apigeeregistry_projects_locations_apis_artifacts_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = apigeeregistry_projects_locations_apis_artifacts_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`apigeeregistry_projects_locations_apis_artifacts_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ApigeeregistryProjectsLocationsApisArtifactsDeleteArgs {
    /// Path parameter: name
    pub name: String,
}

/// DELETE v1/projects/{projectsId}/locations/{locationsId}/apis/{apisId}/artifacts/{artifactsId}
/// Removes a specified artifact.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `apigeeregistry_projects_locations_apis_artifacts_delete_builder()` + `apigeeregistry_projects_locations_apis_artifacts_delete_execute()`.
/// For task-level control, use `apigeeregistry_projects_locations_apis_artifacts_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigeeregistry_projects_locations_apis_artifacts_delete(
    client: &SimpleHttpClient,
    args: &ApigeeregistryProjectsLocationsApisArtifactsDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        apigeeregistry_projects_locations_apis_artifacts_delete_builder(client, &args.name)?;
    apigeeregistry_projects_locations_apis_artifacts_delete_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/apis/{apisId}/artifacts/{artifactsId}
/// Returns a specified artifact.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `apigeeregistry_projects_locations_apis_artifacts_get_execute()` to send, or `apigeeregistry_projects_locations_apis_artifacts_get` for simplest API.

pub fn apigeeregistry_projects_locations_apis_artifacts_get_builder(
    client: &SimpleHttpClient,
    name: &String,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://apigeeregistry.googleapis.com/v1/projects/{}/locations/{locationsId}/apis/{apisId}/artifacts/{artifactsId}",
        name,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/apis/{apisId}/artifacts/{artifactsId}
/// Returns a specified artifact.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `apigeeregistry_projects_locations_apis_artifacts_get_execute()` or `apigeeregistry_projects_locations_apis_artifacts_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `apigeeregistry_projects_locations_apis_artifacts_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigeeregistry_projects_locations_apis_artifacts_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Artifact>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Artifact = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/apis/{apisId}/artifacts/{artifactsId}
/// Returns a specified artifact.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `apigeeregistry_projects_locations_apis_artifacts_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `apigeeregistry_projects_locations_apis_artifacts_get_task()`.
/// For the simplest API, use `apigeeregistry_projects_locations_apis_artifacts_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `apigeeregistry_projects_locations_apis_artifacts_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn apigeeregistry_projects_locations_apis_artifacts_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Artifact>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = apigeeregistry_projects_locations_apis_artifacts_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`apigeeregistry_projects_locations_apis_artifacts_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ApigeeregistryProjectsLocationsApisArtifactsGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/apis/{apisId}/artifacts/{artifactsId}
/// Returns a specified artifact.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `apigeeregistry_projects_locations_apis_artifacts_get_builder()` + `apigeeregistry_projects_locations_apis_artifacts_get_execute()`.
/// For task-level control, use `apigeeregistry_projects_locations_apis_artifacts_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigeeregistry_projects_locations_apis_artifacts_get(
    client: &SimpleHttpClient,
    args: &ApigeeregistryProjectsLocationsApisArtifactsGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Artifact>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = apigeeregistry_projects_locations_apis_artifacts_get_builder(client, &args.name)?;
    apigeeregistry_projects_locations_apis_artifacts_get_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/apis/{apisId}/artifacts/{artifactsId}:getContents
/// Returns the contents of a specified artifact. If artifacts are stored with GZip compression, the default behavior is to return the artifact uncompressed (the mime_type response field indicates the exact format returned).
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `apigeeregistry_projects_locations_apis_artifacts_get_contents_execute()` to send, or `apigeeregistry_projects_locations_apis_artifacts_get_contents` for simplest API.

pub fn apigeeregistry_projects_locations_apis_artifacts_get_contents_builder(
    client: &SimpleHttpClient,
    name: &String,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://apigeeregistry.googleapis.com/v1/projects/{}/locations/{locationsId}/apis/{apisId}/artifacts/{artifactsId}:getContents",
        name,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/apis/{apisId}/artifacts/{artifactsId}:getContents
/// Returns the contents of a specified artifact. If artifacts are stored with GZip compression, the default behavior is to return the artifact uncompressed (the mime_type response field indicates the exact format returned).
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `apigeeregistry_projects_locations_apis_artifacts_get_contents_execute()` or `apigeeregistry_projects_locations_apis_artifacts_get_contents`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `apigeeregistry_projects_locations_apis_artifacts_get_contents_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigeeregistry_projects_locations_apis_artifacts_get_contents_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<HttpBody>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: HttpBody = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/apis/{apisId}/artifacts/{artifactsId}:getContents
/// Returns the contents of a specified artifact. If artifacts are stored with GZip compression, the default behavior is to return the artifact uncompressed (the mime_type response field indicates the exact format returned).
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `apigeeregistry_projects_locations_apis_artifacts_get_contents_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `apigeeregistry_projects_locations_apis_artifacts_get_contents_task()`.
/// For the simplest API, use `apigeeregistry_projects_locations_apis_artifacts_get_contents()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `apigeeregistry_projects_locations_apis_artifacts_get_contents_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn apigeeregistry_projects_locations_apis_artifacts_get_contents_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<HttpBody>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = apigeeregistry_projects_locations_apis_artifacts_get_contents_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`apigeeregistry_projects_locations_apis_artifacts_get_contents`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ApigeeregistryProjectsLocationsApisArtifactsGetContentsArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/apis/{apisId}/artifacts/{artifactsId}:getContents
/// Returns the contents of a specified artifact. If artifacts are stored with GZip compression, the default behavior is to return the artifact uncompressed (the mime_type response field indicates the exact format returned).
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `apigeeregistry_projects_locations_apis_artifacts_get_contents_builder()` + `apigeeregistry_projects_locations_apis_artifacts_get_contents_execute()`.
/// For task-level control, use `apigeeregistry_projects_locations_apis_artifacts_get_contents_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigeeregistry_projects_locations_apis_artifacts_get_contents(
    client: &SimpleHttpClient,
    args: &ApigeeregistryProjectsLocationsApisArtifactsGetContentsArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<HttpBody>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        apigeeregistry_projects_locations_apis_artifacts_get_contents_builder(client, &args.name)?;
    apigeeregistry_projects_locations_apis_artifacts_get_contents_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/apis/{apisId}/artifacts/{artifactsId}:getIamPolicy
/// Gets the access control policy for a resource. Returns an empty policy if the resource exists and does not have a policy set.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `apigeeregistry_projects_locations_apis_artifacts_get_iam_policy_execute()` to send, or `apigeeregistry_projects_locations_apis_artifacts_get_iam_policy` for simplest API.

pub fn apigeeregistry_projects_locations_apis_artifacts_get_iam_policy_builder(
    client: &SimpleHttpClient,
    resource: &String,
    options_requestedPolicyVersion: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://apigeeregistry.googleapis.com/v1/projects/{}/locations/{locationsId}/apis/{apisId}/artifacts/{artifactsId}:getIamPolicy",
        resource,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = options_requestedPolicyVersion.as_ref() {
        query_parts.push(format!("options.requestedPolicyVersion={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/apis/{apisId}/artifacts/{artifactsId}:getIamPolicy
/// Gets the access control policy for a resource. Returns an empty policy if the resource exists and does not have a policy set.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `apigeeregistry_projects_locations_apis_artifacts_get_iam_policy_execute()` or `apigeeregistry_projects_locations_apis_artifacts_get_iam_policy`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `apigeeregistry_projects_locations_apis_artifacts_get_iam_policy_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigeeregistry_projects_locations_apis_artifacts_get_iam_policy_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Policy>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Policy = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/apis/{apisId}/artifacts/{artifactsId}:getIamPolicy
/// Gets the access control policy for a resource. Returns an empty policy if the resource exists and does not have a policy set.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `apigeeregistry_projects_locations_apis_artifacts_get_iam_policy_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `apigeeregistry_projects_locations_apis_artifacts_get_iam_policy_task()`.
/// For the simplest API, use `apigeeregistry_projects_locations_apis_artifacts_get_iam_policy()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `apigeeregistry_projects_locations_apis_artifacts_get_iam_policy_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn apigeeregistry_projects_locations_apis_artifacts_get_iam_policy_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Policy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = apigeeregistry_projects_locations_apis_artifacts_get_iam_policy_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`apigeeregistry_projects_locations_apis_artifacts_get_iam_policy`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ApigeeregistryProjectsLocationsApisArtifactsGetIamPolicyArgs {
    /// Path parameter: resource
    pub resource: String,
    /// Query parameter: options_requestedPolicyVersion
    pub options_requestedPolicyVersion: Option<Option<String>>,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/apis/{apisId}/artifacts/{artifactsId}:getIamPolicy
/// Gets the access control policy for a resource. Returns an empty policy if the resource exists and does not have a policy set.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `apigeeregistry_projects_locations_apis_artifacts_get_iam_policy_builder()` + `apigeeregistry_projects_locations_apis_artifacts_get_iam_policy_execute()`.
/// For task-level control, use `apigeeregistry_projects_locations_apis_artifacts_get_iam_policy_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigeeregistry_projects_locations_apis_artifacts_get_iam_policy(
    client: &SimpleHttpClient,
    args: &ApigeeregistryProjectsLocationsApisArtifactsGetIamPolicyArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Policy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = apigeeregistry_projects_locations_apis_artifacts_get_iam_policy_builder(
        client,
        &args.resource,
        &args.options_requestedPolicyVersion,
    )?;
    apigeeregistry_projects_locations_apis_artifacts_get_iam_policy_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/apis/{apisId}/artifacts
/// Returns matching artifacts.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `apigeeregistry_projects_locations_apis_artifacts_list_execute()` to send, or `apigeeregistry_projects_locations_apis_artifacts_list` for simplest API.

pub fn apigeeregistry_projects_locations_apis_artifacts_list_builder(
    client: &SimpleHttpClient,
    parent: &String,
    filter: &Option<Option<String>>,
    orderBy: &Option<Option<String>>,
    pageSize: &Option<Option<String>>,
    pageToken: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://apigeeregistry.googleapis.com/v1/projects/{}/locations/{locationsId}/apis/{apisId}/artifacts",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter.as_ref() {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = orderBy.as_ref() {
        query_parts.push(format!("orderBy={}", val));
    }
    if let Some(val) = pageSize.as_ref() {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/apis/{apisId}/artifacts
/// Returns matching artifacts.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `apigeeregistry_projects_locations_apis_artifacts_list_execute()` or `apigeeregistry_projects_locations_apis_artifacts_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `apigeeregistry_projects_locations_apis_artifacts_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigeeregistry_projects_locations_apis_artifacts_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ListArtifactsResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListArtifactsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/apis/{apisId}/artifacts
/// Returns matching artifacts.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `apigeeregistry_projects_locations_apis_artifacts_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `apigeeregistry_projects_locations_apis_artifacts_list_task()`.
/// For the simplest API, use `apigeeregistry_projects_locations_apis_artifacts_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `apigeeregistry_projects_locations_apis_artifacts_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn apigeeregistry_projects_locations_apis_artifacts_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListArtifactsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = apigeeregistry_projects_locations_apis_artifacts_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`apigeeregistry_projects_locations_apis_artifacts_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ApigeeregistryProjectsLocationsApisArtifactsListArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: filter
    pub filter: Option<Option<String>>,
    /// Query parameter: orderBy
    pub orderBy: Option<Option<String>>,
    /// Query parameter: pageSize
    pub pageSize: Option<Option<String>>,
    /// Query parameter: pageToken
    pub pageToken: Option<Option<String>>,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/apis/{apisId}/artifacts
/// Returns matching artifacts.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `apigeeregistry_projects_locations_apis_artifacts_list_builder()` + `apigeeregistry_projects_locations_apis_artifacts_list_execute()`.
/// For task-level control, use `apigeeregistry_projects_locations_apis_artifacts_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigeeregistry_projects_locations_apis_artifacts_list(
    client: &SimpleHttpClient,
    args: &ApigeeregistryProjectsLocationsApisArtifactsListArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListArtifactsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = apigeeregistry_projects_locations_apis_artifacts_list_builder(
        client,
        &args.parent,
        &args.filter,
        &args.orderBy,
        &args.pageSize,
        &args.pageToken,
    )?;
    apigeeregistry_projects_locations_apis_artifacts_list_execute(builder)
}

/// PUT v1/projects/{projectsId}/locations/{locationsId}/apis/{apisId}/artifacts/{artifactsId}
/// Used to replace a specified artifact.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `apigeeregistry_projects_locations_apis_artifacts_replace_artifact_execute()` to send, or `apigeeregistry_projects_locations_apis_artifacts_replace_artifact` for simplest API.

pub fn apigeeregistry_projects_locations_apis_artifacts_replace_artifact_builder(
    client: &SimpleHttpClient,
    name: &String,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://apigeeregistry.googleapis.com/v1/projects/{}/locations/{locationsId}/apis/{apisId}/artifacts/{artifactsId}",
        name,
    );

    // Build request
    let builder = client
        .put(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// PUT v1/projects/{projectsId}/locations/{locationsId}/apis/{apisId}/artifacts/{artifactsId}
/// Used to replace a specified artifact.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `apigeeregistry_projects_locations_apis_artifacts_replace_artifact_execute()` or `apigeeregistry_projects_locations_apis_artifacts_replace_artifact`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `apigeeregistry_projects_locations_apis_artifacts_replace_artifact_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigeeregistry_projects_locations_apis_artifacts_replace_artifact_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Artifact>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Artifact = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// PUT v1/projects/{projectsId}/locations/{locationsId}/apis/{apisId}/artifacts/{artifactsId}
/// Used to replace a specified artifact.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `apigeeregistry_projects_locations_apis_artifacts_replace_artifact_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `apigeeregistry_projects_locations_apis_artifacts_replace_artifact_task()`.
/// For the simplest API, use `apigeeregistry_projects_locations_apis_artifacts_replace_artifact()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `apigeeregistry_projects_locations_apis_artifacts_replace_artifact_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn apigeeregistry_projects_locations_apis_artifacts_replace_artifact_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Artifact>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = apigeeregistry_projects_locations_apis_artifacts_replace_artifact_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`apigeeregistry_projects_locations_apis_artifacts_replace_artifact`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ApigeeregistryProjectsLocationsApisArtifactsReplaceArtifactArgs {
    /// Path parameter: name
    pub name: String,
}

/// PUT v1/projects/{projectsId}/locations/{locationsId}/apis/{apisId}/artifacts/{artifactsId}
/// Used to replace a specified artifact.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `apigeeregistry_projects_locations_apis_artifacts_replace_artifact_builder()` + `apigeeregistry_projects_locations_apis_artifacts_replace_artifact_execute()`.
/// For task-level control, use `apigeeregistry_projects_locations_apis_artifacts_replace_artifact_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigeeregistry_projects_locations_apis_artifacts_replace_artifact(
    client: &SimpleHttpClient,
    args: &ApigeeregistryProjectsLocationsApisArtifactsReplaceArtifactArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Artifact>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = apigeeregistry_projects_locations_apis_artifacts_replace_artifact_builder(
        client, &args.name,
    )?;
    apigeeregistry_projects_locations_apis_artifacts_replace_artifact_execute(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/apis/{apisId}/artifacts/{artifactsId}:setIamPolicy
/// Sets the access control policy on the specified resource. Replaces any existing policy. Can return NOT_FOUND, INVALID_ARGUMENT, and PERMISSION_DENIED errors.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `apigeeregistry_projects_locations_apis_artifacts_set_iam_policy_execute()` to send, or `apigeeregistry_projects_locations_apis_artifacts_set_iam_policy` for simplest API.

pub fn apigeeregistry_projects_locations_apis_artifacts_set_iam_policy_builder(
    client: &SimpleHttpClient,
    resource: &String,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://apigeeregistry.googleapis.com/v1/projects/{}/locations/{locationsId}/apis/{apisId}/artifacts/{artifactsId}:setIamPolicy",
        resource,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/apis/{apisId}/artifacts/{artifactsId}:setIamPolicy
/// Sets the access control policy on the specified resource. Replaces any existing policy. Can return NOT_FOUND, INVALID_ARGUMENT, and PERMISSION_DENIED errors.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `apigeeregistry_projects_locations_apis_artifacts_set_iam_policy_execute()` or `apigeeregistry_projects_locations_apis_artifacts_set_iam_policy`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `apigeeregistry_projects_locations_apis_artifacts_set_iam_policy_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigeeregistry_projects_locations_apis_artifacts_set_iam_policy_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Policy>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Policy = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/apis/{apisId}/artifacts/{artifactsId}:setIamPolicy
/// Sets the access control policy on the specified resource. Replaces any existing policy. Can return NOT_FOUND, INVALID_ARGUMENT, and PERMISSION_DENIED errors.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `apigeeregistry_projects_locations_apis_artifacts_set_iam_policy_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `apigeeregistry_projects_locations_apis_artifacts_set_iam_policy_task()`.
/// For the simplest API, use `apigeeregistry_projects_locations_apis_artifacts_set_iam_policy()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `apigeeregistry_projects_locations_apis_artifacts_set_iam_policy_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn apigeeregistry_projects_locations_apis_artifacts_set_iam_policy_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Policy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = apigeeregistry_projects_locations_apis_artifacts_set_iam_policy_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`apigeeregistry_projects_locations_apis_artifacts_set_iam_policy`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ApigeeregistryProjectsLocationsApisArtifactsSetIamPolicyArgs {
    /// Path parameter: resource
    pub resource: String,
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/apis/{apisId}/artifacts/{artifactsId}:setIamPolicy
/// Sets the access control policy on the specified resource. Replaces any existing policy. Can return NOT_FOUND, INVALID_ARGUMENT, and PERMISSION_DENIED errors.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `apigeeregistry_projects_locations_apis_artifacts_set_iam_policy_builder()` + `apigeeregistry_projects_locations_apis_artifacts_set_iam_policy_execute()`.
/// For task-level control, use `apigeeregistry_projects_locations_apis_artifacts_set_iam_policy_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigeeregistry_projects_locations_apis_artifacts_set_iam_policy(
    client: &SimpleHttpClient,
    args: &ApigeeregistryProjectsLocationsApisArtifactsSetIamPolicyArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Policy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = apigeeregistry_projects_locations_apis_artifacts_set_iam_policy_builder(
        client,
        &args.resource,
    )?;
    apigeeregistry_projects_locations_apis_artifacts_set_iam_policy_execute(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/apis/{apisId}/artifacts/{artifactsId}:testIamPermissions
/// Returns permissions that a caller has on the specified resource. If the resource does not exist, this will return an empty set of permissions, not a NOT_FOUND error. Note: This operation is designed to be used for building permission-aware UIs and command-line tools, not for authorization checking. This operation may "fail open" without warning.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `apigeeregistry_projects_locations_apis_artifacts_test_iam_permissions_execute()` to send, or `apigeeregistry_projects_locations_apis_artifacts_test_iam_permissions` for simplest API.

pub fn apigeeregistry_projects_locations_apis_artifacts_test_iam_permissions_builder(
    client: &SimpleHttpClient,
    resource: &String,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://apigeeregistry.googleapis.com/v1/projects/{}/locations/{locationsId}/apis/{apisId}/artifacts/{artifactsId}:testIamPermissions",
        resource,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/apis/{apisId}/artifacts/{artifactsId}:testIamPermissions
/// Returns permissions that a caller has on the specified resource. If the resource does not exist, this will return an empty set of permissions, not a NOT_FOUND error. Note: This operation is designed to be used for building permission-aware UIs and command-line tools, not for authorization checking. This operation may "fail open" without warning.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `apigeeregistry_projects_locations_apis_artifacts_test_iam_permissions_execute()` or `apigeeregistry_projects_locations_apis_artifacts_test_iam_permissions`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `apigeeregistry_projects_locations_apis_artifacts_test_iam_permissions_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigeeregistry_projects_locations_apis_artifacts_test_iam_permissions_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<TestIamPermissionsResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: TestIamPermissionsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/apis/{apisId}/artifacts/{artifactsId}:testIamPermissions
/// Returns permissions that a caller has on the specified resource. If the resource does not exist, this will return an empty set of permissions, not a NOT_FOUND error. Note: This operation is designed to be used for building permission-aware UIs and command-line tools, not for authorization checking. This operation may "fail open" without warning.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `apigeeregistry_projects_locations_apis_artifacts_test_iam_permissions_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `apigeeregistry_projects_locations_apis_artifacts_test_iam_permissions_task()`.
/// For the simplest API, use `apigeeregistry_projects_locations_apis_artifacts_test_iam_permissions()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `apigeeregistry_projects_locations_apis_artifacts_test_iam_permissions_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn apigeeregistry_projects_locations_apis_artifacts_test_iam_permissions_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<TestIamPermissionsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = apigeeregistry_projects_locations_apis_artifacts_test_iam_permissions_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`apigeeregistry_projects_locations_apis_artifacts_test_iam_permissions`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ApigeeregistryProjectsLocationsApisArtifactsTestIamPermissionsArgs {
    /// Path parameter: resource
    pub resource: String,
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/apis/{apisId}/artifacts/{artifactsId}:testIamPermissions
/// Returns permissions that a caller has on the specified resource. If the resource does not exist, this will return an empty set of permissions, not a NOT_FOUND error. Note: This operation is designed to be used for building permission-aware UIs and command-line tools, not for authorization checking. This operation may "fail open" without warning.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `apigeeregistry_projects_locations_apis_artifacts_test_iam_permissions_builder()` + `apigeeregistry_projects_locations_apis_artifacts_test_iam_permissions_execute()`.
/// For task-level control, use `apigeeregistry_projects_locations_apis_artifacts_test_iam_permissions_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigeeregistry_projects_locations_apis_artifacts_test_iam_permissions(
    client: &SimpleHttpClient,
    args: &ApigeeregistryProjectsLocationsApisArtifactsTestIamPermissionsArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<TestIamPermissionsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = apigeeregistry_projects_locations_apis_artifacts_test_iam_permissions_builder(
        client,
        &args.resource,
    )?;
    apigeeregistry_projects_locations_apis_artifacts_test_iam_permissions_execute(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/apis/{apisId}/deployments
/// Creates a specified deployment.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `apigeeregistry_projects_locations_apis_deployments_create_execute()` to send, or `apigeeregistry_projects_locations_apis_deployments_create` for simplest API.

pub fn apigeeregistry_projects_locations_apis_deployments_create_builder(
    client: &SimpleHttpClient,
    parent: &String,
    apiDeploymentId: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://apigeeregistry.googleapis.com/v1/projects/{}/locations/{locationsId}/apis/{apisId}/deployments",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = apiDeploymentId.as_ref() {
        query_parts.push(format!("apiDeploymentId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .post(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/apis/{apisId}/deployments
/// Creates a specified deployment.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `apigeeregistry_projects_locations_apis_deployments_create_execute()` or `apigeeregistry_projects_locations_apis_deployments_create`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `apigeeregistry_projects_locations_apis_deployments_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigeeregistry_projects_locations_apis_deployments_create_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ApiDeployment>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ApiDeployment = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/apis/{apisId}/deployments
/// Creates a specified deployment.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `apigeeregistry_projects_locations_apis_deployments_create_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `apigeeregistry_projects_locations_apis_deployments_create_task()`.
/// For the simplest API, use `apigeeregistry_projects_locations_apis_deployments_create()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `apigeeregistry_projects_locations_apis_deployments_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn apigeeregistry_projects_locations_apis_deployments_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ApiDeployment>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = apigeeregistry_projects_locations_apis_deployments_create_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`apigeeregistry_projects_locations_apis_deployments_create`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ApigeeregistryProjectsLocationsApisDeploymentsCreateArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: apiDeploymentId
    pub apiDeploymentId: Option<Option<String>>,
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/apis/{apisId}/deployments
/// Creates a specified deployment.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `apigeeregistry_projects_locations_apis_deployments_create_builder()` + `apigeeregistry_projects_locations_apis_deployments_create_execute()`.
/// For task-level control, use `apigeeregistry_projects_locations_apis_deployments_create_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigeeregistry_projects_locations_apis_deployments_create(
    client: &SimpleHttpClient,
    args: &ApigeeregistryProjectsLocationsApisDeploymentsCreateArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ApiDeployment>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = apigeeregistry_projects_locations_apis_deployments_create_builder(
        client,
        &args.parent,
        &args.apiDeploymentId,
    )?;
    apigeeregistry_projects_locations_apis_deployments_create_execute(builder)
}

/// DELETE v1/projects/{projectsId}/locations/{locationsId}/apis/{apisId}/deployments/{deploymentsId}
/// Removes a specified deployment, all revisions, and all child resources (e.g., artifacts).
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `apigeeregistry_projects_locations_apis_deployments_delete_execute()` to send, or `apigeeregistry_projects_locations_apis_deployments_delete` for simplest API.

pub fn apigeeregistry_projects_locations_apis_deployments_delete_builder(
    client: &SimpleHttpClient,
    name: &String,
    force: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://apigeeregistry.googleapis.com/v1/projects/{}/locations/{locationsId}/apis/{apisId}/deployments/{deploymentsId}",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = force.as_ref() {
        query_parts.push(format!("force={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .delete(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// DELETE v1/projects/{projectsId}/locations/{locationsId}/apis/{apisId}/deployments/{deploymentsId}
/// Removes a specified deployment, all revisions, and all child resources (e.g., artifacts).
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `apigeeregistry_projects_locations_apis_deployments_delete_execute()` or `apigeeregistry_projects_locations_apis_deployments_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `apigeeregistry_projects_locations_apis_deployments_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigeeregistry_projects_locations_apis_deployments_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Empty>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Empty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// DELETE v1/projects/{projectsId}/locations/{locationsId}/apis/{apisId}/deployments/{deploymentsId}
/// Removes a specified deployment, all revisions, and all child resources (e.g., artifacts).
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `apigeeregistry_projects_locations_apis_deployments_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `apigeeregistry_projects_locations_apis_deployments_delete_task()`.
/// For the simplest API, use `apigeeregistry_projects_locations_apis_deployments_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `apigeeregistry_projects_locations_apis_deployments_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn apigeeregistry_projects_locations_apis_deployments_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = apigeeregistry_projects_locations_apis_deployments_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`apigeeregistry_projects_locations_apis_deployments_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ApigeeregistryProjectsLocationsApisDeploymentsDeleteArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: force
    pub force: Option<Option<String>>,
}

/// DELETE v1/projects/{projectsId}/locations/{locationsId}/apis/{apisId}/deployments/{deploymentsId}
/// Removes a specified deployment, all revisions, and all child resources (e.g., artifacts).
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `apigeeregistry_projects_locations_apis_deployments_delete_builder()` + `apigeeregistry_projects_locations_apis_deployments_delete_execute()`.
/// For task-level control, use `apigeeregistry_projects_locations_apis_deployments_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigeeregistry_projects_locations_apis_deployments_delete(
    client: &SimpleHttpClient,
    args: &ApigeeregistryProjectsLocationsApisDeploymentsDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = apigeeregistry_projects_locations_apis_deployments_delete_builder(
        client,
        &args.name,
        &args.force,
    )?;
    apigeeregistry_projects_locations_apis_deployments_delete_execute(builder)
}

/// DELETE v1/projects/{projectsId}/locations/{locationsId}/apis/{apisId}/deployments/{deploymentsId}:deleteRevision
/// Deletes a revision of a deployment.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `apigeeregistry_projects_locations_apis_deployments_delete_revision_execute()` to send, or `apigeeregistry_projects_locations_apis_deployments_delete_revision` for simplest API.

pub fn apigeeregistry_projects_locations_apis_deployments_delete_revision_builder(
    client: &SimpleHttpClient,
    name: &String,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://apigeeregistry.googleapis.com/v1/projects/{}/locations/{locationsId}/apis/{apisId}/deployments/{deploymentsId}:deleteRevision",
        name,
    );

    // Build request
    let builder = client
        .delete(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// DELETE v1/projects/{projectsId}/locations/{locationsId}/apis/{apisId}/deployments/{deploymentsId}:deleteRevision
/// Deletes a revision of a deployment.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `apigeeregistry_projects_locations_apis_deployments_delete_revision_execute()` or `apigeeregistry_projects_locations_apis_deployments_delete_revision`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `apigeeregistry_projects_locations_apis_deployments_delete_revision_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigeeregistry_projects_locations_apis_deployments_delete_revision_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ApiDeployment>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ApiDeployment = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// DELETE v1/projects/{projectsId}/locations/{locationsId}/apis/{apisId}/deployments/{deploymentsId}:deleteRevision
/// Deletes a revision of a deployment.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `apigeeregistry_projects_locations_apis_deployments_delete_revision_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `apigeeregistry_projects_locations_apis_deployments_delete_revision_task()`.
/// For the simplest API, use `apigeeregistry_projects_locations_apis_deployments_delete_revision()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `apigeeregistry_projects_locations_apis_deployments_delete_revision_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn apigeeregistry_projects_locations_apis_deployments_delete_revision_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ApiDeployment>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = apigeeregistry_projects_locations_apis_deployments_delete_revision_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`apigeeregistry_projects_locations_apis_deployments_delete_revision`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ApigeeregistryProjectsLocationsApisDeploymentsDeleteRevisionArgs {
    /// Path parameter: name
    pub name: String,
}

/// DELETE v1/projects/{projectsId}/locations/{locationsId}/apis/{apisId}/deployments/{deploymentsId}:deleteRevision
/// Deletes a revision of a deployment.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `apigeeregistry_projects_locations_apis_deployments_delete_revision_builder()` + `apigeeregistry_projects_locations_apis_deployments_delete_revision_execute()`.
/// For task-level control, use `apigeeregistry_projects_locations_apis_deployments_delete_revision_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigeeregistry_projects_locations_apis_deployments_delete_revision(
    client: &SimpleHttpClient,
    args: &ApigeeregistryProjectsLocationsApisDeploymentsDeleteRevisionArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ApiDeployment>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = apigeeregistry_projects_locations_apis_deployments_delete_revision_builder(
        client, &args.name,
    )?;
    apigeeregistry_projects_locations_apis_deployments_delete_revision_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/apis/{apisId}/deployments/{deploymentsId}
/// Returns a specified deployment.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `apigeeregistry_projects_locations_apis_deployments_get_execute()` to send, or `apigeeregistry_projects_locations_apis_deployments_get` for simplest API.

pub fn apigeeregistry_projects_locations_apis_deployments_get_builder(
    client: &SimpleHttpClient,
    name: &String,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://apigeeregistry.googleapis.com/v1/projects/{}/locations/{locationsId}/apis/{apisId}/deployments/{deploymentsId}",
        name,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/apis/{apisId}/deployments/{deploymentsId}
/// Returns a specified deployment.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `apigeeregistry_projects_locations_apis_deployments_get_execute()` or `apigeeregistry_projects_locations_apis_deployments_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `apigeeregistry_projects_locations_apis_deployments_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigeeregistry_projects_locations_apis_deployments_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ApiDeployment>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ApiDeployment = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/apis/{apisId}/deployments/{deploymentsId}
/// Returns a specified deployment.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `apigeeregistry_projects_locations_apis_deployments_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `apigeeregistry_projects_locations_apis_deployments_get_task()`.
/// For the simplest API, use `apigeeregistry_projects_locations_apis_deployments_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `apigeeregistry_projects_locations_apis_deployments_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn apigeeregistry_projects_locations_apis_deployments_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ApiDeployment>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = apigeeregistry_projects_locations_apis_deployments_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`apigeeregistry_projects_locations_apis_deployments_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ApigeeregistryProjectsLocationsApisDeploymentsGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/apis/{apisId}/deployments/{deploymentsId}
/// Returns a specified deployment.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `apigeeregistry_projects_locations_apis_deployments_get_builder()` + `apigeeregistry_projects_locations_apis_deployments_get_execute()`.
/// For task-level control, use `apigeeregistry_projects_locations_apis_deployments_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigeeregistry_projects_locations_apis_deployments_get(
    client: &SimpleHttpClient,
    args: &ApigeeregistryProjectsLocationsApisDeploymentsGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ApiDeployment>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        apigeeregistry_projects_locations_apis_deployments_get_builder(client, &args.name)?;
    apigeeregistry_projects_locations_apis_deployments_get_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/apis/{apisId}/deployments/{deploymentsId}:getIamPolicy
/// Gets the access control policy for a resource. Returns an empty policy if the resource exists and does not have a policy set.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `apigeeregistry_projects_locations_apis_deployments_get_iam_policy_execute()` to send, or `apigeeregistry_projects_locations_apis_deployments_get_iam_policy` for simplest API.

pub fn apigeeregistry_projects_locations_apis_deployments_get_iam_policy_builder(
    client: &SimpleHttpClient,
    resource: &String,
    options_requestedPolicyVersion: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://apigeeregistry.googleapis.com/v1/projects/{}/locations/{locationsId}/apis/{apisId}/deployments/{deploymentsId}:getIamPolicy",
        resource,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = options_requestedPolicyVersion.as_ref() {
        query_parts.push(format!("options.requestedPolicyVersion={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/apis/{apisId}/deployments/{deploymentsId}:getIamPolicy
/// Gets the access control policy for a resource. Returns an empty policy if the resource exists and does not have a policy set.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `apigeeregistry_projects_locations_apis_deployments_get_iam_policy_execute()` or `apigeeregistry_projects_locations_apis_deployments_get_iam_policy`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `apigeeregistry_projects_locations_apis_deployments_get_iam_policy_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigeeregistry_projects_locations_apis_deployments_get_iam_policy_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Policy>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Policy = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/apis/{apisId}/deployments/{deploymentsId}:getIamPolicy
/// Gets the access control policy for a resource. Returns an empty policy if the resource exists and does not have a policy set.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `apigeeregistry_projects_locations_apis_deployments_get_iam_policy_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `apigeeregistry_projects_locations_apis_deployments_get_iam_policy_task()`.
/// For the simplest API, use `apigeeregistry_projects_locations_apis_deployments_get_iam_policy()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `apigeeregistry_projects_locations_apis_deployments_get_iam_policy_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn apigeeregistry_projects_locations_apis_deployments_get_iam_policy_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Policy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = apigeeregistry_projects_locations_apis_deployments_get_iam_policy_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`apigeeregistry_projects_locations_apis_deployments_get_iam_policy`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ApigeeregistryProjectsLocationsApisDeploymentsGetIamPolicyArgs {
    /// Path parameter: resource
    pub resource: String,
    /// Query parameter: options_requestedPolicyVersion
    pub options_requestedPolicyVersion: Option<Option<String>>,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/apis/{apisId}/deployments/{deploymentsId}:getIamPolicy
/// Gets the access control policy for a resource. Returns an empty policy if the resource exists and does not have a policy set.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `apigeeregistry_projects_locations_apis_deployments_get_iam_policy_builder()` + `apigeeregistry_projects_locations_apis_deployments_get_iam_policy_execute()`.
/// For task-level control, use `apigeeregistry_projects_locations_apis_deployments_get_iam_policy_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigeeregistry_projects_locations_apis_deployments_get_iam_policy(
    client: &SimpleHttpClient,
    args: &ApigeeregistryProjectsLocationsApisDeploymentsGetIamPolicyArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Policy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = apigeeregistry_projects_locations_apis_deployments_get_iam_policy_builder(
        client,
        &args.resource,
        &args.options_requestedPolicyVersion,
    )?;
    apigeeregistry_projects_locations_apis_deployments_get_iam_policy_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/apis/{apisId}/deployments
/// Returns matching deployments.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `apigeeregistry_projects_locations_apis_deployments_list_execute()` to send, or `apigeeregistry_projects_locations_apis_deployments_list` for simplest API.

pub fn apigeeregistry_projects_locations_apis_deployments_list_builder(
    client: &SimpleHttpClient,
    parent: &String,
    filter: &Option<Option<String>>,
    orderBy: &Option<Option<String>>,
    pageSize: &Option<Option<String>>,
    pageToken: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://apigeeregistry.googleapis.com/v1/projects/{}/locations/{locationsId}/apis/{apisId}/deployments",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter.as_ref() {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = orderBy.as_ref() {
        query_parts.push(format!("orderBy={}", val));
    }
    if let Some(val) = pageSize.as_ref() {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/apis/{apisId}/deployments
/// Returns matching deployments.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `apigeeregistry_projects_locations_apis_deployments_list_execute()` or `apigeeregistry_projects_locations_apis_deployments_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `apigeeregistry_projects_locations_apis_deployments_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigeeregistry_projects_locations_apis_deployments_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ListApiDeploymentsResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListApiDeploymentsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/apis/{apisId}/deployments
/// Returns matching deployments.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `apigeeregistry_projects_locations_apis_deployments_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `apigeeregistry_projects_locations_apis_deployments_list_task()`.
/// For the simplest API, use `apigeeregistry_projects_locations_apis_deployments_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `apigeeregistry_projects_locations_apis_deployments_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn apigeeregistry_projects_locations_apis_deployments_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ListApiDeploymentsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = apigeeregistry_projects_locations_apis_deployments_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`apigeeregistry_projects_locations_apis_deployments_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ApigeeregistryProjectsLocationsApisDeploymentsListArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: filter
    pub filter: Option<Option<String>>,
    /// Query parameter: orderBy
    pub orderBy: Option<Option<String>>,
    /// Query parameter: pageSize
    pub pageSize: Option<Option<String>>,
    /// Query parameter: pageToken
    pub pageToken: Option<Option<String>>,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/apis/{apisId}/deployments
/// Returns matching deployments.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `apigeeregistry_projects_locations_apis_deployments_list_builder()` + `apigeeregistry_projects_locations_apis_deployments_list_execute()`.
/// For task-level control, use `apigeeregistry_projects_locations_apis_deployments_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigeeregistry_projects_locations_apis_deployments_list(
    client: &SimpleHttpClient,
    args: &ApigeeregistryProjectsLocationsApisDeploymentsListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ListApiDeploymentsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = apigeeregistry_projects_locations_apis_deployments_list_builder(
        client,
        &args.parent,
        &args.filter,
        &args.orderBy,
        &args.pageSize,
        &args.pageToken,
    )?;
    apigeeregistry_projects_locations_apis_deployments_list_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/apis/{apisId}/deployments/{deploymentsId}:listRevisions
/// Lists all revisions of a deployment. Revisions are returned in descending order of revision creation time.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `apigeeregistry_projects_locations_apis_deployments_list_revisions_execute()` to send, or `apigeeregistry_projects_locations_apis_deployments_list_revisions` for simplest API.

pub fn apigeeregistry_projects_locations_apis_deployments_list_revisions_builder(
    client: &SimpleHttpClient,
    name: &String,
    filter: &Option<Option<String>>,
    pageSize: &Option<Option<String>>,
    pageToken: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://apigeeregistry.googleapis.com/v1/projects/{}/locations/{locationsId}/apis/{apisId}/deployments/{deploymentsId}:listRevisions",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter.as_ref() {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = pageSize.as_ref() {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/apis/{apisId}/deployments/{deploymentsId}:listRevisions
/// Lists all revisions of a deployment. Revisions are returned in descending order of revision creation time.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `apigeeregistry_projects_locations_apis_deployments_list_revisions_execute()` or `apigeeregistry_projects_locations_apis_deployments_list_revisions`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `apigeeregistry_projects_locations_apis_deployments_list_revisions_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigeeregistry_projects_locations_apis_deployments_list_revisions_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ListApiDeploymentRevisionsResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListApiDeploymentRevisionsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/apis/{apisId}/deployments/{deploymentsId}:listRevisions
/// Lists all revisions of a deployment. Revisions are returned in descending order of revision creation time.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `apigeeregistry_projects_locations_apis_deployments_list_revisions_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `apigeeregistry_projects_locations_apis_deployments_list_revisions_task()`.
/// For the simplest API, use `apigeeregistry_projects_locations_apis_deployments_list_revisions()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `apigeeregistry_projects_locations_apis_deployments_list_revisions_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn apigeeregistry_projects_locations_apis_deployments_list_revisions_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ListApiDeploymentRevisionsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = apigeeregistry_projects_locations_apis_deployments_list_revisions_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`apigeeregistry_projects_locations_apis_deployments_list_revisions`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ApigeeregistryProjectsLocationsApisDeploymentsListRevisionsArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: filter
    pub filter: Option<Option<String>>,
    /// Query parameter: pageSize
    pub pageSize: Option<Option<String>>,
    /// Query parameter: pageToken
    pub pageToken: Option<Option<String>>,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/apis/{apisId}/deployments/{deploymentsId}:listRevisions
/// Lists all revisions of a deployment. Revisions are returned in descending order of revision creation time.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `apigeeregistry_projects_locations_apis_deployments_list_revisions_builder()` + `apigeeregistry_projects_locations_apis_deployments_list_revisions_execute()`.
/// For task-level control, use `apigeeregistry_projects_locations_apis_deployments_list_revisions_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigeeregistry_projects_locations_apis_deployments_list_revisions(
    client: &SimpleHttpClient,
    args: &ApigeeregistryProjectsLocationsApisDeploymentsListRevisionsArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ListApiDeploymentRevisionsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = apigeeregistry_projects_locations_apis_deployments_list_revisions_builder(
        client,
        &args.name,
        &args.filter,
        &args.pageSize,
        &args.pageToken,
    )?;
    apigeeregistry_projects_locations_apis_deployments_list_revisions_execute(builder)
}

/// PATCH v1/projects/{projectsId}/locations/{locationsId}/apis/{apisId}/deployments/{deploymentsId}
/// Used to modify a specified deployment.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `apigeeregistry_projects_locations_apis_deployments_patch_execute()` to send, or `apigeeregistry_projects_locations_apis_deployments_patch` for simplest API.

pub fn apigeeregistry_projects_locations_apis_deployments_patch_builder(
    client: &SimpleHttpClient,
    name: &String,
    allowMissing: &Option<Option<String>>,
    updateMask: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://apigeeregistry.googleapis.com/v1/projects/{}/locations/{locationsId}/apis/{apisId}/deployments/{deploymentsId}",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = allowMissing.as_ref() {
        query_parts.push(format!("allowMissing={}", val));
    }
    if let Some(val) = updateMask.as_ref() {
        query_parts.push(format!("updateMask={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .patch(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// PATCH v1/projects/{projectsId}/locations/{locationsId}/apis/{apisId}/deployments/{deploymentsId}
/// Used to modify a specified deployment.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `apigeeregistry_projects_locations_apis_deployments_patch_execute()` or `apigeeregistry_projects_locations_apis_deployments_patch`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `apigeeregistry_projects_locations_apis_deployments_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigeeregistry_projects_locations_apis_deployments_patch_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ApiDeployment>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ApiDeployment = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// PATCH v1/projects/{projectsId}/locations/{locationsId}/apis/{apisId}/deployments/{deploymentsId}
/// Used to modify a specified deployment.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `apigeeregistry_projects_locations_apis_deployments_patch_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `apigeeregistry_projects_locations_apis_deployments_patch_task()`.
/// For the simplest API, use `apigeeregistry_projects_locations_apis_deployments_patch()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `apigeeregistry_projects_locations_apis_deployments_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn apigeeregistry_projects_locations_apis_deployments_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ApiDeployment>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = apigeeregistry_projects_locations_apis_deployments_patch_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`apigeeregistry_projects_locations_apis_deployments_patch`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ApigeeregistryProjectsLocationsApisDeploymentsPatchArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: allowMissing
    pub allowMissing: Option<Option<String>>,
    /// Query parameter: updateMask
    pub updateMask: Option<Option<String>>,
}

/// PATCH v1/projects/{projectsId}/locations/{locationsId}/apis/{apisId}/deployments/{deploymentsId}
/// Used to modify a specified deployment.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `apigeeregistry_projects_locations_apis_deployments_patch_builder()` + `apigeeregistry_projects_locations_apis_deployments_patch_execute()`.
/// For task-level control, use `apigeeregistry_projects_locations_apis_deployments_patch_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigeeregistry_projects_locations_apis_deployments_patch(
    client: &SimpleHttpClient,
    args: &ApigeeregistryProjectsLocationsApisDeploymentsPatchArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ApiDeployment>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = apigeeregistry_projects_locations_apis_deployments_patch_builder(
        client,
        &args.name,
        &args.allowMissing,
        &args.updateMask,
    )?;
    apigeeregistry_projects_locations_apis_deployments_patch_execute(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/apis/{apisId}/deployments/{deploymentsId}:rollback
/// Sets the current revision to a specified prior revision. Note that this creates a new revision with a new revision ID.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `apigeeregistry_projects_locations_apis_deployments_rollback_execute()` to send, or `apigeeregistry_projects_locations_apis_deployments_rollback` for simplest API.

pub fn apigeeregistry_projects_locations_apis_deployments_rollback_builder(
    client: &SimpleHttpClient,
    name: &String,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://apigeeregistry.googleapis.com/v1/projects/{}/locations/{locationsId}/apis/{apisId}/deployments/{deploymentsId}:rollback",
        name,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/apis/{apisId}/deployments/{deploymentsId}:rollback
/// Sets the current revision to a specified prior revision. Note that this creates a new revision with a new revision ID.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `apigeeregistry_projects_locations_apis_deployments_rollback_execute()` or `apigeeregistry_projects_locations_apis_deployments_rollback`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `apigeeregistry_projects_locations_apis_deployments_rollback_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigeeregistry_projects_locations_apis_deployments_rollback_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ApiDeployment>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ApiDeployment = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/apis/{apisId}/deployments/{deploymentsId}:rollback
/// Sets the current revision to a specified prior revision. Note that this creates a new revision with a new revision ID.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `apigeeregistry_projects_locations_apis_deployments_rollback_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `apigeeregistry_projects_locations_apis_deployments_rollback_task()`.
/// For the simplest API, use `apigeeregistry_projects_locations_apis_deployments_rollback()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `apigeeregistry_projects_locations_apis_deployments_rollback_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn apigeeregistry_projects_locations_apis_deployments_rollback_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ApiDeployment>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = apigeeregistry_projects_locations_apis_deployments_rollback_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`apigeeregistry_projects_locations_apis_deployments_rollback`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ApigeeregistryProjectsLocationsApisDeploymentsRollbackArgs {
    /// Path parameter: name
    pub name: String,
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/apis/{apisId}/deployments/{deploymentsId}:rollback
/// Sets the current revision to a specified prior revision. Note that this creates a new revision with a new revision ID.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `apigeeregistry_projects_locations_apis_deployments_rollback_builder()` + `apigeeregistry_projects_locations_apis_deployments_rollback_execute()`.
/// For task-level control, use `apigeeregistry_projects_locations_apis_deployments_rollback_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigeeregistry_projects_locations_apis_deployments_rollback(
    client: &SimpleHttpClient,
    args: &ApigeeregistryProjectsLocationsApisDeploymentsRollbackArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ApiDeployment>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        apigeeregistry_projects_locations_apis_deployments_rollback_builder(client, &args.name)?;
    apigeeregistry_projects_locations_apis_deployments_rollback_execute(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/apis/{apisId}/deployments/{deploymentsId}:setIamPolicy
/// Sets the access control policy on the specified resource. Replaces any existing policy. Can return NOT_FOUND, INVALID_ARGUMENT, and PERMISSION_DENIED errors.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `apigeeregistry_projects_locations_apis_deployments_set_iam_policy_execute()` to send, or `apigeeregistry_projects_locations_apis_deployments_set_iam_policy` for simplest API.

pub fn apigeeregistry_projects_locations_apis_deployments_set_iam_policy_builder(
    client: &SimpleHttpClient,
    resource: &String,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://apigeeregistry.googleapis.com/v1/projects/{}/locations/{locationsId}/apis/{apisId}/deployments/{deploymentsId}:setIamPolicy",
        resource,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/apis/{apisId}/deployments/{deploymentsId}:setIamPolicy
/// Sets the access control policy on the specified resource. Replaces any existing policy. Can return NOT_FOUND, INVALID_ARGUMENT, and PERMISSION_DENIED errors.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `apigeeregistry_projects_locations_apis_deployments_set_iam_policy_execute()` or `apigeeregistry_projects_locations_apis_deployments_set_iam_policy`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `apigeeregistry_projects_locations_apis_deployments_set_iam_policy_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigeeregistry_projects_locations_apis_deployments_set_iam_policy_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Policy>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Policy = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/apis/{apisId}/deployments/{deploymentsId}:setIamPolicy
/// Sets the access control policy on the specified resource. Replaces any existing policy. Can return NOT_FOUND, INVALID_ARGUMENT, and PERMISSION_DENIED errors.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `apigeeregistry_projects_locations_apis_deployments_set_iam_policy_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `apigeeregistry_projects_locations_apis_deployments_set_iam_policy_task()`.
/// For the simplest API, use `apigeeregistry_projects_locations_apis_deployments_set_iam_policy()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `apigeeregistry_projects_locations_apis_deployments_set_iam_policy_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn apigeeregistry_projects_locations_apis_deployments_set_iam_policy_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Policy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = apigeeregistry_projects_locations_apis_deployments_set_iam_policy_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`apigeeregistry_projects_locations_apis_deployments_set_iam_policy`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ApigeeregistryProjectsLocationsApisDeploymentsSetIamPolicyArgs {
    /// Path parameter: resource
    pub resource: String,
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/apis/{apisId}/deployments/{deploymentsId}:setIamPolicy
/// Sets the access control policy on the specified resource. Replaces any existing policy. Can return NOT_FOUND, INVALID_ARGUMENT, and PERMISSION_DENIED errors.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `apigeeregistry_projects_locations_apis_deployments_set_iam_policy_builder()` + `apigeeregistry_projects_locations_apis_deployments_set_iam_policy_execute()`.
/// For task-level control, use `apigeeregistry_projects_locations_apis_deployments_set_iam_policy_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigeeregistry_projects_locations_apis_deployments_set_iam_policy(
    client: &SimpleHttpClient,
    args: &ApigeeregistryProjectsLocationsApisDeploymentsSetIamPolicyArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Policy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = apigeeregistry_projects_locations_apis_deployments_set_iam_policy_builder(
        client,
        &args.resource,
    )?;
    apigeeregistry_projects_locations_apis_deployments_set_iam_policy_execute(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/apis/{apisId}/deployments/{deploymentsId}:tagRevision
/// Adds a tag to a specified revision of a deployment.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `apigeeregistry_projects_locations_apis_deployments_tag_revision_execute()` to send, or `apigeeregistry_projects_locations_apis_deployments_tag_revision` for simplest API.

pub fn apigeeregistry_projects_locations_apis_deployments_tag_revision_builder(
    client: &SimpleHttpClient,
    name: &String,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://apigeeregistry.googleapis.com/v1/projects/{}/locations/{locationsId}/apis/{apisId}/deployments/{deploymentsId}:tagRevision",
        name,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/apis/{apisId}/deployments/{deploymentsId}:tagRevision
/// Adds a tag to a specified revision of a deployment.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `apigeeregistry_projects_locations_apis_deployments_tag_revision_execute()` or `apigeeregistry_projects_locations_apis_deployments_tag_revision`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `apigeeregistry_projects_locations_apis_deployments_tag_revision_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigeeregistry_projects_locations_apis_deployments_tag_revision_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ApiDeployment>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ApiDeployment = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/apis/{apisId}/deployments/{deploymentsId}:tagRevision
/// Adds a tag to a specified revision of a deployment.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `apigeeregistry_projects_locations_apis_deployments_tag_revision_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `apigeeregistry_projects_locations_apis_deployments_tag_revision_task()`.
/// For the simplest API, use `apigeeregistry_projects_locations_apis_deployments_tag_revision()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `apigeeregistry_projects_locations_apis_deployments_tag_revision_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn apigeeregistry_projects_locations_apis_deployments_tag_revision_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ApiDeployment>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = apigeeregistry_projects_locations_apis_deployments_tag_revision_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`apigeeregistry_projects_locations_apis_deployments_tag_revision`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ApigeeregistryProjectsLocationsApisDeploymentsTagRevisionArgs {
    /// Path parameter: name
    pub name: String,
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/apis/{apisId}/deployments/{deploymentsId}:tagRevision
/// Adds a tag to a specified revision of a deployment.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `apigeeregistry_projects_locations_apis_deployments_tag_revision_builder()` + `apigeeregistry_projects_locations_apis_deployments_tag_revision_execute()`.
/// For task-level control, use `apigeeregistry_projects_locations_apis_deployments_tag_revision_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigeeregistry_projects_locations_apis_deployments_tag_revision(
    client: &SimpleHttpClient,
    args: &ApigeeregistryProjectsLocationsApisDeploymentsTagRevisionArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ApiDeployment>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = apigeeregistry_projects_locations_apis_deployments_tag_revision_builder(
        client, &args.name,
    )?;
    apigeeregistry_projects_locations_apis_deployments_tag_revision_execute(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/apis/{apisId}/deployments/{deploymentsId}:testIamPermissions
/// Returns permissions that a caller has on the specified resource. If the resource does not exist, this will return an empty set of permissions, not a NOT_FOUND error. Note: This operation is designed to be used for building permission-aware UIs and command-line tools, not for authorization checking. This operation may "fail open" without warning.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `apigeeregistry_projects_locations_apis_deployments_test_iam_permissions_execute()` to send, or `apigeeregistry_projects_locations_apis_deployments_test_iam_permissions` for simplest API.

pub fn apigeeregistry_projects_locations_apis_deployments_test_iam_permissions_builder(
    client: &SimpleHttpClient,
    resource: &String,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://apigeeregistry.googleapis.com/v1/projects/{}/locations/{locationsId}/apis/{apisId}/deployments/{deploymentsId}:testIamPermissions",
        resource,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/apis/{apisId}/deployments/{deploymentsId}:testIamPermissions
/// Returns permissions that a caller has on the specified resource. If the resource does not exist, this will return an empty set of permissions, not a NOT_FOUND error. Note: This operation is designed to be used for building permission-aware UIs and command-line tools, not for authorization checking. This operation may "fail open" without warning.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `apigeeregistry_projects_locations_apis_deployments_test_iam_permissions_execute()` or `apigeeregistry_projects_locations_apis_deployments_test_iam_permissions`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `apigeeregistry_projects_locations_apis_deployments_test_iam_permissions_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigeeregistry_projects_locations_apis_deployments_test_iam_permissions_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<TestIamPermissionsResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: TestIamPermissionsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/apis/{apisId}/deployments/{deploymentsId}:testIamPermissions
/// Returns permissions that a caller has on the specified resource. If the resource does not exist, this will return an empty set of permissions, not a NOT_FOUND error. Note: This operation is designed to be used for building permission-aware UIs and command-line tools, not for authorization checking. This operation may "fail open" without warning.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `apigeeregistry_projects_locations_apis_deployments_test_iam_permissions_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `apigeeregistry_projects_locations_apis_deployments_test_iam_permissions_task()`.
/// For the simplest API, use `apigeeregistry_projects_locations_apis_deployments_test_iam_permissions()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `apigeeregistry_projects_locations_apis_deployments_test_iam_permissions_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn apigeeregistry_projects_locations_apis_deployments_test_iam_permissions_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<TestIamPermissionsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task =
        apigeeregistry_projects_locations_apis_deployments_test_iam_permissions_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`apigeeregistry_projects_locations_apis_deployments_test_iam_permissions`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ApigeeregistryProjectsLocationsApisDeploymentsTestIamPermissionsArgs {
    /// Path parameter: resource
    pub resource: String,
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/apis/{apisId}/deployments/{deploymentsId}:testIamPermissions
/// Returns permissions that a caller has on the specified resource. If the resource does not exist, this will return an empty set of permissions, not a NOT_FOUND error. Note: This operation is designed to be used for building permission-aware UIs and command-line tools, not for authorization checking. This operation may "fail open" without warning.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `apigeeregistry_projects_locations_apis_deployments_test_iam_permissions_builder()` + `apigeeregistry_projects_locations_apis_deployments_test_iam_permissions_execute()`.
/// For task-level control, use `apigeeregistry_projects_locations_apis_deployments_test_iam_permissions_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigeeregistry_projects_locations_apis_deployments_test_iam_permissions(
    client: &SimpleHttpClient,
    args: &ApigeeregistryProjectsLocationsApisDeploymentsTestIamPermissionsArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<TestIamPermissionsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = apigeeregistry_projects_locations_apis_deployments_test_iam_permissions_builder(
        client,
        &args.resource,
    )?;
    apigeeregistry_projects_locations_apis_deployments_test_iam_permissions_execute(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/apis/{apisId}/deployments/{deploymentsId}/artifacts
/// Creates a specified artifact.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `apigeeregistry_projects_locations_apis_deployments_artifacts_create_execute()` to send, or `apigeeregistry_projects_locations_apis_deployments_artifacts_create` for simplest API.

pub fn apigeeregistry_projects_locations_apis_deployments_artifacts_create_builder(
    client: &SimpleHttpClient,
    parent: &String,
    artifactId: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://apigeeregistry.googleapis.com/v1/projects/{}/locations/{locationsId}/apis/{apisId}/deployments/{deploymentsId}/artifacts",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = artifactId.as_ref() {
        query_parts.push(format!("artifactId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .post(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/apis/{apisId}/deployments/{deploymentsId}/artifacts
/// Creates a specified artifact.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `apigeeregistry_projects_locations_apis_deployments_artifacts_create_execute()` or `apigeeregistry_projects_locations_apis_deployments_artifacts_create`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `apigeeregistry_projects_locations_apis_deployments_artifacts_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigeeregistry_projects_locations_apis_deployments_artifacts_create_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Artifact>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Artifact = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/apis/{apisId}/deployments/{deploymentsId}/artifacts
/// Creates a specified artifact.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `apigeeregistry_projects_locations_apis_deployments_artifacts_create_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `apigeeregistry_projects_locations_apis_deployments_artifacts_create_task()`.
/// For the simplest API, use `apigeeregistry_projects_locations_apis_deployments_artifacts_create()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `apigeeregistry_projects_locations_apis_deployments_artifacts_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn apigeeregistry_projects_locations_apis_deployments_artifacts_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Artifact>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = apigeeregistry_projects_locations_apis_deployments_artifacts_create_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`apigeeregistry_projects_locations_apis_deployments_artifacts_create`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ApigeeregistryProjectsLocationsApisDeploymentsArtifactsCreateArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: artifactId
    pub artifactId: Option<Option<String>>,
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/apis/{apisId}/deployments/{deploymentsId}/artifacts
/// Creates a specified artifact.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `apigeeregistry_projects_locations_apis_deployments_artifacts_create_builder()` + `apigeeregistry_projects_locations_apis_deployments_artifacts_create_execute()`.
/// For task-level control, use `apigeeregistry_projects_locations_apis_deployments_artifacts_create_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigeeregistry_projects_locations_apis_deployments_artifacts_create(
    client: &SimpleHttpClient,
    args: &ApigeeregistryProjectsLocationsApisDeploymentsArtifactsCreateArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Artifact>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = apigeeregistry_projects_locations_apis_deployments_artifacts_create_builder(
        client,
        &args.parent,
        &args.artifactId,
    )?;
    apigeeregistry_projects_locations_apis_deployments_artifacts_create_execute(builder)
}

/// DELETE v1/projects/{projectsId}/locations/{locationsId}/apis/{apisId}/deployments/{deploymentsId}/artifacts/{artifactsId}
/// Removes a specified artifact.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `apigeeregistry_projects_locations_apis_deployments_artifacts_delete_execute()` to send, or `apigeeregistry_projects_locations_apis_deployments_artifacts_delete` for simplest API.

pub fn apigeeregistry_projects_locations_apis_deployments_artifacts_delete_builder(
    client: &SimpleHttpClient,
    name: &String,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://apigeeregistry.googleapis.com/v1/projects/{}/locations/{locationsId}/apis/{apisId}/deployments/{deploymentsId}/artifacts/{artifactsId}",
        name,
    );

    // Build request
    let builder = client
        .delete(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// DELETE v1/projects/{projectsId}/locations/{locationsId}/apis/{apisId}/deployments/{deploymentsId}/artifacts/{artifactsId}
/// Removes a specified artifact.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `apigeeregistry_projects_locations_apis_deployments_artifacts_delete_execute()` or `apigeeregistry_projects_locations_apis_deployments_artifacts_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `apigeeregistry_projects_locations_apis_deployments_artifacts_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigeeregistry_projects_locations_apis_deployments_artifacts_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Empty>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Empty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// DELETE v1/projects/{projectsId}/locations/{locationsId}/apis/{apisId}/deployments/{deploymentsId}/artifacts/{artifactsId}
/// Removes a specified artifact.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `apigeeregistry_projects_locations_apis_deployments_artifacts_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `apigeeregistry_projects_locations_apis_deployments_artifacts_delete_task()`.
/// For the simplest API, use `apigeeregistry_projects_locations_apis_deployments_artifacts_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `apigeeregistry_projects_locations_apis_deployments_artifacts_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn apigeeregistry_projects_locations_apis_deployments_artifacts_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = apigeeregistry_projects_locations_apis_deployments_artifacts_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`apigeeregistry_projects_locations_apis_deployments_artifacts_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ApigeeregistryProjectsLocationsApisDeploymentsArtifactsDeleteArgs {
    /// Path parameter: name
    pub name: String,
}

/// DELETE v1/projects/{projectsId}/locations/{locationsId}/apis/{apisId}/deployments/{deploymentsId}/artifacts/{artifactsId}
/// Removes a specified artifact.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `apigeeregistry_projects_locations_apis_deployments_artifacts_delete_builder()` + `apigeeregistry_projects_locations_apis_deployments_artifacts_delete_execute()`.
/// For task-level control, use `apigeeregistry_projects_locations_apis_deployments_artifacts_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigeeregistry_projects_locations_apis_deployments_artifacts_delete(
    client: &SimpleHttpClient,
    args: &ApigeeregistryProjectsLocationsApisDeploymentsArtifactsDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = apigeeregistry_projects_locations_apis_deployments_artifacts_delete_builder(
        client, &args.name,
    )?;
    apigeeregistry_projects_locations_apis_deployments_artifacts_delete_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/apis/{apisId}/deployments/{deploymentsId}/artifacts/{artifactsId}
/// Returns a specified artifact.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `apigeeregistry_projects_locations_apis_deployments_artifacts_get_execute()` to send, or `apigeeregistry_projects_locations_apis_deployments_artifacts_get` for simplest API.

pub fn apigeeregistry_projects_locations_apis_deployments_artifacts_get_builder(
    client: &SimpleHttpClient,
    name: &String,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://apigeeregistry.googleapis.com/v1/projects/{}/locations/{locationsId}/apis/{apisId}/deployments/{deploymentsId}/artifacts/{artifactsId}",
        name,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/apis/{apisId}/deployments/{deploymentsId}/artifacts/{artifactsId}
/// Returns a specified artifact.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `apigeeregistry_projects_locations_apis_deployments_artifacts_get_execute()` or `apigeeregistry_projects_locations_apis_deployments_artifacts_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `apigeeregistry_projects_locations_apis_deployments_artifacts_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigeeregistry_projects_locations_apis_deployments_artifacts_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Artifact>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Artifact = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/apis/{apisId}/deployments/{deploymentsId}/artifacts/{artifactsId}
/// Returns a specified artifact.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `apigeeregistry_projects_locations_apis_deployments_artifacts_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `apigeeregistry_projects_locations_apis_deployments_artifacts_get_task()`.
/// For the simplest API, use `apigeeregistry_projects_locations_apis_deployments_artifacts_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `apigeeregistry_projects_locations_apis_deployments_artifacts_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn apigeeregistry_projects_locations_apis_deployments_artifacts_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Artifact>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = apigeeregistry_projects_locations_apis_deployments_artifacts_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`apigeeregistry_projects_locations_apis_deployments_artifacts_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ApigeeregistryProjectsLocationsApisDeploymentsArtifactsGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/apis/{apisId}/deployments/{deploymentsId}/artifacts/{artifactsId}
/// Returns a specified artifact.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `apigeeregistry_projects_locations_apis_deployments_artifacts_get_builder()` + `apigeeregistry_projects_locations_apis_deployments_artifacts_get_execute()`.
/// For task-level control, use `apigeeregistry_projects_locations_apis_deployments_artifacts_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigeeregistry_projects_locations_apis_deployments_artifacts_get(
    client: &SimpleHttpClient,
    args: &ApigeeregistryProjectsLocationsApisDeploymentsArtifactsGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Artifact>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = apigeeregistry_projects_locations_apis_deployments_artifacts_get_builder(
        client, &args.name,
    )?;
    apigeeregistry_projects_locations_apis_deployments_artifacts_get_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/apis/{apisId}/deployments/{deploymentsId}/artifacts/{artifactsId}:getContents
/// Returns the contents of a specified artifact. If artifacts are stored with GZip compression, the default behavior is to return the artifact uncompressed (the mime_type response field indicates the exact format returned).
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `apigeeregistry_projects_locations_apis_deployments_artifacts_get_contents_execute()` to send, or `apigeeregistry_projects_locations_apis_deployments_artifacts_get_contents` for simplest API.

pub fn apigeeregistry_projects_locations_apis_deployments_artifacts_get_contents_builder(
    client: &SimpleHttpClient,
    name: &String,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://apigeeregistry.googleapis.com/v1/projects/{}/locations/{locationsId}/apis/{apisId}/deployments/{deploymentsId}/artifacts/{artifactsId}:getContents",
        name,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/apis/{apisId}/deployments/{deploymentsId}/artifacts/{artifactsId}:getContents
/// Returns the contents of a specified artifact. If artifacts are stored with GZip compression, the default behavior is to return the artifact uncompressed (the mime_type response field indicates the exact format returned).
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `apigeeregistry_projects_locations_apis_deployments_artifacts_get_contents_execute()` or `apigeeregistry_projects_locations_apis_deployments_artifacts_get_contents`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `apigeeregistry_projects_locations_apis_deployments_artifacts_get_contents_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigeeregistry_projects_locations_apis_deployments_artifacts_get_contents_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<HttpBody>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: HttpBody = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/apis/{apisId}/deployments/{deploymentsId}/artifacts/{artifactsId}:getContents
/// Returns the contents of a specified artifact. If artifacts are stored with GZip compression, the default behavior is to return the artifact uncompressed (the mime_type response field indicates the exact format returned).
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `apigeeregistry_projects_locations_apis_deployments_artifacts_get_contents_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `apigeeregistry_projects_locations_apis_deployments_artifacts_get_contents_task()`.
/// For the simplest API, use `apigeeregistry_projects_locations_apis_deployments_artifacts_get_contents()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `apigeeregistry_projects_locations_apis_deployments_artifacts_get_contents_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn apigeeregistry_projects_locations_apis_deployments_artifacts_get_contents_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<HttpBody>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task =
        apigeeregistry_projects_locations_apis_deployments_artifacts_get_contents_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`apigeeregistry_projects_locations_apis_deployments_artifacts_get_contents`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ApigeeregistryProjectsLocationsApisDeploymentsArtifactsGetContentsArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/apis/{apisId}/deployments/{deploymentsId}/artifacts/{artifactsId}:getContents
/// Returns the contents of a specified artifact. If artifacts are stored with GZip compression, the default behavior is to return the artifact uncompressed (the mime_type response field indicates the exact format returned).
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `apigeeregistry_projects_locations_apis_deployments_artifacts_get_contents_builder()` + `apigeeregistry_projects_locations_apis_deployments_artifacts_get_contents_execute()`.
/// For task-level control, use `apigeeregistry_projects_locations_apis_deployments_artifacts_get_contents_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigeeregistry_projects_locations_apis_deployments_artifacts_get_contents(
    client: &SimpleHttpClient,
    args: &ApigeeregistryProjectsLocationsApisDeploymentsArtifactsGetContentsArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<HttpBody>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        apigeeregistry_projects_locations_apis_deployments_artifacts_get_contents_builder(
            client, &args.name,
        )?;
    apigeeregistry_projects_locations_apis_deployments_artifacts_get_contents_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/apis/{apisId}/deployments/{deploymentsId}/artifacts
/// Returns matching artifacts.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `apigeeregistry_projects_locations_apis_deployments_artifacts_list_execute()` to send, or `apigeeregistry_projects_locations_apis_deployments_artifacts_list` for simplest API.

pub fn apigeeregistry_projects_locations_apis_deployments_artifacts_list_builder(
    client: &SimpleHttpClient,
    parent: &String,
    filter: &Option<Option<String>>,
    orderBy: &Option<Option<String>>,
    pageSize: &Option<Option<String>>,
    pageToken: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://apigeeregistry.googleapis.com/v1/projects/{}/locations/{locationsId}/apis/{apisId}/deployments/{deploymentsId}/artifacts",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter.as_ref() {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = orderBy.as_ref() {
        query_parts.push(format!("orderBy={}", val));
    }
    if let Some(val) = pageSize.as_ref() {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/apis/{apisId}/deployments/{deploymentsId}/artifacts
/// Returns matching artifacts.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `apigeeregistry_projects_locations_apis_deployments_artifacts_list_execute()` or `apigeeregistry_projects_locations_apis_deployments_artifacts_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `apigeeregistry_projects_locations_apis_deployments_artifacts_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigeeregistry_projects_locations_apis_deployments_artifacts_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ListArtifactsResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListArtifactsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/apis/{apisId}/deployments/{deploymentsId}/artifacts
/// Returns matching artifacts.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `apigeeregistry_projects_locations_apis_deployments_artifacts_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `apigeeregistry_projects_locations_apis_deployments_artifacts_list_task()`.
/// For the simplest API, use `apigeeregistry_projects_locations_apis_deployments_artifacts_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `apigeeregistry_projects_locations_apis_deployments_artifacts_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn apigeeregistry_projects_locations_apis_deployments_artifacts_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListArtifactsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = apigeeregistry_projects_locations_apis_deployments_artifacts_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`apigeeregistry_projects_locations_apis_deployments_artifacts_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ApigeeregistryProjectsLocationsApisDeploymentsArtifactsListArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: filter
    pub filter: Option<Option<String>>,
    /// Query parameter: orderBy
    pub orderBy: Option<Option<String>>,
    /// Query parameter: pageSize
    pub pageSize: Option<Option<String>>,
    /// Query parameter: pageToken
    pub pageToken: Option<Option<String>>,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/apis/{apisId}/deployments/{deploymentsId}/artifacts
/// Returns matching artifacts.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `apigeeregistry_projects_locations_apis_deployments_artifacts_list_builder()` + `apigeeregistry_projects_locations_apis_deployments_artifacts_list_execute()`.
/// For task-level control, use `apigeeregistry_projects_locations_apis_deployments_artifacts_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigeeregistry_projects_locations_apis_deployments_artifacts_list(
    client: &SimpleHttpClient,
    args: &ApigeeregistryProjectsLocationsApisDeploymentsArtifactsListArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListArtifactsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = apigeeregistry_projects_locations_apis_deployments_artifacts_list_builder(
        client,
        &args.parent,
        &args.filter,
        &args.orderBy,
        &args.pageSize,
        &args.pageToken,
    )?;
    apigeeregistry_projects_locations_apis_deployments_artifacts_list_execute(builder)
}

/// PUT v1/projects/{projectsId}/locations/{locationsId}/apis/{apisId}/deployments/{deploymentsId}/artifacts/{artifactsId}
/// Used to replace a specified artifact.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `apigeeregistry_projects_locations_apis_deployments_artifacts_replace_artifact_execute()` to send, or `apigeeregistry_projects_locations_apis_deployments_artifacts_replace_artifact` for simplest API.

pub fn apigeeregistry_projects_locations_apis_deployments_artifacts_replace_artifact_builder(
    client: &SimpleHttpClient,
    name: &String,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://apigeeregistry.googleapis.com/v1/projects/{}/locations/{locationsId}/apis/{apisId}/deployments/{deploymentsId}/artifacts/{artifactsId}",
        name,
    );

    // Build request
    let builder = client
        .put(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// PUT v1/projects/{projectsId}/locations/{locationsId}/apis/{apisId}/deployments/{deploymentsId}/artifacts/{artifactsId}
/// Used to replace a specified artifact.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `apigeeregistry_projects_locations_apis_deployments_artifacts_replace_artifact_execute()` or `apigeeregistry_projects_locations_apis_deployments_artifacts_replace_artifact`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `apigeeregistry_projects_locations_apis_deployments_artifacts_replace_artifact_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigeeregistry_projects_locations_apis_deployments_artifacts_replace_artifact_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Artifact>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Artifact = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// PUT v1/projects/{projectsId}/locations/{locationsId}/apis/{apisId}/deployments/{deploymentsId}/artifacts/{artifactsId}
/// Used to replace a specified artifact.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `apigeeregistry_projects_locations_apis_deployments_artifacts_replace_artifact_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `apigeeregistry_projects_locations_apis_deployments_artifacts_replace_artifact_task()`.
/// For the simplest API, use `apigeeregistry_projects_locations_apis_deployments_artifacts_replace_artifact()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `apigeeregistry_projects_locations_apis_deployments_artifacts_replace_artifact_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn apigeeregistry_projects_locations_apis_deployments_artifacts_replace_artifact_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Artifact>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = apigeeregistry_projects_locations_apis_deployments_artifacts_replace_artifact_task(
        builder,
    )?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`apigeeregistry_projects_locations_apis_deployments_artifacts_replace_artifact`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ApigeeregistryProjectsLocationsApisDeploymentsArtifactsReplaceArtifactArgs {
    /// Path parameter: name
    pub name: String,
}

/// PUT v1/projects/{projectsId}/locations/{locationsId}/apis/{apisId}/deployments/{deploymentsId}/artifacts/{artifactsId}
/// Used to replace a specified artifact.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `apigeeregistry_projects_locations_apis_deployments_artifacts_replace_artifact_builder()` + `apigeeregistry_projects_locations_apis_deployments_artifacts_replace_artifact_execute()`.
/// For task-level control, use `apigeeregistry_projects_locations_apis_deployments_artifacts_replace_artifact_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigeeregistry_projects_locations_apis_deployments_artifacts_replace_artifact(
    client: &SimpleHttpClient,
    args: &ApigeeregistryProjectsLocationsApisDeploymentsArtifactsReplaceArtifactArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Artifact>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        apigeeregistry_projects_locations_apis_deployments_artifacts_replace_artifact_builder(
            client, &args.name,
        )?;
    apigeeregistry_projects_locations_apis_deployments_artifacts_replace_artifact_execute(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/apis/{apisId}/versions
/// Creates a specified version.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `apigeeregistry_projects_locations_apis_versions_create_execute()` to send, or `apigeeregistry_projects_locations_apis_versions_create` for simplest API.

pub fn apigeeregistry_projects_locations_apis_versions_create_builder(
    client: &SimpleHttpClient,
    parent: &String,
    apiVersionId: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://apigeeregistry.googleapis.com/v1/projects/{}/locations/{locationsId}/apis/{apisId}/versions",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = apiVersionId.as_ref() {
        query_parts.push(format!("apiVersionId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .post(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/apis/{apisId}/versions
/// Creates a specified version.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `apigeeregistry_projects_locations_apis_versions_create_execute()` or `apigeeregistry_projects_locations_apis_versions_create`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `apigeeregistry_projects_locations_apis_versions_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigeeregistry_projects_locations_apis_versions_create_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ApiVersion>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ApiVersion = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/apis/{apisId}/versions
/// Creates a specified version.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `apigeeregistry_projects_locations_apis_versions_create_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `apigeeregistry_projects_locations_apis_versions_create_task()`.
/// For the simplest API, use `apigeeregistry_projects_locations_apis_versions_create()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `apigeeregistry_projects_locations_apis_versions_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn apigeeregistry_projects_locations_apis_versions_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ApiVersion>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = apigeeregistry_projects_locations_apis_versions_create_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`apigeeregistry_projects_locations_apis_versions_create`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ApigeeregistryProjectsLocationsApisVersionsCreateArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: apiVersionId
    pub apiVersionId: Option<Option<String>>,
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/apis/{apisId}/versions
/// Creates a specified version.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `apigeeregistry_projects_locations_apis_versions_create_builder()` + `apigeeregistry_projects_locations_apis_versions_create_execute()`.
/// For task-level control, use `apigeeregistry_projects_locations_apis_versions_create_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigeeregistry_projects_locations_apis_versions_create(
    client: &SimpleHttpClient,
    args: &ApigeeregistryProjectsLocationsApisVersionsCreateArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ApiVersion>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = apigeeregistry_projects_locations_apis_versions_create_builder(
        client,
        &args.parent,
        &args.apiVersionId,
    )?;
    apigeeregistry_projects_locations_apis_versions_create_execute(builder)
}

/// DELETE v1/projects/{projectsId}/locations/{locationsId}/apis/{apisId}/versions/{versionsId}
/// Removes a specified version and all of the resources that it owns.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `apigeeregistry_projects_locations_apis_versions_delete_execute()` to send, or `apigeeregistry_projects_locations_apis_versions_delete` for simplest API.

pub fn apigeeregistry_projects_locations_apis_versions_delete_builder(
    client: &SimpleHttpClient,
    name: &String,
    force: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://apigeeregistry.googleapis.com/v1/projects/{}/locations/{locationsId}/apis/{apisId}/versions/{versionsId}",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = force.as_ref() {
        query_parts.push(format!("force={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .delete(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// DELETE v1/projects/{projectsId}/locations/{locationsId}/apis/{apisId}/versions/{versionsId}
/// Removes a specified version and all of the resources that it owns.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `apigeeregistry_projects_locations_apis_versions_delete_execute()` or `apigeeregistry_projects_locations_apis_versions_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `apigeeregistry_projects_locations_apis_versions_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigeeregistry_projects_locations_apis_versions_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Empty>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Empty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// DELETE v1/projects/{projectsId}/locations/{locationsId}/apis/{apisId}/versions/{versionsId}
/// Removes a specified version and all of the resources that it owns.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `apigeeregistry_projects_locations_apis_versions_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `apigeeregistry_projects_locations_apis_versions_delete_task()`.
/// For the simplest API, use `apigeeregistry_projects_locations_apis_versions_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `apigeeregistry_projects_locations_apis_versions_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn apigeeregistry_projects_locations_apis_versions_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = apigeeregistry_projects_locations_apis_versions_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`apigeeregistry_projects_locations_apis_versions_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ApigeeregistryProjectsLocationsApisVersionsDeleteArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: force
    pub force: Option<Option<String>>,
}

/// DELETE v1/projects/{projectsId}/locations/{locationsId}/apis/{apisId}/versions/{versionsId}
/// Removes a specified version and all of the resources that it owns.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `apigeeregistry_projects_locations_apis_versions_delete_builder()` + `apigeeregistry_projects_locations_apis_versions_delete_execute()`.
/// For task-level control, use `apigeeregistry_projects_locations_apis_versions_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigeeregistry_projects_locations_apis_versions_delete(
    client: &SimpleHttpClient,
    args: &ApigeeregistryProjectsLocationsApisVersionsDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = apigeeregistry_projects_locations_apis_versions_delete_builder(
        client,
        &args.name,
        &args.force,
    )?;
    apigeeregistry_projects_locations_apis_versions_delete_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/apis/{apisId}/versions/{versionsId}
/// Returns a specified version.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `apigeeregistry_projects_locations_apis_versions_get_execute()` to send, or `apigeeregistry_projects_locations_apis_versions_get` for simplest API.

pub fn apigeeregistry_projects_locations_apis_versions_get_builder(
    client: &SimpleHttpClient,
    name: &String,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://apigeeregistry.googleapis.com/v1/projects/{}/locations/{locationsId}/apis/{apisId}/versions/{versionsId}",
        name,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/apis/{apisId}/versions/{versionsId}
/// Returns a specified version.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `apigeeregistry_projects_locations_apis_versions_get_execute()` or `apigeeregistry_projects_locations_apis_versions_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `apigeeregistry_projects_locations_apis_versions_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigeeregistry_projects_locations_apis_versions_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ApiVersion>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ApiVersion = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/apis/{apisId}/versions/{versionsId}
/// Returns a specified version.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `apigeeregistry_projects_locations_apis_versions_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `apigeeregistry_projects_locations_apis_versions_get_task()`.
/// For the simplest API, use `apigeeregistry_projects_locations_apis_versions_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `apigeeregistry_projects_locations_apis_versions_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn apigeeregistry_projects_locations_apis_versions_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ApiVersion>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = apigeeregistry_projects_locations_apis_versions_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`apigeeregistry_projects_locations_apis_versions_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ApigeeregistryProjectsLocationsApisVersionsGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/apis/{apisId}/versions/{versionsId}
/// Returns a specified version.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `apigeeregistry_projects_locations_apis_versions_get_builder()` + `apigeeregistry_projects_locations_apis_versions_get_execute()`.
/// For task-level control, use `apigeeregistry_projects_locations_apis_versions_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigeeregistry_projects_locations_apis_versions_get(
    client: &SimpleHttpClient,
    args: &ApigeeregistryProjectsLocationsApisVersionsGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ApiVersion>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = apigeeregistry_projects_locations_apis_versions_get_builder(client, &args.name)?;
    apigeeregistry_projects_locations_apis_versions_get_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/apis/{apisId}/versions/{versionsId}:getIamPolicy
/// Gets the access control policy for a resource. Returns an empty policy if the resource exists and does not have a policy set.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `apigeeregistry_projects_locations_apis_versions_get_iam_policy_execute()` to send, or `apigeeregistry_projects_locations_apis_versions_get_iam_policy` for simplest API.

pub fn apigeeregistry_projects_locations_apis_versions_get_iam_policy_builder(
    client: &SimpleHttpClient,
    resource: &String,
    options_requestedPolicyVersion: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://apigeeregistry.googleapis.com/v1/projects/{}/locations/{locationsId}/apis/{apisId}/versions/{versionsId}:getIamPolicy",
        resource,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = options_requestedPolicyVersion.as_ref() {
        query_parts.push(format!("options.requestedPolicyVersion={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/apis/{apisId}/versions/{versionsId}:getIamPolicy
/// Gets the access control policy for a resource. Returns an empty policy if the resource exists and does not have a policy set.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `apigeeregistry_projects_locations_apis_versions_get_iam_policy_execute()` or `apigeeregistry_projects_locations_apis_versions_get_iam_policy`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `apigeeregistry_projects_locations_apis_versions_get_iam_policy_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigeeregistry_projects_locations_apis_versions_get_iam_policy_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Policy>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Policy = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/apis/{apisId}/versions/{versionsId}:getIamPolicy
/// Gets the access control policy for a resource. Returns an empty policy if the resource exists and does not have a policy set.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `apigeeregistry_projects_locations_apis_versions_get_iam_policy_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `apigeeregistry_projects_locations_apis_versions_get_iam_policy_task()`.
/// For the simplest API, use `apigeeregistry_projects_locations_apis_versions_get_iam_policy()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `apigeeregistry_projects_locations_apis_versions_get_iam_policy_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn apigeeregistry_projects_locations_apis_versions_get_iam_policy_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Policy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = apigeeregistry_projects_locations_apis_versions_get_iam_policy_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`apigeeregistry_projects_locations_apis_versions_get_iam_policy`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ApigeeregistryProjectsLocationsApisVersionsGetIamPolicyArgs {
    /// Path parameter: resource
    pub resource: String,
    /// Query parameter: options_requestedPolicyVersion
    pub options_requestedPolicyVersion: Option<Option<String>>,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/apis/{apisId}/versions/{versionsId}:getIamPolicy
/// Gets the access control policy for a resource. Returns an empty policy if the resource exists and does not have a policy set.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `apigeeregistry_projects_locations_apis_versions_get_iam_policy_builder()` + `apigeeregistry_projects_locations_apis_versions_get_iam_policy_execute()`.
/// For task-level control, use `apigeeregistry_projects_locations_apis_versions_get_iam_policy_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigeeregistry_projects_locations_apis_versions_get_iam_policy(
    client: &SimpleHttpClient,
    args: &ApigeeregistryProjectsLocationsApisVersionsGetIamPolicyArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Policy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = apigeeregistry_projects_locations_apis_versions_get_iam_policy_builder(
        client,
        &args.resource,
        &args.options_requestedPolicyVersion,
    )?;
    apigeeregistry_projects_locations_apis_versions_get_iam_policy_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/apis/{apisId}/versions
/// Returns matching versions.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `apigeeregistry_projects_locations_apis_versions_list_execute()` to send, or `apigeeregistry_projects_locations_apis_versions_list` for simplest API.

pub fn apigeeregistry_projects_locations_apis_versions_list_builder(
    client: &SimpleHttpClient,
    parent: &String,
    filter: &Option<Option<String>>,
    orderBy: &Option<Option<String>>,
    pageSize: &Option<Option<String>>,
    pageToken: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://apigeeregistry.googleapis.com/v1/projects/{}/locations/{locationsId}/apis/{apisId}/versions",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter.as_ref() {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = orderBy.as_ref() {
        query_parts.push(format!("orderBy={}", val));
    }
    if let Some(val) = pageSize.as_ref() {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/apis/{apisId}/versions
/// Returns matching versions.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `apigeeregistry_projects_locations_apis_versions_list_execute()` or `apigeeregistry_projects_locations_apis_versions_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `apigeeregistry_projects_locations_apis_versions_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigeeregistry_projects_locations_apis_versions_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ListApiVersionsResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListApiVersionsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/apis/{apisId}/versions
/// Returns matching versions.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `apigeeregistry_projects_locations_apis_versions_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `apigeeregistry_projects_locations_apis_versions_list_task()`.
/// For the simplest API, use `apigeeregistry_projects_locations_apis_versions_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `apigeeregistry_projects_locations_apis_versions_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn apigeeregistry_projects_locations_apis_versions_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListApiVersionsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = apigeeregistry_projects_locations_apis_versions_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`apigeeregistry_projects_locations_apis_versions_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ApigeeregistryProjectsLocationsApisVersionsListArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: filter
    pub filter: Option<Option<String>>,
    /// Query parameter: orderBy
    pub orderBy: Option<Option<String>>,
    /// Query parameter: pageSize
    pub pageSize: Option<Option<String>>,
    /// Query parameter: pageToken
    pub pageToken: Option<Option<String>>,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/apis/{apisId}/versions
/// Returns matching versions.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `apigeeregistry_projects_locations_apis_versions_list_builder()` + `apigeeregistry_projects_locations_apis_versions_list_execute()`.
/// For task-level control, use `apigeeregistry_projects_locations_apis_versions_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigeeregistry_projects_locations_apis_versions_list(
    client: &SimpleHttpClient,
    args: &ApigeeregistryProjectsLocationsApisVersionsListArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListApiVersionsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = apigeeregistry_projects_locations_apis_versions_list_builder(
        client,
        &args.parent,
        &args.filter,
        &args.orderBy,
        &args.pageSize,
        &args.pageToken,
    )?;
    apigeeregistry_projects_locations_apis_versions_list_execute(builder)
}

/// PATCH v1/projects/{projectsId}/locations/{locationsId}/apis/{apisId}/versions/{versionsId}
/// Used to modify a specified version.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `apigeeregistry_projects_locations_apis_versions_patch_execute()` to send, or `apigeeregistry_projects_locations_apis_versions_patch` for simplest API.

pub fn apigeeregistry_projects_locations_apis_versions_patch_builder(
    client: &SimpleHttpClient,
    name: &String,
    allowMissing: &Option<Option<String>>,
    updateMask: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://apigeeregistry.googleapis.com/v1/projects/{}/locations/{locationsId}/apis/{apisId}/versions/{versionsId}",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = allowMissing.as_ref() {
        query_parts.push(format!("allowMissing={}", val));
    }
    if let Some(val) = updateMask.as_ref() {
        query_parts.push(format!("updateMask={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .patch(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// PATCH v1/projects/{projectsId}/locations/{locationsId}/apis/{apisId}/versions/{versionsId}
/// Used to modify a specified version.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `apigeeregistry_projects_locations_apis_versions_patch_execute()` or `apigeeregistry_projects_locations_apis_versions_patch`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `apigeeregistry_projects_locations_apis_versions_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigeeregistry_projects_locations_apis_versions_patch_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ApiVersion>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ApiVersion = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// PATCH v1/projects/{projectsId}/locations/{locationsId}/apis/{apisId}/versions/{versionsId}
/// Used to modify a specified version.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `apigeeregistry_projects_locations_apis_versions_patch_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `apigeeregistry_projects_locations_apis_versions_patch_task()`.
/// For the simplest API, use `apigeeregistry_projects_locations_apis_versions_patch()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `apigeeregistry_projects_locations_apis_versions_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn apigeeregistry_projects_locations_apis_versions_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ApiVersion>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = apigeeregistry_projects_locations_apis_versions_patch_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`apigeeregistry_projects_locations_apis_versions_patch`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ApigeeregistryProjectsLocationsApisVersionsPatchArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: allowMissing
    pub allowMissing: Option<Option<String>>,
    /// Query parameter: updateMask
    pub updateMask: Option<Option<String>>,
}

/// PATCH v1/projects/{projectsId}/locations/{locationsId}/apis/{apisId}/versions/{versionsId}
/// Used to modify a specified version.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `apigeeregistry_projects_locations_apis_versions_patch_builder()` + `apigeeregistry_projects_locations_apis_versions_patch_execute()`.
/// For task-level control, use `apigeeregistry_projects_locations_apis_versions_patch_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigeeregistry_projects_locations_apis_versions_patch(
    client: &SimpleHttpClient,
    args: &ApigeeregistryProjectsLocationsApisVersionsPatchArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ApiVersion>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = apigeeregistry_projects_locations_apis_versions_patch_builder(
        client,
        &args.name,
        &args.allowMissing,
        &args.updateMask,
    )?;
    apigeeregistry_projects_locations_apis_versions_patch_execute(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/apis/{apisId}/versions/{versionsId}:setIamPolicy
/// Sets the access control policy on the specified resource. Replaces any existing policy. Can return NOT_FOUND, INVALID_ARGUMENT, and PERMISSION_DENIED errors.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `apigeeregistry_projects_locations_apis_versions_set_iam_policy_execute()` to send, or `apigeeregistry_projects_locations_apis_versions_set_iam_policy` for simplest API.

pub fn apigeeregistry_projects_locations_apis_versions_set_iam_policy_builder(
    client: &SimpleHttpClient,
    resource: &String,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://apigeeregistry.googleapis.com/v1/projects/{}/locations/{locationsId}/apis/{apisId}/versions/{versionsId}:setIamPolicy",
        resource,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/apis/{apisId}/versions/{versionsId}:setIamPolicy
/// Sets the access control policy on the specified resource. Replaces any existing policy. Can return NOT_FOUND, INVALID_ARGUMENT, and PERMISSION_DENIED errors.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `apigeeregistry_projects_locations_apis_versions_set_iam_policy_execute()` or `apigeeregistry_projects_locations_apis_versions_set_iam_policy`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `apigeeregistry_projects_locations_apis_versions_set_iam_policy_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigeeregistry_projects_locations_apis_versions_set_iam_policy_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Policy>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Policy = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/apis/{apisId}/versions/{versionsId}:setIamPolicy
/// Sets the access control policy on the specified resource. Replaces any existing policy. Can return NOT_FOUND, INVALID_ARGUMENT, and PERMISSION_DENIED errors.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `apigeeregistry_projects_locations_apis_versions_set_iam_policy_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `apigeeregistry_projects_locations_apis_versions_set_iam_policy_task()`.
/// For the simplest API, use `apigeeregistry_projects_locations_apis_versions_set_iam_policy()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `apigeeregistry_projects_locations_apis_versions_set_iam_policy_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn apigeeregistry_projects_locations_apis_versions_set_iam_policy_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Policy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = apigeeregistry_projects_locations_apis_versions_set_iam_policy_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`apigeeregistry_projects_locations_apis_versions_set_iam_policy`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ApigeeregistryProjectsLocationsApisVersionsSetIamPolicyArgs {
    /// Path parameter: resource
    pub resource: String,
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/apis/{apisId}/versions/{versionsId}:setIamPolicy
/// Sets the access control policy on the specified resource. Replaces any existing policy. Can return NOT_FOUND, INVALID_ARGUMENT, and PERMISSION_DENIED errors.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `apigeeregistry_projects_locations_apis_versions_set_iam_policy_builder()` + `apigeeregistry_projects_locations_apis_versions_set_iam_policy_execute()`.
/// For task-level control, use `apigeeregistry_projects_locations_apis_versions_set_iam_policy_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigeeregistry_projects_locations_apis_versions_set_iam_policy(
    client: &SimpleHttpClient,
    args: &ApigeeregistryProjectsLocationsApisVersionsSetIamPolicyArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Policy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = apigeeregistry_projects_locations_apis_versions_set_iam_policy_builder(
        client,
        &args.resource,
    )?;
    apigeeregistry_projects_locations_apis_versions_set_iam_policy_execute(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/apis/{apisId}/versions/{versionsId}:testIamPermissions
/// Returns permissions that a caller has on the specified resource. If the resource does not exist, this will return an empty set of permissions, not a NOT_FOUND error. Note: This operation is designed to be used for building permission-aware UIs and command-line tools, not for authorization checking. This operation may "fail open" without warning.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `apigeeregistry_projects_locations_apis_versions_test_iam_permissions_execute()` to send, or `apigeeregistry_projects_locations_apis_versions_test_iam_permissions` for simplest API.

pub fn apigeeregistry_projects_locations_apis_versions_test_iam_permissions_builder(
    client: &SimpleHttpClient,
    resource: &String,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://apigeeregistry.googleapis.com/v1/projects/{}/locations/{locationsId}/apis/{apisId}/versions/{versionsId}:testIamPermissions",
        resource,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/apis/{apisId}/versions/{versionsId}:testIamPermissions
/// Returns permissions that a caller has on the specified resource. If the resource does not exist, this will return an empty set of permissions, not a NOT_FOUND error. Note: This operation is designed to be used for building permission-aware UIs and command-line tools, not for authorization checking. This operation may "fail open" without warning.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `apigeeregistry_projects_locations_apis_versions_test_iam_permissions_execute()` or `apigeeregistry_projects_locations_apis_versions_test_iam_permissions`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `apigeeregistry_projects_locations_apis_versions_test_iam_permissions_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigeeregistry_projects_locations_apis_versions_test_iam_permissions_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<TestIamPermissionsResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: TestIamPermissionsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/apis/{apisId}/versions/{versionsId}:testIamPermissions
/// Returns permissions that a caller has on the specified resource. If the resource does not exist, this will return an empty set of permissions, not a NOT_FOUND error. Note: This operation is designed to be used for building permission-aware UIs and command-line tools, not for authorization checking. This operation may "fail open" without warning.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `apigeeregistry_projects_locations_apis_versions_test_iam_permissions_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `apigeeregistry_projects_locations_apis_versions_test_iam_permissions_task()`.
/// For the simplest API, use `apigeeregistry_projects_locations_apis_versions_test_iam_permissions()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `apigeeregistry_projects_locations_apis_versions_test_iam_permissions_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn apigeeregistry_projects_locations_apis_versions_test_iam_permissions_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<TestIamPermissionsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = apigeeregistry_projects_locations_apis_versions_test_iam_permissions_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`apigeeregistry_projects_locations_apis_versions_test_iam_permissions`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ApigeeregistryProjectsLocationsApisVersionsTestIamPermissionsArgs {
    /// Path parameter: resource
    pub resource: String,
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/apis/{apisId}/versions/{versionsId}:testIamPermissions
/// Returns permissions that a caller has on the specified resource. If the resource does not exist, this will return an empty set of permissions, not a NOT_FOUND error. Note: This operation is designed to be used for building permission-aware UIs and command-line tools, not for authorization checking. This operation may "fail open" without warning.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `apigeeregistry_projects_locations_apis_versions_test_iam_permissions_builder()` + `apigeeregistry_projects_locations_apis_versions_test_iam_permissions_execute()`.
/// For task-level control, use `apigeeregistry_projects_locations_apis_versions_test_iam_permissions_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigeeregistry_projects_locations_apis_versions_test_iam_permissions(
    client: &SimpleHttpClient,
    args: &ApigeeregistryProjectsLocationsApisVersionsTestIamPermissionsArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<TestIamPermissionsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = apigeeregistry_projects_locations_apis_versions_test_iam_permissions_builder(
        client,
        &args.resource,
    )?;
    apigeeregistry_projects_locations_apis_versions_test_iam_permissions_execute(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/apis/{apisId}/versions/{versionsId}/artifacts
/// Creates a specified artifact.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `apigeeregistry_projects_locations_apis_versions_artifacts_create_execute()` to send, or `apigeeregistry_projects_locations_apis_versions_artifacts_create` for simplest API.

pub fn apigeeregistry_projects_locations_apis_versions_artifacts_create_builder(
    client: &SimpleHttpClient,
    parent: &String,
    artifactId: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://apigeeregistry.googleapis.com/v1/projects/{}/locations/{locationsId}/apis/{apisId}/versions/{versionsId}/artifacts",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = artifactId.as_ref() {
        query_parts.push(format!("artifactId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .post(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/apis/{apisId}/versions/{versionsId}/artifacts
/// Creates a specified artifact.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `apigeeregistry_projects_locations_apis_versions_artifacts_create_execute()` or `apigeeregistry_projects_locations_apis_versions_artifacts_create`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `apigeeregistry_projects_locations_apis_versions_artifacts_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigeeregistry_projects_locations_apis_versions_artifacts_create_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Artifact>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Artifact = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/apis/{apisId}/versions/{versionsId}/artifacts
/// Creates a specified artifact.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `apigeeregistry_projects_locations_apis_versions_artifacts_create_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `apigeeregistry_projects_locations_apis_versions_artifacts_create_task()`.
/// For the simplest API, use `apigeeregistry_projects_locations_apis_versions_artifacts_create()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `apigeeregistry_projects_locations_apis_versions_artifacts_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn apigeeregistry_projects_locations_apis_versions_artifacts_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Artifact>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = apigeeregistry_projects_locations_apis_versions_artifacts_create_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`apigeeregistry_projects_locations_apis_versions_artifacts_create`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ApigeeregistryProjectsLocationsApisVersionsArtifactsCreateArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: artifactId
    pub artifactId: Option<Option<String>>,
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/apis/{apisId}/versions/{versionsId}/artifacts
/// Creates a specified artifact.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `apigeeregistry_projects_locations_apis_versions_artifacts_create_builder()` + `apigeeregistry_projects_locations_apis_versions_artifacts_create_execute()`.
/// For task-level control, use `apigeeregistry_projects_locations_apis_versions_artifacts_create_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigeeregistry_projects_locations_apis_versions_artifacts_create(
    client: &SimpleHttpClient,
    args: &ApigeeregistryProjectsLocationsApisVersionsArtifactsCreateArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Artifact>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = apigeeregistry_projects_locations_apis_versions_artifacts_create_builder(
        client,
        &args.parent,
        &args.artifactId,
    )?;
    apigeeregistry_projects_locations_apis_versions_artifacts_create_execute(builder)
}

/// DELETE v1/projects/{projectsId}/locations/{locationsId}/apis/{apisId}/versions/{versionsId}/artifacts/{artifactsId}
/// Removes a specified artifact.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `apigeeregistry_projects_locations_apis_versions_artifacts_delete_execute()` to send, or `apigeeregistry_projects_locations_apis_versions_artifacts_delete` for simplest API.

pub fn apigeeregistry_projects_locations_apis_versions_artifacts_delete_builder(
    client: &SimpleHttpClient,
    name: &String,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://apigeeregistry.googleapis.com/v1/projects/{}/locations/{locationsId}/apis/{apisId}/versions/{versionsId}/artifacts/{artifactsId}",
        name,
    );

    // Build request
    let builder = client
        .delete(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// DELETE v1/projects/{projectsId}/locations/{locationsId}/apis/{apisId}/versions/{versionsId}/artifacts/{artifactsId}
/// Removes a specified artifact.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `apigeeregistry_projects_locations_apis_versions_artifacts_delete_execute()` or `apigeeregistry_projects_locations_apis_versions_artifacts_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `apigeeregistry_projects_locations_apis_versions_artifacts_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigeeregistry_projects_locations_apis_versions_artifacts_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Empty>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Empty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// DELETE v1/projects/{projectsId}/locations/{locationsId}/apis/{apisId}/versions/{versionsId}/artifacts/{artifactsId}
/// Removes a specified artifact.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `apigeeregistry_projects_locations_apis_versions_artifacts_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `apigeeregistry_projects_locations_apis_versions_artifacts_delete_task()`.
/// For the simplest API, use `apigeeregistry_projects_locations_apis_versions_artifacts_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `apigeeregistry_projects_locations_apis_versions_artifacts_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn apigeeregistry_projects_locations_apis_versions_artifacts_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = apigeeregistry_projects_locations_apis_versions_artifacts_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`apigeeregistry_projects_locations_apis_versions_artifacts_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ApigeeregistryProjectsLocationsApisVersionsArtifactsDeleteArgs {
    /// Path parameter: name
    pub name: String,
}

/// DELETE v1/projects/{projectsId}/locations/{locationsId}/apis/{apisId}/versions/{versionsId}/artifacts/{artifactsId}
/// Removes a specified artifact.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `apigeeregistry_projects_locations_apis_versions_artifacts_delete_builder()` + `apigeeregistry_projects_locations_apis_versions_artifacts_delete_execute()`.
/// For task-level control, use `apigeeregistry_projects_locations_apis_versions_artifacts_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigeeregistry_projects_locations_apis_versions_artifacts_delete(
    client: &SimpleHttpClient,
    args: &ApigeeregistryProjectsLocationsApisVersionsArtifactsDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = apigeeregistry_projects_locations_apis_versions_artifacts_delete_builder(
        client, &args.name,
    )?;
    apigeeregistry_projects_locations_apis_versions_artifacts_delete_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/apis/{apisId}/versions/{versionsId}/artifacts/{artifactsId}
/// Returns a specified artifact.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `apigeeregistry_projects_locations_apis_versions_artifacts_get_execute()` to send, or `apigeeregistry_projects_locations_apis_versions_artifacts_get` for simplest API.

pub fn apigeeregistry_projects_locations_apis_versions_artifacts_get_builder(
    client: &SimpleHttpClient,
    name: &String,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://apigeeregistry.googleapis.com/v1/projects/{}/locations/{locationsId}/apis/{apisId}/versions/{versionsId}/artifacts/{artifactsId}",
        name,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/apis/{apisId}/versions/{versionsId}/artifacts/{artifactsId}
/// Returns a specified artifact.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `apigeeregistry_projects_locations_apis_versions_artifacts_get_execute()` or `apigeeregistry_projects_locations_apis_versions_artifacts_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `apigeeregistry_projects_locations_apis_versions_artifacts_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigeeregistry_projects_locations_apis_versions_artifacts_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Artifact>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Artifact = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/apis/{apisId}/versions/{versionsId}/artifacts/{artifactsId}
/// Returns a specified artifact.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `apigeeregistry_projects_locations_apis_versions_artifacts_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `apigeeregistry_projects_locations_apis_versions_artifacts_get_task()`.
/// For the simplest API, use `apigeeregistry_projects_locations_apis_versions_artifacts_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `apigeeregistry_projects_locations_apis_versions_artifacts_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn apigeeregistry_projects_locations_apis_versions_artifacts_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Artifact>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = apigeeregistry_projects_locations_apis_versions_artifacts_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`apigeeregistry_projects_locations_apis_versions_artifacts_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ApigeeregistryProjectsLocationsApisVersionsArtifactsGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/apis/{apisId}/versions/{versionsId}/artifacts/{artifactsId}
/// Returns a specified artifact.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `apigeeregistry_projects_locations_apis_versions_artifacts_get_builder()` + `apigeeregistry_projects_locations_apis_versions_artifacts_get_execute()`.
/// For task-level control, use `apigeeregistry_projects_locations_apis_versions_artifacts_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigeeregistry_projects_locations_apis_versions_artifacts_get(
    client: &SimpleHttpClient,
    args: &ApigeeregistryProjectsLocationsApisVersionsArtifactsGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Artifact>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        apigeeregistry_projects_locations_apis_versions_artifacts_get_builder(client, &args.name)?;
    apigeeregistry_projects_locations_apis_versions_artifacts_get_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/apis/{apisId}/versions/{versionsId}/artifacts/{artifactsId}:getContents
/// Returns the contents of a specified artifact. If artifacts are stored with GZip compression, the default behavior is to return the artifact uncompressed (the mime_type response field indicates the exact format returned).
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `apigeeregistry_projects_locations_apis_versions_artifacts_get_contents_execute()` to send, or `apigeeregistry_projects_locations_apis_versions_artifacts_get_contents` for simplest API.

pub fn apigeeregistry_projects_locations_apis_versions_artifacts_get_contents_builder(
    client: &SimpleHttpClient,
    name: &String,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://apigeeregistry.googleapis.com/v1/projects/{}/locations/{locationsId}/apis/{apisId}/versions/{versionsId}/artifacts/{artifactsId}:getContents",
        name,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/apis/{apisId}/versions/{versionsId}/artifacts/{artifactsId}:getContents
/// Returns the contents of a specified artifact. If artifacts are stored with GZip compression, the default behavior is to return the artifact uncompressed (the mime_type response field indicates the exact format returned).
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `apigeeregistry_projects_locations_apis_versions_artifacts_get_contents_execute()` or `apigeeregistry_projects_locations_apis_versions_artifacts_get_contents`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `apigeeregistry_projects_locations_apis_versions_artifacts_get_contents_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigeeregistry_projects_locations_apis_versions_artifacts_get_contents_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<HttpBody>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: HttpBody = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/apis/{apisId}/versions/{versionsId}/artifacts/{artifactsId}:getContents
/// Returns the contents of a specified artifact. If artifacts are stored with GZip compression, the default behavior is to return the artifact uncompressed (the mime_type response field indicates the exact format returned).
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `apigeeregistry_projects_locations_apis_versions_artifacts_get_contents_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `apigeeregistry_projects_locations_apis_versions_artifacts_get_contents_task()`.
/// For the simplest API, use `apigeeregistry_projects_locations_apis_versions_artifacts_get_contents()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `apigeeregistry_projects_locations_apis_versions_artifacts_get_contents_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn apigeeregistry_projects_locations_apis_versions_artifacts_get_contents_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<HttpBody>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task =
        apigeeregistry_projects_locations_apis_versions_artifacts_get_contents_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`apigeeregistry_projects_locations_apis_versions_artifacts_get_contents`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ApigeeregistryProjectsLocationsApisVersionsArtifactsGetContentsArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/apis/{apisId}/versions/{versionsId}/artifacts/{artifactsId}:getContents
/// Returns the contents of a specified artifact. If artifacts are stored with GZip compression, the default behavior is to return the artifact uncompressed (the mime_type response field indicates the exact format returned).
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `apigeeregistry_projects_locations_apis_versions_artifacts_get_contents_builder()` + `apigeeregistry_projects_locations_apis_versions_artifacts_get_contents_execute()`.
/// For task-level control, use `apigeeregistry_projects_locations_apis_versions_artifacts_get_contents_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigeeregistry_projects_locations_apis_versions_artifacts_get_contents(
    client: &SimpleHttpClient,
    args: &ApigeeregistryProjectsLocationsApisVersionsArtifactsGetContentsArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<HttpBody>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = apigeeregistry_projects_locations_apis_versions_artifacts_get_contents_builder(
        client, &args.name,
    )?;
    apigeeregistry_projects_locations_apis_versions_artifacts_get_contents_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/apis/{apisId}/versions/{versionsId}/artifacts/{artifactsId}:getIamPolicy
/// Gets the access control policy for a resource. Returns an empty policy if the resource exists and does not have a policy set.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `apigeeregistry_projects_locations_apis_versions_artifacts_get_iam_policy_execute()` to send, or `apigeeregistry_projects_locations_apis_versions_artifacts_get_iam_policy` for simplest API.

pub fn apigeeregistry_projects_locations_apis_versions_artifacts_get_iam_policy_builder(
    client: &SimpleHttpClient,
    resource: &String,
    options_requestedPolicyVersion: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://apigeeregistry.googleapis.com/v1/projects/{}/locations/{locationsId}/apis/{apisId}/versions/{versionsId}/artifacts/{artifactsId}:getIamPolicy",
        resource,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = options_requestedPolicyVersion.as_ref() {
        query_parts.push(format!("options.requestedPolicyVersion={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/apis/{apisId}/versions/{versionsId}/artifacts/{artifactsId}:getIamPolicy
/// Gets the access control policy for a resource. Returns an empty policy if the resource exists and does not have a policy set.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `apigeeregistry_projects_locations_apis_versions_artifacts_get_iam_policy_execute()` or `apigeeregistry_projects_locations_apis_versions_artifacts_get_iam_policy`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `apigeeregistry_projects_locations_apis_versions_artifacts_get_iam_policy_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigeeregistry_projects_locations_apis_versions_artifacts_get_iam_policy_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Policy>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Policy = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/apis/{apisId}/versions/{versionsId}/artifacts/{artifactsId}:getIamPolicy
/// Gets the access control policy for a resource. Returns an empty policy if the resource exists and does not have a policy set.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `apigeeregistry_projects_locations_apis_versions_artifacts_get_iam_policy_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `apigeeregistry_projects_locations_apis_versions_artifacts_get_iam_policy_task()`.
/// For the simplest API, use `apigeeregistry_projects_locations_apis_versions_artifacts_get_iam_policy()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `apigeeregistry_projects_locations_apis_versions_artifacts_get_iam_policy_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn apigeeregistry_projects_locations_apis_versions_artifacts_get_iam_policy_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Policy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task =
        apigeeregistry_projects_locations_apis_versions_artifacts_get_iam_policy_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`apigeeregistry_projects_locations_apis_versions_artifacts_get_iam_policy`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ApigeeregistryProjectsLocationsApisVersionsArtifactsGetIamPolicyArgs {
    /// Path parameter: resource
    pub resource: String,
    /// Query parameter: options_requestedPolicyVersion
    pub options_requestedPolicyVersion: Option<Option<String>>,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/apis/{apisId}/versions/{versionsId}/artifacts/{artifactsId}:getIamPolicy
/// Gets the access control policy for a resource. Returns an empty policy if the resource exists and does not have a policy set.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `apigeeregistry_projects_locations_apis_versions_artifacts_get_iam_policy_builder()` + `apigeeregistry_projects_locations_apis_versions_artifacts_get_iam_policy_execute()`.
/// For task-level control, use `apigeeregistry_projects_locations_apis_versions_artifacts_get_iam_policy_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigeeregistry_projects_locations_apis_versions_artifacts_get_iam_policy(
    client: &SimpleHttpClient,
    args: &ApigeeregistryProjectsLocationsApisVersionsArtifactsGetIamPolicyArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Policy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = apigeeregistry_projects_locations_apis_versions_artifacts_get_iam_policy_builder(
        client,
        &args.resource,
        &args.options_requestedPolicyVersion,
    )?;
    apigeeregistry_projects_locations_apis_versions_artifacts_get_iam_policy_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/apis/{apisId}/versions/{versionsId}/artifacts
/// Returns matching artifacts.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `apigeeregistry_projects_locations_apis_versions_artifacts_list_execute()` to send, or `apigeeregistry_projects_locations_apis_versions_artifacts_list` for simplest API.

pub fn apigeeregistry_projects_locations_apis_versions_artifacts_list_builder(
    client: &SimpleHttpClient,
    parent: &String,
    filter: &Option<Option<String>>,
    orderBy: &Option<Option<String>>,
    pageSize: &Option<Option<String>>,
    pageToken: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://apigeeregistry.googleapis.com/v1/projects/{}/locations/{locationsId}/apis/{apisId}/versions/{versionsId}/artifacts",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter.as_ref() {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = orderBy.as_ref() {
        query_parts.push(format!("orderBy={}", val));
    }
    if let Some(val) = pageSize.as_ref() {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/apis/{apisId}/versions/{versionsId}/artifacts
/// Returns matching artifacts.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `apigeeregistry_projects_locations_apis_versions_artifacts_list_execute()` or `apigeeregistry_projects_locations_apis_versions_artifacts_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `apigeeregistry_projects_locations_apis_versions_artifacts_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigeeregistry_projects_locations_apis_versions_artifacts_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ListArtifactsResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListArtifactsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/apis/{apisId}/versions/{versionsId}/artifacts
/// Returns matching artifacts.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `apigeeregistry_projects_locations_apis_versions_artifacts_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `apigeeregistry_projects_locations_apis_versions_artifacts_list_task()`.
/// For the simplest API, use `apigeeregistry_projects_locations_apis_versions_artifacts_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `apigeeregistry_projects_locations_apis_versions_artifacts_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn apigeeregistry_projects_locations_apis_versions_artifacts_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListArtifactsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = apigeeregistry_projects_locations_apis_versions_artifacts_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`apigeeregistry_projects_locations_apis_versions_artifacts_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ApigeeregistryProjectsLocationsApisVersionsArtifactsListArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: filter
    pub filter: Option<Option<String>>,
    /// Query parameter: orderBy
    pub orderBy: Option<Option<String>>,
    /// Query parameter: pageSize
    pub pageSize: Option<Option<String>>,
    /// Query parameter: pageToken
    pub pageToken: Option<Option<String>>,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/apis/{apisId}/versions/{versionsId}/artifacts
/// Returns matching artifacts.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `apigeeregistry_projects_locations_apis_versions_artifacts_list_builder()` + `apigeeregistry_projects_locations_apis_versions_artifacts_list_execute()`.
/// For task-level control, use `apigeeregistry_projects_locations_apis_versions_artifacts_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigeeregistry_projects_locations_apis_versions_artifacts_list(
    client: &SimpleHttpClient,
    args: &ApigeeregistryProjectsLocationsApisVersionsArtifactsListArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListArtifactsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = apigeeregistry_projects_locations_apis_versions_artifacts_list_builder(
        client,
        &args.parent,
        &args.filter,
        &args.orderBy,
        &args.pageSize,
        &args.pageToken,
    )?;
    apigeeregistry_projects_locations_apis_versions_artifacts_list_execute(builder)
}

/// PUT v1/projects/{projectsId}/locations/{locationsId}/apis/{apisId}/versions/{versionsId}/artifacts/{artifactsId}
/// Used to replace a specified artifact.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `apigeeregistry_projects_locations_apis_versions_artifacts_replace_artifact_execute()` to send, or `apigeeregistry_projects_locations_apis_versions_artifacts_replace_artifact` for simplest API.

pub fn apigeeregistry_projects_locations_apis_versions_artifacts_replace_artifact_builder(
    client: &SimpleHttpClient,
    name: &String,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://apigeeregistry.googleapis.com/v1/projects/{}/locations/{locationsId}/apis/{apisId}/versions/{versionsId}/artifacts/{artifactsId}",
        name,
    );

    // Build request
    let builder = client
        .put(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// PUT v1/projects/{projectsId}/locations/{locationsId}/apis/{apisId}/versions/{versionsId}/artifacts/{artifactsId}
/// Used to replace a specified artifact.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `apigeeregistry_projects_locations_apis_versions_artifacts_replace_artifact_execute()` or `apigeeregistry_projects_locations_apis_versions_artifacts_replace_artifact`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `apigeeregistry_projects_locations_apis_versions_artifacts_replace_artifact_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigeeregistry_projects_locations_apis_versions_artifacts_replace_artifact_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Artifact>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Artifact = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// PUT v1/projects/{projectsId}/locations/{locationsId}/apis/{apisId}/versions/{versionsId}/artifacts/{artifactsId}
/// Used to replace a specified artifact.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `apigeeregistry_projects_locations_apis_versions_artifacts_replace_artifact_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `apigeeregistry_projects_locations_apis_versions_artifacts_replace_artifact_task()`.
/// For the simplest API, use `apigeeregistry_projects_locations_apis_versions_artifacts_replace_artifact()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `apigeeregistry_projects_locations_apis_versions_artifacts_replace_artifact_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn apigeeregistry_projects_locations_apis_versions_artifacts_replace_artifact_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Artifact>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task =
        apigeeregistry_projects_locations_apis_versions_artifacts_replace_artifact_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`apigeeregistry_projects_locations_apis_versions_artifacts_replace_artifact`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ApigeeregistryProjectsLocationsApisVersionsArtifactsReplaceArtifactArgs {
    /// Path parameter: name
    pub name: String,
}

/// PUT v1/projects/{projectsId}/locations/{locationsId}/apis/{apisId}/versions/{versionsId}/artifacts/{artifactsId}
/// Used to replace a specified artifact.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `apigeeregistry_projects_locations_apis_versions_artifacts_replace_artifact_builder()` + `apigeeregistry_projects_locations_apis_versions_artifacts_replace_artifact_execute()`.
/// For task-level control, use `apigeeregistry_projects_locations_apis_versions_artifacts_replace_artifact_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigeeregistry_projects_locations_apis_versions_artifacts_replace_artifact(
    client: &SimpleHttpClient,
    args: &ApigeeregistryProjectsLocationsApisVersionsArtifactsReplaceArtifactArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Artifact>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        apigeeregistry_projects_locations_apis_versions_artifacts_replace_artifact_builder(
            client, &args.name,
        )?;
    apigeeregistry_projects_locations_apis_versions_artifacts_replace_artifact_execute(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/apis/{apisId}/versions/{versionsId}/artifacts/{artifactsId}:setIamPolicy
/// Sets the access control policy on the specified resource. Replaces any existing policy. Can return NOT_FOUND, INVALID_ARGUMENT, and PERMISSION_DENIED errors.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `apigeeregistry_projects_locations_apis_versions_artifacts_set_iam_policy_execute()` to send, or `apigeeregistry_projects_locations_apis_versions_artifacts_set_iam_policy` for simplest API.

pub fn apigeeregistry_projects_locations_apis_versions_artifacts_set_iam_policy_builder(
    client: &SimpleHttpClient,
    resource: &String,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://apigeeregistry.googleapis.com/v1/projects/{}/locations/{locationsId}/apis/{apisId}/versions/{versionsId}/artifacts/{artifactsId}:setIamPolicy",
        resource,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/apis/{apisId}/versions/{versionsId}/artifacts/{artifactsId}:setIamPolicy
/// Sets the access control policy on the specified resource. Replaces any existing policy. Can return NOT_FOUND, INVALID_ARGUMENT, and PERMISSION_DENIED errors.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `apigeeregistry_projects_locations_apis_versions_artifacts_set_iam_policy_execute()` or `apigeeregistry_projects_locations_apis_versions_artifacts_set_iam_policy`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `apigeeregistry_projects_locations_apis_versions_artifacts_set_iam_policy_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigeeregistry_projects_locations_apis_versions_artifacts_set_iam_policy_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Policy>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Policy = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/apis/{apisId}/versions/{versionsId}/artifacts/{artifactsId}:setIamPolicy
/// Sets the access control policy on the specified resource. Replaces any existing policy. Can return NOT_FOUND, INVALID_ARGUMENT, and PERMISSION_DENIED errors.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `apigeeregistry_projects_locations_apis_versions_artifacts_set_iam_policy_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `apigeeregistry_projects_locations_apis_versions_artifacts_set_iam_policy_task()`.
/// For the simplest API, use `apigeeregistry_projects_locations_apis_versions_artifacts_set_iam_policy()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `apigeeregistry_projects_locations_apis_versions_artifacts_set_iam_policy_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn apigeeregistry_projects_locations_apis_versions_artifacts_set_iam_policy_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Policy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task =
        apigeeregistry_projects_locations_apis_versions_artifacts_set_iam_policy_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`apigeeregistry_projects_locations_apis_versions_artifacts_set_iam_policy`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ApigeeregistryProjectsLocationsApisVersionsArtifactsSetIamPolicyArgs {
    /// Path parameter: resource
    pub resource: String,
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/apis/{apisId}/versions/{versionsId}/artifacts/{artifactsId}:setIamPolicy
/// Sets the access control policy on the specified resource. Replaces any existing policy. Can return NOT_FOUND, INVALID_ARGUMENT, and PERMISSION_DENIED errors.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `apigeeregistry_projects_locations_apis_versions_artifacts_set_iam_policy_builder()` + `apigeeregistry_projects_locations_apis_versions_artifacts_set_iam_policy_execute()`.
/// For task-level control, use `apigeeregistry_projects_locations_apis_versions_artifacts_set_iam_policy_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigeeregistry_projects_locations_apis_versions_artifacts_set_iam_policy(
    client: &SimpleHttpClient,
    args: &ApigeeregistryProjectsLocationsApisVersionsArtifactsSetIamPolicyArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Policy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = apigeeregistry_projects_locations_apis_versions_artifacts_set_iam_policy_builder(
        client,
        &args.resource,
    )?;
    apigeeregistry_projects_locations_apis_versions_artifacts_set_iam_policy_execute(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/apis/{apisId}/versions/{versionsId}/artifacts/{artifactsId}:testIamPermissions
/// Returns permissions that a caller has on the specified resource. If the resource does not exist, this will return an empty set of permissions, not a NOT_FOUND error. Note: This operation is designed to be used for building permission-aware UIs and command-line tools, not for authorization checking. This operation may "fail open" without warning.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `apigeeregistry_projects_locations_apis_versions_artifacts_test_iam_permissions_execute()` to send, or `apigeeregistry_projects_locations_apis_versions_artifacts_test_iam_permissions` for simplest API.

pub fn apigeeregistry_projects_locations_apis_versions_artifacts_test_iam_permissions_builder(
    client: &SimpleHttpClient,
    resource: &String,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://apigeeregistry.googleapis.com/v1/projects/{}/locations/{locationsId}/apis/{apisId}/versions/{versionsId}/artifacts/{artifactsId}:testIamPermissions",
        resource,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/apis/{apisId}/versions/{versionsId}/artifacts/{artifactsId}:testIamPermissions
/// Returns permissions that a caller has on the specified resource. If the resource does not exist, this will return an empty set of permissions, not a NOT_FOUND error. Note: This operation is designed to be used for building permission-aware UIs and command-line tools, not for authorization checking. This operation may "fail open" without warning.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `apigeeregistry_projects_locations_apis_versions_artifacts_test_iam_permissions_execute()` or `apigeeregistry_projects_locations_apis_versions_artifacts_test_iam_permissions`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `apigeeregistry_projects_locations_apis_versions_artifacts_test_iam_permissions_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigeeregistry_projects_locations_apis_versions_artifacts_test_iam_permissions_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<TestIamPermissionsResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: TestIamPermissionsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/apis/{apisId}/versions/{versionsId}/artifacts/{artifactsId}:testIamPermissions
/// Returns permissions that a caller has on the specified resource. If the resource does not exist, this will return an empty set of permissions, not a NOT_FOUND error. Note: This operation is designed to be used for building permission-aware UIs and command-line tools, not for authorization checking. This operation may "fail open" without warning.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `apigeeregistry_projects_locations_apis_versions_artifacts_test_iam_permissions_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `apigeeregistry_projects_locations_apis_versions_artifacts_test_iam_permissions_task()`.
/// For the simplest API, use `apigeeregistry_projects_locations_apis_versions_artifacts_test_iam_permissions()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `apigeeregistry_projects_locations_apis_versions_artifacts_test_iam_permissions_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn apigeeregistry_projects_locations_apis_versions_artifacts_test_iam_permissions_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<TestIamPermissionsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = apigeeregistry_projects_locations_apis_versions_artifacts_test_iam_permissions_task(
        builder,
    )?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`apigeeregistry_projects_locations_apis_versions_artifacts_test_iam_permissions`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ApigeeregistryProjectsLocationsApisVersionsArtifactsTestIamPermissionsArgs {
    /// Path parameter: resource
    pub resource: String,
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/apis/{apisId}/versions/{versionsId}/artifacts/{artifactsId}:testIamPermissions
/// Returns permissions that a caller has on the specified resource. If the resource does not exist, this will return an empty set of permissions, not a NOT_FOUND error. Note: This operation is designed to be used for building permission-aware UIs and command-line tools, not for authorization checking. This operation may "fail open" without warning.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `apigeeregistry_projects_locations_apis_versions_artifacts_test_iam_permissions_builder()` + `apigeeregistry_projects_locations_apis_versions_artifacts_test_iam_permissions_execute()`.
/// For task-level control, use `apigeeregistry_projects_locations_apis_versions_artifacts_test_iam_permissions_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigeeregistry_projects_locations_apis_versions_artifacts_test_iam_permissions(
    client: &SimpleHttpClient,
    args: &ApigeeregistryProjectsLocationsApisVersionsArtifactsTestIamPermissionsArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<TestIamPermissionsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        apigeeregistry_projects_locations_apis_versions_artifacts_test_iam_permissions_builder(
            client,
            &args.resource,
        )?;
    apigeeregistry_projects_locations_apis_versions_artifacts_test_iam_permissions_execute(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/apis/{apisId}/versions/{versionsId}/specs
/// Creates a specified spec.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `apigeeregistry_projects_locations_apis_versions_specs_create_execute()` to send, or `apigeeregistry_projects_locations_apis_versions_specs_create` for simplest API.

pub fn apigeeregistry_projects_locations_apis_versions_specs_create_builder(
    client: &SimpleHttpClient,
    parent: &String,
    apiSpecId: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://apigeeregistry.googleapis.com/v1/projects/{}/locations/{locationsId}/apis/{apisId}/versions/{versionsId}/specs",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = apiSpecId.as_ref() {
        query_parts.push(format!("apiSpecId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .post(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/apis/{apisId}/versions/{versionsId}/specs
/// Creates a specified spec.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `apigeeregistry_projects_locations_apis_versions_specs_create_execute()` or `apigeeregistry_projects_locations_apis_versions_specs_create`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `apigeeregistry_projects_locations_apis_versions_specs_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigeeregistry_projects_locations_apis_versions_specs_create_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ApiSpec>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ApiSpec = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/apis/{apisId}/versions/{versionsId}/specs
/// Creates a specified spec.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `apigeeregistry_projects_locations_apis_versions_specs_create_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `apigeeregistry_projects_locations_apis_versions_specs_create_task()`.
/// For the simplest API, use `apigeeregistry_projects_locations_apis_versions_specs_create()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `apigeeregistry_projects_locations_apis_versions_specs_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn apigeeregistry_projects_locations_apis_versions_specs_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ApiSpec>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = apigeeregistry_projects_locations_apis_versions_specs_create_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`apigeeregistry_projects_locations_apis_versions_specs_create`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ApigeeregistryProjectsLocationsApisVersionsSpecsCreateArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: apiSpecId
    pub apiSpecId: Option<Option<String>>,
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/apis/{apisId}/versions/{versionsId}/specs
/// Creates a specified spec.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `apigeeregistry_projects_locations_apis_versions_specs_create_builder()` + `apigeeregistry_projects_locations_apis_versions_specs_create_execute()`.
/// For task-level control, use `apigeeregistry_projects_locations_apis_versions_specs_create_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigeeregistry_projects_locations_apis_versions_specs_create(
    client: &SimpleHttpClient,
    args: &ApigeeregistryProjectsLocationsApisVersionsSpecsCreateArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ApiSpec>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = apigeeregistry_projects_locations_apis_versions_specs_create_builder(
        client,
        &args.parent,
        &args.apiSpecId,
    )?;
    apigeeregistry_projects_locations_apis_versions_specs_create_execute(builder)
}

/// DELETE v1/projects/{projectsId}/locations/{locationsId}/apis/{apisId}/versions/{versionsId}/specs/{specsId}
/// Removes a specified spec, all revisions, and all child resources (e.g., artifacts).
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `apigeeregistry_projects_locations_apis_versions_specs_delete_execute()` to send, or `apigeeregistry_projects_locations_apis_versions_specs_delete` for simplest API.

pub fn apigeeregistry_projects_locations_apis_versions_specs_delete_builder(
    client: &SimpleHttpClient,
    name: &String,
    force: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://apigeeregistry.googleapis.com/v1/projects/{}/locations/{locationsId}/apis/{apisId}/versions/{versionsId}/specs/{specsId}",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = force.as_ref() {
        query_parts.push(format!("force={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .delete(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// DELETE v1/projects/{projectsId}/locations/{locationsId}/apis/{apisId}/versions/{versionsId}/specs/{specsId}
/// Removes a specified spec, all revisions, and all child resources (e.g., artifacts).
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `apigeeregistry_projects_locations_apis_versions_specs_delete_execute()` or `apigeeregistry_projects_locations_apis_versions_specs_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `apigeeregistry_projects_locations_apis_versions_specs_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigeeregistry_projects_locations_apis_versions_specs_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Empty>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Empty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// DELETE v1/projects/{projectsId}/locations/{locationsId}/apis/{apisId}/versions/{versionsId}/specs/{specsId}
/// Removes a specified spec, all revisions, and all child resources (e.g., artifacts).
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `apigeeregistry_projects_locations_apis_versions_specs_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `apigeeregistry_projects_locations_apis_versions_specs_delete_task()`.
/// For the simplest API, use `apigeeregistry_projects_locations_apis_versions_specs_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `apigeeregistry_projects_locations_apis_versions_specs_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn apigeeregistry_projects_locations_apis_versions_specs_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = apigeeregistry_projects_locations_apis_versions_specs_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`apigeeregistry_projects_locations_apis_versions_specs_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ApigeeregistryProjectsLocationsApisVersionsSpecsDeleteArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: force
    pub force: Option<Option<String>>,
}

/// DELETE v1/projects/{projectsId}/locations/{locationsId}/apis/{apisId}/versions/{versionsId}/specs/{specsId}
/// Removes a specified spec, all revisions, and all child resources (e.g., artifacts).
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `apigeeregistry_projects_locations_apis_versions_specs_delete_builder()` + `apigeeregistry_projects_locations_apis_versions_specs_delete_execute()`.
/// For task-level control, use `apigeeregistry_projects_locations_apis_versions_specs_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigeeregistry_projects_locations_apis_versions_specs_delete(
    client: &SimpleHttpClient,
    args: &ApigeeregistryProjectsLocationsApisVersionsSpecsDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = apigeeregistry_projects_locations_apis_versions_specs_delete_builder(
        client,
        &args.name,
        &args.force,
    )?;
    apigeeregistry_projects_locations_apis_versions_specs_delete_execute(builder)
}

/// DELETE v1/projects/{projectsId}/locations/{locationsId}/apis/{apisId}/versions/{versionsId}/specs/{specsId}:deleteRevision
/// Deletes a revision of a spec.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `apigeeregistry_projects_locations_apis_versions_specs_delete_revision_execute()` to send, or `apigeeregistry_projects_locations_apis_versions_specs_delete_revision` for simplest API.

pub fn apigeeregistry_projects_locations_apis_versions_specs_delete_revision_builder(
    client: &SimpleHttpClient,
    name: &String,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://apigeeregistry.googleapis.com/v1/projects/{}/locations/{locationsId}/apis/{apisId}/versions/{versionsId}/specs/{specsId}:deleteRevision",
        name,
    );

    // Build request
    let builder = client
        .delete(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// DELETE v1/projects/{projectsId}/locations/{locationsId}/apis/{apisId}/versions/{versionsId}/specs/{specsId}:deleteRevision
/// Deletes a revision of a spec.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `apigeeregistry_projects_locations_apis_versions_specs_delete_revision_execute()` or `apigeeregistry_projects_locations_apis_versions_specs_delete_revision`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `apigeeregistry_projects_locations_apis_versions_specs_delete_revision_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigeeregistry_projects_locations_apis_versions_specs_delete_revision_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ApiSpec>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ApiSpec = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// DELETE v1/projects/{projectsId}/locations/{locationsId}/apis/{apisId}/versions/{versionsId}/specs/{specsId}:deleteRevision
/// Deletes a revision of a spec.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `apigeeregistry_projects_locations_apis_versions_specs_delete_revision_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `apigeeregistry_projects_locations_apis_versions_specs_delete_revision_task()`.
/// For the simplest API, use `apigeeregistry_projects_locations_apis_versions_specs_delete_revision()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `apigeeregistry_projects_locations_apis_versions_specs_delete_revision_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn apigeeregistry_projects_locations_apis_versions_specs_delete_revision_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ApiSpec>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = apigeeregistry_projects_locations_apis_versions_specs_delete_revision_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`apigeeregistry_projects_locations_apis_versions_specs_delete_revision`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ApigeeregistryProjectsLocationsApisVersionsSpecsDeleteRevisionArgs {
    /// Path parameter: name
    pub name: String,
}

/// DELETE v1/projects/{projectsId}/locations/{locationsId}/apis/{apisId}/versions/{versionsId}/specs/{specsId}:deleteRevision
/// Deletes a revision of a spec.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `apigeeregistry_projects_locations_apis_versions_specs_delete_revision_builder()` + `apigeeregistry_projects_locations_apis_versions_specs_delete_revision_execute()`.
/// For task-level control, use `apigeeregistry_projects_locations_apis_versions_specs_delete_revision_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigeeregistry_projects_locations_apis_versions_specs_delete_revision(
    client: &SimpleHttpClient,
    args: &ApigeeregistryProjectsLocationsApisVersionsSpecsDeleteRevisionArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ApiSpec>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = apigeeregistry_projects_locations_apis_versions_specs_delete_revision_builder(
        client, &args.name,
    )?;
    apigeeregistry_projects_locations_apis_versions_specs_delete_revision_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/apis/{apisId}/versions/{versionsId}/specs/{specsId}
/// Returns a specified spec.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `apigeeregistry_projects_locations_apis_versions_specs_get_execute()` to send, or `apigeeregistry_projects_locations_apis_versions_specs_get` for simplest API.

pub fn apigeeregistry_projects_locations_apis_versions_specs_get_builder(
    client: &SimpleHttpClient,
    name: &String,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://apigeeregistry.googleapis.com/v1/projects/{}/locations/{locationsId}/apis/{apisId}/versions/{versionsId}/specs/{specsId}",
        name,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/apis/{apisId}/versions/{versionsId}/specs/{specsId}
/// Returns a specified spec.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `apigeeregistry_projects_locations_apis_versions_specs_get_execute()` or `apigeeregistry_projects_locations_apis_versions_specs_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `apigeeregistry_projects_locations_apis_versions_specs_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigeeregistry_projects_locations_apis_versions_specs_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ApiSpec>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ApiSpec = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/apis/{apisId}/versions/{versionsId}/specs/{specsId}
/// Returns a specified spec.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `apigeeregistry_projects_locations_apis_versions_specs_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `apigeeregistry_projects_locations_apis_versions_specs_get_task()`.
/// For the simplest API, use `apigeeregistry_projects_locations_apis_versions_specs_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `apigeeregistry_projects_locations_apis_versions_specs_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn apigeeregistry_projects_locations_apis_versions_specs_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ApiSpec>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = apigeeregistry_projects_locations_apis_versions_specs_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`apigeeregistry_projects_locations_apis_versions_specs_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ApigeeregistryProjectsLocationsApisVersionsSpecsGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/apis/{apisId}/versions/{versionsId}/specs/{specsId}
/// Returns a specified spec.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `apigeeregistry_projects_locations_apis_versions_specs_get_builder()` + `apigeeregistry_projects_locations_apis_versions_specs_get_execute()`.
/// For task-level control, use `apigeeregistry_projects_locations_apis_versions_specs_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigeeregistry_projects_locations_apis_versions_specs_get(
    client: &SimpleHttpClient,
    args: &ApigeeregistryProjectsLocationsApisVersionsSpecsGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ApiSpec>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        apigeeregistry_projects_locations_apis_versions_specs_get_builder(client, &args.name)?;
    apigeeregistry_projects_locations_apis_versions_specs_get_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/apis/{apisId}/versions/{versionsId}/specs/{specsId}:getContents
/// Returns the contents of a specified spec. If specs are stored with GZip compression, the default behavior is to return the spec uncompressed (the mime_type response field indicates the exact format returned).
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `apigeeregistry_projects_locations_apis_versions_specs_get_contents_execute()` to send, or `apigeeregistry_projects_locations_apis_versions_specs_get_contents` for simplest API.

pub fn apigeeregistry_projects_locations_apis_versions_specs_get_contents_builder(
    client: &SimpleHttpClient,
    name: &String,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://apigeeregistry.googleapis.com/v1/projects/{}/locations/{locationsId}/apis/{apisId}/versions/{versionsId}/specs/{specsId}:getContents",
        name,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/apis/{apisId}/versions/{versionsId}/specs/{specsId}:getContents
/// Returns the contents of a specified spec. If specs are stored with GZip compression, the default behavior is to return the spec uncompressed (the mime_type response field indicates the exact format returned).
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `apigeeregistry_projects_locations_apis_versions_specs_get_contents_execute()` or `apigeeregistry_projects_locations_apis_versions_specs_get_contents`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `apigeeregistry_projects_locations_apis_versions_specs_get_contents_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigeeregistry_projects_locations_apis_versions_specs_get_contents_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<HttpBody>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: HttpBody = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/apis/{apisId}/versions/{versionsId}/specs/{specsId}:getContents
/// Returns the contents of a specified spec. If specs are stored with GZip compression, the default behavior is to return the spec uncompressed (the mime_type response field indicates the exact format returned).
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `apigeeregistry_projects_locations_apis_versions_specs_get_contents_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `apigeeregistry_projects_locations_apis_versions_specs_get_contents_task()`.
/// For the simplest API, use `apigeeregistry_projects_locations_apis_versions_specs_get_contents()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `apigeeregistry_projects_locations_apis_versions_specs_get_contents_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn apigeeregistry_projects_locations_apis_versions_specs_get_contents_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<HttpBody>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = apigeeregistry_projects_locations_apis_versions_specs_get_contents_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`apigeeregistry_projects_locations_apis_versions_specs_get_contents`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ApigeeregistryProjectsLocationsApisVersionsSpecsGetContentsArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/apis/{apisId}/versions/{versionsId}/specs/{specsId}:getContents
/// Returns the contents of a specified spec. If specs are stored with GZip compression, the default behavior is to return the spec uncompressed (the mime_type response field indicates the exact format returned).
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `apigeeregistry_projects_locations_apis_versions_specs_get_contents_builder()` + `apigeeregistry_projects_locations_apis_versions_specs_get_contents_execute()`.
/// For task-level control, use `apigeeregistry_projects_locations_apis_versions_specs_get_contents_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigeeregistry_projects_locations_apis_versions_specs_get_contents(
    client: &SimpleHttpClient,
    args: &ApigeeregistryProjectsLocationsApisVersionsSpecsGetContentsArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<HttpBody>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = apigeeregistry_projects_locations_apis_versions_specs_get_contents_builder(
        client, &args.name,
    )?;
    apigeeregistry_projects_locations_apis_versions_specs_get_contents_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/apis/{apisId}/versions/{versionsId}/specs/{specsId}:getIamPolicy
/// Gets the access control policy for a resource. Returns an empty policy if the resource exists and does not have a policy set.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `apigeeregistry_projects_locations_apis_versions_specs_get_iam_policy_execute()` to send, or `apigeeregistry_projects_locations_apis_versions_specs_get_iam_policy` for simplest API.

pub fn apigeeregistry_projects_locations_apis_versions_specs_get_iam_policy_builder(
    client: &SimpleHttpClient,
    resource: &String,
    options_requestedPolicyVersion: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://apigeeregistry.googleapis.com/v1/projects/{}/locations/{locationsId}/apis/{apisId}/versions/{versionsId}/specs/{specsId}:getIamPolicy",
        resource,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = options_requestedPolicyVersion.as_ref() {
        query_parts.push(format!("options.requestedPolicyVersion={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/apis/{apisId}/versions/{versionsId}/specs/{specsId}:getIamPolicy
/// Gets the access control policy for a resource. Returns an empty policy if the resource exists and does not have a policy set.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `apigeeregistry_projects_locations_apis_versions_specs_get_iam_policy_execute()` or `apigeeregistry_projects_locations_apis_versions_specs_get_iam_policy`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `apigeeregistry_projects_locations_apis_versions_specs_get_iam_policy_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigeeregistry_projects_locations_apis_versions_specs_get_iam_policy_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Policy>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Policy = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/apis/{apisId}/versions/{versionsId}/specs/{specsId}:getIamPolicy
/// Gets the access control policy for a resource. Returns an empty policy if the resource exists and does not have a policy set.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `apigeeregistry_projects_locations_apis_versions_specs_get_iam_policy_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `apigeeregistry_projects_locations_apis_versions_specs_get_iam_policy_task()`.
/// For the simplest API, use `apigeeregistry_projects_locations_apis_versions_specs_get_iam_policy()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `apigeeregistry_projects_locations_apis_versions_specs_get_iam_policy_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn apigeeregistry_projects_locations_apis_versions_specs_get_iam_policy_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Policy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = apigeeregistry_projects_locations_apis_versions_specs_get_iam_policy_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`apigeeregistry_projects_locations_apis_versions_specs_get_iam_policy`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ApigeeregistryProjectsLocationsApisVersionsSpecsGetIamPolicyArgs {
    /// Path parameter: resource
    pub resource: String,
    /// Query parameter: options_requestedPolicyVersion
    pub options_requestedPolicyVersion: Option<Option<String>>,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/apis/{apisId}/versions/{versionsId}/specs/{specsId}:getIamPolicy
/// Gets the access control policy for a resource. Returns an empty policy if the resource exists and does not have a policy set.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `apigeeregistry_projects_locations_apis_versions_specs_get_iam_policy_builder()` + `apigeeregistry_projects_locations_apis_versions_specs_get_iam_policy_execute()`.
/// For task-level control, use `apigeeregistry_projects_locations_apis_versions_specs_get_iam_policy_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigeeregistry_projects_locations_apis_versions_specs_get_iam_policy(
    client: &SimpleHttpClient,
    args: &ApigeeregistryProjectsLocationsApisVersionsSpecsGetIamPolicyArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Policy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = apigeeregistry_projects_locations_apis_versions_specs_get_iam_policy_builder(
        client,
        &args.resource,
        &args.options_requestedPolicyVersion,
    )?;
    apigeeregistry_projects_locations_apis_versions_specs_get_iam_policy_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/apis/{apisId}/versions/{versionsId}/specs
/// Returns matching specs.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `apigeeregistry_projects_locations_apis_versions_specs_list_execute()` to send, or `apigeeregistry_projects_locations_apis_versions_specs_list` for simplest API.

pub fn apigeeregistry_projects_locations_apis_versions_specs_list_builder(
    client: &SimpleHttpClient,
    parent: &String,
    filter: &Option<Option<String>>,
    orderBy: &Option<Option<String>>,
    pageSize: &Option<Option<String>>,
    pageToken: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://apigeeregistry.googleapis.com/v1/projects/{}/locations/{locationsId}/apis/{apisId}/versions/{versionsId}/specs",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter.as_ref() {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = orderBy.as_ref() {
        query_parts.push(format!("orderBy={}", val));
    }
    if let Some(val) = pageSize.as_ref() {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/apis/{apisId}/versions/{versionsId}/specs
/// Returns matching specs.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `apigeeregistry_projects_locations_apis_versions_specs_list_execute()` or `apigeeregistry_projects_locations_apis_versions_specs_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `apigeeregistry_projects_locations_apis_versions_specs_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigeeregistry_projects_locations_apis_versions_specs_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ListApiSpecsResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListApiSpecsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/apis/{apisId}/versions/{versionsId}/specs
/// Returns matching specs.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `apigeeregistry_projects_locations_apis_versions_specs_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `apigeeregistry_projects_locations_apis_versions_specs_list_task()`.
/// For the simplest API, use `apigeeregistry_projects_locations_apis_versions_specs_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `apigeeregistry_projects_locations_apis_versions_specs_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn apigeeregistry_projects_locations_apis_versions_specs_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListApiSpecsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = apigeeregistry_projects_locations_apis_versions_specs_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`apigeeregistry_projects_locations_apis_versions_specs_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ApigeeregistryProjectsLocationsApisVersionsSpecsListArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: filter
    pub filter: Option<Option<String>>,
    /// Query parameter: orderBy
    pub orderBy: Option<Option<String>>,
    /// Query parameter: pageSize
    pub pageSize: Option<Option<String>>,
    /// Query parameter: pageToken
    pub pageToken: Option<Option<String>>,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/apis/{apisId}/versions/{versionsId}/specs
/// Returns matching specs.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `apigeeregistry_projects_locations_apis_versions_specs_list_builder()` + `apigeeregistry_projects_locations_apis_versions_specs_list_execute()`.
/// For task-level control, use `apigeeregistry_projects_locations_apis_versions_specs_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigeeregistry_projects_locations_apis_versions_specs_list(
    client: &SimpleHttpClient,
    args: &ApigeeregistryProjectsLocationsApisVersionsSpecsListArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListApiSpecsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = apigeeregistry_projects_locations_apis_versions_specs_list_builder(
        client,
        &args.parent,
        &args.filter,
        &args.orderBy,
        &args.pageSize,
        &args.pageToken,
    )?;
    apigeeregistry_projects_locations_apis_versions_specs_list_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/apis/{apisId}/versions/{versionsId}/specs/{specsId}:listRevisions
/// Lists all revisions of a spec. Revisions are returned in descending order of revision creation time.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `apigeeregistry_projects_locations_apis_versions_specs_list_revisions_execute()` to send, or `apigeeregistry_projects_locations_apis_versions_specs_list_revisions` for simplest API.

pub fn apigeeregistry_projects_locations_apis_versions_specs_list_revisions_builder(
    client: &SimpleHttpClient,
    name: &String,
    filter: &Option<Option<String>>,
    pageSize: &Option<Option<String>>,
    pageToken: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://apigeeregistry.googleapis.com/v1/projects/{}/locations/{locationsId}/apis/{apisId}/versions/{versionsId}/specs/{specsId}:listRevisions",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter.as_ref() {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = pageSize.as_ref() {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/apis/{apisId}/versions/{versionsId}/specs/{specsId}:listRevisions
/// Lists all revisions of a spec. Revisions are returned in descending order of revision creation time.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `apigeeregistry_projects_locations_apis_versions_specs_list_revisions_execute()` or `apigeeregistry_projects_locations_apis_versions_specs_list_revisions`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `apigeeregistry_projects_locations_apis_versions_specs_list_revisions_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigeeregistry_projects_locations_apis_versions_specs_list_revisions_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ListApiSpecRevisionsResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListApiSpecRevisionsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/apis/{apisId}/versions/{versionsId}/specs/{specsId}:listRevisions
/// Lists all revisions of a spec. Revisions are returned in descending order of revision creation time.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `apigeeregistry_projects_locations_apis_versions_specs_list_revisions_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `apigeeregistry_projects_locations_apis_versions_specs_list_revisions_task()`.
/// For the simplest API, use `apigeeregistry_projects_locations_apis_versions_specs_list_revisions()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `apigeeregistry_projects_locations_apis_versions_specs_list_revisions_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn apigeeregistry_projects_locations_apis_versions_specs_list_revisions_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ListApiSpecRevisionsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = apigeeregistry_projects_locations_apis_versions_specs_list_revisions_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`apigeeregistry_projects_locations_apis_versions_specs_list_revisions`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ApigeeregistryProjectsLocationsApisVersionsSpecsListRevisionsArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: filter
    pub filter: Option<Option<String>>,
    /// Query parameter: pageSize
    pub pageSize: Option<Option<String>>,
    /// Query parameter: pageToken
    pub pageToken: Option<Option<String>>,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/apis/{apisId}/versions/{versionsId}/specs/{specsId}:listRevisions
/// Lists all revisions of a spec. Revisions are returned in descending order of revision creation time.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `apigeeregistry_projects_locations_apis_versions_specs_list_revisions_builder()` + `apigeeregistry_projects_locations_apis_versions_specs_list_revisions_execute()`.
/// For task-level control, use `apigeeregistry_projects_locations_apis_versions_specs_list_revisions_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigeeregistry_projects_locations_apis_versions_specs_list_revisions(
    client: &SimpleHttpClient,
    args: &ApigeeregistryProjectsLocationsApisVersionsSpecsListRevisionsArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ListApiSpecRevisionsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = apigeeregistry_projects_locations_apis_versions_specs_list_revisions_builder(
        client,
        &args.name,
        &args.filter,
        &args.pageSize,
        &args.pageToken,
    )?;
    apigeeregistry_projects_locations_apis_versions_specs_list_revisions_execute(builder)
}

/// PATCH v1/projects/{projectsId}/locations/{locationsId}/apis/{apisId}/versions/{versionsId}/specs/{specsId}
/// Used to modify a specified spec.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `apigeeregistry_projects_locations_apis_versions_specs_patch_execute()` to send, or `apigeeregistry_projects_locations_apis_versions_specs_patch` for simplest API.

pub fn apigeeregistry_projects_locations_apis_versions_specs_patch_builder(
    client: &SimpleHttpClient,
    name: &String,
    allowMissing: &Option<Option<String>>,
    updateMask: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://apigeeregistry.googleapis.com/v1/projects/{}/locations/{locationsId}/apis/{apisId}/versions/{versionsId}/specs/{specsId}",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = allowMissing.as_ref() {
        query_parts.push(format!("allowMissing={}", val));
    }
    if let Some(val) = updateMask.as_ref() {
        query_parts.push(format!("updateMask={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .patch(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// PATCH v1/projects/{projectsId}/locations/{locationsId}/apis/{apisId}/versions/{versionsId}/specs/{specsId}
/// Used to modify a specified spec.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `apigeeregistry_projects_locations_apis_versions_specs_patch_execute()` or `apigeeregistry_projects_locations_apis_versions_specs_patch`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `apigeeregistry_projects_locations_apis_versions_specs_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigeeregistry_projects_locations_apis_versions_specs_patch_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ApiSpec>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ApiSpec = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// PATCH v1/projects/{projectsId}/locations/{locationsId}/apis/{apisId}/versions/{versionsId}/specs/{specsId}
/// Used to modify a specified spec.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `apigeeregistry_projects_locations_apis_versions_specs_patch_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `apigeeregistry_projects_locations_apis_versions_specs_patch_task()`.
/// For the simplest API, use `apigeeregistry_projects_locations_apis_versions_specs_patch()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `apigeeregistry_projects_locations_apis_versions_specs_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn apigeeregistry_projects_locations_apis_versions_specs_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ApiSpec>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = apigeeregistry_projects_locations_apis_versions_specs_patch_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`apigeeregistry_projects_locations_apis_versions_specs_patch`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ApigeeregistryProjectsLocationsApisVersionsSpecsPatchArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: allowMissing
    pub allowMissing: Option<Option<String>>,
    /// Query parameter: updateMask
    pub updateMask: Option<Option<String>>,
}

/// PATCH v1/projects/{projectsId}/locations/{locationsId}/apis/{apisId}/versions/{versionsId}/specs/{specsId}
/// Used to modify a specified spec.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `apigeeregistry_projects_locations_apis_versions_specs_patch_builder()` + `apigeeregistry_projects_locations_apis_versions_specs_patch_execute()`.
/// For task-level control, use `apigeeregistry_projects_locations_apis_versions_specs_patch_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigeeregistry_projects_locations_apis_versions_specs_patch(
    client: &SimpleHttpClient,
    args: &ApigeeregistryProjectsLocationsApisVersionsSpecsPatchArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ApiSpec>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = apigeeregistry_projects_locations_apis_versions_specs_patch_builder(
        client,
        &args.name,
        &args.allowMissing,
        &args.updateMask,
    )?;
    apigeeregistry_projects_locations_apis_versions_specs_patch_execute(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/apis/{apisId}/versions/{versionsId}/specs/{specsId}:rollback
/// Sets the current revision to a specified prior revision. Note that this creates a new revision with a new revision ID.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `apigeeregistry_projects_locations_apis_versions_specs_rollback_execute()` to send, or `apigeeregistry_projects_locations_apis_versions_specs_rollback` for simplest API.

pub fn apigeeregistry_projects_locations_apis_versions_specs_rollback_builder(
    client: &SimpleHttpClient,
    name: &String,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://apigeeregistry.googleapis.com/v1/projects/{}/locations/{locationsId}/apis/{apisId}/versions/{versionsId}/specs/{specsId}:rollback",
        name,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/apis/{apisId}/versions/{versionsId}/specs/{specsId}:rollback
/// Sets the current revision to a specified prior revision. Note that this creates a new revision with a new revision ID.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `apigeeregistry_projects_locations_apis_versions_specs_rollback_execute()` or `apigeeregistry_projects_locations_apis_versions_specs_rollback`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `apigeeregistry_projects_locations_apis_versions_specs_rollback_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigeeregistry_projects_locations_apis_versions_specs_rollback_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ApiSpec>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ApiSpec = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/apis/{apisId}/versions/{versionsId}/specs/{specsId}:rollback
/// Sets the current revision to a specified prior revision. Note that this creates a new revision with a new revision ID.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `apigeeregistry_projects_locations_apis_versions_specs_rollback_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `apigeeregistry_projects_locations_apis_versions_specs_rollback_task()`.
/// For the simplest API, use `apigeeregistry_projects_locations_apis_versions_specs_rollback()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `apigeeregistry_projects_locations_apis_versions_specs_rollback_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn apigeeregistry_projects_locations_apis_versions_specs_rollback_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ApiSpec>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = apigeeregistry_projects_locations_apis_versions_specs_rollback_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`apigeeregistry_projects_locations_apis_versions_specs_rollback`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ApigeeregistryProjectsLocationsApisVersionsSpecsRollbackArgs {
    /// Path parameter: name
    pub name: String,
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/apis/{apisId}/versions/{versionsId}/specs/{specsId}:rollback
/// Sets the current revision to a specified prior revision. Note that this creates a new revision with a new revision ID.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `apigeeregistry_projects_locations_apis_versions_specs_rollback_builder()` + `apigeeregistry_projects_locations_apis_versions_specs_rollback_execute()`.
/// For task-level control, use `apigeeregistry_projects_locations_apis_versions_specs_rollback_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigeeregistry_projects_locations_apis_versions_specs_rollback(
    client: &SimpleHttpClient,
    args: &ApigeeregistryProjectsLocationsApisVersionsSpecsRollbackArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ApiSpec>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        apigeeregistry_projects_locations_apis_versions_specs_rollback_builder(client, &args.name)?;
    apigeeregistry_projects_locations_apis_versions_specs_rollback_execute(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/apis/{apisId}/versions/{versionsId}/specs/{specsId}:setIamPolicy
/// Sets the access control policy on the specified resource. Replaces any existing policy. Can return NOT_FOUND, INVALID_ARGUMENT, and PERMISSION_DENIED errors.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `apigeeregistry_projects_locations_apis_versions_specs_set_iam_policy_execute()` to send, or `apigeeregistry_projects_locations_apis_versions_specs_set_iam_policy` for simplest API.

pub fn apigeeregistry_projects_locations_apis_versions_specs_set_iam_policy_builder(
    client: &SimpleHttpClient,
    resource: &String,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://apigeeregistry.googleapis.com/v1/projects/{}/locations/{locationsId}/apis/{apisId}/versions/{versionsId}/specs/{specsId}:setIamPolicy",
        resource,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/apis/{apisId}/versions/{versionsId}/specs/{specsId}:setIamPolicy
/// Sets the access control policy on the specified resource. Replaces any existing policy. Can return NOT_FOUND, INVALID_ARGUMENT, and PERMISSION_DENIED errors.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `apigeeregistry_projects_locations_apis_versions_specs_set_iam_policy_execute()` or `apigeeregistry_projects_locations_apis_versions_specs_set_iam_policy`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `apigeeregistry_projects_locations_apis_versions_specs_set_iam_policy_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigeeregistry_projects_locations_apis_versions_specs_set_iam_policy_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Policy>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Policy = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/apis/{apisId}/versions/{versionsId}/specs/{specsId}:setIamPolicy
/// Sets the access control policy on the specified resource. Replaces any existing policy. Can return NOT_FOUND, INVALID_ARGUMENT, and PERMISSION_DENIED errors.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `apigeeregistry_projects_locations_apis_versions_specs_set_iam_policy_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `apigeeregistry_projects_locations_apis_versions_specs_set_iam_policy_task()`.
/// For the simplest API, use `apigeeregistry_projects_locations_apis_versions_specs_set_iam_policy()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `apigeeregistry_projects_locations_apis_versions_specs_set_iam_policy_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn apigeeregistry_projects_locations_apis_versions_specs_set_iam_policy_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Policy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = apigeeregistry_projects_locations_apis_versions_specs_set_iam_policy_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`apigeeregistry_projects_locations_apis_versions_specs_set_iam_policy`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ApigeeregistryProjectsLocationsApisVersionsSpecsSetIamPolicyArgs {
    /// Path parameter: resource
    pub resource: String,
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/apis/{apisId}/versions/{versionsId}/specs/{specsId}:setIamPolicy
/// Sets the access control policy on the specified resource. Replaces any existing policy. Can return NOT_FOUND, INVALID_ARGUMENT, and PERMISSION_DENIED errors.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `apigeeregistry_projects_locations_apis_versions_specs_set_iam_policy_builder()` + `apigeeregistry_projects_locations_apis_versions_specs_set_iam_policy_execute()`.
/// For task-level control, use `apigeeregistry_projects_locations_apis_versions_specs_set_iam_policy_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigeeregistry_projects_locations_apis_versions_specs_set_iam_policy(
    client: &SimpleHttpClient,
    args: &ApigeeregistryProjectsLocationsApisVersionsSpecsSetIamPolicyArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Policy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = apigeeregistry_projects_locations_apis_versions_specs_set_iam_policy_builder(
        client,
        &args.resource,
    )?;
    apigeeregistry_projects_locations_apis_versions_specs_set_iam_policy_execute(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/apis/{apisId}/versions/{versionsId}/specs/{specsId}:tagRevision
/// Adds a tag to a specified revision of a spec.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `apigeeregistry_projects_locations_apis_versions_specs_tag_revision_execute()` to send, or `apigeeregistry_projects_locations_apis_versions_specs_tag_revision` for simplest API.

pub fn apigeeregistry_projects_locations_apis_versions_specs_tag_revision_builder(
    client: &SimpleHttpClient,
    name: &String,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://apigeeregistry.googleapis.com/v1/projects/{}/locations/{locationsId}/apis/{apisId}/versions/{versionsId}/specs/{specsId}:tagRevision",
        name,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/apis/{apisId}/versions/{versionsId}/specs/{specsId}:tagRevision
/// Adds a tag to a specified revision of a spec.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `apigeeregistry_projects_locations_apis_versions_specs_tag_revision_execute()` or `apigeeregistry_projects_locations_apis_versions_specs_tag_revision`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `apigeeregistry_projects_locations_apis_versions_specs_tag_revision_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigeeregistry_projects_locations_apis_versions_specs_tag_revision_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ApiSpec>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ApiSpec = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/apis/{apisId}/versions/{versionsId}/specs/{specsId}:tagRevision
/// Adds a tag to a specified revision of a spec.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `apigeeregistry_projects_locations_apis_versions_specs_tag_revision_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `apigeeregistry_projects_locations_apis_versions_specs_tag_revision_task()`.
/// For the simplest API, use `apigeeregistry_projects_locations_apis_versions_specs_tag_revision()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `apigeeregistry_projects_locations_apis_versions_specs_tag_revision_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn apigeeregistry_projects_locations_apis_versions_specs_tag_revision_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ApiSpec>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = apigeeregistry_projects_locations_apis_versions_specs_tag_revision_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`apigeeregistry_projects_locations_apis_versions_specs_tag_revision`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ApigeeregistryProjectsLocationsApisVersionsSpecsTagRevisionArgs {
    /// Path parameter: name
    pub name: String,
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/apis/{apisId}/versions/{versionsId}/specs/{specsId}:tagRevision
/// Adds a tag to a specified revision of a spec.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `apigeeregistry_projects_locations_apis_versions_specs_tag_revision_builder()` + `apigeeregistry_projects_locations_apis_versions_specs_tag_revision_execute()`.
/// For task-level control, use `apigeeregistry_projects_locations_apis_versions_specs_tag_revision_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigeeregistry_projects_locations_apis_versions_specs_tag_revision(
    client: &SimpleHttpClient,
    args: &ApigeeregistryProjectsLocationsApisVersionsSpecsTagRevisionArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ApiSpec>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = apigeeregistry_projects_locations_apis_versions_specs_tag_revision_builder(
        client, &args.name,
    )?;
    apigeeregistry_projects_locations_apis_versions_specs_tag_revision_execute(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/apis/{apisId}/versions/{versionsId}/specs/{specsId}:testIamPermissions
/// Returns permissions that a caller has on the specified resource. If the resource does not exist, this will return an empty set of permissions, not a NOT_FOUND error. Note: This operation is designed to be used for building permission-aware UIs and command-line tools, not for authorization checking. This operation may "fail open" without warning.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `apigeeregistry_projects_locations_apis_versions_specs_test_iam_permissions_execute()` to send, or `apigeeregistry_projects_locations_apis_versions_specs_test_iam_permissions` for simplest API.

pub fn apigeeregistry_projects_locations_apis_versions_specs_test_iam_permissions_builder(
    client: &SimpleHttpClient,
    resource: &String,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://apigeeregistry.googleapis.com/v1/projects/{}/locations/{locationsId}/apis/{apisId}/versions/{versionsId}/specs/{specsId}:testIamPermissions",
        resource,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/apis/{apisId}/versions/{versionsId}/specs/{specsId}:testIamPermissions
/// Returns permissions that a caller has on the specified resource. If the resource does not exist, this will return an empty set of permissions, not a NOT_FOUND error. Note: This operation is designed to be used for building permission-aware UIs and command-line tools, not for authorization checking. This operation may "fail open" without warning.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `apigeeregistry_projects_locations_apis_versions_specs_test_iam_permissions_execute()` or `apigeeregistry_projects_locations_apis_versions_specs_test_iam_permissions`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `apigeeregistry_projects_locations_apis_versions_specs_test_iam_permissions_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigeeregistry_projects_locations_apis_versions_specs_test_iam_permissions_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<TestIamPermissionsResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: TestIamPermissionsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/apis/{apisId}/versions/{versionsId}/specs/{specsId}:testIamPermissions
/// Returns permissions that a caller has on the specified resource. If the resource does not exist, this will return an empty set of permissions, not a NOT_FOUND error. Note: This operation is designed to be used for building permission-aware UIs and command-line tools, not for authorization checking. This operation may "fail open" without warning.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `apigeeregistry_projects_locations_apis_versions_specs_test_iam_permissions_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `apigeeregistry_projects_locations_apis_versions_specs_test_iam_permissions_task()`.
/// For the simplest API, use `apigeeregistry_projects_locations_apis_versions_specs_test_iam_permissions()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `apigeeregistry_projects_locations_apis_versions_specs_test_iam_permissions_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn apigeeregistry_projects_locations_apis_versions_specs_test_iam_permissions_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<TestIamPermissionsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task =
        apigeeregistry_projects_locations_apis_versions_specs_test_iam_permissions_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`apigeeregistry_projects_locations_apis_versions_specs_test_iam_permissions`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ApigeeregistryProjectsLocationsApisVersionsSpecsTestIamPermissionsArgs {
    /// Path parameter: resource
    pub resource: String,
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/apis/{apisId}/versions/{versionsId}/specs/{specsId}:testIamPermissions
/// Returns permissions that a caller has on the specified resource. If the resource does not exist, this will return an empty set of permissions, not a NOT_FOUND error. Note: This operation is designed to be used for building permission-aware UIs and command-line tools, not for authorization checking. This operation may "fail open" without warning.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `apigeeregistry_projects_locations_apis_versions_specs_test_iam_permissions_builder()` + `apigeeregistry_projects_locations_apis_versions_specs_test_iam_permissions_execute()`.
/// For task-level control, use `apigeeregistry_projects_locations_apis_versions_specs_test_iam_permissions_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigeeregistry_projects_locations_apis_versions_specs_test_iam_permissions(
    client: &SimpleHttpClient,
    args: &ApigeeregistryProjectsLocationsApisVersionsSpecsTestIamPermissionsArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<TestIamPermissionsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        apigeeregistry_projects_locations_apis_versions_specs_test_iam_permissions_builder(
            client,
            &args.resource,
        )?;
    apigeeregistry_projects_locations_apis_versions_specs_test_iam_permissions_execute(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/apis/{apisId}/versions/{versionsId}/specs/{specsId}/artifacts
/// Creates a specified artifact.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `apigeeregistry_projects_locations_apis_versions_specs_artifacts_create_execute()` to send, or `apigeeregistry_projects_locations_apis_versions_specs_artifacts_create` for simplest API.

pub fn apigeeregistry_projects_locations_apis_versions_specs_artifacts_create_builder(
    client: &SimpleHttpClient,
    parent: &String,
    artifactId: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://apigeeregistry.googleapis.com/v1/projects/{}/locations/{locationsId}/apis/{apisId}/versions/{versionsId}/specs/{specsId}/artifacts",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = artifactId.as_ref() {
        query_parts.push(format!("artifactId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .post(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/apis/{apisId}/versions/{versionsId}/specs/{specsId}/artifacts
/// Creates a specified artifact.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `apigeeregistry_projects_locations_apis_versions_specs_artifacts_create_execute()` or `apigeeregistry_projects_locations_apis_versions_specs_artifacts_create`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `apigeeregistry_projects_locations_apis_versions_specs_artifacts_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigeeregistry_projects_locations_apis_versions_specs_artifacts_create_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Artifact>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Artifact = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/apis/{apisId}/versions/{versionsId}/specs/{specsId}/artifacts
/// Creates a specified artifact.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `apigeeregistry_projects_locations_apis_versions_specs_artifacts_create_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `apigeeregistry_projects_locations_apis_versions_specs_artifacts_create_task()`.
/// For the simplest API, use `apigeeregistry_projects_locations_apis_versions_specs_artifacts_create()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `apigeeregistry_projects_locations_apis_versions_specs_artifacts_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn apigeeregistry_projects_locations_apis_versions_specs_artifacts_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Artifact>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task =
        apigeeregistry_projects_locations_apis_versions_specs_artifacts_create_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`apigeeregistry_projects_locations_apis_versions_specs_artifacts_create`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ApigeeregistryProjectsLocationsApisVersionsSpecsArtifactsCreateArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: artifactId
    pub artifactId: Option<Option<String>>,
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/apis/{apisId}/versions/{versionsId}/specs/{specsId}/artifacts
/// Creates a specified artifact.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `apigeeregistry_projects_locations_apis_versions_specs_artifacts_create_builder()` + `apigeeregistry_projects_locations_apis_versions_specs_artifacts_create_execute()`.
/// For task-level control, use `apigeeregistry_projects_locations_apis_versions_specs_artifacts_create_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigeeregistry_projects_locations_apis_versions_specs_artifacts_create(
    client: &SimpleHttpClient,
    args: &ApigeeregistryProjectsLocationsApisVersionsSpecsArtifactsCreateArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Artifact>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = apigeeregistry_projects_locations_apis_versions_specs_artifacts_create_builder(
        client,
        &args.parent,
        &args.artifactId,
    )?;
    apigeeregistry_projects_locations_apis_versions_specs_artifacts_create_execute(builder)
}

/// DELETE v1/projects/{projectsId}/locations/{locationsId}/apis/{apisId}/versions/{versionsId}/specs/{specsId}/artifacts/{artifactsId}
/// Removes a specified artifact.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `apigeeregistry_projects_locations_apis_versions_specs_artifacts_delete_execute()` to send, or `apigeeregistry_projects_locations_apis_versions_specs_artifacts_delete` for simplest API.

pub fn apigeeregistry_projects_locations_apis_versions_specs_artifacts_delete_builder(
    client: &SimpleHttpClient,
    name: &String,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://apigeeregistry.googleapis.com/v1/projects/{}/locations/{locationsId}/apis/{apisId}/versions/{versionsId}/specs/{specsId}/artifacts/{artifactsId}",
        name,
    );

    // Build request
    let builder = client
        .delete(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// DELETE v1/projects/{projectsId}/locations/{locationsId}/apis/{apisId}/versions/{versionsId}/specs/{specsId}/artifacts/{artifactsId}
/// Removes a specified artifact.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `apigeeregistry_projects_locations_apis_versions_specs_artifacts_delete_execute()` or `apigeeregistry_projects_locations_apis_versions_specs_artifacts_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `apigeeregistry_projects_locations_apis_versions_specs_artifacts_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigeeregistry_projects_locations_apis_versions_specs_artifacts_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Empty>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Empty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// DELETE v1/projects/{projectsId}/locations/{locationsId}/apis/{apisId}/versions/{versionsId}/specs/{specsId}/artifacts/{artifactsId}
/// Removes a specified artifact.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `apigeeregistry_projects_locations_apis_versions_specs_artifacts_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `apigeeregistry_projects_locations_apis_versions_specs_artifacts_delete_task()`.
/// For the simplest API, use `apigeeregistry_projects_locations_apis_versions_specs_artifacts_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `apigeeregistry_projects_locations_apis_versions_specs_artifacts_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn apigeeregistry_projects_locations_apis_versions_specs_artifacts_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task =
        apigeeregistry_projects_locations_apis_versions_specs_artifacts_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`apigeeregistry_projects_locations_apis_versions_specs_artifacts_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ApigeeregistryProjectsLocationsApisVersionsSpecsArtifactsDeleteArgs {
    /// Path parameter: name
    pub name: String,
}

/// DELETE v1/projects/{projectsId}/locations/{locationsId}/apis/{apisId}/versions/{versionsId}/specs/{specsId}/artifacts/{artifactsId}
/// Removes a specified artifact.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `apigeeregistry_projects_locations_apis_versions_specs_artifacts_delete_builder()` + `apigeeregistry_projects_locations_apis_versions_specs_artifacts_delete_execute()`.
/// For task-level control, use `apigeeregistry_projects_locations_apis_versions_specs_artifacts_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigeeregistry_projects_locations_apis_versions_specs_artifacts_delete(
    client: &SimpleHttpClient,
    args: &ApigeeregistryProjectsLocationsApisVersionsSpecsArtifactsDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = apigeeregistry_projects_locations_apis_versions_specs_artifacts_delete_builder(
        client, &args.name,
    )?;
    apigeeregistry_projects_locations_apis_versions_specs_artifacts_delete_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/apis/{apisId}/versions/{versionsId}/specs/{specsId}/artifacts/{artifactsId}
/// Returns a specified artifact.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `apigeeregistry_projects_locations_apis_versions_specs_artifacts_get_execute()` to send, or `apigeeregistry_projects_locations_apis_versions_specs_artifacts_get` for simplest API.

pub fn apigeeregistry_projects_locations_apis_versions_specs_artifacts_get_builder(
    client: &SimpleHttpClient,
    name: &String,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://apigeeregistry.googleapis.com/v1/projects/{}/locations/{locationsId}/apis/{apisId}/versions/{versionsId}/specs/{specsId}/artifacts/{artifactsId}",
        name,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/apis/{apisId}/versions/{versionsId}/specs/{specsId}/artifacts/{artifactsId}
/// Returns a specified artifact.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `apigeeregistry_projects_locations_apis_versions_specs_artifacts_get_execute()` or `apigeeregistry_projects_locations_apis_versions_specs_artifacts_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `apigeeregistry_projects_locations_apis_versions_specs_artifacts_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigeeregistry_projects_locations_apis_versions_specs_artifacts_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Artifact>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Artifact = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/apis/{apisId}/versions/{versionsId}/specs/{specsId}/artifacts/{artifactsId}
/// Returns a specified artifact.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `apigeeregistry_projects_locations_apis_versions_specs_artifacts_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `apigeeregistry_projects_locations_apis_versions_specs_artifacts_get_task()`.
/// For the simplest API, use `apigeeregistry_projects_locations_apis_versions_specs_artifacts_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `apigeeregistry_projects_locations_apis_versions_specs_artifacts_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn apigeeregistry_projects_locations_apis_versions_specs_artifacts_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Artifact>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = apigeeregistry_projects_locations_apis_versions_specs_artifacts_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`apigeeregistry_projects_locations_apis_versions_specs_artifacts_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ApigeeregistryProjectsLocationsApisVersionsSpecsArtifactsGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/apis/{apisId}/versions/{versionsId}/specs/{specsId}/artifacts/{artifactsId}
/// Returns a specified artifact.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `apigeeregistry_projects_locations_apis_versions_specs_artifacts_get_builder()` + `apigeeregistry_projects_locations_apis_versions_specs_artifacts_get_execute()`.
/// For task-level control, use `apigeeregistry_projects_locations_apis_versions_specs_artifacts_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigeeregistry_projects_locations_apis_versions_specs_artifacts_get(
    client: &SimpleHttpClient,
    args: &ApigeeregistryProjectsLocationsApisVersionsSpecsArtifactsGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Artifact>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = apigeeregistry_projects_locations_apis_versions_specs_artifacts_get_builder(
        client, &args.name,
    )?;
    apigeeregistry_projects_locations_apis_versions_specs_artifacts_get_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/apis/{apisId}/versions/{versionsId}/specs/{specsId}/artifacts/{artifactsId}:getContents
/// Returns the contents of a specified artifact. If artifacts are stored with GZip compression, the default behavior is to return the artifact uncompressed (the mime_type response field indicates the exact format returned).
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `apigeeregistry_projects_locations_apis_versions_specs_artifacts_get_contents_execute()` to send, or `apigeeregistry_projects_locations_apis_versions_specs_artifacts_get_contents` for simplest API.

pub fn apigeeregistry_projects_locations_apis_versions_specs_artifacts_get_contents_builder(
    client: &SimpleHttpClient,
    name: &String,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://apigeeregistry.googleapis.com/v1/projects/{}/locations/{locationsId}/apis/{apisId}/versions/{versionsId}/specs/{specsId}/artifacts/{artifactsId}:getContents",
        name,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/apis/{apisId}/versions/{versionsId}/specs/{specsId}/artifacts/{artifactsId}:getContents
/// Returns the contents of a specified artifact. If artifacts are stored with GZip compression, the default behavior is to return the artifact uncompressed (the mime_type response field indicates the exact format returned).
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `apigeeregistry_projects_locations_apis_versions_specs_artifacts_get_contents_execute()` or `apigeeregistry_projects_locations_apis_versions_specs_artifacts_get_contents`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `apigeeregistry_projects_locations_apis_versions_specs_artifacts_get_contents_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigeeregistry_projects_locations_apis_versions_specs_artifacts_get_contents_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<HttpBody>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: HttpBody = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/apis/{apisId}/versions/{versionsId}/specs/{specsId}/artifacts/{artifactsId}:getContents
/// Returns the contents of a specified artifact. If artifacts are stored with GZip compression, the default behavior is to return the artifact uncompressed (the mime_type response field indicates the exact format returned).
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `apigeeregistry_projects_locations_apis_versions_specs_artifacts_get_contents_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `apigeeregistry_projects_locations_apis_versions_specs_artifacts_get_contents_task()`.
/// For the simplest API, use `apigeeregistry_projects_locations_apis_versions_specs_artifacts_get_contents()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `apigeeregistry_projects_locations_apis_versions_specs_artifacts_get_contents_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn apigeeregistry_projects_locations_apis_versions_specs_artifacts_get_contents_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<HttpBody>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task =
        apigeeregistry_projects_locations_apis_versions_specs_artifacts_get_contents_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`apigeeregistry_projects_locations_apis_versions_specs_artifacts_get_contents`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ApigeeregistryProjectsLocationsApisVersionsSpecsArtifactsGetContentsArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/apis/{apisId}/versions/{versionsId}/specs/{specsId}/artifacts/{artifactsId}:getContents
/// Returns the contents of a specified artifact. If artifacts are stored with GZip compression, the default behavior is to return the artifact uncompressed (the mime_type response field indicates the exact format returned).
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `apigeeregistry_projects_locations_apis_versions_specs_artifacts_get_contents_builder()` + `apigeeregistry_projects_locations_apis_versions_specs_artifacts_get_contents_execute()`.
/// For task-level control, use `apigeeregistry_projects_locations_apis_versions_specs_artifacts_get_contents_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigeeregistry_projects_locations_apis_versions_specs_artifacts_get_contents(
    client: &SimpleHttpClient,
    args: &ApigeeregistryProjectsLocationsApisVersionsSpecsArtifactsGetContentsArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<HttpBody>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        apigeeregistry_projects_locations_apis_versions_specs_artifacts_get_contents_builder(
            client, &args.name,
        )?;
    apigeeregistry_projects_locations_apis_versions_specs_artifacts_get_contents_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/apis/{apisId}/versions/{versionsId}/specs/{specsId}/artifacts/{artifactsId}:getIamPolicy
/// Gets the access control policy for a resource. Returns an empty policy if the resource exists and does not have a policy set.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `apigeeregistry_projects_locations_apis_versions_specs_artifacts_get_iam_policy_execute()` to send, or `apigeeregistry_projects_locations_apis_versions_specs_artifacts_get_iam_policy` for simplest API.

pub fn apigeeregistry_projects_locations_apis_versions_specs_artifacts_get_iam_policy_builder(
    client: &SimpleHttpClient,
    resource: &String,
    options_requestedPolicyVersion: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://apigeeregistry.googleapis.com/v1/projects/{}/locations/{locationsId}/apis/{apisId}/versions/{versionsId}/specs/{specsId}/artifacts/{artifactsId}:getIamPolicy",
        resource,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = options_requestedPolicyVersion.as_ref() {
        query_parts.push(format!("options.requestedPolicyVersion={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/apis/{apisId}/versions/{versionsId}/specs/{specsId}/artifacts/{artifactsId}:getIamPolicy
/// Gets the access control policy for a resource. Returns an empty policy if the resource exists and does not have a policy set.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `apigeeregistry_projects_locations_apis_versions_specs_artifacts_get_iam_policy_execute()` or `apigeeregistry_projects_locations_apis_versions_specs_artifacts_get_iam_policy`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `apigeeregistry_projects_locations_apis_versions_specs_artifacts_get_iam_policy_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigeeregistry_projects_locations_apis_versions_specs_artifacts_get_iam_policy_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Policy>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Policy = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/apis/{apisId}/versions/{versionsId}/specs/{specsId}/artifacts/{artifactsId}:getIamPolicy
/// Gets the access control policy for a resource. Returns an empty policy if the resource exists and does not have a policy set.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `apigeeregistry_projects_locations_apis_versions_specs_artifacts_get_iam_policy_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `apigeeregistry_projects_locations_apis_versions_specs_artifacts_get_iam_policy_task()`.
/// For the simplest API, use `apigeeregistry_projects_locations_apis_versions_specs_artifacts_get_iam_policy()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `apigeeregistry_projects_locations_apis_versions_specs_artifacts_get_iam_policy_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn apigeeregistry_projects_locations_apis_versions_specs_artifacts_get_iam_policy_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Policy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = apigeeregistry_projects_locations_apis_versions_specs_artifacts_get_iam_policy_task(
        builder,
    )?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`apigeeregistry_projects_locations_apis_versions_specs_artifacts_get_iam_policy`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ApigeeregistryProjectsLocationsApisVersionsSpecsArtifactsGetIamPolicyArgs {
    /// Path parameter: resource
    pub resource: String,
    /// Query parameter: options_requestedPolicyVersion
    pub options_requestedPolicyVersion: Option<Option<String>>,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/apis/{apisId}/versions/{versionsId}/specs/{specsId}/artifacts/{artifactsId}:getIamPolicy
/// Gets the access control policy for a resource. Returns an empty policy if the resource exists and does not have a policy set.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `apigeeregistry_projects_locations_apis_versions_specs_artifacts_get_iam_policy_builder()` + `apigeeregistry_projects_locations_apis_versions_specs_artifacts_get_iam_policy_execute()`.
/// For task-level control, use `apigeeregistry_projects_locations_apis_versions_specs_artifacts_get_iam_policy_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigeeregistry_projects_locations_apis_versions_specs_artifacts_get_iam_policy(
    client: &SimpleHttpClient,
    args: &ApigeeregistryProjectsLocationsApisVersionsSpecsArtifactsGetIamPolicyArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Policy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        apigeeregistry_projects_locations_apis_versions_specs_artifacts_get_iam_policy_builder(
            client,
            &args.resource,
            &args.options_requestedPolicyVersion,
        )?;
    apigeeregistry_projects_locations_apis_versions_specs_artifacts_get_iam_policy_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/apis/{apisId}/versions/{versionsId}/specs/{specsId}/artifacts
/// Returns matching artifacts.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `apigeeregistry_projects_locations_apis_versions_specs_artifacts_list_execute()` to send, or `apigeeregistry_projects_locations_apis_versions_specs_artifacts_list` for simplest API.

pub fn apigeeregistry_projects_locations_apis_versions_specs_artifacts_list_builder(
    client: &SimpleHttpClient,
    parent: &String,
    filter: &Option<Option<String>>,
    orderBy: &Option<Option<String>>,
    pageSize: &Option<Option<String>>,
    pageToken: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://apigeeregistry.googleapis.com/v1/projects/{}/locations/{locationsId}/apis/{apisId}/versions/{versionsId}/specs/{specsId}/artifacts",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter.as_ref() {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = orderBy.as_ref() {
        query_parts.push(format!("orderBy={}", val));
    }
    if let Some(val) = pageSize.as_ref() {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/apis/{apisId}/versions/{versionsId}/specs/{specsId}/artifacts
/// Returns matching artifacts.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `apigeeregistry_projects_locations_apis_versions_specs_artifacts_list_execute()` or `apigeeregistry_projects_locations_apis_versions_specs_artifacts_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `apigeeregistry_projects_locations_apis_versions_specs_artifacts_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigeeregistry_projects_locations_apis_versions_specs_artifacts_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ListArtifactsResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListArtifactsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/apis/{apisId}/versions/{versionsId}/specs/{specsId}/artifacts
/// Returns matching artifacts.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `apigeeregistry_projects_locations_apis_versions_specs_artifacts_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `apigeeregistry_projects_locations_apis_versions_specs_artifacts_list_task()`.
/// For the simplest API, use `apigeeregistry_projects_locations_apis_versions_specs_artifacts_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `apigeeregistry_projects_locations_apis_versions_specs_artifacts_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn apigeeregistry_projects_locations_apis_versions_specs_artifacts_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListArtifactsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = apigeeregistry_projects_locations_apis_versions_specs_artifacts_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`apigeeregistry_projects_locations_apis_versions_specs_artifacts_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ApigeeregistryProjectsLocationsApisVersionsSpecsArtifactsListArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: filter
    pub filter: Option<Option<String>>,
    /// Query parameter: orderBy
    pub orderBy: Option<Option<String>>,
    /// Query parameter: pageSize
    pub pageSize: Option<Option<String>>,
    /// Query parameter: pageToken
    pub pageToken: Option<Option<String>>,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/apis/{apisId}/versions/{versionsId}/specs/{specsId}/artifacts
/// Returns matching artifacts.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `apigeeregistry_projects_locations_apis_versions_specs_artifacts_list_builder()` + `apigeeregistry_projects_locations_apis_versions_specs_artifacts_list_execute()`.
/// For task-level control, use `apigeeregistry_projects_locations_apis_versions_specs_artifacts_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigeeregistry_projects_locations_apis_versions_specs_artifacts_list(
    client: &SimpleHttpClient,
    args: &ApigeeregistryProjectsLocationsApisVersionsSpecsArtifactsListArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListArtifactsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = apigeeregistry_projects_locations_apis_versions_specs_artifacts_list_builder(
        client,
        &args.parent,
        &args.filter,
        &args.orderBy,
        &args.pageSize,
        &args.pageToken,
    )?;
    apigeeregistry_projects_locations_apis_versions_specs_artifacts_list_execute(builder)
}

/// PUT v1/projects/{projectsId}/locations/{locationsId}/apis/{apisId}/versions/{versionsId}/specs/{specsId}/artifacts/{artifactsId}
/// Used to replace a specified artifact.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `apigeeregistry_projects_locations_apis_versions_specs_artifacts_replace_artifact_execute()` to send, or `apigeeregistry_projects_locations_apis_versions_specs_artifacts_replace_artifact` for simplest API.

pub fn apigeeregistry_projects_locations_apis_versions_specs_artifacts_replace_artifact_builder(
    client: &SimpleHttpClient,
    name: &String,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://apigeeregistry.googleapis.com/v1/projects/{}/locations/{locationsId}/apis/{apisId}/versions/{versionsId}/specs/{specsId}/artifacts/{artifactsId}",
        name,
    );

    // Build request
    let builder = client
        .put(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// PUT v1/projects/{projectsId}/locations/{locationsId}/apis/{apisId}/versions/{versionsId}/specs/{specsId}/artifacts/{artifactsId}
/// Used to replace a specified artifact.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `apigeeregistry_projects_locations_apis_versions_specs_artifacts_replace_artifact_execute()` or `apigeeregistry_projects_locations_apis_versions_specs_artifacts_replace_artifact`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `apigeeregistry_projects_locations_apis_versions_specs_artifacts_replace_artifact_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigeeregistry_projects_locations_apis_versions_specs_artifacts_replace_artifact_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Artifact>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Artifact = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// PUT v1/projects/{projectsId}/locations/{locationsId}/apis/{apisId}/versions/{versionsId}/specs/{specsId}/artifacts/{artifactsId}
/// Used to replace a specified artifact.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `apigeeregistry_projects_locations_apis_versions_specs_artifacts_replace_artifact_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `apigeeregistry_projects_locations_apis_versions_specs_artifacts_replace_artifact_task()`.
/// For the simplest API, use `apigeeregistry_projects_locations_apis_versions_specs_artifacts_replace_artifact()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `apigeeregistry_projects_locations_apis_versions_specs_artifacts_replace_artifact_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn apigeeregistry_projects_locations_apis_versions_specs_artifacts_replace_artifact_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Artifact>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task =
        apigeeregistry_projects_locations_apis_versions_specs_artifacts_replace_artifact_task(
            builder,
        )?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`apigeeregistry_projects_locations_apis_versions_specs_artifacts_replace_artifact`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ApigeeregistryProjectsLocationsApisVersionsSpecsArtifactsReplaceArtifactArgs {
    /// Path parameter: name
    pub name: String,
}

/// PUT v1/projects/{projectsId}/locations/{locationsId}/apis/{apisId}/versions/{versionsId}/specs/{specsId}/artifacts/{artifactsId}
/// Used to replace a specified artifact.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `apigeeregistry_projects_locations_apis_versions_specs_artifacts_replace_artifact_builder()` + `apigeeregistry_projects_locations_apis_versions_specs_artifacts_replace_artifact_execute()`.
/// For task-level control, use `apigeeregistry_projects_locations_apis_versions_specs_artifacts_replace_artifact_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigeeregistry_projects_locations_apis_versions_specs_artifacts_replace_artifact(
    client: &SimpleHttpClient,
    args: &ApigeeregistryProjectsLocationsApisVersionsSpecsArtifactsReplaceArtifactArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Artifact>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        apigeeregistry_projects_locations_apis_versions_specs_artifacts_replace_artifact_builder(
            client, &args.name,
        )?;
    apigeeregistry_projects_locations_apis_versions_specs_artifacts_replace_artifact_execute(
        builder,
    )
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/apis/{apisId}/versions/{versionsId}/specs/{specsId}/artifacts/{artifactsId}:setIamPolicy
/// Sets the access control policy on the specified resource. Replaces any existing policy. Can return NOT_FOUND, INVALID_ARGUMENT, and PERMISSION_DENIED errors.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `apigeeregistry_projects_locations_apis_versions_specs_artifacts_set_iam_policy_execute()` to send, or `apigeeregistry_projects_locations_apis_versions_specs_artifacts_set_iam_policy` for simplest API.

pub fn apigeeregistry_projects_locations_apis_versions_specs_artifacts_set_iam_policy_builder(
    client: &SimpleHttpClient,
    resource: &String,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://apigeeregistry.googleapis.com/v1/projects/{}/locations/{locationsId}/apis/{apisId}/versions/{versionsId}/specs/{specsId}/artifacts/{artifactsId}:setIamPolicy",
        resource,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/apis/{apisId}/versions/{versionsId}/specs/{specsId}/artifacts/{artifactsId}:setIamPolicy
/// Sets the access control policy on the specified resource. Replaces any existing policy. Can return NOT_FOUND, INVALID_ARGUMENT, and PERMISSION_DENIED errors.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `apigeeregistry_projects_locations_apis_versions_specs_artifacts_set_iam_policy_execute()` or `apigeeregistry_projects_locations_apis_versions_specs_artifacts_set_iam_policy`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `apigeeregistry_projects_locations_apis_versions_specs_artifacts_set_iam_policy_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigeeregistry_projects_locations_apis_versions_specs_artifacts_set_iam_policy_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Policy>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Policy = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/apis/{apisId}/versions/{versionsId}/specs/{specsId}/artifacts/{artifactsId}:setIamPolicy
/// Sets the access control policy on the specified resource. Replaces any existing policy. Can return NOT_FOUND, INVALID_ARGUMENT, and PERMISSION_DENIED errors.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `apigeeregistry_projects_locations_apis_versions_specs_artifacts_set_iam_policy_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `apigeeregistry_projects_locations_apis_versions_specs_artifacts_set_iam_policy_task()`.
/// For the simplest API, use `apigeeregistry_projects_locations_apis_versions_specs_artifacts_set_iam_policy()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `apigeeregistry_projects_locations_apis_versions_specs_artifacts_set_iam_policy_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn apigeeregistry_projects_locations_apis_versions_specs_artifacts_set_iam_policy_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Policy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = apigeeregistry_projects_locations_apis_versions_specs_artifacts_set_iam_policy_task(
        builder,
    )?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`apigeeregistry_projects_locations_apis_versions_specs_artifacts_set_iam_policy`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ApigeeregistryProjectsLocationsApisVersionsSpecsArtifactsSetIamPolicyArgs {
    /// Path parameter: resource
    pub resource: String,
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/apis/{apisId}/versions/{versionsId}/specs/{specsId}/artifacts/{artifactsId}:setIamPolicy
/// Sets the access control policy on the specified resource. Replaces any existing policy. Can return NOT_FOUND, INVALID_ARGUMENT, and PERMISSION_DENIED errors.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `apigeeregistry_projects_locations_apis_versions_specs_artifacts_set_iam_policy_builder()` + `apigeeregistry_projects_locations_apis_versions_specs_artifacts_set_iam_policy_execute()`.
/// For task-level control, use `apigeeregistry_projects_locations_apis_versions_specs_artifacts_set_iam_policy_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigeeregistry_projects_locations_apis_versions_specs_artifacts_set_iam_policy(
    client: &SimpleHttpClient,
    args: &ApigeeregistryProjectsLocationsApisVersionsSpecsArtifactsSetIamPolicyArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Policy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        apigeeregistry_projects_locations_apis_versions_specs_artifacts_set_iam_policy_builder(
            client,
            &args.resource,
        )?;
    apigeeregistry_projects_locations_apis_versions_specs_artifacts_set_iam_policy_execute(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/apis/{apisId}/versions/{versionsId}/specs/{specsId}/artifacts/{artifactsId}:testIamPermissions
/// Returns permissions that a caller has on the specified resource. If the resource does not exist, this will return an empty set of permissions, not a NOT_FOUND error. Note: This operation is designed to be used for building permission-aware UIs and command-line tools, not for authorization checking. This operation may "fail open" without warning.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `apigeeregistry_projects_locations_apis_versions_specs_artifacts_test_iam_permissions_execute()` to send, or `apigeeregistry_projects_locations_apis_versions_specs_artifacts_test_iam_permissions` for simplest API.

pub fn apigeeregistry_projects_locations_apis_versions_specs_artifacts_test_iam_permissions_builder(
    client: &SimpleHttpClient,
    resource: &String,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://apigeeregistry.googleapis.com/v1/projects/{}/locations/{locationsId}/apis/{apisId}/versions/{versionsId}/specs/{specsId}/artifacts/{artifactsId}:testIamPermissions",
        resource,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/apis/{apisId}/versions/{versionsId}/specs/{specsId}/artifacts/{artifactsId}:testIamPermissions
/// Returns permissions that a caller has on the specified resource. If the resource does not exist, this will return an empty set of permissions, not a NOT_FOUND error. Note: This operation is designed to be used for building permission-aware UIs and command-line tools, not for authorization checking. This operation may "fail open" without warning.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `apigeeregistry_projects_locations_apis_versions_specs_artifacts_test_iam_permissions_execute()` or `apigeeregistry_projects_locations_apis_versions_specs_artifacts_test_iam_permissions`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `apigeeregistry_projects_locations_apis_versions_specs_artifacts_test_iam_permissions_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigeeregistry_projects_locations_apis_versions_specs_artifacts_test_iam_permissions_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<TestIamPermissionsResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: TestIamPermissionsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/apis/{apisId}/versions/{versionsId}/specs/{specsId}/artifacts/{artifactsId}:testIamPermissions
/// Returns permissions that a caller has on the specified resource. If the resource does not exist, this will return an empty set of permissions, not a NOT_FOUND error. Note: This operation is designed to be used for building permission-aware UIs and command-line tools, not for authorization checking. This operation may "fail open" without warning.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `apigeeregistry_projects_locations_apis_versions_specs_artifacts_test_iam_permissions_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `apigeeregistry_projects_locations_apis_versions_specs_artifacts_test_iam_permissions_task()`.
/// For the simplest API, use `apigeeregistry_projects_locations_apis_versions_specs_artifacts_test_iam_permissions()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `apigeeregistry_projects_locations_apis_versions_specs_artifacts_test_iam_permissions_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn apigeeregistry_projects_locations_apis_versions_specs_artifacts_test_iam_permissions_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<TestIamPermissionsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task =
        apigeeregistry_projects_locations_apis_versions_specs_artifacts_test_iam_permissions_task(
            builder,
        )?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`apigeeregistry_projects_locations_apis_versions_specs_artifacts_test_iam_permissions`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ApigeeregistryProjectsLocationsApisVersionsSpecsArtifactsTestIamPermissionsArgs {
    /// Path parameter: resource
    pub resource: String,
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/apis/{apisId}/versions/{versionsId}/specs/{specsId}/artifacts/{artifactsId}:testIamPermissions
/// Returns permissions that a caller has on the specified resource. If the resource does not exist, this will return an empty set of permissions, not a NOT_FOUND error. Note: This operation is designed to be used for building permission-aware UIs and command-line tools, not for authorization checking. This operation may "fail open" without warning.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `apigeeregistry_projects_locations_apis_versions_specs_artifacts_test_iam_permissions_builder()` + `apigeeregistry_projects_locations_apis_versions_specs_artifacts_test_iam_permissions_execute()`.
/// For task-level control, use `apigeeregistry_projects_locations_apis_versions_specs_artifacts_test_iam_permissions_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigeeregistry_projects_locations_apis_versions_specs_artifacts_test_iam_permissions(
    client: &SimpleHttpClient,
    args: &ApigeeregistryProjectsLocationsApisVersionsSpecsArtifactsTestIamPermissionsArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<TestIamPermissionsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = apigeeregistry_projects_locations_apis_versions_specs_artifacts_test_iam_permissions_builder(client, &args.resource)?;
    apigeeregistry_projects_locations_apis_versions_specs_artifacts_test_iam_permissions_execute(
        builder,
    )
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/artifacts
/// Creates a specified artifact.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `apigeeregistry_projects_locations_artifacts_create_execute()` to send, or `apigeeregistry_projects_locations_artifacts_create` for simplest API.

pub fn apigeeregistry_projects_locations_artifacts_create_builder(
    client: &SimpleHttpClient,
    parent: &String,
    artifactId: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://apigeeregistry.googleapis.com/v1/projects/{}/locations/{locationsId}/artifacts",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = artifactId.as_ref() {
        query_parts.push(format!("artifactId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .post(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/artifacts
/// Creates a specified artifact.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `apigeeregistry_projects_locations_artifacts_create_execute()` or `apigeeregistry_projects_locations_artifacts_create`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `apigeeregistry_projects_locations_artifacts_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigeeregistry_projects_locations_artifacts_create_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Artifact>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Artifact = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/artifacts
/// Creates a specified artifact.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `apigeeregistry_projects_locations_artifacts_create_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `apigeeregistry_projects_locations_artifacts_create_task()`.
/// For the simplest API, use `apigeeregistry_projects_locations_artifacts_create()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `apigeeregistry_projects_locations_artifacts_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn apigeeregistry_projects_locations_artifacts_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Artifact>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = apigeeregistry_projects_locations_artifacts_create_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`apigeeregistry_projects_locations_artifacts_create`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ApigeeregistryProjectsLocationsArtifactsCreateArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: artifactId
    pub artifactId: Option<Option<String>>,
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/artifacts
/// Creates a specified artifact.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `apigeeregistry_projects_locations_artifacts_create_builder()` + `apigeeregistry_projects_locations_artifacts_create_execute()`.
/// For task-level control, use `apigeeregistry_projects_locations_artifacts_create_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigeeregistry_projects_locations_artifacts_create(
    client: &SimpleHttpClient,
    args: &ApigeeregistryProjectsLocationsArtifactsCreateArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Artifact>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = apigeeregistry_projects_locations_artifacts_create_builder(
        client,
        &args.parent,
        &args.artifactId,
    )?;
    apigeeregistry_projects_locations_artifacts_create_execute(builder)
}

/// DELETE v1/projects/{projectsId}/locations/{locationsId}/artifacts/{artifactsId}
/// Removes a specified artifact.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `apigeeregistry_projects_locations_artifacts_delete_execute()` to send, or `apigeeregistry_projects_locations_artifacts_delete` for simplest API.

pub fn apigeeregistry_projects_locations_artifacts_delete_builder(
    client: &SimpleHttpClient,
    name: &String,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://apigeeregistry.googleapis.com/v1/projects/{}/locations/{locationsId}/artifacts/{artifactsId}",
        name,
    );

    // Build request
    let builder = client
        .delete(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// DELETE v1/projects/{projectsId}/locations/{locationsId}/artifacts/{artifactsId}
/// Removes a specified artifact.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `apigeeregistry_projects_locations_artifacts_delete_execute()` or `apigeeregistry_projects_locations_artifacts_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `apigeeregistry_projects_locations_artifacts_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigeeregistry_projects_locations_artifacts_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Empty>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Empty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// DELETE v1/projects/{projectsId}/locations/{locationsId}/artifacts/{artifactsId}
/// Removes a specified artifact.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `apigeeregistry_projects_locations_artifacts_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `apigeeregistry_projects_locations_artifacts_delete_task()`.
/// For the simplest API, use `apigeeregistry_projects_locations_artifacts_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `apigeeregistry_projects_locations_artifacts_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn apigeeregistry_projects_locations_artifacts_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = apigeeregistry_projects_locations_artifacts_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`apigeeregistry_projects_locations_artifacts_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ApigeeregistryProjectsLocationsArtifactsDeleteArgs {
    /// Path parameter: name
    pub name: String,
}

/// DELETE v1/projects/{projectsId}/locations/{locationsId}/artifacts/{artifactsId}
/// Removes a specified artifact.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `apigeeregistry_projects_locations_artifacts_delete_builder()` + `apigeeregistry_projects_locations_artifacts_delete_execute()`.
/// For task-level control, use `apigeeregistry_projects_locations_artifacts_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigeeregistry_projects_locations_artifacts_delete(
    client: &SimpleHttpClient,
    args: &ApigeeregistryProjectsLocationsArtifactsDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = apigeeregistry_projects_locations_artifacts_delete_builder(client, &args.name)?;
    apigeeregistry_projects_locations_artifacts_delete_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/artifacts/{artifactsId}
/// Returns a specified artifact.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `apigeeregistry_projects_locations_artifacts_get_execute()` to send, or `apigeeregistry_projects_locations_artifacts_get` for simplest API.

pub fn apigeeregistry_projects_locations_artifacts_get_builder(
    client: &SimpleHttpClient,
    name: &String,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://apigeeregistry.googleapis.com/v1/projects/{}/locations/{locationsId}/artifacts/{artifactsId}",
        name,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/artifacts/{artifactsId}
/// Returns a specified artifact.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `apigeeregistry_projects_locations_artifacts_get_execute()` or `apigeeregistry_projects_locations_artifacts_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `apigeeregistry_projects_locations_artifacts_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigeeregistry_projects_locations_artifacts_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Artifact>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Artifact = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/artifacts/{artifactsId}
/// Returns a specified artifact.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `apigeeregistry_projects_locations_artifacts_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `apigeeregistry_projects_locations_artifacts_get_task()`.
/// For the simplest API, use `apigeeregistry_projects_locations_artifacts_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `apigeeregistry_projects_locations_artifacts_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn apigeeregistry_projects_locations_artifacts_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Artifact>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = apigeeregistry_projects_locations_artifacts_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`apigeeregistry_projects_locations_artifacts_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ApigeeregistryProjectsLocationsArtifactsGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/artifacts/{artifactsId}
/// Returns a specified artifact.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `apigeeregistry_projects_locations_artifacts_get_builder()` + `apigeeregistry_projects_locations_artifacts_get_execute()`.
/// For task-level control, use `apigeeregistry_projects_locations_artifacts_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigeeregistry_projects_locations_artifacts_get(
    client: &SimpleHttpClient,
    args: &ApigeeregistryProjectsLocationsArtifactsGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Artifact>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = apigeeregistry_projects_locations_artifacts_get_builder(client, &args.name)?;
    apigeeregistry_projects_locations_artifacts_get_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/artifacts/{artifactsId}:getContents
/// Returns the contents of a specified artifact. If artifacts are stored with GZip compression, the default behavior is to return the artifact uncompressed (the mime_type response field indicates the exact format returned).
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `apigeeregistry_projects_locations_artifacts_get_contents_execute()` to send, or `apigeeregistry_projects_locations_artifacts_get_contents` for simplest API.

pub fn apigeeregistry_projects_locations_artifacts_get_contents_builder(
    client: &SimpleHttpClient,
    name: &String,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://apigeeregistry.googleapis.com/v1/projects/{}/locations/{locationsId}/artifacts/{artifactsId}:getContents",
        name,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/artifacts/{artifactsId}:getContents
/// Returns the contents of a specified artifact. If artifacts are stored with GZip compression, the default behavior is to return the artifact uncompressed (the mime_type response field indicates the exact format returned).
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `apigeeregistry_projects_locations_artifacts_get_contents_execute()` or `apigeeregistry_projects_locations_artifacts_get_contents`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `apigeeregistry_projects_locations_artifacts_get_contents_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigeeregistry_projects_locations_artifacts_get_contents_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<HttpBody>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: HttpBody = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/artifacts/{artifactsId}:getContents
/// Returns the contents of a specified artifact. If artifacts are stored with GZip compression, the default behavior is to return the artifact uncompressed (the mime_type response field indicates the exact format returned).
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `apigeeregistry_projects_locations_artifacts_get_contents_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `apigeeregistry_projects_locations_artifacts_get_contents_task()`.
/// For the simplest API, use `apigeeregistry_projects_locations_artifacts_get_contents()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `apigeeregistry_projects_locations_artifacts_get_contents_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn apigeeregistry_projects_locations_artifacts_get_contents_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<HttpBody>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = apigeeregistry_projects_locations_artifacts_get_contents_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`apigeeregistry_projects_locations_artifacts_get_contents`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ApigeeregistryProjectsLocationsArtifactsGetContentsArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/artifacts/{artifactsId}:getContents
/// Returns the contents of a specified artifact. If artifacts are stored with GZip compression, the default behavior is to return the artifact uncompressed (the mime_type response field indicates the exact format returned).
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `apigeeregistry_projects_locations_artifacts_get_contents_builder()` + `apigeeregistry_projects_locations_artifacts_get_contents_execute()`.
/// For task-level control, use `apigeeregistry_projects_locations_artifacts_get_contents_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigeeregistry_projects_locations_artifacts_get_contents(
    client: &SimpleHttpClient,
    args: &ApigeeregistryProjectsLocationsArtifactsGetContentsArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<HttpBody>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        apigeeregistry_projects_locations_artifacts_get_contents_builder(client, &args.name)?;
    apigeeregistry_projects_locations_artifacts_get_contents_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/artifacts/{artifactsId}:getIamPolicy
/// Gets the access control policy for a resource. Returns an empty policy if the resource exists and does not have a policy set.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `apigeeregistry_projects_locations_artifacts_get_iam_policy_execute()` to send, or `apigeeregistry_projects_locations_artifacts_get_iam_policy` for simplest API.

pub fn apigeeregistry_projects_locations_artifacts_get_iam_policy_builder(
    client: &SimpleHttpClient,
    resource: &String,
    options_requestedPolicyVersion: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://apigeeregistry.googleapis.com/v1/projects/{}/locations/{locationsId}/artifacts/{artifactsId}:getIamPolicy",
        resource,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = options_requestedPolicyVersion.as_ref() {
        query_parts.push(format!("options.requestedPolicyVersion={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/artifacts/{artifactsId}:getIamPolicy
/// Gets the access control policy for a resource. Returns an empty policy if the resource exists and does not have a policy set.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `apigeeregistry_projects_locations_artifacts_get_iam_policy_execute()` or `apigeeregistry_projects_locations_artifacts_get_iam_policy`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `apigeeregistry_projects_locations_artifacts_get_iam_policy_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigeeregistry_projects_locations_artifacts_get_iam_policy_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Policy>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Policy = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/artifacts/{artifactsId}:getIamPolicy
/// Gets the access control policy for a resource. Returns an empty policy if the resource exists and does not have a policy set.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `apigeeregistry_projects_locations_artifacts_get_iam_policy_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `apigeeregistry_projects_locations_artifacts_get_iam_policy_task()`.
/// For the simplest API, use `apigeeregistry_projects_locations_artifacts_get_iam_policy()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `apigeeregistry_projects_locations_artifacts_get_iam_policy_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn apigeeregistry_projects_locations_artifacts_get_iam_policy_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Policy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = apigeeregistry_projects_locations_artifacts_get_iam_policy_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`apigeeregistry_projects_locations_artifacts_get_iam_policy`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ApigeeregistryProjectsLocationsArtifactsGetIamPolicyArgs {
    /// Path parameter: resource
    pub resource: String,
    /// Query parameter: options_requestedPolicyVersion
    pub options_requestedPolicyVersion: Option<Option<String>>,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/artifacts/{artifactsId}:getIamPolicy
/// Gets the access control policy for a resource. Returns an empty policy if the resource exists and does not have a policy set.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `apigeeregistry_projects_locations_artifacts_get_iam_policy_builder()` + `apigeeregistry_projects_locations_artifacts_get_iam_policy_execute()`.
/// For task-level control, use `apigeeregistry_projects_locations_artifacts_get_iam_policy_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigeeregistry_projects_locations_artifacts_get_iam_policy(
    client: &SimpleHttpClient,
    args: &ApigeeregistryProjectsLocationsArtifactsGetIamPolicyArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Policy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = apigeeregistry_projects_locations_artifacts_get_iam_policy_builder(
        client,
        &args.resource,
        &args.options_requestedPolicyVersion,
    )?;
    apigeeregistry_projects_locations_artifacts_get_iam_policy_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/artifacts
/// Returns matching artifacts.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `apigeeregistry_projects_locations_artifacts_list_execute()` to send, or `apigeeregistry_projects_locations_artifacts_list` for simplest API.

pub fn apigeeregistry_projects_locations_artifacts_list_builder(
    client: &SimpleHttpClient,
    parent: &String,
    filter: &Option<Option<String>>,
    orderBy: &Option<Option<String>>,
    pageSize: &Option<Option<String>>,
    pageToken: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://apigeeregistry.googleapis.com/v1/projects/{}/locations/{locationsId}/artifacts",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter.as_ref() {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = orderBy.as_ref() {
        query_parts.push(format!("orderBy={}", val));
    }
    if let Some(val) = pageSize.as_ref() {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/artifacts
/// Returns matching artifacts.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `apigeeregistry_projects_locations_artifacts_list_execute()` or `apigeeregistry_projects_locations_artifacts_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `apigeeregistry_projects_locations_artifacts_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigeeregistry_projects_locations_artifacts_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ListArtifactsResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListArtifactsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/artifacts
/// Returns matching artifacts.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `apigeeregistry_projects_locations_artifacts_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `apigeeregistry_projects_locations_artifacts_list_task()`.
/// For the simplest API, use `apigeeregistry_projects_locations_artifacts_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `apigeeregistry_projects_locations_artifacts_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn apigeeregistry_projects_locations_artifacts_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListArtifactsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = apigeeregistry_projects_locations_artifacts_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`apigeeregistry_projects_locations_artifacts_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ApigeeregistryProjectsLocationsArtifactsListArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: filter
    pub filter: Option<Option<String>>,
    /// Query parameter: orderBy
    pub orderBy: Option<Option<String>>,
    /// Query parameter: pageSize
    pub pageSize: Option<Option<String>>,
    /// Query parameter: pageToken
    pub pageToken: Option<Option<String>>,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/artifacts
/// Returns matching artifacts.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `apigeeregistry_projects_locations_artifacts_list_builder()` + `apigeeregistry_projects_locations_artifacts_list_execute()`.
/// For task-level control, use `apigeeregistry_projects_locations_artifacts_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigeeregistry_projects_locations_artifacts_list(
    client: &SimpleHttpClient,
    args: &ApigeeregistryProjectsLocationsArtifactsListArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListArtifactsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = apigeeregistry_projects_locations_artifacts_list_builder(
        client,
        &args.parent,
        &args.filter,
        &args.orderBy,
        &args.pageSize,
        &args.pageToken,
    )?;
    apigeeregistry_projects_locations_artifacts_list_execute(builder)
}

/// PUT v1/projects/{projectsId}/locations/{locationsId}/artifacts/{artifactsId}
/// Used to replace a specified artifact.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `apigeeregistry_projects_locations_artifacts_replace_artifact_execute()` to send, or `apigeeregistry_projects_locations_artifacts_replace_artifact` for simplest API.

pub fn apigeeregistry_projects_locations_artifacts_replace_artifact_builder(
    client: &SimpleHttpClient,
    name: &String,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://apigeeregistry.googleapis.com/v1/projects/{}/locations/{locationsId}/artifacts/{artifactsId}",
        name,
    );

    // Build request
    let builder = client
        .put(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// PUT v1/projects/{projectsId}/locations/{locationsId}/artifacts/{artifactsId}
/// Used to replace a specified artifact.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `apigeeregistry_projects_locations_artifacts_replace_artifact_execute()` or `apigeeregistry_projects_locations_artifacts_replace_artifact`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `apigeeregistry_projects_locations_artifacts_replace_artifact_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigeeregistry_projects_locations_artifacts_replace_artifact_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Artifact>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Artifact = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// PUT v1/projects/{projectsId}/locations/{locationsId}/artifacts/{artifactsId}
/// Used to replace a specified artifact.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `apigeeregistry_projects_locations_artifacts_replace_artifact_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `apigeeregistry_projects_locations_artifacts_replace_artifact_task()`.
/// For the simplest API, use `apigeeregistry_projects_locations_artifacts_replace_artifact()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `apigeeregistry_projects_locations_artifacts_replace_artifact_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn apigeeregistry_projects_locations_artifacts_replace_artifact_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Artifact>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = apigeeregistry_projects_locations_artifacts_replace_artifact_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`apigeeregistry_projects_locations_artifacts_replace_artifact`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ApigeeregistryProjectsLocationsArtifactsReplaceArtifactArgs {
    /// Path parameter: name
    pub name: String,
}

/// PUT v1/projects/{projectsId}/locations/{locationsId}/artifacts/{artifactsId}
/// Used to replace a specified artifact.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `apigeeregistry_projects_locations_artifacts_replace_artifact_builder()` + `apigeeregistry_projects_locations_artifacts_replace_artifact_execute()`.
/// For task-level control, use `apigeeregistry_projects_locations_artifacts_replace_artifact_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigeeregistry_projects_locations_artifacts_replace_artifact(
    client: &SimpleHttpClient,
    args: &ApigeeregistryProjectsLocationsArtifactsReplaceArtifactArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Artifact>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        apigeeregistry_projects_locations_artifacts_replace_artifact_builder(client, &args.name)?;
    apigeeregistry_projects_locations_artifacts_replace_artifact_execute(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/artifacts/{artifactsId}:setIamPolicy
/// Sets the access control policy on the specified resource. Replaces any existing policy. Can return NOT_FOUND, INVALID_ARGUMENT, and PERMISSION_DENIED errors.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `apigeeregistry_projects_locations_artifacts_set_iam_policy_execute()` to send, or `apigeeregistry_projects_locations_artifacts_set_iam_policy` for simplest API.

pub fn apigeeregistry_projects_locations_artifacts_set_iam_policy_builder(
    client: &SimpleHttpClient,
    resource: &String,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://apigeeregistry.googleapis.com/v1/projects/{}/locations/{locationsId}/artifacts/{artifactsId}:setIamPolicy",
        resource,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/artifacts/{artifactsId}:setIamPolicy
/// Sets the access control policy on the specified resource. Replaces any existing policy. Can return NOT_FOUND, INVALID_ARGUMENT, and PERMISSION_DENIED errors.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `apigeeregistry_projects_locations_artifacts_set_iam_policy_execute()` or `apigeeregistry_projects_locations_artifacts_set_iam_policy`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `apigeeregistry_projects_locations_artifacts_set_iam_policy_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigeeregistry_projects_locations_artifacts_set_iam_policy_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Policy>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Policy = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/artifacts/{artifactsId}:setIamPolicy
/// Sets the access control policy on the specified resource. Replaces any existing policy. Can return NOT_FOUND, INVALID_ARGUMENT, and PERMISSION_DENIED errors.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `apigeeregistry_projects_locations_artifacts_set_iam_policy_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `apigeeregistry_projects_locations_artifacts_set_iam_policy_task()`.
/// For the simplest API, use `apigeeregistry_projects_locations_artifacts_set_iam_policy()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `apigeeregistry_projects_locations_artifacts_set_iam_policy_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn apigeeregistry_projects_locations_artifacts_set_iam_policy_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Policy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = apigeeregistry_projects_locations_artifacts_set_iam_policy_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`apigeeregistry_projects_locations_artifacts_set_iam_policy`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ApigeeregistryProjectsLocationsArtifactsSetIamPolicyArgs {
    /// Path parameter: resource
    pub resource: String,
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/artifacts/{artifactsId}:setIamPolicy
/// Sets the access control policy on the specified resource. Replaces any existing policy. Can return NOT_FOUND, INVALID_ARGUMENT, and PERMISSION_DENIED errors.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `apigeeregistry_projects_locations_artifacts_set_iam_policy_builder()` + `apigeeregistry_projects_locations_artifacts_set_iam_policy_execute()`.
/// For task-level control, use `apigeeregistry_projects_locations_artifacts_set_iam_policy_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigeeregistry_projects_locations_artifacts_set_iam_policy(
    client: &SimpleHttpClient,
    args: &ApigeeregistryProjectsLocationsArtifactsSetIamPolicyArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Policy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        apigeeregistry_projects_locations_artifacts_set_iam_policy_builder(client, &args.resource)?;
    apigeeregistry_projects_locations_artifacts_set_iam_policy_execute(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/artifacts/{artifactsId}:testIamPermissions
/// Returns permissions that a caller has on the specified resource. If the resource does not exist, this will return an empty set of permissions, not a NOT_FOUND error. Note: This operation is designed to be used for building permission-aware UIs and command-line tools, not for authorization checking. This operation may "fail open" without warning.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `apigeeregistry_projects_locations_artifacts_test_iam_permissions_execute()` to send, or `apigeeregistry_projects_locations_artifacts_test_iam_permissions` for simplest API.

pub fn apigeeregistry_projects_locations_artifacts_test_iam_permissions_builder(
    client: &SimpleHttpClient,
    resource: &String,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://apigeeregistry.googleapis.com/v1/projects/{}/locations/{locationsId}/artifacts/{artifactsId}:testIamPermissions",
        resource,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/artifacts/{artifactsId}:testIamPermissions
/// Returns permissions that a caller has on the specified resource. If the resource does not exist, this will return an empty set of permissions, not a NOT_FOUND error. Note: This operation is designed to be used for building permission-aware UIs and command-line tools, not for authorization checking. This operation may "fail open" without warning.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `apigeeregistry_projects_locations_artifacts_test_iam_permissions_execute()` or `apigeeregistry_projects_locations_artifacts_test_iam_permissions`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `apigeeregistry_projects_locations_artifacts_test_iam_permissions_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigeeregistry_projects_locations_artifacts_test_iam_permissions_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<TestIamPermissionsResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: TestIamPermissionsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/artifacts/{artifactsId}:testIamPermissions
/// Returns permissions that a caller has on the specified resource. If the resource does not exist, this will return an empty set of permissions, not a NOT_FOUND error. Note: This operation is designed to be used for building permission-aware UIs and command-line tools, not for authorization checking. This operation may "fail open" without warning.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `apigeeregistry_projects_locations_artifacts_test_iam_permissions_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `apigeeregistry_projects_locations_artifacts_test_iam_permissions_task()`.
/// For the simplest API, use `apigeeregistry_projects_locations_artifacts_test_iam_permissions()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `apigeeregistry_projects_locations_artifacts_test_iam_permissions_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn apigeeregistry_projects_locations_artifacts_test_iam_permissions_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<TestIamPermissionsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = apigeeregistry_projects_locations_artifacts_test_iam_permissions_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`apigeeregistry_projects_locations_artifacts_test_iam_permissions`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ApigeeregistryProjectsLocationsArtifactsTestIamPermissionsArgs {
    /// Path parameter: resource
    pub resource: String,
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/artifacts/{artifactsId}:testIamPermissions
/// Returns permissions that a caller has on the specified resource. If the resource does not exist, this will return an empty set of permissions, not a NOT_FOUND error. Note: This operation is designed to be used for building permission-aware UIs and command-line tools, not for authorization checking. This operation may "fail open" without warning.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `apigeeregistry_projects_locations_artifacts_test_iam_permissions_builder()` + `apigeeregistry_projects_locations_artifacts_test_iam_permissions_execute()`.
/// For task-level control, use `apigeeregistry_projects_locations_artifacts_test_iam_permissions_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigeeregistry_projects_locations_artifacts_test_iam_permissions(
    client: &SimpleHttpClient,
    args: &ApigeeregistryProjectsLocationsArtifactsTestIamPermissionsArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<TestIamPermissionsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = apigeeregistry_projects_locations_artifacts_test_iam_permissions_builder(
        client,
        &args.resource,
    )?;
    apigeeregistry_projects_locations_artifacts_test_iam_permissions_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/documents:getIamPolicy
/// Gets the access control policy for a resource. Returns an empty policy if the resource exists and does not have a policy set.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `apigeeregistry_projects_locations_documents_get_iam_policy_execute()` to send, or `apigeeregistry_projects_locations_documents_get_iam_policy` for simplest API.

pub fn apigeeregistry_projects_locations_documents_get_iam_policy_builder(
    client: &SimpleHttpClient,
    resource: &String,
    options_requestedPolicyVersion: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://apigeeregistry.googleapis.com/v1/projects/{}/locations/{locationsId}/documents:getIamPolicy",
        resource,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = options_requestedPolicyVersion.as_ref() {
        query_parts.push(format!("options.requestedPolicyVersion={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/documents:getIamPolicy
/// Gets the access control policy for a resource. Returns an empty policy if the resource exists and does not have a policy set.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `apigeeregistry_projects_locations_documents_get_iam_policy_execute()` or `apigeeregistry_projects_locations_documents_get_iam_policy`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `apigeeregistry_projects_locations_documents_get_iam_policy_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigeeregistry_projects_locations_documents_get_iam_policy_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Policy>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Policy = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/documents:getIamPolicy
/// Gets the access control policy for a resource. Returns an empty policy if the resource exists and does not have a policy set.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `apigeeregistry_projects_locations_documents_get_iam_policy_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `apigeeregistry_projects_locations_documents_get_iam_policy_task()`.
/// For the simplest API, use `apigeeregistry_projects_locations_documents_get_iam_policy()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `apigeeregistry_projects_locations_documents_get_iam_policy_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn apigeeregistry_projects_locations_documents_get_iam_policy_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Policy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = apigeeregistry_projects_locations_documents_get_iam_policy_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`apigeeregistry_projects_locations_documents_get_iam_policy`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ApigeeregistryProjectsLocationsDocumentsGetIamPolicyArgs {
    /// Path parameter: resource
    pub resource: String,
    /// Query parameter: options_requestedPolicyVersion
    pub options_requestedPolicyVersion: Option<Option<String>>,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/documents:getIamPolicy
/// Gets the access control policy for a resource. Returns an empty policy if the resource exists and does not have a policy set.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `apigeeregistry_projects_locations_documents_get_iam_policy_builder()` + `apigeeregistry_projects_locations_documents_get_iam_policy_execute()`.
/// For task-level control, use `apigeeregistry_projects_locations_documents_get_iam_policy_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigeeregistry_projects_locations_documents_get_iam_policy(
    client: &SimpleHttpClient,
    args: &ApigeeregistryProjectsLocationsDocumentsGetIamPolicyArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Policy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = apigeeregistry_projects_locations_documents_get_iam_policy_builder(
        client,
        &args.resource,
        &args.options_requestedPolicyVersion,
    )?;
    apigeeregistry_projects_locations_documents_get_iam_policy_execute(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/documents:setIamPolicy
/// Sets the access control policy on the specified resource. Replaces any existing policy. Can return NOT_FOUND, INVALID_ARGUMENT, and PERMISSION_DENIED errors.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `apigeeregistry_projects_locations_documents_set_iam_policy_execute()` to send, or `apigeeregistry_projects_locations_documents_set_iam_policy` for simplest API.

pub fn apigeeregistry_projects_locations_documents_set_iam_policy_builder(
    client: &SimpleHttpClient,
    resource: &String,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://apigeeregistry.googleapis.com/v1/projects/{}/locations/{locationsId}/documents:setIamPolicy",
        resource,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/documents:setIamPolicy
/// Sets the access control policy on the specified resource. Replaces any existing policy. Can return NOT_FOUND, INVALID_ARGUMENT, and PERMISSION_DENIED errors.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `apigeeregistry_projects_locations_documents_set_iam_policy_execute()` or `apigeeregistry_projects_locations_documents_set_iam_policy`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `apigeeregistry_projects_locations_documents_set_iam_policy_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigeeregistry_projects_locations_documents_set_iam_policy_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Policy>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Policy = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/documents:setIamPolicy
/// Sets the access control policy on the specified resource. Replaces any existing policy. Can return NOT_FOUND, INVALID_ARGUMENT, and PERMISSION_DENIED errors.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `apigeeregistry_projects_locations_documents_set_iam_policy_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `apigeeregistry_projects_locations_documents_set_iam_policy_task()`.
/// For the simplest API, use `apigeeregistry_projects_locations_documents_set_iam_policy()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `apigeeregistry_projects_locations_documents_set_iam_policy_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn apigeeregistry_projects_locations_documents_set_iam_policy_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Policy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = apigeeregistry_projects_locations_documents_set_iam_policy_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`apigeeregistry_projects_locations_documents_set_iam_policy`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ApigeeregistryProjectsLocationsDocumentsSetIamPolicyArgs {
    /// Path parameter: resource
    pub resource: String,
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/documents:setIamPolicy
/// Sets the access control policy on the specified resource. Replaces any existing policy. Can return NOT_FOUND, INVALID_ARGUMENT, and PERMISSION_DENIED errors.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `apigeeregistry_projects_locations_documents_set_iam_policy_builder()` + `apigeeregistry_projects_locations_documents_set_iam_policy_execute()`.
/// For task-level control, use `apigeeregistry_projects_locations_documents_set_iam_policy_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigeeregistry_projects_locations_documents_set_iam_policy(
    client: &SimpleHttpClient,
    args: &ApigeeregistryProjectsLocationsDocumentsSetIamPolicyArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Policy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        apigeeregistry_projects_locations_documents_set_iam_policy_builder(client, &args.resource)?;
    apigeeregistry_projects_locations_documents_set_iam_policy_execute(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/documents:testIamPermissions
/// Returns permissions that a caller has on the specified resource. If the resource does not exist, this will return an empty set of permissions, not a NOT_FOUND error. Note: This operation is designed to be used for building permission-aware UIs and command-line tools, not for authorization checking. This operation may "fail open" without warning.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `apigeeregistry_projects_locations_documents_test_iam_permissions_execute()` to send, or `apigeeregistry_projects_locations_documents_test_iam_permissions` for simplest API.

pub fn apigeeregistry_projects_locations_documents_test_iam_permissions_builder(
    client: &SimpleHttpClient,
    resource: &String,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://apigeeregistry.googleapis.com/v1/projects/{}/locations/{locationsId}/documents:testIamPermissions",
        resource,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/documents:testIamPermissions
/// Returns permissions that a caller has on the specified resource. If the resource does not exist, this will return an empty set of permissions, not a NOT_FOUND error. Note: This operation is designed to be used for building permission-aware UIs and command-line tools, not for authorization checking. This operation may "fail open" without warning.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `apigeeregistry_projects_locations_documents_test_iam_permissions_execute()` or `apigeeregistry_projects_locations_documents_test_iam_permissions`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `apigeeregistry_projects_locations_documents_test_iam_permissions_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigeeregistry_projects_locations_documents_test_iam_permissions_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<TestIamPermissionsResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: TestIamPermissionsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/documents:testIamPermissions
/// Returns permissions that a caller has on the specified resource. If the resource does not exist, this will return an empty set of permissions, not a NOT_FOUND error. Note: This operation is designed to be used for building permission-aware UIs and command-line tools, not for authorization checking. This operation may "fail open" without warning.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `apigeeregistry_projects_locations_documents_test_iam_permissions_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `apigeeregistry_projects_locations_documents_test_iam_permissions_task()`.
/// For the simplest API, use `apigeeregistry_projects_locations_documents_test_iam_permissions()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `apigeeregistry_projects_locations_documents_test_iam_permissions_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn apigeeregistry_projects_locations_documents_test_iam_permissions_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<TestIamPermissionsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = apigeeregistry_projects_locations_documents_test_iam_permissions_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`apigeeregistry_projects_locations_documents_test_iam_permissions`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ApigeeregistryProjectsLocationsDocumentsTestIamPermissionsArgs {
    /// Path parameter: resource
    pub resource: String,
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/documents:testIamPermissions
/// Returns permissions that a caller has on the specified resource. If the resource does not exist, this will return an empty set of permissions, not a NOT_FOUND error. Note: This operation is designed to be used for building permission-aware UIs and command-line tools, not for authorization checking. This operation may "fail open" without warning.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `apigeeregistry_projects_locations_documents_test_iam_permissions_builder()` + `apigeeregistry_projects_locations_documents_test_iam_permissions_execute()`.
/// For task-level control, use `apigeeregistry_projects_locations_documents_test_iam_permissions_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigeeregistry_projects_locations_documents_test_iam_permissions(
    client: &SimpleHttpClient,
    args: &ApigeeregistryProjectsLocationsDocumentsTestIamPermissionsArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<TestIamPermissionsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = apigeeregistry_projects_locations_documents_test_iam_permissions_builder(
        client,
        &args.resource,
    )?;
    apigeeregistry_projects_locations_documents_test_iam_permissions_execute(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/instances
/// Provisions instance resources for the Registry.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `apigeeregistry_projects_locations_instances_create_execute()` to send, or `apigeeregistry_projects_locations_instances_create` for simplest API.

pub fn apigeeregistry_projects_locations_instances_create_builder(
    client: &SimpleHttpClient,
    parent: &String,
    instanceId: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://apigeeregistry.googleapis.com/v1/projects/{}/locations/{locationsId}/instances",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = instanceId.as_ref() {
        query_parts.push(format!("instanceId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .post(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/instances
/// Provisions instance resources for the Registry.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `apigeeregistry_projects_locations_instances_create_execute()` or `apigeeregistry_projects_locations_instances_create`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `apigeeregistry_projects_locations_instances_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigeeregistry_projects_locations_instances_create_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/instances
/// Provisions instance resources for the Registry.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `apigeeregistry_projects_locations_instances_create_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `apigeeregistry_projects_locations_instances_create_task()`.
/// For the simplest API, use `apigeeregistry_projects_locations_instances_create()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `apigeeregistry_projects_locations_instances_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn apigeeregistry_projects_locations_instances_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = apigeeregistry_projects_locations_instances_create_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`apigeeregistry_projects_locations_instances_create`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ApigeeregistryProjectsLocationsInstancesCreateArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: instanceId
    pub instanceId: Option<Option<String>>,
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/instances
/// Provisions instance resources for the Registry.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `apigeeregistry_projects_locations_instances_create_builder()` + `apigeeregistry_projects_locations_instances_create_execute()`.
/// For task-level control, use `apigeeregistry_projects_locations_instances_create_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigeeregistry_projects_locations_instances_create(
    client: &SimpleHttpClient,
    args: &ApigeeregistryProjectsLocationsInstancesCreateArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = apigeeregistry_projects_locations_instances_create_builder(
        client,
        &args.parent,
        &args.instanceId,
    )?;
    apigeeregistry_projects_locations_instances_create_execute(builder)
}

/// DELETE v1/projects/{projectsId}/locations/{locationsId}/instances/{instancesId}
/// Deletes the Registry instance.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `apigeeregistry_projects_locations_instances_delete_execute()` to send, or `apigeeregistry_projects_locations_instances_delete` for simplest API.

pub fn apigeeregistry_projects_locations_instances_delete_builder(
    client: &SimpleHttpClient,
    name: &String,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://apigeeregistry.googleapis.com/v1/projects/{}/locations/{locationsId}/instances/{instancesId}",
        name,
    );

    // Build request
    let builder = client
        .delete(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// DELETE v1/projects/{projectsId}/locations/{locationsId}/instances/{instancesId}
/// Deletes the Registry instance.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `apigeeregistry_projects_locations_instances_delete_execute()` or `apigeeregistry_projects_locations_instances_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `apigeeregistry_projects_locations_instances_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigeeregistry_projects_locations_instances_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// DELETE v1/projects/{projectsId}/locations/{locationsId}/instances/{instancesId}
/// Deletes the Registry instance.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `apigeeregistry_projects_locations_instances_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `apigeeregistry_projects_locations_instances_delete_task()`.
/// For the simplest API, use `apigeeregistry_projects_locations_instances_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `apigeeregistry_projects_locations_instances_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn apigeeregistry_projects_locations_instances_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = apigeeregistry_projects_locations_instances_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`apigeeregistry_projects_locations_instances_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ApigeeregistryProjectsLocationsInstancesDeleteArgs {
    /// Path parameter: name
    pub name: String,
}

/// DELETE v1/projects/{projectsId}/locations/{locationsId}/instances/{instancesId}
/// Deletes the Registry instance.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `apigeeregistry_projects_locations_instances_delete_builder()` + `apigeeregistry_projects_locations_instances_delete_execute()`.
/// For task-level control, use `apigeeregistry_projects_locations_instances_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigeeregistry_projects_locations_instances_delete(
    client: &SimpleHttpClient,
    args: &ApigeeregistryProjectsLocationsInstancesDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = apigeeregistry_projects_locations_instances_delete_builder(client, &args.name)?;
    apigeeregistry_projects_locations_instances_delete_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/instances/{instancesId}
/// Gets details of a single Instance.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `apigeeregistry_projects_locations_instances_get_execute()` to send, or `apigeeregistry_projects_locations_instances_get` for simplest API.

pub fn apigeeregistry_projects_locations_instances_get_builder(
    client: &SimpleHttpClient,
    name: &String,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://apigeeregistry.googleapis.com/v1/projects/{}/locations/{locationsId}/instances/{instancesId}",
        name,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/instances/{instancesId}
/// Gets details of a single Instance.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `apigeeregistry_projects_locations_instances_get_execute()` or `apigeeregistry_projects_locations_instances_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `apigeeregistry_projects_locations_instances_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigeeregistry_projects_locations_instances_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Instance>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Instance = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/instances/{instancesId}
/// Gets details of a single Instance.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `apigeeregistry_projects_locations_instances_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `apigeeregistry_projects_locations_instances_get_task()`.
/// For the simplest API, use `apigeeregistry_projects_locations_instances_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `apigeeregistry_projects_locations_instances_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn apigeeregistry_projects_locations_instances_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Instance>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = apigeeregistry_projects_locations_instances_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`apigeeregistry_projects_locations_instances_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ApigeeregistryProjectsLocationsInstancesGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/instances/{instancesId}
/// Gets details of a single Instance.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `apigeeregistry_projects_locations_instances_get_builder()` + `apigeeregistry_projects_locations_instances_get_execute()`.
/// For task-level control, use `apigeeregistry_projects_locations_instances_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigeeregistry_projects_locations_instances_get(
    client: &SimpleHttpClient,
    args: &ApigeeregistryProjectsLocationsInstancesGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Instance>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = apigeeregistry_projects_locations_instances_get_builder(client, &args.name)?;
    apigeeregistry_projects_locations_instances_get_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/instances/{instancesId}:getIamPolicy
/// Gets the access control policy for a resource. Returns an empty policy if the resource exists and does not have a policy set.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `apigeeregistry_projects_locations_instances_get_iam_policy_execute()` to send, or `apigeeregistry_projects_locations_instances_get_iam_policy` for simplest API.

pub fn apigeeregistry_projects_locations_instances_get_iam_policy_builder(
    client: &SimpleHttpClient,
    resource: &String,
    options_requestedPolicyVersion: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://apigeeregistry.googleapis.com/v1/projects/{}/locations/{locationsId}/instances/{instancesId}:getIamPolicy",
        resource,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = options_requestedPolicyVersion.as_ref() {
        query_parts.push(format!("options.requestedPolicyVersion={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/instances/{instancesId}:getIamPolicy
/// Gets the access control policy for a resource. Returns an empty policy if the resource exists and does not have a policy set.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `apigeeregistry_projects_locations_instances_get_iam_policy_execute()` or `apigeeregistry_projects_locations_instances_get_iam_policy`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `apigeeregistry_projects_locations_instances_get_iam_policy_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigeeregistry_projects_locations_instances_get_iam_policy_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Policy>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Policy = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/instances/{instancesId}:getIamPolicy
/// Gets the access control policy for a resource. Returns an empty policy if the resource exists and does not have a policy set.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `apigeeregistry_projects_locations_instances_get_iam_policy_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `apigeeregistry_projects_locations_instances_get_iam_policy_task()`.
/// For the simplest API, use `apigeeregistry_projects_locations_instances_get_iam_policy()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `apigeeregistry_projects_locations_instances_get_iam_policy_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn apigeeregistry_projects_locations_instances_get_iam_policy_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Policy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = apigeeregistry_projects_locations_instances_get_iam_policy_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`apigeeregistry_projects_locations_instances_get_iam_policy`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ApigeeregistryProjectsLocationsInstancesGetIamPolicyArgs {
    /// Path parameter: resource
    pub resource: String,
    /// Query parameter: options_requestedPolicyVersion
    pub options_requestedPolicyVersion: Option<Option<String>>,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/instances/{instancesId}:getIamPolicy
/// Gets the access control policy for a resource. Returns an empty policy if the resource exists and does not have a policy set.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `apigeeregistry_projects_locations_instances_get_iam_policy_builder()` + `apigeeregistry_projects_locations_instances_get_iam_policy_execute()`.
/// For task-level control, use `apigeeregistry_projects_locations_instances_get_iam_policy_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigeeregistry_projects_locations_instances_get_iam_policy(
    client: &SimpleHttpClient,
    args: &ApigeeregistryProjectsLocationsInstancesGetIamPolicyArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Policy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = apigeeregistry_projects_locations_instances_get_iam_policy_builder(
        client,
        &args.resource,
        &args.options_requestedPolicyVersion,
    )?;
    apigeeregistry_projects_locations_instances_get_iam_policy_execute(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/instances/{instancesId}:setIamPolicy
/// Sets the access control policy on the specified resource. Replaces any existing policy. Can return NOT_FOUND, INVALID_ARGUMENT, and PERMISSION_DENIED errors.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `apigeeregistry_projects_locations_instances_set_iam_policy_execute()` to send, or `apigeeregistry_projects_locations_instances_set_iam_policy` for simplest API.

pub fn apigeeregistry_projects_locations_instances_set_iam_policy_builder(
    client: &SimpleHttpClient,
    resource: &String,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://apigeeregistry.googleapis.com/v1/projects/{}/locations/{locationsId}/instances/{instancesId}:setIamPolicy",
        resource,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/instances/{instancesId}:setIamPolicy
/// Sets the access control policy on the specified resource. Replaces any existing policy. Can return NOT_FOUND, INVALID_ARGUMENT, and PERMISSION_DENIED errors.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `apigeeregistry_projects_locations_instances_set_iam_policy_execute()` or `apigeeregistry_projects_locations_instances_set_iam_policy`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `apigeeregistry_projects_locations_instances_set_iam_policy_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigeeregistry_projects_locations_instances_set_iam_policy_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Policy>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Policy = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/instances/{instancesId}:setIamPolicy
/// Sets the access control policy on the specified resource. Replaces any existing policy. Can return NOT_FOUND, INVALID_ARGUMENT, and PERMISSION_DENIED errors.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `apigeeregistry_projects_locations_instances_set_iam_policy_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `apigeeregistry_projects_locations_instances_set_iam_policy_task()`.
/// For the simplest API, use `apigeeregistry_projects_locations_instances_set_iam_policy()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `apigeeregistry_projects_locations_instances_set_iam_policy_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn apigeeregistry_projects_locations_instances_set_iam_policy_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Policy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = apigeeregistry_projects_locations_instances_set_iam_policy_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`apigeeregistry_projects_locations_instances_set_iam_policy`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ApigeeregistryProjectsLocationsInstancesSetIamPolicyArgs {
    /// Path parameter: resource
    pub resource: String,
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/instances/{instancesId}:setIamPolicy
/// Sets the access control policy on the specified resource. Replaces any existing policy. Can return NOT_FOUND, INVALID_ARGUMENT, and PERMISSION_DENIED errors.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `apigeeregistry_projects_locations_instances_set_iam_policy_builder()` + `apigeeregistry_projects_locations_instances_set_iam_policy_execute()`.
/// For task-level control, use `apigeeregistry_projects_locations_instances_set_iam_policy_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigeeregistry_projects_locations_instances_set_iam_policy(
    client: &SimpleHttpClient,
    args: &ApigeeregistryProjectsLocationsInstancesSetIamPolicyArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Policy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        apigeeregistry_projects_locations_instances_set_iam_policy_builder(client, &args.resource)?;
    apigeeregistry_projects_locations_instances_set_iam_policy_execute(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/instances/{instancesId}:testIamPermissions
/// Returns permissions that a caller has on the specified resource. If the resource does not exist, this will return an empty set of permissions, not a NOT_FOUND error. Note: This operation is designed to be used for building permission-aware UIs and command-line tools, not for authorization checking. This operation may "fail open" without warning.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `apigeeregistry_projects_locations_instances_test_iam_permissions_execute()` to send, or `apigeeregistry_projects_locations_instances_test_iam_permissions` for simplest API.

pub fn apigeeregistry_projects_locations_instances_test_iam_permissions_builder(
    client: &SimpleHttpClient,
    resource: &String,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://apigeeregistry.googleapis.com/v1/projects/{}/locations/{locationsId}/instances/{instancesId}:testIamPermissions",
        resource,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/instances/{instancesId}:testIamPermissions
/// Returns permissions that a caller has on the specified resource. If the resource does not exist, this will return an empty set of permissions, not a NOT_FOUND error. Note: This operation is designed to be used for building permission-aware UIs and command-line tools, not for authorization checking. This operation may "fail open" without warning.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `apigeeregistry_projects_locations_instances_test_iam_permissions_execute()` or `apigeeregistry_projects_locations_instances_test_iam_permissions`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `apigeeregistry_projects_locations_instances_test_iam_permissions_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigeeregistry_projects_locations_instances_test_iam_permissions_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<TestIamPermissionsResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: TestIamPermissionsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/instances/{instancesId}:testIamPermissions
/// Returns permissions that a caller has on the specified resource. If the resource does not exist, this will return an empty set of permissions, not a NOT_FOUND error. Note: This operation is designed to be used for building permission-aware UIs and command-line tools, not for authorization checking. This operation may "fail open" without warning.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `apigeeregistry_projects_locations_instances_test_iam_permissions_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `apigeeregistry_projects_locations_instances_test_iam_permissions_task()`.
/// For the simplest API, use `apigeeregistry_projects_locations_instances_test_iam_permissions()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `apigeeregistry_projects_locations_instances_test_iam_permissions_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn apigeeregistry_projects_locations_instances_test_iam_permissions_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<TestIamPermissionsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = apigeeregistry_projects_locations_instances_test_iam_permissions_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`apigeeregistry_projects_locations_instances_test_iam_permissions`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ApigeeregistryProjectsLocationsInstancesTestIamPermissionsArgs {
    /// Path parameter: resource
    pub resource: String,
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/instances/{instancesId}:testIamPermissions
/// Returns permissions that a caller has on the specified resource. If the resource does not exist, this will return an empty set of permissions, not a NOT_FOUND error. Note: This operation is designed to be used for building permission-aware UIs and command-line tools, not for authorization checking. This operation may "fail open" without warning.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `apigeeregistry_projects_locations_instances_test_iam_permissions_builder()` + `apigeeregistry_projects_locations_instances_test_iam_permissions_execute()`.
/// For task-level control, use `apigeeregistry_projects_locations_instances_test_iam_permissions_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigeeregistry_projects_locations_instances_test_iam_permissions(
    client: &SimpleHttpClient,
    args: &ApigeeregistryProjectsLocationsInstancesTestIamPermissionsArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<TestIamPermissionsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = apigeeregistry_projects_locations_instances_test_iam_permissions_builder(
        client,
        &args.resource,
    )?;
    apigeeregistry_projects_locations_instances_test_iam_permissions_execute(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/operations/{operationsId}:cancel
/// Starts asynchronous cancellation on a long-running operation. The server makes a best effort to cancel the operation, but success is not guaranteed. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED. Clients can use Operations.GetOperation or other methods to check whether the cancellation succeeded or whether the operation completed despite cancellation. On successful cancellation, the operation is not deleted; instead, it becomes an operation with an Operation.error value with a google.rpc.Status.code of 1, corresponding to Code.CANCELLED.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `apigeeregistry_projects_locations_operations_cancel_execute()` to send, or `apigeeregistry_projects_locations_operations_cancel` for simplest API.

pub fn apigeeregistry_projects_locations_operations_cancel_builder(
    client: &SimpleHttpClient,
    name: &String,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://apigeeregistry.googleapis.com/v1/projects/{}/locations/{locationsId}/operations/{operationsId}:cancel",
        name,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/operations/{operationsId}:cancel
/// Starts asynchronous cancellation on a long-running operation. The server makes a best effort to cancel the operation, but success is not guaranteed. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED. Clients can use Operations.GetOperation or other methods to check whether the cancellation succeeded or whether the operation completed despite cancellation. On successful cancellation, the operation is not deleted; instead, it becomes an operation with an Operation.error value with a google.rpc.Status.code of 1, corresponding to Code.CANCELLED.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `apigeeregistry_projects_locations_operations_cancel_execute()` or `apigeeregistry_projects_locations_operations_cancel`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `apigeeregistry_projects_locations_operations_cancel_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigeeregistry_projects_locations_operations_cancel_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Empty>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Empty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/operations/{operationsId}:cancel
/// Starts asynchronous cancellation on a long-running operation. The server makes a best effort to cancel the operation, but success is not guaranteed. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED. Clients can use Operations.GetOperation or other methods to check whether the cancellation succeeded or whether the operation completed despite cancellation. On successful cancellation, the operation is not deleted; instead, it becomes an operation with an Operation.error value with a google.rpc.Status.code of 1, corresponding to Code.CANCELLED.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `apigeeregistry_projects_locations_operations_cancel_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `apigeeregistry_projects_locations_operations_cancel_task()`.
/// For the simplest API, use `apigeeregistry_projects_locations_operations_cancel()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `apigeeregistry_projects_locations_operations_cancel_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn apigeeregistry_projects_locations_operations_cancel_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = apigeeregistry_projects_locations_operations_cancel_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`apigeeregistry_projects_locations_operations_cancel`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ApigeeregistryProjectsLocationsOperationsCancelArgs {
    /// Path parameter: name
    pub name: String,
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/operations/{operationsId}:cancel
/// Starts asynchronous cancellation on a long-running operation. The server makes a best effort to cancel the operation, but success is not guaranteed. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED. Clients can use Operations.GetOperation or other methods to check whether the cancellation succeeded or whether the operation completed despite cancellation. On successful cancellation, the operation is not deleted; instead, it becomes an operation with an Operation.error value with a google.rpc.Status.code of 1, corresponding to Code.CANCELLED.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `apigeeregistry_projects_locations_operations_cancel_builder()` + `apigeeregistry_projects_locations_operations_cancel_execute()`.
/// For task-level control, use `apigeeregistry_projects_locations_operations_cancel_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigeeregistry_projects_locations_operations_cancel(
    client: &SimpleHttpClient,
    args: &ApigeeregistryProjectsLocationsOperationsCancelArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = apigeeregistry_projects_locations_operations_cancel_builder(client, &args.name)?;
    apigeeregistry_projects_locations_operations_cancel_execute(builder)
}

/// DELETE v1/projects/{projectsId}/locations/{locationsId}/operations/{operationsId}
/// Deletes a long-running operation. This method indicates that the client is no longer interested in the operation result. It does not cancel the operation. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `apigeeregistry_projects_locations_operations_delete_execute()` to send, or `apigeeregistry_projects_locations_operations_delete` for simplest API.

pub fn apigeeregistry_projects_locations_operations_delete_builder(
    client: &SimpleHttpClient,
    name: &String,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://apigeeregistry.googleapis.com/v1/projects/{}/locations/{locationsId}/operations/{operationsId}",
        name,
    );

    // Build request
    let builder = client
        .delete(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// DELETE v1/projects/{projectsId}/locations/{locationsId}/operations/{operationsId}
/// Deletes a long-running operation. This method indicates that the client is no longer interested in the operation result. It does not cancel the operation. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `apigeeregistry_projects_locations_operations_delete_execute()` or `apigeeregistry_projects_locations_operations_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `apigeeregistry_projects_locations_operations_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigeeregistry_projects_locations_operations_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Empty>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Empty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// DELETE v1/projects/{projectsId}/locations/{locationsId}/operations/{operationsId}
/// Deletes a long-running operation. This method indicates that the client is no longer interested in the operation result. It does not cancel the operation. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `apigeeregistry_projects_locations_operations_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `apigeeregistry_projects_locations_operations_delete_task()`.
/// For the simplest API, use `apigeeregistry_projects_locations_operations_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `apigeeregistry_projects_locations_operations_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn apigeeregistry_projects_locations_operations_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = apigeeregistry_projects_locations_operations_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`apigeeregistry_projects_locations_operations_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ApigeeregistryProjectsLocationsOperationsDeleteArgs {
    /// Path parameter: name
    pub name: String,
}

/// DELETE v1/projects/{projectsId}/locations/{locationsId}/operations/{operationsId}
/// Deletes a long-running operation. This method indicates that the client is no longer interested in the operation result. It does not cancel the operation. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `apigeeregistry_projects_locations_operations_delete_builder()` + `apigeeregistry_projects_locations_operations_delete_execute()`.
/// For task-level control, use `apigeeregistry_projects_locations_operations_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigeeregistry_projects_locations_operations_delete(
    client: &SimpleHttpClient,
    args: &ApigeeregistryProjectsLocationsOperationsDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = apigeeregistry_projects_locations_operations_delete_builder(client, &args.name)?;
    apigeeregistry_projects_locations_operations_delete_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `apigeeregistry_projects_locations_operations_get_execute()` to send, or `apigeeregistry_projects_locations_operations_get` for simplest API.

pub fn apigeeregistry_projects_locations_operations_get_builder(
    client: &SimpleHttpClient,
    name: &String,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://apigeeregistry.googleapis.com/v1/projects/{}/locations/{locationsId}/operations/{operationsId}",
        name,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `apigeeregistry_projects_locations_operations_get_execute()` or `apigeeregistry_projects_locations_operations_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `apigeeregistry_projects_locations_operations_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigeeregistry_projects_locations_operations_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `apigeeregistry_projects_locations_operations_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `apigeeregistry_projects_locations_operations_get_task()`.
/// For the simplest API, use `apigeeregistry_projects_locations_operations_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `apigeeregistry_projects_locations_operations_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn apigeeregistry_projects_locations_operations_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = apigeeregistry_projects_locations_operations_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`apigeeregistry_projects_locations_operations_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ApigeeregistryProjectsLocationsOperationsGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `apigeeregistry_projects_locations_operations_get_builder()` + `apigeeregistry_projects_locations_operations_get_execute()`.
/// For task-level control, use `apigeeregistry_projects_locations_operations_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigeeregistry_projects_locations_operations_get(
    client: &SimpleHttpClient,
    args: &ApigeeregistryProjectsLocationsOperationsGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = apigeeregistry_projects_locations_operations_get_builder(client, &args.name)?;
    apigeeregistry_projects_locations_operations_get_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/operations
/// Lists operations that match the specified filter in the request. If the server doesn't support this method, it returns UNIMPLEMENTED.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `apigeeregistry_projects_locations_operations_list_execute()` to send, or `apigeeregistry_projects_locations_operations_list` for simplest API.

pub fn apigeeregistry_projects_locations_operations_list_builder(
    client: &SimpleHttpClient,
    name: &String,
    filter: &Option<Option<String>>,
    pageSize: &Option<Option<String>>,
    pageToken: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://apigeeregistry.googleapis.com/v1/projects/{}/locations/{locationsId}/operations",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter.as_ref() {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = pageSize.as_ref() {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/operations
/// Lists operations that match the specified filter in the request. If the server doesn't support this method, it returns UNIMPLEMENTED.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `apigeeregistry_projects_locations_operations_list_execute()` or `apigeeregistry_projects_locations_operations_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `apigeeregistry_projects_locations_operations_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigeeregistry_projects_locations_operations_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ListOperationsResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListOperationsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/operations
/// Lists operations that match the specified filter in the request. If the server doesn't support this method, it returns UNIMPLEMENTED.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `apigeeregistry_projects_locations_operations_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `apigeeregistry_projects_locations_operations_list_task()`.
/// For the simplest API, use `apigeeregistry_projects_locations_operations_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `apigeeregistry_projects_locations_operations_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn apigeeregistry_projects_locations_operations_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListOperationsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = apigeeregistry_projects_locations_operations_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`apigeeregistry_projects_locations_operations_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ApigeeregistryProjectsLocationsOperationsListArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: filter
    pub filter: Option<Option<String>>,
    /// Query parameter: pageSize
    pub pageSize: Option<Option<String>>,
    /// Query parameter: pageToken
    pub pageToken: Option<Option<String>>,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/operations
/// Lists operations that match the specified filter in the request. If the server doesn't support this method, it returns UNIMPLEMENTED.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `apigeeregistry_projects_locations_operations_list_builder()` + `apigeeregistry_projects_locations_operations_list_execute()`.
/// For task-level control, use `apigeeregistry_projects_locations_operations_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigeeregistry_projects_locations_operations_list(
    client: &SimpleHttpClient,
    args: &ApigeeregistryProjectsLocationsOperationsListArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListOperationsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = apigeeregistry_projects_locations_operations_list_builder(
        client,
        &args.name,
        &args.filter,
        &args.pageSize,
        &args.pageToken,
    )?;
    apigeeregistry_projects_locations_operations_list_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/runtime:getIamPolicy
/// Gets the access control policy for a resource. Returns an empty policy if the resource exists and does not have a policy set.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `apigeeregistry_projects_locations_runtime_get_iam_policy_execute()` to send, or `apigeeregistry_projects_locations_runtime_get_iam_policy` for simplest API.

pub fn apigeeregistry_projects_locations_runtime_get_iam_policy_builder(
    client: &SimpleHttpClient,
    resource: &String,
    options_requestedPolicyVersion: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://apigeeregistry.googleapis.com/v1/projects/{}/locations/{locationsId}/runtime:getIamPolicy",
        resource,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = options_requestedPolicyVersion.as_ref() {
        query_parts.push(format!("options.requestedPolicyVersion={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/runtime:getIamPolicy
/// Gets the access control policy for a resource. Returns an empty policy if the resource exists and does not have a policy set.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `apigeeregistry_projects_locations_runtime_get_iam_policy_execute()` or `apigeeregistry_projects_locations_runtime_get_iam_policy`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `apigeeregistry_projects_locations_runtime_get_iam_policy_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigeeregistry_projects_locations_runtime_get_iam_policy_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Policy>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Policy = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/runtime:getIamPolicy
/// Gets the access control policy for a resource. Returns an empty policy if the resource exists and does not have a policy set.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `apigeeregistry_projects_locations_runtime_get_iam_policy_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `apigeeregistry_projects_locations_runtime_get_iam_policy_task()`.
/// For the simplest API, use `apigeeregistry_projects_locations_runtime_get_iam_policy()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `apigeeregistry_projects_locations_runtime_get_iam_policy_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn apigeeregistry_projects_locations_runtime_get_iam_policy_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Policy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = apigeeregistry_projects_locations_runtime_get_iam_policy_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`apigeeregistry_projects_locations_runtime_get_iam_policy`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ApigeeregistryProjectsLocationsRuntimeGetIamPolicyArgs {
    /// Path parameter: resource
    pub resource: String,
    /// Query parameter: options_requestedPolicyVersion
    pub options_requestedPolicyVersion: Option<Option<String>>,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/runtime:getIamPolicy
/// Gets the access control policy for a resource. Returns an empty policy if the resource exists and does not have a policy set.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `apigeeregistry_projects_locations_runtime_get_iam_policy_builder()` + `apigeeregistry_projects_locations_runtime_get_iam_policy_execute()`.
/// For task-level control, use `apigeeregistry_projects_locations_runtime_get_iam_policy_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigeeregistry_projects_locations_runtime_get_iam_policy(
    client: &SimpleHttpClient,
    args: &ApigeeregistryProjectsLocationsRuntimeGetIamPolicyArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Policy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = apigeeregistry_projects_locations_runtime_get_iam_policy_builder(
        client,
        &args.resource,
        &args.options_requestedPolicyVersion,
    )?;
    apigeeregistry_projects_locations_runtime_get_iam_policy_execute(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/runtime:setIamPolicy
/// Sets the access control policy on the specified resource. Replaces any existing policy. Can return NOT_FOUND, INVALID_ARGUMENT, and PERMISSION_DENIED errors.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `apigeeregistry_projects_locations_runtime_set_iam_policy_execute()` to send, or `apigeeregistry_projects_locations_runtime_set_iam_policy` for simplest API.

pub fn apigeeregistry_projects_locations_runtime_set_iam_policy_builder(
    client: &SimpleHttpClient,
    resource: &String,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://apigeeregistry.googleapis.com/v1/projects/{}/locations/{locationsId}/runtime:setIamPolicy",
        resource,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/runtime:setIamPolicy
/// Sets the access control policy on the specified resource. Replaces any existing policy. Can return NOT_FOUND, INVALID_ARGUMENT, and PERMISSION_DENIED errors.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `apigeeregistry_projects_locations_runtime_set_iam_policy_execute()` or `apigeeregistry_projects_locations_runtime_set_iam_policy`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `apigeeregistry_projects_locations_runtime_set_iam_policy_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigeeregistry_projects_locations_runtime_set_iam_policy_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Policy>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Policy = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/runtime:setIamPolicy
/// Sets the access control policy on the specified resource. Replaces any existing policy. Can return NOT_FOUND, INVALID_ARGUMENT, and PERMISSION_DENIED errors.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `apigeeregistry_projects_locations_runtime_set_iam_policy_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `apigeeregistry_projects_locations_runtime_set_iam_policy_task()`.
/// For the simplest API, use `apigeeregistry_projects_locations_runtime_set_iam_policy()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `apigeeregistry_projects_locations_runtime_set_iam_policy_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn apigeeregistry_projects_locations_runtime_set_iam_policy_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Policy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = apigeeregistry_projects_locations_runtime_set_iam_policy_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`apigeeregistry_projects_locations_runtime_set_iam_policy`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ApigeeregistryProjectsLocationsRuntimeSetIamPolicyArgs {
    /// Path parameter: resource
    pub resource: String,
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/runtime:setIamPolicy
/// Sets the access control policy on the specified resource. Replaces any existing policy. Can return NOT_FOUND, INVALID_ARGUMENT, and PERMISSION_DENIED errors.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `apigeeregistry_projects_locations_runtime_set_iam_policy_builder()` + `apigeeregistry_projects_locations_runtime_set_iam_policy_execute()`.
/// For task-level control, use `apigeeregistry_projects_locations_runtime_set_iam_policy_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigeeregistry_projects_locations_runtime_set_iam_policy(
    client: &SimpleHttpClient,
    args: &ApigeeregistryProjectsLocationsRuntimeSetIamPolicyArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Policy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        apigeeregistry_projects_locations_runtime_set_iam_policy_builder(client, &args.resource)?;
    apigeeregistry_projects_locations_runtime_set_iam_policy_execute(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/runtime:testIamPermissions
/// Returns permissions that a caller has on the specified resource. If the resource does not exist, this will return an empty set of permissions, not a NOT_FOUND error. Note: This operation is designed to be used for building permission-aware UIs and command-line tools, not for authorization checking. This operation may "fail open" without warning.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `apigeeregistry_projects_locations_runtime_test_iam_permissions_execute()` to send, or `apigeeregistry_projects_locations_runtime_test_iam_permissions` for simplest API.

pub fn apigeeregistry_projects_locations_runtime_test_iam_permissions_builder(
    client: &SimpleHttpClient,
    resource: &String,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://apigeeregistry.googleapis.com/v1/projects/{}/locations/{locationsId}/runtime:testIamPermissions",
        resource,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/runtime:testIamPermissions
/// Returns permissions that a caller has on the specified resource. If the resource does not exist, this will return an empty set of permissions, not a NOT_FOUND error. Note: This operation is designed to be used for building permission-aware UIs and command-line tools, not for authorization checking. This operation may "fail open" without warning.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `apigeeregistry_projects_locations_runtime_test_iam_permissions_execute()` or `apigeeregistry_projects_locations_runtime_test_iam_permissions`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `apigeeregistry_projects_locations_runtime_test_iam_permissions_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigeeregistry_projects_locations_runtime_test_iam_permissions_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<TestIamPermissionsResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: TestIamPermissionsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/runtime:testIamPermissions
/// Returns permissions that a caller has on the specified resource. If the resource does not exist, this will return an empty set of permissions, not a NOT_FOUND error. Note: This operation is designed to be used for building permission-aware UIs and command-line tools, not for authorization checking. This operation may "fail open" without warning.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `apigeeregistry_projects_locations_runtime_test_iam_permissions_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `apigeeregistry_projects_locations_runtime_test_iam_permissions_task()`.
/// For the simplest API, use `apigeeregistry_projects_locations_runtime_test_iam_permissions()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `apigeeregistry_projects_locations_runtime_test_iam_permissions_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn apigeeregistry_projects_locations_runtime_test_iam_permissions_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<TestIamPermissionsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = apigeeregistry_projects_locations_runtime_test_iam_permissions_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`apigeeregistry_projects_locations_runtime_test_iam_permissions`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ApigeeregistryProjectsLocationsRuntimeTestIamPermissionsArgs {
    /// Path parameter: resource
    pub resource: String,
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/runtime:testIamPermissions
/// Returns permissions that a caller has on the specified resource. If the resource does not exist, this will return an empty set of permissions, not a NOT_FOUND error. Note: This operation is designed to be used for building permission-aware UIs and command-line tools, not for authorization checking. This operation may "fail open" without warning.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `apigeeregistry_projects_locations_runtime_test_iam_permissions_builder()` + `apigeeregistry_projects_locations_runtime_test_iam_permissions_execute()`.
/// For task-level control, use `apigeeregistry_projects_locations_runtime_test_iam_permissions_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apigeeregistry_projects_locations_runtime_test_iam_permissions(
    client: &SimpleHttpClient,
    args: &ApigeeregistryProjectsLocationsRuntimeTestIamPermissionsArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<TestIamPermissionsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = apigeeregistry_projects_locations_runtime_test_iam_permissions_builder(
        client,
        &args.resource,
    )?;
    apigeeregistry_projects_locations_runtime_test_iam_permissions_execute(builder)
}

// =============================================================================
// ResourceIdentifier implementation for Location
// =============================================================================

/// ResourceIdentifier implementation for Location with ApigeeregistryProjectsLocationsGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ApigeeregistryProjectsLocationsGetArgs> for Location {
    fn generate_resource_id(&self, input: &ApigeeregistryProjectsLocationsGetArgs) -> String {
        format!("gcp::apigeeregistry::Location/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::apigeeregistry::Location"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for ListLocationsResponse
// =============================================================================

/// ResourceIdentifier implementation for ListLocationsResponse with ApigeeregistryProjectsLocationsListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ApigeeregistryProjectsLocationsListArgs> for ListLocationsResponse {
    fn generate_resource_id(&self, input: &ApigeeregistryProjectsLocationsListArgs) -> String {
        format!("gcp::apigeeregistry::ListLocationsResponse/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::apigeeregistry::ListLocationsResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Api
// =============================================================================

/// ResourceIdentifier implementation for Api with ApigeeregistryProjectsLocationsApisCreateArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ApigeeregistryProjectsLocationsApisCreateArgs> for Api {
    fn generate_resource_id(
        &self,
        input: &ApigeeregistryProjectsLocationsApisCreateArgs,
    ) -> String {
        format!("gcp::apigeeregistry::Api/{}", input.parent)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::apigeeregistry::Api"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Empty
// =============================================================================

/// ResourceIdentifier implementation for Empty with ApigeeregistryProjectsLocationsApisDeleteArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ApigeeregistryProjectsLocationsApisDeleteArgs> for Empty {
    fn generate_resource_id(
        &self,
        input: &ApigeeregistryProjectsLocationsApisDeleteArgs,
    ) -> String {
        format!("gcp::apigeeregistry::Empty/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::apigeeregistry::Empty"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Api
// =============================================================================

/// ResourceIdentifier implementation for Api with ApigeeregistryProjectsLocationsApisGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ApigeeregistryProjectsLocationsApisGetArgs> for Api {
    fn generate_resource_id(&self, input: &ApigeeregistryProjectsLocationsApisGetArgs) -> String {
        format!("gcp::apigeeregistry::Api/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::apigeeregistry::Api"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Policy
// =============================================================================

/// ResourceIdentifier implementation for Policy with ApigeeregistryProjectsLocationsApisGetIamPolicyArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ApigeeregistryProjectsLocationsApisGetIamPolicyArgs> for Policy {
    fn generate_resource_id(
        &self,
        input: &ApigeeregistryProjectsLocationsApisGetIamPolicyArgs,
    ) -> String {
        format!("gcp::apigeeregistry::Policy/{}", input.resource)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::apigeeregistry::Policy"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for ListApisResponse
// =============================================================================

/// ResourceIdentifier implementation for ListApisResponse with ApigeeregistryProjectsLocationsApisListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ApigeeregistryProjectsLocationsApisListArgs> for ListApisResponse {
    fn generate_resource_id(&self, input: &ApigeeregistryProjectsLocationsApisListArgs) -> String {
        format!("gcp::apigeeregistry::ListApisResponse/{}", input.parent)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::apigeeregistry::ListApisResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Api
// =============================================================================

/// ResourceIdentifier implementation for Api with ApigeeregistryProjectsLocationsApisPatchArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ApigeeregistryProjectsLocationsApisPatchArgs> for Api {
    fn generate_resource_id(&self, input: &ApigeeregistryProjectsLocationsApisPatchArgs) -> String {
        format!("gcp::apigeeregistry::Api/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::apigeeregistry::Api"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Policy
// =============================================================================

/// ResourceIdentifier implementation for Policy with ApigeeregistryProjectsLocationsApisSetIamPolicyArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ApigeeregistryProjectsLocationsApisSetIamPolicyArgs> for Policy {
    fn generate_resource_id(
        &self,
        input: &ApigeeregistryProjectsLocationsApisSetIamPolicyArgs,
    ) -> String {
        format!("gcp::apigeeregistry::Policy/{}", input.resource)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::apigeeregistry::Policy"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for TestIamPermissionsResponse
// =============================================================================

/// ResourceIdentifier implementation for TestIamPermissionsResponse with ApigeeregistryProjectsLocationsApisTestIamPermissionsArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ApigeeregistryProjectsLocationsApisTestIamPermissionsArgs>
    for TestIamPermissionsResponse
{
    fn generate_resource_id(
        &self,
        input: &ApigeeregistryProjectsLocationsApisTestIamPermissionsArgs,
    ) -> String {
        format!(
            "gcp::apigeeregistry::TestIamPermissionsResponse/{}",
            input.resource
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::apigeeregistry::TestIamPermissionsResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Artifact
// =============================================================================

/// ResourceIdentifier implementation for Artifact with ApigeeregistryProjectsLocationsApisArtifactsCreateArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ApigeeregistryProjectsLocationsApisArtifactsCreateArgs> for Artifact {
    fn generate_resource_id(
        &self,
        input: &ApigeeregistryProjectsLocationsApisArtifactsCreateArgs,
    ) -> String {
        format!("gcp::apigeeregistry::Artifact/{}", input.parent)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::apigeeregistry::Artifact"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Empty
// =============================================================================

/// ResourceIdentifier implementation for Empty with ApigeeregistryProjectsLocationsApisArtifactsDeleteArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ApigeeregistryProjectsLocationsApisArtifactsDeleteArgs> for Empty {
    fn generate_resource_id(
        &self,
        input: &ApigeeregistryProjectsLocationsApisArtifactsDeleteArgs,
    ) -> String {
        format!("gcp::apigeeregistry::Empty/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::apigeeregistry::Empty"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Artifact
// =============================================================================

/// ResourceIdentifier implementation for Artifact with ApigeeregistryProjectsLocationsApisArtifactsGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ApigeeregistryProjectsLocationsApisArtifactsGetArgs> for Artifact {
    fn generate_resource_id(
        &self,
        input: &ApigeeregistryProjectsLocationsApisArtifactsGetArgs,
    ) -> String {
        format!("gcp::apigeeregistry::Artifact/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::apigeeregistry::Artifact"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for HttpBody
// =============================================================================

/// ResourceIdentifier implementation for HttpBody with ApigeeregistryProjectsLocationsApisArtifactsGetContentsArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ApigeeregistryProjectsLocationsApisArtifactsGetContentsArgs> for HttpBody {
    fn generate_resource_id(
        &self,
        input: &ApigeeregistryProjectsLocationsApisArtifactsGetContentsArgs,
    ) -> String {
        format!("gcp::apigeeregistry::HttpBody/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::apigeeregistry::HttpBody"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Policy
// =============================================================================

/// ResourceIdentifier implementation for Policy with ApigeeregistryProjectsLocationsApisArtifactsGetIamPolicyArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ApigeeregistryProjectsLocationsApisArtifactsGetIamPolicyArgs> for Policy {
    fn generate_resource_id(
        &self,
        input: &ApigeeregistryProjectsLocationsApisArtifactsGetIamPolicyArgs,
    ) -> String {
        format!("gcp::apigeeregistry::Policy/{}", input.resource)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::apigeeregistry::Policy"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for ListArtifactsResponse
// =============================================================================

/// ResourceIdentifier implementation for ListArtifactsResponse with ApigeeregistryProjectsLocationsApisArtifactsListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ApigeeregistryProjectsLocationsApisArtifactsListArgs>
    for ListArtifactsResponse
{
    fn generate_resource_id(
        &self,
        input: &ApigeeregistryProjectsLocationsApisArtifactsListArgs,
    ) -> String {
        format!(
            "gcp::apigeeregistry::ListArtifactsResponse/{}",
            input.parent
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::apigeeregistry::ListArtifactsResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Artifact
// =============================================================================

/// ResourceIdentifier implementation for Artifact with ApigeeregistryProjectsLocationsApisArtifactsReplaceArtifactArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ApigeeregistryProjectsLocationsApisArtifactsReplaceArtifactArgs>
    for Artifact
{
    fn generate_resource_id(
        &self,
        input: &ApigeeregistryProjectsLocationsApisArtifactsReplaceArtifactArgs,
    ) -> String {
        format!("gcp::apigeeregistry::Artifact/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::apigeeregistry::Artifact"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Policy
// =============================================================================

/// ResourceIdentifier implementation for Policy with ApigeeregistryProjectsLocationsApisArtifactsSetIamPolicyArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ApigeeregistryProjectsLocationsApisArtifactsSetIamPolicyArgs> for Policy {
    fn generate_resource_id(
        &self,
        input: &ApigeeregistryProjectsLocationsApisArtifactsSetIamPolicyArgs,
    ) -> String {
        format!("gcp::apigeeregistry::Policy/{}", input.resource)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::apigeeregistry::Policy"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for TestIamPermissionsResponse
// =============================================================================

/// ResourceIdentifier implementation for TestIamPermissionsResponse with ApigeeregistryProjectsLocationsApisArtifactsTestIamPermissionsArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ApigeeregistryProjectsLocationsApisArtifactsTestIamPermissionsArgs>
    for TestIamPermissionsResponse
{
    fn generate_resource_id(
        &self,
        input: &ApigeeregistryProjectsLocationsApisArtifactsTestIamPermissionsArgs,
    ) -> String {
        format!(
            "gcp::apigeeregistry::TestIamPermissionsResponse/{}",
            input.resource
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::apigeeregistry::TestIamPermissionsResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for ApiDeployment
// =============================================================================

/// ResourceIdentifier implementation for ApiDeployment with ApigeeregistryProjectsLocationsApisDeploymentsCreateArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ApigeeregistryProjectsLocationsApisDeploymentsCreateArgs>
    for ApiDeployment
{
    fn generate_resource_id(
        &self,
        input: &ApigeeregistryProjectsLocationsApisDeploymentsCreateArgs,
    ) -> String {
        format!("gcp::apigeeregistry::ApiDeployment/{}", input.parent)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::apigeeregistry::ApiDeployment"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Empty
// =============================================================================

/// ResourceIdentifier implementation for Empty with ApigeeregistryProjectsLocationsApisDeploymentsDeleteArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ApigeeregistryProjectsLocationsApisDeploymentsDeleteArgs> for Empty {
    fn generate_resource_id(
        &self,
        input: &ApigeeregistryProjectsLocationsApisDeploymentsDeleteArgs,
    ) -> String {
        format!("gcp::apigeeregistry::Empty/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::apigeeregistry::Empty"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for ApiDeployment
// =============================================================================

/// ResourceIdentifier implementation for ApiDeployment with ApigeeregistryProjectsLocationsApisDeploymentsDeleteRevisionArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ApigeeregistryProjectsLocationsApisDeploymentsDeleteRevisionArgs>
    for ApiDeployment
{
    fn generate_resource_id(
        &self,
        input: &ApigeeregistryProjectsLocationsApisDeploymentsDeleteRevisionArgs,
    ) -> String {
        format!("gcp::apigeeregistry::ApiDeployment/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::apigeeregistry::ApiDeployment"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for ApiDeployment
// =============================================================================

/// ResourceIdentifier implementation for ApiDeployment with ApigeeregistryProjectsLocationsApisDeploymentsGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ApigeeregistryProjectsLocationsApisDeploymentsGetArgs> for ApiDeployment {
    fn generate_resource_id(
        &self,
        input: &ApigeeregistryProjectsLocationsApisDeploymentsGetArgs,
    ) -> String {
        format!("gcp::apigeeregistry::ApiDeployment/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::apigeeregistry::ApiDeployment"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Policy
// =============================================================================

/// ResourceIdentifier implementation for Policy with ApigeeregistryProjectsLocationsApisDeploymentsGetIamPolicyArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ApigeeregistryProjectsLocationsApisDeploymentsGetIamPolicyArgs> for Policy {
    fn generate_resource_id(
        &self,
        input: &ApigeeregistryProjectsLocationsApisDeploymentsGetIamPolicyArgs,
    ) -> String {
        format!("gcp::apigeeregistry::Policy/{}", input.resource)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::apigeeregistry::Policy"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for ListApiDeploymentsResponse
// =============================================================================

/// ResourceIdentifier implementation for ListApiDeploymentsResponse with ApigeeregistryProjectsLocationsApisDeploymentsListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ApigeeregistryProjectsLocationsApisDeploymentsListArgs>
    for ListApiDeploymentsResponse
{
    fn generate_resource_id(
        &self,
        input: &ApigeeregistryProjectsLocationsApisDeploymentsListArgs,
    ) -> String {
        format!(
            "gcp::apigeeregistry::ListApiDeploymentsResponse/{}",
            input.parent
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::apigeeregistry::ListApiDeploymentsResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for ListApiDeploymentRevisionsResponse
// =============================================================================

/// ResourceIdentifier implementation for ListApiDeploymentRevisionsResponse with ApigeeregistryProjectsLocationsApisDeploymentsListRevisionsArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ApigeeregistryProjectsLocationsApisDeploymentsListRevisionsArgs>
    for ListApiDeploymentRevisionsResponse
{
    fn generate_resource_id(
        &self,
        input: &ApigeeregistryProjectsLocationsApisDeploymentsListRevisionsArgs,
    ) -> String {
        format!(
            "gcp::apigeeregistry::ListApiDeploymentRevisionsResponse/{}",
            input.name
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::apigeeregistry::ListApiDeploymentRevisionsResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for ApiDeployment
// =============================================================================

/// ResourceIdentifier implementation for ApiDeployment with ApigeeregistryProjectsLocationsApisDeploymentsPatchArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ApigeeregistryProjectsLocationsApisDeploymentsPatchArgs> for ApiDeployment {
    fn generate_resource_id(
        &self,
        input: &ApigeeregistryProjectsLocationsApisDeploymentsPatchArgs,
    ) -> String {
        format!("gcp::apigeeregistry::ApiDeployment/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::apigeeregistry::ApiDeployment"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for ApiDeployment
// =============================================================================

/// ResourceIdentifier implementation for ApiDeployment with ApigeeregistryProjectsLocationsApisDeploymentsRollbackArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ApigeeregistryProjectsLocationsApisDeploymentsRollbackArgs>
    for ApiDeployment
{
    fn generate_resource_id(
        &self,
        input: &ApigeeregistryProjectsLocationsApisDeploymentsRollbackArgs,
    ) -> String {
        format!("gcp::apigeeregistry::ApiDeployment/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::apigeeregistry::ApiDeployment"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Policy
// =============================================================================

/// ResourceIdentifier implementation for Policy with ApigeeregistryProjectsLocationsApisDeploymentsSetIamPolicyArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ApigeeregistryProjectsLocationsApisDeploymentsSetIamPolicyArgs> for Policy {
    fn generate_resource_id(
        &self,
        input: &ApigeeregistryProjectsLocationsApisDeploymentsSetIamPolicyArgs,
    ) -> String {
        format!("gcp::apigeeregistry::Policy/{}", input.resource)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::apigeeregistry::Policy"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for ApiDeployment
// =============================================================================

/// ResourceIdentifier implementation for ApiDeployment with ApigeeregistryProjectsLocationsApisDeploymentsTagRevisionArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ApigeeregistryProjectsLocationsApisDeploymentsTagRevisionArgs>
    for ApiDeployment
{
    fn generate_resource_id(
        &self,
        input: &ApigeeregistryProjectsLocationsApisDeploymentsTagRevisionArgs,
    ) -> String {
        format!("gcp::apigeeregistry::ApiDeployment/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::apigeeregistry::ApiDeployment"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for TestIamPermissionsResponse
// =============================================================================

/// ResourceIdentifier implementation for TestIamPermissionsResponse with ApigeeregistryProjectsLocationsApisDeploymentsTestIamPermissionsArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ApigeeregistryProjectsLocationsApisDeploymentsTestIamPermissionsArgs>
    for TestIamPermissionsResponse
{
    fn generate_resource_id(
        &self,
        input: &ApigeeregistryProjectsLocationsApisDeploymentsTestIamPermissionsArgs,
    ) -> String {
        format!(
            "gcp::apigeeregistry::TestIamPermissionsResponse/{}",
            input.resource
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::apigeeregistry::TestIamPermissionsResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Artifact
// =============================================================================

/// ResourceIdentifier implementation for Artifact with ApigeeregistryProjectsLocationsApisDeploymentsArtifactsCreateArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ApigeeregistryProjectsLocationsApisDeploymentsArtifactsCreateArgs>
    for Artifact
{
    fn generate_resource_id(
        &self,
        input: &ApigeeregistryProjectsLocationsApisDeploymentsArtifactsCreateArgs,
    ) -> String {
        format!("gcp::apigeeregistry::Artifact/{}", input.parent)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::apigeeregistry::Artifact"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Empty
// =============================================================================

/// ResourceIdentifier implementation for Empty with ApigeeregistryProjectsLocationsApisDeploymentsArtifactsDeleteArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ApigeeregistryProjectsLocationsApisDeploymentsArtifactsDeleteArgs>
    for Empty
{
    fn generate_resource_id(
        &self,
        input: &ApigeeregistryProjectsLocationsApisDeploymentsArtifactsDeleteArgs,
    ) -> String {
        format!("gcp::apigeeregistry::Empty/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::apigeeregistry::Empty"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Artifact
// =============================================================================

/// ResourceIdentifier implementation for Artifact with ApigeeregistryProjectsLocationsApisDeploymentsArtifactsGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ApigeeregistryProjectsLocationsApisDeploymentsArtifactsGetArgs>
    for Artifact
{
    fn generate_resource_id(
        &self,
        input: &ApigeeregistryProjectsLocationsApisDeploymentsArtifactsGetArgs,
    ) -> String {
        format!("gcp::apigeeregistry::Artifact/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::apigeeregistry::Artifact"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for HttpBody
// =============================================================================

/// ResourceIdentifier implementation for HttpBody with ApigeeregistryProjectsLocationsApisDeploymentsArtifactsGetContentsArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ApigeeregistryProjectsLocationsApisDeploymentsArtifactsGetContentsArgs>
    for HttpBody
{
    fn generate_resource_id(
        &self,
        input: &ApigeeregistryProjectsLocationsApisDeploymentsArtifactsGetContentsArgs,
    ) -> String {
        format!("gcp::apigeeregistry::HttpBody/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::apigeeregistry::HttpBody"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for ListArtifactsResponse
// =============================================================================

/// ResourceIdentifier implementation for ListArtifactsResponse with ApigeeregistryProjectsLocationsApisDeploymentsArtifactsListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ApigeeregistryProjectsLocationsApisDeploymentsArtifactsListArgs>
    for ListArtifactsResponse
{
    fn generate_resource_id(
        &self,
        input: &ApigeeregistryProjectsLocationsApisDeploymentsArtifactsListArgs,
    ) -> String {
        format!(
            "gcp::apigeeregistry::ListArtifactsResponse/{}",
            input.parent
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::apigeeregistry::ListArtifactsResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Artifact
// =============================================================================

/// ResourceIdentifier implementation for Artifact with ApigeeregistryProjectsLocationsApisDeploymentsArtifactsReplaceArtifactArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ApigeeregistryProjectsLocationsApisDeploymentsArtifactsReplaceArtifactArgs>
    for Artifact
{
    fn generate_resource_id(
        &self,
        input: &ApigeeregistryProjectsLocationsApisDeploymentsArtifactsReplaceArtifactArgs,
    ) -> String {
        format!("gcp::apigeeregistry::Artifact/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::apigeeregistry::Artifact"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for ApiVersion
// =============================================================================

/// ResourceIdentifier implementation for ApiVersion with ApigeeregistryProjectsLocationsApisVersionsCreateArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ApigeeregistryProjectsLocationsApisVersionsCreateArgs> for ApiVersion {
    fn generate_resource_id(
        &self,
        input: &ApigeeregistryProjectsLocationsApisVersionsCreateArgs,
    ) -> String {
        format!("gcp::apigeeregistry::ApiVersion/{}", input.parent)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::apigeeregistry::ApiVersion"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Empty
// =============================================================================

/// ResourceIdentifier implementation for Empty with ApigeeregistryProjectsLocationsApisVersionsDeleteArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ApigeeregistryProjectsLocationsApisVersionsDeleteArgs> for Empty {
    fn generate_resource_id(
        &self,
        input: &ApigeeregistryProjectsLocationsApisVersionsDeleteArgs,
    ) -> String {
        format!("gcp::apigeeregistry::Empty/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::apigeeregistry::Empty"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for ApiVersion
// =============================================================================

/// ResourceIdentifier implementation for ApiVersion with ApigeeregistryProjectsLocationsApisVersionsGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ApigeeregistryProjectsLocationsApisVersionsGetArgs> for ApiVersion {
    fn generate_resource_id(
        &self,
        input: &ApigeeregistryProjectsLocationsApisVersionsGetArgs,
    ) -> String {
        format!("gcp::apigeeregistry::ApiVersion/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::apigeeregistry::ApiVersion"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Policy
// =============================================================================

/// ResourceIdentifier implementation for Policy with ApigeeregistryProjectsLocationsApisVersionsGetIamPolicyArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ApigeeregistryProjectsLocationsApisVersionsGetIamPolicyArgs> for Policy {
    fn generate_resource_id(
        &self,
        input: &ApigeeregistryProjectsLocationsApisVersionsGetIamPolicyArgs,
    ) -> String {
        format!("gcp::apigeeregistry::Policy/{}", input.resource)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::apigeeregistry::Policy"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for ListApiVersionsResponse
// =============================================================================

/// ResourceIdentifier implementation for ListApiVersionsResponse with ApigeeregistryProjectsLocationsApisVersionsListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ApigeeregistryProjectsLocationsApisVersionsListArgs>
    for ListApiVersionsResponse
{
    fn generate_resource_id(
        &self,
        input: &ApigeeregistryProjectsLocationsApisVersionsListArgs,
    ) -> String {
        format!(
            "gcp::apigeeregistry::ListApiVersionsResponse/{}",
            input.parent
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::apigeeregistry::ListApiVersionsResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for ApiVersion
// =============================================================================

/// ResourceIdentifier implementation for ApiVersion with ApigeeregistryProjectsLocationsApisVersionsPatchArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ApigeeregistryProjectsLocationsApisVersionsPatchArgs> for ApiVersion {
    fn generate_resource_id(
        &self,
        input: &ApigeeregistryProjectsLocationsApisVersionsPatchArgs,
    ) -> String {
        format!("gcp::apigeeregistry::ApiVersion/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::apigeeregistry::ApiVersion"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Policy
// =============================================================================

/// ResourceIdentifier implementation for Policy with ApigeeregistryProjectsLocationsApisVersionsSetIamPolicyArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ApigeeregistryProjectsLocationsApisVersionsSetIamPolicyArgs> for Policy {
    fn generate_resource_id(
        &self,
        input: &ApigeeregistryProjectsLocationsApisVersionsSetIamPolicyArgs,
    ) -> String {
        format!("gcp::apigeeregistry::Policy/{}", input.resource)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::apigeeregistry::Policy"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for TestIamPermissionsResponse
// =============================================================================

/// ResourceIdentifier implementation for TestIamPermissionsResponse with ApigeeregistryProjectsLocationsApisVersionsTestIamPermissionsArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ApigeeregistryProjectsLocationsApisVersionsTestIamPermissionsArgs>
    for TestIamPermissionsResponse
{
    fn generate_resource_id(
        &self,
        input: &ApigeeregistryProjectsLocationsApisVersionsTestIamPermissionsArgs,
    ) -> String {
        format!(
            "gcp::apigeeregistry::TestIamPermissionsResponse/{}",
            input.resource
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::apigeeregistry::TestIamPermissionsResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Artifact
// =============================================================================

/// ResourceIdentifier implementation for Artifact with ApigeeregistryProjectsLocationsApisVersionsArtifactsCreateArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ApigeeregistryProjectsLocationsApisVersionsArtifactsCreateArgs>
    for Artifact
{
    fn generate_resource_id(
        &self,
        input: &ApigeeregistryProjectsLocationsApisVersionsArtifactsCreateArgs,
    ) -> String {
        format!("gcp::apigeeregistry::Artifact/{}", input.parent)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::apigeeregistry::Artifact"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Empty
// =============================================================================

/// ResourceIdentifier implementation for Empty with ApigeeregistryProjectsLocationsApisVersionsArtifactsDeleteArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ApigeeregistryProjectsLocationsApisVersionsArtifactsDeleteArgs> for Empty {
    fn generate_resource_id(
        &self,
        input: &ApigeeregistryProjectsLocationsApisVersionsArtifactsDeleteArgs,
    ) -> String {
        format!("gcp::apigeeregistry::Empty/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::apigeeregistry::Empty"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Artifact
// =============================================================================

/// ResourceIdentifier implementation for Artifact with ApigeeregistryProjectsLocationsApisVersionsArtifactsGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ApigeeregistryProjectsLocationsApisVersionsArtifactsGetArgs> for Artifact {
    fn generate_resource_id(
        &self,
        input: &ApigeeregistryProjectsLocationsApisVersionsArtifactsGetArgs,
    ) -> String {
        format!("gcp::apigeeregistry::Artifact/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::apigeeregistry::Artifact"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for HttpBody
// =============================================================================

/// ResourceIdentifier implementation for HttpBody with ApigeeregistryProjectsLocationsApisVersionsArtifactsGetContentsArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ApigeeregistryProjectsLocationsApisVersionsArtifactsGetContentsArgs>
    for HttpBody
{
    fn generate_resource_id(
        &self,
        input: &ApigeeregistryProjectsLocationsApisVersionsArtifactsGetContentsArgs,
    ) -> String {
        format!("gcp::apigeeregistry::HttpBody/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::apigeeregistry::HttpBody"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Policy
// =============================================================================

/// ResourceIdentifier implementation for Policy with ApigeeregistryProjectsLocationsApisVersionsArtifactsGetIamPolicyArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ApigeeregistryProjectsLocationsApisVersionsArtifactsGetIamPolicyArgs>
    for Policy
{
    fn generate_resource_id(
        &self,
        input: &ApigeeregistryProjectsLocationsApisVersionsArtifactsGetIamPolicyArgs,
    ) -> String {
        format!("gcp::apigeeregistry::Policy/{}", input.resource)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::apigeeregistry::Policy"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for ListArtifactsResponse
// =============================================================================

/// ResourceIdentifier implementation for ListArtifactsResponse with ApigeeregistryProjectsLocationsApisVersionsArtifactsListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ApigeeregistryProjectsLocationsApisVersionsArtifactsListArgs>
    for ListArtifactsResponse
{
    fn generate_resource_id(
        &self,
        input: &ApigeeregistryProjectsLocationsApisVersionsArtifactsListArgs,
    ) -> String {
        format!(
            "gcp::apigeeregistry::ListArtifactsResponse/{}",
            input.parent
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::apigeeregistry::ListArtifactsResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Artifact
// =============================================================================

/// ResourceIdentifier implementation for Artifact with ApigeeregistryProjectsLocationsApisVersionsArtifactsReplaceArtifactArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ApigeeregistryProjectsLocationsApisVersionsArtifactsReplaceArtifactArgs>
    for Artifact
{
    fn generate_resource_id(
        &self,
        input: &ApigeeregistryProjectsLocationsApisVersionsArtifactsReplaceArtifactArgs,
    ) -> String {
        format!("gcp::apigeeregistry::Artifact/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::apigeeregistry::Artifact"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Policy
// =============================================================================

/// ResourceIdentifier implementation for Policy with ApigeeregistryProjectsLocationsApisVersionsArtifactsSetIamPolicyArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ApigeeregistryProjectsLocationsApisVersionsArtifactsSetIamPolicyArgs>
    for Policy
{
    fn generate_resource_id(
        &self,
        input: &ApigeeregistryProjectsLocationsApisVersionsArtifactsSetIamPolicyArgs,
    ) -> String {
        format!("gcp::apigeeregistry::Policy/{}", input.resource)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::apigeeregistry::Policy"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for TestIamPermissionsResponse
// =============================================================================

/// ResourceIdentifier implementation for TestIamPermissionsResponse with ApigeeregistryProjectsLocationsApisVersionsArtifactsTestIamPermissionsArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ApigeeregistryProjectsLocationsApisVersionsArtifactsTestIamPermissionsArgs>
    for TestIamPermissionsResponse
{
    fn generate_resource_id(
        &self,
        input: &ApigeeregistryProjectsLocationsApisVersionsArtifactsTestIamPermissionsArgs,
    ) -> String {
        format!(
            "gcp::apigeeregistry::TestIamPermissionsResponse/{}",
            input.resource
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::apigeeregistry::TestIamPermissionsResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for ApiSpec
// =============================================================================

/// ResourceIdentifier implementation for ApiSpec with ApigeeregistryProjectsLocationsApisVersionsSpecsCreateArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ApigeeregistryProjectsLocationsApisVersionsSpecsCreateArgs> for ApiSpec {
    fn generate_resource_id(
        &self,
        input: &ApigeeregistryProjectsLocationsApisVersionsSpecsCreateArgs,
    ) -> String {
        format!("gcp::apigeeregistry::ApiSpec/{}", input.parent)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::apigeeregistry::ApiSpec"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Empty
// =============================================================================

/// ResourceIdentifier implementation for Empty with ApigeeregistryProjectsLocationsApisVersionsSpecsDeleteArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ApigeeregistryProjectsLocationsApisVersionsSpecsDeleteArgs> for Empty {
    fn generate_resource_id(
        &self,
        input: &ApigeeregistryProjectsLocationsApisVersionsSpecsDeleteArgs,
    ) -> String {
        format!("gcp::apigeeregistry::Empty/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::apigeeregistry::Empty"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for ApiSpec
// =============================================================================

/// ResourceIdentifier implementation for ApiSpec with ApigeeregistryProjectsLocationsApisVersionsSpecsDeleteRevisionArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ApigeeregistryProjectsLocationsApisVersionsSpecsDeleteRevisionArgs>
    for ApiSpec
{
    fn generate_resource_id(
        &self,
        input: &ApigeeregistryProjectsLocationsApisVersionsSpecsDeleteRevisionArgs,
    ) -> String {
        format!("gcp::apigeeregistry::ApiSpec/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::apigeeregistry::ApiSpec"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for ApiSpec
// =============================================================================

/// ResourceIdentifier implementation for ApiSpec with ApigeeregistryProjectsLocationsApisVersionsSpecsGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ApigeeregistryProjectsLocationsApisVersionsSpecsGetArgs> for ApiSpec {
    fn generate_resource_id(
        &self,
        input: &ApigeeregistryProjectsLocationsApisVersionsSpecsGetArgs,
    ) -> String {
        format!("gcp::apigeeregistry::ApiSpec/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::apigeeregistry::ApiSpec"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for HttpBody
// =============================================================================

/// ResourceIdentifier implementation for HttpBody with ApigeeregistryProjectsLocationsApisVersionsSpecsGetContentsArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ApigeeregistryProjectsLocationsApisVersionsSpecsGetContentsArgs>
    for HttpBody
{
    fn generate_resource_id(
        &self,
        input: &ApigeeregistryProjectsLocationsApisVersionsSpecsGetContentsArgs,
    ) -> String {
        format!("gcp::apigeeregistry::HttpBody/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::apigeeregistry::HttpBody"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Policy
// =============================================================================

/// ResourceIdentifier implementation for Policy with ApigeeregistryProjectsLocationsApisVersionsSpecsGetIamPolicyArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ApigeeregistryProjectsLocationsApisVersionsSpecsGetIamPolicyArgs>
    for Policy
{
    fn generate_resource_id(
        &self,
        input: &ApigeeregistryProjectsLocationsApisVersionsSpecsGetIamPolicyArgs,
    ) -> String {
        format!("gcp::apigeeregistry::Policy/{}", input.resource)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::apigeeregistry::Policy"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for ListApiSpecsResponse
// =============================================================================

/// ResourceIdentifier implementation for ListApiSpecsResponse with ApigeeregistryProjectsLocationsApisVersionsSpecsListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ApigeeregistryProjectsLocationsApisVersionsSpecsListArgs>
    for ListApiSpecsResponse
{
    fn generate_resource_id(
        &self,
        input: &ApigeeregistryProjectsLocationsApisVersionsSpecsListArgs,
    ) -> String {
        format!("gcp::apigeeregistry::ListApiSpecsResponse/{}", input.parent)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::apigeeregistry::ListApiSpecsResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for ListApiSpecRevisionsResponse
// =============================================================================

/// ResourceIdentifier implementation for ListApiSpecRevisionsResponse with ApigeeregistryProjectsLocationsApisVersionsSpecsListRevisionsArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ApigeeregistryProjectsLocationsApisVersionsSpecsListRevisionsArgs>
    for ListApiSpecRevisionsResponse
{
    fn generate_resource_id(
        &self,
        input: &ApigeeregistryProjectsLocationsApisVersionsSpecsListRevisionsArgs,
    ) -> String {
        format!(
            "gcp::apigeeregistry::ListApiSpecRevisionsResponse/{}",
            input.name
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::apigeeregistry::ListApiSpecRevisionsResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for ApiSpec
// =============================================================================

/// ResourceIdentifier implementation for ApiSpec with ApigeeregistryProjectsLocationsApisVersionsSpecsPatchArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ApigeeregistryProjectsLocationsApisVersionsSpecsPatchArgs> for ApiSpec {
    fn generate_resource_id(
        &self,
        input: &ApigeeregistryProjectsLocationsApisVersionsSpecsPatchArgs,
    ) -> String {
        format!("gcp::apigeeregistry::ApiSpec/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::apigeeregistry::ApiSpec"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for ApiSpec
// =============================================================================

/// ResourceIdentifier implementation for ApiSpec with ApigeeregistryProjectsLocationsApisVersionsSpecsRollbackArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ApigeeregistryProjectsLocationsApisVersionsSpecsRollbackArgs> for ApiSpec {
    fn generate_resource_id(
        &self,
        input: &ApigeeregistryProjectsLocationsApisVersionsSpecsRollbackArgs,
    ) -> String {
        format!("gcp::apigeeregistry::ApiSpec/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::apigeeregistry::ApiSpec"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Policy
// =============================================================================

/// ResourceIdentifier implementation for Policy with ApigeeregistryProjectsLocationsApisVersionsSpecsSetIamPolicyArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ApigeeregistryProjectsLocationsApisVersionsSpecsSetIamPolicyArgs>
    for Policy
{
    fn generate_resource_id(
        &self,
        input: &ApigeeregistryProjectsLocationsApisVersionsSpecsSetIamPolicyArgs,
    ) -> String {
        format!("gcp::apigeeregistry::Policy/{}", input.resource)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::apigeeregistry::Policy"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for ApiSpec
// =============================================================================

/// ResourceIdentifier implementation for ApiSpec with ApigeeregistryProjectsLocationsApisVersionsSpecsTagRevisionArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ApigeeregistryProjectsLocationsApisVersionsSpecsTagRevisionArgs>
    for ApiSpec
{
    fn generate_resource_id(
        &self,
        input: &ApigeeregistryProjectsLocationsApisVersionsSpecsTagRevisionArgs,
    ) -> String {
        format!("gcp::apigeeregistry::ApiSpec/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::apigeeregistry::ApiSpec"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for TestIamPermissionsResponse
// =============================================================================

/// ResourceIdentifier implementation for TestIamPermissionsResponse with ApigeeregistryProjectsLocationsApisVersionsSpecsTestIamPermissionsArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ApigeeregistryProjectsLocationsApisVersionsSpecsTestIamPermissionsArgs>
    for TestIamPermissionsResponse
{
    fn generate_resource_id(
        &self,
        input: &ApigeeregistryProjectsLocationsApisVersionsSpecsTestIamPermissionsArgs,
    ) -> String {
        format!(
            "gcp::apigeeregistry::TestIamPermissionsResponse/{}",
            input.resource
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::apigeeregistry::TestIamPermissionsResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Artifact
// =============================================================================

/// ResourceIdentifier implementation for Artifact with ApigeeregistryProjectsLocationsApisVersionsSpecsArtifactsCreateArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ApigeeregistryProjectsLocationsApisVersionsSpecsArtifactsCreateArgs>
    for Artifact
{
    fn generate_resource_id(
        &self,
        input: &ApigeeregistryProjectsLocationsApisVersionsSpecsArtifactsCreateArgs,
    ) -> String {
        format!("gcp::apigeeregistry::Artifact/{}", input.parent)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::apigeeregistry::Artifact"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Empty
// =============================================================================

/// ResourceIdentifier implementation for Empty with ApigeeregistryProjectsLocationsApisVersionsSpecsArtifactsDeleteArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ApigeeregistryProjectsLocationsApisVersionsSpecsArtifactsDeleteArgs>
    for Empty
{
    fn generate_resource_id(
        &self,
        input: &ApigeeregistryProjectsLocationsApisVersionsSpecsArtifactsDeleteArgs,
    ) -> String {
        format!("gcp::apigeeregistry::Empty/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::apigeeregistry::Empty"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Artifact
// =============================================================================

/// ResourceIdentifier implementation for Artifact with ApigeeregistryProjectsLocationsApisVersionsSpecsArtifactsGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ApigeeregistryProjectsLocationsApisVersionsSpecsArtifactsGetArgs>
    for Artifact
{
    fn generate_resource_id(
        &self,
        input: &ApigeeregistryProjectsLocationsApisVersionsSpecsArtifactsGetArgs,
    ) -> String {
        format!("gcp::apigeeregistry::Artifact/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::apigeeregistry::Artifact"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for HttpBody
// =============================================================================

/// ResourceIdentifier implementation for HttpBody with ApigeeregistryProjectsLocationsApisVersionsSpecsArtifactsGetContentsArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ApigeeregistryProjectsLocationsApisVersionsSpecsArtifactsGetContentsArgs>
    for HttpBody
{
    fn generate_resource_id(
        &self,
        input: &ApigeeregistryProjectsLocationsApisVersionsSpecsArtifactsGetContentsArgs,
    ) -> String {
        format!("gcp::apigeeregistry::HttpBody/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::apigeeregistry::HttpBody"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Policy
// =============================================================================

/// ResourceIdentifier implementation for Policy with ApigeeregistryProjectsLocationsApisVersionsSpecsArtifactsGetIamPolicyArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ApigeeregistryProjectsLocationsApisVersionsSpecsArtifactsGetIamPolicyArgs>
    for Policy
{
    fn generate_resource_id(
        &self,
        input: &ApigeeregistryProjectsLocationsApisVersionsSpecsArtifactsGetIamPolicyArgs,
    ) -> String {
        format!("gcp::apigeeregistry::Policy/{}", input.resource)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::apigeeregistry::Policy"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for ListArtifactsResponse
// =============================================================================

/// ResourceIdentifier implementation for ListArtifactsResponse with ApigeeregistryProjectsLocationsApisVersionsSpecsArtifactsListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ApigeeregistryProjectsLocationsApisVersionsSpecsArtifactsListArgs>
    for ListArtifactsResponse
{
    fn generate_resource_id(
        &self,
        input: &ApigeeregistryProjectsLocationsApisVersionsSpecsArtifactsListArgs,
    ) -> String {
        format!(
            "gcp::apigeeregistry::ListArtifactsResponse/{}",
            input.parent
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::apigeeregistry::ListArtifactsResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Artifact
// =============================================================================

/// ResourceIdentifier implementation for Artifact with ApigeeregistryProjectsLocationsApisVersionsSpecsArtifactsReplaceArtifactArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl
    ResourceIdentifier<ApigeeregistryProjectsLocationsApisVersionsSpecsArtifactsReplaceArtifactArgs>
    for Artifact
{
    fn generate_resource_id(
        &self,
        input: &ApigeeregistryProjectsLocationsApisVersionsSpecsArtifactsReplaceArtifactArgs,
    ) -> String {
        format!("gcp::apigeeregistry::Artifact/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::apigeeregistry::Artifact"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Policy
// =============================================================================

/// ResourceIdentifier implementation for Policy with ApigeeregistryProjectsLocationsApisVersionsSpecsArtifactsSetIamPolicyArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ApigeeregistryProjectsLocationsApisVersionsSpecsArtifactsSetIamPolicyArgs>
    for Policy
{
    fn generate_resource_id(
        &self,
        input: &ApigeeregistryProjectsLocationsApisVersionsSpecsArtifactsSetIamPolicyArgs,
    ) -> String {
        format!("gcp::apigeeregistry::Policy/{}", input.resource)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::apigeeregistry::Policy"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for TestIamPermissionsResponse
// =============================================================================

/// ResourceIdentifier implementation for TestIamPermissionsResponse with ApigeeregistryProjectsLocationsApisVersionsSpecsArtifactsTestIamPermissionsArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl
    ResourceIdentifier<
        ApigeeregistryProjectsLocationsApisVersionsSpecsArtifactsTestIamPermissionsArgs,
    > for TestIamPermissionsResponse
{
    fn generate_resource_id(
        &self,
        input: &ApigeeregistryProjectsLocationsApisVersionsSpecsArtifactsTestIamPermissionsArgs,
    ) -> String {
        format!(
            "gcp::apigeeregistry::TestIamPermissionsResponse/{}",
            input.resource
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::apigeeregistry::TestIamPermissionsResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Artifact
// =============================================================================

/// ResourceIdentifier implementation for Artifact with ApigeeregistryProjectsLocationsArtifactsCreateArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ApigeeregistryProjectsLocationsArtifactsCreateArgs> for Artifact {
    fn generate_resource_id(
        &self,
        input: &ApigeeregistryProjectsLocationsArtifactsCreateArgs,
    ) -> String {
        format!("gcp::apigeeregistry::Artifact/{}", input.parent)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::apigeeregistry::Artifact"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Empty
// =============================================================================

/// ResourceIdentifier implementation for Empty with ApigeeregistryProjectsLocationsArtifactsDeleteArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ApigeeregistryProjectsLocationsArtifactsDeleteArgs> for Empty {
    fn generate_resource_id(
        &self,
        input: &ApigeeregistryProjectsLocationsArtifactsDeleteArgs,
    ) -> String {
        format!("gcp::apigeeregistry::Empty/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::apigeeregistry::Empty"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Artifact
// =============================================================================

/// ResourceIdentifier implementation for Artifact with ApigeeregistryProjectsLocationsArtifactsGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ApigeeregistryProjectsLocationsArtifactsGetArgs> for Artifact {
    fn generate_resource_id(
        &self,
        input: &ApigeeregistryProjectsLocationsArtifactsGetArgs,
    ) -> String {
        format!("gcp::apigeeregistry::Artifact/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::apigeeregistry::Artifact"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for HttpBody
// =============================================================================

/// ResourceIdentifier implementation for HttpBody with ApigeeregistryProjectsLocationsArtifactsGetContentsArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ApigeeregistryProjectsLocationsArtifactsGetContentsArgs> for HttpBody {
    fn generate_resource_id(
        &self,
        input: &ApigeeregistryProjectsLocationsArtifactsGetContentsArgs,
    ) -> String {
        format!("gcp::apigeeregistry::HttpBody/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::apigeeregistry::HttpBody"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Policy
// =============================================================================

/// ResourceIdentifier implementation for Policy with ApigeeregistryProjectsLocationsArtifactsGetIamPolicyArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ApigeeregistryProjectsLocationsArtifactsGetIamPolicyArgs> for Policy {
    fn generate_resource_id(
        &self,
        input: &ApigeeregistryProjectsLocationsArtifactsGetIamPolicyArgs,
    ) -> String {
        format!("gcp::apigeeregistry::Policy/{}", input.resource)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::apigeeregistry::Policy"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for ListArtifactsResponse
// =============================================================================

/// ResourceIdentifier implementation for ListArtifactsResponse with ApigeeregistryProjectsLocationsArtifactsListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ApigeeregistryProjectsLocationsArtifactsListArgs>
    for ListArtifactsResponse
{
    fn generate_resource_id(
        &self,
        input: &ApigeeregistryProjectsLocationsArtifactsListArgs,
    ) -> String {
        format!(
            "gcp::apigeeregistry::ListArtifactsResponse/{}",
            input.parent
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::apigeeregistry::ListArtifactsResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Artifact
// =============================================================================

/// ResourceIdentifier implementation for Artifact with ApigeeregistryProjectsLocationsArtifactsReplaceArtifactArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ApigeeregistryProjectsLocationsArtifactsReplaceArtifactArgs> for Artifact {
    fn generate_resource_id(
        &self,
        input: &ApigeeregistryProjectsLocationsArtifactsReplaceArtifactArgs,
    ) -> String {
        format!("gcp::apigeeregistry::Artifact/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::apigeeregistry::Artifact"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Policy
// =============================================================================

/// ResourceIdentifier implementation for Policy with ApigeeregistryProjectsLocationsArtifactsSetIamPolicyArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ApigeeregistryProjectsLocationsArtifactsSetIamPolicyArgs> for Policy {
    fn generate_resource_id(
        &self,
        input: &ApigeeregistryProjectsLocationsArtifactsSetIamPolicyArgs,
    ) -> String {
        format!("gcp::apigeeregistry::Policy/{}", input.resource)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::apigeeregistry::Policy"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for TestIamPermissionsResponse
// =============================================================================

/// ResourceIdentifier implementation for TestIamPermissionsResponse with ApigeeregistryProjectsLocationsArtifactsTestIamPermissionsArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ApigeeregistryProjectsLocationsArtifactsTestIamPermissionsArgs>
    for TestIamPermissionsResponse
{
    fn generate_resource_id(
        &self,
        input: &ApigeeregistryProjectsLocationsArtifactsTestIamPermissionsArgs,
    ) -> String {
        format!(
            "gcp::apigeeregistry::TestIamPermissionsResponse/{}",
            input.resource
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::apigeeregistry::TestIamPermissionsResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Policy
// =============================================================================

/// ResourceIdentifier implementation for Policy with ApigeeregistryProjectsLocationsDocumentsGetIamPolicyArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ApigeeregistryProjectsLocationsDocumentsGetIamPolicyArgs> for Policy {
    fn generate_resource_id(
        &self,
        input: &ApigeeregistryProjectsLocationsDocumentsGetIamPolicyArgs,
    ) -> String {
        format!("gcp::apigeeregistry::Policy/{}", input.resource)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::apigeeregistry::Policy"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Policy
// =============================================================================

/// ResourceIdentifier implementation for Policy with ApigeeregistryProjectsLocationsDocumentsSetIamPolicyArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ApigeeregistryProjectsLocationsDocumentsSetIamPolicyArgs> for Policy {
    fn generate_resource_id(
        &self,
        input: &ApigeeregistryProjectsLocationsDocumentsSetIamPolicyArgs,
    ) -> String {
        format!("gcp::apigeeregistry::Policy/{}", input.resource)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::apigeeregistry::Policy"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for TestIamPermissionsResponse
// =============================================================================

/// ResourceIdentifier implementation for TestIamPermissionsResponse with ApigeeregistryProjectsLocationsDocumentsTestIamPermissionsArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ApigeeregistryProjectsLocationsDocumentsTestIamPermissionsArgs>
    for TestIamPermissionsResponse
{
    fn generate_resource_id(
        &self,
        input: &ApigeeregistryProjectsLocationsDocumentsTestIamPermissionsArgs,
    ) -> String {
        format!(
            "gcp::apigeeregistry::TestIamPermissionsResponse/{}",
            input.resource
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::apigeeregistry::TestIamPermissionsResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with ApigeeregistryProjectsLocationsInstancesCreateArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ApigeeregistryProjectsLocationsInstancesCreateArgs> for Operation {
    fn generate_resource_id(
        &self,
        input: &ApigeeregistryProjectsLocationsInstancesCreateArgs,
    ) -> String {
        format!("gcp::apigeeregistry::Operation/{}", input.parent)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::apigeeregistry::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with ApigeeregistryProjectsLocationsInstancesDeleteArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ApigeeregistryProjectsLocationsInstancesDeleteArgs> for Operation {
    fn generate_resource_id(
        &self,
        input: &ApigeeregistryProjectsLocationsInstancesDeleteArgs,
    ) -> String {
        format!("gcp::apigeeregistry::Operation/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::apigeeregistry::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Instance
// =============================================================================

/// ResourceIdentifier implementation for Instance with ApigeeregistryProjectsLocationsInstancesGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ApigeeregistryProjectsLocationsInstancesGetArgs> for Instance {
    fn generate_resource_id(
        &self,
        input: &ApigeeregistryProjectsLocationsInstancesGetArgs,
    ) -> String {
        format!("gcp::apigeeregistry::Instance/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::apigeeregistry::Instance"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Policy
// =============================================================================

/// ResourceIdentifier implementation for Policy with ApigeeregistryProjectsLocationsInstancesGetIamPolicyArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ApigeeregistryProjectsLocationsInstancesGetIamPolicyArgs> for Policy {
    fn generate_resource_id(
        &self,
        input: &ApigeeregistryProjectsLocationsInstancesGetIamPolicyArgs,
    ) -> String {
        format!("gcp::apigeeregistry::Policy/{}", input.resource)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::apigeeregistry::Policy"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Policy
// =============================================================================

/// ResourceIdentifier implementation for Policy with ApigeeregistryProjectsLocationsInstancesSetIamPolicyArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ApigeeregistryProjectsLocationsInstancesSetIamPolicyArgs> for Policy {
    fn generate_resource_id(
        &self,
        input: &ApigeeregistryProjectsLocationsInstancesSetIamPolicyArgs,
    ) -> String {
        format!("gcp::apigeeregistry::Policy/{}", input.resource)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::apigeeregistry::Policy"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for TestIamPermissionsResponse
// =============================================================================

/// ResourceIdentifier implementation for TestIamPermissionsResponse with ApigeeregistryProjectsLocationsInstancesTestIamPermissionsArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ApigeeregistryProjectsLocationsInstancesTestIamPermissionsArgs>
    for TestIamPermissionsResponse
{
    fn generate_resource_id(
        &self,
        input: &ApigeeregistryProjectsLocationsInstancesTestIamPermissionsArgs,
    ) -> String {
        format!(
            "gcp::apigeeregistry::TestIamPermissionsResponse/{}",
            input.resource
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::apigeeregistry::TestIamPermissionsResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Empty
// =============================================================================

/// ResourceIdentifier implementation for Empty with ApigeeregistryProjectsLocationsOperationsCancelArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ApigeeregistryProjectsLocationsOperationsCancelArgs> for Empty {
    fn generate_resource_id(
        &self,
        input: &ApigeeregistryProjectsLocationsOperationsCancelArgs,
    ) -> String {
        format!("gcp::apigeeregistry::Empty/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::apigeeregistry::Empty"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Empty
// =============================================================================

/// ResourceIdentifier implementation for Empty with ApigeeregistryProjectsLocationsOperationsDeleteArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ApigeeregistryProjectsLocationsOperationsDeleteArgs> for Empty {
    fn generate_resource_id(
        &self,
        input: &ApigeeregistryProjectsLocationsOperationsDeleteArgs,
    ) -> String {
        format!("gcp::apigeeregistry::Empty/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::apigeeregistry::Empty"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with ApigeeregistryProjectsLocationsOperationsGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ApigeeregistryProjectsLocationsOperationsGetArgs> for Operation {
    fn generate_resource_id(
        &self,
        input: &ApigeeregistryProjectsLocationsOperationsGetArgs,
    ) -> String {
        format!("gcp::apigeeregistry::Operation/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::apigeeregistry::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for ListOperationsResponse
// =============================================================================

/// ResourceIdentifier implementation for ListOperationsResponse with ApigeeregistryProjectsLocationsOperationsListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ApigeeregistryProjectsLocationsOperationsListArgs>
    for ListOperationsResponse
{
    fn generate_resource_id(
        &self,
        input: &ApigeeregistryProjectsLocationsOperationsListArgs,
    ) -> String {
        format!("gcp::apigeeregistry::ListOperationsResponse/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::apigeeregistry::ListOperationsResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Policy
// =============================================================================

/// ResourceIdentifier implementation for Policy with ApigeeregistryProjectsLocationsRuntimeGetIamPolicyArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ApigeeregistryProjectsLocationsRuntimeGetIamPolicyArgs> for Policy {
    fn generate_resource_id(
        &self,
        input: &ApigeeregistryProjectsLocationsRuntimeGetIamPolicyArgs,
    ) -> String {
        format!("gcp::apigeeregistry::Policy/{}", input.resource)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::apigeeregistry::Policy"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Policy
// =============================================================================

/// ResourceIdentifier implementation for Policy with ApigeeregistryProjectsLocationsRuntimeSetIamPolicyArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ApigeeregistryProjectsLocationsRuntimeSetIamPolicyArgs> for Policy {
    fn generate_resource_id(
        &self,
        input: &ApigeeregistryProjectsLocationsRuntimeSetIamPolicyArgs,
    ) -> String {
        format!("gcp::apigeeregistry::Policy/{}", input.resource)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::apigeeregistry::Policy"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for TestIamPermissionsResponse
// =============================================================================

/// ResourceIdentifier implementation for TestIamPermissionsResponse with ApigeeregistryProjectsLocationsRuntimeTestIamPermissionsArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ApigeeregistryProjectsLocationsRuntimeTestIamPermissionsArgs>
    for TestIamPermissionsResponse
{
    fn generate_resource_id(
        &self,
        input: &ApigeeregistryProjectsLocationsRuntimeTestIamPermissionsArgs,
    ) -> String {
        format!(
            "gcp::apigeeregistry::TestIamPermissionsResponse/{}",
            input.resource
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::apigeeregistry::TestIamPermissionsResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}
