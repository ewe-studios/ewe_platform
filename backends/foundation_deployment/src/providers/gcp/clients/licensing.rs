//! Auto-generated API clients for Google Cloud Platform - licensing.
//!
//! Generated by `cargo run --bin ewe_platform gen_provider_clients`.
//! DO NOT EDIT MANUALLY.
//!
//! Feature flag: `gcp`


#![cfg(feature = "gcp")]

use crate::providers::gcp::clients::types::*;
use crate::providers::gcp::resources::*;
use foundation_core::valtron::{execute, StreamIterator, StreamIteratorExt};
use foundation_core::wire::simple_http::client::{
    body_reader, ClientRequestBuilder, RequestIntro, SimpleHttpClient, SystemDnsResolver,
};

/// GET apps/licensing/v1/product/{productId}/sku/{skuId}/user/{userId}
/// Revoke a license.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `licensing_license_assignments_delete_execute()` to send, or `licensing_license_assignments_delete` for simplest API.

pub fn licensing_license_assignments_delete_builder(
    client: &SimpleHttpClient,
    productId: &str,
    skuId: &str,
    userId: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comapps/licensing/v1/product/{}/sku/{}/user/{}",
        productId, skuId, userId,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET apps/licensing/v1/product/{productId}/sku/{skuId}/user/{userId}
/// Revoke a license.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `licensing_license_assignments_delete_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from licensing_license_assignments_delete_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn licensing_license_assignments_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Empty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET apps/licensing/v1/product/{productId}/sku/{skuId}/user/{userId}
/// Revoke a license.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `licensing_license_assignments_delete_builder()` + `licensing_license_assignments_delete_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn licensing_license_assignments_delete(
    client: &SimpleHttpClient,
    productId: &str,
    skuId: &str,
    userId: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = licensing_license_assignments_delete_builder(client, productId, skuId, userId)?;
    licensing_license_assignments_delete_execute(builder)
}

/// GET apps/licensing/v1/product/{productId}/sku/{skuId}/user/{userId}
/// Get a specific user's license by product SKU.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `licensing_license_assignments_get_execute()` to send, or `licensing_license_assignments_get` for simplest API.

pub fn licensing_license_assignments_get_builder(
    client: &SimpleHttpClient,
    productId: &str,
    skuId: &str,
    userId: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comapps/licensing/v1/product/{}/sku/{}/user/{}",
        productId, skuId, userId,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET apps/licensing/v1/product/{productId}/sku/{skuId}/user/{userId}
/// Get a specific user's license by product SKU.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `licensing_license_assignments_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from licensing_license_assignments_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn licensing_license_assignments_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<LicenseAssignment>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: LicenseAssignment = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET apps/licensing/v1/product/{productId}/sku/{skuId}/user/{userId}
/// Get a specific user's license by product SKU.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `licensing_license_assignments_get_builder()` + `licensing_license_assignments_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn licensing_license_assignments_get(
    client: &SimpleHttpClient,
    productId: &str,
    skuId: &str,
    userId: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<LicenseAssignment>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = licensing_license_assignments_get_builder(client, productId, skuId, userId)?;
    licensing_license_assignments_get_execute(builder)
}

/// GET apps/licensing/v1/product/{productId}/sku/{skuId}/user
/// Assign a license.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `licensing_license_assignments_insert_execute()` to send, or `licensing_license_assignments_insert` for simplest API.

pub fn licensing_license_assignments_insert_builder(
    client: &SimpleHttpClient,
    productId: &str,
    skuId: &str,
    body: &LicenseAssignmentInsert,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comapps/licensing/v1/product/{}/sku/{}/user",
        productId, skuId,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET apps/licensing/v1/product/{productId}/sku/{skuId}/user
/// Assign a license.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `licensing_license_assignments_insert_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from licensing_license_assignments_insert_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn licensing_license_assignments_insert_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<LicenseAssignment>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: LicenseAssignment = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET apps/licensing/v1/product/{productId}/sku/{skuId}/user
/// Assign a license.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `licensing_license_assignments_insert_builder()` + `licensing_license_assignments_insert_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn licensing_license_assignments_insert(
    client: &SimpleHttpClient,
    productId: &str,
    skuId: &str,
    body: &LicenseAssignmentInsert,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<LicenseAssignment>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = licensing_license_assignments_insert_builder(client, productId, skuId, body)?;
    licensing_license_assignments_insert_execute(builder)
}

/// GET apps/licensing/v1/product/{productId}/users
/// List all users assigned licenses for a specific product SKU.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `licensing_license_assignments_list_for_product_execute()` to send, or `licensing_license_assignments_list_for_product` for simplest API.

pub fn licensing_license_assignments_list_for_product_builder(
    client: &SimpleHttpClient,
    productId: &str,
    customerId: Option<&str>,
    maxResults: Option<i32>,
    pageToken: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comapps/licensing/v1/product/{}/users",
        productId,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "customerId",
            customerId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "maxResults",
            maxResults
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageToken",
            pageToken
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET apps/licensing/v1/product/{productId}/users
/// List all users assigned licenses for a specific product SKU.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `licensing_license_assignments_list_for_product_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from licensing_license_assignments_list_for_product_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn licensing_license_assignments_list_for_product_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<LicenseAssignmentList>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: LicenseAssignmentList = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET apps/licensing/v1/product/{productId}/users
/// List all users assigned licenses for a specific product SKU.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `licensing_license_assignments_list_for_product_builder()` + `licensing_license_assignments_list_for_product_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn licensing_license_assignments_list_for_product(
    client: &SimpleHttpClient,
    productId: &str,
    customerId: Option<&str>,
    maxResults: Option<i32>,
    pageToken: Option<&str>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<LicenseAssignmentList>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = licensing_license_assignments_list_for_product_builder(
        client, productId, customerId, maxResults, pageToken,
    )?;
    licensing_license_assignments_list_for_product_execute(builder)
}

/// GET apps/licensing/v1/product/{productId}/sku/{skuId}/users
/// List all users assigned licenses for a specific product SKU.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `licensing_license_assignments_list_for_product_and_sku_execute()` to send, or `licensing_license_assignments_list_for_product_and_sku` for simplest API.

pub fn licensing_license_assignments_list_for_product_and_sku_builder(
    client: &SimpleHttpClient,
    productId: &str,
    skuId: &str,
    customerId: Option<&str>,
    maxResults: Option<i32>,
    pageToken: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comapps/licensing/v1/product/{}/sku/{}/users",
        productId, skuId,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "customerId",
            customerId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "maxResults",
            maxResults
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageToken",
            pageToken
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET apps/licensing/v1/product/{productId}/sku/{skuId}/users
/// List all users assigned licenses for a specific product SKU.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `licensing_license_assignments_list_for_product_and_sku_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from licensing_license_assignments_list_for_product_and_sku_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn licensing_license_assignments_list_for_product_and_sku_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<LicenseAssignmentList>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: LicenseAssignmentList = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET apps/licensing/v1/product/{productId}/sku/{skuId}/users
/// List all users assigned licenses for a specific product SKU.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `licensing_license_assignments_list_for_product_and_sku_builder()` + `licensing_license_assignments_list_for_product_and_sku_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn licensing_license_assignments_list_for_product_and_sku(
    client: &SimpleHttpClient,
    productId: &str,
    skuId: &str,
    customerId: Option<&str>,
    maxResults: Option<i32>,
    pageToken: Option<&str>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<LicenseAssignmentList>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = licensing_license_assignments_list_for_product_and_sku_builder(
        client, productId, skuId, customerId, maxResults, pageToken,
    )?;
    licensing_license_assignments_list_for_product_and_sku_execute(builder)
}

/// GET apps/licensing/v1/product/{productId}/sku/{skuId}/user/{userId}
/// Reassign a user's product SKU with a different SKU in the same product. This method supports patch semantics.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `licensing_license_assignments_patch_execute()` to send, or `licensing_license_assignments_patch` for simplest API.

pub fn licensing_license_assignments_patch_builder(
    client: &SimpleHttpClient,
    productId: &str,
    skuId: &str,
    userId: &str,
    body: &LicenseAssignment,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comapps/licensing/v1/product/{}/sku/{}/user/{}",
        productId, skuId, userId,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET apps/licensing/v1/product/{productId}/sku/{skuId}/user/{userId}
/// Reassign a user's product SKU with a different SKU in the same product. This method supports patch semantics.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `licensing_license_assignments_patch_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from licensing_license_assignments_patch_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn licensing_license_assignments_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<LicenseAssignment>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: LicenseAssignment = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET apps/licensing/v1/product/{productId}/sku/{skuId}/user/{userId}
/// Reassign a user's product SKU with a different SKU in the same product. This method supports patch semantics.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `licensing_license_assignments_patch_builder()` + `licensing_license_assignments_patch_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn licensing_license_assignments_patch(
    client: &SimpleHttpClient,
    productId: &str,
    skuId: &str,
    userId: &str,
    body: &LicenseAssignment,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<LicenseAssignment>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        licensing_license_assignments_patch_builder(client, productId, skuId, userId, body)?;
    licensing_license_assignments_patch_execute(builder)
}

/// GET apps/licensing/v1/product/{productId}/sku/{skuId}/user/{userId}
/// Reassign a user's product SKU with a different SKU in the same product.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `licensing_license_assignments_update_execute()` to send, or `licensing_license_assignments_update` for simplest API.

pub fn licensing_license_assignments_update_builder(
    client: &SimpleHttpClient,
    productId: &str,
    skuId: &str,
    userId: &str,
    body: &LicenseAssignment,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comapps/licensing/v1/product/{}/sku/{}/user/{}",
        productId, skuId, userId,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET apps/licensing/v1/product/{productId}/sku/{skuId}/user/{userId}
/// Reassign a user's product SKU with a different SKU in the same product.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `licensing_license_assignments_update_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from licensing_license_assignments_update_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn licensing_license_assignments_update_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<LicenseAssignment>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: LicenseAssignment = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET apps/licensing/v1/product/{productId}/sku/{skuId}/user/{userId}
/// Reassign a user's product SKU with a different SKU in the same product.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `licensing_license_assignments_update_builder()` + `licensing_license_assignments_update_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn licensing_license_assignments_update(
    client: &SimpleHttpClient,
    productId: &str,
    skuId: &str,
    userId: &str,
    body: &LicenseAssignment,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<LicenseAssignment>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        licensing_license_assignments_update_builder(client, productId, skuId, userId, body)?;
    licensing_license_assignments_update_execute(builder)
}
