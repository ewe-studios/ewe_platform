//! Auto-generated API clients for gcp/licensing.
//!
//! Generated by `cargo run --bin ewe_platform gen_provider_clients`.
//! DO NOT EDIT MANUALLY.
//!
//! Feature flag: `gcp`

#![cfg(feature = "gcp")]


use crate::providers::gcp::clients::types::*;
use crate::providers::gcp::resources::*;
use foundation_core::valtron::{
    execute, BoxedSendExecutionAction, StreamIterator, StreamIteratorExt, TaskIterator,
    TaskIteratorExt,
};
use foundation_core::wire::simple_http::client::{
    body_reader, ClientRequestBuilder, RequestIntro, SimpleHttpClient, SystemDnsResolver,
};
use foundation_macros::JsonHash;
use serde::Serialize;

/// GET apps/licensing/v1/product/{productId}/sku/{skuId}/user/{userId}
/// Revoke a license.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `licensing_license_assignments_delete_execute()` to send, or `licensing_license_assignments_delete` for simplest API.

pub fn licensing_license_assignments_delete_builder(
    client: &SimpleHttpClient,
    productId: &String,
    skuId: &String,
    userId: &String,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://licensing.googleapis.com/apps/licensing/v1/product/{}/sku/{}/user/{}",
        productId, skuId, userId,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET apps/licensing/v1/product/{productId}/sku/{skuId}/user/{userId}
/// Revoke a license.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `licensing_license_assignments_delete_execute()` or `licensing_license_assignments_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `licensing_license_assignments_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn licensing_license_assignments_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Empty>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Empty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET apps/licensing/v1/product/{productId}/sku/{skuId}/user/{userId}
/// Revoke a license.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `licensing_license_assignments_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `licensing_license_assignments_delete_task()`.
/// For the simplest API, use `licensing_license_assignments_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `licensing_license_assignments_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn licensing_license_assignments_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = licensing_license_assignments_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`licensing_license_assignments_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct LicensingLicenseAssignmentsDeleteArgs {
    /// Path parameter: productId
    pub productId: String,
    /// Path parameter: skuId
    pub skuId: String,
    /// Path parameter: userId
    pub userId: String,
}

/// GET apps/licensing/v1/product/{productId}/sku/{skuId}/user/{userId}
/// Revoke a license.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `licensing_license_assignments_delete_builder()` + `licensing_license_assignments_delete_execute()`.
/// For task-level control, use `licensing_license_assignments_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn licensing_license_assignments_delete(
    client: &SimpleHttpClient,
    args: &LicensingLicenseAssignmentsDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = licensing_license_assignments_delete_builder(
        client,
        &args.productId,
        &args.skuId,
        &args.userId,
    )?;
    licensing_license_assignments_delete_execute(builder)
}

/// GET apps/licensing/v1/product/{productId}/sku/{skuId}/user
/// Assign a license.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `licensing_license_assignments_insert_execute()` to send, or `licensing_license_assignments_insert` for simplest API.

pub fn licensing_license_assignments_insert_builder(
    client: &SimpleHttpClient,
    productId: &String,
    skuId: &String,
    body: &LicenseAssignmentInsert,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://licensing.googleapis.com/apps/licensing/v1/product/{}/sku/{}/user",
        productId, skuId,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET apps/licensing/v1/product/{productId}/sku/{skuId}/user
/// Assign a license.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `licensing_license_assignments_insert_execute()` or `licensing_license_assignments_insert`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `licensing_license_assignments_insert_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn licensing_license_assignments_insert_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<LicenseAssignment>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: LicenseAssignment = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET apps/licensing/v1/product/{productId}/sku/{skuId}/user
/// Assign a license.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `licensing_license_assignments_insert_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `licensing_license_assignments_insert_task()`.
/// For the simplest API, use `licensing_license_assignments_insert()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `licensing_license_assignments_insert_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn licensing_license_assignments_insert_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<LicenseAssignment>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = licensing_license_assignments_insert_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`licensing_license_assignments_insert`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct LicensingLicenseAssignmentsInsertArgs {
    /// Path parameter: productId
    pub productId: String,
    /// Path parameter: skuId
    pub skuId: String,
    /// Request body.
    pub body: LicenseAssignmentInsert,
}

/// GET apps/licensing/v1/product/{productId}/sku/{skuId}/user
/// Assign a license.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `licensing_license_assignments_insert_builder()` + `licensing_license_assignments_insert_execute()`.
/// For task-level control, use `licensing_license_assignments_insert_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn licensing_license_assignments_insert(
    client: &SimpleHttpClient,
    args: &LicensingLicenseAssignmentsInsertArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<LicenseAssignment>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = licensing_license_assignments_insert_builder(
        client,
        &args.productId,
        &args.skuId,
        &args.body,
    )?;
    licensing_license_assignments_insert_execute(builder)
}
