//! Auto-generated API clients for gcp/appengine.
//!
//! Generated by `cargo run --bin ewe_platform gen_provider_clients`.
//! DO NOT EDIT MANUALLY.

use crate::providers::gcp::clients::types::*;
use crate::providers::gcp::resources::*;
use foundation_core::valtron::{execute, StreamIterator, StreamIteratorExt};
use foundation_core::wire::simple_http::client::{
    body_reader, ClientRequestBuilder, RequestIntro, SimpleHttpClient, SystemDnsResolver,
};

/// GET v1/apps
/// Creates an App Engine application for a Google Cloud Platform project. Required fields: id - The ID of the target Cloud Platform project. location - The region (<https://cloud.google.`com/appengine/docs/locations`>) where you want the App Engine application located.For more information about App Engine applications, see Managing Projects, Applications, and Billing (<https://cloud.google.`com/appengine/docs/standard/python/console/`>).
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `appengine_apps_create_execute()` to send, or `appengine_apps_create` for simplest API.

pub fn appengine_apps_create_builder(
    client: &SimpleHttpClient,
    body: &Application,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/apps",);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v1/apps
/// Creates an App Engine application for a Google Cloud Platform project. Required fields: id - The ID of the target Cloud Platform project. location - The region (<https://cloud.google.`com/appengine/docs/locations`>) where you want the App Engine application located.For more information about App Engine applications, see Managing Projects, Applications, and Billing (<https://cloud.google.`com/appengine/docs/standard/python/console/`>).
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `appengine_apps_create_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from appengine_apps_create_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn appengine_apps_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/apps
/// Creates an App Engine application for a Google Cloud Platform project. Required fields: id - The ID of the target Cloud Platform project. location - The region (<https://cloud.google.`com/appengine/docs/locations`>) where you want the App Engine application located.For more information about App Engine applications, see Managing Projects, Applications, and Billing (<https://cloud.google.`com/appengine/docs/standard/python/console/`>).
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `appengine_apps_create_builder()` + `appengine_apps_create_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn appengine_apps_create(
    client: &SimpleHttpClient,
    body: &Application,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = appengine_apps_create_builder(client, body)?;
    appengine_apps_create_execute(builder)
}

/// GET v1/apps/{appsId}
/// Gets information about an application.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `appengine_apps_get_execute()` to send, or `appengine_apps_get` for simplest API.

pub fn appengine_apps_get_builder(
    client: &SimpleHttpClient,
    appsId: &str,
    includeExtraData: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/apps/{}", appsId,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "includeExtraData",
            includeExtraData
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET v1/apps/{appsId}
/// Gets information about an application.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `appengine_apps_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from appengine_apps_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn appengine_apps_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Application>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Application = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/apps/{appsId}
/// Gets information about an application.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `appengine_apps_get_builder()` + `appengine_apps_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn appengine_apps_get(
    client: &SimpleHttpClient,
    appsId: &str,
    includeExtraData: Option<&str>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Application>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = appengine_apps_get_builder(client, appsId, includeExtraData)?;
    appengine_apps_get_execute(builder)
}

/// GET v1/apps/{appsId}:listRuntimes
/// Lists all the available runtimes for the application.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `appengine_apps_list_runtimes_execute()` to send, or `appengine_apps_list_runtimes` for simplest API.

pub fn appengine_apps_list_runtimes_builder(
    client: &SimpleHttpClient,
    appsId: &str,
    environment: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/apps/{}:listRuntimes", appsId,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "environment",
            environment
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET v1/apps/{appsId}:listRuntimes
/// Lists all the available runtimes for the application.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `appengine_apps_list_runtimes_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from appengine_apps_list_runtimes_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn appengine_apps_list_runtimes_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListRuntimesResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListRuntimesResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/apps/{appsId}:listRuntimes
/// Lists all the available runtimes for the application.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `appengine_apps_list_runtimes_builder()` + `appengine_apps_list_runtimes_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn appengine_apps_list_runtimes(
    client: &SimpleHttpClient,
    appsId: &str,
    environment: Option<&str>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListRuntimesResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = appengine_apps_list_runtimes_builder(client, appsId, environment)?;
    appengine_apps_list_runtimes_execute(builder)
}

/// GET v1/apps/{appsId}
/// Updates the specified Application resource. You can update the following fields: auth_domain - Google authentication domain for controlling user access to the application. default_cookie_expiration - Cookie expiration policy for the application. iap - Identity-Aware Proxy properties for the application.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `appengine_apps_patch_execute()` to send, or `appengine_apps_patch` for simplest API.

pub fn appengine_apps_patch_builder(
    client: &SimpleHttpClient,
    appsId: &str,
    updateMask: Option<&str>,
    body: &Application,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/apps/{}", appsId,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "updateMask",
            updateMask
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v1/apps/{appsId}
/// Updates the specified Application resource. You can update the following fields: auth_domain - Google authentication domain for controlling user access to the application. default_cookie_expiration - Cookie expiration policy for the application. iap - Identity-Aware Proxy properties for the application.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `appengine_apps_patch_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from appengine_apps_patch_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn appengine_apps_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/apps/{appsId}
/// Updates the specified Application resource. You can update the following fields: auth_domain - Google authentication domain for controlling user access to the application. default_cookie_expiration - Cookie expiration policy for the application. iap - Identity-Aware Proxy properties for the application.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `appengine_apps_patch_builder()` + `appengine_apps_patch_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn appengine_apps_patch(
    client: &SimpleHttpClient,
    appsId: &str,
    updateMask: Option<&str>,
    body: &Application,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = appengine_apps_patch_builder(client, appsId, updateMask, body)?;
    appengine_apps_patch_execute(builder)
}

/// GET v1/apps/{appsId}:repair
/// Recreates the required App Engine features for the specified App Engine application, for example a Cloud Storage bucket or App Engine service account. Use this method if you receive an error message about a missing feature, for example, Error retrieving the App Engine service account. If you have deleted your App Engine service account, this will not be able to recreate it. Instead, you should attempt to use the IAM undelete API if possible at <https://cloud.google.`com/iam/reference/rest/v1/projects`.serviceA`ccounts/undelete`?apix_params=%7B"name"%3A"projects%2F-%2FserviceAccounts%2Funique_id"%2C"resource"%3A%7B%7D%7D> . If the deletion was recent, the numeric ID can be found in the Cloud Console Activity Log.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `appengine_apps_repair_execute()` to send, or `appengine_apps_repair` for simplest API.

pub fn appengine_apps_repair_builder(
    client: &SimpleHttpClient,
    appsId: &str,
    body: &RepairApplicationRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/apps/{}:repair", appsId,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v1/apps/{appsId}:repair
/// Recreates the required App Engine features for the specified App Engine application, for example a Cloud Storage bucket or App Engine service account. Use this method if you receive an error message about a missing feature, for example, Error retrieving the App Engine service account. If you have deleted your App Engine service account, this will not be able to recreate it. Instead, you should attempt to use the IAM undelete API if possible at <https://cloud.google.`com/iam/reference/rest/v1/projects`.serviceA`ccounts/undelete`?apix_params=%7B"name"%3A"projects%2F-%2FserviceAccounts%2Funique_id"%2C"resource"%3A%7B%7D%7D> . If the deletion was recent, the numeric ID can be found in the Cloud Console Activity Log.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `appengine_apps_repair_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from appengine_apps_repair_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn appengine_apps_repair_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/apps/{appsId}:repair
/// Recreates the required App Engine features for the specified App Engine application, for example a Cloud Storage bucket or App Engine service account. Use this method if you receive an error message about a missing feature, for example, Error retrieving the App Engine service account. If you have deleted your App Engine service account, this will not be able to recreate it. Instead, you should attempt to use the IAM undelete API if possible at <https://cloud.google.`com/iam/reference/rest/v1/projects`.serviceA`ccounts/undelete`?apix_params=%7B"name"%3A"projects%2F-%2FserviceAccounts%2Funique_id"%2C"resource"%3A%7B%7D%7D> . If the deletion was recent, the numeric ID can be found in the Cloud Console Activity Log.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `appengine_apps_repair_builder()` + `appengine_apps_repair_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn appengine_apps_repair(
    client: &SimpleHttpClient,
    appsId: &str,
    body: &RepairApplicationRequest,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = appengine_apps_repair_builder(client, appsId, body)?;
    appengine_apps_repair_execute(builder)
}

/// GET v1/apps/{appsId}/authorizedCertificates
/// Uploads the specified SSL certificate.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `appengine_apps_authorized_certificates_create_execute()` to send, or `appengine_apps_authorized_certificates_create` for simplest API.

pub fn appengine_apps_authorized_certificates_create_builder(
    client: &SimpleHttpClient,
    appsId: &str,
    body: &AuthorizedCertificate,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comv1/apps/{}/authorizedCertificates",
        appsId,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v1/apps/{appsId}/authorizedCertificates
/// Uploads the specified SSL certificate.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `appengine_apps_authorized_certificates_create_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from appengine_apps_authorized_certificates_create_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn appengine_apps_authorized_certificates_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<AuthorizedCertificate>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: AuthorizedCertificate = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/apps/{appsId}/authorizedCertificates
/// Uploads the specified SSL certificate.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `appengine_apps_authorized_certificates_create_builder()` + `appengine_apps_authorized_certificates_create_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn appengine_apps_authorized_certificates_create(
    client: &SimpleHttpClient,
    appsId: &str,
    body: &AuthorizedCertificate,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<AuthorizedCertificate>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = appengine_apps_authorized_certificates_create_builder(client, appsId, body)?;
    appengine_apps_authorized_certificates_create_execute(builder)
}

/// GET v1/apps/{appsId}/authorizedCertificates/{authorizedCertificatesId}
/// Deletes the specified SSL certificate.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `appengine_apps_authorized_certificates_delete_execute()` to send, or `appengine_apps_authorized_certificates_delete` for simplest API.

pub fn appengine_apps_authorized_certificates_delete_builder(
    client: &SimpleHttpClient,
    appsId: &str,
    authorizedCertificatesId: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comv1/apps/{}/authorizedCertificates/{}",
        appsId, authorizedCertificatesId,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/apps/{appsId}/authorizedCertificates/{authorizedCertificatesId}
/// Deletes the specified SSL certificate.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `appengine_apps_authorized_certificates_delete_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from appengine_apps_authorized_certificates_delete_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn appengine_apps_authorized_certificates_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Empty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/apps/{appsId}/authorizedCertificates/{authorizedCertificatesId}
/// Deletes the specified SSL certificate.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `appengine_apps_authorized_certificates_delete_builder()` + `appengine_apps_authorized_certificates_delete_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn appengine_apps_authorized_certificates_delete(
    client: &SimpleHttpClient,
    appsId: &str,
    authorizedCertificatesId: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = appengine_apps_authorized_certificates_delete_builder(
        client,
        appsId,
        authorizedCertificatesId,
    )?;
    appengine_apps_authorized_certificates_delete_execute(builder)
}

/// GET v1/apps/{appsId}/authorizedCertificates/{authorizedCertificatesId}
/// Gets the specified SSL certificate.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `appengine_apps_authorized_certificates_get_execute()` to send, or `appengine_apps_authorized_certificates_get` for simplest API.

pub fn appengine_apps_authorized_certificates_get_builder(
    client: &SimpleHttpClient,
    appsId: &str,
    authorizedCertificatesId: &str,
    view: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comv1/apps/{}/authorizedCertificates/{}",
        appsId, authorizedCertificatesId,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "view",
            view.as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET v1/apps/{appsId}/authorizedCertificates/{authorizedCertificatesId}
/// Gets the specified SSL certificate.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `appengine_apps_authorized_certificates_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from appengine_apps_authorized_certificates_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn appengine_apps_authorized_certificates_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<AuthorizedCertificate>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: AuthorizedCertificate = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/apps/{appsId}/authorizedCertificates/{authorizedCertificatesId}
/// Gets the specified SSL certificate.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `appengine_apps_authorized_certificates_get_builder()` + `appengine_apps_authorized_certificates_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn appengine_apps_authorized_certificates_get(
    client: &SimpleHttpClient,
    appsId: &str,
    authorizedCertificatesId: &str,
    view: Option<&str>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<AuthorizedCertificate>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = appengine_apps_authorized_certificates_get_builder(
        client,
        appsId,
        authorizedCertificatesId,
        view,
    )?;
    appengine_apps_authorized_certificates_get_execute(builder)
}

/// GET v1/apps/{appsId}/authorizedCertificates
/// Lists all SSL certificates the user is authorized to administer.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `appengine_apps_authorized_certificates_list_execute()` to send, or `appengine_apps_authorized_certificates_list` for simplest API.

pub fn appengine_apps_authorized_certificates_list_builder(
    client: &SimpleHttpClient,
    appsId: &str,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
    view: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comv1/apps/{}/authorizedCertificates",
        appsId,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "pageSize",
            pageSize
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageToken",
            pageToken
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "view",
            view.as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET v1/apps/{appsId}/authorizedCertificates
/// Lists all SSL certificates the user is authorized to administer.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `appengine_apps_authorized_certificates_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from appengine_apps_authorized_certificates_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn appengine_apps_authorized_certificates_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ListAuthorizedCertificatesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListAuthorizedCertificatesResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/apps/{appsId}/authorizedCertificates
/// Lists all SSL certificates the user is authorized to administer.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `appengine_apps_authorized_certificates_list_builder()` + `appengine_apps_authorized_certificates_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn appengine_apps_authorized_certificates_list(
    client: &SimpleHttpClient,
    appsId: &str,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
    view: Option<&str>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ListAuthorizedCertificatesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = appengine_apps_authorized_certificates_list_builder(
        client, appsId, pageSize, pageToken, view,
    )?;
    appengine_apps_authorized_certificates_list_execute(builder)
}

/// GET v1/apps/{appsId}/authorizedCertificates/{authorizedCertificatesId}
/// Updates the specified SSL certificate. To renew a certificate and maintain its existing domain mappings, update certificate_data with a new certificate. The new certificate must be applicable to the same domains as the original certificate. The certificate display_name may also be updated.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `appengine_apps_authorized_certificates_patch_execute()` to send, or `appengine_apps_authorized_certificates_patch` for simplest API.

pub fn appengine_apps_authorized_certificates_patch_builder(
    client: &SimpleHttpClient,
    appsId: &str,
    authorizedCertificatesId: &str,
    updateMask: Option<&str>,
    body: &AuthorizedCertificate,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comv1/apps/{}/authorizedCertificates/{}",
        appsId, authorizedCertificatesId,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "updateMask",
            updateMask
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v1/apps/{appsId}/authorizedCertificates/{authorizedCertificatesId}
/// Updates the specified SSL certificate. To renew a certificate and maintain its existing domain mappings, update certificate_data with a new certificate. The new certificate must be applicable to the same domains as the original certificate. The certificate display_name may also be updated.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `appengine_apps_authorized_certificates_patch_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from appengine_apps_authorized_certificates_patch_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn appengine_apps_authorized_certificates_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<AuthorizedCertificate>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: AuthorizedCertificate = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/apps/{appsId}/authorizedCertificates/{authorizedCertificatesId}
/// Updates the specified SSL certificate. To renew a certificate and maintain its existing domain mappings, update certificate_data with a new certificate. The new certificate must be applicable to the same domains as the original certificate. The certificate display_name may also be updated.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `appengine_apps_authorized_certificates_patch_builder()` + `appengine_apps_authorized_certificates_patch_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn appengine_apps_authorized_certificates_patch(
    client: &SimpleHttpClient,
    appsId: &str,
    authorizedCertificatesId: &str,
    updateMask: Option<&str>,
    body: &AuthorizedCertificate,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<AuthorizedCertificate>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = appengine_apps_authorized_certificates_patch_builder(
        client,
        appsId,
        authorizedCertificatesId,
        updateMask,
        body,
    )?;
    appengine_apps_authorized_certificates_patch_execute(builder)
}

/// GET v1/apps/{appsId}/authorizedDomains
/// Lists all domains the user is authorized to administer.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `appengine_apps_authorized_domains_list_execute()` to send, or `appengine_apps_authorized_domains_list` for simplest API.

pub fn appengine_apps_authorized_domains_list_builder(
    client: &SimpleHttpClient,
    appsId: &str,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comv1/apps/{}/authorizedDomains",
        appsId,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "pageSize",
            pageSize
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageToken",
            pageToken
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET v1/apps/{appsId}/authorizedDomains
/// Lists all domains the user is authorized to administer.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `appengine_apps_authorized_domains_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from appengine_apps_authorized_domains_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn appengine_apps_authorized_domains_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ListAuthorizedDomainsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListAuthorizedDomainsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/apps/{appsId}/authorizedDomains
/// Lists all domains the user is authorized to administer.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `appengine_apps_authorized_domains_list_builder()` + `appengine_apps_authorized_domains_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn appengine_apps_authorized_domains_list(
    client: &SimpleHttpClient,
    appsId: &str,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ListAuthorizedDomainsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        appengine_apps_authorized_domains_list_builder(client, appsId, pageSize, pageToken)?;
    appengine_apps_authorized_domains_list_execute(builder)
}

/// GET v1/apps/{appsId}/domainMappings
/// Maps a domain to an application. A user must be authorized to administer a domain in order to map it to an application. For a list of available authorized domains, see AuthorizedDomains.ListAuthorizedDomains.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `appengine_apps_domain_mappings_create_execute()` to send, or `appengine_apps_domain_mappings_create` for simplest API.

pub fn appengine_apps_domain_mappings_create_builder(
    client: &SimpleHttpClient,
    appsId: &str,
    overrideStrategy: Option<&str>,
    body: &DomainMapping,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/apps/{}/domainMappings", appsId,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "overrideStrategy",
            overrideStrategy
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v1/apps/{appsId}/domainMappings
/// Maps a domain to an application. A user must be authorized to administer a domain in order to map it to an application. For a list of available authorized domains, see AuthorizedDomains.ListAuthorizedDomains.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `appengine_apps_domain_mappings_create_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from appengine_apps_domain_mappings_create_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn appengine_apps_domain_mappings_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/apps/{appsId}/domainMappings
/// Maps a domain to an application. A user must be authorized to administer a domain in order to map it to an application. For a list of available authorized domains, see AuthorizedDomains.ListAuthorizedDomains.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `appengine_apps_domain_mappings_create_builder()` + `appengine_apps_domain_mappings_create_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn appengine_apps_domain_mappings_create(
    client: &SimpleHttpClient,
    appsId: &str,
    overrideStrategy: Option<&str>,
    body: &DomainMapping,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        appengine_apps_domain_mappings_create_builder(client, appsId, overrideStrategy, body)?;
    appengine_apps_domain_mappings_create_execute(builder)
}

/// GET v1/apps/{appsId}/domainMappings/{domainMappingsId}
/// Deletes the specified domain mapping. A user must be authorized to administer the associated domain in order to delete a DomainMapping resource.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `appengine_apps_domain_mappings_delete_execute()` to send, or `appengine_apps_domain_mappings_delete` for simplest API.

pub fn appengine_apps_domain_mappings_delete_builder(
    client: &SimpleHttpClient,
    appsId: &str,
    domainMappingsId: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comv1/apps/{}/domainMappings/{}",
        appsId, domainMappingsId,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/apps/{appsId}/domainMappings/{domainMappingsId}
/// Deletes the specified domain mapping. A user must be authorized to administer the associated domain in order to delete a DomainMapping resource.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `appengine_apps_domain_mappings_delete_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from appengine_apps_domain_mappings_delete_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn appengine_apps_domain_mappings_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/apps/{appsId}/domainMappings/{domainMappingsId}
/// Deletes the specified domain mapping. A user must be authorized to administer the associated domain in order to delete a DomainMapping resource.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `appengine_apps_domain_mappings_delete_builder()` + `appengine_apps_domain_mappings_delete_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn appengine_apps_domain_mappings_delete(
    client: &SimpleHttpClient,
    appsId: &str,
    domainMappingsId: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = appengine_apps_domain_mappings_delete_builder(client, appsId, domainMappingsId)?;
    appengine_apps_domain_mappings_delete_execute(builder)
}

/// GET v1/apps/{appsId}/domainMappings/{domainMappingsId}
/// Gets the specified domain mapping.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `appengine_apps_domain_mappings_get_execute()` to send, or `appengine_apps_domain_mappings_get` for simplest API.

pub fn appengine_apps_domain_mappings_get_builder(
    client: &SimpleHttpClient,
    appsId: &str,
    domainMappingsId: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comv1/apps/{}/domainMappings/{}",
        appsId, domainMappingsId,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/apps/{appsId}/domainMappings/{domainMappingsId}
/// Gets the specified domain mapping.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `appengine_apps_domain_mappings_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from appengine_apps_domain_mappings_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn appengine_apps_domain_mappings_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<DomainMapping>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: DomainMapping = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/apps/{appsId}/domainMappings/{domainMappingsId}
/// Gets the specified domain mapping.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `appengine_apps_domain_mappings_get_builder()` + `appengine_apps_domain_mappings_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn appengine_apps_domain_mappings_get(
    client: &SimpleHttpClient,
    appsId: &str,
    domainMappingsId: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<DomainMapping>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = appengine_apps_domain_mappings_get_builder(client, appsId, domainMappingsId)?;
    appengine_apps_domain_mappings_get_execute(builder)
}

/// GET v1/apps/{appsId}/domainMappings
/// Lists the domain mappings on an application.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `appengine_apps_domain_mappings_list_execute()` to send, or `appengine_apps_domain_mappings_list` for simplest API.

pub fn appengine_apps_domain_mappings_list_builder(
    client: &SimpleHttpClient,
    appsId: &str,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/apps/{}/domainMappings", appsId,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "pageSize",
            pageSize
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageToken",
            pageToken
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET v1/apps/{appsId}/domainMappings
/// Lists the domain mappings on an application.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `appengine_apps_domain_mappings_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from appengine_apps_domain_mappings_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn appengine_apps_domain_mappings_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ListDomainMappingsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListDomainMappingsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/apps/{appsId}/domainMappings
/// Lists the domain mappings on an application.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `appengine_apps_domain_mappings_list_builder()` + `appengine_apps_domain_mappings_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn appengine_apps_domain_mappings_list(
    client: &SimpleHttpClient,
    appsId: &str,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ListDomainMappingsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = appengine_apps_domain_mappings_list_builder(client, appsId, pageSize, pageToken)?;
    appengine_apps_domain_mappings_list_execute(builder)
}

/// GET v1/apps/{appsId}/domainMappings/{domainMappingsId}
/// Updates the specified domain mapping. To map an SSL certificate to a domain mapping, update certificate_id to point to an AuthorizedCertificate resource. A user must be authorized to administer the associated domain in order to update a DomainMapping resource.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `appengine_apps_domain_mappings_patch_execute()` to send, or `appengine_apps_domain_mappings_patch` for simplest API.

pub fn appengine_apps_domain_mappings_patch_builder(
    client: &SimpleHttpClient,
    appsId: &str,
    domainMappingsId: &str,
    updateMask: Option<&str>,
    body: &DomainMapping,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comv1/apps/{}/domainMappings/{}",
        appsId, domainMappingsId,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "updateMask",
            updateMask
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v1/apps/{appsId}/domainMappings/{domainMappingsId}
/// Updates the specified domain mapping. To map an SSL certificate to a domain mapping, update certificate_id to point to an AuthorizedCertificate resource. A user must be authorized to administer the associated domain in order to update a DomainMapping resource.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `appengine_apps_domain_mappings_patch_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from appengine_apps_domain_mappings_patch_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn appengine_apps_domain_mappings_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/apps/{appsId}/domainMappings/{domainMappingsId}
/// Updates the specified domain mapping. To map an SSL certificate to a domain mapping, update certificate_id to point to an AuthorizedCertificate resource. A user must be authorized to administer the associated domain in order to update a DomainMapping resource.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `appengine_apps_domain_mappings_patch_builder()` + `appengine_apps_domain_mappings_patch_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn appengine_apps_domain_mappings_patch(
    client: &SimpleHttpClient,
    appsId: &str,
    domainMappingsId: &str,
    updateMask: Option<&str>,
    body: &DomainMapping,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = appengine_apps_domain_mappings_patch_builder(
        client,
        appsId,
        domainMappingsId,
        updateMask,
        body,
    )?;
    appengine_apps_domain_mappings_patch_execute(builder)
}

/// GET v1/apps/{appsId}/firewall/ingressRules:batchUpdate
/// Replaces the entire firewall ruleset in one bulk operation. This overrides and replaces the rules of an existing firewall with the new rules.If the final rule does not match traffic with the '*' wildcard IP range, then an "allow all" rule is explicitly added to the end of the list.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `appengine_apps_firewall_ingress_rules_batch_update_execute()` to send, or `appengine_apps_firewall_ingress_rules_batch_update` for simplest API.

pub fn appengine_apps_firewall_ingress_rules_batch_update_builder(
    client: &SimpleHttpClient,
    appsId: &str,
    body: &BatchUpdateIngressRulesRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comv1/apps/{}/firewall/ingressRules:batchUpdate",
        appsId,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v1/apps/{appsId}/firewall/ingressRules:batchUpdate
/// Replaces the entire firewall ruleset in one bulk operation. This overrides and replaces the rules of an existing firewall with the new rules.If the final rule does not match traffic with the '*' wildcard IP range, then an "allow all" rule is explicitly added to the end of the list.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `appengine_apps_firewall_ingress_rules_batch_update_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from appengine_apps_firewall_ingress_rules_batch_update_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn appengine_apps_firewall_ingress_rules_batch_update_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<BatchUpdateIngressRulesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: BatchUpdateIngressRulesResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/apps/{appsId}/firewall/ingressRules:batchUpdate
/// Replaces the entire firewall ruleset in one bulk operation. This overrides and replaces the rules of an existing firewall with the new rules.If the final rule does not match traffic with the '*' wildcard IP range, then an "allow all" rule is explicitly added to the end of the list.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `appengine_apps_firewall_ingress_rules_batch_update_builder()` + `appengine_apps_firewall_ingress_rules_batch_update_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn appengine_apps_firewall_ingress_rules_batch_update(
    client: &SimpleHttpClient,
    appsId: &str,
    body: &BatchUpdateIngressRulesRequest,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<BatchUpdateIngressRulesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = appengine_apps_firewall_ingress_rules_batch_update_builder(client, appsId, body)?;
    appengine_apps_firewall_ingress_rules_batch_update_execute(builder)
}

/// GET v1/apps/{appsId}/firewall/ingressRules
/// Creates a firewall rule for the application.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `appengine_apps_firewall_ingress_rules_create_execute()` to send, or `appengine_apps_firewall_ingress_rules_create` for simplest API.

pub fn appengine_apps_firewall_ingress_rules_create_builder(
    client: &SimpleHttpClient,
    appsId: &str,
    body: &FirewallRule,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comv1/apps/{}/firewall/ingressRules",
        appsId,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v1/apps/{appsId}/firewall/ingressRules
/// Creates a firewall rule for the application.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `appengine_apps_firewall_ingress_rules_create_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from appengine_apps_firewall_ingress_rules_create_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn appengine_apps_firewall_ingress_rules_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<FirewallRule>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: FirewallRule = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/apps/{appsId}/firewall/ingressRules
/// Creates a firewall rule for the application.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `appengine_apps_firewall_ingress_rules_create_builder()` + `appengine_apps_firewall_ingress_rules_create_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn appengine_apps_firewall_ingress_rules_create(
    client: &SimpleHttpClient,
    appsId: &str,
    body: &FirewallRule,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<FirewallRule>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = appengine_apps_firewall_ingress_rules_create_builder(client, appsId, body)?;
    appengine_apps_firewall_ingress_rules_create_execute(builder)
}

/// GET v1/apps/{appsId}/firewall/ingressRules/{ingressRulesId}
/// Deletes the specified firewall rule.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `appengine_apps_firewall_ingress_rules_delete_execute()` to send, or `appengine_apps_firewall_ingress_rules_delete` for simplest API.

pub fn appengine_apps_firewall_ingress_rules_delete_builder(
    client: &SimpleHttpClient,
    appsId: &str,
    ingressRulesId: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comv1/apps/{}/firewall/ingressRules/{}",
        appsId, ingressRulesId,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/apps/{appsId}/firewall/ingressRules/{ingressRulesId}
/// Deletes the specified firewall rule.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `appengine_apps_firewall_ingress_rules_delete_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from appengine_apps_firewall_ingress_rules_delete_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn appengine_apps_firewall_ingress_rules_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Empty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/apps/{appsId}/firewall/ingressRules/{ingressRulesId}
/// Deletes the specified firewall rule.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `appengine_apps_firewall_ingress_rules_delete_builder()` + `appengine_apps_firewall_ingress_rules_delete_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn appengine_apps_firewall_ingress_rules_delete(
    client: &SimpleHttpClient,
    appsId: &str,
    ingressRulesId: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        appengine_apps_firewall_ingress_rules_delete_builder(client, appsId, ingressRulesId)?;
    appengine_apps_firewall_ingress_rules_delete_execute(builder)
}

/// GET v1/apps/{appsId}/firewall/ingressRules/{ingressRulesId}
/// Gets the specified firewall rule.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `appengine_apps_firewall_ingress_rules_get_execute()` to send, or `appengine_apps_firewall_ingress_rules_get` for simplest API.

pub fn appengine_apps_firewall_ingress_rules_get_builder(
    client: &SimpleHttpClient,
    appsId: &str,
    ingressRulesId: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comv1/apps/{}/firewall/ingressRules/{}",
        appsId, ingressRulesId,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/apps/{appsId}/firewall/ingressRules/{ingressRulesId}
/// Gets the specified firewall rule.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `appengine_apps_firewall_ingress_rules_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from appengine_apps_firewall_ingress_rules_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn appengine_apps_firewall_ingress_rules_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<FirewallRule>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: FirewallRule = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/apps/{appsId}/firewall/ingressRules/{ingressRulesId}
/// Gets the specified firewall rule.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `appengine_apps_firewall_ingress_rules_get_builder()` + `appengine_apps_firewall_ingress_rules_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn appengine_apps_firewall_ingress_rules_get(
    client: &SimpleHttpClient,
    appsId: &str,
    ingressRulesId: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<FirewallRule>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        appengine_apps_firewall_ingress_rules_get_builder(client, appsId, ingressRulesId)?;
    appengine_apps_firewall_ingress_rules_get_execute(builder)
}

/// GET v1/apps/{appsId}/firewall/ingressRules
/// Lists the firewall rules of an application.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `appengine_apps_firewall_ingress_rules_list_execute()` to send, or `appengine_apps_firewall_ingress_rules_list` for simplest API.

pub fn appengine_apps_firewall_ingress_rules_list_builder(
    client: &SimpleHttpClient,
    appsId: &str,
    matchingAddress: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comv1/apps/{}/firewall/ingressRules",
        appsId,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "matchingAddress",
            matchingAddress
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageSize",
            pageSize
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageToken",
            pageToken
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET v1/apps/{appsId}/firewall/ingressRules
/// Lists the firewall rules of an application.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `appengine_apps_firewall_ingress_rules_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from appengine_apps_firewall_ingress_rules_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn appengine_apps_firewall_ingress_rules_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListIngressRulesResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListIngressRulesResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/apps/{appsId}/firewall/ingressRules
/// Lists the firewall rules of an application.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `appengine_apps_firewall_ingress_rules_list_builder()` + `appengine_apps_firewall_ingress_rules_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn appengine_apps_firewall_ingress_rules_list(
    client: &SimpleHttpClient,
    appsId: &str,
    matchingAddress: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListIngressRulesResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = appengine_apps_firewall_ingress_rules_list_builder(
        client,
        appsId,
        matchingAddress,
        pageSize,
        pageToken,
    )?;
    appengine_apps_firewall_ingress_rules_list_execute(builder)
}

/// GET v1/apps/{appsId}/firewall/ingressRules/{ingressRulesId}
/// Updates the specified firewall rule.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `appengine_apps_firewall_ingress_rules_patch_execute()` to send, or `appengine_apps_firewall_ingress_rules_patch` for simplest API.

pub fn appengine_apps_firewall_ingress_rules_patch_builder(
    client: &SimpleHttpClient,
    appsId: &str,
    ingressRulesId: &str,
    updateMask: Option<&str>,
    body: &FirewallRule,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comv1/apps/{}/firewall/ingressRules/{}",
        appsId, ingressRulesId,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "updateMask",
            updateMask
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v1/apps/{appsId}/firewall/ingressRules/{ingressRulesId}
/// Updates the specified firewall rule.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `appengine_apps_firewall_ingress_rules_patch_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from appengine_apps_firewall_ingress_rules_patch_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn appengine_apps_firewall_ingress_rules_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<FirewallRule>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: FirewallRule = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/apps/{appsId}/firewall/ingressRules/{ingressRulesId}
/// Updates the specified firewall rule.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `appengine_apps_firewall_ingress_rules_patch_builder()` + `appengine_apps_firewall_ingress_rules_patch_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn appengine_apps_firewall_ingress_rules_patch(
    client: &SimpleHttpClient,
    appsId: &str,
    ingressRulesId: &str,
    updateMask: Option<&str>,
    body: &FirewallRule,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<FirewallRule>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = appengine_apps_firewall_ingress_rules_patch_builder(
        client,
        appsId,
        ingressRulesId,
        updateMask,
        body,
    )?;
    appengine_apps_firewall_ingress_rules_patch_execute(builder)
}

/// GET v1/apps/{appsId}/locations/{locationsId}
/// Gets information about a location.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `appengine_apps_locations_get_execute()` to send, or `appengine_apps_locations_get` for simplest API.

pub fn appengine_apps_locations_get_builder(
    client: &SimpleHttpClient,
    appsId: &str,
    locationsId: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comv1/apps/{}/locations/{}",
        appsId, locationsId,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/apps/{appsId}/locations/{locationsId}
/// Gets information about a location.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `appengine_apps_locations_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from appengine_apps_locations_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn appengine_apps_locations_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Location>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Location = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/apps/{appsId}/locations/{locationsId}
/// Gets information about a location.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `appengine_apps_locations_get_builder()` + `appengine_apps_locations_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn appengine_apps_locations_get(
    client: &SimpleHttpClient,
    appsId: &str,
    locationsId: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Location>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = appengine_apps_locations_get_builder(client, appsId, locationsId)?;
    appengine_apps_locations_get_execute(builder)
}

/// GET v1/apps/{appsId}/locations
/// Lists information about the supported locations for this service.This method lists locations based on the resource scope provided in the ListLocationsRequest.name field: Global locations: If name is empty, the method lists the public locations available to all projects. Project-specific locations: If name follows the format `projects/{project}`, the method lists locations visible to that specific project. This includes public, private, or other project-specific locations enabled for the project.For `gRPC` and client library implementations, the resource name is passed as the name field. For direct service calls, the resource name is incorporated into the request path based on the specific service implementation and version.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `appengine_apps_locations_list_execute()` to send, or `appengine_apps_locations_list` for simplest API.

pub fn appengine_apps_locations_list_builder(
    client: &SimpleHttpClient,
    appsId: &str,
    extraLocationTypes: Option<&str>,
    filter: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/apps/{}/locations", appsId,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "extraLocationTypes",
            extraLocationTypes
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "filter",
            filter
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageSize",
            pageSize
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageToken",
            pageToken
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET v1/apps/{appsId}/locations
/// Lists information about the supported locations for this service.This method lists locations based on the resource scope provided in the ListLocationsRequest.name field: Global locations: If name is empty, the method lists the public locations available to all projects. Project-specific locations: If name follows the format `projects/{project}`, the method lists locations visible to that specific project. This includes public, private, or other project-specific locations enabled for the project.For `gRPC` and client library implementations, the resource name is passed as the name field. For direct service calls, the resource name is incorporated into the request path based on the specific service implementation and version.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `appengine_apps_locations_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from appengine_apps_locations_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn appengine_apps_locations_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListLocationsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListLocationsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/apps/{appsId}/locations
/// Lists information about the supported locations for this service.This method lists locations based on the resource scope provided in the ListLocationsRequest.name field: Global locations: If name is empty, the method lists the public locations available to all projects. Project-specific locations: If name follows the format `projects/{project}`, the method lists locations visible to that specific project. This includes public, private, or other project-specific locations enabled for the project.For `gRPC` and client library implementations, the resource name is passed as the name field. For direct service calls, the resource name is incorporated into the request path based on the specific service implementation and version.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `appengine_apps_locations_list_builder()` + `appengine_apps_locations_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn appengine_apps_locations_list(
    client: &SimpleHttpClient,
    appsId: &str,
    extraLocationTypes: Option<&str>,
    filter: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListLocationsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = appengine_apps_locations_list_builder(
        client,
        appsId,
        extraLocationTypes,
        filter,
        pageSize,
        pageToken,
    )?;
    appengine_apps_locations_list_execute(builder)
}

/// GET v1/apps/{appsId}/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `appengine_apps_operations_get_execute()` to send, or `appengine_apps_operations_get` for simplest API.

pub fn appengine_apps_operations_get_builder(
    client: &SimpleHttpClient,
    appsId: &str,
    operationsId: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comv1/apps/{}/operations/{}",
        appsId, operationsId,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/apps/{appsId}/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `appengine_apps_operations_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from appengine_apps_operations_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn appengine_apps_operations_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/apps/{appsId}/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `appengine_apps_operations_get_builder()` + `appengine_apps_operations_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn appengine_apps_operations_get(
    client: &SimpleHttpClient,
    appsId: &str,
    operationsId: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = appengine_apps_operations_get_builder(client, appsId, operationsId)?;
    appengine_apps_operations_get_execute(builder)
}

/// GET v1/apps/{appsId}/operations
/// Lists operations that match the specified filter in the request. If the server doesn't support this method, it returns UNIMPLEMENTED.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `appengine_apps_operations_list_execute()` to send, or `appengine_apps_operations_list` for simplest API.

pub fn appengine_apps_operations_list_builder(
    client: &SimpleHttpClient,
    appsId: &str,
    filter: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
    returnPartialSuccess: Option<bool>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/apps/{}/operations", appsId,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "filter",
            filter
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageSize",
            pageSize
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageToken",
            pageToken
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "returnPartialSuccess",
            returnPartialSuccess
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET v1/apps/{appsId}/operations
/// Lists operations that match the specified filter in the request. If the server doesn't support this method, it returns UNIMPLEMENTED.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `appengine_apps_operations_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from appengine_apps_operations_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn appengine_apps_operations_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListOperationsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListOperationsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/apps/{appsId}/operations
/// Lists operations that match the specified filter in the request. If the server doesn't support this method, it returns UNIMPLEMENTED.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `appengine_apps_operations_list_builder()` + `appengine_apps_operations_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn appengine_apps_operations_list(
    client: &SimpleHttpClient,
    appsId: &str,
    filter: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
    returnPartialSuccess: Option<bool>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListOperationsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = appengine_apps_operations_list_builder(
        client,
        appsId,
        filter,
        pageSize,
        pageToken,
        returnPartialSuccess,
    )?;
    appengine_apps_operations_list_execute(builder)
}

/// GET v1/apps/{appsId}/services/{servicesId}
/// Deletes the specified service and all enclosed versions.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `appengine_apps_services_delete_execute()` to send, or `appengine_apps_services_delete` for simplest API.

pub fn appengine_apps_services_delete_builder(
    client: &SimpleHttpClient,
    appsId: &str,
    servicesId: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comv1/apps/{}/services/{}",
        appsId, servicesId,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/apps/{appsId}/services/{servicesId}
/// Deletes the specified service and all enclosed versions.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `appengine_apps_services_delete_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from appengine_apps_services_delete_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn appengine_apps_services_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/apps/{appsId}/services/{servicesId}
/// Deletes the specified service and all enclosed versions.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `appengine_apps_services_delete_builder()` + `appengine_apps_services_delete_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn appengine_apps_services_delete(
    client: &SimpleHttpClient,
    appsId: &str,
    servicesId: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = appengine_apps_services_delete_builder(client, appsId, servicesId)?;
    appengine_apps_services_delete_execute(builder)
}

/// GET v1/apps/{appsId}/services/{servicesId}
/// Gets the current configuration of the specified service.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `appengine_apps_services_get_execute()` to send, or `appengine_apps_services_get` for simplest API.

pub fn appengine_apps_services_get_builder(
    client: &SimpleHttpClient,
    appsId: &str,
    servicesId: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comv1/apps/{}/services/{}",
        appsId, servicesId,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/apps/{appsId}/services/{servicesId}
/// Gets the current configuration of the specified service.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `appengine_apps_services_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from appengine_apps_services_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn appengine_apps_services_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Service>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Service = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/apps/{appsId}/services/{servicesId}
/// Gets the current configuration of the specified service.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `appengine_apps_services_get_builder()` + `appengine_apps_services_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn appengine_apps_services_get(
    client: &SimpleHttpClient,
    appsId: &str,
    servicesId: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Service>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = appengine_apps_services_get_builder(client, appsId, servicesId)?;
    appengine_apps_services_get_execute(builder)
}

/// GET v1/apps/{appsId}/services
/// Lists all the services in the application.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `appengine_apps_services_list_execute()` to send, or `appengine_apps_services_list` for simplest API.

pub fn appengine_apps_services_list_builder(
    client: &SimpleHttpClient,
    appsId: &str,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/apps/{}/services", appsId,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "pageSize",
            pageSize
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageToken",
            pageToken
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET v1/apps/{appsId}/services
/// Lists all the services in the application.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `appengine_apps_services_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from appengine_apps_services_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn appengine_apps_services_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListServicesResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListServicesResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/apps/{appsId}/services
/// Lists all the services in the application.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `appengine_apps_services_list_builder()` + `appengine_apps_services_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn appengine_apps_services_list(
    client: &SimpleHttpClient,
    appsId: &str,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListServicesResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = appengine_apps_services_list_builder(client, appsId, pageSize, pageToken)?;
    appengine_apps_services_list_execute(builder)
}

/// GET v1/apps/{appsId}/services/{servicesId}
/// Updates the configuration of the specified service.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `appengine_apps_services_patch_execute()` to send, or `appengine_apps_services_patch` for simplest API.

pub fn appengine_apps_services_patch_builder(
    client: &SimpleHttpClient,
    appsId: &str,
    servicesId: &str,
    migrateTraffic: Option<bool>,
    updateMask: Option<&str>,
    body: &Service,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comv1/apps/{}/services/{}",
        appsId, servicesId,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "migrateTraffic",
            migrateTraffic
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "updateMask",
            updateMask
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v1/apps/{appsId}/services/{servicesId}
/// Updates the configuration of the specified service.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `appengine_apps_services_patch_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from appengine_apps_services_patch_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn appengine_apps_services_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/apps/{appsId}/services/{servicesId}
/// Updates the configuration of the specified service.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `appengine_apps_services_patch_builder()` + `appengine_apps_services_patch_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn appengine_apps_services_patch(
    client: &SimpleHttpClient,
    appsId: &str,
    servicesId: &str,
    migrateTraffic: Option<bool>,
    updateMask: Option<&str>,
    body: &Service,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = appengine_apps_services_patch_builder(
        client,
        appsId,
        servicesId,
        migrateTraffic,
        updateMask,
        body,
    )?;
    appengine_apps_services_patch_execute(builder)
}

/// GET v1/apps/{appsId}/services/{servicesId}/versions
/// Deploys code and resource files to a new version.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `appengine_apps_services_versions_create_execute()` to send, or `appengine_apps_services_versions_create` for simplest API.

pub fn appengine_apps_services_versions_create_builder(
    client: &SimpleHttpClient,
    appsId: &str,
    servicesId: &str,
    body: &Version,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comv1/apps/{}/services/{}/versions",
        appsId, servicesId,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v1/apps/{appsId}/services/{servicesId}/versions
/// Deploys code and resource files to a new version.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `appengine_apps_services_versions_create_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from appengine_apps_services_versions_create_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn appengine_apps_services_versions_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/apps/{appsId}/services/{servicesId}/versions
/// Deploys code and resource files to a new version.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `appengine_apps_services_versions_create_builder()` + `appengine_apps_services_versions_create_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn appengine_apps_services_versions_create(
    client: &SimpleHttpClient,
    appsId: &str,
    servicesId: &str,
    body: &Version,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        appengine_apps_services_versions_create_builder(client, appsId, servicesId, body)?;
    appengine_apps_services_versions_create_execute(builder)
}

/// GET v1/apps/{appsId}/services/{servicesId}/versions/{versionsId}
/// Deletes an existing Version resource.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `appengine_apps_services_versions_delete_execute()` to send, or `appengine_apps_services_versions_delete` for simplest API.

pub fn appengine_apps_services_versions_delete_builder(
    client: &SimpleHttpClient,
    appsId: &str,
    servicesId: &str,
    versionsId: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comv1/apps/{}/services/{}/versions/{}",
        appsId, servicesId, versionsId,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/apps/{appsId}/services/{servicesId}/versions/{versionsId}
/// Deletes an existing Version resource.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `appengine_apps_services_versions_delete_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from appengine_apps_services_versions_delete_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn appengine_apps_services_versions_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/apps/{appsId}/services/{servicesId}/versions/{versionsId}
/// Deletes an existing Version resource.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `appengine_apps_services_versions_delete_builder()` + `appengine_apps_services_versions_delete_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn appengine_apps_services_versions_delete(
    client: &SimpleHttpClient,
    appsId: &str,
    servicesId: &str,
    versionsId: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        appengine_apps_services_versions_delete_builder(client, appsId, servicesId, versionsId)?;
    appengine_apps_services_versions_delete_execute(builder)
}

/// GET v1/apps/{appsId}/services/{servicesId}/versions/{versionsId}:exportAppImage
/// Exports a user image to Artifact Registry.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `appengine_apps_services_versions_export_app_image_execute()` to send, or `appengine_apps_services_versions_export_app_image` for simplest API.

pub fn appengine_apps_services_versions_export_app_image_builder(
    client: &SimpleHttpClient,
    appsId: &str,
    servicesId: &str,
    versionsId: &str,
    body: &ExportAppImageRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comv1/apps/{}/services/{}/versions/{}:exportAppImage",
        appsId, servicesId, versionsId,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v1/apps/{appsId}/services/{servicesId}/versions/{versionsId}:exportAppImage
/// Exports a user image to Artifact Registry.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `appengine_apps_services_versions_export_app_image_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from appengine_apps_services_versions_export_app_image_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn appengine_apps_services_versions_export_app_image_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/apps/{appsId}/services/{servicesId}/versions/{versionsId}:exportAppImage
/// Exports a user image to Artifact Registry.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `appengine_apps_services_versions_export_app_image_builder()` + `appengine_apps_services_versions_export_app_image_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn appengine_apps_services_versions_export_app_image(
    client: &SimpleHttpClient,
    appsId: &str,
    servicesId: &str,
    versionsId: &str,
    body: &ExportAppImageRequest,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = appengine_apps_services_versions_export_app_image_builder(
        client, appsId, servicesId, versionsId, body,
    )?;
    appengine_apps_services_versions_export_app_image_execute(builder)
}

/// GET v1/apps/{appsId}/services/{servicesId}/versions/{versionsId}
/// Gets the specified Version resource. By default, only a BASIC_VIEW will be returned. Specify the FULL_VIEW parameter to get the full resource.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `appengine_apps_services_versions_get_execute()` to send, or `appengine_apps_services_versions_get` for simplest API.

pub fn appengine_apps_services_versions_get_builder(
    client: &SimpleHttpClient,
    appsId: &str,
    servicesId: &str,
    versionsId: &str,
    view: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comv1/apps/{}/services/{}/versions/{}",
        appsId, servicesId, versionsId,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "view",
            view.as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET v1/apps/{appsId}/services/{servicesId}/versions/{versionsId}
/// Gets the specified Version resource. By default, only a BASIC_VIEW will be returned. Specify the FULL_VIEW parameter to get the full resource.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `appengine_apps_services_versions_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from appengine_apps_services_versions_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn appengine_apps_services_versions_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Version>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Version = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/apps/{appsId}/services/{servicesId}/versions/{versionsId}
/// Gets the specified Version resource. By default, only a BASIC_VIEW will be returned. Specify the FULL_VIEW parameter to get the full resource.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `appengine_apps_services_versions_get_builder()` + `appengine_apps_services_versions_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn appengine_apps_services_versions_get(
    client: &SimpleHttpClient,
    appsId: &str,
    servicesId: &str,
    versionsId: &str,
    view: Option<&str>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Version>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        appengine_apps_services_versions_get_builder(client, appsId, servicesId, versionsId, view)?;
    appengine_apps_services_versions_get_execute(builder)
}

/// GET v1/apps/{appsId}/services/{servicesId}/versions
/// Lists the versions of a service.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `appengine_apps_services_versions_list_execute()` to send, or `appengine_apps_services_versions_list` for simplest API.

pub fn appengine_apps_services_versions_list_builder(
    client: &SimpleHttpClient,
    appsId: &str,
    servicesId: &str,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
    view: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comv1/apps/{}/services/{}/versions",
        appsId, servicesId,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "pageSize",
            pageSize
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageToken",
            pageToken
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "view",
            view.as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET v1/apps/{appsId}/services/{servicesId}/versions
/// Lists the versions of a service.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `appengine_apps_services_versions_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from appengine_apps_services_versions_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn appengine_apps_services_versions_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListVersionsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListVersionsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/apps/{appsId}/services/{servicesId}/versions
/// Lists the versions of a service.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `appengine_apps_services_versions_list_builder()` + `appengine_apps_services_versions_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn appengine_apps_services_versions_list(
    client: &SimpleHttpClient,
    appsId: &str,
    servicesId: &str,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
    view: Option<&str>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListVersionsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = appengine_apps_services_versions_list_builder(
        client, appsId, servicesId, pageSize, pageToken, view,
    )?;
    appengine_apps_services_versions_list_execute(builder)
}

/// GET v1/apps/{appsId}/services/{servicesId}/versions/{versionsId}
/// Updates the specified Version resource. You can specify the following fields depending on the App Engine environment and type of scaling that the version resource uses:Standard environment instance_class (<https://cloud.google.`com/appengine/docs/admin-api/reference/rest/v1/apps`.services.versions#Version.FIELDS.instance_class>)automatic scaling in the standard environment: automatic_scaling.min_idle_instances (<https://cloud.google.`com/appengine/docs/admin-api/reference/rest/v1/apps`.services.versions#Version.FIELDS.automatic_scaling>) automatic_scaling.max_idle_instances (<https://cloud.google.`com/appengine/docs/admin-api/reference/rest/v1/apps`.services.versions#Version.FIELDS.automatic_scaling>) `automaticScaling`.standard_scheduler_settings.max_instances (<https://cloud.google.`com/appengine/docs/admin-api/reference/rest/v1/apps`.services.versions#StandardSchedulerSettings>) `automaticScaling`.standard_scheduler_settings.min_instances (<https://cloud.google.`com/appengine/docs/admin-api/reference/rest/v1/apps`.services.versions#StandardSchedulerSettings>) `automaticScaling`.standard_scheduler_settings.target_cpu_utilization (<https://cloud.google.`com/appengine/docs/admin-api/reference/rest/v1/apps`.services.versions#StandardSchedulerSettings>) `automaticScaling`.standard_scheduler_settings.target_throughput_utilization (<https://cloud.google.`com/appengine/docs/admin-api/reference/rest/v1/apps`.services.versions#StandardSchedulerSettings>)basic scaling or manual scaling in the standard environment: serving_status (<https://cloud.google.`com/appengine/docs/admin-api/reference/rest/v1/apps`.services.versions#Version.FIELDS.serving_status>) manual_scaling.instances (<https://cloud.google.`com/appengine/docs/admin-api/reference/rest/v1/apps`.services.versions#manualscaling>)Flexible environment serving_status (<https://cloud.google.`com/appengine/docs/admin-api/reference/rest/v1/apps`.services.versions#Version.FIELDS.serving_status>)automatic scaling in the flexible environment: automatic_scaling.min_total_instances (<https://cloud.google.`com/appengine/docs/admin-api/reference/rest/v1/apps`.services.versions#Version.FIELDS.automatic_scaling>) automatic_scaling.max_total_instances (<https://cloud.google.`com/appengine/docs/admin-api/reference/rest/v1/apps`.services.versions#Version.FIELDS.automatic_scaling>) automatic_scaling.cool_down_period_sec (<https://cloud.google.`com/appengine/docs/admin-api/reference/rest/v1/apps`.services.versions#Version.FIELDS.automatic_scaling>) automatic_scaling.cpu_utilization.target_utilization (<https://cloud.google.`com/appengine/docs/admin-api/reference/rest/v1/apps`.services.versions#Version.FIELDS.automatic_scaling>)manual scaling in the flexible environment: manual_scaling.instances (<https://cloud.google.`com/appengine/docs/admin-api/reference/rest/v1/apps`.services.versions#manualscaling>)
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `appengine_apps_services_versions_patch_execute()` to send, or `appengine_apps_services_versions_patch` for simplest API.

pub fn appengine_apps_services_versions_patch_builder(
    client: &SimpleHttpClient,
    appsId: &str,
    servicesId: &str,
    versionsId: &str,
    updateMask: Option<&str>,
    body: &Version,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comv1/apps/{}/services/{}/versions/{}",
        appsId, servicesId, versionsId,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "updateMask",
            updateMask
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v1/apps/{appsId}/services/{servicesId}/versions/{versionsId}
/// Updates the specified Version resource. You can specify the following fields depending on the App Engine environment and type of scaling that the version resource uses:Standard environment instance_class (<https://cloud.google.`com/appengine/docs/admin-api/reference/rest/v1/apps`.services.versions#Version.FIELDS.instance_class>)automatic scaling in the standard environment: automatic_scaling.min_idle_instances (<https://cloud.google.`com/appengine/docs/admin-api/reference/rest/v1/apps`.services.versions#Version.FIELDS.automatic_scaling>) automatic_scaling.max_idle_instances (<https://cloud.google.`com/appengine/docs/admin-api/reference/rest/v1/apps`.services.versions#Version.FIELDS.automatic_scaling>) `automaticScaling`.standard_scheduler_settings.max_instances (<https://cloud.google.`com/appengine/docs/admin-api/reference/rest/v1/apps`.services.versions#StandardSchedulerSettings>) `automaticScaling`.standard_scheduler_settings.min_instances (<https://cloud.google.`com/appengine/docs/admin-api/reference/rest/v1/apps`.services.versions#StandardSchedulerSettings>) `automaticScaling`.standard_scheduler_settings.target_cpu_utilization (<https://cloud.google.`com/appengine/docs/admin-api/reference/rest/v1/apps`.services.versions#StandardSchedulerSettings>) `automaticScaling`.standard_scheduler_settings.target_throughput_utilization (<https://cloud.google.`com/appengine/docs/admin-api/reference/rest/v1/apps`.services.versions#StandardSchedulerSettings>)basic scaling or manual scaling in the standard environment: serving_status (<https://cloud.google.`com/appengine/docs/admin-api/reference/rest/v1/apps`.services.versions#Version.FIELDS.serving_status>) manual_scaling.instances (<https://cloud.google.`com/appengine/docs/admin-api/reference/rest/v1/apps`.services.versions#manualscaling>)Flexible environment serving_status (<https://cloud.google.`com/appengine/docs/admin-api/reference/rest/v1/apps`.services.versions#Version.FIELDS.serving_status>)automatic scaling in the flexible environment: automatic_scaling.min_total_instances (<https://cloud.google.`com/appengine/docs/admin-api/reference/rest/v1/apps`.services.versions#Version.FIELDS.automatic_scaling>) automatic_scaling.max_total_instances (<https://cloud.google.`com/appengine/docs/admin-api/reference/rest/v1/apps`.services.versions#Version.FIELDS.automatic_scaling>) automatic_scaling.cool_down_period_sec (<https://cloud.google.`com/appengine/docs/admin-api/reference/rest/v1/apps`.services.versions#Version.FIELDS.automatic_scaling>) automatic_scaling.cpu_utilization.target_utilization (<https://cloud.google.`com/appengine/docs/admin-api/reference/rest/v1/apps`.services.versions#Version.FIELDS.automatic_scaling>)manual scaling in the flexible environment: manual_scaling.instances (<https://cloud.google.`com/appengine/docs/admin-api/reference/rest/v1/apps`.services.versions#manualscaling>)
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `appengine_apps_services_versions_patch_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from appengine_apps_services_versions_patch_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn appengine_apps_services_versions_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/apps/{appsId}/services/{servicesId}/versions/{versionsId}
/// Updates the specified Version resource. You can specify the following fields depending on the App Engine environment and type of scaling that the version resource uses:Standard environment instance_class (<https://cloud.google.`com/appengine/docs/admin-api/reference/rest/v1/apps`.services.versions#Version.FIELDS.instance_class>)automatic scaling in the standard environment: automatic_scaling.min_idle_instances (<https://cloud.google.`com/appengine/docs/admin-api/reference/rest/v1/apps`.services.versions#Version.FIELDS.automatic_scaling>) automatic_scaling.max_idle_instances (<https://cloud.google.`com/appengine/docs/admin-api/reference/rest/v1/apps`.services.versions#Version.FIELDS.automatic_scaling>) `automaticScaling`.standard_scheduler_settings.max_instances (<https://cloud.google.`com/appengine/docs/admin-api/reference/rest/v1/apps`.services.versions#StandardSchedulerSettings>) `automaticScaling`.standard_scheduler_settings.min_instances (<https://cloud.google.`com/appengine/docs/admin-api/reference/rest/v1/apps`.services.versions#StandardSchedulerSettings>) `automaticScaling`.standard_scheduler_settings.target_cpu_utilization (<https://cloud.google.`com/appengine/docs/admin-api/reference/rest/v1/apps`.services.versions#StandardSchedulerSettings>) `automaticScaling`.standard_scheduler_settings.target_throughput_utilization (<https://cloud.google.`com/appengine/docs/admin-api/reference/rest/v1/apps`.services.versions#StandardSchedulerSettings>)basic scaling or manual scaling in the standard environment: serving_status (<https://cloud.google.`com/appengine/docs/admin-api/reference/rest/v1/apps`.services.versions#Version.FIELDS.serving_status>) manual_scaling.instances (<https://cloud.google.`com/appengine/docs/admin-api/reference/rest/v1/apps`.services.versions#manualscaling>)Flexible environment serving_status (<https://cloud.google.`com/appengine/docs/admin-api/reference/rest/v1/apps`.services.versions#Version.FIELDS.serving_status>)automatic scaling in the flexible environment: automatic_scaling.min_total_instances (<https://cloud.google.`com/appengine/docs/admin-api/reference/rest/v1/apps`.services.versions#Version.FIELDS.automatic_scaling>) automatic_scaling.max_total_instances (<https://cloud.google.`com/appengine/docs/admin-api/reference/rest/v1/apps`.services.versions#Version.FIELDS.automatic_scaling>) automatic_scaling.cool_down_period_sec (<https://cloud.google.`com/appengine/docs/admin-api/reference/rest/v1/apps`.services.versions#Version.FIELDS.automatic_scaling>) automatic_scaling.cpu_utilization.target_utilization (<https://cloud.google.`com/appengine/docs/admin-api/reference/rest/v1/apps`.services.versions#Version.FIELDS.automatic_scaling>)manual scaling in the flexible environment: manual_scaling.instances (<https://cloud.google.`com/appengine/docs/admin-api/reference/rest/v1/apps`.services.versions#manualscaling>)
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `appengine_apps_services_versions_patch_builder()` + `appengine_apps_services_versions_patch_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn appengine_apps_services_versions_patch(
    client: &SimpleHttpClient,
    appsId: &str,
    servicesId: &str,
    versionsId: &str,
    updateMask: Option<&str>,
    body: &Version,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = appengine_apps_services_versions_patch_builder(
        client, appsId, servicesId, versionsId, updateMask, body,
    )?;
    appengine_apps_services_versions_patch_execute(builder)
}

/// GET v1/apps/{appsId}/services/{servicesId}/versions/{versionsId}/instances/{instancesId}:debug
/// Enables debugging on a VM instance. This allows you to use the SSH command to connect to the virtual machine where the instance lives. While in "debug mode", the instance continues to serve live traffic. You should delete the instance when you are done debugging and then allow the system to take over and determine if another instance should be started.Only applicable for instances in App Engine flexible environment.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `appengine_apps_services_versions_instances_debug_execute()` to send, or `appengine_apps_services_versions_instances_debug` for simplest API.

pub fn appengine_apps_services_versions_instances_debug_builder(
    client: &SimpleHttpClient,
    appsId: &str,
    instancesId: &str,
    servicesId: &str,
    versionsId: &str,
    body: &DebugInstanceRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comv1/apps/{}/services/{}/versions/{}/instances/{}:debug",
        appsId, instancesId, servicesId, versionsId,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v1/apps/{appsId}/services/{servicesId}/versions/{versionsId}/instances/{instancesId}:debug
/// Enables debugging on a VM instance. This allows you to use the SSH command to connect to the virtual machine where the instance lives. While in "debug mode", the instance continues to serve live traffic. You should delete the instance when you are done debugging and then allow the system to take over and determine if another instance should be started.Only applicable for instances in App Engine flexible environment.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `appengine_apps_services_versions_instances_debug_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from appengine_apps_services_versions_instances_debug_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn appengine_apps_services_versions_instances_debug_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/apps/{appsId}/services/{servicesId}/versions/{versionsId}/instances/{instancesId}:debug
/// Enables debugging on a VM instance. This allows you to use the SSH command to connect to the virtual machine where the instance lives. While in "debug mode", the instance continues to serve live traffic. You should delete the instance when you are done debugging and then allow the system to take over and determine if another instance should be started.Only applicable for instances in App Engine flexible environment.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `appengine_apps_services_versions_instances_debug_builder()` + `appengine_apps_services_versions_instances_debug_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn appengine_apps_services_versions_instances_debug(
    client: &SimpleHttpClient,
    appsId: &str,
    instancesId: &str,
    servicesId: &str,
    versionsId: &str,
    body: &DebugInstanceRequest,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = appengine_apps_services_versions_instances_debug_builder(
        client,
        appsId,
        instancesId,
        servicesId,
        versionsId,
        body,
    )?;
    appengine_apps_services_versions_instances_debug_execute(builder)
}

/// GET v1/apps/{appsId}/services/{servicesId}/versions/{versionsId}/instances/{instancesId}
/// Stops a running instance.The instance might be automatically recreated based on the scaling settings of the version. For more information, see "How Instances are Managed" (standard environment (<https://cloud.google.`com/appengine/docs/standard/python/how-instances-are-managed`>) | flexible environment (<https://cloud.google.`com/appengine/docs/flexible/python/how-instances-are-managed`>)).To ensure that instances are not re-created and avoid getting billed, you can stop all instances within the target version by changing the serving status of the version to STOPPED with the apps.services.versions.patch (<https://cloud.google.`com/appengine/docs/admin-api/reference/rest/v1/apps`.services.`versions/patch`>) method.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `appengine_apps_services_versions_instances_delete_execute()` to send, or `appengine_apps_services_versions_instances_delete` for simplest API.

pub fn appengine_apps_services_versions_instances_delete_builder(
    client: &SimpleHttpClient,
    appsId: &str,
    instancesId: &str,
    servicesId: &str,
    versionsId: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comv1/apps/{}/services/{}/versions/{}/instances/{}",
        appsId, instancesId, servicesId, versionsId,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/apps/{appsId}/services/{servicesId}/versions/{versionsId}/instances/{instancesId}
/// Stops a running instance.The instance might be automatically recreated based on the scaling settings of the version. For more information, see "How Instances are Managed" (standard environment (<https://cloud.google.`com/appengine/docs/standard/python/how-instances-are-managed`>) | flexible environment (<https://cloud.google.`com/appengine/docs/flexible/python/how-instances-are-managed`>)).To ensure that instances are not re-created and avoid getting billed, you can stop all instances within the target version by changing the serving status of the version to STOPPED with the apps.services.versions.patch (<https://cloud.google.`com/appengine/docs/admin-api/reference/rest/v1/apps`.services.`versions/patch`>) method.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `appengine_apps_services_versions_instances_delete_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from appengine_apps_services_versions_instances_delete_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn appengine_apps_services_versions_instances_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/apps/{appsId}/services/{servicesId}/versions/{versionsId}/instances/{instancesId}
/// Stops a running instance.The instance might be automatically recreated based on the scaling settings of the version. For more information, see "How Instances are Managed" (standard environment (<https://cloud.google.`com/appengine/docs/standard/python/how-instances-are-managed`>) | flexible environment (<https://cloud.google.`com/appengine/docs/flexible/python/how-instances-are-managed`>)).To ensure that instances are not re-created and avoid getting billed, you can stop all instances within the target version by changing the serving status of the version to STOPPED with the apps.services.versions.patch (<https://cloud.google.`com/appengine/docs/admin-api/reference/rest/v1/apps`.services.`versions/patch`>) method.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `appengine_apps_services_versions_instances_delete_builder()` + `appengine_apps_services_versions_instances_delete_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn appengine_apps_services_versions_instances_delete(
    client: &SimpleHttpClient,
    appsId: &str,
    instancesId: &str,
    servicesId: &str,
    versionsId: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = appengine_apps_services_versions_instances_delete_builder(
        client,
        appsId,
        instancesId,
        servicesId,
        versionsId,
    )?;
    appengine_apps_services_versions_instances_delete_execute(builder)
}

/// GET v1/apps/{appsId}/services/{servicesId}/versions/{versionsId}/instances/{instancesId}
/// Gets instance information.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `appengine_apps_services_versions_instances_get_execute()` to send, or `appengine_apps_services_versions_instances_get` for simplest API.

pub fn appengine_apps_services_versions_instances_get_builder(
    client: &SimpleHttpClient,
    appsId: &str,
    instancesId: &str,
    servicesId: &str,
    versionsId: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comv1/apps/{}/services/{}/versions/{}/instances/{}",
        appsId, instancesId, servicesId, versionsId,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/apps/{appsId}/services/{servicesId}/versions/{versionsId}/instances/{instancesId}
/// Gets instance information.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `appengine_apps_services_versions_instances_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from appengine_apps_services_versions_instances_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn appengine_apps_services_versions_instances_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Instance>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Instance = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/apps/{appsId}/services/{servicesId}/versions/{versionsId}/instances/{instancesId}
/// Gets instance information.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `appengine_apps_services_versions_instances_get_builder()` + `appengine_apps_services_versions_instances_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn appengine_apps_services_versions_instances_get(
    client: &SimpleHttpClient,
    appsId: &str,
    instancesId: &str,
    servicesId: &str,
    versionsId: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Instance>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = appengine_apps_services_versions_instances_get_builder(
        client,
        appsId,
        instancesId,
        servicesId,
        versionsId,
    )?;
    appengine_apps_services_versions_instances_get_execute(builder)
}

/// GET v1/apps/{appsId}/services/{servicesId}/versions/{versionsId}/instances
/// Lists the instances of a version.Tip: To aggregate details about instances over time, see the Stackdriver Monitoring API (<https://cloud.google.`com/monitoring/api/ref_v3/rest/v3/projects`.timeS`eries/list`>).
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `appengine_apps_services_versions_instances_list_execute()` to send, or `appengine_apps_services_versions_instances_list` for simplest API.

pub fn appengine_apps_services_versions_instances_list_builder(
    client: &SimpleHttpClient,
    appsId: &str,
    servicesId: &str,
    versionsId: &str,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comv1/apps/{}/services/{}/versions/{}/instances",
        appsId, servicesId, versionsId,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "pageSize",
            pageSize
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageToken",
            pageToken
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET v1/apps/{appsId}/services/{servicesId}/versions/{versionsId}/instances
/// Lists the instances of a version.Tip: To aggregate details about instances over time, see the Stackdriver Monitoring API (<https://cloud.google.`com/monitoring/api/ref_v3/rest/v3/projects`.timeS`eries/list`>).
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `appengine_apps_services_versions_instances_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from appengine_apps_services_versions_instances_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn appengine_apps_services_versions_instances_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListInstancesResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListInstancesResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/apps/{appsId}/services/{servicesId}/versions/{versionsId}/instances
/// Lists the instances of a version.Tip: To aggregate details about instances over time, see the Stackdriver Monitoring API (<https://cloud.google.`com/monitoring/api/ref_v3/rest/v3/projects`.timeS`eries/list`>).
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `appengine_apps_services_versions_instances_list_builder()` + `appengine_apps_services_versions_instances_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn appengine_apps_services_versions_instances_list(
    client: &SimpleHttpClient,
    appsId: &str,
    servicesId: &str,
    versionsId: &str,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListInstancesResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = appengine_apps_services_versions_instances_list_builder(
        client, appsId, servicesId, versionsId, pageSize, pageToken,
    )?;
    appengine_apps_services_versions_instances_list_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/applications/{applicationsId}
/// Updates the specified Application resource. You can update the following fields: auth_domain - Google authentication domain for controlling user access to the application. default_cookie_expiration - Cookie expiration policy for the application. iap - Identity-Aware Proxy properties for the application.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `appengine_projects_locations_applications_patch_execute()` to send, or `appengine_projects_locations_applications_patch` for simplest API.

pub fn appengine_projects_locations_applications_patch_builder(
    client: &SimpleHttpClient,
    applicationsId: &str,
    locationsId: &str,
    projectsId: &str,
    updateMask: Option<&str>,
    body: &Application,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comv1/projects/{}/locations/{}/applications/{}",
        applicationsId, locationsId, projectsId,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "updateMask",
            updateMask
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/applications/{applicationsId}
/// Updates the specified Application resource. You can update the following fields: auth_domain - Google authentication domain for controlling user access to the application. default_cookie_expiration - Cookie expiration policy for the application. iap - Identity-Aware Proxy properties for the application.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `appengine_projects_locations_applications_patch_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from appengine_projects_locations_applications_patch_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn appengine_projects_locations_applications_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/applications/{applicationsId}
/// Updates the specified Application resource. You can update the following fields: auth_domain - Google authentication domain for controlling user access to the application. default_cookie_expiration - Cookie expiration policy for the application. iap - Identity-Aware Proxy properties for the application.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `appengine_projects_locations_applications_patch_builder()` + `appengine_projects_locations_applications_patch_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn appengine_projects_locations_applications_patch(
    client: &SimpleHttpClient,
    applicationsId: &str,
    locationsId: &str,
    projectsId: &str,
    updateMask: Option<&str>,
    body: &Application,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = appengine_projects_locations_applications_patch_builder(
        client,
        applicationsId,
        locationsId,
        projectsId,
        updateMask,
        body,
    )?;
    appengine_projects_locations_applications_patch_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/applications/{applicationsId}/authorizedCertificates
/// Uploads the specified SSL certificate.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `appengine_projects_locations_applications_authorized_certificates_create_execute()` to send, or `appengine_projects_locations_applications_authorized_certificates_create` for simplest API.

pub fn appengine_projects_locations_applications_authorized_certificates_create_builder(
    client: &SimpleHttpClient,
    applicationsId: &str,
    locationsId: &str,
    projectsId: &str,
    body: &AuthorizedCertificate,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comv1/projects/{}/locations/{}/applications/{}/authorizedCertificates",
        applicationsId, locationsId, projectsId,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/applications/{applicationsId}/authorizedCertificates
/// Uploads the specified SSL certificate.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `appengine_projects_locations_applications_authorized_certificates_create_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from appengine_projects_locations_applications_authorized_certificates_create_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn appengine_projects_locations_applications_authorized_certificates_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<AuthorizedCertificate>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: AuthorizedCertificate = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/applications/{applicationsId}/authorizedCertificates
/// Uploads the specified SSL certificate.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `appengine_projects_locations_applications_authorized_certificates_create_builder()` + `appengine_projects_locations_applications_authorized_certificates_create_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn appengine_projects_locations_applications_authorized_certificates_create(
    client: &SimpleHttpClient,
    applicationsId: &str,
    locationsId: &str,
    projectsId: &str,
    body: &AuthorizedCertificate,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<AuthorizedCertificate>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = appengine_projects_locations_applications_authorized_certificates_create_builder(
        client,
        applicationsId,
        locationsId,
        projectsId,
        body,
    )?;
    appengine_projects_locations_applications_authorized_certificates_create_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/applications/{applicationsId}/authorizedCertificates/{authorizedCertificatesId}
/// Deletes the specified SSL certificate.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `appengine_projects_locations_applications_authorized_certificates_delete_execute()` to send, or `appengine_projects_locations_applications_authorized_certificates_delete` for simplest API.

pub fn appengine_projects_locations_applications_authorized_certificates_delete_builder(
    client: &SimpleHttpClient,
    applicationsId: &str,
    authorizedCertificatesId: &str,
    locationsId: &str,
    projectsId: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comv1/projects/{}/locations/{}/applications/{}/authorizedCertificates/{}",
        applicationsId,
        authorizedCertificatesId,
        locationsId,
        projectsId,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/applications/{applicationsId}/authorizedCertificates/{authorizedCertificatesId}
/// Deletes the specified SSL certificate.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `appengine_projects_locations_applications_authorized_certificates_delete_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from appengine_projects_locations_applications_authorized_certificates_delete_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn appengine_projects_locations_applications_authorized_certificates_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Empty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/applications/{applicationsId}/authorizedCertificates/{authorizedCertificatesId}
/// Deletes the specified SSL certificate.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `appengine_projects_locations_applications_authorized_certificates_delete_builder()` + `appengine_projects_locations_applications_authorized_certificates_delete_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn appengine_projects_locations_applications_authorized_certificates_delete(
    client: &SimpleHttpClient,
    applicationsId: &str,
    authorizedCertificatesId: &str,
    locationsId: &str,
    projectsId: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = appengine_projects_locations_applications_authorized_certificates_delete_builder(
        client,
        applicationsId,
        authorizedCertificatesId,
        locationsId,
        projectsId,
    )?;
    appengine_projects_locations_applications_authorized_certificates_delete_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/applications/{applicationsId}/authorizedCertificates/{authorizedCertificatesId}
/// Gets the specified SSL certificate.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `appengine_projects_locations_applications_authorized_certificates_get_execute()` to send, or `appengine_projects_locations_applications_authorized_certificates_get` for simplest API.

pub fn appengine_projects_locations_applications_authorized_certificates_get_builder(
    client: &SimpleHttpClient,
    applicationsId: &str,
    authorizedCertificatesId: &str,
    locationsId: &str,
    projectsId: &str,
    view: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comv1/projects/{}/locations/{}/applications/{}/authorizedCertificates/{}",
        applicationsId,
        authorizedCertificatesId,
        locationsId,
        projectsId,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "view",
            view.as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/applications/{applicationsId}/authorizedCertificates/{authorizedCertificatesId}
/// Gets the specified SSL certificate.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `appengine_projects_locations_applications_authorized_certificates_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from appengine_projects_locations_applications_authorized_certificates_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn appengine_projects_locations_applications_authorized_certificates_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<AuthorizedCertificate>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: AuthorizedCertificate = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/applications/{applicationsId}/authorizedCertificates/{authorizedCertificatesId}
/// Gets the specified SSL certificate.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `appengine_projects_locations_applications_authorized_certificates_get_builder()` + `appengine_projects_locations_applications_authorized_certificates_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn appengine_projects_locations_applications_authorized_certificates_get(
    client: &SimpleHttpClient,
    applicationsId: &str,
    authorizedCertificatesId: &str,
    locationsId: &str,
    projectsId: &str,
    view: Option<&str>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<AuthorizedCertificate>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = appengine_projects_locations_applications_authorized_certificates_get_builder(
        client,
        applicationsId,
        authorizedCertificatesId,
        locationsId,
        projectsId,
        view,
    )?;
    appengine_projects_locations_applications_authorized_certificates_get_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/applications/{applicationsId}/authorizedCertificates
/// Lists all SSL certificates the user is authorized to administer.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `appengine_projects_locations_applications_authorized_certificates_list_execute()` to send, or `appengine_projects_locations_applications_authorized_certificates_list` for simplest API.

pub fn appengine_projects_locations_applications_authorized_certificates_list_builder(
    client: &SimpleHttpClient,
    applicationsId: &str,
    locationsId: &str,
    projectsId: &str,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
    view: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comv1/projects/{}/locations/{}/applications/{}/authorizedCertificates",
        applicationsId, locationsId, projectsId,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "pageSize",
            pageSize
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageToken",
            pageToken
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "view",
            view.as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/applications/{applicationsId}/authorizedCertificates
/// Lists all SSL certificates the user is authorized to administer.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `appengine_projects_locations_applications_authorized_certificates_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from appengine_projects_locations_applications_authorized_certificates_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn appengine_projects_locations_applications_authorized_certificates_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ListAuthorizedCertificatesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListAuthorizedCertificatesResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/applications/{applicationsId}/authorizedCertificates
/// Lists all SSL certificates the user is authorized to administer.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `appengine_projects_locations_applications_authorized_certificates_list_builder()` + `appengine_projects_locations_applications_authorized_certificates_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn appengine_projects_locations_applications_authorized_certificates_list(
    client: &SimpleHttpClient,
    applicationsId: &str,
    locationsId: &str,
    projectsId: &str,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
    view: Option<&str>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ListAuthorizedCertificatesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = appengine_projects_locations_applications_authorized_certificates_list_builder(
        client,
        applicationsId,
        locationsId,
        projectsId,
        pageSize,
        pageToken,
        view,
    )?;
    appengine_projects_locations_applications_authorized_certificates_list_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/applications/{applicationsId}/authorizedCertificates/{authorizedCertificatesId}
/// Updates the specified SSL certificate. To renew a certificate and maintain its existing domain mappings, update certificate_data with a new certificate. The new certificate must be applicable to the same domains as the original certificate. The certificate display_name may also be updated.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `appengine_projects_locations_applications_authorized_certificates_patch_execute()` to send, or `appengine_projects_locations_applications_authorized_certificates_patch` for simplest API.

pub fn appengine_projects_locations_applications_authorized_certificates_patch_builder(
    client: &SimpleHttpClient,
    applicationsId: &str,
    authorizedCertificatesId: &str,
    locationsId: &str,
    projectsId: &str,
    updateMask: Option<&str>,
    body: &AuthorizedCertificate,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comv1/projects/{}/locations/{}/applications/{}/authorizedCertificates/{}",
        applicationsId,
        authorizedCertificatesId,
        locationsId,
        projectsId,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "updateMask",
            updateMask
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/applications/{applicationsId}/authorizedCertificates/{authorizedCertificatesId}
/// Updates the specified SSL certificate. To renew a certificate and maintain its existing domain mappings, update certificate_data with a new certificate. The new certificate must be applicable to the same domains as the original certificate. The certificate display_name may also be updated.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `appengine_projects_locations_applications_authorized_certificates_patch_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from appengine_projects_locations_applications_authorized_certificates_patch_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn appengine_projects_locations_applications_authorized_certificates_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<AuthorizedCertificate>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: AuthorizedCertificate = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/applications/{applicationsId}/authorizedCertificates/{authorizedCertificatesId}
/// Updates the specified SSL certificate. To renew a certificate and maintain its existing domain mappings, update certificate_data with a new certificate. The new certificate must be applicable to the same domains as the original certificate. The certificate display_name may also be updated.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `appengine_projects_locations_applications_authorized_certificates_patch_builder()` + `appengine_projects_locations_applications_authorized_certificates_patch_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn appengine_projects_locations_applications_authorized_certificates_patch(
    client: &SimpleHttpClient,
    applicationsId: &str,
    authorizedCertificatesId: &str,
    locationsId: &str,
    projectsId: &str,
    updateMask: Option<&str>,
    body: &AuthorizedCertificate,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<AuthorizedCertificate>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = appengine_projects_locations_applications_authorized_certificates_patch_builder(
        client,
        applicationsId,
        authorizedCertificatesId,
        locationsId,
        projectsId,
        updateMask,
        body,
    )?;
    appengine_projects_locations_applications_authorized_certificates_patch_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/applications/{applicationsId}/authorizedDomains
/// Lists all domains the user is authorized to administer.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `appengine_projects_locations_applications_authorized_domains_list_execute()` to send, or `appengine_projects_locations_applications_authorized_domains_list` for simplest API.

pub fn appengine_projects_locations_applications_authorized_domains_list_builder(
    client: &SimpleHttpClient,
    applicationsId: &str,
    locationsId: &str,
    projectsId: &str,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comv1/projects/{}/locations/{}/applications/{}/authorizedDomains",
        applicationsId, locationsId, projectsId,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "pageSize",
            pageSize
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageToken",
            pageToken
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/applications/{applicationsId}/authorizedDomains
/// Lists all domains the user is authorized to administer.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `appengine_projects_locations_applications_authorized_domains_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from appengine_projects_locations_applications_authorized_domains_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn appengine_projects_locations_applications_authorized_domains_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ListAuthorizedDomainsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListAuthorizedDomainsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/applications/{applicationsId}/authorizedDomains
/// Lists all domains the user is authorized to administer.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `appengine_projects_locations_applications_authorized_domains_list_builder()` + `appengine_projects_locations_applications_authorized_domains_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn appengine_projects_locations_applications_authorized_domains_list(
    client: &SimpleHttpClient,
    applicationsId: &str,
    locationsId: &str,
    projectsId: &str,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ListAuthorizedDomainsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = appengine_projects_locations_applications_authorized_domains_list_builder(
        client,
        applicationsId,
        locationsId,
        projectsId,
        pageSize,
        pageToken,
    )?;
    appengine_projects_locations_applications_authorized_domains_list_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/applications/{applicationsId}/domainMappings
/// Maps a domain to an application. A user must be authorized to administer a domain in order to map it to an application. For a list of available authorized domains, see AuthorizedDomains.ListAuthorizedDomains.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `appengine_projects_locations_applications_domain_mappings_create_execute()` to send, or `appengine_projects_locations_applications_domain_mappings_create` for simplest API.

pub fn appengine_projects_locations_applications_domain_mappings_create_builder(
    client: &SimpleHttpClient,
    applicationsId: &str,
    locationsId: &str,
    projectsId: &str,
    overrideStrategy: Option<&str>,
    body: &DomainMapping,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comv1/projects/{}/locations/{}/applications/{}/domainMappings",
        applicationsId, locationsId, projectsId,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "overrideStrategy",
            overrideStrategy
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/applications/{applicationsId}/domainMappings
/// Maps a domain to an application. A user must be authorized to administer a domain in order to map it to an application. For a list of available authorized domains, see AuthorizedDomains.ListAuthorizedDomains.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `appengine_projects_locations_applications_domain_mappings_create_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from appengine_projects_locations_applications_domain_mappings_create_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn appengine_projects_locations_applications_domain_mappings_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/applications/{applicationsId}/domainMappings
/// Maps a domain to an application. A user must be authorized to administer a domain in order to map it to an application. For a list of available authorized domains, see AuthorizedDomains.ListAuthorizedDomains.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `appengine_projects_locations_applications_domain_mappings_create_builder()` + `appengine_projects_locations_applications_domain_mappings_create_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn appengine_projects_locations_applications_domain_mappings_create(
    client: &SimpleHttpClient,
    applicationsId: &str,
    locationsId: &str,
    projectsId: &str,
    overrideStrategy: Option<&str>,
    body: &DomainMapping,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = appengine_projects_locations_applications_domain_mappings_create_builder(
        client,
        applicationsId,
        locationsId,
        projectsId,
        overrideStrategy,
        body,
    )?;
    appengine_projects_locations_applications_domain_mappings_create_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/applications/{applicationsId}/domainMappings/{domainMappingsId}
/// Deletes the specified domain mapping. A user must be authorized to administer the associated domain in order to delete a DomainMapping resource.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `appengine_projects_locations_applications_domain_mappings_delete_execute()` to send, or `appengine_projects_locations_applications_domain_mappings_delete` for simplest API.

pub fn appengine_projects_locations_applications_domain_mappings_delete_builder(
    client: &SimpleHttpClient,
    applicationsId: &str,
    domainMappingsId: &str,
    locationsId: &str,
    projectsId: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comv1/projects/{}/locations/{}/applications/{}/domainMappings/{}",
        applicationsId, domainMappingsId, locationsId, projectsId,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/applications/{applicationsId}/domainMappings/{domainMappingsId}
/// Deletes the specified domain mapping. A user must be authorized to administer the associated domain in order to delete a DomainMapping resource.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `appengine_projects_locations_applications_domain_mappings_delete_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from appengine_projects_locations_applications_domain_mappings_delete_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn appengine_projects_locations_applications_domain_mappings_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/applications/{applicationsId}/domainMappings/{domainMappingsId}
/// Deletes the specified domain mapping. A user must be authorized to administer the associated domain in order to delete a DomainMapping resource.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `appengine_projects_locations_applications_domain_mappings_delete_builder()` + `appengine_projects_locations_applications_domain_mappings_delete_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn appengine_projects_locations_applications_domain_mappings_delete(
    client: &SimpleHttpClient,
    applicationsId: &str,
    domainMappingsId: &str,
    locationsId: &str,
    projectsId: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = appengine_projects_locations_applications_domain_mappings_delete_builder(
        client,
        applicationsId,
        domainMappingsId,
        locationsId,
        projectsId,
    )?;
    appengine_projects_locations_applications_domain_mappings_delete_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/applications/{applicationsId}/domainMappings/{domainMappingsId}
/// Gets the specified domain mapping.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `appengine_projects_locations_applications_domain_mappings_get_execute()` to send, or `appengine_projects_locations_applications_domain_mappings_get` for simplest API.

pub fn appengine_projects_locations_applications_domain_mappings_get_builder(
    client: &SimpleHttpClient,
    applicationsId: &str,
    domainMappingsId: &str,
    locationsId: &str,
    projectsId: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comv1/projects/{}/locations/{}/applications/{}/domainMappings/{}",
        applicationsId, domainMappingsId, locationsId, projectsId,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/applications/{applicationsId}/domainMappings/{domainMappingsId}
/// Gets the specified domain mapping.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `appengine_projects_locations_applications_domain_mappings_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from appengine_projects_locations_applications_domain_mappings_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn appengine_projects_locations_applications_domain_mappings_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<DomainMapping>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: DomainMapping = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/applications/{applicationsId}/domainMappings/{domainMappingsId}
/// Gets the specified domain mapping.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `appengine_projects_locations_applications_domain_mappings_get_builder()` + `appengine_projects_locations_applications_domain_mappings_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn appengine_projects_locations_applications_domain_mappings_get(
    client: &SimpleHttpClient,
    applicationsId: &str,
    domainMappingsId: &str,
    locationsId: &str,
    projectsId: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<DomainMapping>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = appengine_projects_locations_applications_domain_mappings_get_builder(
        client,
        applicationsId,
        domainMappingsId,
        locationsId,
        projectsId,
    )?;
    appengine_projects_locations_applications_domain_mappings_get_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/applications/{applicationsId}/domainMappings
/// Lists the domain mappings on an application.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `appengine_projects_locations_applications_domain_mappings_list_execute()` to send, or `appengine_projects_locations_applications_domain_mappings_list` for simplest API.

pub fn appengine_projects_locations_applications_domain_mappings_list_builder(
    client: &SimpleHttpClient,
    applicationsId: &str,
    locationsId: &str,
    projectsId: &str,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comv1/projects/{}/locations/{}/applications/{}/domainMappings",
        applicationsId, locationsId, projectsId,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "pageSize",
            pageSize
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageToken",
            pageToken
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/applications/{applicationsId}/domainMappings
/// Lists the domain mappings on an application.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `appengine_projects_locations_applications_domain_mappings_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from appengine_projects_locations_applications_domain_mappings_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn appengine_projects_locations_applications_domain_mappings_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ListDomainMappingsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListDomainMappingsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/applications/{applicationsId}/domainMappings
/// Lists the domain mappings on an application.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `appengine_projects_locations_applications_domain_mappings_list_builder()` + `appengine_projects_locations_applications_domain_mappings_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn appengine_projects_locations_applications_domain_mappings_list(
    client: &SimpleHttpClient,
    applicationsId: &str,
    locationsId: &str,
    projectsId: &str,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ListDomainMappingsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = appengine_projects_locations_applications_domain_mappings_list_builder(
        client,
        applicationsId,
        locationsId,
        projectsId,
        pageSize,
        pageToken,
    )?;
    appengine_projects_locations_applications_domain_mappings_list_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/applications/{applicationsId}/domainMappings/{domainMappingsId}
/// Updates the specified domain mapping. To map an SSL certificate to a domain mapping, update certificate_id to point to an AuthorizedCertificate resource. A user must be authorized to administer the associated domain in order to update a DomainMapping resource.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `appengine_projects_locations_applications_domain_mappings_patch_execute()` to send, or `appengine_projects_locations_applications_domain_mappings_patch` for simplest API.

pub fn appengine_projects_locations_applications_domain_mappings_patch_builder(
    client: &SimpleHttpClient,
    applicationsId: &str,
    domainMappingsId: &str,
    locationsId: &str,
    projectsId: &str,
    updateMask: Option<&str>,
    body: &DomainMapping,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comv1/projects/{}/locations/{}/applications/{}/domainMappings/{}",
        applicationsId, domainMappingsId, locationsId, projectsId,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "updateMask",
            updateMask
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/applications/{applicationsId}/domainMappings/{domainMappingsId}
/// Updates the specified domain mapping. To map an SSL certificate to a domain mapping, update certificate_id to point to an AuthorizedCertificate resource. A user must be authorized to administer the associated domain in order to update a DomainMapping resource.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `appengine_projects_locations_applications_domain_mappings_patch_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from appengine_projects_locations_applications_domain_mappings_patch_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn appengine_projects_locations_applications_domain_mappings_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/applications/{applicationsId}/domainMappings/{domainMappingsId}
/// Updates the specified domain mapping. To map an SSL certificate to a domain mapping, update certificate_id to point to an AuthorizedCertificate resource. A user must be authorized to administer the associated domain in order to update a DomainMapping resource.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `appengine_projects_locations_applications_domain_mappings_patch_builder()` + `appengine_projects_locations_applications_domain_mappings_patch_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn appengine_projects_locations_applications_domain_mappings_patch(
    client: &SimpleHttpClient,
    applicationsId: &str,
    domainMappingsId: &str,
    locationsId: &str,
    projectsId: &str,
    updateMask: Option<&str>,
    body: &DomainMapping,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = appengine_projects_locations_applications_domain_mappings_patch_builder(
        client,
        applicationsId,
        domainMappingsId,
        locationsId,
        projectsId,
        updateMask,
        body,
    )?;
    appengine_projects_locations_applications_domain_mappings_patch_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/applications/{applicationsId}/services/{servicesId}
/// Deletes the specified service and all enclosed versions.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `appengine_projects_locations_applications_services_delete_execute()` to send, or `appengine_projects_locations_applications_services_delete` for simplest API.

pub fn appengine_projects_locations_applications_services_delete_builder(
    client: &SimpleHttpClient,
    applicationsId: &str,
    locationsId: &str,
    projectsId: &str,
    servicesId: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comv1/projects/{}/locations/{}/applications/{}/services/{}",
        applicationsId, locationsId, projectsId, servicesId,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/applications/{applicationsId}/services/{servicesId}
/// Deletes the specified service and all enclosed versions.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `appengine_projects_locations_applications_services_delete_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from appengine_projects_locations_applications_services_delete_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn appengine_projects_locations_applications_services_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/applications/{applicationsId}/services/{servicesId}
/// Deletes the specified service and all enclosed versions.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `appengine_projects_locations_applications_services_delete_builder()` + `appengine_projects_locations_applications_services_delete_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn appengine_projects_locations_applications_services_delete(
    client: &SimpleHttpClient,
    applicationsId: &str,
    locationsId: &str,
    projectsId: &str,
    servicesId: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = appengine_projects_locations_applications_services_delete_builder(
        client,
        applicationsId,
        locationsId,
        projectsId,
        servicesId,
    )?;
    appengine_projects_locations_applications_services_delete_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/applications/{applicationsId}/services/{servicesId}
/// Updates the configuration of the specified service.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `appengine_projects_locations_applications_services_patch_execute()` to send, or `appengine_projects_locations_applications_services_patch` for simplest API.

pub fn appengine_projects_locations_applications_services_patch_builder(
    client: &SimpleHttpClient,
    applicationsId: &str,
    locationsId: &str,
    projectsId: &str,
    servicesId: &str,
    migrateTraffic: Option<bool>,
    updateMask: Option<&str>,
    body: &Service,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comv1/projects/{}/locations/{}/applications/{}/services/{}",
        applicationsId, locationsId, projectsId, servicesId,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "migrateTraffic",
            migrateTraffic
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "updateMask",
            updateMask
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/applications/{applicationsId}/services/{servicesId}
/// Updates the configuration of the specified service.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `appengine_projects_locations_applications_services_patch_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from appengine_projects_locations_applications_services_patch_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn appengine_projects_locations_applications_services_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/applications/{applicationsId}/services/{servicesId}
/// Updates the configuration of the specified service.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `appengine_projects_locations_applications_services_patch_builder()` + `appengine_projects_locations_applications_services_patch_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn appengine_projects_locations_applications_services_patch(
    client: &SimpleHttpClient,
    applicationsId: &str,
    locationsId: &str,
    projectsId: &str,
    servicesId: &str,
    migrateTraffic: Option<bool>,
    updateMask: Option<&str>,
    body: &Service,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = appengine_projects_locations_applications_services_patch_builder(
        client,
        applicationsId,
        locationsId,
        projectsId,
        servicesId,
        migrateTraffic,
        updateMask,
        body,
    )?;
    appengine_projects_locations_applications_services_patch_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/applications/{applicationsId}/services/{servicesId}/versions/{versionsId}
/// Deletes an existing Version resource.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `appengine_projects_locations_applications_services_versions_delete_execute()` to send, or `appengine_projects_locations_applications_services_versions_delete` for simplest API.

pub fn appengine_projects_locations_applications_services_versions_delete_builder(
    client: &SimpleHttpClient,
    applicationsId: &str,
    locationsId: &str,
    projectsId: &str,
    servicesId: &str,
    versionsId: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comv1/projects/{}/locations/{}/applications/{}/services/{}/versions/{}",
        applicationsId,
        locationsId,
        projectsId,
        servicesId,
        versionsId,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/applications/{applicationsId}/services/{servicesId}/versions/{versionsId}
/// Deletes an existing Version resource.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `appengine_projects_locations_applications_services_versions_delete_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from appengine_projects_locations_applications_services_versions_delete_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn appengine_projects_locations_applications_services_versions_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/applications/{applicationsId}/services/{servicesId}/versions/{versionsId}
/// Deletes an existing Version resource.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `appengine_projects_locations_applications_services_versions_delete_builder()` + `appengine_projects_locations_applications_services_versions_delete_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn appengine_projects_locations_applications_services_versions_delete(
    client: &SimpleHttpClient,
    applicationsId: &str,
    locationsId: &str,
    projectsId: &str,
    servicesId: &str,
    versionsId: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = appengine_projects_locations_applications_services_versions_delete_builder(
        client,
        applicationsId,
        locationsId,
        projectsId,
        servicesId,
        versionsId,
    )?;
    appengine_projects_locations_applications_services_versions_delete_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/applications/{applicationsId}/services/{servicesId}/versions/{versionsId}:exportAppImage
/// Exports a user image to Artifact Registry.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `appengine_projects_locations_applications_services_versions_export_app_image_execute()` to send, or `appengine_projects_locations_applications_services_versions_export_app_image` for simplest API.

pub fn appengine_projects_locations_applications_services_versions_export_app_image_builder(
    client: &SimpleHttpClient,
    applicationsId: &str,
    locationsId: &str,
    projectsId: &str,
    servicesId: &str,
    versionsId: &str,
    body: &ExportAppImageRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comv1/projects/{}/locations/{}/applications/{}/services/{}/versions/{}:exportAppImage",
        applicationsId,
        locationsId,
        projectsId,
        servicesId,
        versionsId,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/applications/{applicationsId}/services/{servicesId}/versions/{versionsId}:exportAppImage
/// Exports a user image to Artifact Registry.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `appengine_projects_locations_applications_services_versions_export_app_image_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from appengine_projects_locations_applications_services_versions_export_app_image_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn appengine_projects_locations_applications_services_versions_export_app_image_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/applications/{applicationsId}/services/{servicesId}/versions/{versionsId}:exportAppImage
/// Exports a user image to Artifact Registry.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `appengine_projects_locations_applications_services_versions_export_app_image_builder()` + `appengine_projects_locations_applications_services_versions_export_app_image_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn appengine_projects_locations_applications_services_versions_export_app_image(
    client: &SimpleHttpClient,
    applicationsId: &str,
    locationsId: &str,
    projectsId: &str,
    servicesId: &str,
    versionsId: &str,
    body: &ExportAppImageRequest,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        appengine_projects_locations_applications_services_versions_export_app_image_builder(
            client,
            applicationsId,
            locationsId,
            projectsId,
            servicesId,
            versionsId,
            body,
        )?;
    appengine_projects_locations_applications_services_versions_export_app_image_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/applications/{applicationsId}/services/{servicesId}/versions/{versionsId}
/// Updates the specified Version resource. You can specify the following fields depending on the App Engine environment and type of scaling that the version resource uses:Standard environment instance_class (<https://cloud.google.`com/appengine/docs/admin-api/reference/rest/v1/apps`.services.versions#Version.FIELDS.instance_class>)automatic scaling in the standard environment: automatic_scaling.min_idle_instances (<https://cloud.google.`com/appengine/docs/admin-api/reference/rest/v1/apps`.services.versions#Version.FIELDS.automatic_scaling>) automatic_scaling.max_idle_instances (<https://cloud.google.`com/appengine/docs/admin-api/reference/rest/v1/apps`.services.versions#Version.FIELDS.automatic_scaling>) `automaticScaling`.standard_scheduler_settings.max_instances (<https://cloud.google.`com/appengine/docs/admin-api/reference/rest/v1/apps`.services.versions#StandardSchedulerSettings>) `automaticScaling`.standard_scheduler_settings.min_instances (<https://cloud.google.`com/appengine/docs/admin-api/reference/rest/v1/apps`.services.versions#StandardSchedulerSettings>) `automaticScaling`.standard_scheduler_settings.target_cpu_utilization (<https://cloud.google.`com/appengine/docs/admin-api/reference/rest/v1/apps`.services.versions#StandardSchedulerSettings>) `automaticScaling`.standard_scheduler_settings.target_throughput_utilization (<https://cloud.google.`com/appengine/docs/admin-api/reference/rest/v1/apps`.services.versions#StandardSchedulerSettings>)basic scaling or manual scaling in the standard environment: serving_status (<https://cloud.google.`com/appengine/docs/admin-api/reference/rest/v1/apps`.services.versions#Version.FIELDS.serving_status>) manual_scaling.instances (<https://cloud.google.`com/appengine/docs/admin-api/reference/rest/v1/apps`.services.versions#manualscaling>)Flexible environment serving_status (<https://cloud.google.`com/appengine/docs/admin-api/reference/rest/v1/apps`.services.versions#Version.FIELDS.serving_status>)automatic scaling in the flexible environment: automatic_scaling.min_total_instances (<https://cloud.google.`com/appengine/docs/admin-api/reference/rest/v1/apps`.services.versions#Version.FIELDS.automatic_scaling>) automatic_scaling.max_total_instances (<https://cloud.google.`com/appengine/docs/admin-api/reference/rest/v1/apps`.services.versions#Version.FIELDS.automatic_scaling>) automatic_scaling.cool_down_period_sec (<https://cloud.google.`com/appengine/docs/admin-api/reference/rest/v1/apps`.services.versions#Version.FIELDS.automatic_scaling>) automatic_scaling.cpu_utilization.target_utilization (<https://cloud.google.`com/appengine/docs/admin-api/reference/rest/v1/apps`.services.versions#Version.FIELDS.automatic_scaling>)manual scaling in the flexible environment: manual_scaling.instances (<https://cloud.google.`com/appengine/docs/admin-api/reference/rest/v1/apps`.services.versions#manualscaling>)
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `appengine_projects_locations_applications_services_versions_patch_execute()` to send, or `appengine_projects_locations_applications_services_versions_patch` for simplest API.

pub fn appengine_projects_locations_applications_services_versions_patch_builder(
    client: &SimpleHttpClient,
    applicationsId: &str,
    locationsId: &str,
    projectsId: &str,
    servicesId: &str,
    versionsId: &str,
    updateMask: Option<&str>,
    body: &Version,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comv1/projects/{}/locations/{}/applications/{}/services/{}/versions/{}",
        applicationsId,
        locationsId,
        projectsId,
        servicesId,
        versionsId,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "updateMask",
            updateMask
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/applications/{applicationsId}/services/{servicesId}/versions/{versionsId}
/// Updates the specified Version resource. You can specify the following fields depending on the App Engine environment and type of scaling that the version resource uses:Standard environment instance_class (<https://cloud.google.`com/appengine/docs/admin-api/reference/rest/v1/apps`.services.versions#Version.FIELDS.instance_class>)automatic scaling in the standard environment: automatic_scaling.min_idle_instances (<https://cloud.google.`com/appengine/docs/admin-api/reference/rest/v1/apps`.services.versions#Version.FIELDS.automatic_scaling>) automatic_scaling.max_idle_instances (<https://cloud.google.`com/appengine/docs/admin-api/reference/rest/v1/apps`.services.versions#Version.FIELDS.automatic_scaling>) `automaticScaling`.standard_scheduler_settings.max_instances (<https://cloud.google.`com/appengine/docs/admin-api/reference/rest/v1/apps`.services.versions#StandardSchedulerSettings>) `automaticScaling`.standard_scheduler_settings.min_instances (<https://cloud.google.`com/appengine/docs/admin-api/reference/rest/v1/apps`.services.versions#StandardSchedulerSettings>) `automaticScaling`.standard_scheduler_settings.target_cpu_utilization (<https://cloud.google.`com/appengine/docs/admin-api/reference/rest/v1/apps`.services.versions#StandardSchedulerSettings>) `automaticScaling`.standard_scheduler_settings.target_throughput_utilization (<https://cloud.google.`com/appengine/docs/admin-api/reference/rest/v1/apps`.services.versions#StandardSchedulerSettings>)basic scaling or manual scaling in the standard environment: serving_status (<https://cloud.google.`com/appengine/docs/admin-api/reference/rest/v1/apps`.services.versions#Version.FIELDS.serving_status>) manual_scaling.instances (<https://cloud.google.`com/appengine/docs/admin-api/reference/rest/v1/apps`.services.versions#manualscaling>)Flexible environment serving_status (<https://cloud.google.`com/appengine/docs/admin-api/reference/rest/v1/apps`.services.versions#Version.FIELDS.serving_status>)automatic scaling in the flexible environment: automatic_scaling.min_total_instances (<https://cloud.google.`com/appengine/docs/admin-api/reference/rest/v1/apps`.services.versions#Version.FIELDS.automatic_scaling>) automatic_scaling.max_total_instances (<https://cloud.google.`com/appengine/docs/admin-api/reference/rest/v1/apps`.services.versions#Version.FIELDS.automatic_scaling>) automatic_scaling.cool_down_period_sec (<https://cloud.google.`com/appengine/docs/admin-api/reference/rest/v1/apps`.services.versions#Version.FIELDS.automatic_scaling>) automatic_scaling.cpu_utilization.target_utilization (<https://cloud.google.`com/appengine/docs/admin-api/reference/rest/v1/apps`.services.versions#Version.FIELDS.automatic_scaling>)manual scaling in the flexible environment: manual_scaling.instances (<https://cloud.google.`com/appengine/docs/admin-api/reference/rest/v1/apps`.services.versions#manualscaling>)
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `appengine_projects_locations_applications_services_versions_patch_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from appengine_projects_locations_applications_services_versions_patch_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn appengine_projects_locations_applications_services_versions_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/applications/{applicationsId}/services/{servicesId}/versions/{versionsId}
/// Updates the specified Version resource. You can specify the following fields depending on the App Engine environment and type of scaling that the version resource uses:Standard environment instance_class (<https://cloud.google.`com/appengine/docs/admin-api/reference/rest/v1/apps`.services.versions#Version.FIELDS.instance_class>)automatic scaling in the standard environment: automatic_scaling.min_idle_instances (<https://cloud.google.`com/appengine/docs/admin-api/reference/rest/v1/apps`.services.versions#Version.FIELDS.automatic_scaling>) automatic_scaling.max_idle_instances (<https://cloud.google.`com/appengine/docs/admin-api/reference/rest/v1/apps`.services.versions#Version.FIELDS.automatic_scaling>) `automaticScaling`.standard_scheduler_settings.max_instances (<https://cloud.google.`com/appengine/docs/admin-api/reference/rest/v1/apps`.services.versions#StandardSchedulerSettings>) `automaticScaling`.standard_scheduler_settings.min_instances (<https://cloud.google.`com/appengine/docs/admin-api/reference/rest/v1/apps`.services.versions#StandardSchedulerSettings>) `automaticScaling`.standard_scheduler_settings.target_cpu_utilization (<https://cloud.google.`com/appengine/docs/admin-api/reference/rest/v1/apps`.services.versions#StandardSchedulerSettings>) `automaticScaling`.standard_scheduler_settings.target_throughput_utilization (<https://cloud.google.`com/appengine/docs/admin-api/reference/rest/v1/apps`.services.versions#StandardSchedulerSettings>)basic scaling or manual scaling in the standard environment: serving_status (<https://cloud.google.`com/appengine/docs/admin-api/reference/rest/v1/apps`.services.versions#Version.FIELDS.serving_status>) manual_scaling.instances (<https://cloud.google.`com/appengine/docs/admin-api/reference/rest/v1/apps`.services.versions#manualscaling>)Flexible environment serving_status (<https://cloud.google.`com/appengine/docs/admin-api/reference/rest/v1/apps`.services.versions#Version.FIELDS.serving_status>)automatic scaling in the flexible environment: automatic_scaling.min_total_instances (<https://cloud.google.`com/appengine/docs/admin-api/reference/rest/v1/apps`.services.versions#Version.FIELDS.automatic_scaling>) automatic_scaling.max_total_instances (<https://cloud.google.`com/appengine/docs/admin-api/reference/rest/v1/apps`.services.versions#Version.FIELDS.automatic_scaling>) automatic_scaling.cool_down_period_sec (<https://cloud.google.`com/appengine/docs/admin-api/reference/rest/v1/apps`.services.versions#Version.FIELDS.automatic_scaling>) automatic_scaling.cpu_utilization.target_utilization (<https://cloud.google.`com/appengine/docs/admin-api/reference/rest/v1/apps`.services.versions#Version.FIELDS.automatic_scaling>)manual scaling in the flexible environment: manual_scaling.instances (<https://cloud.google.`com/appengine/docs/admin-api/reference/rest/v1/apps`.services.versions#manualscaling>)
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `appengine_projects_locations_applications_services_versions_patch_builder()` + `appengine_projects_locations_applications_services_versions_patch_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn appengine_projects_locations_applications_services_versions_patch(
    client: &SimpleHttpClient,
    applicationsId: &str,
    locationsId: &str,
    projectsId: &str,
    servicesId: &str,
    versionsId: &str,
    updateMask: Option<&str>,
    body: &Version,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = appengine_projects_locations_applications_services_versions_patch_builder(
        client,
        applicationsId,
        locationsId,
        projectsId,
        servicesId,
        versionsId,
        updateMask,
        body,
    )?;
    appengine_projects_locations_applications_services_versions_patch_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/applications/{applicationsId}/services/{servicesId}/versions/{versionsId}/instances/{instancesId}:debug
/// Enables debugging on a VM instance. This allows you to use the SSH command to connect to the virtual machine where the instance lives. While in "debug mode", the instance continues to serve live traffic. You should delete the instance when you are done debugging and then allow the system to take over and determine if another instance should be started.Only applicable for instances in App Engine flexible environment.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `appengine_projects_locations_applications_services_versions_instances_debug_execute()` to send, or `appengine_projects_locations_applications_services_versions_instances_debug` for simplest API.

pub fn appengine_projects_locations_applications_services_versions_instances_debug_builder(
    client: &SimpleHttpClient,
    applicationsId: &str,
    instancesId: &str,
    locationsId: &str,
    projectsId: &str,
    servicesId: &str,
    versionsId: &str,
    body: &DebugInstanceRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comv1/projects/{}/locations/{}/applications/{}/services/{}/versions/{}/instances/{}:debug",
        applicationsId,
        instancesId,
        locationsId,
        projectsId,
        servicesId,
        versionsId,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/applications/{applicationsId}/services/{servicesId}/versions/{versionsId}/instances/{instancesId}:debug
/// Enables debugging on a VM instance. This allows you to use the SSH command to connect to the virtual machine where the instance lives. While in "debug mode", the instance continues to serve live traffic. You should delete the instance when you are done debugging and then allow the system to take over and determine if another instance should be started.Only applicable for instances in App Engine flexible environment.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `appengine_projects_locations_applications_services_versions_instances_debug_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from appengine_projects_locations_applications_services_versions_instances_debug_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn appengine_projects_locations_applications_services_versions_instances_debug_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/applications/{applicationsId}/services/{servicesId}/versions/{versionsId}/instances/{instancesId}:debug
/// Enables debugging on a VM instance. This allows you to use the SSH command to connect to the virtual machine where the instance lives. While in "debug mode", the instance continues to serve live traffic. You should delete the instance when you are done debugging and then allow the system to take over and determine if another instance should be started.Only applicable for instances in App Engine flexible environment.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `appengine_projects_locations_applications_services_versions_instances_debug_builder()` + `appengine_projects_locations_applications_services_versions_instances_debug_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn appengine_projects_locations_applications_services_versions_instances_debug(
    client: &SimpleHttpClient,
    applicationsId: &str,
    instancesId: &str,
    locationsId: &str,
    projectsId: &str,
    servicesId: &str,
    versionsId: &str,
    body: &DebugInstanceRequest,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        appengine_projects_locations_applications_services_versions_instances_debug_builder(
            client,
            applicationsId,
            instancesId,
            locationsId,
            projectsId,
            servicesId,
            versionsId,
            body,
        )?;
    appengine_projects_locations_applications_services_versions_instances_debug_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/applications/{applicationsId}/services/{servicesId}/versions/{versionsId}/instances/{instancesId}
/// Stops a running instance.The instance might be automatically recreated based on the scaling settings of the version. For more information, see "How Instances are Managed" (standard environment (<https://cloud.google.`com/appengine/docs/standard/python/how-instances-are-managed`>) | flexible environment (<https://cloud.google.`com/appengine/docs/flexible/python/how-instances-are-managed`>)).To ensure that instances are not re-created and avoid getting billed, you can stop all instances within the target version by changing the serving status of the version to STOPPED with the apps.services.versions.patch (<https://cloud.google.`com/appengine/docs/admin-api/reference/rest/v1/apps`.services.`versions/patch`>) method.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `appengine_projects_locations_applications_services_versions_instances_delete_execute()` to send, or `appengine_projects_locations_applications_services_versions_instances_delete` for simplest API.

pub fn appengine_projects_locations_applications_services_versions_instances_delete_builder(
    client: &SimpleHttpClient,
    applicationsId: &str,
    instancesId: &str,
    locationsId: &str,
    projectsId: &str,
    servicesId: &str,
    versionsId: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comv1/projects/{}/locations/{}/applications/{}/services/{}/versions/{}/instances/{}",
        applicationsId,
        instancesId,
        locationsId,
        projectsId,
        servicesId,
        versionsId,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/applications/{applicationsId}/services/{servicesId}/versions/{versionsId}/instances/{instancesId}
/// Stops a running instance.The instance might be automatically recreated based on the scaling settings of the version. For more information, see "How Instances are Managed" (standard environment (<https://cloud.google.`com/appengine/docs/standard/python/how-instances-are-managed`>) | flexible environment (<https://cloud.google.`com/appengine/docs/flexible/python/how-instances-are-managed`>)).To ensure that instances are not re-created and avoid getting billed, you can stop all instances within the target version by changing the serving status of the version to STOPPED with the apps.services.versions.patch (<https://cloud.google.`com/appengine/docs/admin-api/reference/rest/v1/apps`.services.`versions/patch`>) method.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `appengine_projects_locations_applications_services_versions_instances_delete_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from appengine_projects_locations_applications_services_versions_instances_delete_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn appengine_projects_locations_applications_services_versions_instances_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/applications/{applicationsId}/services/{servicesId}/versions/{versionsId}/instances/{instancesId}
/// Stops a running instance.The instance might be automatically recreated based on the scaling settings of the version. For more information, see "How Instances are Managed" (standard environment (<https://cloud.google.`com/appengine/docs/standard/python/how-instances-are-managed`>) | flexible environment (<https://cloud.google.`com/appengine/docs/flexible/python/how-instances-are-managed`>)).To ensure that instances are not re-created and avoid getting billed, you can stop all instances within the target version by changing the serving status of the version to STOPPED with the apps.services.versions.patch (<https://cloud.google.`com/appengine/docs/admin-api/reference/rest/v1/apps`.services.`versions/patch`>) method.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `appengine_projects_locations_applications_services_versions_instances_delete_builder()` + `appengine_projects_locations_applications_services_versions_instances_delete_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn appengine_projects_locations_applications_services_versions_instances_delete(
    client: &SimpleHttpClient,
    applicationsId: &str,
    instancesId: &str,
    locationsId: &str,
    projectsId: &str,
    servicesId: &str,
    versionsId: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        appengine_projects_locations_applications_services_versions_instances_delete_builder(
            client,
            applicationsId,
            instancesId,
            locationsId,
            projectsId,
            servicesId,
            versionsId,
        )?;
    appengine_projects_locations_applications_services_versions_instances_delete_execute(builder)
}
