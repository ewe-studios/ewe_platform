//! Auto-generated API clients for Google Cloud Platform - chromemanagement.
//!
//! Generated by `cargo run --bin ewe_platform gen_provider_clients`.
//! DO NOT EDIT MANUALLY.
//!
//! Feature flag: `gcp`


#![cfg(feature = "gcp")]

use crate::providers::gcp::clients::types::*;
use crate::providers::gcp::resources::*;
use foundation_core::valtron::{execute, StreamIterator, StreamIteratorExt};
use foundation_core::wire::simple_http::client::{
    body_reader, ClientRequestBuilder, RequestIntro, SimpleHttpClient, SystemDnsResolver,
};

/// GET v1/{+customer}/apps:countChromeAppRequests
/// Generate summary of app installation requests.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `chromemanagement_customers_apps_count_chrome_app_requests_execute()` to send, or `chromemanagement_customers_apps_count_chrome_app_requests` for simplest API.

pub fn chromemanagement_customers_apps_count_chrome_app_requests_builder(
    client: &SimpleHttpClient,
    customer: &str,
    orderBy: Option<&str>,
    orgUnitId: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comv1/{+customer}/apps:countChromeAppRequests",
        customer,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "orderBy",
            orderBy
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "orgUnitId",
            orgUnitId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageSize",
            pageSize
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageToken",
            pageToken
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET v1/{+customer}/apps:countChromeAppRequests
/// Generate summary of app installation requests.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `chromemanagement_customers_apps_count_chrome_app_requests_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from chromemanagement_customers_apps_count_chrome_app_requests_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn chromemanagement_customers_apps_count_chrome_app_requests_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleChromeManagementV1CountChromeAppRequestsResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleChromeManagementV1CountChromeAppRequestsResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+customer}/apps:countChromeAppRequests
/// Generate summary of app installation requests.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `chromemanagement_customers_apps_count_chrome_app_requests_builder()` + `chromemanagement_customers_apps_count_chrome_app_requests_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn chromemanagement_customers_apps_count_chrome_app_requests(
    client: &SimpleHttpClient,
    customer: &str,
    orderBy: Option<&str>,
    orgUnitId: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleChromeManagementV1CountChromeAppRequestsResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = chromemanagement_customers_apps_count_chrome_app_requests_builder(
        client, customer, orderBy, orgUnitId, pageSize, pageToken,
    )?;
    chromemanagement_customers_apps_count_chrome_app_requests_execute(builder)
}

/// GET v1/{+customer}/apps:fetchDevicesRequestingExtension
/// Get a list of devices that have requested to install an extension.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `chromemanagement_customers_apps_fetch_devices_requesting_extension_execute()` to send, or `chromemanagement_customers_apps_fetch_devices_requesting_extension` for simplest API.

pub fn chromemanagement_customers_apps_fetch_devices_requesting_extension_builder(
    client: &SimpleHttpClient,
    customer: &str,
    extensionId: Option<&str>,
    orgUnitId: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comv1/{+customer}/apps:fetchDevicesRequestingExtension",
        customer,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "extensionId",
            extensionId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "orgUnitId",
            orgUnitId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageSize",
            pageSize
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageToken",
            pageToken
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET v1/{+customer}/apps:fetchDevicesRequestingExtension
/// Get a list of devices that have requested to install an extension.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `chromemanagement_customers_apps_fetch_devices_requesting_extension_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from chromemanagement_customers_apps_fetch_devices_requesting_extension_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn chromemanagement_customers_apps_fetch_devices_requesting_extension_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleChromeManagementV1FetchDevicesRequestingExtensionResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleChromeManagementV1FetchDevicesRequestingExtensionResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+customer}/apps:fetchDevicesRequestingExtension
/// Get a list of devices that have requested to install an extension.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `chromemanagement_customers_apps_fetch_devices_requesting_extension_builder()` + `chromemanagement_customers_apps_fetch_devices_requesting_extension_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn chromemanagement_customers_apps_fetch_devices_requesting_extension(
    client: &SimpleHttpClient,
    customer: &str,
    extensionId: Option<&str>,
    orgUnitId: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleChromeManagementV1FetchDevicesRequestingExtensionResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = chromemanagement_customers_apps_fetch_devices_requesting_extension_builder(
        client,
        customer,
        extensionId,
        orgUnitId,
        pageSize,
        pageToken,
    )?;
    chromemanagement_customers_apps_fetch_devices_requesting_extension_execute(builder)
}

/// GET v1/{+customer}/apps:fetchUsersRequestingExtension
/// Get a list of users that have requested to install an extension.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `chromemanagement_customers_apps_fetch_users_requesting_extension_execute()` to send, or `chromemanagement_customers_apps_fetch_users_requesting_extension` for simplest API.

pub fn chromemanagement_customers_apps_fetch_users_requesting_extension_builder(
    client: &SimpleHttpClient,
    customer: &str,
    extensionId: Option<&str>,
    orgUnitId: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comv1/{+customer}/apps:fetchUsersRequestingExtension",
        customer,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "extensionId",
            extensionId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "orgUnitId",
            orgUnitId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageSize",
            pageSize
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageToken",
            pageToken
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET v1/{+customer}/apps:fetchUsersRequestingExtension
/// Get a list of users that have requested to install an extension.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `chromemanagement_customers_apps_fetch_users_requesting_extension_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from chromemanagement_customers_apps_fetch_users_requesting_extension_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn chromemanagement_customers_apps_fetch_users_requesting_extension_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleChromeManagementV1FetchUsersRequestingExtensionResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleChromeManagementV1FetchUsersRequestingExtensionResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+customer}/apps:fetchUsersRequestingExtension
/// Get a list of users that have requested to install an extension.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `chromemanagement_customers_apps_fetch_users_requesting_extension_builder()` + `chromemanagement_customers_apps_fetch_users_requesting_extension_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn chromemanagement_customers_apps_fetch_users_requesting_extension(
    client: &SimpleHttpClient,
    customer: &str,
    extensionId: Option<&str>,
    orgUnitId: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleChromeManagementV1FetchUsersRequestingExtensionResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = chromemanagement_customers_apps_fetch_users_requesting_extension_builder(
        client,
        customer,
        extensionId,
        orgUnitId,
        pageSize,
        pageToken,
    )?;
    chromemanagement_customers_apps_fetch_users_requesting_extension_execute(builder)
}

/// GET v1/{+name}
/// Get a specific app for a customer by its resource name.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `chromemanagement_customers_apps_android_get_execute()` to send, or `chromemanagement_customers_apps_android_get` for simplest API.

pub fn chromemanagement_customers_apps_android_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/{+name}", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+name}
/// Get a specific app for a customer by its resource name.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `chromemanagement_customers_apps_android_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from chromemanagement_customers_apps_android_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn chromemanagement_customers_apps_android_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleChromeManagementV1AppDetails>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleChromeManagementV1AppDetails = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+name}
/// Get a specific app for a customer by its resource name.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `chromemanagement_customers_apps_android_get_builder()` + `chromemanagement_customers_apps_android_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn chromemanagement_customers_apps_android_get(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleChromeManagementV1AppDetails>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = chromemanagement_customers_apps_android_get_builder(client, name)?;
    chromemanagement_customers_apps_android_get_execute(builder)
}

/// GET v1/{+name}
/// Get a specific app for a customer by its resource name.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `chromemanagement_customers_apps_chrome_get_execute()` to send, or `chromemanagement_customers_apps_chrome_get` for simplest API.

pub fn chromemanagement_customers_apps_chrome_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/{+name}", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+name}
/// Get a specific app for a customer by its resource name.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `chromemanagement_customers_apps_chrome_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from chromemanagement_customers_apps_chrome_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn chromemanagement_customers_apps_chrome_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleChromeManagementV1AppDetails>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleChromeManagementV1AppDetails = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+name}
/// Get a specific app for a customer by its resource name.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `chromemanagement_customers_apps_chrome_get_builder()` + `chromemanagement_customers_apps_chrome_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn chromemanagement_customers_apps_chrome_get(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleChromeManagementV1AppDetails>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = chromemanagement_customers_apps_chrome_get_builder(client, name)?;
    chromemanagement_customers_apps_chrome_get_execute(builder)
}

/// GET v1/{+name}
/// Get a specific app for a customer by its resource name.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `chromemanagement_customers_apps_web_get_execute()` to send, or `chromemanagement_customers_apps_web_get` for simplest API.

pub fn chromemanagement_customers_apps_web_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/{+name}", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+name}
/// Get a specific app for a customer by its resource name.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `chromemanagement_customers_apps_web_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from chromemanagement_customers_apps_web_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn chromemanagement_customers_apps_web_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleChromeManagementV1AppDetails>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleChromeManagementV1AppDetails = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+name}
/// Get a specific app for a customer by its resource name.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `chromemanagement_customers_apps_web_get_builder()` + `chromemanagement_customers_apps_web_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn chromemanagement_customers_apps_web_get(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleChromeManagementV1AppDetails>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = chromemanagement_customers_apps_web_get_builder(client, name)?;
    chromemanagement_customers_apps_web_get_execute(builder)
}

/// GET v1/{+name}:claim
/// Claims a certificate provisioning process. For each certificate provisioning process, this operation can succeed only for one caller_instance_id.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `chromemanagement_customers_certificate_provisioning_processes_claim_execute()` to send, or `chromemanagement_customers_certificate_provisioning_processes_claim` for simplest API.

pub fn chromemanagement_customers_certificate_provisioning_processes_claim_builder(
    client: &SimpleHttpClient,
    name: &str,
    body: &GoogleChromeManagementVersionsV1ClaimCertificateProvisioningProcessRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/{+name}:claim", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v1/{+name}:claim
/// Claims a certificate provisioning process. For each certificate provisioning process, this operation can succeed only for one caller_instance_id.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `chromemanagement_customers_certificate_provisioning_processes_claim_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from chromemanagement_customers_certificate_provisioning_processes_claim_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn chromemanagement_customers_certificate_provisioning_processes_claim_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<
                    GoogleChromeManagementVersionsV1ClaimCertificateProvisioningProcessResponse,
                >,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleChromeManagementVersionsV1ClaimCertificateProvisioningProcessResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+name}:claim
/// Claims a certificate provisioning process. For each certificate provisioning process, this operation can succeed only for one caller_instance_id.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `chromemanagement_customers_certificate_provisioning_processes_claim_builder()` + `chromemanagement_customers_certificate_provisioning_processes_claim_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn chromemanagement_customers_certificate_provisioning_processes_claim(
    client: &SimpleHttpClient,
    name: &str,
    body: &GoogleChromeManagementVersionsV1ClaimCertificateProvisioningProcessRequest,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<
                    GoogleChromeManagementVersionsV1ClaimCertificateProvisioningProcessResponse,
                >,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = chromemanagement_customers_certificate_provisioning_processes_claim_builder(
        client, name, body,
    )?;
    chromemanagement_customers_certificate_provisioning_processes_claim_execute(builder)
}

/// GET v1/{+name}
/// Retrieves a certificate provisioning process.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `chromemanagement_customers_certificate_provisioning_processes_get_execute()` to send, or `chromemanagement_customers_certificate_provisioning_processes_get` for simplest API.

pub fn chromemanagement_customers_certificate_provisioning_processes_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/{+name}", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+name}
/// Retrieves a certificate provisioning process.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `chromemanagement_customers_certificate_provisioning_processes_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from chromemanagement_customers_certificate_provisioning_processes_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn chromemanagement_customers_certificate_provisioning_processes_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleChromeManagementVersionsV1CertificateProvisioningProcess>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleChromeManagementVersionsV1CertificateProvisioningProcess =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+name}
/// Retrieves a certificate provisioning process.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `chromemanagement_customers_certificate_provisioning_processes_get_builder()` + `chromemanagement_customers_certificate_provisioning_processes_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn chromemanagement_customers_certificate_provisioning_processes_get(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleChromeManagementVersionsV1CertificateProvisioningProcess>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        chromemanagement_customers_certificate_provisioning_processes_get_builder(client, name)?;
    chromemanagement_customers_certificate_provisioning_processes_get_execute(builder)
}

/// GET v1/{+name}:setFailure
/// Marks a certificate provisioning process as failed.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `chromemanagement_customers_certificate_provisioning_processes_set_failure_execute()` to send, or `chromemanagement_customers_certificate_provisioning_processes_set_failure` for simplest API.

pub fn chromemanagement_customers_certificate_provisioning_processes_set_failure_builder(
    client: &SimpleHttpClient,
    name: &str,
    body: &GoogleChromeManagementVersionsV1SetFailureRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/{+name}:setFailure", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v1/{+name}:setFailure
/// Marks a certificate provisioning process as failed.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `chromemanagement_customers_certificate_provisioning_processes_set_failure_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from chromemanagement_customers_certificate_provisioning_processes_set_failure_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn chromemanagement_customers_certificate_provisioning_processes_set_failure_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleChromeManagementVersionsV1SetFailureResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleChromeManagementVersionsV1SetFailureResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+name}:setFailure
/// Marks a certificate provisioning process as failed.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `chromemanagement_customers_certificate_provisioning_processes_set_failure_builder()` + `chromemanagement_customers_certificate_provisioning_processes_set_failure_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn chromemanagement_customers_certificate_provisioning_processes_set_failure(
    client: &SimpleHttpClient,
    name: &str,
    body: &GoogleChromeManagementVersionsV1SetFailureRequest,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleChromeManagementVersionsV1SetFailureResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        chromemanagement_customers_certificate_provisioning_processes_set_failure_builder(
            client, name, body,
        )?;
    chromemanagement_customers_certificate_provisioning_processes_set_failure_execute(builder)
}

/// GET v1/{+name}:signData
/// Requests the client that initiated a certificate provisioning process to sign data. This should only be called after ClaimCertificateProvisioningProcess has been successfully executed.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `chromemanagement_customers_certificate_provisioning_processes_sign_data_execute()` to send, or `chromemanagement_customers_certificate_provisioning_processes_sign_data` for simplest API.

pub fn chromemanagement_customers_certificate_provisioning_processes_sign_data_builder(
    client: &SimpleHttpClient,
    name: &str,
    body: &GoogleChromeManagementVersionsV1SignDataRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/{+name}:signData", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v1/{+name}:signData
/// Requests the client that initiated a certificate provisioning process to sign data. This should only be called after ClaimCertificateProvisioningProcess has been successfully executed.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `chromemanagement_customers_certificate_provisioning_processes_sign_data_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from chromemanagement_customers_certificate_provisioning_processes_sign_data_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn chromemanagement_customers_certificate_provisioning_processes_sign_data_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+name}:signData
/// Requests the client that initiated a certificate provisioning process to sign data. This should only be called after ClaimCertificateProvisioningProcess has been successfully executed.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `chromemanagement_customers_certificate_provisioning_processes_sign_data_builder()` + `chromemanagement_customers_certificate_provisioning_processes_sign_data_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn chromemanagement_customers_certificate_provisioning_processes_sign_data(
    client: &SimpleHttpClient,
    name: &str,
    body: &GoogleChromeManagementVersionsV1SignDataRequest,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = chromemanagement_customers_certificate_provisioning_processes_sign_data_builder(
        client, name, body,
    )?;
    chromemanagement_customers_certificate_provisioning_processes_sign_data_execute(builder)
}

/// GET v1/{+name}:uploadCertificate
/// Uploads a successfully issued certificate for a certificate provisioning process.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `chromemanagement_customers_certificate_provisioning_processes_upload_certificate_execute()` to send, or `chromemanagement_customers_certificate_provisioning_processes_upload_certificate` for simplest API.

pub fn chromemanagement_customers_certificate_provisioning_processes_upload_certificate_builder(
    client: &SimpleHttpClient,
    name: &str,
    body: &GoogleChromeManagementVersionsV1UploadCertificateRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/{+name}:uploadCertificate", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v1/{+name}:uploadCertificate
/// Uploads a successfully issued certificate for a certificate provisioning process.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `chromemanagement_customers_certificate_provisioning_processes_upload_certificate_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from chromemanagement_customers_certificate_provisioning_processes_upload_certificate_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn chromemanagement_customers_certificate_provisioning_processes_upload_certificate_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleChromeManagementVersionsV1UploadCertificateResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleChromeManagementVersionsV1UploadCertificateResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+name}:uploadCertificate
/// Uploads a successfully issued certificate for a certificate provisioning process.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `chromemanagement_customers_certificate_provisioning_processes_upload_certificate_builder()` + `chromemanagement_customers_certificate_provisioning_processes_upload_certificate_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn chromemanagement_customers_certificate_provisioning_processes_upload_certificate(
    client: &SimpleHttpClient,
    name: &str,
    body: &GoogleChromeManagementVersionsV1UploadCertificateRequest,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleChromeManagementVersionsV1UploadCertificateResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        chromemanagement_customers_certificate_provisioning_processes_upload_certificate_builder(
            client, name, body,
        )?;
    chromemanagement_customers_certificate_provisioning_processes_upload_certificate_execute(
        builder,
    )
}

/// GET v1/{+name}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `chromemanagement_customers_certificate_provisioning_processes_operations_get_execute()` to send, or `chromemanagement_customers_certificate_provisioning_processes_operations_get` for simplest API.

pub fn chromemanagement_customers_certificate_provisioning_processes_operations_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/{+name}", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+name}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `chromemanagement_customers_certificate_provisioning_processes_operations_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from chromemanagement_customers_certificate_provisioning_processes_operations_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn chromemanagement_customers_certificate_provisioning_processes_operations_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+name}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `chromemanagement_customers_certificate_provisioning_processes_operations_get_builder()` + `chromemanagement_customers_certificate_provisioning_processes_operations_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn chromemanagement_customers_certificate_provisioning_processes_operations_get(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        chromemanagement_customers_certificate_provisioning_processes_operations_get_builder(
            client, name,
        )?;
    chromemanagement_customers_certificate_provisioning_processes_operations_get_execute(builder)
}

/// GET v1/{+name}
/// Deletes the data collected from a Chrome browser profile.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `chromemanagement_customers_profiles_delete_execute()` to send, or `chromemanagement_customers_profiles_delete` for simplest API.

pub fn chromemanagement_customers_profiles_delete_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/{+name}", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+name}
/// Deletes the data collected from a Chrome browser profile.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `chromemanagement_customers_profiles_delete_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from chromemanagement_customers_profiles_delete_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn chromemanagement_customers_profiles_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleProtobufEmpty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+name}
/// Deletes the data collected from a Chrome browser profile.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `chromemanagement_customers_profiles_delete_builder()` + `chromemanagement_customers_profiles_delete_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn chromemanagement_customers_profiles_delete(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = chromemanagement_customers_profiles_delete_builder(client, name)?;
    chromemanagement_customers_profiles_delete_execute(builder)
}

/// GET v1/{+name}
/// Gets a Chrome browser profile with customer ID and profile permanent ID.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `chromemanagement_customers_profiles_get_execute()` to send, or `chromemanagement_customers_profiles_get` for simplest API.

pub fn chromemanagement_customers_profiles_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/{+name}", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+name}
/// Gets a Chrome browser profile with customer ID and profile permanent ID.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `chromemanagement_customers_profiles_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from chromemanagement_customers_profiles_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn chromemanagement_customers_profiles_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleChromeManagementVersionsV1ChromeBrowserProfile>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleChromeManagementVersionsV1ChromeBrowserProfile =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+name}
/// Gets a Chrome browser profile with customer ID and profile permanent ID.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `chromemanagement_customers_profiles_get_builder()` + `chromemanagement_customers_profiles_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn chromemanagement_customers_profiles_get(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleChromeManagementVersionsV1ChromeBrowserProfile>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = chromemanagement_customers_profiles_get_builder(client, name)?;
    chromemanagement_customers_profiles_get_execute(builder)
}

/// GET v1/{+parent}/profiles
/// Lists Chrome browser profiles of a customer based on the given search and sorting criteria.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `chromemanagement_customers_profiles_list_execute()` to send, or `chromemanagement_customers_profiles_list` for simplest API.

pub fn chromemanagement_customers_profiles_list_builder(
    client: &SimpleHttpClient,
    parent: &str,
    filter: Option<&str>,
    orderBy: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/{+parent}/profiles", parent,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "filter",
            filter
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "orderBy",
            orderBy
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageSize",
            pageSize
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageToken",
            pageToken
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET v1/{+parent}/profiles
/// Lists Chrome browser profiles of a customer based on the given search and sorting criteria.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `chromemanagement_customers_profiles_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from chromemanagement_customers_profiles_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn chromemanagement_customers_profiles_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleChromeManagementVersionsV1ListChromeBrowserProfilesResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleChromeManagementVersionsV1ListChromeBrowserProfilesResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+parent}/profiles
/// Lists Chrome browser profiles of a customer based on the given search and sorting criteria.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `chromemanagement_customers_profiles_list_builder()` + `chromemanagement_customers_profiles_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn chromemanagement_customers_profiles_list(
    client: &SimpleHttpClient,
    parent: &str,
    filter: Option<&str>,
    orderBy: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleChromeManagementVersionsV1ListChromeBrowserProfilesResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = chromemanagement_customers_profiles_list_builder(
        client, parent, filter, orderBy, pageSize, pageToken,
    )?;
    chromemanagement_customers_profiles_list_execute(builder)
}

/// GET v1/{+parent}/commands
/// Creates a Chrome browser profile remote command.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `chromemanagement_customers_profiles_commands_create_execute()` to send, or `chromemanagement_customers_profiles_commands_create` for simplest API.

pub fn chromemanagement_customers_profiles_commands_create_builder(
    client: &SimpleHttpClient,
    parent: &str,
    body: &GoogleChromeManagementVersionsV1ChromeBrowserProfileCommand,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/{+parent}/commands", parent,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v1/{+parent}/commands
/// Creates a Chrome browser profile remote command.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `chromemanagement_customers_profiles_commands_create_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from chromemanagement_customers_profiles_commands_create_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn chromemanagement_customers_profiles_commands_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleChromeManagementVersionsV1ChromeBrowserProfileCommand>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleChromeManagementVersionsV1ChromeBrowserProfileCommand =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+parent}/commands
/// Creates a Chrome browser profile remote command.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `chromemanagement_customers_profiles_commands_create_builder()` + `chromemanagement_customers_profiles_commands_create_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn chromemanagement_customers_profiles_commands_create(
    client: &SimpleHttpClient,
    parent: &str,
    body: &GoogleChromeManagementVersionsV1ChromeBrowserProfileCommand,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleChromeManagementVersionsV1ChromeBrowserProfileCommand>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        chromemanagement_customers_profiles_commands_create_builder(client, parent, body)?;
    chromemanagement_customers_profiles_commands_create_execute(builder)
}

/// GET v1/{+name}
/// Gets a Chrome browser profile remote command.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `chromemanagement_customers_profiles_commands_get_execute()` to send, or `chromemanagement_customers_profiles_commands_get` for simplest API.

pub fn chromemanagement_customers_profiles_commands_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/{+name}", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+name}
/// Gets a Chrome browser profile remote command.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `chromemanagement_customers_profiles_commands_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from chromemanagement_customers_profiles_commands_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn chromemanagement_customers_profiles_commands_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleChromeManagementVersionsV1ChromeBrowserProfileCommand>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleChromeManagementVersionsV1ChromeBrowserProfileCommand =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+name}
/// Gets a Chrome browser profile remote command.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `chromemanagement_customers_profiles_commands_get_builder()` + `chromemanagement_customers_profiles_commands_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn chromemanagement_customers_profiles_commands_get(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleChromeManagementVersionsV1ChromeBrowserProfileCommand>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = chromemanagement_customers_profiles_commands_get_builder(client, name)?;
    chromemanagement_customers_profiles_commands_get_execute(builder)
}

/// GET v1/{+parent}/commands
/// Lists remote commands of a Chrome browser profile.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `chromemanagement_customers_profiles_commands_list_execute()` to send, or `chromemanagement_customers_profiles_commands_list` for simplest API.

pub fn chromemanagement_customers_profiles_commands_list_builder(
    client: &SimpleHttpClient,
    parent: &str,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/{+parent}/commands", parent,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "pageSize",
            pageSize
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageToken",
            pageToken
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET v1/{+parent}/commands
/// Lists remote commands of a Chrome browser profile.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `chromemanagement_customers_profiles_commands_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from chromemanagement_customers_profiles_commands_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn chromemanagement_customers_profiles_commands_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<
                    GoogleChromeManagementVersionsV1ListChromeBrowserProfileCommandsResponse,
                >,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleChromeManagementVersionsV1ListChromeBrowserProfileCommandsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+parent}/commands
/// Lists remote commands of a Chrome browser profile.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `chromemanagement_customers_profiles_commands_list_builder()` + `chromemanagement_customers_profiles_commands_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn chromemanagement_customers_profiles_commands_list(
    client: &SimpleHttpClient,
    parent: &str,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<
                    GoogleChromeManagementVersionsV1ListChromeBrowserProfileCommandsResponse,
                >,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = chromemanagement_customers_profiles_commands_list_builder(
        client, parent, pageSize, pageToken,
    )?;
    chromemanagement_customers_profiles_commands_list_execute(builder)
}

/// GET v1/{+customer}/reports:countActiveDevices
/// Get a count of active devices per set time frames.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `chromemanagement_customers_reports_count_active_devices_execute()` to send, or `chromemanagement_customers_reports_count_active_devices` for simplest API.

pub fn chromemanagement_customers_reports_count_active_devices_builder(
    client: &SimpleHttpClient,
    customer: &str,
    date_day: Option<i32>,
    date_month: Option<i32>,
    date_year: Option<i32>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comv1/{+customer}/reports:countActiveDevices",
        customer,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "date_day",
            date_day
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "date_month",
            date_month
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "date_year",
            date_year
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET v1/{+customer}/reports:countActiveDevices
/// Get a count of active devices per set time frames.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `chromemanagement_customers_reports_count_active_devices_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from chromemanagement_customers_reports_count_active_devices_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn chromemanagement_customers_reports_count_active_devices_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleChromeManagementV1CountActiveDevicesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleChromeManagementV1CountActiveDevicesResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+customer}/reports:countActiveDevices
/// Get a count of active devices per set time frames.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `chromemanagement_customers_reports_count_active_devices_builder()` + `chromemanagement_customers_reports_count_active_devices_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn chromemanagement_customers_reports_count_active_devices(
    client: &SimpleHttpClient,
    customer: &str,
    date_day: Option<i32>,
    date_month: Option<i32>,
    date_year: Option<i32>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleChromeManagementV1CountActiveDevicesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = chromemanagement_customers_reports_count_active_devices_builder(
        client, customer, date_day, date_month, date_year,
    )?;
    chromemanagement_customers_reports_count_active_devices_execute(builder)
}

/// GET v1/{+customer}/reports:countChromeBrowsersNeedingAttention
/// Count of Chrome Browsers that have been recently enrolled, have new policy to be synced, or have no recent activity.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `chromemanagement_customers_reports_count_chrome_browsers_needing_attention_execute()` to send, or `chromemanagement_customers_reports_count_chrome_browsers_needing_attention` for simplest API.

pub fn chromemanagement_customers_reports_count_chrome_browsers_needing_attention_builder(
    client: &SimpleHttpClient,
    customer: &str,
    orgUnitId: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comv1/{+customer}/reports:countChromeBrowsersNeedingAttention",
        customer,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "orgUnitId",
            orgUnitId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET v1/{+customer}/reports:countChromeBrowsersNeedingAttention
/// Count of Chrome Browsers that have been recently enrolled, have new policy to be synced, or have no recent activity.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `chromemanagement_customers_reports_count_chrome_browsers_needing_attention_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from chromemanagement_customers_reports_count_chrome_browsers_needing_attention_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn chromemanagement_customers_reports_count_chrome_browsers_needing_attention_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleChromeManagementV1CountChromeBrowsersNeedingAttentionResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleChromeManagementV1CountChromeBrowsersNeedingAttentionResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+customer}/reports:countChromeBrowsersNeedingAttention
/// Count of Chrome Browsers that have been recently enrolled, have new policy to be synced, or have no recent activity.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `chromemanagement_customers_reports_count_chrome_browsers_needing_attention_builder()` + `chromemanagement_customers_reports_count_chrome_browsers_needing_attention_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn chromemanagement_customers_reports_count_chrome_browsers_needing_attention(
    client: &SimpleHttpClient,
    customer: &str,
    orgUnitId: Option<&str>,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleChromeManagementV1CountChromeBrowsersNeedingAttentionResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        chromemanagement_customers_reports_count_chrome_browsers_needing_attention_builder(
            client, customer, orgUnitId,
        )?;
    chromemanagement_customers_reports_count_chrome_browsers_needing_attention_execute(builder)
}

/// GET v1/{+customer}/reports:countChromeCrashEvents
/// Get a count of Chrome crash events.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `chromemanagement_customers_reports_count_chrome_crash_events_execute()` to send, or `chromemanagement_customers_reports_count_chrome_crash_events` for simplest API.

pub fn chromemanagement_customers_reports_count_chrome_crash_events_builder(
    client: &SimpleHttpClient,
    customer: &str,
    filter: Option<&str>,
    orderBy: Option<&str>,
    orgUnitId: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comv1/{+customer}/reports:countChromeCrashEvents",
        customer,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "filter",
            filter
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "orderBy",
            orderBy
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "orgUnitId",
            orgUnitId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET v1/{+customer}/reports:countChromeCrashEvents
/// Get a count of Chrome crash events.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `chromemanagement_customers_reports_count_chrome_crash_events_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from chromemanagement_customers_reports_count_chrome_crash_events_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn chromemanagement_customers_reports_count_chrome_crash_events_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleChromeManagementV1CountChromeCrashEventsResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleChromeManagementV1CountChromeCrashEventsResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+customer}/reports:countChromeCrashEvents
/// Get a count of Chrome crash events.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `chromemanagement_customers_reports_count_chrome_crash_events_builder()` + `chromemanagement_customers_reports_count_chrome_crash_events_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn chromemanagement_customers_reports_count_chrome_crash_events(
    client: &SimpleHttpClient,
    customer: &str,
    filter: Option<&str>,
    orderBy: Option<&str>,
    orgUnitId: Option<&str>,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleChromeManagementV1CountChromeCrashEventsResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = chromemanagement_customers_reports_count_chrome_crash_events_builder(
        client, customer, filter, orderBy, orgUnitId,
    )?;
    chromemanagement_customers_reports_count_chrome_crash_events_execute(builder)
}

/// GET v1/{+customer}/reports:countChromeDevicesReachingAutoExpirationDate
/// Generate report of the number of devices expiring in each month of the selected time frame. Devices are grouped by auto update expiration date and model. Further information can be found [here](<https://support.google.`com/chrome/a/answer/10564947`>).
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `chromemanagement_customers_reports_count_chrome_devices_reaching_auto_expiration_date_execute()` to send, or `chromemanagement_customers_reports_count_chrome_devices_reaching_auto_expiration_date` for simplest API.

pub fn chromemanagement_customers_reports_count_chrome_devices_reaching_auto_expiration_date_builder(
    client: &SimpleHttpClient,
    customer: &str,
    maxAueDate: Option<&str>,
    minAueDate: Option<&str>,
    orgUnitId: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comv1/{+customer}/reports:countChromeDevicesReachingAutoExpirationDate",
        customer,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "maxAueDate",
            maxAueDate
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "minAueDate",
            minAueDate
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "orgUnitId",
            orgUnitId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET v1/{+customer}/reports:countChromeDevicesReachingAutoExpirationDate
/// Generate report of the number of devices expiring in each month of the selected time frame. Devices are grouped by auto update expiration date and model. Further information can be found [here](<https://support.google.`com/chrome/a/answer/10564947`>).
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `chromemanagement_customers_reports_count_chrome_devices_reaching_auto_expiration_date_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from chromemanagement_customers_reports_count_chrome_devices_reaching_auto_expiration_date_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn chromemanagement_customers_reports_count_chrome_devices_reaching_auto_expiration_date_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<
                    GoogleChromeManagementV1CountChromeDevicesReachingAutoExpirationDateResponse,
                >,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleChromeManagementV1CountChromeDevicesReachingAutoExpirationDateResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+customer}/reports:countChromeDevicesReachingAutoExpirationDate
/// Generate report of the number of devices expiring in each month of the selected time frame. Devices are grouped by auto update expiration date and model. Further information can be found [here](<https://support.google.`com/chrome/a/answer/10564947`>).
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `chromemanagement_customers_reports_count_chrome_devices_reaching_auto_expiration_date_builder()` + `chromemanagement_customers_reports_count_chrome_devices_reaching_auto_expiration_date_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn chromemanagement_customers_reports_count_chrome_devices_reaching_auto_expiration_date(
    client: &SimpleHttpClient,
    customer: &str,
    maxAueDate: Option<&str>,
    minAueDate: Option<&str>,
    orgUnitId: Option<&str>,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<
                    GoogleChromeManagementV1CountChromeDevicesReachingAutoExpirationDateResponse,
                >,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = chromemanagement_customers_reports_count_chrome_devices_reaching_auto_expiration_date_builder(client, customer, maxAueDate, minAueDate, orgUnitId)?;
    chromemanagement_customers_reports_count_chrome_devices_reaching_auto_expiration_date_execute(
        builder,
    )
}

/// GET v1/{+customer}/reports:countChromeDevicesThatNeedAttention
/// Counts of ChromeOS devices that have not synced policies or have lacked user activity in the past 28 days, are out of date, or are not complaint. Further information can be found here <https://support.google.`com/chrome/a/answer/10564947`>
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `chromemanagement_customers_reports_count_chrome_devices_that_need_attention_execute()` to send, or `chromemanagement_customers_reports_count_chrome_devices_that_need_attention` for simplest API.

pub fn chromemanagement_customers_reports_count_chrome_devices_that_need_attention_builder(
    client: &SimpleHttpClient,
    customer: &str,
    orgUnitId: Option<&str>,
    readMask: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comv1/{+customer}/reports:countChromeDevicesThatNeedAttention",
        customer,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "orgUnitId",
            orgUnitId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "readMask",
            readMask
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET v1/{+customer}/reports:countChromeDevicesThatNeedAttention
/// Counts of ChromeOS devices that have not synced policies or have lacked user activity in the past 28 days, are out of date, or are not complaint. Further information can be found here <https://support.google.`com/chrome/a/answer/10564947`>
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `chromemanagement_customers_reports_count_chrome_devices_that_need_attention_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from chromemanagement_customers_reports_count_chrome_devices_that_need_attention_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn chromemanagement_customers_reports_count_chrome_devices_that_need_attention_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleChromeManagementV1CountChromeDevicesThatNeedAttentionResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleChromeManagementV1CountChromeDevicesThatNeedAttentionResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+customer}/reports:countChromeDevicesThatNeedAttention
/// Counts of ChromeOS devices that have not synced policies or have lacked user activity in the past 28 days, are out of date, or are not complaint. Further information can be found here <https://support.google.`com/chrome/a/answer/10564947`>
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `chromemanagement_customers_reports_count_chrome_devices_that_need_attention_builder()` + `chromemanagement_customers_reports_count_chrome_devices_that_need_attention_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn chromemanagement_customers_reports_count_chrome_devices_that_need_attention(
    client: &SimpleHttpClient,
    customer: &str,
    orgUnitId: Option<&str>,
    readMask: Option<&str>,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleChromeManagementV1CountChromeDevicesThatNeedAttentionResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        chromemanagement_customers_reports_count_chrome_devices_that_need_attention_builder(
            client, customer, orgUnitId, readMask,
        )?;
    chromemanagement_customers_reports_count_chrome_devices_that_need_attention_execute(builder)
}

/// GET v1/{+customer}/reports:countChromeHardwareFleetDevices
/// Counts of devices with a specific hardware specification from the requested hardware type (for example model name, processor type). Further information can be found here <https://support.google.`com/chrome/a/answer/10564947`>
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `chromemanagement_customers_reports_count_chrome_hardware_fleet_devices_execute()` to send, or `chromemanagement_customers_reports_count_chrome_hardware_fleet_devices` for simplest API.

pub fn chromemanagement_customers_reports_count_chrome_hardware_fleet_devices_builder(
    client: &SimpleHttpClient,
    customer: &str,
    orgUnitId: Option<&str>,
    readMask: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comv1/{+customer}/reports:countChromeHardwareFleetDevices",
        customer,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "orgUnitId",
            orgUnitId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "readMask",
            readMask
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET v1/{+customer}/reports:countChromeHardwareFleetDevices
/// Counts of devices with a specific hardware specification from the requested hardware type (for example model name, processor type). Further information can be found here <https://support.google.`com/chrome/a/answer/10564947`>
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `chromemanagement_customers_reports_count_chrome_hardware_fleet_devices_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from chromemanagement_customers_reports_count_chrome_hardware_fleet_devices_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn chromemanagement_customers_reports_count_chrome_hardware_fleet_devices_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleChromeManagementV1CountChromeHardwareFleetDevicesResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleChromeManagementV1CountChromeHardwareFleetDevicesResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+customer}/reports:countChromeHardwareFleetDevices
/// Counts of devices with a specific hardware specification from the requested hardware type (for example model name, processor type). Further information can be found here <https://support.google.`com/chrome/a/answer/10564947`>
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `chromemanagement_customers_reports_count_chrome_hardware_fleet_devices_builder()` + `chromemanagement_customers_reports_count_chrome_hardware_fleet_devices_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn chromemanagement_customers_reports_count_chrome_hardware_fleet_devices(
    client: &SimpleHttpClient,
    customer: &str,
    orgUnitId: Option<&str>,
    readMask: Option<&str>,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleChromeManagementV1CountChromeHardwareFleetDevicesResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = chromemanagement_customers_reports_count_chrome_hardware_fleet_devices_builder(
        client, customer, orgUnitId, readMask,
    )?;
    chromemanagement_customers_reports_count_chrome_hardware_fleet_devices_execute(builder)
}

/// GET v1/{+customer}/reports:countChromeVersions
/// Generate report of installed Chrome versions.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `chromemanagement_customers_reports_count_chrome_versions_execute()` to send, or `chromemanagement_customers_reports_count_chrome_versions` for simplest API.

pub fn chromemanagement_customers_reports_count_chrome_versions_builder(
    client: &SimpleHttpClient,
    customer: &str,
    filter: Option<&str>,
    orgUnitId: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comv1/{+customer}/reports:countChromeVersions",
        customer,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "filter",
            filter
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "orgUnitId",
            orgUnitId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageSize",
            pageSize
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageToken",
            pageToken
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET v1/{+customer}/reports:countChromeVersions
/// Generate report of installed Chrome versions.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `chromemanagement_customers_reports_count_chrome_versions_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from chromemanagement_customers_reports_count_chrome_versions_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn chromemanagement_customers_reports_count_chrome_versions_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleChromeManagementV1CountChromeVersionsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleChromeManagementV1CountChromeVersionsResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+customer}/reports:countChromeVersions
/// Generate report of installed Chrome versions.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `chromemanagement_customers_reports_count_chrome_versions_builder()` + `chromemanagement_customers_reports_count_chrome_versions_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn chromemanagement_customers_reports_count_chrome_versions(
    client: &SimpleHttpClient,
    customer: &str,
    filter: Option<&str>,
    orgUnitId: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleChromeManagementV1CountChromeVersionsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = chromemanagement_customers_reports_count_chrome_versions_builder(
        client, customer, filter, orgUnitId, pageSize, pageToken,
    )?;
    chromemanagement_customers_reports_count_chrome_versions_execute(builder)
}

/// GET v1/{+customer}/reports:countDevicesPerBootType
/// Get a count of devices per boot type.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `chromemanagement_customers_reports_count_devices_per_boot_type_execute()` to send, or `chromemanagement_customers_reports_count_devices_per_boot_type` for simplest API.

pub fn chromemanagement_customers_reports_count_devices_per_boot_type_builder(
    client: &SimpleHttpClient,
    customer: &str,
    date_day: Option<i32>,
    date_month: Option<i32>,
    date_year: Option<i32>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comv1/{+customer}/reports:countDevicesPerBootType",
        customer,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "date_day",
            date_day
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "date_month",
            date_month
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "date_year",
            date_year
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET v1/{+customer}/reports:countDevicesPerBootType
/// Get a count of devices per boot type.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `chromemanagement_customers_reports_count_devices_per_boot_type_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from chromemanagement_customers_reports_count_devices_per_boot_type_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn chromemanagement_customers_reports_count_devices_per_boot_type_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleChromeManagementV1CountDevicesPerBootTypeResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleChromeManagementV1CountDevicesPerBootTypeResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+customer}/reports:countDevicesPerBootType
/// Get a count of devices per boot type.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `chromemanagement_customers_reports_count_devices_per_boot_type_builder()` + `chromemanagement_customers_reports_count_devices_per_boot_type_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn chromemanagement_customers_reports_count_devices_per_boot_type(
    client: &SimpleHttpClient,
    customer: &str,
    date_day: Option<i32>,
    date_month: Option<i32>,
    date_year: Option<i32>,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleChromeManagementV1CountDevicesPerBootTypeResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = chromemanagement_customers_reports_count_devices_per_boot_type_builder(
        client, customer, date_day, date_month, date_year,
    )?;
    chromemanagement_customers_reports_count_devices_per_boot_type_execute(builder)
}

/// GET v1/{+customer}/reports:countDevicesPerReleaseChannel
/// Get a count of devices per channel.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `chromemanagement_customers_reports_count_devices_per_release_channel_execute()` to send, or `chromemanagement_customers_reports_count_devices_per_release_channel` for simplest API.

pub fn chromemanagement_customers_reports_count_devices_per_release_channel_builder(
    client: &SimpleHttpClient,
    customer: &str,
    date_day: Option<i32>,
    date_month: Option<i32>,
    date_year: Option<i32>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comv1/{+customer}/reports:countDevicesPerReleaseChannel",
        customer,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "date_day",
            date_day
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "date_month",
            date_month
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "date_year",
            date_year
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET v1/{+customer}/reports:countDevicesPerReleaseChannel
/// Get a count of devices per channel.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `chromemanagement_customers_reports_count_devices_per_release_channel_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from chromemanagement_customers_reports_count_devices_per_release_channel_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn chromemanagement_customers_reports_count_devices_per_release_channel_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleChromeManagementV1CountDevicesPerReleaseChannelResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleChromeManagementV1CountDevicesPerReleaseChannelResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+customer}/reports:countDevicesPerReleaseChannel
/// Get a count of devices per channel.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `chromemanagement_customers_reports_count_devices_per_release_channel_builder()` + `chromemanagement_customers_reports_count_devices_per_release_channel_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn chromemanagement_customers_reports_count_devices_per_release_channel(
    client: &SimpleHttpClient,
    customer: &str,
    date_day: Option<i32>,
    date_month: Option<i32>,
    date_year: Option<i32>,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleChromeManagementV1CountDevicesPerReleaseChannelResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = chromemanagement_customers_reports_count_devices_per_release_channel_builder(
        client, customer, date_day, date_month, date_year,
    )?;
    chromemanagement_customers_reports_count_devices_per_release_channel_execute(builder)
}

/// GET v1/{+customer}/reports:countInstalledApps
/// Generate report of app installations.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `chromemanagement_customers_reports_count_installed_apps_execute()` to send, or `chromemanagement_customers_reports_count_installed_apps` for simplest API.

pub fn chromemanagement_customers_reports_count_installed_apps_builder(
    client: &SimpleHttpClient,
    customer: &str,
    filter: Option<&str>,
    orderBy: Option<&str>,
    orgUnitId: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comv1/{+customer}/reports:countInstalledApps",
        customer,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "filter",
            filter
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "orderBy",
            orderBy
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "orgUnitId",
            orgUnitId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageSize",
            pageSize
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageToken",
            pageToken
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET v1/{+customer}/reports:countInstalledApps
/// Generate report of app installations.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `chromemanagement_customers_reports_count_installed_apps_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from chromemanagement_customers_reports_count_installed_apps_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn chromemanagement_customers_reports_count_installed_apps_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleChromeManagementV1CountInstalledAppsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleChromeManagementV1CountInstalledAppsResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+customer}/reports:countInstalledApps
/// Generate report of app installations.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `chromemanagement_customers_reports_count_installed_apps_builder()` + `chromemanagement_customers_reports_count_installed_apps_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn chromemanagement_customers_reports_count_installed_apps(
    client: &SimpleHttpClient,
    customer: &str,
    filter: Option<&str>,
    orderBy: Option<&str>,
    orgUnitId: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleChromeManagementV1CountInstalledAppsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = chromemanagement_customers_reports_count_installed_apps_builder(
        client, customer, filter, orderBy, orgUnitId, pageSize, pageToken,
    )?;
    chromemanagement_customers_reports_count_installed_apps_execute(builder)
}

/// GET v1/{+customer}/reports:countPrintJobsByPrinter
/// Get a summary of printing done by each printer.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `chromemanagement_customers_reports_count_print_jobs_by_printer_execute()` to send, or `chromemanagement_customers_reports_count_print_jobs_by_printer` for simplest API.

pub fn chromemanagement_customers_reports_count_print_jobs_by_printer_builder(
    client: &SimpleHttpClient,
    customer: &str,
    filter: Option<&str>,
    orderBy: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
    printerOrgUnitId: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comv1/{+customer}/reports:countPrintJobsByPrinter",
        customer,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "filter",
            filter
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "orderBy",
            orderBy
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageSize",
            pageSize
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageToken",
            pageToken
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "printerOrgUnitId",
            printerOrgUnitId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET v1/{+customer}/reports:countPrintJobsByPrinter
/// Get a summary of printing done by each printer.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `chromemanagement_customers_reports_count_print_jobs_by_printer_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from chromemanagement_customers_reports_count_print_jobs_by_printer_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn chromemanagement_customers_reports_count_print_jobs_by_printer_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleChromeManagementV1CountPrintJobsByPrinterResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleChromeManagementV1CountPrintJobsByPrinterResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+customer}/reports:countPrintJobsByPrinter
/// Get a summary of printing done by each printer.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `chromemanagement_customers_reports_count_print_jobs_by_printer_builder()` + `chromemanagement_customers_reports_count_print_jobs_by_printer_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn chromemanagement_customers_reports_count_print_jobs_by_printer(
    client: &SimpleHttpClient,
    customer: &str,
    filter: Option<&str>,
    orderBy: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
    printerOrgUnitId: Option<&str>,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleChromeManagementV1CountPrintJobsByPrinterResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = chromemanagement_customers_reports_count_print_jobs_by_printer_builder(
        client,
        customer,
        filter,
        orderBy,
        pageSize,
        pageToken,
        printerOrgUnitId,
    )?;
    chromemanagement_customers_reports_count_print_jobs_by_printer_execute(builder)
}

/// GET v1/{+customer}/reports:countPrintJobsByUser
/// Get a summary of printing done by each user.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `chromemanagement_customers_reports_count_print_jobs_by_user_execute()` to send, or `chromemanagement_customers_reports_count_print_jobs_by_user` for simplest API.

pub fn chromemanagement_customers_reports_count_print_jobs_by_user_builder(
    client: &SimpleHttpClient,
    customer: &str,
    filter: Option<&str>,
    orderBy: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
    printerOrgUnitId: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comv1/{+customer}/reports:countPrintJobsByUser",
        customer,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "filter",
            filter
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "orderBy",
            orderBy
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageSize",
            pageSize
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageToken",
            pageToken
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "printerOrgUnitId",
            printerOrgUnitId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET v1/{+customer}/reports:countPrintJobsByUser
/// Get a summary of printing done by each user.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `chromemanagement_customers_reports_count_print_jobs_by_user_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from chromemanagement_customers_reports_count_print_jobs_by_user_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn chromemanagement_customers_reports_count_print_jobs_by_user_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleChromeManagementV1CountPrintJobsByUserResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleChromeManagementV1CountPrintJobsByUserResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+customer}/reports:countPrintJobsByUser
/// Get a summary of printing done by each user.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `chromemanagement_customers_reports_count_print_jobs_by_user_builder()` + `chromemanagement_customers_reports_count_print_jobs_by_user_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn chromemanagement_customers_reports_count_print_jobs_by_user(
    client: &SimpleHttpClient,
    customer: &str,
    filter: Option<&str>,
    orderBy: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
    printerOrgUnitId: Option<&str>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleChromeManagementV1CountPrintJobsByUserResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = chromemanagement_customers_reports_count_print_jobs_by_user_builder(
        client,
        customer,
        filter,
        orderBy,
        pageSize,
        pageToken,
        printerOrgUnitId,
    )?;
    chromemanagement_customers_reports_count_print_jobs_by_user_execute(builder)
}

/// GET v1/{+customer}/reports:enumeratePrintJobs
/// Get a list of print jobs.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `chromemanagement_customers_reports_enumerate_print_jobs_execute()` to send, or `chromemanagement_customers_reports_enumerate_print_jobs` for simplest API.

pub fn chromemanagement_customers_reports_enumerate_print_jobs_builder(
    client: &SimpleHttpClient,
    customer: &str,
    filter: Option<&str>,
    orderBy: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
    printerOrgUnitId: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comv1/{+customer}/reports:enumeratePrintJobs",
        customer,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "filter",
            filter
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "orderBy",
            orderBy
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageSize",
            pageSize
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageToken",
            pageToken
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "printerOrgUnitId",
            printerOrgUnitId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET v1/{+customer}/reports:enumeratePrintJobs
/// Get a list of print jobs.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `chromemanagement_customers_reports_enumerate_print_jobs_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from chromemanagement_customers_reports_enumerate_print_jobs_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn chromemanagement_customers_reports_enumerate_print_jobs_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleChromeManagementV1EnumeratePrintJobsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleChromeManagementV1EnumeratePrintJobsResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+customer}/reports:enumeratePrintJobs
/// Get a list of print jobs.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `chromemanagement_customers_reports_enumerate_print_jobs_builder()` + `chromemanagement_customers_reports_enumerate_print_jobs_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn chromemanagement_customers_reports_enumerate_print_jobs(
    client: &SimpleHttpClient,
    customer: &str,
    filter: Option<&str>,
    orderBy: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
    printerOrgUnitId: Option<&str>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleChromeManagementV1EnumeratePrintJobsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = chromemanagement_customers_reports_enumerate_print_jobs_builder(
        client,
        customer,
        filter,
        orderBy,
        pageSize,
        pageToken,
        printerOrgUnitId,
    )?;
    chromemanagement_customers_reports_enumerate_print_jobs_execute(builder)
}

/// GET v1/{+customer}/reports:findInstalledAppDevices
/// Generate report of managed Chrome browser devices that have a specified app installed.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `chromemanagement_customers_reports_find_installed_app_devices_execute()` to send, or `chromemanagement_customers_reports_find_installed_app_devices` for simplest API.

pub fn chromemanagement_customers_reports_find_installed_app_devices_builder(
    client: &SimpleHttpClient,
    customer: &str,
    appId: Option<&str>,
    appType: Option<&str>,
    filter: Option<&str>,
    orderBy: Option<&str>,
    orgUnitId: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comv1/{+customer}/reports:findInstalledAppDevices",
        customer,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "appId",
            appId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "appType",
            appType
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "filter",
            filter
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "orderBy",
            orderBy
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "orgUnitId",
            orgUnitId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageSize",
            pageSize
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageToken",
            pageToken
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET v1/{+customer}/reports:findInstalledAppDevices
/// Generate report of managed Chrome browser devices that have a specified app installed.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `chromemanagement_customers_reports_find_installed_app_devices_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from chromemanagement_customers_reports_find_installed_app_devices_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn chromemanagement_customers_reports_find_installed_app_devices_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleChromeManagementV1FindInstalledAppDevicesResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleChromeManagementV1FindInstalledAppDevicesResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+customer}/reports:findInstalledAppDevices
/// Generate report of managed Chrome browser devices that have a specified app installed.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `chromemanagement_customers_reports_find_installed_app_devices_builder()` + `chromemanagement_customers_reports_find_installed_app_devices_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn chromemanagement_customers_reports_find_installed_app_devices(
    client: &SimpleHttpClient,
    customer: &str,
    appId: Option<&str>,
    appType: Option<&str>,
    filter: Option<&str>,
    orderBy: Option<&str>,
    orgUnitId: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleChromeManagementV1FindInstalledAppDevicesResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = chromemanagement_customers_reports_find_installed_app_devices_builder(
        client, customer, appId, appType, filter, orderBy, orgUnitId, pageSize, pageToken,
    )?;
    chromemanagement_customers_reports_find_installed_app_devices_execute(builder)
}

/// GET v1/{+name}
/// Get telemetry device.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `chromemanagement_customers_telemetry_devices_get_execute()` to send, or `chromemanagement_customers_telemetry_devices_get` for simplest API.

pub fn chromemanagement_customers_telemetry_devices_get_builder(
    client: &SimpleHttpClient,
    name: &str,
    readMask: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/{+name}", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "readMask",
            readMask
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET v1/{+name}
/// Get telemetry device.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `chromemanagement_customers_telemetry_devices_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from chromemanagement_customers_telemetry_devices_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn chromemanagement_customers_telemetry_devices_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleChromeManagementV1TelemetryDevice>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleChromeManagementV1TelemetryDevice =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+name}
/// Get telemetry device.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `chromemanagement_customers_telemetry_devices_get_builder()` + `chromemanagement_customers_telemetry_devices_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn chromemanagement_customers_telemetry_devices_get(
    client: &SimpleHttpClient,
    name: &str,
    readMask: Option<&str>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleChromeManagementV1TelemetryDevice>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = chromemanagement_customers_telemetry_devices_get_builder(client, name, readMask)?;
    chromemanagement_customers_telemetry_devices_get_execute(builder)
}

/// GET v1/{+parent}/telemetry/devices
/// List all telemetry devices.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `chromemanagement_customers_telemetry_devices_list_execute()` to send, or `chromemanagement_customers_telemetry_devices_list` for simplest API.

pub fn chromemanagement_customers_telemetry_devices_list_builder(
    client: &SimpleHttpClient,
    parent: &str,
    filter: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
    readMask: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comv1/{+parent}/telemetry/devices",
        parent,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "filter",
            filter
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageSize",
            pageSize
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageToken",
            pageToken
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "readMask",
            readMask
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET v1/{+parent}/telemetry/devices
/// List all telemetry devices.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `chromemanagement_customers_telemetry_devices_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from chromemanagement_customers_telemetry_devices_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn chromemanagement_customers_telemetry_devices_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleChromeManagementV1ListTelemetryDevicesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleChromeManagementV1ListTelemetryDevicesResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+parent}/telemetry/devices
/// List all telemetry devices.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `chromemanagement_customers_telemetry_devices_list_builder()` + `chromemanagement_customers_telemetry_devices_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn chromemanagement_customers_telemetry_devices_list(
    client: &SimpleHttpClient,
    parent: &str,
    filter: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
    readMask: Option<&str>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleChromeManagementV1ListTelemetryDevicesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = chromemanagement_customers_telemetry_devices_list_builder(
        client, parent, filter, pageSize, pageToken, readMask,
    )?;
    chromemanagement_customers_telemetry_devices_list_execute(builder)
}

/// GET v1/{+parent}/telemetry/events
/// List telemetry events.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `chromemanagement_customers_telemetry_events_list_execute()` to send, or `chromemanagement_customers_telemetry_events_list` for simplest API.

pub fn chromemanagement_customers_telemetry_events_list_builder(
    client: &SimpleHttpClient,
    parent: &str,
    filter: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
    readMask: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comv1/{+parent}/telemetry/events",
        parent,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "filter",
            filter
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageSize",
            pageSize
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageToken",
            pageToken
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "readMask",
            readMask
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET v1/{+parent}/telemetry/events
/// List telemetry events.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `chromemanagement_customers_telemetry_events_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from chromemanagement_customers_telemetry_events_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn chromemanagement_customers_telemetry_events_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleChromeManagementV1ListTelemetryEventsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleChromeManagementV1ListTelemetryEventsResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+parent}/telemetry/events
/// List telemetry events.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `chromemanagement_customers_telemetry_events_list_builder()` + `chromemanagement_customers_telemetry_events_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn chromemanagement_customers_telemetry_events_list(
    client: &SimpleHttpClient,
    parent: &str,
    filter: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
    readMask: Option<&str>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleChromeManagementV1ListTelemetryEventsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = chromemanagement_customers_telemetry_events_list_builder(
        client, parent, filter, pageSize, pageToken, readMask,
    )?;
    chromemanagement_customers_telemetry_events_list_execute(builder)
}

/// GET v1/{+parent}/telemetry/notificationConfigs
/// Create a telemetry notification config.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `chromemanagement_customers_telemetry_notification_configs_create_execute()` to send, or `chromemanagement_customers_telemetry_notification_configs_create` for simplest API.

pub fn chromemanagement_customers_telemetry_notification_configs_create_builder(
    client: &SimpleHttpClient,
    parent: &str,
    body: &GoogleChromeManagementV1TelemetryNotificationConfig,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comv1/{+parent}/telemetry/notificationConfigs",
        parent,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v1/{+parent}/telemetry/notificationConfigs
/// Create a telemetry notification config.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `chromemanagement_customers_telemetry_notification_configs_create_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from chromemanagement_customers_telemetry_notification_configs_create_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn chromemanagement_customers_telemetry_notification_configs_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleChromeManagementV1TelemetryNotificationConfig>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleChromeManagementV1TelemetryNotificationConfig =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+parent}/telemetry/notificationConfigs
/// Create a telemetry notification config.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `chromemanagement_customers_telemetry_notification_configs_create_builder()` + `chromemanagement_customers_telemetry_notification_configs_create_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn chromemanagement_customers_telemetry_notification_configs_create(
    client: &SimpleHttpClient,
    parent: &str,
    body: &GoogleChromeManagementV1TelemetryNotificationConfig,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleChromeManagementV1TelemetryNotificationConfig>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = chromemanagement_customers_telemetry_notification_configs_create_builder(
        client, parent, body,
    )?;
    chromemanagement_customers_telemetry_notification_configs_create_execute(builder)
}

/// GET v1/{+name}
/// Delete a telemetry notification config.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `chromemanagement_customers_telemetry_notification_configs_delete_execute()` to send, or `chromemanagement_customers_telemetry_notification_configs_delete` for simplest API.

pub fn chromemanagement_customers_telemetry_notification_configs_delete_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/{+name}", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+name}
/// Delete a telemetry notification config.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `chromemanagement_customers_telemetry_notification_configs_delete_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from chromemanagement_customers_telemetry_notification_configs_delete_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn chromemanagement_customers_telemetry_notification_configs_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleProtobufEmpty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+name}
/// Delete a telemetry notification config.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `chromemanagement_customers_telemetry_notification_configs_delete_builder()` + `chromemanagement_customers_telemetry_notification_configs_delete_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn chromemanagement_customers_telemetry_notification_configs_delete(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        chromemanagement_customers_telemetry_notification_configs_delete_builder(client, name)?;
    chromemanagement_customers_telemetry_notification_configs_delete_execute(builder)
}

/// GET v1/{+parent}/telemetry/notificationConfigs
/// List all telemetry notification configs.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `chromemanagement_customers_telemetry_notification_configs_list_execute()` to send, or `chromemanagement_customers_telemetry_notification_configs_list` for simplest API.

pub fn chromemanagement_customers_telemetry_notification_configs_list_builder(
    client: &SimpleHttpClient,
    parent: &str,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comv1/{+parent}/telemetry/notificationConfigs",
        parent,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "pageSize",
            pageSize
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageToken",
            pageToken
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET v1/{+parent}/telemetry/notificationConfigs
/// List all telemetry notification configs.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `chromemanagement_customers_telemetry_notification_configs_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from chromemanagement_customers_telemetry_notification_configs_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn chromemanagement_customers_telemetry_notification_configs_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleChromeManagementV1ListTelemetryNotificationConfigsResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleChromeManagementV1ListTelemetryNotificationConfigsResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+parent}/telemetry/notificationConfigs
/// List all telemetry notification configs.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `chromemanagement_customers_telemetry_notification_configs_list_builder()` + `chromemanagement_customers_telemetry_notification_configs_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn chromemanagement_customers_telemetry_notification_configs_list(
    client: &SimpleHttpClient,
    parent: &str,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleChromeManagementV1ListTelemetryNotificationConfigsResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = chromemanagement_customers_telemetry_notification_configs_list_builder(
        client, parent, pageSize, pageToken,
    )?;
    chromemanagement_customers_telemetry_notification_configs_list_execute(builder)
}

/// GET v1/{+name}
/// Get telemetry user.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `chromemanagement_customers_telemetry_users_get_execute()` to send, or `chromemanagement_customers_telemetry_users_get` for simplest API.

pub fn chromemanagement_customers_telemetry_users_get_builder(
    client: &SimpleHttpClient,
    name: &str,
    readMask: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/{+name}", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "readMask",
            readMask
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET v1/{+name}
/// Get telemetry user.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `chromemanagement_customers_telemetry_users_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from chromemanagement_customers_telemetry_users_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn chromemanagement_customers_telemetry_users_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleChromeManagementV1TelemetryUser>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleChromeManagementV1TelemetryUser = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+name}
/// Get telemetry user.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `chromemanagement_customers_telemetry_users_get_builder()` + `chromemanagement_customers_telemetry_users_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn chromemanagement_customers_telemetry_users_get(
    client: &SimpleHttpClient,
    name: &str,
    readMask: Option<&str>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleChromeManagementV1TelemetryUser>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = chromemanagement_customers_telemetry_users_get_builder(client, name, readMask)?;
    chromemanagement_customers_telemetry_users_get_execute(builder)
}

/// GET v1/{+parent}/telemetry/users
/// List all telemetry users.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `chromemanagement_customers_telemetry_users_list_execute()` to send, or `chromemanagement_customers_telemetry_users_list` for simplest API.

pub fn chromemanagement_customers_telemetry_users_list_builder(
    client: &SimpleHttpClient,
    parent: &str,
    filter: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
    readMask: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comv1/{+parent}/telemetry/users",
        parent,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "filter",
            filter
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageSize",
            pageSize
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageToken",
            pageToken
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "readMask",
            readMask
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET v1/{+parent}/telemetry/users
/// List all telemetry users.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `chromemanagement_customers_telemetry_users_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from chromemanagement_customers_telemetry_users_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn chromemanagement_customers_telemetry_users_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleChromeManagementV1ListTelemetryUsersResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleChromeManagementV1ListTelemetryUsersResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+parent}/telemetry/users
/// List all telemetry users.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `chromemanagement_customers_telemetry_users_list_builder()` + `chromemanagement_customers_telemetry_users_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn chromemanagement_customers_telemetry_users_list(
    client: &SimpleHttpClient,
    parent: &str,
    filter: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
    readMask: Option<&str>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleChromeManagementV1ListTelemetryUsersResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = chromemanagement_customers_telemetry_users_list_builder(
        client, parent, filter, pageSize, pageToken, readMask,
    )?;
    chromemanagement_customers_telemetry_users_list_execute(builder)
}

/// GET v1/{+name}:move
/// Moves a third party chrome profile user to a destination OU. All profiles associated to that user will be moved to the destination OU.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `chromemanagement_customers_third_party_profile_users_move_execute()` to send, or `chromemanagement_customers_third_party_profile_users_move` for simplest API.

pub fn chromemanagement_customers_third_party_profile_users_move_builder(
    client: &SimpleHttpClient,
    name: &str,
    body: &GoogleChromeManagementVersionsV1MoveThirdPartyProfileUserRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/{+name}:move", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v1/{+name}:move
/// Moves a third party chrome profile user to a destination OU. All profiles associated to that user will be moved to the destination OU.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `chromemanagement_customers_third_party_profile_users_move_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from chromemanagement_customers_third_party_profile_users_move_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn chromemanagement_customers_third_party_profile_users_move_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleChromeManagementVersionsV1MoveThirdPartyProfileUserResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleChromeManagementVersionsV1MoveThirdPartyProfileUserResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+name}:move
/// Moves a third party chrome profile user to a destination OU. All profiles associated to that user will be moved to the destination OU.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `chromemanagement_customers_third_party_profile_users_move_builder()` + `chromemanagement_customers_third_party_profile_users_move_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn chromemanagement_customers_third_party_profile_users_move(
    client: &SimpleHttpClient,
    name: &str,
    body: &GoogleChromeManagementVersionsV1MoveThirdPartyProfileUserRequest,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleChromeManagementVersionsV1MoveThirdPartyProfileUserResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        chromemanagement_customers_third_party_profile_users_move_builder(client, name, body)?;
    chromemanagement_customers_third_party_profile_users_move_execute(builder)
}

/// GET v1/{+name}:cancel
/// Starts asynchronous cancellation on a long-running operation. The server makes a best effort to cancel the operation, but success is not guaranteed. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED. Clients can use Operations.GetOperation or other methods to check whether the cancellation succeeded or whether the operation completed despite cancellation. On successful cancellation, the operation is not deleted; instead, it becomes an operation with an Operation.error value with a google.rpc.Status.code of 1, corresponding to Code.CANCELLED.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `chromemanagement_operations_cancel_execute()` to send, or `chromemanagement_operations_cancel` for simplest API.

pub fn chromemanagement_operations_cancel_builder(
    client: &SimpleHttpClient,
    name: &str,
    body: &GoogleLongrunningCancelOperationRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/{+name}:cancel", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v1/{+name}:cancel
/// Starts asynchronous cancellation on a long-running operation. The server makes a best effort to cancel the operation, but success is not guaranteed. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED. Clients can use Operations.GetOperation or other methods to check whether the cancellation succeeded or whether the operation completed despite cancellation. On successful cancellation, the operation is not deleted; instead, it becomes an operation with an Operation.error value with a google.rpc.Status.code of 1, corresponding to Code.CANCELLED.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `chromemanagement_operations_cancel_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from chromemanagement_operations_cancel_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn chromemanagement_operations_cancel_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleProtobufEmpty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+name}:cancel
/// Starts asynchronous cancellation on a long-running operation. The server makes a best effort to cancel the operation, but success is not guaranteed. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED. Clients can use Operations.GetOperation or other methods to check whether the cancellation succeeded or whether the operation completed despite cancellation. On successful cancellation, the operation is not deleted; instead, it becomes an operation with an Operation.error value with a google.rpc.Status.code of 1, corresponding to Code.CANCELLED.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `chromemanagement_operations_cancel_builder()` + `chromemanagement_operations_cancel_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn chromemanagement_operations_cancel(
    client: &SimpleHttpClient,
    name: &str,
    body: &GoogleLongrunningCancelOperationRequest,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = chromemanagement_operations_cancel_builder(client, name, body)?;
    chromemanagement_operations_cancel_execute(builder)
}

/// GET v1/{+name}
/// Deletes a long-running operation. This method indicates that the client is no longer interested in the operation result. It does not cancel the operation. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `chromemanagement_operations_delete_execute()` to send, or `chromemanagement_operations_delete` for simplest API.

pub fn chromemanagement_operations_delete_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/{+name}", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+name}
/// Deletes a long-running operation. This method indicates that the client is no longer interested in the operation result. It does not cancel the operation. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `chromemanagement_operations_delete_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from chromemanagement_operations_delete_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn chromemanagement_operations_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleProtobufEmpty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+name}
/// Deletes a long-running operation. This method indicates that the client is no longer interested in the operation result. It does not cancel the operation. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `chromemanagement_operations_delete_builder()` + `chromemanagement_operations_delete_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn chromemanagement_operations_delete(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = chromemanagement_operations_delete_builder(client, name)?;
    chromemanagement_operations_delete_execute(builder)
}

/// GET v1/{+name}
/// Lists operations that match the specified filter in the request. If the server doesn't support this method, it returns UNIMPLEMENTED.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `chromemanagement_operations_list_execute()` to send, or `chromemanagement_operations_list` for simplest API.

pub fn chromemanagement_operations_list_builder(
    client: &SimpleHttpClient,
    name: &str,
    filter: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
    returnPartialSuccess: Option<bool>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/{+name}", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "filter",
            filter
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageSize",
            pageSize
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageToken",
            pageToken
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "returnPartialSuccess",
            returnPartialSuccess
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET v1/{+name}
/// Lists operations that match the specified filter in the request. If the server doesn't support this method, it returns UNIMPLEMENTED.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `chromemanagement_operations_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from chromemanagement_operations_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn chromemanagement_operations_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningListOperationsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningListOperationsResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+name}
/// Lists operations that match the specified filter in the request. If the server doesn't support this method, it returns UNIMPLEMENTED.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `chromemanagement_operations_list_builder()` + `chromemanagement_operations_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn chromemanagement_operations_list(
    client: &SimpleHttpClient,
    name: &str,
    filter: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
    returnPartialSuccess: Option<bool>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningListOperationsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = chromemanagement_operations_list_builder(
        client,
        name,
        filter,
        pageSize,
        pageToken,
        returnPartialSuccess,
    )?;
    chromemanagement_operations_list_execute(builder)
}
