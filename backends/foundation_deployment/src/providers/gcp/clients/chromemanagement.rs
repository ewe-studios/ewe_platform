//! Auto-generated API clients for gcp/chromemanagement.
//!
//! Generated by `cargo run --bin ewe_platform gen_provider_clients`.
//! DO NOT EDIT MANUALLY.
//!
//! Feature flag: `gcp`

#![cfg(feature = "gcp")]

pub mod types;

use crate::providers::gcp::clients::types::*;
use crate::providers::gcp::resources::*;
use foundation_core::valtron::{
    execute, BoxedSendExecutionAction, StreamIterator, StreamIteratorExt, TaskIterator,
    TaskIteratorExt,
};
use foundation_core::wire::simple_http::client::{
    body_reader, ClientRequestBuilder, RequestIntro, SimpleHttpClient, SystemDnsResolver,
};
use foundation_macros::JsonHash;
use serde::Serialize;

/// GET v1/customers/{customersId}/apps:countChromeAppRequests
/// Generate summary of app installation requests.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `chromemanagement_customers_apps_count_chrome_app_requests_execute()` to send, or `chromemanagement_customers_apps_count_chrome_app_requests` for simplest API.

pub fn chromemanagement_customers_apps_count_chrome_app_requests_builder(
    client: &SimpleHttpClient,
    customer: String,
    orderBy: Option<String>,
    orgUnitId: Option<String>,
    pageSize: Option<i32>,
    pageToken: Option<String>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://chromemanagement.googleapis.com/v1/customers/{}/apps:countChromeAppRequests",
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = orderBy {
        query_parts.push(format!("orderBy={}", val));
    }
    if let Some(val) = orgUnitId {
        query_parts.push(format!("orgUnitId={}", val));
    }
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/customers/{customersId}/apps:countChromeAppRequests
/// Generate summary of app installation requests.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `chromemanagement_customers_apps_count_chrome_app_requests_execute()` or `chromemanagement_customers_apps_count_chrome_app_requests`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `chromemanagement_customers_apps_count_chrome_app_requests_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn chromemanagement_customers_apps_count_chrome_app_requests_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<GoogleChromeManagementV1CountChromeAppRequestsResponse>,
                ApiError,
            >,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleChromeManagementV1CountChromeAppRequestsResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/customers/{customersId}/apps:countChromeAppRequests
/// Generate summary of app installation requests.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `chromemanagement_customers_apps_count_chrome_app_requests_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `chromemanagement_customers_apps_count_chrome_app_requests_task()`.
/// For the simplest API, use `chromemanagement_customers_apps_count_chrome_app_requests()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `chromemanagement_customers_apps_count_chrome_app_requests_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn chromemanagement_customers_apps_count_chrome_app_requests_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleChromeManagementV1CountChromeAppRequestsResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = chromemanagement_customers_apps_count_chrome_app_requests_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`chromemanagement_customers_apps_count_chrome_app_requests`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ChromemanagementCustomersAppsCountChromeAppRequestsArgs {
    /// Path parameter: customer
    pub customer: String,
    /// Query parameter: orderBy
    pub orderBy: Option<String>,
    /// Query parameter: orgUnitId
    pub orgUnitId: Option<String>,
    /// Query parameter: pageSize
    pub pageSize: Option<i32>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
}

/// GET v1/customers/{customersId}/apps:countChromeAppRequests
/// Generate summary of app installation requests.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `chromemanagement_customers_apps_count_chrome_app_requests_builder()` + `chromemanagement_customers_apps_count_chrome_app_requests_execute()`.
/// For task-level control, use `chromemanagement_customers_apps_count_chrome_app_requests_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn chromemanagement_customers_apps_count_chrome_app_requests(
    client: &SimpleHttpClient,
    args: &ChromemanagementCustomersAppsCountChromeAppRequestsArgs,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleChromeManagementV1CountChromeAppRequestsResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = chromemanagement_customers_apps_count_chrome_app_requests_builder(
        client,
        args.customer.clone(),
        args.orderBy.clone(),
        args.orgUnitId.clone(),
        args.pageSize.clone(),
        args.pageToken.clone(),
    )?;
    chromemanagement_customers_apps_count_chrome_app_requests_execute(builder)
}

/// GET v1/customers/{customersId}/apps:fetchDevicesRequestingExtension
/// Get a list of devices that have requested to install an extension.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `chromemanagement_customers_apps_fetch_devices_requesting_extension_execute()` to send, or `chromemanagement_customers_apps_fetch_devices_requesting_extension` for simplest API.

pub fn chromemanagement_customers_apps_fetch_devices_requesting_extension_builder(
    client: &SimpleHttpClient,
    customer: String,
    extensionId: Option<String>,
    orgUnitId: Option<String>,
    pageSize: Option<i32>,
    pageToken: Option<String>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://chromemanagement.googleapis.com/v1/customers/{}/apps:fetchDevicesRequestingExtension",
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = extensionId {
        query_parts.push(format!("extensionId={}", val));
    }
    if let Some(val) = orgUnitId {
        query_parts.push(format!("orgUnitId={}", val));
    }
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/customers/{customersId}/apps:fetchDevicesRequestingExtension
/// Get a list of devices that have requested to install an extension.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `chromemanagement_customers_apps_fetch_devices_requesting_extension_execute()` or `chromemanagement_customers_apps_fetch_devices_requesting_extension`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `chromemanagement_customers_apps_fetch_devices_requesting_extension_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn chromemanagement_customers_apps_fetch_devices_requesting_extension_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<GoogleChromeManagementV1FetchDevicesRequestingExtensionResponse>,
                ApiError,
            >,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleChromeManagementV1FetchDevicesRequestingExtensionResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/customers/{customersId}/apps:fetchDevicesRequestingExtension
/// Get a list of devices that have requested to install an extension.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `chromemanagement_customers_apps_fetch_devices_requesting_extension_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `chromemanagement_customers_apps_fetch_devices_requesting_extension_task()`.
/// For the simplest API, use `chromemanagement_customers_apps_fetch_devices_requesting_extension()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `chromemanagement_customers_apps_fetch_devices_requesting_extension_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn chromemanagement_customers_apps_fetch_devices_requesting_extension_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleChromeManagementV1FetchDevicesRequestingExtensionResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = chromemanagement_customers_apps_fetch_devices_requesting_extension_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`chromemanagement_customers_apps_fetch_devices_requesting_extension`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ChromemanagementCustomersAppsFetchDevicesRequestingExtensionArgs {
    /// Path parameter: customer
    pub customer: String,
    /// Query parameter: extensionId
    pub extensionId: Option<String>,
    /// Query parameter: orgUnitId
    pub orgUnitId: Option<String>,
    /// Query parameter: pageSize
    pub pageSize: Option<i32>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
}

/// GET v1/customers/{customersId}/apps:fetchDevicesRequestingExtension
/// Get a list of devices that have requested to install an extension.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `chromemanagement_customers_apps_fetch_devices_requesting_extension_builder()` + `chromemanagement_customers_apps_fetch_devices_requesting_extension_execute()`.
/// For task-level control, use `chromemanagement_customers_apps_fetch_devices_requesting_extension_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn chromemanagement_customers_apps_fetch_devices_requesting_extension(
    client: &SimpleHttpClient,
    args: &ChromemanagementCustomersAppsFetchDevicesRequestingExtensionArgs,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleChromeManagementV1FetchDevicesRequestingExtensionResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = chromemanagement_customers_apps_fetch_devices_requesting_extension_builder(
        client,
        args.customer.clone(),
        args.extensionId.clone(),
        args.orgUnitId.clone(),
        args.pageSize.clone(),
        args.pageToken.clone(),
    )?;
    chromemanagement_customers_apps_fetch_devices_requesting_extension_execute(builder)
}

/// GET v1/customers/{customersId}/apps:fetchUsersRequestingExtension
/// Get a list of users that have requested to install an extension.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `chromemanagement_customers_apps_fetch_users_requesting_extension_execute()` to send, or `chromemanagement_customers_apps_fetch_users_requesting_extension` for simplest API.

pub fn chromemanagement_customers_apps_fetch_users_requesting_extension_builder(
    client: &SimpleHttpClient,
    customer: String,
    extensionId: Option<String>,
    orgUnitId: Option<String>,
    pageSize: Option<i32>,
    pageToken: Option<String>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://chromemanagement.googleapis.com/v1/customers/{}/apps:fetchUsersRequestingExtension",
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = extensionId {
        query_parts.push(format!("extensionId={}", val));
    }
    if let Some(val) = orgUnitId {
        query_parts.push(format!("orgUnitId={}", val));
    }
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/customers/{customersId}/apps:fetchUsersRequestingExtension
/// Get a list of users that have requested to install an extension.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `chromemanagement_customers_apps_fetch_users_requesting_extension_execute()` or `chromemanagement_customers_apps_fetch_users_requesting_extension`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `chromemanagement_customers_apps_fetch_users_requesting_extension_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn chromemanagement_customers_apps_fetch_users_requesting_extension_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<GoogleChromeManagementV1FetchUsersRequestingExtensionResponse>,
                ApiError,
            >,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleChromeManagementV1FetchUsersRequestingExtensionResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/customers/{customersId}/apps:fetchUsersRequestingExtension
/// Get a list of users that have requested to install an extension.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `chromemanagement_customers_apps_fetch_users_requesting_extension_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `chromemanagement_customers_apps_fetch_users_requesting_extension_task()`.
/// For the simplest API, use `chromemanagement_customers_apps_fetch_users_requesting_extension()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `chromemanagement_customers_apps_fetch_users_requesting_extension_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn chromemanagement_customers_apps_fetch_users_requesting_extension_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleChromeManagementV1FetchUsersRequestingExtensionResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = chromemanagement_customers_apps_fetch_users_requesting_extension_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`chromemanagement_customers_apps_fetch_users_requesting_extension`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ChromemanagementCustomersAppsFetchUsersRequestingExtensionArgs {
    /// Path parameter: customer
    pub customer: String,
    /// Query parameter: extensionId
    pub extensionId: Option<String>,
    /// Query parameter: orgUnitId
    pub orgUnitId: Option<String>,
    /// Query parameter: pageSize
    pub pageSize: Option<i32>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
}

/// GET v1/customers/{customersId}/apps:fetchUsersRequestingExtension
/// Get a list of users that have requested to install an extension.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `chromemanagement_customers_apps_fetch_users_requesting_extension_builder()` + `chromemanagement_customers_apps_fetch_users_requesting_extension_execute()`.
/// For task-level control, use `chromemanagement_customers_apps_fetch_users_requesting_extension_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn chromemanagement_customers_apps_fetch_users_requesting_extension(
    client: &SimpleHttpClient,
    args: &ChromemanagementCustomersAppsFetchUsersRequestingExtensionArgs,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleChromeManagementV1FetchUsersRequestingExtensionResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = chromemanagement_customers_apps_fetch_users_requesting_extension_builder(
        client,
        args.customer.clone(),
        args.extensionId.clone(),
        args.orgUnitId.clone(),
        args.pageSize.clone(),
        args.pageToken.clone(),
    )?;
    chromemanagement_customers_apps_fetch_users_requesting_extension_execute(builder)
}

/// GET v1/customers/{customersId}/profiles
/// Lists Chrome browser profiles of a customer based on the given search and sorting criteria.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `chromemanagement_customers_profiles_list_execute()` to send, or `chromemanagement_customers_profiles_list` for simplest API.

pub fn chromemanagement_customers_profiles_list_builder(
    client: &SimpleHttpClient,
    parent: String,
    filter: Option<String>,
    orderBy: Option<String>,
    pageSize: Option<i32>,
    pageToken: Option<String>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!("https://chromemanagement.googleapis.com/v1/customers/{}/profiles",);

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = orderBy {
        query_parts.push(format!("orderBy={}", val));
    }
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/customers/{customersId}/profiles
/// Lists Chrome browser profiles of a customer based on the given search and sorting criteria.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `chromemanagement_customers_profiles_list_execute()` or `chromemanagement_customers_profiles_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `chromemanagement_customers_profiles_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn chromemanagement_customers_profiles_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<GoogleChromeManagementVersionsV1ListChromeBrowserProfilesResponse>,
                ApiError,
            >,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleChromeManagementVersionsV1ListChromeBrowserProfilesResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/customers/{customersId}/profiles
/// Lists Chrome browser profiles of a customer based on the given search and sorting criteria.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `chromemanagement_customers_profiles_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `chromemanagement_customers_profiles_list_task()`.
/// For the simplest API, use `chromemanagement_customers_profiles_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `chromemanagement_customers_profiles_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn chromemanagement_customers_profiles_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleChromeManagementVersionsV1ListChromeBrowserProfilesResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = chromemanagement_customers_profiles_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`chromemanagement_customers_profiles_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ChromemanagementCustomersProfilesListArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: filter
    pub filter: Option<String>,
    /// Query parameter: orderBy
    pub orderBy: Option<String>,
    /// Query parameter: pageSize
    pub pageSize: Option<i32>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
}

/// GET v1/customers/{customersId}/profiles
/// Lists Chrome browser profiles of a customer based on the given search and sorting criteria.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `chromemanagement_customers_profiles_list_builder()` + `chromemanagement_customers_profiles_list_execute()`.
/// For task-level control, use `chromemanagement_customers_profiles_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn chromemanagement_customers_profiles_list(
    client: &SimpleHttpClient,
    args: &ChromemanagementCustomersProfilesListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleChromeManagementVersionsV1ListChromeBrowserProfilesResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = chromemanagement_customers_profiles_list_builder(
        client,
        args.parent.clone(),
        args.filter.clone(),
        args.orderBy.clone(),
        args.pageSize.clone(),
        args.pageToken.clone(),
    )?;
    chromemanagement_customers_profiles_list_execute(builder)
}

/// GET v1/customers/{customersId}/reports:countActiveDevices
/// Get a count of active devices per set time frames.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `chromemanagement_customers_reports_count_active_devices_execute()` to send, or `chromemanagement_customers_reports_count_active_devices` for simplest API.

pub fn chromemanagement_customers_reports_count_active_devices_builder(
    client: &SimpleHttpClient,
    customer: String,
    date_day: Option<i32>,
    date_month: Option<i32>,
    date_year: Option<i32>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://chromemanagement.googleapis.com/v1/customers/{}/reports:countActiveDevices",
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = date_day {
        query_parts.push(format!("date.day={}", val));
    }
    if let Some(val) = date_month {
        query_parts.push(format!("date.month={}", val));
    }
    if let Some(val) = date_year {
        query_parts.push(format!("date.year={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/customers/{customersId}/reports:countActiveDevices
/// Get a count of active devices per set time frames.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `chromemanagement_customers_reports_count_active_devices_execute()` or `chromemanagement_customers_reports_count_active_devices`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `chromemanagement_customers_reports_count_active_devices_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn chromemanagement_customers_reports_count_active_devices_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<GoogleChromeManagementV1CountActiveDevicesResponse>,
                ApiError,
            >,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleChromeManagementV1CountActiveDevicesResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/customers/{customersId}/reports:countActiveDevices
/// Get a count of active devices per set time frames.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `chromemanagement_customers_reports_count_active_devices_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `chromemanagement_customers_reports_count_active_devices_task()`.
/// For the simplest API, use `chromemanagement_customers_reports_count_active_devices()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `chromemanagement_customers_reports_count_active_devices_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn chromemanagement_customers_reports_count_active_devices_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleChromeManagementV1CountActiveDevicesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = chromemanagement_customers_reports_count_active_devices_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`chromemanagement_customers_reports_count_active_devices`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ChromemanagementCustomersReportsCountActiveDevicesArgs {
    /// Path parameter: customer
    pub customer: String,
    /// Query parameter: date_day
    pub date_day: Option<i32>,
    /// Query parameter: date_month
    pub date_month: Option<i32>,
    /// Query parameter: date_year
    pub date_year: Option<i32>,
}

/// GET v1/customers/{customersId}/reports:countActiveDevices
/// Get a count of active devices per set time frames.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `chromemanagement_customers_reports_count_active_devices_builder()` + `chromemanagement_customers_reports_count_active_devices_execute()`.
/// For task-level control, use `chromemanagement_customers_reports_count_active_devices_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn chromemanagement_customers_reports_count_active_devices(
    client: &SimpleHttpClient,
    args: &ChromemanagementCustomersReportsCountActiveDevicesArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleChromeManagementV1CountActiveDevicesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = chromemanagement_customers_reports_count_active_devices_builder(
        client,
        args.customer.clone(),
        args.date_day.clone(),
        args.date_month.clone(),
        args.date_year.clone(),
    )?;
    chromemanagement_customers_reports_count_active_devices_execute(builder)
}

/// GET v1/customers/{customersId}/reports:countChromeBrowsersNeedingAttention
/// Count of Chrome Browsers that have been recently enrolled, have new policy to be synced, or have no recent activity.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `chromemanagement_customers_reports_count_chrome_browsers_needing_attention_execute()` to send, or `chromemanagement_customers_reports_count_chrome_browsers_needing_attention` for simplest API.

pub fn chromemanagement_customers_reports_count_chrome_browsers_needing_attention_builder(
    client: &SimpleHttpClient,
    customer: String,
    orgUnitId: Option<String>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://chromemanagement.googleapis.com/v1/customers/{}/reports:countChromeBrowsersNeedingAttention",
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = orgUnitId {
        query_parts.push(format!("orgUnitId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/customers/{customersId}/reports:countChromeBrowsersNeedingAttention
/// Count of Chrome Browsers that have been recently enrolled, have new policy to be synced, or have no recent activity.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `chromemanagement_customers_reports_count_chrome_browsers_needing_attention_execute()` or `chromemanagement_customers_reports_count_chrome_browsers_needing_attention`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `chromemanagement_customers_reports_count_chrome_browsers_needing_attention_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn chromemanagement_customers_reports_count_chrome_browsers_needing_attention_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<GoogleChromeManagementV1CountChromeBrowsersNeedingAttentionResponse>,
                ApiError,
            >,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleChromeManagementV1CountChromeBrowsersNeedingAttentionResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/customers/{customersId}/reports:countChromeBrowsersNeedingAttention
/// Count of Chrome Browsers that have been recently enrolled, have new policy to be synced, or have no recent activity.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `chromemanagement_customers_reports_count_chrome_browsers_needing_attention_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `chromemanagement_customers_reports_count_chrome_browsers_needing_attention_task()`.
/// For the simplest API, use `chromemanagement_customers_reports_count_chrome_browsers_needing_attention()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `chromemanagement_customers_reports_count_chrome_browsers_needing_attention_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn chromemanagement_customers_reports_count_chrome_browsers_needing_attention_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleChromeManagementV1CountChromeBrowsersNeedingAttentionResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task =
        chromemanagement_customers_reports_count_chrome_browsers_needing_attention_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`chromemanagement_customers_reports_count_chrome_browsers_needing_attention`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ChromemanagementCustomersReportsCountChromeBrowsersNeedingAttentionArgs {
    /// Path parameter: customer
    pub customer: String,
    /// Query parameter: orgUnitId
    pub orgUnitId: Option<String>,
}

/// GET v1/customers/{customersId}/reports:countChromeBrowsersNeedingAttention
/// Count of Chrome Browsers that have been recently enrolled, have new policy to be synced, or have no recent activity.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `chromemanagement_customers_reports_count_chrome_browsers_needing_attention_builder()` + `chromemanagement_customers_reports_count_chrome_browsers_needing_attention_execute()`.
/// For task-level control, use `chromemanagement_customers_reports_count_chrome_browsers_needing_attention_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn chromemanagement_customers_reports_count_chrome_browsers_needing_attention(
    client: &SimpleHttpClient,
    args: &ChromemanagementCustomersReportsCountChromeBrowsersNeedingAttentionArgs,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleChromeManagementV1CountChromeBrowsersNeedingAttentionResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        chromemanagement_customers_reports_count_chrome_browsers_needing_attention_builder(
            client,
            args.customer.clone(),
            args.orgUnitId.clone(),
        )?;
    chromemanagement_customers_reports_count_chrome_browsers_needing_attention_execute(builder)
}

/// GET v1/customers/{customersId}/reports:countChromeCrashEvents
/// Get a count of Chrome crash events.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `chromemanagement_customers_reports_count_chrome_crash_events_execute()` to send, or `chromemanagement_customers_reports_count_chrome_crash_events` for simplest API.

pub fn chromemanagement_customers_reports_count_chrome_crash_events_builder(
    client: &SimpleHttpClient,
    customer: String,
    filter: Option<String>,
    orderBy: Option<String>,
    orgUnitId: Option<String>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://chromemanagement.googleapis.com/v1/customers/{}/reports:countChromeCrashEvents",
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = orderBy {
        query_parts.push(format!("orderBy={}", val));
    }
    if let Some(val) = orgUnitId {
        query_parts.push(format!("orgUnitId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/customers/{customersId}/reports:countChromeCrashEvents
/// Get a count of Chrome crash events.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `chromemanagement_customers_reports_count_chrome_crash_events_execute()` or `chromemanagement_customers_reports_count_chrome_crash_events`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `chromemanagement_customers_reports_count_chrome_crash_events_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn chromemanagement_customers_reports_count_chrome_crash_events_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<GoogleChromeManagementV1CountChromeCrashEventsResponse>,
                ApiError,
            >,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleChromeManagementV1CountChromeCrashEventsResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/customers/{customersId}/reports:countChromeCrashEvents
/// Get a count of Chrome crash events.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `chromemanagement_customers_reports_count_chrome_crash_events_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `chromemanagement_customers_reports_count_chrome_crash_events_task()`.
/// For the simplest API, use `chromemanagement_customers_reports_count_chrome_crash_events()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `chromemanagement_customers_reports_count_chrome_crash_events_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn chromemanagement_customers_reports_count_chrome_crash_events_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleChromeManagementV1CountChromeCrashEventsResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = chromemanagement_customers_reports_count_chrome_crash_events_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`chromemanagement_customers_reports_count_chrome_crash_events`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ChromemanagementCustomersReportsCountChromeCrashEventsArgs {
    /// Path parameter: customer
    pub customer: String,
    /// Query parameter: filter
    pub filter: Option<String>,
    /// Query parameter: orderBy
    pub orderBy: Option<String>,
    /// Query parameter: orgUnitId
    pub orgUnitId: Option<String>,
}

/// GET v1/customers/{customersId}/reports:countChromeCrashEvents
/// Get a count of Chrome crash events.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `chromemanagement_customers_reports_count_chrome_crash_events_builder()` + `chromemanagement_customers_reports_count_chrome_crash_events_execute()`.
/// For task-level control, use `chromemanagement_customers_reports_count_chrome_crash_events_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn chromemanagement_customers_reports_count_chrome_crash_events(
    client: &SimpleHttpClient,
    args: &ChromemanagementCustomersReportsCountChromeCrashEventsArgs,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleChromeManagementV1CountChromeCrashEventsResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = chromemanagement_customers_reports_count_chrome_crash_events_builder(
        client,
        args.customer.clone(),
        args.filter.clone(),
        args.orderBy.clone(),
        args.orgUnitId.clone(),
    )?;
    chromemanagement_customers_reports_count_chrome_crash_events_execute(builder)
}

/// GET v1/customers/{customersId}/reports:countChromeDevicesReachingAutoExpirationDate
/// Generate report of the number of devices expiring in each month of the selected time frame. Devices are grouped by auto update expiration date and model. Further information can be found [here](<https://support.google.`com/chrome/a/answer/10564947`>).
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `chromemanagement_customers_reports_count_chrome_devices_reaching_auto_expiration_date_execute()` to send, or `chromemanagement_customers_reports_count_chrome_devices_reaching_auto_expiration_date` for simplest API.

pub fn chromemanagement_customers_reports_count_chrome_devices_reaching_auto_expiration_date_builder(
    client: &SimpleHttpClient,
    customer: String,
    maxAueDate: Option<String>,
    minAueDate: Option<String>,
    orgUnitId: Option<String>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://chromemanagement.googleapis.com/v1/customers/{}/reports:countChromeDevicesReachingAutoExpirationDate",
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = maxAueDate {
        query_parts.push(format!("maxAueDate={}", val));
    }
    if let Some(val) = minAueDate {
        query_parts.push(format!("minAueDate={}", val));
    }
    if let Some(val) = orgUnitId {
        query_parts.push(format!("orgUnitId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/customers/{customersId}/reports:countChromeDevicesReachingAutoExpirationDate
/// Generate report of the number of devices expiring in each month of the selected time frame. Devices are grouped by auto update expiration date and model. Further information can be found [here](<https://support.google.`com/chrome/a/answer/10564947`>).
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `chromemanagement_customers_reports_count_chrome_devices_reaching_auto_expiration_date_execute()` or `chromemanagement_customers_reports_count_chrome_devices_reaching_auto_expiration_date`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `chromemanagement_customers_reports_count_chrome_devices_reaching_auto_expiration_date_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn chromemanagement_customers_reports_count_chrome_devices_reaching_auto_expiration_date_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<
                    GoogleChromeManagementV1CountChromeDevicesReachingAutoExpirationDateResponse,
                >,
                ApiError,
            >,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, intro, headers, .. } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleChromeManagementV1CountChromeDevicesReachingAutoExpirationDateResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/customers/{customersId}/reports:countChromeDevicesReachingAutoExpirationDate
/// Generate report of the number of devices expiring in each month of the selected time frame. Devices are grouped by auto update expiration date and model. Further information can be found [here](<https://support.google.`com/chrome/a/answer/10564947`>).
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `chromemanagement_customers_reports_count_chrome_devices_reaching_auto_expiration_date_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `chromemanagement_customers_reports_count_chrome_devices_reaching_auto_expiration_date_task()`.
/// For the simplest API, use `chromemanagement_customers_reports_count_chrome_devices_reaching_auto_expiration_date()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `chromemanagement_customers_reports_count_chrome_devices_reaching_auto_expiration_date_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn chromemanagement_customers_reports_count_chrome_devices_reaching_auto_expiration_date_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<
                    GoogleChromeManagementV1CountChromeDevicesReachingAutoExpirationDateResponse,
                >,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task =
        chromemanagement_customers_reports_count_chrome_devices_reaching_auto_expiration_date_task(
            builder,
        )?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`chromemanagement_customers_reports_count_chrome_devices_reaching_auto_expiration_date`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ChromemanagementCustomersReportsCountChromeDevicesReachingAutoExpirationDateArgs {
    /// Path parameter: customer
    pub customer: String,
    /// Query parameter: maxAueDate
    pub maxAueDate: Option<String>,
    /// Query parameter: minAueDate
    pub minAueDate: Option<String>,
    /// Query parameter: orgUnitId
    pub orgUnitId: Option<String>,
}

/// GET v1/customers/{customersId}/reports:countChromeDevicesReachingAutoExpirationDate
/// Generate report of the number of devices expiring in each month of the selected time frame. Devices are grouped by auto update expiration date and model. Further information can be found [here](<https://support.google.`com/chrome/a/answer/10564947`>).
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `chromemanagement_customers_reports_count_chrome_devices_reaching_auto_expiration_date_builder()` + `chromemanagement_customers_reports_count_chrome_devices_reaching_auto_expiration_date_execute()`.
/// For task-level control, use `chromemanagement_customers_reports_count_chrome_devices_reaching_auto_expiration_date_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn chromemanagement_customers_reports_count_chrome_devices_reaching_auto_expiration_date(
    client: &SimpleHttpClient,
    args: &ChromemanagementCustomersReportsCountChromeDevicesReachingAutoExpirationDateArgs,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<
                    GoogleChromeManagementV1CountChromeDevicesReachingAutoExpirationDateResponse,
                >,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = chromemanagement_customers_reports_count_chrome_devices_reaching_auto_expiration_date_builder(client, args.customer.clone(), args.maxAueDate.clone(), args.minAueDate.clone(), args.orgUnitId.clone())?;
    chromemanagement_customers_reports_count_chrome_devices_reaching_auto_expiration_date_execute(
        builder,
    )
}

/// GET v1/customers/{customersId}/reports:countChromeDevicesThatNeedAttention
/// Counts of ChromeOS devices that have not synced policies or have lacked user activity in the past 28 days, are out of date, or are not complaint. Further information can be found here <https://support.google.`com/chrome/a/answer/10564947`>
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `chromemanagement_customers_reports_count_chrome_devices_that_need_attention_execute()` to send, or `chromemanagement_customers_reports_count_chrome_devices_that_need_attention` for simplest API.

pub fn chromemanagement_customers_reports_count_chrome_devices_that_need_attention_builder(
    client: &SimpleHttpClient,
    customer: String,
    orgUnitId: Option<String>,
    readMask: Option<String>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://chromemanagement.googleapis.com/v1/customers/{}/reports:countChromeDevicesThatNeedAttention",
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = orgUnitId {
        query_parts.push(format!("orgUnitId={}", val));
    }
    if let Some(val) = readMask {
        query_parts.push(format!("readMask={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/customers/{customersId}/reports:countChromeDevicesThatNeedAttention
/// Counts of ChromeOS devices that have not synced policies or have lacked user activity in the past 28 days, are out of date, or are not complaint. Further information can be found here <https://support.google.`com/chrome/a/answer/10564947`>
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `chromemanagement_customers_reports_count_chrome_devices_that_need_attention_execute()` or `chromemanagement_customers_reports_count_chrome_devices_that_need_attention`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `chromemanagement_customers_reports_count_chrome_devices_that_need_attention_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn chromemanagement_customers_reports_count_chrome_devices_that_need_attention_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<GoogleChromeManagementV1CountChromeDevicesThatNeedAttentionResponse>,
                ApiError,
            >,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleChromeManagementV1CountChromeDevicesThatNeedAttentionResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/customers/{customersId}/reports:countChromeDevicesThatNeedAttention
/// Counts of ChromeOS devices that have not synced policies or have lacked user activity in the past 28 days, are out of date, or are not complaint. Further information can be found here <https://support.google.`com/chrome/a/answer/10564947`>
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `chromemanagement_customers_reports_count_chrome_devices_that_need_attention_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `chromemanagement_customers_reports_count_chrome_devices_that_need_attention_task()`.
/// For the simplest API, use `chromemanagement_customers_reports_count_chrome_devices_that_need_attention()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `chromemanagement_customers_reports_count_chrome_devices_that_need_attention_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn chromemanagement_customers_reports_count_chrome_devices_that_need_attention_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleChromeManagementV1CountChromeDevicesThatNeedAttentionResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task =
        chromemanagement_customers_reports_count_chrome_devices_that_need_attention_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`chromemanagement_customers_reports_count_chrome_devices_that_need_attention`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ChromemanagementCustomersReportsCountChromeDevicesThatNeedAttentionArgs {
    /// Path parameter: customer
    pub customer: String,
    /// Query parameter: orgUnitId
    pub orgUnitId: Option<String>,
    /// Query parameter: readMask
    pub readMask: Option<String>,
}

/// GET v1/customers/{customersId}/reports:countChromeDevicesThatNeedAttention
/// Counts of ChromeOS devices that have not synced policies or have lacked user activity in the past 28 days, are out of date, or are not complaint. Further information can be found here <https://support.google.`com/chrome/a/answer/10564947`>
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `chromemanagement_customers_reports_count_chrome_devices_that_need_attention_builder()` + `chromemanagement_customers_reports_count_chrome_devices_that_need_attention_execute()`.
/// For task-level control, use `chromemanagement_customers_reports_count_chrome_devices_that_need_attention_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn chromemanagement_customers_reports_count_chrome_devices_that_need_attention(
    client: &SimpleHttpClient,
    args: &ChromemanagementCustomersReportsCountChromeDevicesThatNeedAttentionArgs,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleChromeManagementV1CountChromeDevicesThatNeedAttentionResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        chromemanagement_customers_reports_count_chrome_devices_that_need_attention_builder(
            client,
            args.customer.clone(),
            args.orgUnitId.clone(),
            args.readMask.clone(),
        )?;
    chromemanagement_customers_reports_count_chrome_devices_that_need_attention_execute(builder)
}

/// GET v1/customers/{customersId}/reports:countChromeHardwareFleetDevices
/// Counts of devices with a specific hardware specification from the requested hardware type (for example model name, processor type). Further information can be found here <https://support.google.`com/chrome/a/answer/10564947`>
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `chromemanagement_customers_reports_count_chrome_hardware_fleet_devices_execute()` to send, or `chromemanagement_customers_reports_count_chrome_hardware_fleet_devices` for simplest API.

pub fn chromemanagement_customers_reports_count_chrome_hardware_fleet_devices_builder(
    client: &SimpleHttpClient,
    customer: String,
    orgUnitId: Option<String>,
    readMask: Option<String>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://chromemanagement.googleapis.com/v1/customers/{}/reports:countChromeHardwareFleetDevices",
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = orgUnitId {
        query_parts.push(format!("orgUnitId={}", val));
    }
    if let Some(val) = readMask {
        query_parts.push(format!("readMask={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/customers/{customersId}/reports:countChromeHardwareFleetDevices
/// Counts of devices with a specific hardware specification from the requested hardware type (for example model name, processor type). Further information can be found here <https://support.google.`com/chrome/a/answer/10564947`>
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `chromemanagement_customers_reports_count_chrome_hardware_fleet_devices_execute()` or `chromemanagement_customers_reports_count_chrome_hardware_fleet_devices`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `chromemanagement_customers_reports_count_chrome_hardware_fleet_devices_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn chromemanagement_customers_reports_count_chrome_hardware_fleet_devices_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<GoogleChromeManagementV1CountChromeHardwareFleetDevicesResponse>,
                ApiError,
            >,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleChromeManagementV1CountChromeHardwareFleetDevicesResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/customers/{customersId}/reports:countChromeHardwareFleetDevices
/// Counts of devices with a specific hardware specification from the requested hardware type (for example model name, processor type). Further information can be found here <https://support.google.`com/chrome/a/answer/10564947`>
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `chromemanagement_customers_reports_count_chrome_hardware_fleet_devices_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `chromemanagement_customers_reports_count_chrome_hardware_fleet_devices_task()`.
/// For the simplest API, use `chromemanagement_customers_reports_count_chrome_hardware_fleet_devices()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `chromemanagement_customers_reports_count_chrome_hardware_fleet_devices_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn chromemanagement_customers_reports_count_chrome_hardware_fleet_devices_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleChromeManagementV1CountChromeHardwareFleetDevicesResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task =
        chromemanagement_customers_reports_count_chrome_hardware_fleet_devices_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`chromemanagement_customers_reports_count_chrome_hardware_fleet_devices`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ChromemanagementCustomersReportsCountChromeHardwareFleetDevicesArgs {
    /// Path parameter: customer
    pub customer: String,
    /// Query parameter: orgUnitId
    pub orgUnitId: Option<String>,
    /// Query parameter: readMask
    pub readMask: Option<String>,
}

/// GET v1/customers/{customersId}/reports:countChromeHardwareFleetDevices
/// Counts of devices with a specific hardware specification from the requested hardware type (for example model name, processor type). Further information can be found here <https://support.google.`com/chrome/a/answer/10564947`>
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `chromemanagement_customers_reports_count_chrome_hardware_fleet_devices_builder()` + `chromemanagement_customers_reports_count_chrome_hardware_fleet_devices_execute()`.
/// For task-level control, use `chromemanagement_customers_reports_count_chrome_hardware_fleet_devices_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn chromemanagement_customers_reports_count_chrome_hardware_fleet_devices(
    client: &SimpleHttpClient,
    args: &ChromemanagementCustomersReportsCountChromeHardwareFleetDevicesArgs,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleChromeManagementV1CountChromeHardwareFleetDevicesResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = chromemanagement_customers_reports_count_chrome_hardware_fleet_devices_builder(
        client,
        args.customer.clone(),
        args.orgUnitId.clone(),
        args.readMask.clone(),
    )?;
    chromemanagement_customers_reports_count_chrome_hardware_fleet_devices_execute(builder)
}

/// GET v1/customers/{customersId}/reports:countChromeVersions
/// Generate report of installed Chrome versions.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `chromemanagement_customers_reports_count_chrome_versions_execute()` to send, or `chromemanagement_customers_reports_count_chrome_versions` for simplest API.

pub fn chromemanagement_customers_reports_count_chrome_versions_builder(
    client: &SimpleHttpClient,
    customer: String,
    filter: Option<String>,
    orgUnitId: Option<String>,
    pageSize: Option<i32>,
    pageToken: Option<String>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://chromemanagement.googleapis.com/v1/customers/{}/reports:countChromeVersions",
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = orgUnitId {
        query_parts.push(format!("orgUnitId={}", val));
    }
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/customers/{customersId}/reports:countChromeVersions
/// Generate report of installed Chrome versions.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `chromemanagement_customers_reports_count_chrome_versions_execute()` or `chromemanagement_customers_reports_count_chrome_versions`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `chromemanagement_customers_reports_count_chrome_versions_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn chromemanagement_customers_reports_count_chrome_versions_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<GoogleChromeManagementV1CountChromeVersionsResponse>,
                ApiError,
            >,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleChromeManagementV1CountChromeVersionsResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/customers/{customersId}/reports:countChromeVersions
/// Generate report of installed Chrome versions.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `chromemanagement_customers_reports_count_chrome_versions_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `chromemanagement_customers_reports_count_chrome_versions_task()`.
/// For the simplest API, use `chromemanagement_customers_reports_count_chrome_versions()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `chromemanagement_customers_reports_count_chrome_versions_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn chromemanagement_customers_reports_count_chrome_versions_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleChromeManagementV1CountChromeVersionsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = chromemanagement_customers_reports_count_chrome_versions_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`chromemanagement_customers_reports_count_chrome_versions`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ChromemanagementCustomersReportsCountChromeVersionsArgs {
    /// Path parameter: customer
    pub customer: String,
    /// Query parameter: filter
    pub filter: Option<String>,
    /// Query parameter: orgUnitId
    pub orgUnitId: Option<String>,
    /// Query parameter: pageSize
    pub pageSize: Option<i32>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
}

/// GET v1/customers/{customersId}/reports:countChromeVersions
/// Generate report of installed Chrome versions.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `chromemanagement_customers_reports_count_chrome_versions_builder()` + `chromemanagement_customers_reports_count_chrome_versions_execute()`.
/// For task-level control, use `chromemanagement_customers_reports_count_chrome_versions_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn chromemanagement_customers_reports_count_chrome_versions(
    client: &SimpleHttpClient,
    args: &ChromemanagementCustomersReportsCountChromeVersionsArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleChromeManagementV1CountChromeVersionsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = chromemanagement_customers_reports_count_chrome_versions_builder(
        client,
        args.customer.clone(),
        args.filter.clone(),
        args.orgUnitId.clone(),
        args.pageSize.clone(),
        args.pageToken.clone(),
    )?;
    chromemanagement_customers_reports_count_chrome_versions_execute(builder)
}

/// GET v1/customers/{customersId}/reports:countDevicesPerBootType
/// Get a count of devices per boot type.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `chromemanagement_customers_reports_count_devices_per_boot_type_execute()` to send, or `chromemanagement_customers_reports_count_devices_per_boot_type` for simplest API.

pub fn chromemanagement_customers_reports_count_devices_per_boot_type_builder(
    client: &SimpleHttpClient,
    customer: String,
    date_day: Option<i32>,
    date_month: Option<i32>,
    date_year: Option<i32>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://chromemanagement.googleapis.com/v1/customers/{}/reports:countDevicesPerBootType",
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = date_day {
        query_parts.push(format!("date.day={}", val));
    }
    if let Some(val) = date_month {
        query_parts.push(format!("date.month={}", val));
    }
    if let Some(val) = date_year {
        query_parts.push(format!("date.year={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/customers/{customersId}/reports:countDevicesPerBootType
/// Get a count of devices per boot type.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `chromemanagement_customers_reports_count_devices_per_boot_type_execute()` or `chromemanagement_customers_reports_count_devices_per_boot_type`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `chromemanagement_customers_reports_count_devices_per_boot_type_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn chromemanagement_customers_reports_count_devices_per_boot_type_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<GoogleChromeManagementV1CountDevicesPerBootTypeResponse>,
                ApiError,
            >,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleChromeManagementV1CountDevicesPerBootTypeResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/customers/{customersId}/reports:countDevicesPerBootType
/// Get a count of devices per boot type.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `chromemanagement_customers_reports_count_devices_per_boot_type_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `chromemanagement_customers_reports_count_devices_per_boot_type_task()`.
/// For the simplest API, use `chromemanagement_customers_reports_count_devices_per_boot_type()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `chromemanagement_customers_reports_count_devices_per_boot_type_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn chromemanagement_customers_reports_count_devices_per_boot_type_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleChromeManagementV1CountDevicesPerBootTypeResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = chromemanagement_customers_reports_count_devices_per_boot_type_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`chromemanagement_customers_reports_count_devices_per_boot_type`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ChromemanagementCustomersReportsCountDevicesPerBootTypeArgs {
    /// Path parameter: customer
    pub customer: String,
    /// Query parameter: date_day
    pub date_day: Option<i32>,
    /// Query parameter: date_month
    pub date_month: Option<i32>,
    /// Query parameter: date_year
    pub date_year: Option<i32>,
}

/// GET v1/customers/{customersId}/reports:countDevicesPerBootType
/// Get a count of devices per boot type.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `chromemanagement_customers_reports_count_devices_per_boot_type_builder()` + `chromemanagement_customers_reports_count_devices_per_boot_type_execute()`.
/// For task-level control, use `chromemanagement_customers_reports_count_devices_per_boot_type_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn chromemanagement_customers_reports_count_devices_per_boot_type(
    client: &SimpleHttpClient,
    args: &ChromemanagementCustomersReportsCountDevicesPerBootTypeArgs,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleChromeManagementV1CountDevicesPerBootTypeResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = chromemanagement_customers_reports_count_devices_per_boot_type_builder(
        client,
        args.customer.clone(),
        args.date_day.clone(),
        args.date_month.clone(),
        args.date_year.clone(),
    )?;
    chromemanagement_customers_reports_count_devices_per_boot_type_execute(builder)
}

/// GET v1/customers/{customersId}/reports:countDevicesPerReleaseChannel
/// Get a count of devices per channel.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `chromemanagement_customers_reports_count_devices_per_release_channel_execute()` to send, or `chromemanagement_customers_reports_count_devices_per_release_channel` for simplest API.

pub fn chromemanagement_customers_reports_count_devices_per_release_channel_builder(
    client: &SimpleHttpClient,
    customer: String,
    date_day: Option<i32>,
    date_month: Option<i32>,
    date_year: Option<i32>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://chromemanagement.googleapis.com/v1/customers/{}/reports:countDevicesPerReleaseChannel",
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = date_day {
        query_parts.push(format!("date.day={}", val));
    }
    if let Some(val) = date_month {
        query_parts.push(format!("date.month={}", val));
    }
    if let Some(val) = date_year {
        query_parts.push(format!("date.year={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/customers/{customersId}/reports:countDevicesPerReleaseChannel
/// Get a count of devices per channel.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `chromemanagement_customers_reports_count_devices_per_release_channel_execute()` or `chromemanagement_customers_reports_count_devices_per_release_channel`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `chromemanagement_customers_reports_count_devices_per_release_channel_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn chromemanagement_customers_reports_count_devices_per_release_channel_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<GoogleChromeManagementV1CountDevicesPerReleaseChannelResponse>,
                ApiError,
            >,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleChromeManagementV1CountDevicesPerReleaseChannelResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/customers/{customersId}/reports:countDevicesPerReleaseChannel
/// Get a count of devices per channel.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `chromemanagement_customers_reports_count_devices_per_release_channel_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `chromemanagement_customers_reports_count_devices_per_release_channel_task()`.
/// For the simplest API, use `chromemanagement_customers_reports_count_devices_per_release_channel()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `chromemanagement_customers_reports_count_devices_per_release_channel_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn chromemanagement_customers_reports_count_devices_per_release_channel_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleChromeManagementV1CountDevicesPerReleaseChannelResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = chromemanagement_customers_reports_count_devices_per_release_channel_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`chromemanagement_customers_reports_count_devices_per_release_channel`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ChromemanagementCustomersReportsCountDevicesPerReleaseChannelArgs {
    /// Path parameter: customer
    pub customer: String,
    /// Query parameter: date_day
    pub date_day: Option<i32>,
    /// Query parameter: date_month
    pub date_month: Option<i32>,
    /// Query parameter: date_year
    pub date_year: Option<i32>,
}

/// GET v1/customers/{customersId}/reports:countDevicesPerReleaseChannel
/// Get a count of devices per channel.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `chromemanagement_customers_reports_count_devices_per_release_channel_builder()` + `chromemanagement_customers_reports_count_devices_per_release_channel_execute()`.
/// For task-level control, use `chromemanagement_customers_reports_count_devices_per_release_channel_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn chromemanagement_customers_reports_count_devices_per_release_channel(
    client: &SimpleHttpClient,
    args: &ChromemanagementCustomersReportsCountDevicesPerReleaseChannelArgs,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleChromeManagementV1CountDevicesPerReleaseChannelResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = chromemanagement_customers_reports_count_devices_per_release_channel_builder(
        client,
        args.customer.clone(),
        args.date_day.clone(),
        args.date_month.clone(),
        args.date_year.clone(),
    )?;
    chromemanagement_customers_reports_count_devices_per_release_channel_execute(builder)
}

/// GET v1/customers/{customersId}/reports:countInstalledApps
/// Generate report of app installations.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `chromemanagement_customers_reports_count_installed_apps_execute()` to send, or `chromemanagement_customers_reports_count_installed_apps` for simplest API.

pub fn chromemanagement_customers_reports_count_installed_apps_builder(
    client: &SimpleHttpClient,
    customer: String,
    filter: Option<String>,
    orderBy: Option<String>,
    orgUnitId: Option<String>,
    pageSize: Option<i32>,
    pageToken: Option<String>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://chromemanagement.googleapis.com/v1/customers/{}/reports:countInstalledApps",
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = orderBy {
        query_parts.push(format!("orderBy={}", val));
    }
    if let Some(val) = orgUnitId {
        query_parts.push(format!("orgUnitId={}", val));
    }
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/customers/{customersId}/reports:countInstalledApps
/// Generate report of app installations.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `chromemanagement_customers_reports_count_installed_apps_execute()` or `chromemanagement_customers_reports_count_installed_apps`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `chromemanagement_customers_reports_count_installed_apps_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn chromemanagement_customers_reports_count_installed_apps_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<GoogleChromeManagementV1CountInstalledAppsResponse>,
                ApiError,
            >,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleChromeManagementV1CountInstalledAppsResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/customers/{customersId}/reports:countInstalledApps
/// Generate report of app installations.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `chromemanagement_customers_reports_count_installed_apps_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `chromemanagement_customers_reports_count_installed_apps_task()`.
/// For the simplest API, use `chromemanagement_customers_reports_count_installed_apps()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `chromemanagement_customers_reports_count_installed_apps_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn chromemanagement_customers_reports_count_installed_apps_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleChromeManagementV1CountInstalledAppsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = chromemanagement_customers_reports_count_installed_apps_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`chromemanagement_customers_reports_count_installed_apps`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ChromemanagementCustomersReportsCountInstalledAppsArgs {
    /// Path parameter: customer
    pub customer: String,
    /// Query parameter: filter
    pub filter: Option<String>,
    /// Query parameter: orderBy
    pub orderBy: Option<String>,
    /// Query parameter: orgUnitId
    pub orgUnitId: Option<String>,
    /// Query parameter: pageSize
    pub pageSize: Option<i32>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
}

/// GET v1/customers/{customersId}/reports:countInstalledApps
/// Generate report of app installations.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `chromemanagement_customers_reports_count_installed_apps_builder()` + `chromemanagement_customers_reports_count_installed_apps_execute()`.
/// For task-level control, use `chromemanagement_customers_reports_count_installed_apps_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn chromemanagement_customers_reports_count_installed_apps(
    client: &SimpleHttpClient,
    args: &ChromemanagementCustomersReportsCountInstalledAppsArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleChromeManagementV1CountInstalledAppsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = chromemanagement_customers_reports_count_installed_apps_builder(
        client,
        args.customer.clone(),
        args.filter.clone(),
        args.orderBy.clone(),
        args.orgUnitId.clone(),
        args.pageSize.clone(),
        args.pageToken.clone(),
    )?;
    chromemanagement_customers_reports_count_installed_apps_execute(builder)
}

/// GET v1/customers/{customersId}/reports:countPrintJobsByPrinter
/// Get a summary of printing done by each printer.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `chromemanagement_customers_reports_count_print_jobs_by_printer_execute()` to send, or `chromemanagement_customers_reports_count_print_jobs_by_printer` for simplest API.

pub fn chromemanagement_customers_reports_count_print_jobs_by_printer_builder(
    client: &SimpleHttpClient,
    customer: String,
    filter: Option<String>,
    orderBy: Option<String>,
    pageSize: Option<i32>,
    pageToken: Option<String>,
    printerOrgUnitId: Option<String>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://chromemanagement.googleapis.com/v1/customers/{}/reports:countPrintJobsByPrinter",
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = orderBy {
        query_parts.push(format!("orderBy={}", val));
    }
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = printerOrgUnitId {
        query_parts.push(format!("printerOrgUnitId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/customers/{customersId}/reports:countPrintJobsByPrinter
/// Get a summary of printing done by each printer.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `chromemanagement_customers_reports_count_print_jobs_by_printer_execute()` or `chromemanagement_customers_reports_count_print_jobs_by_printer`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `chromemanagement_customers_reports_count_print_jobs_by_printer_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn chromemanagement_customers_reports_count_print_jobs_by_printer_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<GoogleChromeManagementV1CountPrintJobsByPrinterResponse>,
                ApiError,
            >,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleChromeManagementV1CountPrintJobsByPrinterResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/customers/{customersId}/reports:countPrintJobsByPrinter
/// Get a summary of printing done by each printer.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `chromemanagement_customers_reports_count_print_jobs_by_printer_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `chromemanagement_customers_reports_count_print_jobs_by_printer_task()`.
/// For the simplest API, use `chromemanagement_customers_reports_count_print_jobs_by_printer()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `chromemanagement_customers_reports_count_print_jobs_by_printer_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn chromemanagement_customers_reports_count_print_jobs_by_printer_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleChromeManagementV1CountPrintJobsByPrinterResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = chromemanagement_customers_reports_count_print_jobs_by_printer_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`chromemanagement_customers_reports_count_print_jobs_by_printer`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ChromemanagementCustomersReportsCountPrintJobsByPrinterArgs {
    /// Path parameter: customer
    pub customer: String,
    /// Query parameter: filter
    pub filter: Option<String>,
    /// Query parameter: orderBy
    pub orderBy: Option<String>,
    /// Query parameter: pageSize
    pub pageSize: Option<i32>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
    /// Query parameter: printerOrgUnitId
    pub printerOrgUnitId: Option<String>,
}

/// GET v1/customers/{customersId}/reports:countPrintJobsByPrinter
/// Get a summary of printing done by each printer.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `chromemanagement_customers_reports_count_print_jobs_by_printer_builder()` + `chromemanagement_customers_reports_count_print_jobs_by_printer_execute()`.
/// For task-level control, use `chromemanagement_customers_reports_count_print_jobs_by_printer_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn chromemanagement_customers_reports_count_print_jobs_by_printer(
    client: &SimpleHttpClient,
    args: &ChromemanagementCustomersReportsCountPrintJobsByPrinterArgs,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleChromeManagementV1CountPrintJobsByPrinterResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = chromemanagement_customers_reports_count_print_jobs_by_printer_builder(
        client,
        args.customer.clone(),
        args.filter.clone(),
        args.orderBy.clone(),
        args.pageSize.clone(),
        args.pageToken.clone(),
        args.printerOrgUnitId.clone(),
    )?;
    chromemanagement_customers_reports_count_print_jobs_by_printer_execute(builder)
}

/// GET v1/customers/{customersId}/reports:countPrintJobsByUser
/// Get a summary of printing done by each user.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `chromemanagement_customers_reports_count_print_jobs_by_user_execute()` to send, or `chromemanagement_customers_reports_count_print_jobs_by_user` for simplest API.

pub fn chromemanagement_customers_reports_count_print_jobs_by_user_builder(
    client: &SimpleHttpClient,
    customer: String,
    filter: Option<String>,
    orderBy: Option<String>,
    pageSize: Option<i32>,
    pageToken: Option<String>,
    printerOrgUnitId: Option<String>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://chromemanagement.googleapis.com/v1/customers/{}/reports:countPrintJobsByUser",
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = orderBy {
        query_parts.push(format!("orderBy={}", val));
    }
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = printerOrgUnitId {
        query_parts.push(format!("printerOrgUnitId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/customers/{customersId}/reports:countPrintJobsByUser
/// Get a summary of printing done by each user.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `chromemanagement_customers_reports_count_print_jobs_by_user_execute()` or `chromemanagement_customers_reports_count_print_jobs_by_user`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `chromemanagement_customers_reports_count_print_jobs_by_user_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn chromemanagement_customers_reports_count_print_jobs_by_user_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<GoogleChromeManagementV1CountPrintJobsByUserResponse>,
                ApiError,
            >,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleChromeManagementV1CountPrintJobsByUserResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/customers/{customersId}/reports:countPrintJobsByUser
/// Get a summary of printing done by each user.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `chromemanagement_customers_reports_count_print_jobs_by_user_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `chromemanagement_customers_reports_count_print_jobs_by_user_task()`.
/// For the simplest API, use `chromemanagement_customers_reports_count_print_jobs_by_user()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `chromemanagement_customers_reports_count_print_jobs_by_user_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn chromemanagement_customers_reports_count_print_jobs_by_user_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleChromeManagementV1CountPrintJobsByUserResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = chromemanagement_customers_reports_count_print_jobs_by_user_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`chromemanagement_customers_reports_count_print_jobs_by_user`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ChromemanagementCustomersReportsCountPrintJobsByUserArgs {
    /// Path parameter: customer
    pub customer: String,
    /// Query parameter: filter
    pub filter: Option<String>,
    /// Query parameter: orderBy
    pub orderBy: Option<String>,
    /// Query parameter: pageSize
    pub pageSize: Option<i32>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
    /// Query parameter: printerOrgUnitId
    pub printerOrgUnitId: Option<String>,
}

/// GET v1/customers/{customersId}/reports:countPrintJobsByUser
/// Get a summary of printing done by each user.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `chromemanagement_customers_reports_count_print_jobs_by_user_builder()` + `chromemanagement_customers_reports_count_print_jobs_by_user_execute()`.
/// For task-level control, use `chromemanagement_customers_reports_count_print_jobs_by_user_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn chromemanagement_customers_reports_count_print_jobs_by_user(
    client: &SimpleHttpClient,
    args: &ChromemanagementCustomersReportsCountPrintJobsByUserArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleChromeManagementV1CountPrintJobsByUserResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = chromemanagement_customers_reports_count_print_jobs_by_user_builder(
        client,
        args.customer.clone(),
        args.filter.clone(),
        args.orderBy.clone(),
        args.pageSize.clone(),
        args.pageToken.clone(),
        args.printerOrgUnitId.clone(),
    )?;
    chromemanagement_customers_reports_count_print_jobs_by_user_execute(builder)
}

/// GET v1/customers/{customersId}/reports:enumeratePrintJobs
/// Get a list of print jobs.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `chromemanagement_customers_reports_enumerate_print_jobs_execute()` to send, or `chromemanagement_customers_reports_enumerate_print_jobs` for simplest API.

pub fn chromemanagement_customers_reports_enumerate_print_jobs_builder(
    client: &SimpleHttpClient,
    customer: String,
    filter: Option<String>,
    orderBy: Option<String>,
    pageSize: Option<i32>,
    pageToken: Option<String>,
    printerOrgUnitId: Option<String>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://chromemanagement.googleapis.com/v1/customers/{}/reports:enumeratePrintJobs",
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = orderBy {
        query_parts.push(format!("orderBy={}", val));
    }
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = printerOrgUnitId {
        query_parts.push(format!("printerOrgUnitId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/customers/{customersId}/reports:enumeratePrintJobs
/// Get a list of print jobs.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `chromemanagement_customers_reports_enumerate_print_jobs_execute()` or `chromemanagement_customers_reports_enumerate_print_jobs`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `chromemanagement_customers_reports_enumerate_print_jobs_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn chromemanagement_customers_reports_enumerate_print_jobs_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<GoogleChromeManagementV1EnumeratePrintJobsResponse>,
                ApiError,
            >,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleChromeManagementV1EnumeratePrintJobsResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/customers/{customersId}/reports:enumeratePrintJobs
/// Get a list of print jobs.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `chromemanagement_customers_reports_enumerate_print_jobs_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `chromemanagement_customers_reports_enumerate_print_jobs_task()`.
/// For the simplest API, use `chromemanagement_customers_reports_enumerate_print_jobs()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `chromemanagement_customers_reports_enumerate_print_jobs_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn chromemanagement_customers_reports_enumerate_print_jobs_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleChromeManagementV1EnumeratePrintJobsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = chromemanagement_customers_reports_enumerate_print_jobs_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`chromemanagement_customers_reports_enumerate_print_jobs`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ChromemanagementCustomersReportsEnumeratePrintJobsArgs {
    /// Path parameter: customer
    pub customer: String,
    /// Query parameter: filter
    pub filter: Option<String>,
    /// Query parameter: orderBy
    pub orderBy: Option<String>,
    /// Query parameter: pageSize
    pub pageSize: Option<i32>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
    /// Query parameter: printerOrgUnitId
    pub printerOrgUnitId: Option<String>,
}

/// GET v1/customers/{customersId}/reports:enumeratePrintJobs
/// Get a list of print jobs.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `chromemanagement_customers_reports_enumerate_print_jobs_builder()` + `chromemanagement_customers_reports_enumerate_print_jobs_execute()`.
/// For task-level control, use `chromemanagement_customers_reports_enumerate_print_jobs_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn chromemanagement_customers_reports_enumerate_print_jobs(
    client: &SimpleHttpClient,
    args: &ChromemanagementCustomersReportsEnumeratePrintJobsArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleChromeManagementV1EnumeratePrintJobsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = chromemanagement_customers_reports_enumerate_print_jobs_builder(
        client,
        args.customer.clone(),
        args.filter.clone(),
        args.orderBy.clone(),
        args.pageSize.clone(),
        args.pageToken.clone(),
        args.printerOrgUnitId.clone(),
    )?;
    chromemanagement_customers_reports_enumerate_print_jobs_execute(builder)
}

/// GET v1/customers/{customersId}/reports:findInstalledAppDevices
/// Generate report of managed Chrome browser devices that have a specified app installed.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `chromemanagement_customers_reports_find_installed_app_devices_execute()` to send, or `chromemanagement_customers_reports_find_installed_app_devices` for simplest API.

pub fn chromemanagement_customers_reports_find_installed_app_devices_builder(
    client: &SimpleHttpClient,
    customer: String,
    appId: Option<String>,
    appType: Option<String>,
    filter: Option<String>,
    orderBy: Option<String>,
    orgUnitId: Option<String>,
    pageSize: Option<i32>,
    pageToken: Option<String>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://chromemanagement.googleapis.com/v1/customers/{}/reports:findInstalledAppDevices",
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = appId {
        query_parts.push(format!("appId={}", val));
    }
    if let Some(val) = appType {
        query_parts.push(format!("appType={}", val));
    }
    if let Some(val) = filter {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = orderBy {
        query_parts.push(format!("orderBy={}", val));
    }
    if let Some(val) = orgUnitId {
        query_parts.push(format!("orgUnitId={}", val));
    }
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/customers/{customersId}/reports:findInstalledAppDevices
/// Generate report of managed Chrome browser devices that have a specified app installed.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `chromemanagement_customers_reports_find_installed_app_devices_execute()` or `chromemanagement_customers_reports_find_installed_app_devices`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `chromemanagement_customers_reports_find_installed_app_devices_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn chromemanagement_customers_reports_find_installed_app_devices_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<GoogleChromeManagementV1FindInstalledAppDevicesResponse>,
                ApiError,
            >,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleChromeManagementV1FindInstalledAppDevicesResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/customers/{customersId}/reports:findInstalledAppDevices
/// Generate report of managed Chrome browser devices that have a specified app installed.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `chromemanagement_customers_reports_find_installed_app_devices_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `chromemanagement_customers_reports_find_installed_app_devices_task()`.
/// For the simplest API, use `chromemanagement_customers_reports_find_installed_app_devices()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `chromemanagement_customers_reports_find_installed_app_devices_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn chromemanagement_customers_reports_find_installed_app_devices_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleChromeManagementV1FindInstalledAppDevicesResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = chromemanagement_customers_reports_find_installed_app_devices_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`chromemanagement_customers_reports_find_installed_app_devices`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ChromemanagementCustomersReportsFindInstalledAppDevicesArgs {
    /// Path parameter: customer
    pub customer: String,
    /// Query parameter: appId
    pub appId: Option<String>,
    /// Query parameter: appType
    pub appType: Option<String>,
    /// Query parameter: filter
    pub filter: Option<String>,
    /// Query parameter: orderBy
    pub orderBy: Option<String>,
    /// Query parameter: orgUnitId
    pub orgUnitId: Option<String>,
    /// Query parameter: pageSize
    pub pageSize: Option<i32>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
}

/// GET v1/customers/{customersId}/reports:findInstalledAppDevices
/// Generate report of managed Chrome browser devices that have a specified app installed.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `chromemanagement_customers_reports_find_installed_app_devices_builder()` + `chromemanagement_customers_reports_find_installed_app_devices_execute()`.
/// For task-level control, use `chromemanagement_customers_reports_find_installed_app_devices_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn chromemanagement_customers_reports_find_installed_app_devices(
    client: &SimpleHttpClient,
    args: &ChromemanagementCustomersReportsFindInstalledAppDevicesArgs,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleChromeManagementV1FindInstalledAppDevicesResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = chromemanagement_customers_reports_find_installed_app_devices_builder(
        client,
        args.customer.clone(),
        args.appId.clone(),
        args.appType.clone(),
        args.filter.clone(),
        args.orderBy.clone(),
        args.orgUnitId.clone(),
        args.pageSize.clone(),
        args.pageToken.clone(),
    )?;
    chromemanagement_customers_reports_find_installed_app_devices_execute(builder)
}

/// GET v1/customers/{customersId}/telemetry/devices
/// List all telemetry devices.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `chromemanagement_customers_telemetry_devices_list_execute()` to send, or `chromemanagement_customers_telemetry_devices_list` for simplest API.

pub fn chromemanagement_customers_telemetry_devices_list_builder(
    client: &SimpleHttpClient,
    parent: String,
    filter: Option<String>,
    pageSize: Option<i32>,
    pageToken: Option<String>,
    readMask: Option<String>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url =
        format!("https://chromemanagement.googleapis.com/v1/customers/{}/telemetry/devices",);

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = readMask {
        query_parts.push(format!("readMask={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/customers/{customersId}/telemetry/devices
/// List all telemetry devices.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `chromemanagement_customers_telemetry_devices_list_execute()` or `chromemanagement_customers_telemetry_devices_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `chromemanagement_customers_telemetry_devices_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn chromemanagement_customers_telemetry_devices_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<GoogleChromeManagementV1ListTelemetryDevicesResponse>,
                ApiError,
            >,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleChromeManagementV1ListTelemetryDevicesResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/customers/{customersId}/telemetry/devices
/// List all telemetry devices.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `chromemanagement_customers_telemetry_devices_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `chromemanagement_customers_telemetry_devices_list_task()`.
/// For the simplest API, use `chromemanagement_customers_telemetry_devices_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `chromemanagement_customers_telemetry_devices_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn chromemanagement_customers_telemetry_devices_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleChromeManagementV1ListTelemetryDevicesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = chromemanagement_customers_telemetry_devices_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`chromemanagement_customers_telemetry_devices_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ChromemanagementCustomersTelemetryDevicesListArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: filter
    pub filter: Option<String>,
    /// Query parameter: pageSize
    pub pageSize: Option<i32>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
    /// Query parameter: readMask
    pub readMask: Option<String>,
}

/// GET v1/customers/{customersId}/telemetry/devices
/// List all telemetry devices.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `chromemanagement_customers_telemetry_devices_list_builder()` + `chromemanagement_customers_telemetry_devices_list_execute()`.
/// For task-level control, use `chromemanagement_customers_telemetry_devices_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn chromemanagement_customers_telemetry_devices_list(
    client: &SimpleHttpClient,
    args: &ChromemanagementCustomersTelemetryDevicesListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleChromeManagementV1ListTelemetryDevicesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = chromemanagement_customers_telemetry_devices_list_builder(
        client,
        args.parent.clone(),
        args.filter.clone(),
        args.pageSize.clone(),
        args.pageToken.clone(),
        args.readMask.clone(),
    )?;
    chromemanagement_customers_telemetry_devices_list_execute(builder)
}

/// GET v1/customers/{customersId}/telemetry/events
/// List telemetry events.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `chromemanagement_customers_telemetry_events_list_execute()` to send, or `chromemanagement_customers_telemetry_events_list` for simplest API.

pub fn chromemanagement_customers_telemetry_events_list_builder(
    client: &SimpleHttpClient,
    parent: String,
    filter: Option<String>,
    pageSize: Option<i32>,
    pageToken: Option<String>,
    readMask: Option<String>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url =
        format!("https://chromemanagement.googleapis.com/v1/customers/{}/telemetry/events",);

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = readMask {
        query_parts.push(format!("readMask={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/customers/{customersId}/telemetry/events
/// List telemetry events.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `chromemanagement_customers_telemetry_events_list_execute()` or `chromemanagement_customers_telemetry_events_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `chromemanagement_customers_telemetry_events_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn chromemanagement_customers_telemetry_events_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<GoogleChromeManagementV1ListTelemetryEventsResponse>,
                ApiError,
            >,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleChromeManagementV1ListTelemetryEventsResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/customers/{customersId}/telemetry/events
/// List telemetry events.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `chromemanagement_customers_telemetry_events_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `chromemanagement_customers_telemetry_events_list_task()`.
/// For the simplest API, use `chromemanagement_customers_telemetry_events_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `chromemanagement_customers_telemetry_events_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn chromemanagement_customers_telemetry_events_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleChromeManagementV1ListTelemetryEventsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = chromemanagement_customers_telemetry_events_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`chromemanagement_customers_telemetry_events_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ChromemanagementCustomersTelemetryEventsListArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: filter
    pub filter: Option<String>,
    /// Query parameter: pageSize
    pub pageSize: Option<i32>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
    /// Query parameter: readMask
    pub readMask: Option<String>,
}

/// GET v1/customers/{customersId}/telemetry/events
/// List telemetry events.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `chromemanagement_customers_telemetry_events_list_builder()` + `chromemanagement_customers_telemetry_events_list_execute()`.
/// For task-level control, use `chromemanagement_customers_telemetry_events_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn chromemanagement_customers_telemetry_events_list(
    client: &SimpleHttpClient,
    args: &ChromemanagementCustomersTelemetryEventsListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleChromeManagementV1ListTelemetryEventsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = chromemanagement_customers_telemetry_events_list_builder(
        client,
        args.parent.clone(),
        args.filter.clone(),
        args.pageSize.clone(),
        args.pageToken.clone(),
        args.readMask.clone(),
    )?;
    chromemanagement_customers_telemetry_events_list_execute(builder)
}

/// GET v1/customers/{customersId}/telemetry/notificationConfigs
/// Create a telemetry notification config.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `chromemanagement_customers_telemetry_notification_configs_create_execute()` to send, or `chromemanagement_customers_telemetry_notification_configs_create` for simplest API.

pub fn chromemanagement_customers_telemetry_notification_configs_create_builder(
    client: &SimpleHttpClient,
    parent: String,
    body: &GoogleChromeManagementV1TelemetryNotificationConfig,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://chromemanagement.googleapis.com/v1/customers/{}/telemetry/notificationConfigs",
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/customers/{customersId}/telemetry/notificationConfigs
/// Create a telemetry notification config.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `chromemanagement_customers_telemetry_notification_configs_create_execute()` or `chromemanagement_customers_telemetry_notification_configs_create`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `chromemanagement_customers_telemetry_notification_configs_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn chromemanagement_customers_telemetry_notification_configs_create_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<GoogleChromeManagementV1TelemetryNotificationConfig>,
                ApiError,
            >,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleChromeManagementV1TelemetryNotificationConfig =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/customers/{customersId}/telemetry/notificationConfigs
/// Create a telemetry notification config.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `chromemanagement_customers_telemetry_notification_configs_create_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `chromemanagement_customers_telemetry_notification_configs_create_task()`.
/// For the simplest API, use `chromemanagement_customers_telemetry_notification_configs_create()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `chromemanagement_customers_telemetry_notification_configs_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn chromemanagement_customers_telemetry_notification_configs_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleChromeManagementV1TelemetryNotificationConfig>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = chromemanagement_customers_telemetry_notification_configs_create_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`chromemanagement_customers_telemetry_notification_configs_create`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ChromemanagementCustomersTelemetryNotificationConfigsCreateArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Request body.
    pub body: GoogleChromeManagementV1TelemetryNotificationConfig,
}

/// GET v1/customers/{customersId}/telemetry/notificationConfigs
/// Create a telemetry notification config.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `chromemanagement_customers_telemetry_notification_configs_create_builder()` + `chromemanagement_customers_telemetry_notification_configs_create_execute()`.
/// For task-level control, use `chromemanagement_customers_telemetry_notification_configs_create_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn chromemanagement_customers_telemetry_notification_configs_create(
    client: &SimpleHttpClient,
    args: &ChromemanagementCustomersTelemetryNotificationConfigsCreateArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleChromeManagementV1TelemetryNotificationConfig>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = chromemanagement_customers_telemetry_notification_configs_create_builder(
        client,
        args.parent.clone(),
        &args.body,
    )?;
    chromemanagement_customers_telemetry_notification_configs_create_execute(builder)
}

/// GET v1/customers/{customersId}/telemetry/users
/// List all telemetry users.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `chromemanagement_customers_telemetry_users_list_execute()` to send, or `chromemanagement_customers_telemetry_users_list` for simplest API.

pub fn chromemanagement_customers_telemetry_users_list_builder(
    client: &SimpleHttpClient,
    parent: String,
    filter: Option<String>,
    pageSize: Option<i32>,
    pageToken: Option<String>,
    readMask: Option<String>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url =
        format!("https://chromemanagement.googleapis.com/v1/customers/{}/telemetry/users",);

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = readMask {
        query_parts.push(format!("readMask={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/customers/{customersId}/telemetry/users
/// List all telemetry users.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `chromemanagement_customers_telemetry_users_list_execute()` or `chromemanagement_customers_telemetry_users_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `chromemanagement_customers_telemetry_users_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn chromemanagement_customers_telemetry_users_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<GoogleChromeManagementV1ListTelemetryUsersResponse>,
                ApiError,
            >,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleChromeManagementV1ListTelemetryUsersResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/customers/{customersId}/telemetry/users
/// List all telemetry users.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `chromemanagement_customers_telemetry_users_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `chromemanagement_customers_telemetry_users_list_task()`.
/// For the simplest API, use `chromemanagement_customers_telemetry_users_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `chromemanagement_customers_telemetry_users_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn chromemanagement_customers_telemetry_users_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleChromeManagementV1ListTelemetryUsersResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = chromemanagement_customers_telemetry_users_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`chromemanagement_customers_telemetry_users_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ChromemanagementCustomersTelemetryUsersListArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: filter
    pub filter: Option<String>,
    /// Query parameter: pageSize
    pub pageSize: Option<i32>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
    /// Query parameter: readMask
    pub readMask: Option<String>,
}

/// GET v1/customers/{customersId}/telemetry/users
/// List all telemetry users.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `chromemanagement_customers_telemetry_users_list_builder()` + `chromemanagement_customers_telemetry_users_list_execute()`.
/// For task-level control, use `chromemanagement_customers_telemetry_users_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn chromemanagement_customers_telemetry_users_list(
    client: &SimpleHttpClient,
    args: &ChromemanagementCustomersTelemetryUsersListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleChromeManagementV1ListTelemetryUsersResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = chromemanagement_customers_telemetry_users_list_builder(
        client,
        args.parent.clone(),
        args.filter.clone(),
        args.pageSize.clone(),
        args.pageToken.clone(),
        args.readMask.clone(),
    )?;
    chromemanagement_customers_telemetry_users_list_execute(builder)
}

/// GET v1/operations/{operationsId}:cancel
/// Starts asynchronous cancellation on a long-running operation. The server makes a best effort to cancel the operation, but success is not guaranteed. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED. Clients can use Operations.GetOperation or other methods to check whether the cancellation succeeded or whether the operation completed despite cancellation. On successful cancellation, the operation is not deleted; instead, it becomes an operation with an Operation.error value with a google.rpc.Status.code of 1, corresponding to Code.CANCELLED.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `chromemanagement_operations_cancel_execute()` to send, or `chromemanagement_operations_cancel` for simplest API.

pub fn chromemanagement_operations_cancel_builder(
    client: &SimpleHttpClient,
    name: String,
    body: &GoogleLongrunningCancelOperationRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!("https://chromemanagement.googleapis.com/v1/operations/{}:cancel",);

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/operations/{operationsId}:cancel
/// Starts asynchronous cancellation on a long-running operation. The server makes a best effort to cancel the operation, but success is not guaranteed. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED. Clients can use Operations.GetOperation or other methods to check whether the cancellation succeeded or whether the operation completed despite cancellation. On successful cancellation, the operation is not deleted; instead, it becomes an operation with an Operation.error value with a google.rpc.Status.code of 1, corresponding to Code.CANCELLED.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `chromemanagement_operations_cancel_execute()` or `chromemanagement_operations_cancel`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `chromemanagement_operations_cancel_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn chromemanagement_operations_cancel_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleProtobufEmpty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/operations/{operationsId}:cancel
/// Starts asynchronous cancellation on a long-running operation. The server makes a best effort to cancel the operation, but success is not guaranteed. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED. Clients can use Operations.GetOperation or other methods to check whether the cancellation succeeded or whether the operation completed despite cancellation. On successful cancellation, the operation is not deleted; instead, it becomes an operation with an Operation.error value with a google.rpc.Status.code of 1, corresponding to Code.CANCELLED.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `chromemanagement_operations_cancel_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `chromemanagement_operations_cancel_task()`.
/// For the simplest API, use `chromemanagement_operations_cancel()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `chromemanagement_operations_cancel_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn chromemanagement_operations_cancel_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = chromemanagement_operations_cancel_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`chromemanagement_operations_cancel`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ChromemanagementOperationsCancelArgs {
    /// Path parameter: name
    pub name: String,
    /// Request body.
    pub body: GoogleLongrunningCancelOperationRequest,
}

/// GET v1/operations/{operationsId}:cancel
/// Starts asynchronous cancellation on a long-running operation. The server makes a best effort to cancel the operation, but success is not guaranteed. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED. Clients can use Operations.GetOperation or other methods to check whether the cancellation succeeded or whether the operation completed despite cancellation. On successful cancellation, the operation is not deleted; instead, it becomes an operation with an Operation.error value with a google.rpc.Status.code of 1, corresponding to Code.CANCELLED.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `chromemanagement_operations_cancel_builder()` + `chromemanagement_operations_cancel_execute()`.
/// For task-level control, use `chromemanagement_operations_cancel_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn chromemanagement_operations_cancel(
    client: &SimpleHttpClient,
    args: &ChromemanagementOperationsCancelArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        chromemanagement_operations_cancel_builder(client, args.name.clone(), &args.body)?;
    chromemanagement_operations_cancel_execute(builder)
}

/// GET v1/operations/{operationsId}
/// Deletes a long-running operation. This method indicates that the client is no longer interested in the operation result. It does not cancel the operation. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `chromemanagement_operations_delete_execute()` to send, or `chromemanagement_operations_delete` for simplest API.

pub fn chromemanagement_operations_delete_builder(
    client: &SimpleHttpClient,
    name: String,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!("https://chromemanagement.googleapis.com/v1/operations/{}",);

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/operations/{operationsId}
/// Deletes a long-running operation. This method indicates that the client is no longer interested in the operation result. It does not cancel the operation. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `chromemanagement_operations_delete_execute()` or `chromemanagement_operations_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `chromemanagement_operations_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn chromemanagement_operations_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleProtobufEmpty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/operations/{operationsId}
/// Deletes a long-running operation. This method indicates that the client is no longer interested in the operation result. It does not cancel the operation. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `chromemanagement_operations_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `chromemanagement_operations_delete_task()`.
/// For the simplest API, use `chromemanagement_operations_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `chromemanagement_operations_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn chromemanagement_operations_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = chromemanagement_operations_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`chromemanagement_operations_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ChromemanagementOperationsDeleteArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/operations/{operationsId}
/// Deletes a long-running operation. This method indicates that the client is no longer interested in the operation result. It does not cancel the operation. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `chromemanagement_operations_delete_builder()` + `chromemanagement_operations_delete_execute()`.
/// For task-level control, use `chromemanagement_operations_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn chromemanagement_operations_delete(
    client: &SimpleHttpClient,
    args: &ChromemanagementOperationsDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = chromemanagement_operations_delete_builder(client, args.name.clone())?;
    chromemanagement_operations_delete_execute(builder)
}
